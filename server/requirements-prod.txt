# ADR-027 (aba-opcoes F1, 2026-09-09): os pins exatos abaixo são as versões
# MEDIDAS na venv local, que roda Python 3.14. PRODUÇÃO RODA 3.12
# (`server/.python-version`). Se a resolução falhar em 3.12, NÃO relaxe o pin
# por conta própria — pare e leve ao Alex (PLANO §3.7): o pin existe porque o
# SDK do MCP arrasta fastapi/starlette/pydantic/uvicorn junto, e deixar essas
# quatro flutuando é como o build de produção diverge do que a suíte testou.
fastapi==0.141.1
starlette==1.6.0
pydantic==2.13.5
uvicorn[standard]==0.52.4
httpx>=0.27
# ADR-027 (aba-opcoes F1): SDK do serviço MCP autenticado.
#
# PIN EXATO desde 2026-09-10 (achado A-04 da auditoria). A linha TEM de ser
# idêntica à do `requirements.txt` — é o que o guardião
# `test_guardiao_b_mcp_esta_nos_requirements_e_igual_nos_dois` protege: faixa
# diferente entre os dois arquivos é o modo silencioso de produção resolver um
# SDK que a suíte nunca exercitou.
#
# `httpx2` ganhou linha própria (o SDK só pede `>=2.5.0`) e `mcp-types` NÃO
# ganha: `mcp==2.2.0` já o exige como `mcp-types==2.2.0` nos próprios
# metadados. Ver a nota longa no `requirements.txt`.
mcp==2.2.0
httpx2==2.12.0
PyJWT[crypto]>=2.8
h2>=4.1
