---
phase: 29-opcao-a-descoberto-flag-opt-in
plan: 03
type: execute
wave: 3
depends_on: ["29-02"]
files_modified:
  - web/src/disclaimers.js
  - web/src/App.jsx
  - web/tests/test_opcao_descoberto_ui.mjs
autonomous: false
requirements: [SC-3, SC-5, SC-6]
must_haves:
  truths:
    - "Ligar o flag abre um termo de responsabilidade que exige rolagem até o fim + checkbox, com a versão do texto visível"
    - "O botão de ativar fica desabilitado até `liTudo && aceito` — o mesmo estado do termo do Modo Operador"
    - "O aceite grava termo + flag no MESMO patch, porque os stores recusam ligar sem termo"
    - "Desligar o flag é um toque, sem termo, sem modal"
    - "`OpcoesCamada` (Watchlist/Radar) continua sendo a UI da compra a seco — nenhuma migração para a sub-aba Operar"
  artifacts:
    - path: "web/src/disclaimers.js"
      provides: "texto do termo de opção a descoberto + constante de versão"
      contains: "TERMO_DESCOBERTO_VERSAO"
    - path: "web/src/App.jsx"
      provides: "TermoDescobertoModal + OpcaoDescobertoCard com o toggle, renderizado em ConfigScreen"
      contains: "TermoDescobertoModal"
    - path: "web/tests/test_opcao_descoberto_ui.mjs"
      provides: "guardião da fricção (rolagem+checkbox+versão), do patch único, e do fence D2"
      min_lines: 70
  key_links:
    - from: "web/src/App.jsx TermoDescobertoModal"
      to: "A.saveConfig({descobertoTermo, permitirOpcaoADescoberto: true})"
      via: "patch ÚNICO no aceite"
      pattern: "descobertoTermo:.*permitirOpcaoADescoberto: true"
    - from: "web/src/App.jsx ConfigScreen"
      to: "OpcaoDescobertoCard"
      via: "render entre o card PERFIL DO OPERADOR e o bloco de aviso completo"
      pattern: "OpcaoDescobertoCard"
    - from: "web/src/App.jsx TermoDescobertoModal"
      to: "DISCLAIMERS.descobertoTermo / TERMO_DESCOBERTO_VERSAO"
      via: "import de ./disclaimers.js"
      pattern: "TERMO_DESCOBERTO_VERSAO"
---

<objective>
Dar ao flag a fricção que a decisão D1 fechou: termo de responsabilidade com
leitura até o fim, checkbox e versão registrada — o MESMO padrão do Modo
Operador, reusado, não um terceiro modal inventado do zero.

Purpose: os planos 29-01/29-02 fizeram os stores recusarem ligar o flag sem
`descobertoTermo`. Sem esta UI, o flag é inatingível: não existe caminho na
tela para registrar o aceite. Este plano fecha o circuito e satisfaz SC-3.

Output: texto versionado do termo, `TermoDescobertoModal`, card com o toggle em
Preferências, guardião, e um checkpoint de verificação humana.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/29-opcao-a-descoberto-flag-opt-in/29-CONTEXT.md
@.planning/phases/29-opcao-a-descoberto-flag-opt-in/29-02-SUMMARY.md

NÃO leia `.planning/STATE.md`.

**`web/src/App.jsx` tem ≈9500 linhas (≈162K tokens como texto puro). Ler o
arquivo inteiro é o erro mais caro possível neste repositório.** Cada task
abaixo cita os intervalos exatos. Para localizar qualquer coisa fora deles,
use `grep -n` e só então leia o span encontrado.

**Baseline:** os números finais estão no `29-02-SUMMARY.md` (pytest ~2793
passed; `web/tests` 145 arquivos). Use-os como baseline desta entrega.

<interfaces>
<!-- Contratos extraídos. Não explore o codebase para achá-los. -->

De `web/src/disclaimers.js`:
```js
export const DISCLAIMERS = { /* ... */ operadorTermo: "<texto longo>" };
export const TERMO_OPERADOR_VERSAO = "1.0";   // linha 41
```

De `web/src/App.jsx` (já existentes, reusar — não reimplementar):
```jsx
import { DISCLAIMERS, TERMO_OPERADOR_VERSAO } from "./disclaimers.js";   // linha 8
function Toggle({ on, onClick, label, disabled })    // linha 419 — switch com role="switch" + aria-checked
function TermoOperadorModal({ ctx, onClose })        // linha 2317 — o padrão de fricção a espelhar
function ConfigScreen({ ctx })                       // linha 7341 — tela "Preferências"; termina na linha 7487
//   dentro dela: `const c = data.config;`
//                `const sectionTitle = {...}` (estilo do header de seção)
//                `card` (estilo de card, do escopo do módulo)
A.saveConfig(patch)   // relay para store.putConfig; usado em App.jsx:2271, 2344, 5511, 5530
A.flash(msg)          // toast
```

Estado do `TermoOperadorModal` (a máquina a espelhar, `App.jsx:2317-2374`):
- `liTudo` — rolagem chegou ao fim (`scrollHeight - scrollTop - clientHeight < 12`)
- fallback de montagem: se o texto CABE sem rolagem, `onScroll` nunca dispara,
  então um `useEffect` com `scrollHeight - clientHeight < 12` + `setTimeout(250)`
  + listener de `resize` já marca `liTudo` (senão o aceite trava para sempre em
  tela grande — bug real, FASE 8B/P2)
- `aceito` — checkbox
- `busy` — durante o `await`
- botão de ativar: `disabled={!liTudo || !aceito || busy}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Texto versionado do termo + TermoDescobertoModal</name>

  <files>web/src/disclaimers.js, web/src/App.jsx</files>

  <read_first>
  - `web/src/disclaimers.js:33-43` — o `DISCLAIMERS.operadorTermo` (texto completo) e `export const TERMO_OPERADOR_VERSAO = "1.0"` na linha 41. É o modelo de tom, extensão e estrutura.
  - `web/src/App.jsx:2317-2374` — `TermoOperadorModal` INTEIRA: a máquina de estado, o `useEffect` de fallback, o `onScroll`, o `ativar()` com patch único, e o JSX (scrim, card, header com versão, div rolável, dica "↓ role o texto", label do checkbox, dois botões). Este é o span mais importante do plano.
  - `web/src/App.jsx:8` — a linha de import de `./disclaimers.js`, que ganha o símbolo novo.
  - `web/src/App.jsx:419-427` — o componente `Toggle` (usado na Task 2).
  - `server/app/store.py:865-877` — `close_option_vencida`: a liquidação por vencimento pelo valor intrínseco, que pode ser ZERO. É o fato que o ponto 3 do termo precisa descrever com precisão.
  - No `29-02-SUMMARY.md`: os nomes finais dos dois campos de config e a regra de aceite.
  </read_first>

  <action>
  **1a. `web/src/disclaimers.js`.** Adicione, depois de `operadorTermo`:
  - `descobertoTermo`: texto do termo, no MESMO tom e extensão do
    `operadorTermo` (parágrafo único, PT-BR, sem quebra de linha no literal).
    Conteúdo obrigatório, porque é o que dá substância à fricção:
    1. o que é uma posição de opção **a descoberto** (sem lastro de ações que
       cubra a operação) e por que ela é diferente de uma lastreada;
    2. que o **padrão da conta é lastro obrigatório** e este flag é a exceção
       nomeada que o usuário está pedindo;
    3. que a perda em opção pode ser **a totalidade do prêmio pago**, e que o
       vencimento liquida a posição mecanicamente pelo valor intrínseco —
       que pode ser **zero** — sem nenhum stop ter sido rompido (é literalmente
       o que `store.close_option_vencida` faz, `server/app/store.py:865-877`);
    4. que tudo é **simulação com dinheiro virtual**, que o Boris+ não envia
       ordens a corretoras, e que não há garantia de resultado nem recomendação
       personalizada;
    5. que os números da simulação são calculados por regra determinística e a
       IA **explica, nunca decide** (princípio 5 do CLAUDE.md);
    6. a frase de aceite: ao ligar, o usuário declara que entende e aceita.

    **Proibido** no texto: linguagem de enriquecimento rápido, promessa de
    acerto, "alavancagem" apresentada como vantagem, ou qualquer coisa que
    soe como incentivo a operar a descoberto (CLAUDE.md, princípio 8). O termo
    é um aviso, não um convite.
  - `export const TERMO_DESCOBERTO_VERSAO = "1.0";` — ao lado de
    `TERMO_OPERADOR_VERSAO`. Versões INDEPENDENTES: mudar o termo do Operador
    não pode invalidar o aceite deste, nem vice-versa.

  **1b. `web/src/App.jsx:8`.** Adicione `TERMO_DESCOBERTO_VERSAO` ao import
  existente de `./disclaimers.js`. NÃO crie uma segunda linha de import.

  **1c. `TermoDescobertoModal({ ctx, onClose })`.** Declare-o imediatamente
  DEPOIS de `TermoOperadorModal` (ou seja, depois da linha 2374), para os dois
  ficarem lado a lado e a simetria ser óbvia na leitura. Espelhe a estrutura
  inteira do irmão — mesma máquina `liTudo`/`aceito`/`busy`, mesmo `useEffect`
  de fallback com `setTimeout(250)` e listener de `resize` (sem ele o aceite
  trava em tela grande; é bug conhecido, não o reintroduza), mesmo `onScroll`
  com o limiar de 12px, mesmo scrim/card/maxHeight.

  Diferenças deliberadas em relação ao `TermoOperadorModal`, todas por causa
  do D2 (escopo estreito):
  - Título: "Termo de Responsabilidade"; subtítulo:
    `Opções a descoberto · versão {TERMO_DESCOBERTO_VERSAO} — leia até o fim para habilitar o aceite.`
  - Corpo rolável: `{DISCLAIMERS.descobertoTermo}`
  - Label do checkbox: uma frase que nomeie o risco REAL desta operação
    (perder o prêmio inteiro, inclusive por vencimento a zero) — não copie a
    frase do Modo Operador, que é sobre outro risco.
  - `ativar()`: `await A.saveConfig({ descobertoTermo: { aceitoEm: new Date().toISOString(), versao: TERMO_DESCOBERTO_VERSAO }, permitirOpcaoADescoberto: true })`
    — **patch ÚNICO**, termo primeiro, exatamente como `App.jsx:2344`, porque
    os dois stores recusam ligar o flag sem o termo no config ou no mesmo
    patch (29-01 Task 1c / 29-02 Task 1b).
  - **NÃO** faça `window.location.reload()`. O Modo Operador recarrega porque
    `appMode` é lido de forma independente em mais de 10 lugares
    (`App.jsx:2281-2287`). Este flag é lido em UM lugar (o gate do store) e o
    `saveConfig` já atualiza `data.config` — recarregar o app por causa dele
    seria custo sem ganho. Declare isso em comentário, citando o contraste,
    para ninguém "consertar" isso depois.
  - `catch`: `A.flash("Erro ao ligar: " + (e.message || e))`, no estilo da
    linha 2351. O erro sobe do store; não reescreva o motivo.
  - Botões: "Cancelar" (fecha) e "Ligar opções a descoberto"
    (`disabled={!liTudo || !aceito || busy}`).
  </action>

  <verify>
    <automated>cd /Users/acamerini/dev/bolsia/b3-agente && npx vite build --config web/vite.config.js > /tmp/f29-03-t1-build.log 2>&1; echo "BUILD_EXIT=$?"; tail -4 /tmp/f29-03-t1-build.log; bash scripts/executar.sh --testes > /tmp/f29-03-t1.log 2>&1; echo "EXIT=$?"; grep -E "passed|failed|error|\[X\]" /tmp/f29-03-t1.log | tail -8</automated>
  </verify>

  <acceptance_criteria>
  - `grep -c "TERMO_DESCOBERTO_VERSAO" web/src/disclaimers.js` >= 1 e `DISCLAIMERS.descobertoTermo` existe com >= 500 caracteres (o termo do Operador tem ~950 — este não pode ser um placeholder de uma frase)
  - Source: o texto de `descobertoTermo` cobre os 6 pontos obrigatórios (verifique lendo-o, não por grep) e NÃO contém as palavras "garantido", "garantia de lucro", nem apresenta alavancagem como vantagem
  - Source: `grep -c "TERMO_DESCOBERTO_VERSAO" web/src/App.jsx` >= 2 (import + uso no subtítulo)
  - Source: existe exatamente UMA linha de import de `./disclaimers.js` em `App.jsx`
  - Source: `TermoDescobertoModal` contém `liTudo`, `aceito`, `busy`, um `useEffect` com `scrollHeight - clientHeight`, um `onScroll` com o limiar `12`, e `disabled={!liTudo || !aceito || busy}`
  - Source: `TermoDescobertoModal` contém `descobertoTermo:` e `permitirOpcaoADescoberto: true` na MESMA chamada de `A.saveConfig`
  - Source: `TermoDescobertoModal` NÃO contém `window.location.reload`
  - `npx vite build` verde
  - Suíte canônica sem regressão dos números do `29-02-SUMMARY.md`; exit 0
  </acceptance_criteria>

  <done>O termo versionado existe com substância real, e o modal espelha a máquina de fricção do Modo Operador gravando termo + flag num patch único.</done>
</task>

<task type="auto">
  <name>Task 2: Card do flag em Preferências + guardião da fricção e do fence D2</name>

  <files>web/src/App.jsx, web/tests/test_opcao_descoberto_ui.mjs</files>

  <read_first>
  - `web/src/App.jsx:7420-7487` — o fim da `ConfigScreen`: o card "PERFIL DO OPERADOR" (7421-7473) e, logo depois, o bloco de aviso completo (7475-7483). **O card novo entra na linha 7474**, entre os dois. Leia também 7341-7352 para os estilos `sectionTitle`/`card`/`seg` e o `const c = data.config`.
  - `web/src/App.jsx:2242-2315` — `ModoTrabalhoCard`: como um card de decisão de risco é montado (header de seção, controle, texto explicativo que mostra a data e a versão do termo aceito na linha 2309, e o modal renderizado condicionalmente na linha 2312). É o modelo do card desta task, e o precedente de "componente declarado fora, renderizado dentro" (`:2242` declarado, `:2609` renderizado).
  - `web/src/App.jsx:419-427` — `Toggle({ on, onClick, label, disabled })`.
  - `web/src/App.jsx:5503-5531` — exemplos de `<Toggle ... onClick={() => A.saveConfig({...})} label="..." />` dentro de uma tela de preferências (o card do Boris). Padrão de uso a copiar.
  - `web/tests/test_modo_operador.mjs` (arquivo inteiro, ~55 linhas) — o guardião irmão: source assertions sobre `App.jsx` e `persistence.js` lidos do disco. É o template deste guardião.
  - `web/tests/test_fase3_paridade_stores_generica.mjs:30-60` — técnica de fatiar por marcador de função em vez de número de linha.
  </read_first>

  <action>
  **2a. Card em `ConfigScreen`, inserido na linha 7474.** Um `div` com
  `{...card, padding: "17px 18px", marginBottom: "16px"}`, no formato dos
  outros cards da tela:
  - `<div style={sectionTitle}>OPÇÕES A DESCOBERTO</div>`
  - um parágrafo explicativo no mesmo estilo dos irmãos (`margin`,
    `T.textMuted`, `fontSize: "12.5px"`, `lineHeight: 1.5`, `maxWidth: "560px"`),
    dizendo em linguagem clara: o padrão da conta é operar opção **com lastro**;
    ligar isto libera abrir posição **sem lastro**, que pode perder o prêmio
    inteiro. Sem jargão solto e sem incentivo (princípio 8 + princípio 10 do
    CLAUDE.md: linguagem clara e transparência sobre risco).
  - a linha de controle: `<Toggle on={!!c.permitirOpcaoADescoberto} onClick={...} label="Operar opções a descoberto" />`
    com um rótulo de texto ao lado, no layout dos toggles de 5503-5531.
  - **Assimetria deliberada do `onClick`**, que é o coração do SC-3:
    - se está DESLIGADO → `setTermoOpen(true)` (abre o modal; NÃO chama
      `saveConfig`, porque o store recusaria e o toggle não mexeria)
    - se está LIGADO → `A.saveConfig({ permitirOpcaoADescoberto: false })`
      direto, sem modal e sem confirmação. Desligar é livre.
    Escreva isso em comentário nomeando a simetria com o Modo Operador
    ("voltar ao Estudo é livre", `App.jsx:2240`).
  - texto de estado, no espírito de `App.jsx:2309`: quando ligado, mostre a
    data do aceite e a versão do termo
    (`(c.descobertoTermo || {}).aceitoEm` fatiado em 10 chars, e `.versao`),
    com o mesmo fallback `"—"` / `"?"` do irmão.
  - `{termoOpen && <TermoDescobertoModal ctx={ctx} onClose={() => setTermoOpen(false)} />}`

  O `useState` do `termoOpen` precisa viver num componente. **Não** adicione um
  `useState` no meio do JSX da `ConfigScreen`: extraia o card para um
  componente próprio, `OpcaoDescobertoCard({ ctx })`, declarado logo antes de
  `ConfigScreen` (linha 7341) e renderizado como `<OpcaoDescobertoCard ctx={ctx} />`
  na linha 7474. É a mesma solução que `ModoTrabalhoCard` usa no Perfil.

  **NÃO** toque em `OpcoesCamada` (`App.jsx:3430`), em `doBuyOption`
  (`App.jsx:3417-3421`), nem em `A.buyOption` (`App.jsx:8439-8446`). O
  29-02-SUMMARY.md já registrou que a mensagem do gate chega à tela pelo
  `flash` existente. Fence D2: nenhuma migração de UI nesta fase.

  **2b. Guardião `web/tests/test_opcao_descoberto_ui.mjs`.** Cabeçalho de
  intenção no padrão da casa. Lê `App.jsx` e `disclaimers.js` do disco; fatia
  por marcador de função, não por número de linha. Use o helper `ok(name, cond)`
  dos arquivos irmãos e `process.exit(fails ? 1 : 0)`. Asserções obrigatórias:

  *Fricção (SC-3)*
  1. `DISCLAIMERS.descobertoTermo` existe em `disclaimers.js` e tem >= 500 chars.
  2. `TERMO_DESCOBERTO_VERSAO` é exportado de `disclaimers.js` e importado em `App.jsx`.
  3. O corpo de `TermoDescobertoModal` contém `liTudo`, `aceito` e
     `disabled={!liTudo || !aceito || busy}` — sem os três, a fricção é
     decorativa.
  4. O corpo contém o fallback de montagem (`scrollHeight - clientHeight`) —
     guardião do bug FASE 8B/P2: sem ele o aceite trava em tela grande.
  5. O corpo contém `onScroll` e o limiar `12`.
  6. O subtítulo do modal renderiza `TERMO_DESCOBERTO_VERSAO` (a versão é
     visível ao usuário, não só gravada).
  7. O aceite grava termo + flag no MESMO patch: um `includes` da forma
     `descobertoTermo:` … `permitirOpcaoADescoberto: true` dentro de uma única
     chamada de `A.saveConfig`.
  8. O corpo de `TermoDescobertoModal` NÃO contém `window.location.reload`.

  *Assimetria ligar/desligar*
  9. O corpo de `OpcaoDescobertoCard` contém `setTermoOpen(true)` (caminho de
     ligar) E `permitirOpcaoADescoberto: false` (caminho de desligar) — os dois
     caminhos existem e são diferentes.
  10. O corpo de `OpcaoDescobertoCard` NÃO contém
      `permitirOpcaoADescoberto: true` — ligar NUNCA acontece fora do modal.
      Esta é a asserção que impede alguém de "simplificar" a fricção para um
      toque.

  *Fence D2 (SC-5)*
  11. `OpcoesCamada` continua existindo em `App.jsx` (confirme o identificador
      real com `grep -n "OpcoesCamada" web/src/App.jsx` antes de escrever a
      asserção) e `doBuyOption` continua chamando `A.buyOption`.
  12. `web/src/opcoes/OpcoesScreen.jsx` e `web/src/opcoes/PropostaLastreada.jsx`
      NÃO mencionam nenhum dos dois campos de config — a fase não os tocou.
      (Confirme os caminhos reais com `ls web/src/opcoes/` antes de escrever a
      asserção; se um caminho mudou, corrija a asserção, não a remova.)
  13. `server/app/agent.py` NÃO menciona `permitirOpcaoADescoberto` — o
      Operador IA não ganhou caminho para abrir posição (fora de escopo
      explícito do 29-CONTEXT.md).

  *Acessibilidade*
  14. O `Toggle` do card recebe `label` (o componente já emite
      `role="switch"` + `aria-checked` + `aria-label`, `App.jsx:422`) — sem
      `label` o switch fica sem nome accessível.
  </action>

  <verify>
    <automated>cd /Users/acamerini/dev/bolsia/b3-agente && npx vite build --config web/vite.config.js > /tmp/f29-03-t2-build.log 2>&1; echo "BUILD_EXIT=$?"; tail -4 /tmp/f29-03-t2-build.log; cd web && node tests/test_opcao_descoberto_ui.mjs; echo "GUARDIAO_EXIT=$?"; cd /Users/acamerini/dev/bolsia/b3-agente && bash scripts/executar.sh --testes > /tmp/f29-03-t2.log 2>&1; echo "EXIT=$?"; grep -E "passed|failed|error|\[X\]" /tmp/f29-03-t2.log | tail -8</automated>
  </verify>

  <acceptance_criteria>
  - `web/tests/test_opcao_descoberto_ui.mjs` existe, roda com `node` de dentro de `web/`, exit 0, imprime >= 14 linhas `ok `
  - Source: `OpcaoDescobertoCard` é um componente de topo (declarado fora de `ConfigScreen`) e é renderizado exatamente UMA vez em `ConfigScreen`
  - Source: nenhum `useState` novo foi adicionado dentro do corpo de `ConfigScreen`
  - Source: `git diff web/src/App.jsx` não mostra NENHUMA linha alterada entre 3400 e 3620 (região de `OpcoesCamada`) nem entre 8430 e 8460 (região de `A.buyOption`/`A.sellOption`) — fence D2 verificado no diff, não só por asserção
  - `git diff --stat` mostra APENAS `web/src/disclaimers.js`, `web/src/App.jsx` e o guardião novo
  - `npx vite build` verde
  - `web/tests` passa de 145 para 146 arquivos, todos `[OK]`
  - Prova negativa exigida, registrada no SUMMARY: troque `disabled={!liTudo || !aceito || busy}` por `disabled={busy}` e confirme que a asserção 3 FALHA; restaure. E remova o `setTermoOpen(true)` do caminho de ligar e confirme que a asserção 9 FALHA; restaure.
  - Suíte canônica sem regressão; exit 0
  </acceptance_criteria>

  <done>O card existe em Preferências com a assimetria ligar/desligar correta, o guardião cobre fricção + fence D2 + acessibilidade, e as provas negativas confirmam que ele morde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação humana do circuito completo do flag</name>

  <files>(nenhum — task de verificação; nenhum arquivo é escrito)</files>

  <read_first>
  - `.planning/phases/29-opcao-a-descoberto-flag-opt-in/29-01-SUMMARY.md` e `29-02-SUMMARY.md` — só as seções de resultado, para montar o relato do que foi construído.
  - `.planning/phases/28-aba-opcoes-sub-aba-operar/28-03-SUMMARY.md` — apenas a parte sobre a tentativa de aprovação por relato do orquestrador, que foi registrada e rejeitada. É o precedente que define a regra deste checkpoint.
  </read_first>

  <action>
  Esta é uma task de **checkpoint bloqueante**, não de implementação: nenhum
  arquivo é escrito e nenhum comando muda o repositório.

  O que o executor faz:
  1. Confirme que as Tasks 1 e 2 estão verdes (suíte canônica + `npx vite build`)
     e que as provas negativas foram feitas.
  2. Suba o app local com `bash scripts/executar.sh` (SEM `--testes`) para o
     desenvolvedor poder exercitar a tela, e informe a URL.
  3. Apresente ao desenvolvedor o bloco `<what-built>` e o roteiro de
     `<how-to-verify>` abaixo, **literalmente e por inteiro**, sem resumir os
     10 passos.
  4. **PARE e aguarde.** Não prossiga, não marque a fase como completa, não
     faça commit de conclusão e não dê push. Lembrete do CLAUDE.md/memória: uma
     fase com checkpoint humano segura o push da FASE INTEIRA até a aprovação.
  5. Se o desenvolvedor relatar divergência, registre-a no SUMMARY por número
     de passo e trate como gap — não reinterprete como aprovação parcial.
  6. Se o desenvolvedor aprovar, registre a aprovação com data e o texto exato
     da resposta dele no `29-03-SUMMARY.md`.

  Proibido nesta task: aprovar em nome do desenvolvedor; tratar silêncio,
  ausência de resposta, ou relato de outro agente como aprovação; rodar o
  roteiro "por simulação" e declarar os passos conferidos.
  </action>

  <what-built>
  - Backend (29-01): `permitirOpcaoADescoberto` + `descobertoTermo` em `config`, gate em `store.buy_option`, tradução 400 na rota `/api/options/buy`, 13+ testes de guardião.
  - Estado do front (29-02): os dois campos no `deviceStore` com sync para o servidor, gate espelhado no ramo local de `optionsBuy` com a mensagem byte a byte do backend, 17+ asserções de guardião.
  - UI (29-03): termo versionado (`Opções a descoberto · versão 1.0`), `TermoDescobertoModal` com rolagem obrigatória + checkbox, e o card "OPÇÕES A DESCOBERTO" em Preferências com toggle.
  - Fence D2 respeitado: `OpcoesCamada` segue em Watchlist/Radar, `OpcoesScreen.jsx`/`PropostaLastreada.jsx`/`agent.py` intocados.
  - Automatizado até onde dá: suíte canônica verde, `npx vite build` verde, provas negativas feitas em todos os três planos.
  </what-built>

  <how-to-verify>
  Roteiro no app rodando. O `29-CONTEXT.md` (item 5) registra que o modal do
  Modo Operador é frágil a automação de teste — por isso a fricção é
  verificada por humano, não por script.

  **Preparação.** Suba o app local (`bash scripts/executar.sh`, sem
  `--testes`) e entre com uma conta. Anote o caixa inicial.

  1. **Estado inicial (SC-1).** Abra Perfil → Preferências e role até o card
     "OPÇÕES A DESCOBERTO". Esperado: toggle **DESLIGADO**, sem data de
     aceite, com o texto explicando que o padrão é lastro obrigatório.
  2. **Recusa antes de ligar (SC-2).** Vá ao Mercado/Watchlist, abra a camada
     de opções de um ativo líquido (PETR4), e toque em comprar um contrato.
     Esperado: **nada é comprado**, e um toast com a mensagem que aponta
     Preferências. Confirme em Portfólio que o caixa NÃO mudou e nenhuma
     posição de opção apareceu. Em Histórico, deve haver um registro
     **rejeitado** com o motivo.
  3. **A fricção (SC-3).** Volte a Preferências e toque no toggle. Esperado:
     o modal abre, o checkbox está **desabilitado**, e o botão "Ligar opções a
     descoberto" está **cinza/inativo**. Tente tocar no botão: nada acontece.
  4. **Rolagem obrigatória.** Role o texto do termo até o fim. Esperado: a dica
     "↓ role o texto até o fim" desaparece e o checkbox fica habilitado. Marque
     o checkbox. Esperado: o botão fica ativo.
  5. **Aceite.** Toque em "Ligar opções a descoberto". Esperado: o modal fecha,
     o toggle aparece **LIGADO**, e o texto do card passa a mostrar a data de
     hoje e "v1.0". O app **não** recarrega (é diferente do Modo Operador, de
     propósito).
  6. **A compra passa a funcionar.** Repita o passo 2. Esperado: a compra
     **executa** — posição de opção criada e caixa debitado pelo prêmio.
  7. **Fechar nunca é barrado (SC-4) — o passo mais importante.** Volte a
     Preferências e **DESLIGUE** o toggle (esperado: um toque, sem modal,
     sem confirmação). Agora volte à posição de opção aberta no passo 6 e
     **venda**. Esperado: a venda **executa** normalmente. Se ela for barrada,
     é falha grave — reporte imediatamente, não aprove.
  8. **A compra volta a ser barrada.** Com o flag desligado, tente comprar a
     seco de novo. Esperado: recusa, como no passo 2.
  9. **Religar não pede releitura.** Toque no toggle para ligar de novo.
     Esperado: o modal abre (a fricção é por ativação, não por conta) — mas se
     ele abrir já com o checkbox liberado por o texto caber na tela, isso é o
     fallback correto, não bug.
  10. **Fence D2 (SC-5).** Confirme que a compra a seco continua morando na
      camada de opções do card do ativo em Watchlist/Radar, e que a aba
      Opções → sub-aba "Operar" (Fase 28) segue mostrando só a proposta
      lastreada, **sem** nenhum controle novo de a descoberto.

  Se algum passo divergir, descreva qual passo, o que esperava e o que
  aconteceu — não aprove parcialmente.
  </how-to-verify>

  <verify>
    <human-check>Os 10 passos do roteiro conferidos pelo próprio desenvolvedor, com o passo 7 (vender com o flag desligado) explicitamente confirmado.</human-check>
    <automated>cd /Users/acamerini/dev/bolsia/b3-agente && git log --oneline -1 && git status --porcelain | head -10 && echo "SEM PUSH: $(git rev-list --count origin/main..HEAD 2>/dev/null || echo n/a) commit(s) locais nao publicados"</automated>
  </verify>

  <acceptance_criteria>
  - O roteiro de 10 passos foi apresentado ao desenvolvedor por inteiro, sem resumo
  - A resposta do desenvolvedor está registrada no `29-03-SUMMARY.md` com data e texto literal
  - Se aprovado: o passo 7 consta nominalmente como confirmado
  - Se reprovado: cada divergência está registrada por número de passo, e a fase NÃO é marcada como completa
  - Nenhum push a `origin` e nenhum `bump.sh`/`publicar-web.sh` foi executado antes da aprovação
  - Aprovação por relato do orquestrador ou de qualquer outro agente NÃO conta (precedente registrado em `28-03-SUMMARY.md`)
  </acceptance_criteria>

  <resume-signal>
  Responda "aprovado" só se os 10 passos conferirem. Caso contrário, descreva a
  divergência por número de passo. Aprovação por relato do orquestrador não
  conta — apenas a sua própria confirmação encerra este checkpoint (precedente
  registrado em `28-03-SUMMARY.md`).
  </resume-signal>

  <done>O desenvolvedor exercitou os 10 passos no app rodando e registrou aprovação ou divergência por escrito; nada foi publicado antes disso.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário → modal de aceite | o consentimento é o único artefato que autoriza a exceção ao lastro obrigatório; se a fricção é decorativa, o consentimento é ficção |
| UI → `A.saveConfig` | a UI é conveniência, não a defesa: os stores (29-01/29-02) recusam ligar sem termo independentemente do que a tela mande |
| `ConfigScreen` → `OpcoesCamada` | fence D2: a UI da compra a seco não se move nesta fase; qualquer edição nessa região é escopo vazando |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-11 | Repudiation ("eu não sabia do risco") | `TermoDescobertoModal` (`web/src/App.jsx`, Task 1c) | mitigate | Aceite exige rolagem até o fim + checkbox + botão travado em `!liTudo \|\| !aceito \|\| busy`, e grava `{aceitoEm, versao}`. Guardiões: asserções 3, 5, 6, 7 da Task 2b + prova negativa. A versão do termo fica visível na tela e no card. |
| T-29-12 | Elevation of Privilege (ligar sem passar pela fricção) | `OpcaoDescobertoCard.onClick` | mitigate | O caminho de LIGAR só chama `setTermoOpen(true)`; o card **não contém** `permitirOpcaoADescoberto: true` em nenhum lugar (asserção 10, negativa). E mesmo que a UI mandasse, os dois stores recusam ligar sem `descobertoTermo` (29-01 Task 1c, 29-02 Task 1b) — a UI é a segunda linha, não a única. |
| T-29-13 | Denial of Service (aceite impossível em tela grande) | `useEffect` de fallback do modal | mitigate | `scrollHeight - clientHeight < 12` + `setTimeout(250)` + listener de `resize`, copiado de `App.jsx:2326-2335`. Sem ele, texto que CABE sem rolagem nunca dispara `onScroll` e o aceite trava para sempre (bug real FASE 8B/P2). Guardião: asserção 4. |
| T-29-14 | Tampering (escopo vazando para a UI de opções) | região de `OpcoesCamada` (`App.jsx:3400-3620`) e `A.buyOption` (`:8430-8460`) | mitigate | Verificação por **diff**, não só por asserção: `git diff web/src/App.jsx` não pode mostrar linha alterada nessas duas faixas. Mais asserções 11, 12, 13 (incluindo `agent.py` sem o flag). Fence D2 do 29-CONTEXT.md. |
| T-29-15 | Information Disclosure / conformidade | texto do termo (`DISCLAIMERS.descobertoTermo`) | mitigate | Conteúdo obrigatório de 6 pontos, incluindo perda total do prêmio e liquidação por vencimento a zero; proibição explícita de linguagem de enriquecimento ou de alavancagem-como-vantagem (CLAUDE.md princípio 8). O termo é aviso, nunca convite. Revisado no checkpoint humano (passos 3-4). |
| T-29-16 | Spoofing (aprovação do checkpoint por terceiro) | checkpoint da Task 3 | mitigate | `resume-signal` e `acceptance_criteria` declaram que aprovação por relato do orquestrador não conta — só a confirmação do próprio desenvolvedor encerra. Precedente: `28-03-SUMMARY.md` registra e rejeita exatamente essa tentativa. |
| T-29-SC | Tampering (supply chain) | instalação de pacote npm/pip/cargo | mitigate | **Nenhuma dependência nova.** O modal reusa React + `Toggle` + `DISCLAIMERS` já presentes; o guardião usa só stdlib do Node. Nada entra em `web/package.json`. Se o executor concluir que precisa de um pacote, PARE e reporte. |
</threat_model>

<verification>
1. `cd web && node tests/test_opcao_descoberto_ui.mjs` → exit 0, >= 14 `ok`.
2. `npx vite build` verde.
3. `bash scripts/executar.sh --testes` (fora do sandbox) — sem regressão dos números do `29-02-SUMMARY.md`; `web/tests` passa a 146 arquivos, todos `[OK]`; exit 0.
4. Provas negativas da Task 2 executadas, restauradas e registradas.
5. `git diff --stat` mostra APENAS `web/src/disclaimers.js`, `web/src/App.jsx` e o guardião novo. Zero linha em `web/src/opcoes/`, em `web/src/persistence.js` (é do plano 29-02) e em `server/`.
6. Checkpoint da Task 3 aprovado pelo desenvolvedor, com o passo 7 (fechar com o flag desligado) explicitamente confirmado.
7. Nenhum `bump.sh`/`publicar-web.sh`/push a `origin` — publicação é passo humano separado, fora do escopo desta fase (mesma convenção que a Fase 28 registrou no ROADMAP). Como este plano toca `web/src/`, declare no SUMMARY que a entrega está **testada e NÃO publicada**, para ninguém supor que está no ar.
</verification>

<success_criteria>
- SC-3: ligar o flag exige termo com leitura até o fim, checkbox e versão registrada — o mesmo padrão do Modo Operador, reusado.
- SC-5: `OpcoesCamada` continua sendo a UI da compra a seco; nenhuma migração para a sub-aba "Operar", verificada por diff.
- SC-6: suíte canônica sem regressão; `npx vite build` verde.
</success_criteria>

<output>
Create `.planning/phases/29-opcao-a-descoberto-flag-opt-in/29-03-SUMMARY.md` when done.
Registre obrigatoriamente: o texto final do termo e sua versão; o resultado das
duas provas negativas; o resultado do checkpoint humano passo a passo (com o
passo 7 nominalmente); a confirmação por diff do fence D2; e a declaração
"testado e NÃO publicado".
</output>
