---
phase: 29-opcao-a-descoberto-flag-opt-in
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - web/src/persistence.js
  - web/tests/test_opcao_descoberto_store.mjs
autonomous: true
requirements: [SC-1, SC-2, SC-4]
must_haves:
  truths:
    - "No iPhone sem sessão, comprar a seco com o flag desligado é recusado pelo próprio `deviceStore` — não existe atalho local para o gate"
    - "`deviceStore.optionsSell` continua fechando posição a seco com o flag desligado, em qualquer estado"
    - "`deviceStore.putConfig` aceita `descobertoTermo` + `permitirOpcaoADescoberto` com a MESMA regra do servidor (ligar exige termo; desligar é livre)"
    - "Os dois campos sobem para o servidor no MESMO `putConfig` que os mudou, quando há sessão — sem janela em que o aparelho está ligado e o servidor não sabe"
    - "A mensagem de recusa do ramo local é byte a byte a do backend — uma redação, não duas"
  artifacts:
    - path: "web/src/persistence.js"
      provides: "sanitizer dos dois campos, regra de aceite no deviceStore.putConfig, sync device->servidor, gate no ramo local de optionsBuy"
      contains: "permitirOpcaoADescoberto"
    - path: "web/tests/test_opcao_descoberto_store.mjs"
      provides: "guardião de paridade da mensagem, do gate local, do sync e da ausência de gate na venda"
      min_lines: 90
  key_links:
    - from: "web/src/persistence.js deviceStore.optionsBuy (ramo sem sessão)"
      to: "doc.config.permitirOpcaoADescoberto"
      via: "guard clause antes de qualquer escrita em doc"
      pattern: "permitirOpcaoADescoberto"
    - from: "web/src/persistence.js deviceStore.putConfig"
      to: "api.putConfig"
      via: "bloco `if (sync.hasSession())` que manda só o que mudou"
      pattern: "descobertoTermo"
    - from: "web/src/persistence.js (string de recusa)"
      to: "server/app/store.py MOTIVO_DESCOBERTO_DESLIGADO"
      via: "literal espelhado, comparado por guardião que lê os dois arquivos"
      pattern: "a descoberto está desligado"
---

<objective>
Espelhar no estado do front o que o plano 29-01 construiu no backend: os dois
campos de config e — o ponto crítico — o gate no ramo LOCAL de
`deviceStore.optionsBuy`, que hoje reimplementa `store.buy_option` em
JavaScript e é a única superfície capaz de abrir posição a seco sem tocar o
servidor.

Purpose: `deviceStore.optionsBuy` (`web/src/persistence.js:1182`) tem dois
ramos — com sessão delega à rota (já gateada pelo 29-01), sem sessão executa a
compra INTEIRA localmente, mexendo em `doc.optionPositions`, `doc.cash` e
`doc.history`. Sem esta task, o gate tem um buraco do tamanho do app iOS
offline. O precedente de que ramos locais espelham o gate do backend já existe
no mesmo arquivo: `persistence.js:1418-1432` reimplementa "Lastro insuficiente"
em JS para a operação lastreada.

Output: paridade real do gate nos dois stores, com guardião próprio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/29-opcao-a-descoberto-flag-opt-in/29-CONTEXT.md
@.planning/phases/29-opcao-a-descoberto-flag-opt-in/29-01-SUMMARY.md

NÃO leia `.planning/STATE.md`. NÃO leia `web/src/App.jsx` (≈9500 linhas,
≈162K tokens) — este plano não toca nenhuma linha dele, e o único span que ele
precisa (8 linhas) está citado na Task 2c. NÃO leia
`web/src/persistence.js` inteiro (1800+ linhas): os intervalos exatos estão em
cada task.

**Baseline da suíte canônica, medida em 2026-09-13 no início desta fase:**
pytest **2780 passed / 5 skipped / 3 xfailed**; web **144 arquivos `.mjs`
todos `[OK]`**; exit 0. O plano 29-01 somou ~13 testes pytest e 0 `.mjs` —
leia os números finais no `29-01-SUMMARY.md` e use-os como baseline desta
entrega.

<interfaces>
<!-- Contratos do arquivo, extraídos. Não explore para achá-los. -->

`web/src/persistence.js` tem DOIS stores completos e paralelos
(`export const store = isNative ? deviceStore() : serverStore()`, linha 1171):

```js
function serverStore()   // linha 112 — web; o merge real é do servidor
  optConfig(patch)       // linha 115 — echo otimista GENÉRICO: copia TODAS as
                         //   chaves do patch (menos apiKey/clearKey). Já cobre
                         //   campos novos sem edição — NÃO precisa de mudança.
  putConfig(patch)       // linha 150 — relay: sync.mutate("putConfig", ...)
  optionsBuy(body)       // linha 262 — relay puro: api.optionsBuy(body)
  optionsSell(body)      // linha 263 — relay puro

function deviceStore()   // linha 374 — iOS, local-first
  _registrarRejeicaoLocal(tipo, t, qty, price, motivo)   // linha 539
  putConfig(patch)       // linha 702 — espelho MANUAL de store.py.set_config
  optionsBuy(body)       // linha 1182 — ramo com sessão + ramo LOCAL completo
  optionsSell(body)      // linha 1228 — ramo com sessão + ramo LOCAL completo
```

Consequência que decide o escopo deste plano: **`serverStore` não precisa de
nenhuma linha nova.** `optConfig` é genérico e `optionsBuy`/`optionsSell` são
relays. Toda a mudança é no `deviceStore`. Declare isso no SUMMARY para
ninguém confundir com esquecimento de paridade.

Guardião de paridade existente, que NÃO precisa de atualização:
`web/tests/test_fase3_paridade_stores_generica.mjs` compara NOMES de método
entre os dois stores. Este plano não adiciona método nenhum (reusa
`putConfig`/`optionsBuy`), então a allowlist de assimetrias intencionais fica
intocada.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Os dois campos no deviceStore — sanitizer, regra de aceite e sync para o servidor</name>

  <files>web/src/persistence.js</files>

  <read_first>
  - `web/src/persistence.js:470-476` — saneamento do doc carregado do disco; a linha 473 trata `operadorTermo` (`!== null && typeof !== "object"` → `null`). É o padrão a espelhar.
  - `web/src/persistence.js:747-755` — bloco "FASE 7 (F7.1) — Modo Operador" dentro de `deviceStore.putConfig`: sanea `operadorTermo` e aplica "operador só liga com termo já aceito". Espelho EXATO do `store.py.set_config` — é o padrão desta task.
  - `web/src/persistence.js:776-781` — exemplos de campo booleano simples no mesmo `putConfig` (`vozAtiva`, `fabVisivel`), com o comentário que explica por que a allowlist existe.
  - `web/src/persistence.js:783-834` — bloco `if (sync.hasSession())`: manda ao servidor SÓ o campo que o chamador mexeu, chave a chave. A linha 805 é o caso `operadorTermo`; 828-832 é a regra "ligar exige o termo no MESMO patch quando o servidor ainda não o tem". Leia os comentários: eles nomeiam dois incidentes reais (appMode e initialBudget) causados por esquecer exatamente esta parte.
  - `web/tests/test_putconfig_so_o_que_mudou.mjs` (arquivo inteiro) — guardião que asserte a disciplina "sem carona" do bloco de sync. Leia para não quebrá-lo.
  - No `29-01-SUMMARY.md`: os nomes finais dos dois campos e a regra de aceite implementada no servidor.
  </read_first>

  <action>
  **1a. Saneamento do doc, `web/src/persistence.js:473`.** Logo depois da linha
  que sanea `operadorTermo`, adicione duas linhas no mesmo estilo:
  - `descobertoTermo`: se não é `null` e não é `"object"`, volta a `null`
  - `permitirOpcaoADescoberto`: coage para booleano (`!!`), de forma que um doc
    antigo sem a chave ou com lixo (`"true"`, `1`) resolva para `false` —
    fail-closed, mesma disciplina do backend (29-01, Task 2b decisão 1).

  **1b. `deviceStore.putConfig`, depois do bloco de `appMode` (linha 755).**
  Espelho ESTRUTURAL do que o `set_config` do servidor faz (leia a regra final
  no `29-01-SUMMARY.md`, não invente variação):
  - se `patch.descobertoTermo` é objeto com `aceitoEm` E `versao`, grava
    `c.descobertoTermo = { aceitoEm: String(...).slice(0, 40), versao: String(...).slice(0, 10) }`
    — os MESMOS limites de 40/10 do servidor (`store.py:279`), senão um
    aparelho grava mais do que o servidor aceita e os dois divergem em
    silêncio.
  - se `"permitirOpcaoADescoberto" in patch`: liga só se
    `c.descobertoTermo` já é objeto; desligar é sempre livre.
    Escreva na forma negativa, espelhando a linha 753 (`if (!(patch.X && !(c.Y && typeof c.Y === "object"))) ...`),
    para o guardião conseguir asserir a forma.
  - comentário nomeando: desligar não exige termo e NÃO apaga
    `descobertoTermo` (religar não pede releitura), igual a `operadorTermo`.

  **1c. Sync device→servidor, dentro do `if (sync.hasSession())` (linha 802).**
  Adicione, no estilo chave-a-chave já usado:
  - `if ("descobertoTermo" in patch) enviar.descobertoTermo = c.descobertoTermo;`
  - `if ("permitirOpcaoADescoberto" in patch) enviar.permitirOpcaoADescoberto = c.permitirOpcaoADescoberto;`
  - e o espelho da regra de 828-832: se `enviar.permitirOpcaoADescoberto === true`
    e não há `enviar.descobertoTermo` mas `c.descobertoTermo` existe, mande o
    termo junto — senão o servidor recusa ligar (por desenho, 29-01 Task 1c) e
    o aparelho fica ligado com o servidor desligado. Este é exatamente o
    incidente que os comentários de 783-800 descrevem para `appMode`; não
    repita a classe.

  **NÃO** mande o objeto `c` inteiro nem adicione campo de carona: o guardião
  `test_putconfig_so_o_que_mudou.mjs` asserte a ausência dessa forma.
  **NÃO** toque em `serverStore` — `optConfig` (linha 115) já copia qualquer
  chave do patch, e o merge real é do servidor. Confirme isso lendo as linhas
  117-120 e registre no SUMMARY.
  </action>

  <verify>
    <automated>cd /Users/acamerini/dev/bolsia/b3-agente && bash scripts/executar.sh --testes > /tmp/f29-02-t1.log 2>&1; echo "EXIT=$?"; grep -E "passed|failed|error|\[X\]" /tmp/f29-02-t1.log | tail -8</automated>
  </verify>

  <acceptance_criteria>
  - Source: `grep -c "descobertoTermo" web/src/persistence.js` >= 5 e `grep -c "permitirOpcaoADescoberto" web/src/persistence.js` >= 4
  - Source: `grep -n "descobertoTermo" web/src/persistence.js` mostra ocorrências no saneamento (~linha 474), no `putConfig` (~756) e no bloco de sync (~806) — as três, não só uma
  - Source: zero ocorrência dos dois nomes DENTRO do corpo de `serverStore()` (entre `function serverStore()` na linha 112 e `function deviceStore()` na linha 374) — a mudança é toda no deviceStore, por desenho
  - Comportamento (o guardião da Task 3 cobre, mas confirme antes): `putConfig({permitirOpcaoADescoberto: true})` sem termo deixa `false`; com termo no mesmo patch, `true`; `putConfig({permitirOpcaoADescoberto: false})` sempre resulta `false` e preserva `descobertoTermo`
  - Comportamento: com sessão, `putConfig({permitirOpcaoADescoberto: true, descobertoTermo: {...}})` chama `api.putConfig` com um corpo que contém AMBOS os campos
  - Comportamento: `putConfig({theme: "dark"})` continua NÃO chamando `api.putConfig` (`test_putconfig_so_o_que_mudou.mjs` e `test_appmode_sincroniza_servidor.mjs` seguem verdes)
  - `npx vite build` verde (front editado — grep e teste estático não pegam erro de sintaxe JS)
  - Suíte canônica sem regressão da baseline do `29-01-SUMMARY.md`; exit 0
  </acceptance_criteria>

  <done>Os dois campos existem no deviceStore com a mesma regra do servidor, sobem no mesmo putConfig que os mudou, e nenhum guardião de sync existente quebrou.</done>
</task>

<task type="auto">
  <name>Task 2: O gate no ramo LOCAL de optionsBuy — e a prova de que optionsSell não o recebe</name>

  <files>web/src/persistence.js</files>

  <read_first>
  - `web/src/persistence.js:1182-1227` — `deviceStore.optionsBuy` inteira. Linhas 1186-1193 são o ramo COM sessão (delega à rota, já gateada pelo 29-01 — não mexa nele). Linhas 1194-1226 são o ramo LOCAL que reimplementa `store.buy_option` em JS: é onde o gate entra.
  - `web/src/persistence.js:1228-1250` — `deviceStore.optionsSell`. **Leia para saber o que NÃO tocar**: nenhuma linha muda.
  - `web/src/persistence.js:539-548` — `_registrarRejeicaoLocal(tipo, t, qty, price, motivo)`: espelho JS de `store.registrar_rejeicao`, com a mesma poda por `CAP_REJEICOES_LOCAL`.
  - `web/src/persistence.js:1415-1432` — precedente EXATO de gate espelhado no ramo local: `_registrarRejeicaoLocal(...)` + `write()` + `throw new Error(motivo)`, com a mensagem "Lastro insuficiente: ..." idêntica à do backend. É a forma de três linhas a copiar.
  - `web/src/App.jsx:8439-8446` — **só estas 8 linhas** (`A.buyOption`), para a confirmação da Task 2c. Não leia mais nada do App.jsx.
  - No `29-01-SUMMARY.md`: a string exata de `MOTIVO_DESCOBERTO_DESLIGADO`.
  </read_first>

  <action>
  **2a. O gate.** No ramo LOCAL de `deviceStore.optionsBuy` (depois da linha
  1183 `ensure();` e do `if (sync.hasSession()) {...}`, ou seja a partir da
  linha 1194), insira o gate **ANTES** da chamada `api.optionsChain` da linha
  1194. Duas razões para vir antes, ambas a declarar em comentário:
  1. uma compra que já está proibida não pode queimar requisição do provedor
     de opções (orçamento brapi de 15k/mês para o app inteiro, ADR-008) — é o
     mesmo argumento de `test_options_buy_qty_nao_inteiro_400_resto_do_a00b`
     ("pedido inválido não busca cadeia de opções");
  2. o gate não depende de preço nenhum, então nada se ganha esperando.

  Forma, espelhando `persistence.js:1418-1422`:
  - declare no topo do módulo (perto de `CAP_REJEICOES_LOCAL`, linha 53) a
    constante `MOTIVO_DESCOBERTO_DESLIGADO`, com o valor **byte a byte** igual
    ao do `server/app/store.py` (copie do `29-01-SUMMARY.md`; não redigite).
    Comentário obrigatório: "espelho byte a byte de
    `server/app/store.py:MOTIVO_DESCOBERTO_DESLIGADO` — guardião
    `web/tests/test_opcao_descoberto_store.mjs` compara os dois arquivos".
  - no gate: se `!doc.config.permitirOpcaoADescoberto`, chame
    `_registrarRejeicaoLocal("COMPRA", body.contractSymbol, <qty normalizada>, null, MOTIVO_DESCOBERTO_DESLIGADO)`,
    depois `write()`, depois `throw new Error(MOTIVO_DESCOBERTO_DESLIGADO)`.
  - `price` é `null` de propósito: o gate roda antes de existir preço, e a
    convenção da casa é `null`, nunca `0.0` (`store.py:747-749`,
    `test_m3_format_pede_null_nunca_zero`). `_registrarRejeicaoLocal` já
    preserva `null` (linha 540).
  - a `qty` gravada na rejeição é a normalizada por lote de 100 — reuse a
    mesma expressão `Math.max(100, Math.round((body.qty || 0) / 100) * 100)`
    que a linha 1200 já usa, para o histórico de rejeição casar com o de
    execução.
  - fail-closed: `doc.config.permitirOpcaoADescoberto` ausente/undefined é
    falsy → barra. A coação da Task 1a já garante booleano, mas a guard clause
    não depende dela.

  **2b. `deviceStore.optionsSell` NÃO recebe gate.** Adicione UM comentário de
  linha no topo dela (linha 1228) declarando a decisão e a razão (guardrail do
  CLAUDE.md "Stop/alvo nunca são vetados"; fechar posição já aberta nunca é
  barrado pelo flag, inclusive se o flag foi desligado depois da abertura) e
  nomeando o guardião que protege isso. Nenhuma outra linha dela muda.

  **2c. `A.buyOption` no App.jsx NÃO muda.** Confirme por leitura de
  `web/src/App.jsx:8439-8446` (só este span, 8 linhas — não leia o arquivo)
  que o `catch` já faz `flash("Compra de opção: " + (e.message || e))`, ou
  seja a mensagem do backend/do gate local já chega à tela sem código novo.
  Registre a confirmação no SUMMARY com a citação de linha. Se por algum
  motivo NÃO chegar, pare e reporte — não improvise um caminho de erro novo
  (D2 do 29-CONTEXT.md: o único comportamento novo na UI existente é a
  recusa, e ela vem do backend).
  </action>

  <verify>
    <automated>cd /Users/acamerini/dev/bolsia/b3-agente && npx vite build --config web/vite.config.js > /tmp/f29-02-t2-build.log 2>&1; echo "BUILD_EXIT=$?"; tail -4 /tmp/f29-02-t2-build.log; bash scripts/executar.sh --testes > /tmp/f29-02-t2.log 2>&1; echo "EXIT=$?"; grep -E "passed|failed|error|\[X\]" /tmp/f29-02-t2.log | tail -8</automated>
  </verify>

  <acceptance_criteria>
  - Source: a constante `MOTIVO_DESCOBERTO_DESLIGADO` existe em `web/src/persistence.js` e seu valor é **idêntico byte a byte** ao literal de `server/app/store.py` (a Task 3 automatiza essa comparação; confirme aqui com um diff manual das duas strings)
  - Source assertion (a decisão de SC-4): o corpo de `deviceStore.optionsSell` — de `async optionsSell(body) {` até `async putOptionPosition(` — NÃO contém `permitirOpcaoADescoberto`
  - Source: no corpo de `deviceStore.optionsBuy`, o índice da primeira ocorrência de `permitirOpcaoADescoberto` é MENOR que o da primeira ocorrência de `api.optionsChain` — o gate vem antes de gastar requisição do provedor
  - Comportamento (sem sessão, flag desligado): `optionsBuy(...)` rejeita com `Error` cuja `message` é exatamente a constante; `doc.optionPositions` inalterado; `doc.cash` inalterado; `doc.history[0].status === "rejeitada"` com `type === "COMPRA"` e `price === null`
  - Comportamento (sem sessão, flag ligado): `optionsBuy(...)` executa como antes — posição criada, caixa debitado
  - Comportamento (sem sessão): ligar o flag → comprar a seco → DESLIGAR o flag → `optionsSell(...)` executa, `doc.optionPositions` fica `[]` e o caixa é creditado
  - `npx vite build` verde
  - Suíte canônica sem regressão; exit 0
  </acceptance_criteria>

  <done>O ramo local de compra a seco tem o mesmo gate do backend com a mesma mensagem, a venda local está provadamente intocada, e o build passa.</done>
</task>

<task type="auto">
  <name>Task 3: Guardião do store — paridade da mensagem, gate local, sync e a ausência de gate na venda</name>

  <files>web/tests/test_opcao_descoberto_store.mjs</files>

  <read_first>
  - `web/tests/test_appmode_sincroniza_servidor.mjs` (arquivo inteiro, ~75 linhas) — template EXATO deste tipo de teste: monta `deviceStore` com `api` mockado, alterna `sync.hasSession()`, e asserte se `PUT /api/config` foi chamado e com qual corpo. É o guardião irmão (`appMode`/`operadorTermo`) do que esta task escreve.
  - `web/tests/test_modo_operador.mjs` (arquivo inteiro, ~55 linhas) — template das source assertions do padrão de termo: lê `persistence.js` e `App.jsx` do disco e asserte a presença de formas específicas (ex.: linha 46, `pers.includes('patch.appMode === "operador" && !(c.operadorTermo')`).
  - `web/tests/test_fase3_paridade_stores_generica.mjs:30-60` — como um guardião deste repo fatia `persistence.js` por marcador de função (`src.indexOf("function serverStore()")`) em vez de por número de linha. Use a MESMA técnica: números de linha envelhecem, marcadores não.
  - `web/src/persistence.js:1171` — `export const store = isNative ? deviceStore() : serverStore();` e como os testes existentes contornam isso para instanciar o `deviceStore` explicitamente (veja o topo do `test_appmode_sincroniza_servidor.mjs`).
  </read_first>

  <action>
  Crie `web/tests/test_opcao_descoberto_store.mjs`. Cabeçalho de intenção no
  padrão da casa (comentário de bloco nomeando o que o arquivo PROTEGE, por
  que existe, e qual incidente ele previne). Rode sem build:
  `node web/tests/test_opcao_descoberto_store.mjs` de dentro de `web/`.
  O runner (`scripts/executar.sh:33`) faz glob em `web/tests/*.mjs` — o arquivo
  é descoberto automaticamente, não há registro a fazer.

  Use o helper `ok(name, cond)` no formato dos arquivos irmãos e
  `process.exit(fails ? 1 : 0)` no fim.

  Asserções obrigatórias:

  **Grupo A — paridade da mensagem (a que mais vale)**
  1. Leia `server/app/store.py` E `web/src/persistence.js` do disco. Extraia o
     valor do literal `MOTIVO_DESCOBERTO_DESLIGADO` de cada um (regex sobre a
     atribuição) e asserte **igualdade byte a byte**. Se o regex não casar em
     qualquer um dos dois arquivos, FALHE com mensagem explícita ("a constante
     mudou de forma — o guardião não pode passar sem conseguir lê-la"), nunca
     passe por vacuidade. Esta é a mesma disciplina da paridade
     `defaults.py` ↔ `catalog.js` do CLAUDE.md.

  **Grupo B — forma do gate (source assertions, fatiando por marcador)**
  2. No corpo de `deviceStore.optionsBuy`, `permitirOpcaoADescoberto` aparece.
  3. No corpo de `deviceStore.optionsBuy`, o índice da primeira ocorrência de
     `permitirOpcaoADescoberto` < índice da primeira de `api.optionsChain`
     (gate antes de gastar requisição do provedor).
  4. No corpo de `deviceStore.optionsSell`, `permitirOpcaoADescoberto` **NÃO**
     aparece. Guardião da DECISÃO SC-4, não só do comportamento.
  5. No corpo de `serverStore()`, nenhum dos dois nomes aparece (a mudança é
     toda do deviceStore, por desenho — `optConfig` é genérico).
  6. A regra de aceite existe no `putConfig` do deviceStore na forma negativa
     esperada (um `includes` sobre a substring da condição, no estilo de
     `test_modo_operador.mjs:46`).

  **Grupo C — comportamento do deviceStore (instância real, `api` mockado)**
  7. Default: doc novo → `getState().config.permitirOpcaoADescoberto === false`
     e `descobertoTermo === null`.
  8. `putConfig({permitirOpcaoADescoberto: true})` sem termo → segue `false`.
  9. `putConfig({descobertoTermo: {aceitoEm, versao}, permitirOpcaoADescoberto: true})` → `true`.
  10. `putConfig({permitirOpcaoADescoberto: false})` → `false`, e
      `descobertoTermo` continua objeto.
  11. Doc carregado com lixo (`permitirOpcaoADescoberto: "true"`) é saneado
      para booleano `false` — fail-closed.
  12. Sem sessão, flag desligado: `optionsBuy(...)` rejeita; a `message` é a
      constante; `optionPositions` vazio; `cash` inalterado;
      `history[0].status === "rejeitada"` e `history[0].price === null`.
  13. Sem sessão, flag desligado: `api.optionsChain` **não** foi chamado (conte
      as chamadas no mock) — o gate não queima requisição do provedor.
  14. **A sequência do 29-CONTEXT.md**: sem sessão, ligue o flag → compre a
      seco (mock de cadeia OK) → DESLIGUE o flag → `optionsSell(...)` executa.
      Asserte `optionPositions === []` e caixa creditado. Este é o teste que o
      quality gate da fase exige nominalmente.

  **Grupo D — sync (com sessão)**
  15. Com sessão, `putConfig({descobertoTermo, permitirOpcaoADescoberto: true})`
      chama `api.putConfig` com um corpo contendo AMBOS os campos.
  16. Com sessão, `putConfig({permitirOpcaoADescoberto: true})` quando o termo
      já está no doc local manda o termo de carona (regra espelhada de
      `persistence.js:828-832`) — senão o servidor recusaria ligar.
  17. Com sessão, `putConfig({theme: "dark"})` NÃO leva nenhum dos dois campos
      no corpo (disciplina "sem carona").
  </action>

  <verify>
    <automated>cd /Users/acamerini/dev/bolsia/b3-agente/web && node tests/test_opcao_descoberto_store.mjs; echo "GUARDIAO_EXIT=$?"; cd /Users/acamerini/dev/bolsia/b3-agente && bash scripts/executar.sh --testes > /tmp/f29-02-t3.log 2>&1; echo "EXIT=$?"; grep -E "passed|failed|error|\[X\]" /tmp/f29-02-t3.log | tail -8</automated>
  </verify>

  <acceptance_criteria>
  - `web/tests/test_opcao_descoberto_store.mjs` existe, roda com `node` de dentro de `web/`, exit 0, e imprime >= 17 linhas `ok `
  - A asserção 1 compara os dois arquivos de verdade (lê `server/app/store.py` do disco) e falha explicitamente se o regex não casar
  - A asserção 14 executa a sequência ligar→comprar→desligar→vender
  - Prova negativa exigida, em DUAS partes, registrada no SUMMARY:
    (a) comente o gate da Task 2a e confirme que as asserções 2, 3, 12 e 13 FALHAM; restaure.
    (b) altere um caractere da constante em `web/src/persistence.js` e confirme que a asserção 1 FALHA; restaure.
    Um guardião que passa com e sem o que ele guarda não guarda nada.
  - `web/tests/*.mjs` passa de 144 para 145 arquivos, todos `[OK]`
  - Suíte canônica sem regressão dos números do `29-01-SUMMARY.md`; exit 0
  </acceptance_criteria>

  <done>O guardião existe, cobre paridade de mensagem, forma do gate, comportamento e sync, e as duas provas negativas confirmam que ele detecta a remoção do gate e a divergência da mensagem.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app iOS sem sessão → `deviceStore` local | o gate do backend não é atravessado: `deviceStore.optionsBuy` reimplementa a compra em JS e escreve direto em `doc.optionPositions`/`doc.cash`/`doc.history` |
| `deviceStore` → `api.putConfig` | o aparelho é autoridade local do config; se ele liga o flag e não sincroniza, aparelho e servidor divergem em silêncio |
| literal em `store.py` ↔ literal em `persistence.js` | duas redações da mesma recusa é divergência esperando acontecer (a paridade `defaults.py`↔`catalog.js` do CLAUDE.md existe por esse motivo) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-06 | Elevation of Privilege | ramo LOCAL de `deviceStore.optionsBuy` (`web/src/persistence.js:1194-1226`) — abre posição a seco sem tocar o servidor | mitigate | Gate espelhado no ramo local ANTES do fetch de cadeia (Task 2a), com `_registrarRejeicaoLocal` + `throw`, exatamente como `persistence.js:1418-1422` já faz para "Lastro insuficiente". Guardiões: asserções 2, 3, 12, 13 da Task 3 + prova negativa (a). |
| T-29-07 | Denial of Service (usuário trancado na própria posição, no aparelho) | ramo LOCAL de `deviceStore.optionsSell` (`persistence.js:1238-1249`) | mitigate | Nenhuma linha de `optionsSell` muda. Provado por source assertion (asserção 4, ausência do nome do flag no corpo) E por comportamento na sequência ligar→comprar→desligar→vender (asserção 14). |
| T-29-08 | Tampering (divergência silenciosa de mensagem) | constante duplicada em Python e JS | mitigate | Guardião que lê os DOIS arquivos do disco e compara byte a byte (asserção 1), com falha explícita se não conseguir extrair o literal — nunca passa por vacuidade. Prova negativa (b) confirma. |
| T-29-09 | Tampering (aparelho ligado, servidor desligado) | bloco `if (sync.hasSession())` de `deviceStore.putConfig` | mitigate | Os dois campos sobem no MESMO `putConfig` que os mudou, chave a chave, e o termo vai de carona quando o flag liga (Task 1c) — porque o servidor recusa ligar sem termo por desenho. É a classe de incidente que `persistence.js:783-800` documenta para `appMode`. Guardiões: asserções 15, 16, 17. |
| T-29-10 | Tampering (config com lixo lido do disco) | saneamento do doc (`persistence.js:473`) | mitigate | `permitirOpcaoADescoberto` é coagido para booleano e `descobertoTermo` não-objeto volta a `null` (Task 1a). `"true"`/`1` no disco não destravam nada: fail-closed. Guardião: asserção 11. |
| T-29-SC | Tampering (supply chain) | instalação de pacote npm/pip/cargo | mitigate | **Nenhuma dependência nova.** O guardião usa só `fs`/`path`/`url` da stdlib do Node, no formato dos `.mjs` existentes; nada é adicionado a `web/package.json`. Se o executor concluir que precisa de um pacote, PARE e reporte. |
</threat_model>

<verification>
1. `cd web && node tests/test_opcao_descoberto_store.mjs` → exit 0, >= 17 `ok`.
2. `npx vite build` verde (front editado; grep e teste estático não pegam erro de sintaxe JS).
3. `bash scripts/executar.sh --testes` (fora do sandbox) — sem regressão dos números do `29-01-SUMMARY.md`; `web/tests` passa a 145 arquivos, todos `[OK]`; exit 0.
4. Provas negativas (a) e (b) da Task 3 executadas, restauradas, e registradas no SUMMARY.
5. `git diff --stat` mostra APENAS `web/src/persistence.js` e o guardião novo. Zero linha em `web/src/App.jsx`, `web/src/opcoes/OpcoesScreen.jsx`, `web/src/opcoes/PropostaLastreada.jsx` ou `server/app/agent.py` — fence D2 do 29-CONTEXT.md.
6. Nenhum `bump.sh`/`publicar-web.sh`/push a `origin`.
</verification>

<success_criteria>
- SC-1: doc novo e doc legado com lixo resolvem para DESLIGADO no aparelho.
- SC-2: compra a seco no ramo local é recusada pelo próprio store, com a mensagem do backend, sem queimar requisição do provedor.
- SC-4: `deviceStore.optionsSell` nunca gateado, provado por source assertion e pela sequência ligar→abrir→desligar→fechar.
- SC-6: suíte canônica sem regressão; `npx vite build` verde.
</success_criteria>

<output>
Create `.planning/phases/29-opcao-a-descoberto-flag-opt-in/29-02-SUMMARY.md` when done.
Registre obrigatoriamente: a confirmação (com citação de linha) de que
`serverStore` não precisou de mudança e por quê; a confirmação de que
`A.buyOption` (`App.jsx:8439-8446`) já repassa a mensagem do gate sem código
novo; o resultado das duas provas negativas; e a contagem de `.mjs` antes/depois.
</output>
