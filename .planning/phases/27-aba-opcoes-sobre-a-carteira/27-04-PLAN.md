---
phase: 27-aba-opcoes-sobre-a-carteira
plan: 04
type: execute
wave: 3
depends_on: ["27-02", "27-03"]
files_modified:
  - web/src/opcoes/OpcoesScreen.jsx
  - web/src/opcoes/ReguaRegime.jsx
  - web/src/opcoes/unidades.js
  - web/src/opcoes/useOpcoesMcp.js
  - web/src/copy.js
  - web/tests/test_opcoes_leitura_interna_ui.mjs
  - web/tests/test_opcoes_analisar_ui.mjs
autonomous: false
requirements: [SC-3, D1, D4, G3, G4]
user_setup: []

must_haves:
  truths:
    - "Tendencia, volatilidade e suporte/resistencia aparecem na tela assim que o ativo e selecionado, sem mexer no contador de cota"
    - "A regua de sete pregoes mostra se o regime da semana mudou, com hoje destacado"
    - "O bloco interno e a regua concordam entre si: o ultimo segmento diz o mesmo regime que a linha de tendencia"
    - "A volatilidade do motor interno (percentual) e a do servico (fracao) nunca passam pelo mesmo formatador"
  artifacts:
    - path: "web/src/opcoes/ReguaRegime.jsx"
      provides: "regua de 7 segmentos com o estado medido por pregao"
      min_lines: 40
    - path: "web/src/opcoes/unidades.js"
      provides: "formatador de volatilidade ESCOLHIDO pela unidade declarada no contrato (modulo puro, testavel por node)"
    - path: "web/tests/test_opcoes_leitura_interna_ui.mjs"
      provides: "guardiao do bloco interno, da regua, do rotulo gratis e da unidade"
  key_links:
    - from: "web/src/opcoes/useOpcoesMcp.js"
      to: "store.opcoesTecnico"
      via: "leitura interna disparada pela troca de ticker, custo zero"
      pattern: "store\\.opcoesTecnico"
    - from: "web/src/opcoes/OpcoesScreen.jsx"
      to: "web/src/opcoes/ReguaRegime.jsx"
      via: "render da regua dentro do bloco de leitura interna"
      pattern: "ReguaRegime"
    - from: "web/src/opcoes/OpcoesScreen.jsx"
      to: "web/src/opcoes/unidades.js"
      via: "formatador escolhido por volatilidade.unidade"
      pattern: "formatarVolatilidade"
---

<objective>
A leitura tecnica interna do 27-03 vira o primeiro bloco que a pessoa ve ao
escolher um ativo — de graca — e a evolucao de sete pregoes vira a regua de
regime do prototipo aprovado.

Purpose: sem a regua, o numero de hoje continua sem dizer se a volatilidade
subiu ou desabou na semana (D4). E sem o bloco interno, escolher um ativo
continua so tendo resposta paga.
Output: bloco de leitura interna com carimbo e selo de gratuidade,
`ReguaRegime.jsx`, e o formatador de volatilidade escolhido pela unidade
DECLARADA no contrato.

**Revisao de 2026-09-13 (verificacao adversarial):** este plano tinha uma
terceira task — "custo declarado em TODO controle que consome cota". Ela saiu
para o **27-05**, por custo de contexto: com as correcoes C2 e C3, ela passou a
mover `mcpLeitura` para fora do `useEffect`, criar um controle novo, reescrever
o guardiao de custo com cruzamento backend x front sobre treze metodos e tocar
`CriarSetup.jsx` — seis arquivos e um guardiao nao-trivial. Somada as duas
tasks que ficam, o plano passaria de 70% de contexto, e a regra da casa e ~50%.
Nada foi reduzido: o escopo inteiro continua na fase, num plano proprio.
Tres correcoes tambem entraram aqui: **C6** (o formatador e ESCOLHIDO pela
`volatilidade.unidade`, com guardiao permanente, em vez de um `grep` de uma
vez so mais o olho do humano), **C7** (o periodo da leitura interna fica
FIXO — sem isso, cada segmento da regua degradava para `confiavel:false`
enquanto a linha de tendencia logo acima dizia confiavel) e **C9** (a Task 1
era a unica das doze da fase sem injecao de defeito declarada, justamente
carregando metade do criterio 3 e a armadilha de unidade).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-aba-opcoes-sobre-a-carteira/27-CONTEXT.md
@.planning/phases/27-aba-opcoes-sobre-a-carteira/27-02-SUMMARY.md
@.planning/phases/27-aba-opcoes-sobre-a-carteira/27-03-SUMMARY.md
@CLAUDE.md
@.claude/skills/didatica-boris/SKILL.md

<interfaces>
<!-- Contratos prontos. Nao explorar o repo. -->

Do 27-03, nos DOIS stores:
- `store.opcoesTecnico(ticker, { period })` -> `{ t, tendencia, volatilidade,
  niveis, regua, carimbo, fonte: "motor interno", custoMcp: 0, at, degradado }`.
  · `tendencia`: `{ regime, direcao, forca, adx14, base, confiavel, sma9, sma21, sma50, sma200 }`
  · `volatilidade`: `{ hv21Pct, hv63Pct, atr14Pct, unidade: "pct", motivo? }`
    ATENCAO A UNIDADE: aqui e PERCENTUAL. O `behavior.hv21` do servico MCP vem
    em FRACAO e a tela ja o converte com `fracPct`. Nao passe um pelo helper do
    outro — e como se erra 10x em silencio.
  · `niveis`: `{ supports, resistances, nearestSupport, nearestResistance,
    distanceToSupportPct, distanceToResistancePct }`
  · `regua`: `{ itens: [{ data, regime, direcao, forca, confiavel, hoje }], motivo }`
  · `carimbo`: `{ snapshotId, asOf, generatedAt, source, cacheStatus }`
  · Regimes possiveis (fonte: `server/app/regime.py`): `tendencia_alta`,
    `tendencia_baixa`, `lateral`, `indefinido`.

De `web/src/opcoes/OpcoesScreen.jsx` (apos o 27-02):
- Helpers ja existentes: `ehNum`, `fmt`, `pct`, `fracPct`, `txt`, `faixa`,
  `Linha`, `Kicker`, `Aviso`, `RazaoGanhoPerda`, `LacunasDaLeitura`.
- Bloco `VARKEY`/`TOKENS`/`T` local (sem import de `App.jsx`). Os tokens de
  cor da marca que existem na paleta e podem entrar em `TOKENS`: `positive`,
  `positiveTint`, `positiveTint10`, `negative`, `accent`, `accentSoft`,
  `accentTint10`, `bgCard`, `textMuted`, `textFaint`, `borderSubtle`,
  `borderFaint`. **Nenhum hex literal, nenhuma cor nova** — `test_brand_book_v2_tokens.mjs`.
- Os controles que chamam o servico MCP e o custo de cada um sao materia do
  **27-05**; este plano nao mexe em nenhum deles. O unico rotulo de custo que
  ele cria e o de GRATUIDADE (`cp.opcoesSemCusto`), derivado de `custoMcp: 0`.

De `web/src/copy.js`: `COPY.estudo` e `COPY.operador` com conjunto de chaves
IDENTICO (`web/tests/test_copy_theme.mjs:27`). `opcoesCustoChamadas(n)` ja
existe e e a forma canonica de declarar custo.

Guardioes que ja olham este codigo: `test_opcoes_mcp_aba_ui.mjs` (ordem dos
quatro estados, sem import de `App.jsx`, bloco de tokens local em todo
componente que desenha), `test_brand_book_v2_tokens.mjs`,
`test_opcoes_analisar_ui.mjs`, `test_opcoes_criar_setup_ui.mjs`,
`test_vocabulario_opcoes.mjs`, e os dois criados no 27-02.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bloco de leitura interna do ativo, com carimbo e rotulo de gratuidade</name>

  <read_first>
    - `web/src/opcoes/useOpcoesMcp.js` — os dois `useEffect` (status e leitura) e o `return` do hook, ja com o que o 27-02 acrescentou.
    - `web/src/opcoes/OpcoesScreen.jsx`, bloco "LEITURA DO ATIVO" (`cp.opcoesLeituraTitulo`) — o formato de `Linha` que a leitura do servico usa; o bloco interno e irmao dele, nao substituto.
    - `.planning/phases/27-aba-opcoes-sobre-a-carteira/27-03-SUMMARY.md` — o formato exato devolvido por `/api/options/tecnico/{ticker}`.
    - `.planning/phases/27-aba-opcoes-sobre-a-carteira/27-CONTEXT.md`, D1 e D4.
  </read_first>

  <files>web/src/opcoes/useOpcoesMcp.js, web/src/opcoes/unidades.js, web/src/opcoes/OpcoesScreen.jsx, web/src/copy.js, web/tests/test_opcoes_analisar_ui.mjs</files>

  <action>
    **(a) Hook.** Acrescentar o trio `tecnico`, carregado por `useEffect` na
    troca de `ticker` (mesmo padrao e mesma disciplina de invalidacao do efeito
    de `leitura`: `tickerRef` + contador proprio `tecnicoRef`, para que a
    resposta de PETR4 nunca pinte a tela de VALE3). Guarda de metodo ausente
    (`typeof store.opcoesTecnico !== "function"` -> `VAZIO`).
    Comentario obrigatorio explicando por que este efeito PODE disparar sozinho
    quando quase nenhum outro pode: a rota e interna e declara `custoMcp: 0`
    (27-03) — o que o paragrafo 3.3 do ADR-027 proibe e gastar cota do servico
    sem clique, e aqui nao ha cota nenhuma a gastar. Acrescentar `tecnico` ao
    `return` do hook.

    **PERIODO FIXO (correcao C7).** A chamada e
    `store.opcoesTecnico(t, { period: PERIODO_DA_LEITURA })`, com
    `PERIODO_DA_LEITURA = "1y"` declarado como constante nomeada no topo de
    `useOpcoesMcp.js`, com comentario dizendo as duas coisas que o numero
    resolve: (1) a regua precisa de pelo menos sete velas FECHADAS na cauda —
    com um periodo curto ela viria truncada e o `motivo` apareceria toda vez,
    sem defeito nenhum; (2) o periodo escolhe a CAUDA, nao o historico: o
    warmup das medias longas e fixo em `2y` no backend
    (`server/app/candles.py:30`, `FETCH_RANGE`), entao a confiabilidade da
    SMA200 nao depende deste valor — o que ele nao pode e variar por chamada,
    porque dois periodos diferentes na mesma tela produziriam duas leituras do
    mesmo dia. Nao passar `period` e a alternativa pior: o default fica
    implicito no backend e some do fonte que a tela le.

    **(a-bis) Guardiao que esta alinea quebra.** `test_opcoes_analisar_ui.mjs`,
    secao 8, foi reescrita no 27-02 com a allowlist `EFEITO_PERMITIDO`. O efeito
    de `tecnico` e um `store.*` novo dentro de `useEffect`: acrescente
    `"opcoesTecnico"` a allowlist, com nota datada `2026-09-13 (Fase 27)` e a
    justificativa NOMEADA — `custoMcp: 0` no contrato da rota, provado no
    backend por bomba no `call_tool` (27-03). Sem esta linha o guardiao reprova
    a suite; com ela, ele passa a provar que a lista de "pode rodar sozinho" e
    curta, explicita e justificada, em vez de crescer por acidente.

    **(b) Bloco na tela.** Renderizar ANTES do bloco "LEITURA DO ATIVO" do
    servico (a leitura de graca vem primeiro; a paga so quando a pessoa clica) e
    DEPOIS do lastro do 27-02. Conteudo, com os helpers existentes:
    · `Kicker` com `cp.opcoesInternaTitulo` (chave nova).
    · `Linha` de tendencia: o `regime` traduzido por uma tabela de rotulos em
      `copy.js` (`opcoesRegimeRotulo`, objeto com as quatro chaves de
      `regime.REGIMES`), mais `forca` e `base`. Quando `confiavel` e `false`,
      uma linha de ressalva (`cp.opcoesRegimeNaoConfiavel`) dizendo que a
      janela de 200 pregoes nao fechou e a leitura caiu na media de 50 — e
      ressalva, nao erro; nao esconda o valor.
    · `Linha` de HV 21 e HV 63 pelo formatador da alinea (c) — **nunca** com
      `pct`/`fracPct` escritos a mao. Comentario obrigatorio registrando a
      diferenca de unidade entre esta fonte e a do servico, e a armadilha de
      10x.
    · `Linha` de suporte e resistencia mais proximos, com a distancia em %.
    · Rodape com o carimbo (`cp.opcoesInternaCarimbo`, funcao de
      `(asOf, fonte)`): de qual pregao e a leitura e de onde veio a serie
      (`carimbo.source`) — principio 3 do CLAUDE.md. Quando `source` e nulo,
      travessao com motivo, nunca um nome de fonte por default (e o mesmo
      defeito que o cabecalho da aba ja corrigiu com "Fonte: —").
    · Um selo `cp.opcoesSemCusto` (chave nova, texto curto tipo "gratis — motor
      do proprio app") **derivado de `tecnico.dados.custoMcp === 0`**, nunca
      escrito fixo. Se o backend um dia passar a cobrar, o selo tem de sumir
      sozinho.
    · Estado vazio/erro proprio: enquanto carrega, nada de bloco vazio; com
      erro, `Aviso` com a mensagem e a frase de que nada foi estimado no lugar.
      Este bloco degradar **nao** pode derrubar a leitura do servico nem os
      vigias — sao fontes independentes.
    Todas as chaves novas nos DOIS blocos de `copy.js`, com voz por modo.

    **(c) A unidade deixa de ser anotacao e vira despachante (correcao C6).**
    Hoje `hv21Pct` (interno, PERCENTUAL) e `behavior.hv21` (servico, FRACAO,
    convertido por `fracPct` em `OpcoesScreen.jsx:39`) ficam na MESMA tela. O
    27-03 declara `volatilidade.unidade: "pct"` no contrato, mas ninguem
    consome esse campo — e um campo que ninguem consome nao impede erro nenhum.
    Criar `web/src/opcoes/unidades.js`, modulo PURO (zero JSX, zero import de
    `App.jsx`, zero `Math.sqrt/log/pow/exp` — a regex `RECALCULO` de
    `test_opcoes_analisar_ui.mjs:394` varre TODO arquivo de `web/src/opcoes/`),
    exportando:
    · `UNIDADES = { pct: "pct", frac: "frac" }`;
    · `formatarVolatilidade(valor, unidade, casas = 1) -> string`, que ESCOLHE
      o formatador pela `unidade` recebida: `"pct"` formata o numero como ja
      esta; `"frac"` multiplica por 100; **qualquer outra coisa** (`undefined`,
      `null`, `"percent"`, string vazia) devolve travessao com motivo — nunca
      um palpite. Este ultimo ramo e o ponto: adivinhar a unidade e exatamente
      o erro de 10x que se quer impossivel, e "nao sei em que unidade isto
      esta" e informacao legitima (principio 4).
    O modulo e importavel por `node` (e `.js`, nao `.jsx`), e por isso o
    guardiao consegue exercitar a FUNCAO, nao so o fonte — que e a diferenca
    entre um teste permanente e um `grep` de uma vez so.
    `OpcoesScreen.jsx` passa a chamar
    `formatarVolatilidade(v.hv21Pct, v.unidade)` para o bloco interno e
    **continua** usando `fracPct(behavior.hv21)` para o bloco do servico (aquele
    contrato nao declara unidade; nao invente um campo no que o servico manda).
  </action>

  <verify>
    <automated>cd /Users/acamerini/dev/borisv2 && node web/tests/test_copy_theme.mjs && node web/tests/test_opcoes_analisar_ui.mjs && node web/tests/test_opcoes_mcp_aba_ui.mjs && node web/tests/test_brand_book_v2_tokens.mjs && cd web && npx vite build</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "store.opcoesTecnico" web/src/opcoes/useOpcoesMcp.js` devolve 1.
    - `grep -n "PERIODO_DA_LEITURA" web/src/opcoes/useOpcoesMcp.js` casa a declaracao e o uso (periodo FIXO, nao implicito).
    - `grep -n "custoMcp" web/src/opcoes/OpcoesScreen.jsx` casa ao menos uma linha (o selo e DERIVADO, nao escrito fixo).
    - `node -e "import('./web/src/copy.js').then(m=>{const k=['opcoesInternaTitulo','opcoesInternaCarimbo','opcoesSemCusto','opcoesRegimeRotulo','opcoesRegimeNaoConfiavel'];for(const b of ['estudo','operador'])for(const x of k)if(!(x in m.COPY[b]))throw new Error(b+'.'+x)})"` sai 0.
    - `grep -nE "fracPct\(.*hv(21|63)Pct" web/src/opcoes/OpcoesScreen.jsx` nao casa nada (unidade errada).
    - `node -e "import('./web/src/opcoes/unidades.js').then(m=>{const a=m.formatarVolatilidade(31,'pct'),b=m.formatarVolatilidade(0.31,'frac'),c=m.formatarVolatilidade(31,undefined);if(a!==b)throw new Error(a+' != '+b);if(/\d/.test(c))throw new Error('unidade desconhecida virou numero: '+c)})"` sai 0.
    - `node web/tests/test_opcoes_analisar_ui.mjs` sai 0 e `grep -n "opcoesTecnico" web/tests/test_opcoes_analisar_ui.mjs` casa a entrada na allowlist `EFEITO_PERMITIDO`.
    - Os quatro `.mjs` do `<verify>` saem 0; `npx vite build` verde.
    - **Nao-vacuidade (C9 — tripla, esta task nao tinha nenhuma):**
      (1) **unidade**: trocar no fonte a chamada por `formatarVolatilidade(v.hv21Pct, "frac")` faz o guardiao da Task 2 FALHAR (a assercao "nenhum campo de `tecnico.dados` passa por conversao de fracao"), e o valor na tela cai de 31,0% para 0,3% — rode, veja os dois, reverta;
      (2) **selo escrito fixo**: trocar o selo derivado por um texto constante faz a assercao de `custoMcp` FALHAR;
      (3) **efeito de custo**: trocar `store.opcoesTecnico` por `store.mcpLeitura` dentro do efeito faz `test_opcoes_analisar_ui.mjs` FALHAR pela allowlist. Rode as tres injecoes, confirme, reverta.
  </acceptance_criteria>

  <done>Escolher um ativo mostra tendencia, volatilidade e niveis na hora, com o carimbo de onde vieram e o selo de que nao custaram nada.</done>
</task>

<task type="auto">
  <name>Task 2: ReguaRegime.jsx — sete pregoes, hoje destacado</name>

  <read_first>
    - `web/src/opcoes/SetupChart.jsx` — o componente mais parecido em tamanho e responsabilidade; dele sai o padrao de bloco `VARKEY`/`TOKENS`/`T` local que `test_opcoes_mcp_aba_ui.mjs` exige de TODO componente que desenha.
    - `web/src/App.jsx:69-108` (as duas paletas) — os nomes de token de cor que existem (`positive`, `negative`, `accent`, `positiveTint`, `textMuted`); nenhum hex literal.
    - `.planning/phases/27-aba-opcoes-sobre-a-carteira/27-CONTEXT.md`, D4 (regua de regime: 7 segmentos, um por pregao, cor do estado medido, hoje destacado).
    - `server/app/regime.py:60-80` — os quatro valores possiveis de `regime`.
  </read_first>

  <files>web/src/opcoes/ReguaRegime.jsx, web/src/opcoes/OpcoesScreen.jsx, web/src/copy.js, web/tests/test_opcoes_leitura_interna_ui.mjs</files>

  <action>
    Criar `web/src/opcoes/ReguaRegime.jsx`: componente puro
    `ReguaRegime({ regua, cp })`, com bloco `VARKEY`/`TOKENS`/`T` proprio e
    ZERO import de `App.jsx`.

    Desenho: uma faixa horizontal de ate 7 segmentos de largura igual, o mais
    antigo a esquerda. Cor por regime, mapeada por uma tabela nomeada
    (`CORES_DO_REGIME`) sobre os tokens EXISTENTES: `tendencia_alta` ->
    `positive`, `tendencia_baixa` -> `negative`, `lateral` -> `textMuted`,
    `indefinido` -> `borderSubtle`. Nenhum hex, nenhuma cor nova.
    Segmento com `confiavel: false` recebe opacidade reduzida **e** o motivo no
    `title`/`aria-label` — a diferenca entre "medido com a janela cheia" e
    "medido com a janela curta" nao pode ficar so na cor, porque cor sozinha
    nao e informacao acessivel.
    O segmento com `hoje: true` recebe destaque de borda (token `accent`), e o
    rotulo abaixo da faixa nomeia o dia (`item.data`).
    Acessibilidade: a faixa e uma lista com `role="list"`, cada segmento com
    `role="listitem"` e um `aria-label` completo ("29/08: tendencia de alta,
    forca forte"); a informacao inteira precisa existir em TEXTO, nao so em
    cor (principio 10 do CLAUDE.md).
    `regua.motivo` presente (serie curta) vira uma linha de rodape abaixo da
    faixa, com o motivo verbatim — a faixa mostra menos segmentos e DIZ por que,
    nunca preenche os que faltam.
    `regua` ausente ou `itens` vazio: o componente devolve `null` e o bloco pai
    mostra o motivo — regua vazia desenhada seria lida como "a semana inteira
    indefinida", que e afirmacao diferente de "nao ha dado".

    Renderizar `<ReguaRegime regua={tecnico.dados && tecnico.dados.regua} cp={cp} />`
    dentro do bloco da Task 1, logo abaixo da linha de tendencia — e ali que a
    pergunta "mudou esta semana?" nasce.
    Chaves novas em `copy.js` (DOIS blocos): `opcoesReguaTitulo`,
    `opcoesReguaAjuda` (explica que cada segmento e um pregao fechado, que a cor
    e o estado MEDIDO naquele dia e que nada ali e previsao) e
    `opcoesReguaSemDados`.

    Criar `web/tests/test_opcoes_leitura_interna_ui.mjs` derivando da fonte:
    · `ReguaRegime.jsx` nao importa `App.jsx` e tem o bloco de tokens local;
    · `CORES_DO_REGIME` cobre EXATAMENTE os quatro regimes — derive a lista
      lendo `server/app/regime.py` e extraindo a tupla `REGIMES`, nao a
      redigite (e o padrao de guardiao que a Fase 26 estabeleceu: derivar da
      fonte, nunca repetir o texto que se trava);
    · o fonte do componente nao contem hex literal (`/#[0-9a-fA-F]{3,8}\b/`);
    · todo segmento tem `aria-label` (a informacao existe em texto);
    · o bloco interno da tela renderiza `ReguaRegime` e le `tecnico.dados`;
    · as chaves novas existem nos dois blocos de `COPY`;
    · **(C6) unidade** — nenhum campo de `tecnico.dados` passa por `fracPct`.
      Derive do fonte de `OpcoesScreen.jsx` (comentarios removidos) TODA
      ocorrencia de `fracPct(` e exija que o argumento NAO comece por `tecnico`
      nem contenha `hv21Pct`/`hv63Pct`/`atr14Pct`; e, pelo lado positivo (sem o
      qual a assercao passa por vacuidade), que exista ao menos uma chamada a
      `formatarVolatilidade(` cujo segundo argumento seja lido do proprio dado
      (`.unidade`), e nao uma string literal. Chame `formatarVolatilidade` de
      verdade no teste (o modulo e `.js`, importavel): `(31,"pct")` e
      `(0.31,"frac")` dao o MESMO texto; `(31,"frac")` **nao** da esse texto; e
      unidade desconhecida devolve travessao sem digito nenhum;
    · **(C7) concordancia** — o ultimo segmento da regua e a linha de tendencia
      leem o MESMO objeto: exija que o fonte passe `tecnico.dados.regua` ao
      `ReguaRegime` e `tecnico.dados.tendencia` a linha de tendencia, e que a
      tela **nao** derive regime por conta propria em lugar nenhum (nenhuma
      comparacao de `close` com `sma` dentro de `web/src/opcoes/`). O oraculo
      de valor dessa concordancia e do backend
      (`test_opcoes_tecnico.py`, 27-03: ultimo item da regua == `classificar(snap)`);
      o que se tranca aqui e que a tela nao introduza uma terceira regua.
    Cabecalho do arquivo nomeando o defeito que cada assercao reprova.
  </action>

  <verify>
    <automated>cd /Users/acamerini/dev/borisv2 && node web/tests/test_opcoes_leitura_interna_ui.mjs && node web/tests/test_opcoes_mcp_aba_ui.mjs && node web/tests/test_brand_book_v2_tokens.mjs && node web/tests/test_copy_theme.mjs && node web/tests/test_acessibilidade_acordeao.mjs && cd web && npx vite build</automated>
  </verify>

  <acceptance_criteria>
    - `web/src/opcoes/ReguaRegime.jsx` existe com ao menos 40 linhas e exporta um componente default.
    - `grep -cE "#[0-9a-fA-F]{3,8}" web/src/opcoes/ReguaRegime.jsx` devolve 0.
    - `node web/tests/test_opcoes_leitura_interna_ui.mjs` sai 0 com ao menos 12 assercoes.
    - `grep -n "formatarVolatilidade" web/src/opcoes/OpcoesScreen.jsx` casa ao menos uma linha, e o segundo argumento e lido do dado (`.unidade`), nao uma string literal.
    - `npx vite build` verde.
    - **Nao-vacuidade (dupla)**: (1) acrescentar um quinto regime a `server/app/regime.py::REGIMES` sem tocar em `CORES_DO_REGIME` faz `test_opcoes_leitura_interna_ui.mjs` FALHAR (e a prova de que o guardiao DERIVA da fonte em vez de repetir a lista); (2) trocar `formatarVolatilidade(v.hv21Pct, v.unidade)` por `fracPct(v.hv21Pct)` faz o mesmo teste FALHAR na secao de unidade. Rode as duas injecoes, confirme, reverta.
  </acceptance_criteria>

  <done>A semana aparece como sete estados medidos, com hoje destacado, e a informacao existe em texto para quem nao le cor.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: leitura interna e regua de regime, no aparelho</name>
  <what-built>
    A leitura tecnica interna na tela, com carimbo e selo de gratuidade, e a
    regua de sete pregoes. Os guardioes travam estrutura, cor, unidade e a
    concordancia entre a regua e a linha de tendencia — mas **nenhum deles ve a
    tela**, e dois riscos so aparecem no aparelho: a regua em 375 px (sete
    segmentos numa faixa estreita) e a unidade da volatilidade (percentual do
    motor interno x fracao do servico, lado a lado no mesmo ecra).
    `ui_safety_gate` esta ligado nesta fase. O custo declarado em cada controle
    e o 27-05 — nao o cobre aqui.
  </what-built>
  <action>PAUSE. Nao execute nada: apresente o roteiro de verificacao abaixo ao
  desenvolvedor e aguarde o sinal de retomada. Registre a resposta dele no
  SUMMARY, verbatim.</action>
  <how-to-verify>
    1. `cd web && npm run dev`, aba Opcoes, com carteira que tenha posicao.
    2. Anote o `usado` da cota no cabecalho. Escolha um ativo. O bloco de
       leitura interna aparece com tendencia, HV 21, HV 63, suporte e
       resistencia — e o `usado` **nao muda**. Se mudar, a leitura interna
       esta gastando cota e o D1 nao foi cumprido.
    3. Confira a regua: sete segmentos (ou menos, com motivo), hoje destacado,
       e o dia de cada segmento legivel. Em 375 px nada estoura na horizontal.
    4. **Concordancia (C7)**: o ULTIMO segmento da regua e a linha de tendencia
       logo acima dizem a MESMA coisa — mesmo regime, e nenhum dos dois
       aparece com ressalva de confiabilidade enquanto o outro nao. Se o
       segmento de hoje vier "lateral / janela curta" e a linha disser
       "tendencia de alta, confiavel", PARE: e o defeito do periodo/janela, e
       ele engana em vez de informar.
    5. Compare a HV 21 do bloco interno com a do bloco do servico ("LEITURA DO
       ATIVO" — abra-o pelo botao, que custa 3). As duas precisam estar na
       MESMA ordem de grandeza (ex.: 31,0% e 31,2%). Se uma mostrar 31,0% e a
       outra 0,3%, a unidade foi trocada — PARE e corrija antes de aprovar.
    6. VoiceOver (ou leitor de tela do navegador) sobre a regua: cada segmento
       anuncia o dia e o estado. Cor sozinha nao basta.
    7. Troque para o Modo Operador e confira que os textos novos mudam de voz.
  </how-to-verify>
  <resume-signal>Responda "aprovado" ou descreva o que ficou errado, com o numero do passo.</resume-signal>
  <done>O desenvolvedor respondeu "aprovado" ou apontou o que corrigir, e a resposta esta registrada no SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tela -> cota do servico MCP | todo controle pode virar chamada paga; o teto de 2.000/dia e do SERVIDOR, compartilhado |
| numero exibido -> decisao da pessoa | volatilidade e niveis entram na decisao de montar estrutura; unidade errada e numero errado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-16 | Denial of Service | efeito de `tecnico` no hook | mitigate | so a rota interna (`custoMcp: 0`) dispara por efeito; guardiao do 27-02 continua reprovando chamada de custo dentro de `useEffect` |
| T-27-18 | Tampering | unidade da volatilidade | mitigate | o formatador e ESCOLHIDO pelo campo `unidade` do contrato (`unidades.js`), unidade desconhecida vira travessao; guardiao PERMANENTE exercita a funcao e reprova `fracPct` sobre campo de `tecnico.dados`; passo 5 do checkpoint compara os dois blocos na tela |
| T-27-21 | Repudiation | regua x linha de tendencia | mitigate | `n_i` derivado de `historyStats.candlesAvailable` (27-03, C7) + periodo FIXO na chamada + teste de backend que exige o ultimo item da regua identico a `classificar(snap)`; sem isso, dois vereditos contraditorios sobre o mesmo dia na mesma tela |
| T-27-19 | Information Disclosure | selo "gratis" | mitigate | derivado de `custoMcp === 0` vindo do backend, nunca escrito fixo — se a rota passar a cobrar, o selo some sozinho |
| T-27-20 | Repudiation | regua desenhada com dados parciais | mitigate | `motivo` verbatim no rodape e menos segmentos; componente devolve `null` com `itens` vazio, em vez de sete segmentos "indefinido" |
| T-27-SC | Tampering | instalacao de pacote | n/a | nenhum pacote novo nesta fase |
</threat_model>

<verification>
FORA do sandbox (`dangerouslyDisableSandbox: true` — dentro dele ~26 testes
falham falsamente por bloqueio do bundle de CAs):

`bash scripts/executar.sh --testes`

Baseline: **2691 passed, 5 skipped, 3 xfailed, 0 failed + 137/137 `.mjs`** mais
o delta acumulado de 27-01/02/03. Front editado -> `cd web && npx vite build`
verde antes de declarar pronto.

O ultimo plano da fase e o **27-05** (custo declarado em todo controle). A
publicacao (`scripts/bump.sh` + `scripts/publicar-web.sh`) e o deploy do backend
continuam sendo etapa HUMANA separada, depois dele — sem eles a fase fica
testada e nunca vai ao ar (licao registrada em
`fase-sem-plano-de-publicacao-front`).
</verification>

<success_criteria>
- Escolher um ativo mostra tendencia, volatilidade, suporte/resistencia e a regua de 7 pregoes sem mover o contador de cota (criterio 3 do ROADMAP).
- O selo de gratuidade e derivado de `custoMcp`, nunca escrito fixo.
- A regua usa so tokens do Brand Book v2 e carrega a informacao em texto, nao so em cor.
- O ultimo segmento da regua concorda com a linha de tendencia (mesmo regime, mesma confiabilidade).
- A volatilidade e formatada pela unidade DECLARADA no contrato; unidade desconhecida vira travessao, nunca palpite.
- Suite canonica sem regressao; `npx vite build` verde; checkpoint humano aprovado.
</success_criteria>

<output>
Create `.planning/phases/27-aba-opcoes-sobre-a-carteira/27-04-SUMMARY.md` when done
</output>
