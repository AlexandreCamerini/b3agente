---
phase: 31-varredura-oportunidades-opcoes
plan: 04
type: execute
wave: 3
depends_on: ["31-02", "31-03"]
files_modified:
  - web/src/copy.js
  - web/src/opcoes/estruturaParaPayoff.js
  - web/src/App.jsx
  - web/tests/test_curadoria_ui.mjs
  - web/tests/test_estrutura_para_payoff.mjs
autonomous: false
requirements: [SC-2, SC-3, SC-4, SC-5, SC-7]
must_haves:
  truths:
    - "O bloco de curadoria mostra o TIPO de cada estrutura, não assume mais que toda oportunidade é venda coberta"
    - "Cada item tem chave de render estável, inclusive collar (que não tem contractSymbol)"
    - "O bloco mostra a curva de payoff da estrutura nº 1, uma por vez, sem overlay"
    - "Nenhum aviso ou CTA sobre opção a descoberto aparece para conta sem o flag — o gate não é substituído por texto"
    - "A ordem exibida continua sendo a do motor: nenhum sort/reverse/comparação de razao no componente"
  artifacts:
    - path: "web/src/opcoes/estruturaParaPayoff.js"
      provides: "adaptador puro do envelope PT do motor para o envelope que PayoffChart consome"
      contains: "export function estruturaParaPayoff"
    - path: "web/src/App.jsx"
      provides: "CuradoriaEstruturas com rótulo de tipo, chave estável, resumo da varredura e payoff do nº 1"
      contains: "idCandidato"
    - path: "web/src/copy.js"
      provides: "copy da curadoria nos dois modos, sem assumir venda coberta como único universo"
      contains: "curadoriaTipo"
  key_links:
    - from: "web/src/App.jsx"
      to: "web/src/opcoes/PayoffChart.jsx"
      via: "render da curva do item nº 1 via adaptador"
      pattern: "PayoffChart"
    - from: "web/src/App.jsx"
      to: "server/app/main.py"
      via: "meta.candidatosPorTipo / tetoVencimentos de GET /api/options/curadoria"
      pattern: "candidatosPorTipo"
---

<objective>
Fechar a fase no cliente: o bloco de curadoria em Posições deixa de assumir
"venda coberta" como única estrutura possível, ganha rótulo de tipo, chave de
render estável, o resumo do que a varredura cobriu (4 estruturas × até 2
vencimentos) e a curva de payoff da estrutura nº 1 — uma por vez, sem overlay
(D-07/D-08).

Purpose: SC-2, SC-3, SC-4 e SC-5 ficam visíveis e verificáveis ao vivo. O
`top` continua cortado em 4 por `rankear`, e a consequência conhecida de D-06
(put/collar/descoberto rankeiam estruturalmente mal na fórmula única) faz o
top-4 poder ficar todo de venda coberta — é o resumo da varredura que mostra
que as 4 estruturas foram de fato varridas, sem criar seção separada por
objetivo (proibido por D-06).
Output: copy nos dois modos, adaptador puro PT→envelope do payoff,
`CuradoriaEstruturas` estendido, guardiões atualizados e checkpoint de
verificação ao vivo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-varredura-oportunidades-opcoes/31-CONTEXT.md
@.planning/phases/31-varredura-oportunidades-opcoes/31-PATTERNS.md
@.planning/phases/31-varredura-oportunidades-opcoes/31-02-SUMMARY.md
@.planning/phases/31-varredura-oportunidades-opcoes/31-03-SUMMARY.md
@CLAUDE.md
@.claude/skills/didatica-boris/SKILL.md

NÃO leia `.planning/STATE.md`. `web/src/App.jsx` tem ~9.300 linhas — NÃO leia
inteiro; use os intervalos de `<read_first>` e `grep -n`.

**Fences desta fase, não negociar:**
- D-05: conta sem `permitirOpcaoADescoberto` não vê oportunidade a descoberto
  **nem com aviso**. É proibido criar texto do tipo "ative o flag para ver
  mais oportunidades" — seria substituir o gate por marketing.
- D-06: uma fórmula, uma lista. Proibido criar seção "receita × proteção".
- D-08: sem overlay de múltiplos candidatos na mesma curva, sem
  toque/zoom/tooltip no payoff.
- Guardrail CVM: `item.manchete` renderizado VERBATIM; o front nunca compõe
  manchete a partir de strike/prêmio.

<interfaces>
Do Plano 02, `GET /api/options/curadoria` devolve (envelope de hoje + campos
novos):
- `top[]`: `tipo` (`call_coberta`|`put_protecao`|`collar`|
  `opcao_a_descoberto`), `idCandidato` (string estável, não-vazia),
  `ticker`, `contractSymbol` (null no collar), `strike`/`strikeCall`/
  `strikePut`, `expiration`, `diasParaVencimento`, `contratos`, `qtyAcoes`,
  `premioUnitario`/`premioTotal` (ASSINADOS: negativo = o usuário paga),
  `liquidez.faixa`, `razao`, `manchete`, `didatica`, `precoObjeto`,
  `estrutura` (envelope PT do motor), `posicaoNoRanking`.
- meta: `avaliadas`, `ignoradas`, `degradados`, `candidatosAvaliados`,
  `source`, `candidatosPorTipo` (4 chaves, sempre presentes),
  `vencimentosPorTicker`, `tetoVencimentos`.

`estrutura` (envelope PT de `opcoes_motor.avaliar`): `custo_liquido`,
`fluxo`, `ganho_maximo`, `perda_maxima` (null = ilimitado),
`ganho_ilimitado`, `perda_ilimitada`, `breakevens[]`, `delta_total`,
`curva[]` (`{preco_objeto, resultado}`), `pernas[]`, `unidade`.

`PayoffChart({ estrutura, emReais, cp, palette })` consome o envelope
INGLÊS: `name`, `legs[]`, `payoff[]` (`{underlying, result}`),
`breakevens[]`, `net_cost`, `max_gain`, `max_loss`, `unlimited_gain`,
`unlimited_loss`, `scenarios`. É por isso que o adaptador existe.

`web/src/App.jsx`: `CuradoriaEstruturas` (4179-4249), `useCuradoria`
(4498-4547), call site em `CarteiraScreen` (4667-4678); `ctx.palette` existe
(App.jsx:9277) mas hoje não chega a este bloco.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: copy dos dois modos — a lista não é mais só de venda coberta</name>
  <files>web/src/copy.js</files>
  <read_first>
    - web/src/copy.js:595-620 (bloco `curadoria*` do registro educacional/estudo)
    - web/src/copy.js:1098-1115 (bloco `curadoria*` do registro operador)
    - web/tests/test_curadoria_ui.mjs:1-60 (o guardião que conta as chaves de copy nos dois modos)
    - .claude/skills/didatica-boris/SKILL.md (registro por modo: estudo = professor, operador = mesa)
  </read_first>
  <behavior>
    - Toda chave `curadoria*` existe nos DOIS registros, como string literal.
    - Nenhuma frase promete lucro, garante resultado ou usa linguagem de enriquecimento (princípios 6 e 8 do CLAUDE.md).
    - Nenhuma frase menciona ligar o flag de opção a descoberto nem "liberar mais oportunidades" (D-05).
    - Nenhuma chave nova afirma algo sobre uma operação específica (guardrail CVM) — são rótulo e resumo.
  </behavior>
  <action>
Em `web/src/copy.js`, nos DOIS blocos `curadoria*`:

1. Reescreva as chaves que hoje afirmam "vendas cobertas", porque o universo
   mudou (D-04): `curadoriaTitulo` (hoje "AS 4 MELHORES VENDAS COBERTAS" →
   "AS 4 MELHORES OPORTUNIDADES DE OPÇÕES"), `curadoriaSubtitulo`
   (mantendo a afirmação de que a ordem é do motor e não muda com o texto da
   IA), `curadoriaCarregando` e `curadoriaVazio` (o motivo do vazio deixa de
   ser só "call líquida": passa a nomear "nenhuma estrutura elegível nos
   vencimentos varridos"). Preserve o registro de cada modo — estudo
   explica, operador fala curto como mesa.
2. Acrescente as chaves novas, string literal, nos dois registros:
   `curadoriaTipoCallCoberta` ("venda coberta"), `curadoriaTipoPutProtecao`
   ("put de proteção"), `curadoriaTipoCollar` ("trava protetora"),
   `curadoriaTipoDescoberto` ("a descoberto"), `curadoriaVarreduraRotulo`
   (rótulo da linha de resumo, ex.: "o que esta varredura cobriu") e
   `curadoriaPayoffRotulo` (ex.: "Curva de resultado da estrutura nº 1").
3. `curadoriaRazaoRotulo` continua existindo e continua sendo prêmio sobre
   perda máxima — a fórmula é uma só (D-06). Acrescente
   `curadoriaRazaoAjuda` nos dois registros: uma frase explicando que prêmio
   NEGATIVO significa que a estrutura custa dinheiro para montar (proteção),
   e que por isso ela aparece embaixo na mesma régua. É didática sobre o
   número que o motor calculou (princípio 5), não promoção de estrutura.
  </action>
  <verify>
    <automated>node web/tests/test_curadoria_ui.mjs</automated>
  </verify>
  <acceptance_criteria>
    - As chaves `curadoria*` têm exatamente o mesmo conjunto nos dois registros (o guardião da Task 3 passa a provar isso; nesta task, confira com `grep -c "curadoria" web/src/copy.js` e liste as chaves no SUMMARY).
    - `grep -in "vendas cobertas" web/src/copy.js` não retorna nenhuma linha dentro dos blocos `curadoria*`.
    - `grep -in "flag\|ative\|libere\|desbloquei" web/src/copy.js` não retorna nenhuma linha nos blocos `curadoria*` (D-05).
    - Nenhuma frase nova contém "garant", "lucro certo", "sempre ganha", "rentabilidade garantida".
  </acceptance_criteria>
  <done>Copy dos dois modos descreve um ranking de 4 estruturas, com rótulos de tipo e o resumo da varredura, sem prometer resultado e sem vender o flag da Fase 29.</done>
</task>

<task type="auto">
  <name>Task 2: adaptador puro + CuradoriaEstruturas com tipo, chave estável, resumo e payoff do nº 1</name>
  <files>web/src/opcoes/estruturaParaPayoff.js, web/src/App.jsx</files>
  <read_first>
    - web/src/App.jsx:4179-4249 (`CuradoriaEstruturas` inteiro — é o alvo)
    - web/src/App.jsx:4290-4315 (`PropostaDaPosicao`, ramo multi-candidato: o precedente de chave composta `key={c.tipo + "-" + (c.contractSymbol || "collar")}` para estrutura sem contrato único)
    - web/src/App.jsx:4660-4680 (call site em `CarteiraScreen`, onde entra a prop `palette`)
    - web/src/opcoes/PayoffChart.jsx:71-110 (quais campos do envelope inglês o componente lê de verdade)
    - server/app/opcoes_payoff.py:210-228 (chaves exatas do envelope PT que o adaptador recebe)
  </read_first>
  <behavior>
    - `estruturaParaPayoff(estruturaPt, "PETR4")` devolve `{name, legs, payoff, breakevens, net_cost, max_gain, max_loss, unlimited_gain, unlimited_loss}` com os MESMOS valores numéricos da entrada — nenhuma multiplicação, divisão, soma ou arredondamento.
    - Entrada `null`/sem `curva` → devolve `null` (o chamador simplesmente não renderiza o gráfico), nunca lança.
    - `perda_maxima: null` + `perda_ilimitada: true` chega como `max_loss: null` + `unlimited_loss: true` (o "null nunca 0.0" do repositório atravessa o adaptador).
    - Cada card do carrossel mostra o rótulo do tipo e a manchete verbatim; a chave de render é `item.idCandidato`.
    - Dois itens `collar` do mesmo ticker (ambos com `contractSymbol: null`) renderizam sem warning de chave duplicada.
    - O bloco mostra UMA curva (a do item `posicaoNoRanking === 1`); nenhum seletor, nenhum overlay, nenhum handler de toque.
    - Conta sem o flag: `meta.candidatosPorTipo.opcao_a_descoberto === 0` e a linha de resumo mostra 0 para esse tipo, sem nenhum texto convidando a ligar o flag.
    - A lista continua lendo `top`, nunca `narrativa.estruturas`, e não ordena nada.
  </behavior>
  <action>
**(a) `web/src/opcoes/estruturaParaPayoff.js` (arquivo novo, puro).**
Exporte `estruturaParaPayoff(estrutura, nome)` que faz APENAS renomeação de
chave do envelope PT para o inglês que `PayoffChart` já consome:
`curva[] → payoff[]` (`preco_objeto → underlying`, `resultado → result`),
`breakevens → breakevens`, `custo_liquido → net_cost`,
`ganho_maximo → max_gain`, `perda_maxima → max_loss`,
`ganho_ilimitado → unlimited_gain`, `perda_ilimitada → unlimited_loss`,
`pernas → legs`, `nome → name`, `scenarios` ausente (o motor interno não
produz cenários; `PayoffChart` já trata a ausência). Zero aritmética: o
cabeçalho do arquivo deve dizer, no estilo dos módulos vizinhos, que
qualquer conta aqui criaria uma segunda versão de um número que o backend já
calculou (princípio 5 do CLAUDE.md) e que o guardião da Task 3 proíbe
operador aritmético sobre esses campos. Entrada inválida (`null`, sem
`curva` array, `curva` com menos de 2 pontos) → `return null`.

**(b) `CuradoriaEstruturas` em `web/src/App.jsx`.** Quatro mudanças, sem
tocar na ordem nem nos números:

1. **Chave estável**: `key={item.contractSymbol}` → `key={item.idCandidato}`
   (com fallback `|| item.contractSymbol` só por segurança de payload
   antigo). Comente citando o precedente do `PropostaDaPosicao`
   (App.jsx:4303) e o motivo: collar não tem `contractSymbol` (duas pernas),
   e `null` como chave de lista colide.
2. **Rótulo de tipo**: acima da manchete, um chip pequeno com o rótulo do
   tipo, resolvido por um mapa local `tipo → chave de copy`
   (`call_coberta` → `cp.curadoriaTipoCallCoberta`, etc.), com fallback para
   string vazia em tipo desconhecido. O chip é rótulo de categoria — não
   encosta na manchete, que continua verbatim. Acrescente também
   `aria-label` do botão citando o tipo, junto com a posição e o ticker.
3. **Linha de resumo da varredura**: abaixo do carrossel (ou logo abaixo do
   subtítulo, escolha de layout é discricionária), uma linha só de texto,
   montada de `meta.candidatosPorTipo` e `meta.tetoVencimentos`, no formato
   "<`cp.curadoriaVarreduraRotulo`>: N venda coberta · N put de proteção · N
   trava protetora · N a descoberto · até N vencimentos por posição". É a
   evidência visível de SC-2/SC-3 quando o top-4 fica todo de um tipo só.
   Renderize só quando `meta` existir; nunca invente número quando o campo
   faltar (mostre "—" por chave ausente, nunca 0 inventado — princípio 4).
   **Proibido** transformar essa linha numa segunda lista/seção por objetivo
   (D-06) ou em CTA.
4. **Payoff do nº 1**: abaixo da lista e ANTES do bloco de narração,
   renderize `<PayoffChart estrutura={estruturaParaPayoff(top[0].estrutura,
   top[0].ticker)} emReais={null} cp={cp} palette={palette} />` precedido do
   rótulo `cp.curadoriaPayoffRotulo`, e só quando o adaptador devolver algo
   não-nulo. `emReais={null}` é deliberado: o backend desta rota não manda o
   bloco em reais, e multiplicar no front criaria a segunda versão da conta
   que o repositório proíbe. Acrescente `import PayoffChart from
   "./opcoes/PayoffChart.jsx";` e o import do adaptador no topo de
   `App.jsx`, junto dos imports de `./opcoes/*` que já existem (linhas
   12-13). Passe `palette={ctx.palette}` a partir do call site em
   `CarteiraScreen` (4667) e acrescente `palette` à lista de props do
   componente.

Nada de `useState` novo no componente, nada de seletor de estrutura: uma
curva, a do nº 1 (D-07/D-08).
  </action>
  <verify>
    <automated>cd web && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vite build` verde (regra do CLAUDE.md: front editado exige build antes de declarar ok).
    - `grep -n "key={item.idCandidato" web/src/App.jsx` casa; `grep -n "key={item.contractSymbol}" web/src/App.jsx` não casa mais no bloco da curadoria.
    - `grep -n "estruturaParaPayoff" web/src/App.jsx` mostra import + uso único (um gráfico, não um por item).
    - `grep -c "PayoffChart" web/src/App.jsx` == 2 (import + render único).
    - Dentro da fatia de `CuradoriaEstruturas`: zero ocorrência de `.sort(`, `.reverse(`, `razao >`/`razao <` (a ordem é do motor).
    - `grep -n "useState" web/src/App.jsx` não mostra nenhuma ocorrência nova dentro da fatia de `CuradoriaEstruturas`.
  </acceptance_criteria>
  <done>O bloco mostra os 4 tipos com chave estável, o resumo da varredura e uma curva de payoff — sem overlay, sem interatividade, sem reordenar nada.</done>
</task>

<task type="auto">
  <name>Task 3: guardiões estáticos atualizados + adaptador testado</name>
  <files>web/tests/test_curadoria_ui.mjs, web/tests/test_estrutura_para_payoff.mjs</files>
  <read_first>
    - web/tests/test_curadoria_ui.mjs (arquivo inteiro, 193 linhas — as 8 regras que ele já tranca)
    - web/src/App.jsx:4179-4260 (o componente já modificado na Task 2)
    - web/src/opcoes/estruturaParaPayoff.js (o adaptador da Task 2)
  </read_first>
  <behavior>
    - `test_curadoria_ui.mjs` continua trancando as 8 regras da Fase 30 (manchete verbatim, sem sort, narrar só por toque, lê `top`, bloco único, best-effort, sem promessa de lucro) — nenhuma é apagada.
    - Regras novas trancadas: paridade das chaves `curadoria*` nos dois modos com a contagem NOVA; chave de render é `idCandidato`; existe rótulo de tipo para os 4 tipos; a linha de resumo lê `meta.candidatosPorTipo`; existe exatamente UM `PayoffChart` renderizado no bloco; nenhuma copy da curadoria menciona ligar o flag (D-05).
    - `test_estrutura_para_payoff.mjs` prova a identidade numérica do adaptador (entrada == saída, campo a campo), o `null` seguro e a travessia de `null` em `max_loss` com `unlimited_loss: true`.
    - Os dois arquivos saem != 0 quando qualquer regra cai.
  </behavior>
  <action>
1. **`web/tests/test_curadoria_ui.mjs`** — estenda, não reescreva. Atualize a
   asserção de contagem de chaves de copy (hoje "as 11 chaves") para o
   número novo, com NOTA no comentário nomeando a Fase 31 e D-04 como a
   mudança deliberada (guardião não se apaga; reversão deliberada atualiza o
   guardião com nota). Acrescente as asserções novas do `<behavior>` no
   mesmo estilo (`ok(nome, cond)` sobre a fatia
   `app.slice(iCuradoria, iPDP)`), sempre depois de filtrar linhas de
   comentário para qualquer contagem.
2. **`web/tests/test_estrutura_para_payoff.mjs`** (novo) — importa o
   adaptador de verdade (`import { estruturaParaPayoff } from
   "../src/opcoes/estruturaParaPayoff.js"`), monta um envelope PT de
   exemplo com valores "feios" (ex.: `custo_liquido: 27.37`,
   `breakevens: [28.9]`, curva de 3 pontos) e compara campo a campo com
   igualdade ESTRITA — nenhuma tolerância de ponto flutuante, porque
   tolerância aqui esconderia exatamente a conta que o adaptador não pode
   fazer. Mais: entrada `null` → `null`; `curva` com 1 ponto → `null`;
   `perda_maxima: null` + `perda_ilimitada: true` → `max_loss: null` +
   `unlimited_loss: true`; e uma asserção estática sobre o FONTE do
   adaptador proibindo operador aritmético sobre os campos financeiros
   (regex sobre `readFileSync` do próprio arquivo, mesma técnica do guardião
   de `PayoffChart`).
3. Confirme que os dois `.mjs` são descobertos por
   `bash scripts/executar.sh --testes` (o arquivo novo precisa aparecer na
   contagem).
  </action>
  <verify>
    <automated>node web/tests/test_curadoria_ui.mjs && node web/tests/test_estrutura_para_payoff.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Os dois comandos saem 0; `bash scripts/executar.sh --testes` exit 0 com a contagem de `.mjs` maior que a baseline (números no SUMMARY).
    - Prova negativa real (duas): (i) volte `key={item.contractSymbol}` e confirme que o guardião cai; (ii) faça o adaptador multiplicar `net_cost` por 100 e confirme que `test_estrutura_para_payoff.mjs` cai. Reverta as duas com `git diff` limpo; comandos e saídas no SUMMARY.
    - Nenhuma das 8 regras originais do `test_curadoria_ui.mjs` foi removida (`grep -c "^ok(\|  ok(" web/tests/test_curadoria_ui.mjs` maior que antes; liste antes/depois).
  </acceptance_criteria>
  <done>As regras novas do bloco e a pureza do adaptador são testes que quebram; nenhuma regra da Fase 30 foi perdida no caminho.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: verificação ao vivo em 375px, com o flag desligado e ligado</name>
  <files>(nenhum — checkpoint de verificação, não de edição)</files>
  <read_first>
    - .planning/phases/31-varredura-oportunidades-opcoes/31-CONTEXT.md (D-05 e D-07: o que o Alex pediu para ver)
  </read_first>
  <action>
Não edite código nesta task. Antes de chamar o Alex, deixe o ambiente pronto
e traga evidência sua: confirme `bash scripts/executar.sh --testes` exit 0 e
`npx vite build` verde, garanta que o servidor local está no ar servindo o
bundle recém-construído e que existe uma conta de teste com pelo menos 2
posições de 200+ ações. Então apresente o roteiro de `<how-to-verify>` e
PARE — não declare a fase aprovada por conta própria e não aceite relato de
terceiro: só a resposta literal do Alex nesta conversa fecha este checkpoint
(mesma regra que fechou o checkpoint da Fase 29).
  </action>
  <what-built>
    Varredura de até 2 vencimentos por posição × 4 estruturas (venda coberta,
    put de proteção, collar, opção a descoberto), ranking único por prêmio
    líquido ÷ perda máxima, gate de descoberta do `permitirOpcaoADescoberto`,
    bloco de Posições com rótulo de tipo + resumo da varredura + curva de
    payoff do nº 1, e `PayoffChart` legível em 375px.
  </what-built>
  <how-to-verify>
    Servidor local já rodando (`bash scripts/executar.sh` ou o processo que
    você já usa). Navegador de desktop com o DevTools em 375px de largura
    (iPhone SE) — o app nativo do iPhone NÃO reflete esta fase sem `cap sync`
    + build novo no Xcode; isso é pendência operacional conhecida, não
    defeito (mesmo achado do checkpoint da Fase 29).

    1. Abra a aba **Posições** com pelo menos 2 posições de 200+ ações.
       Confirme que o bloco de curadoria aparece com título novo (não "AS 4
       MELHORES VENDAS COBERTAS").
    2. Cada card mostra um **rótulo de tipo** e a manchete do motor logo
       abaixo, sem texto truncado.
    3. A **linha de resumo** aparece com as 4 contagens e o teto de
       vencimentos. Confirme que o número de vencimentos citado é 2.
    4. A **curva de payoff** do item nº 1 aparece abaixo da lista: eixos e
       rótulos legíveis em 375px, sem texto cortado na borda, sem rótulos
       sobrepostos. Compare com a curva da aba Opções (mesma largura) — as
       duas devem estar legíveis.
    5. **Com `permitirOpcaoADescoberto` DESLIGADO** (Perfil → Preferências):
       nenhum card "a descoberto" na lista, e a contagem "a descoberto" no
       resumo é 0. Confirme que NÃO existe nenhum convite/CTA para ligar o
       flag (isso seria violação de D-05).
    6. **Ligue o flag** (aceitando o termo da Fase 29) e recarregue Posições:
       a contagem "a descoberto" no resumo pode passar de 0. Nenhum outro
       comportamento muda.
    7. Toque em "Pedir explicação da IA": o texto descreve as estruturas na
       ordem mostrada e não chama prêmio negativo de receita.
    8. Desligue o flag de novo e confirme que a Fase 29 segue intacta: você
       ainda consegue FECHAR uma posição de opção já aberta (o flag nunca
       bloqueia saída).

    Relate o que divergir, com print se possível.
  </how-to-verify>
  <verify>
    <human-check>Alex confirma na conversa os 8 passos acima, em 375px, com o flag desligado e ligado.</human-check>
  </verify>
  <acceptance_criteria>
    - Resposta literal do Alex nesta conversa ("aprovado" ou a divergência descrita), registrada no SUMMARY.
    - Nenhum passo do roteiro é declarado verde por inferência do executor.
  </acceptance_criteria>
  <resume-signal>
    Responda "aprovado" (ou descreva o que divergiu). Se aprovado, diga
    também se quer PUBLICAR agora: esta fase toca `web/src/`, então ir ao ar
    exige `scripts/bump.sh` e depois `scripts/publicar-web.sh` — passo humano
    separado, nunca automático, e nunca editando `server/web_dist` direto
    (guardrail do CLAUDE.md).
  </resume-signal>
  <done>Checkpoint fechado com a resposta literal do Alex, e a decisão de publicar (agora ou depois) registrada no SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → UI | `top`/`meta` chegam do servidor e são renderizados no cliente |
| UI → usuário | o que aparece na tela é a afirmação do produto sobre risco e oportunidade |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-11 | Tampering | `CuradoriaEstruturas` | mitigate | guardião proíbe `.sort(`/`.reverse(`/comparação de `razao` no componente — a ordem exibida é a do motor (princípio 5) |
| T-31-12 | Information Disclosure | resumo da varredura | mitigate | contagens vêm de `meta` do próprio escopo; oportunidade a descoberto com flag desligado tem contagem 0 e nenhum CTA (D-05), verificado no checkpoint |
| T-31-13 | Spoofing (dado inventado) | linha de resumo / payoff | mitigate | campo ausente vira "—", nunca 0; `emReais={null}` evita conta no front; adaptador proibido de fazer aritmética (guardião com regex) |
| T-31-14 | Repudiation | manchete do card | mitigate | `item.manchete` verbatim, guardião da Fase 30 preservado (guardrail CVM) |
| T-31-SC | Tampering | npm installs | mitigate | nenhuma dependência nova — `web/package.json` e lockfile intocados |
</threat_model>

<verification>
- `cd web && npx vite build` verde.
- `node web/tests/test_curadoria_ui.mjs` e `node web/tests/test_estrutura_para_payoff.mjs` saem 0.
- `bash scripts/executar.sh --testes` exit 0 (as DUAS suítes), sem regressão contra a baseline da fase.
- Checkpoint humano aprovado (Task 4).
</verification>

<success_criteria>
- Os 4 tipos de estrutura são distinguíveis na tela e a varredura é auditável pelo resumo (SC-2).
- Conta sem o flag não vê oportunidade a descoberto nem convite para ligá-lo (SC-3).
- Uma régua só, uma lista só (SC-4).
- Payoff legível em 375px, uma estrutura por vez (SC-5).
- Suíte canônica verde + `vite build` verde (SC-7).
- Publicação continua sendo passo humano separado (`bump.sh` → `publicar-web.sh`), nomeada no checkpoint.
</success_criteria>

<output>
Crie `.planning/phases/31-varredura-oportunidades-opcoes/31-04-SUMMARY.md`
com: chaves de copy antes/depois, as duas provas negativas, contagem de
`.mjs` antes/depois, resultado do `vite build`, o relato literal do
checkpoint humano e o estado de publicação (no ar ou pendente).
</output>
