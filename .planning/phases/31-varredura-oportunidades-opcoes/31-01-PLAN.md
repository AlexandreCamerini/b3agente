---
phase: 31-varredura-oportunidades-opcoes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/opcoes_curadoria.py
  - server/app/skill_ref.py
  - server/tests/test_opcoes_curadoria.py
autonomous: true
requirements: [SC-2, SC-3, SC-4]
must_haves:
  truths:
    - "candidatos_da_posicao gera candidatos das 4 estruturas do motor interno (call_coberta, put_protecao, collar, opcao_a_descoberto) a partir de UMA cadeia em memória"
    - "Candidato a descoberto só existe quando permitir_a_descoberto=True; com o flag desligado a lista sai sem nenhum item desse tipo, sem exceção e sem aviso"
    - "As 4 estruturas são ordenadas pela MESMA fórmula prêmio líquido ÷ perda máxima — sem fórmula por tipo, sem seção por objetivo"
    - "manchete/didatica de toda estrutura continuam vindo só de skill_ref.opcoes_lastreadas_txt — nenhuma frase composta neste módulo"
  artifacts:
    - path: "server/app/opcoes_curadoria.py"
      provides: "geração de candidatos das 4 estruturas + prêmio líquido com sinal + gate de descoberto + proximos_vencimentos"
      contains: "opcao_a_descoberto"
    - path: "server/app/skill_ref.py"
      provides: "frase canônica da opção a descoberto nos dois registros (operador/educacional)"
      contains: "opcao_a_descoberto"
    - path: "server/tests/test_opcoes_curadoria.py"
      provides: "guardiões das 4 estruturas, do sinal do prêmio e do gate de descoberto"
      contains: "def test_"
  key_links:
    - from: "server/app/opcoes_curadoria.py"
      to: "server/app/opcoes_motor.py"
      via: "rastrear/perna_de_contrato/perna_de_acao/avaliar"
      pattern: "opcoes_motor\\.(rastrear|avaliar|perna_de_contrato|perna_de_acao)"
    - from: "server/app/opcoes_curadoria.py"
      to: "server/app/skill_ref.py"
      via: "opcoes_lastreadas_txt para manchete/didatica de cada tipo"
      pattern: "skill_ref\\.opcoes_lastreadas_txt"
---

<objective>
Estender o motor PURO da curadoria (Fase 30) de "1 estrutura (venda coberta)"
para "as 4 estruturas do motor interno", mantendo UMA fórmula de ranking
(prêmio líquido ÷ perda máxima, D-06) e introduzindo o primeiro ponto do
sistema onde `permitirOpcaoADescoberto` é checado no momento da DESCOBERTA,
não da execução (D-05).

Purpose: SC-2, SC-3 e SC-4 da Fase 31. Este plano não toca rede, banco, rota
nem LLM — é aritmética determinística sobre uma cadeia já em memória, do
mesmo jeito que a Fase 30 deixou.
Output: `opcoes_curadoria.py` com os 4 ramos de estrutura, `premio_liquido_
unitario()`, `idCandidato`, `proximos_vencimentos()` e o parâmetro
`permitir_a_descoberto`; `skill_ref` com a frase canônica da opção a
descoberto; guardiões novos em `test_opcoes_curadoria.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-varredura-oportunidades-opcoes/31-CONTEXT.md
@.planning/phases/31-varredura-oportunidades-opcoes/31-PATTERNS.md
@CLAUDE.md

NÃO leia `.planning/STATE.md` (900+ linhas de narrativa histórica, custo alto
e sem valor para esta tarefa).

**Baseline da suíte**: meça UMA vez, antes da primeira edição, em `714101b`:
`bash scripts/executar.sh --testes` (as DUAS suítes — pytest + `web/tests/
*.mjs`). Registre os números no SUMMARY. A suíte não roda de novo até o fim
do plano.

`server/app/skill_ref.py` é grande — NÃO leia inteiro; use os intervalos de
`<read_first>` e `grep -n` para qualquer coisa fora deles.

<interfaces>
Já existente, e que este plano CONSOME sem alterar:

- `opcoes_motor.rastrear(cadeia, filtros) -> list[dict]` — filtros usados no
  repositório: `{"tipo": "call", "referencia": spot, "relacao": "acima",
  "criterio": "min", "n": N, "liquidez_minima": PISO}` e, para put,
  `{"tipo": "put", "referencia": spot, "relacao": "abaixo_ou_igual",
  "criterio": "max", "n": N, "liquidez_minima": PISO}`. Nunca levanta;
  devolve `[]` quando nada passa.
- `opcoes_motor.perna_de_contrato(contrato, lado, quantidade=1)` — `lado` é
  `"venda"` ou `"compra"`; levanta `ValueError` para `optionType` inválido ou
  `lastPrice` ausente/<=0.
- `opcoes_motor.perna_de_acao(ticker, preco, quantidade=1)`.
- `opcoes_motor.avaliar(pernas) -> dict` com as chaves PT:
  `custo_liquido` (>0 débito, <0 crédito), `fluxo`, `ganho_maximo`,
  `perda_maxima` (None quando o lado é ilimitado), `ganho_ilimitado`,
  `perda_ilimitada`, `breakevens`, `delta_total`, `curva`
  (`[{"preco_objeto","resultado"}]`), `pernas`, `unidade`.
- `skill_ref.opcoes_lastreadas_txt(modo, chave, **dados)` — interpola
  `{marcador}` por `str.replace`; **chave desconhecida cai silenciosamente na
  frase `sem_setup`** (skill_ref.py:624-636). É por isso que a Task 1 precisa
  criar a chave nova ANTES do ramo que a usa.
- `skill_ref.num_br(v)` — formatação PT-BR; todo número entra na frase já
  formatado por ele.
- `opcoes_lastreadas._bloco_liquidez(contrato, liq, modo)`,
  `_dias_ate(expiration, hoje)`, `_PRAZO_MIN_DIAS`(15)/`_PRAZO_MAX_DIAS`(60).
- `options_quant.liquidity_score(volume, openInterest, bid, ask)`,
  `LIQUIDEZ_NEGOCIAVEL`.
- `store.qty_livre(posicao)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: prêmio líquido com sinal, idCandidato, proximos_vencimentos e a frase da opção a descoberto</name>
  <files>server/app/opcoes_curadoria.py, server/app/skill_ref.py, server/tests/test_opcoes_curadoria.py</files>
  <read_first>
    - server/app/opcoes_curadoria.py (arquivo inteiro, 298 linhas — é o arquivo a estender)
    - server/app/skill_ref.py:525-600 (dict `OPCOES_LASTREADAS`, registros `operador` e `educacional`)
    - server/app/skill_ref.py:615-640 (`opcoes_lastreadas_txt` — o fallback silencioso para `sem_setup`)
    - server/app/opcoes_lastreadas.py:62-185 (`_propor_collar` — de onde sai o padrão `caixa_pernas = avaliar(pernas_opcao)` / `custo_liquido` como prêmio líquido da estrutura, e a forma de `pernasContratos`)
    - server/tests/test_opcoes_curadoria.py (arquivo inteiro — fixtures `_contrato`/`_cadeia` e o estilo dos guardiões existentes)
  </read_first>
  <behavior>
    - `premio_liquido_unitario([perna de call vendida])` devolve +lastPrice (crédito recebido, positivo).
    - `premio_liquido_unitario([perna de put comprada])` devolve -lastPrice (débito pago, negativo).
    - `premio_liquido_unitario([call vendida, put comprada])` devolve `premio_call - premio_put`, com o sinal que sair (crédito positivo, débito negativo).
    - Um candidato `call_coberta` gerado depois desta refatoração tem `premioUnitario` e `razao` IDÊNTICOS aos de antes (os testes já existentes de `call_coberta` continuam passando sem edição).
    - `proximos_vencimentos(["2026-09-11","2026-10-16","2026-11-20"], date(2026,9,14))` devolve `["2026-10-16","2026-11-20"]` (passado descartado, ordenado, teto 2).
    - `proximos_vencimentos` tolera item malformado/None/lista vazia devolvendo `[]` ou a lista parcial — nunca levanta.
    - `opcoes_lastreadas_txt("operador", "opcao_a_descoberto", ...)` NÃO devolve a frase de `sem_setup`; o mesmo no registro `educacional`.
  </behavior>
  <action>
Três edições, nesta ordem.

**(a) `server/app/skill_ref.py` — a frase canônica que ainda não existe.**
No dict `OPCOES_LASTREADAS`, acrescente a chave `"opcao_a_descoberto"` nos
DOIS registros, ao lado de `"put_protecao"`:

- registro `"operador"` (fala como mesa, verbo de ordem): texto no formato
  "Comprar {n} call(s) de {ticker} strike {strike} a seco por R$
  {premioTotal} — sem lastro em ações: o risco máximo é todo o prêmio pago."
- registro `"educacional"` (condição, nunca ordem, mesmo registro
  condicional de `put_protecao`): "Se você tivesse comprado esta call a seco
  agora, pagaria R$ {premioTotal} e perderia todo esse valor se a opção
  virasse pó no vencimento — nenhuma ação sua entra como lastro."

Marcadores permitidos: `{n}`, `{ticker}`, `{strike}`, `{premioTotal}`,
`{qtyAcoes}` (os mesmos que o chamador já formata com `num_br`). Comente
acima das duas entradas, no estilo das vizinhas, que a estrutura é a da
Fase 29 (`store.buy_option`, compra a seco) e que a frase só é emitida na
varredura quando `permitirOpcaoADescoberto` está ligado (D-05). Nenhuma
palavra de promessa de lucro/enriquecimento (princípios 6 e 8 do CLAUDE.md).

**(b) `server/app/opcoes_curadoria.py` — constantes e funções puras novas.**
Acrescente, junto das constantes existentes (`TOPO`, `STRIKES_POR_POSICAO`,
`PISO_LIQUIDEZ`):

- `VENCIMENTOS_POR_POSICAO = 2` — teto FIXO de vencimentos varridos por
  posição elegível (D-01). Comentário obrigatório citando o custo: cada
  vencimento a mais é 1 chamada de `get_options_chain` por posição, contra o
  orçamento medido do mydata (60/min · 2.000/dia,
  `docs/MEDICAO-Mydata-2026-08-27.md`). Quem subir esta constante paga rede
  por posição × vencimento — não é parâmetro de ajuste fino.
- `CONTRATOS_A_DESCOBERTO = 1` — a opção a descoberto não tem lastro contra
  o qual dimensionar, e a varredura é DESCOBERTA (não execução): 1 contrato
  = 100 unidades do objeto, fixo. A checagem de caixa acontece na execução
  (`store.buy_option`), nunca aqui.
- `TIPOS = ("call_coberta", "put_protecao", "collar", "opcao_a_descoberto")`
  — o universo fechado desta fase (D-04).

E três funções puras:

- `premio_liquido_unitario(pernas_opcao) -> float`: devolve
  `round(-opcoes_motor.avaliar(pernas_opcao)["custo_liquido"], 2)`, ou seja o
  fluxo de caixa líquido das pernas de OPÇÃO por unidade do objeto, positivo
  quando o usuário RECEBE e negativo quando PAGA. Recebe só as pernas de
  opção (nunca a perna de ação — incluí-la somaria o preço do papel ao
  "prêmio"). Mesmo padrão que `_propor_collar` já usa (`caixa_pernas =
  avaliar(pernas_opcao)`); é reuso do número que o motor já calcula, não uma
  segunda aritmética. Docstring precisa dizer explicitamente que prêmio
  negativo é resultado ESPERADO em put de proteção / collar de débito /
  opção a descoberto (D-06) e NÃO deve ser filtrado.
- `id_candidato(tipo, ticker, expiration, contract_symbol=None, strike_call=None, strike_put=None) -> str`:
  identidade estável e total de um candidato, no formato
  `"{tipo}:{ticker}:{expiration}:{contract_symbol or f'{strike_call}/{strike_put}'}"`.
  Existe por dois motivos nomeados: (1) collar não tem `contractSymbol`
  (duas pernas — `opcoes_lastreadas.py:145-156` já explica por que
  preencher com uma das pernas seria mentira), e sem um último critério
  TOTAL dois collars empatados voltariam em ordem de chegada; (2) o front
  precisa de uma chave de React estável (`web/src/App.jsx:4191`, hoje
  `key={item.contractSymbol}`, que colide em `null`).
- `proximos_vencimentos(expirations, hoje, *, teto=VENCIMENTOS_POR_POSICAO) -> list[str]`:
  recebe a lista `expirations` da cadeia ADR-004 (strings ISO), devolve até
  `teto` datas ESTRITAMENTE futuras, ordenadas crescente, sem repetição.
  Mesma tolerância a item malformado de `options_provider_mydata.
  _primeiro_vencimento_futuro` (`dt.date.fromisoformat` dentro de
  `try/except (TypeError, ValueError): continue`), `sorted()` defensivo pelo
  mesmo motivo (D-05 da quick 260914-b6p: a ordem da lista é contrato de
  terceiro não documentado). Entrada não-lista → `[]`. Função PURA: `hoje`
  entra por argumento, nunca lê relógio.

**(c) refatore o ramo `call_coberta` existente para usar as funções novas**,
sem mudar um número: `premio_unitario` passa a ser
`premio_liquido_unitario([perna da call vendida])` (que dá exatamente
`+lastPrice`, idêntico ao `round(premio, 2)` de hoje) e o dict do candidato
ganha `"idCandidato": id_candidato("call_coberta", underlying,
chain.get("expiration"), contrato.get("contractSymbol"))`. `premioTotal`
continua `round(premio_liquido_unitario * qty_acoes, 2)`.

**(d) atualize os dois docstrings de `server/app/opcoes_curadoria.py` que
hoje mandam o leitor PARAR** — o docstring do módulo (topo do arquivo) e o
de `candidatos_da_posicao` afirmam literalmente "Universo (D1): SÓ venda
coberta... pare: é mudança de escopo de produto (D1)" e "Janelas (D3):
vários STRIKES dentro do vencimento ÚNICO... nunca múltiplos vencimentos" —
exatamente o que esta fase reverte de propósito. Troque a linguagem de
"pare" por uma nota citando Fase 31/D-04 (as 4 estruturas substituem o
universo restrito da Fase 30/D1) e Fase 31/D-01 (teto de 2 vencimentos
substitui o vencimento único da Fase 30/D3) como a supersessão deliberada —
mesma disciplina de "reversão deliberada atualiza o guardião com nota" já
usada nos testes.

Testes novos em `server/tests/test_opcoes_curadoria.py` (mesmo estilo dos
existentes, um `def test_` por regra): sinal do prêmio nos 3 arranjos de
pernas; `proximos_vencimentos` (feliz, passado, malformado, lista vazia,
teto); `idCandidato` presente e estável no candidato de venda coberta;
paridade da chave `opcao_a_descoberto` nos dois registros de
`OPCOES_LASTREADAS` + prova de que ela NÃO cai no fallback `sem_setup`.
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/test_opcoes_curadoria.py tests/test_skill_ref.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_opcoes_curadoria.py` passa, incluindo TODOS os testes que já existiam antes deste plano, sem edição neles (prova de que a refatoração de `call_coberta` é neutra em números).
    - `grep -n "opcao_a_descoberto" server/app/skill_ref.py` retorna pelo menos 2 ocorrências (registro operador + educacional).
    - `grep -vn '^\s*#' server/app/opcoes_curadoria.py | grep -c "VENCIMENTOS_POR_POSICAO"` >= 2 (definição + uso/docstring), com o valor literal `2`.
    - `grep -n "pare: é mudança de escopo\|vencimento ÚNICO" server/app/opcoes_curadoria.py` não retorna nenhuma ocorrência (docstrings do módulo e de `candidatos_da_posicao` atualizados, sem a linguagem "pare" da Fase 30/D1/D3).
    - Prova negativa real: apague o corpo de `proximos_vencimentos` (return `[]` fixo) e confirme que o teste nomeado cai; reverta com `git diff` limpo. Registre comando e saída no SUMMARY.
  </acceptance_criteria>
  <done>Prêmio líquido com sinal, `idCandidato`, `proximos_vencimentos` e a frase canônica da opção a descoberto existem e estão testados; o candidato de venda coberta produz exatamente os mesmos números de antes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ramos put_protecao e collar em candidatos_da_posicao</name>
  <files>server/app/opcoes_curadoria.py, server/tests/test_opcoes_curadoria.py</files>
  <read_first>
    - server/app/opcoes_curadoria.py:54-180 (o ramo `call_coberta` já refatorado na Task 1 — é o molde literal de cada ramo novo)
    - server/app/opcoes_lastreadas.py:62-185 (`_propor_collar` — seleção da call, montagem das 3 pernas, escolha da PIOR perna para liquidez, forma de `pernasContratos`)
    - server/app/opcoes_lastreadas.py:260-300 (filtro `rastrear` da PUT: `relacao="abaixo_ou_igual"`, `criterio="max"`)
    - server/app/skill_ref.py:536-565 (marcadores exatos das frases `put_protecao` e `collar` — `{strikeCall}`/`{strikePut}`/`{qtyAcoes}`)
  </read_first>
  <behavior>
    - Cadeia com calls e puts líquidas e posição de 200 ações livres → a lista devolvida contém candidatos `tipo="put_protecao"` e `tipo="collar"` além dos `call_coberta`.
    - Candidato `put_protecao` tem `premioUnitario` NEGATIVO (débito) e `razao` negativa — e continua na lista (D-06 aceita o ranking ruim; filtrar seria mudar a decisão).
    - Candidato `collar` tem `contractSymbol=None`, `strikeCall`/`strikePut` preenchidos, `pernasContratos` com as 2 pernas e `idCandidato` não-vazio.
    - Cadeia sem nenhuma PUT líquida → nenhum candidato `put_protecao` nem `collar`, e os `call_coberta` continuam saindo normalmente (porta fechada devolve `[]` daquele ramo, nunca derruba a varredura).
    - Contrato de put com `lastPrice` ausente → aquele candidato é pulado (`ValueError` de `perna_de_contrato` capturado), os demais sobrevivem.
    - `perda_maxima` não numérica ou <= 0 em qualquer ramo → candidato descartado (mesma porta de hoje, sem razão indefinida publicada).
  </behavior>
  <action>
Dentro de `candidatos_da_posicao`, DEPOIS do `rastrear` de calls que já
existe, acrescente uma segunda seleção em memória (zero rede):

```
selecionados_put = opcoes_motor.rastrear(chain, {
    "tipo": "put", "referencia": spot, "relacao": "abaixo_ou_igual",
    "criterio": "max", "n": n, "liquidez_minima": PISO_LIQUIDEZ,
})
```
`liquidez_minima=PISO_LIQUIDEZ` explícito pelo MESMO motivo já comentado no
ramo das calls (sem ele, `rastrear` faz a segunda passada em
`LIQUIDEZ_DIFICIL` e um contrato DIFÍCIL entraria num ranking que o usuário
lê como "as melhores oportunidades"). Nenhuma porta nova: `selecionados_put`
vazio simplesmente não gera esses dois ramos.

**Ramo `put_protecao`** — um candidato por put selecionada:
pernas de opção = `[perna_de_contrato(put, "compra", quantidade=1)]`;
pernas da estrutura = `[perna_de_acao(underlying, spot, quantidade=1),
*pernas_opcao]`; `estrutura = avaliar(pernas)`. `premio_unitario =
premio_liquido_unitario(pernas_opcao)` (negativo). `contratos =
qty_livre_val // 100`, `qty_acoes = contratos * 100`, `premio_total =
round(premio_unitario * qty_acoes, 2)`. `manchete`/`didatica` via
`skill_ref.opcoes_lastreadas_txt(modo|"educacional", "put_protecao", n=...,
ticker=..., strike=num_br(strike), premioTotal=num_br(abs(premio_total)),
qtyAcoes=...)` — a frase canônica diz "pagaria R$ {premioTotal}", então o
marcador recebe o MÓDULO; o sinal vive no campo JSON `premioUnitario`/
`premioTotal`, não na frase. Comente essa assimetria na linha, senão ela se
lê como bug. Demais campos: mesma forma do ramo de venda coberta
(`ticker`, `contractSymbol`, `optionType`, `strike`, `expiration`,
`diasParaVencimento`, `contratos`, `qtyAcoes`, `liquidez` via
`_bloco_liquidez`, `estrutura`, `razao`, `precoObjeto`, `idCandidato`).

**Ramo `collar`** — um candidato por put selecionada, usando SEMPRE
`selecionados[0]` (a call OTM de menor strike, mesma régua de
`_propor_collar`): pernas de opção = `[perna_de_contrato(call, "venda"),
perna_de_contrato(put, "compra")]`; pernas da estrutura = ação + as duas.
`premio_unitario = premio_liquido_unitario(pernas_opcao)` (crédito positivo
quando a call vale mais que a put, débito negativo no caso contrário).
`contratos = qty_livre_val // 100` — **sem gate de caixa** aqui, ao
contrário de `_propor_collar`: `candidatos_da_posicao` é PURA e não recebe
`cash`; a varredura é descoberta, o caixa é cobrado na execução
(`store.abrir_collar`). Comente isso explicitamente para que ninguém
"conserte" adicionando um parâmetro de caixa. Campos específicos, copiados
de `_propor_collar` (opcoes_lastreadas.py:143-165): `contractSymbol: None`,
`optionType: None`, `strike: None`, `strikeCall`, `strikePut`,
`pernasContratos` (lista das 2 pernas com `contractSymbol`/`optionType`/
`lado`/`strike`/`premioUnitario` de cada). `liquidez` = `_bloco_liquidez` da
PIOR perna (menor `liquidity_score`), mesma regra e mesmo motivo de
`_propor_collar:113-124`. `manchete`/`didatica` com a chave `"collar"` e os
marcadores `{n}`, `{ticker}`, `{strikeCall}`, `{strikePut}`, `{qtyAcoes}`.

Os dois ramos ficam dentro do MESMO laço de tolerância a falha do ramo atual
(`try: ... except ValueError: continue`) e repetem a porta
`perda_maxima` não-numérica/<=0 → `continue`. Nenhum ramo levanta, nenhum
devolve `None` — contrato de porta fechada do módulo (docstring, linhas
64-67).

Testes novos em `test_opcoes_curadoria.py`: um por item do `<behavior>`.
Inclua um teste que prova que o candidato de put com prêmio negativo
SOBREVIVE à geração (é o ponto onde um executor futuro tenderia a filtrar —
e filtrar reabriria D-06).
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/test_opcoes_curadoria.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_opcoes_curadoria.py` verde, com testes nomeados cobrindo os 6 itens do `<behavior>`.
    - `grep -c '"tipo": "put_protecao"\|"tipo": "collar"' server/app/opcoes_curadoria.py` >= 2.
    - Nenhum `raise` novo em `opcoes_curadoria.py` fora de `exigir_ranking`: `grep -vn '^\s*#' server/app/opcoes_curadoria.py | grep -n "raise " ` só mostra linhas dentro de `exigir_ranking`.
    - Nenhum parâmetro `cash`/`caixa` foi adicionado a `candidatos_da_posicao` (`grep -n "def candidatos_da_posicao" -A 12 server/app/opcoes_curadoria.py` não contém `cash`).
  </acceptance_criteria>
  <done>Put de proteção e collar saem da mesma cadeia em memória, com prêmio líquido assinado, liquidez da pior perna e manchete verbatim do `skill_ref`; nenhuma chamada de rede nova, nenhum filtro que D-06 não pediu.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ramo opcao_a_descoberto com gate de descoberta (D-05), ordem total e narração ciente do tipo</name>
  <files>server/app/opcoes_curadoria.py, server/tests/test_opcoes_curadoria.py</files>
  <read_first>
    - server/app/opcoes_curadoria.py (arquivo inteiro, já com as Tasks 1 e 2 aplicadas)
    - server/app/store.py:815-830 (gate de EXECUÇÃO de `buy_option`: `cfg.get("permitirOpcaoADescoberto")` + `registrar_rejeicao` + `raise`) — ler para replicar a LEITURA do flag e NÃO replicar a postura de recusa
    - server/app/curadoria_narrativa.py:40-89 (`narrar` chama `exigir_ranking(top)` antes de tocar no LLM — o guardrail que não pode ser contornado)
  </read_first>
  <behavior>
    - `candidatos_da_posicao(..., permitir_a_descoberto=False)` (default) → nenhum item com `tipo="opcao_a_descoberto"` na lista, e os outros 3 tipos saem normalmente.
    - `candidatos_da_posicao(..., permitir_a_descoberto=True)` → itens `opcao_a_descoberto` aparecem, com `contratos=1`, `qtyAcoes=100`, `premioUnitario` negativo e `perda_maxima` igual ao prêmio pago.
    - Nenhuma chamada de `rastrear` extra é feita pelo ramo a descoberto (ele reusa `selecionados`): o número de chamadas a `opcoes_motor.rastrear` por invocação continua 2 (calls + puts), com ou sem o flag.
    - `rankear` devolve ordem TOTAL mesmo com dois collars empatados em `razao` e `premioUnitario` (ambos com `contractSymbol=None`): a ordem é estável entre execuções.
    - `exigir_ranking` continua aceitando os dicts mínimos dos testes antigos (nenhum campo novo virou obrigatório).
    - `narrativa_user` nomeia o TIPO de cada estrutura e não imprime `None` para o collar; e `narrativa_system` proíbe descrever prêmio negativo como receita.
  </behavior>
  <action>
**(a) Assinatura e gate.** Acrescente o parâmetro keyword-only
`permitir_a_descoberto: bool = False` a `candidatos_da_posicao`. O default
`False` é FAIL-CLOSED e proposital: um chamador que esqueça de passar o flag
nunca vaza oportunidade a descoberto. Documente na docstring que este é o
primeiro ponto do sistema onde `permitirOpcaoADescoberto` é lido na
DESCOBERTA (D-05) e que a semântica aqui é **pulo silencioso** — sem
`raise`, sem `registrar_rejeicao`, sem aviso ao usuário — deliberadamente
diferente do gate de EXECUÇÃO em `store.buy_option` (store.py:815-828), que
recusa alto e registra rejeição. Os dois comportamentos NÃO devem ser
unificados: um é filtro de exibição, o outro é recusa de ordem.

**(b) Ramo `opcao_a_descoberto`** (só quando `permitir_a_descoberto` é
verdadeiro): para cada contrato já em `selecionados` (as MESMAS calls OTM do
ramo de venda coberta — zero `rastrear` novo, zero rede nova), pernas de
opção = `[perna_de_contrato(contrato, "compra", quantidade=1)]` e pernas da
estrutura = as mesmas (sem perna de ação — é exatamente o que "a descoberto"
significa: nenhum lastro). `contratos = CONTRATOS_A_DESCOBERTO`,
`qty_acoes = contratos * 100`, `premio_unitario = premio_liquido_unitario
(pernas_opcao)` (negativo), `premio_total = round(premio_unitario *
qty_acoes, 2)`. `manchete`/`didatica` com a chave `"opcao_a_descoberto"`
criada na Task 1, `premioTotal=num_br(abs(premio_total))` (mesma assimetria
sinal-no-campo/módulo-na-frase da put). Comentário obrigatório no ramo:
escopo de BUSCA continua sendo a carteira do usuário mesmo sem exigir lastro
(D-09) — a oportunidade a descoberto é gerada sobre os tickers que o usuário
já tem, nunca sobre o catálogo B3 inteiro.
Escolha registrada (discrição do plano, não decisão nova de produto): a
estrutura "a descoberto" desta fase é a COMPRA de call a seco — o caminho
que a Fase 29 gateou (`store.buy_option`). Venda de call nua não entra:
`avaliar` devolve `perda_maxima=None` para perda ilimitada e a porta de
`perda_maxima` já existente descartaria o candidato de qualquer jeito;
publicar razão sobre perda indefinida violaria o princípio 4 do CLAUDE.md.
Deixe isso escrito no comentário do ramo.

**(c) Ordem total em `rankear`.** Troque a chave de ordenação por
`(-c["razao"], -(c.get("premioUnitario") or 0.0), c.get("contractSymbol") or
"", c.get("idCandidato") or "")`. O `contractSymbol` permanece como TERCEIRO
critério de propósito (preserva byte a byte a ordem que os guardiões da Fase
30 já travaram); `idCandidato` entra como QUARTO só para desempatar o que
antes não tinha como desempatar (collar, `contractSymbol=None`). Atualize a
docstring dizendo por que a chave cresceu. **Não** acrescente campo
obrigatório em `exigir_ranking` — os guardiões da Fase 30 alimentam dicts
mínimos e devem continuar passando sem edição.

**(d) Narração ciente do tipo.** Em `narrativa_user`, acrescente um dict
`_ROTULO_TIPO` (`call_coberta` → "venda coberta", `put_protecao` → "put de
proteção", `collar` → "trava protetora (collar)", `opcao_a_descoberto` →
"opção a descoberto") e inclua o rótulo no início de cada linha; onde hoje
imprime `contractSymbol`, use `item.get("contractSymbol") or f"call
{strikeCall} / put {strikePut}"` para o collar. Troque o rótulo "prêmio
unitário" por "prêmio líquido unitário (positivo = recebido, negativo =
pago)". Em `narrativa_system`, acrescente à lista de regras: descrever um
prêmio líquido negativo como receita é PROIBIDO — número negativo significa
que o usuário paga para montar a estrutura. A lista de campos serializados
continua FECHADA (T-30-03): nada de `curva`, nada de dado de conta.

Testes novos: um por item do `<behavior>`, mais um guardião explícito de
D-05 no nível do motor — `permitir_a_descoberto=False` sobre uma cadeia que
PRODUZIRIA candidatos a descoberto devolve zero itens desse tipo.
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/test_opcoes_curadoria.py tests/test_opcoes_curadoria_narrativa.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_opcoes_curadoria.py tests/test_opcoes_curadoria_narrativa.py` verde, com os guardiões da Fase 30 intactos (nenhuma edição nos testes antigos).
    - `grep -n "permitir_a_descoberto" server/app/opcoes_curadoria.py` mostra o parâmetro com default `False` e o uso no ramo.
    - Prova negativa real: inverta o gate (`if not permitir_a_descoberto` → `if True`) e confirme que o teste de D-05 cai; reverta com `git diff` limpo. Comando e saída no SUMMARY.
    - `bash scripts/executar.sh --testes` exit 0 e sem regressão contra a baseline medida na Task 1 (números no SUMMARY).
  </acceptance_criteria>
  <done>As 4 estruturas saem de uma cadeia em memória sob a mesma fórmula; a oportunidade a descoberto só existe com o flag ligado, por pulo silencioso; a ordem é total mesmo para estruturas sem `contractSymbol`; a narração nomeia o tipo e não pode chamar débito de receita.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config da conta → geração de candidatos | `permitirOpcaoADescoberto` cruza da persistência para a camada de descoberta pela primeira vez (D-05) |
| motor determinístico → prompt de LLM | `narrativa_user` serializa candidatos das 4 estruturas para o modelo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Elevation of Privilege | `candidatos_da_posicao` (ramo a descoberto) | mitigate | parâmetro `permitir_a_descoberto` keyword-only com default `False` (fail-closed) + guardião que prova zero candidatos com o flag desligado |
| T-31-02 | Tampering | `rankear`/`exigir_ranking` | mitigate | chave de ordenação total e fechada; `exigir_ranking` sem campo novo; nenhum campo de LLM participa da ordem (princípio 5 do CLAUDE.md) |
| T-31-04 | Information Disclosure | `narrativa_user` | mitigate | lista de campos serializados continua FECHADA (sem `curva`, sem saldo, sem id de sessão), agora com rótulo de tipo apenas |
| T-31-06 | Repudiation/Integrity | frase da opção a descoberto | mitigate | manchete só de `skill_ref.opcoes_lastreadas_txt` (guardrail CVM); chave nova criada ANTES do ramo para não cair no fallback silencioso `sem_setup` |
| T-31-SC | Tampering | npm/pip installs | mitigate | este plano não instala nenhum pacote — `git diff --stat` não pode tocar `server/requirements*.txt` nem `package.json` |
</threat_model>

<verification>
- `cd server && .venv/bin/python -m pytest tests/test_opcoes_curadoria.py tests/test_opcoes_curadoria_narrativa.py tests/test_skill_ref.py -q` verde.
- `bash scripts/executar.sh --testes` exit 0, sem regressão contra a baseline medida na Task 1.
- Nenhum arquivo fora de `files_modified` alterado (`git status --short`).
</verification>

<success_criteria>
- As 4 estruturas de D-04 são geradas a partir de uma única cadeia em memória, sem nenhuma chamada de rede nova neste plano.
- O ranking usa uma fórmula só, com prêmio líquido assinado (SC-4/D-06), e put/collar/descoberto NÃO são filtrados por ranquearem mal.
- A oportunidade a descoberto é invisível por padrão e só existe com `permitir_a_descoberto=True` (SC-3/D-05).
- Zero mudança em `store.py` (Fase 29 intacta).
</success_criteria>

<output>
Crie `.planning/phases/31-varredura-oportunidades-opcoes/31-01-SUMMARY.md` ao
terminar, com: baseline medida, números finais das duas suítes, as duas
provas negativas (comando + saída), e a lista de campos do candidato de cada
tipo (interface que o Plano 02 e o Plano 04 consomem).
</output>
