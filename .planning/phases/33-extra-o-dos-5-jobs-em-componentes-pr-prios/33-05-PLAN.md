---
phase: 33-extra-o-dos-5-jobs-em-componentes-pr-prios
plan: 05
type: execute
wave: 5
depends_on: [33-04]
files_modified:
  - web/src/opcoes/SecaoAnalisar.jsx
  - web/src/opcoes/uiOpcoes.jsx
  - web/src/opcoes/OpcoesScreen.jsx
  - web/tests/test_opcoes_subabas_ui.mjs
  - web/tests/test_opcoes_analisar_ui.mjs
  - web/tests/test_opcoes_mcp_aba_ui.mjs
autonomous: true
requirements: [REORG-01, REORG-02, REORG-03, REORG-05, REORG-06]

must_haves:
  truths:
    - "D-01: o job 'analisar um ticker manualmente' vive em web/src/opcoes/SecaoAnalisar.jsx — a tabela de comportamento (LEITURA DO ATIVO) e o montador de estrutura (O QUE DÁ PARA MONTAR)"
    - "D-03: os 5 jobs estão extraídos ao fim deste plano; nada mudou de comportamento, dado ou ordem visível em nenhum deles"
    - "D-04a: SubAbaOperar deixa de buscar gate/proposta por conta própria — recebe opcoesPorTicker/opcoesPorTickerCarregando por prop, do MESMO fan-out que a sub-aba Setups já paga, e a contagem de store.optionsGate/optionsProposta em OpcoesScreen.jsx cai de 1 para 0 com nota datada no guardião"
    - "D-02: a regra 3 de test_opcoes_subabas_ui.mjs vira varredura de diretório (1x na pasta, 0x no orquestrador) com nota datada da queda 1→0, em vez de ter o número esperado ajustado"
    - "REORG-02: a guarda 'proposta só é pedida quando gate.liquida' não desaparece — ela passa a ser medida em useOpcoesPropostas.js, que já a implementa, em vez de na fatia de SubAbaOperar"
    - "REORG-03: SecaoAnalisar não chama store.* nem instancia hook; o único useState que pode nascer nela é `painel` (cadeia/operáveis), que é visual e exclusivo deste job"
    - "REORG-06: o guardrail CVM generalizado no 33-02 cobre SecaoAnalisar automaticamente — verificado por injeção, não por analogia"
  artifacts:
    - path: "web/src/opcoes/SecaoAnalisar.jsx"
      provides: "Job 3 — leitura do ativo + montador de estrutura, com painel local de cadeia/operáveis"
      contains: "export default function SecaoAnalisar"
      min_lines: 150
  key_links:
    - from: "web/src/opcoes/OpcoesScreen.jsx"
      to: "web/src/opcoes/SecaoAnalisar.jsx"
      via: "<SecaoAnalisar .../> no topo do ramo DADOS, antes de <SecaoComparar"
      pattern: "<SecaoAnalisar"
    - from: "web/src/opcoes/OpcoesScreen.jsx"
      to: "SubAbaOperar"
      via: "opcoesPorTicker/opcoesPorTickerCarregando descendo por prop (D-04a), no lugar dos dois useEffect locais"
      pattern: "opcoesPorTicker=\\{opcoesPorTicker\\}"
---

<objective>
Extrair o job 3 ("analisar um ticker manualmente") para
`web/src/opcoes/SecaoAnalisar.jsx` — fechando a extração dos 5 jobs — e dobrar
o TODO do fetch redundante (D-04a): `SubAbaOperar` para de buscar
gate/proposta por conta própria e passa a ler o fan-out que a aba já paga.

Purpose: passo 5 e último de D-03, deixado para o fim por ser o mais
entrelaçado com `SubAbaOperar`. O fold-in D-04a não é escopo extra: é
exatamente o que REORG-03 já exige (dado chega por prop do orquestrador).
Output: 1 componente novo, 2 `useEffect` a menos, 3 guardiões reapontados,
suíte canônica verde, fase pronta para a 34.

**Escopo que este plano NÃO tem:** mover `cabecalho`, `seletor`,
`LastroDoAtivo`, `LeituraInterna` ou `blocoLeituraDoServico` — essa é a moldura
compartilhada pelos jobs 3/4/5 (escolha de ticker, leitura grátis, porta da
leitura paga), não um job, e onde ela mora é decisão da Fase 34 (hub ×
workspace). A regra 11 de `test_opcoes_subabas_ui.mjs` continua exigindo os
cinco nomes em `OpcoesScreen.jsx` e isso é proposital. Também fora de escopo:
tocar `App.jsx`, mudar navegação, mudar visual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/33-extra-o-dos-5-jobs-em-componentes-pr-prios/33-CONTEXT.md
@.planning/phases/33-extra-o-dos-5-jobs-em-componentes-pr-prios/33-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/todos/pending/subaba-operar-fetch-redundante-gate-proposta.md
@.planning/phases/33-extra-o-dos-5-jobs-em-componentes-pr-prios/33-04-SUMMARY.md

<interfaces>
Medido no planejamento (2026-09-19) — reconfirmar linhas por Grep antes de editar.

**Fonte a extrair** (`OpcoesScreen.jsx`, ramo "4. DADOS"): do
`<Kicker>{cp.opcoesLeituraTitulo || "LEITURA DO ATIVO"}</Kicker>` de DENTRO do
ramo de dados (hoje linha 913 — atenção: existe um segundo
`cp.opcoesLeituraTitulo` na linha 667, dentro de `blocoLeituraDoServico`, que
NÃO se move) até imediatamente antes de `<SecaoComparar` (posto lá pelo 33-04).
Contém: a tabela de `behavior` (`<Linha>` por indicador, com `txt`/`fmt`/`pct`/
`fracPct`/`faixa`), `<LacunasDaLeitura>`, o seletor de tese, vencimento e lote,
o botão "Montar estrutura", a cascata de `proposta`
(`carregando → erro → vazio com motivo → dados`) com `<PayoffChart>`,
`<RazaoGanhoPerda razao={proposta.dados.razaoGanhoPerda}>`, `<Pernas>`,
`<Cenarios>`, e os botões "Ver a cadeia"/"Ver as operáveis" com o estado
`painel`.

**`painel`** (`const [painel, setPainel] = useState("")`, hoje linha 372,
valores `"" | "cadeia" | "operaveis"`) é o ÚNICO estado que desce para dentro de
uma seção nesta fase — é visual e exclusivo deste job (ARCHITECTURE.md,
"Estado local que atravessa jobs"). `ticker`, `tese`, `vencimento`, `lote`
FICAM no orquestrador (Pitfall 6).

**D-04a — o que foi medido e por que a substituição é segura:**
- `SubAbaOperar` (hoje 1406-1546) tem dois `useEffect` (1432-1446):
  `store.optionsGate(ticker)` e, guardado por `gate && gate.liquida`,
  `store.optionsProposta(ticker, true)`.
- `useOpcoesPropostas(store, tickers)` (`web/src/opcoes/useOpcoesPropostas.js`)
  faz o MESMO par para TODOS os tickers da carteira: `store.optionsGate(t)` e,
  sob `if (gate && gate.liquida)`, `store.optionsProposta(t, true)` — mesmo
  método, mesmo `multiperna: true`, mesma guarda. Guarda o resultado como
  `propostas[t] = { gate, proposta }`.
- O universo de `SubAbaOperar` é a CARTEIRA (Fase 27, D3), e o fan-out varre
  exatamente `carteira.map((p) => p.t)` — ou seja, o ticker selecionado em
  Operar está SEMPRE coberto, e o hook re-dispara quando a chave de tickers
  muda (posição nova entra no fan-out por si). Por isso não é preciso manter
  fetch de fallback: manter um seria a segunda fonte que o guardião existe para
  impedir. Se a medição ao exercitar o app contradisser isso, PARAR e registrar
  — não improvisar um fallback silencioso.
- `carregando` do hook é global do fan-out (não por ticker). O estado de
  carregamento do ticker selecionado passa a ser
  "`opcoesPorTickerCarregando` e ainda não há entrada para este ticker".
- `posAberta` e `candidatos` em `SubAbaOperar` derivam de `prop` — passam a
  derivar de `opcoesPorTicker[ticker] && opcoesPorTicker[ticker].proposta` e
  `...gate`, mantendo as MESMAS fórmulas (`Array.isArray`, casamento por
  `contractSymbol`).

**Guardiões que ancoram aqui** (medido por grep, 2026-09-19):
- `test_opcoes_subabas_ui.mjs` regra 3 (123-126): `store.optionsGate(` e
  `store.optionsProposta(` exatamente 1× em `OpcoesScreen.jsx` — cai para 0
  com D-04a.
- `test_opcoes_subabas_ui.mjs` regra 4 (128-139): a guarda medida entre
  `setProp(null);` e a chamada, DENTRO da fatia de `SubAbaOperar` — o trecho
  inteiro deixa de existir.
- `test_opcoes_analisar_ui.mjs` item 5 (fatia "Analisar" e `iAnalisar`), item 13
  (`razao={proposta.dados.razaoGanhoPerda}`), e a asserção da faixa de 63
  (`const faixa = ` + `valor={faixa(behavior.range_63_sessions)}`, ~linha 528).
- `test_opcoes_mcp_aba_ui.mjs` seção 2 (128-137): `iDados = indexOf("cp.opcoesLeituraTitulo", iRender)`.
  ARMADILHA REAL: como `blocoLeituraDoServico` também contém essa chave e NÃO
  se move, o guardião continuaria PASSANDO — apontando para o convite de
  leitura paga em vez do ramo de dados. Passar sem medir o que se propõe a
  medir é o modo de falha que esta fase inteira tem de evitar.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar SecaoAnalisar.jsx e religar OpcoesScreen.jsx</name>
  <files>web/src/opcoes/SecaoAnalisar.jsx, web/src/opcoes/uiOpcoes.jsx, web/src/opcoes/OpcoesScreen.jsx</files>
  <read_first>
    - web/src/opcoes/OpcoesScreen.jsx — faixas 905-1135 (o bloco a extrair) e 360-380 (os useState, inclusive `painel`). Reconfirmar por Grep de `cp.opcoesAnalisarTitulo`.
    - web/src/opcoes/SecaoComparar.jsx — o irmão criado no 33-04: mesma classe de problema (bloco do ramo DADOS com cascata própria alimentada por prop), já resolvido uma vez nesta fase.
    - web/src/opcoes/uiOpcoes.jsx — o que já está compartilhado depois de 4 planos.
    - .planning/research/PITFALLS.md — Pitfall 6 (estado) e Pitfall 7 (não remover informação).
  </read_first>
  <action>
Criar `web/src/opcoes/SecaoAnalisar.jsx`, `export default function SecaoAnalisar`,
recebendo por prop: `ticker`, `behavior`, `lacunas`, `leitura`, `tese`,
`setTese`, `vencimento`, `setVencimento`, `lote`, `setLote`, `proposta`,
`montarProposta` (o disparo que hoje está inline), `expirations`, `custos`,
`cp`, `palette`, `ctx` (só se o painel de cadeia/operáveis precisar de algo de
`ctx`, como `SetupChart` precisa de `ctx.PriceChart`). Declarar DENTRO dela o
único `useState` permitido: `painel` (`"" | "cadeia" | "operaveis"`), movido do
orquestrador.

Regras que a extração não pode afrouxar:
- `ticker`/`tese`/`vencimento`/`lote` chegam por prop com seus setters —
  proibido `useState` para qualquer um deles (Pitfall 6).
- A cascata de `proposta` mantém a ordem dos ramos
  (`carregando → erro → vazio com motivo → dados`) e o erro continua escolhido
  pelo `code` via `<ErroDoMcp>`.
- Nada é recalculado: `behavior`, `razaoGanhoPerda`, breakevens e valores em
  reais chegam prontos do backend. Proibido multiplicar por `lote`, proibido
  misturar `emReais` com `breakeven`, proibido dividir para obter a razão,
  proibido `Math.sqrt/log/pow/exp` — as quatro regras já são varridas por
  diretório desde o 33-01, e agora valem para este arquivo automaticamente.
- `LacunasDaLeitura`, `Linha`, `RazaoGanhoPerda`, `Pernas`, `TabelaDeOpcoes`,
  `Cenarios` e os formatadores `faixa`/`fmt`/`pct`/`fracPct`/`txt`: mover para
  `uiOpcoes.jsx` o que o orquestrador AINDA usar depois desta extração e
  importar dos dois lados; mover para dentro de `SecaoAnalisar.jsx` o que ficar
  com um consumidor só. Nunca duplicar componente com lógica; espelho declarado
  só para token de tema, constante de estilo e formatador de uma linha (regra
  fixada no 33-01).
- Botões "Ver a cadeia"/"Ver as operáveis" continuam declarando o custo no
  próprio controle, lido de `custos`.

Em `OpcoesScreen.jsx`: apagar o JSX movido e o `useState` de `painel`; manter
`behavior`/`lacunas`/`expirations` e as derivações do hook; renderizar
`<SecaoAnalisar ... />` na POSIÇÃO EXATA de hoje — primeiro elemento do ramo
"4. DADOS", antes de `<SecaoComparar`. O `cp.opcoesLeituraTitulo` de
`blocoLeituraDoServico` (o convite da leitura paga) FICA intocado; registrar em
comentário datado que existem duas ocorrências dessa chave por razões
diferentes, para ninguém "consolidar" as duas depois.

NÃO commitar: commit único no fim da Task 3.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('web/src/opcoes/SecaoAnalisar.jsx','utf8'); const o=require('fs').readFileSync('web/src/opcoes/OpcoesScreen.jsx','utf8'); const sem=(x)=>x.replace(/\/\*[\s\S]*?\*\//g,'').split('\n').filter(l=>!/^\s*\/\//.test(l)).join('\n'); const c=sem(s); const f=(m)=>{console.log('FALHOU '+m);process.exitCode=1}; if(/store\.|api\./.test(c))f('SecaoAnalisar chama store/api'); if(/useOpcoesMcp|useOpcoesPropostas/.test(c))f('instancia hook'); if(/App\.jsx/.test(c))f('importa App.jsx'); if(/\.manchete/.test(c))f('renderiza manchete (REORG-06)'); if(/useState\((?!\"\")/.test(c)&&!/const \[painel, setPainel\] = useState/.test(c))f('useState indevido'); if(/useState\([^)]*\)/.test(c)&&(c.match(/useState\(/g)||[]).length>1)f('mais de um useState na secao'); if(/\* lote\b|\blote \*/.test(c))f('multiplica por lote'); if(!/<SecaoAnalisar/.test(o))f('OpcoesScreen nao renderiza SecaoAnalisar'); if(/const \[painel/.test(o))f('painel sobrou no orquestrador'); if(o.indexOf('<SecaoAnalisar')>o.indexOf('<SecaoComparar'))f('ordem job3 antes de job4 quebrada'); console.log(process.exitCode?'':'ok estrutura de SecaoAnalisar')"</automated>
    <automated>npx vite build</automated>
  </verify>
  <done>`SecaoAnalisar.jsx` existe com `painel` como único estado local; `OpcoesScreen.jsx` renderiza `<SecaoAnalisar` antes de `<SecaoComparar`; nenhuma duplicação de componente com lógica; `vite build` verde.</done>
</task>

<task type="auto">
  <name>Task 2: Fold-in D-04a — SubAbaOperar lê o fan-out em vez de refazer a busca</name>
  <files>web/src/opcoes/OpcoesScreen.jsx</files>
  <read_first>
    - .planning/todos/pending/subaba-operar-fetch-redundante-gate-proposta.md — os 3 passos que o TODO pede, incluindo a nota datada no guardião.
    - web/src/opcoes/useOpcoesPropostas.js — arquivo inteiro (o fan-out que já faz o mesmo par de chamadas, com a mesma guarda e o mesmo `multiperna: true`).
    - web/src/opcoes/OpcoesScreen.jsx — faixa 1406-1470 (`SubAbaOperar`: os dois `useEffect`, `posAberta`, `candidatos`) e a linha onde `useOpcoesPropostas` é chamado no topo.
  </read_first>
  <action>
Antes de editar, confirmar por leitura (e registrar no SUMMARY) as três
premissas que tornam a substituição honesta:
(a) o fan-out chama `store.optionsProposta(t, true)` — mesmo `multiperna` que
`SubAbaOperar` usa; (b) o fan-out guarda `{ gate, proposta }` por ticker, com a
mesma guarda `gate && gate.liquida`; (c) o universo do fan-out
(`carteira.map((p) => p.t)`) contém sempre o ticker selecionado em Operar.
Se alguma premissa não se sustentar, PARAR e registrar no SUMMARY em vez de
improvisar.

Então: passar `opcoesPorTicker` e `opcoesPorTickerCarregando` como props para
`<SubAbaOperar>`; apagar os DOIS `useEffect` locais e os `useState` de `gate` e
`prop`; derivar `const entrada = opcoesPorTicker[ticker] || null;`,
`const gate = entrada && entrada.gate;`, `const prop = entrada && entrada.proposta;`.
O estado de carregamento do ticker passa a ser
`opcoesPorTickerCarregando && !entrada`. `posAberta` e `candidatos` continuam
com as MESMAS fórmulas, agora sobre `prop` derivado. Nenhuma chamada de rede
nova, nenhum fallback de fetch local — se o ticker não estiver no fan-out, o
estado correto é "ainda não varrido", não uma segunda busca (fallback seria a
segunda fonte que o guardião da regra 3 existe para impedir).

Comentário datado no lugar dos `useEffect` apagados explicando: o par
gate→proposta não sumiu, mudou de dono; a rota é interna e de custo ZERO, então
o que se ganha não é cota de MCP, é uma busca a menos e uma fonte só para os
dois destinos da aba.
  </action>
  <verify>
    <automated>node -e "const o=require('fs').readFileSync('web/src/opcoes/OpcoesScreen.jsx','utf8'); const sem=(x)=>x.replace(/\/\*[\s\S]*?\*\//g,'').split('\n').filter(l=>!/^\s*\/\//.test(l)).join('\n'); const c=sem(o); const f=(m)=>{console.log('FALHOU '+m);process.exitCode=1}; if(/store\.optionsGate\(/.test(c))f('optionsGate ainda em OpcoesScreen'); if(/store\.optionsProposta\(/.test(c))f('optionsProposta ainda em OpcoesScreen'); if(!/opcoesPorTicker=\{opcoesPorTicker\}/.test(c))f('fan-out nao desce para SubAbaOperar'); const h=sem(require('fs').readFileSync('web/src/opcoes/useOpcoesPropostas.js','utf8')); if(!/if \(gate && gate\.liquida\)/.test(h))f('a guarda de liquidez nao esta no hook'); if(!/store\.optionsProposta\(t, true\)/.test(h))f('hook nao usa multiperna=true'); console.log(process.exitCode?'':'ok fold-in D-04a')"</automated>
    <automated>npx vite build</automated>
  </verify>
  <done>Os dois `useEffect` e os dois `useState` saíram de `SubAbaOperar`; `store.optionsGate(`/`store.optionsProposta(` não aparecem mais em `OpcoesScreen.jsx`; `SubAbaOperar` lê `opcoesPorTicker[ticker]`; as 3 premissas confirmadas por leitura e registradas; `vite build` verde.</done>
</task>

<task type="auto">
  <name>Task 3: Reapontar os 3 guardiões (regra 3 com nota datada da queda 1→0)</name>
  <files>web/tests/test_opcoes_subabas_ui.mjs, web/tests/test_opcoes_analisar_ui.mjs, web/tests/test_opcoes_mcp_aba_ui.mjs</files>
  <read_first>
    - web/tests/test_opcoes_subabas_ui.mjs — o cabeçalho (itens 3 e 4 da lista de defeitos que ele reprova) e as linhas 118-140.
    - web/tests/test_opcoes_analisar_ui.mjs — itens 5 e 13 e a asserção da faixa de 63 (~528).
    - web/tests/test_opcoes_mcp_aba_ui.mjs — linhas 120-140 (a cadeia de estados e o `iDados`).
    - .planning/todos/pending/subaba-operar-fetch-redundante-gate-proposta.md — a seção "Guardião que prende a decisão".
  </read_first>
  <action>
Censo final da fase: `grep -rln "OpcoesScreen.jsx" web/tests/*.mjs` e
`grep -n "opcoesAnalisarTitulo\|opcoesLeituraTitulo\|optionsGate\|optionsProposta\|setProp(null)\|behavior\.\|razaoGanhoPerda\|painel" web/tests/*.mjs`.
Conferir também os guardiões que fatiam `SubAbaOperar`
(`test_opcoes_proposta_ui.mjs`, `test_opcoes_collar_ui.mjs`,
`test_opcoes_multi_candidato_ui.mjs`, `test_faixa_liquidez_ui.mjs`,
`test_fase22_componentes_compartilhados.mjs`): eles continuam válidos porque a
fatia existe, mas qualquer asserção que dependa de `gate`/`prop` como ESTADO
LOCAL precisa passar a olhar a prop. Qualquer arquivo afetado fora da lista
entra no escopo e vai ao SUMMARY.

1. `test_opcoes_subabas_ui.mjs` regra 3 — nota datada
   `2026-09-19, Fase 33 (33-05), fold-in D-04a`: a contagem esperada em
   `OpcoesScreen.jsx` cai de 1 para 0 porque a busca MIGROU DE DONO, não porque
   desapareceu. A garantia "uma fonte só" fica MAIS forte, não mais fraca, e
   passa a ser medida assim: `store.optionsGate(` e `store.optionsProposta(`
   aparecem exatamente 1× cada somando TODOS os arquivos de `web/src/opcoes/`
   (varredura `readdirSync`, padrão das linhas 100-116), e 0× em
   `OpcoesScreen.jsx`. A ocorrência legítima é a de `useOpcoesPropostas.js`.
2. `test_opcoes_subabas_ui.mjs` regra 4 — a guarda "proposta só sob
   `gate.liquida`" deixa de existir na fatia de `SubAbaOperar` e passa a ser
   medida em `useOpcoesPropostas.js`: `if (gate && gate.liquida)` ANTES de
   `store.optionsProposta(`, no mesmo encadeamento. NÃO deletar a regra —
   reapontar. Acrescentar asserção nova de contrapartida: `SubAbaOperar` não
   contém `store.options` nenhum (nem gate, nem proposta), o que trava o
   retorno do fetch redundante.
3. `test_opcoes_analisar_ui.mjs` — item 5: a fatia "Analisar" passa a ser o
   arquivo `SecaoAnalisar.jsx` inteiro, com os quatro estados na mesma ordem e
   `<ErroDoMcp erro=` presente; a asserção de ORDEM entre seções passa a medir,
   em `OpcoesScreen.jsx`, `<SecaoAnalisar` < `<SecaoComparar` < `<SecaoSetups`.
   Item 13: `razao={proposta.dados.razaoGanhoPerda}` passa a ser medido em
   `SecaoAnalisar.jsx`; a garantia "nas DUAS seções" agora soma
   `SecaoAnalisar.jsx` + `SecaoComparar.jsx`. Asserção da faixa de 63: `const faixa = `
   e `valor={faixa(behavior.range_63_sessions)}` passam a ser exigidos onde o
   helper e o uso ficaram (`uiOpcoes.jsx` e `SecaoAnalisar.jsx`) — a garantia é
   "UM travessão, nunca '— – —'", e ela não pode virar vácua por troca de
   arquivo.
4. `test_opcoes_mcp_aba_ui.mjs` seção 2 — esta é a correção mais importante
   deste plano, e ela NÃO é disparada por falha: o guardião continuaria PASSANDO
   porque `cp.opcoesLeituraTitulo` também existe em `blocoLeituraDoServico`, que
   não se move. Trocar `iDados` de `indexOf("cp.opcoesLeituraTitulo", iRender)`
   para `indexOf("<SecaoAnalisar", iRender)`, com nota datada explicando
   exatamente isso: o marcador antigo passou a ser ambíguo (duas ocorrências,
   propósitos diferentes) e o guardião mediria o convite de leitura paga
   pensando estar medindo o ramo de dados. A ordem exigida
   (`carregando → erro → vazio com motivo → dados`) não muda.

Commit único cobrindo as Tasks 1, 2 e 3:
`refactor(33-05): extrai SecaoAnalisar e faz SubAbaOperar ler o fan-out (D-04a)`.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <done>Regra 3 com nota datada e medida por varredura (1× na pasta, 0× no orquestrador); regra 4 reapontada para o hook + asserção nova contra o retorno do fetch; itens 5/13/faixa de Analisar reapontados; `iDados` de `mcp_aba` desambiguado com nota; suíte canônica exit 0; um commit só.</done>
</task>

<task type="auto">
  <name>Task 4: Prova negativa (D-04a, CVM em SecaoAnalisar, faixa) + baseline final da fase</name>
  <files>web/src/opcoes/SecaoAnalisar.jsx, web/src/opcoes/OpcoesScreen.jsx</files>
  <read_first>
    - web/tests/test_opcoes_subabas_ui.mjs — regras 3 e 4 já reescritas.
    - web/tests/test_opcoes_analisar_ui.mjs — itens 5/13 e a faixa, já reescritos.
  </read_first>
  <action>
Uma injeção por vez, rodando só o guardião do caso, exigindo exit 1 com a
asserção certa, revertendo com `git checkout --` e conferindo `git diff` vazio.
Comando e saída real de cada uma no SUMMARY:

1. Reintroduzir um `store.optionsGate(ticker)` dentro de `SubAbaOperar` — a
   regra 3 reescrita tem de reprovar por CONTAGEM NA PASTA (2 em vez de 1), e a
   asserção nova da regra 4 tem de reprovar por existir `store.options` na
   fatia. Esta é a prova de que a queda 1→0 não afrouxou nada.
2. Remover `if (gate && gate.liquida)` de `useOpcoesPropostas.js` — a regra 4
   reapontada tem de reprovar. Se não reprovar, a garantia se perdeu na
   migração e o guardião precisa ser corrigido antes de seguir.
3. Renderizar `{proposta.dados.manchete}` dentro de `SecaoAnalisar.jsx` — o
   guardrail CVM generalizado no 33-02 tem de reprovar nomeando o arquivo. É a
   verificação de REORG-06 por medição, não por analogia (Pitfall 4: "se a suíte
   passa depois de introduzir esse código, isso NÃO prova que o guardrail está
   respeitado").
4. Trocar `valor={faixa(behavior.range_63_sessions)}` por dois extremos crus
   (`{behavior.range_63_sessions.lowest} – {behavior.range_63_sessions.highest}`)
   — a asserção da faixa tem de reprovar ("— – —": travessão travestido de
   faixa).
5. Multiplicar um valor em reais por `lote` dentro de `SecaoAnalisar.jsx` — a
   varredura por diretório convertida no 33-01 tem de reprovar nomeando o
   arquivo novo. Prova de que a conversão da allowlist fixa valeu.

Fechar a FASE: `bash scripts/executar.sh --testes` (as duas suítes) e
`npx vite build`. Registrar no SUMMARY a tabela final: baseline do início da
fase (33-01) × contagem agora, com o delta de `.mjs` explicado item por item.
Registrar também, explicitamente: os 5 jobs extraídos, `App.jsx` intocado em
toda a fase (`git diff --stat origin/main...HEAD -- web/src/App.jsx` vazio) e
que NADA foi publicado.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
    <automated>git status --porcelain && git diff --stat origin/main...HEAD -- web/src/App.jsx</automated>
  </verify>
  <done>5 injeções com saída real registrada, todas revertidas; suíte canônica exit 0 com a tabela baseline×final no SUMMARY; `vite build` verde; `App.jsx` sem uma linha de diff em toda a fase; nada publicado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | JSX já publicado muda de arquivo, e um par de chamadas a rotas INTERNAS de custo zero (`/api/options/gate`, `/api/options/proposta`) passa a ter um dono só em vez de dois. Nenhuma rota nova, nenhum input novo, nenhuma permissão nova, tráfego líquido MENOR. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-15 | Tampering | fonte de `gate`/`proposta` (D-04a) | mitigate | Duas buscas da mesma coisa divergem na próxima correção feita só numa delas — é o defeito que a regra 3 sempre existiu para impedir. A migração torna a garantia mais forte (1× na pasta inteira, 0× no orquestrador) e a injeção 1 da Task 4 prova. |
| T-33-16 | Denial of Service (cota do usuário) | `useOpcoesPropostas` | accept | As duas rotas são internas e de custo ZERO no cap do MCP (ADR-027 §3.3, já declarado por escrito nas Emendas 1 e 2); o fold-in REDUZ o número de requisições por sessão. Nenhum efeito novo, nenhum disparo em mount que não existisse. |
| T-33-17 | — (compliance CVM) | `SecaoAnalisar.jsx` | mitigate | A seção exibe proposta do motor e é o candidato mais óbvio a recompor manchete. Coberta pelo guardrail generalizado no 33-02; a injeção 3 da Task 4 mede a cobertura em vez de presumi-la. Controle REGULATÓRIO, não de segurança — e não pode regredir por mudança de topologia. |
| T-33-18 | — (princípio 4) | `faixa`, cascata de proposta | mitigate | Travessão travestido de faixa e vazio antes da medição são as duas formas de a tela afirmar o que ninguém mediu; injeções 4 e (no 33-04) 3 provam a cobertura. |
</threat_model>

<verification>
1. `bash scripts/executar.sh --testes` — pytest + `.mjs`, exit 0. `scripts/test.sh` sozinho não conta.
2. `npx vite build` verde.
3. `grep -nE "store\.|useState" web/src/opcoes/SecaoAnalisar.jsx` → só o `useState` de `painel`.
4. `grep -cE "store\.options(Gate|Proposta)\(" web/src/opcoes/OpcoesScreen.jsx` → 0.
5. As 5 injeções da Task 4 com saída real no SUMMARY.
6. `git diff --stat origin/main...HEAD -- web/src/App.jsx` vazio (REORG-04 em toda a fase).
7. Conferência final da fase: os 5 arquivos `Secao*.jsx` existem, nenhum importa `App.jsx`, nenhum instancia hook de dados, nenhum redeclara `ticker`/`tese`/`vencimento`/`lote`.
</verification>

<success_criteria>
- `SecaoAnalisar.jsx` existe com `painel` como único estado local; os 5 jobs de REORG-01 estão extraídos.
- D-04a fechado: `SubAbaOperar` sem os dois `useEffect`, lendo `opcoesPorTicker`; regra 3 com nota datada e garantia medida por varredura.
- `iDados` de `test_opcoes_mcp_aba_ui.mjs` desambiguado — o guardião passou a medir o que diz medir.
- REORG-06 verificado por injeção em `SecaoAnalisar.jsx`, não por analogia.
- Suíte canônica verde com a tabela baseline×final no SUMMARY; `vite build` verde; `App.jsx` intocado em toda a fase.
- NÃO publicado: sem `bump.sh`, sem `publicar-web.sh`, sem push. A Fase 34 só começa com esta suíte verde (dependência bloqueante registrada no ROADMAP).
</success_criteria>

<output>
Create `.planning/phases/33-extra-o-dos-5-jobs-em-componentes-pr-prios/33-05-SUMMARY.md` when done, com: as 3 premissas de D-04a confirmadas por leitura (`arquivo:linha`), as 5 injeções (comando + saída real), a tabela baseline(33-01)×final da suíte com o delta de `.mjs` explicado, e a confirmação de que `App.jsx` não recebeu uma linha de diff na fase.
</output>
