---
phase: 33-extra-o-dos-5-jobs-em-componentes-pr-prios
plan: 03
type: execute
wave: 3
depends_on: [33-02]
files_modified:
  - web/src/opcoes/SecaoSetups.jsx
  - web/src/opcoes/uiOpcoes.jsx
  - web/src/opcoes/OpcoesScreen.jsx
  - web/tests/test_opcoes_mcp_aba_ui.mjs
  - web/tests/test_opcoes_vigias_ui.mjs
  - web/tests/test_opcoes_criar_setup_ui.mjs
  - web/tests/test_opcoes_custo_declarado.mjs
  - web/tests/test_opcoes_analisar_ui.mjs
autonomous: true
requirements: [REORG-01, REORG-02, REORG-03, REORG-05]

must_haves:
  truths:
    - "D-01: o job 'gerenciar/criar setups salvos' vive em web/src/opcoes/SecaoSetups.jsx, envolvendo o CriarSetup.jsx que já existia sem renomeá-lo nem reescrevê-lo"
    - "D-03: a listagem 'SETUPS GRAVADOS' e o bloco 'CRIAR UM SETUP' aparecem na mesma posição, com os mesmos dados e o mesmo custo declarado — nada de novo, nada removido"
    - "REORG-02: 'sem avaliação hoje' (naoAvaliado) continua sendo um estado próprio, nunca fundido com a cascata de erro do fetch — ausência de medição não é medição de ausência"
    - "REORG-03: SecaoSetups não chama store.* nem instancia hook — recebe leitura/setups, grafico e os 4 callbacks de setup por prop, e a tabela de custos por prop"
    - "D-02: a regra que cruza a tabela CUSTO_DA_ACAO com os controles passa a varrer OpcoesScreen.jsx + todos os Secao*.jsx, em vez de exigir que o controle viva no arquivo do orquestrador"
  artifacts:
    - path: "web/src/opcoes/SecaoSetups.jsx"
      provides: "Job 5 — listagem de setups gravados + porta de criação"
      contains: "export default function SecaoSetups"
      min_lines: 110
  key_links:
    - from: "web/src/opcoes/OpcoesScreen.jsx"
      to: "web/src/opcoes/SecaoSetups.jsx"
      via: "<SecaoSetups .../> no ramo DADOS, na posição exata de hoje (depois de COMPARAR OS VENCIMENTOS)"
      pattern: "<SecaoSetups"
    - from: "web/src/opcoes/SecaoSetups.jsx"
      to: "web/src/opcoes/CriarSetup.jsx"
      via: "import default CriarSetup + named BotaoDesativar, com custos por prop"
      pattern: "from \"./CriarSetup.jsx\""
---

<objective>
Extrair o job 5 ("gerenciar/criar setups salvos") de `OpcoesScreen.jsx` para
`web/src/opcoes/SecaoSetups.jsx`: a listagem "SETUPS GRAVADOS" (hoje inline) e
a porta "CRIAR UM SETUP" (que só envolve o `CriarSetup.jsx` já existente),
preservando comportamento, dados, ordem e custo declarado.

Purpose: passo 3 de D-03. É o primeiro dos três jobs que vivem DENTRO do ramo
"DADOS" da cascata compartilhada, então é aqui que se fixa a regra de como uma
seção sai de dentro daquela cascata sem levá-la embora.
Output: 1 componente novo, 5 guardiões reapontados (incluindo a migração do
cruzamento de custo para varredura de diretório), suíte canônica verde.

**Escopo que este plano NÃO tem:** renomear ou alterar `CriarSetup.jsx`
(D-01 é explícito: os componentes que já existem não são renomeados), tirar a
cascata `carregando→erro→vazio→dados` de `OpcoesScreen.jsx` (ela FICA no
orquestrador — mover a cascata é redesenho de navegação, Fase 34), tocar
`App.jsx`, e qualquer mudança visual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/33-extra-o-dos-5-jobs-em-componentes-pr-prios/33-CONTEXT.md
@.planning/phases/33-extra-o-dos-5-jobs-em-componentes-pr-prios/33-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/phases/33-extra-o-dos-5-jobs-em-componentes-pr-prios/33-02-SUMMARY.md

<interfaces>
Medido no planejamento (2026-09-19) — reconfirmar as linhas por Grep antes de
editar, elas deslocam a cada plano desta fase:

Fonte a extrair, `OpcoesScreen.jsx`:
- Listagem: a partir do `<Kicker>{cp.opcoesSetupsTitulo || "SETUPS GRAVADOS"}</Kicker>`
  (hoje linha 1224) até o fim do bloco de criação (hoje ~1376), incluindo:
  o aviso de `naoAvaliado`, o ramo `setups.length === 0` com `cp.opcoesSemSetups`,
  o `map` dos setups (avaliação, conditions, backtest, botão de gráfico,
  `<BotaoDesativar>`), e o bloco `podeCriarSetup ? (<Kicker>{cp.opcoesCriarTitulo}
  </Kicker><CriarSetup .../>) : null`.
- A regra de chave, que NÃO pode ser reimplementada nem deduzida:
  `const chave = s.nomeNoServico || s.name;` — o que VIAJA ao serviço usa
  `nomeNoServico`; o que a pessoa LÊ usa `name`. `abrirGrafico(chave)`,
  `grafico.setup === chave`, `nome={chave}` e `nomeVisivel={s.name}` no
  `<BotaoDesativar>`. Deduzir um do outro em JavaScript recriaria o prefixo do
  hash da conta — foi o defeito real do 27-02.
- `podeCriarSetup` é derivado do RBAC no orquestrador
  (`permissoes.includes("opcoes.criar_setup")`, com `permissoes` vindo de
  `ctx.authUser.permissions`) — a derivação FICA em `OpcoesScreen.jsx` e desce
  por prop booleana. Não reimplementar checagem de permissão na seção.
- `CUSTO_DA_ACAO` fica em `OpcoesScreen.jsx` e desce como `custos` — hoje já
  desce assim para `<CriarSetup>` e `<BotaoDesativar>`.
- `BotaoDesativar` é export NOMEADO de `CriarSetup.jsx`
  (`CriarSetup.jsx:525`), importado hoje em `OpcoesScreen.jsx:37` —
  o import migra junto com o JSX.

Semântica que não pode ser fundida (comentário em OpcoesScreen.jsx ~1264):
`naoAvaliado` NÃO é erro. É "o serviço não avaliou hoje", com texto próprio.
Fundir com a cascata de erro do fetch afirmaria uma leitura negativa sobre uma
medição que ninguém fez (princípio 4 do CLAUDE.md).

Guardiões que ancoram neste bloco (medido por grep, 2026-09-19):
- `test_opcoes_vigias_ui.mjs` seção 4b (linhas ~139-148) — TODAS as asserções da
  regra de chave acima. Mora no guardião dos vigias por acidente histórico
  (achado de execução do 27-02); migra de fonte aqui.
- `test_opcoes_criar_setup_ui.mjs` linhas ~75-88 — `podeCriarSetup`,
  `{podeCriarSetup ? (...<CriarSetup` e `...<BotaoDesativar` por proximidade.
- `test_opcoes_custo_declarado.mjs` linhas ~199-210 e 245-246 — o cruzamento
  "todo controle da tabela declara o custo com `cp.opcoesCustoChamadas`" varre
  linhas de `OpcoesScreen.jsx` + `CriarSetup.jsx`, e exige
  `custos={CUSTO_DA_ACAO}` ao menos 2× em `OpcoesScreen.jsx`.
- `test_opcoes_analisar_ui.mjs` linhas ~141-146 — `iSetups` é
  `indexOf("cp.opcoesSetupsTitulo")`, usado como limite superior da fatia de
  "COMPARAR OS VENCIMENTOS".
- `test_opcoes_mcp_aba_ui.mjs` — `abrirGrafico` e `opcoesSetupsTitulo` (este
  último só na lista de paridade de copy, que não se move).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar SecaoSetups.jsx e religar OpcoesScreen.jsx</name>
  <files>web/src/opcoes/SecaoSetups.jsx, web/src/opcoes/uiOpcoes.jsx, web/src/opcoes/OpcoesScreen.jsx</files>
  <read_first>
    - web/src/opcoes/OpcoesScreen.jsx — faixas 1220-1385 (listagem + criação) e 900-915 (o ramo DADOS onde a seção vai ser renderizada). Reconfirmar por Grep de `cp.opcoesSetupsTitulo` e `cp.opcoesCriarTitulo`.
    - web/src/opcoes/CriarSetup.jsx — o comentário de topo e a assinatura de `CriarSetup` e de `BotaoDesativar` (linha ~525). É o analog primário: "componente sem fonte de dado própria".
    - web/src/opcoes/SecaoVigias.jsx e web/src/opcoes/uiOpcoes.jsx — o padrão já estabelecido nos planos 33-01/33-02 (header de proveniência, tokens locais, o que importar em vez de redeclarar).
    - .planning/phases/33-extra-o-dos-5-jobs-em-componentes-pr-prios/33-PATTERNS.md — seção "web/src/opcoes/SecaoSetups.jsx".
  </read_first>
  <action>
Criar `web/src/opcoes/SecaoSetups.jsx`, `export default function SecaoSetups`,
recebendo por prop: `ticker`, `setups`, `naoAvaliado`, `grafico`,
`abrirGrafico`, `fecharGrafico`, `setupNovo`, `compilarSetup`,
`confirmarSetup`, `desativarSetup`, `podeCriarSetup`, `custos`, `cp`, `ctx`
(apenas se o painel de gráfico precisar de `ctx.PriceChart`, como
`SetupChart.jsx` já faz — se não precisar, não receber). Mover o JSX verbatim,
incluindo os comentários de decisão das Fases 27/24 que explicam a regra de
chave e o estado `naoAvaliado`.

Regras que a extração NÃO pode afrouxar:
- `const chave = s.nomeNoServico || s.name;` vai verbatim para dentro da seção;
  `abrirGrafico(chave)`, `grafico.setup === chave`, `nome={chave}`,
  `nomeVisivel={s.name}` e o nome exibido `{s.name}` continuam exatamente como
  estão. Nenhuma derivação nova, nenhum prefixo recomposto em JS.
- O custo do botão de compilar/desativar continua saindo de `custos[...]`
  (prop), com `cp.opcoesCustoChamadas`, nunca um número literal.
- `naoAvaliado` mantém aviso próprio, separado do ramo de erro.
- Se a seção precisar de `Kicker`/`Aviso`/`ErroDoMcp`, importar de
  `./uiOpcoes.jsx`. Se precisar de uma constante de estilo ou componente
  compartilhado que ainda está em `OpcoesScreen.jsx` e que o orquestrador AINDA
  usa (ex.: `CAIXA`, `Linha`), mover essa peça para `uiOpcoes.jsx` e importar
  dos dois lados — nunca duplicar. Constante de estilo usada só por esta seção
  pode ser espelho declarado local (mesma regra fixada no 33-01).

Em `OpcoesScreen.jsx`: apagar o JSX movido, manter as derivações
(`setups`, `naoAvaliado`, `podeCriarSetup`, `grafico` e os 4 callbacks vêm do
hook/RBAC e continuam ali) e renderizar `<SecaoSetups ... />` na POSIÇÃO EXATA
de hoje: dentro do ramo "4. DADOS" da cascata, depois do bloco de comparação de
vencimentos. O aviso `cp.opcoesSemSetups` que vive no ramo "3. VAZIO" da
cascata (hoje ~904-906) FICA onde está — ele é medido pela leitura e é o que
fala quando não há leitura pedida; não é duplicata da mensagem interna da
seção. Registrar isso em comentário datado, senão o próximo leitor vai "limpar"
uma das duas.

NÃO commitar: commit único no fim da Task 2.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('web/src/opcoes/SecaoSetups.jsx','utf8'); const o=require('fs').readFileSync('web/src/opcoes/OpcoesScreen.jsx','utf8'); const f=(m)=>{console.log('FALHOU '+m);process.exitCode=1}; if(/store\.|api\./.test(s))f('SecaoSetups chama store/api'); if(/useOpcoesMcp|useOpcoesPropostas|useCuradoria/.test(s))f('instancia hook'); if(/App\.jsx/.test(s))f('importa App.jsx'); if(/\.manchete/.test(s))f('renderiza manchete (REORG-06)'); if(/permissions|opcoes\.criar_setup/.test(s))f('reimplementa checagem de permissao'); if(!/const chave = s\.nomeNoServico \|\| s\.name;/.test(s))f('regra de chave do armazem nao migrou verbatim'); if(!/nomeVisivel=\{s\.name\}/.test(s)||!/nome=\{chave\}/.test(s))f('BotaoDesativar perdeu a separacao chave/nome'); if(/nome=\{s\.name\}/.test(s))f('nome da pessoa viajando como chave do armazem'); if(!/<SecaoSetups/.test(o))f('OpcoesScreen nao renderiza SecaoSetups'); if(!/const CUSTO_DA_ACAO/.test(o))f('CUSTO_DA_ACAO saiu do orquestrador'); console.log(process.exitCode?'':'ok estrutura de SecaoSetups')"</automated>
    <automated>npx vite build</automated>
  </verify>
  <done>`SecaoSetups.jsx` existe com a listagem e a porta de criação; `OpcoesScreen.jsx` renderiza `<SecaoSetups` na posição de hoje dentro do ramo DADOS; regra de chave verbatim; nenhuma duplicação de helper; `vite build` verde.</done>
</task>

<task type="auto">
  <name>Task 2: Reapontar os 5 guardiões e migrar o cruzamento de custo para varredura</name>
  <files>web/tests/test_opcoes_vigias_ui.mjs, web/tests/test_opcoes_criar_setup_ui.mjs, web/tests/test_opcoes_custo_declarado.mjs, web/tests/test_opcoes_analisar_ui.mjs, web/tests/test_opcoes_mcp_aba_ui.mjs</files>
  <read_first>
    - web/tests/test_opcoes_vigias_ui.mjs — seção 4b (linhas ~128-148) com o comentário que explica por que ela existe (achado de execução do 27-02: 422 `setup_desconhecido` nos dois botões, em produção).
    - web/tests/test_opcoes_custo_declarado.mjs — linhas 195-247 (o cruzamento de custo e o `custos={CUSTO_DA_ACAO}`).
    - web/tests/test_opcoes_criar_setup_ui.mjs — linhas 70-90.
    - web/tests/test_opcoes_analisar_ui.mjs — linhas 140-150 (a ordem dos jobs 3→4→5) e 530-540 (o precedente de varredura por diretório no próprio arquivo).
    - .planning/research/PITFALLS.md — Pitfall 3.
  </read_first>
  <action>
Censo: `grep -n "opcoesSetupsTitulo\|opcoesCriarTitulo\|CriarSetup\|BotaoDesativar\|abrirGrafico\|nomeNoServico\|podeCriarSetup\|CUSTO_DA_ACAO" web/tests/*.mjs`.
Arquivo fora da lista abaixo entra no escopo e vai ao SUMMARY.

1. `test_opcoes_vigias_ui.mjs` seção 4b — trocar a fonte lida de
   `OpcoesScreen.jsx` para `SecaoSetups.jsx`, mantendo TODAS as cinco asserções
   e o comentário de contexto. Nota datada dizendo que a regra continua sendo a
   mesma frase ("o que viaja usa `nomeNoServico`, o que a pessoa lê usa
   `name`") e que só o arquivo mudou. Cuidado com o índice `-1`: exigir
   `>= 0` antes de qualquer fatiamento.
2. `test_opcoes_criar_setup_ui.mjs` — `podeCriarSetup` continua exigido
   DECLARADO em `OpcoesScreen.jsx` (é derivação de RBAC e fica no
   orquestrador); o gate visual (`{podeCriarSetup ? (...<CriarSetup`) e
   `...<BotaoDesativar`) passa a ser medido em `SecaoSetups.jsx`, agora sobre a
   prop. A garantia "a porta de criação só existe com a permissão" não muda.
3. `test_opcoes_custo_declarado.mjs` — migrar o cruzamento para varredura: as
   `linhasDeCusto` passam a ser coletadas de `OpcoesScreen.jsx` +
   `CriarSetup.jsx` + todos os `Secao*.jsx` de `web/src/opcoes/`
   (`readdirSync`, padrão de `test_opcoes_subabas_ui.mjs:100-116`), preservando
   a sanidade "há pelo menos tantas declarações quanto ações na tabela". A
   asserção `custos={CUSTO_DA_ACAO}` ao menos 2× passa a somar
   `OpcoesScreen.jsx` + `Secao*.jsx` — a garantia é "a tabela chega aos
   controles por PROP", não "a linha mora no arquivo do orquestrador". A leitura
   da tabela `CUSTO_DA_ACAO` e o cruzamento com os `_cap_check(uid, N)` do
   Python continuam saindo de `OpcoesScreen.jsx`, que é onde a tabela vive —
   NÃO mover a tabela.
4. `test_opcoes_analisar_ui.mjs` linhas 141-146 — `iSetups` passa de
   `indexOf("cp.opcoesSetupsTitulo")` para `indexOf("<SecaoSetups")` em
   `OpcoesScreen.jsx`. A relação de ordem `iAnalisar < iPossib < iSetups` fica;
   os outros dois marcadores só migram nos planos 33-04/33-05, então nesta
   rodada conviverão um tag e dois `cp.X` — aceitável e registrado em nota, já
   que a asserção mede POSIÇÃO no mesmo arquivo.
5. `test_opcoes_mcp_aba_ui.mjs` — conferir se alguma asserção fatia
   `abrirGrafico` a partir de `OpcoesScreen.jsx`; se sim, reapontar para
   `SecaoSetups.jsx`. A lista `CHAVES` de paridade de copy não se move.

Commit único com a Task 1: `refactor(33-03): extrai SecaoSetups de OpcoesScreen`.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <done>Os 5 guardiões medem a topologia nova; o cruzamento de custo virou varredura de diretório com sanidade preservada; nenhuma asserção deletada ou enfraquecida; suíte canônica exit 0; um commit só.</done>
</task>

<task type="auto">
  <name>Task 3: Prova negativa (regra de chave + custo + permissão) e baseline</name>
  <files>web/src/opcoes/SecaoSetups.jsx</files>
  <read_first>
    - web/tests/test_opcoes_vigias_ui.mjs — seção 4b já reapontada.
    - web/tests/test_opcoes_custo_declarado.mjs — o cruzamento já convertido em varredura.
  </read_first>
  <action>
Uma injeção por vez em `SecaoSetups.jsx`, rodando só o guardião do caso,
confirmando exit 1 com a asserção certa, revertendo com `git checkout --` e
conferindo `git diff` vazio. Comando e saída real de cada uma no SUMMARY:

1. Trocar `abrirGrafico(chave)` por `abrirGrafico(s.name)` — a seção 4b tem de
   reprovar. É exatamente o defeito que produziu 422 `setup_desconhecido` em
   produção no 27-02; se o guardião não reprovar depois de ter mudado de
   arquivo, ele estava inerte.
2. Trocar `nome={chave}` por `nome={s.name}` no `<BotaoDesativar>` — tem de
   reprovar.
3. Exibir `{s.nomeNoServico}` na tela (o hash da conta como rótulo) — se
   nenhum guardião reprovar, criar a asserção que proíbe `nomeNoServico` em
   texto renderizado de `web/src/opcoes/` e repetir. É o dano medido pela
   injeção nº 4 do 27-01.
4. Trocar o custo do botão por um literal `2` — o cruzamento convertido tem de
   reprovar.
5. Renderizar `<CriarSetup>` fora do gate `podeCriarSetup` — tem de reprovar.

Fechar com `bash scripts/executar.sh --testes` e `npx vite build`, números no
SUMMARY (baseline de entrada do 33-04).
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
    <automated>git status --porcelain</automated>
  </verify>
  <done>5 injeções com saída real registrada (a 3 gerou asserção nova se não existia), todas revertidas; suíte canônica exit 0 com números no SUMMARY; `vite build` verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | JSX já publicado muda de arquivo. Nenhuma rota, input, permissão ou segredo novo. O que cruza fronteira aqui é o NOME que viaja ao serviço MCP — e ele continua saindo exatamente da mesma expressão. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-08 | Information Disclosure | `SecaoSetups.jsx` (nome do setup) | mitigate | `nomeNoServico` carrega `sha256(uid)[:8]` — é endereço no armazém COMPARTILHADO do serviço, não rótulo. Nunca renderizado; a asserção migra de arquivo junto com o código, e a injeção 3 da Task 3 prova que ela reprova de verdade. |
| T-33-09 | Elevation of Privilege | gate `podeCriarSetup` | mitigate | A derivação de permissão FICA no orquestrador e desce como booleano; a seção não reimplementa checagem de RBAC. O gate real é do backend (`require_permission("opcoes.criar_setup")`) — o gate visual nunca foi a defesa, e a injeção 5 garante que ele não desapareça em silêncio. |
| T-33-10 | Spoofing | chave que viaja ao serviço MCP | mitigate | Confundir `name` com `nomeNoServico` manda ao armazém compartilhado um nome sem prefixo de conta — 422 em produção no melhor caso. Injeções 1 e 2 provam a cobertura. |
| T-33-11 | — (compliance) | custo declarado no controle | mitigate | ADR-027 §3.3: custo de MCP aparece ANTES do clique, lido da tabela espelhada no `_cap_check` do backend. É controle de transparência de custo, não de segurança; o cruzamento virou varredura para não ficar cego a arquivo novo. |
</threat_model>

<verification>
1. `bash scripts/executar.sh --testes` — pytest + `.mjs`, exit 0.
2. `npx vite build` verde.
3. `grep -n "store\.\|useOpcoesMcp\|permissions" web/src/opcoes/SecaoSetups.jsx` → zero.
4. As 5 injeções da Task 3 com saída real no SUMMARY.
5. Conferir à mão que os DOIS avisos de "nenhum setup" continuam existindo e em ramos diferentes (o da cascata VAZIO em `OpcoesScreen.jsx`, o interno em `SecaoSetups.jsx`) — nenhum foi "limpado" como duplicata.
6. `git diff --stat web/src/App.jsx` vazio.
</verification>

<success_criteria>
- `SecaoSetups.jsx` existe, envolve `CriarSetup.jsx` sem renomeá-lo, e preserva a regra de chave do armazém verbatim.
- Ordem visível idêntica; `naoAvaliado` segue como estado próprio.
- 5 guardiões reapontados; cruzamento de custo virou varredura de diretório; nenhuma garantia afrouxada.
- Suíte canônica e `vite build` verdes; `App.jsx` intocado.
- NÃO publicado: sem `bump.sh`, sem `publicar-web.sh`, sem push.
</success_criteria>

<output>
Create `.planning/phases/33-extra-o-dos-5-jobs-em-componentes-pr-prios/33-03-SUMMARY.md` when done, com as 5 injeções (comando + saída real), os números da suíte e qualquer guardião novo achado pelo censo.
</output>
