---
phase: 12-limites-do-plano-gratuito-ativos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/plan.py
  - server/tests/test_fase3_gate_plano.py
  - server/tests/test_fase5_gate_mensal.py
autonomous: true
requirements: [CAP-01, CAP-02, CAP-04, CAP-07]

must_haves:
  truths:
    - "D-01: plan.PLAN_FREE['max_watchlist'] vale 10 e plan.PLAN_FREE['max_analyses_per_month'] vale 30"
    - "D-01: plan.PLAN_PRO continua com os dois limites None (ilimitado)"
    - "D-05: A recusa de can_add_ticker declara só fato+motivo, sem nenhuma palavra de CTA/upgrade"
    - "Os dois guardiões pré-existentes que travavam 'nenhum limite comercial ativado' foram ATUALIZADOS com nota de reversão deliberada, não apagados"
    - "A suíte pytest inteira continua verde com os limites ligados"
  artifacts:
    - path: "server/app/plan.py"
      provides: "Limites reais do PLAN_FREE + copy de recusa sem CTA"
      contains: "max_watchlist"
    - path: "server/tests/test_fase3_gate_plano.py"
      provides: "Guardião (g) invertido: agora trava que os limites do FREE estão ATIVOS"
      contains: "PLAN_FREE"
    - path: "server/tests/test_fase5_gate_mensal.py"
      provides: "Guardião de /api/ai/quota atualizado: monthLimit deixa de ser None para conta logada free"
      contains: "monthLimit"
  key_links:
    - from: "server/app/plan.py::can_add_ticker"
      to: "server/app/plan.py::can_analyze"
      via: "mesmo padrão de frase de recusa (fato+motivo, sem CTA)"
      pattern: "Voce atingiu o limite de"
    - from: "server/tests/test_fase3_gate_plano.py"
      to: "server/app/plan.py::PLAN_FREE"
      via: "assert direto nos números ativados"
      pattern: "PLAN_FREE\\[.max_watchlist.\\] == 10"
---

<objective>
Ligar os números reais do plano gratuito em `server/app/plan.py` (D-01) e
tirar o tom de CTA da mensagem de recusa de `can_add_ticker` (D-05, CAP-07),
atualizando os guardiões de teste pré-existentes que hoje travam exatamente o
estado oposto ("nenhum limite comercial ativado").

Purpose: os hooks `can_add_ticker`/`can_analyze` e toda a fiação de gate já
existem e já leem contagem real (`_gate_analise` → `metering.month_used`,
`watchlist_add` → `len(watchlist)`). O que falta é o NÚMERO. Sem este plano,
nenhum dos dois gates jamais nega — a comparação é sempre contra `None`.

Output: `PLAN_FREE` com `max_watchlist=10`/`max_analyses_per_month=30`,
`PLAN_PRO` intocado (ilimitado), copy de recusa conforme princípio 8 do
CLAUDE.md, e os dois guardiões de fronteira atualizados com nota de reversão
deliberada (guardrail "guardiões de teste não se apagam").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-limites-do-plano-gratuito-ativos/12-CONTEXT.md
@.planning/phases/12-limites-do-plano-gratuito-ativos/12-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contratos que o executor precisa conhecer. NÃO explorar o codebase para achá-los. -->

server/app/plan.py (estado ATUAL, antes deste plano):
- `PLAN_FREE = {"id": "free", "max_watchlist": None, "max_analyses_per_month": None, "byok_required": False}`
- `PLAN_PRO  = {"id": "pro",  "max_watchlist": None, "max_analyses_per_month": None, "byok_required": False}`
- `ACTIVE_PLAN = PLAN_FREE` (fallback de escopo anônimo — D-06: sem mudança)
- `current_plan(user) -> dict` — resolve por `users.plan`, fail-closed para FREE
- `can_add_ticker(current_count: int, plan: dict|None) -> (bool, str|None)` — nega quando `current_count >= limit`
- `can_analyze(used_this_month: int, plan: dict|None) -> (bool, str|None)` — nega quando `used_this_month >= limit`
- `requires_subscription(feature, user) -> bool` — sempre False (fora de escopo, v2)

Consumidores dos limites (NÃO tocados neste plano, só afetados):
- `server/app/main.py::_gate_analise` → `plan.can_analyze(metering.month_used(_conn, scope), plan=plano)`
- `server/app/main.py::watchlist_add` → `plan.can_add_ticker(len(store.get(_conn, "watchlist", user_id=scope)), plan=_plano_do_escopo(scope))`
- `server/app/main.py::ai_quota` → devolve `monthLimit = _plano_do_escopo(scope).get("max_analyses_per_month")`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ativar os limites do PLAN_FREE e tirar o CTA da recusa de can_add_ticker</name>
  <files>server/app/plan.py</files>
  <read_first>
    - server/app/plan.py (arquivo inteiro — docstring de módulo, os dois dicts de plano, os dois hooks; é curto, ~102 linhas)
    - docs/adr/010-planos-e-cap-gratuito.md (decisões 1, 2, 4 e 5 — o que a ativação significa e o que continua pendente)
    - .planning/phases/12-limites-do-plano-gratuito-ativos/12-CONTEXT.md (D-01, D-05, D-06)
  </read_first>
  <action>
    Aplicar D-01: em `PLAN_FREE`, trocar `"max_watchlist": None` por `"max_watchlist": 10` e
    `"max_analyses_per_month": None` por `"max_analyses_per_month": 30`. `PLAN_PRO` fica byte-idêntico
    (ambos `None` = ilimitado, é isso que garante CAP-04). `byok_required` continua `False` nos dois —
    não faz parte desta fase.

    Aplicar D-05: na `can_add_ticker`, trocar SÓ a f-string de recusa. Texto atual (com CTA):
    `"O plano {plan['id']} permite ate {limit} ativos. Faca upgrade para adicionar mais."`
    Texto novo (fato+motivo, sem CTA):
    `"Voce atingiu o limite de {limit} ativos do plano {plan['id']}."`
    Manter a grafia SEM acento, exatamente como o módulo já faz (o `can_analyze` vizinho usa
    "Voce atingiu o limite de {limit} analises/mes do plano {plan['id']}." — é a referência de estilo).
    NÃO tocar na f-string de `can_analyze`: ela já está no formato correto e é o padrão a espelhar.

    Atualizar os comentários que passam a mentir depois da ativação (a prosa é load-bearing neste
    módulo — outros arquivos citam o contrato daqui):
    - Docstring do módulo (linhas 1-14 aprox.): a abertura "NADA aqui cobra ou bloqueia hoje: todas as
      funcoes liberam tudo" e "Quando a monetizacao entrar, os limites abaixo passam a valer" ficam
      falsas. Reescrever esse trecho declarando que a v1.3/Fase 12 ativou os DOIS limites do PLAN_FREE
      (10 ativos, 30 analises/mes) e que o PLAN_PRO segue ilimitado POR DECISÃO (sem loja/IAP neste
      milestone), citando ADR-010.
    - NÃO alterar o bloco "Contrato de contagem (C-32 ...)" (linhas ~16-24): continua exato e é
      referenciado por comentários de `main.py`/`metering.py` e por um guardião de teste que exige a
      palavra "metering" no fonte. A única frase ali que pode ser ajustada é a que fala em futuro
      ("Quando o ADR-010 ligar `max_analyses_per_month`") — pode virar passado, mantendo C-32/C-33
      nomeados literalmente.
    - Comentário da linha do dict (`# futuro: ex. 10 ativos no gratuito` e `# futuro: ex. 30
      analises/mes no gratuito`): trocar por comentário que registre que o valor está ATIVO desde a
      v1.3 (ADR-010) — ou remover o comentário inline, desde que a docstring do módulo carregue a
      informação. `byok_required` mantém o seu `# futuro:`.
    - Comentário do `ACTIVE_PLAN` (linhas ~42-44): a frase "tudo liberado (os limites de
      PLAN_FREE/PLAN_PRO continuam None ...)" ficou falsa. Reescrever preservando o que ainda vale:
      `ACTIVE_PLAN` é o fallback de quem não tem `user` (escopo anônimo) e aponta para `PLAN_FREE` —
      ou seja, anônimo passa a valer os mesmos dois limites (D-06, comportamento intencional).
    - Cabeçalho de seção `# ---- GANCHOS (hoje retornam sempre permitido) ----`: ficou falso; trocar
      por algo como `# ---- GATES DE PLANO (ATIVOS desde a v1.3 para o PLAN_FREE) ----`.

    Não adicionar campo novo, não criar função nova, não mexer em `current_plan`, `plan_at_least` nem
    `requires_subscription`.
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -c "from app import plan; assert plan.PLAN_FREE['max_watchlist']==10; assert plan.PLAN_FREE['max_analyses_per_month']==30; assert plan.PLAN_PRO['max_watchlist'] is None; assert plan.PLAN_PRO['max_analyses_per_month'] is None; ok,m=plan.can_add_ticker(10,plan=plan.PLAN_FREE); assert ok is False and m=='Voce atingiu o limite de 10 ativos do plano free.', m; assert plan.can_add_ticker(9,plan=plan.PLAN_FREE)==(True,None); assert plan.can_analyze(30,plan=plan.PLAN_FREE)[0] is False; assert plan.can_analyze(300,plan=plan.PLAN_PRO)==(True,None); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from app import plan; print(plan.PLAN_FREE)"` mostra `max_watchlist: 10` e `max_analyses_per_month: 30`
    - `plan.PLAN_PRO["max_watchlist"] is None` e `plan.PLAN_PRO["max_analyses_per_month"] is None` continuam verdadeiros
    - `plan.can_add_ticker(10, plan=plan.PLAN_FREE)` devolve exatamente `(False, "Voce atingiu o limite de 10 ativos do plano free.")`
    - `plan.can_add_ticker(9, plan=plan.PLAN_FREE)` devolve `(True, None)` — a fronteira 9→10 libera, só o 11º nega
    - `plan.can_analyze(29, plan=plan.PLAN_FREE)` devolve `(True, None)` e `plan.can_analyze(30, plan=plan.PLAN_FREE)[0]` é `False`
    - `plan.can_add_ticker(9999, plan=plan.PLAN_PRO)` e `plan.can_analyze(9999, plan=plan.PLAN_PRO)` devolvem `(True, None)`
    - `grep -ci "upgrade" server/app/plan.py` retorna `0` (nenhuma ocorrência de "upgrade" em lugar nenhum do arquivo, nem em comentário)
    - `grep -c "metering" server/app/plan.py` retorna valor `>= 1` (o guardião `test_plan_py_declara_por_escrito_que_metering_e_o_contador_unico` exige)
    - `grep -c "C-32" server/app/plan.py` e `grep -c "C-33" server/app/plan.py` retornam `>= 1` cada (contrato preservado por nome)
    - `grep -c "NADA aqui cobra ou bloqueia hoje" server/app/plan.py` retorna `0` (a frase que virou falsa saiu)
    - A f-string de `can_analyze` continua literalmente `f"Voce atingiu o limite de {limit} analises/mes do plano {plan['id']}."` (não foi editada)
  </acceptance_criteria>
  <done>`PLAN_FREE` nega a partir do 11º ativo e da 31ª análise; `PLAN_PRO` nunca nega; a recusa de watchlist não contém CTA.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar (não apagar) os dois guardiões que travavam "nenhum limite comercial ativado"</name>
  <files>server/tests/test_fase3_gate_plano.py, server/tests/test_fase5_gate_mensal.py</files>
  <read_first>
    - server/tests/test_fase3_gate_plano.py (docstring do topo, linhas 1-30 — ela já documenta uma reversão anterior, FIX-C01, e é o MODELO de como anotar esta; e a seção "(g) guardião de fronteira D-03", linhas ~178-187)
    - server/tests/test_fase5_gate_mensal.py (docstring do topo, linhas 1-28; e `test_ai_quota_conta_logada_devolve_month_used_inteiro`, linhas ~177-188)
    - server/app/plan.py (já editado na Task 1 — os valores que os guardiões passam a travar)
    - CLAUDE.md, seção "Guardrails do repositório": "Guardiões de teste não se apagam — reversão deliberada atualiza o guardião com nota"
  </read_first>
  <action>
    Dois guardiões pré-existentes travam exatamente o estado que a Task 1 acabou de inverter. Eles
    NÃO podem ser apagados nem marcados com skip — são atualizados no lugar, com nota explícita de
    reversão deliberada apontando para a Fase 12 / D-01 / ADR-010, no mesmo estilo da nota
    "ATUALIZADO (Plano 04-04, FIX-C01)" que já existe no topo de `test_fase3_gate_plano.py`.

    (a) `server/tests/test_fase3_gate_plano.py::test_d03_nenhum_limite_comercial_ativado`
    (linhas ~182-186) hoje assere `PLAN_FREE["max_watchlist"] is None` e
    `PLAN_FREE["max_analyses_per_month"] is None`. Inverter as duas asserções do FREE para
    `== 10` e `== 30`, MANTENDO as duas asserções do `PLAN_PRO` como `is None` (é o guardião de
    CAP-04: se alguém ligar limite no pro, este teste grita). Renomear a função para refletir o
    novo contrato — sugestão: `test_d01_limites_do_plano_free_ativos` — e adicionar docstring
    dizendo que é a reversão deliberada de `test_d03_nenhum_limite_comercial_ativado`, decidida na
    Fase 12 (12-CONTEXT.md D-01), e que o PLAN_PRO ilimitado é decisão comercial (sem loja/IAP no
    v1.3), não lacuna técnica. Atualizar também o comentário de seção acima dela
    ("(g) guardião de fronteira D-03: nenhum limite comercial ativado") e o trecho da docstring do
    topo do arquivo que afirma "`PLAN_FREE`/`PLAN_PRO` continuam com todos os limites `None`
    (ilimitado) — este arquivo trava exatamente isso (guardião (g))" — acrescentar um parágrafo
    "ATUALIZADO (Fase 12, v1.3)" no mesmo formato do parágrafo FIX-C01 já presente, sem apagar o
    histórico do que era travado antes.

    (b) `server/tests/test_fase5_gate_mensal.py::test_ai_quota_conta_logada_devolve_month_used_inteiro`
    (linha ~187): `assert body["monthLimit"] is None  # ADR-010 pendente, nenhum limite comercial ativo`
    passa a `assert body["monthLimit"] == 30` com comentário novo registrando que a Fase 12 ativou o
    limite mensal do FREE (ADR-010 saiu de pendente). A asserção `body["monthUsed"] == 3` fica
    intacta. NÃO alterar `test_ai_quota_escopo_anonimo_devolve_month_used_none` — o ramo anônimo de
    `/api/ai/quota` devolve `monthUsed`/`monthLimit` `None` por código próprio (early return antes de
    resolver o plano), então ele continua verde e continua correto.
    Acrescentar, na docstring do topo desse arquivo, uma nota "ATUALIZADO (Fase 12, v1.3)" dizendo
    que o cenário hipotético descrito no cabeçalho ("quebraria silenciosamente no dia em que o
    ADR-010 populasse o número") deixou de ser hipotético — o número foi populado e o ledger real
    é o que decide o gate.

    Não adicionar casos de teste novos aqui (a cobertura nova de comportamento é dos planos 12-02 e
    12-03). Este task é cirúrgico: inverter o que virou falso, com rastro escrito.
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/test_fase3_gate_plano.py tests/test_fase5_gate_mensal.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && .venv/bin/python -m pytest tests/test_fase3_gate_plano.py tests/test_fase5_gate_mensal.py -q` termina com 0 falhas
    - `grep -c "test_d03_nenhum_limite_comercial_ativado" server/tests/test_fase3_gate_plano.py` retorna `>= 1` — o NOME antigo continua citado (na nota de reversão), provando que o guardião foi atualizado e não apagado
    - `grep -c "PLAN_PRO\[.max_watchlist.\] is None" server/tests/test_fase3_gate_plano.py` retorna `>= 1` — o guardião de CAP-04 (pro ilimitado) sobreviveu
    - `grep -c "monthLimit.. == 30" server/tests/test_fase5_gate_mensal.py` retorna `>= 1`
    - `grep -c "monthLimit.. is None" server/tests/test_fase5_gate_mensal.py` retorna `>= 1` — o caso anônimo continua travado
    - Ambos os arquivos contêm a string `Fase 12` (nota de reversão datada e rastreável)
    - Nenhum `@pytest.mark.skip`, `@pytest.mark.xfail` ou teste comentado foi introduzido: `grep -c "skip\|xfail" server/tests/test_fase3_gate_plano.py server/tests/test_fase5_gate_mensal.py` retorna `0` em ambos
  </acceptance_criteria>
  <done>A suíte de backend volta ao verde com os limites ligados, e os dois guardiões passaram a travar o estado NOVO com rastro escrito do estado antigo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conta autenticada → resolução de plano | `_plano_do_escopo(scope)` lê `users.plan` do banco; o valor decide se o cap incide. Nenhum dado do request participa. |
| valores de limite → decisão de negação | Constantes de módulo em `plan.py`; não vêm de env, de request nem do banco. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Elevation of Privilege | `plan.current_plan` / `_plano_do_escopo` — conta free ser tratada como pro depois da ativação | mitigate | Nada muda no caminho de resolução; o fail-closed existente (valor desconhecido/erro de leitura → `ACTIVE_PLAN`=FREE) é travado por `test_current_plan_valor_desconhecido_cai_para_free_fail_closed` e `test_rota_watchlist_add_falha_ao_ler_usuario_degrada_para_active_plan`, ambos preservados na Task 2 |
| T-12-02 | Tampering | `PLAN_PRO` ganhar limite por engano, capando conta paga | mitigate | Task 2 mantém as duas asserções `PLAN_PRO[...] is None` no guardião atualizado |
| T-12-03 | Denial of Service (auto-infligido) | Ativar 30 análises/mês em conta anônima (`ACTIVE_PLAN`) travar uso legítimo pré-login | accept | D-06: `metering.month_used` devolve 0 para o balde anônimo (`user_id=None` não acumula por conta), então o cap anônimo nunca dispara na prática; comportamento intencional e documentado |
| T-12-04 | Information Disclosure | Mensagem de recusa expõe o `id` do plano da conta | accept | Só revela ao próprio dono da sessão um dado que ele já vê na UI; sem PII, sem valor para terceiro |
| T-12-SC | Tampering (supply chain) | instalação de pacote npm/pip/cargo | n/a | Este plano não instala nenhum pacote — só edita fonte Python já versionado. Sem gate de legitimidade aplicável |
</threat_model>

<verification>
1. `cd server && .venv/bin/python -m pytest -q` — suíte de backend inteira verde (não só os dois arquivos tocados; a ativação é global e pode acordar teste alheio)
2. `grep -ri "upgrade" server/app/plan.py` — sem resultado (CAP-07)
3. `cd server && .venv/bin/python -c "from app import plan; print(plan.PLAN_FREE, plan.PLAN_PRO)"` — confere 10/30 no free e None/None no pro
4. `bash scripts/executar.sh --testes` — suíte canônica completa (pytest + web/tests/*.mjs) verde; CLAUDE.md exige as DUAS suítes como critério de validado, não só o pytest do passo 1
</verification>

<success_criteria>
- `PLAN_FREE` nega a partir do 11º ativo e da 31ª análise do mês; `PLAN_PRO` nunca nega (CAP-04)
- A frase de recusa de watchlist é fato+motivo, sem CTA (CAP-07)
- Os dois guardiões pré-existentes foram atualizados com nota de reversão, nenhum apagado nem skipado
- `bash scripts/executar.sh --testes` (suíte canônica, não só `pytest -q`) fica verde
</success_criteria>

<output>
Create `.planning/phases/12-limites-do-plano-gratuito-ativos/12-01-SUMMARY.md` when done
</output>
