---
phase: 08-interface-e-ia-da-sele-o-din-mica-vocabul-rio-novo-skill-ref
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/skill_ref.py
  - server/tests/test_skill_ref.py
  - web/src/copy.js
  - web/tests/test_vocabulario_espelho.mjs
autonomous: true
requirements: [ADR17-B34-01]

must_haves:
  truths:
    - "Existe frase canônica por modo para os 6 estados do histórico medido (elegível, inelegível, amostra insuficiente, nunca medido, aposentado, desatualizado)"
    - "A frase do backend e a do front são a MESMA string — divergir quebra a suíte"
    - "O contraste do ADR-017 nunca aparece com o número positivo sozinho: os dois números vivem na mesma string"
    - "O ramo Estudo do vocabulário novo não contém verbo de ordem"
  artifacts:
    - path: "server/app/skill_ref.py"
      provides: "HISTORICO, HISTORICO_ROTULO, ENTRADA_AUTO e historico_txt() — fonte única do vocabulário novo"
      contains: "HISTORICO_ROTULO"
    - path: "web/src/copy.js"
      provides: "espelho COPY[modo].historico / .historicoRotulo / .entradaAuto + helper historicoTxt()"
      contains: "historicoRotulo"
    - path: "web/tests/test_vocabulario_espelho.mjs"
      provides: "guardião cruzado skill_ref.py ↔ copy.js para as chaves novas"
      contains: "HISTORICO"
    - path: "server/tests/test_skill_ref.py"
      provides: "guardião Python do vocabulário novo (chaves espelhadas entre modos, sem verbo de ordem no educacional)"
      contains: "HISTORICO"
  key_links:
    - from: "web/tests/test_vocabulario_espelho.mjs"
      to: "server/app/skill_ref.py"
      via: "leitura do fonte Python + comparação byte a byte com COPY importado"
      pattern: "skill_ref\\.py"
    - from: "web/src/copy.js"
      to: "server/app/skill_ref.py"
      via: "espelho manual de string, travado pelo guardião"
      pattern: "historico"
---

<objective>
Criar o vocabulário canônico que hoje não existe para o histórico medido por setup — os 6
estados do dado que o Bloco 1 já produz — em `server/app/skill_ref.py` (fonte única) com
espelho byte-idêntico em `web/src/copy.js`, e estender o guardião de paridade para cobrir as
chaves novas.

Purpose: sem este plano, os Planos 03 e 04 desta fase teriam que hardcodar texto sensível
dentro de componentes React — exatamente o que a skill `didatica-boris` proíbe ("Texto novo
entra nessas estruturas, não solto no front"). Este plano é a fundação de contrato dos dois
planos de UI que vêm depois.

Output: `skill_ref.HISTORICO`/`HISTORICO_ROTULO`/`ENTRADA_AUTO` + `historico_txt()`, o espelho
em `COPY[modo]` + `historicoTxt()`, e dois guardiões (pytest e node) que travam a paridade.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/skills/didatica-boris/SKILL.md
@.planning/phases/08-interface-e-ia-da-sele-o-din-mica-vocabul-rio-novo-skill-ref/08-CONTEXT.md
@.planning/phases/08-interface-e-ia-da-sele-o-din-mica-vocabul-rio-novo-skill-ref/08-UI-SPEC.md
@server/app/skill_ref.py
@web/src/copy.js

<interfaces>
<!-- Shape do dado já entregue pela Fase 7 — NÃO recalcular nada aqui. -->

server/app/signal_ledger.py:222-246 (`_fundir`) — dict `historico` por setup:
  {expR: float|None, n: int, medidoAte: "YYYY-MM-DD"|None,
   elegivel: bool|None, insuficiente: bool,
   expRJanela: float|None, nJanela: int,
   janelaRef: "AAAA"|None, calculadoEm: "ISO-8601 UTC"|None}

server/app/signal_ledger.py:35 — MIN_N_JANELA = 40 (o "n<40" citado na cópia).

Padrão a replicar (NÃO inventar formato novo):
  server/app/skill_ref.py:239-265  TIMING = {"operador": {...}, "educacional": {...}}
  server/app/skill_ref.py:268-272  def timing_txt(modo, estado, hora="") -> str
  web/src/copy.js:18                COPY = { estudo: {...}, operador: {...} }
  web/src/copy.js:214               export function copyFor(mode)

Convenção de chave de modo (NÃO unificar nesta fase, 08-CONTEXT.md):
  backend = "operador" | "educacional"
  front   = "operador" | "estudo"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vocabulário canônico do histórico medido em skill_ref.py</name>
  <files>server/app/skill_ref.py, server/tests/test_skill_ref.py</files>
  <read_first>
    - server/app/skill_ref.py (linhas 206-287: `vocab`, `TIMING`, `timing_txt`, `decisoes_txt` —
      o padrão exato de dict-por-modo + função helper que interpola placeholder)
    - server/tests/test_skill_ref.py (o estilo dos guardiões já existentes deste módulo)
    - .planning/phases/08-.../08-UI-SPEC.md (secção "Copywriting Contract" — a tabela de
      6 estados com o texto LITERAL de cada modo, e a frase de contraste com os dois números)
    - .claude/skills/didatica-boris/SKILL.md ("Vocabulário por modo (não negociável)")
  </read_first>
  <behavior>
    - `skill_ref.HISTORICO` tem exatamente os modos "operador" e "educacional", e os dois têm
      exatamente o mesmo conjunto de chaves: {elegivel, inelegivel, insuficiente, nunca_medido,
      aposentado, desatualizado}.
    - `skill_ref.HISTORICO_ROTULO` tem os dois modos com exatamente o mesmo conjunto de chaves:
      {elegivel, inelegivel, insuficiente, nunca_medido, aposentado} (sem `desatualizado` — ele
      é modificador de timestamp, nunca pill própria; UI-SPEC "Interaction & State Inventory").
    - `skill_ref.ENTRADA_AUTO` tem os dois modos com as chaves {regra, contraste}.
    - `ENTRADA_AUTO[modo]["contraste"]` contém, na MESMA string, "−0,099R" e "+0,005R" — nos
      dois modos. Um teste falha se qualquer um dos dois números aparecer sem o outro.
    - `historico_txt("operador", "elegivel", janela="2025")` devolve a frase com "2025" no lugar
      de "{janela}"; `historico_txt("operador", "desatualizado", medido_ate="2026-08-19")`
      devolve a frase com a data no lugar de "{medidoAte}".
    - `historico_txt` com modo desconhecido cai em "educacional"; com estado desconhecido cai
      em "nunca_medido" (nunca levanta, nunca devolve string vazia).
    - Nenhuma string de `HISTORICO["educacional"]`, `HISTORICO_ROTULO["educacional"]` ou
      `ENTRADA_AUTO["educacional"]` contém COMPRAR, VENDER, COMPRE, VENDA, "registrar entrada"
      ou "registrar saída" (regra de verbo de ordem do ramo educacional).
  </behavior>
  <action>
    Em `server/app/skill_ref.py`, LOGO DEPOIS de `timing_txt` (mantendo a organização por
    seção com o comentário-cabeçalho no mesmo estilo dos blocos vizinhos), acrescente três
    dicionários e uma função. Os textos são LITERAIS aprovados no `08-UI-SPEC.md` — copie
    exatamente, incluindo acentuação, o traço "—" (em-dash), o sinal "−" (minus U+2212 nos
    números negativos) e os glifos "✓"/"✗":

    `HISTORICO`
      operador.elegivel        = "✓ ELEGÍVEL — vantagem estatística medida na janela {janela}."
      operador.inelegivel      = "✗ NÃO ELEGÍVEL — sem vantagem estatística medida na janela {janela}."
      educacional.elegivel     = "Estudo: vantagem estatística medida na janela {janela}."
      educacional.inelegivel   = "Estudo: sem vantagem estatística medida na janela {janela}."
      insuficiente (nos DOIS, texto idêntico)  = "Amostra insuficiente (n<40) — ausência de evidência não é prova de mau desempenho."
      nunca_medido (nos DOIS)                  = "Sem histórico medido ainda."
      aposentado   (nos DOIS)                  = "Padrão gráfico identificado, sem vantagem estatística medida (ADR-016)."
      desatualizado (nos DOIS)                 = "Medido até {medidoAte} — dado pode estar desatualizado."

    `HISTORICO_ROTULO` (rótulo curto da pill; o guardião do front confere que é ele que a UI
    desenha, e a frase inteira acima é que vira o texto acessível)
      operador    = {elegivel: "✓ ELEGÍVEL", inelegivel: "✗ NÃO ELEGÍVEL",
                     insuficiente: "AMOSTRA INSUFICIENTE (n<40)",
                     nunca_medido: "SEM HISTÓRICO MEDIDO", aposentado: "APOSENTADO (ADR-016)"}
      educacional = {elegivel: "VANTAGEM MEDIDA", inelegivel: "SEM VANTAGEM MEDIDA",
                     insuficiente: "AMOSTRA INSUFICIENTE (n<40)",
                     nunca_medido: "SEM HISTÓRICO MEDIDO", aposentado: "APOSENTADO (ADR-016)"}

    `ENTRADA_AUTO` (transparência do gate do Modo Operador — consumido pelo card de status
    único do C-19 no Plano 08-04)
      operador.regra    = "Entrada automática só executa em setup com vantagem estatística medida na janela anterior — sem vantagem medida, o Operador sinaliza e não executa."
      educacional.regra = "No Modo Operador, a entrada automática só executa em setup com vantagem estatística medida na janela anterior — sem vantagem medida, ele sinaliza e não executa."
      contraste (nos DOIS, texto idêntico) = "Sem filtro: −0,099R por sinal (todos os setups, 15 anos) · Com filtro (setups elegíveis na janela anterior): +0,005R — estatisticamente um empate, não lucro."

    `def historico_txt(modo: str, estado: str, janela: str = "", medido_ate: str = "") -> str:`
    — mesmo idioma de `timing_txt`: resolve o modo (fallback "educacional"), pega a frase do
    estado (fallback `nunca_medido`), e substitui "{janela}" por `janela or "?"` e
    "{medidoAte}" por `medido_ate or "?"`.

    Escreva o comentário-cabeçalho da seção registrando POR QUÊ estes textos existem: o Bloco 1
    (ADR-017) mede, o Bloco 3 exibe, e resultado negativo tem o MESMO peso que positivo (regra
    da skill `didatica-boris`, nenhuma manipulação visual de resultado). Registre também que
    `contraste` é número FIXO de backtest (ADR-016/017), não computação viva — não ligar a
    endpoint.

    NÃO toque em `vocab`, `TIMING`, `PRINCIPIOS`, `DIDATICA` nem em `defaults.py`: qualquer
    mudança em `defaults.py` acionaria o guardião de paridade byte-exata com `catalog.js`
    (`server/tests/test_auditoria_prompts.py:197`), e esta fase não muda prompt de IA.

    Acrescente os guardiões novos ao FIM de `server/tests/test_skill_ref.py` (não reescreva o
    arquivo), cobrindo cada item do bloco `<behavior>`.
  </action>
  <verify>
    <automated>cd server && ./.venv/bin/python -m pytest -q tests/test_skill_ref.py</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && ./.venv/bin/python -m pytest -q tests/test_skill_ref.py` sai 0.
    - `cd server && ./.venv/bin/python -m pytest -q` (suíte inteira) sai 0 — em especial `tests/test_auditoria_prompts.py` continua verde, provando que `defaults.py`/`catalog.js` não foram tocados.
    - `grep -n "^HISTORICO = {" server/app/skill_ref.py` casa exatamente 1 linha.
    - `grep -n "^HISTORICO_ROTULO = {" server/app/skill_ref.py` casa exatamente 1 linha.
    - `grep -n "^ENTRADA_AUTO = {" server/app/skill_ref.py` casa exatamente 1 linha.
    - `cd server && ./.venv/bin/python -c "from app import skill_ref as s; assert set(s.HISTORICO['operador']) == set(s.HISTORICO['educacional']) == {'elegivel','inelegivel','insuficiente','nunca_medido','aposentado','desatualizado'}"` sai 0.
    - `cd server && ./.venv/bin/python -c "from app import skill_ref as s; t=s.ENTRADA_AUTO['operador']['contraste']; assert '−0,099R' in t and '+0,005R' in t"` sai 0.
    - `git diff --stat server/app/defaults.py web/src/catalog.js` não lista nenhuma mudança.
  </acceptance_criteria>
  <done>`skill_ref.py` é a fonte única dos 6 estados do histórico, dos 5 rótulos de pill e das 2 frases de transparência do gate, com helper de interpolação e guardião pytest próprio.</done>
</task>

<task type="auto">
  <name>Task 2: Espelho em copy.js com helper de interpolação</name>
  <files>web/src/copy.js</files>
  <read_first>
    - web/src/copy.js (arquivo inteiro — a convenção do cabeçalho: "chaves idênticas nos dois
      modos", "funções para textos com variáveis", "vocabulário de ordem PROIBIDO no ramo
      estudo"; e `copyFor` no fim)
    - web/tests/test_copy_theme.mjs (linhas 26-31: o guardião existente que compara
      `Object.keys(COPY.estudo)` com `Object.keys(COPY.operador)` e varre os VALORES do ramo
      estudo atrás de verbo de ordem — chave nova precisa passar nos dois)
    - server/app/skill_ref.py (a seção HISTORICO criada na Task 1 — a fonte a espelhar)
  </read_first>
  <action>
    Em `web/src/copy.js`, acrescente a CADA ramo (`estudo` e `operador`) três chaves novas, no
    fim do respectivo bloco, com comentário curto apontando a fonte
    (`server/app/skill_ref.py` seção HISTORICO — ADR-017 Bloco 3):

      `historico`       — objeto com as 6 chaves de estado, strings BYTE A BYTE iguais às de
                          `skill_ref.HISTORICO[modo]` (estudo espelha `educacional`).
      `historicoRotulo` — objeto com as 5 chaves de rótulo, byte a byte iguais a
                          `skill_ref.HISTORICO_ROTULO[modo]`.
      `entradaAuto`     — objeto {regra, contraste}, byte a byte igual a
                          `skill_ref.ENTRADA_AUTO[modo]`.

    Mantenha os placeholders literais "{janela}" e "{medidoAte}" nas strings — a interpolação é
    do helper, não do dicionário (é isso que permite a comparação byte a byte com o Python).

    Acrescente, junto de `copyFor`, o helper exportado:
    `export function historicoTxt(mode, estado, vals)` — resolve o modo pelo mesmo critério de
    `copyFor` (`"operador"` ou fallback estudo), pega
    `COPY[modo].historico[estado] || COPY[modo].historico.nunca_medido`, e substitui
    "{janela}" por `(vals && vals.janela) || "?"` e "{medidoAte}" por
    `(vals && vals.medidoAte) || "?"`. Espelho de `skill_ref.historico_txt`.

    Atenção ao guardião existente `test_copy_theme.mjs`: ele faz
    `Object.values(COPY.estudo).map(v => typeof v === "function" ? ... : v)` e depois
    `JSON.stringify` antes de varrer verbo de ordem. Objetos aninhados entram inteiros no
    JSON — por isso NENHUMA string do ramo estudo pode conter COMPRAR/VENDER (as frases
    aprovadas não contêm; não improvise).

    NÃO altere nenhuma chave existente de `copy.js` e não mexa em `disclaimers.js`.
  </action>
  <verify>
    <automated>cd web && node tests/test_copy_theme.mjs && node tests/test_rr_min_fonte_unica.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && node tests/test_copy_theme.mjs` sai 0 (chaves espelhadas nos dois modos + ramo estudo sem verbo de ordem).
    - `cd web && node -e "import('./src/copy.js').then(m=>{const a=Object.keys(m.COPY.estudo.historico).sort().join(),b=Object.keys(m.COPY.operador.historico).sort().join();if(a!==b)process.exit(1)})"` sai 0.
    - `cd web && node -e "import('./src/copy.js').then(m=>{if(m.historicoTxt('operador','elegivel',{janela:'2025'})!=='✓ ELEGÍVEL — vantagem estatística medida na janela 2025.')process.exit(1)})"` sai 0.
    - `cd web && node -e "import('./src/copy.js').then(m=>{if(m.historicoTxt('banana','xpto')!==m.COPY.estudo.historico.nunca_medido)process.exit(1)})"` sai 0.
    - `cd web && npx vite build` sai 0.
  </acceptance_criteria>
  <done>`copy.js` carrega o espelho completo do vocabulário novo e o helper de interpolação; nenhuma tela precisa hardcodar texto do histórico.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Guardião cruzado skill_ref.py ↔ copy.js</name>
  <files>web/tests/test_vocabulario_espelho.mjs</files>
  <read_first>
    - web/tests/test_vocabulario_espelho.mjs (arquivo inteiro — o guardião existente compara
      `TIMING_STYLE` do App.jsx com `conceitos.ROTULOS`; as asserções novas ENTRAM neste
      arquivo, sem apagar nenhuma das atuais)
    - web/tests/test_rr_min_fonte_unica.mjs (linhas 1-45 — o padrão desta base para guardião
      cruzado front×backend: `import { COPY } from "../src/copy.js"` funciona em node, e o
      lado Python é lido como TEXTO e parseado por regex)
    - server/app/skill_ref.py (o bloco HISTORICO/HISTORICO_ROTULO/ENTRADA_AUTO da Task 1)
    - web/src/copy.js (o espelho da Task 2)
  </read_first>
  <behavior>
    - O guardião falha se uma chave de estado existir em `skill_ref.HISTORICO[modo]` e não em
      `COPY[modoJS].historico` (e vice-versa), nos dois modos.
    - O guardião falha se qualquer string divergir por UM byte entre os dois arquivos.
    - O guardião falha se `App.jsx` contiver, hardcodada, qualquer uma das frases/rótulos do
      vocabulário novo (texto sensível só pode viver em `copy.js`).
    - O guardião falha se a frase de contraste tiver só um dos dois números.
    - As asserções atuais do arquivo (TIMING_STYLE ↔ conceitos.ROTULOS, CONDIÇÃO/GATILHO)
      continuam presentes e passando.
  </behavior>
  <action>
    Estenda `web/tests/test_vocabulario_espelho.mjs` (append, sem remover nada) com um bloco
    novo comentado como "ADR-017 Bloco 3 — histórico medido por setup".

    Leia `server/app/skill_ref.py` como texto (caminho resolvido por
    `new URL(..., import.meta.url)`, NUNCA relativo ao cwd — `scripts/executar.sh` roda os
    runners com cwd=web) e `import { COPY } from "../src/copy.js"`.

    Parse do lado Python: para cada nome de dicionário em ["HISTORICO", "HISTORICO_ROTULO",
    "ENTRADA_AUTO"], recorte o bloco do fonte com uma âncora ancorada em início de linha
    (`^NOME = {` ... até a linha `^}`) — âncora ancorada é obrigatória, senão `HISTORICO`
    casaria dentro de `HISTORICO_ROTULO`. Dentro do bloco, recorte cada sub-bloco de modo
    (`"operador": {` / `"educacional": {`) e extraia os pares `"chave": "valor"`. Trate a
    escapagem de aspas do Python como inexistente (as frases aprovadas não têm aspas
    internas) e falhe explicitamente se o parse devolver 0 chaves — parse mudo é a falha que
    o comentário de `test_rr_min_fonte_unica.mjs` já registra nesta base.

    Mapeamento de modo: `educacional` (Python) ↔ `estudo` (JS); `operador` ↔ `operador`.

    Asserções:
      1. Para os 3 dicionários × 2 modos: conjunto de chaves idêntico entre Python e JS.
      2. Para cada chave: string idêntica byte a byte.
      3. `HISTORICO` tem exatamente {elegivel, inelegivel, insuficiente, nunca_medido,
         aposentado, desatualizado}; `HISTORICO_ROTULO` tem exatamente as 5 (sem
         `desatualizado`); `ENTRADA_AUTO` tem exatamente {regra, contraste}.
      4. `contraste` contém "−0,099R" E "+0,005R" nos dois modos (o teste falha se um aparecer
         sem o outro — regra do 08-CONTEXT.md: nunca o filtrado sozinho).
      5. `App.jsx` NÃO contém, como literal, nenhum dos valores de `historico`,
         `historicoRotulo` e `entradaAuto` dos dois modos (varra os valores; ignore os que são
         só placeholder). Isso é o que impede o texto de voltar a nascer solto no componente.

    Atualize o comentário do topo do arquivo registrando que o guardião agora cobre DOIS
    espelhos: o do timing (original) e o do histórico medido (ADR-017 Bloco 3).
  </action>
  <verify>
    <automated>cd web && node tests/test_vocabulario_espelho.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && node tests/test_vocabulario_espelho.mjs` sai 0 e imprime linhas "ok " para as asserções novas dos 3 dicionários.
    - Sabotagem controlada em sandbox: copie `server/app/skill_ref.py` para um temp, troque um byte de uma frase de `HISTORICO`, e comprove que o guardião FALHA ao ler a cópia (via um override de caminho por env var, no mesmo idioma de `B3_SKILL_REF_PATH` em `test_rr_min_fonte_unica.mjs`); restaure em seguida. O arquivo real do servidor nunca é alterado.
    - `bash scripts/executar.sh --testes` sai 0 (as DUAS suítes).
    - `grep -c "rotuloArmado" web/tests/test_vocabulario_espelho.mjs` ≥ 1 — as asserções originais do arquivo continuam lá.
  </acceptance_criteria>
  <done>Divergir uma letra entre `skill_ref.py` e `copy.js`, ou hardcodar a frase no `App.jsx`, quebra a suíte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vocabulário canônico → prompt de LLM | `skill_ref` alimenta prompts (`llm.py`, `assistente.py`, `defaults.py`); texto novo mal colocado vira instrução para o modelo |
| Vocabulário canônico → tela do usuário | O texto exibido é afirmação de produto sobre desempenho medido — enquadramento regulatório (CVM) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `server/app/defaults.py` ↔ `web/src/catalog.js` | mitigate | Nenhuma edição em `defaults.py`; aceite exige `git diff --stat` limpo nos dois arquivos e `tests/test_auditoria_prompts.py` verde |
| T-08-02 | Information Disclosure | Frase de contraste (−0,099R / +0,005R) | mitigate | Os dois números vivem numa string única; guardião falha se só um aparecer — impossível exibir o positivo isolado |
| T-08-03 | Repudiation | Divergência silenciosa skill_ref ↔ copy.js | mitigate | Guardião cruzado byte a byte na Task 3, com sabotagem controlada comprovando que ele pega a divergência |
| T-08-04 | Elevation of Privilege | Ramo `estudo` ganhando verbo de ordem | mitigate | `test_copy_theme.mjs` (existente) varre os valores do ramo estudo; guardião pytest novo faz o mesmo no lado Python |

Sem instalação de pacote npm/pip/cargo neste plano — o gate de legitimidade de pacote não se aplica.
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` — as DUAS suítes verdes (pytest + `web/tests/*.mjs`).
- `cd web && npx vite build` sai 0.
- `git diff --stat server/app/defaults.py web/src/catalog.js` vazio.
</verification>

<success_criteria>
- Os 6 estados do histórico têm frase canônica por modo em `skill_ref.py`, com espelho byte-idêntico em `copy.js`.
- `historico_txt` (Python) e `historicoTxt` (JS) interpolam `{janela}`/`{medidoAte}` com o mesmo contrato e degradam para `nunca_medido` em estado desconhecido.
- O guardião cruzado falha em sabotagem controlada e passa no repositório íntegro.
- Nenhum prompt de IA foi alterado.
</success_criteria>

<output>
Create `.planning/phases/08-interface-e-ia-da-sele-o-din-mica-vocabul-rio-novo-skill-ref/08-01-SUMMARY.md` when done
</output>
