---
phase: 38-kb-did-tica-ampliada
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/entendimento.jsx
  - web/src/App.jsx
  - web/tests/test_entendimento_modulo.mjs
  - web/tests/test_conceito_ui.mjs
  - web/tests/test_setor_toque.mjs
  - web/tests/test_boris_chat.mjs
  - web/tests/test_fonte_explicacao.mjs
  - web/tests/test_numeros_fundamentados.mjs
  - web/tests/test_perfil_reorg.mjs
autonomous: true
requirements: [KB-02]

must_haves:
  truths:
    - "SetorAlvo/ConceitoSheet vivem num módulo compartilhado fora de App.jsx, importável por OpcoesScreen.jsx sem ciclo [KB-02, ROADMAP SC#4]"
    - "Nenhum comportamento muda: toque no sublinhado, link de Portfólio, via proativa do gatilho e AssistenteBox funcionam como antes (extração pura)"
    - "Os guardiões que liam o código movido continuam medindo o MESMO contrato, agora no arquivo novo — nenhuma asserção apagada"
  artifacts:
    - path: "web/src/entendimento.jsx"
      provides: "AiNote, SUBLINHADO, SetorAlvo, AssistenteBox, ConceitoSheet (+ CONCEITO_BLOCOS/SR_ONLY privados), T local espelhado"
      contains: "export function ConceitoSheet"
    - path: "web/tests/test_entendimento_modulo.mjs"
      provides: "guardião da extração: isolamento, tokens, ausência em App.jsx"
      min_lines: 40
  key_links:
    - from: "web/src/App.jsx"
      to: "web/src/entendimento.jsx"
      via: "import nomeado"
      pattern: "from \"\\./entendimento\\.jsx\""
    - from: "web/src/entendimento.jsx"
      to: "web/src/persistence.js"
      via: "store.conceito / store.assistente (sem passar por App.jsx)"
      pattern: "from \"\\./persistence\\.js\""
---

<objective>
Extrair a camada de entendimento de `web/src/App.jsx` para um módulo terceiro,
`web/src/entendimento.jsx` — REFATORAÇÃO PURA, zero mudança de comportamento —
e reapontar os 6 guardiões que hoje leem o código movido por regex sobre
`App.jsx`.

Purpose: ROADMAP SC#4 e o texto de KB-02 exigem `SetorAlvo`/`ConceitoSheet` fora
de `App.jsx`, importáveis por `web/src/opcoes/OpcoesScreen.jsx` sem que ele
importe `App.jsx` (isolamento ADR-027). Isolar a mudança estrutural num plano
próprio, sem feature misturada, é o que torna o diff revisável e a regressão
detectável (padrão das Fases 32/33, `web/src/opcoes/uiOpcoes.jsx`).

Nome do módulo: `entendimento.jsx` (não `didatica.jsx`, sugestão A3 do
RESEARCH, não travada): `didatica` já é o nome do estado/prop que carrega o
catálogo de `/api/conceitos` em todo `App.jsx`/`ConceitoSheet`; um módulo
homônimo convida import que sombreia a prop. "Camada de entendimento" é o nome
que o próprio código usa (`App.jsx:2810`).

Nota de caminho: o CONTEXT cita `web/src/OpcoesScreen.jsx:20-24`; o arquivo real
é `web/src/opcoes/OpcoesScreen.jsx` (o comentário de isolamento está nas linhas
20-24 dele). Este plano NÃO toca `OpcoesScreen.jsx` — o import dele entra no
38-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-kb-did-tica-ampliada/38-RESEARCH.md
@.planning/phases/38-kb-did-tica-ampliada/38-PATTERNS.md
@CLAUDE.md

<interfaces>
Código a mover (web/src/App.jsx, estado em 2026-09-23 — conferir por Grep antes de cortar, as linhas podem ter andado):
- `const AiNote = ({ at, source = "ia" }) => (...)` — linhas ~533-546, com o comentário FIX-C01 acima. Consumidores que FICAM em App.jsx: `<AiNote at={an.at} source={source} />` em `AnalysisView` (~1521).
- `CONCEITO_BLOCOS` (~2818), `SUBLINHADO` (~2842), `SR_ONLY` (~2846), `function SetorAlvo({ setorId, dados, rotulo, A, didatica, ativo = true, style, children })` (~2848), `function AssistenteBox({ cid, dados, setor, tela })` (~2876), `function ConceitoSheet({ cid, dados, setor, onClose, onTrocar, didatica, voltar })` (~2941-3013) — bloco contíguo que começa no comentário "---- Camada de entendimento: a FOLHA de explicação" (~2810) e termina antes de "---- O PET" (~3015). `TIMING_STYLE` (~2795-2808) FICA em App.jsx.
- `SUBLINHADO` também é usado FORA do bloco (App.jsx ~3259, 3267, 3424, 3727, 3743) → App importa.
- Dependências do código movido: `useState`, `useEffect` (react); `store` (./persistence.js); `Markdown` (./markdown.jsx); tokens `T.*`.

Padrão de token espelhado (web/src/opcoes/uiOpcoes.jsx:29-31):
`const VARKEY = (k) => "--" + k.replace(/[A-Z]/g, (c) => "-" + c.toLowerCase());`
`const TOKENS = [...]; const T = Object.fromEntries(TOKENS.map((k) => [k, \`var(${VARKEY(k)})\`]));`
App.jsx gera as MESMAS variáveis CSS (`App.jsx:153-156`), então `var(--text-faint)` resolve igual nos dois temas × dois modos.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: criar web/src/entendimento.jsx e fazer App.jsx importar dele (movimento puro)</name>
  <files>web/src/entendimento.jsx, web/src/App.jsx</files>
  <read_first>
    - web/src/App.jsx — linhas 1-55 (imports), 530-548 (AiNote), 2805-3015 (bloco inteiro a mover)
    - web/src/opcoes/uiOpcoes.jsx — linhas 1-35 (cabeçalho de isolamento + T espelhado: o tom e a regra a replicar)
    - web/src/opcoes/OpcoesScreen.jsx — linhas 88-99 (lista TOKENS com o aviso da Fase 35: token fora do array vira `undefined` calado)
  </read_first>
  <action>
    1. Criar `web/src/entendimento.jsx` com, nesta ordem:
       - Cabeçalho de comentário PT-BR: o que é (camada de entendimento: folha de explicação, setor tocável, assistente de pergunta única, marcador de IA), por que saiu de `App.jsx` (Fase 38, KB-02/SC#4: importável por `App.jsx` E `opcoes/OpcoesScreen.jsx`), a regra "este módulo não importa `App.jsx` nem `OpcoesScreen.jsx` — seria ciclo e quebraria o isolamento do ADR-027", e a regra aceita de duplicação do `T` espelhado (mesmo texto-base de `uiOpcoes.jsx`).
       - Imports: `useState`, `useEffect` de "react"; `store` de "./persistence.js"; `Markdown` de "./markdown.jsx".
       - `VARKEY` + `TOKENS` + `T` locais. `TOKENS` = exatamente as chaves usadas pelo código movido: textFaint, textMuted, textSecondary, textPrimary, borderSubtle, borderFaint, bgBase, bgPanel, scrim, accent, accentTint10, negative (conferir por Grep `T\.[a-zA-Z0-9]+` no texto movido — se aparecer chave fora desta lista, acrescentá-la; nenhuma chave pode ficar fora).
       - Os blocos movidos VERBATIM (código e comentários, byte a byte, só com `export` acrescentado), nesta ordem: `AiNote` (com o comentário FIX-C01), `CONCEITO_BLOCOS` (com o comentário da folha), `SUBLINHADO` (com o comentário do padrão Duolingo), `SR_ONLY`, `SetorAlvo`, `AssistenteBox`, `ConceitoSheet`. A ordem SetorAlvo → AssistenteBox é contrato: `test_setor_toque.mjs` fatia entre os dois; `AiNote` fica ANTES de `SetorAlvo` para não cair dentro dessa fatia.
       - Exports nomeados: `export const AiNote`, `export const SUBLINHADO`, `export function SetorAlvo`, `export function AssistenteBox`, `export function ConceitoSheet`. `CONCEITO_BLOCOS` e `SR_ONLY` ficam privados (só o módulo usa).
    2. Em `web/src/App.jsx`: remover os blocos movidos (AiNote com seu comentário; o bloco de ~2810 até o fim de `ConceitoSheet`, preservando `TIMING_STYLE` e o comentário "---- O PET" que vem depois) e acrescentar, junto dos outros imports de módulo local, `import { AiNote, SUBLINHADO, SetorAlvo, ConceitoSheet } from "./entendimento.jsx";` com comentário de uma linha "Fase 38 (38-02): camada de entendimento extraída para módulo terceiro — importável também por opcoes/OpcoesScreen.jsx sem ciclo". Nenhum call-site em App.jsx muda (`<SetorAlvo ...>`, `<ConceitoSheet ...>` global em ~9366, `<AiNote .../>` em AnalysisView, spreads de `SUBLINHADO`).
    3. NÃO alterar lógica, props, strings ou estilos do código movido — qualquer mudança de comportamento é do 38-03. Se `vite build` acusar símbolo faltando, o conserto é import, nunca redefinição local.
  </action>
  <verify>
    <automated>cd web && npx vite build 2>&1 | tail -5 && grep -c "from \"./entendimento.jsx\"" src/App.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vite build` termina sem erro
    - `grep -c "from \"./entendimento.jsx\"" web/src/App.jsx` retorna 1
    - `grep -cE "^(export )?function (ConceitoSheet|SetorAlvo|AssistenteBox)\(|^(export )?const (AiNote|SUBLINHADO|SR_ONLY|CONCEITO_BLOCOS) = " web/src/App.jsx` retorna 0
    - `grep -cE "^export (function (ConceitoSheet|SetorAlvo|AssistenteBox)\(|const (AiNote|SUBLINHADO) = )" web/src/entendimento.jsx` retorna 5
    - `grep -cE "from \"[^\"]*(App|OpcoesScreen)\.jsx\"" web/src/entendimento.jsx` retorna 0
    - `grep -c "Não consegui carregar a explicação agora. O card continua válido." web/src/entendimento.jsx` retorna 1
  </acceptance_criteria>
  <done>Módulo novo com os 7 símbolos, App.jsx importando 4 deles, build verde, nenhum call-site alterado. Commit `refactor(38-02): extrai camada de entendimento para entendimento.jsx`.</done>
</task>

<task type="auto">
  <name>Task 2: reapontar os 6 guardiões + guardião novo da extração</name>
  <files>web/tests/test_conceito_ui.mjs, web/tests/test_setor_toque.mjs, web/tests/test_boris_chat.mjs, web/tests/test_fonte_explicacao.mjs, web/tests/test_numeros_fundamentados.mjs, web/tests/test_perfil_reorg.mjs, web/tests/test_entendimento_modulo.mjs</files>
  <read_first>
    - web/tests/test_conceito_ui.mjs — arquivo inteiro (183 linhas; ~12 asserções leem código que mudou de arquivo)
    - web/tests/test_setor_toque.mjs — linhas 20-100 (fatia `setorAlvo` entre "function SetorAlvo(" e "function AssistenteBox(")
    - web/tests/test_boris_chat.mjs — linhas 85-100
    - web/tests/test_fonte_explicacao.mjs — linhas 25-70 (localização de AiNote por indexOf, contagem de strings "1x no arquivo", `<AiNote />` "fora de AnalysisView")
    - web/tests/test_numeros_fundamentados.mjs — linhas 40-50
    - web/tests/test_perfil_reorg.mjs — linhas 125-150 (`hubTightEnd = app.indexOf("function SetorAlvo(")` delimita o FIM do PerfilHub — vira -1 após a extração)
  </read_first>
  <action>
    Regra de reapontamento (guardrail "guardiões de teste não se apagam"):
    - Rodar `for t in web/tests/*.mjs; do node "$t" >/dev/null 2>&1 || echo "FALHOU: $t"; done` e tratar CADA `FALHOU` assim: se a asserção falhou porque o código que ela mede mudou de arquivo, carregar `web/src/entendimento.jsx` (variável `ent`, via `readFileSync` como o arquivo já faz com `App.jsx`) e apontar a asserção para ele; se uma asserção com `&&` mede um pedaço que ficou em App.jsx e outro que foi para o módulo, dividir o teste da condição entre `app` e `ent` mantendo o MESMO regex de cada pedaço. Asserção que continua passando em `app` fica intocada. NUNCA afrouxar regex, apagar asserção ou mudar texto esperado. Se algo falhar por motivo que NÃO seja mudança de arquivo, é regressão da Task 1 — consertar o código, não o teste.
    - Cada asserção reapontada ganha, na linha de cima, o comentário `// Fase 38 (38-02, 2026-09-23): <símbolo> migrou para web/src/entendimento.jsx — asserção reapontada, contrato intacto.`

    Reapontamentos esperados (confirmar contra a execução real):
    - `test_conceito_ui.mjs`: ConceitoSheet + `store.conceito(cid, { dados })`; ordem dos blocos; bloco vazio `(c[chave] || []).length > 0`; texto de falha da folha; assinatura de SetorAlvo + `A.abrirSetor(setorId, cid, dados, origem)`; `didatica.setores) ? didatica.setores[setorId] : null`; `const SUBLINHADO = ` (a ausência de `ConceitoDot` passa a ser verificada em `app` E em `ent`); `zIndex: 86, background: T.scrim, ...`; `aria-label={"O que é " + rotulo + "?"} style={SR_ONLY}` + `clipPath`; `‹ voltar` (em `ent`) com `voltarConceito: () => setConceitoAberto` (em `app`); lookup `(didatica.conceitos || []).find(...)`. A asserção "a folha abre SOBRE o card" (`zIndex: 75` + `alignItems: "flex-end"`): se ainda passar em `app`, fica; se não, reapontar só o pedaço que migrou.
    - `test_setor_toque.mjs`: a fatia `setorAlvo` passa a ser tirada de `ent`; asserções sobre `abrirSetor: (setorId, cid, dados, origem)` ficam em `app`.
    - `test_boris_chat.mjs`: as duas asserções de AssistenteBox (linhas ~92-96) → `ent`.
    - `test_fonte_explicacao.mjs`: bloco `aiNote` localizado em `ent`; as contagens "aparece 1x no arquivo" passam a exigir 1x em `ent` E 0x em `app` (prova de que não duplicou na extração); a busca de `<AiNote />` "fora de AnalysisView" passa a ser em `ent` (é o uso dentro de AssistenteBox); asserções de AnalysisView ficam em `app`.
    - `test_numeros_fundamentados.mjs`: `<Markdown text={r.texto} />` (AssistenteBox) → `ent`.
    - `test_perfil_reorg.mjs`: o marcador de FIM do PerfilHub deixa de ser `function SetorAlvo(`; usar `app.indexOf("\nconst TIMING_STYLE = {", hubStart)` (a próxima declaração de topo após PerfilHub) e acrescentar asserção de parse-não-mudo: a fatia contém `hubGroup` e NÃO contém `function ` de topo de outro componente.

    Criar `web/tests/test_entendimento_modulo.mjs` (mesmo esqueleto `ok()/fails` dos outros `.mjs`, roda sem build), com:
    (a) `entendimento.jsx` exporta `ConceitoSheet`, `SetorAlvo`, `AssistenteBox`, `AiNote`, `SUBLINHADO`;
    (b) `entendimento.jsx` não importa `App.jsx` nem `OpcoesScreen.jsx`;
    (c) todo `T.<chave>` usado em `entendimento.jsx` está no array `TOKENS` do próprio arquivo (extrair as duas listas por regex e comparar; imprimir as faltantes) — o defeito calado que a Fase 35 achou em `SecaoVigias.jsx`;
    (d) `App.jsx` não define mais nenhum dos 7 símbolos e importa de `./entendimento.jsx`;
    (e) `opcoes/OpcoesScreen.jsx` continua sem importar `App.jsx`;
    (f) prova negativa do (c): uma cópia em memória do texto do módulo com `T.tokenInexistente` injetado faz a checagem acusar 1 faltante (o guardião do guardião, padrão de `test_kb.py::test_negacao_detecta_a_forma_permitida_e_a_proibida`).
  </action>
  <verify>
    <automated>cd web && for t in tests/*.mjs; do node "$t" >/dev/null 2>&1 || echo "FALHOU: $t"; done; echo fim</automated>
  </verify>
  <acceptance_criteria>
    - O loop do `<verify>` imprime só `fim` (zero `FALHOU`); se sobrar falha, ela tem de ser ambiental e já conhecida (ex.: `test_ios_assets.mjs` sem `web/ios/`, gitignored) e vai listada no SUMMARY com o motivo
    - `node web/tests/test_entendimento_modulo.mjs` termina com "todos os testes passaram"
    - `grep -c "Fase 38 (38-02" web/tests/test_conceito_ui.mjs` ≥ 5 (reapontamentos anotados)
    - `git diff --stat web/tests/` mostra só inserções/alterações nos 6 arquivos + o novo — nenhuma linha `ok(` removida sem substituição (conferir `git diff web/tests/ | grep -c "^-ok("` == `git diff web/tests/ | grep -c "^+ok("` menos as asserções novas)
    - `bash scripts/executar.sh --testes` verde (se travar no host por `lsof`/`ps` — achado ambiental da Fase 35 —, rodar `cd server && ./.venv/bin/python -m pytest -q` + o loop dos `.mjs` e registrar o contorno no SUMMARY)
  </acceptance_criteria>
  <done>Os 6 guardiões medem o mesmo contrato no arquivo novo, com nota datada; guardião novo trava isolamento, tokens e a ausência em App.jsx. Commit `test(38-02): reaponta guardioes da camada de entendimento`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhuma nova | refatoração pura de front; nenhuma rota, dado ou entrada nova |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-06 | Tampering (integridade do isolamento) | `entendimento.jsx` × `OpcoesScreen.jsx` × `App.jsx` | mitigate | guardião (b)/(e) do `test_entendimento_modulo.mjs` + `test_opcoes_subabas_ui.mjs` já existente: nenhum dos dois importa App.jsx |
| T-38-07 | Repudiation (guardião silenciado) | 6 arquivos de teste reapontados | mitigate | regra explícita: reapontar, nunca apagar/afrouxar; nota datada por asserção; contagem de `ok(` no diff |
| T-38-08 | Integrity (princípio 4/7: texto de degradação e de IA) | `AiNote`, texto de falha da folha | mitigate | movidos byte a byte; `test_fonte_explicacao.mjs` passa a exigir 1x no módulo e 0x em App.jsx (sem duplicação divergente) |
| T-38-SC | Tampering (supply chain) | npm | accept | zero pacote novo |
</threat_model>

<verification>
1. `cd web && npx vite build` verde.
2. Todos os `web/tests/*.mjs` verdes (ou só as falhas ambientais conhecidas).
3. `cd server && ./.venv/bin/python -m pytest -q` verde (nada de backend muda, mas a suíte canônica roda as duas).
</verification>

<success_criteria>
- `SetorAlvo`/`ConceitoSheet` fora de App.jsx, num módulo que nenhum dos dois lados do isolamento importa ciclicamente.
- Zero mudança de comportamento; guardiões intactos no contrato.
</success_criteria>

<output>
Create `.planning/phases/38-kb-did-tica-ampliada/38-02-SUMMARY.md` when done.
</output>
