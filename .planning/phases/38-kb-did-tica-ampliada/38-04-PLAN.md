---
phase: 38-kb-did-tica-ampliada
plan: 04
type: execute
wave: 3
depends_on: ["38-03"]
files_modified:
  - web/src/glossario.js
  - web/tests/test_glossario.mjs
  - web/src/copy.js
  - web/src/App.jsx
autonomous: true
requirements: [KB-01]

must_haves:
  truths:
    - "Usuário abre Perfil → Glossário e vê os verbetes agrupados nas 9 famílias, recolhidas por padrão [KB-01, D-01, D-06]"
    - "Usuário digita e a lista filtra a cada tecla, sem limite de resultados, sem ida ao servidor [D-03, D-04]"
    - "Busca sem resultado mostra 'Nenhum verbete encontrado para \"{termo}\".' e a navegação por família continua visível abaixo [D-05]"
    - "Tocar um verbete abre a mesma ConceitoSheet do app, pela fonte kb [ROADMAP SC#2]"
    - "Falha do catálogo mostra erro com 'tentar de novo' — nunca lista parcial ou inventada [princípio 4]"
  artifacts:
    - path: "web/src/glossario.js"
      provides: "normalizarBusca, filtrarVerbetes, agruparPorFamilia (puros)"
      exports: ["normalizarBusca", "filtrarVerbetes", "agruparPorFamilia", "catalogoKbValido", "verbeteDoCatalogo"]
    - path: "web/tests/test_glossario.mjs"
      provides: "guardião comportamental do filtro + estático da tela/tile"
      min_lines: 70
  key_links:
    - from: "PerfilHub (tile Glossário)"
      to: "perfilView === \"glossario\""
      via: "onOpen(\"glossario\")"
      pattern: "onOpen\\(\"glossario\"\\)"
    - from: "TelaGlossario (linha de verbete)"
      to: "A.abrirVerbeteKb"
      via: "onClick"
      pattern: "abrirVerbeteKb\\("
    - from: "TelaGlossario (campo de busca)"
      to: "filtrarVerbetes"
      via: "derivação síncrona a cada render"
      pattern: "filtrarVerbetes\\("
---

<objective>
Entregar KB-01: a tela de busca de verbetes dentro de Perfil (D-06), com busca
livre + navegação por família na mesma tela (D-01), filtro live client-side sem
limite (D-03/D-04) e estado vazio que não esconde as famílias (D-05).

Purpose: a KB tem 83 verbetes e ponta a ponta de busca (`kbBuscar`) sem NENHUM
consumidor de UI hoje (achado do scout, STATE.md). Esta é a porta de entrada.
Output: filtros puros testados em `glossario.js`, `TelaGlossario` + tile +
rota de sub-tela em `App.jsx`, textos em `copy.js`.

Três escolhas de implementação (Claude's Discretion / Open Question #1 do
RESEARCH), declaradas para revisão:
1. Casamento do filtro: SUBSTRING normalizada (sem acento, minúscula) sobre
   título, termos e id — não a fronteira de palavra de `kb.buscar()`. D-02 trava
   o `kb.buscar()` do backend (usado pelo assistente), que esta tela NÃO chama;
   busca live exige casar prefixo ("rs" → RSI) enquanto se digita, o que a
   fronteira de palavra não faz. Troca de função pura, reversível.
2. Tile "Glossário" no grupo "Ajuda" do PerfilHub, `wide`, logo abaixo de "Como
   funciona" — o 38-UI-SPEC.md (status draft) pedia tile não-`wide` no `hubGrid`
   de "IA e desempenho"; glossário não é IA, e o grupo "Ajuda" já existe com o
   par temático. D-06 (tile em Perfil, padrão ProfileTile) respeitado.
3. Subtítulo do tile com a CONTAGEM REAL do catálogo carregado ("83 termos de
   ...") em vez do "83" literal do UI-SPEC: com a didática desligada o backend
   serve 74 (38-01), e número fixo na tela seria afirmação não medida.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-kb-did-tica-ampliada/38-CONTEXT.md
@.planning/phases/38-kb-did-tica-ampliada/38-UI-SPEC.md
@.planning/phases/38-kb-did-tica-ampliada/38-03-SUMMARY.md
@CLAUDE.md

<interfaces>
`ctx.kbCatalogo` (38-03): `undefined` (carregando) | `null` (falhou) | `{ modo, familias: [{id, rotulo}], verbetes: [{id, familia, titulo, texto, veja, termos}] }`.
`ctx.recarregarKb()` — refaz o fetch (retry).
`ctx.A.abrirVerbeteKb(vid)` — abre a folha global pela fonte kb.
`web/src/glossario.js` (38-03) já exporta `catalogoKbValido`, `verbeteDoCatalogo` — este plano ACRESCENTA exports, não altera os dois.

web/src/App.jsx (conferir por Grep):
- `function ProfileTile({ icon, title, sub, onClick, wide })` (~2473); `hubGroup`/`hubGrid` (~2486-2487).
- `function AjudaScreen({ ctx })` (~2587) e `function PerfilHub({ ctx, onOpen })` (logo depois); grupo `<div style={hubGroup}>Ajuda</div>` + `<ProfileTile wide onClick={() => onOpen("ajuda")} title="Como funciona" .../>` no fim do hub.
- Roteamento `tab === "perfil" && (perfilView === "plano" ? ... : perfilView === "ajuda" ? (<><BackHeader title="Como funciona" onBack={() => setPerfilView("hub")} /><AjudaScreen ctx={ctx} /></>) : <PerfilHub ctx={ctx} onOpen={setPerfilView} />)`.
- Token `field` (~365) para o input; skeleton `className="sk"` (~416).
- `test_perfil_reorg.mjs` coleta os tiles por `title="..."` LITERAL entre `function PerfilHub(` e `\nconst TIMING_STYLE = {` — o tile novo precisa de `title="Glossário"` literal, e `TelaGlossario` NÃO pode ser declarada dentro dessa fatia.

web/src/copy.js: `COPY.estudo` (~linha 19) e `COPY.operador` (~792); chave de referência `concentracaoLink: "saiba mais"` (~565 e ~1232) — chave nova entra nos DOIS blocos.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: filtros puros da busca (glossario.js) com guardião comportamental</name>
  <files>web/src/glossario.js, web/tests/test_glossario.mjs</files>
  <read_first>
    - web/src/glossario.js — o que o 38-03 criou (não reescrever os dois helpers existentes)
    - server/app/kb.py — `_normalizar` (linhas 60-65): mesma normalização NFD, para o front e o backend concordarem sobre acento/caixa
  </read_first>
  <behavior>
    - `normalizarBusca("  Índice ")` → `"indice"`; `normalizarBusca(null)` → `""`
    - `filtrarVerbetes(v, "")` e `filtrarVerbetes(v, "   ")` → `[]`
    - `filtrarVerbetes(v, "rs")` contém o verbete de título "RSI (IFR) — Índice de Força Relativa" (prefixo acha durante a digitação)
    - `filtrarVerbetes(v, "ifr")` acha o RSI pelo título; `filtrarVerbetes(v, "sobrevendido")` acha o RSI só pelos `termos`
    - `filtrarVerbetes(v, "indice")` e `filtrarVerbetes(v, "ÍNDICE")` dão o mesmo resultado (acento/caixa)
    - ordenação: título que COMEÇA com a busca antes de título que só CONTÉM, que vem antes de casamento só por termo/id; empate mantém a ordem do catálogo
    - sem limite: fixture com 7 verbetes casando "setup" devolve 7
    - `filtrarVerbetes(v, "([")`, `"*"`, `"\\"` não lançam exceção (sem RegExp construída do input)
    - `agruparPorFamilia(v, familias)` devolve `[{ id, rotulo, verbetes }]` na ORDEM de `familias`, só famílias com ≥1 verbete; verbete de família desconhecida não entra em grupo nenhum
  </behavior>
  <action>
    Acrescentar a `web/src/glossario.js`, mantendo o módulo puro (zero import):
    - `normalizarBusca(s)`: `String(s ?? "")` → `normalize("NFD")` → remover `/[̀-ͯ]/g` → `toLowerCase()` → `trim()`.
    - `filtrarVerbetes(verbetes, termo)`: `q = normalizarBusca(termo)`; vazio → `[]`; para cada verbete calcula posto 0 (título normalizado `startsWith(q)`), 1 (título `includes(q)`), 2 (algum `termos[i]` normalizado ou o `id` `includes(q)`), descarta sem posto; ordena por posto de forma ESTÁVEL (índice original como desempate). Só `String.prototype.includes/startsWith` — nenhuma `RegExp` a partir do input (T-38-14). Sem `slice`/limite (D-03).
    - `agruparPorFamilia(verbetes, familias)` conforme `<behavior>`.
    Comentário no topo das funções novas: por que substring e não fronteira de palavra (D-02 trava o backend; busca live precisa de prefixo), por que sem limite (D-03), e que a fórmula do assistente (`kb._pontuar`) NÃO é replicada aqui de propósito (Don't Hand-Roll do RESEARCH: duas implementações da mesma pontuação divergiriam).

    Criar `web/tests/test_glossario.mjs` (importa `../src/glossario.js`, esqueleto `ok()/fails`, roda sem build) com um fixture local de ~10 verbetes cobrindo os casos do `<behavior>`, incluindo o RSI com `termos: ["rsi", "ifr", "índice de força relativa", "sobrecomprado", "sobrevendido"]`.
  </action>
  <verify>
    <automated>node web/tests/test_glossario.mjs && node web/tests/test_kb_ponte.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node web/tests/test_glossario.mjs` passa com ≥ 12 asserções comportamentais (`grep -c "^ok(" web/tests/test_glossario.mjs` ≥ 12)
    - `node web/tests/test_kb_ponte.mjs` continua passando (helpers do 38-03 intactos)
    - `grep -cE "new RegExp|RegExp\(" web/src/glossario.js` == 0
    - `grep -cE "^import" web/src/glossario.js` == 0
  </acceptance_criteria>
  <done>Filtro e agrupamento puros, testados por comportamento. Commit `feat(38-04): filtros puros do glossario`.</done>
</task>

<task type="auto">
  <name>Task 2: TelaGlossario + tile em Perfil + rota de sub-tela + textos</name>
  <files>web/src/App.jsx, web/src/copy.js, web/tests/test_glossario.mjs</files>
  <read_first>
    - web/src/App.jsx — `ProfileTile` + `hubGroup`/`hubGrid` (~2473-2490), `AjudaScreen` (início, para o padrão de tela focada), o grupo "Ajuda" no fim de `PerfilHub`, o roteamento `perfilView === "ajuda"` (Grep), token `field` (~365)
    - web/src/copy.js — em volta de `concentracaoLink` nos dois blocos (~555-570 e ~1225-1235)
    - web/tests/test_perfil_reorg.mjs — linhas 125-160 (como os tiles são coletados e as citações "Perfil → X" conferidas)
    - .planning/phases/38-kb-did-tica-ampliada/38-UI-SPEC.md — "Spacing Scale", "Typography", "Color", "Copywriting Contract", seção 2 do "Phase-Specific Component Contract"
  </read_first>
  <action>
    1. `web/src/copy.js` — acrescentar nos DOIS blocos (`estudo` e `operador`), textos idênticos (são rótulos de interface, não voz de modo): `glossarioSub: (n) => (n ? n + " termos" : "Termos") + " de indicadores, estruturas e mecânica da B3 — busque ou navegue por categoria"`; `glossarioBuscaPlaceholder: "Buscar um termo (ex.: RSI, stop, IPO...)"`; `glossarioBuscaRotulo: "Buscar no glossário"`; `glossarioLimpar: "Limpar busca"`; `glossarioVazio: (termo) => \`Nenhum verbete encontrado para "${termo}".\``; `glossarioErro: "Não consegui carregar o glossário agora. Toque para tentar de novo."`. Comentário de bloco "Fase 38 (38-04, KB-01)".
    2. `web/src/App.jsx` — `function TelaGlossario({ ctx })` declarada IMEDIATAMENTE ANTES de `function PerfilHub(` (fora da fatia que `test_perfil_reorg.mjs` lê). Comportamento:
       - lê `ctx.kbCatalogo`, `ctx.recarregarKb`, `ctx.A`, `ctx.cp`; estado local `busca` (string) e `abertas` (conjunto de ids de família expandidos, vazio = tudo recolhido).
       - `undefined` → 3 blocos `className="sk"` (altura ~44px, raio 11px) — skeleton existente, sem spinner novo; aparece só enquanto o fetch único está em curso, nunca durante a digitação.
       - `null` → um `<button>` de largura total com `cp.glossarioErro`, `onClick={ctx.recarregarKb}`, estilo de aviso neutro (`T.textMuted`, borda `T.borderSubtle`, minHeight 44px) — nenhuma lista.
       - pronto → campo de busca: `<input type="search">` com o estilo `field` em largura total, `placeholder={cp.glossarioBuscaPlaceholder}`, `aria-label={cp.glossarioBuscaRotulo}`, `value`/`onChange` controlados, sem debounce e sem mínimo de caracteres; quando `busca` não vazio, botão "×" interno à direita (fundo transparente, altura interna do campo, `aria-label={cp.glossarioLimpar}`) que zera `busca`.
       - resultados, derivados SÍNCRONO a cada render (`useMemo` opcional) de `filtrarVerbetes(kbCatalogo.verbetes, busca)` e `agruparPorFamilia(kbCatalogo.verbetes, kbCatalogo.familias)`:
         (i) `busca` vazia → acordeão das famílias; (ii) `busca` com ≥1 resultado → lista plana, sem cap, sem agrupamento, cada linha com o rótulo da família como meta; (iii) `busca` com 0 resultado → `<p role="status">` com `cp.glossarioVazio(busca.trim())` ACIMA do acordeão, que continua visível e interativo (D-05).
       - cabeçalho de família: `<button aria-expanded={aberta}>` com o rótulo do backend em tipografia kicker (10px, 800, letterSpacing 0.08em, uppercase), contagem `(n)` e chevron; cor `T.accent` quando expandida, `T.textSecondary` em repouso; alterna `abertas` sem navegar.
       - linha de verbete (acordeão e lista plana, MESMO componente local): `<button>` minHeight 44px, padding 12px 14px, fundo `T.bgCard`, borda `T.borderSubtle`, raio 11px; título 13.5px/700 `T.textPrimary`; meta 11.5px `T.textMuted`; `onClick={() => ctx.A.abrirVerbeteKb(v.id)}`.
       - cores/espaços só dos tokens e da escala do UI-SPEC; accent só onde o UI-SPEC permite.
    3. `PerfilHub` — no grupo "Ajuda", logo abaixo do tile "Como funciona": `<ProfileTile wide onClick={() => onOpen("glossario")} title="Glossário" sub={ctx.cp.glossarioSub(ctx.kbCatalogo && ctx.kbCatalogo.verbetes ? ctx.kbCatalogo.verbetes.length : 0)} icon={...}/>` — `title` LITERAL (o guardião de perfil coleta por atributo literal); ícone SVG 19×19 viewBox 24, `stroke="currentColor"` strokeWidth ~1.8, round caps, glifo de livro aberto, no padrão dos irmãos.
    4. Roteamento: novo ramo `perfilView === "glossario"` → `(<><BackHeader title="Glossário" onBack={() => setPerfilView("hub")} /><TelaGlossario ctx={ctx} /></>)`, encaixado na cadeia ternária antes do ramo `"ajuda"`.
    5. Acrescentar a `web/tests/test_glossario.mjs` a parte estática sobre `App.jsx`/`copy.js`: `function TelaGlossario({ ctx })` existe e vem antes de `function PerfilHub(`; `onOpen("glossario")` e `title="Glossário"` no hub; ramo `perfilView === "glossario"`; `filtrarVerbetes(` e `agruparPorFamilia(` usados em TelaGlossario; `abrirVerbeteKb(` na linha de verbete; `aria-expanded` no cabeçalho; `recarregarKb` no estado de erro; as 6 chaves `glossario*` presentes nos DOIS blocos de `copy.js` (contar 2 ocorrências de cada nome) e a string exata "Nenhum verbete encontrado para"; `import` de `./glossario.js` em App.jsx.
  </action>
  <verify>
    <automated>node web/tests/test_glossario.mjs && node web/tests/test_perfil_reorg.mjs && cd web && npx vite build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `test_glossario.mjs` e `test_perfil_reorg.mjs` passam; `npx vite build` verde
    - `grep -c 'perfilView === "glossario"' web/src/App.jsx` == 1 e `grep -c 'onOpen("glossario")' web/src/App.jsx` == 1
    - `grep -c "glossarioVazio" web/src/copy.js` == 2
    - Loop completo `for t in web/tests/*.mjs; do node "$t" >/dev/null 2>&1 || echo "FALHOU: $t"; done` sem falha nova
    - Com backend + Vite locais de pé, `curl -s localhost:8787/api/kb/catalogo | head -c 200` responde JSON (pré-condição do checkpoint visual do 38-06); se não der para subir o ambiente, registrar no SUMMARY — a verificação visual fica no 38-06
  </acceptance_criteria>
  <done>Perfil → Glossário funcional nos três estados (carregando/erro/pronto) e nos três modos de resultado (famílias/lista/vazio+famílias). Commit `feat(38-04): tela Glossario em Perfil (KB-01)`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário → campo de busca | texto livre digitado; NUNCA sai do aparelho (filtro local) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-14 | Denial of service (ReDoS local) | `filtrarVerbetes` | mitigate | só `includes`/`startsWith`; zero `RegExp` do input — grep no acceptance + casos `"(["`, `"*"`, `"\\"` no teste |
| T-38-15 | Information disclosure | termo buscado | accept | não é enviado a servidor nem logado; nenhuma telemetria nova nesta tela |
| T-38-16 | Integrity (princípio 4) | catálogo ausente/falho | mitigate | três estados explícitos; erro com retry, sem lista parcial; contagem do tile vem do catálogo carregado, não de literal |
| T-38-17 | Tampering / XSS | render de títulos/rótulos | mitigate | nós de texto React; nenhum HTML cru |
</threat_model>

<verification>
1. `node web/tests/test_glossario.mjs` verde.
2. `cd web && npx vite build` verde.
3. Loop de todos os `.mjs` sem falha nova.
</verification>

<success_criteria>
- KB-01 entregue: busca livre + famílias, live, sem limite, vazio honesto, erro com retry.
- Tocar verbete abre a `ConceitoSheet` do app (fonte kb).
</success_criteria>

<output>
Create `.planning/phases/38-kb-did-tica-ampliada/38-04-SUMMARY.md` when done.
</output>
