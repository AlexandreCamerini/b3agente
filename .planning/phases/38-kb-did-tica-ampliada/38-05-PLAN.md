---
phase: 38-kb-did-tica-ampliada
plan: 05
type: execute
wave: 4
depends_on: ["38-04"]
files_modified:
  - web/src/glossario.js
  - web/src/copy.js
  - web/src/App.jsx
  - web/src/opcoes/OpcoesScreen.jsx
  - web/tests/test_kb_ancoras.mjs
  - server/tests/test_kb_ancoras.py
autonomous: false
requirements: [KB-02]

must_haves:
  truths:
    - "Nas abas Acompanhar, Radar, Watchlist e Opções o usuário vê um link 'saiba mais' que abre SEMPRE o mesmo verbete aprovado pelo Alex para aquela aba [KB-02, D-07, D-08]"
    - "OpcoesScreen.jsx abre o verbete com a ConceitoSheet importada do módulo compartilhado, sem importar nada de App.jsx [ROADMAP SC#4]"
    - "Nenhum link aponta para verbete inexistente: o link só aparece com a didática ligada e o verbete presente no catálogo carregado, e um guardião de backend reprova vid que não existe em kb.py"
  artifacts:
    - path: "web/src/glossario.js"
      provides: "ANCORAS_KB — fonte única dos 4 vid aprovados"
      contains: "export const ANCORAS_KB"
    - path: "web/tests/test_kb_ancoras.mjs"
      provides: "guardião dos 4 links (vid fixo, fonte kb, isolamento de Opções)"
      min_lines: 40
    - path: "server/tests/test_kb_ancoras.py"
      provides: "guardião cross-linguagem: todo vid de ANCORAS_KB existe em kb.catalogo()"
      min_lines: 20
  key_links:
    - from: "EvolucaoScreen / RadarScreen / MercadoScreen"
      to: "A.abrirVerbeteKb(ANCORAS_KB.<aba>)"
      via: "botão 'saiba mais'"
      pattern: "abrirVerbeteKb\\(ANCORAS_KB\\."
    - from: "web/src/opcoes/OpcoesScreen.jsx"
      to: "web/src/entendimento.jsx"
      via: "import { ConceitoSheet } + estado local"
      pattern: "from \"\\.\\./entendimento\\.jsx\""
---

<objective>
Entregar KB-02: um link "saiba mais" fixo por aba (D-07) nas 4 abas sem
cobertura, com o `vid` de cada uma APROVADO PELO ALEX antes de qualquer código
(D-08 — por isso a Task 1 é um checkpoint de decisão bloqueante).

Purpose: hoje só o Portfólio tem ponta de entrada para a KB (alerta de
concentração → `diversificacao`). Output: `ANCORAS_KB` em `glossario.js`
(fonte única), 3 links em `App.jsx`, 1 link em `OpcoesScreen.jsx` com
`ConceitoSheet` importada de `../entendimento.jsx` e estado local, dois
guardiões (front + backend).

Mecanismo em Opções (RESEARCH Pattern 3, Opção B — segue o texto travado do
CONTEXT: "ambos importam do módulo novo, nenhum importa do outro"):
`OpcoesScreen.jsx` monta a PRÓPRIA instância de `ConceitoSheet`, com estado
local. Tensão conhecida, declarada e não resolvida aqui: o MESMO arquivo já
abre o chip de liquidez pelo overlay GLOBAL (`A.abrirVerbete("liquidez-opcao",
...)`, guardado por `test_faixa_liquidez_ui.mjs`) — após este plano, Opções
passa a ter duas vias de folha (global para o chip ancorado, local para o
"saiba mais"). Efeito colateral a conferir no 38-06: a folha local não entra em
`overlayLivre` do App, então o FAB do Boris não se esconde sob ela.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-kb-did-tica-ampliada/38-CONTEXT.md
@.planning/phases/38-kb-did-tica-ampliada/38-UI-SPEC.md
@.planning/phases/38-kb-did-tica-ampliada/38-04-SUMMARY.md
@CLAUDE.md

<interfaces>
`ctx.A.abrirVerbeteKb(vid)` (38-03) — abre a folha global pela fonte kb.
`ctx.kbCatalogo` (38-03) — `undefined | null | { modo, familias, verbetes }`.
`verbeteDoCatalogo(cat, vid)` (web/src/glossario.js, 38-03) — verbete ou `null`.
`ConceitoSheet` (web/src/entendimento.jsx, 38-02/03) — props `{ cid, dados, setor, onClose, onTrocar, didatica, voltar, fonte = "conceito", kbCatalogo = null }`.

Precedente visual (web/src/App.jsx ~4358, Portfólio):
`{ctx.didatica && ctx.didatica.ligada && (<button type="button" onClick={() => A.abrirVerbete("diversificacao", {...})} style={{ background: "transparent", border: "none", padding: 0, marginTop: "6px", color: T.accent, fontWeight: 700, fontSize: "12px", textDecoration: "none" }}>{cp.concentracaoLink}</button>)}`

Pontos de inserção (Grep para confirmar — ficam logo após o parágrafo de introdução sob o `<h1>` de cada tela, "uma linha genérica, não banner", sem competir com o veredito/CTA principal):
- `function EvolucaoScreen({ ctx })` (~2057): após o `<p>` de `cp.resumoDia(...)`.
- `function MercadoScreen({ ctx })` (~3853): após a linha de `cp.subtituloWatchlist`.
- `function RadarScreen({ ctx })` (~6678): após `<p ...>{cp.subtituloRadar}</p>`.
- `web/src/opcoes/OpcoesScreen.jsx` `export default function OpcoesScreen({ ctx })` (~246): após `<p ...>{cp.subtituloOpcoes || ""}</p>` (antes de `{subabas}`, visível nas duas sub-abas). Hooks do componente começam ~276 — o `useState` novo entra junto deles, antes de qualquer return. `T` local já tem `accent` em `TOKENS` (~95).
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Alex aprova o verbete de cada aba (D-08)</name>
  <decision>Qual `vid` (dentre os verbetes de `server/app/kb.py`) o "saiba mais" de cada uma das 4 abas abre — fixo, sem lógica por contexto (D-07).</decision>
  <context>
    D-08 exige aprovação explícita antes de codar. A proposta abaixo vem do
    38-UI-SPEC.md, validada contra o catálogo real no 38-RESEARCH.md (os 4 ids
    existem em `kb.py`). O CONTEXT sugeria, como critério, família
    `estados_app` para Acompanhar — a proposta diverge ali (escolheu
    `mercado_b3`), por isso há alternativa nessa família.
    Observação: `confluencia` é verbete DERIVADO de `conceitos.py`; aberto pela
    KB mostra a versão genérica (sem números de ativo) — correto para link fixo.
  </context>
  <options>
    <option id="proposta">
      <name>Proposta do UI-SPEC: Acompanhar → mkt-carteira-simulada · Radar → confluencia · Watchlist → ind-rsi · Opções → mkt-opcao</name>
      <pros>Cada verbete define a mecânica central da tela; `mkt-carteira-simulada` reforça na home o princípio 1 (saldo fictício); `mkt-opcao` é pré-requisito de toda a aba Opções.</pros>
      <cons>Acompanhar foge da família sugerida no CONTEXT (`estados_app`); `ind-rsi` elege um indicador entre 12 como "porta" da Watchlist.</cons>
    </option>
    <option id="alternativa-acompanhar">
      <name>Igual à proposta, mas Acompanhar → estado-armado (família estados_app, sugestão do CONTEXT)</name>
      <pros>Alinha com o critério do CONTEXT; explica o estado que o usuário mais vê nos cards.</pros>
      <cons>A tela Acompanhar é patrimônio/curva/resumo do dia, não o card de timing — o verbete fica menos colado ao que está na tela.</cons>
    </option>
    <option id="alternativa-watchlist">
      <name>Igual à proposta, mas Watchlist → familia-tendencia (em vez de ind-rsi)</name>
      <pros>Porta mais conceitual (família de leitura) e o "veja também" leva aos indicadores.</pros>
      <cons>Menos reconhecível para o iniciante que já ouviu falar de RSI.</cons>
    </option>
    <option id="outra">
      <name>Outra combinação — o Alex dita os 4 ids</name>
      <pros>Controle total.</pros>
      <cons>O executor valida cada id contra `kb.catalogo()` antes de codar; id inexistente volta para o Alex.</cons>
    </option>
  </options>
  <read_first>
    - .planning/phases/38-kb-did-tica-ampliada/38-UI-SPEC.md — seção 3 (tabela com justificativa file:line)
    - .planning/phases/38-kb-did-tica-ampliada/38-CONTEXT.md — D-07, D-08 e "Claude's Discretion"
  </read_first>
  <action>
    Apresentar as opções ao Alex e PARAR. Não escrever código desta fase antes da resposta. Com a resposta: (a) se "outra", conferir cada id com `cd server && ./.venv/bin/python -c "from app import kb; print(kb.verbete('<id>') is not None)"` e devolver ao Alex qualquer id que imprima `False`; (b) registrar a tabela aprovada (aba → vid → família) no topo do `38-05-SUMMARY.md` como "Resolução de D-08", com a data.
  </action>
  <acceptance_criteria>
    - Resposta explícita do Alex registrada (uma das 4 opções, ou os 4 ids ditados)
    - Os 4 ids aprovados existem em `kb.catalogo()` (comando do `<action>` imprime `True` para cada)
  </acceptance_criteria>
  <verify>
    <automated>cd server && ./.venv/bin/python -c "from app import kb; ids={'mkt-carteira-simulada','confluencia','ind-rsi','mkt-opcao','estado-armado','familia-tendencia'}; print(all(kb.verbete(i) for i in ids))"</automated>
  </verify>
  <resume-signal>Responda "proposta", "alternativa-acompanhar", "alternativa-watchlist" ou dite os 4 ids (Acompanhar, Radar, Watchlist, Opções)</resume-signal>
  <done>Tabela aba → vid aprovada e registrada.</done>
</task>

<task type="auto">
  <name>Task 2: ANCORAS_KB + 3 links em App.jsx + guardião de backend</name>
  <files>web/src/glossario.js, web/src/copy.js, web/src/App.jsx, server/tests/test_kb_ancoras.py</files>
  <read_first>
    - web/src/glossario.js — inteiro (curto)
    - web/src/App.jsx — ~20 linhas em volta de cada ponto de inserção (Grep por `cp.resumoDia(`, `cp.subtituloWatchlist`, `cp.subtituloRadar`) e o precedente `A.abrirVerbete("diversificacao"` (~4358); conferir como cada tela obtém `A`/`cp`/`didatica` do `ctx`
    - web/src/copy.js — em volta de `concentracaoLink` nos dois blocos
    - server/tests/test_kb.py — fixture `_flags_limpas` (reusar o padrão)
  </read_first>
  <action>
    1. `web/src/glossario.js`: `export const ANCORAS_KB = Object.freeze({ evolucao: "<vid aprovado>", radar: "<vid>", mercado: "<vid>", opcoes: "<vid>" })` com os ids da Task 1 e comentário: D-07 (fixo por aba), D-08 (aprovado pelo Alex em <data>, ver 38-05-SUMMARY.md), chaves = ids de `tab` do App, e o aviso de que `server/tests/test_kb_ancoras.py` reprova id inexistente em `kb.py`.
    2. `web/src/copy.js`: chave `saibaMais: "saiba mais"` nos DOIS blocos (mesma string de `concentracaoLink`, que fica intocada — ela é do alerta de concentração, esta é genérica), comentário "Fase 38 (38-05, KB-02)".
    3. `web/src/App.jsx`: importar `ANCORAS_KB` (e `verbeteDoCatalogo`, se ainda não importado) de `./glossario.js`. Em `EvolucaoScreen`, `RadarScreen` e `MercadoScreen`, no ponto de inserção do `<interfaces>`, o botão com o estilo EXATO do precedente de Portfólio (`background: "transparent", border: "none", padding: 0, marginTop: "6px", color: T.accent, fontWeight: 700, fontSize: "12px", textDecoration: "none"`), `type="button"`, texto `{cp.saibaMais}`, `onClick={() => A.abrirVerbeteKb(ANCORAS_KB.<evolucao|radar|mercado>)}`, renderizado SÓ quando `ctx.didatica && ctx.didatica.ligada && verbeteDoCatalogo(ctx.kbCatalogo, ANCORAS_KB.<aba>)` (mesmo portão do precedente + verbete presente: sem link morto enquanto carrega, com falha ou com didática desligada). NÃO usar `A.abrirSetor` (poluiria `gestoUso`) nem `A.openConceito` (poluiria `coach_tip_*`).
    4. Criar `server/tests/test_kb_ancoras.py`: lê `web/src/glossario.js` do repositório (caminho relativo a `__file__`), extrai por regex o objeto `ANCORAS_KB` e seus 4 pares, e afirma: exatamente as chaves `evolucao`, `radar`, `mercado`, `opcoes`; cada valor existe em `{v["id"] for v in kb.catalogo()}` com flags limpas; cada valor tem `texto` não vazio nos dois modos. Precedente de guardião cross-linguagem: a paridade `defaults.py` ↔ `catalog.js` (CLAUDE.md). Prova negativa no mesmo arquivo: a função de extração aplicada a um texto fixo com `ANCORAS_KB = Object.freeze({ evolucao: "id-inexistente" ... })` produz um id que NÃO está no catálogo.
  </action>
  <verify>
    <automated>cd server && ./.venv/bin/python -m pytest -q tests/test_kb_ancoras.py && cd ../web && npx vite build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "abrirVerbeteKb(ANCORAS_KB\." web/src/App.jsx` == 3
    - `grep -c "saibaMais" web/src/copy.js` == 2
    - `pytest tests/test_kb_ancoras.py` verde
    - `npx vite build` verde
    - `node web/tests/test_concentracao_carteira.mjs` e `node web/tests/test_c09_drawdown_alerta.mjs` passam sem edição (links novos não entram no bloco do card de Portfólio)
  </acceptance_criteria>
  <done>3 links fixos em App.jsx com portão honesto; fonte única dos vids travada contra o catálogo real. Commit `feat(38-05): saiba mais fixo em Acompanhar/Radar/Watchlist (KB-02)`.</done>
</task>

<task type="auto">
  <name>Task 3: link em Opções com ConceitoSheet local importada do módulo compartilhado + guardião</name>
  <files>web/src/opcoes/OpcoesScreen.jsx, web/tests/test_kb_ancoras.mjs</files>
  <read_first>
    - web/src/opcoes/OpcoesScreen.jsx — linhas 1-100 (bloco de imports com os comentários de isolamento + TOKENS) e 246-330 (início do componente, hooks) e ~773-785 (h1/subtítulo)
    - web/tests/test_opcoes_subabas_ui.mjs — linhas 80-110 (invariante "OpcoesScreen.jsx não importa App.jsx")
    - web/tests/test_faixa_liquidez_ui.mjs — linhas 140-160 (o chip de liquidez usa o overlay global — NÃO mexer)
    - web/src/App.jsx — `A.trocarConceito`/`A.voltarConceito` (o padrão de trilha a espelhar localmente)
  </read_first>
  <action>
    1. `web/src/opcoes/OpcoesScreen.jsx`:
       - Imports, no bloco existente, cada um com comentário no MESMO estilo dos vizinhos ("Fase 38 (38-05): módulo terceiro, nenhum import de App.jsx — isolamento ADR-027 intacto"): `import { ConceitoSheet } from "../entendimento.jsx";` e `import { ANCORAS_KB, verbeteDoCatalogo } from "../glossario.js";`.
       - Hook novo junto dos demais `useState` do componente: `const [verbeteAberto, setVerbeteAberto] = useState(null);` guardando `{ cid, trilha: [] }`.
       - Após o `<p>` de `cp.subtituloOpcoes`: o botão com o estilo EXATO do precedente (usar o `T` local — `accent` já está em `TOKENS`), texto `{cp.saibaMais || "saiba mais"}`, `onClick={() => setVerbeteAberto({ cid: ANCORAS_KB.opcoes, trilha: [] })}`, renderizado só com `ctx.didatica && ctx.didatica.ligada && verbeteDoCatalogo(ctx.kbCatalogo, ANCORAS_KB.opcoes)`.
       - No fim do `<section>` do return principal: `{verbeteAberto && <ConceitoSheet cid={verbeteAberto.cid} dados={null} fonte="kb" kbCatalogo={ctx.kbCatalogo} didatica={ctx.didatica} onClose={() => setVerbeteAberto(null)} onTrocar={(vid) => setVerbeteAberto((v) => ({ cid: vid, trilha: [...v.trilha, v.cid] }))} voltar={verbeteAberto.trilha.length ? () => setVerbeteAberto((v) => ({ cid: v.trilha[v.trilha.length - 1], trilha: v.trilha.slice(0, -1) })) : null} />}` — mesma semântica de trilha do App, estado próprio; dado atravessa o isolamento por `ctx` (canal estabelecido), nunca por import.
       - NÃO tocar a chamada existente `A.abrirVerbete("liquidez-opcao", ...)` em `SubAbaOperar`.
    2. Criar `web/tests/test_kb_ancoras.mjs` (estático, roda sem build): `ANCORAS_KB` com as 4 chaves; as 3 chamadas `abrirVerbeteKb(ANCORAS_KB.evolucao|radar|mercado)` em App.jsx, cada uma dentro da fatia da tela certa (fatiar `function EvolucaoScreen(` / `function MercadoScreen(` / `function RadarScreen(` até o próximo `\nfunction ` de topo) e cada uma guardada por `didatica.ligada` e `verbeteDoCatalogo(`; nenhum dos 4 links usa `abrirSetor`/`openConceito`/`abrirVerbete(` antigo; `OpcoesScreen.jsx` importa `../entendimento.jsx` e `../glossario.js`, NÃO importa `App.jsx`, monta `<ConceitoSheet` com `fonte="kb"` e `kbCatalogo={ctx.kbCatalogo}`, e continua contendo `abrirVerbete("liquidez-opcao"`; estilo do link idêntico ao precedente (`color: T.accent, fontWeight: 700, fontSize: "12px"`) nas 4 ocorrências.
  </action>
  <verify>
    <automated>node web/tests/test_kb_ancoras.mjs && node web/tests/test_opcoes_subabas_ui.mjs && node web/tests/test_faixa_liquidez_ui.mjs && node web/tests/test_entendimento_modulo.mjs && cd web && npx vite build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Os 4 testes do `<verify>` passam e `npx vite build` verde
    - `grep -cE "from \"[^\"]*App\.jsx\"" web/src/opcoes/OpcoesScreen.jsx` == 0
    - `grep -c "from \"../entendimento.jsx\"" web/src/opcoes/OpcoesScreen.jsx` == 1
    - Loop completo `for t in web/tests/*.mjs; do node "$t" >/dev/null 2>&1 || echo "FALHOU: $t"; done` sem falha nova, e `cd server && ./.venv/bin/python -m pytest -q` verde
  </acceptance_criteria>
  <done>4/4 abas com "saiba mais" fixo e aprovado; Opções abre a folha pelo módulo compartilhado sem tocar App.jsx. Commit `feat(38-05): saiba mais em Opcoes via entendimento.jsx (KB-02)`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App.jsx ↔ opcoes/OpcoesScreen.jsx | fronteira de isolamento de import (ADR-027); dado atravessa só por `ctx` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-18 | Tampering (isolamento) | import em OpcoesScreen.jsx | mitigate | só módulos terceiros (`entendimento.jsx`, `glossario.js`); `test_opcoes_subabas_ui.mjs` + `test_kb_ancoras.mjs` reprovam import de App.jsx |
| T-38-19 | Integrity (link morto / dado inválido) | 4 links fixos | mitigate | portão `didatica.ligada` + `verbeteDoCatalogo(...)` no render; `test_kb_ancoras.py` reprova vid ausente de `kb.py` |
| T-38-20 | Repudiation (decisão não registrada) | escolha dos vids | mitigate | checkpoint:decision bloqueante + registro datado no SUMMARY + comentário em `ANCORAS_KB` |
| T-38-21 | Integrity (telemetria) | `gestoUso`/`coach_tip_*` | mitigate | links usam `abrirVerbeteKb`/estado local, nunca `abrirSetor`/`openConceito` — asserção negativa no guardião |
</threat_model>

<verification>
1. `node web/tests/test_kb_ancoras.mjs` e `pytest tests/test_kb_ancoras.py` verdes.
2. `cd web && npx vite build` verde.
3. Suítes completas sem falha nova.
</verification>

<success_criteria>
- KB-02: 4 abas com "saiba mais" fixo, vids aprovados pelo Alex.
- SC#4: OpcoesScreen.jsx importa a ConceitoSheet do módulo compartilhado e não importa App.jsx.
</success_criteria>

<output>
Create `.planning/phases/38-kb-did-tica-ampliada/38-05-SUMMARY.md` when done.
</output>
