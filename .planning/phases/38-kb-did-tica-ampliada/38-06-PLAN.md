---
phase: 38-kb-did-tica-ampliada
plan: 06
type: execute
wave: 5
depends_on: ["38-05"]
files_modified:
  - web/src/version.js
  - server/web_dist
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [KB-01, KB-02]

must_haves:
  truths:
    - "O Alex viu, rodando, a busca do Glossário, os 4 'saiba mais' e os fluxos antigos da folha, e aprovou [KB-01, KB-02]"
    - "A fase está NO AR para o PWA — front (carimbo novo) e backend (rota /api/kb/catalogo) — conferido em produção, não só verde na suíte local"
    - "KB-01/KB-02 marcados Done e a Fase 38 fechada em ROADMAP/STATE, editados À MÃO"
  artifacts:
    - path: "web/src/version.js"
      provides: "BUILD_ID novo carimbando a entrega da Fase 38"
      contains: "F10-"
    - path: ".planning/REQUIREMENTS.md"
      provides: "KB-01 e KB-02 Done"
      contains: "KB-02"
  key_links:
    - from: "web/src/version.js (BUILD_ID)"
      to: "server/web_dist"
      via: "scripts/bump.sh seguido de scripts/publicar-web.sh"
      pattern: "F10-[0-9]{8}-[0-9]{2}"
---

<objective>
Fechar a fase: verificação humana ao vivo, publicação (front + backend juntos —
a rota nova do 38-01 e o bundle que a consome precisam subir no mesmo deploy) e
atualização à MÃO dos documentos de planejamento.

Purpose: plano que toca `web/src/` sem task de bump+publicação fica testado e
nunca vai ao ar (defeito de processo registrado na memória do projeto). E o
checkpoint humano segura o push da FASE INTEIRA, não só desta task.
Output: build carimbado no ar, `/api/kb/catalogo` respondendo em produção, docs
coerentes com a realidade.
</objective>

<repo_guardrail>
**Não chamar mutador de estado do `gsd-sdk` neste plano.** `state.advance-plan`,
`state.record-session`, `state.record-metric`, `state.add-decision` corrompem o
`.planning/STATE.md` deste repositório (decisão do Alex, 2026-09-11, CLAUDE.md).
Editar `STATE.md`/`ROADMAP.md`/`REQUIREMENTS.md` com a ferramenta Edit e conferir
com `git diff .planning/` ANTES do commit. Verbos de LEITURA do SDK seguem ok.
</repo_guardrail>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-kb-did-tica-ampliada/38-CONTEXT.md
@.planning/phases/38-kb-did-tica-ampliada/38-05-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: verificação humana — Glossário, 4 "saiba mais" e regressão da folha</name>
  <what-built>
    - Perfil → grupo Ajuda → tile "Glossário" (contagem real de termos) abre uma
      tela com busca live + 9 famílias recolhidas (38-04).
    - "saiba mais" fixo em Acompanhar, Radar, Watchlist (overlay global) e
      Opções (folha local importada de `entendimento.jsx`), com os vids
      aprovados na Resolução de D-08 (38-05-SUMMARY.md).
    - A `ConceitoSheet` agora tem duas fontes: `conceito` (ancorada nos números
      do card, comportamento antigo) e `kb` (verbete genérico do glossário,
      sem caixa "Pergunte à IA") (38-03).
    - `SetorAlvo`/`ConceitoSheet`/`AssistenteBox`/`AiNote` moraram de arquivo
      (38-02) — os fluxos antigos precisam estar idênticos.
  </what-built>
  <how-to-verify>
    Subir backend + Vite locais (`bash scripts/executar.sh`; se travar em
    `lsof`/`ps` — achado ambiental da Fase 35 —, subir backend e Vite
    diretamente). Conferir antes: `curl -s localhost:8787/api/kb/catalogo`
    devolve JSON com `verbetes`.

    1. **Glossário, famílias** — Perfil → Ajuda → Glossário: o subtítulo do
       tile mostra a contagem (83 com a didática ligada); a tela abre com as 9
       famílias recolhidas; expandir e recolher uma.
    2. **Busca live** — digitar "rs": o RSI aparece já na 2ª letra; "índice" e
       "indice" dão o mesmo resultado; "ifr" acha o RSI; a lista não para em 5
       (ex.: "setup").
    3. **Vazio** — digitar "xyzzy": aparece `Nenhum verbete encontrado para
       "xyzzy".` e as famílias CONTINUAM visíveis e clicáveis abaixo; o "×"
       limpa e volta às famílias.
    4. **Folha kb** — tocar um verbete: título, subtítulo "Verbete do glossário
       — explicação geral, sem números de nenhum ativo.", um parágrafo, chips
       "veja também" que navegam e "‹ voltar" que volta; SEM a caixa "Pergunte
       à IA sobre estes números".
    5. **4 abas** — em Acompanhar, Radar, Watchlist e Opções, o "saiba mais"
       aparece como uma linha discreta sob a introdução e abre o verbete
       aprovado de cada uma.
    6. **Opções** — abrir a folha pelo "saiba mais", navegar um "veja também",
       voltar, fechar; conferir se o FAB do Boris fica visível por cima/por
       baixo da folha local (limitação declarada no 38-05 — descrever o que
       viu); o chip de liquidez de um candidato (sub-aba de operar) continua
       abrindo a explicação ANCORADA de sempre.
    7. **Regressão da folha ancorada** — na Watchlist, tocar um termo
       sublinhado de um card: a folha mostra "Explicação com os números deste
       ativo, agora.", os blocos O QUE O APP NÃO FAZ / O QUE É / O QUE
       ACONTECE e a caixa "Pergunte à IA"; no Portfólio com concentração alta,
       o "saiba mais" de diversificação continua citando ticker e %.
    8. **Modo Operador** — trocar para Operador: reabrir o Glossário, buscar
       "atingido" — o título vem "Gatilho atingido" (no Estudo: "Condição de
       estudo atingida").
    9. **375px** — tela do Glossário e as 4 linhas "saiba mais" sem quebra.

    Divergência nos itens 4 ou 7 (texto genérico onde deveria haver número, ou
    número onde deveria ser genérico) é BLOQUEANTE: é o princípio 1 quebrado.
  </how-to-verify>
  <read_first>
    - .planning/phases/38-kb-did-tica-ampliada/38-05-SUMMARY.md — Resolução de D-08 (os vids a conferir no item 5)
    - .planning/phases/38-kb-did-tica-ampliada/38-UI-SPEC.md — Copywriting Contract (strings exatas)
  </read_first>
  <action>
    Subir o ambiente local, apresentar ao Alex o roteiro de 9 itens e PARAR.
    Não publicar, não dar push e não seguir para a Task 2 antes da resposta
    (checkpoint bloqueante segura o push da FASE INTEIRA).
  </action>
  <acceptance_criteria>
    - O Alex responde "aprovado" ou descreve divergências. Nenhum push/publicação antes disso.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — verificação visual/interativa por desenho; os substitutos automatizados são `node web/tests/test_glossario.mjs`, `node web/tests/test_kb_ancoras.mjs`, `node web/tests/test_kb_ponte.mjs` e `pytest tests/test_kb_catalogo.py`, que cobrem estrutura e contrato mas não o render real</automated>
  </verify>
  <resume-signal>Digite "aprovado" ou descreva o que divergiu</resume-signal>
  <done>Aprovação humana registrada, ou lista de divergências a corrigir antes de publicar.</done>
</task>

<task type="auto">
  <name>Task 2: publicar (front + backend no mesmo push) e conferir em produção</name>
  <files>web/src/version.js, server/web_dist</files>
  <read_first>
    - scripts/bump.sh — linhas 1-25
    - scripts/publicar-web.sh — o fluxo inteiro
    - CLAUDE.md — "Publicação" e "Três esquemas de versão"
  </read_first>
  <action>
    Ordem obrigatória, sem atalho:
    1. `git fetch origin && git merge origin/main` ANTES de carimbar (`bump.sh` deriva o próximo BUILD_ID do valor LOCAL; sessão paralela que carimbou hoje colide). Conflito em `server/web_dist` se resolve REGERANDO, nunca mesclando.
    2. Suíte canônica pré-bump: `bash scripts/executar.sh --testes` (contorno se travar: pytest + loop dos `.mjs`, registrando no SUMMARY).
    3. `bash scripts/bump.sh`, depois `bash scripts/publicar-web.sh`. Nunca editar `server/web_dist` à mão.
    4. Commit + push em `v2/interacao-estrutural` e fast-forward de `origin/main` (padrão da Fase 35: publicação é push direto, sem PR). O Railway (rootDirectory `/server`) sobe o backend com a rota nova E o `web_dist` novo no mesmo deploy.
    5. Conferir no ar (aguardar o redeploy, ~3-4 min na Fase 35): `curl -s https://boris.semente.dev/api/health` mostra o `SERVER_BUILD_ID` derivado do BUILD_ID novo; `curl -s "https://boris.semente.dev/api/kb/catalogo" | python3 -c "import sys,json; d=json.load(sys.stdin); print(len(d['verbetes']), len(d['familias']))"` imprime a contagem (83 9 com a didática ligada em produção). Colar as duas saídas no SUMMARY.

    Limitação a declarar no SUMMARY, não a resolver: o app iOS carrega bundle LOCAL (sem `server.url`) — Glossário e "saiba mais" só chegam ao iPhone num build novo de TestFlight; a rota de backend já vale para ele. Decisão do Alex, fora do escopo.
    Falha de rede por sandbox/TLS: reportar o erro exato e o passo, sem contorno inventado.
  </action>
  <acceptance_criteria>
    - `grep -E "F10-[0-9]{8}-[0-9]{2}" web/src/version.js` mostra carimbo com a data real de hoje
    - `/api/health` de produção com o `SERVER_BUILD_ID` novo (saída colada no SUMMARY)
    - `/api/kb/catalogo` de produção respondendo com `verbetes` e `familias` (saída colada no SUMMARY)
    - `git log origin/main -1 --format=%H` == `git rev-parse HEAD`
  </acceptance_criteria>
  <verify>
    <automated>grep -E "F10-[0-9]{8}-[0-9]{2}" web/src/version.js</automated>
  </verify>
  <done>Fase 38 no ar no PWA, rota nova conferida em produção, pendência iOS registrada.</done>
</task>

<task type="auto">
  <name>Task 3: fechar os documentos de planejamento à mão</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — seção "KB Didática" (~linhas 13-22) e a tabela de rastreio (~60-70)
    - .planning/ROADMAP.md — item da Fase 38 no topo (~linha 20) e a seção "### Phase 38" (~209-230)
    - .planning/STATE.md — frontmatter, "Current Position" e o formato das entradas "Posição anterior"
  </read_first>
  <action>
    Com a ferramenta Edit (nunca mutador do `gsd-sdk`):
    1. `REQUIREMENTS.md`: `- [x]` em KB-01 e KB-02; `Pending` → `Done` nas duas linhas da tabela.
    2. `ROADMAP.md`: `- [x]` na Fase 38 com `(6/6 plans) — completed <data>`; na seção da fase, marcar os 6 planos como concluídos com a data.
    3. `STATE.md`: frontmatter (`status`, `last_updated`, `last_activity`, `progress`); "Current Position" reescrito para a Fase 38 fechada (o que foi entregue, BUILD_ID publicado, a Resolução de D-08, as decisões de implementação que o próximo leitor não deve redescobrir: discriminador `fonte` com default `conceito` e ação separada `abrirVerbeteKb`; lookup de "veja também" por fonte; filtro por substring e não por fronteira de palavra; tile no grupo Ajuda; contagem dinâmica no tile; catálogo exclui verbete de texto vazio com a didática desligada; módulo chamado `entendimento.jsx`); a posição anterior vai para "Posição anterior", texto da época preservado.
    4. Pendências abertas, declaradas sem fingir que foram feitas: build iOS/TestFlight; tensão das duas vias de folha em Opções (global no chip de liquidez, local no "saiba mais") + o que o item 6 do checkpoint mostrou sobre o FAB; a Fase 39 (ESTADO-01) herda `entendimento.jsx`/`glossario.js` como módulos terceiros já disponíveis.
    Commit dos docs separado (`docs(38): fecha fase 38`), depois de conferir `git diff .planning/`.
  </action>
  <acceptance_criteria>
    - `grep -c "KB-0[12].*Done" .planning/REQUIREMENTS.md` == 2 e `grep -c "\[ \] \*\*KB-0" .planning/REQUIREMENTS.md` == 0
    - `grep -n "Phase 38" .planning/ROADMAP.md | head -1` mostra `[x]`
    - `git diff .planning/STATE.md` mostra só as seções pretendidas — sem texto de sessão antiga sobrescrito, sem métrica fora de tabela, sem contador de planos errado
    - `bash scripts/executar.sh --testes` (ou o contorno) ainda na baseline
  </acceptance_criteria>
  <verify>
    <automated>git diff --stat .planning/ && grep -c "KB-0[12].*Done" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>Documentos refletem a realidade, editados à mão, conferidos por diff, com pendências declaradas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → produção (Railway) | push publica backend (`server/`) e `server/web_dist` juntos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-22 | Tampering (entrega) | `web/src/version.js` → `server/web_dist` | mitigate | merge de `origin/main` antes do `bump.sh`; `bump.sh` antes de `publicar-web.sh`; carimbo conferido em `/api/health` |
| T-38-23 | Integrity (front sem backend) | bundle novo consumindo `/api/kb/catalogo` | mitigate | backend e `web_dist` no MESMO push; rota conferida em produção; o front já degrada para erro com retry se a rota faltar (38-03/04) |
| T-38-24 | Repudiation (histórico) | `.planning/STATE.md`/`ROADMAP.md` | mitigate | edição à mão + `git diff` antes do commit; mutadores do SDK proibidos |
| T-38-25 | Elevation of privilege | nenhum | accept | nenhuma mudança de RBAC, auth ou permissão na fase; rota nova é pública e somente leitura, como a irmã |
</threat_model>

<verification>
1. Aprovação humana antes de qualquer push.
2. BUILD_ID novo e `/api/kb/catalogo` conferidos em produção.
3. `git diff .planning/` revisado antes do commit de docs.
</verification>

<success_criteria>
- Alex aprovou ao vivo; fase no ar no PWA com backend e front juntos.
- KB-01/KB-02 Done; Fase 38 fechada à mão; pendências declaradas.
</success_criteria>

<output>
Create `.planning/phases/38-kb-did-tica-ampliada/38-06-SUMMARY.md` when done.
</output>
