---
phase: 38-kb-did-tica-ampliada
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/kb.py
  - server/app/main.py
  - server/tests/test_kb.py
  - server/tests/test_kb_catalogo.py
autonomous: true
requirements: [KB-01, KB-02]

must_haves:
  truths:
    - "Todo verbete servido pela KB tem um título legível nos dois modos — a folha nunca abre com cabeçalho vazio [KB-01, KB-02]"
    - "O front obtém o catálogo inteiro (verbetes + 9 famílias rotuladas) numa chamada só, pública e sem custo de LLM [KB-01, D-04]"
    - "kb.buscar()/resolver() e GET /api/kb/buscar seguem com o mesmo comportamento e o mesmo limite=5 [D-02, D-03]"
    - "Com a camada didática desligada (B3_DIDATICA_OFF=1), o catálogo NÃO serve verbete de texto vazio — princípio 4, sem inventar [KB-01]"
  artifacts:
    - path: "server/app/kb.py"
      provides: "campo `titulo` em todo verbete, FAMILIAS rotuladas, catalogo_formatado(modo)"
      contains: "def catalogo_formatado"
    - path: "server/app/main.py"
      provides: "GET /api/kb/catalogo"
      contains: "/api/kb/catalogo"
    - path: "server/tests/test_kb_catalogo.py"
      provides: "guardião da rota nova (forma, modo, degradação, D-03)"
      min_lines: 60
  key_links:
    - from: "server/app/main.py (get_kb_catalogo)"
      to: "kb.catalogo_formatado"
      via: "chamada direta, modo resolvido pelo mesmo padrão de get_kb_buscar"
      pattern: "kb\\.catalogo_formatado\\("
    - from: "server/app/kb.py (_de_conceito)"
      to: "conceitos.CONCEITOS[cid][\"titulo\"]"
      via: "referência, não cópia (D4 do módulo)"
      pattern: "\"titulo\": c\\[\"titulo\"\\]"
---

<objective>
Dar à KB de mecânica B3 (`server/app/kb.py`) as duas peças que o front precisa
para KB-01/KB-02 e que hoje não existem: (1) um `titulo` por verbete, nos dois
modos; (2) uma rota pública que devolve o catálogo COMPLETO de uma vez
(`GET /api/kb/catalogo`, D-04), com as 9 famílias rotuladas e os `termos` de
busca de cada verbete.

Purpose: sem `titulo`, a `ConceitoSheet` renderiza `<h2>` vazio para 74 dos 83
verbetes (achado HIGH do 38-RESEARCH.md, Pattern 1 e Pitfall 6); sem a rota,
a busca live client-side (D-04) não tem o que filtrar.
Output: `kb.py` com `titulo`/`FAMILIAS`/`catalogo_formatado`, rota nova em
`main.py`, dois guardiões (um estendido, um novo).

Separação deliberada (Pitfall 6): a Task 1 é AUTORIA DE CONTEÚDO (65 títulos,
inertes até a Task 2 os ler); a Task 2 é CÓDIGO. Commits separados, para a
revisão de texto não se misturar com a revisão de código.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-kb-did-tica-ampliada/38-CONTEXT.md
@.planning/phases/38-kb-did-tica-ampliada/38-RESEARCH.md
@.claude/skills/didatica-boris/SKILL.md
@CLAUDE.md

<interfaces>
Contrato ATUAL (server/app/kb.py):
- `catalogo() -> list[dict]` — 83 verbetes; cada um `{id, termos: tuple, familia, texto: {educacional, operador}, veja: list}`.
- `_de_conceito(cid)` (linha ~94) monta os 9 verbetes derivados de `conceitos.CONCEITOS`; `conceitos.CONCEITOS[cid]["titulo"]` JÁ é `{educacional, operador}`.
- `_de_timing(vid)` (linha ~136) monta os 5 `estado-*` a partir de `_ESTADOS_TIMING`.
- `_modelo_verbete(chave)` (linha ~768) monta os 9 `modelo-*` a partir de `technical_models.MODELS[chave]["label"]`.
- Listas literais com os 60 verbetes nativos: `_INDICADORES` (12), `_ESTRUTURA` (9), `_FAMILIAS` (5), `_SETUPS` (9), `_PLANO_RISCO_EXTRA` (3), `_FUNDAMENTOS_EXTRA` (3), `_MERCADO_B3` (15), `_KPIS` (4).
- `formatar(v, modo) -> {id, familia, texto, veja}` (linha ~1618) — usado por `resolver()` e `GET /api/kb/buscar`.
- `buscar(pergunta, limite=5)` — NÃO muda (D-02/D-03).

Rota irmã (server/app/main.py:4595) — padrão a copiar:
`@app.get("/api/kb/buscar") async def get_kb_buscar(q: str = "", modo: Optional[str] = None, scope: Optional[str] = Depends(current_scope))`;
`cfg = store.get(_conn, "config", user_id=scope) or {}`;
`voc = "operador" if (modo or cfg.get("appMode")) == "operador" else "educacional"`.

Contrato NOVO que os planos 38-03/04/05 consomem (não mudar a forma):
`GET /api/kb/catalogo?modo=` → `{"modo": "educacional"|"operador", "familias": [{"id": str, "rotulo": str}, ... 9, na ordem de FAMILIAS], "verbetes": [{"id", "familia", "titulo", "texto", "veja": [str], "termos": [str]}, ...]}`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: autoria dos títulos (conteúdo, inerte até a Task 2)</name>
  <files>server/app/kb.py</files>
  <read_first>
    - server/app/kb.py — linhas 1-60 (docstring: formato do verbete e lei de vocabulário), 119-160 (`_ESTADOS_TIMING`/`_de_timing`), e o início de CADA lista literal (`_INDICADORES` ~167, `_ESTRUTURA` ~435, `_FAMILIAS` ~610, `_SETUPS` ~798, `_PLANO_RISCO_EXTRA` ~974, `_FUNDAMENTOS_EXTRA` ~1040, `_MERCADO_B3`, `_KPIS` ~1451) — use Grep por `"id": "` para localizar cada dict, não releia o arquivo inteiro
    - .claude/skills/didatica-boris/SKILL.md — seção "Vocabulário por modo" (Estudo descreve condição, nunca ordem)
    - server/tests/test_kb.py — `EXPRESSOES_PROIBIDAS` e `test_modo_educacional_sem_verbo_de_ordem_obvio` (os títulos vão passar pelos mesmos crivos na Task 2)
  </read_first>
  <action>
    1. Declarar, logo abaixo de `_normalizar`, o helper puro `_titulo(edu: str, ope: Optional[str] = None) -> dict` que devolve `{"educacional": edu, "operador": ope or edu}` — título é substantivo neutro na maioria dos verbetes; só forka onde o vocabulário do modo difere (skill didatica-boris).
    2. Acrescentar a chave `"titulo": _titulo(...)` em CADA um dos 60 dicts nativos, logo após `"id"`, com o texto EXATO da tabela abaixo (a tabela é o artefato de revisão do Alex — não parafrasear). Um valor = mesmo texto nos dois modos.
    3. Declarar `_TITULO_TIMING = {vid: _titulo(...)}` logo abaixo de `_ESTADOS_TIMING` (não alterar a tupla existente de `_ESTADOS_TIMING`, cuja desestruturação em `_de_timing` fica intacta).
    4. NÃO tocar `_de_conceito` (os 9 derivados herdam `conceitos.CONCEITOS[cid]["titulo"]` na Task 2 — referência, não cópia) nem `_modelo_verbete` (os 9 `modelo-*` derivam de `MODELS[chave]["label"]` na Task 2). Nenhuma outra linha muda nesta task: `formatar()` ainda ignora a chave nova, então o comportamento é idêntico até a Task 2.

    Tabela (id → título; "E / O" quando forka por modo):
    - indicadores: `ind-rsi` → "RSI (IFR) — Índice de Força Relativa"; `ind-macd` → "MACD — convergência e divergência de médias"; `ind-atr` → "ATR — amplitude média real"; `ind-adx` → "ADX — força da tendência"; `ind-medias-rapidas` → "Médias móveis rápidas (MME9 e MME21)"; `ind-medias-lentas` → "Médias móveis lentas (MMA50 e MMA200)"; `ind-vwap` → "VWAP — preço médio ponderado por volume"; `ind-estocastico` → "Estocástico"; `ind-obv` → "OBV — volume acumulado"; `ind-volume-relativo` → "Volume relativo"; `ind-volume-anormal` → "Volume anormal"; `ind-volatilidade-historica` → "Volatilidade histórica (HV)".
    - estrutura: `estr-topo-fundo` → "Topos e fundos"; `estr-suporte` → "Suporte"; `estr-resistencia` → "Resistência"; `estr-congestao` → "Congestão (lateralização)"; `estr-rompimento` → "Rompimento"; `estr-reteste` → "Reteste"; `estr-falso-rompimento` → "Falso rompimento"; `estr-gap` → "Gap"; `estr-pivo` → "Pivô".
    - familias: `familia-tendencia` → "Família tendência"; `familia-momentum` → "Família momentum"; `familia-price-action` → "Família price action"; `familia-volume` → "Família volume"; `familia-volatilidade` → "Família volatilidade".
    - setups: `setup-9-1` → "Setup 9.1 (virada da MME9)"; `setup-9-2` → "Setup 9.2 (correção de um candle)"; `setup-9-3` → "Setup 9.3 (retomada após correção)"; `setup-ifr2` → "IFR2"; `setup-pfr` → "Setup PFR"; `setup-123` → "Setup 123"; `setup-ponto-continuo` → "Ponto Contínuo"; `setup-inside-bar` → "Inside Bar"; `setup-9-4-larry-williams` → "Máx/Mín de Larry Williams (9.4)".
    - plano_risco: `risco-rr` → "Relação risco-retorno (R:R)"; `risco-dimensionamento` → "Dimensionamento da posição"; `risco-invalidacao` → "Invalidação da tese".
    - fundamentos: `fund-pl` → "P/L — preço sobre lucro"; `fund-roe` → "ROE — retorno sobre patrimônio"; `fund-divida-ebitda` → "Dívida/EBITDA".
    - mercado_b3: `mkt-ticker` → "Ticker (código de negociação)"; `mkt-lote` → "Lote padrão e fracionário"; `mkt-pregao` → "Pregão"; `mkt-liquidez` → "Liquidez de um ativo"; `mkt-etf` → "ETF (fundo de índice)"; `mkt-opcao` → "Opção"; `mkt-carteira-simulada` → "Carteira simulada"; `mkt-tipos-ordem` → "Tipos de ordem"; `mkt-day-trade-swing` → "Day trade e swing trade"; `mkt-tributacao` → "Tributação de ações"; `mkt-proventos` → "Proventos (dividendos e JCP)"; `mkt-liquidacao` → "Liquidação da operação" (sem "D+2" no título: prazo em título envelhece em silêncio; o texto já trata); `mkt-termo-futuro` → "Mercado a termo e futuro"; `mkt-ipo` → "IPO (oferta pública inicial)"; `mkt-indices` → "Índices da bolsa (Ibovespa, IFIX)".
    - estados_app (KPIs): `kpi-direcao` → "Direção (viés da leitura)"; `kpi-conviccao` → "Convicção"; `kpi-qualidade` → "Qualidade da leitura"; `kpi-recomendacao` → E "Recomendação (veredito de estudo)" / O "Recomendação (decisão de mesa)".
    - `_TITULO_TIMING`: `estado-sem-plano` → "Sem plano"; `estado-sem-dado` → "Sem dado de timing"; `estado-armado` → "Plano armado"; `estado-atingido` → E "Condição de estudo atingida" / O "Gatilho atingido"; `estado-esticado` → "Movimento esticado".

    Se algum id da tabela não existir no arquivo (ou existir um dict nativo sem linha na tabela), PARAR e reportar a divergência — não inventar título para id não listado.
  </action>
  <verify>
    <automated>cd server && ./.venv/bin/python -c "from app import kb; import collections; nat=[v for v in kb.catalogo() if 'titulo' in v]; print(len(nat))" && ./.venv/bin/python -m pytest -q tests/test_kb.py tests/test_kb_espelho.py tests/test_assistente_kb.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"titulo": _titulo(' server/app/kb.py` retorna 60 (os dicts nativos; `_TITULO_TIMING` usa `_titulo(` em 5 linhas próprias — conferir com `grep -n "_TITULO_TIMING" -A 8 server/app/kb.py`)
    - O script do `<verify>` imprime `60` (os 60 nativos agora carregam a chave; os 23 derivados ainda não — ganham na Task 2)
    - `pytest tests/test_kb.py tests/test_kb_espelho.py tests/test_assistente_kb.py` passa sem alteração de teste (a chave nova é inerte)
    - `grep -n "def _titulo" server/app/kb.py` retorna 1 linha
  </acceptance_criteria>
  <done>65 títulos autorados exatamente como a tabela, sem mudança de comportamento observável; commit próprio `feat(38-01): titulos dos verbetes da KB (conteudo)`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: titulo servido + FAMILIAS + catalogo_formatado + GET /api/kb/catalogo</name>
  <files>server/app/kb.py, server/app/main.py, server/tests/test_kb.py, server/tests/test_kb_catalogo.py</files>
  <read_first>
    - server/app/kb.py — `_de_conceito` (~94-112), `_de_timing` (~136-147), `_modelo_verbete` (~768-780), `formatar` (~1618-1628), docstring (~25-48)
    - server/app/main.py — linhas 4586-4610 (`GET /api/kb/buscar`, o padrão a copiar)
    - server/tests/test_kb.py — fixture `_flags_limpas`, `EXPRESSOES_PROIBIDAS`, `_ocorrencias_fora_de_negacao`, `test_modo_educacional_sem_verbo_de_ordem_obvio`
    - server/tests/test_assistente_kb.py — linhas 30-57 (fixtures de flag e `cli` com `TestClient(app)`) e 141-160 (testes da rota irmã)
  </read_first>
  <behavior>
    - `kb.verbete("gatilho")["titulo"] == conceitos.CONCEITOS["gatilho"]["titulo"]` (referência, não cópia)
    - `kb.verbete("modelo-completo")["titulo"]["educacional"] == "Modelo " + MODELS["completo"]["label"]`
    - `kb.formatar(kb.verbete("estado-atingido"), "educacional")["titulo"] == "Condição de estudo atingida"` e no modo `"operador"` == `"Gatilho atingido"`
    - todo verbete tem `titulo` str não vazia nos dois modos, sem expressão proibida fora de negação e sem verbo de ordem no educacional
    - `[f for f, _ in kb.FAMILIAS]` == `["indicadores", "estrutura", "familias", "modelos", "setups", "plano_risco", "fundamentos", "mercado_b3", "estados_app"]` e o conjunto bate com as famílias presentes no catálogo
    - `inspect.signature(kb.buscar).parameters["limite"].default == 5` (D-02/D-03)
    - `GET /api/kb/catalogo` sem Authorization → 200, `len(verbetes) == 83` com flags limpas, cada verbete com `id, familia, titulo, texto, veja, termos` (texto e titulo não vazios), `familias` = 9 dicts `{id, rotulo}` na ordem de FAMILIAS
    - `?modo=operador` → `modo == "operador"` e título de `estado-atingido` == "Gatilho atingido"; `?modo=xpto` → `modo == "educacional"`
    - com `B3_DIDATICA_OFF=1`: 200, nenhum verbete com `texto == ""`, os 9 ids de `conceitos.CONCEITOS` ausentes, 74 verbetes
    - `GET /api/kb/buscar?q=setup` continua devolvendo no máximo 5 resultados
    - com flags limpas, todo id em `veja` de um verbete servido existe entre os ids servidos
  </behavior>
  <action>
    Em `server/app/kb.py`:
    1. `_de_conceito`: acrescentar `"titulo": c["titulo"]` ao dict devolvido (mesmo objeto de `conceitos.CONCEITOS` — referência, não cópia; D4 do docstring do módulo).
    2. `_de_timing`: acrescentar `"titulo": _TITULO_TIMING[vid]`.
    3. `_modelo_verbete`: acrescentar `"titulo": _titulo("Modelo " + m["label"])` — derivado de `MODELS`, para o verbete não envelhecer quando o rótulo mudar.
    4. `formatar()`: acrescentar a chave `"titulo"` resolvida pelo MESMO padrão de `texto` (`voc` do modo, cai para educacional, depois `""`). Aditivo: `resolver()` e `/api/kb/buscar` passam a carregar `titulo` também (JSON aceita campo extra; Open Question #2 do RESEARCH resolvida assim — sem ramificar por chamador).
    5. Declarar a constante pública `FAMILIAS` (tupla de pares `(id, rotulo)`, nesta ordem e com estes rótulos, mesmos nos dois modos): indicadores "Indicadores"; estrutura "Estrutura de preço"; familias "Famílias de leitura"; modelos "Modelos de análise"; setups "Setups"; plano_risco "Plano e risco"; fundamentos "Fundamentos"; mercado_b3 "Mercado e B3"; estados_app "Estados e leituras do app". Rótulo mora no BACKEND (skill didatica-boris: o front não compõe vocabulário).
    6. Nova função pública `catalogo_formatado(modo: str) -> dict` que devolve `{"modo": voc, "familias": [{"id": i, "rotulo": r} for i, r in FAMILIAS], "verbetes": [...]}`, onde cada verbete é `formatar(v, voc)` acrescido de `"termos": list(v["termos"])` (a busca client-side precisa dos sinônimos — "IFR" acha RSI), e verbete cujo `texto` formatado seja `""` é EXCLUÍDO (Pitfall 4 do RESEARCH: com `B3_DIDATICA_OFF=1` os 9 derivados de `conceitos.py` perdem o texto; servir título sem corpo seria dado inválido na tela — princípio 4 do CLAUDE.md).
    7. Atualizar o docstring do módulo: `titulo` no "Formato de um verbete"; `FAMILIAS` e `catalogo_formatado(modo)` em "Funções públicas", citando a rota que a consome.

    Em `server/app/main.py`, logo após `get_kb_buscar`: rota `@app.get("/api/kb/catalogo")` `async def get_kb_catalogo(modo: Optional[str] = None, scope: Optional[str] = Depends(current_scope))`, resolvendo `voc` EXATAMENTE como a rota irmã (lendo só `appMode` da config — nada mais da config sai na resposta) e devolvendo `kb.catalogo_formatado(voc)`. Import local `from . import kb` como a irmã. Docstring: pública, sem custo, sem conta, o front busca uma vez por modo (D-04) e filtra localmente. Sem cache de servidor (o catálogo já é "custo desprezível", `kb.py:1556-1561`).

    Testes: em `server/tests/test_kb.py` ACRESCENTAR (sem alterar os existentes — guardião não se apaga) os casos de título/FAMILIAS/assinatura do `<behavior>`; criar `server/tests/test_kb_catalogo.py` com os casos da rota, reusando o padrão de fixture de flags limpas + `TestClient(app)` de `test_assistente_kb.py`, e `monkeypatch.setenv("B3_DIDATICA_OFF", "1")` no caso degradado.
  </action>
  <verify>
    <automated>cd server && ./.venv/bin/python -m pytest -q tests/test_kb.py tests/test_kb_catalogo.py tests/test_kb_espelho.py tests/test_assistente_kb.py tests/test_conceitos.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"/api/kb/catalogo"' server/app/main.py` retorna 1 linha, e `grep -n "kb.catalogo_formatado(" server/app/main.py` retorna 1 linha
    - `grep -n '"titulo": c\["titulo"\]' server/app/kb.py` retorna 1 linha (referência, não cópia)
    - `grep -n "def buscar(pergunta: str, limite: int = 5)" server/app/kb.py` continua retornando 1 linha (D-02/D-03 intactos)
    - `pytest tests/test_kb.py tests/test_kb_catalogo.py tests/test_kb_espelho.py tests/test_assistente_kb.py tests/test_conceitos.py` verde
    - `test_kb_catalogo.py` tem pelo menos 8 funções `def test_` (conferir com `grep -c "^def test_" server/tests/test_kb_catalogo.py`)
    - Suíte backend inteira verde: `cd server && ./.venv/bin/python -m pytest -q` (baseline anterior + testes novos, zero regressão)
  </acceptance_criteria>
  <done>Rota pública do catálogo completo no ar localmente, título servido em todas as rotas da KB, guardiões novos travando forma, modo, degradação e o limite intacto do assistente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente anônimo → `GET /api/kb/catalogo` | rota pública nova; entrada = query param `modo` + Bearer opcional |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Tampering | param `modo` da rota nova | mitigate | allowlist implícita de 2 valores (`"operador"` → operador; qualquer outro → educacional), idêntica a `get_kb_buscar`; teste `?modo=xpto` → educacional |
| T-38-02 | Information disclosure | `get_kb_catalogo` lendo `config` do escopo | mitigate | a rota lê SÓ `cfg.get("appMode")` para escolher o vocabulário; a resposta é `kb.catalogo_formatado(voc)` e nada da config é ecoado — guardado pela asserção de chaves exatas da resposta (`modo`, `familias`, `verbetes`) no teste |
| T-38-03 | Denial of service | `GET /api/kb/catalogo` sem auth | accept | puro em memória, 83 itens curtos, zero I/O externo e zero LLM — mesmo perfil da rota irmã pública `/api/kb/buscar` já em produção |
| T-38-04 | Integrity (princípio 4) | verbete com texto vazio sob `B3_DIDATICA_OFF` | mitigate | `catalogo_formatado` exclui verbete de texto vazio; teste degradado garante 0 itens vazios |
| T-38-05 | Tampering (regressão do assistente) | `formatar()`/`buscar()` compartilhados com `/api/assistente` | mitigate | mudança só aditiva (`titulo`); `limite=5` travado por teste de assinatura; `test_assistente_kb.py` no verify |
</threat_model>

<verification>
1. `cd server && ./.venv/bin/python -m pytest -q` verde.
2. `curl -s localhost:8787/api/kb/catalogo | python3 -c "import sys,json; d=json.load(sys.stdin); print(len(d['verbetes']), len(d['familias']))"` imprime `83 9` com o backend local no ar (se o backend não estiver de pé, o teste de rota via TestClient é a prova suficiente).
</verification>

<success_criteria>
- 83/83 verbetes com título nos dois modos; 9 famílias rotuladas pelo backend.
- `GET /api/kb/catalogo` público, sem LLM, honesto sob didática desligada.
- `kb.buscar`/`resolver`/`/api/kb/buscar` sem regressão.
</success_criteria>

<output>
Create `.planning/phases/38-kb-did-tica-ampliada/38-01-SUMMARY.md` when done.
</output>
