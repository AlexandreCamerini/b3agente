---
phase: 21-duplica-o-removida-e-portf-lio-consolidado
plan: 04
type: execute
wave: 4
depends_on: ["21-03"]
files_modified:
  - web/src/version.js
  - server/app/main.py
  - server/web_dist
autonomous: true
requirements: [DEDUP-01, DEDUP-02, DEDUP-03, FIX-03]

must_haves:
  truths:
    - "A Fase 21 está publicada — não fica testada no repositório e ausente do bundle que o servidor serve"
    - "O carimbo de entrega é novo e coerente nos três elos: web/src/version.js, server/app/main.py e o bundle em server/web_dist"
    - "Os quatro critérios de sucesso da fase valem no BUNDLE DE PRODUÇÃO (minificado), não só no servidor de desenvolvimento"
    - "Nada foi enviado a origin: a decisão a/b/c sobre publicar junto as Fases 17/18/19 (checkpoints humanos pendentes) continua sendo do Alex"
  artifacts:
    - path: "web/src/version.js"
      provides: "BUILD_ID novo desta entrega"
      contains: "BUILD_ID"
    - path: "server/web_dist"
      provides: "bundle publicado com a desduplicação da Fase 21"
  key_links:
    - from: "web/src/version.js"
      to: "server/web_dist"
      via: "scripts/bump.sh seguido de scripts/publicar-web.sh"
      pattern: "BUILD_ID"
    - from: "web/src/version.js"
      to: "server/app/main.py:SERVER_BUILD_ID"
      via: "sincronização automática dentro de publicar-web.sh"
      pattern: "SERVER_BUILD_ID"
---

<objective>
Publicar a Fase 21 e provar que os quatro critérios de sucesso valem no
bundle de produção, não só no Vite de desenvolvimento.

Purpose: este repositório já pagou por isso — "Fase sem plano de publicação
front": plano que toca `web/src/` sem bump + publicação fica mergeado,
testado e nunca vai ao ar. A `Nota de publicação` da Fase 21 no ROADMAP
existe exatamente por causa desse histórico.

Output: carimbo novo nos três elos, `server/web_dist` republicado, suíte
canônica verde DEPOIS da publicação, e os quatro critérios remedidos contra
o bundle de produção (ou entregues ao orquestrador com roteiro exato, se a
ferramenta de navegador não estiver disponível ao executor).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/21-duplica-o-removida-e-portf-lio-consolidado/21-CONTEXT.md
@.planning/phases/21-duplica-o-removida-e-portf-lio-consolidado/21-01-SUMMARY.md
@.planning/phases/21-duplica-o-removida-e-portf-lio-consolidado/21-02-SUMMARY.md
@.planning/phases/21-duplica-o-removida-e-portf-lio-consolidado/21-03-SUMMARY.md
@.planning/phases/20-funda-o-estrutural-e-tipogr-fica/20-04-SUMMARY.md
@CLAUDE.md

<interfaces>
`scripts/bump.sh` — avança o `BUILD_ID` em `web/src/version.js`. Sem
argumento, gera o próximo do dia (`F10-AAAAMMDD-NN`). É comando MANUAL de
propósito: o carimbo marca ENTREGA, não build.

`scripts/publicar-web.sh` — builda `web/` e publica em `server/web_dist`, a
ÚNICA árvore que o Railway enxerga (`rootDirectory=/server`). Ele TAMBÉM
sincroniza `SERVER_BUILD_ID` em `server/app/main.py` a partir do carimbo
local — por isso, com front sendo publicado, NÃO se edita `SERVER_BUILD_ID`
à mão. Nunca editar `server/web_dist` à mão. Ordem obrigatória: `bump.sh`
ANTES de `publicar-web.sh`.

`bash scripts/executar.sh --testes` — suíte canônica (pytest +
`web/tests/*.mjs`). Roda DEPOIS da publicação: há guardiões de carimbo/bundle
que só têm o que medir com o `web_dist` novo no lugar.

`bash scripts/executar.sh --prod` — sobe o modo PROD local: build do web +
servidor único em `:8787`, servindo o bundle real em vez do Vite. É onde os
quatro critérios são remedidos.
`bash scripts/executar.sh --stop` — encerra e libera as portas.

Carimbo vigente antes desta fase: `F10-20260905-02` (em `web/src/version.js`
e em `server/app/main.py`). Publicado pela Fase 20 (plano 20-04).
</interfaces>

<risco_herdado>
**A publicação é cumulativa e carrega TRÊS fases sem checkpoint humano
aprovado.** `.planning/STATE.md` registra:
- **Fase 17** (`17-06-PLAN.md`, Task 2): checkpoint ADIADO, não aprovado —
  mercado fechado na tentativa, roteiro de 10 passos nunca exercitado.
- **Fase 18** (`18-05-PLAN.md`, Task 2): checkpoint PENDENTE, exercitado só
  parcialmente no iPhone em 2026-09-03.
- **Fase 19** (`19-04-PLAN.md`, Task 2): checkpoint PENDENTE, roteiro não
  rodado.

Esses três fluxos JÁ estão dentro de `server/web_dist` (a Fase 19 publicou em
2026-09-04 e a Fase 20 republicou em 2026-09-05). A Fase 21 não os introduz
nem os agrava: ela remove duplicação de exibição e não toca o motor de
opções, o fluxo de aceite, nem contrato de API. O que segue em aberto é a
decisão a/b/c do Alex sobre **enviar tudo isso a `origin`** — e essa decisão
continua sendo dele.

**Este plano NÃO dá `git push`.** Publicar em `server/web_dist` é operação
local no repositório; o deploy só acontece quando o `origin` recebe. O
SUMMARY deve reafirmar que a decisão a/b/c segue aberta e que nada foi
empurrado.
</risco_herdado>

<ferramenta_de_navegador>
**Precedente confirmado nos 4 planos da Fase 20** (`20-01`/`20-02`/`20-03`/
`20-04`-SUMMARY.md): subagentes spawnados via Task NÃO herdam as ferramentas
MCP de navegador do orquestrador (`mcp__computer-use__*`,
`mcp__claude-in-chrome__*`) — bug upstream anthropics/claude-code#13898.

A Task 2 deste plano é escrita para NÃO travar por causa disso. Se as
ferramentas de navegador não estiverem ligadas ao executor, ele faz tudo o
que é automatizável (publicar, medir por grep no bundle, subir o servidor de
produção, deixá-lo NO AR) e entrega ao orquestrador um roteiro numerado e
específico no SUMMARY, seguindo exatamente o padrão da seção "Orchestrator
Live Re-Verification" que a Fase 20 usou.

O que NUNCA é aceitável: aproximar, estimar ou declarar verificado um item
visual que não foi de fato medido. Item não medido é item registrado como
aberto.

**Esta fase é especialmente fácil de verificar sem mercado aberto** — as
quatro mudanças são estruturais/textuais e não dependem de cotação ao vivo.
Só o critério 4 (FIX-03) precisa de um estado de conta específico (1 ou 2
dias de patrimônio registrados), e ele já está provado por teste unitário
real sobre `equityCurve` no plano 21-03 — a verificação visual é confirmação,
não a única evidência.
</ferramenta_de_navegador>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump do carimbo e publicação do front</name>
  <read_first>
    - scripts/bump.sh (cabeçalho — por que o bump é manual e o que quebra sem ele)
    - scripts/publicar-web.sh (a sincronização automática de `SERVER_BUILD_ID` e o destino do build)
    - web/src/version.js (carimbo atual: `F10-20260905-02`)
    - server/app/main.py (a linha de `SERVER_BUILD_ID` e `/api/health` — localizar com `grep -n "SERVER_BUILD_ID" server/app/main.py`)
    - .planning/phases/21-duplica-o-removida-e-portf-lio-consolidado/21-03-SUMMARY.md (estado da suíte antes da publicação)
  </read_first>
  <files>web/src/version.js, server/app/main.py, server/web_dist</files>
  <action>
Nesta ordem exata, sem pular passo:
1. `bash scripts/bump.sh` — sem argumento. O vigente é `F10-20260905-02`, então
   o esperado é o próximo do dia (ou o primeiro de hoje, se a data virou).
2. `bash scripts/publicar-web.sh` — builda `web/` e publica em
   `server/web_dist`; ele sincroniza `SERVER_BUILD_ID` sozinho a partir do
   carimbo local.
3. `bash scripts/executar.sh --testes` — DEPOIS da publicação, para que os
   guardiões de carimbo/bundle meçam o `web_dist` novo.
4. Conferir os TRÊS elos do carimbo: `web/src/version.js`,
   `server/app/main.py:SERVER_BUILD_ID` e o bundle dentro de
   `server/web_dist` carregam o MESMO `BUILD_ID`.
5. Conferir que as mudanças da fase chegaram ao bundle minificado. Como o
   front desta fase é JSX (não CSS em template string como na Fase 20), as
   assinaturas a procurar dentro de `server/web_dist` são textuais e
   sobrevivem à minificação:
   - `PATRIMÔNIO TOTAL` e `EM POSIÇÕES` (rótulos do card consolidado,
     DEDUP-03) — presentes;
   - `Trocar modo` (o link relocado, DEDUP-02) — presente;
   - `a curva aparece a partir do 3º dia` (o texto novo de FIX-03) —
     presente;
   - `Modo do app:` (o rótulo do card removido, DEDUP-02) — **AUSENTE**. Esta
     é a checagem mais valiosa das cinco: prova que a REMOÇÃO chegou ao
     bundle, não só que a adição chegou. Um bundle antigo passaria nas quatro
     primeiras se o build tivesse falhado silenciosamente.

Não editar `server/web_dist` à mão. Não editar `SERVER_BUILD_ID` à mão (o
script sincroniza; edição manual só existe em deploy só-backend, que não é o
caso). **Não dar `git push`** — ver `<risco_herdado>`.

Registrar no SUMMARY o carimbo gerado e a contagem de testes das duas suítes.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `git diff web/src/version.js` mostra um `BUILD_ID` novo no formato `F10-AAAAMMDD-NN`, diferente de `F10-20260905-02`.
    - `git status --porcelain server/web_dist` mostra arquivos modificados (a publicação de fato aconteceu).
    - Os três elos batem: `grep -o 'F10-[0-9]*-[0-9]*' web/src/version.js | head -1` é igual ao valor de `sed -n 's/.*SERVER_BUILD_ID = "\([^"]*\)".*/\1/p' server/app/main.py`, e `grep -rl "$(grep -o 'F10-[0-9]*-[0-9]*' web/src/version.js | head -1)" server/web_dist | head -1` retorna ao menos um arquivo.
    - `grep -rl 'PATRIMÔNIO TOTAL' server/web_dist | head -1` retorna ao menos um arquivo; idem para `Trocar modo` e para `a curva aparece a partir do 3º dia`.
    - `grep -rl 'Modo do app:' server/web_dist` retorna VAZIO — a remoção do DEDUP-02 chegou ao bundle.
    - `bash scripts/executar.sh --testes` sai com código 0 DEPOIS da publicação (as duas suítes).
    - `git diff --stat web/package.json web/package-lock.json server/requirements.txt server/requirements-prod.txt` sai vazio.
    - Nenhum `git push` foi executado (`git status -sb` mostra commits à frente de `origin`).
  </acceptance_criteria>
  <done>Carimbo novo nos três elos, `server/web_dist` republicado com as adições E a remoção da fase dentro, suíte canônica verde após a publicação, nada empurrado para `origin`.</done>
</task>

<task type="auto">
  <name>Task 2: Remedir os quatro critérios contra o bundle de produção</name>
  <read_first>
    - .planning/ROADMAP.md (seção da Phase 21 — os quatro critérios de sucesso, texto literal)
    - .planning/phases/21-duplica-o-removida-e-portf-lio-consolidado/21-01-SUMMARY.md, 21-02-SUMMARY.md, 21-03-SUMMARY.md (o que já foi provado por guardião, para não repetir trabalho nem reabrir o que está fechado)
    - .planning/phases/20-funda-o-estrutural-e-tipogr-fica/20-04-SUMMARY.md (o padrão de handoff "Orchestrator Live Re-Verification" a seguir se o navegador não estiver disponível)
    - scripts/executar.sh (o modo `--prod` e o `--stop`)
  </read_first>
  <files>nenhum arquivo editado nesta task (verificação); o resultado vai para o SUMMARY</files>
  <action>
Subir `bash scripts/executar.sh --prod` (build de produção servido em
`:8787`) e remedir os QUATRO critérios do ROADMAP contra o bundle
minificado, com uma conta local logada.

1. **Curva única (DEDUP-01)** — percorrer Acompanhar e depois Portfólio na
   mesma sessão. O card "PATRIMÔNIO SIMULADO" aparece em Acompanhar e NÃO
   aparece em Portfólio. Registrar a contagem de ocorrências do texto
   `PATRIMÔNIO SIMULADO` no DOM de cada uma das duas telas (esperado: 1 e 0).
2. **Card consolidado (DEDUP-03)** — em Portfólio, em 375×812: um único
   container de card com os quatro valores em grid 2×2. Registrar a
   `gridTemplateColumns` computada do grid interno (esperado `1fr 1fr`, ou
   os dois valores em px que o navegador resolve), a contagem de cards
   (esperado 1, não 4) e — o risco real desta mudança — confirmar que os
   quatro valores CABEM em 375px sem quebrar linha nem estourar: medir
   `scrollWidth === clientWidth` no container do card e confirmar que
   nenhum dos quatro valores tem `scrollWidth > clientWidth`.
3. **Status único (DEDUP-02)** — na tela Operador IA: `Modo do app:` não
   aparece no DOM; o card `OPERADOR NO SERVIDOR` aparece com o toggle; o
   link `Trocar modo →` aparece UMA vez, acima do card-herói, e ao tocá-lo o
   app navega para Perfil. E o chip de modo do `Topbar` segue visível na
   mesma tela (`MODO ESTUDO` ou `MODO OPERADOR`) — é ele que passou a ser a
   fonte única do modo do app. Confirmar também que as duas linhas de
   transparência do `entradaAuto` aparecem dentro do card
   `ENTRADA AUTOMÁTICA`, rolando até ele.
4. **Placeholder de pouco dado (FIX-03)** — o caminho determinístico já está
   provado por teste unitário sobre `equityCurve` (plano 21-03). Aqui, o
   que se verifica é a renderização: numa conta com 0 dias registrados, o
   texto `Sua curva começa amanhã` aparece (não regrediu). Se houver conta
   local com 1 ou 2 dias de snapshot, confirmar o texto
   `a curva aparece a partir do 3º dia` e a AUSÊNCIA de `<path>` de curva
   desenhada. Se não houver conta nesse estado, NÃO forjar dado nem
   estimar: registrar como aberto e apontar que a evidência determinística
   já existe no guardião do 21-03.

Repetir os itens 2 e 3 nos dois temas (claro/escuro) e nos dois modos
(Estudo/Operador), já que o card consolidado e o `AgenteScreen` mudam de cor
por token — é uma passada rápida de screenshot, não uma bateria nova.

Encerrar com `bash scripts/executar.sh --stop`.

**Se as ferramentas de navegador não estiverem disponíveis a você**
(ver `<ferramenta_de_navegador>`): NÃO tente improvisar, NÃO estime valores
e NÃO declare verificado o que não mediu. Em vez disso:
- faça a parte automatizável: suba `--prod`, confirme por `curl`/grep que
  `:8787` está servindo o bundle com o carimbo novo;
- **deixe o servidor NO AR** (não rode `--stop`), para o orquestrador reusar
  sem pagar outro ciclo de build;
- escreva no SUMMARY uma seção "Pendente de verificação ao vivo
  (orquestrador)" com os itens 1-4 acima como roteiro numerado, cada um com
  a tela, o seletor/texto a procurar e o valor esperado — no mesmo formato
  que o `20-04-SUMMARY.md` usou;
- diga explicitamente qual porta e qual carimbo o orquestrador vai encontrar.

No SUMMARY, montar uma tabela dos quatro critérios do ROADMAP × resultado,
cada linha com o número/contagem medido ou com "aberto — roteiro no item N",
nunca com adjetivo.

Reafirmar no SUMMARY: nada foi enviado a `origin`; a decisão a/b/c sobre as
Fases 17/18/19 segue aberta com o Alex.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - O SUMMARY contém uma tabela dos 4 critérios de sucesso do ROADMAP com o resultado medido em `:8787` (produção) — cada linha com um número/contagem medido, ou marcada explicitamente como aberta com roteiro, nunca com adjetivo.
    - Critério 1: contagem de `PATRIMÔNIO SIMULADO` registrada nas duas telas (esperado 1 em Acompanhar, 0 em Portfólio).
    - Critério 2: contagem de cards (1, não 4), `gridTemplateColumns` computada e a medição de `scrollWidth === clientWidth` do card em 375px.
    - Critério 3: `Modo do app:` ausente do DOM, `Trocar modo →` presente 1× acima do card-herói e navegando para Perfil, chip de modo do Topbar visível, e as duas linhas de `entradaAuto` dentro do card `ENTRADA AUTOMÁTICA`.
    - Critério 4: presença do texto de zero dias; e o texto de 1-2 dias medido OU registrado como aberto com a referência à prova unitária do plano 21-03.
    - Se as ferramentas de navegador não estiverem disponíveis: o SUMMARY contém a seção "Pendente de verificação ao vivo (orquestrador)" com roteiro numerado, porta e carimbo, e o servidor foi deixado NO AR (nenhum `--stop` executado).
    - Nenhum valor visual aproximado ou estimado aparece no SUMMARY.
    - `bash scripts/executar.sh --testes` sai com código 0.
    - O SUMMARY afirma explicitamente que nenhum `git push` foi feito e que a decisão a/b/c segue aberta.
  </acceptance_criteria>
  <done>Os quatro critérios do ROADMAP estão medidos contra o bundle de produção com números registrados — ou entregues ao orquestrador como roteiro executável com o servidor já no ar — e o repositório segue sem push para `origin`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo local → `origin`/Railway | A publicação em `server/web_dist` é local; o deploy real acontece no push. Este plano para antes desse limite de propósito. |
| medição real → afirmação no SUMMARY | Um item visual declarado verificado sem ter sido medido contamina o registro da fase e a decisão do Alex depois. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-16 | Repudiation | Fase testada mas invisível em produção (falta de bump/publicação) | mitigate | Task 1 obriga `bump.sh` → `publicar-web.sh` → suíte, com os três elos do carimbo como critério de aceite |
| T-21-17 | Spoofing | Build falhar em silêncio e o `web_dist` antigo passar nas checagens de "texto novo presente" | mitigate | Critério de aceite exige `Modo do app:` AUSENTE do bundle — a checagem de remoção, que só um bundle novo passa |
| T-21-18 | Elevation of privilege | Empurrar a `origin` as Fases 17/18/19 sem checkpoint aprovado, embutindo decisão do Alex numa task de publicação | mitigate | Proibição explícita de `git push`; critério de aceite verifica commits à frente de `origin` sem envio |
| T-21-19 | Spoofing | Item visual declarado verificado sem medição, por falta de ferramenta de navegador | mitigate | Task 2 tem caminho de handoff explícito, proíbe estimativa e exige a seção "Pendente de verificação ao vivo" com roteiro numerado |
| T-21-20 | Information disclosure | Conta local de teste e SQLite local entrarem no commit | mitigate | Critério de aceite limita o diff a `web/src/version.js`, `server/app/main.py` e `server/web_dist` |
| T-21-SC | Tampering | instalações npm/pip/cargo | mitigate | Zero pacote novo na fase inteira; `git diff` dos quatro manifests vazio como critério de aceite |
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` verde DEPOIS da publicação.
- Três elos do carimbo coerentes.
- Adições da fase presentes no bundle E `Modo do app:` ausente dele.
- Quatro critérios do ROADMAP medidos em `:8787`, ou registrados como abertos com roteiro e servidor no ar.
- Nenhum push para `origin`.
</verification>

<success_criteria>
A desduplicação da Fase 21 está publicada, provada no bundle de produção e
travada por guardião na suíte canônica; a decisão de enviar a `origin` (que
arrasta as Fases 17/18/19) fica registrada como aberta, sem ser tomada por um
agente.
</success_criteria>

<output>
Create `.planning/phases/21-duplica-o-removida-e-portf-lio-consolidado/21-04-SUMMARY.md` when done
</output>
