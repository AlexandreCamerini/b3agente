---
phase: 21-duplica-o-removida-e-portf-lio-consolidado
plan: 03
type: execute
wave: 3
depends_on: ["21-02"]
files_modified:
  - web/src/copy.js
  - web/src/App.jsx
  - web/tests/test_fase21_dedup_consolidacao.mjs
autonomous: true
requirements: [FIX-03]

must_haves:
  truths:
    - "Numa conta nova, logo depois da primeira operação (1 ou 2 dias de patrimônio registrados), a área do gráfico mostra um texto dizendo que ainda faltam dados — nunca uma linha reta com escala degenerada"
    - "A curva só é desenhada a partir do 3º dia registrado, quando existe forma de verdade para plotar"
    - "O texto novo do placeholder vive em copy.js nos DOIS modos (Estudo e Operador), nunca hardcodado no componente — a convenção do vocabulário por modo é respeitada"
    - "O estado de zero dias continua com o texto de hoje, byte a byte — a correção não muda o que a conta recém-criada lê"
  artifacts:
    - path: "web/src/copy.js"
      provides: "chave curvaPoucosDias(dias) em COPY.estudo e COPY.operador"
      contains: "curvaPoucosDias"
    - path: "web/src/App.jsx"
      provides: "CapitalCurve com limiar ec.days >= 3 e o terceiro ramo de placeholder"
      contains: "poucosDias"
    - path: "web/tests/test_fase21_dedup_consolidacao.mjs"
      provides: "guardião de FIX-03 somado ao de DEDUP-01/03, incluindo teste unitário real de equityCurve com 1 e 2 snapshots"
      contains: "curvaPoucosDias"
  key_links:
    - from: "web/src/App.jsx (CapitalCurve)"
      to: "web/src/copy.js (COPY[modo].curvaPoucosDias)"
      via: "cp.curvaPoucosDias(ec.days), com cp desestruturado de ctx"
      pattern: "cp\\.curvaPoucosDias\\(ec\\.days\\)"
    - from: "web/src/App.jsx (CapitalCurve)"
      to: "web/src/finance.js (equityCurve)"
      via: "ec.days — contagem de snapshots persistidos, a mesma que a estatística DIAS REGISTRADOS já exibe"
      pattern: "ec\\.days >= 3"
---

<objective>
Fazer o gráfico de patrimônio parar de desenhar uma reta sem sentido quando
ainda há 1 ou 2 dias de dado, e dizer ao usuário o que está acontecendo.

Purpose: é o único item desta fase que corrige um defeito achado ao vivo (uma
conta nova, logo depois da primeira operação, via uma caixa com escala
degenerada). Com `ec.days === 1` e o snapshot datado de hoje, a curva colapsa
para dois pontos — `min`/`max` saem de dois valores e o resultado é um único
segmento diagonal, não uma curva.

Output: limiar de 3 dias em `CapitalCurve`, um terceiro ramo de placeholder
com texto vindo de `copy.js` nos dois modos, e guardião cobrindo o limiar, o
ramo e a paridade das chaves.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/21-duplica-o-removida-e-portf-lio-consolidado/21-CONTEXT.md
@.planning/phases/21-duplica-o-removida-e-portf-lio-consolidado/21-UI-SPEC.md
@.planning/phases/21-duplica-o-removida-e-portf-lio-consolidado/21-PATTERNS.md
@.planning/phases/21-duplica-o-removida-e-portf-lio-consolidado/21-02-SUMMARY.md
@CLAUDE.md

<interfaces>
`equityCurve(snapshots, budget, livePatr, todayYmd)` — `web/src/finance.js:167`,
função PURA, importável em teste sem build (é o que `test_finance.mjs` e
`test_numeros_fundamentados.mjs` já fazem). Retorna, entre outros:
```
{ curve, series, days: series.length, retAcum, drawdown, base, end, datas }
```
`days` é a contagem de snapshots de patrimônio PERSISTIDOS (`series.length`,
verificado em `finance.js:213`) — é exatamente o "histórico de patrimônio" de
que fala o `21-CONTEXT.md` e exatamente o número que a estatística
`DIAS REGISTRADOS` já mostra ao usuário (`App.jsx:1841`).

Gate em `ec.days`, NÃO em `curve.length`: `curve` varia em ±1 conforme o
último snapshot seja de hoje (substitui) ou de ontem (anexa) —
`finance.js:190-200`. Esse ±1 é acidente de implementação, não "quantos dias
de dado existem".

`CapitalCurve` — `App.jsx:1723`. Pontos de edição:
- **1729** `const { data, quotes } = ctx;` → precisa de `cp`. Verificado que
  `ctx.cp` existe: montado em `App.jsx:8760` a partir de
  `copyFor(appMode)` (`App.jsx:7712`). Desestruturar é zero-risco, sem
  mudança de prop em nenhum chamador.
- **1736** `const hasSeries = ec.days >= 1;` → o limiar.
- **1836-1854** o ternário de 2 ramos do rodapé → vira 3 ramos.

`COPY` — `web/src/copy.js:18`, com `estudo:` em 19 e `operador:` em 241. O
padrão a espelhar é `saudacao`/`resumoDia` (arrow function inline dentro do
dicionário do modo, linhas 26-30 e 246-250).
</interfaces>

<guardiao_de_paridade_de_chaves>
`web/tests/test_copy_theme.mjs` compara os CONJUNTOS de chaves de
`COPY.estudo` e `COPY.operador` (`const e = Object.keys(COPY.estudo).sort(), o = Object.keys(COPY.operador).sort();`).
Adicionar `curvaPoucosDias` a apenas um dos dois modos derruba a suíte
canônica. A chave entra nos DOIS, no mesmo commit — o texto pode ser
idêntico, o que não pode é a chave faltar de um lado.
</guardiao_de_paridade_de_chaves>

<guardioes_existentes_que_NAO_mudam>
Varredura feita no planejamento sobre os 115 arquivos de `web/tests/`. Duas
asserções de `test_benchmark_curva.mjs` tocam o mesmo componente e
SOBREVIVEM à mudança — confirme que seguem verdes em vez de assumir:

- linha 79: `"estado vazio (!hasSeries) segue com o mesmo placeholder tracejado"`,
  regex `/M0,72 C60,66 110,58 150,52 C200,45 250,40 300,30/`. Sobrevive
  porque o SVG do rabisco tracejado NÃO é tocado por esta mudança (tanto o
  ramo de 0 dias quanto o de 1-2 dias caem no `hasSeries === false` do SVG).
- linha 84: `/if \(!hasSeries\) return/`. Sobrevive porque a guarda do
  `useEffect` do Ibovespa continua escrita exatamente assim — só o limiar de
  `hasSeries` muda. Subir o limiar para 3 é o efeito colateral CORRETO: não
  há por que buscar o benchmark para comparar com uma curva que não é
  desenhada.

Nenhum teste do repositório afirma o literal `days >= 1` (verificado por
grep). Nenhum outro guardião precisa ser reescrito neste plano.
</guardioes_existentes_que_NAO_mudam>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guardião de FIX-03 escrito antes da mudança (RED)</name>
  <read_first>
    - web/tests/test_fase21_dedup_consolidacao.mjs (criado no plano 21-01 — ESTENDER este arquivo, não criar um segundo)
    - web/tests/test_numeros_fundamentados.mjs:20-35 (o padrão da casa para importar `equityCurve` de finance.js dentro de um teste `.mjs` e exercitá-la com dados sintéticos)
    - web/tests/test_copy_theme.mjs:20-30 (como o repositório importa `COPY` e compara os conjuntos de chaves dos dois modos)
    - web/src/finance.js:167-213 (equityCurve — em especial o ramo de substituir × anexar em 190-200 e `days: series.length` em 213)
    - web/src/App.jsx:1729, :1736, :1836-1854 (os três pontos de edição da Task 2)
  </read_first>
  <files>web/tests/test_fase21_dedup_consolidacao.mjs</files>
  <behavior>
    Contra o código AINDA NÃO editado, as asserções novas devem FALHAR:
    - `hasSeries = ec.days >= 3` no corpo de CapitalCurve → falha (hoje é `>= 1`)
    - existe um flag `poucosDias` → falha (não existe)
    - CapitalCurve chama `cp.curvaPoucosDias(ec.days)` → falha
    - `COPY.estudo.curvaPoucosDias` e `COPY.operador.curvaPoucosDias` são funções → falha
    E devem PASSAR desde já (são fatos sobre `finance.js`, que este plano não altera):
    - `equityCurve` com 1 snapshot devolve `days === 1`
    - `equityCurve` com 2 snapshots devolve `days === 2`
    - com 1 snapshot datado de hoje, `curve.length === 2` (a prova da escala degenerada)
  </behavior>
  <action>
ESTENDER `web/tests/test_fase21_dedup_consolidacao.mjs` (criado no plano
21-01) com uma seção nova, sob um cabeçalho `FIX-03`. Não criar arquivo
novo — o cabeçalho do 21-01 já avisa que este plano estende aquele arquivo.

**Seção A — prova unitária do defeito (importa `finance.js`, função pura):**
Importar `equityCurve` com `await import(...)` no padrão de
`test_numeros_fundamentados.mjs`. Exercitar:
1. `equityCurve([{data:"2026-09-05", patrimonio:10500, base:10000}], 10000, 10500, "2026-09-05")`
   → `days === 1` E `curve.length === 2`. Documentar no comentário que
   `curve.length === 2` é a escala degenerada: `min`/`max` saem de dois
   valores, `span` é a diferença deles, e o resultado é um segmento reto,
   não uma curva.
2. Mesmo snapshot datado de ONTEM (`"2026-09-04"`) com `todayYmd`
   `"2026-09-05"` → `days === 1` e `curve.length === 3` (ramo de anexar).
   Confirma que os dois sub-casos de 1 dia são cobertos uniformemente pelo
   gate em `ec.days`, sem precisar de caso especial mesmo-dia × dia-anterior.
3. Dois snapshots → `days === 2`.
4. Zero snapshots → `days === 0` (o estado de conta nova, que mantém o texto
   de hoje).

**Seção B — limiar e ramo em `App.jsx` (grep sobre o corpo de CapitalCurve):**
Isolar o corpo de `CapitalCurve` pelo padrão da casa
(`indexOf("function CapitalCurve(")` até o próximo `"\nfunction "`), com
`process.exit(1)` e mensagem se não achar. Afirmar:
5. o corpo contém `ec.days >= 3` — o limiar novo;
6. o corpo NÃO contém `ec.days >= 1` — o limiar antigo não sobreviveu em
   lugar nenhum;
7. o corpo contém um flag `poucosDias` definido como `ec.days >= 1 && ec.days < 3`;
8. o corpo contém a chamada literal `cp.curvaPoucosDias(ec.days)` —
   nunca uma frase hardcodada;
9. o corpo desestrutura `cp` de `ctx` (regex sobre a linha de
   `const { data, quotes` aceitando `cp` na lista);
10. o texto do estado ZERO segue byte a byte igual: o corpo contém
    `Sua curva começa amanhã. Volte para vê-la crescer` — a correção não
    pode reescrever o que a conta recém-criada lê;
11. os dois ramos de placeholder usam o MESMO objeto de estilo do
    placeholder de hoje: `fontSize: "11.5px"` com `color: T.textFaint` e
    `lineHeight: 1.5` aparecem pelo menos 2× no corpo;
12. o rabisco tracejado do SVG segue intocado: o corpo ainda contém
    `M0,72 C60,66 110,58 150,52 C200,45 250,40 300,30` (mesma regex do
    `test_benchmark_curva.mjs`, garantindo que os dois guardiões não
    divergem).

**Seção C — a chave de copy nos dois modos (importa `copy.js`):**
Importar `COPY` no padrão de `test_copy_theme.mjs`. Afirmar:
13. `typeof COPY.estudo.curvaPoucosDias === "function"`;
14. `typeof COPY.operador.curvaPoucosDias === "function"`;
15. `COPY.estudo.curvaPoucosDias(1)` menciona "1 dia" (singular) e
    `COPY.estudo.curvaPoucosDias(2)` menciona "2 dias" (plural) — a
    pluralização de fato existe, não é a mesma string nos dois casos;
16. as duas mensagens (1 e 2) mencionam o 3º dia, casando com o limiar do
    código — o texto e o comportamento não podem divergir;
17. o mesmo para `COPY.operador`.

Rodar `node web/tests/test_fase21_dedup_consolidacao.mjs` e confirmar que
falha nas asserções 5, 7, 8, 9, 13, 14 (a 6 passa por acidente hoje? não —
hoje o corpo CONTÉM `ec.days >= 1`, então a 6 também falha; confirme a lista
real na saída) e passa nas 1-4, 10, 11, 12. Registrar a saída literal no
SUMMARY.

NÃO editar `App.jsx` nem `copy.js` nesta task.
  </action>
  <verify>
    <automated>node web/tests/test_fase21_dedup_consolidacao.mjs; test $? -ne 0 && echo "RED confirmado (esperado nesta task)"</automated>
  </verify>
  <acceptance_criteria>
    - `web/tests/test_fase21_dedup_consolidacao.mjs` foi ESTENDIDO (o `git diff` mostra adição), não substituído: as asserções de DEDUP-01/DEDUP-03 do plano 21-01 continuam no arquivo e o total de chamadas `ok(` cresceu.
    - Rodado contra o código não editado, o script sai com código diferente de 0.
    - A saída contém `ok ` para as asserções unitárias de `equityCurve` (days 1 / 2 / 0 e `curve.length === 2` no caso mesmo-dia) — elas provam o defeito e passam desde já.
    - A saída contém `FALHOU` para `ec.days >= 3`, para `poucosDias`, para `cp.curvaPoucosDias(ec.days)` e para as duas chaves de `COPY`.
    - A saída contém `ok ` para "texto do estado zero intocado" e para "rabisco tracejado do SVG intocado".
    - `git status --porcelain web/src/App.jsx web/src/copy.js` sai VAZIO.
  </acceptance_criteria>
  <done>O guardião de FIX-03 existe dentro do arquivo da fase, prova o defeito com um teste unitário real da função pura, e falha pelas razões certas contra o código atual.</done>
</task>

<task type="auto">
  <name>Task 2: Limiar de 3 dias, terceiro ramo de placeholder e a chave nos dois modos (GREEN)</name>
  <read_first>
    - web/src/copy.js:26-30 (saudacao/resumoDia em COPY.estudo — o padrão de arrow function inline a espelhar)
    - web/src/copy.js:246-250 (os mesmos em COPY.operador — a chave nova entra aqui também)
    - web/src/copy.js:11-14 (o cabeçalho com a convenção "chaves idênticas nos dois modos")
    - web/src/App.jsx:1723-1760 (topo de CapitalCurve: desestruturação, hasSeries, o useEffect do Ibovespa)
    - web/src/App.jsx:1836-1854 (o ternário de 2 ramos que vira 3)
    - .planning/phases/21-duplica-o-removida-e-portf-lio-consolidado/21-UI-SPEC.md (FIX-03 + Copywriting Contract — o JSX e o texto já estão escritos, literais)
  </read_first>
  <files>web/src/copy.js, web/src/App.jsx</files>
  <action>
**Edição 1 — `web/src/copy.js`.** Adicionar a chave `curvaPoucosDias` em
`COPY.estudo` E em `COPY.operador`, como arrow function inline junto de
`saudacao`/`resumoDia` (mesmo bloco, mesmo estilo). Texto idêntico nos dois
modos — é afirmação factual sobre disponibilidade de dado, não enquadramento
de decisão, que é onde o tom Estudo × Operador diverge:

```
curvaPoucosDias: (dias) =>
  dias === 1
    ? "Só 1 dia registrado ainda — a curva aparece a partir do 3º dia."
    : "Só 2 dias registrados ainda — a curva aparece a partir do 3º dia.",
```

A chave PRECISA existir nos dois dicionários no mesmo commit: `test_copy_theme.mjs`
compara os conjuntos de chaves e derruba a suíte se faltar de um lado.

**Edição 2 — `web/src/App.jsx`, componente `CapitalCurve`.** Três pontos:

(a) Linha ~1729: `const { data, quotes } = ctx;` → `const { data, quotes, cp } = ctx;`
    Verificado que `ctx.cp` já existe (montado em `App.jsx:8760` a partir de
    `copyFor(appMode)`) — nenhum chamador muda, nenhuma prop nova.

(b) Linha ~1736: trocar o limiar e acrescentar o flag do estado intermediário:
```
const hasSeries = ec.days >= 3;
const poucosDias = ec.days >= 1 && ec.days < 3;
```
    Atualizar o comentário da linha (hoje diz "mostra a curva a partir do 1º
    dia") para registrar a razão do limiar novo: com menos de 3 snapshots a
    escala do eixo Y é degenerada e o desenho vira um segmento reto, não uma
    curva. Comentário datado, no padrão do repositório (Fase 21, FIX-03).

(c) Linhas ~1836-1854: o ternário de 2 ramos vira 3, com o ramo novo NO MEIO
    (`hasSeries ? ... : poucosDias ? ... : ...`). O ramo novo usa exatamente
    o mesmo objeto de estilo do placeholder de zero dias
    (`fontSize: "11.5px", color: T.textFaint, marginTop: "10px", lineHeight: 1.5`)
    e renderiza `{cp.curvaPoucosDias(ec.days)}`. O ramo de zero dias fica
    byte a byte igual ao de hoje.

**O que NÃO muda, e é deliberado:**
- O `useEffect` do Ibovespa (~1750-1758) já faz `if (!hasSeries) return` —
  nenhuma edição ali. Subir o limiar adia a busca do benchmark para o 3º
  dia, que é o efeito correto: não há o que comparar contra uma curva que
  não é desenhada. Manter a expressão literal `if (!hasSeries) return`,
  que `test_benchmark_curva.mjs:84` trava por regex.
- O SVG (~1816-1834) não é tocado. Tanto `poucosDias` quanto o estado de
  zero caem no `hasSeries === false` do ternário do SVG, então o rabisco
  tracejado — que já comunica "ainda sem dado" sem palavra nenhuma — segue
  aparecendo nos dois casos. O `21-CONTEXT.md` pede para mudar o TEXTO do
  caso 1-2, não para remover o visual existente.
- Nenhum cálculo financeiro muda: `equityCurve` não é tocada, só o limiar de
  exibição.

**Depois das duas edições, nesta ordem:**
1. `node web/tests/test_fase21_dedup_consolidacao.mjs` — tudo verde.
2. `node web/tests/test_copy_theme.mjs` — paridade das chaves preservada.
3. `node web/tests/test_benchmark_curva.mjs` — as duas asserções sobre o
   placeholder tracejado e a guarda `if (!hasSeries) return` seguem verdes.
4. `cd web && npx vite build`.
5. `bash scripts/executar.sh --testes`.

Se `test_benchmark_curva.mjs` falhar, PARAR: a premissa deste plano (que
aquelas duas asserções sobrevivem) estava errada e precisa ser reaberta com
o orquestrador, não contornada afrouxando o guardião.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "curvaPoucosDias" web/src/copy.js` retorna exatamente `2` (uma em `COPY.estudo`, uma em `COPY.operador`).
    - `node -e "import('./web/src/copy.js').then(m=>{const e=Object.keys(m.COPY.estudo).sort(),o=Object.keys(m.COPY.operador).sort();process.exit(JSON.stringify(e)===JSON.stringify(o)?0:1)})"` sai com código 0 — os conjuntos de chaves seguem idênticos.
    - `grep -c "ec.days >= 3" web/src/App.jsx` retorna ao menos `1`; `grep -c "ec.days >= 1 && ec.days < 3" web/src/App.jsx` retorna ao menos `1`.
    - `grep -c "ec.days >= 1;" web/src/App.jsx` retorna `0` — o limiar antigo não sobreviveu.
    - `grep -c "cp.curvaPoucosDias(ec.days)" web/src/App.jsx` retorna `1`.
    - `grep -c "Sua curva começa amanhã" web/src/App.jsx` retorna `1` — o texto do estado zero está intacto.
    - `grep -c "if (!hasSeries) return" web/src/App.jsx` retorna ao menos `1` — a guarda do Ibovespa segue com a expressão literal que `test_benchmark_curva.mjs` trava.
    - `node web/tests/test_fase21_dedup_consolidacao.mjs`, `node web/tests/test_copy_theme.mjs` e `node web/tests/test_benchmark_curva.mjs` saem os três com código 0.
    - `cd web && npx vite build` conclui com código 0.
    - `bash scripts/executar.sh --testes` sai com código 0 (as duas suítes).
    - `git diff --stat` lista apenas `web/src/App.jsx`, `web/src/copy.js` e `web/tests/test_fase21_dedup_consolidacao.mjs`; `git diff web/package.json web/package-lock.json` sai vazio.
    - Nenhum `git push` foi executado.
  </acceptance_criteria>
  <done>Com 1 ou 2 dias registrados o gráfico diz o que está faltando em vez de desenhar uma reta; a curva só aparece a partir do 3º dia; o texto novo respeita o vocabulário por modo; e o estado de conta nova não mudou.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dado insuficiente → visualização | Com menos de 3 snapshots não há escala de eixo Y com sentido. Desenhar mesmo assim é apresentar como leitura de desempenho algo que é ruído de escala — atravessa o princípio 4 do CLAUDE.md (não inventar valor quando o dado é insuficiente). |
| texto do produto → dicionário por modo | Toda frase nova de tela passa por `copy.js` nos dois modos; string solta no componente fura a convenção e o guardião de paridade. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-11 | Spoofing | Curva de 2 pontos apresentada como leitura de desempenho (escala degenerada lida como tendência) | mitigate | Limiar `ec.days >= 3` mais placeholder textual; asserções 1-2 do guardião provam a degenerescência com `equityCurve` real |
| T-21-12 | Tampering | Gate escrito sobre `curve.length` em vez de `ec.days`, oscilando ±1 conforme o snapshot seja de hoje ou de ontem | mitigate | Asserções 1 e 2 do guardião exercitam os dois sub-casos e travam `days`; a asserção 5 exige o literal `ec.days >= 3` |
| T-21-13 | Repudiation | Chave de copy só num dos modos, derrubando a paridade que o repositório trata como invariante | mitigate | Chave nos dois dicionários no mesmo commit; critério de aceite roda a comparação de conjuntos de chaves diretamente |
| T-21-14 | Information disclosure | Reescrever sem querer o texto do estado de zero dias, mudando o que a conta recém-criada lê | mitigate | Asserção 10 do guardião trava o texto de hoje byte a byte; critério de aceite confere contagem 1 |
| T-21-15 | Denial of service | Subir o limiar quebrar a guarda do fetch do Ibovespa ou o rabisco tracejado travados por `test_benchmark_curva.mjs` | mitigate | Asserção 12 do guardião duplica a regex do rabisco; a Task 2 roda `test_benchmark_curva.mjs` isolado e manda PARAR se falhar |
| T-21-SC | Tampering | instalações npm/pip/cargo | mitigate | Zero pacote novo neste plano; `git diff` dos manifests vazio como critério de aceite |
</threat_model>

<verification>
- `equityCurve` com 1 e 2 snapshots provada em teste unitário real (função pura, sem build).
- `ec.days >= 3` e `poucosDias` presentes; `ec.days >= 1` ausente.
- `cp.curvaPoucosDias(ec.days)` no ramo novo; texto do estado zero intocado.
- `curvaPoucosDias` nas duas metades de `COPY`, conjuntos de chaves idênticos.
- `test_benchmark_curva.mjs` verde (rabisco tracejado e guarda do Ibovespa intactos).
- `npx vite build` verde e `bash scripts/executar.sh --testes` verde.
- Nenhuma dependência nova, nenhum push.
</verification>

<success_criteria>
O critério 4 do ROADMAP da Fase 21 está satisfeito: numa conta nova, logo
depois da primeira operação, a área do gráfico mostra um placeholder que diz
que ainda faltam dados, e a curva só é desenhada quando há forma de verdade
para desenhar.
</success_criteria>

<output>
Create `.planning/phases/21-duplica-o-removida-e-portf-lio-consolidado/21-03-SUMMARY.md` when done
</output>
