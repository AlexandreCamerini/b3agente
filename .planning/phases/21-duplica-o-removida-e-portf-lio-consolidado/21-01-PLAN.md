---
phase: 21-duplica-o-removida-e-portf-lio-consolidado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/App.jsx
  - web/tests/test_fase21_dedup_consolidacao.mjs
autonomous: true
requirements: [DEDUP-01, DEDUP-03]

must_haves:
  truths:
    - "O card 'PATRIMÔNIO SIMULADO' (CapitalCurve) aparece em exatamente UMA tela do app — Acompanhar; percorrer Acompanhar e Portfólio na mesma sessão não mostra duas cópias idênticas"
    - "Portfólio mostra UM card com patrimônio total, resultado aberto, caixa disponível e em posições em grid 2×2 — não quatro cards empilhados"
    - "Os quatro valores do card consolidado usam a escala numérica nomeada da Fase 20 (numBody/numMicro), dando a ela o primeiro consumidor real de produção fora do Topbar"
    - "Um guardião estático trava as duas mudanças: reintroduzir a segunda CapitalCurve ou re-explodir o card em quatro faz a suíte canônica falhar"
  artifacts:
    - path: "web/src/App.jsx"
      provides: "rota Portfólio sem CapitalCurve duplicada + card consolidado 2×2 no lugar dos 4 cards kpi()"
      contains: "PATRIMÔNIO TOTAL"
    - path: "web/tests/test_fase21_dedup_consolidacao.mjs"
      provides: "guardião estático de DEDUP-01 e DEDUP-03 (grep sobre App.jsx, padrão da casa)"
      contains: "CapitalCurve"
  key_links:
    - from: "web/src/App.jsx (card consolidado do Portfólio)"
      to: "numBody / numMicro / card / kicker / MONO (constantes de módulo, Fase 20)"
      via: "spread de objeto de estilo, nunca redefinição inline"
      pattern: "\\.\\.\\.numBody"
    - from: "web/tests/test_fase21_dedup_consolidacao.mjs"
      to: "web/src/App.jsx"
      via: "readFileSync + regex (guardião estático, o módulo importa @capacitor/core e não é importável fora do build)"
      pattern: "readFileSync"
---

<objective>
Tirar a duplicação do Portfólio: a curva de patrimônio passa a existir só em
Acompanhar, e os quatro cards de KPI empilhados viram um único card denso em
grid 2×2, no mesmo padrão visual do "RESUMO DO DIA".

Purpose: DEDUP-01 e DEDUP-03 são a mesma tela e o mesmo componente
(`CarteiraScreen`) — separá-los em dois planos faria dois agentes editarem o
mesmo arquivo em ondas diferentes sem ganho nenhum. Juntos, são uma mudança
pontual e verificável por grep.

Output: `App.jsx` com uma única chamada de `<CapitalCurve>`, o helper `kpi()`
removido sem órfão, o card consolidado consumindo `numBody`/`numMicro`, e um
guardião novo travando os dois requisitos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/21-duplica-o-removida-e-portf-lio-consolidado/21-CONTEXT.md
@.planning/phases/21-duplica-o-removida-e-portf-lio-consolidado/21-UI-SPEC.md
@.planning/phases/21-duplica-o-removida-e-portf-lio-consolidado/21-PATTERNS.md
@CLAUDE.md

<interfaces>
Constantes de módulo já definidas em `App.jsx` — usar por spread, NUNCA
redefinir inline (linhas re-verificadas ao vivo em 2026-09-05, zero drift em
relação ao `21-UI-SPEC.md`):

- `App.jsx:236` — `const MONO = "ui-monospace,'SF Mono',Menlo,Consolas,monospace";`
- `App.jsx:257` — `const numBody = { fontSize: "18px", fontWeight: 700 };`
- `App.jsx:258` — `const numMicro = { fontSize: "13px", fontWeight: 600 };`
- `App.jsx:292` — `const card = { background: T.bgCard, border: ..., borderRadius: "12px" };`
- `App.jsx:293` — `const kicker = { fontSize: "10px", color: T.textFaint, letterSpacing: "0.06em" };`

Variáveis já derivadas no escopo de `CarteiraScreen` (linhas 4335-4339), todas
vindas de `portfolioMetrics(...)` — o card consolidado só as REEXIBE, não
recalcula nada (CLAUDE.md princípio 5, cálculo determinístico):
`positionsValue` (`m.posVal`), `total` (`m.patr`), `openPnL`, `openPct`,
`data.cash`.

`bash scripts/executar.sh --testes` — suíte canônica (pytest + TODOS os
`web/tests/*.mjs`, descobertos por glob em `scripts/executar.sh:33`). Um
arquivo `.mjs` novo em `web/tests/` entra na suíte automaticamente, sem
registro em lugar nenhum.

`npx vite build` (dentro de `web/`) — obrigatório após editar o front: grep e
teste estático NÃO pegam erro de sintaxe JSX (CLAUDE.md, Validação
obrigatória).
</interfaces>

<estado_verificado_ao_vivo>
Confirmado por `grep -n` no working tree em 2026-09-05, imediatamente antes
de escrever este plano — os números do `21-UI-SPEC.md`/`21-PATTERNS.md`
continuam válidos, mas RE-VERIFIQUE antes de editar (outra fase pode tocar
`App.jsx`):

- `<CapitalCurve` → 2 ocorrências: linha **2029** (`EvolucaoScreen`, a que
  FICA) e linha **9027** (rota Portfólio, a que SAI).
- `kpi(` → 5 ocorrências: definição em **4340** e as 4 chamadas em
  **4359-4362**. Nenhum outro consumidor no arquivo inteiro.
- O grid dos 4 cards vive em **4358-4363**.

**Nenhum guardião existente protege estas duas mudanças.** Varredura feita
sobre os 115 arquivos de `web/tests/`:
- `PATRIMÔNIO TOTAL` / `RESULTADO ABERTO` / `CAIXA DISPONÍVEL` /
  `EM POSIÇÕES` → zero teste cita qualquer um desses rótulos.
- `CapitalCurve` → citado em `test_benchmark_curva.mjs` e
  `test_chart_colors_theme_aware.mjs`, mas AMBOS inspecionam o CORPO da
  função `CapitalCurve` (via `functionBody("CapitalCurve")` /
  `usePalette`), nunca os seus locais de chamada. Remover a chamada da rota
  Portfólio não os afeta.

Ou seja: este plano só ADICIONA guardião, não precisa reescrever nenhum.
(O plano 21-02 é o oposto disso — lá há três guardiões a atualizar.)
</estado_verificado_ao_vivo>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guardião de DEDUP-01/DEDUP-03 escrito antes da mudança (RED)</name>
  <read_first>
    - web/tests/test_fase20_fundacao_visual.mjs (guardião irmão da fase anterior — copiar a forma: readFileSync de App.jsx, helper `ok(nome, cond)`, `process.exit(fails ? 1 : 0)`)
    - web/tests/test_fase3_c19_card_status.mjs:20-33 (padrão da casa para ISOLAR o corpo de um componente e ABORTAR se o marcador sumir, em vez de virar no-op silencioso)
    - web/src/App.jsx:4340-4363 (o helper `kpi()` e as 4 chamadas que este guardião vai travar)
    - web/src/App.jsx:2029 e :9027 (as duas chamadas de `<CapitalCurve>`)
  </read_first>
  <files>web/tests/test_fase21_dedup_consolidacao.mjs</files>
  <behavior>
    Com o `App.jsx` AINDA NÃO editado, o guardião novo deve FALHAR nestas
    asserções (é a prova de que ele tem dentes e não é no-op):
    - "exatamente 1 ocorrência de `<CapitalCurve`" → falha, hoje há 2
    - "helper `kpi(` não existe mais em App.jsx" → falha, hoje há 5 ocorrências
    - "card consolidado usa `...numBody`" → falha, ainda não existe
    E deve PASSAR nesta, que descreve o que não muda:
    - "a chamada sobrevivente de `<CapitalCurve>` está dentro de `EvolucaoScreen`"
  </behavior>
  <action>
Criar `web/tests/test_fase21_dedup_consolidacao.mjs` seguindo o padrão de
guardião estático da casa (regex sobre `App.jsx` lido com `readFileSync` — o
módulo importa `@capacitor/core` e não é importável fora do build; ver o
cabeçalho de `test_fase3_c19_card_status.mjs`).

Cabeçalho do arquivo: comentário datado explicando que este é o guardião da
Fase 21 (v1.5, Redesenho de UI), requisitos DEDUP-01 e DEDUP-03, e que os
planos 21-03 vão ESTENDER este mesmo arquivo com as asserções de FIX-03 —
para o próximo agente não criar um segundo arquivo paralelo.

Asserções obrigatórias (DEDUP-01):
1. `(app.match(/<CapitalCurve/g) || []).length === 1` — exatamente uma
   chamada no arquivo inteiro.
2. A chamada sobrevivente está DENTRO do corpo de `EvolucaoScreen`: isolar o
   corpo com o padrão da casa (`indexOf("function EvolucaoScreen(")` até o
   próximo `"\nfunction "`) e afirmar que esse recorte contém `<CapitalCurve`.
   Se `function EvolucaoScreen(` não for encontrado, ABORTAR com
   `process.exit(1)` e mensagem explícita — nunca passar em silêncio.
3. O recorte de `CarteiraScreen` (mesmo padrão de isolamento) NÃO contém
   `<CapitalCurve`. Isolar `CarteiraScreen` também serve à asserção 5 abaixo.

Asserções obrigatórias (DEDUP-03):
4. `kpi(` não aparece mais em `App.jsx` — `!/\bkpi\(/.test(app)` — helper
   removido sem órfão, junto com as 4 chamadas.
5. Dentro do recorte de `CarteiraScreen`, o card consolidado existe e reusa
   as constantes de módulo em vez de redefinir estilo: o recorte contém
   `...numBody` (spread, pelo menos 4 ocorrências — uma por célula),
   `...numMicro` (pelo menos 1, a sub-linha de percentual) e
   `gridTemplateColumns: "1fr 1fr"`.
6. Os 4 rótulos seguem presentes no recorte de `CarteiraScreen`, com o
   mesmo texto de hoje: `PATRIMÔNIO TOTAL`, `RESULTADO ABERTO`,
   `CAIXA DISPONÍVEL`, `EM POSIÇÕES` — a consolidação não pode perder
   nenhum dos quatro números.
7. Os quatro valores continuam vindo das MESMAS variáveis derivadas de
   `portfolioMetrics`, nunca de conta refeita no JSX: o recorte contém
   `{money(total)}`, `{moneySigned(openPnL)}`, `{pct(openPct)}`,
   `{money(data.cash)}` e `{money(positionsValue)}`.
8. Um único container `card` para as quatro células: no recorte de
   `CarteiraScreen`, a substring `gridTemplateColumns: "repeat(auto-fit,minmax(160px,1fr))"`
   NÃO existe mais (era a assinatura do grid antigo de 4 cards).

Rodar `node web/tests/test_fase21_dedup_consolidacao.mjs` e CONFIRMAR que
ele falha nas asserções 1, 4, 5 e 8 (e passa nas 2, 6, 7). Registrar a saída
literal no SUMMARY — é a prova de que o guardião tem dentes.

NÃO editar `App.jsx` nesta task.
  </action>
  <verify>
    <automated>node web/tests/test_fase21_dedup_consolidacao.mjs; test $? -ne 0 && echo "RED confirmado (esperado nesta task)"</automated>
  </verify>
  <acceptance_criteria>
    - `web/tests/test_fase21_dedup_consolidacao.mjs` existe e é executável com `node` sem build.
    - Rodado contra o `App.jsx` NÃO editado, o script sai com código diferente de 0.
    - A saída contém `FALHOU` para a asserção de contagem de `<CapitalCurve`, para a de `kpi(` e para a de `...numBody`.
    - A saída contém `ok ` para a asserção "chamada sobrevivente dentro de EvolucaoScreen".
    - `git status --porcelain web/src/App.jsx` sai VAZIO (nenhuma edição de App.jsx nesta task).
    - O arquivo aborta com mensagem explícita (não passa em silêncio) se `function EvolucaoScreen(` ou `function CarteiraScreen(` não forem encontrados — verificável lendo o código do próprio guardião.
  </acceptance_criteria>
  <done>O guardião da Fase 21 existe, está na suíte por glob, e provou ter dentes falhando contra o código atual pelas razões certas.</done>
</task>

<task type="auto">
  <name>Task 2: Remover a CapitalCurve duplicada e consolidar os 4 cards em 1 (GREEN)</name>
  <read_first>
    - web/src/App.jsx:9027 (a linha única da rota Portfólio — RE-VERIFICAR com `grep -n "<CapitalCurve" web/src/App.jsx` antes de editar)
    - web/src/App.jsx:4340-4363 (helper `kpi()` + o grid dos 4 cards)
    - web/src/App.jsx:236, 257-258, 292-293 (MONO, numBody, numMicro, card, kicker — as constantes a reusar por spread)
    - web/src/App.jsx:1974-1980 (o card "RESUMO DO DIA" de EvolucaoScreen — a referência byte a byte de densidade/espaçamento pedida pelo 21-CONTEXT.md)
    - .planning/phases/21-duplica-o-removida-e-portf-lio-consolidado/21-UI-SPEC.md (Structural Contract, DEDUP-01 e DEDUP-03 — o JSX alvo já está escrito lá, literal)
  </read_first>
  <files>web/src/App.jsx</files>
  <action>
Duas edições em `App.jsx`, ambas com o JSX alvo já escrito literalmente no
`21-UI-SPEC.md` (Structural Contract) e replicado no `21-PATTERNS.md` — copiar
de lá, não reinventar.

**Edição 1 (DEDUP-01), linha ~9027.** Remover APENAS a substring
`<CapitalCurve ctx={ctx} />` daquela linha. Tudo o mais na linha
(`<CarteiraScreen ctx={ctx} />`, a `<div>` do botão "Ver histórico de
operações" e o fechamento do fragmento) fica byte a byte igual. A chamada de
`EvolucaoScreen` (linha ~2029) NÃO é tocada — nem ela, nem suas props.

**Edição 2 (DEDUP-03), linhas ~4340-4363.** Duas partes:
(a) Apagar o helper `kpi = (label, value, color, sub, subColor) => (...)`
    INTEIRO (definição multi-linha, ~4340-4346). Confirmado por grep que as
    únicas chamadas são as 4 que saem na parte (b): não sobra órfão, não há
    outro consumidor no arquivo.
(b) Substituir o `<div>` do grid `repeat(auto-fit,minmax(160px,1fr))` e as 4
    chamadas `{kpi(...)}` pelo card consolidado do `21-UI-SPEC.md`: um
    `<div style={{ ...card, padding: "16px 18px", margin: "16px 0 18px" }}>`
    envolvendo um `<div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "16px 18px" }}>`
    com 4 células. Cada célula: `<div style={kicker}>RÓTULO</div>` seguido de
    `<div style={{ ...numBody, fontFamily: MONO, color: ... }}>{valor}</div>`.
    A célula "RESULTADO ABERTO" ganha ainda a sub-linha de percentual em
    `{ ...numMicro, fontFamily: MONO, color: ... }` com `{pct(openPct)}`.

Regras que valem para as duas edições:
- Ordem das 4 células preservada, leitura esquerda→direita, cima→baixo:
  Patrimônio total, Resultado aberto, Caixa disponível, Em posições. Sem
  reordenar (decisão registrada no `21-CONTEXT.md`, Claude's Discretion).
- `margin: "16px 0 18px"` no container externo — mesma margem que o grid
  antigo usava, para não mexer no ritmo vertical da tela acima/abaixo.
- Cor da célula "Resultado aberto" mantém o branch existente
  `openPnL >= 0 ? T.positive : T.negative`, tanto no valor quanto na
  sub-linha. Nenhuma lógica nova, nenhum cálculo novo.
- NÃO adicionar overline/heading "PORTFÓLIO" dentro do card: o
  `<h1>{cp.tituloPortfolio}</h1>` logo acima (linha 4351) já dá o contexto, e
  um segundo título seria exatamente a redundância que esta fase remove.
- NÃO introduzir cor de destaque (`T.accent`) nem borda de destaque: os 4
  cards de hoje não têm, o consolidado também não tem.
- Estilo 100% inline com spread das constantes de módulo. Zero CSS Modules,
  zero Tailwind, zero classe nova, zero constante de estilo nova.

Depois das duas edições, nesta ordem:
1. `node web/tests/test_fase21_dedup_consolidacao.mjs` — agora TUDO passa.
2. `cd web && npx vite build` — obrigatório: grep e teste estático não pegam
   erro de sintaxe JSX (CLAUDE.md).
3. `bash scripts/executar.sh --testes` — suíte canônica, as DUAS suítes.

Se a suíte canônica acusar falha em QUALQUER teste que não seja o guardião
novo, PARAR e reportar: a varredura de guardiões deste plano concluiu que
nenhum outro teste cobre estas duas mudanças, então uma falha inesperada
significa que a varredura errou e a premissa precisa ser reaberta, não
contornada.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<CapitalCurve" web/src/App.jsx` retorna exatamente `1`.
    - `grep -n "<CapitalCurve" web/src/App.jsx` aponta para uma linha DENTRO do corpo de `EvolucaoScreen` (linha ~2029), não da rota Portfólio.
    - `grep -c "kpi(" web/src/App.jsx` retorna `0` (helper e as 4 chamadas removidos, sem órfão).
    - `grep -c "repeat(auto-fit,minmax(160px,1fr))" web/src/App.jsx` retorna `0` para a região do Portfólio — a assinatura do grid antigo de 4 cards não existe mais.
    - `grep -c "\.\.\.numBody" web/src/App.jsx` retorna pelo menos `4` (as quatro células do card consolidado).
    - `node web/tests/test_fase21_dedup_consolidacao.mjs` sai com código 0 e imprime `todos os testes passaram`.
    - `cd web && npx vite build` conclui sem erro (código 0).
    - `bash scripts/executar.sh --testes` sai com código 0 — as DUAS suítes (pytest + `web/tests/*.mjs`).
    - `git diff --stat` lista apenas `web/src/App.jsx` e `web/tests/test_fase21_dedup_consolidacao.mjs`; `git diff web/package.json web/package-lock.json` sai vazio (zero dependência nova).
    - Nenhum `git push` foi executado.
  </acceptance_criteria>
  <done>Portfólio tem uma única fonte de patrimônio (o card consolidado 2×2) e nenhuma curva duplicada; a escala numérica da Fase 20 ganhou seu primeiro consumidor real de produção; a suíte canônica está verde e o build de produção compila.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| motor determinístico → JSX de exibição | `portfolioMetrics()` produz patrimônio/PnL/caixa; o card consolidado apenas REEXIBE. Nenhum número pode ser recalculado no JSX (CLAUDE.md princípio 5). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | Card consolidado recalculando patrimônio/PnL no JSX em vez de reexibir `portfolioMetrics` | mitigate | Asserção 7 do guardião exige as chamadas literais `{money(total)}`, `{moneySigned(openPnL)}`, `{pct(openPct)}`, `{money(data.cash)}`, `{money(positionsValue)}` — nenhuma aritmética nova no JSX |
| T-21-02 | Information disclosure | Consolidar 4 cards em 1 perdendo silenciosamente um dos quatro números | mitigate | Asserção 6 do guardião trava os quatro rótulos por texto literal |
| T-21-03 | Repudiation | Remover o helper `kpi()` deixando órfão ou quebrando outro consumidor | mitigate | Varredura `grep -n "kpi("` feita no planejamento (só definição + 4 chamadas); asserção 4 do guardião trava contagem 0 |
| T-21-04 | Denial of service | Erro de sintaxe JSX passando por grep e teste estático e quebrando o app inteiro | mitigate | `npx vite build` obrigatório na Task 2, com código 0 como critério de aceite |
| T-21-SC | Tampering | instalações npm/pip/cargo | mitigate | Zero pacote novo neste plano; `git diff` de `web/package.json`/`package-lock.json` vazio como critério de aceite |
</threat_model>

<verification>
- `<CapitalCurve` com exatamente 1 ocorrência, dentro de `EvolucaoScreen`.
- `kpi(` com 0 ocorrências.
- Card consolidado em grid 2×2 reusando `card`/`kicker`/`numBody`/`numMicro`/`MONO` por spread.
- Guardião novo verde e provado com dentes (falhou antes, passa depois).
- `npx vite build` verde.
- `bash scripts/executar.sh --testes` verde (as duas suítes).
- Nenhuma dependência nova, nenhum push.
</verification>

<success_criteria>
Os critérios 1 e 2 do ROADMAP da Fase 21 estão satisfeitos no código e
travados por guardião: a curva de patrimônio existe em uma única tela, e o
Portfólio abre com um único card denso em vez de quatro empilhados.
</success_criteria>

<output>
Create `.planning/phases/21-duplica-o-removida-e-portf-lio-consolidado/21-01-SUMMARY.md` when done
</output>
