---
phase: 06-instrumentacao-assertividade-adr015
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/analysis_outcomes.py
  - server/app/main.py
  - server/tests/test_analysis_outcomes.py
autonomous: true
requirements: [ADR15-01]
must_haves:
  truths:
    - "Uma análise N1 com plano acionável grava `entrada`, `alvo2`, `rr2`, `confluencia` e `entradaAMercado` no registro de outcome"
    - "Uma análise N2 grava `confluencia` e NÃO grava `entrada`/`alvo2`/`rr2` (a geometria da proposta da IA não vem do plano determinístico)"
    - "Um plano do tipo `a mercado (gatilho já rompido, dentro da zona)` fica marcado com `entradaAMercado: true` — sem esse campo o Plano 02 exigiria toque num gatilho que já rompeu e jogaria o caso adverso para fora do denominador"
    - "Todo registro novo carrega `metodologiaVersao: 2`; registro antigo continua sem o campo e não é reescrito"
    - "`metodologiaVersao` declara QUAIS CAMPOS foram gravados, não qual âncora resolveu o registro — a âncora efetiva é carimbada na resolução (campo `ancora`, Plano 02)"
    - "O CSV de export continua abrindo, agora com as colunas novas no fim"
  artifacts:
    - path: "server/app/analysis_outcomes.py"
      provides: "registrar() com entrada/alvo2/rr2/confluencia/entradaAMercado + METODOLOGIA_ATUAL"
      contains: "METODOLOGIA_ATUAL"
    - path: "server/app/main.py"
      provides: "call sites N1 e N2 passando os campos novos"
      contains: "entrada=plano.get(\"entrada\")"
    - path: "server/tests/test_analysis_outcomes.py"
      provides: "guardiões dos campos novos e da retrocompatibilidade"
      contains: "metodologiaVersao"
  key_links:
    - from: "server/app/main.py"
      to: "server/app/analysis_outcomes.registrar"
      via: "kwargs entrada/alvo2/rr2/confluencia/entrada_a_mercado"
      pattern: "analysis_outcomes\\.registrar\\("
    - from: "server/app/main.py N1"
      to: "setups.plano_do_resultado"
      via: "dict `plano` já em escopo (não recalcular)"
      pattern: "plano\\.get\\(\"entrada\"\\)"
    - from: "server/app/main.py N1"
      to: "tipo de entrada de setups.plano_operacional"
      via: "entrada_a_mercado derivado de plano['tipo']"
      pattern: "entrada_a_mercado="
---

<objective>
Fechar a lacuna de dado que impede corrigir a âncora da barreira tripla:
`analysis_outcomes.registrar` passa a gravar `entrada` (o gatilho do plano
determinístico), `alvo2`, `rr2`, `confluencia` e `entradaAMercado` (o tipo de
entrada do plano), além de carimbar a versão da metodologia de instrumentação
em cada registro novo.

Purpose: hoje 100% dos registros de produção (392) não têm `entrada` nem
`confluencia` — sem esses campos o Plano 02 não tem como ancorar a avaliação
no gatilho, e a pergunta comercial central ("confluência alta acerta mais?")
não tem dado. Mudança PURAMENTE ADITIVA: nenhum registro antigo é migrado,
inferido ou reescrito.

Output: `registrar()` com 5 campos novos + `METODOLOGIA_ATUAL`, os dois call
sites de `main.py` alimentando-os, `to_csv` exportando-os e testes travando
tudo isso.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-instrumentacao-assertividade-adr015/06-CONTEXT.md
@docs/adr/015-assertividade-do-motor-de-recomendacao.md
@CLAUDE.md

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código. Não explorar o repo. -->

server/app/analysis_outcomes.py (assinatura ATUAL, keyword-only):
  registrar(conn, *, ticker, modo, tipo, modelo, setup, recomendacao,
            stop, alvo, preco, snapshot_id, confianca=None, user_id=None,
            regime=None) -> None
  Guard clause existente (linha ~75): `if stop is None or alvo is None or
  preco is None or stop == alvo: return` — NÃO alterar.
  Chaves gravadas hoje: id, ticker, modo, tipo, modelo, setup, recomendacao,
  stopProposto, alvoProposto, precoNaAnalise, snapshotId, confianca, regime,
  criadoEm, prazoPregoes, resultado, precoResolucao, rMultiple, resolvidoEm.

server/app/setups.py — dict devolvido por `plano_operacional` quando operável
(linhas 640-654, NÃO MODIFICAR): decisao, tipo, setup, lado, entrada, stop,
alvo1, alvo2, rr1, rr2, riscoPorAcao, motivo.

server/app/setups.py linhas 602-612 (SÓ LEITURA — é o dado que motiva
`entradaAMercado`). `entrada` NEM SEMPRE é o gatilho:
  entrada = float(gatilho);  tipo = "no rompimento do gatilho"
  if close is not None and rompido and excedente <= ZONA_PERSEGUICAO * risco:
      entrada = float(close)   # entrada a mercado: risco real conta a partir daqui
      tipo = "a mercado (gatilho já rompido, dentro da zona)"
  Ou seja: num plano operável, `plano["tipo"]` tem exatamente DOIS valores —
  "no rompimento do gatilho" e "a mercado (gatilho já rompido, dentro da
  zona)". No segundo caso não há gatilho a ser tocado: a entrada é imediata,
  e exigir toque jogaria justamente o caso ADVERSO (gap contra a posição no
  candle seguinte) para fora do denominador.

server/app/main.py N1 (~1281-1327): `sres = snap["setups"]`,
`plano = setups.plano_do_resultado(sres, close=snap.get("close"))`,
`sres.get("confluencia")` já é usado no `setups_payload` (linha ~1286).

server/app/main.py N2 (~1368-1432): tem `snap` em escopo (portanto
`snap["setups"]`), mas NÃO tem `plano` — e não deve passar a ter (ver Task 2).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: registrar() grava entrada/alvo2/rr2/confluencia/entradaAMercado + versão de metodologia</name>
  <files>server/app/analysis_outcomes.py, server/tests/test_analysis_outcomes.py</files>
  <read_first>
    - server/app/analysis_outcomes.py (arquivo inteiro, 398 linhas — `registrar` linhas 60-100, `to_csv` 245-262)
    - server/tests/test_analysis_outcomes.py (linhas 32-75 = testes de `registrar`; 257-271 = `test_to_csv_colunas_fixas_e_escape`)
    - .planning/phases/06-instrumentacao-assertividade-adr015/06-CONTEXT.md (seção ADR15-01)
  </read_first>
  <behavior>
    - `registrar(..., entrada=12.5, alvo2=15.0, rr2=2.0, confluencia=72, entrada_a_mercado=False)` grava `entrada`/`alvo2`/`rr2` como float, `confluencia` como int, `entradaAMercado is False` e `metodologiaVersao == 2`.
    - `registrar(..., entrada=12.5, entrada_a_mercado=True)` grava `entradaAMercado is True`.
    - `registrar(...)` sem os kwargs novos grava `entrada=None`, `alvo2=None`, `rr2=None`, `confluencia=None`, `entradaAMercado=None` e AINDA ASSIM `metodologiaVersao == 2` (o registro é da instrumentação nova, só não tem geometria de gatilho — caso N2).
    - A guard clause de `stop/alvo/preco` continua descartando o registro antes de qualquer trabalho novo.
    - `to_csv` de um registro novo tem as 5 colunas novas + `metodologiaVersao` DEPOIS das 18 colunas atuais, nessa ordem; registro antigo (sem as chaves) exporta célula vazia, nunca valor inferido.
  </behavior>
  <action>
    Em `server/app/analysis_outcomes.py`:

    1. Logo abaixo de `MIN_N = 10` (linha 33), declarar a constante de versão de
    metodologia de instrumentação: `METODOLOGIA_ATUAL = 2`. Comentário obrigatório
    explicando com precisão o que o campo significa E o que ele NÃO significa:
    `metodologiaVersao` declara QUAIS CAMPOS o registro carrega no momento da
    GRAVAÇÃO (versão 1 = registro anterior ao ADR-015, sem `entrada`; versão 2 =
    registro do ADR15-01 em diante, que carrega `entrada`/`entradaAMercado` quando
    o plano determinístico os definiu). Ele NÃO diz qual âncora resolveu o
    registro — isso é carimbado na resolução pelo campo `ancora` (Plano 02,
    ADR15-02), porque um registro N2 é versão 2 e mesmo assim resolve pela âncora
    de PREÇO. Confundir os dois é como se reintroduz, com outro nome, a mistura de
    metodologias que o ADR-015 corrige. A ausência do campo num registro significa
    versão 1 — registro antigo NUNCA é reescrito para ganhar o campo.

    2. Adicionar um helper puro `_metodologia(entry: dict) -> int` que devolve
    `int(entry.get("metodologiaVersao") or 1)`. Ele será consumido pelos Planos 02
    e 03; declará-lo aqui evita que cada consumidor reimplemente a regra do
    default.

    3. Estender a assinatura keyword-only de `registrar` com cinco parâmetros
    novos, todos `Optional` com default `None`, posicionados depois de `regime`:
    `entrada`, `alvo2`, `rr2`, `confluencia`, `entrada_a_mercado`. Não mexer em
    nenhum parâmetro existente e não mexer na guard clause.

    4. No dict `entry`, acrescentar as chaves com os nomes EXATOS exigidos pelo
    critério de sucesso da fase: `entrada`, `alvo2`, `rr2`, `confluencia` (não
    usar sufixo `Proposto` — o ADR-015 e o ROADMAP nomeiam esses campos assim, e a
    consulta de produção do ADR procurou literalmente por `entrada`). Converter
    para `float` quando não-None (`None if x is None else float(x)`), exceto
    `confluencia`, que é inteiro percentual (`None if x is None else int(x)`).
    Acrescentar `"entradaAMercado": None if entrada_a_mercado is None else
    bool(entrada_a_mercado)` — tri-estado deliberado: None = não sabemos (N2 ou
    registro sem plano), False = entrada no rompimento do gatilho, True = entrada
    a mercado. Acrescentar também `"metodologiaVersao": METODOLOGIA_ATUAL`.
    Comentário obrigatório citando ADR-015 / ADR15-01 com os dois porquês: (a) 0
    de 159 registros resolvidos em produção tinham `entrada`, e sem ele
    `_avaliar_entry` mede o ruído entre o close e o gatilho, não o motor; (b) sem
    `entradaAMercado`, o Plano 02 exigiria toque de gatilho num plano que já
    rompeu, descartando silenciosamente o caso adverso.

    5. Em `to_csv`, acrescentar `"entrada", "alvo2", "rr2", "confluencia",
    "entradaAMercado", "metodologiaVersao"` ao FIM da tupla `cols` (nunca no meio —
    quem consome o CSV posicionalmente não pode quebrar). O `esc()` já devolve
    string vazia para None: registro antigo sai com célula vazia, que é o contrato
    do módulo ("célula vazia = dado indisponível, nunca inferido").

    Em `server/tests/test_analysis_outcomes.py`: escrever os testes do bloco
    `<behavior>` ANTES da implementação (RED), na seção `# ===== registrar() =====`,
    seguindo o estilo dos testes vizinhos (nomes em pt-BR, `db.connect(str(tmp_path
    / "b3.db"))`). Atualizar `test_to_csv_colunas_fixas_e_escape` para a nova
    tupla de colunas — é um guardião existente, então a atualização vem com
    comentário dizendo que as colunas novas entraram no fim por ADR15-01, não que
    o contrato foi afrouxado.
  </action>
  <verify>
    <automated>(cd server && ./.venv/bin/python -m pytest tests/test_analysis_outcomes.py -q)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "METODOLOGIA_ATUAL = 2" server/app/analysis_outcomes.py` retorna 1 linha.
    - `grep -c "def _metodologia" server/app/analysis_outcomes.py` == 1.
    - `(cd server && ./.venv/bin/python -c "from app import analysis_outcomes as a; import inspect; p=inspect.signature(a.registrar).parameters; print(all(k in p for k in ('entrada','alvo2','rr2','confluencia','entrada_a_mercado')))")` imprime `True`.
    - `(cd server && ./.venv/bin/python -c "from app import analysis_outcomes as a; print(a.to_csv([]).strip().split(',')[-6:])")` imprime `['entrada', 'alvo2', 'rr2', 'confluencia', 'entradaAMercado', 'metodologiaVersao']`.
    - O comentário de `METODOLOGIA_ATUAL` distingue "campos gravados" de "âncora de resolução": `grep -A8 "METODOLOGIA_ATUAL = 2" server/app/analysis_outcomes.py | grep -ci "ancora\|âncora"` >= 1.
    - Nenhuma função fora de `registrar`/`to_csv` foi alterada neste task: `git diff --stat server/app/analysis_outcomes.py` mostra apenas adições nas regiões citadas.
    - `pytest tests/test_analysis_outcomes.py -q` passa 100% (nenhum teste existente removido).
  </acceptance_criteria>
  <done>`registrar()` persiste `entrada`/`alvo2`/`rr2`/`confluencia`/`entradaAMercado` e `metodologiaVersao: 2`; `to_csv` exporta as 6 colunas novas no fim; pytest do módulo verde.</done>
</task>

<task type="auto">
  <name>Task 2: N1 e N2 (main.py) alimentam os campos novos</name>
  <files>server/app/main.py</files>
  <read_first>
    - server/app/main.py linhas 1275-1340 (call site N1, dentro de `deep_call`)
    - server/app/main.py linhas 1360-1440 (call site N2, `analyze_technical_model`)
    - server/app/setups.py linhas 596-654 (`plano_operacional` — SÓ LEITURA: confirmar `entrada`/`alvo2`/`rr2` e os DOIS valores possíveis de `plano["tipo"]`)
    - server/app/analysis_outcomes.py (assinatura de `registrar` já estendida na Task 1)
  </read_first>
  <action>
    N1 (`main.py`, bloco `try:` do `analysis_outcomes.registrar` em ~1311-1327):
    acrescentar cinco kwargs à chamada, reusando o `plano` e o `sres` QUE JÁ
    ESTÃO EM ESCOPO (não recalcular `plano_do_resultado`, não refazer o snapshot):
    `entrada=plano.get("entrada")`, `alvo2=plano.get("alvo2")`,
    `rr2=plano.get("rr2")`, `confluencia=sres.get("confluencia")` e
    `entrada_a_mercado=str(plano.get("tipo") or "").startswith("a mercado")`.

    Comentário obrigatório sobre o último kwarg: `setups.py:602-612` põe
    `entrada = close` (não o gatilho) quando o gatilho já rompeu dentro da zona de
    perseguição, e nesse caso `plano["tipo"]` vale `"a mercado (gatilho já
    rompido, dentro da zona)"`. Sem esse marcador, o Plano 02 exigiria "toque no
    gatilho" num plano cuja entrada é IMEDIATA — e um gap contra a posição no
    candle seguinte (o caso ADVERSO) viraria `sem_gatilho` e sairia do
    denominador. É o mesmo viés otimista que o ADR-015 existe para matar. Usar
    `startswith("a mercado")` e não igualdade com a frase inteira: o prefixo é o
    contrato, o resto da frase é explicação para humano.

    NÃO alterar mais nada nesta chamada. Em particular, `alvo=plano.get("alvo1")`
    PERMANECE como está: trocar a barreira de resolução de `alvo1` para `alvo2`
    seria uma segunda mudança de comportamento no mesmo call site, explicitamente
    fora do escopo (06-CONTEXT.md, ADR15-02, último bullet). `alvo2`/`rr2` entram
    como dado para análise futura, não como alvo da barreira tripla.

    N2 (`main.py`, bloco `try:` do `analysis_outcomes.registrar` em ~1416-1432):
    acrescentar SOMENTE `confluencia=(snap.get("setups") or {}).get("confluencia")`.
    Deixar `entrada`, `alvo2`, `rr2` e `entrada_a_mercado` de fora (portanto None
    por default) e escrever o comentário que justifica isso: no N2 o `stop`/`alvo`
    gravados vêm da `proposal` da LLM, ancorada no preço de mercado do momento;
    enxertar o `entrada` do plano determinístico produziria uma geometria
    incoerente (gatilho de um plano com stop/alvo de outro) — exatamente a classe
    de erro que o ADR-015 está corrigindo. `confluencia` é descritiva do snapshot
    e não tem esse problema. Consequência que o Plano 02 honra explicitamente: um
    registro N2 é `metodologiaVersao: 2` mas resolve pela âncora de PREÇO, não
    pela de gatilho — por isso a âncora efetiva é carimbada na resolução (campo
    `ancora`), nunca inferida da versão. Não introduzir `setups.plano_do_resultado`
    no handler do N2.

    Não tocar em nenhuma outra linha de `main.py`.
  </action>
  <verify>
    <automated>(cd server && ./.venv/bin/python -m pytest -q -k "outcome or analyze or technical")</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'entrada=plano.get("entrada")' server/app/main.py` retorna exatamente 1 linha (só o N1).
    - `grep -n 'entrada_a_mercado=' server/app/main.py` retorna exatamente 1 linha (só o N1).
    - `grep -n 'confluencia=' server/app/main.py | grep -v '^\s*#' | wc -l` == 2 (um no N1, um no N2).
    - `grep -n 'alvo=plano.get("alvo1")' server/app/main.py` continua retornando 1 linha (barreira inalterada).
    - `grep -c "plano_do_resultado" server/app/main.py` não aumentou em relação ao HEAD anterior (`git show HEAD:server/app/main.py | grep -c plano_do_resultado`).
    - `git diff server/app/main.py` toca apenas os dois blocos `analysis_outcomes.registrar(...)` e seus comentários.
  </acceptance_criteria>
  <done>N1 alimenta os 5 campos a partir de `plano`/`sres`; N2 alimenta só `confluencia`, com o motivo documentado no código; `alvo=plano.get("alvo1")` e o restante de `main.py` inalterados.</done>
</task>

<task type="auto">
  <name>Task 3: verificação canônica das duas suítes + registro da divergência do ROADMAP</name>
  <files>—</files>
  <read_first>
    - CLAUDE.md (seção "Validação obrigatória")
    - scripts/executar.sh (linhas 19-30, modo `--testes`)
    - .planning/ROADMAP.md linhas 265-290 (critérios de sucesso da Phase 6)
  </read_first>
  <action>
    1. Rodar a suíte canônica do repo — as DUAS suítes, pytest do backend e os
    runners `web/tests/*.mjs`. `scripts/test.sh` sozinho é meia baseline e não
    conta como validação (CLAUDE.md). Se qualquer suíte falhar, corrigir dentro do
    escopo deste plano (campos novos, call sites, colunas do CSV); NÃO relaxar
    guardião existente para fazer passar. Registrar no SUMMARY quantos testes
    rodaram e o resultado.

    2. Registrar no SUMMARY, em seção própria intitulada
    `## Emenda pendente no ROADMAP — critério 1`, a divergência VERIFICADA entre o
    critério 1 da Phase 6 e o que este plano entrega, com o texto de substituição
    pronto. Quem edita o `.planning/ROADMAP.md` é o Plano 03 (Task 3) — duas plans
    da mesma wave não escrevem no mesmo arquivo. Conteúdo obrigatório da seção:

      - Critério 1 hoje diz que `registrar` grava `entrada`, `alvo2`, `rr2` e
        `confluencia` "(N1 e N2, `main.py`)".
      - O que é entregue: N1 grava os quatro + `entradaAMercado`; N2 grava só
        `confluencia`. Motivo verificado: o N2 não tem plano determinístico em
        escopo — seu `stop`/`alvo` vêm da proposta da LLM ancorada no preço, e
        enxertar o `entrada` do plano determinístico criaria geometria incoerente.
      - Texto de substituição literal a aplicar: "1. `analysis_outcomes.registrar`
        grava `entrada`, `alvo2`, `rr2`, `confluencia` e `entradaAMercado` no
        outcome do N1 (`main.py`), e `confluencia` no outcome do N2 — o N2 não tem
        plano determinístico em escopo e por isso não carrega geometria de
        gatilho. [ADR15-01]"
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/executar.sh --testes` termina com exit code 0.
    - Nenhum `[X]` na saída das suítes web.
    - O SUMMARY cita o total de testes do pytest e confirma que nenhum guardião foi removido.
    - O SUMMARY contém a seção `## Emenda pendente no ROADMAP — critério 1` com o texto de substituição literal.
  </acceptance_criteria>
  <done>`bash scripts/executar.sh --testes` com exit 0, nenhum guardião removido e a emenda do critério 1 registrada no SUMMARY para o Plano 03 aplicar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → `registrar()` | `recomendacao`/`confianca` vêm da resposta do modelo; `entrada`/`alvo2`/`rr2`/`confluencia`/`entradaAMercado` vêm do motor determinístico, nunca da LLM |
| kv (SQLite) → painel admin | outcomes crus persistidos por escopo, agregados depois |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | `registrar()` — campos novos alimentados pela LLM em vez do motor | mitigate | N1 lê `plano`/`sres` (saída de `setups.py`); N2 deixa `entrada/alvo2/rr2/entrada_a_mercado` em None em vez de enxertar o plano determinístico numa proposta da IA. Princípio 5 do CLAUDE.md preservado |
| T-06-02 | Information disclosure | `to_csv` com colunas novas | accept | Colunas são dados de mercado/plano do próprio escopo; a rota de export já é por-usuário (`main.py:2346`), sem `user_id` no payload |
| T-06-03 | Denial of service | blob JSON do kv cresce com 6 campos por registro | accept | `CAP=500` e `MAX_DIAS=180` já limitam o blob; 6 escalares por registro é ruído frente ao teto existente |
| T-06-16 | Repudiation | `metodologiaVersao` lido como "âncora usada" e não como "campos gravados", misturando N1/N2 no mesmo rótulo | mitigate | Comentário obrigatório na constante + campo `ancora` carimbado na resolução (Plano 02); critério de aceite verifica o comentário |
| T-06-SC | Tampering | instalação de pacote | accept | Nenhuma dependência nova neste plano (`server/requirements.txt` intocado) |
</threat_model>

<verification>
1. `bash scripts/executar.sh --testes` verde (backend + web).
2. `git diff --stat` limitado a `server/app/analysis_outcomes.py`, `server/app/main.py`, `server/tests/test_analysis_outcomes.py`.
3. Nenhuma migração/reescrita de registro antigo: `grep -n "metodologiaVersao" server/app/analysis_outcomes.py` só aparece em `registrar` (escrita), `_metodologia` (leitura com default) e `to_csv` (export).
</verification>

<success_criteria>
- `registrar()` aceita e persiste `entrada`, `alvo2`, `rr2`, `confluencia`, `entradaAMercado` e carimba `metodologiaVersao: 2`. [ADR15-01]
- N1 alimenta os cinco campos a partir de `plano`/`sres` já em escopo; N2 alimenta só `confluencia`, com o motivo documentado no código.
- `metodologiaVersao` documentado como marcador dos CAMPOS gravados, não da âncora de resolução.
- Registro antigo continua sem os campos e sem `metodologiaVersao` — nada é migrado nem inferido.
- Suíte canônica verde e emenda do critério 1 do ROADMAP registrada no SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/06-instrumentacao-assertividade-adr015/06-01-SUMMARY.md` when done
</output>
