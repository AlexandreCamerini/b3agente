---
phase: 02-realismo-de-mercado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/pending_orders.py
  - server/app/defaults.py
  - server/app/store.py
  - server/tests/test_ordens_pendentes.py
autonomous: true
requirements: [MERC-02, MERC-03, MERC-04]

must_haves:
  truths:
    - "Uma ordem de compra criada fora do pregão reserva o caixa na hora do pedido (D-02: o caixa disponível cai imediatamente)"
    - "Uma ordem de venda pendente reserva a quantidade da posição na hora do pedido (D-02/D-06: não dá para vender a mesma ação duas vezes em duas pendentes)"
    - "Cancelar uma ordem pendente devolve caixa (compra) ou posição (venda) imediatamente, sem esperar o scheduler (D-03: cancelável a qualquer momento antes da execução)"
    - "Executar uma ordem pendente usa o preço passado pelo motor (price_getter) na abertura do pregão seguinte (D-01), nunca um preço inventado, e reusa store.buy/store.sell para o débito/crédito real"
    - "Sem cotação na execução, a ordem PERMANECE pendente e registra o erro — nunca executa com preço fabricado"
    - "Patrimônio total não muda ao criar uma ordem pendente: o que sai de `cash` aparece em `caixaReservado`"
    - "Existe UMA única trava de ordem (`store.ORDER_LOCK`), usada tanto pela execução de pendentes quanto pelo caminho de ordem imediata — nenhum módulo cria trava própria"
    - "Ordem pendente cancelada sozinha na abertura grava um evento marcado com `tag: pendente-cancelada`, para que scheduler e app consigam avisar o usuário"
  artifacts:
    - path: "server/app/pending_orders.py"
      provides: "Motor determinístico de ordens pendentes (criar/cancelar/executar + reserva)"
      contains: "ORDER_LOCK"
      min_lines: 150
    - path: "server/tests/test_ordens_pendentes.py"
      provides: "Guardiões do ciclo de vida da ordem pendente"
      contains: "def test_"
      min_lines: 120
    - path: "server/app/store.py"
      provides: "ORDER_LOCK (trava ÚNICA do motor de ordens) + seção kv pendingOrders + caixaReservado DERIVADO no public_state + scopes_com_pendentes"
      contains: "ORDER_LOCK"
  key_links:
    - from: "server/app/pending_orders.py"
      to: "server/app/store.py"
      via: "reuso de store.buy/store.sell na execução (D-05: mesmo caminho de débito)"
      pattern: "store\\.(buy|sell)\\("
    - from: "server/app/store.py"
      to: "server/app/defaults.py"
      via: "SECTIONS lê d[key] de default_state()"
      pattern: "pendingOrders"
    - from: "server/app/pending_orders.py"
      to: "server/app/store.py"
      via: "ORDER_LOCK é ALIAS do mesmo objeto do store — a mesma trava que /api/buy e /api/sell usam no caminho imediato (plano 02-02)"
      pattern: "ORDER_LOCK = store\\.ORDER_LOCK"
---

<objective>
Construir o motor determinístico de ordens pendentes no backend: modelo de
dados, reserva de caixa (compra) e de posição (venda) no momento do pedido,
cancelamento com devolução imediata, e execução aplicando o preço vindo do
motor de cotações.

Purpose: MERC-02/03/04 dependem inteiramente desta camada. Ela é o primeiro
estado ASSÍNCRONO no motor de ordens do produto — por isso nasce isolada,
testável sem rede e sem HTTP, antes de qualquer rota ou UI existir.
Output: `server/app/pending_orders.py`, seção kv `pendingOrders`, campo
`caixaReservado` no `public_state`, suíte de guardiões.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-realismo-de-mercado/02-CONTEXT.md
@CLAUDE.md
@.planning/codebase/TESTING.md

<interfaces>
Contratos existentes que este plano CONSOME (não reimplementar — ler direto do código):

server/app/store.py
- `SECTIONS` (linha 9) — lista das seções kv por conta.
- `buy(conn, t, qty, price, user_id=None, meta=None, origem="manual") -> None` (linha 530) —
  normaliza qty para lote de 100, faz preço médio ponderado, `cash -= qty*price`,
  insere entrada `{"date","type":"COMPRA","t","qty","price","pnl":None,"origem"}` em `history[0]`.
- `sell(conn, t, price, user_id=None, qty=None, origem="manual") -> pnl|None` (linha 566) —
  venda total (qty=None) ou parcial em lotes de 100; parcial PRESERVA `avg`;
  `cash += sold*price`; entrada `type:"VENDA"` com `pnl`.
- `get(conn, key, user_id=None)`, `now_str()`, `push_agent_log(conn, events, user_id=None)` (linha 838).
- `scopes_com_history(conn) -> list` (linha 494) — padrão a copiar para `scopes_com_pendentes`.
- `public_state(conn, user_id=None) -> dict` (linha 760).

server/app/defaults.py
- `default_state()` — dict com uma chave por seção de `SECTIONS`; `"optionPositions": []`
  (linha 306) é o precedente aditivo mais recente.

server/app/db.py
- `kv_get(conn, key, default, user_id=None)`, `kv_set(conn, key, value, user_id=None)`.
- `_ThreadLocalConnection` (linha 51): UMA conexão SQLite por thread do pool do FastAPI —
  por isso a exclusão mútua entre requisições precisa ser um lock de PROCESSO, não do SQLite.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar pending_orders.py com modelo, lock e criação com reserva</name>
  <files>server/app/pending_orders.py, server/app/store.py, server/tests/test_ordens_pendentes.py</files>
  <read_first>
    - server/app/store.py (linhas 1-20 para SECTIONS, 494-600 para scopes_com_history/buy/sell/_sanitize_trade_meta)
    - server/app/db.py (linhas 36-85: connect, _ThreadLocalConnection, shared)
    - server/app/brapi.py (procurar `class ForaDoPlano` — padrão de exceção de domínio nomeada)
    - .planning/codebase/TESTING.md (seção "Backend Test Framework" — `_fresh_db()` com SQLite temp por teste)
    - .planning/phases/02-realismo-de-mercado/02-CONTEXT.md (D-05, D-06, D-07)
  </read_first>
  <behavior>
    - `criar_compra(conn, user_id, t, qty, preco_ref, meta=None)` com caixa suficiente:
      remove `qty*preco_ref` de `cash`, grava 1 registro em `pendingOrders` com
      `caixaReservado == qty*preco_ref`, e `cash + caixaReservado` é igual ao caixa de antes.
    - `criar_compra` com caixa insuficiente: levanta `CaixaInsuficiente`, NÃO grava
      nada em `pendingOrders` e NÃO altera `cash` (teste assere os dois).
    - `criar_venda(conn, user_id, t, qty)` parcial: `positions[t].qty` cai em `qty`,
      `positions[t].avg` fica INALTERADO, registro pendente guarda `avgReservado == avg`.
    - `criar_venda` total (qty >= posição): a posição some de `positions` e o
      `avgReservado` do registro preserva o preço médio.
    - `criar_venda` de quantidade maior que a disponível (já com outra pendente
      reservando parte): levanta `PosicaoInsuficiente` (D-06 — não vender duas vezes).
    - Duas ordens pendentes no MESMO ticker coexistem, cada uma com seu id (D-07).
    - `listar(conn, user_id)` devolve na ordem de criação (FIFO).
    - `caixa_reservado(conn, user_id)` devolve a soma de `caixaReservado` das compras pendentes.
  </behavior>
  <action>
    Criar `server/app/pending_orders.py` — módulo novo, stdlib + imports locais do app
    (`from . import db, store`), docstring no padrão da casa explicando POR QUE existe
    (Phase 2 / MERC-02..04: primeiro estado assíncrono do motor de ordens; a reserva
    reusa o caminho de débito imediato por decisão D-05, em vez de criar campo
    "caixaDisponível" novo).

    Constantes e exceções de domínio (padrão `brapi.ForaDoPlano`):
    `SECAO = "pendingOrders"`; `ORDER_LOCK = store.ORDER_LOCK` (ALIAS — ver o parágrafo
    seguinte); `class OrdemPendenteErro(Exception)`; `class CaixaInsuficiente(OrdemPendenteErro)`;
    `class PosicaoInsuficiente(OrdemPendenteErro)`; `class OrdemNaoPendente(OrdemPendenteErro)`.

    A TRAVA NASCE EM `server/app/store.py`, não aqui: acrescentar
    `ORDER_LOCK = threading.RLock()` no topo de `store.py` (junto de `SECTIONS`,
    linha 9) e, em `pending_orders.py`, apenas o alias `ORDER_LOCK = store.ORDER_LOCK`
    — assim todo o código deste módulo continua escrevendo `with ORDER_LOCK:` sem
    mudança. O dono de `cash`/`positions` é o store, e o caminho de ordem IMEDIATA
    (`/api/buy`/`/api/sell` em `main.py`, plano 02-02) precisa adquirir EXATAMENTE
    esta trava. Duas travas distintas não excluiriam nada: a execução de pendentes
    roda na primeira passada do scheduler depois da abertura — exatamente a janela em
    que um usuário ao vivo pode mandar uma ordem imediata na MESMA conta — e os dois
    caminhos fazem read-modify-write sobre o mesmo `cash`/`positions`.
    Comentar nos DOIS pontos (definição no store, alias aqui) que a trava existe
    porque `db._ThreadLocalConnection` dá uma conexão por thread do pool do FastAPI:
    `cash` é lido-modificado-escrito em duas etapas e, sem ela, duas escritas
    concorrentes reservam/gastam o mesmo caixa (lost update / double-spend, ameaça
    T-02-02). Anotar a limitação: vale para UM processo — o deploy do Railway é um
    serviço único (server/railway.json), então basta. NÃO criar `threading.RLock()`
    em nenhum outro módulo do backend (há critério de aceite com grep para isso).

    Registro de ordem pendente (dict, JSON-serializável, campos exatos):
    `id` (string `"po_" + secrets.token_hex(4)`), `criadaEm` (`store.now_str()`),
    `tipo` (`"COMPRA"` ou `"VENDA"`), `t`, `qty` (int, lote de 100),
    `precoReferencia` (float, cotação no momento do PEDIDO — informativa, nunca é o
    preço de execução), `caixaReservado` (float, só COMPRA; `None` em VENDA),
    `avgReservado` (float, só VENDA; `None` em COMPRA), `origem` (`"manual"`),
    `meta` (dict sanitizado por `store._sanitize_trade_meta`, só COMPRA),
    `ultimaTentativaEm` (None), `ultimoErro` (None).
    NÃO gravar campo `status` na lista: estar na lista É estar pendente. O `status:
    "pendente"` só é derivado na borda (rota/UI); documentar isso em comentário para
    não haver duas fontes de verdade.

    Funções (todas recebendo `conn` como 1º argumento, padrão do store.py):
    `_ler(conn, user_id)` / `_gravar(conn, user_id, lista)` sobre `db.kv_get/kv_set`
    com a chave `SECAO`; `listar(conn, user_id=None)` (cópia, ordem de criação);
    `caixa_reservado(conn, user_id=None) -> float` (soma dos `caixaReservado`,
    arredondada a 2 casas); `qty_reservada(conn, user_id, t) -> int` (soma das VENDAS
    pendentes do ticker).

    `criar_compra(conn, user_id, t, qty, preco_ref, meta=None) -> dict`: dentro de
    `with ORDER_LOCK:` — normaliza `qty` com a MESMA regra de `store.buy`
    (`max(100, round(qty/100)*100)`); calcula `custo = round(qty*preco_ref, 2)`; lê
    `cash` via `store.get`; se `cash < custo` levanta `CaixaInsuficiente` com a
    mensagem PT-BR `"Caixa insuficiente."` (mesma frase da rota atual, para o cliente
    não ver dois textos); senão grava `cash - custo` e anexa o registro ao FIM da lista.
    Devolve o registro.

    `criar_venda(conn, user_id, t, qty) -> dict`: dentro de `with ORDER_LOCK:` —
    normaliza `qty` com a regra de `store.sell`; localiza a posição; sem posição
    levanta `PosicaoInsuficiente` (`"Sem posicao em " + t`); `disponivel = pos["qty"]`
    (a posição JÁ está líquida das reservas anteriores, porque a reserva subtrai de
    `positions` — não subtrair de novo); se `qty > disponivel` levanta
    `PosicaoInsuficiente` com mensagem citando a quantidade disponível; senão guarda
    `avgReservado = pos["avg"]`, e reduz a posição: se `qty >= pos["qty"]` remove o
    item de `positions`, senão `pos["qty"] -= qty` mantendo `avg` intacto (mesma
    semântica de venda parcial de `store.sell`). Grava `positions` e anexa o registro.

    Nenhuma dessas funções escreve em `history` — ordem pendente ainda não é operação
    (o histórico só recebe a entrada quando executa, via `store.buy`/`store.sell`).

    Escrever os testes em `server/tests/test_ordens_pendentes.py` no padrão da casa:
    helper local `_fresh_db()` com `tempfile.mkdtemp` + `db.connect(path)` +
    `store.ensure_defaults(conn)`, sem conftest, sem mock de SQLite. Cobrir cada item
    do bloco `<behavior>` acima, com comentário de guardião explicando a decisão
    (D-05/D-06/D-07) que cada teste trava.
  </action>
  <verify>
    <automated>cd server && ./.venv/bin/python -m pytest -q tests/test_ordens_pendentes.py</automated>
  </verify>
  <acceptance_criteria>
    - `server/app/pending_orders.py` existe e define exatamente os símbolos: `SECAO`,
      `ORDER_LOCK`, `OrdemPendenteErro`, `CaixaInsuficiente`, `PosicaoInsuficiente`,
      `OrdemNaoPendente`, `listar`, `caixa_reservado`, `qty_reservada`, `criar_compra`,
      `criar_venda` (verificável com `grep -c "^def \|^class \|^ORDER_LOCK\|^SECAO" server/app/pending_orders.py`).
    - Trava única: `grep -n "ORDER_LOCK = threading.RLock()" server/app/store.py` retorna
      1 linha e `grep -n "ORDER_LOCK = store.ORDER_LOCK" server/app/pending_orders.py`
      retorna 1 linha.
    - `grep -rn "RLock()" server/app/` retorna EXATAMENTE 1 linha (a de `store.py`) —
      nenhum segundo lock de ordem no backend.
    - Teste assere `pending_orders.ORDER_LOCK is store.ORDER_LOCK` (identidade do
      objeto, não igualdade de tipo) — é isso que o plano 02-02 vai reusar.
    - `grep -n "with ORDER_LOCK" server/app/pending_orders.py` retorna pelo menos 2
      ocorrências (criar_compra e criar_venda).
    - `grep -c "history" server/app/pending_orders.py` — nenhuma escrita em history
      nas funções de criação (a única menção permitida é em comentário explicando por quê).
    - `cd server && ./.venv/bin/python -m pytest -q tests/test_ordens_pendentes.py` sai 0
      com ao menos 8 testes coletados.
    - Teste explícito assere `cash_depois + caixaReservado == cash_antes` (patrimônio
      conservado) e outro assere que `criar_compra` com caixa insuficiente deixa
      `pendingOrders` vazio E `cash` inalterado.
  </acceptance_criteria>
  <done>
    Criar compra/venda pendente reserva caixa/posição de forma determinística e
    testada, com exclusão mútua explícita, sem tocar em `history`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cancelamento com devolução imediata e execução ao preço do motor</name>
  <files>server/app/pending_orders.py, server/tests/test_ordens_pendentes.py</files>
  <read_first>
    - server/app/pending_orders.py (o que a Task 1 acabou de criar)
    - server/app/store.py (linhas 530-591: buy/sell — a aritmética exata que a execução reusa; linha 838: push_agent_log)
    - server/app/agent.py (linhas 840-871: formato dos eventos de agentLog, chaves `time`/`kind`/`text`)
    - .planning/phases/02-realismo-de-mercado/02-CONTEXT.md (D-01, D-03, D-04, e a nota de discretion "devolver imediatamente no cancelar")
    - CLAUDE.md (princípio 4: sem cotação não se inventa valor; princípio 5: preço vem do motor)
  </read_first>
  <behavior>
    - `cancelar(conn, user_id, order_id)` de uma COMPRA: `cash` volta ao valor de antes
      da criação, o registro some de `pendingOrders`, `history` NÃO ganha entrada.
    - `cancelar` de uma VENDA parcial: `positions[t].qty` volta ao original e `avg`
      continua idêntico (nenhuma reponderação espúria).
    - `cancelar` de uma VENDA TOTAL (posição tinha sumido): a posição reaparece com
      `qty` e `avg` originais.
    - `cancelar` de uma VENDA quando o usuário recomprou o ticker no meio do caminho:
      a posição resultante tem `qty` somado e `avg` reponderado pela MESMA fórmula de
      `store.buy` (teste com números fechados, ex. 100@10 + devolução 100@20 => 200@15).
    - `cancelar` com id inexistente (ou já executado): levanta `OrdemNaoPendente` e não
      muda nada.
    - `executar_pendentes(conn, user_id, price_getter)` de uma COMPRA: chama
      `store.buy` com o preço devolvido pelo `price_getter` (NÃO com `precoReferencia`),
      `history[0]` fica com `type=="COMPRA"` e `origem=="pendente"`, o registro some da
      lista e `caixa_reservado` volta a 0.
    - Execução de COMPRA com preço de abertura MAIOR que a referência: o caixa final é
      `cash_antes_da_criacao - qty*preco_execucao` (a diferença é acertada, não ignorada).
    - Execução de COMPRA cujo custo real excede caixa reservado + caixa livre: a ordem é
      CANCELADA automaticamente, o caixa reservado volta integralmente, e um evento
      `kind:"warn"` com `tag:"pendente-cancelada"` entra em `agentLog` explicando o motivo.
    - O evento de execução bem-sucedida sai com `kind:"buy"` e `tag:"pendente-executada"`;
      o de auto-cancelamento com `kind:"warn"` e `tag:"pendente-cancelada"` — teste assere
      as duas `tag`s, porque scheduler (02-03) e app (02-06) filtram por elas para avisar
      o usuário, e sem a marca o cancelamento automático viraria um sumiço silencioso.
    - Execução de VENDA: `store.sell` é chamado com o preço do `price_getter`, o `pnl`
      no histórico é calculado contra `avgReservado`, e a posição não fica duplicada.
    - `price_getter` que devolve `None`/dict sem `price`: a ordem CONTINUA em
      `pendingOrders`, com `ultimoErro` e `ultimaTentativaEm` preenchidos, `cash` e
      `positions` inalterados (princípio 4).
    - Ordem cancelada entre a leitura do preço e a aplicação: `executar_pendentes` não
      ressuscita a ordem (re-checagem do id dentro do lock).
  </behavior>
  <action>
    Ampliar `server/app/pending_orders.py`.

    Helper compartilhado `_restaurar_posicao(conn, user_id, t, qty, avg)`: espelha o
    ramo de posição de `store.buy` SEM tocar em `cash` nem `history` — se já existe
    posição no ticker, `avg_novo = round((avg_ex*qty_ex + avg*qty) / (qty_ex+qty), 2)`
    e soma a quantidade; senão cria `{"t","qty","avg","stop":None,"alvo":None,"abertaEm":store.now_str()}`.
    Comentar que é o inverso exato da reserva de venda.

    `cancelar(conn, user_id, order_id) -> dict`: dentro de `with ORDER_LOCK:` — relê a
    lista, localiza por `id`; ausente => `OrdemNaoPendente("Ordem pendente não
    encontrada — ela pode já ter sido executada ou cancelada.")`; COMPRA => `cash +=
    caixaReservado` (arredondado a 2 casas); VENDA => `_restaurar_posicao(...,
    avgReservado)`; remove o registro e grava. Devolve o registro removido. A devolução
    é IMEDIATA (não espera tick do scheduler) — decisão registrada em CONTEXT.md.

    `async def executar_pendentes(conn, user_id, price_getter, agora=None) -> list`:
    `price_getter` é um CALLABLE INJETÁVEL (async ou sync — resolver com
    `inspect.isawaitable` no retorno) que recebe o ticker e devolve o payload de
    cotação no contrato de `yahoo.get_quote` (`{"price": float|None, "source": str}`);
    esse desenho é o padrão da casa para código testável sem rede (ver
    `brapi.get_history(..., fetch_json=fake)`).

    Laço, na ORDEM DE CRIAÇÃO (FIFO, D-07): para cada registro, (a) capturar id e
    ticker, (b) obter a cotação FORA do lock — nunca segurar `ORDER_LOCK` durante um
    await de rede, isso bloquearia as threads HTTP; (c) `with ORDER_LOCK:` reler a
    lista e SEGUIR ADIANTE se o id sumiu (foi cancelado durante o await — ameaça
    T-02-04), senão aplicar.

    Sem preço utilizável (`None`, não numérico, `<= 0`): gravar `ultimaTentativaEm =
    store.now_str()` e `ultimoErro` com o texto do provedor, manter na lista, seguir
    para a próxima ordem. Nunca fabricar preço (CLAUDE.md princípio 4).

    COMPRA com preço: `custo = round(qty*preco, 2)`; `cash_livre = store.get(conn,
    "cash", user_id)`; se `cash_livre + caixaReservado < custo`, remover o registro,
    devolver `cash_livre + caixaReservado` para `cash`, empilhar evento
    `{"time": store.now_str(), "kind": "warn", "tag": "pendente-cancelada", "text": ...}`
    dizendo que a ordem pendente de {qty} {t} foi cancelada na abertura porque o preço
    subiu de {precoReferencia} para {preco} e o caixa reservado não cobriu, e que o caixa
    voltou integralmente. A chave `tag` é o que permite ao scheduler (02-03) mandar push
    e ao app (02-06) mostrar um toast SÓ para este `warn`, sem varrer todos os `warn` do
    agente: `store.push_agent_log` grava o dict como veio, então a chave sobrevive
    até o front. Sem ela, o auto-cancelamento — que é comportamento introduzido por
    este plano, não exigido literalmente por MERC-02..04 — sumiria da tela em silêncio,
    contrariando a promessa da fase ("preço, caixa e cancelamento ficam claros"). Caso contrário: devolver o `caixaReservado` para `cash`
    PRIMEIRO e então chamar `store.buy(conn, t, qty, preco, user_id=user_id,
    meta=meta, origem="pendente")` — assim o débito real acontece uma única vez, pelo
    mesmo caminho de sempre (D-05, princípio 5), e a entrada de histórico nasce com
    `origem: "pendente"`, distinguindo-a de `manual`/`automatico`.

    VENDA com preço: `_restaurar_posicao(conn, user_id, t, qty, avgReservado)` e em
    seguida `store.sell(conn, t, preco, user_id=user_id, qty=qty, origem="pendente")`
    — a restauração garante que o `pnl` saia contra o preço médio correto e que o
    caminho de crédito de caixa seja o já existente.

    Em ambos os sucessos: remover o registro da lista, empilhar evento
    `{"kind": "buy", "tag": "pendente-executada", "text": ...}` citando ticker, quantidade, preço executado, a
    fonte da cotação (`source` do payload) e o horário — a transparência de fonte e
    horário é o princípio 3 do CLAUDE.md.

    Ao fim, persistir os eventos com `store.push_agent_log(conn, eventos,
    user_id=user_id)` e DEVOLVER a lista de eventos (quem chama — o scheduler — usa
    para push). Nenhuma exceção de uma ordem derruba as outras: envolver o corpo de
    cada ordem em try/except, registrando `ultimoErro`.

    Acrescentar os testes de `<behavior>` ao mesmo arquivo de teste, dirigindo o async
    com `asyncio.run(...)` dentro de `def test_...` (padrão da casa, sem
    pytest-asyncio) e usando um `price_getter` fake local que conta chamadas.
  </action>
  <verify>
    <automated>cd server && ./.venv/bin/python -m pytest -q tests/test_ordens_pendentes.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "origem=\"pendente\"" server/app/pending_orders.py` aparece nas duas
      chamadas (`store.buy` e `store.sell`).
    - `grep -n "precoReferencia" server/app/pending_orders.py` NÃO aparece como
      argumento de `store.buy`/`store.sell` (o preço de execução nunca é o de referência).
    - A obtenção da cotação está FORA do bloco `with ORDER_LOCK` (verificável lendo
      `executar_pendentes`: o `await`/chamada do `price_getter` precede o `with ORDER_LOCK`).
    - `cd server && ./.venv/bin/python -m pytest -q tests/test_ordens_pendentes.py` sai 0
      com ao menos 18 testes coletados.
    - Existe teste com números fechados para a reponderação de preço médio no
      cancelamento de venda após recompra (ex. 100@10 + 100@20 => 200@15,00).
    - Existe teste que, com `price_getter` devolvendo `{"price": None}`, assere as três
      coisas: ordem continua na lista, `ultimoErro` não é None, `cash` idêntico.
    - `grep -c "pendente-cancelada\|pendente-executada" server/app/pending_orders.py` >= 2
      e existe teste que assere `evento["tag"] == "pendente-cancelada"` no caminho de
      auto-cancelamento por preço na abertura.
  </acceptance_criteria>
  <done>
    Cancelar devolve caixa/posição na hora; executar aplica o preço do motor pelo
    caminho determinístico existente, com falha de cotação preservando a ordem.
  </done>
</task>

<task type="auto">
  <name>Task 3: Ligar pendingOrders ao estado (defaults, SECTIONS, public_state, scopes)</name>
  <files>server/app/defaults.py, server/app/store.py, server/tests/test_ordens_pendentes.py</files>
  <read_first>
    - server/app/defaults.py (procurar `def default_state` e a linha `"optionPositions": []` — precedente aditivo)
    - server/app/store.py (linha 9 SECTIONS; linhas 24-48 ensure_defaults; linhas 494-508 scopes_com_history; linhas 760-789 public_state e USER_SECTIONS)
    - server/app/pending_orders.py (SECAO, listar, caixa_reservado)
    - server/tests/test_persistence.py (padrão de teste de round-trip de seção)
  </read_first>
  <action>
    Em `server/app/defaults.py`, acrescentar `"pendingOrders": []` ao dict de
    `default_state()`, imediatamente após `"optionPositions"`, com comentário
    `# Phase 2 (MERC-02..04): ordens fora do horário de pregão, aguardando abertura`.
    Isto é OBRIGATÓRIO antes de mexer em `SECTIONS`: `store.ensure_defaults` faz
    `d[key]` para cada item de `SECTIONS` e estouraria `KeyError` sem a chave.

    Em `server/app/store.py`:
    (a) acrescentar `"pendingOrders"` ao fim de `SECTIONS` (linha 9);
    (b) em `public_state`, acrescentar duas chaves — `"pendingOrders":
    get(conn, "pendingOrders", user_id=user_id)` e `"caixaReservado":
    <soma das compras pendentes>`. Para evitar import circular
    (`pending_orders` importa `store`), calcular a soma com um helper LOCAL em
    store.py, `caixa_reservado(conn, user_id=None)`, que soma
    `o["caixaReservado"]` dos itens com `tipo == "COMPRA"`; `pending_orders.py`
    passa a delegar seu próprio `caixa_reservado` para o do store (ou o inverso —
    escolher UMA fonte e comentar qual, sem duplicar a aritmética).
    ESCREVER NO DOCSTRING/COMENTÁRIO de `caixa_reservado` que ele é um valor
    DERIVADO, calculado na leitura a partir da lista de pendentes — não é um segundo
    saldo persistido nem um segundo livro-caixa. É por isso que ele NÃO viola D-05:
    o que D-05 proibiu foi criar um campo de saldo paralelo a `cash` que precisasse
    ser sincronizado à mão (risco de dois ledgers divergirem); aqui `cash` continua
    sendo a única fonte de verdade do dinheiro livre, o débito segue o caminho de
    sempre (`store.buy`) e `caixaReservado` é só a soma dos reservados, exposta
    porque MERC-03 exige mostrar esse valor na tela. Coincidência de nome com o termo
    citado em D-05 é isso: coincidência de nome;
    (c) criar `scopes_com_pendentes(conn) -> list` copiando literalmente a estrutura de
    `scopes_com_history` (linhas 494-508), trocando a seção para `pendingOrders` — é
    isto que o scheduler vai varrer, e NÃO `agent.list_server_users`, porque uma ordem
    pendente é do usuário mesmo que ele nunca tenha ligado o Operador;
    (d) confirmar que `USER_SECTIONS` (linha 789) já herda a nova seção por ser
    `SECTIONS + [...]` — se não herdar, corrigir, senão a exclusão/exportação de conta
    deixa lixo para trás.

    Acrescentar ao arquivo de teste: round-trip de persistência da seção nova
    (grava, fecha a conexão, reabre o mesmo arquivo, lê — padrão
    `test_config_persiste_apos_reinicio`); `public_state` de conta nova traz
    `pendingOrders == []` e `caixaReservado == 0`; `public_state` com uma compra
    pendente traz `caixaReservado` igual ao reservado e `cash + caixaReservado` igual
    ao orçamento inicial; `scopes_com_pendentes` enxerga o escopo de um usuário com
    ordem pendente e ignora quem não tem; `USER_SECTIONS` contém `"pendingOrders"`.
  </action>
  <verify>
    <automated>cd server && ./.venv/bin/python -m pytest -q tests/test_ordens_pendentes.py tests/test_persistence.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pendingOrders" server/app/defaults.py server/app/store.py` mostra a
      chave em `default_state`, em `SECTIONS`, em `public_state` e em `scopes_com_pendentes`.
    - `grep -c "caixaReservado" server/app/store.py` >= 2.
    - `cd server && ./.venv/bin/python -c "from app import defaults, store; d=defaults.default_state(); assert all(k in d for k in store.SECTIONS), [k for k in store.SECTIONS if k not in d]"` sai 0.
    - `cd server && ./.venv/bin/python -m pytest -q tests/` sai 0 (suíte backend inteira,
      para provar que a seção nova não quebrou nenhum teste de estado já existente).
    - Teste assere `"pendingOrders" in store.USER_SECTIONS`.
  </acceptance_criteria>
  <done>
    A seção `pendingOrders` nasce em conta nova, viaja no `public_state` junto com
    `caixaReservado`, e o scheduler tem como descobrir quais escopos têm ordem pendente.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| requisição HTTP → motor de ordens | duas requisições concorrentes (mesma conta, threads diferentes do pool anyio) atacam o mesmo `cash`/`positions` |
| scheduler (loop asyncio) → motor de ordens | executa em paralelo com requisições HTTP do mesmo usuário |
| provedor de cotação → motor de ordens | payload externo (brapi/Yahoo) alimenta o preço de execução |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `criar_compra`/`criar_venda` | mitigate | `qty` normalizado para lote de 100 e `preco_ref` só entra pelo chamador (rota), nunca vindo do corpo do cliente — a rota (plano 02) lê a cotação do motor |
| T-02-02 | Elevation of Privilege | reserva de caixa/posição (double-spend) | mitigate | `store.ORDER_LOCK` (threading.RLock de processo, trava ÚNICA) envolvendo o read-modify-write de `cash`/`positions` — usada tanto aqui quanto pelo caminho de ordem imediata em `/api/buy`/`/api/sell` (plano 02-02, T-02-35); `db._ThreadLocalConnection` dá conexão por thread, então o SQLite sozinho não serializa isso |
| T-02-03 | Tampering | execução com preço fabricado | mitigate | `executar_pendentes` só aplica preço numérico `> 0` vindo do `price_getter`; sem preço a ordem permanece pendente com `ultimoErro` (CLAUDE.md princípio 4) |
| T-02-04 | Repudiation | cancelar durante a execução (ordem ressuscitada / executada duas vezes) | mitigate | cotação obtida FORA do lock; dentro do lock a lista é relida e a ordem só é aplicada se o `id` ainda existir — remoção e aplicação são atômicas entre si |
| T-02-05 | Information Disclosure | vazamento entre escopos | mitigate | toda leitura/escrita passa por `user_id` no kv (`db.kv_get/kv_set`); nenhuma função aceita busca de ordem sem `user_id` |
| T-02-36 | Repudiation | auto-cancelamento na abertura sumindo em silêncio | mitigate | evento marcado com `tag: "pendente-cancelada"` (além de `kind: "warn"`), consumido por push no scheduler (02-03) e por toast na abertura do app (02-06) — o usuário nunca perde uma ordem sem saber o motivo |
| T-02-06 | Denial of Service | uma ordem com erro derruba as demais | mitigate | try/except por ordem dentro do laço, registrando `ultimoErro`, no padrão `# noqa: BLE001` já usado no agent.py |
| T-02-SC | Tampering | instalação de pacote npm/pip/cargo | accept | nenhuma dependência nova é introduzida por este plano (stdlib `threading`/`secrets`/`inspect` apenas) — sem superfície de supply chain |
</threat_model>

<verification>
- `cd server && ./.venv/bin/python -m pytest -q tests/` sai 0.
- `bash scripts/executar.sh --testes` sai 0 (suíte canônica COMPLETA — pytest + web/tests/*.mjs;
  `scripts/test.sh` sozinho é meia baseline e não conta, per CLAUDE.md).
- Nenhum arquivo de front foi tocado neste plano, então `npx vite build` não é exigido aqui.
</verification>

<success_criteria>
- Existe um motor de ordens pendentes determinístico, isolado de HTTP e de rede.
- Criar reserva; cancelar devolve na hora; executar aplica o preço do motor pelo
  caminho `store.buy`/`store.sell` já existente.
- `cash + caixaReservado` é invariante em criação e cancelamento.
- Falha de cotação nunca vira preço inventado.
- Suíte backend inteira verde.
</success_criteria>

<output>
Create `.planning/phases/02-realismo-de-mercado/02-01-SUMMARY.md` when done
</output>
