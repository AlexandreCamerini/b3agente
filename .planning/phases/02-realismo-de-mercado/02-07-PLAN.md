---
phase: 02-realismo-de-mercado
plan: 07
type: execute
wave: 6
depends_on: [02-03, 02-06]
files_modified:
  - qa/
autonomous: false
requirements: [MERC-01, MERC-02, MERC-03, MERC-04]

must_haves:
  truths:
    - "O ciclo completo (ordem fora do horário → pendente → execução na abertura → histórico) foi exercitado de ponta a ponta, não só em teste unitário"
    - "O caixa fecha: o que sai do disponível na criação volta no cancelamento, e vira posição na execução"
    - "A suíte canônica COMPLETA (pytest + web/tests) passa e o build do front compila"
    - "Um humano confirmou visualmente o badge pré-login e a seção Pendentes"
  artifacts:
    - path: "qa/"
      provides: "Registro do exercício ponta-a-ponta desta fase (relatório com data, comandos e resultados)"
  key_links:
    - from: "exercício manual"
      to: "rotas reais do servidor local"
      via: "curl/servidor rodando — não só TestClient"
      pattern: "api/market/status"
---

<objective>
Fechar a Phase 2 com verificação REAL: exercitar o ciclo completo contra um
servidor rodando (não só `TestClient`), rodar a suíte canônica completa e o
build do front, e submeter as duas superfícies visuais ao olho do Alex.

Purpose: o histórico deste repositório é explícito — defeitos de UI e de sync
"só a verificação ao vivo pegou". Esta fase muda dinheiro simulado e cria o
primeiro estado assíncrono do motor de ordens; teste verde não é evidência
suficiente.
Output: relatório em `qa/` e aprovação humana registrada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-realismo-de-mercado/02-CONTEXT.md
@.planning/phases/02-realismo-de-mercado/02-UI-SPEC.md
@.planning/phases/02-realismo-de-mercado/02-03-SUMMARY.md
@.planning/phases/02-realismo-de-mercado/02-06-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exercício ponta-a-ponta contra servidor real + suíte canônica</name>
  <files>qa/</files>
  <read_first>
    - CLAUDE.md (seção "Validação obrigatória" — as DUAS suítes; `scripts/test.sh` sozinho não conta)
    - scripts/executar.sh (como a suíte canônica é invocada e o que ela cobre)
    - .planning/phases/02-realismo-de-mercado/02-02-SUMMARY.md e 02-03-SUMMARY.md (nomes exatos das rotas e dos contadores entregues)
    - qa/ (ler 1 relatório recente para copiar o formato e o tom da casa)
  </read_first>
  <action>
    Rodar, nesta ordem, e registrar a saída de cada passo:
    (1) `bash scripts/executar.sh --testes` — suíte canônica COMPLETA (pytest do backend
    + todos os `web/tests/*.mjs`). Nenhum outro comando substitui este.
    (2) `cd web && npx vite build` — o front foi editado em 3 planos; erro de sintaxe JS
    não aparece em grep nem em teste estático.

    Depois, subir o servidor local (`uvicorn app.main:app --port 8787` a partir de
    `server/`, com `B3_DB_PATH` apontando para um banco descartável) e exercitar as
    ROTAS REAIS, não o `TestClient`:
    • `GET /api/market/status` sem header `Authorization` → conferir as 6 chaves e que
      `aberto` bate com o horário real de Brasília naquele momento;
    • criar uma conta de teste, e com `B3_AFTER_MARKET` desligado e o relógio fora do
      pregão (ou monkeypatch de ambiente equivalente documentado no relatório), enviar
      `POST /api/buy` → conferir `pendente: true`, `cash` reduzido, `caixaReservado`
      igual ao reservado, `history` do mesmo tamanho;
    • `DELETE /api/orders/pending/{id}` → conferir `cash` de volta ao valor original e
      `caixaReservado == 0`;
    • criar outra ordem pendente e forçar UMA passada do laço com
      `POST /api/agent/run-now` (ou `scheduler_loop(..., once=True)` com o gate de
      horário satisfeito, documentando como) → conferir que a ordem sumiu de
      `pendingOrders`, virou posição, gerou entrada de `history` com
      `origem: "pendente"`, e que `GET /api/agent/status` mostra `ordensPendentes`
      com o contador do ciclo;
    • repetir o fluxo de VENDA pendente, conferindo que a quantidade reservada sai da
      posição na criação e volta no cancelamento com o mesmo preço médio.

    Fechar com a conta do caixa: registrar no relatório os valores de `cash +
    caixaReservado` antes e depois de cada passo, provando que a soma é conservada
    (exceto na execução, onde vira posição pelo preço do motor).

    Escrever o relatório em `qa/` seguindo o formato dos relatórios existentes (data
    real, o que foi exercitado, comandos exatos, saída resumida, o que passou, o que
    não passou, limitações conhecidas). O histórico não se reescreve: arquivo novo, sem
    tocar nos anteriores.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/executar.sh --testes` sai 0 e a saída registrada mostra as DUAS
      suítes tendo rodado (pytest + os arquivos `.mjs`).
    - `cd web && npx vite build` sai 0.
    - O relatório em `qa/` existe, tem a data real, e contém as respostas cruas (ou
      recortes) de: `/api/market/status`, o `POST /api/buy` pendente, o `DELETE` de
      cancelamento, a execução pela passada do laço e o `/api/agent/status` com
      `ordensPendentes`.
    - O relatório mostra explicitamente a conta `cash + caixaReservado` conservada na
      criação e no cancelamento.
    - Nenhum arquivo anterior de `qa/` foi modificado (`git status` mostra só arquivo novo ali).
  </acceptance_criteria>
  <done>
    O ciclo completo foi exercitado contra o servidor real, com a conta do caixa
    fechando, e as duas suítes + o build estão verdes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificação visual do Alex (badge pré-login e seção Pendentes)</name>
  <files>web/src/App.jsx (verificação — nenhuma edição nesta task)</files>
  <read_first>
    - .planning/phases/02-realismo-de-mercado/02-UI-SPEC.md (contrato visual aprovado — é contra ele que se confere)
    - .planning/phases/02-realismo-de-mercado/02-05-SUMMARY.md e 02-06-SUMMARY.md (o que foi entregue em cada superfície)
    - TESTFLIGHT.md (como levar um build ao iPhone, se a verificação nativa exigir)
  </read_first>
  <action>
    Pausar a execução e apresentar ao Alex o roteiro de verificação abaixo, na ordem.
    Não editar código nesta task. Se ele reportar defeito, registrar exatamente o que
    foi dito (tela, passo, o que viu) no SUMMARY e abrir a correção como trabalho
    subsequente — não corrigir dentro do checkpoint.
  </action>
  <what-built>
    - `MarketStatusBadge` renderizado na tela de LOGIN (antes de autenticar) e no Topbar,
      com 3 estados: aberto (verde), fechado (vermelho), indisponível (âmbar).
    - `BuyModal`/`SellModal` com pill "PENDENTE" e disclaimer de pendência quando o
      mercado está fechado.
    - Seção "Pendentes" no topo do `HistoricoScreen`, com total reservado e cancelamento
      em dois passos.
    - Backend: ordem pendente com reserva de caixa/posição, execução automática na
      primeira passada do laço dentro do pregão, cancelamento com devolução imediata.
  </what-built>
  <how-to-verify>
    1. Rodar o app local (`bash scripts/executar.sh` ou o comando de dev do repositório)
       e abrir a tela de entrada SEM estar logado. Confirmar: o badge de status do
       mercado aparece abaixo da tagline/disclaimer, ANTES do formulário, e o que ele
       diz bate com a realidade (se agora for fora do pregão, deve dizer "Mercado
       fechado"; em dia útil entre 10:00 e 16:55, "Mercado aberto").
    2. Entrar. Confirmar que o mesmo badge aparece no Topbar, na linha de modo, e que
       ele NÃO muda de cor ao alternar Modo Estudo ↔ Modo Operador (a cor é semântica,
       não do modo).
    3. Com o mercado FECHADO, abrir uma compra de qualquer ativo. Confirmar: pill
       "PENDENTE" ao lado de "COMPRA SIMULADA", e o texto explicando que a ordem executa
       ao preço de abertura do próximo pregão e que o caixa é reservado já na
       confirmação. Confirmar a ordem.
    4. Conferir na Topbar: o caixa disponível caiu, mas o PATRIMÔNIO não caiu.
    5. Ir ao Histórico. Confirmar: seção "Pendentes" no topo, com a ordem, o pill âmbar
       "PENDENTE", o total reservado no subtítulo, e a tabela de operações normal abaixo.
    6. Tocar o `✕` da linha. Confirmar: aparece a confirmação em dois passos com o valor
       que volta ao caixa. Tocar "Manter ordem" — nada acontece. Tocar de novo o `✕` e
       então "Confirmar cancelamento" — a ordem some, o caixa volta ao valor original e
       aparece o toast.
    7. No iPhone (TestFlight ou build local): repetir os passos 1, 3, 5 e 6. O app
       nativo usa o `deviceStore`, um caminho de código diferente do web — defeitos de
       sync já apareceram só ali antes.
    8. Reportar qualquer texto que soe como promessa de resultado, qualquer número que
       não bata entre telas, ou qualquer estado que afirme algo sobre o mercado sem dado.
  </how-to-verify>
  <resume-signal>Responda "aprovado" ou descreva o que está errado (tela, passo e o que viu)</resume-signal>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
    <human-check>Alex responde "aprovado" depois de percorrer os 8 passos, incluindo o iPhone</human-check>
  </verify>
  <acceptance_criteria>
    - Os 8 passos do roteiro foram percorridos e o resultado de cada um está registrado.
    - A verificação nativa (passo 7) foi feita ou, se impossível no momento, está
      declarada como limitação conhecida no SUMMARY — nunca omitida em silêncio.
    - Resposta do Alex registrada literalmente ("aprovado" ou a lista de defeitos).
  </acceptance_criteria>
  <done>
    Alex aprovou as duas superfícies visuais, ou os defeitos que ele apontou estão
    registrados com tela/passo/sintoma para virarem trabalho subsequente.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ambiente de teste → banco de produção | um exercício mal configurado pode escrever no banco real |
| build local → TestFlight | o caminho nativo (`deviceStore`) é código diferente do web |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-32 | Tampering | exercício escrevendo no banco de desenvolvimento/produção | mitigate | `B3_DB_PATH` apontado para banco descartável antes de subir o servidor; registrado no relatório |
| T-02-33 | Repudiation | declarar validado sem ter rodado as DUAS suítes | mitigate | critério de aceite exige a saída registrada mostrando pytest E os `.mjs`; `scripts/test.sh` sozinho é explicitamente insuficiente (CLAUDE.md) |
| T-02-34 | Spoofing | defeito só no caminho nativo passando batido | mitigate | passo 7 do checkpoint cobre o iPhone; o `deviceStore` é código distinto do `serverStore` |
| T-02-SC | Tampering | instalação de pacote npm/pip/cargo | accept | nenhuma dependência nova em toda a fase |
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` sai 0.
- `cd web && npx vite build` sai 0.
- Relatório em `qa/` criado, sem alterar nenhum relatório anterior.
- Aprovação humana registrada.
</verification>

<success_criteria>
- Os 5 Success Criteria da Phase 2 verificados ao vivo, não só em teste.
- Conta do caixa fechando em criação, cancelamento e execução.
- Aprovação do Alex nas duas superfícies visuais (web e iPhone).
</success_criteria>

<output>
Create `.planning/phases/02-realismo-de-mercado/02-07-SUMMARY.md` when done
</output>
