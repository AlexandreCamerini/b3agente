---
phase: 09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - server/app/mydata_client.py
  - server/app/options_provider_mydata.py
  - server/app/options_provider.py
  - server/app/options_api.py
  - server/app/main.py
  - server/tests/test_mydata_client.py
  - server/tests/test_options_provider_mydata.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "A cadeia de opções de um ativo vem do mydata, com volatilidade implícita e gregas já calculadas na origem"
    - "D-02: O contrato providerStatus/calls/puts/expirations que o front e o ADR-004 consomem continua idêntico — nenhuma tela muda de forma"
    - "Falha do mydata em opções vai direto para providerStatus=degraded, SEM tentar o Yahoo (D-04)"
    - "Vencimento vem do endpoint dedicado de vencimentos, não é derivado varrendo a cadeia"
    - "Qual provedor de opções está ativo é configuração (env), com o default de produção AINDA no Yahoo até a medição do Plano 09-04"
    - "Volatilidade implícita nula em parte dos contratos é dado legítimo, não erro — e vem rotulada com o motivo"
  artifacts:
    - path: "server/app/mydata_client.py"
      provides: "get_vencimentos + get_options_chain sobre /v1/opcoes"
      exports: ["get_vencimentos", "get_options_chain"]
    - path: "server/app/options_provider_mydata.py"
      provides: "Adaptador do contrato ADR-004 alimentado por gold_opcoes"
      exports: ["get_options"]
    - path: "server/app/options_provider.py"
      provides: "Seletor de provedor de opções por env (alavanca de rollback, não fallback automático)"
      exports: ["provider_name", "get_options"]
  key_links:
    - from: "server/app/options_api.py"
      to: "server/app/options_provider.py"
      via: "import do get_options roteado"
      pattern: "from \\.options_provider import get_options"
    - from: "server/app/options_provider_mydata.py"
      to: "server/app/mydata_client.py"
      via: "get_vencimentos + get_options_chain"
      pattern: "mydata_client\\.(get_vencimentos|get_options_chain)"
    - from: "server/app/options_provider_mydata.py"
      to: "options_api.liquidity_gate"
      via: "providerStatus continua sendo o portão do ADR-004"
      pattern: "providerStatus"
---

<objective>
Migrar a fatia de OPÇÕES/IV para o mydata: cadeia, volatilidade implícita e
gregas passam a vir pré-calculadas do hub, atrás do MESMO contrato
`providerStatus` que o ADR-004 já definiu (D-02) e com degradação direta, sem
fallback para o Yahoo (D-04).

Purpose: o endpoint de opções do Yahoo é não-oficial e responde 401/403/429 —
é a fonte instável que esta migração existe para eliminar. Reintroduzi-la como
fallback anularia o ganho, então D-04 é explícita: falhou, degrada. O mydata
já resolve IV e gregas por Black-Scholes na origem (`nucleos.py:182-223`,
36 testes verdes do lado de lá), o que remove do Boris a necessidade de
adivinhar sigma a partir de HV.

Output: funções de opções no `mydata_client`, o adaptador
`options_provider_mydata.py`, o seletor `options_provider.py` e a troca dos 8
call sites. **O default de produção continua `yahoo`** — a virada é o checkpoint
do Plano 09-06, gated pela medição do Plano 09-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-CONTEXT.md
@.planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-PATTERNS.md
@.planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-01-SUMMARY.md
@docs/adr/004-fonte-de-opcoes-na-v2.md

<assumptions>
**Questão aberta 3 do 09-PATTERNS.md — RESOLVIDA: confiar no mydata.** O
adaptador consome `volatilidade_implicita`, `delta`, `gamma`, `vega`, `theta`,
`rho` e `preco_teorico` como vêm. NENHUM caminho redundante de cálculo local é
criado — centralizar é justamente parar de recalcular. O
`options_api._enrich_contract` existente NÃO é alterado: ele continua rodando
`black_scholes()` para `probITM`/`breakeven`, mas agora recebe em `iv` a
implícita resolvida pelo mydata em vez de cair no `hv21` como proxy. Isso é
melhora de qualidade sem mudança de contrato.

**`openInterest` não tem fonte.** O COTAHIST da B3 não publica open interest e
`gold_opcoes` não tem coluna equivalente. `options_quant.liquidity_score`
(linha 103-118) faz `float(open_interest or 0)`, então `None` é seguro: não
quebra. Mas o teto do score cai de 100 para 60 (`vol_score` 35 + base 25 −
penalidade de spread), e `options_api.liquidity_gate` (linha 153) exige
`score >= 40`. **É uma mudança de comportamento real e precisa ser medida, não
presumida** — há critério de aceite específico para isso na Task 2. Nada em
`options_quant.py` é alterado nesta fase.

**Endpoint separado para vencimentos.** Diferente do Yahoo (que devolve
`expirationDates` dentro da mesma resposta), o mydata tem
`GET /v1/opcoes/{ticker}/vencimentos`. Duas chamadas por cadeia, não uma —
número que entra na conta de cota do Plano 09-04.

**Escopo negativo:**
- `options_provider_yahoo.py` NÃO é apagado nem alterado. Vira código
  histórico e alavanca de rollback por env (D-04: "fica como código histórico,
  não caminho ativo"). Seu teste também fica.
- `options_quant.py` e `options_api._enrich_contract` NÃO são alterados.
- ADR-004 NÃO é reaberta.
- Nenhum default de produção é virado neste plano.
</assumptions>

<interfaces>
<!-- Contratos reais. Não explorar o repositório. -->

Endpoints (fonte: `~/dev/cvm-financas/app/api/main.py:648-770`):

    GET /v1/opcoes/{ticker}
      query: vencimento (AAAA-MM-DD), pregao (AAAA-MM-DD), tipo (CALL|PUT),
             limite (1..2000, default 500), cursor
      200: {"dados": [...gold_opcoes...], "proximo_cursor": <str|null>}
      Sem `pregao`, responde o ÚLTIMO pregão publicado DAQUELE papel.
      Papel sem nenhum pregão: {"dados": [], "proximo_cursor": null}
      ORDER BY dt_vencimento, strike

    GET /v1/opcoes/{ticker}/vencimentos
      query: pregao
      200: {"dados": [{dt_vencimento, contratos, com_sigma, vence_no_pregao,
                       menor_strike, maior_strike}]}
      `vence_no_pregao` = 1 quando o vencimento é no próprio pregão (prazo zero:
      a cadeia inteira sai sem sigma e sem grega — vencimento morto).
      `com_sigma` = quantos contratos daquele vencimento têm IV resolvida.

Colunas de `gold_opcoes` (fonte: `~/dev/cvm-financas/app/seed_fontes.py:924-971`):
    dt_pregao, contrato, ticker_objeto, tipo ('CALL'|'PUT'), strike,
    dt_vencimento, prazo_anos, premio, melhor_oferta_compra,
    melhor_oferta_venda, total_negocios, quantidade_negociada,
    volume_financeiro, preco_objeto, taxa_livre_risco,
    volatilidade_implicita, hv21_objeto, preco_teorico, situacao_sigma,
    delta, gamma, vega, theta, rho, isin, estilo_exercicio, strike_cadastro,
    captura_id, fonte_arquivo, publicado_em_versao
    + `proveniencia` ({sha256, dt_captura, arquivo, arquivo_em}) por linha.
    NÃO existe: openInterest, change, percentChange, currency.
    `situacao_sigma` ∈ {'invertido', 'vence_no_pregao', 'sem_preco_objeto',
                        'sem_premio', 'premio_fora_do_modelo'} — 'invertido'
    significa que a IV foi resolvida; os outros quatro explicam POR QUE não foi.

Contrato de saída a preservar BYTE A BYTE em forma
(fonte: `server/app/options_provider_yahoo.py:45-59` e `129-140`):

    payload degradado: {ticker, symbol, expirations: [], expiration, calls: [],
                        puts: [], source, providerStatus: "degraded", warning}
                       + providerError quando houver
    payload ok:        {ticker, symbol, source, providerStatus: "ok",
                        underlyingPrice, currency, expirations: [...],
                        expiration, calls: [...], puts: [...]}
    contrato limpo:    {contractSymbol, optionType ("call"|"put"), strike,
                        lastPrice, bid, ask, change, percentChange, volume,
                        openInterest, impliedVolatility, inTheMoney, currency,
                        distancePct}
    distancePct = round((strike - spot) / spot * 100, 2) quando spot > 0

Consumidores do contrato (NÃO alterar a forma):
    server/app/options_api.py:100  expirations   → chain.get("expirations")
    server/app/options_api.py:112  chain         → data["calls"]/["puts"]
    server/app/options_api.py:146  liquidity_gate→ data.get("providerStatus") != "ok"
    server/app/main.py:1211        status        → _get_options_for_status(t)
    server/app/main.py:2103, 2138, 2626, 2656, 2776, 2844  option_quotes_getter=
    web/src/persistence.js:1065, 1111, 1396 e web/src/App.jsx:2887, 2947, 2967,
      3028 — todos branqueiam em `providerStatus !== "ok"` (ADR-004)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mydata_client — get_vencimentos e get_options_chain</name>

  <files>server/app/mydata_client.py, server/tests/test_mydata_client.py</files>

  <read_first>
    - server/app/mydata_client.py (estado após o Plano 09-01) — `_paginar`, `_fetch_json`, `MydataIndisponivel`, `LIMITE_MAX`, `normalize_ticker`
    - .planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-PATTERNS.md (tabela de mapeamento de `gold_opcoes`)
  </read_first>

  <behavior>
    - `get_vencimentos("petr4.sa")` chama o path `/v1/opcoes/PETR4/vencimentos` e devolve a lista de `dados` como veio (sem remapear).
    - `get_vencimentos` com `pregao="2026-08-25"` inclui `pregao` nos params; sem ele, não inclui a chave.
    - `get_vencimentos` numa resposta `{"dados": []}` devolve `[]`, sem levantar (papel sem opções é ausência de negócio, não erro).
    - `get_vencimentos` NÃO usa `_paginar` — o endpoint não pagina (não devolve `proximo_cursor`).
    - `get_options_chain("PETR4", vencimento="2026-09-19")` chama `/v1/opcoes/PETR4` com `vencimento` e `limite=2000` nos params.
    - `get_options_chain` percorre `proximo_cursor` (duas páginas encadeadas devolvem as linhas concatenadas).
    - `get_options_chain` sem `MYDATA_TOKEN` levanta `RuntimeError` citando a variável.
    - `get_options_chain` propaga `MydataIndisponivel` (não engole) — quem decide degradar é o adaptador.
  </behavior>

  <action>
    Acrescentar a `server/app/mydata_client.py` duas corrotinas, no mesmo estilo
    de `get_history` (kwarg `fetch_json=None` injetável, `normalize_ticker` na
    entrada, guarda de `_token()` levantando `RuntimeError` que cita
    `MYDATA_TOKEN`):

    `async def get_vencimentos(ticker, pregao=None, *, fetch_json=None) -> list[dict]`
      — chama `/v1/opcoes/{symbol}/vencimentos` com `{"pregao": pregao}` só
      quando `pregao` for truthy. Usa `_fetch_json` DIRETO (não `_paginar`):
      este endpoint devolve `{"dados": [...]}` sem `proximo_cursor`. Devolve
      `resp.get("dados") or []`; se `resp` não for dict, levanta
      `MydataIndisponivel`. NÃO remapeia campos — as chaves
      (`dt_vencimento`, `contratos`, `com_sigma`, `vence_no_pregao`,
      `menor_strike`, `maior_strike`) são consumidas cruas pelo adaptador.

    `async def get_options_chain(ticker, vencimento=None, pregao=None, tipo=None, *, fetch_json=None) -> list[dict]`
      — monta os params `{"limite": LIMITE_MAX}` mais `vencimento`/`pregao`/`tipo`
      quando truthy, e delega a `_paginar(f"/v1/opcoes/{symbol}", params, fetch_json=fetch_json)`.
      Devolve a lista de linhas cruas de `gold_opcoes` — o mapeamento para o
      contrato do ADR-004 é responsabilidade do adaptador (Task 2), não do
      cliente. Manter a separação: o cliente fala o vocabulário do hub, o
      adaptador fala o vocabulário do Boris.

    Documentar em comentário, acima das duas funções, que sem `pregao` o hub
    responde o ÚLTIMO pregão publicado DAQUELE papel (não o último da base) —
    é o que impede um papel que parou de negociar de parecer "sem dado".

    Estender `server/tests/test_mydata_client.py` com os casos de `<behavior>`,
    usando o mesmo fake `fetch_json` que grava `(path, params)`.
  </action>

  <verify>
    <automated>cd server &amp;&amp; python -m pytest tests/test_mydata_client.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - `cd server && python -m pytest tests/test_mydata_client.py -x -q` sai com código 0.
    - `grep -c 'def get_vencimentos' server/app/mydata_client.py` retorna 1 e `grep -c 'def get_options_chain' server/app/mydata_client.py` retorna 1.
    - `grep -v '^#' server/app/mydata_client.py | grep -c '/vencimentos'` retorna >= 1.
    - Existe teste do path de vencimentos: `grep -cE 'def test_.*vencimento' server/tests/test_mydata_client.py` retorna >= 1.
    - O cliente NÃO remapeia campos de opção: `grep -c 'contractSymbol' server/app/mydata_client.py` retorna 0.
  </acceptance_criteria>

  <done>
    O cliente sabe listar vencimentos e buscar a cadeia paginada do mydata,
    devolvendo linhas cruas e propagando falha para quem decide degradar.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: options_provider_mydata — adaptador do contrato ADR-004</name>

  <files>server/app/options_provider_mydata.py, server/tests/test_options_provider_mydata.py</files>

  <read_first>
    - server/app/options_provider_yahoo.py (arquivo inteiro, 143 linhas) — o contrato exato a preservar: `_empty_payload`, `_clean_contract`, cache TTL de módulo, e o padrão de nunca deixar erro bruto vazar
    - server/app/options_api.py (linhas 94-156) — como `expirations`, `chain` e `liquidity_gate` consomem o payload
    - server/app/options_quant.py (linhas 103-118) — `liquidity_score`, para medir o efeito de `openInterest` ausente
    - server/app/mydata_client.py (após a Task 1)
    - docs/adr/004-fonte-de-opcoes-na-v2.md — o contrato `providerStatus` que não pode mudar
  </read_first>

  <behavior>
    - `get_options("PETR4")` com hub sadio devolve `providerStatus == "ok"` e `source == "mydata"`.
    - `expirations` sai da lista do endpoint de vencimentos, em ordem, como strings `AAAA-MM-DD`.
    - Sem `expiration` pedido, o escolhido é o primeiro vencimento com `vence_no_pregao == 0`; se todos vencerem no pregão, o primeiro da lista.
    - Com `expiration` pedido e presente na lista, é ele que vai no filtro `vencimento` da chamada de cadeia.
    - Com `expiration` pedido e AUSENTE da lista, degrada com warning explicando que o vencimento não existe naquele pregão — não devolve outra cadeia calada.
    - `tipo == "CALL"` vira `optionType == "call"` (minúsculo) e cai em `calls`; `"PUT"` vira `"put"` e cai em `puts`.
    - `premio` vira `lastPrice`; `melhor_oferta_compra` vira `bid`; `melhor_oferta_venda` vira `ask`; `quantidade_negociada` vira `volume`.
    - `openInterest` é `None` em todo contrato (a B3 não publica open interest).
    - `volatilidade_implicita` nula NÃO degrada nem vira erro: `impliedVolatility` fica `None` e `ivStatus` carrega o `situacao_sigma` correspondente.
    - `underlyingPrice` sai do primeiro `preco_objeto` não nulo; `currency` é `"BRL"`.
    - `distancePct` usa a mesma fórmula do provider Yahoo e é `None` quando não há spot.
    - `inTheMoney` é derivado do spot: call ITM quando `spot > strike`, put ITM quando `spot < strike`; `False` quando não há spot.
    - Gregas e preço teórico do hub aparecem no contrato como campos ADITIVOS (`greeks`, `theoreticalPrice`) — nenhum campo do contrato antigo é removido.
    - Hub sem nenhum pregão para o papel (`vencimentos` devolve `[]`) → `providerStatus == "degraded"` com warning específico, sem `providerError`.
    - `MydataIndisponivel` levantado pelo cliente → `providerStatus == "degraded"`, `providerError` com a mensagem, e o Yahoo NÃO é chamado (D-04).
    - Erro é cacheado por `_ERROR_TTL` (60s) e sucesso por `_OPTIONS_TTL` (300s) — mesma mecânica do provider Yahoo.
    - Duas chamadas seguidas ao mesmo `(ticker, expiration)` dentro do TTL fazem UMA ida ao cliente (o cache poupa cota).
  </behavior>

  <action>
    Criar `server/app/options_provider_mydata.py` copiando a ESTRUTURA de
    `server/app/options_provider_yahoo.py` e trocando a fonte por trás.

    Módulo: `_OPTIONS_TTL = 300`, `_ERROR_TTL = 60`,
    `_cache: dict[str, tuple[float, dict]] = {}` — idênticos ao provider Yahoo
    (o cache existe para não gastar cota; `options_api.liquidity_gate` conta com
    ele explicitamente, ver docstring da linha 138-141).

    `MYDATA_OPTIONS_WARNING` — texto PT-BR honesto, sem prometer retry rápido e
    sem citar o Yahoo. Deve dizer que a cadeia de opções vem do acervo oficial
    da B3 publicado após o fechamento do pregão e que, neste momento, o hub de
    dados não respondeu. Princípio 4 do CLAUDE.md do repo: mostra o estado
    correto, não inventa.

    `def _empty_payload(ticker, expiration, warning, error=None) -> dict` —
    mesmas chaves de `options_provider_yahoo._empty_payload`, com
    `"source": "mydata"`, `"providerStatus": "degraded"`, e `"symbol"` igual ao
    ticker normalizado (o hub usa o código da B3 direto, sem sufixo `.SA` —
    NÃO usar `catalog.yahoo_symbol` aqui).

    `def _clean_contract(raw: dict, spot) -> dict` — mapeia uma linha de
    `gold_opcoes` para o contrato do ADR-004, nesta correspondência exata:
      contractSymbol ← `contrato`
      optionType     ← `str(raw.get("tipo") or "").lower()`  ("call"/"put")
      strike         ← `strike`
      lastPrice      ← `premio`
      bid            ← `melhor_oferta_compra`
      ask            ← `melhor_oferta_venda`
      change         ← `None`  (COTAHIST não publica variação do contrato)
      percentChange  ← `None`
      volume         ← `quantidade_negociada` ou 0
      openInterest   ← `None`  (SEM FONTE — ver `<assumptions>`)
      impliedVolatility ← `volatilidade_implicita`  (nulo é legítimo)
      inTheMoney     ← derivado do spot conforme `<behavior>`
      currency       ← `"BRL"`
      distancePct    ← mesma fórmula de `options_provider_yahoo._clean_contract`
    e os campos ADITIVOS, que o contrato antigo não tinha e que são o ganho da
    migração:
      ivStatus          ← `situacao_sigma`
      theoreticalPrice  ← `preco_teorico`
      greeks            ← `{"delta","gamma","vega","theta","rho"}` da própria linha
      expiration        ← `dt_vencimento`
      riskFreeRate      ← `taxa_livre_risco`
      exerciseStyle     ← `estilo_exercicio`
    Aditivo, nunca substitutivo: nenhuma chave do contrato antigo some.
    NÃO recalcular nada localmente aqui — IV e gregas vêm resolvidas do hub
    (questão aberta 3, resolvida em `<assumptions>`).

    `async def get_options(ticker, expiration=None) -> dict`:
      1. `t = normalize_ticker(ticker)`; chave de cache `f"{t}:{expiration or 'first'}"`;
         devolve o cacheado se `time.time() - hit[0] < _OPTIONS_TTL`.
      2. `venc = await mydata_client.get_vencimentos(t)`. Vazio → `_empty_payload`
         com warning de "sem cadeia publicada para este ativo", cacheado com
         `_ERROR_TTL`, e `return`.
      3. `expirations = [v["dt_vencimento"] for v in venc]`. Escolha do
         vencimento conforme `<behavior>`.
      4. `linhas = await mydata_client.get_options_chain(t, vencimento=escolhido)`.
      5. `spot` = primeiro `preco_objeto` não nulo das linhas; `pregao` =
         `dt_pregao` da primeira linha.
      6. Monta o payload `ok` com `expirations`, `expiration=escolhido`,
         `calls`/`puts` particionados por `tipo`, `underlyingPrice=spot`,
         `currency="BRL"`, `source="mydata"`, `providerStatus="ok"`, mais os
         aditivos `pregao` e `provenance` (o dict `proveniencia` da primeira
         linha, se houver — rastro até o arquivo oficial, que o contrato do
         Yahoo nunca teve).
      7. TODO o corpo, do passo 2 ao 6, dentro de um `try/except Exception` que
         cai em `_empty_payload(..., error=str(e))` cacheado com `_ERROR_TTL`.
         **Nenhum ramo deste arquivo pode importar ou chamar
         `options_provider_yahoo` — D-04 proíbe fallback para o Yahoo.**

    Criar `server/tests/test_options_provider_mydata.py` cobrindo todos os itens
    de `<behavior>`, com `monkeypatch.setattr(provider.mydata_client, "get_vencimentos"/"get_options_chain", fake)`
    e `provider._cache.clear()` no começo de cada teste — mesmo idioma de
    `server/tests/test_options_provider_yahoo.py`.

    Acrescentar UM teste de medição do efeito de `openInterest=None`:
    construir uma linha de contrato realista de PETR4 (volume alto, bid/ask
    apertados) e assertar o `liquidity_score` resultante, documentando no nome
    do teste o número obtido e se ele passa do corte de 40 usado por
    `options_api.liquidity_gate`. Se NÃO passar, registrar o número no SUMMARY
    como achado de produto para o checkpoint do Plano 09-06 — não "consertar"
    `options_quant.py` por conta própria.
  </action>

  <verify>
    <automated>cd server &amp;&amp; python -m pytest tests/test_options_provider_mydata.py tests/test_options_provider_yahoo.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - `cd server && python -m pytest tests/test_options_provider_mydata.py tests/test_options_provider_yahoo.py -x -q` sai com código 0.
    - `grep -c 'options_provider_yahoo' server/app/options_provider_mydata.py` retorna 0 (D-04: sem fallback ao Yahoo).
    - `grep -c 'yahoo' server/app/options_provider_mydata.py` retorna 0.
    - `grep -v '^#' server/app/options_provider_mydata.py | grep -c 'black_scholes'` retorna 0 (sem recálculo local; a IV vem do hub).
    - `grep -v '^#' server/app/options_provider_mydata.py | grep -c 'situacao_sigma'` retorna >= 1.
    - Todas as chaves do contrato antigo existem no arquivo: `for k in contractSymbol optionType strike lastPrice bid ask change percentChange volume openInterest impliedVolatility inTheMoney currency distancePct; do grep -q "$k" server/app/options_provider_mydata.py || echo "FALTA $k"; done` não imprime nada.
    - `grep -c 'providerStatus' server/app/options_provider_mydata.py` retorna >= 2 (`ok` e `degraded`).
    - Existe teste do caminho degradado sem Yahoo: `grep -cE 'def test_.*degrad' server/tests/test_options_provider_mydata.py` retorna >= 1.
    - Existe o teste de liquidez sem open interest: `grep -cE 'def test_.*(liquid|open_interest)' server/tests/test_options_provider_mydata.py` retorna >= 1.
  </acceptance_criteria>

  <done>
    O adaptador preenche o contrato do ADR-004 a partir de `gold_opcoes`, sem
    recalcular IV/gregas, sem tocar no Yahoo em nenhum ramo, e o efeito da
    ausência de open interest sobre o gate de liquidez está MEDIDO, não presumido.
  </done>
</task>

<task type="auto">
  <name>Task 3: Seletor options_provider + troca dos 8 call sites</name>

  <files>server/app/options_provider.py, server/app/options_api.py, server/app/main.py, server/tests/test_options_provider_mydata.py</files>

  <read_first>
    - server/app/candle_provider.py (linhas 189-213) — o padrão de seletor por env já estabelecido no repo (`_PROVEDORES`, `provider_name()`, `get_provider()`), que este arquivo copia em miniatura
    - server/app/options_api.py (linha 10 e call sites 100, 112, 146)
    - server/app/main.py (linhas 30, 46, 1211-1230, 2103, 2138, 2626, 2656, 2776, 2844) — TODOS os consumidores
    - server/app/options_provider_mydata.py (após a Task 2)
    - server/app/options_provider_yahoo.py (para confirmar a assinatura idêntica `get_options(ticker, expiration=None)`)
  </read_first>

  <action>
    **1. Criar `server/app/options_provider.py`** — seletor mínimo, no espírito
    de `candle_provider.provider_name()/get_provider()`:
      - `_PROVEDORES = {"yahoo": ..., "mydata": ...}` mapeando nome → a corrotina
        `get_options` de cada módulo (import de topo dos dois módulos; nenhum
        deles importa o outro, então não há ciclo).
      - `def provider_name() -> str`: `(os.environ.get("B3_OPTIONS_PROVIDER") or "yahoo").strip().lower()`.
        **O default é `"yahoo"` de propósito**: o critério de aceite obrigatório
        da fase (Folded Todo do 09-CONTEXT.md) exige medir o rate-limit real
        antes de desligar Yahoo/brapi nas fatias migradas em produção. A virada
        para `mydata` é o checkpoint humano do Plano 09-06.
      - Nome desconhecido levanta `ValueError` listando as opções — mesma
        postura de `candle_provider.get_provider()` (linhas 203-205): falhar
        alto explicando o que falta.
      - `async def get_options(ticker, expiration=None) -> dict` despacha para o
        provedor ativo.
      - Docstring do módulo deixando explícito que este seletor é ALAVANCA DE
        ROLLBACK operacional, **não fallback automático**: D-04 proíbe o Yahoo
        como fallback ativo de opções, porque ele é exatamente a fonte instável
        (401/403/429) que a migração elimina. Trocar de provedor aqui é uma
        decisão humana registrada, não um desvio silencioso em tempo de execução.

    **2. Trocar os call sites.** Substituições literais, sem mudar nenhuma
    chamada:
      - `server/app/options_api.py:10` — `from .options_provider_yahoo import get_options`
        → `from .options_provider import get_options`. As linhas 100, 112 e 146
        NÃO mudam.
      - `server/app/main.py:30` — `from . import options_provider_yahoo  # v2: ...`
        → `from . import options_provider  # v2: cotação de contratos (ADR-003/004/005), provedor selecionável`.
      - `server/app/main.py:46` — `from .options_provider_yahoo import get_options as _get_options_for_status`
        → `from .options_provider import get_options as _get_options_for_status`.
      - `server/app/main.py` linhas 2103, 2138, 2626, 2656, 2776, 2844 —
        `options_provider_yahoo.get_options` → `options_provider.get_options`
        (quatro delas são `option_quotes_getter=`, duas são `await ...(...)`).
      - `server/app/main.py:1216` e `main.py:1226` — os dois literais
        `"yfinance"` viram, respectivamente,
        `chain.get("source") or options_provider.provider_name()` e
        `options_provider.provider_name()`. Rotular a fonte errada viola o
        princípio 3 do CLAUDE.md do repo ("dados de mercado exibem fonte").

    Verificar por grep que nenhum `options_provider_yahoo.` sobrou fora do
    próprio módulo, do seletor e do seu teste.

    **3. Testes.** Acrescentar a `server/tests/test_options_provider_mydata.py`
    (ou um `test_options_provider.py` novo, à escolha do executor — declarar no
    SUMMARY) os casos:
      - `provider_name()` sem env devolve `"yahoo"`;
      - com `B3_OPTIONS_PROVIDER=mydata` devolve `"mydata"` e `get_options`
        despacha para o adaptador do mydata (monkeypatch conta a chamada);
      - com `B3_OPTIONS_PROVIDER=banana` levanta `ValueError` citando as opções
        válidas;
      - o módulo do Yahoo NÃO é chamado quando o provedor ativo é `mydata`.
  </action>

  <verify>
    <automated>cd server &amp;&amp; python -m pytest tests/ -q -k "options or main or status"</automated>
  </verify>

  <acceptance_criteria>
    - `cd server && python -m pytest tests/ -q` sai com código 0 (suíte completa do backend — `main.py` foi editado em 8 pontos).
    - `bash scripts/executar.sh --testes` sai com código 0 (suíte canônica COMPLETA).
    - `cd server && python -c "from app import main"` importa sem erro.
    - `grep -rn 'options_provider_yahoo' server/app/ | grep -v 'options_provider_yahoo.py:' | grep -v 'options_provider.py:' | grep -v '^server/app/candle_provider.py:7:'` não imprime nada.
    - `grep -c 'from .options_provider import get_options' server/app/options_api.py` retorna 1.
    - `grep -c '"yfinance"' server/app/main.py` retorna 0.
    - `cd server && python -c "from app import options_provider as p; print(p.provider_name())"` imprime `yahoo` (default de produção INALTERADO nesta fase).
    - `cd server && B3_OPTIONS_PROVIDER=mydata python -c "from app import options_provider as p; print(p.provider_name())"` imprime `mydata`.
    - `cd server && B3_OPTIONS_PROVIDER=banana python -c "from app import options_provider as p; p.get_provider_fn() if hasattr(p,'get_provider_fn') else p.provider_name()" 2>&1 | grep -qi 'banana\|ValueError' && echo OK` imprime `OK`, OU um teste equivalente cobre o caso (declarar qual no SUMMARY).
    - Nenhuma linha de `server/app/options_quant.py` foi alterada: `git diff --stat server/app/options_quant.py` não lista o arquivo.
  </acceptance_criteria>

  <done>
    Todos os consumidores de opções passam pelo seletor; o provedor ativo é
    configuração; o default de produção continua no Yahoo até a medição do
    Plano 09-04; e a fonte exibida ao usuário deixou de ser um literal
    `"yfinance"` desatualizado.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mydata → contrato ADR-004 | IV e gregas calculadas fora do Boris entram num payload que a UI usa para decidir se libera a compra simulada |
| Env de operador → `B3_OPTIONS_PROVIDER` | Decide a fonte de toda cadeia de opções do app |
| `ticker`/`expiration` de rota HTTP → params do hub | Entrada do usuário vira path e query da chamada externa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Tampering | IV/gregas de terceiro alimentando decisão | mitigate | `providerStatus` continua sendo o portão do ADR-004 e nada muda nele: `options_api.liquidity_gate` (linha 149) e o front (`persistence.js:1065,1111,1396`) já bloqueiam compra em qualquer estado != "ok". Números do hub vêm com proveniência (`sha256`, `arquivo`) até o arquivo oficial da B3, rastro que o Yahoo nunca deu |
| T-09-12 | Information Disclosure | `providerError` vazando detalhe técnico para a UI | mitigate | O campo já existe hoje e carrega `str(e)`; as mensagens de `mydata_client` (Plano 09-01, T-09-01) nunca contêm o token. Nenhuma URL com credencial é montada — a chave vai em header |
| T-09-13 | Tampering (injection) | `expiration` vindo do usuário indo em query | mitigate | O vencimento escolhido é sempre um item da lista devolvida pelo endpoint `/vencimentos`; valor de usuário que não está na lista degrada com warning, nunca é repassado ao hub |
| T-09-14 | Denial of Service | 2 chamadas por cadeia × N usuários | mitigate | Cache de módulo com TTL 300s (sucesso) e 60s (erro), compartilhado entre usuários — mesma mecânica que `liquidity_gate` já assume. O volume real entra na conta do Plano 09-04 |
| T-09-15 | Spoofing | 429 forjado degradando opções | accept | O efeito máximo de um 429 (real ou forjado) é `providerStatus="degraded"`, que BLOQUEIA compra e mostra aviso. Degradar é o estado seguro; um atacante que force degradação causa indisponibilidade, não decisão errada |
| T-09-16 | Repudiation | não saber qual fonte serviu a cadeia | mitigate | `source` no payload + `provenance` aditivo + `main.py` deixa de rotular `"yfinance"` fixo |
| T-09-SC | Tampering | instalação de pacote | mitigate | Nenhum pacote novo. Se o executor concluir que precisa de dependência nova, PARE e abra checkpoint humano bloqueante |
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` verde (as DUAS suítes).
- `cd server && python -m pytest tests/ -q` verde — `main.py` foi editado em 8 pontos e é o arquivo mais consumido do backend.
- `cd server && python -c "from app import main"` sem erro.
- Contrato preservado: um payload `ok` do provider mydata e um do provider Yahoo têm o MESMO conjunto de chaves de topo, exceto os aditivos declarados (`pregao`, `provenance`). Provar com um teste que compara `set(payload_mydata) >= set(payload_yahoo_ok)`.
- `git diff --stat server/app/options_quant.py server/app/options_provider_yahoo.py docs/adr/004-fonte-de-opcoes-na-v2.md` não lista nenhum arquivo (nada disso é tocado nesta fase).
</verification>

<success_criteria>
- Cadeia de opções servível pelo mydata, com IV e gregas do hub, atrás do
  contrato ADR-004 intacto.
- Nenhum ramo de código chama o Yahoo como fallback de opções (D-04).
- Provedor de opções é configuração, com default de produção ainda no Yahoo.
- Efeito de `openInterest` ausente sobre o gate de liquidez MEDIDO e registrado.
- Suíte canônica verde.
</success_criteria>

<output>
Create `.planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-03-SUMMARY.md` when done.
Registre no SUMMARY: (a) o `liquidity_score` medido de um contrato líquido sem
open interest e se ele passa do corte de 40 — este número vai para o checkpoint
do Plano 09-06; (b) quantas chamadas ao hub uma cadeia completa custa
(vencimentos + páginas da cadeia), número que o Plano 09-04 consome; (c) a
lista final de campos aditivos entregues além do contrato do ADR-004.
</output>
