---
phase: 09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa
plan: 04
type: execute
wave: 3
depends_on: ["09-02", "09-03"]
files_modified:
  - scripts/medir-mydata.py
  - docs/MEDICAO-Mydata-2026-08-27.md
  - .planning/todos/pending/medir-rate-limit-mydata.md
autonomous: true
requirements: []
user_setup:
  - service: mydata (cvm-financas)
    why: "A perna AO VIVO da medição precisa da chave real — sem ela só há projeção, e projeção sozinha não fecha o critério de aceite da fase"
    env_vars:
      - name: MYDATA_TOKEN
        source: "Chave de prefixo f00b4554 (produção). Exportar no shell local só para rodar a medição; nunca commitar."

must_haves:
  truths:
    - "Existe um número medido — não estimado — de chamadas/minuto e chamadas/dia que as duas fatias migradas custam"
    - "Esse número é comparado explicitamente contra 60/min e 2.000/dia, com veredito CABE ou NÃO CABE"
    - "A chave de produção autentica de fato contra mydata.acamerini.app e devolve dado das duas rotas"
    - "Se não couber na cota, o plano de ação (negociar aumento × reduzir cadência) fica registrado antes de qualquer virada de produção"
    - "O TODO de rate-limit sai de 'pending' com os números anexados"
  artifacts:
    - path: "scripts/medir-mydata.py"
      provides: "Medição em duas pernas: projeção offline do volume + amostra ao vivo contra a API real"
    - path: "docs/MEDICAO-Mydata-2026-08-27.md"
      provides: "Os números medidos, o veredito contra a cota e o plano de ação"
      contains: "veredito"
  key_links:
    - from: "scripts/medir-mydata.py"
      to: "server/app/scanner.get_universe"
      via: "o universo REAL do Radar é a base da projeção, não um número chutado"
      pattern: "scanner\\.get_universe"
    - from: "docs/MEDICAO-Mydata-2026-08-27.md"
      to: "Plano 09-06 (checkpoint de virada)"
      via: "o veredito é o pré-requisito da virada de produção"
      pattern: "60/min|2\\.000/dia"
---

<objective>
Fechar o critério de aceite OBRIGATÓRIO desta fase: medir o volume real de
chamadas ao mydata das duas fatias migradas contra o limite da chave
(60/min · 2.000/dia) ANTES de desligar Yahoo/brapi em produção.

Purpose: o 09-CONTEXT.md dobrou o TODO
`.planning/todos/pending/medir-rate-limit-mydata.md` em critério de aceite
formal, e o próprio TODO diz por quê: a suficiência da cota "não foi medida — é
inferência de arquitetura". O ROADMAP repete a exigência no goal da fase. Sem
este plano, a virada do Plano 09-06 seria feita no escuro, e o princípio 4 do
CLAUDE.md do repo (nunca inventar valor quando a fonte falha) começaria a ser
testado em produção, com usuário na frente.

Output: um script de medição reexecutável, um documento com os números e o
veredito, e o TODO fechado com evidência.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-CONTEXT.md
@.planning/todos/pending/medir-rate-limit-mydata.md
@.planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-02-SUMMARY.md
@.planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-03-SUMMARY.md

<assumptions>
Os dois SUMMARYs anteriores já trazem os custos unitários pedidos:
- 09-02: chamadas por carga cheia (`rng="2y"`) e por delta (`rng="1mo"`) de UM ticker;
- 09-03: chamadas por cadeia de opções (vencimentos + páginas).
Este plano NÃO os re-deriva por leitura de código: ele os **mede executando o
código real** com um contador injetado, e usa os SUMMARYs só como conferência.

**Limite oficial da chave** (`~/dev/cvm-financas/docs/contrato-consumidor.md`,
tabela "Consumidores integrados"): prefixo `f00b4554`, escopo `empresa`,
`demonstracao_financeira`, `negociacao_b3`, `provento_b3` + `fonte:b3`,
**60/min · 2.000/dia**, emitida em 2026-08-26 em produção. A chave anterior
(`dadbd4b0`) NÃO tem `negociacao_b3` — era por ela que candle vinha negado. Se
a medição ao vivo devolver 200 com campos de preço AUSENTES, o sintoma é chave
sem `fonte:b3`, não ausência de dado: registrar isso e parar, não contornar.

**`provento_b3`**: o item 2 do TODO pede confirmar se essa classe já teve a
primeira carga completa do lado do cvm-financas. Ela está FORA das duas fatias
migradas nesta fase (só COTAHIST diário e opções). A verificação entra no
documento como linha de status, não como bloqueio desta fase.
</assumptions>

<interfaces>
Convenções do repositório para este tipo de artefato:
- Scripts de medição: `scripts/medir-*.py`, executados por
  `server/.venv/bin/python scripts/<nome>.py`, docstring de topo declarando o
  que o script NÃO toca (produção, banco, `/data`), fases selecionáveis por
  `--fases`, saída em Markdown no stdout + JSON opcional por `--saida`.
  Referência: `scripts/medir-yahoo-intraday.py`.
- Documentos de medição: `docs/MEDICAO-<Fonte>-<AAAA-MM-DD>.md`.
  Referência: `docs/MEDICAO-Brapi-2026-08-11.md`,
  `docs/MEDICAO-Yahoo-Intraday-2026-07-30.md`.

Fontes dos números da projeção (todas dentro do repo):
- `server/app/scanner.get_universe()` → o universo REAL do Radar (env
  `B3_SCAN_UNIVERSE` > `DEFAULT_UNIVERSE`, com teto `MAX_UNIVERSE`).
- `server/app/candle_cache.ranges_for("1d")` → `("2y", "1mo")`, os dois ranges
  que a carga cheia e o delta usam.
- `server/app/candle_cache._MIN_DELTA_INTERVAL` = 45.0 s → piso entre dois
  deltas do MESMO ticker.
- `server/app/options_provider_mydata._OPTIONS_TTL` = 300 s → cadeia de opções
  compartilhada entre usuários dentro da janela.
- `server/app/mydata_budget.quota_min()/quota_dia()` → 60 / 2000.
- `server/app/mydata_client.PAGINAS_MAX` → teto de páginas por chamada lógica.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/medir-mydata.py — projeção offline do volume de chamadas</name>

  <files>scripts/medir-mydata.py</files>

  <read_first>
    - scripts/medir-yahoo-intraday.py (linhas 1-60) — a forma do script de medição do repo: docstring do que NÃO toca, `--fases`, saída Markdown + `--saida` JSON
    - server/app/scanner.py (linhas 90-111) — `get_universe`, `DEFAULT_UNIVERSE`, `MAX_UNIVERSE`
    - server/app/candle_cache.py (linhas 27-50 e 180-215) — `ranges_for`, `_MIN_DELTA_INTERVAL`, e o fluxo miss/delta/fresh que define quantas chamadas cada acesso custa
    - server/app/mydata_client.py e server/app/options_provider_mydata.py — para injetar o contador nas funções reais
    - .planning/todos/pending/medir-rate-limit-mydata.md — os 3 itens que a medição precisa responder
  </read_first>

  <action>
    Criar `scripts/medir-mydata.py` com duas fases selecionáveis por `--fases`
    (default: só `projecao`; `vivo` exige `--vivo` explícito para não gastar cota
    por acidente).

    **Fase `projecao` (offline, determinística, ZERO rede).** Ela mede o custo
    unitário EXECUTANDO o código real com um `fetch_json` fake injetado que
    conta chamadas — não lendo o código e estimando. Passos:
      1. `universo = scanner.get_universe()`; registrar `len(universo)` e a
         origem (default × `B3_SCAN_UNIVERSE`).
      2. Custo de candle: chamar `mydata_client.get_history(t, rng="2y", fetch_json=contador)`
         e `..., rng="1mo", ...` com uma resposta fake de uma página, e depois
         com uma resposta fake de 3 páginas encadeadas, registrando o número de
         chamadas HTTP em cada caso. Isso dá `custo_carga_cheia` e `custo_delta`
         em chamadas por ticker, com e sem paginação.
      3. Custo de opções: chamar `options_provider_mydata.get_options(t)` com
         `get_vencimentos`/`get_options_chain` fakes contando chamadas, e
         registrar `custo_cadeia` (esperado: 1 de vencimentos + N de páginas).
      4. Projetar três cenários, cada um com `chamadas/dia` e `pico chamadas/min`:
         - **frio** — redeploy do Railway com L2 vazio: carga cheia do universo
           inteiro de uma vez;
         - **morno** — L2 quente, delta do universo inteiro na varredura do
           Radar, na cadência real do scheduler (ler o `interval_s` do
           `scheduler_loop` e o piso de `_MIN_DELTA_INTERVAL`);
         - **opções** — cadeias abertas por usuário, parametrizado por
           `--cadeias-dia` (default 200) com o TTL de 300 s absorvendo repetição.
      5. Somar os três e comparar com `mydata_budget.quota_min()` e
         `quota_dia()`. Emitir o **veredito**: `CABE`, `CABE COM FOLGA < 20%`
         ou `NÃO CABE`, com o número exato em cada caso.
      6. Emitir também o `intervaloMinimoSeguro` implícito: qual cadência de
         varredura mantém o pico abaixo de 60/min — o análogo de
         `brapi_budget.projecao()`.

    **Fase `vivo` (rede real, só com `--vivo`).** Amostra pequena e barata,
    default `--amostra 5` tickers:
      1. Confirmar que `MYDATA_TOKEN` está no ambiente. **Ausente → imprimir o
         que falta e sair com código 2. NÃO simular, NÃO estimar, NÃO seguir.**
      2. Para cada ticker da amostra: `GET /v1/cotacoes/{t}` com `limite=5` e
         `GET /v1/opcoes/{t}/vencimentos`. Registrar por chamada: status,
         latência ms, `X-Quota-Limite`, `X-Quota-Restante`, nº de linhas, e se
         os campos de preço vieram preenchidos.
      3. **Guarda de escopo de chave:** 200 com `dados` não vazio mas
         `preco_fechamento` ausente em todas as linhas é o sintoma documentado
         de chave sem `fonte:b3` (contrato-consumidor.md). Reportar isso como
         achado nomeado, não como "sem dado".
      4. Reconciliar: a queda observada em `X-Quota-Restante` bate com o número
         de chamadas feitas? Divergência é sinal de que a previsão local
         (`mydata_budget`) e a verdade do hub discordam — registrar.
      5. Nunca imprimir o token, nem inteiro nem parcial.

    Saída: tabelas Markdown no stdout e JSON bruto opcional por `--saida`.
    O script NÃO importa `app.main`, NÃO abre o banco de produção, NÃO escreve
    em `/data` — declarar isso na docstring de topo, como
    `scripts/medir-yahoo-intraday.py` faz.
  </action>

  <verify>
    <automated>server/.venv/bin/python scripts/medir-mydata.py --fases projecao</automated>
  </verify>

  <acceptance_criteria>
    - `server/.venv/bin/python scripts/medir-mydata.py --fases projecao` sai com código 0 e imprime, no stdout, o tamanho do universo, os custos unitários e a linha de veredito.
    - A saída contém a string `60/min` e a string `2.000/dia` (ou `2000/dia`) — a comparação é explícita, não implícita.
    - A saída contém uma das palavras `CABE` ou `NÃO CABE`.
    - A fase de projeção não faz rede: `server/.venv/bin/python scripts/medir-mydata.py --fases projecao` roda com `MYDATA_TOKEN` ausente do ambiente e ainda assim sai 0.
    - Sem `--vivo`, a fase de rede não roda: `server/.venv/bin/python scripts/medir-mydata.py --fases vivo` (sem `--vivo`) recusa e explica.
    - `MYDATA_TOKEN= server/.venv/bin/python scripts/medir-mydata.py --fases vivo --vivo` sai com código 2 e a mensagem cita `MYDATA_TOKEN`.
    - `grep -c 'scanner.get_universe' scripts/medir-mydata.py` retorna >= 1 (o universo é o real, não um número literal).
    - `grep -cE 'app\.main|B3_DB_PATH' scripts/medir-mydata.py` retorna 0 (não toca produção).
    - `grep -c 'MYDATA_TOKEN' scripts/medir-mydata.py` retorna >= 1 e nenhuma linha imprime o VALOR do token: `grep -nE 'print.*(_token\(\)|MYDATA_TOKEN\])' scripts/medir-mydata.py` não imprime nada.
  </acceptance_criteria>

  <done>
    O script mede os custos unitários executando o código real, projeta os três
    cenários sobre o universo real do Radar e emite um veredito explícito contra
    60/min e 2.000/dia — sem tocar produção e sem exigir credencial para a perna
    offline.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rodar a medição ao vivo e publicar o veredito</name>

  <files>docs/MEDICAO-Mydata-2026-08-27.md, .planning/todos/pending/medir-rate-limit-mydata.md</files>

  <read_first>
    - scripts/medir-mydata.py (após a Task 1)
    - docs/MEDICAO-Brapi-2026-08-11.md — estrutura, tom e nível de detalhe esperados de um documento de medição neste repo
    - .planning/todos/pending/medir-rate-limit-mydata.md — os 3 itens a responder
    - ~/dev/cvm-financas/docs/contrato-consumidor.md — tabela de consumidores integrados (limite oficial) e a linha de status de `provento_b3`
  </read_first>

  <action>
    1. Rodar `server/.venv/bin/python scripts/medir-mydata.py --fases projecao,vivo --vivo --amostra 5 --saida /tmp/medicao-mydata.json`.

       **Se `MYDATA_TOKEN` não estiver no ambiente:** PARE. Não invente número,
       não rode só a projeção e declare o critério fechado. Reporte que a perna
       ao vivo exige a chave (item de `user_setup` deste plano), registre a
       projeção obtida, e marque o plano como bloqueado aguardando a variável.
       Princípio 4 do CLAUDE.md do repo se aplica à própria medição.

    2. Escrever `docs/MEDICAO-Mydata-2026-08-27.md` (ajustar a data do nome para
       a data real da execução) com estas seções:
       - **O que foi medido e como** — comando exato, data, universo usado.
       - **Custos unitários medidos** — chamadas por carga cheia de candle, por
         delta, por cadeia de opções; com e sem paginação.
       - **Projeção** — a tabela dos três cenários (frio, morno, opções) com
         chamadas/dia e pico chamadas/min.
       - **Amostra ao vivo** — por ticker: status, latência, `X-Quota-Limite`,
         `X-Quota-Restante`, linhas devolvidas, campos de preço presentes.
       - **Reconciliação previsão × verdade** — o contador local bate com a
         queda de `X-Quota-Restante`?
       - **Veredito** — CABE / NÃO CABE em 60/min e em 2.000/dia, com o número.
       - **Item 2 do TODO** — status de `provento_b3` do lado do cvm-financas,
         com a ressalva de que a classe está fora das duas fatias desta fase.
       - **Plano de ação** — se NÃO couber: qual das duas saídas (negociar
         aumento de quota com o lado cvm-financas × reduzir a cadência de
         varredura para o `intervaloMinimoSeguro` calculado), com o número que
         sustenta a escolha. Se couber: qual a folga e a partir de que tamanho
         de universo ela acaba.
       - **Pré-condição da virada** — declarar explicitamente que este documento
         é o pré-requisito do checkpoint do Plano 09-06 e que, sem `CABE`, a
         virada de `B3_CANDLE_PROVIDER`/`B3_OPTIONS_PROVIDER` não acontece.

    3. Atualizar `.planning/todos/pending/medir-rate-limit-mydata.md`: anexar ao
       fim uma seção `## Resultado (AAAA-MM-DD)` com o veredito em uma linha e o
       link para o documento de medição. **Não apagar o texto original** — o
       repositório não reescreve histórico. Se o processo GSD do projeto move
       TODOs fechados para `.planning/todos/done/`, mover o arquivo INTEIRO com
       `git mv` preservando o conteúdo; senão, deixar em `pending` com o
       resultado anexado e explicar no SUMMARY.
  </action>

  <verify>
    <automated>test -f docs/MEDICAO-Mydata-*.md &amp;&amp; grep -qiE 'CABE' docs/MEDICAO-Mydata-*.md &amp;&amp; grep -qE '60/min' docs/MEDICAO-Mydata-*.md &amp;&amp; echo OK</automated>
  </verify>

  <acceptance_criteria>
    - Existe `docs/MEDICAO-Mydata-<data>.md` e ele contém as 8 seções listadas no `<action>` (verificar por grep de cada título).
    - O documento contém um veredito literal: `grep -ciE '^\*\*?veredito' docs/MEDICAO-Mydata-*.md` retorna >= 1.
    - O documento contém números concretos da amostra ao vivo, não placeholders: `grep -cE 'X-Quota-Restante' docs/MEDICAO-Mydata-*.md` retorna >= 1 e não há a string `TBD` nem `TODO` no arquivo (`grep -cE 'TBD|XXX' docs/MEDICAO-Mydata-*.md` retorna 0).
    - `.planning/todos/pending/medir-rate-limit-mydata.md` (ou seu destino em `done/`) contém a seção `## Resultado` e o texto original intacto: `grep -c 'Vira critério de aceite da fase' <arquivo>` retorna 1.
    - Nenhum token no repositório: `grep -rn 'f00b4554' docs/ .planning/todos/ | grep -v 'prefixo\|prefix'` não imprime linha alguma que contenha uma chave completa (o PREFIXO de 8 caracteres é público e pode aparecer; a chave completa nunca).
    - `bash scripts/executar.sh --testes` continua verde (este plano não muda código de aplicação, mas a suíte confirma que nada foi arrastado).
  </acceptance_criteria>

  <done>
    Existe um número medido, publicado, comparado contra a cota, com veredito e
    plano de ação — o critério de aceite obrigatório da fase está fechado com
    evidência, não com inferência.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell local do desenvolvedor → mydata em produção | A perna ao vivo usa a chave REAL contra o hub REAL e consome cota real |
| Saída da medição → repositório git | Números e cabeçalhos de resposta viram arquivo commitado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-17 | Information Disclosure | token vazando para stdout, JSON de saída ou doc commitado | mitigate | O script nunca imprime `_token()`; critério de aceite grepa por `print` do valor; o doc registra apenas o PREFIXO público (`f00b4554`) e critério de aceite grepa por chave completa em `docs/` e `.planning/` |
| T-09-18 | Denial of Service (auto-infligido) | medição ao vivo queimando a cota do dia | mitigate | Perna ao vivo só roda com `--vivo` EXPLÍCITO, amostra default de 5 tickers × 2 rotas = 10 chamadas, `limite=5` nas cotações. Custo máximo 0,5% da cota diária |
| T-09-19 | Repudiation | virar produção sem evidência do que foi medido | mitigate | O documento é pré-requisito declarado do checkpoint do Plano 09-06; o comando exato e a data ficam registrados para reexecução |
| T-09-20 | Tampering | medição que "passa" por medir a coisa errada | mitigate | Os custos unitários são obtidos EXECUTANDO `mydata_client`/`options_provider_mydata` reais com contador injetado, e o universo vem de `scanner.get_universe()` — não há número literal chutado no script |
| T-09-SC | Tampering | instalação de pacote | mitigate | Nenhum pacote novo. Se o executor concluir que precisa de dependência nova, PARE e abra checkpoint humano bloqueante |
</threat_model>

<verification>
- `server/.venv/bin/python scripts/medir-mydata.py --fases projecao` roda em qualquer máquina, sem credencial, e imprime o veredito da projeção.
- O documento de medição existe, tem números reais e um veredito.
- `bash scripts/executar.sh --testes` verde.
- `git status --short` não mostra nenhum arquivo de segredo novo (`.env`, `*.key`, JSON com token).
</verification>

<success_criteria>
- Volume real de chamadas MEDIDO (não estimado) e comparado a 60/min · 2.000/dia.
- Veredito publicado, com plano de ação para o caso de não caber.
- A chave de produção confirmada funcionando contra as duas rotas, com escopo
  `fonte:b3` verificado.
- TODO de rate-limit fechado com evidência anexada, sem reescrever o texto original.
</success_criteria>

<output>
Create `.planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-04-SUMMARY.md` when done.
Registre no SUMMARY, em destaque: (a) o VEREDITO em uma linha; (b) o pico
chamadas/min e o total chamadas/dia projetados; (c) o `intervaloMinimoSeguro`
calculado; (d) se a perna ao vivo rodou ou ficou bloqueada por falta de
`MYDATA_TOKEN`. O checkpoint do Plano 09-06 lê exatamente esses quatro itens.
</output>
