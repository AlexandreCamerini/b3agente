---
phase: 09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa
plan: 05
type: execute
wave: 4
depends_on: ["09-04"]
files_modified:
  - server/app/b3_historical.py
  - server/tests/test_b3_historical.py
  - server/app/agent.py
  - server/app/db.py
  - server/app/main.py
  - server/app/rbac.py
  - docs/adr/019-cotahist-diario-b3.md
  - docs/adr/020-centralizacao-de-dados-no-mydata.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "O Boris deixa de manter uma ingestão própria e paralela do COTAHIST — a duplicação que criou esta fase acabou"
    - "A decisão sobre o código NÃO COMMITADO de b3_historical foi tomada pelo Alex, não presumida pelo executor"
    - "Nenhum caminho de código no servidor baixa arquivo da B3 por conta própria"
    - "O registro histórico da decisão de 25/08 (ADR-019) permanece legível — histórico se carimba, não se reescreve"
    - "Existe uma ADR nova dizendo o que foi superado, por quê, e o que deliberadamente NÃO foi"
  artifacts:
    - path: "docs/adr/020-centralizacao-de-dados-no-mydata.md"
      provides: "Supersessão parcial de ADR-019 e ADR-008; não-supersessão de ADR-001 e ADR-004"
      contains: "ADR-019"
    - path: "docs/adr/019-cotahist-diario-b3.md"
      provides: "Registro da época, carimbado com o status de superada"
      contains: "Superada parcialmente pela ADR-020"
  key_links:
    - from: "server/app/agent.py::scheduler_loop"
      to: "b3_historical.maybe_run"
      via: "REMOÇÃO do hook — o laço não pode mais baixar COTAHIST"
      pattern: "b3_historical"
---

<objective>
Aposentar a ingestão paralela de COTAHIST do Boris, agora que o mydata cobre a
fatia diária, e registrar a supersessão numa ADR nova.

Purpose: o achado que originou esta fase é literal — em 25/08 o Boris
reimplementou em paralelo a ingestão do COTAHIST que o hub já roda em produção,
"duas capturas do mesmo arquivo oficial, com proveniências separadas: exatamente
o que a fronteira existe para impedir". Manter os dois é pagar duas vezes pelo
mesmo dado e criar dois números possíveis para o mesmo pregão.

Output: a ingestão paralela sai (pela via que o Alex decidir), o hook do
scheduler que baixava arquivo da B3 sai, o ADR-019 é carimbado, e `docs/adr/020`
registra o que foi superado e o que deliberadamente não foi.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-CONTEXT.md
@.planning/notes/boris-pp-centralizacao-dados-mydata.md
@docs/adr/019-cotahist-diario-b3.md
@.planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-04-SUMMARY.md

<assumptions>
**Estado de partida, verificado em 2026-08-27 e a ser RECONFIRMADO na Task 1.**
O trabalho do ADR-019 está na árvore de trabalho e **NÃO COMMITADO**:

    untracked:   server/app/b3_historical.py
                 server/tests/test_b3_historical.py
                 docs/adr/019-cotahist-diario-b3.md
    modificado:  server/app/agent.py   (+8 linhas: hook b3_historical.maybe_run em scheduler_loop)
                 server/app/db.py      (+36: tabelas b3_daily_imports e b3_daily_quotes)
                 server/app/main.py    (+46: import + GET/POST /api/admin/b3/cotahist*)
                 server/app/rbac.py    (+-1: entidade "b3_daily_import" em fontes_dados.configurar)

Ou seja: **nada disso jamais rodou em produção.** Nenhuma linha foi importada,
nenhuma tabela foi criada no SQLite do Railway, nenhum arquivo da B3 foi baixado
por um servidor do Boris. Isso muda o custo de cada saída — e é por isso que a
decisão é do Alex, não do executor.

**Guardrails do repositório que se aplicam aqui:**
- "Histórico não se reescreve" vale para `qa/`, `ESTADO-*`, `CHECKOUT-*` e
  RELEASES. `docs/adr/` registra decisões e também não tem texto apagado — uma
  ADR superada ganha cabeçalho de status, não some.
- "Não-destrutivo por padrão: confirme antes de deletar" (CLAUDE.md global).
- "Guardiões de teste não se apagam — reversão deliberada atualiza o guardião
  com nota." `test_b3_historical.py` é guardião de um comportamento revertido
  de propósito; a nota é a ADR-020.

**Escopo negativo:** este plano NÃO vira nenhum default de produção
(`B3_CANDLE_PROVIDER`/`B3_OPTIONS_PROVIDER` ficam como estão — isso é o Plano
09-06) e NÃO toca `web/`.
</assumptions>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decisão do Alex sobre o código não commitado do ADR-019</name>

  <read_first>
    - Rodar `git status --short` e `git diff --stat server/app/agent.py server/app/db.py server/app/main.py server/app/rbac.py` ANTES de apresentar a decisão, e reportar o estado REAL encontrado. Se ele divergir do descrito em `<assumptions>` (por exemplo, alguém commitou nesse meio-tempo), apresentar a divergência primeiro e recalibrar as opções — não seguir com o roteiro escrito.
    - docs/adr/019-cotahist-diario-b3.md
    - .planning/notes/boris-pp-centralizacao-dados-mydata.md
  </read_first>

  <action>
    NÃO executar nenhuma remoção, `rm`, `git rm`, `git commit` ou edição de
    arquivo nesta task. A única ação permitida é DIAGNÓSTICO E APRESENTAÇÃO:

    1. Rodar `git status --short` e
       `git diff --stat server/app/agent.py server/app/db.py server/app/main.py server/app/rbac.py`,
       e comparar com o estado descrito em `<assumptions>` (3 arquivos untracked
       + 91 linhas de diff em 4 arquivos).
    2. Rodar `git log --oneline -3` para confirmar que nenhum commit recente já
       incorporou esse trabalho.
    3. Verificar se as tabelas `b3_daily_imports`/`b3_daily_quotes` existem em
       algum banco local (`server/data/*.db`) — se existirem, isso muda o custo
       da opção `reverter` e precisa entrar na apresentação.
    4. Apresentar ao Alex o estado real encontrado, as três opções abaixo com
       seus prós e contras, e a recomendação. Se o estado real divergir do
       esperado, apresentar a divergência PRIMEIRO e recalibrar as opções antes
       de pedir a decisão.
    5. Aguardar a resposta. A Task 2 é quem executa.
  </action>

  <decision>
    O que fazer com a implementação de ingestão paralela do COTAHIST
    (`b3_historical.py` + 91 linhas de fiação em 4 arquivos + ADR-019), que está
    na árvore de trabalho, não commitada, e que esta fase aposenta.
  </decision>

  <context>
    Em 25/08 o Boris implementou a leitura diária do COTAHIST da B3 (ADR-019:
    baixa o ZIP, valida o TXT de posições fixas de 245 colunas, grava em
    `b3_daily_quotes`, SHA-256 para idempotência, job no scheduler às 20:30 BRT).
    Um dia depois, a auditoria do lado cvm-financas apontou que o hub já roda
    exatamente essa ingestão em produção — mesma fonte, mesmo arquivo,
    proveniências separadas. Foi o achado que criou esta Fase 9.

    Os Planos 09-01 a 09-03 já entregaram o caminho pelo mydata; o 09-04 já
    mediu se a cota aguenta. O trabalho do ADR-019 nunca foi commitado nem
    publicado: nenhuma tabela existe no banco de produção, nenhum download jamais
    saiu de um servidor do Boris.

    Um detalhe que pesa na balança: o ADR-019 entrega algo que o mydata NÃO
    entrega hoje — a linha COTAHIST CRUA, com `bdi_code`, `market_type`,
    `specification`, `reference_term`, `average`, `exercise_price`. O
    `gold_cotacoes` do mydata é um recorte à vista (`tp_mercado='010'`) já
    normalizado. Nada no Boris consome esses campos crus hoje, e o mydata expõe
    `/v1/meta/linhagem` para rastro. Mas é uma capacidade que sai junto.
  </context>

  <options>
    <option id="reverter">
      <name>Reverter tudo — a ingestão paralela nunca existe</name>
      <pros>
        Dedup total e imediata: uma fonte, uma proveniência, zero código morto.
        Nada a manter, nada a explicar depois. O banco de produção nunca ganha
        duas tabelas que ninguém lê. É a leitura literal do achado que criou a
        fase. Menor superfície: uma dependência externa a menos (download direto
        de `bvmf.bmfbovespa.com.br`) e um par de endpoints admin a menos.
      </pros>
      <cons>
        Perde-se o trabalho do disco (do git nunca esteve lá). Se o mydata cair
        de vez no futuro, a ingestão própria teria que ser reescrita — embora o
        texto do ADR-019 fique como registro de como fazer.
      </cons>
    </option>
    <option id="commitar-e-remover">
      <name>Commitar como está e remover num commit seguinte</name>
      <pros>
        O código fica no histórico do git, recuperável por `git revert`. O
        ADR-019 fica commitado como decisão tomada de fato, não como rascunho.
        Rastro cronológico honesto do que aconteceu entre 25 e 27/08.
      </pros>
      <cons>
        Dois commits para chegar ao mesmo lugar. E o commit intermediário publica
        em `main` uma migração de banco (`CREATE TABLE b3_daily_imports` /
        `b3_daily_quotes`) que roda em TODO boot de `db.init_db` — se algum
        deploy pegar esse commit, as tabelas nascem no SQLite do Railway e ficam
        lá vazias para sempre, porque `init_db` não tem DROP. Sujeira permanente
        no banco por uma decisão já revertida.
      </cons>
    </option>
    <option id="congelar">
      <name>Manter o módulo, desligado e sem fiação</name>
      <pros>
        `b3_historical.py` e seu teste continuam no disco como plano B
        executável; só o hook do scheduler, as rotas admin, as tabelas e a
        entidade RBAC saem. Se o mydata falhar estruturalmente, religar é pequeno.
      </pros>
      <cons>
        Código morto com teste verde é a pior categoria de dívida: parece
        mantido, não é exercitado, e envelhece em silêncio contra um layout de
        arquivo (COTAHIST, 245 colunas de posição fixa) que ninguém monitora.
        Contradiz o próprio motivo da fase, e o teste verde dá falsa confiança de
        que o caminho funciona.
      </cons>
    </option>
  </options>

  <recommendation>
    `reverter`. O código nunca foi commitado nem publicado, então "perder" é
    perder um arquivo do disco cujo raciocínio está integralmente preservado no
    texto do ADR-019 e na nota de centralização — e o ADR-020 da Task 3 registra
    por que ele existiu e por que saiu. `commitar-e-remover` tem custo
    assimétrico e permanente (tabelas órfãs no SQLite de produção, sem caminho de
    DROP) por um ganho de rastro que a ADR-020 já dá. `congelar` entrega o pior
    dos dois: mantém a manutenção sem entregar o uso.
  </recommendation>

  <acceptance_criteria>
    - A saída de `git status --short` e `git diff --stat` dos 4 arquivos foi apresentada LITERALMENTE ao Alex, não parafraseada.
    - Qualquer divergência em relação ao estado descrito em `<assumptions>` foi sinalizada antes da pergunta.
    - Nenhum arquivo foi criado, removido, editado ou commitado durante esta task: `git status --short` imediatamente após é idêntico ao de antes.
    - A resposta do Alex foi registrada literalmente (`reverter` | `commitar-e-remover` | `congelar` | outra), e a Task 2 só começa depois dela.
  </acceptance_criteria>

  <resume-signal>Responda: `reverter`, `commitar-e-remover`, `congelar` — ou descreva outra saída.</resume-signal>

  <done>
    O Alex escolheu uma das saídas (ou descreveu outra), com o `git status` real
    na mesa, e a escolha está registrada literalmente. Nenhum arquivo foi tocado
    nesta task.
  </done>
</task>

<task type="auto">
  <name>Task 2: Executar a decisão e desfiar o scheduler</name>

  <files>server/app/b3_historical.py, server/tests/test_b3_historical.py, server/app/agent.py, server/app/db.py, server/app/main.py, server/app/rbac.py, docs/adr/019-cotahist-diario-b3.md</files>

  <read_first>
    - A resposta do Alex na Task 1 (obrigatório — não executar nada antes dela)
    - server/app/agent.py (linhas 1145-1210) — o bloco de hooks do `scheduler_loop`, para remover SÓ o do `b3_historical` sem encostar nos vizinhos (analytics, automacao, obs-metricas, fundamentals.maybe_warm, signal_ledger_job)
    - server/app/db.py (linhas 140-190) — as duas tabelas adicionadas e o que vem antes/depois delas em `init_db`
    - server/app/main.py (linhas 40-47 e 710-762) — o import e as duas rotas admin
    - server/app/rbac.py (linhas 85-95) — o dicionário `ENTIDADES_POR_PERMISSAO`
  </read_first>

  <action>
    Executar a saída escolhida pelo Alex. Em TODAS as saídas, a fiação abaixo é
    desfeita — é ela que faz o servidor baixar arquivo da B3, e é ela que a fase
    aposenta:

    **a) `server/app/agent.py`** — remover o bloco `try/except` que faz
    `from . import b3_historical` e `await b3_historical.maybe_run(conn)`
    (inserido logo depois do hook `[obs-metricas]`, ~8 linhas). Os hooks vizinhos
    ficam intactos.

    **b) `server/app/main.py`** — remover a linha de import
    `from . import b3_historical` e as duas rotas `GET /api/admin/b3/cotahist` e
    `POST /api/admin/b3/cotahist/import` (funções `admin_b3_cotahist_get` e
    `admin_b3_cotahist_import`, ~46 linhas contíguas entre `admin_summary` e o
    bloco de comentário da central de administração do ADR-013). Não encostar em
    `admin_summary` nem no bloco seguinte.

    **c) `server/app/db.py`** — remover os dois `CREATE TABLE IF NOT EXISTS`
    (`b3_daily_imports`, `b3_daily_quotes`) e seus dois `CREATE INDEX`.
    **Não** acrescentar `DROP TABLE`: o `init_db` deste repo é aditivo e
    idempotente por desenho, e como estas tabelas nunca foram criadas em
    produção (ver `<assumptions>`), não há nada a derrubar. Se a Task 1 revelar
    que elas JÁ EXISTEM em algum banco real, PARE e reporte antes de decidir
    sobre DROP — apagar tabela é ação destrutiva e exige confirmação humana
    própria.

    **d) `server/app/rbac.py`** — reverter
    `"fontes_dados.configurar": {"brapi_spot_intervalo", "b3_daily_import"}`
    para `{"brapi_spot_intervalo"}`. Verificar se algum teste de RBAC assere o
    conteúdo desse conjunto e ajustá-lo junto, se houver.

    **e) `server/app/b3_historical.py` e `server/tests/test_b3_historical.py`** —
    conforme a decisão:
      - `reverter`: apagar os dois arquivos do disco (`rm`, não `git rm` — são
        untracked). **NÃO apagar `docs/adr/019-cotahist-diario-b3.md`**: o texto
        da ADR é o registro da decisão da época, o ADR-020 vai referenciá-lo, e
        ele é COMMITADO nesta fase com o cabeçalho de status do item (f).
      - `commitar-e-remover`: primeiro commit com tudo como está hoje (mensagem
        citando ADR-019); segundo commit removendo a fiação (a→d) e os dois
        arquivos. Ao executar, atenção ao aviso do próprio `<options>`: o commit
        intermediário publica migração de banco.
      - `congelar`: manter os dois arquivos, desfazer só a fiação (a→d), e
        acrescentar no topo de `b3_historical.py` um bloco de comentário dizendo
        em que data e por qual decisão ele saiu do caminho ativo, apontando para
        `docs/adr/020`. No topo de `test_b3_historical.py`, a nota que o
        guardrail do repositório exige ("reversão deliberada atualiza o guardião
        com nota").

    **f) `docs/adr/019-cotahist-diario-b3.md`** — em TODAS as saídas: acrescentar
    um cabeçalho de status logo abaixo do título, no formato exato
    `**Status:** Superada parcialmente pela ADR-020 (AAAA-MM-DD) — a fatia diária passa a vir do mydata.`
    **Não alterar uma linha do texto original abaixo dele.** O repositório não
    reescreve o registro do que se pensava na época; ele carimba.

    Ao final, provar por grep que o servidor não tem mais caminho para baixar
    COTAHIST da B3 por conta própria.
  </action>

  <verify>
    <automated>cd server &amp;&amp; python -c "from app import main" &amp;&amp; python -m pytest tests/ -q</automated>
  </verify>

  <acceptance_criteria>
    - `cd server && python -c "from app import main"` importa sem erro.
    - `cd server && python -m pytest tests/ -q` sai com código 0.
    - `bash scripts/executar.sh --testes` sai com código 0 (suíte canônica COMPLETA: pytest + `web/tests/*.mjs`).
    - `grep -c 'b3_historical' server/app/agent.py` retorna 0.
    - `grep -c 'b3_historical' server/app/main.py` retorna 0.
    - `grep -cE 'b3_daily_quotes|b3_daily_imports' server/app/db.py` retorna 0.
    - `grep -c 'b3_daily_import' server/app/rbac.py` retorna 0.
    - Nenhum download direto da B3 sobrou: na saída `reverter`, `grep -rlE 'bvmf.bmfbovespa.com.br|COTAHIST_D' server/app/` não imprime nada; nas saídas `congelar`/`commitar-e-remover`, imprime SOMENTE `server/app/b3_historical.py`.
    - Os hooks vizinhos do scheduler continuam lá: `grep -cE 'analytics|maybe_warm|signal_ledger' server/app/agent.py` retorna >= 3.
    - `docs/adr/019-cotahist-diario-b3.md` existe e contém `Superada parcialmente pela ADR-020`, com o corpo original intacto — provar comparando `wc -l` antes/depois: diferença de no máximo 2 linhas.
    - Na saída `reverter`: `test ! -f server/app/b3_historical.py && test ! -f server/tests/test_b3_historical.py && echo OK` imprime `OK`.
    - Na saída `congelar`: `grep -c 'ADR-020' server/tests/test_b3_historical.py` retorna >= 1 (o guardião ganhou a nota exigida pelo guardrail do repositório).
  </acceptance_criteria>

  <done>
    Nenhum caminho de código no servidor baixa COTAHIST da B3 por conta própria;
    a fiação em `agent.py`, `main.py`, `db.py` e `rbac.py` foi desfeita sem
    encostar nos vizinhos; o ADR-019 ficou carimbado, não reescrito; e a suíte
    canônica está verde.
  </done>
</task>

<task type="auto">
  <name>Task 3: ADR-020 — o que foi superado, e o que deliberadamente não foi</name>

  <files>docs/adr/020-centralizacao-de-dados-no-mydata.md</files>

  <read_first>
    - docs/adr/019-cotahist-diario-b3.md — formato e tom das ADRs deste repo
    - docs/adr/008-fonte-de-cotacoes-selecionavel.md — o escopo de brapi que esta fase reduz
    - docs/adr/001-fonte-de-dados-intraday.md — o que NÃO é tocado
    - docs/adr/004-fonte-de-opcoes-na-v2.md — o contrato que NÃO é reaberto
    - .planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-CONTEXT.md (seção `<canonical_refs>`)
    - Os SUMMARYs de 09-01 a 09-04 — decisões e números REAIS para citar (nada de número inventado nesta ADR)
    - docs/MEDICAO-Mydata-*.md — a medição do Plano 09-04
  </read_first>

  <action>
    Escrever `docs/adr/020-centralizacao-de-dados-no-mydata.md` no formato das
    ADRs existentes do repo. Seções obrigatórias:

    **Contexto** — o achado literal: em 25/08 o Boris reimplementou em paralelo a
    ingestão do COTAHIST que o hub `mydata.acamerini.app` (`~/dev/cvm-financas`)
    já rodava em produção; duas capturas do mesmo arquivo oficial, com
    proveniências separadas. Citar a auditoria do lado cvm-financas (26/08) e a
    nota `.planning/notes/boris-pp-centralizacao-dados-mydata.md`.

    **Decisão** — as quatro decisões travadas do 09-CONTEXT.md, cada uma com o ID
    original para rastro:
      - D-01: `MydataProvider` atrás da interface `CandleProvider`, reusando o
        cache L2 e todos os call sites atuais.
      - D-02: `options_provider_mydata` atrás do mesmo contrato `providerStatus`.
      - D-03: candles degradam mydata→brapi→Yahoo, priorizando disponibilidade
        sobre pureza de proveniência única; `candle_cache` grava `src` por registro.
      - D-04: opções NÃO caem no Yahoo — falha vai direto a
        `providerStatus="degraded"`, porque o Yahoo é exatamente a fonte instável
        (401/403/429) que a migração elimina.
    Mais as decisões que o planejamento resolveu e que precisam de registro:
      - refresh diário é PASS-THROUGH pelo `candle_cache` existente, não job em
        lote — nenhum hook novo no scheduler;
      - IV e gregas vêm pré-calculadas do hub; nenhum recálculo local redundante;
      - `openInterest` não tem fonte no COTAHIST — registrar o efeito MEDIDO no
        `liquidity_score` (número que veio do 09-03-SUMMARY, não estimativa);
      - o provedor de opções passa a ser selecionável por env
        (`B3_OPTIONS_PROVIDER`) como ALAVANCA DE ROLLBACK, não como fallback
        automático;
      - o fallback de candles deixou de ser um salto só e virou cadeia
        (`B3_CANDLE_FALLBACK` aceita lista separada por vírgula).

    **Status das ADRs anteriores** — uma tabela, sem ambiguidade:

    | ADR | O que era | Status após esta |
    |---|---|---|
    | 001 — intraday Yahoo 15min | Yahoo dono do intraday | **INTOCADA.** O COTAHIST só publica após o fechamento do pregão; a fatia intraday não tem substituto no mydata |
    | 004 — fonte de opções na v2 | contrato `providerStatus` | **NÃO REABERTA.** Só a implementação por trás mudou |
    | 008 — brapi master de diário/spot | brapi master das duas fatias | **ESCOPO REDUZIDO** a spot ao vivo. O diário passa ao mydata; a brapi vira o primeiro elo de fallback |
    | 019 — COTAHIST diário próprio | ingestão própria do ZIP da B3 | **SUPERADA na fatia diária.** Registrar aqui a decisão da Task 1 sobre o destino do código |

    **Consequências** — o que se ganha (uma proveniência só, `sha256`/`arquivo`
    até o arquivo oficial em toda linha, IV e gregas prontas, uma superfície de
    ingestão a menos) e o que se paga (dependência de um serviço externo com cota
    de 60/min · 2.000/dia; perda dos campos crus do COTAHIST que o
    `gold_cotacoes` não expõe; `openInterest` ausente nas opções).

    **Medição** — os números do Plano 09-04, com link para
    `docs/MEDICAO-Mydata-<data>.md`: pico chamadas/min, total chamadas/dia,
    veredito e `intervaloMinimoSeguro`. Uma ADR deste repo não afirma
    suficiência de cota sem número — é a mesma postura de `ADR-008` e de
    `docs/MEDICAO-Brapi-2026-08-11.md`.

    **Reversibilidade** — como voltar: `B3_CANDLE_PROVIDER=brapi` e
    `B3_OPTIONS_PROVIDER=yahoo` restauram o comportamento anterior sem deploy de
    código. Deixar isso explícito é o que torna a migração barata de desfazer.
  </action>

  <verify>
    <automated>test -f docs/adr/020-centralizacao-de-dados-no-mydata.md &amp;&amp; grep -q 'ADR-019' docs/adr/020-centralizacao-de-dados-no-mydata.md &amp;&amp; grep -q 'B3_CANDLE_PROVIDER' docs/adr/020-centralizacao-de-dados-no-mydata.md &amp;&amp; echo OK</automated>
  </verify>

  <acceptance_criteria>
    - `docs/adr/020-centralizacao-de-dados-no-mydata.md` existe.
    - Contém as 6 seções: `grep -ciE '^#+ *(Contexto|Decisão|Status das ADRs|Consequências|Medição|Reversibilidade)' docs/adr/020-centralizacao-de-dados-no-mydata.md` retorna 6.
    - As quatro decisões travadas estão rastreadas por ID: `grep -cE 'D-0[1234]' docs/adr/020-centralizacao-de-dados-no-mydata.md` retorna >= 4.
    - As quatro ADRs anteriores aparecem na tabela de status: `for a in 001 004 008 019; do grep -q "ADR-$a\|| $a " docs/adr/020-centralizacao-de-dados-no-mydata.md || echo "FALTA ADR-$a"; done` não imprime nada.
    - A seção Medição cita números concretos e o link do documento: `grep -cE 'MEDICAO-Mydata' docs/adr/020-centralizacao-de-dados-no-mydata.md` retorna >= 1 e não há `TBD`/`XXX` no arquivo.
    - A reversibilidade é literal: `grep -c 'B3_OPTIONS_PROVIDER=yahoo' docs/adr/020-centralizacao-de-dados-no-mydata.md` retorna >= 1.
    - Nenhuma chave completa no arquivo: `grep -cE '[a-f0-9]{32,}' docs/adr/020-centralizacao-de-dados-no-mydata.md` retorna 0.
  </acceptance_criteria>

  <done>
    Existe uma ADR que diz o que foi superado (019 na fatia diária, 008 no
    escopo), o que NÃO foi (001 e 004), com que números a decisão foi tomada, e
    como desfazer por env sem deploy.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor → árvore de trabalho não commitada | 91 linhas de diff e 3 arquivos untracked de outra sessão de trabalho podem ser destruídos por um passo em falso |
| Executor → SQLite de produção | Remoção de `CREATE TABLE` em `init_db` beira a fronteira de dado persistido |
| `docs/adr/` → registro histórico do projeto | Apagar texto de ADR destrói o raciocínio da época |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-21 | Tampering (destruição de trabalho) | diffs não commitados de `b3_historical` | mitigate | Task 1 é `checkpoint:decision` BLOQUEANTE e obriga a reconfirmar `git status` antes de apresentar as opções; nenhuma remoção acontece antes da resposta do Alex; divergência do estado esperado obriga a recalibrar em vez de seguir o roteiro |
| T-09-22 | Tampering (dado persistido) | remoção dos `CREATE TABLE` em `db.init_db` | mitigate | A ação PROÍBE explicitamente acrescentar `DROP TABLE`, e manda PARAR e reportar se as tabelas existirem em algum banco real. Remover o `CREATE` de um `init_db` idempotente não apaga dado nenhum |
| T-09-23 | Repudiation | apagar o registro da decisão de 25/08 | mitigate | ADR-019 é preservado em todas as saídas, apenas carimbado com uma linha de status; critério de aceite mede a diferença de linhas (máximo 2) para provar que nada foi reescrito |
| T-09-24 | Denial of Service | remover o hook errado do `scheduler_loop` e derrubar radar/ordens | mitigate | A ação nomeia o bloco exato pelo vizinho anterior (`[obs-metricas]`) e o critério de aceite verifica que os hooks vizinhos (analytics, `maybe_warm`, `signal_ledger`) continuam presentes; `pytest tests/` completo roda depois |
| T-09-25 | Elevation of Privilege | reverter `rbac.py` de forma a alargar permissão | mitigate | A reversão REMOVE uma entidade do conjunto de `fontes_dados.configurar` — reduz superfície, não amplia. Critério de aceite grepa por `b3_daily_import` em `rbac.py` |
| T-09-SC | Tampering | instalação de pacote | mitigate | Nenhum pacote novo. Se o executor concluir que precisa de dependência nova, PARE e abra checkpoint humano bloqueante |
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` verde (as DUAS suítes).
- `cd server && python -c "from app import main"` sem erro.
- `git status --short` mostra um estado coerente com a decisão do Alex e nenhum arquivo de outra sessão destruído por acidente.
- `grep -rn 'b3_historical' server/` só retorna linhas dentro do próprio módulo (saídas `congelar`/`commitar-e-remover`) ou nada (saída `reverter`).
- **Regra de processo desta fase (lição do incidente da Fase 8, ver STATE.md):** esta fase tem checkpoint humano bloqueante. Nenhum `git push` de NENHUMA wave sai antes da aprovação do checkpoint do Plano 09-06, mesmo que o commit de uma wave pareça inócuo isolado.
</verification>

<success_criteria>
- A decisão sobre o código não commitado foi do Alex, com o estado real do
  `git status` na mesa.
- Nenhum caminho no servidor baixa COTAHIST da B3 por conta própria.
- ADR-019 carimbado, não reescrito; ADR-020 escrita com números reais.
- Suíte canônica verde.
</success_criteria>

<output>
Create `.planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-05-SUMMARY.md` when done.
Registre no SUMMARY: (a) a saída escolhida pelo Alex e a justificativa dele, se
houver; (b) o `git status --short` encontrado na Task 1 comparado ao esperado;
(c) confirmação de que o ADR-019 teve só a linha de status acrescentada.
</output>
