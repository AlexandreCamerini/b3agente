---
phase: 18-aba-opcoes
plan: 04
type: execute
wave: 4
depends_on: ["18-03"]
files_modified:
  - web/tests/test_carteira_opcoes_tira.mjs
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03]

must_haves:
  truths:
    - "Existe um guardião que quebra se a manchete da tira voltar a ser composta no front"
    - "Existe um guardião que quebra se a tira voltar a aparecer com carteira vazia"
    - "Existe um guardião que quebra se o estado vazio da tira for removido (volta ao silêncio)"
    - "Existe um guardião que quebra se a busca de proposta deixar de ser best-effort"
    - "A suíte canônica inteira (pytest + web/tests) fica verde"
  artifacts:
    - path: "web/tests/test_carteira_opcoes_tira.mjs"
      provides: "guardião estático da tira de oportunidades e do detalhe em Posições"
      min_lines: 60
      contains: "OportunidadesOpcoes"
  key_links:
    - from: "web/tests/test_carteira_opcoes_tira.mjs"
      to: "web/src/App.jsx"
      via: "readFileSync + asserções de regex sobre o fonte"
      pattern: "readFileSync"
    - from: "web/tests/test_carteira_opcoes_tira.mjs"
      to: "web/src/copy.js"
      via: "import { COPY }"
      pattern: "import \\{ COPY \\}"
---

<objective>
Trancar a CLASSE dos erros que esta fase pode reintroduzir, com um guardião
estático novo no padrão da casa, e provar que a suíte canônica inteira
(pytest do backend + `web/tests/*.mjs`) segue verde com a tira no ar.

Purpose: as regras que esta fase depende — manchete só do motor, estado vazio
explícito na tira, silêncio deliberado no card individual, busca best-effort,
"nunca estrutura sobre ticker sem posição real" — são invisíveis para quem
editar `App.jsx` daqui a três meses. O guardião é o que impede a regressão sem
exigir que o autor conheça o histórico.

Output: `web/tests/test_carteira_opcoes_tira.mjs` (recolhido automaticamente
pelo laço `for t in web/tests/*.mjs` de `scripts/executar.sh:33`) e a suíte
canônica verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-aba-opcoes/18-CONTEXT.md
@.planning/phases/18-aba-opcoes/18-PATTERNS.md
@.planning/phases/18-aba-opcoes/18-01-SUMMARY.md
@.planning/phases/18-aba-opcoes/18-02-SUMMARY.md
@.planning/phases/18-aba-opcoes/18-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Identificadores que o guardião assere. Vieram dos Planos 18-01/02/03. -->
- `web/src/copy.js`: `tiraOpcoesTitulo`, `tiraOpcoesVerDetalhe`,
  `tiraOpcoesCarregando`, `tiraOpcoesSemCobertura`, `tiraOpcoesSemSetup`,
  `linhaPropostaNaPosicao` — nos dois ramos.
- `web/src/App.jsx`: `function useOpcoesPropostas`, `function OportunidadesOpcoes`,
  `function PropostaDaPosicao`, `<OportunidadesOpcoes`, `<PropostaDaPosicao`,
  `id={"posicao-" + p.t}`, `abrirOpcoesDe`, `opcoesFor`.

Suíte canônica (CLAUDE.md — as DUAS suítes; `scripts/test.sh` sozinho é meia
baseline e não conta como validação): `bash scripts/executar.sh --testes`.
O laço `for t in web/tests/*.mjs` (scripts/executar.sh:33) recolhe o arquivo
novo automaticamente — não há registro manual a fazer.
</interfaces>

<convencao_de_guardiao>
Padrão "static source inspection" da casa (ver `test_opcoes_proposta_ui.mjs`,
`test_carteira_lastro_ui.mjs`, `test_fase5_appmode_fonte_unica.mjs`): roda sem
build e sem DOM — `readFileSync` de `web/src/App.jsx` + `import { COPY }` de
`web/src/copy.js`, um helper `ok(nome, cond)` que conta falhas, e
`process.exit(1)` no fim se houver falha.

Higiene de contagem (regra do repositório): `grep`/`match` sobre o fonte conta
COMENTÁRIO também. Asserção que conte ocorrências precisa filtrar linhas que
começam com `//` antes de contar — senão o próprio comentário explicativo do
guardião o auto-invalida (o precedente está em
`test_fase5_appmode_fonte_unica.mjs:38-42`).

Guardião não se apaga: este arquivo é aditivo. Nenhuma asserção de
`test_opcoes_proposta_ui.mjs`, `test_carteira_lastro_ui.mjs` ou
`test_opcoes_collar_ui.mjs` pode ser removida ou enfraquecida para fazer a
suíte passar.
</convencao_de_guardiao>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guardião estático da tira de oportunidades e do detalhe em Posições</name>
  <read_first>
    - web/tests/test_opcoes_proposta_ui.mjs (arquivo inteiro — o padrão exato de helper, de fatiamento por índice entre âncoras e de asserção negativa)
    - web/tests/test_carteira_lastro_ui.mjs (arquivo inteiro — o padrão de asserção sobre `CarteiraScreen` e sobre chaves de copy nos dois ramos)
    - web/tests/test_fase5_appmode_fonte_unica.mjs linhas 30-50 (filtro de linhas de comentário antes de contar — higiene obrigatória)
    - web/src/App.jsx (os três blocos novos: `useOpcoesPropostas`, `OportunidadesOpcoes`, `PropostaDaPosicao`, e a montagem em `CarteiraScreen`)
    - web/src/copy.js (as seis chaves novas)
  </read_first>
  <files>web/tests/test_carteira_opcoes_tira.mjs</files>
  <action>
Criar `web/tests/test_carteira_opcoes_tira.mjs`, guardião estático da Fase 18.

Cabeçalho: comentário no padrão da casa explicando o que está sendo trancado e
POR QUÊ (a razão, não a implementação): manchete só do motor (guardrail CVM),
estado vazio agregado obrigatório × silêncio deliberado do card individual
(ADR-004, App.jsx:3484-3489), busca best-effort, uma busca por ticker servindo
as duas superfícies, e "estrutura só sobre posição real". Registrar a linha de
execução isolada: `node web/tests/test_carteira_opcoes_tira.mjs`.

Preâmbulo idêntico ao dos irmãos: `readFileSync` de `../src/App.jsx`,
`import { COPY } from "../src/copy.js"`, helper `ok`, e o filtro de linhas de
comentário (`fonteSemComentario`) para as asserções que CONTAM ocorrências.

Asserções obrigatórias:

**(1) Copy nos dois ramos.** As seis chaves
(`tiraOpcoesTitulo`, `tiraOpcoesVerDetalhe`, `tiraOpcoesCarregando`,
`tiraOpcoesSemCobertura`, `tiraOpcoesSemSetup`, `linhaPropostaNaPosicao`)
existem em `COPY.estudo` E em `COPY.operador`; todas são `typeof === "string"`;
`tiraOpcoesSemCobertura` e `tiraOpcoesSemSetup` DIFEREM entre os dois ramos
(voz de professor × voz de mesa realmente diferenciada);
`tiraOpcoesSemCobertura !== tiraOpcoesSemSetup` dentro de cada ramo (os dois
motivos de NAV-03 são de fato mensagens distintas, não a mesma frase duplicada).

**(2) Guardrail CVM na tira.** Fatiar o bloco de `OportunidadesOpcoes` por
índice (de `app.indexOf("function OportunidadesOpcoes")` até o índice da
próxima definição de função — `function PropostaDaPosicao`) e, sobre essa
fatia: contém `.manchete}` (render direto); NÃO contém template string nem
concatenação envolvendo manchete (asserções negativas: nada que case
`` /`[^`]*\$\{[^}]*manchete/ `` nem `/manchete[^}]*\+/`); NÃO contém
`"Vender " +`; a linha que renderiza a manchete não contém `T.accent`.

**(3) Estado vazio de NAV-03 presente e explícito.** A fatia de
`OportunidadesOpcoes` referencia `cp.tiraOpcoesSemCobertura` E
`cp.tiraOpcoesSemSetup` E `cp.tiraOpcoesCarregando`. Asserção estrutural: o
índice de `cp.tiraOpcoesCarregando` na fatia é MENOR que o índice de
`cp.tiraOpcoesSemCobertura` — o ramo de carregamento é avaliado antes do
vazio, senão a tira mente durante a busca.

**(4) Tira ausente com carteira vazia.** No fonte, `<OportunidadesOpcoes`
aparece DEPOIS de `function CarteiraScreen(` e ANTES de
`{data.positions.length === 0 && (`; e os 120 caracteres imediatamente
anteriores a `<OportunidadesOpcoes` contêm `data.positions.length > 0`.

**(5) Estrutura só sobre posição real.** A chamada de `useOpcoesPropostas(`
dentro de `CarteiraScreen` recebe `data.positions.map(` como argumento —
asserção positiva sobre a string da chamada; e asserção negativa: a chamada
não menciona `watchlist` nem `radar`.

**(6) Best-effort preservado.** Fatiar o bloco de `useOpcoesPropostas` e
assertar que toda ocorrência de `store.optionsGate(` e de
`store.optionsProposta(` na fatia é seguida, em até 200 caracteres, de
`.catch(`. Contar sobre a fatia SEM linhas de comentário.

**(7) Uma busca por ticker, não duas.** Contar, sobre `fonteSemComentario`,
as ocorrências de `store.optionsGate(`: precisa ser exatamente 2 (a de
`AtivoCard`, que serve Watchlist/Radar, e a do hook, que serve Posições). O
mesmo para `store.optionsProposta(`: exatamente 2. Uma terceira ocorrência
significa que alguém reintroduziu fetch duplicado na mesma tela.

**(8) Silêncio deliberado do card individual.** A fatia de
`PropostaDaPosicao` contém uma guarda de retorno `null` quando não há
proposta — assertar que a fatia case `/if \(!r \|\| !r\.proposta\) return null;/`
(ou a forma equivalente literalmente escrita pelo Plano 18-02, verificada no
fonte antes de escrever a regex). A fatia NÃO contém `cp.tiraOpcoesSemCobertura`
nem `cp.tiraOpcoesSemSetup` — o estado vazio agregado não pode vazar para
dentro do card por posição.

**(9) Estado por ticker no padrão da casa.** `CarteiraScreen` declara
`const [opcoesFor, setOpcoesFor] = useState(null);` e o laço de posições lê
`opcoesFor === p.t` — a mesma forma de `histFor`/`editFor`, cujos usos
continuam presentes (assertar que `histFor === p.t` e `editFor === p.t`
seguem no fonte: o padrão foi copiado, não substituído).

**(10) Âncora de scroll ligada.** O fonte contém `id={"posicao-" + p.t}` e
contém `getElementById("posicao-" + t)` com `scrollIntoView(`; e
`abrirOpcoesDe` chama `setOpcoesFor(`.

**(11) Assinatura de PropostaLastreada intocada.** Reassertar
`/function PropostaLastreada\(\{ r, operador, cp, busy, onAbrir, onFechar, posAberta \}\)/`
e que existem agora DUAS ocorrências de `<PropostaLastreada` no fonte sem
comentários (a de `AtivoCard` e a de `PropostaDaPosicao`) — prova de que a
Fase 18 ADICIONOU um ponto de renderização em vez de mover o existente.

Fechamento no padrão: `if (fails) { console.error(...); process.exit(1); }`
seguido de um `console.log` de sucesso.

Antes de escrever qualquer regex, LER o trecho correspondente de
`web/src/App.jsx` e casar a asserção com o texto que realmente está lá — regex
escrita de memória contra código que não foi lido produz guardião falso-verde
ou falso-vermelho.
  </action>
  <verify>
    <automated>node web/tests/test_carteira_opcoes_tira.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node web/tests/test_carteira_opcoes_tira.mjs` sai com código 0 e imprime uma linha `ok ` por asserção.
    - O arquivo contém pelo menos 11 chamadas de `ok(` (uma por bloco de asserção acima).
    - O arquivo importa `COPY` de `../src/copy.js` e lê `../src/App.jsx` com `readFileSync`.
    - O arquivo NÃO importa nada de `web/dist` nem depende de build/DOM (roda com `node` puro).
    - Prova de mordida (executar e reverter, registrando o resultado no SUMMARY): trocar temporariamente `{pr.manchete}` por uma string composta em `OportunidadesOpcoes` faz o guardião FALHAR; reverter faz voltar a passar. O mesmo para remover a guarda `data.positions.length > 0` da montagem da tira.
    - Nenhuma linha foi removida ou enfraquecida em `web/tests/test_opcoes_proposta_ui.mjs`, `web/tests/test_carteira_lastro_ui.mjs` ou `web/tests/test_opcoes_collar_ui.mjs` — `git diff --stat web/tests/` mostra apenas o arquivo novo.
  </acceptance_criteria>
  <done>O guardião existe, passa, e falha de verdade quando as regras da fase são violadas (prova de mordida registrada).</done>
</task>

<task type="auto">
  <name>Task 2: Suíte canônica completa verde</name>
  <read_first>
    - CLAUDE.md (seção "Validação obrigatória" — as DUAS suítes; `scripts/test.sh` sozinho não conta)
    - scripts/executar.sh (linhas em volta de 33 — o laço que recolhe `web/tests/*.mjs`)
  </read_first>
  <files>web/tests/test_carteira_opcoes_tira.mjs</files>
  <action>
Rodar a suíte canônica inteira e deixá-la verde:

1. `cd web && npx vite build` — obrigatório antes de declarar qualquer coisa
   pronta quando `web/src/` foi editado (guardrail do CLAUDE.md: grep e teste
   estático não pegam erro de sintaxe JS/JSX).
2. `bash scripts/executar.sh --testes` — pytest do backend + todos os
   `web/tests/*.mjs`, incluindo o guardião novo.

Se algum teste EXISTENTE falhar, a correção é no código da Fase 18, não no
teste. Enfraquecer ou apagar guardião é proibido pelo CLAUDE.md; a única
exceção é reversão deliberada de decisão, que exigiria atualizar o guardião
COM nota explicando a reversão — o que não é o caso desta fase (ela é
puramente aditiva sobre Watchlist/Radar).

Se a falha for de dependência ausente no worktree (`web/node_modules`
inexistente — precedente registrado no SUMMARY do Plano 17-04), rodar
`npm install` em `web/` uma vez: instalação das dependências JÁ declaradas em
`package.json`/`package-lock.json`, nenhum pacote novo. Confirmar depois que
`git diff --stat web/package.json web/package-lock.json` sai vazio.

Registrar no SUMMARY o número de testes do backend e a contagem de arquivos
`.mjs` que passaram, para comparação com a próxima fase.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx vite build` conclui sem erro.
    - `bash scripts/executar.sh --testes` sai com código 0 — as DUAS suítes verdes (pytest + `web/tests/*.mjs`).
    - A saída inclui o guardião novo `test_carteira_opcoes_tira.mjs` entre os arquivos executados.
    - `git diff --stat web/package.json web/package-lock.json package.json package-lock.json` sai vazio (nenhum pacote novo).
    - `git status --porcelain server/web_dist` sai vazio — este plano NÃO publica; a publicação é o Plano 18-05.
    - `git diff --stat web/tests/` mostra exclusivamente o arquivo novo.
  </acceptance_criteria>
  <done>Build limpo e suíte canônica completa verde, com o guardião da Fase 18 incluído.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| autor futuro → invariantes da fase | o guardião é a única barreira entre uma edição desavisada em `App.jsx` e a quebra do guardrail CVM / do estado vazio de NAV-03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-12 | Tampering | guardiões existentes | mitigate | `git diff --stat web/tests/` precisa mostrar SÓ o arquivo novo; enfraquecer guardião para passar a suíte é proibido pelo CLAUDE.md |
| T-18-13 | Repudiation | guardião falso-verde (regex que nunca morde) | mitigate | Prova de mordida obrigatória: quebrar a manchete e a guarda de carteira vazia de propósito, confirmar a falha, reverter — resultado registrado no SUMMARY |
| T-18-14 | Tampering | contagem auto-invalidável por comentário | mitigate | Asserções que contam ocorrências filtram linhas iniciadas por `//` antes de contar (precedente `test_fase5_appmode_fonte_unica.mjs:38-42`) |
| T-18-SC | Tampering | npm installs | mitigate | Nenhum pacote novo; `npm install` só é permitido para materializar dependências já travadas no lockfile, com `git diff` dos manifestos vazio depois |
</threat_model>

<verification>
1. `node web/tests/test_carteira_opcoes_tira.mjs` — guardião novo verde.
2. Prova de mordida — o guardião falha quando a manchete é composta e quando a guarda de carteira vazia some.
3. `cd web && npx vite build` — build limpo.
4. `bash scripts/executar.sh --testes` — suíte canônica completa verde.
5. `git diff --stat web/tests/` mostra só o arquivo novo.
6. `git status --porcelain server/web_dist` vazio.
</verification>

<success_criteria>
- Guardião novo existe, passa e comprovadamente morde.
- Nenhum guardião existente enfraquecido.
- Suíte canônica (as duas) verde com build do Vite limpo.
</success_criteria>

<output>
Create `.planning/phases/18-aba-opcoes/18-04-SUMMARY.md` when done
</output>
