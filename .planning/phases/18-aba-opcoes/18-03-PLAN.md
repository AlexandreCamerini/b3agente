---
phase: 18-aba-opcoes
plan: 03
type: execute
wave: 3
depends_on: ["18-02"]
files_modified:
  - web/src/App.jsx
autonomous: true
requirements: [NAV-01, NAV-03]

must_haves:
  truths:
    - "No topo de Posições o usuário vê, de uma vez, todas as posições com proposta de opções ativa"
    - "Tocar um item da tira abre o detalhe da estrutura dentro do card daquela posição e leva o card para a vista"
    - "Quando nenhuma posição tem proposta ativa, a tira diz o motivo (sem cobertura líquida × sem setup técnico hoje) em vez de sumir"
    - "Enquanto as buscas estão em voo a tira diz que está procurando, nunca 'não há oportunidade'"
    - "Carteira sem nenhuma posição não mostra a tira (o estado vazio de portfólio já cobre esse caso)"
  artifacts:
    - path: "web/src/App.jsx"
      provides: "componente OportunidadesOpcoes — tira agregada + estados vazios explícitos"
      contains: "function OportunidadesOpcoes"
    - path: "web/src/App.jsx"
      provides: "montagem da tira em CarteiraScreen com guarda de carteira vazia e scroll-para-o-card"
      contains: "<OportunidadesOpcoes"
  key_links:
    - from: "web/src/App.jsx (OportunidadesOpcoes)"
      to: "opcoesFor / scrollIntoView"
      via: "onAbrir(ticker) do item da tira"
      pattern: "getElementById\\(\"posicao-\""
    - from: "web/src/App.jsx (OportunidadesOpcoes)"
      to: "proposta.manchete"
      via: "render direto, sem composição"
      pattern: "\\{pr\\.manchete\\}|\\{proposta\\.manchete\\}"
---

<objective>
NAV-01 + NAV-03: a tira "Oportunidades de opções" no topo de Posições/Portfólio
— um item por posição com proposta ativa, e um estado vazio EXPLÍCITO com o
motivo quando não há nenhuma.

Purpose: hoje a proposta de opções só é descoberta abrindo card por card. A
tira é a superfície de descoberta agregada: uma olhada mostra onde há estrutura
possível hoje. E, ao contrário do card individual (que cala deliberadamente,
ADR-004), aqui o silêncio é o erro — NAV-03 exige o motivo.

Output: componente `OportunidadesOpcoes` + montagem em `CarteiraScreen` acima
da lista de posições, com o toque no item abrindo `opcoesFor` (Plano 18-02) e
rolando até o card ancorado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-aba-opcoes/18-CONTEXT.md
@.planning/phases/18-aba-opcoes/18-PATTERNS.md
@.planning/phases/18-aba-opcoes/18-01-SUMMARY.md
@.planning/phases/18-aba-opcoes/18-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contratos que este plano consome. Não explorar o codebase para descobri-los. -->

Do Plano 18-01 (`web/src/App.jsx` e `web/src/copy.js`):
- `useOpcoesPropostas(tickers)` → `{ propostas, carregando }`;
  `propostas[ticker]` = `{ gate, proposta }`. `gate` é a resposta de
  `store.optionsGate` (`{ ticker, liquida, providerStatus }`). `proposta` é a
  resposta de `store.optionsProposta` (`{ proposta|null, motivo, motivoTexto, ... }`)
  ou `null`. Ticker ainda não respondido não tem chave.
- `cp.tiraOpcoesTitulo`, `cp.tiraOpcoesVerDetalhe`, `cp.tiraOpcoesCarregando`,
  `cp.tiraOpcoesSemCobertura`, `cp.tiraOpcoesSemSetup` (todas string, nos dois modos).

Do Plano 18-02 (já em `CarteiraScreen`):
- `const { propostas: opcoesPorTicker, carregando: opcoesCarregando } = useOpcoesPropostas(data.positions.map((p) => p.t));`
- `const [opcoesFor, setOpcoesFor] = useState(null);`
- cada card de posição tem `id={"posicao-" + p.t}`.

Rótulos de tipo de estrutura já existentes nos dois modos (reusar, NÃO criar
novos — App.jsx:3043-3044 mostra a escolha):
- `cp.eyebrowPropostaCollar` quando `proposta.tipo === "collar"`
- `cp.eyebrowPropostaCall` quando `proposta.optionType === "call"`
- `cp.eyebrowPropostaPut` caso contrário

Precedente de scroll já em produção (App.jsx:7444-7450), a replicar:
`setTimeout(..., ms)` → `document.getElementById(...)` → guarda `if (!el) return;`
→ `el.scrollIntoView({ behavior: "smooth", block: "center" })`.
</interfaces>

<guardrails>
- **Manchete (guardrail CVM, CLAUDE.md):** o resumo de cada item da tira
  renderiza `proposta.manchete` VERBATIM. Proibido compor frase nova a partir
  de `strike`/`contratos`/`optionType`/`premioTotal` no componente. O único
  texto que a tira acrescenta em volta é rótulo (`cp.eyebrow*`,
  `cp.tiraOpcoesVerDetalhe`) e o ticker.
- **Nenhum fetch novo:** `OportunidadesOpcoes` recebe `propostas`/`carregando`
  por prop. Uma chamada de `store.optionsGate`/`optionsProposta` dentro deste
  componente dobraria o tráfego que o hook do 18-01 existe para evitar.
- **Nenhum texto hardcodado:** todo texto da tira vem de `cp.*`.
- `test_opcoes_proposta_ui.mjs` exige que a PRIMEIRA ocorrência de
  `<PropostaLastreada` continue antes da primeira de `<OpcoesCamada`.
  `OportunidadesOpcoes` não renderiza nenhum dos dois — não há risco, desde
  que nenhuma definição de componente existente seja movida de lugar.
</guardrails>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Componente OportunidadesOpcoes — itens agregados e os três estados</name>
  <read_first>
    - web/src/App.jsx linhas 3036-3064 (`PropostaLastreada`: como `isCollar`/`isCall` escolhem o eyebrow e como `{p.manchete}` é renderizado direto — a mesma escolha que a tira replica)
    - web/src/App.jsx linhas 3484-3495 (comentário do silêncio deliberado do card individual — a razão que a tira INVERTE, e que não pode ser contradita no texto)
    - web/src/App.jsx linhas 3150-3167 (`OpcoesCamada`: padrão visual do bloco de opções — glifo ⚡, borda, `T.bgBase`, tamanhos de fonte)
    - web/src/copy.js (as cinco chaves `tiraOpcoes*` criadas no Plano 18-01)
    - web/tests/test_opcoes_proposta_ui.mjs linhas 47-62 (asserções de guardrail CVM que a tira precisa satisfazer no espírito e que o Plano 18-04 vai estender ao componente novo)
  </read_first>
  <files>web/src/App.jsx</files>
  <action>
Criar `function OportunidadesOpcoes({ propostas, carregando, positions, cp, onAbrir })`
em `web/src/App.jsx`, posicionada IMEDIATAMENTE ANTES de
`function PropostaDaPosicao` (criado no Plano 18-02) — ou seja, depois de
`AtivoCard` e antes do bloco de componentes de Posições. Não mover nenhuma
definição existente.

Lógica de derivação (pura, no topo do componente):

- `const itens = (positions || []).filter((p) => { const e = propostas[p.t]; return !!(e && e.gate && e.gate.liquida && e.proposta && e.proposta.proposta); });`
  — item só existe quando o gate aprovou E veio proposta CONCRETA (o ramo
  "sem proposta" de `PropostaLastreada` não vira item de tira).
- `const algumLiquido = (positions || []).some((p) => { const e = propostas[p.t]; return !!(e && e.gate && e.gate.liquida); });`
  — decide QUAL dos dois motivos de NAV-03 exibir.

Renderização, exatamente três estados mutuamente exclusivos, todos sob o mesmo
cabeçalho `cp.tiraOpcoesTitulo` (o título aparece nos três — a seção nunca
desaparece silenciosamente quando há posições):

1. `itens.length > 0` → lista horizontal rolável dos itens.
   Contêiner: `display:flex`, `gap`, `overflowX:"auto"`, com
   `WebkitOverflowScrolling:"touch"` e `scrollbarWidth:"none"` (o app roda em
   WKWebView no iPhone). Cada item é um `<button type="button">` (não `<div>`
   com onClick — precisa ser focável por teclado e anunciável por leitor de
   tela), com `aria-label` incluindo o ticker, `minHeight:"44px"` (alvo de
   toque) e `onClick={() => onAbrir(p.t)}`.
   Conteúdo de cada item, nesta ordem vertical:
   - eyebrow do tipo de estrutura: `pr.tipo === "collar" ? cp.eyebrowPropostaCollar : pr.optionType === "call" ? cp.eyebrowPropostaCall : cp.eyebrowPropostaPut` (mesma escolha de App.jsx:3043-3044), no estilo eyebrow da casa (10px/800, `letterSpacing`, `T.accent`);
   - ticker em `MONO`, peso 800;
   - `{pr.manchete}` renderizado DIRETO, sem interpolação, com cor por
     polaridade seguindo a mesma regra do card
     (`pr.optionType === "call" ? T.positive : T.negative`; nunca `T.accent`
     na linha da manchete);
   - `cp.tiraOpcoesVerDetalhe` como micro-rótulo de ação, `T.textFaint`.
   Onde `pr` é `propostas[p.t].proposta.proposta`.
   Item com largura mínima fixa (ex.: `minWidth:"210px"`) e
   `whiteSpace:"normal"` para a manchete não ser truncada — cortar a manchete
   com reticências reescreveria a afirmação do motor.

2. `itens.length === 0 && carregando` → uma linha de texto
   `cp.tiraOpcoesCarregando` em `T.textFaint`. Esse ramo vem ANTES dos vazios:
   sem ele a tira pisca "não há oportunidade" durante a busca e mente.

3. `itens.length === 0 && !carregando` → estado vazio explícito (NAV-03):
   `algumLiquido ? cp.tiraOpcoesSemSetup : cp.tiraOpcoesSemCobertura`,
   em caixa discreta (`T.bgCard`, borda `T.borderFaint`, 11.5-12px,
   `lineHeight:1.5`), sem botão e sem CTA — é ESTADO, não ação (mesmo
   precedente de `AvisoLiquidacao`, cujo guardião proíbe `<button` no bloco).

O componente NÃO decide se aparece com carteira vazia — isso é guarda do
chamador (Task 2). Não adicionar `positions.length === 0` como quarto ramo:
duas guardas para a mesma condição divergem com o tempo.

Comentário de cabeçalho no padrão da casa: citar Fase 18 (Plano 03,
NAV-01/NAV-03); registrar explicitamente que aqui o estado vazio é
OBRIGATÓRIO e por quê — o card individual cala de propósito (App.jsx:3484-3489,
ADR-004: evitar "seis avisos idênticos por tela"), mas a tira é a superfície
agregada e some uma vez só, então sumir sem explicação seria o usuário
concluindo que o produto quebrou. Registrar também que a manchete é sempre a
do motor (guardrail CVM) e que nenhum fetch acontece aqui.
  </action>
  <verify>
    <automated>cd web && npx vite build && node tests/test_opcoes_proposta_ui.mjs && node tests/test_copy_theme.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx vite build` conclui sem erro.
    - `grep -c "function OportunidadesOpcoes" web/src/App.jsx` retorna 1.
    - A definição está entre `function AtivoCard(` e `function PropostaDaPosicao`: `node -e "const s=require('fs').readFileSync('web/src/App.jsx','utf8');const a=s.indexOf('function AtivoCard(');const o=s.indexOf('function OportunidadesOpcoes');const d=s.indexOf('function PropostaDaPosicao');process.exit(a<o&&o<d?0:1)"`
    - O corpo do componente NÃO contém `store.optionsGate` nem `store.optionsProposta`.
    - O corpo contém as cinco chaves de copy: `cp.tiraOpcoesTitulo`, `cp.tiraOpcoesCarregando`, `cp.tiraOpcoesSemCobertura`, `cp.tiraOpcoesSemSetup`, `cp.tiraOpcoesVerDetalhe`.
    - O corpo reusa `cp.eyebrowPropostaCall`, `cp.eyebrowPropostaPut` e `cp.eyebrowPropostaCollar` — nenhum rótulo novo de tipo de estrutura foi inventado.
    - A manchete é renderizada direto: o corpo contém `.manchete}` e NÃO contém nenhuma concatenação de manchete (nenhum `"Vender " +`, nenhum `` `${ `` envolvendo `manchete`).
    - A linha que renderiza a manchete não usa `T.accent`.
    - Cada item é um elemento `<button` com `aria-label` — o corpo NÃO usa `<div onClick=` para o item clicável.
    - O ramo de carregamento é avaliado antes dos ramos vazios (leitura do fonte: a condição com `carregando` aparece antes da condição com `algumLiquido`).
    - O bloco do estado vazio não contém `<button`.
    - `node web/tests/test_opcoes_proposta_ui.mjs` e `node web/tests/test_copy_theme.mjs` saem com código 0.
  </acceptance_criteria>
  <done>`OportunidadesOpcoes` existe, com os três estados (itens / carregando / vazio com motivo), manchete do motor verbatim, zero fetch e zero texto hardcodado.</done>
</task>

<task type="auto">
  <name>Task 2: Montar a tira em CarteiraScreen e ligar o toque ao card da posição</name>
  <read_first>
    - web/src/App.jsx linhas 3905-3915 (topo de `CarteiraScreen` já estendido pelo Plano 18-02: `opcoesFor`, `opcoesPorTicker`, `opcoesCarregando`)
    - web/src/App.jsx linhas 3956-3981 (aviso de concentração + bloco de estado vazio `data.positions.length === 0` — o ponto de montagem fica entre os dois)
    - web/src/App.jsx linhas 7444-7451 (mecanismo de scroll já em produção: `setTimeout` → `getElementById` → guarda → `scrollIntoView`)
    - web/src/App.jsx linha 3992 (o `<div>` do card de posição, que o Plano 18-02 ancorou com `id={"posicao-" + p.t}`)
  </read_first>
  <files>web/src/App.jsx</files>
  <action>
1. Handler de abertura, definido no corpo de `CarteiraScreen` (ao lado dos
   outros estados, antes do `return`):
   `const abrirOpcoesDe = (t) => { setOpcoesFor(t); setTimeout(() => { const el = document.getElementById("posicao-" + t); if (!el) return; el.scrollIntoView({ behavior: "smooth", block: "center" }); }, 60); };`
   O `setTimeout` existe porque o `scrollIntoView` precisa acontecer DEPOIS do
   re-render que expande o detalhe — rolar antes leva o card para a posição
   errada, já que a altura muda ao abrir. Mesmo mecanismo do deep link de push
   (App.jsx:7444-7450), com a guarda `if (!el) return;` preservada. Comentar
   essa razão no fonte.
   Não usar `useRef` por item: não existe esse padrão no arquivo e a âncora por
   `id` já é o precedente da casa.

2. Montagem da tira, entre o IIFE do aviso de concentração (termina na linha
   3972) e o bloco `{data.positions.length === 0 && (` (linha 3974):
   `{data.positions.length > 0 && (<OportunidadesOpcoes propostas={opcoesPorTicker} carregando={opcoesCarregando} positions={data.positions} cp={cp} onAbrir={abrirOpcoesDe} />)}`
   A guarda `data.positions.length > 0` é NAV-03 segunda metade: carteira sem
   nenhuma posição não mostra a tira, porque o estado vazio de portfólio logo
   abaixo (linhas 3974-3980) já explica o que fazer — duas mensagens para a
   mesma ausência seria ruído.

3. Nada mais muda. Não mexer nos KPIs, na linha de patrimônio de opções, no
   aviso de concentração, no laço de posições nem no `PropostaDaPosicao`
   montado pelo Plano 18-02.
  </action>
  <verify>
    <automated>cd web && npx vite build && node tests/test_carteira_lastro_ui.mjs && node tests/test_opcoes_proposta_ui.mjs && node tests/test_concentracao_carteira.mjs && node tests/test_fase2_portfolio.mjs && node tests/test_copy_theme.mjs && node tests/test_acessibilidade_acordeao.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx vite build` conclui sem erro.
    - `grep -c "<OportunidadesOpcoes" web/src/App.jsx` retorna 1.
    - A montagem está guardada por `data.positions.length > 0` — o fonte contém `{data.positions.length > 0 && (` imediatamente antes de `<OportunidadesOpcoes`.
    - A montagem aparece ANTES do bloco `{data.positions.length === 0 && (` no fonte: `node -e "const s=require('fs').readFileSync('web/src/App.jsx','utf8');process.exit(s.indexOf('<OportunidadesOpcoes')<s.indexOf('{data.positions.length === 0 && (')?0:1)"`
    - `grep -c 'getElementById("posicao-" + t)' web/src/App.jsx` retorna 1, dentro de `abrirOpcoesDe`, com `scrollIntoView({ behavior: "smooth", block: "center" })` e a guarda `if (!el) return;`.
    - `abrirOpcoesDe` chama `setOpcoesFor(t)` antes do `setTimeout`.
    - `node web/tests/test_carteira_lastro_ui.mjs`, `node web/tests/test_opcoes_proposta_ui.mjs`, `node web/tests/test_concentracao_carteira.mjs` e `node web/tests/test_fase2_portfolio.mjs` saem todos com código 0.
    - `git diff web/src/App.jsx` não altera nenhuma chamada de `portfolioMetrics(` nem o corpo de `AtivoCard`.
  </acceptance_criteria>
  <done>A tira aparece no topo de Posições quando há posições; tocar um item expande o detalhe daquela posição e rola o card para a vista; carteira vazia não mostra a tira.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| motor determinístico → tira agregada | N manchetes atravessam para uma superfície nova; é o ponto onde uma composição client-side seria mais tentadora |
| navegador → API de opções | fan-out por ticker; sem rota nova, sem entrada de dado nova |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-08 | Spoofing | resumo do item da tira | mitigate | `proposta.manchete` renderizada verbatim; proibido concatenar strike/contratos/prêmio; critério de aceite proíbe qualquer template string envolvendo `manchete`; guardião do Plano 18-04 tranca a classe |
| T-18-09 | Information Disclosure | estado vazio da tira | mitigate | Os dois motivos vêm de `cp.tiraOpcoesSemCobertura`/`SemSetup` — descrevem o estado do gate/leitura técnica, nunca vazam mensagem de erro do provedor (essa é engolida pelo `.catch` do hook) |
| T-18-10 | Denial of Service | render da tira sobre carteira grande | accept | Simulador educacional, carteiras de poucas posições; fan-out por ticker é o precedente de custo já aceito no ADR-004 e no 18-CONTEXT.md; rota bulk explicitamente deferida |
| T-18-11 | Tampering | tira mostrando estrutura sobre ticker sem posição | mitigate | A lista de itens deriva SÓ de `data.positions`; não há caminho de código que injete ticker de watchlist/radar na tira |
| T-18-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote novo; `git diff --stat web/package.json package-lock.json` precisa sair vazio |
</threat_model>

<verification>
1. `cd web && npx vite build` — sintaxe JSX válida.
2. `node web/tests/test_opcoes_proposta_ui.mjs` — guardrail CVM e ordem de componentes.
3. `node web/tests/test_carteira_lastro_ui.mjs` — asserções negativas da carteira.
4. `node web/tests/test_concentracao_carteira.mjs` e `test_fase2_portfolio.mjs` — a tela de Posições não regrediu.
5. `node web/tests/test_copy_theme.mjs` — textos vêm do dicionário.
6. `git diff --stat web/package.json package-lock.json` vazio.
</verification>

<success_criteria>
- Tira renderizada acima da lista de posições, com um item por posição que tem
  proposta concreta, manchete do motor verbatim.
- Toque no item abre o detalhe naquela posição e rola o card para a vista.
- Sem propostas: motivo explícito, escolhido entre "sem cobertura líquida" e
  "cobertura líquida sem setup técnico hoje".
- Durante a busca: texto de carregamento, nunca estado vazio.
- Carteira vazia: tira ausente.
</success_criteria>

<output>
Create `.planning/phases/18-aba-opcoes/18-03-SUMMARY.md` when done
</output>
