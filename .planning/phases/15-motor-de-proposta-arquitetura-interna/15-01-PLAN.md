---
phase: 15-motor-de-proposta-arquitetura-interna
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - server/app/opcoes_payoff.py
  - server/tests/test_opcoes_payoff.py
autonomous: true
requirements: [ENG-02]

must_haves:
  truths:
    - "O sistema calcula custo líquido, ganho máximo, perda máxima, breakevens e delta somado de uma estrutura de N pernas (1, 2 ou mais) sem tocar rede, banco ou LLM."
    - "Uma venda coberta (ação comprada + call vendida) aparece como ganho LIMITADO, não ilimitado."
    - "Um collar (ação + call vendida + put comprada) aparece travado dos dois lados: ganho máximo e perda máxima ambos numéricos."
    - "Perna malformada (strike ausente, prêmio negativo, booleano no lugar de número) é recusada com mensagem que nomeia a perna e o campo — nunca calculada silenciosamente."
    - "Delta somado com alguma perna sem delta declara a falta (`pernas_sem_delta` > 0 + `motivo`) em vez de apresentar soma parcial como completa."
  artifacts:
    - path: "server/app/opcoes_payoff.py"
      provides: "Aritmética pura de payoff de N pernas portada de calculos.py (ENG-02)"
      exports: ["perfil_da_estrutura", "resultado_no_vencimento", "custo_liquido", "TIPOS_PERNA", "LADOS"]
      min_lines: 120
    - path: "server/tests/test_opcoes_payoff.py"
      provides: "Guardião da aritmética portada — casos numéricos fechados"
      min_lines: 120
  key_links:
    - from: "server/app/opcoes_payoff.py"
      to: "stdlib apenas"
      via: "ausência de import de rede/estado"
      pattern: "^(import|from) (?!httpx|socket|urllib|requests|asyncio|\\.)"
---

<objective>
Portar a aritmética pura de payoff de `~/dev/MCP/servers/mydata/calculos.py`
para dentro do repo do Boris como `server/app/opcoes_payoff.py`, adaptada ao
vocabulário e às convenções do projeto e coberta por testes próprios.

Purpose: ENG-02. Esta é a base matemática de que o motor de proposta
(`avaliar()`, Plano 03) e as 3 estruturas da Fase 16 dependem. Sem ela,
collar não tem como existir — venda coberta e put de proteção hoje só sabem
falar de UMA perna.
Output: módulo puro `opcoes_payoff.py` + suíte `test_opcoes_payoff.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-motor-de-proposta-arquitetura-interna/15-CONTEXT.md
@CLAUDE.md

Fonte a portar (repo EXTERNO — só leitura, NUNCA editar):
`~/dev/MCP/servers/mydata/calculos.py`, linhas 255-464 (bloco "estrutura de
múltiplas pernas") + os utilitários `_numero` (linha 76) e `_tipo` (linha 87).

Padrão de módulo puro já em produção no Boris:
@server/app/opcoes_lastreadas.py (docstring de topo: "Módulo PURO — sem rede,
sem banco, sem LLM"; `hoje` entra por argumento)
</context>

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código real. -->

Vocabulário de PERNA (fixado por `calculos.py`, mantido palavra por palavra —
CONTEXT ENG-04 proíbe inventar vocabulário novo):

  perna = {
    "contrato": <str|None>,        # identificador do contrato (contractSymbol)
    "tipo": "CALL" | "PUT" | "ACAO",
    "lado": "compra" | "venda",
    "strike": <float>,             # 0.0 obrigatório quando tipo == "ACAO"
    "premio": <float>,             # prêmio da opção; PREÇO DA AÇÃO quando ACAO
    "quantidade": <float>,         # > 0
    "delta": <float|None>,
  }

Saída de `perfil_da_estrutura(pernas)` (chaves EXATAS, herdadas da fonte):

  {
    "pernas": [ {contrato, tipo, lado, quantidade, strike, premio} ],
    "custo_liquido": <float>,
    "fluxo": "debito" | "credito" | "neutro",
    "ganho_maximo": <float|None>,      # None SOMENTE quando ganho_ilimitado
    "perda_maxima": <float|None>,      # None SOMENTE quando perda_ilimitada
    "ganho_ilimitado": <bool>,
    "perda_ilimitada": <bool>,
    "breakevens": [<float>, ...],      # ordenada, sem repetição
    "delta_total": {"valor": <float>, "pernas_sem_delta": <int>, "motivo": <str|None>},
    "curva": [ {"preco_objeto": <float>, "resultado": <float>} ],
    "unidade": <str>,
  }

Convenções não-negociáveis da fonte, que valem também aqui:
  1. Tudo POR UNIDADE DO OBJETO (uma ação). Lote de 100 multiplica do lado de
     fora — nunca embutir o fator aqui.
  2. `None` sempre tem motivo declarado. Casa com o guardião do repo
     (`test_m3_format_pede_null_nunca_zero`): nulo nunca vira 0.0 mudo.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalização de perna, custo líquido e resultado no vencimento</name>
  <files>server/app/opcoes_payoff.py, server/tests/test_opcoes_payoff.py</files>
  <read_first>
    - `~/dev/MCP/servers/mydata/calculos.py` linhas 55-90 (constantes TIPOS,
      TIPOS_PERNA, LADOS e os utilitários `_numero`/`_tipo`) e 259-350
      (`_validar_perna`, `_custo_liquido`, `_resultado_no_vencimento`,
      `resultado_no_vencimento`) — a fonte a portar.
    - `server/app/opcoes_lastreadas.py` linhas 1-56 — o padrão de módulo puro
      do Boris: docstring de topo declarando a pureza, constantes privadas com
      `_`, helpers curtos, comentário explicando POR QUE, não O QUE.
    - `server/tests/test_opcoes_lastreadas_proposta.py` linhas 1-40 — o padrão
      de teste do projeto: docstring de topo citando fase/plano, `from app
      import <modulo>`, nome de teste em PT-BR descrevendo o comportamento.
  </read_first>
  <behavior>
    - `_validar_perna({"tipo":"CALL","lado":"compra","strike":40,"premio":1.5}, 1)`
      devolve dict normalizado com `sinal` = 1.0 e `quantidade` = 1.0 (default).
    - `lado` == "venda" produz `sinal` == -1.0.
    - `tipo` == "ACAO" com `strike` omitido normaliza `strike` para 0.0.
    - `tipo` == "ACAO" com `strike` != 0 levanta `ValueError` citando o índice
      da perna e a palavra "ACAO".
    - `tipo` == "ACAO" com `premio` <= 0 levanta `ValueError` (a ação precisa
      do preço do papel em `premio`).
    - `strike` booleano (`True`) levanta `ValueError` — `bool` é subclasse de
      `int` e um `True` viraria 1.0 silenciosamente.
    - `tipo` fora de CALL/PUT/ACAO, `lado` fora de compra/venda, `premio`
      negativo e `quantidade` <= 0 cada um levanta `ValueError` que nomeia o
      índice da perna e o campo problemático.
    - `custo_liquido` de [ACAO compra premio 30 qtd 1, CALL venda premio 1.5
      qtd 1] == 28.5 (débito de 30 menos crédito de 1.5).
    - `custo_liquido` de [CALL venda premio 1.5] == -1.5 (crédito).
    - `resultado_no_vencimento([CALL compra strike 40 premio 2], preco=45)`
      == 3.0.
    - `resultado_no_vencimento([PUT compra strike 40 premio 2], preco=35)`
      == 3.0.
    - `resultado_no_vencimento([ACAO compra premio 30], preco=41.03)` == 11.03
      — preço arbitrário entre strikes, não só nos pontos de dobra.
    - `resultado_no_vencimento([], preco=10)` levanta `ValueError` citando
      "sem pernas".
    - `resultado_no_vencimento([perna válida], preco=-1)` levanta `ValueError`
      citando preço inválido.
  </behavior>
  <action>
    Escreva PRIMEIRO `server/tests/test_opcoes_payoff.py` com um teste por
    item do bloco `<behavior>` acima, rode e confirme que falham (RED), depois
    crie `server/app/opcoes_payoff.py` para passá-los (GREEN).

    Docstring de topo do módulo, em PT-BR, seguindo o padrão de
    `opcoes_lastreadas.py`: declarar que é módulo PURO (sem rede, sem banco,
    sem LLM, sem leitura de relógio), citar a origem (`calculos.py` do repo
    `~/dev/MCP`, portado na Fase 15 por ENG-02) e as DUAS convenções herdadas
    — (1) tudo por unidade do objeto, lote multiplica fora; (2) nulo sempre
    tem motivo, nunca `None` mudo nem 0.0 no lugar de desconhecido.

    Portar, adaptando comentário e mensagem de erro ao PT-BR já usado no
    Boris, com estes nomes públicos e privados exatos:
      - constantes `TIPOS = ("CALL", "PUT")`, `TIPOS_PERNA = TIPOS + ("ACAO",)`,
        `LADOS = ("compra", "venda")`
      - `_numero(valor)` — aceita int/float, RECUSA `bool` (retorna None)
      - `_tipo(contrato, permitidos=TIPOS)` — uppercase + allowlist
      - `_validar_perna(perna, i)` — devolve o dict normalizado com as chaves
        `contrato`, `tipo`, `lado`, `sinal`, `strike`, `premio`, `quantidade`,
        `delta`; levanta `ValueError` com o índice `i` na mensagem
      - `custo_liquido(normalizadas)` — PÚBLICA no Boris (a fonte a tem como
        `_custo_liquido`); soma `sinal * quantidade * premio`, arredondada a
        4 casas
      - `_resultado_no_vencimento(pernas, preco, custo_liquido)` — intrínseco
        por tipo: ACAO vale o próprio `preco`, CALL vale `max(preco - strike,
        0)`, PUT vale `max(strike - preco, 0)`
      - `resultado_no_vencimento(pernas, preco)` — API pública que valida,
        calcula o custo líquido e delega

    NÃO portar deste arquivo: `FERIADOS_B3`, `spread`, `moneyness`,
    `breakeven_compra_seco`, `prazo_em_pregoes`, `filtrar_por_delta`. Os três
    primeiros já têm equivalente em `server/app/options_quant.py`
    (`liquidity_score`, `breakeven`, `intrinsic_value`) e duplicá-los criaria
    duas escalas para a mesma pergunta; `filtrar_por_delta` é exatamente o
    critério por delta que ENG-01 PROÍBE (a régua do Boris é liquidez +
    strike extremo); `FERIADOS_B3`/`prazo_em_pregoes` são calendário, fora do
    escopo do payoff.

    Imports permitidos: nenhum além de `from __future__ import annotations` e
    tipos do `typing`. Nada de `httpx`, `asyncio`, `datetime`, `store`, `db`,
    `skill_ref`.
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/test_opcoes_payoff.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && .venv/bin/python -m pytest tests/test_opcoes_payoff.py -q` sai 0 com pelo menos 13 testes coletados.
    - `cd server && .venv/bin/python -c "from app import opcoes_payoff; print(opcoes_payoff.TIPOS_PERNA)"` imprime `('CALL', 'PUT', 'ACAO')`.
    - `cd server && .venv/bin/python -c "from app import opcoes_payoff as m; print(m.custo_liquido([m._validar_perna({'tipo':'ACAO','lado':'compra','premio':30},1), m._validar_perna({'tipo':'CALL','lado':'venda','strike':32,'premio':1.5},2)]))"` imprime `28.5`.
    - `cd server && .venv/bin/python -c "from app import opcoes_payoff as m; m._validar_perna({'tipo':'CALL','lado':'compra','strike':True,'premio':1},1)" 2>&1 | grep -q ValueError` — booleano em strike é recusado.
    - `grep -vE '^\s*#' server/app/opcoes_payoff.py | grep -cE '^\s*(import|from)\s+(httpx|asyncio|socket|urllib|requests)' | grep -qx 0` — nenhum import de rede.
    - `grep -q "filtrar_por_delta" server/app/opcoes_payoff.py; test $? -ne 0` — o critério por delta proibido por ENG-01 não entrou junto no port.
  </acceptance_criteria>
  <done>`opcoes_payoff.py` valida pernas, calcula custo líquido e resultado no vencimento em qualquer preço, recusando perna malformada com mensagem que nomeia perna e campo; a suíte nova passa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Perfil da estrutura — extremos, breakevens e delta somado</name>
  <files>server/app/opcoes_payoff.py, server/tests/test_opcoes_payoff.py</files>
  <read_first>
    - `server/app/opcoes_payoff.py` — o estado atual do módulo após a Task 1.
    - `~/dev/MCP/servers/mydata/calculos.py` linhas 352-464
      (`perfil_da_estrutura`, `_breakevens`, `_delta_total`), incluindo o
      comentário sobre `inclinacao_direita` — é ele que faz a venda coberta
      aparecer como ganho LIMITADO em vez de ilimitado.
    - `server/tests/test_opcoes_payoff.py` — os testes já escritos na Task 1.
  </read_first>
  <behavior>
    - Call seca comprada (CALL compra strike 40 premio 2): `ganho_ilimitado`
      True, `ganho_maximo` None, `perda_maxima` 2.0, `breakevens` == [42.0],
      `fluxo` == "debito".
    - Call seca VENDIDA (CALL venda strike 40 premio 2): `perda_ilimitada`
      True, `perda_maxima` None, `ganho_maximo` 2.0, `fluxo` == "credito".
    - Venda coberta (ACAO compra premio 30 + CALL venda strike 32 premio 1.5):
      `ganho_ilimitado` False, `perda_ilimitada` False (a inclinação da cauda
      direita é 0), `ganho_maximo` == 3.5, `breakevens` == [28.5].
    - Put de proteção (ACAO compra premio 30 + PUT compra strike 28 premio 1):
      `perda_ilimitada` False, `perda_maxima` == 3.0, `ganho_ilimitado` True,
      `ganho_maximo` None.
    - Collar (ACAO compra premio 30 + CALL venda strike 33 premio 1 + PUT
      compra strike 28 premio 0.8): `ganho_ilimitado` False E
      `perda_ilimitada` False — travado dos DOIS lados, com `ganho_maximo` e
      `perda_maxima` ambos numéricos e não-None.
    - `delta_total` de [CALL compra delta 0.5 qtd 1, CALL venda delta 0.3
      qtd 1] == {"valor": 0.2, "pernas_sem_delta": 0, "motivo": None}.
    - `delta_total` com uma perna sem `delta`: `pernas_sem_delta` == 1 e
      `motivo` não-None (string explicando que a soma é parcial).
    - `curva` avalia exatamente o conjunto ordenado {0.0} ∪ {strikes}, sem
      repetição — a perna ACAO (strike 0) não duplica o ponto zero.
    - `perfil_da_estrutura([])` levanta `ValueError` citando "sem pernas".
    - `unidade` presente na saída, string mencionando que é por ação e que o
      lote multiplica do lado de fora.
  </behavior>
  <action>
    Escreva primeiro os testes do bloco `<behavior>` (RED), depois implemente
    (GREEN), acrescentando a `server/app/opcoes_payoff.py`:
      - `perfil_da_estrutura(pernas)` — API pública principal do módulo
      - `_breakevens(curva, inclinacao_direita)`
      - `_delta_total(pernas)`

    Regras estruturais que os testes acima provam e que NÃO podem ser
    simplificadas:
      - `inclinacao_direita` soma `sinal * quantidade` SOMENTE das pernas de
        tipo CALL e ACAO (PUT já não vale nada acima de todos os strikes).
        É a linha que decide `ganho_ilimitado`/`perda_ilimitada`; sem ela a
        função devolveria o maior número que viu e chamaria de ganho máximo,
        e uma call vendida a descoberto passaria por operação limitada.
      - `ganho_maximo` é None quando e somente quando `ganho_ilimitado`;
        `perda_maxima` é None quando e somente quando `perda_ilimitada`.
        Nunca 0.0 no lugar de "ilimitado" (guardrail do repo: nulo nunca vira
        zero mudo).
      - `perda_maxima` é `round(-pior, 4)` quando o pior resultado é negativo,
        e `0.0` quando nenhum ponto da curva é negativo — 0.0 aqui significa
        "não perde", não "desconhecido".
      - `_breakevens` cobre os DOIS casos: cruzamento entre dois pontos
        avaliados (interpolação linear, exata porque o trecho é reto) e
        cruzamento na cauda à direita do último strike, onde quem responde é
        a inclinação. Resultado ordenado e sem duplicata.
      - `_delta_total` conta as pernas sem delta e devolve `motivo` não-None
        quando `pernas_sem_delta` > 0 — soma incompleta apresentada como
        completa é pior que soma nenhuma.

    Acrescente ao final da suíte um teste de identidade cruzada:
    `resultado_no_vencimento(pernas, preco)` deve devolver exatamente o mesmo
    número que o ponto correspondente de `perfil_da_estrutura(pernas)["curva"]`
    para um `preco` que coincida com um strike — as duas APIs não podem
    divergir.
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/test_opcoes_payoff.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && .venv/bin/python -m pytest tests/test_opcoes_payoff.py -q` sai 0.
    - `cd server && .venv/bin/python -c "from app.opcoes_payoff import perfil_da_estrutura as p; r=p([{'tipo':'ACAO','lado':'compra','premio':30},{'tipo':'CALL','lado':'venda','strike':32,'premio':1.5}]); print(r['ganho_ilimitado'], r['ganho_maximo'], r['breakevens'])"` imprime `False 3.5 [28.5]`.
    - `cd server && .venv/bin/python -c "from app.opcoes_payoff import perfil_da_estrutura as p; r=p([{'tipo':'ACAO','lado':'compra','premio':30},{'tipo':'CALL','lado':'venda','strike':33,'premio':1},{'tipo':'PUT','lado':'compra','strike':28,'premio':0.8}]); print(r['ganho_ilimitado'], r['perda_ilimitada'], r['ganho_maximo'] is not None, r['perda_maxima'] is not None)"` imprime `False False True True`.
    - `cd server && .venv/bin/python -c "from app.opcoes_payoff import perfil_da_estrutura as p; r=p([{'tipo':'CALL','lado':'venda','strike':40,'premio':2}]); print(r['perda_ilimitada'], r['perda_maxima'])"` imprime `True None`.
    - `cd server && .venv/bin/python -c "from app.opcoes_payoff import perfil_da_estrutura as p; r=p([{'tipo':'CALL','lado':'compra','strike':40,'premio':2,'delta':0.5},{'tipo':'CALL','lado':'venda','strike':45,'premio':1}]); print(r['delta_total']['pernas_sem_delta'], r['delta_total']['motivo'] is not None)"` imprime `1 True`.
    - `cd server && .venv/bin/python -m pytest tests/ -q` sai 0 — nenhuma suíte existente quebrada.
  </acceptance_criteria>
  <done>`perfil_da_estrutura` produz custo líquido, ganho/perda máximos com ilimitado explícito, breakevens e delta somado com falta declarada, para 1, 2 e 3 pernas; venda coberta e collar aparecem travadas, call vendida a seco aparece com perda ilimitada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cadeia de opções (mydata/COTAHIST) → `opcoes_payoff` | Dado externo não confiável entra como `strike`/`premio`/`delta` de perna. O provedor pode publicar `null`, `0`, string ou campo ausente — nenhum contrato garante numérico. |
| chamador interno (Fase 16) → `opcoes_payoff` | Pernas montadas por código do Boris; erro de construção é bug, não ataque, mas produz número financeiro errado se não for recusado. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | `_validar_perna` em `opcoes_payoff.py` | mitigate | `_numero()` recusa `bool` e não-numérico; strike <= 0 (exceto ACAO), prêmio negativo e quantidade <= 0 levantam `ValueError` nomeando perna e campo. Testado nos itens 5-7 do `<behavior>` da Task 1. |
| T-15-02 | Information disclosure | mensagens de `ValueError` | mitigate | Mensagem cita apenas índice da perna e o valor do campo recebido — módulo puro, sem token, sem path, sem estado de usuário no escopo (verificável: nenhum import de `db`/`auth`/`mydata_client`). |
| T-15-03 | Denial of service | `perfil_da_estrutura` / `_breakevens` | accept | Curva avalia |strikes|+1 pontos e `_breakevens` é linear sobre ela; a cadeia ADR-004 de um vencimento tem dezenas de contratos e as estruturas do v1 têm no máximo 3 pernas. Custo O(n) sobre entrada já limitada a montante pelo `rastrear()` do Plano 03. |
| T-15-04 | Repudiation | número financeiro sem rastro | accept | O motor é puro e determinístico: a mesma entrada produz a mesma saída, e a saída inclui `pernas` normalizadas + `curva`, o que permite reauditar qualquer número a partir do próprio retorno. Persistência/auditoria é escopo da Fase 17. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo é instalado nesta fase — o módulo é stdlib-only (`typing` + `__future__`), sem alteração em `server/requirements.txt`. Verificável: `git diff --stat server/requirements*.txt` vazio ao fim do plano. |
</threat_model>

<verification>
- `cd server && .venv/bin/python -m pytest tests/test_opcoes_payoff.py -q` sai 0.
- `cd server && .venv/bin/python -m pytest tests/ -q` sai 0 (nenhuma regressão).
- `git diff --stat server/requirements.txt server/requirements-prod.txt` vazio.
- `server/app/opcoes_payoff.py` não importa `httpx`, `asyncio`, `socket`,
  `urllib`, `requests`, nem qualquer módulo `app.*` de estado (`store`, `db`,
  `skill_ref`, `main`).
</verification>

<success_criteria>
- `opcoes_payoff.perfil_da_estrutura` devolve custo líquido, ganho/perda
  máximos (com `ganho_ilimitado`/`perda_ilimitada` explícitos), breakevens
  ordenados e delta somado com contagem de pernas sem delta, para N pernas.
- Venda coberta (2 pernas) e collar (3 pernas) aparecem como estruturas
  travadas; call vendida a seco aparece com perda ilimitada e `perda_maxima`
  None.
- Perna malformada é recusada com `ValueError` que nomeia perna e campo.
- Nenhum número financeiro é produzido a partir de valor inventado: campo
  ausente vira recusa, nunca 0.0.
</success_criteria>

<output>
Create `.planning/phases/15-motor-de-proposta-arquitetura-interna/15-01-SUMMARY.md` when done
</output>
