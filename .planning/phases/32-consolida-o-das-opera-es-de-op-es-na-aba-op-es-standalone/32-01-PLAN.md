---
phase: 32-consolida-o-das-opera-es-de-op-es-na-aba-op-es-standalone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/copy.js
  - web/tests/test_consolidacao_opcoes_copy.mjs
  - web/tests/test_carteira_opcoes_tira.mjs
autonomous: true
requirements: [D-01, D-03, D-05]

must_haves:
  truths:
    - "Os dois blocos cross-carteira têm rótulos que dizem, em texto, qual motor cada um representa"
    - "A frase-ponte que nega hierarquia entre os dois motores existe no vocabulário, nos dois modos"
    - "A linha de chamada de Posições tem texto para os quatro estados (contagem, vazio, carregando, erro)"
    - "O estado de erro da curadoria tem texto próprio, distinto do texto de 'nada elegível'"
  artifacts:
    - path: "web/src/copy.js"
      provides: "chaves novas duasLeiturasIntro, tiraOpcoesSubtitulo, curadoriaErroBusca, curadoriaErroBuscaCta, linhaChamadaOpcoes* nos dois modos"
      contains: "duasLeiturasIntro"
    - path: "web/tests/test_consolidacao_opcoes_copy.mjs"
      provides: "guardião de paridade Estudo/Operador das chaves novas + checklist de palavras proibidas"
      contains: "PROIBIDAS"
  key_links:
    - from: "web/src/copy.js COPY.estudo"
      to: "web/src/copy.js COPY.operador"
      via: "mesmo conjunto de chaves nos dois blocos"
      pattern: "linhaChamadaOpcoesTexto"
---

<objective>
Entregar TODO o vocabulário novo desta fase antes de qualquer componente mudar
de arquivo, para que os planos seguintes só referenciem chaves que já existem
nos dois modos.

Purpose: o texto é a entrega de produto mais importante da fase (D-05: os dois
motores ficam lado a lado e só o rótulo impede a leitura "um é o certo, o
outro é o alternativo"). Fazer isso primeiro também impede o anti-padrão de
compor frase dentro do componente na pressa de mover o JSX.
Output: `web/src/copy.js` com 8 chaves novas + 3 conteúdos reescritos, e um
guardião próprio que cobre as chaves que vivem fora de `OpcoesScreen.jsx`
(hoje `test_vocabulario_opcoes.mjs` só varre aquele arquivo).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-consolida-o-das-opera-es-de-op-es-na-aba-op-es-standalone/32-CONTEXT.md
@.planning/phases/32-consolida-o-das-opera-es-de-op-es-na-aba-op-es-standalone/32-UI-SPEC.md
@.claude/skills/didatica-boris/SKILL.md

<interfaces>
Estrutura de `web/src/copy.js` (confirmada por leitura em 2026-09-15):

- `COPY.estudo` — bloco das chaves de opções em torno das linhas 377-666
  (`opcoesSubaba*`, `tiraOpcoes*` em 585-594, `curadoria*` em 617-666).
- `COPY.operador` — mesmas chaves, bloco em torno das linhas 952-1180
  (`tiraOpcoes*` em 1140-1148, `curadoria*` em 1155-1180).
- Chaves podem ser string OU função (`concentracaoCorpo(t, pct)`,
  `ctaFecharLastreada(...)` já são funções no arquivo) — o guardião novo
  precisa distinguir tipo por chave, não presumir string em todas.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever as chaves novas e reescrever os três conteúdos nos dois modos</name>
  <files>web/src/copy.js</files>
  <read_first>
    - `web/src/copy.js` linhas 575-670 (bloco `tiraOpcoes*`/`curadoria*` de `COPY.estudo`) e 1130-1190 (o espelho em `COPY.operador`) — para copiar a forma de comentário e a ordem das chaves.
    - `.planning/phases/32-.../32-UI-SPEC.md` seções "Os dois rótulos (D-05)", "Linha de chamada em Posições" e "Correção de estado obrigatória" — as strings são literais do contrato aprovado, não devem ser reescritas com outras palavras.
    - `.claude/skills/didatica-boris/SKILL.md` — vocabulário por modo: texto novo entra nos DOIS blocos, nunca em um só.
  </read_first>
  <action>
Em `COPY.estudo` e `COPY.operador`, adicionar as chaves novas e reescrever os
conteúdos abaixo. Nenhuma chave existente é renomeada ou removida.

CHAVES NOVAS (mesmo nome nos dois blocos):
- `duasLeiturasIntro` — frase-ponte do D-05. Estudo e Operador recebem o MESMO
  texto: "Duas leituras diferentes da sua carteira — nenhuma é mais certa que
  a outra: uma parte do que a leitura técnica confirma agora, a outra varre a
  cadeia inteira sem exigir essa confirmação." Comentar acima da chave que o
  texto é idêntico nos dois modos DE PROPÓSITO (é constatação de fato sobre
  dois motores, não voz de personagem) e que ele é a mitigação do risco
  regulatório do D-05 — por isso o componente que o renderiza não pode
  colapsá-lo.
- `tiraOpcoesSubtitulo` — Estudo: "Só aparecem aqui as posições em que a
  leitura técnica do próprio ativo confirma a estrutura agora — o mesmo motor
  que decide o gatilho do Radar." Operador: "Só entram aqui posições cuja
  leitura técnica confirma a estrutura agora — mesmo motor do gatilho do
  Radar."
- `curadoriaErroBusca` — mesmo texto nos dois modos: "Não foi possível varrer
  sua carteira agora. Isto não significa que não há oportunidade — significa
  que a busca falhou. Toque para tentar de novo." Comentar que esta chave
  existe porque `curadoriaVazio` ("nenhuma estrutura elegível") era exibida
  também quando a busca FALHAVA, o que afirma um resultado que ninguém mediu
  (princípio 4 do CLAUDE.md).
- `curadoriaErroBuscaCta` — mesmo texto nos dois modos: "Tentar de novo".
- `linhaChamadaOpcoesTexto` — FUNÇÃO de `n` (número inteiro), mesma nos dois
  modos: devolve "1 oportunidade de opções nas suas posições" quando `n === 1`
  e `n + " oportunidades de opções nas suas posições"` caso contrário.
- `linhaChamadaOpcoesVazia` — "Nenhuma oportunidade de opções agora" (idêntico
  nos dois modos).
- `linhaChamadaOpcoesCarregando` — "Verificando oportunidades de opções…"
  (idêntico nos dois modos).
- `linhaChamadaOpcoesErro` — "Não foi possível verificar agora — toque para
  ver na aba Opções" (idêntico nos dois modos).
- `linhaChamadaOpcoesAria` — FUNÇÃO de `texto` (string), mesma nos dois modos:
  devolve `texto + " — abrir aba Opções"`. Existe para o leitor de tela não
  anunciar só a seta.

CONTEÚDO REESCRITO (chave mantida, valor novo):
- `tiraOpcoesTitulo` — Estudo: "OPORTUNIDADES CONFIRMADAS PELA LEITURA
  TÉCNICA". Operador: "CONFIRMADAS PELA LEITURA TÉCNICA". Antes era
  "OPORTUNIDADES DE OPÇÕES" nos dois; passa a DIFERIR entre modos.
- `curadoriaSubtitulo` — Estudo: "As 4 melhores por prêmio ÷ perda máxima,
  entre todas as posições e vencimentos varridos — inclusive as que a leitura
  técnica ainda não confirma. A ordem é do motor; não muda com a explicação
  da IA." Operador: "As 4 melhores por prêmio ÷ perda máxima, entre todas as
  posições e vencimentos varridos — inclusive as que a leitura técnica ainda
  não confirma. Ordem do motor; a IA não reordena."

`curadoriaTitulo` NÃO muda (segue "AS 4 MELHORES OPORTUNIDADES DE OPÇÕES" nos
dois modos).

Cada bloco novo leva comentário datado no padrão do arquivo: "Fase 32 (32-01,
D-01/D-03/D-05)" com a razão da escolha, não só o que mudou. O comentário do
par `tiraOpcoesTitulo` deve registrar que os dois títulos agora NOMEIAM o
motor porque os dois blocos passam a conviver na mesma tela.
  </action>
  <verify>
    <automated>node -e "import('./web/src/copy.js').then(m=>{const e=m.COPY.estudo,o=m.COPY.operador;const ks=['duasLeiturasIntro','tiraOpcoesSubtitulo','curadoriaErroBusca','curadoriaErroBuscaCta','linhaChamadaOpcoesTexto','linhaChamadaOpcoesVazia','linhaChamadaOpcoesCarregando','linhaChamadaOpcoesErro','linhaChamadaOpcoesAria'];const f=ks.filter(k=>e[k]===undefined||o[k]===undefined);if(f.length)throw new Error('faltando: '+f);if(typeof e.linhaChamadaOpcoesTexto!=='function')throw new Error('linhaChamadaOpcoesTexto deve ser funcao');if(e.linhaChamadaOpcoesTexto(1).indexOf('1 oportunidade de')!==0)throw new Error('singular errado');if(e.tiraOpcoesTitulo===o.tiraOpcoesTitulo)throw new Error('tiraOpcoesTitulo deveria diferir entre modos');console.log('ok')})"</automated>
  </verify>
  <acceptance_criteria>
    - O comando de verify acima sai com código 0 e imprime `ok`.
    - `grep -c "duasLeiturasIntro" web/src/copy.js` devolve 2 ou mais (uma por bloco de modo; comentários adicionais são permitidos).
    - `web/src/copy.js` contém a string `Duas leituras diferentes da sua carteira` exatamente como no UI-SPEC.
    - `web/src/copy.js` contém a string `Não foi possível varrer sua carteira agora.`
    - `COPY.estudo.curadoriaSubtitulo !== COPY.operador.curadoriaSubtitulo` (verificado pelo guardião da Task 2).
    - Nenhuma chave existente foi removida: `node -e "import('./web/src/copy.js').then(m=>{['curadoriaTitulo','curadoriaVazio','tiraOpcoesSemCobertura','linhaPropostaNaPosicao'].forEach(k=>{if(!m.COPY.estudo[k]||!m.COPY.operador[k])throw new Error('removida: '+k)});console.log('ok')})"` sai 0.
  </acceptance_criteria>
  <done>As 9 chaves novas existem nos dois modos com os tipos corretos (7 strings + 2 funções), e os 3 conteúdos reescritos batem literalmente com o UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Guardião das chaves que vivem fora de OpcoesScreen.jsx + ajuste de contagem na tira</name>
  <files>web/tests/test_consolidacao_opcoes_copy.mjs, web/tests/test_carteira_opcoes_tira.mjs</files>
  <read_first>
    - `web/tests/test_curadoria_ui.mjs` linhas 95-200 — o padrão `chavesCuradoriaDe(copy)` de paridade de CONJUNTO (não lista fixa) e a lista `PROIBIDAS`; copiar a forma, não duplicar o arquivo.
    - `web/tests/test_carteira_opcoes_tira.mjs` linhas 34-78 — as regras de contagem ("7 chaves da tira", "todas as 7 são string literal") que a chave nova `tiraOpcoesSubtitulo` invalida.
    - `web/tests/test_vocabulario_opcoes.mjs` linhas 31-88 — confirma que o escopo dele é só `OpcoesScreen.jsx`, o que deixa `linhaChamadaOpcoes*` (que vai viver em `App.jsx`) descoberta.
  </read_first>
  <action>
Criar `web/tests/test_consolidacao_opcoes_copy.mjs`, guardião novo com
cabeçalho datado ("Fase 32 (32-01) — 2026-09-15") explicando por que ele
existe: `test_vocabulario_opcoes.mjs` só varre `OpcoesScreen.jsx`, e as chaves
`linhaChamadaOpcoes*` são consumidas em `App.jsx` (CarteiraScreen), portanto
ficariam sem nenhuma cobertura de paridade.

Regras do guardião novo (usar o mesmo helper `ok(...)` dos outros arquivos de
`web/tests/`, import de `COPY` a partir de `../src/copy.js`):
1. Cada uma das 9 chaves novas existe em `COPY.estudo` E em `COPY.operador`.
2. Tipo por chave: `linhaChamadaOpcoesTexto` e `linhaChamadaOpcoesAria` são
   `function` nos dois modos; as outras 7 são string não-vazia nos dois modos.
3. Paridade de CONJUNTO por prefixo: o conjunto de chaves que começam com
   `linhaChamadaOpcoes` é idêntico nos dois modos (comparar arrays ordenados).
4. `linhaChamadaOpcoesTexto(1)` contém "1 oportunidade" (singular) e
   `linhaChamadaOpcoesTexto(3)` contém "3 oportunidades" (plural), nos dois
   modos.
5. `linhaChamadaOpcoesTexto(0)` NÃO é usado como caminho de vazio: a chave
   `linhaChamadaOpcoesVazia` existe e é diferente de `linhaChamadaOpcoesTexto(0)`
   — o estado "zero" tem frase própria.
6. `linhaChamadaOpcoesErro` é diferente de `linhaChamadaOpcoesVazia` nos dois
   modos (erro de busca não pode reusar a frase de resultado zero — princípio
   4 do CLAUDE.md).
7. `curadoriaErroBusca` é diferente de `curadoriaVazio` nos dois modos, pela
   mesma razão.
8. `duasLeiturasIntro` é idêntico nos dois modos (decisão registrada, não
   acidente) e menciona as duas leituras sem hierarquia: contém "nenhuma é
   mais certa que a outra".
9. `tiraOpcoesTitulo` e `tiraOpcoesSubtitulo` diferem entre Estudo e Operador
   (voz de professor × voz de mesa), e `curadoriaSubtitulo` idem.
10. Checklist regulatório: nenhuma das 9 chaves novas nem dos 3 conteúdos
    reescritos contém qualquer palavra da lista PROIBIDAS
    (`garantido`, `lucro garantido`, `sem risco`, `certeza`), aplicada
    case-insensitive sobre o resultado das funções também (chamar
    `linhaChamadaOpcoesTexto(3)` e `linhaChamadaOpcoesAria("x")` e varrer o
    retorno).
11. Sanidade da regex de PROIBIDAS: uma string de controle contendo
    "lucro garantido" é detectada (mesmo padrão de sanidade já usado em
    `test_opcoes_analisar_ui.mjs`).

Em `web/tests/test_carteira_opcoes_tira.mjs`: atualizar as duas regras de
contagem de "7 chaves da tira" para incluir `tiraOpcoesSubtitulo` (8 chaves),
mantendo a asserção "todas são string literal (não função)". Adicionar nota
datada no comentário do arquivo: "2026-09-15, Fase 32 (32-01): a tira ganhou
`tiraOpcoesSubtitulo` porque os dois blocos cross-carteira passam a conviver
na mesma tela e o título sozinho não diz mais qual motor é qual (D-05)."
NÃO remover nenhuma regra existente deste arquivo.
  </action>
  <verify>
    <automated>node web/tests/test_consolidacao_opcoes_copy.mjs && node web/tests/test_carteira_opcoes_tira.mjs && node web/tests/test_curadoria_ui.mjs && node web/tests/test_vocabulario_opcoes.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node web/tests/test_consolidacao_opcoes_copy.mjs` sai com código 0.
    - `node web/tests/test_carteira_opcoes_tira.mjs` sai com código 0.
    - `node web/tests/test_curadoria_ui.mjs` sai com código 0 (não deve ter sido afetado por este plano).
    - `grep -v '^\s*//' web/tests/test_consolidacao_opcoes_copy.mjs | grep -c "PROIBIDAS"` devolve valor maior que 0.
    - `grep -c "2026-09-15" web/tests/test_carteira_opcoes_tira.mjs` devolve valor maior que 0 (nota datada presente).
    - `web/tests/test_carteira_opcoes_tira.mjs` não perdeu regras: `grep -c "^ok(\|  ok(" web/tests/test_carteira_opcoes_tira.mjs` devolve valor maior ou igual ao de antes da edição (registrar o número antes e depois no SUMMARY).
  </acceptance_criteria>
  <done>O guardião novo passa, cobre as 9 chaves novas nos dois modos incluindo o checklist de palavras proibidas, e a contagem da tira foi atualizada com nota datada sem perder nenhuma regra.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhuma nova | Este plano edita apenas strings de vocabulário e um arquivo de teste. Não cria rota, não aceita input, não expõe dado. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Information disclosure (regulatório, não técnico) | strings novas em `copy.js` | mitigate | Checklist PROIBIDAS aplicado por teste às 9 chaves novas e aos 3 conteúdos reescritos (regra 10 da Task 2); nenhuma frase promete resultado ou compara os dois motores por qualidade |
| T-32-02 | Tampering (integridade da afirmação do motor) | `curadoriaErroBusca` | mitigate | Frase de erro é distinta da frase de vazio, testado — impede a tela afirmar "nada elegível" quando a causa foi falha de busca (princípio 4 do CLAUDE.md) |
| T-32-SC | Tampering (cadeia de suprimentos) | npm install | accept | Este plano não instala nenhum pacote; `32-RESEARCH.md` §"Package Legitimacy Audit" registra N/A para a fase inteira |

Superfície de ameaça técnica INALTERADA neste plano: nenhuma rota nova,
nenhum input novo aceito do cliente, nenhum dado novo exposto.
</threat_model>

<verification>
- `node web/tests/test_consolidacao_opcoes_copy.mjs` — passa
- `node web/tests/test_carteira_opcoes_tira.mjs` — passa
- `node web/tests/test_curadoria_ui.mjs` — passa (não regrediu)
- `npm --prefix web run build` — sai 0 (copy.js é importado pelo bundle)
</verification>

<success_criteria>
- As 9 chaves novas existem nos dois modos, com os tipos certos.
- Os 3 conteúdos reescritos batem literalmente com o `32-UI-SPEC.md`.
- Existe guardião cobrindo as chaves que vivem fora de `OpcoesScreen.jsx`.
- Nenhuma regra de guardião foi apagada; a única alterada tem nota datada.
</success_criteria>

<output>
Create `.planning/phases/32-consolida-o-das-opera-es-de-op-es-na-aba-op-es-standalone/32-01-SUMMARY.md` when done
</output>
