---
phase: 32-consolida-o-das-opera-es-de-op-es-na-aba-op-es-standalone
plan: 03
type: execute
wave: 3
depends_on: ["32-01", "32-02"]
files_modified:
  - web/src/opcoes/useOpcoesPropostas.js
  - web/src/opcoes/OportunidadesOpcoes.jsx
  - web/src/opcoes/CuradoriaEstruturas.jsx
  - web/src/opcoes/OpcoesScreen.jsx
  - web/src/App.jsx
  - web/tests/test_curadoria_ui.mjs
  - web/tests/test_carteira_opcoes_tira.mjs
  - web/tests/test_opcoes_subabas_ui.mjs
  - web/tests/test_opcoes_consolidacao_ui.mjs
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07]

must_haves:
  truths:
    - "Abrir a aba Opções mostra, antes de qualquer outra coisa da sub-aba Setups, a frase que nega hierarquia entre os dois motores e os dois blocos cross-carteira"
    - "A tela de Posições não exibe mais a tira agregada nem a lista curada — sobra UMA linha de chamada, discreta, com contagem (D-01)"
    - "Clicar nessa linha leva à aba Opções, na lista de oportunidades, sem ativo pré-selecionado e sem deep-link para uma oportunidade específica (D-02)"
    - "O bloco do motor COM gate técnico continua com superfície cross-posição visível, no topo da sub-aba Setups ao lado da lista curada — não foi deletado nem empurrado para dentro de Operar (D-07)"
    - "O número da linha de chamada é o mesmo número de itens da lista curada exibida em Opções"
    - "Quando a varredura falha, a tela diz que a busca falhou — não que não há oportunidade"
    - "Executar um collar curado continua funcionando a partir do card na aba Opções"
  artifacts:
    - path: "web/src/opcoes/useOpcoesPropostas.js"
      provides: "fan-out gate+proposta por ticker, agora consumido pela aba Opções"
      exports: ["useOpcoesPropostas"]
    - path: "web/src/opcoes/OpcoesScreen.jsx"
      provides: "frase-ponte + Bloco A + Bloco B no topo da sub-aba Setups"
      contains: "blocoCuradoria"
    - path: "web/src/App.jsx"
      provides: "linha de chamada única em CarteiraScreen com os quatro estados"
      contains: "LinhaChamadaOpcoes"
    - path: "web/tests/test_opcoes_consolidacao_ui.mjs"
      provides: "guardião da consolidação: ordem dos blocos, frase sempre no DOM, ausência dos blocos em Posições"
  key_links:
    - from: "web/src/App.jsx LinhaChamadaOpcoes"
      to: "ctx.goOpcoes"
      via: "onClick"
      pattern: "goOpcoes"
    - from: "web/src/App.jsx LinhaChamadaOpcoes"
      to: "ctx.curadoria.top.length"
      via: "contagem lida da fonte única (D-03)"
      pattern: "curadoria"
    - from: "web/src/opcoes/OpcoesScreen.jsx blocoCuradoria"
      to: "ctx.A.executarCandidatoCurado"
      via: "onExecutar"
      pattern: "executarCandidatoCurado"
    - from: "web/src/opcoes/OpcoesScreen.jsx blocoCuradoria"
      to: "escolherTicker + setSubaba"
      via: "onAbrir (substitui o scrollIntoView que só existia em CarteiraScreen)"
      pattern: "setSubaba\\(\"operar\"\\)"
---

<objective>
Fazer a mudança de tela: os dois blocos cross-carteira saem de Posições e
aparecem no topo da sub-aba Setups da aba Opções, com a frase-ponte
obrigatória; Posições fica com uma única linha de chamada com contagem real.

Purpose: é a entrega da fase. D-01 (Posições perde os blocos), D-02 (o clique
leva à lista), D-03 (contagem da mesma fonte), D-04/D-07 (onde cada bloco
aterrissa), D-05 (os dois motores lado a lado, rotulados), D-06 (sem inventar
busca nem impedir uma depois).
Output: aba Opções com os dois blocos e a frase-ponte; Posições com uma linha;
o estado de erro da curadoria deixando de se disfarçar de "nada elegível".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/32-consolida-o-das-opera-es-de-op-es-na-aba-op-es-standalone/32-CONTEXT.md
@.planning/phases/32-consolida-o-das-opera-es-de-op-es-na-aba-op-es-standalone/32-UI-SPEC.md
@.planning/phases/32-consolida-o-das-opera-es-de-op-es-na-aba-op-es-standalone/32-PATTERNS.md
@.planning/phases/32-consolida-o-das-opera-es-de-op-es-na-aba-op-es-standalone/32-02-SUMMARY.md

<interfaces>
Confirmado por leitura direta (2026-09-15) — não explorar para redescobrir:

`OpcoesScreen.jsx:273` — `export default function OpcoesScreen({ ctx })`; tem
`cp`, `store`, `palette`, `carteira` (posições filtradas), `ticker`,
`escolherTicker(t)`, `subaba`/`setSubaba`, `seletor`, `cabecalho`,
`blocoVigias`, `subabas`.
`OpcoesScreen.jsx:347` — `const escolherTicker = (t) => { setTicker(t === ticker ? "" : t); ... }` (é TOGGLE; ver ressalva na action).
`OpcoesScreen.jsx:686-711` — o `return`: `<h1>` → `<p>` subtítulo → `{subabas}`
→ ramo `subaba !== "setups" ? <SubAbaOperar .../> : <> {cabecalho} {blocoVigias}
{carteira.length > 0 ? seletor : null} ... </>`.

`App.jsx` depois do Plano 32-02: `ctx.curadoria` = `{ top, meta, carregando,
erro, narrativa, narrando, erroNarrativa, narrar, recarregar }`;
`ctx.goOpcoes` = `() => navigate("opcoes")`; `ctx.A.executarCandidatoCurado`;
`useOpcoesPropostas(tickers)` ainda definido em `App.jsx` e chamado uma vez em
`CarteiraScreen` (`App.jsx:4755`); o resultado alimenta DOIS consumidores —
`OportunidadesOpcoes` (`App.jsx:4852`, removido neste plano) e
`PropostaDaPosicao` (`App.jsx:5009`, removido só no Plano 32-04); `abrirOpcoesDe(t)` (`App.jsx:4769`) faz
`setOpcoesFor(t)` + `scrollIntoView("#posicao-"+t)`.

Chaves de copy entregues pelo Plano 32-01 (usar, não inventar):
`duasLeiturasIntro`, `tiraOpcoesSubtitulo`, `curadoriaErroBusca`,
`curadoriaErroBuscaCta`, `linhaChamadaOpcoesTexto(n)`,
`linhaChamadaOpcoesVazia`, `linhaChamadaOpcoesCarregando`,
`linhaChamadaOpcoesErro`, `linhaChamadaOpcoesAria(texto)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useOpcoesPropostas vira módulo e a aba Opções monta frase-ponte + Bloco A + Bloco B</name>
  <files>web/src/opcoes/useOpcoesPropostas.js, web/src/opcoes/OportunidadesOpcoes.jsx, web/src/opcoes/CuradoriaEstruturas.jsx, web/src/opcoes/OpcoesScreen.jsx, web/src/App.jsx</files>
  <read_first>
    - `web/src/App.jsx` linhas 4622-4678 — `useOpcoesPropostas` inteiro, com o comentário sobre chave primitiva e `marcarPendenteResolvido`.
    - `web/src/opcoes/OpcoesScreen.jsx` linhas 273-360 — corpo de `OpcoesScreen`, `escolherTicker`, `irParaVigia`, `useOpcoesMcp(store, ticker)` (o padrão de hook que recebe `store` por parâmetro).
    - `web/src/opcoes/OpcoesScreen.jsx` linhas 542-600 — `blocoVigias`, o molde de "bloco cross-carteira montado como const de JSX antes do return, com cascata carregando → erro → vazio → dados".
    - `web/src/opcoes/OpcoesScreen.jsx` linhas 660-720 — `subabas` e o `return`, onde os blocos novos são inseridos.
    - `web/src/opcoes/CuradoriaEstruturas.jsx` inteiro (criado no Plano 32-02) — em especial o ramo `top.length === 0 && !carregando` que hoje engole o estado de erro, e o `handleExecutar`.
    - `.planning/phases/32-.../32-UI-SPEC.md` §"Contrato de composição", §"Os dois rótulos (D-05)", §"Correção de estado obrigatória", §"Navegação de dentro do painel curado até a posição".
  </read_first>
  <action>
**(a) Extrair o hook.** Criar `web/src/opcoes/useOpcoesPropostas.js` com
`export function useOpcoesPropostas(store, tickers)` — mesmo corpo de
`App.jsx:4632-4678`, com `store` recebido por parâmetro (padrão de
`useOpcoesMcp(store, ticker)`) em vez do `store` de módulo de `App.jsx`. Guarda
nova: se `store` for falsy, o efeito retorna sem buscar. Cabeçalho no padrão
ADR-027 Emenda 3, com nota "Fase 32 (32-03): o consumidor deste hook deixou de
ser `CarteiraScreen` e passou a ser a aba Opções — o fan-out agora só dispara
quando a aba Opções abre, e não mais a cada entrada em Posições (redução de
tráfego, não aumento)".

Remover de `App.jsx` a DEFINIÇÃO do hook e acrescentar o import do módulo novo
(`useOpcoesPropostas` a partir de `./opcoes/useOpcoesPropostas.js`).

**A chamada em `CarteiraScreen` NÃO sai neste plano** — ela passa a receber
`store` como primeiro argumento, mantendo os mesmos nomes locais
(`opcoesPorTicker`, `opcoesCarregando`). Razão, e isto é uma armadilha real:
`PropostaDaPosicao` (`App.jsx` em torno da linha 5009) ainda consome
`(opcoesPorTicker[p.t] || {}).proposta` dentro do `.map` de `data.positions`,
e só sai no Plano 32-04. Apagar a declaração aqui deixaria um identificador
livre em JSX — que `vite build` NÃO pega (esbuild, sem TypeScript e sem ESLint
configurado em `web/`) e que nenhum guardião `.mjs` pega (são checagens
estáticas de string; nenhuma renderiza `CarteiraScreen`). O sintoma seria
`ReferenceError` em render para qualquer usuário com posições — o caminho
principal da fase. A chamada e o import saem no Plano 32-04, junto com o
último consumidor.

Custo temporário aceito, limitado à wave 3: entre este plano e o 32-04 o
fan-out de gate/proposta roda nas duas telas. Nada disso vai a produção — a
publicação só acontece no Plano 32-05, depois de o 32-04 ter removido a
chamada de Posições.

**(b) Bloco A ganha subtítulo.** Em `OportunidadesOpcoes.jsx`, renderizar
`cp.tiraOpcoesSubtitulo` como linha de 11.5px em `T.textMuted` logo abaixo do
eyebrow `cp.tiraOpcoesTitulo`, com `marginBottom: "8px"` e `lineHeight: 1.4` —
mesma métrica do subtítulo de `CuradoriaEstruturas`. Nenhuma outra mudança no
componente.

**(c) Bloco B passa a ler `erro`.** Em `CuradoriaEstruturas.jsx`:
- Adicionar prop `onRecarregar`.
- Inserir o ramo de erro com PRECEDÊNCIA sobre o ramo de vazio: quando
  `erro` é verdadeiro, renderizar `cp.curadoriaErroBusca` numa caixa com
  `color: T.warn` e borda `T.warn` (mesmo padrão visual do bloco de erro de
  execução já presente no componente), com um botão `cp.curadoriaErroBuscaCta`
  que chama `onRecarregar`. O ramo `top.length === 0 && !carregando` que hoje
  mostra `cp.curadoriaVazio` passa a exigir também `!erro`.
- Comentar por que: `useCuradoria` já rastreava `erro`, o componente nunca o
  lia, e a tela afirmava "nenhuma estrutura elegível" quando a causa real era
  "não consegui consultar" — violação ativa do princípio 4 do CLAUDE.md,
  corrigida aqui porque esta fase já mexe neste componente.
- Depois de uma execução bem-sucedida (o ramo `execAtual.ok`), chamar
  `onRecarregar` uma vez, para o candidato executado sair da lista. Guardar
  com `if (onRecarregar)`.

**(d) Montar os blocos em `OpcoesScreen`.** No corpo de `OpcoesScreen`, antes
do `return`, criar três `const` de JSX no molde de `blocoVigias`:
- `const fraseDuasLeituras` — um `<p>` sempre no DOM, sem estado de colapso,
  sem `aria-expanded`, sem toggle, renderizando `cp.duasLeiturasIntro` em
  12px/`T.textMuted`/`lineHeight: 1.5`. Comentar que ela é obrigatória por
  D-05 e que torná-la colapsável seria regressão regulatória, não ajuste
  visual.
- `const blocoOportunidades` — `<OportunidadesOpcoes propostas={...}
  carregando={...} positions={carteira} cp={cp} onAbrir={irParaOperar} />`,
  alimentado por `useOpcoesPropostas(store, carteira.map((p) => p.t))`
  chamado no corpo do componente.
- `const blocoCuradoria` — `<CuradoriaEstruturas>` alimentado por
  `ctx.curadoria`: `top`, `meta`, `carregando`, `erro`, `narrativa`,
  `narrando`, `erroNarrativa`, `onNarrar={() => ctx.curadoria.narrar(ctx.data && ctx.data.config)}`,
  `onRecarregar={ctx.curadoria.recarregar}`, `cp={cp}`,
  `onAbrir={irParaOperar}`,
  `onExecutar={(cand, o) => ctx.A.executarCandidatoCurado(cand, o)}`,
  `operador={!!(ctx && ctx.operador)}`, `palette={palette}`.

Adicionar `const irParaOperar = (t) => { if (t && t !== ticker) escolherTicker(t); setSubaba("operar"); };`
— usa a MESMA guarda de `irParaVigia` (`OpcoesScreen.jsx:357`) porque
`escolherTicker` é toggle e chamá-lo cru com o ticker já aberto
DESSELECIONARIA o ativo, o oposto de "me leve até ele". Este é o substituto do
`abrirOpcoesDe`/`scrollIntoView`, que dependia de um elemento `#posicao-<t>`
que só existe em `CarteiraScreen` (Pitfall 4 do 32-RESEARCH.md).

**(e) Inserir na árvore.** No ramo `subaba === "setups"` do `return`
(`OpcoesScreen.jsx:704-711`), a ordem passa a ser, dentro do mesmo `<>`:
`{cabecalho}` → `{fraseDuasLeituras}` → `{blocoOportunidades}` →
`{blocoCuradoria}` → `{blocoVigias}` → `{carteira.length > 0 ? seletor : null}`
→ resto inalterado. Os três blocos novos ficam DENTRO da sub-aba Setups, nunca
acima de `{subabas}` — é a Leitura A, decidida pelo Alex em 2026-09-15 (Open
Question #1 do UI-SPEC, resolvida). Comentar isso no ponto de inserção.

Restrições que valem para toda esta task: nenhum `position: sticky` ou `fixed`
em nenhum dos três blocos novos (D-06 — não desenhar nada que impeça uma barra
de busca depois); nenhum campo de busca ou filtro novo; nenhuma composição de
manchete; nenhuma chamada a `store.*`/`api.*` dentro dos componentes movidos.
  </action>
  <verify>
    <automated>npm --prefix web run build && node -e "const s=require('fs').readFileSync('web/src/opcoes/OpcoesScreen.jsx','utf8');const f=s.indexOf('{fraseDuasLeituras}'),a=s.indexOf('{blocoOportunidades}'),b=s.indexOf('{blocoCuradoria}'),v=s.indexOf('{blocoVigias}');if(!(f>-1&&a>f&&b>a&&v>b))throw new Error('ordem errada: '+[f,a,b,v]);if(/position:\s*[\"']?(sticky|fixed)/.test(s.slice(f,v)))throw new Error('sticky/fixed proibido nos blocos novos');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix web run build` sai com código 0.
    - O comando de verify imprime `ok` (ordem frase → Bloco A → Bloco B → vigias, sem sticky/fixed).
    - `grep -c "function useOpcoesPropostas" web/src/App.jsx` devolve 0 e o mesmo grep em `web/src/opcoes/useOpcoesPropostas.js` devolve 1.
    - A referência pendurada NÃO foi criada: `grep -c "useOpcoesPropostas" web/src/App.jsx` devolve 2 — o import e a chamada em `CarteiraScreen`, que sobrevive até o Plano 32-04.
    - Invariante de escopo: `opcoesPorTicker` continua declarado no mesmo escopo em que é usado. Registrar no SUMMARY a linha da declaração e a linha do uso em `PropostaDaPosicao`, lado a lado.
    - `grep -c "store.optionsGate(" web/src/opcoes/OpcoesScreen.jsx` continua devolvendo 1 (o de `SubAbaOperar`; o fan-out mora no módulo novo, não neste arquivo — regra 3 de `test_opcoes_subabas_ui.mjs` preservada).
    - `grep -c "setSubaba(\"operar\")" web/src/opcoes/OpcoesScreen.jsx` devolve valor maior que 0 e `grep -c "scrollIntoView" web/src/opcoes/OpcoesScreen.jsx` devolve 0.
    - `grep -c "executarCandidatoCurado" web/src/opcoes/OpcoesScreen.jsx` devolve 1.
    - `grep -c "curadoriaErroBusca" web/src/opcoes/CuradoriaEstruturas.jsx` devolve valor maior que 0 e o ramo de vazio exige `!erro`: `grep -c "!erro" web/src/opcoes/CuradoriaEstruturas.jsx` devolve valor maior que 0.
    - `grep -c "tiraOpcoesSubtitulo" web/src/opcoes/OportunidadesOpcoes.jsx` devolve 1.
    - `grep -v '^\s*//' web/src/opcoes/OpcoesScreen.jsx | grep -c "aria-expanded"` não aumentou por causa da frase-ponte (ela não é colapsável); registrar antes/depois no SUMMARY.
    - Cadeia de execução intacta: `git diff --name-only` NÃO lista `web/src/opcoes/executarCandidato.js`, `web/src/persistence.js`, `web/src/api.js` nem `server/app/main.py`.
  </acceptance_criteria>
  <done>A aba Opções, na sub-aba Setups, abre mostrando a frase-ponte, o Bloco A e o Bloco B antes dos vigias; o painel curado executa pelo mesmo `ctx.A.executarCandidatoCurado` e o "ver posição" leva à sub-aba Operar com o ticker escolhido.</done>
</task>

<task type="auto">
  <name>Task 2: Posições perde os dois blocos e ganha a linha de chamada única</name>
  <files>web/src/App.jsx</files>
  <read_first>
    - `web/src/App.jsx` linhas 4738-4790 — corpo de `CarteiraScreen`: `opcoesFor`, `abrirOpcoesDe`, a leitura de `ctx.curadoria` que o Plano 32-02 deixou.
    - `web/src/App.jsx` linhas 4846-4881 — os dois call sites a remover e a guarda `data.positions.length > 0` que os envolve, mais o bloco de portfólio vazio logo abaixo.
    - `web/src/App.jsx` linhas 4478-4483 — o toggle de `PropostaDaPosicao`: é o analog de estilo literal da linha de chamada (padding, gap 7px, ⚡ em `T.accent`, seta em `T.textFaint`).
    - `.planning/phases/32-.../32-UI-SPEC.md` §"Linha de chamada em Posições" — estilo, os quatro estados e a regra de nunca mostrar "0" no estado de erro.
  </read_first>
  <action>
**(a) Remover.** De `CarteiraScreen`: apagar o bloco JSX
`{data.positions.length > 0 && (<> <OportunidadesOpcoes .../> <CuradoriaEstruturas .../> </>)}`
inteiro, e a função `abrirOpcoesDe` (o scroll-to-id não tem mais chamador nesta
tela; `opcoesFor`/`setOpcoesFor` continuam, porque `PropostaDaPosicao` ainda os
usa até o Plano 32-04). Remover também os imports de `OportunidadesOpcoes` e
`CuradoriaEstruturas` de `App.jsx`, que ficam órfãos.

**NÃO remover** o destructuring `{ propostas: opcoesPorTicker, carregando:
opcoesCarregando }` nem a chamada de `useOpcoesPropostas` em `CarteiraScreen`:
`PropostaDaPosicao` ainda consome `opcoesPorTicker` e só sai no Plano 32-04.
`opcoesCarregando` fica temporariamente sem consumidor (variável não usada, sem
erro) — deixar nota de uma linha dizendo isso e apontando o 32-04, para
ninguém "limpar" a declaração inteira e derrubar a tela em render.

**(b) Criar `function LinhaChamadaOpcoes({ curadoria, cp, onIr })`** em
`App.jsx`, logo antes de `function CarteiraScreen(`. Um único `<button
type="button">`, estilo derivado do toggle de `PropostaDaPosicao`: `display:
flex`, `width: 100%`, `alignItems: center`, `justifyContent: space-between`,
`minHeight: "44px"`, `padding: "9px 2px"`, `background: transparent`, `border:
none`, `borderBottom: 1px solid T.borderFaint`, `cursor: pointer`. Conteúdo:
`<span>` com ícone e texto (gap 8px, 13px) à esquerda e `<span>` com `→` em
`T.textFaint` à direita. `onClick={onIr}`. `aria-label={cp.linhaChamadaOpcoesAria(texto)}`.

Os quatro estados, nesta ordem de precedência, decididos a partir de
`curadoria` (`{ top, carregando, erro }`) — nunca de um número recalculado:
1. `erro` → texto `cp.linhaChamadaOpcoesErro`, cor do texto e do ícone
   `T.warn`, peso 700. **Nunca exibir contagem neste estado** (mostrar "0"
   quando a busca falhou é inventar valor — princípio 4 do CLAUDE.md).
2. `carregando` → texto `cp.linhaChamadaOpcoesCarregando`, cor `T.textFaint`,
   SEM o ícone `⚡` (não prometer resultado antes de saber).
3. `top.length === 0` → texto `cp.linhaChamadaOpcoesVazia`, cor `T.textFaint`,
   ícone em `T.textFaint`, peso normal (não 700). A linha continua visível e
   clicável — nunca some (princípio 9 do CLAUDE.md).
4. caso contrário → texto `cp.linhaChamadaOpcoesTexto(curadoria.top.length)`,
   ícone `⚡` em `T.accent`, cor `T.textPrimary`, peso 700.

Comentar acima do componente: a contagem vem EXCLUSIVAMENTE de
`curadoria.top.length`, o mesmo objeto que alimenta a lista na aba Opções
(D-03), e que esse número é teto 4 por desenho do motor (`opcoes_curadoria`
devolve no máximo 4) — a linha diz quantas aparecem na lista, nunca "o total
da carteira".

**(c) Renderizar.** Em `CarteiraScreen`, no lugar exato de onde os dois blocos
saíram (acima da guarda de portfólio vazio), sob a MESMA guarda
`data.positions.length > 0`: `<LinhaChamadaOpcoes curadoria={ctx.curadoria}
cp={cp} onIr={ctx.goOpcoes} />`. Comentar que a guarda é a mesma de antes
porque o estado vazio de portfólio logo abaixo já explica a ausência de
posições, e duas mensagens para a mesma ausência seria ruído (razão herdada da
Fase 18).
  </action>
  <verify>
    <automated>npm --prefix web run build && node -e "const s=require('fs').readFileSync('web/src/App.jsx','utf8');if(/<OportunidadesOpcoes|<CuradoriaEstruturas/.test(s))throw new Error('call site remanescente em App.jsx');if(!/function LinhaChamadaOpcoes/.test(s))throw new Error('LinhaChamadaOpcoes ausente');if(/abrirOpcoesDe/.test(s))throw new Error('abrirOpcoesDe deveria ter sido removido');const i=s.indexOf('function LinhaChamadaOpcoes');const f=s.slice(i,s.indexOf('function CarteiraScreen('));if(/top\.length\s*\)/.test(f)&&!/linhaChamadaOpcoesTexto/.test(f))throw new Error('contagem fora da chave de copy');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix web run build` sai com código 0.
    - O comando de verify imprime `ok`.
    - `grep -c "<OportunidadesOpcoes\|<CuradoriaEstruturas" web/src/App.jsx` devolve 0.
    - `grep -c "abrirOpcoesDe" web/src/App.jsx` devolve 0.
    - `grep -c "<LinhaChamadaOpcoes" web/src/App.jsx` devolve exatamente 1 (D-01: uma única linha de chamada).
    - A linha usa a navegação do ctx: `grep -c "onIr={ctx.goOpcoes}" web/src/App.jsx` devolve 1.
    - Os quatro estados existem no corpo do componente: `grep -c "linhaChamadaOpcoesErro\|linhaChamadaOpcoesCarregando\|linhaChamadaOpcoesVazia\|linhaChamadaOpcoesTexto" web/src/App.jsx` devolve 4.
    - O estado de erro não mostra número: no trecho entre `function LinhaChamadaOpcoes` e `function CarteiraScreen(`, a chamada a `linhaChamadaOpcoesTexto(` aparece exatamente 1× e não está no ramo de `erro` (verificar por leitura e registrar no SUMMARY).
    - `grep -c "curadoria={ctx.curadoria}" web/src/App.jsx` devolve 1 (fonte única, D-03).
  </acceptance_criteria>
  <done>Posições exibe uma única linha de chamada com contagem real vinda de `ctx.curadoria.top.length`, com os quatro estados distintos, e nenhum outro conteúdo de opções cross-posição.</done>
</task>

<task type="auto">
  <name>Task 3: Guardiões de âncora de USO + guardião novo da consolidação</name>
  <files>web/tests/test_curadoria_ui.mjs, web/tests/test_carteira_opcoes_tira.mjs, web/tests/test_opcoes_subabas_ui.mjs, web/tests/test_opcoes_consolidacao_ui.mjs</files>
  <read_first>
    - `web/tests/test_curadoria_ui.mjs` (versão já reescrita pelo Plano 32-02) — as regras de call site que ainda apontam para `App.jsx`.
    - `web/tests/test_carteira_opcoes_tira.mjs` (versão já reescrita pelo Plano 32-02) — as regras "`<OportunidadesOpcoes` aparece depois de `function CarteiraScreen(`" e "antes da guarda de portfólio vazio", que este plano invalida.
    - `web/tests/test_opcoes_subabas_ui.mjs` linhas 43-160 — regra 3 (contagem de `store.optionsGate(`/`optionsProposta(` em `OpcoesScreen.jsx`), regra de isolamento de import e o padrão multi-arquivo.
    - `web/tests/test_consolidacao_opcoes_copy.mjs` (criado no Plano 32-01) — reusar o helper `ok(...)` e a forma do arquivo.
  </read_first>
  <action>
Reescrever, nunca apagar; nota datada em cada arquivo tocado: "2026-09-15,
Fase 32 (32-03): os dois blocos cross-carteira mudaram de TELA — de
`CarteiraScreen` (`App.jsx`) para o topo da sub-aba Setups
(`OpcoesScreen.jsx`). As âncoras de USO passam a apontar para lá; Posições
passa a ter uma linha de chamada no lugar."

1. `test_curadoria_ui.mjs`: as regras de call site (`<CuradoriaEstruturas`
   aparece 1× no fonte; vem depois de `<OportunidadesOpcoes`; está dentro da
   guarda `data.positions.length > 0`) passam a rodar contra
   `OpcoesScreen.jsx`. Substituir a guarda `data.positions.length > 0` pela
   regra equivalente no destino: `{blocoCuradoria}` aparece depois de
   `{blocoOportunidades}` e antes de `{blocoVigias}`, dentro do ramo
   `subaba === "setups"`. Acrescentar: `<CuradoriaEstruturas` aparece 0× em
   `App.jsx`.
2. `test_carteira_opcoes_tira.mjs`: idem para `<OportunidadesOpcoes`. As
   regras que travavam a posição dentro de `CarteiraScreen` são substituídas
   por regras equivalentes no novo destino, e ganham a regra de que `App.jsx`
   não renderiza mais nenhum dos dois. Manter todas as regras de copy.
3. `test_opcoes_subabas_ui.mjs`: confirmar (e, se necessário, ajustar com
   nota) que `store.optionsGate(`/`store.optionsProposta(` continuam
   aparecendo exatamente 1× em `OpcoesScreen.jsx` — o fan-out novo mora em
   `useOpcoesPropostas.js`, que é outro arquivo. Acrescentar regra: nenhum
   dos módulos de `web/src/opcoes/` importa `App.jsx` (estender a regra de
   isolamento já existente aos arquivos criados nos Planos 32-02/32-03).
4. Criar `web/tests/test_opcoes_consolidacao_ui.mjs`, guardião da fase, com
   estas regras (todas por leitura estática de source):
   - Ordem no ramo `setups` de `OpcoesScreen.jsx`: `{fraseDuasLeituras}` <
     `{blocoOportunidades}` < `{blocoCuradoria}` < `{blocoVigias}`.
   - A frase-ponte é incondicional: o trecho que renderiza
     `cp.duasLeiturasIntro` não está dentro de um `&&`/ternário que dependa de
     `carteira.length`, `ticker`, `subaba` ou de qualquer estado de
     carregamento, e não tem `aria-expanded` nem `onClick` (D-05: nunca
     colapsável).
   - `App.jsx` não contém `<OportunidadesOpcoes` nem `<CuradoriaEstruturas`.
   - `App.jsx` contém `<LinhaChamadaOpcoes` exatamente 1× (D-01).
   - `LinhaChamadaOpcoes` referencia as quatro chaves de estado e chama
     `cp.linhaChamadaOpcoesTexto(` exatamente 1× (a contagem tem uma única
     origem, D-03).
   - `LinhaChamadaOpcoes` não recalcula contagem: seu corpo não contém
     `.filter(`, `.reduce(` nem `positions` — o número vem pronto de
     `curadoria.top.length`.
   - Navegação (D-02): o `onClick`/`onIr` da linha resolve para `ctx.goOpcoes`
     e `App.jsx` define `goOpcoes` como `navigate("opcoes")`; nenhum
     parâmetro de ticker/candidato é passado (sem deep-link).
   - D-06: no trecho de `OpcoesScreen.jsx` entre `{fraseDuasLeituras}` e
     `{blocoVigias}` não aparece `position: "sticky"`, `position: "fixed"`,
     nem nenhum `<input` de busca/filtro.
   - Regressão do collar curado: `OpcoesScreen.jsx` liga `onExecutar` a
     `ctx.A.executarCandidatoCurado` exatamente 1×;
     `web/src/opcoes/executarCandidato.js` continua despachando
     `optionsCuradoriaAbrirCollar` para `tipo === "collar"`; nenhum
     `store.`/`api.` é chamado de dentro de `CuradoriaEstruturas.jsx`.
   - Estado de erro do Bloco B: `CuradoriaEstruturas.jsx` referencia
     `cp.curadoriaErroBusca` e o ramo que mostra `cp.curadoriaVazio` exige
     `!erro` (a tela não afirma "nada elegível" quando a busca falhou).
   - Pitfall 4: `CuradoriaEstruturas.jsx` e `OpcoesScreen.jsx` não contêm
     `scrollIntoView` nem a âncora `"posicao-"`.
   - **Identificador pendurado em `App.jsx`** — a classe de defeito que este
     plano quase criou e que nem o build nem os guardiões de string pegam:
     para cada um de `opcoesPorTicker`, `opcoesCarregando` e `opcoesFor`, se o
     nome aparece USADO em `App.jsx` (ocorrência fora da linha de declaração),
     então a declaração correspondente também existe no arquivo. Implementar
     contando ocorrências e exigindo pelo menos uma linha de declaração
     (`const` ou destructuring) quando a contagem de uso for maior que zero.
     A regra vale para as duas waves e é o que impede um plano futuro remover
     a fonte antes do consumidor.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/executar.sh --testes` sai com código 0.
    - `node web/tests/test_opcoes_consolidacao_ui.mjs` sai 0 e o arquivo contém pelo menos 12 chamadas a `ok(`: `grep -v '^\s*//' web/tests/test_opcoes_consolidacao_ui.mjs | grep -c "ok("` devolve valor maior ou igual a 12.
    - `grep -c "2026-09-15" web/tests/test_curadoria_ui.mjs web/tests/test_carteira_opcoes_tira.mjs web/tests/test_opcoes_subabas_ui.mjs` devolve valor maior que 0 em cada um.
    - Nenhuma regra perdida: a contagem de `ok(` em `test_curadoria_ui.mjs` e `test_carteira_opcoes_tira.mjs` é maior ou igual à de antes deste plano (registrar antes/depois no SUMMARY).
    - `ls web/tests/*.mjs | wc -l` é maior que o valor de antes do plano (o guardião novo entrou, nenhum saiu).
    - O guardião novo falha se a frase-ponte virar colapsável: injetar temporariamente `aria-expanded` no trecho da frase, confirmar que `node web/tests/test_opcoes_consolidacao_ui.mjs` sai diferente de 0, e reverter a injeção (teste de sanidade do guardião; registrar no SUMMARY).
  </acceptance_criteria>
  <done>A suíte canônica passa inteira, existe um guardião dedicado à consolidação que reprova a volta dos blocos para Posições e a perda da frase-ponte, e nenhuma regra antiga foi descartada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → `POST /api/options/curadoria/abrir-collar` | Fronteira JÁ EXISTENTE (quick `260915-ndt`). Este plano muda de QUAL tela o botão é clicado, não o corpo enviado nem quem pode clicar. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-05 | Elevation of privilege | painel inline de `CuradoriaEstruturas` na aba Opções | mitigate | O ramo `{!operador && ...}`/`{operador && ...}` é movido verbatim; guardião reescrito continua exigindo o CTA condicionado a `operador`. A defesa real é o 403 do servidor (ADR-013), inalterado — nenhuma rota, permissão ou gate mudou |
| T-32-06 | Tampering (integridade da afirmação da tela) | ramo de erro do Bloco B e estado de erro da linha de chamada | mitigate | Erro passa a ter texto próprio e precedência sobre o vazio; a linha de chamada nunca exibe contagem no estado de erro. Travado por asserção no guardião novo |
| T-32-07 | Repudiation (rastreabilidade da execução) | cadeia `onExecutar` → `ctx.A.executarCandidatoCurado` → `executarCandidato.js` → rota | mitigate | A cadeia não é tocada; o guardião novo trava que `onExecutar` resolve para `ctx.A.executarCandidatoCurado` e que `executarCandidato.js` segue despachando `optionsCuradoriaAbrirCollar` para collar. `git diff --name-only` não pode listar `executarCandidato.js`/`persistence.js`/`api.js`/`server/` |
| T-32-SC | Tampering (cadeia de suprimentos) | npm install | accept | Nenhum pacote instalado nesta fase |

Fora esses itens, a superfície de ameaça é INALTERADA: nenhuma rota nova,
nenhum input novo aceito do cliente, nenhum dado novo exposto — só mudou em
que tela um componente já existente é renderizado.
</threat_model>

<verification>
- `npm --prefix web run build` — sai 0
- `bash scripts/executar.sh --testes` — sai 0
- `git diff --name-only` não inclui `web/src/opcoes/executarCandidato.js`, `web/src/persistence.js`, `web/src/api.js` nem nada sob `server/`
</verification>

<success_criteria>
- A sub-aba Setups abre com frase-ponte → Bloco A → Bloco B → vigias.
- Posições tem exatamente uma linha de chamada e nenhum bloco de opções cross-posição.
- A contagem da linha vem de `ctx.curadoria.top.length`, sem segunda chamada.
- O estado de erro da curadoria não se disfarça mais de "nada elegível".
- Nada nesta fase impede acrescentar busca na aba depois (D-06).
</success_criteria>

<output>
Create `.planning/phases/32-consolida-o-das-opera-es-de-op-es-na-aba-op-es-standalone/32-03-SUMMARY.md` when done
</output>
