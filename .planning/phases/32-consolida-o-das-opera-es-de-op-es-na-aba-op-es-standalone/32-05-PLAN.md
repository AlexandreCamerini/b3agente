---
phase: 32-consolida-o-das-opera-es-de-op-es-na-aba-op-es-standalone
plan: 05
type: execute
wave: 5
depends_on: ["32-03", "32-04"]
files_modified:
  - server/web_dist
  - web/package.json
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07]

must_haves:
  truths:
    - "Executar o collar curado a partir do card, na aba Opções, abre a posição de verdade"
    - "A linha de chamada em Posições leva à aba Opções e o número bate com o que a lista mostra"
    - "A versão publicada em produção contém a consolidação (o merge não fica testado e invisível)"
  artifacts:
    - path: "server/web_dist"
      provides: "bundle publicado com a consolidação, a única árvore que o Railway enxerga"
  key_links:
    - from: "card curado na aba Opções"
      to: "POST /api/options/curadoria/abrir-collar"
      via: "ctx.A.executarCandidatoCurado → executarCandidato.js → store.optionsCuradoriaAbrirCollar"
      pattern: "optionsCuradoriaAbrirCollar"
---

<objective>
Provar ao vivo que a consolidação não quebrou a rota de execução do collar
curado, colher a aprovação do Alex sobre o resultado visual, e publicar.

Purpose: duas razões específicas deste repositório. (1) O `32-CONTEXT.md` marca
a cadeia de execução do collar curado (quick `260915-ndt`) como a regressão
mais provável da fase — e a pesquisa mostrou que ela é invisível para teste
estático, porque a cadeia não depende de onde o card é renderizado; só um
clique real prova. (2) Fase que toca `web/src/` sem task de publicação fica
testada e nunca vai ao ar (lição registrada em `fase-sem-plano-de-publicacao-front`).
Output: evidência de execução real, aprovação do Alex e bundle publicado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/32-consolida-o-das-opera-es-de-op-es-na-aba-op-es-standalone/32-CONTEXT.md
@.planning/phases/32-consolida-o-das-opera-es-de-op-es-na-aba-op-es-standalone/32-03-SUMMARY.md
@.planning/phases/32-consolida-o-das-opera-es-de-op-es-na-aba-op-es-standalone/32-04-SUMMARY.md
@.planning/quick/260915-ndt-re-derivar-execucao-de-candidato-curado-/260915-ndt-PLAN.md

<interfaces>
Cadeia de execução do collar curado, rastreada ponta a ponta no
`32-RESEARCH.md` (deve estar INTACTA depois da fase):

1. clique no card de `CuradoriaEstruturas` abre a confirmação inline
2. botão executar chama o `handleExecutar` local, que chama
   `onExecutar(item, { aceitaLiquidezDificil })`
3. o call site injeta `onExecutar` como `ctx.A.executarCandidatoCurado`
4. `A.executarCandidatoCurado` chama `executarCandidato(cand, { store, aceitaLiquidezDificil })`
   de `web/src/opcoes/executarCandidato.js` — módulo puro, sem closure sobre `App.jsx`
5. para `tipo === "collar"`, o método resolvido é `optionsCuradoriaAbrirCollar`,
   que chama `POST /api/options/curadoria/abrir-collar`

Publicação (disciplina do repositório):
- `bash scripts/bump.sh` ANTES de `bash scripts/publicar-web.sh`; nunca editar
  `server/web_dist` à mão.
- `bump.sh` deriva do valor LOCAL: mesclar `origin/main` ANTES de bumpar, e
  `web_dist` se REGENERA, não se mescla (lição `bump-colide-em-sessao-paralela`).
- Railway só observa `main`: publicar exige push nas DUAS branches — a de
  trabalho e `main` (lição registrada no STATE.md em 2026-09-15).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditoria de não-regressão da cadeia de execução e da suíte inteira</name>
  <files>(nenhum arquivo de produção é modificado nesta task)</files>
  <read_first>
    - `web/src/opcoes/executarCandidato.js` inteiro — o despacho por tipo; confirmar que nenhuma linha mudou nesta fase.
    - `.planning/quick/260915-ndt-re-derivar-execucao-de-candidato-curado-/260915-ndt-PLAN.md` — o contrato da rota e do despacho.
    - `web/src/opcoes/OpcoesScreen.jsx` — o ponto onde `onExecutar` é ligado a `ctx.A.executarCandidatoCurado` (entregue pelo Plano 32-03).
    - `.planning/phases/32-.../32-RESEARCH.md` seção "Cadeia de execução do collar curado".
  </read_first>
  <action>
Auditoria de evidência, sem editar código de produção. Produzir, no SUMMARY,
cada item abaixo com o comando e a saída:

1. `git diff --name-only main...HEAD` (ou o range que cobre a fase) NÃO lista
   `web/src/opcoes/executarCandidato.js`, `web/src/persistence.js`,
   `web/src/api.js` nem nenhum arquivo sob `server/app/`. Se listar, é ACHADO:
   parar e reportar antes de qualquer publicação.
2. `web/src/opcoes/executarCandidato.js` continua mapeando `tipo === "collar"`
   para `optionsCuradoriaAbrirCollar`; `web/src/persistence.js` continua com o
   método nos DOIS stores (`deviceStore` e `serverStore`) — a paridade
   obrigatória do CLAUDE.md.
3. `ctx.A.executarCandidatoCurado` tem exatamente um chamador de UI, em
   `web/src/opcoes/OpcoesScreen.jsx`, e zero em `web/src/App.jsx`.
4. `bash scripts/executar.sh --testes` — as DUAS suítes, saída 0. Registrar o
   total de testes de cada suíte.
5. `npm --prefix web run build` — saída 0.
6. Rodar a bateria completa de guardiões tocados na fase, nominalmente, e
   registrar a saída de cada um: `test_consolidacao_opcoes_copy.mjs`,
   `test_opcoes_consolidacao_ui.mjs`, `test_curadoria_ui.mjs`,
   `test_carteira_opcoes_tira.mjs`, `test_opcoes_multi_candidato_ui.mjs`,
   `test_opcoes_subabas_ui.mjs`, `test_opcoes_proposta_ui.mjs`,
   `test_opcoes_collar_ui.mjs`, `test_opcoes_analisar_ui.mjs`,
   `test_vocabulario_opcoes.mjs`, `test_wiring_deps.mjs`.
7. Contagem de regras: para cada guardião reescrito nos Planos 32-02/03/04,
   comparar o número de chamadas `ok(` com o valor registrado nos SUMMARYs
   correspondentes. Nenhum pode ter diminuído. Se algum diminuiu, é ACHADO:
   reportar qual regra sumiu e por quê antes de seguir.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes && npm --prefix web run build</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/executar.sh --testes` sai com código 0.
    - `npm --prefix web run build` sai com código 0.
    - `git diff --name-only main...HEAD` não contém `executarCandidato.js`, `persistence.js`, `api.js` nem `server/app/`.
    - `grep -c "optionsCuradoriaAbrirCollar" web/src/opcoes/executarCandidato.js` devolve valor maior que 0.
    - `grep -c "optionsCuradoriaAbrirCollar" web/src/persistence.js` devolve valor maior ou igual a 2 (um por store).
    - `grep -c "executarCandidatoCurado" web/src/opcoes/OpcoesScreen.jsx` devolve 1 e `grep -v "^\s*//" web/src/App.jsx | grep -c "A.executarCandidatoCurado"` devolve 0.
    - O SUMMARY contém a tabela antes/depois da contagem de `ok(` por guardião, sem nenhuma redução.
  </acceptance_criteria>
  <done>Existe evidência escrita de que a cadeia de execução não foi tocada, a suíte canônica passa inteira e nenhum guardião perdeu regra.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificação ao vivo com o Alex</name>
  <files>(nenhum arquivo é modificado — é verificação humana bloqueante)</files>
  <read_first>
    - `.planning/phases/32-.../32-UI-SPEC.md` seções "Linha de chamada em Posições", "Os dois rótulos (D-05)" e "Estados completos por bloco" — o roteiro abaixo confere exatamente o que o contrato aprovado prometeu.
    - `.planning/phases/32-.../32-03-SUMMARY.md` e `32-04-SUMMARY.md` — o que cada plano entregou, para o roteiro não conferir coisa que ficou fora.
  </read_first>
  <action>
Subir o app localmente e conduzir com o Alex os dez itens de `how-to-verify`
abaixo, nesta ordem, registrando a resposta dele item por item no SUMMARY. O
item 5 (execução real do collar curado, em Modo Operador, com posição aberta e
caixa alterado) é o que prova a não-regressão marcada como mais provável da
fase — nenhum teste estático cobre essa cadeia. Nada é publicado antes da
aprovação: o checkpoint segura o push da FASE INTEIRA, não só desta task.
Se qualquer item sair diferente do esperado, PARAR e reportar a divergência
com a mensagem de erro literal, sem tentar corrigir dentro deste plano.
  </action>
  <what-built>
Posições passou a ter uma única linha de chamada com contagem; a aba Opções, na
sub-aba Setups, abre com a frase-ponte dos dois motores, o bloco "confirmadas
pela leitura técnica" e a lista das 4 melhores, antes dos vigias; a sub-aba
Operar passou a mostrar N candidatos lado a lado quando existem. A cadeia de
execução do collar curado não foi tocada — mas isso é verificável por clique,
não por teste estático.
  </what-built>
  <how-to-verify>
Rodar local (`bash scripts/executar.sh` ou o fluxo de dev habitual) e conferir,
nesta ordem:

1. **Posições** — existe UMA linha, discreta, com "N oportunidades de opções
   nas suas posições" e uma seta. Não existe mais nenhuma tira de cartões, nem
   a lista das 4, nem o acordeão "Estrutura de opções possível nesta posição"
   dentro de cada card de posição.
2. **Contagem (D-03)** — anotar o N da linha. Tocar na linha: deve abrir a aba
   Opções, na lista, sem nenhum ativo pré-selecionado. Conferir que a lista das
   4 melhores mostra exatamente N cartões.
3. **Frase-ponte (D-05)** — o texto "Duas leituras diferentes da sua carteira…"
   está visível acima dos dois blocos e não tem como recolher.
4. **Rótulos (D-05)** — o primeiro bloco diz que é o que a leitura técnica
   confirma; o segundo diz que varre a cadeia inteira, inclusive o que a
   leitura técnica não confirma.
5. **Execução do collar curado (a regressão mais provável)** — em Modo
   Operador, tocar num card de collar da lista das 4, confirmar inline e
   executar. A posição tem de aparecer de verdade na carteira e o caixa tem de
   mudar. Se der erro, copiar a mensagem inteira.
6. **"Ver detalhe da posição"** dentro do painel inline — tem de levar à
   sub-aba Operar com aquele ativo escolhido, não fazer nada nem rolar.
7. **Erro de busca** — se possível (modo avião, ou backend derrubado por um
   instante), confirmar que o bloco das 4 diz que a BUSCA FALHOU e oferece
   "Tentar de novo" — e que a linha em Posições diz "Não foi possível
   verificar agora", SEM mostrar o número zero.
8. **Multi-candidato** — numa posição com 2+ candidatos, a sub-aba Operar
   mostra os dois cartões lado a lado, com a mesma largura.
9. **Modo Estudo** — trocar para Estudo e confirmar que nenhum botão de
   executar aparece em nenhum dos blocos.
10. **Rolagem (D-06)** — a aba fica mais longa, e isso é esperado. Só confirmar
    que nada ficou grudado no topo cobrindo conteúdo.
  </how-to-verify>
  <resume-signal>Responder "aprovado" ou descrever o que saiu diferente, item por item</resume-signal>
  <verify>
    <automated>echo "checkpoint bloqueante — verificacao humana; a evidencia e a resposta do Alex registrada no SUMMARY"</automated>
  </verify>
  <acceptance_criteria>
    - O SUMMARY registra a resposta do Alex para cada um dos 10 itens, nominalmente.
    - O item 5 registra o resultado da execução real: ticker, tipo da estrutura, posição aberta e variação de caixa observada — ou a mensagem de erro literal, se falhou.
    - O item 2 registra o número N da linha de chamada e o número de cartões da lista curada, e os dois são iguais.
    - Nenhum passo de publicação foi executado antes da aprovação (verificável por `git log`: nenhum commit de bump/publicação anterior à data do checkpoint).
  </acceptance_criteria>
  <done>O Alex aprovou explicitamente, ou as divergências estão registradas com evidência literal e a fase não avançou para a publicação.</done>
</task>

<task type="auto">
  <name>Task 3: Bump, publicação do front e push nas duas branches</name>
  <files>web/package.json, server/web_dist</files>
  <read_first>
    - `scripts/publicar-web.sh` linhas 1-40 — os três modos e a razão de `server/web_dist` ser a única árvore que o Railway enxerga.
    - `scripts/bump.sh` — como o número é derivado (valor LOCAL, daí a ordem obrigatória de mesclar antes).
    - `.planning/STATE.md`, entrada de 2026-09-15 sobre o Railway observar apenas `main`.
  </read_first>
  <action>
Executar na ordem, parando e reportando em qualquer falha:

1. Mesclar `origin/main` na branch de trabalho ANTES de bumpar (o `bump.sh`
   deriva do valor local; bumpar antes do merge colide com sessão paralela).
   Se houver conflito em `server/web_dist`, NÃO mesclar o dist: ele se
   regenera no passo 3.
2. `bash scripts/bump.sh`.
3. `bash scripts/publicar-web.sh` — builda e publica em `server/web_dist`.
4. Revisar o diff gerado (`git status` + `git diff --stat`) e conferir que o
   dist novo contém a consolidação.
5. `bash scripts/executar.sh --testes` de novo, depois do bump/publicação.
6. Commit dos artefatos de publicação.
7. Push nas DUAS branches: a branch de trabalho E `main`
   (`git push origin HEAD:main` além do push normal) — o Railway só observa
   `main`, e push apenas na branch de trabalho deixa a fase testada e
   invisível em produção.
8. Confirmar o deploy: consultar `/api/health` e o carimbo de build da
   produção (boris.semente.dev) e registrar o valor no SUMMARY.

Nenhum passo deste plano edita `server/web_dist` à mão.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes && git status --porcelain</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/executar.sh --testes` sai com código 0 depois do bump e da publicação.
    - `git log --oneline -1` mostra o commit de publicação, e `git status --porcelain` fica vazio ao final.
    - `git rev-parse HEAD` e `git rev-parse origin/main` apontam para o mesmo commit (o push em `main` aconteceu).
    - A versão carimbada em produção, consultada por HTTP, bate com a gerada pelo `bump.sh`; o valor consultado está registrado no SUMMARY.
    - `server/web_dist` foi regenerado pelo script, nunca editado à mão: o diff do dist é resultado de build (nenhum hunk manual).
  </acceptance_criteria>
  <done>A consolidação está em produção, com o carimbo de build confirmado por consulta HTTP, e as duas branches apontam para o mesmo commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repositório → produção (Railway, `main`) | Fronteira de deploy JÁ EXISTENTE. Este plano publica o bundle; não muda quem pode publicar nem o que o servidor aceita. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-10 | Tampering (artefato de build) | `server/web_dist` | mitigate | Dist só é gerado por `scripts/publicar-web.sh`; edição manual é proibida por guardrail do repositório e verificada pelo critério de aceite (diff é resultado de build) |
| T-32-11 | Denial of service (publicar quebrado) | deploy em `main` | mitigate | Ordem obrigatória: auditoria + suíte canônica, depois checkpoint humano bloqueante com execução real do collar, e só então bump/publicação. O push da fase inteira fica retido até a aprovação |
| T-32-12 | Information disclosure | segredos no bundle | accept | Nenhuma variável nova foi adicionada nesta fase; `BRAPI_TOKEN`/chaves seguem só no env do servidor, e nenhum arquivo de `web/src/` tocado nesta fase lê segredo |
| T-32-SC | Tampering (cadeia de suprimentos) | npm install durante o build | accept | Nenhum pacote novo foi adicionado na fase; o build usa o `package-lock.json` já versionado |

Superfície de ameaça técnica INALTERADA quanto a rota, input e autorização.
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` — sai 0 antes e depois da publicação
- `npm --prefix web run build` — sai 0
- Clique real no card de collar curado abre posição real (checkpoint, Task 2 item 5)
- Carimbo de build da produção consultado por HTTP bate com o gerado
</verification>

<success_criteria>
- Evidência escrita de que a cadeia de execução do collar curado não foi tocada e continua funcionando por clique real.
- Aprovação do Alex nos 10 itens da verificação ao vivo.
- Consolidação em produção, confirmada por consulta ao carimbo de build.
- Branch de trabalho e `main` no mesmo commit.
</success_criteria>

<output>
Create `.planning/phases/32-consolida-o-das-opera-es-de-op-es-na-aba-op-es-standalone/32-05-SUMMARY.md` when done
</output>
