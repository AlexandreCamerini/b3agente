---
phase: 39-reestrutura-o-de-navega-o-da-aba-op-es
plan: 05
type: execute
wave: 4
depends_on: ["39-04"]
files_modified:
  - web/tests/test_opcoes_subabas_ui.mjs
  - web/tests/test_opcoes_hub_workspace_ui.mjs
  - web/tests/test_opcoes_jornada_ui.mjs
  - web/tests/test_opcoes_consolidacao_ui.mjs
  - web/tests/test_curadoria_ui.mjs
  - web/tests/test_carteira_opcoes_tira.mjs
  - web/tests/test_opcoes_multi_candidato_ui.mjs
  - web/tests/test_opcoes_proposta_ui.mjs
  - web/tests/test_opcoes_collar_ui.mjs
  - web/tests/test_faixa_liquidez_ui.mjs
  - web/tests/test_fase22_componentes_compartilhados.mjs
  - web/tests/test_opcoes_analisar_ui.mjs
  - web/tests/test_opcoes_vigias_ui.mjs
  - web/tests/test_opcoes_custo_declarado.mjs
  - web/tests/test_kb_ancoras.mjs
  - web/tests/test_opcoes_mcp_aba_ui.mjs
  - web/tests/test_opcoes_analisar_executar_ui.mjs
autonomous: true
requirements: [NAV-01]

must_haves:
  truths:
    - "A suíte canônica (pytest + web/tests/*.mjs) volta à baseline conhecida depois da reestruturação — nenhuma falha nova além das ambientais já documentadas"
    - "Todo invariante que a navegação nova MANTÉM continua travado, re-ancorado no lugar novo (ex.: PropostaDoAtivo herda as travas de SubAbaOperar)"
    - "Todo invariante que a Fase 39 REVERTEU de propósito está reescrito com nota datada citando a decisão (D-XX), nunca apagado em silêncio"
  artifacts:
    - path: "web/tests/test_opcoes_subabas_ui.mjs"
      provides: "travas do ex-SubAbaOperar re-ancoradas em PropostaDoAtivo"
      contains: "PropostaDoAtivo"
    - path: "web/tests/test_opcoes_hub_workspace_ui.mjs"
      provides: "asserções do split hub/workspace convertidas em asserções da aba Montar, com nota de reversão"
      contains: "Fase 39"
  key_links:
    - from: "web/tests/*.mjs"
      to: "web/src/opcoes/OpcoesScreen.jsx (estrutura do Plano 04)"
      via: "fatias por nome de função/const re-ancoradas"
      pattern: "PropostaDoAtivo|abaOpcoes"
---

<objective>
Reconciliar os guardiões que a reestruturação do Plano 04 deixou vermelhos, sem
enfraquecer nenhum invariante que continua valendo, e fechar a fase com a suíte
canônica na baseline.

Purpose: regra do repositório "guardiões de teste não se apagam — reversão
deliberada atualiza o guardião com nota". Output: suíte verde, histórico de
decisão preservado nos próprios testes.
</objective>

<repo_guardrail>
Regra de classificação, aplicada a CADA `ok(` que falhar (registrar a
classificação por arquivo no SUMMARY):

(a) **Âncora mudou, invariante vale** -> re-ancorar mantendo a MESMA condição. Mapa de âncoras: `function SubAbaOperar` -> `function PropostaDoAtivo`; `SecaoDescobrir.jsx` -> `AbaOportunidades.jsx` (carimbo agregado/OportunidadesOpcoes) e `AbaRecomendadas.jsx` (carimbo da curadoria/CuradoriaEstruturas); `hubTopo`/`workspaceTopo` -> ramo `abaOpcoes === "montar"`; `const subabas = (`/`const workspacePillRow = (` -> `const abaBar = (`; "saiba mais" no topo -> `infoDaAba(` nas 3 abas; `opcoesCustoFrescor` no `cabecalho` -> `DetalheInfo` do rodapé; `<SecaoVigias` no hub -> dentro de `<VigiasSheet`.

(b) **Invariante revertido de propósito pela Fase 39** -> reescrever a asserção para o invariante NOVO, com comentário "Fase 39 (NAV-01, D-XX, 2026-09-24): <o que mudou e por quê>". Lista fechada de reversões autorizadas:
  - frase-ponte `duasLeiturasIntro` adjacente aos dois blocos (Fase 32 D-05 / Fase 33 D-05) -> os dois motores não dividem tela; a negação de hierarquia vive em `curadoriaSubtitulo` (asserção nova: curadoriaSubtitulo dos 2 modos contém "não" + "promessa" e "Oportunidades") — Plano 39-02, fork 3;
  - sub-abas Setups/Operar e pill row Analisar/Comparar/Setups salvos (Fases 28/34) -> `abaBar` de 3 abas fixas (D-01);
  - `WorkspaceHeader` + botão Voltar e "seletor não vaza para o workspace" (Fase 34 NAV-03) -> o seletor de chips é SEMPRE visível em Montar e é o único caminho de troca/desseleção (D-04, UI-SPEC);
  - gate do ramo DADOS por pill e contagem de 3 ocorrências de `abaWorkspace` (Fases 34/35) -> Analisar e Setups salvos sempre visíveis com leitura; Comparar atrás de `compararAberto` (D-06);
  - convite pago escondido na pill "Setups salvos" (Fase 35 D-02) -> convite sob `podePedirLeitura` apenas (não há mais pill);
  - bloco "Seus vigias" sempre visível no hub (Fase 27/33) -> badge + sheet (D-07);
  - "saiba mais" fixo no topo (Fase 38 D-07/D-08) -> ⓘ por aba, mesmo vid (D-13);
  - custo do frescor inline no cabeçalho (Fase 27-05) -> ⓘ no rodapé, sem condição de render (D-14);
  - linha de razão/"Pontuação de curadoria" (Fase 37 D-04) -> "1ª de N" (D-14);
  - `goOpcoes` sem parâmetro (Fase 32) -> id de aba allowlisted (já feito no Plano 02).

(c) **Qualquer outra falha** = defeito do Plano 04, não do teste -> corrigir o CÓDIGO (OpcoesScreen.jsx etc.) e registrar no SUMMARY como desvio. Nunca afrouxar asserção para caber.

Nunca remover um `ok(` sem um substituto que teste a condição (a) ou (b). Contagem de `ok(` por arquivo antes/depois vai para o SUMMARY.
</repo_guardrail>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-reestrutura-o-de-navega-o-da-aba-op-es/39-CONTEXT.md
@.planning/phases/39-reestrutura-o-de-navega-o-da-aba-op-es/39-04-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: guardiões estruturais da navegação (subabas, hub/workspace, jornada, consolidação, curadoria)</name>
  <files>web/tests/test_opcoes_subabas_ui.mjs, web/tests/test_opcoes_hub_workspace_ui.mjs, web/tests/test_opcoes_jornada_ui.mjs, web/tests/test_opcoes_consolidacao_ui.mjs, web/tests/test_curadoria_ui.mjs</files>
  <read_first>
    - .planning/phases/39-reestrutura-o-de-navega-o-da-aba-op-es/39-04-SUMMARY.md (lista de guardiões vermelhos)
    - Cada um dos 5 arquivos de teste, INTEIRO (cabeçalho de itens + corpo)
    - web/src/opcoes/OpcoesScreen.jsx, web/src/opcoes/AbaOportunidades.jsx, web/src/opcoes/AbaRecomendadas.jsx (estado pós-Plano 04)
  </read_first>
  <action>
    Para cada arquivo: rodar, listar os `ok(` vermelhos, classificar por (a)/(b)/(c) do repo_guardrail e aplicar. Pontos específicos já conhecidos:
    - `test_opcoes_subabas_ui.mjs`: a fatia `function SubAbaOperar` vira `function PropostaDoAtivo` e TODAS as travas dela continuam (sem store.mcp*, sem store.options*, usa PropostaLastreada/CandidatoOpcao/useAceiteLastreado, sem window.confirm próprio, sem A.abrirLastreada direto, modo só de ctx.operador, sem watchlist, toda cp.X existe nos dois modos); a asserção `const subabas = (` vira `const abaBar = (` com os mesmos checks de aria-pressed/44px; a varredura `RENDERIZADORES_DE_MANCHETE` continua (AbaOportunidades/AbaRecomendadas NÃO entram na allowlist porque não renderizam manchete); contagens de arquivos da pasta (>= 10/15) seguem válidas.
    - `test_opcoes_hub_workspace_ui.mjs`: itens 1-7 (WorkspaceHeader) são reversão (b) — substituir por asserções equivalentes sobre o que tomou o lugar (seletor em Montar com aria-pressed/44px; `escolherTicker` continua o ÚNICO caminho de reset, incluindo `setCompararAberto(false)`; `setTicker("")` não aparece solto fora de `escolherTicker`); itens 8-9 (carregando/erro únicos, sem duplicação) continuam valendo DENTRO do ramo montar — re-ancorar (a); item 13 (adjacência frase-ponte/Vigias) é reversão (b); asserções da pill row (34-03) viram asserções da `abaBar` e do gate `compararAberto`. `SecaoSetups.jsx` mantendo `cp.opcoesSetupsTitulo`/`cp.opcoesCriarTitulo` continua (a).
    - `test_opcoes_jornada_ui.mjs`: "Passo 1 de 2"/"Passo 2 de 2", "leitura já feita" e `cp.opcoesLeituraConcluidaAjuda` exatamente 1x continuam (a), re-ancorados no ramo montar; o gate `abaWorkspace !== "setups"` do convite é reversão (b) -> convite sob `podePedirLeitura`; a varredura PROIBIDO_SOBRE_ACCENT (#fff/white/color-mix sobre accent) passa a incluir CuradoriaEstruturas.jsx SÓ para o botão Executar (a correção do Plano 03) — se o regex do arquivo inteiro pegar o `color-mix(... T.warn ...)` legítimo de fundo de erro, restringir a checagem à fatia do botão Executar, com nota.
    - `test_opcoes_consolidacao_ui.mjs`: asserções sobre SecaoDescobrir/frase-ponte são (b); `onExecutar={(cand, o) => ctx.A.executarCandidatoCurado(cand, o)}` exatamente 1x em OpcoesScreen.jsx continua (a); App.jsx `onIr` já na forma do Plano 02.
    - `test_curadoria_ui.mjs` (itens 302-349 da Fase 32/33): `curadoria={ctx.curadoria}` inteiro agora vai para `<AbaRecomendadas`; `AbaRecomendadas.jsx` lê `curadoria.top`/`curadoria.meta` sem reordenar; `<CuradoriaEstruturas` exatamente 1x na pasta (em AbaRecomendadas.jsx) e 0x em OpcoesScreen.jsx; a ordem "OportunidadesOpcoes antes de CuradoriaEstruturas no mesmo arquivo" e "SecaoDescobrir antes de SecaoVigias" são (b) — substituir por "cada uma mora no próprio arquivo de aba e cada arquivo tem o seu CarimboFrescor".
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2 && for t in test_opcoes_subabas_ui test_opcoes_hub_workspace_ui test_opcoes_jornada_ui test_opcoes_consolidacao_ui test_curadoria_ui test_opcoes_nav_tres_abas_ui test_opcoes_nav_primitivos_ui; do node web/tests/$t.mjs >/dev/null || { echo "FALHOU $t"; exit 1; }; done; echo GRUPO1_OK</automated>
  </verify>
  <acceptance_criteria>
    - Os 7 arquivos do verify saem 0 (imprime GRUPO1_OK)
    - `grep -c "Fase 39" web/tests/test_opcoes_hub_workspace_ui.mjs` >= 3 (notas de reversão presentes)
    - `grep -c "function PropostaDoAtivo" web/tests/test_opcoes_subabas_ui.mjs` >= 1
    - Para cada um dos 5 arquivos, número de `ok(` depois >= número antes (conferido por `grep -c "ok(" ` antes/depois, valores no SUMMARY)
  </acceptance_criteria>
  <done>Guardiões estruturais verdes, invariantes vigentes re-ancorados, reversões com nota datada.</done>
</task>

<task type="auto">
  <name>Task 2: guardiões restantes + suíte canônica + build</name>
  <files>web/tests/test_carteira_opcoes_tira.mjs, web/tests/test_opcoes_multi_candidato_ui.mjs, web/tests/test_opcoes_proposta_ui.mjs, web/tests/test_opcoes_collar_ui.mjs, web/tests/test_faixa_liquidez_ui.mjs, web/tests/test_fase22_componentes_compartilhados.mjs, web/tests/test_opcoes_analisar_ui.mjs, web/tests/test_opcoes_vigias_ui.mjs, web/tests/test_opcoes_custo_declarado.mjs, web/tests/test_kb_ancoras.mjs, web/tests/test_opcoes_mcp_aba_ui.mjs, web/tests/test_opcoes_analisar_executar_ui.mjs</files>
  <read_first>
    - .planning/phases/39-reestrutura-o-de-navega-o-da-aba-op-es/39-04-SUMMARY.md (lista de guardiões vermelhos)
    - Cada arquivo de teste vermelho, INTEIRO
    - web/src/opcoes/OpcoesScreen.jsx (estado pós-Plano 04)
  </read_first>
  <action>
    Mesmo procedimento (a)/(b)/(c). Pontos conhecidos:
    - multi_candidato / proposta / collar / faixa_liquidez / fase22: referências a `SubAbaOperar` são (a) -> `PropostaDoAtivo`; o ramo multi-candidato (carrossel de CandidatoOpcao com largura igual + FonteDoDadoProposta) e o `onVerbeteLiquidez` via `A.abrirVerbete("liquidez-opcao", ...)` continuam exigidos.
    - carteira_opcoes_tira: `onAbrir={irParaOperar}` é (b) -> em OpcoesScreen o `onAbrir` de `<AbaOportunidades` é `alternarOportunidade` e abre `<PropostaDoAtivo` inline (fork 1, Leitura B'); OportunidadesOpcoes continua chamando `onAbrir(p.t)`.
    - vigias_ui: `onIr={irParaVigia}` continua (a); a asserção de que o bloco "Seus vigias" aparece sem ticker vira (b): `<SecaoVigias` dentro de `<VigiasSheet` e `<VigiasBadge` fora dos ramos de aba; o custo do "Atualizar" (CUSTO_DA_ACAO.listarVigias) continua declarado dentro da seção.
    - custo_declarado: `cp.opcoesCustoFrescor` continua referenciado em OpcoesScreen.jsx (a) — agora dentro de `<DetalheInfo` no rodapé; manter a exigência "sem condição de render" aplicada ao DetalheInfo (ele está fora dos ramos de aba).
    - kb_ancoras: a fatia `ANCORAS_KB.opcoes) && (` do botão fixo é (b) -> exigir o mesmo portão (`ctx.didatica.ligada` + `verbeteDoCatalogo(ctx.kbCatalogo, ANCORAS_KB.opcoes)`) na definição de `abrirSaibaMais`, `infoDaAba(` 3x, e a folha local `ConceitoSheet fonte="kb"` intacta; OpcoesScreen continua sem import de App.jsx.
    - analisar_ui / analisar_executar_ui / mcp_aba_ui: "custo de MCP só em clique" e "ticker nasce vazio" continuam (a); `SecaoAnalisar` agora sem gate de aba — ajustar a âncora do gate para o ramo montar.
    Depois, a suíte canônica completa: `bash scripts/executar.sh --testes`. Baseline conhecida a comparar (STATE.md, Fase 38): pytest ~2964+ passed com até 27 falhas de TLS-sandbox pré-existentes (rodar fora do sandbox se o número divergir, registrando os dois) e todos os `.mjs` verdes exceto `test_ios_assets.mjs` (ambiental — `web/ios/` gitignored). Qualquer outra falha é da fase e tem de ser corrigida antes de fechar. Por fim `cd web && npx vite build`.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2 && bash scripts/executar.sh --testes; for t in web/tests/*.mjs; do node "$t" >/dev/null 2>&1 || echo "VERMELHO $t"; done; (cd web && npx vite build >/dev/null && echo BUILD_OK)</automated>
  </verify>
  <acceptance_criteria>
    - O loop dos `.mjs` imprime no máximo `VERMELHO web/tests/test_ios_assets.mjs` (ambiental, pré-existente)
    - pytest sem falha nova além do conjunto TLS-sandbox documentado (lista de falhas do pytest colada no SUMMARY; se divergir, rodar fora do sandbox e colar as duas medições)
    - `npx vite build` BUILD_OK
    - Nenhum arquivo de `web/tests/` deletado nesta fase: `git log --diff-filter=D --name-only --format= 535a3d6..HEAD -- web/tests server/tests` vazio (535a3d6 = último commit antes do planejamento da Fase 39)
  </acceptance_criteria>
  <done>Suíte canônica na baseline, build verde, cada reversão documentada no próprio teste.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guardiões -> produção | testes são o que impede regressão de CVM/ADR-027/custo de MCP entrar no ar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-19 | Repudiation (histórico de decisão) | web/tests/*.mjs | mitigate | nenhum ok( removido sem substituto; nota datada com D-XX em toda reversão; contagem antes/depois no SUMMARY |
| T-39-20 | Tampering (guardião afrouxado) | travas CVM / ADR-027 / §3.3 | mitigate | regra (c): falha fora da lista de reversões autorizadas é defeito de código, corrigido no código |
| T-39-21 | Elevation of privilege | nenhum | accept | plano só toca testes |
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` na baseline; loop `.mjs` só com a falha ambiental conhecida; vite build verde.
</verification>

<success_criteria>
- Suíte canônica verde (baseline) com as travas de segurança/regulatórias intactas e reancoradas.
</success_criteria>

<output>
Create `.planning/phases/39-reestrutura-o-de-navega-o-da-aba-op-es/39-05-SUMMARY.md` when done
</output>
