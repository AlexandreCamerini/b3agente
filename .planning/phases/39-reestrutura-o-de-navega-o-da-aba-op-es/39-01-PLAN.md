---
phase: 39-reestrutura-o-de-navega-o-da-aba-op-es
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/options_quant.py
  - server/app/options_api.py
  - server/app/opcoes_curadoria.py
  - server/app/main.py
  - server/tests/test_opcoes_curadoria_piso.py
  - server/tests/test_opcoes_curadoria.py
  - server/tests/test_opcoes_curadoria_narrativa.py
  - server/tests/test_opcoes_curadoria_rota.py
autonomous: true
requirements: [NAV-01]

must_haves:
  truths:
    - "A lista de Recomendadas (GET /api/options/curadoria) só contém candidatos com probOtm >= 0.60, calculada por Black-Scholes a partir de dado real (D-08)"
    - "Dentro do piso, a ordem é por prêmio anualizado ((prêmio/spot) x (365/dias)) decrescente, não mais por prêmio/perda máxima (D-09)"
    - "Candidato sem volatilidade utilizável (IV do contrato inválida E hv21 indisponível) nunca é admitido com probabilidade inventada — é contado em semProbabilidade (princípio 4)"
    - "A resposta da rota diz QUAL vazio aconteceu: candidatosAvaliados (pré-piso), admitidosNoPiso, reprovadosNoPiso, semProbabilidade e pisoProbOtm (contrato de dado do D-11)"
    - "A etapa de IA (narração) é estruturalmente incapaz de receber candidato abaixo do piso ou fora da ordem nova (exigir_ranking)"
    - "O ENG-01 (seleção de contrato por liquidez + strike extremo) continua intacto — o piso é admissão+ordenação, dito em comentário no código (D-10)"
  artifacts:
    - path: "server/app/opcoes_curadoria.py"
      provides: "PISO_PROB_OTM, vol_do_contrato, prob_otm, premio_anualizado, aplicar_piso, rankear por premioAnualizado, exigir_ranking estendido"
      contains: "PISO_PROB_OTM = 0.60"
    - path: "server/app/options_quant.py"
      provides: "TAXA_LIVRE_DE_RISCO_REFERENCIA nomeada (antes literal 0.105)"
      contains: "TAXA_LIVRE_DE_RISCO_REFERENCIA"
    - path: "server/app/main.py"
      provides: "_hv21_do_ativo lazy + meta do piso em _curadoria_top e no fallback de options_curadoria"
      contains: "aplicar_piso"
    - path: "server/tests/test_opcoes_curadoria_piso.py"
      provides: "guardião do piso D-08/D-09/D-10/D-11"
      min_lines: 120
  key_links:
    - from: "server/app/main.py (_curadoria_top)"
      to: "opcoes_curadoria.aplicar_piso -> opcoes_curadoria.rankear"
      via: "piso aplicado ANTES do rankear, sobre o pool inteiro"
      pattern: "aplicar_piso\\(pool"
    - from: "server/app/opcoes_curadoria.py (prob_otm)"
      to: "options_quant.black_scholes"
      via: "import direto, taxa TAXA_LIVRE_DE_RISCO_REFERENCIA"
      pattern: "black_scholes\\("
    - from: "server/app/opcoes_curadoria.py (exigir_ranking)"
      to: "narrativa_user / curadoria_narrativa.py"
      via: "exigir_ranking valida probOtm >= PISO_PROB_OTM e premioAnualizado monotônico"
      pattern: "PISO_PROB_OTM"
---

<objective>
Trocar a métrica da lista de Recomendadas (hoje o motor de curadoria,
`opcoes_curadoria.py`) por: filtro de ADMISSÃO por probabilidade de a opção
terminar fora do dinheiro (>= 60%, Black-Scholes, D-08) + ORDENAÇÃO por prêmio
anualizado (D-09), com contrato de dado que distingue "nada varrido" de "varreu
e ninguém passou no piso" (D-11). Backend puro + rota; nenhum arquivo de front.

Purpose: D-08/D-09/D-10/D-11/D-12 do 39-CONTEXT.md. A aba "Recomendadas" (Plano
03/04) só pode mostrar o estado vazio certo se a rota disser por que está vazia.
Output: motor com piso e ordem nova, rota com meta do piso, guardiões
atualizados com nota de reversão deliberada (Fase 31 D-06 e ranking por razão).
</objective>

<repo_guardrail>
- D-12: NÃO tocar `server/app/opcoes_lastreadas.py` (motor de Oportunidades). Só `opcoes_curadoria.py` + o chamador em `main.py`.
- Princípio 5 do CLAUDE.md: todo número (probOtm, premioAnualizado) é calculado aqui, determinístico; a IA só narra.
- Guardiões não se apagam: teste que travava a ordem por `razao` é ATUALIZADO com nota datada "Fase 39 (NAV-01, D-09): ordem passou a ser por prêmio anualizado — reversão deliberada", nunca removido sem substituto.
- Não chamar mutadores do gsd-sdk (`state.*`).
</repo_guardrail>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-reestrutura-o-de-navega-o-da-aba-op-es/39-CONTEXT.md
@.planning/phases/39-reestrutura-o-de-navega-o-da-aba-op-es/39-PATTERNS.md
@CLAUDE.md

<interfaces>
De server/app/options_quant.py (existente, não muda de assinatura):
- black_scholes(option_type: str, spot: float, strike: float, t_years: float, rate: float, vol: float, dividend_yield: float = 0.0) -> Optional[BlackScholesResult]  — devolve None quando kind inválido, spot/strike/t/vol <= 0
- BlackScholesResult.prob_itm: float (0..1, arredondado 4 casas)
- years_to_expiration(days) -> float  (dias/365)
- historical_volatility(closes, window=21) -> Optional[float]  (FRAÇÃO anualizada, ex. 0.31)

De server/app/opcoes_curadoria.py (existente):
- candidatos_da_posicao(underlying, chain, spot, posicao, modo, hoje, *, n=STRIKES_POR_POSICAO, permitir_a_descoberto=False) -> list[dict]
  cada candidato tem: tipo, ticker, contractSymbol, optionType, strike, expiration, diasParaVencimento, contratos, qtyAcoes, premioUnitario, premioTotal, liquidez, estrutura, razao, manchete, didatica, precoObjeto, idCandidato (+ strikeCall/strikePut/pernasContratos no collar)
- rankear(candidatos, *, topo=TOPO) -> list[dict] (acrescenta posicaoNoRanking 1..N)
- exigir_ranking(top) -> None | ValueError
- narrativa_system(modo) / narrativa_user(top, modo)

De server/app/main.py (existente):
- async _curadoria_scan_posicao(t, posicao, modo, hoje, *, permitir_a_descoberto=False) -> (candidatos, chains_por_expiration, vencs_varridos, degradado)
- async _curadoria_top(scope, modo, permitir_a_descoberto=False) -> (top, meta)
- GET /api/options/curadoria -> {"top", "modo", "fonte", "at", **meta}; o front lê a resposta INTEIRA como `meta` (App.jsx useCuradoria: setMeta(r)) — chave nova de meta chega ao front sem mudança no App.jsx.

Unidade de IV: os provedores entregam FRAÇÃO (fixtures: `impliedVolatility: 0.3`, mydata `volatilidade_implicita: 0.28`); mydata pode mandar null legitimamente.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: motor puro — probOtm, prêmio anualizado, piso de admissão, ordem nova</name>
  <files>server/app/options_quant.py, server/app/options_api.py, server/app/opcoes_curadoria.py, server/tests/test_opcoes_curadoria_piso.py, server/tests/test_opcoes_curadoria.py, server/tests/test_opcoes_curadoria_narrativa.py</files>
  <read_first>
    - server/app/opcoes_curadoria.py (arquivo inteiro — docstring do módulo, candidatos_da_posicao 163-545, rankear 552-580, exigir_ranking 583-613, narrativa_system/narrativa_user 620-703)
    - server/app/options_quant.py (linhas 1-100: black_scholes, years_to_expiration, historical_volatility)
    - server/app/options_api.py (linhas 93-110: _enrich_contract, único uso do literal 0.105)
    - server/app/opcoes_payoff.py (linhas 18-24: texto do ENG-01 a citar no comentário D-10)
    - server/tests/test_opcoes_curadoria.py (helpers _candidato_sintetico ~561 e _sintetico_por_tipo ~384; testes de rankear/exigir_ranking 356-620)
    - server/tests/test_opcoes_curadoria_narrativa.py (helper _item ~59 e testes de ordem 120-130, 320-390)
  </read_first>
  <behavior>
    - vol_do_contrato({"impliedVolatility": 0.3}, hv21=None) -> (0.3, "implicita")
    - vol_do_contrato({"impliedVolatility": None}, hv21=0.25) -> (0.25, "historica_21d"); idem para IV 0, IV True (bool), IV 7.0, IV -1, IV 0.005
    - vol_do_contrato({"impliedVolatility": None}, hv21=None) -> (None, None)
    - prob_otm("call", 30.0, 33.0, 30, 0.3) == round(1 - black_scholes("call", 30.0, 33.0, 30/365, TAXA_LIVRE_DE_RISCO_REFERENCIA, 0.3, 0.0).prob_itm, 4); vol None ou dias 0 -> None
    - premio_anualizado(1.62, 38.82, 23) == round((1.62/38.82)*(365/23), 6)
    - collar: probOtm == round(max(0, min(1, 1 - itm_call - itm_put)), 4); volatilidadeFonte "historica_21d" se QUALQUER perna usou hv21; None se qualquer perna sem vol
    - aplicar_piso: probOtm 0.60 admitido; 0.5999 reprovado; None contado em semProbabilidade; ordem de entrada preservada; devolve (admitidos, {"admitidosNoPiso","reprovadosNoPiso","semProbabilidade"})
    - rankear ordena por -premioAnualizado, depois -premioUnitario, contractSymbol, idCandidato; permutar a entrada não muda a saída
    - exigir_ranking levanta ValueError nomeando o defeito quando: premioAnualizado ausente/não numérico; premioAnualizado crescente entre itens; probOtm ausente ou < PISO_PROB_OTM; posicaoNoRanking fora de 1..N; mais de TOPO itens
    - narrativa_user não contém mais "razão"; contém "prêmio anualizado" e "probabilidade estimada" e diz "histórica" quando volatilidadeFonte == "historica_21d"
  </behavior>
  <action>
    RED primeiro: criar `server/tests/test_opcoes_curadoria_piso.py` com os casos do bloco behavior (valores esperados calculados chamando `black_scholes` direto no teste, nunca número mágico) e rodar — tem de falhar. Commit `test(39-01): piso de admissão e prêmio anualizado (RED)`.

    GREEN, nesta ordem:
    1. `options_quant.py`: constante de módulo `TAXA_LIVRE_DE_RISCO_REFERENCIA = 0.105` com comentário "referência fixa herdada da rota educacional; troca por Selic/CDI real é melhoria futura deferida no 39-CONTEXT.md". `options_api.py:_enrich_contract` passa a usar a constante no lugar do literal `0.105` (valor idêntico, zero mudança de comportamento).
    2. `opcoes_curadoria.py`, novas constantes e funções PURAS (sem rede, sem relógio — mesma disciplina do módulo): `PISO_PROB_OTM = 0.60`; `VOL_MIN = 0.01` e `VOL_MAX = 5.0` (faixa de IV aceita como FRAÇÃO; fora dela = "não utilizável", nunca reinterpretada como percentual — é a classe de erro de 10x que `web/src/opcoes/unidades.js` documenta); `vol_do_contrato(contrato, hv21)` -> tuple(vol|None, fonte|None) com fontes "implicita"/"historica_21d" (bool excluído explicitamente, mesmo padrão do guard de `perda_maxima`); `prob_otm(option_type, spot, strike, dias, vol)` -> float|None usando `black_scholes(..., years_to_expiration(dias), TAXA_LIVRE_DE_RISCO_REFERENCIA, vol, 0.0)`; `premio_anualizado(premio_unitario, spot, dias)` -> round((premio_unitario/spot)*(365/dias), 6), None se spot<=0 ou dias<=0.
    3. `candidatos_da_posicao` ganha o kwarg `hv21: float | None = None` (keyword-only, default None — nenhum chamador existente quebra). Em CADA um dos 4 ramos, junto do cálculo de `razao` (que CONTINUA no dict — a UI só deixa de exibi-lo), anexar `probOtm`, `volatilidadeFonte` e `premioAnualizado`: call_coberta e opcao_a_descoberto = prob_otm da call; put_protecao = prob_otm da put; collar = 1 - P(call ITM) - P(put ITM), clampado em [0,1] (eventos disjuntos porque strikePut <= spot < strikeCall). `candidatos_da_posicao` NÃO filtra pelo piso — ela enumera; quem admite é `aplicar_piso` (é isso que permite ao chamador contar o pool pré-piso para o D-11, e mantém a re-derivação de `options_curadoria_abrir_collar` achando o candidato pelo id).
    4. `aplicar_piso(candidatos, *, piso=PISO_PROB_OTM)` -> (admitidos, contagem). Docstring com a nota D-10 OBRIGATÓRIA: "piso de ADMISSÃO + critério de ORDEM sobre candidatos já selecionados; NÃO é seleção de contrato dentro da cadeia — o ENG-01 (opcoes_payoff.py:22-24: 'a régua do Boris é liquidez + strike extremo') continua sendo quem escolhe QUAL contrato; delta/probabilidade não escolhe contrato aqui". Comentário também ao lado de `PISO_PROB_OTM`.
    5. `rankear`: chave passa a `(-c["premioAnualizado"], -(c.get("premioUnitario") or 0.0), c.get("contractSymbol") or "", c.get("idCandidato") or "")`; docstring reescrita dizendo a ordem nova e a nota de reversão (Fase 30/31 ordenavam por razão prêmio/perda máxima; Fase 39 D-09 troca). `exigir_ranking`: troca a checagem de `razao` numérica/monotônica por `premioAnualizado` numérica/não-crescente e ACRESCENTA `probOtm` numérica e >= PISO_PROB_OTM por item (a narração fica incapaz de receber candidato abaixo do piso).
    6. Comentários que hoje dizem "rankeados naturalmente mal pela MESMA fórmula (prêmio ÷ perda máxima), nunca filtrados" (docstring do módulo e de `premio_liquido_unitario`, Fase 31/D-06) ganham nota datada: D-06 proibia filtrar por SINAL de prêmio e isso continua valendo; o filtro da Fase 39 é outro critério (probabilidade OTM, D-08) e, como consequência nomeada, put de proteção/collar perto do dinheiro raramente passam no piso.
    7. `narrativa_system`: a linha "(razão prêmio recebido / perda máxima)" vira "A lista já foi filtrada e ordenada por um motor determinístico: só entram candidatos com pelo menos 60% de probabilidade ESTIMADA (modelo Black-Scholes) de a opção terminar fora do dinheiro, na ordem do prêmio anualizado." e acrescenta as regras "É PROIBIDO apresentar a probabilidade estimada como certeza ou como chance de lucro." e "Quando a estimativa usar volatilidade histórica, diga isso explicitamente." (princípios 6/7 do CLAUDE.md). `narrativa_user`: o trecho "razão {razao}" vira "prêmio anualizado {num_br(premioAnualizado*100)}%, probabilidade estimada de terminar fora do dinheiro {num_br(probOtm*100)}% (volatilidade implícita do contrato | volatilidade histórica de 21 pregões do ativo)", escolhendo o rótulo por `volatilidadeFonte`.
    8. Atualizar os builders sintéticos `_candidato_sintetico`/`_sintetico_por_tipo` (test_opcoes_curadoria.py) e `_item` (test_opcoes_curadoria_narrativa.py) para carregarem `premioAnualizado` (derivado do mesmo número que hoje gera a ordem esperada, para os testes de ordem continuarem significativos) e `probOtm` >= 0.60; testes que travavam "ordena por razao" são reescritos para premioAnualizado COM a nota de reversão deliberada no comentário do teste. Nenhum `def test_` removido sem substituto equivalente.

    Commit GREEN `feat(39-01): piso de probabilidade OTM + ordem por prêmio anualizado na curadoria`.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/server && .venv/bin/python -m pytest tests/test_opcoes_curadoria_piso.py tests/test_opcoes_curadoria.py tests/test_opcoes_curadoria_narrativa.py tests/test_options_quant.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PISO_PROB_OTM = 0.60" server/app/opcoes_curadoria.py` == 1
    - `grep -n "ENG-01" server/app/opcoes_curadoria.py` acha a nota D-10 na docstring de `aplicar_piso`
    - `grep -v "^\s*#" server/app/options_api.py | grep -c "0\.105"` == 0 e `grep -c "TAXA_LIVRE_DE_RISCO_REFERENCIA" server/app/options_api.py` >= 1
    - `grep -c "\-c\[\"razao\"\]" server/app/opcoes_curadoria.py` == 0 e `grep -c "premioAnualizado" server/app/opcoes_curadoria.py` >= 6
    - `git diff --stat server/app/opcoes_lastreadas.py` vazio (D-12)
    - pytest dos 4 arquivos acima: 0 falhas; test_opcoes_curadoria_piso.py tem >= 15 testes
    - `grep -c "def test_" server/tests/test_opcoes_curadoria.py` >= 61 (nenhum teste apagado)
  </acceptance_criteria>
  <done>Motor puro com piso e ordem nova, guardiões verdes, nota D-10 no código, zero toque em opcoes_lastreadas.py.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: rota — hv21 lazy por ticker, piso antes do rankear, meta do D-11</name>
  <files>server/app/main.py, server/tests/test_opcoes_curadoria_rota.py</files>
  <read_first>
    - server/app/main.py (linhas 3285-3490: _curadoria_scan_posicao, _curadoria_top, options_curadoria e o fallback de meta no except)
    - server/app/main.py (linhas 3540-3600: options_curadoria_abrir_collar — só para confirmar que a re-derivação por idCandidato não depende do piso)
    - server/app/options_api.py (linhas 65-90: _technical_context, padrão de get_history + sanitize_candles)
    - server/app/candle_provider.py (linhas 405-420: get_history debita orçamento a cada chamada, SEM cache — por isso o hv21 é lazy)
    - server/tests/test_opcoes_curadoria_rota.py (fixtures _cadeia/_contrato 20-140 e os testes de contagem 143-330, ordem 364-420)
  </read_first>
  <behavior>
    - Cadeia com IV válida em todos os contratos: `candle_provider.get_history` chamado 0 vezes
    - Cadeia sem IV (impliedVolatility None): `get_history` chamado exatamente 1 vez por ticker, mesmo com 2 vencimentos varridos
    - get_history levantando exceção: sem 500; candidatos contados em semProbabilidade; top vazio; meta.semProbabilidade == meta.candidatosAvaliados
    - Candidato com probOtm < 0.60 não aparece em top; meta.reprovadosNoPiso conta ele
    - Resposta sempre traz pisoProbOtm == 0.6, admitidosNoPiso, reprovadosNoPiso, semProbabilidade — inclusive no fallback do except
    - top continua com no máximo 4 itens e em ordem de premioAnualizado não-crescente
  </behavior>
  <action>
    RED: acrescentar os casos do behavior em `server/tests/test_opcoes_curadoria_rota.py` (monkeypatch de `candle_provider.get_history` com contador, mesmo estilo do mock de `get_quote` já existente ~linha 91-96) e rodar — falham. Commit `test(39-01): rota da curadoria com piso e hv21 lazy (RED)`.

    GREEN em `server/app/main.py`:
    1. Nova `async def _hv21_do_ativo(t)` -> float|None: `candle_provider.get_history(t, rng="3mo", interval="1d")` ("3mo" está dentro de `brapi.PLAN_RANGES`, sem cair em ForaDoPlano), closes via `indicators.sanitize_candles` (confirmar por grep que `indicators` já é importado em main.py; se não, filtrar closes numéricos > 0 à mão), `historical_volatility(closes, 21)`; qualquer exceção -> None. Docstring declara o custo: 1 chamada de candles por TICKER, só quando algum candidato ficou sem IV utilizável; `get_history` debita orçamento e não tem cache — por isso é lazy e memoizado por varredura.
    2. `_curadoria_scan_posicao`: memo local `hv21_cache = {"buscada": False, "valor": None}`. Depois de cada chamada a `candidatos_da_posicao` (primária e extras): se algum candidato daquela cadeia tem `probOtm is None` e o hv21 ainda não foi buscado, buscar UMA vez (`_hv21_do_ativo`), e se veio número, recalcular aquela cadeia com `candidatos_da_posicao(..., hv21=valor)` (pura, cadeia já em memória, zero rede nova); chamadas seguintes da mesma varredura já passam `hv21=valor`. Assinatura e tupla de retorno da função NÃO mudam (options_curadoria_abrir_collar continua igual). Atualizar a docstring de `_curadoria_top`: a frase "Não busca technical_snapshot/setups/candles" ganha nota datada "Fase 39 (D-08): busca candles SÓ como fallback de volatilidade, 1x por ticker, só quando falta IV — reversão parcial deliberada".
    3. `_curadoria_top`: depois de montar `pool` e `candidatos_por_tipo` (que continuam sobre o pool PRÉ-piso, é a evidência da varredura), `admitidos, contagem_piso = opcoes_curadoria.aplicar_piso(pool)`; meta ganha `"pisoProbOtm": opcoes_curadoria.PISO_PROB_OTM` e `**contagem_piso`; retorno vira `opcoes_curadoria.rankear(admitidos), meta`. `candidatosAvaliados` continua `len(pool)` (pré-piso) — é ele que deixa o front distinguir "nada varrido" (0) de "varreu, ninguém passou" (>0).
    4. `options_curadoria`, ramo `except`: o dict de fallback ganha `"pisoProbOtm": opcoes_curadoria.PISO_PROB_OTM, "admitidosNoPiso": 0, "reprovadosNoPiso": 0, "semProbabilidade": 0`.
    5. Testes de rota existentes que agora esvaziam porque os strikes da fixture ficam perto do dinheiro (ex.: `test_teto_de_quatro`, `test_ordem_e_do_motor`): ajustar a FIXTURE (strikes mais fora do dinheiro e/ou IV menor, calculando que passam no piso) — NUNCA baixar `PISO_PROB_OTM` nem monkeypatchar o piso para fazer teste de produção passar. Asserções de ordem por `razao` viram por `premioAnualizado` com nota de reversão. Contagens de `get_options` continuam as mesmas (fixtures têm IV 0.3 -> zero get_history).

    Commit GREEN `feat(39-01): meta do piso na rota da curadoria + hv21 lazy`.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/server && .venv/bin/python -m pytest tests/test_opcoes_curadoria_rota.py tests/test_curadoria_collar_rota.py tests/test_opcoes_curadoria_narrativa.py tests/test_opcoes_curadoria_piso.py tests/test_opcoes_curadoria.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "aplicar_piso(pool" server/app/main.py` acha 1 ocorrência, ANTES de `rankear(` na mesma função
    - `grep -c "pisoProbOtm" server/app/main.py` >= 2 (meta normal + fallback do except)
    - `grep -n "async def _hv21_do_ativo" server/app/main.py` acha a função; `grep -n 'rng="3mo"' server/app/main.py` acha a chamada
    - pytest dos 5 arquivos: 0 falhas; test_curadoria_collar_rota.py passa SEM edição (re-derivação intacta)
    - teste novo prova 0 chamadas de get_history com IV presente e exatamente 1 por ticker sem IV
  </acceptance_criteria>
  <done>Rota devolve só admitidos, na ordem nova, com meta suficiente para os 3 vazios do D-11; custo de candles só quando falta IV.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provedor de opções -> motor | IV/strike/prêmio vêm de terceiro (Yahoo/mydata), podem vir nulos ou em unidade errada |
| motor -> LLM (narração) | lista rankeada serializada em prompt |
| cliente -> GET /api/options/curadoria | rota sem corpo; escopo por token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Tampering | piso/ordem da curadoria | mitigate | PISO_PROB_OTM é constante de servidor; a rota GET não aceita corpo; nenhum parâmetro de cliente muda piso ou ordem |
| T-39-02 | Integrity (princípio 4) | vol_do_contrato / prob_otm | mitigate | IV fora de [0.01, 5.0], bool ou nula -> hv21; sem os dois -> probOtm None -> candidato NÃO admitido e contado em semProbabilidade; nunca probabilidade inventada |
| T-39-03 | Integrity (LLM) | exigir_ranking -> narrativa_user | mitigate | exigir_ranking recusa item com probOtm < piso ou premioAnualizado fora de ordem antes de montar prompt; regra "probabilidade estimada não é certeza" no system |
| T-39-04 | Denial of Service (cota) | _hv21_do_ativo -> candle_provider.get_history | mitigate | lazy (só sem IV) e memoizado 1x por ticker por varredura; exceção vira None, nunca 500 |
| T-39-05 | Elevation of privilege | nenhum | accept | nenhuma mudança de auth/RBAC; execução continua re-derivada no servidor (abrir-collar intocado) |
</threat_model>

<verification>
- pytest dos arquivos de curadoria verde; `git diff --stat server/app/opcoes_lastreadas.py` vazio.
- `curl -s localhost:8787/api/options/curadoria` (com backend local) devolve `pisoProbOtm: 0.6` e as 3 contagens.
</verification>

<success_criteria>
- Recomendadas só admite probOtm >= 60%, ordena por prêmio anualizado, e a resposta diz o motivo de qualquer vazio.
- Nenhum guardião apagado; reversões com nota datada.
</success_criteria>

<output>
Create `.planning/phases/39-reestrutura-o-de-navega-o-da-aba-op-es/39-01-SUMMARY.md` when done
</output>
