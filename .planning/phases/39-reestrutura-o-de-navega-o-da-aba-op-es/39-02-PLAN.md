---
phase: 39-reestrutura-o-de-navega-o-da-aba-op-es
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/copy.js
  - web/src/opcoes/uiOpcoes.jsx
  - web/src/opcoes/VigiasSheet.jsx
  - web/src/App.jsx
  - web/tests/test_consolidacao_opcoes_copy.mjs
  - web/tests/test_opcoes_consolidacao_ui.mjs
  - web/tests/test_opcoes_nav_primitivos_ui.mjs
autonomous: true
requirements: [NAV-01]

must_haves:
  truths:
    - "Toda chave de copy nova da fase existe nos DOIS modos (Estudo e Operador), com o tipo declarado (string ou função)"
    - "Nenhum título da aba Opções repete 'Oportunidades' com significado diferente: o eyebrow de Recomendadas deixa de dizer 'OPORTUNIDADES' (SC#4)"
    - "A frase de enquadramento de Recomendadas (curadoriaSubtitulo) nomeia o piso, a ordem, e diz que é um critério entre outros e não promessa — sucessora da frase-ponte da Fase 32"
    - "A linha de chamada em Posições conta 'estruturas recomendadas' e leva direto à aba Recomendadas, não à Oportunidades (outro motor)"
    - "Existe um sheet de Vigias e um badge com contador reutilizáveis, sem nenhum import de App.jsx (ADR-027)"
  artifacts:
    - path: "web/src/opcoes/VigiasSheet.jsx"
      provides: "VigiasBadge (olho + contador) e VigiasSheet (shell de bottom sheet zIndex 86)"
      exports: ["VigiasBadge", "VigiasSheet"]
    - path: "web/src/opcoes/uiOpcoes.jsx"
      provides: "CarimboFrescor (movido de SecaoDescobrir), DetalheInfo (ⓘ disclosure), BotaoSaibaMais (ⓘ por aba)"
      exports: ["CarimboFrescor", "DetalheInfo", "BotaoSaibaMais"]
    - path: "web/src/copy.js"
      provides: "chaves novas e reescritas da Fase 39 nos dois modos"
      contains: "opcoesAbaRecomendadas"
    - path: "web/tests/test_opcoes_nav_primitivos_ui.mjs"
      provides: "guardião dos primitivos e das chaves novas"
      min_lines: 80
  key_links:
    - from: "web/src/App.jsx (LinhaChamadaOpcoes)"
      to: "ctx.goOpcoes('recomendadas') -> opcoesAbaInicial"
      via: "estado one-shot no App, consumido pelo OpcoesScreen no mount (Plano 04)"
      pattern: "goOpcoes\\(\"recomendadas\"\\)"
    - from: "web/src/opcoes/VigiasSheet.jsx"
      to: "web/src/entendimento.jsx (ConceitoSheet shell)"
      via: "mesma geometria/zIndex copiada, sem import de App.jsx"
      pattern: "zIndex: 86"
---

<objective>
Contratos da interface nova, antes de qualquer rewiring: todas as chaves de copy
da fase, os primitivos compartilhados (ⓘ por aba, ⓘ de detalhe de bastidor,
carimbo de frescor) e o sheet/badge de Vigias — mais o canal one-shot para a
linha de chamada de Posições abrir direto em Recomendadas.

Purpose: interface-first. Os Planos 03/04 constroem contra estes contratos sem
caçar nada no código. Output: copy.js completo da fase, `VigiasSheet.jsx`,
primitivos em `uiOpcoes.jsx`, deep-link em App.jsx, guardião novo.
</objective>

<repo_guardrail>
- ADR-027: nenhum arquivo de `web/src/opcoes/` importa `App.jsx`; token via espelho `VARKEY/TOKENS/T` local, e TODO token usado tem de estar no array `TOKENS` do arquivo (token fora do array vira `undefined` calado — defeito real já pego na Fase 35).
- Chave de copy entra nos DOIS modos (`COPY.estudo` e `COPY.operador`), mesmo nome.
- Chaves retiradas de uso (`opcoesSubabaSetups`, `opcoesSubabaOperar`, `opcoesAbaAnalisar`, `opcoesAbaComparar`, `opcoesAbaSetupsSalvos`, `duasLeiturasIntro`, `opcoesOperarIntro`, `opcoesOperarEscolherPosicao`) NÃO são deletadas de copy.js (UI-SPEC, Copywriting Contract).
- Guardião atualizado leva nota datada "Fase 39 (NAV-01, D-XX)"; nenhum `ok(` removido sem substituto.
- Checklist de copy: nenhuma string nova contém "garantido", "lucro garantido", "sem risco", "certeza" (PROIBIDAS de test_curadoria_ui.mjs) nem "flag", "ative", "libere", "desbloque" (PROIBIDAS_D05).
</repo_guardrail>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-reestrutura-o-de-navega-o-da-aba-op-es/39-CONTEXT.md
@.planning/phases/39-reestrutura-o-de-navega-o-da-aba-op-es/39-UI-SPEC.md
@.planning/phases/39-reestrutura-o-de-navega-o-da-aba-op-es/39-PATTERNS.md
@.claude/skills/didatica-boris/SKILL.md
@CLAUDE.md

<interfaces>
Contratos que ESTE plano cria (consumidos pelos Planos 03 e 04):

uiOpcoes.jsx
- export function CarimboFrescor({ at, source, cp })  — cópia VERBATIM de SecaoDescobrir.jsx:118-127 (SecaoDescobrir.jsx continua com a cópia local até o Plano 04 deletar o arquivo)
- export function DetalheInfo({ rotulo, children })  — botão "ⓘ " + rotulo, alterna conteúdo inline; estado local useState(false); aria-expanded
- export function BotaoSaibaMais({ onClick, ariaLabel, cp })  — devolve null quando !onClick; texto visível "ⓘ " + (cp.saibaMais || "saiba mais")

VigiasSheet.jsx
- export function VigiasBadge({ n, medido, onAbrir, cp })
- export function VigiasSheet({ aberto, onFechar, cp, children })  — null quando !aberto

App.jsx (ctx)
- ctx.goOpcoes(aba?)  — aba opcional string; sem aba = comportamento de hoje
- ctx.opcoesAbaInicial: string | null
- ctx.limparOpcoesAbaInicial(): void

Existentes a reusar (não mudam):
- web/src/entendimento.jsx:198-206 — shell do ConceitoSheet: `position: fixed, inset: 0, zIndex: 86, background: T.scrim, display: flex, alignItems: flex-end, justifyContent: center`; painel `width 100%, maxWidth 520px, background T.bgPanel, borderTop 1px T.borderSubtle, borderRadius "18px 18px 0 0", padding "16px 18px calc(18px + env(safe-area-inset-bottom))", maxHeight "82vh", overflowY auto`; alça 38x4 px
- web/src/opcoes/OpcoesScreen.jsx:140-144 — BOTAO (44px, raio 11px)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: copy.js — chaves novas e reescritas da Fase 39, nos dois modos</name>
  <files>web/src/copy.js, web/tests/test_consolidacao_opcoes_copy.mjs</files>
  <read_first>
    - web/src/copy.js (bloco Estudo ~130-280, 420-460, 580-590, 680-820; bloco Operador ~910-1000, 1130-1160, 1265-1430 — localizar por grep de `opcoesAbaAnalisar`, `curadoriaSubtitulo`, `linhaChamadaOpcoesTexto`, `opcoesLeituraConcluidaAjuda`, `opcoesLastroAjuda`)
    - web/tests/test_consolidacao_opcoes_copy.mjs (inteiro — asserções de valor de linhaChamadaOpcoes* e CONTEUDO_REESCRITO)
    - web/tests/test_curadoria_ui.mjs (linhas 205-230 e 386-450: lista CHAVES de 26 strings e PROIBIDAS/PROIBIDAS_D05)
    - .planning/phases/39-reestrutura-o-de-navega-o-da-aba-op-es/39-UI-SPEC.md (Copywriting Contract)
  </read_first>
  <action>
    Adicionar nos DOIS modos, perto das chaves irmãs (mesmo agrupamento de hoje), com comentário "Fase 39 (NAV-01, D-XX)":

    Navegação (idênticas nos dois modos — é navegação, não voz): `opcoesAbaOportunidades: "Oportunidades"`, `opcoesAbaRecomendadas: "Recomendadas"`, `opcoesAbaMontar: "Montar"`, `opcoesVerOutrosVencimentos: "Ver outros vencimentos"`, `opcoesOcultarOutrosVencimentos: "Ocultar outros vencimentos"`, `opcoesVigiasSheetTitulo: "Seus vigias"`, `opcoesVigiasFechar: "Fechar"`, `opcoesVigiasAbrirSheet: (n) => n === 1 ? "1 vigia — abrir" : n > 1 ? n + " vigias — abrir" : "Vigias — abrir"` (D-07), `opcoesSaibaMaisAria: (aba) => "O que é uma opção — " + aba` (D-13).

    Com voz por modo: `opcoesMontarTitulo` (Estudo "MONTAR UMA ESTRUTURA" / Operador "MONTAR ESTRUTURA"); `opcoesMontarNoAtivo` função (t) (Estudo "Montar outra estrutura com " + t / Operador "Montar com " + t); `opcoesCustoFrescorRotulo` (Estudo "quanto esta aba consome da sua cota" / Operador "custo da aba") e `opcoesLastroAjudaRotulo` (Estudo "como o lastro é contado" / Operador "regra do lastro") — rótulos dos ⓘ de bastidor do D-14.

    Recomendadas (D-08/D-09/D-11/D-14): `curadoriaPosicaoRotulo: (pos, total) => pos + "ª de " + total` (idêntica); `curadoriaVazioPiso` função (pct) — Estudo: `Nenhum candidato com pelo menos ${pct}% de chance estimada de terminar fora do dinheiro (OTM) no vencimento hoje. Isto não significa que não há oportunidade — significa que nenhum passou no piso mínimo desta lista.` / Operador: `Nenhum candidato com ≥ ${pct}% de chance estimada de terminar OTM hoje — nenhum passou no piso desta lista, o que não é o mesmo que não haver oportunidade.`; `curadoriaVazioSemProb` string — Estudo: "Não há dados suficientes para concluir: a volatilidade dos contratos varridos hoje não está disponível, então a chance de terminar fora do dinheiro não pôde ser estimada e nenhum candidato entrou na lista." / Operador: "Não há dados suficientes para concluir: sem volatilidade para estimar a chance OTM dos candidatos de hoje — lista vazia por falta de dado, não por reprovação."; `curadoriaProbOtmRotulo` (Estudo "Chance estimada de terminar fora do dinheiro (OTM)" / Operador "Prob. estimada OTM"); `curadoriaPremioAnualizadoRotulo` (Estudo "Prêmio anualizado (critério da ordem)" / Operador "Prêmio anualizado"); `curadoriaVolImplicita: "vol. implícita"` e `curadoriaVolHistorica: "vol. histórica de 21 pregões"` (idênticas).

    Reescritas (valor muda, chave fica):
    - `curadoriaTitulo`, os dois modos: "AS 4 PRIMEIRAS DO RANKING" — hoje "AS 4 MELHORES OPORTUNIDADES DE OPÇÕES" colide com o rótulo da aba vizinha "Oportunidades" (SC#4) e "melhores" vira veredito.
    - `curadoriaSubtitulo` (resolução do fork 3 do UI-SPEC — a sucessora da frase-ponte da Fase 32 é ESTA frase, dentro da aba). Estudo: "Só entram aqui candidatos com pelo menos 60% de chance estimada (modelo Black-Scholes) de a opção terminar fora do dinheiro no vencimento, ordenados por prêmio anualizado. É um critério de ordenação entre outros possíveis — não uma promessa de resultado nem uma leitura mais certa que a aba Oportunidades. Varre todas as posições e vencimentos, inclusive o que a leitura técnica ainda não confirma. A ordem é do motor; não muda com a explicação da IA." Operador: "Candidatos com ≥ 60% de chance estimada (Black-Scholes) de terminar OTM, ordenados por prêmio anualizado — um critério entre outros, não promessa de resultado nem leitura mais certa que Oportunidades. Inclui o que a leitura técnica ainda não confirma. Ordem do motor; a IA não reordena." (Mantém as 3 funções da frase-ponte: nega hierarquia entre os dois motores, nomeia o critério, diz que a IA não reordena.)
    - `linhaChamadaOpcoesTexto` (os dois modos): (n) => n === 1 ? "1 estrutura recomendada nas suas posições" : `${n} estruturas recomendadas nas suas posições`; `linhaChamadaOpcoesVazia`: "Nenhuma estrutura recomendada agora"; `linhaChamadaOpcoesCarregando`: "Verificando estruturas recomendadas…". (`linhaChamadaOpcoesErro`/`Aria` inalteradas.) Motivo: a contagem vem da curadoria (aba Recomendadas); "oportunidades" passaria a nomear o OUTRO motor.
    - `opcoesLeituraConcluidaAjuda`: Estudo "Leitura concluída — monte a estrutura abaixo. Para comparar vencimentos, use o link logo depois dela." / Operador "Leitura feita — monte abaixo; outros vencimentos no link depois." (a pill row Analisar/Comparar deixa de existir).

    Atualizar `web/tests/test_consolidacao_opcoes_copy.mjs`: asserções de singular/plural passam a exigir "1 estrutura recomendada"/"3 estruturas recomendadas", com nota datada "Fase 39 (NAV-01, D-02/D-03): contagem é da aba Recomendadas"; `CONTEUDO_REESCRITO` continua válido (as chaves existem); asserção sobre `duasLeiturasIntro` continua (a chave não é deletada). Não mexer em `test_curadoria_ui.mjs` aqui (é do Plano 03).
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2 && node web/tests/test_consolidacao_opcoes_copy.mjs && node web/tests/test_curadoria_ui.mjs && node web/tests/test_vocabulario_opcoes.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Para cada uma das 26 chaves novas/reescritas listadas na action: `node -e` importando `web/src/copy.js` confirma presença nos dois modos com o tipo certo (as funções `curadoriaPosicaoRotulo(1,4) === "1ª de 4"`, `curadoriaVazioPiso(60)` contém "60%", `opcoesVigiasAbrirSheet(0) === "Vigias — abrir"`)
    - `grep -c "AS 4 MELHORES OPORTUNIDADES" web/src/copy.js` == 0
    - `curadoriaSubtitulo` dos dois modos contém "60%" e "não promessa"/"não uma promessa" e "prêmio anualizado"
    - `curadoriaVazioSemProb` dos dois modos começa com "Não há dados suficientes para concluir"
    - `grep -c "opcoesSubabaOperar\|duasLeiturasIntro" web/src/copy.js` >= 4 (chaves retiradas de uso NÃO deletadas)
    - test_consolidacao_opcoes_copy.mjs, test_curadoria_ui.mjs e test_vocabulario_opcoes.mjs saem com código 0
  </acceptance_criteria>
  <done>copy.js tem todo o vocabulário da fase nos dois modos, sem palavra proibida, e o guardião de copy da consolidação reflete a contagem nova.</done>
</task>

<task type="auto">
  <name>Task 2: primitivos compartilhados + VigiasSheet.jsx (badge e sheet)</name>
  <files>web/src/opcoes/uiOpcoes.jsx, web/src/opcoes/VigiasSheet.jsx, web/tests/test_opcoes_nav_primitivos_ui.mjs</files>
  <read_first>
    - web/src/opcoes/uiOpcoes.jsx (inteiro — convenção de espelho de token na linha 30, estilo dos primitivos existentes)
    - web/src/opcoes/SecaoDescobrir.jsx (linhas 115-127: CarimboFrescor a copiar verbatim)
    - web/src/entendimento.jsx (linhas 180-215: shell do ConceitoSheet)
    - web/src/opcoes/OpcoesScreen.jsx (linhas 98-107 TOKENS; 140-144 BOTAO; 540-546 derivação de listaDeVigias)
    - web/tests/test_opcoes_vigias_ui.mjs (primeiras 60 linhas — estilo dos guardiões de fonte: leitura por fs, `ok(nome, cond)`, remoção de comentário antes de contar)
  </read_first>
  <action>
    `uiOpcoes.jsx`: acrescentar "accent" e "textFaint" ao array `TOKENS` (hoje ausentes — sem isso `T.accent` vira `undefined`). Exportar:
    - `CarimboFrescor({ at, source, cp })` — corpo VERBATIM de SecaoDescobrir.jsx:118-127 (mesmo estilo 10.5px/T.textFaint, mesmo vocabulário `opcoesConsultadoEmRotulo`/`opcoesFonteRotulo`/`opcoesFrescorNaoMedido`), comentário de proveniência "movido de SecaoDescobrir.jsx (Fase 33, D-04b) na Fase 39 — os dois blocos cross-carteira viram abas separadas e cada uma leva o seu carimbo (todo carimbo-frescor-blocos-cross-carteira, folded no 39-CONTEXT.md)".
    - `DetalheInfo({ rotulo, children })` — o "ⓘ" de bastidor do D-14: `useState(false)` local; `<button type="button" aria-expanded={aberto}>` com texto "ⓘ " + rotulo, `minHeight: "44px"`, `background: "transparent"`, `border: "none"`, `padding: 0`, `color: T.textMuted` (NÃO accent — accent é reservado ao ⓘ das abas pelo UI-SPEC), `fontWeight: 700`, `fontSize: "11.5px"`, `textAlign: "left"`; aberto -> `<div>` com `fontSize: "11.5px"`, `color: T.textMuted`, `lineHeight: 1.5`, `whiteSpace: "pre-wrap"` renderizando `children` como nó de texto.
    - `BotaoSaibaMais({ onClick, ariaLabel, cp })` — o ⓘ contextual por aba (D-13): `if (!onClick) return null`; `<button type="button" onClick={onClick} aria-label={ariaLabel}>` texto "ⓘ " + (cp.saibaMais || "saiba mais"), `minHeight: "44px"`, `padding: "0 4px"`, `background: "transparent"`, `border: "none"`, `color: T.accent`, `fontWeight: 700`, `fontSize: "13px"`.
    Importar `useState` de "react" se ainda não importado.

    Criar `web/src/opcoes/VigiasSheet.jsx` (D-07; ícone decidido: OLHO — "vigia" no vocabulário do produto é observar, não notificar, recomendação do UI-SPEC adotada), cabeçalho de comentário explicando ADR-027 e a origem do shell (entendimento.jsx), espelho de token local com TOKENS = ["bgPanel", "borderSubtle", "textSecondary", "accent", "scrim"]:
    - `export function VigiasBadge({ n, medido, onAbrir, cp })`: `<button type="button" onClick={onAbrir} aria-label={(cp.opcoesVigiasAbrirSheet || ((k) => "Vigias — abrir"))(medido ? n : 0)}>`, estilo `minHeight: "44px"`, `padding: "8px 10px"`, `borderRadius: "11px"`, `border: 1px solid T.borderSubtle`, `background: T.bgPanel`, `color: T.textSecondary`, `display: "inline-flex"`, `alignItems: "center"`, `gap: "6px"`; SVG 24x24 `viewBox="0 0 24 24"`, `fill="none"`, `stroke="currentColor"`, `strokeWidth="2"`, `aria-hidden="true"`, olho = path "M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z" + circle cx=12 cy=12 r=3; número SÓ quando `medido && n > 0`, em `<span>` com `color: T.accent`, `fontWeight: 700`, `fontSize: "13px"` (nunca "0" antes de medir — princípio 4).
    - `export function VigiasSheet({ aberto, onFechar, cp, children })`: `if (!aberto) return null`; overlay `<div onClick={onFechar} role="dialog" aria-modal="true" aria-label={cp.opcoesVigiasSheetTitulo || "Seus vigias"}>` com o MESMO objeto de estilo do ConceitoSheet (zIndex 86 — não inventar z-index novo); painel com `onClick={(e) => e.stopPropagation()}` e a MESMA geometria (maxWidth 520px, borderRadius "18px 18px 0 0", padding com safe-area, maxHeight "82vh", overflowY auto) + alça 38x4; linha de topo com botão `type="button"` "Fechar" (`cp.opcoesVigiasFechar`) 44px chamando `onFechar`; depois `{children}`.
    Nenhum fetch, nenhum `store.`/`ctx.`, nenhum import de App.jsx.

    Criar `web/tests/test_opcoes_nav_primitivos_ui.mjs` (mesmo estilo de guardião por leitura de fonte + import de COPY), cada item nomeando o defeito que reprova: exports existem; VigiasSheet.jsx sem `import` de App.jsx e sem `store.`/`fetch(`; `zIndex: 86` e `82vh` e `safe-area-inset-bottom` presentes; `role="dialog"` e `aria-modal`; badge renderiza o número só sob `medido && n > 0`; `T.accent` aparece só no número do badge (não no botão inteiro); todo `T.<token>` usado em VigiasSheet.jsx e uiOpcoes.jsx está no array TOKENS do próprio arquivo; DetalheInfo e BotaoSaibaMais têm `aria-expanded`/`aria-label` e `minHeight: "44px"`; BotaoSaibaMais devolve null sem onClick; as chaves novas da Task 1 existem nos dois modos (lista explícita).
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2 && node web/tests/test_opcoes_nav_primitivos_ui.mjs && node web/tests/test_opcoes_vigias_ui.mjs && (cd web && npx vite build >/dev/null && echo BUILD_OK)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function CarimboFrescor\|export function DetalheInfo\|export function BotaoSaibaMais" web/src/opcoes/uiOpcoes.jsx` == 3
    - `grep -c "export function VigiasBadge\|export function VigiasSheet" web/src/opcoes/VigiasSheet.jsx` == 2
    - `grep -c "App.jsx" web/src/opcoes/VigiasSheet.jsx` só em comentário (`grep -v "^\s*//\|^\s*\*" web/src/opcoes/VigiasSheet.jsx | grep -c "App.jsx"` == 0)
    - `grep -n '"accent"' web/src/opcoes/uiOpcoes.jsx` acha o token no array TOKENS
    - test_opcoes_nav_primitivos_ui.mjs sai 0 com >= 15 asserções; `npx vite build` imprime BUILD_OK
  </acceptance_criteria>
  <done>Primitivos e sheet prontos, isolados (ADR-027), tokens declarados, guardião novo verde, build verde.</done>
</task>

<task type="auto">
  <name>Task 3: deep-link one-shot — linha de chamada de Posições abre Opções já em Recomendadas</name>
  <files>web/src/App.jsx, web/tests/test_opcoes_consolidacao_ui.mjs, web/tests/test_opcoes_nav_primitivos_ui.mjs</files>
  <read_first>
    - web/src/App.jsx (linha ~4168-4215 LinhaChamadaOpcoes; ~4303 call site `onIr={ctx.goOpcoes}`; ~7671 `navigate`; ~8940-8990 montagem do ctx com goOpcoes/goCarteira; a área de `useState` do componente App para declarar o estado novo)
    - web/tests/test_opcoes_consolidacao_ui.mjs (linhas 20-30 cabeçalho item 7 e 160-175 asserções de goOpcoes)
  </read_first>
  <action>
    Por que existe (decisão do planner, registrada): a linha de chamada de Posições conta itens da CURADORIA (aba Recomendadas). Com as 3 abas, levar a pessoa para a aba padrão "Oportunidades" (outro motor, outra lista) quebra a promessa da frase que ela tocou. O canal é one-shot para não virar persistência de aba (isso é a Fase 40, ESTADO-01).

    Em `App.jsx`:
    1. Estado novo no App: `const [opcoesAbaInicial, setOpcoesAbaInicial] = useState(null);` com comentário "Fase 39 (NAV-01): pedido one-shot de aba inicial da tela Opções — consumido e limpo pelo OpcoesScreen no mount; não é persistência (Fase 40)".
    2. `goOpcoes: (aba) => { if (typeof aba === "string") setOpcoesAbaInicial(aba); navigate("opcoes"); }` — sem argumento, comportamento idêntico ao de hoje. A validação contra as 3 abas válidas é do OpcoesScreen (Plano 04), que ignora valor desconhecido.
    3. ctx ganha `opcoesAbaInicial` e `limparOpcoesAbaInicial: () => setOpcoesAbaInicial(null)`, ao lado de `goOpcoes`.
    4. Call site ~4303: `onIr={() => ctx.goOpcoes("recomendadas")}` (arrow — passar `ctx.goOpcoes` cru faria o evento de clique virar o argumento `aba`).
    Nenhuma outra mudança em App.jsx; `BottomNav.defs` intocado.

    Atualizar `web/tests/test_opcoes_consolidacao_ui.mjs` itens 168-171 (reversão deliberada, nota datada "Fase 39 (NAV-01): a linha de chamada leva à aba Recomendadas; o parâmetro é id de ABA, nunca ticker/candidato — o invariante 'sem ticker/candidato' continua"): passa a exigir `onIr={() => ctx.goOpcoes("recomendadas")}` e `goOpcoes:` recebendo um parâmetro cujo único uso é `setOpcoesAbaInicial` guardado por `typeof aba === "string"` antes de `navigate("opcoes")`. Acrescentar ao guardião novo `test_opcoes_nav_primitivos_ui.mjs`: ctx expõe `opcoesAbaInicial` e `limparOpcoesAbaInicial`.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2 && node web/tests/test_opcoes_consolidacao_ui.mjs && node web/tests/test_opcoes_nav_primitivos_ui.mjs && node web/tests/test_curadoria_ui.mjs && (cd web && npx vite build >/dev/null && echo BUILD_OK)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'onIr={() => ctx.goOpcoes("recomendadas")}' web/src/App.jsx` == 1
    - `grep -c "limparOpcoesAbaInicial" web/src/App.jsx` >= 1 e `grep -c "opcoesAbaInicial" web/src/App.jsx` >= 3
    - test_opcoes_consolidacao_ui.mjs sai 0 inteiro (SecaoDescobrir ainda existe intacto neste plano; só as asserções 168-171 mudam de forma)
    - test_opcoes_nav_primitivos_ui.mjs e test_curadoria_ui.mjs saem 0; vite build BUILD_OK
  </acceptance_criteria>
  <done>Tocar a linha de Posições pede a aba Recomendadas; tocar Opções no BottomNav continua abrindo a aba padrão.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| copy.js -> DOM | strings estáticas renderizadas pelo React |
| App ctx -> OpcoesScreen | id de aba viaja por estado local, nunca por URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-06 | Tampering (XSS) | copy.js / DetalheInfo / VigiasSheet | mitigate | todo texto vai como nó de texto React (escapado); nenhum `dangerouslySetInnerHTML` introduzido (guardião pode checar `grep -c dangerouslySetInnerHTML` == 0 nos arquivos novos) |
| T-39-07 | Tampering | ctx.goOpcoes(aba) | mitigate | aba é string de estado interno; OpcoesScreen (Plano 04) só aceita os 3 ids da allowlist, desconhecido cai no padrão |
| T-39-08 | Information disclosure (regulatório) | curadoriaSubtitulo / curadoriaTitulo | mitigate | frase nomeia piso+critério, nega hierarquia e promessa; PROIBIDAS/PROIBIDAS_D05 checadas |
| T-39-09 | Elevation of privilege | nenhum | accept | nenhuma rota, permissão ou store tocados |
</threat_model>

<verification>
- `node web/tests/test_opcoes_nav_primitivos_ui.mjs`, `test_consolidacao_opcoes_copy.mjs`, `test_curadoria_ui.mjs` verdes; `npx vite build` verde.
</verification>

<success_criteria>
- Todos os contratos que os Planos 03/04 consomem existem, testados, sem import de App.jsx.
- Nenhuma chave antiga deletada; nenhuma palavra proibida nova.
</success_criteria>

<output>
Create `.planning/phases/39-reestrutura-o-de-navega-o-da-aba-op-es/39-02-SUMMARY.md` when done
</output>
