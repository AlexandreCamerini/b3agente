---
phase: 39-reestrutura-o-de-navega-o-da-aba-op-es
plan: 03
type: execute
wave: 2
depends_on: ["39-01", "39-02"]
files_modified:
  - web/src/opcoes/CuradoriaEstruturas.jsx
  - web/src/opcoes/OportunidadesOpcoes.jsx
  - web/tests/test_curadoria_ui.mjs
autonomous: true
requirements: [NAV-01]

must_haves:
  truths:
    - "O card de Recomendadas mostra a posição no ranking como '1ª de N', e o score bruto 'Pontuação de curadoria: 0.02' não aparece mais (D-14, SC#5)"
    - "Lista vazia de Recomendadas diz QUAL vazio é: sem dado para estimar, ninguém passou no piso de 60%, ou nada elegível — nunca um genérico que esconde o piso (D-11)"
    - "O painel inline de Recomendadas mostra a chance estimada de terminar OTM (com a fonte da volatilidade) e o prêmio anualizado que ordena a lista"
    - "O botão Executar de Recomendadas tem contraste AA nos 4 temas x modos (T.onAccent, não #fff literal)"
    - "Os cards de Oportunidades anunciam estado expandido (aria-expanded) para o painel inline que o Plano 04 monta"
  artifacts:
    - path: "web/src/opcoes/CuradoriaEstruturas.jsx"
      provides: "cascata de 5 estados, posição 1ª de N, linhas probOtm/prêmio anualizado, T.onAccent, slot infoBotao"
      contains: "curadoriaVazioPiso"
    - path: "web/src/opcoes/OportunidadesOpcoes.jsx"
      provides: "props abertoTicker (aria-expanded) e infoBotao"
      contains: "aria-expanded"
    - path: "web/tests/test_curadoria_ui.mjs"
      provides: "guardião D-11/D-14/cor + paridade do piso front x backend"
      contains: "PISO_PROB_OTM"
  key_links:
    - from: "web/src/opcoes/CuradoriaEstruturas.jsx"
      to: "meta.candidatosAvaliados / meta.semProbabilidade / meta.pisoProbOtm (Plano 01)"
      via: "leitura tipada (typeof === 'number'); campo ausente cai no vazio genérico, nunca inventa"
      pattern: "meta\\.semProbabilidade"
    - from: "web/tests/test_curadoria_ui.mjs"
      to: "server/app/opcoes_curadoria.py PISO_PROB_OTM"
      via: "leitura do .py e comparação com o '60%' da copy"
      pattern: "PISO_PROB_OTM"
---

<objective>
Adaptar os dois componentes cross-carteira que viram o conteúdo das abas
Oportunidades e Recomendadas: Recomendadas ganha os estados do piso (D-11), a
posição no ranking no lugar do score bruto (D-14), as duas grandezas que
explicam a ordem nova (probabilidade OTM estimada e prêmio anualizado) e a
correção obrigatória de cor do botão Executar; Oportunidades ganha o
`aria-expanded` e o slot do ⓘ. A execução inline de Recomendadas (D-05) já
existe e é preservada byte a byte no fluxo (`handleExecutar`, consentimento de
liquidez DIFÍCIL, gate de Modo Estudo).

Purpose: D-05/D-11/D-14 e a correção de cor exigida pelo UI-SPEC (Dimension 3).
Output: dois componentes prontos para serem montados pelas abas no Plano 04.
</objective>

<repo_guardrail>
- Guardrail CVM: `item.manchete`/`pr.manchete` renderizadas VERBATIM; nenhum sort/reverse/comparação de `razao` ou `premioAnualizado` no front — `top` é iterado na ordem recebida.
- Princípio 4: campo ausente vira "—" (ou o vazio genérico), nunca 0 nem porcentagem inventada.
- ADR-027: nenhum import de App.jsx; token novo usado entra no array TOKENS do arquivo.
- Guardião atualizado com nota datada; nenhum `ok(` removido sem substituto.
</repo_guardrail>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-reestrutura-o-de-navega-o-da-aba-op-es/39-CONTEXT.md
@.planning/phases/39-reestrutura-o-de-navega-o-da-aba-op-es/39-UI-SPEC.md
@.planning/phases/39-reestrutura-o-de-navega-o-da-aba-op-es/39-01-SUMMARY.md
@.planning/phases/39-reestrutura-o-de-navega-o-da-aba-op-es/39-02-SUMMARY.md

<interfaces>
Resposta de GET /api/options/curadoria depois do Plano 01 (chega inteira como `meta` via useCuradoria em App.jsx):
- top[i]: campos de sempre + probOtm (0..1, 4 casas), volatilidadeFonte ("implicita" | "historica_21d"), premioAnualizado (fração anual, 6 casas; negativo = débito), posicaoNoRanking
- meta.candidatosAvaliados: int (pool PRÉ-piso), meta.admitidosNoPiso, meta.reprovadosNoPiso, meta.semProbabilidade: int, meta.pisoProbOtm: 0.6

Copy do Plano 02 (dois modos): curadoriaPosicaoRotulo(pos, total), curadoriaVazioPiso(pct), curadoriaVazioSemProb, curadoriaProbOtmRotulo, curadoriaPremioAnualizadoRotulo, curadoriaVolImplicita, curadoriaVolHistorica.

CuradoriaEstruturas props hoje: { top, meta, carregando, erro, concluido, narrativa, narrando, erroNarrativa, onNarrar, onRecarregar, cp, onAbrir, onExecutar, operador, palette } — acrescenta `infoBotao` (ReactNode opcional).
OportunidadesOpcoes props hoje: { propostas, carregando, positions, cp, onAbrir } — acrescenta `abertoTicker` (string|null) e `infoBotao` (ReactNode opcional).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CuradoriaEstruturas — D-11 (5 estados), D-14 (1ª de N), linhas da ordem nova, T.onAccent</name>
  <files>web/src/opcoes/CuradoriaEstruturas.jsx, web/tests/test_curadoria_ui.mjs</files>
  <read_first>
    - web/src/opcoes/CuradoriaEstruturas.jsx (inteiro; linhas-alvo 35, 159-160, 184-195, 206-228, 264-271, 294-328)
    - web/tests/test_curadoria_ui.mjs (inteiro — 26 CHAVES, PROIBIDAS, asserções 230-250 de manchete/sort/razao, 451-503 do painel inline)
    - web/src/opcoes/OpcoesScreen.jsx (linhas 146-159: BOTAO_PRIMARIO — referência do T.onAccent)
    - server/app/opcoes_curadoria.py (a linha `PISO_PROB_OTM = 0.60` criada no Plano 01)
  </read_first>
  <behavior>
    - Fonte do componente contém `cp.curadoriaPosicaoRotulo(cand.posicaoNoRanking, top.length)` e NÃO contém `curadoriaRazaoRotulo` nem `razao.toFixed`
    - Ordem dos ramos vazios no fonte: naoMedido < erro < curadoriaVazioSemProb < curadoriaVazioPiso < curadoriaVazio (indexOf crescente)
    - Botão Executar com `color: T.onAccent`; `"#fff"` não aparece no código do módulo; "onAccent" está no array TOKENS
    - Copy: curadoriaSubtitulo dos dois modos contém `${pct}%` onde pct = PISO_PROB_OTM do .py x 100; curadoriaVazioPiso(pct) contém o mesmo número
    - Nenhum `.sort(`/`.reverse(`/comparação de `premioAnualizado` no componente
  </behavior>
  <action>
    RED: acrescentar ao `web/tests/test_curadoria_ui.mjs` um bloco "(Fase 39, NAV-01)" com as asserções do behavior + PROIBIDAS/PROIBIDAS_D05 aplicadas também às chaves string novas (`curadoriaVazioSemProb`, `curadoriaProbOtmRotulo`, `curadoriaPremioAnualizadoRotulo`) e ao retorno de `curadoriaVazioPiso(60)`. Paridade do piso: ler `server/app/opcoes_curadoria.py` por fs, extrair `PISO_PROB_OTM\s*=\s*0\.(\d+)`, converter para porcentagem inteira e exigir que ela apareça no `curadoriaSubtitulo` dos dois modos — o "60%" da copy passa a ser espelho declarado do backend, igual `CUSTO_DA_ACAO` × `_cap_check` (test_opcoes_custo_declarado.mjs). As funções novas NÃO entram na lista `CHAVES` (que exige string literal); ficam numa lista própria `CHAVES_FUNCAO_FASE39`. Rodar: tem de falhar. Commit `test(39-03): guardião D-11/D-14/onAccent da curadoria (RED)`.

    GREEN em `CuradoriaEstruturas.jsx` (comentários datados "Fase 39 (NAV-01, D-XX)"):
    1. TOKENS ganha "onAccent" e "bgPanel" se usados; linha 268: `color: "#fff"` -> `color: T.onAccent` (correção obrigatória do UI-SPEC/Dimension 3 — mesma razão medida na Fase 35: #fff reprova AA em Dark·Estudo 2,90:1 e Dark·Operador 2,10:1).
    2. Prop nova `infoBotao`: o eyebrow `cp.curadoriaTitulo` (linha 159) passa a morar numa linha `display: "flex", alignItems: "center", justifyContent: "space-between", gap: "8px"` com `{infoBotao || null}` à direita — estilo do eyebrow inalterado.
    3. D-14: remover a linha 194 inteira (`{cp.curadoriaRazaoRotulo}: {cand.razao...toFixed(2)}`) e pôr no mesmo lugar, mesmo estilo (10.5px, T.textFaint, marginTop 6px), `cp.curadoriaPosicaoRotulo ? cp.curadoriaPosicaoRotulo(cand.posicaoNoRanking, top.length) : ""`. O eyebrow `{cand.posicaoNoRanking}. {cand.ticker}` (linha 184) fica. `cand.razao` não é mais lido em lugar nenhum do componente.
    4. Painel inline (depois da linha do prêmio, ~211): duas linhas no MESMO padrão label-esquerda/valor-MONO-direita já usado pelas linhas de perda/breakeven — `cp.curadoriaProbOtmRotulo` com valor `pctFmt(item.probOtm)` + " · " + rótulo da fonte (`cp.curadoriaVolImplicita` quando `item.volatilidadeFonte === "implicita"`, `cp.curadoriaVolHistorica` quando "historica_21d", nada quando ausente), e `cp.curadoriaPremioAnualizadoRotulo` com `pctFmt(item.premioAnualizado)`. `pctFmt = (v) => (typeof v === "number" && isFinite(v) ? nf1.format(v * 100) + "%" : "—")` com `nf1 = new Intl.NumberFormat("pt-BR", { minimumFractionDigits: 1, maximumFractionDigits: 1 })` declarado junto de `nf2`. É formatação de exibição de número já calculado no backend, não aritmética financeira (princípio 5).
    5. D-11, cascata de vazios logo após o ramo `erro` (linhas 306-328), todos com o MESMO bloco visual do vazio genérico de hoje (T.bgCard, T.borderFaint, T.textSecondary — nunca T.warn, que é reservado a falha de busca, regra de cor do UI-SPEC): derivar `avaliados`, `semProb`, `piso` de `meta` só quando `typeof === "number"`; `vazioSemProb = avaliados > 0 && semProb === avaliados`; `vazioPiso = !vazioSemProb && avaliados > 0 && piso !== null`. Ramos, nesta ordem: `top.length === 0 && !naoMedido && !erro && vazioSemProb` -> `cp.curadoriaVazioSemProb`; `... && vazioPiso` -> `cp.curadoriaVazioPiso(Math.round(piso * 100))`; `... && !vazioSemProb && !vazioPiso` -> `cp.curadoriaVazio` (genérico, inclusive quando o backend é antigo e não manda os campos). Sem CTA em nenhum dos três (é estado, não ação).
    6. Nada mais muda: `handleExecutar`, `abertoId`, consentimento de liquidez, gate `!operador`, payoff do nº 1, narração, `onAbrir(item.ticker)` como link secundário (exatamente 1x), `top.map(` na ordem recebida.

    Commit GREEN `feat(39-03): Recomendadas com piso, posição no ranking e onAccent`.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2 && node web/tests/test_curadoria_ui.mjs && (cd web && npx vite build >/dev/null && echo BUILD_OK)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "curadoriaRazaoRotulo\|razao.toFixed" web/src/opcoes/CuradoriaEstruturas.jsx` == 0
    - `grep -c "curadoriaPosicaoRotulo(cand.posicaoNoRanking, top.length)" web/src/opcoes/CuradoriaEstruturas.jsx` == 1
    - `grep -v "^\s*//" web/src/opcoes/CuradoriaEstruturas.jsx | grep -c '"#fff"'` == 0 e `grep -c "color: T.onAccent" web/src/opcoes/CuradoriaEstruturas.jsx` == 1
    - `grep -n '"onAccent"' web/src/opcoes/CuradoriaEstruturas.jsx` acha o token no array TOKENS
    - `grep -c "curadoriaVazioSemProb\|curadoriaVazioPiso" web/src/opcoes/CuradoriaEstruturas.jsx` >= 2
    - test_curadoria_ui.mjs sai 0 (inclui as asserções antigas: manchete verbatim, sem sort/reverse, onAbrir 1x, porLote null-safe); vite build BUILD_OK
  </acceptance_criteria>
  <done>Recomendadas mostra posição em vez de score, diz o motivo de cada vazio, explica a ordem com números do motor e tem botão Executar legível.</done>
</task>

<task type="auto">
  <name>Task 2: OportunidadesOpcoes — aria-expanded + slot do ⓘ</name>
  <files>web/src/opcoes/OportunidadesOpcoes.jsx, web/tests/test_curadoria_ui.mjs</files>
  <read_first>
    - web/src/opcoes/OportunidadesOpcoes.jsx (inteiro, 135 linhas)
    - web/tests/test_carteira_opcoes_tira.mjs (asserções sobre OportunidadesOpcoes — onClick onAbrir(p.t), 3 variantes de vazio, manchete verbatim)
  </read_first>
  <action>
    Em `OportunidadesOpcoes.jsx` (comentário datado "Fase 39 (NAV-01, D-02/D-05)"):
    1. Props novas `abertoTicker` e `infoBotao`, ambas opcionais (default: nenhum efeito — um chamador que não passa continua idêntico).
    2. Eyebrow `cp.tiraOpcoesTitulo` (linha 83) numa linha flex `alignItems: "center", justifyContent: "space-between", gap: "8px"` com `{infoBotao || null}` à direita; estilo do eyebrow inalterado.
    3. No `<button>` do card (linha ~100): `aria-expanded={abertoTicker === p.t}`. O `onClick={() => onAbrir(p.t)}` NÃO muda — quem decide que o clique alterna um painel inline (Leitura B' do fork, ver Plano 04) é o pai.
    4. As 3 variantes de vazio (`tiraOpcoesSemSetup`/`SemMercado`/`SemCobertura`), o ramo carregando e a manchete verbatim com cor por polaridade ficam intactos.
    Acrescentar a `web/tests/test_curadoria_ui.mjs` (bloco Fase 39) duas asserções sobre OportunidadesOpcoes.jsx: `aria-expanded={abertoTicker === p.t}` presente e `onClick={() => onAbrir(p.t)}` ainda presente exatamente 1x.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2 && node web/tests/test_carteira_opcoes_tira.mjs && node web/tests/test_curadoria_ui.mjs && (cd web && npx vite build >/dev/null && echo BUILD_OK)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "aria-expanded={abertoTicker === p.t}" web/src/opcoes/OportunidadesOpcoes.jsx` == 1
    - `grep -c "onAbrir(p.t)" web/src/opcoes/OportunidadesOpcoes.jsx` == 1
    - `grep -c "tiraOpcoesSemSetup\|tiraOpcoesSemMercado\|tiraOpcoesSemCobertura" web/src/opcoes/OportunidadesOpcoes.jsx` == 3 (na mesma linha ternária; conferir que nenhuma sumiu)
    - test_carteira_opcoes_tira.mjs e test_curadoria_ui.mjs saem 0; vite build BUILD_OK
  </acceptance_criteria>
  <done>Oportunidades pronta para o painel inline do Plano 04, sem mudança de comportamento para quem não passa as props novas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resposta da curadoria -> UI | números do motor exibidos; UI não recalcula |
| UI -> execução | clique em Executar despacha ctx.A.executarCandidatoCurado (servidor re-deriva) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-10 | Integrity (princípio 4) | cascata de vazios D-11 | mitigate | só distingue vazio-piso/vazio-sem-prob com campos numéricos presentes; ausente cai no genérico — nunca afirma "passou pelo piso" sem medição |
| T-39-11 | Tampering (ordem) | top.map | mitigate | guardião proíbe sort/reverse/comparação de premioAnualizado/razao no componente; ordem é do backend |
| T-39-12 | Elevation of privilege | botão Executar | accept | fluxo intocado: Modo Estudo sem botão (defesa de UI) e 403/re-derivação no servidor (defesa real), mesmo contrato da quick 260915-j5l |
| T-39-13 | Tampering (XSS) | linhas novas do painel | mitigate | valores formatados como texto React; nenhum HTML cru |
</threat_model>

<verification>
- test_curadoria_ui.mjs, test_carteira_opcoes_tira.mjs verdes; vite build verde.
</verification>

<success_criteria>
- D-11 com 3 vazios distintos + erro + carregando; D-14 sem score bruto; cor AA no Executar; Oportunidades com aria-expanded.
</success_criteria>

<output>
Create `.planning/phases/39-reestrutura-o-de-navega-o-da-aba-op-es/39-03-SUMMARY.md` when done
</output>
