---
phase: 39-reestrutura-o-de-navega-o-da-aba-op-es
plan: 06
type: execute
wave: 5
depends_on: ["39-01", "39-05"]
files_modified:
  - web/src/version.js
  - server/web_dist
  - server/app/main.py
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/todos/pending/subaba-operar-fetch-redundante-gate-proposta.md
  - .planning/todos/pending/carimbo-frescor-blocos-cross-carteira.md
  - .planning/todos/resolved/subaba-operar-fetch-redundante-gate-proposta.md
  - .planning/todos/resolved/carimbo-frescor-blocos-cross-carteira.md
autonomous: false
requirements: [NAV-01]

must_haves:
  truths:
    - "O Alex viu, rodando, as 3 abas fixas, o badge/sheet de Vigias, a execução inline em Recomendadas e na proposta de Oportunidades, Montar com Comparar inline, e aprovou [NAV-01]"
    - "A fase está NO AR no PWA — front (BUILD_ID novo) e backend (piso da curadoria) no mesmo push, conferidos em produção"
    - "NAV-01 Done e Fase 39 fechada em ROADMAP/STATE/REQUIREMENTS, editados À MÃO; os dois todos resolvidos movidos com nota"
  artifacts:
    - path: "web/src/version.js"
      provides: "BUILD_ID novo carimbando a entrega da Fase 39"
      contains: "F10-"
    - path: ".planning/REQUIREMENTS.md"
      provides: "NAV-01 Done"
      contains: "NAV-01"
  key_links:
    - from: "web/src/version.js (BUILD_ID)"
      to: "server/web_dist + SERVER_BUILD_ID em /api/health"
      via: "scripts/bump.sh seguido de scripts/publicar-web.sh"
      pattern: "F10-[0-9]{8}-[0-9]{2}"
---

<objective>
Fechar a fase: verificação humana ao vivo (inclui as decisões de fork que o
planner tomou no lugar do Alex), publicação front+backend no mesmo push e
documentos de planejamento atualizados à mão.

Purpose: plano que toca `web/src/` sem task de bump+publicação fica testado e
nunca vai ao ar (defeito de processo da memória do projeto); o checkpoint
humano segura o push da FASE INTEIRA. Output: build no ar, piso conferido em
produção, docs coerentes.
</objective>

<repo_guardrail>
- Não chamar mutador de estado do `gsd-sdk` (`state.advance-plan`, `state.record-session`, `state.record-metric`, `state.add-decision`) — corrompem `.planning/STATE.md` (decisão do Alex, 2026-09-11). Editar à mão com Edit; `git diff .planning/` antes do commit.
- `scripts/bump.sh` antes de `scripts/publicar-web.sh`; nunca editar `server/web_dist` à mão; `git fetch origin && git merge origin/main` ANTES do bump (bump.sh deriva do valor LOCAL — sessão paralela colide).
- Histórico não se reescreve: o comentário de `SERVER_BUILD_ID` em main.py ganha a entrada nova no topo e preserva o texto anterior como `HISTORICO`.
- Nenhum push antes do "aprovado" da Task 1.
</repo_guardrail>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-reestrutura-o-de-navega-o-da-aba-op-es/39-CONTEXT.md
@.planning/phases/39-reestrutura-o-de-navega-o-da-aba-op-es/39-04-PLAN.md
@.planning/phases/39-reestrutura-o-de-navega-o-da-aba-op-es/39-05-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: verificação humana ao vivo — 3 abas, Vigias, execução, Montar, forks decididos</name>
  <what-built>
    - Aba Opções com 3 abas fixas (Oportunidades / Recomendadas / Montar), sem sub-aba Operar nem pill row interna (39-04).
    - Vigias em badge com contador (ícone de olho) no cabeçalho, abrindo sheet (39-02/04).
    - Recomendadas com piso de 60% de chance estimada OTM, ordem por prêmio anualizado, "1ª de N", linhas de probabilidade/prêmio anualizado no painel, 3 vazios distintos, botão Executar em T.onAccent (39-01/03).
    - Oportunidades: tocar um card expande inline a proposta pronta do motor com gate (aceitar/fechar como na antiga sub-aba Operar) + link "Montar outra estrutura com X" (fork 1 decidido pelo planner: Leitura B', ver 39-04-PLAN.md).
    - Montar: seletor, cabeçalho de frescor só com ticker, ⓘ do lastro, leitura paga, SecaoAnalisar com executar, link "Ver outros vencimentos" inline, Setups salvos abaixo (fork 2).
    - ⓘ "saiba mais" dentro de cada aba; custo do frescor atrás de ⓘ no rodapé.
    - Posições: linha "N estruturas recomendadas" abre Opções já em Recomendadas.
  </what-built>
  <how-to-verify>
    Subir backend + Vite locais (`bash scripts/executar.sh`; se travar em `lsof`/`ps`, subir backend e Vite diretamente). Conferir antes: `curl -s localhost:8787/api/options/curadoria` devolve `pisoProbOtm` 0.6 e as chaves `admitidosNoPiso`/`reprovadosNoPiso`/`semProbabilidade`.

    1. **3 abas fixas** — abrir Opções: a abaBar mostra Oportunidades/Recomendadas/Montar; escolher um ticker em Montar e trocar de aba: as 3 continuam as mesmas; voltar a Montar mantém o ticker.
    2. **Vigias** — o badge aparece nas 3 abas; o número só aparece quando há vigia; o sheet abre e fecha (Fechar e toque fora); tocar um vigia fecha o sheet e abre Montar naquele ativo.
    3. **Oportunidades (fork 1)** — tocar um card expande a proposta pronta abaixo do carrossel; em Operador aparece o CTA de abrir (e o de fechar se já houver posição); em Estudo não há CTA; o link "Montar outra estrutura com X" leva a Montar. **Pergunta ao Alex:** manter este painel inline (B') ou preferir que o card só leve a Montar (Leitura A do UI-SPEC)?
    4. **Recomendadas** — card mostra "1ª de N" e NÃO mostra "Pontuação de curadoria"; o painel mostra prêmio, perda, breakeven, chance estimada OTM com a fonte da volatilidade, prêmio anualizado; Executar (Operador) legível nos 4 temas × modos; subtítulo cita os 60% e "não promessa". Se a lista vier vazia, a frase diz qual vazio é.
    5. **Montar** — sem ticker, só o seletor e "Escolha um ativo…"; com ticker, cabeçalho Pregão/Fonte, ⓘ "como o lastro é contado" recolhido, leitura paga com custo no botão, montar + executar; "Ver outros vencimentos" expande/recolhe Comparar no mesmo lugar; Setups salvos abaixo.
    6. **ⓘ** — "ⓘ saiba mais" em cada uma das 3 abas abre o verbete "mkt-opcao"; não há mais "saiba mais" solto no topo; o ⓘ do rodapé mostra o texto do custo do frescor.
    7. **Posições** — a linha diz "N estruturas recomendadas…" e abre Opções direto em Recomendadas; depois, tocar Opções no menu inferior abre em Oportunidades.
    8. **Rede** — com o DevTools aberto, trocar entre as 3 abas e abrir/fechar o sheet NÃO dispara chamada `/api/options/mcp/*` (só o `status` do mount e os botões que declaram preço).
    9. **375px** — abaBar e badge sem overflow horizontal (se quebrar linha, é o `flexWrap` previsto).
    10. **Modo Estudo** — nenhum botão de execução em Oportunidades, Recomendadas nem Montar.

    Divergência nos itens 3, 4, 8 ou 10 é BLOQUEANTE. **Riscos para o Alex decidir (não bloqueiam a publicação sozinhos, mas precisam de resposta):** (a) o rótulo "Recomendadas" convive com o disclaimer "nada aqui é recomendação" e com a leitura regulatória (CVM) de recomendação — D-03 travou o nome; manter?; (b) com o piso de 60% OTM, put de proteção e collar perto do dinheiro quase nunca entram na lista, e a compra a seco (opção a descoberto, quando ligada) entra justamente quando tem ≥ 60% de chance de virar pó — D-08 aplicado literalmente; (c) o pedido original "mostrar 3 e deixar escolher mais" não virou decisão D-XX e não foi implementado (a lista segue com as 4 primeiras).
  </how-to-verify>
  <read_first>
    - .planning/phases/39-reestrutura-o-de-navega-o-da-aba-op-es/39-04-PLAN.md (seção "Resolução dos forks")
    - .planning/phases/39-reestrutura-o-de-navega-o-da-aba-op-es/39-UI-SPEC.md (Copywriting Contract)
  </read_first>
  <action>
    Subir o ambiente local, apresentar ao Alex o roteiro de 10 itens + as 3 perguntas de risco e PARAR. Não publicar, não dar push, não seguir para a Task 2 antes da resposta. Se o Alex escolher a Leitura A no item 3 ou mudar algo, registrar como divergência a corrigir (plano de gap) antes de publicar.
  </action>
  <acceptance_criteria>
    - O Alex responde "aprovado" (com as respostas às perguntas de risco) ou descreve divergências. Nenhum push/publicação antes disso.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — verificação visual/interativa por desenho; substitutos automatizados: `node web/tests/test_opcoes_nav_tres_abas_ui.mjs`, `node web/tests/test_opcoes_nav_primitivos_ui.mjs`, `node web/tests/test_curadoria_ui.mjs`, `cd server && .venv/bin/python -m pytest tests/test_opcoes_curadoria_piso.py tests/test_opcoes_curadoria_rota.py -q`</automated>
  </verify>
  <resume-signal>Digite "aprovado" (e as respostas a, b, c) ou descreva o que divergiu</resume-signal>
  <done>Aprovação humana registrada, ou lista de divergências a corrigir antes de publicar.</done>
</task>

<task type="auto">
  <name>Task 2: publicar (front + backend no mesmo push) e conferir em produção</name>
  <files>web/src/version.js, server/web_dist, server/app/main.py</files>
  <read_first>
    - scripts/bump.sh (linhas 1-40)
    - scripts/publicar-web.sh (inteiro)
    - server/app/main.py (linha do `SERVER_BUILD_ID = ...` ~1662 e o formato do comentário com HISTORICO)
    - CLAUDE.md — "Publicação" e "Três esquemas de versão"
  </read_first>
  <action>
    Ordem obrigatória:
    1. `git fetch origin && git merge origin/main` antes de carimbar. Conflito em `server/web_dist` se resolve REGERANDO, nunca mesclando.
    2. Suíte canônica pré-bump: `bash scripts/executar.sh --testes` na baseline do 39-05 (registrar números no SUMMARY).
    3. `bash scripts/bump.sh`, depois `bash scripts/publicar-web.sh`. Se `npm ci` der EPERM de sandbox (achado da Fase 38), rodar só o passo de build fora do sandbox e registrar — nenhum código de produto tocado para contornar.
    4. Comentário do `SERVER_BUILD_ID` em main.py: entrada nova no topo descrevendo a Fase 39 (3 abas fixas; Vigias em sheet; Operar dissolvida em painel inline de Oportunidades — Leitura B'; curadoria com piso de 60% OTM Black-Scholes, ordem por prêmio anualizado, hv21 lazy, meta do D-11; ⓘ por aba; bastidor atrás de ⓘ; deep-link Posições -> Recomendadas) e o texto anterior preservado integralmente como `HISTORICO (entrega anterior, <id>)`.
    5. Commit + push em `v2/interacao-estrutural` e fast-forward de `origin/main` (padrão das Fases 35/38: publicação é push direto).
    6. Conferir no ar (redeploy leva ~3-6 min): `curl -s https://boris.semente.dev/api/health` com o `SERVER_BUILD_ID` novo; `curl -s https://boris.semente.dev/api/options/curadoria | python3 -c "import sys,json; d=json.load(sys.stdin); print(d.get('pisoProbOtm'), d.get('candidatosAvaliados'), d.get('admitidosNoPiso'), d.get('semProbabilidade'))"` imprime `0.6 ...` (escopo anônimo pode vir com carteira vazia — o que importa é a CHAVE existir; se a rota exigir login e devolver 401, registrar e conferir só pelo health). Colar as saídas no SUMMARY.
    Limitação a declarar, não resolver: o app iOS carrega bundle local — a navegação nova só chega ao iPhone num build novo de TestFlight. O backend (piso) já vale para ele; o bundle iOS antigo continua funcionando e ainda mostra "Pontuação de curadoria" porque o backend continua mandando `razao` no dict (compatibilidade preservada de propósito) — mas a lista que ele recebe já é a filtrada e ordenada pela regra nova.
  </action>
  <acceptance_criteria>
    - `grep -E "F10-[0-9]{8}-[0-9]{2}" web/src/version.js` com a data real de hoje
    - `/api/health` de produção com o `SERVER_BUILD_ID` novo (saída no SUMMARY)
    - `/api/options/curadoria` de produção devolvendo `pisoProbOtm` (saída no SUMMARY) ou o 401 registrado
    - `git log origin/main -1 --format=%H` == `git rev-parse HEAD`
  </acceptance_criteria>
  <verify>
    <automated>grep -E "F10-[0-9]{8}-[0-9]{2}" /Users/acamerini/dev/borisv2/web/src/version.js</automated>
  </verify>
  <done>Fase 39 no ar no PWA, piso conferido em produção, pendência iOS registrada.</done>
</task>

<task type="auto">
  <name>Task 3: fechar os documentos de planejamento à mão + todos resolvidos</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/todos/pending/subaba-operar-fetch-redundante-gate-proposta.md, .planning/todos/pending/carimbo-frescor-blocos-cross-carteira.md, .planning/todos/resolved/subaba-operar-fetch-redundante-gate-proposta.md, .planning/todos/resolved/carimbo-frescor-blocos-cross-carteira.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (seção "Reestruturação de navegação" e a tabela de rastreio ~85-95)
    - .planning/ROADMAP.md (item da Fase 39 no topo ~linha 21 e a seção "### Phase 39")
    - .planning/STATE.md (frontmatter, "Current Position", "Stopped at", formato das entradas "Posição anterior")
    - .planning/todos/resolved/ (um arquivo existente, para o formato de nota de resolução)
  </read_first>
  <action>
    Com Edit (nunca mutador do gsd-sdk):
    1. REQUIREMENTS.md: `- [x] **NAV-01**`; `Pending` -> `Done` na linha NAV-01 da tabela; linha "Last updated" nova.
    2. ROADMAP.md: `- [x] Phase 39 ... (6/6 plans) — completed <data>`; na seção da fase, os 6 planos marcados.
    3. STATE.md: frontmatter (`status`, `last_updated`, `last_activity`, `progress` — contador de planos e percent coerentes com a barra); "Current Position" reescrito para a Fase 39 fechada: o que foi entregue, BUILD_ID publicado, e as decisões que o próximo leitor não deve redescobrir — estado único `abaOpcoes` + `ABAS_OPCOES`; fork 1 = Leitura B' (PropostaDoAtivo é o único consumidor de PropostaLastreada/CandidatoOpcao/useAceiteLastreado); fork 2 = SecaoSetups em Montar; fork 3 = curadoriaSubtitulo como sucessora da frase-ponte; custo do frescor no rodapé porque mcpStatus sai no mount da tela; `candidatos_da_posicao` enumera e `aplicar_piso` admite (é isso que dá o pool pré-piso para o D-11); hv21 lazy 1x por ticker; `razao` continua no dict por compatibilidade; deep-link one-shot `opcoesAbaInicial` (a Fase 40/ESTADO-01 precisa conviver com ele). As respostas do Alex às perguntas de risco (a)(b)(c) da Task 1 vão como decisões ou pendências. A posição anterior vai para "Posição anterior", texto da época preservado.
    4. Mover com `git mv` os dois todos para `.planning/todos/resolved/`, acrescentando no fim de cada um uma seção "Resolução (Fase 39, <data>)": `subaba-operar-fetch-redundante-gate-proposta.md` — resolvido por remoção (D-05 dissolveu SubAbaOperar; o fetch já tinha saído no fold-in D-04a da 33-05, e PropostaDoAtivo lê o fan-out único); `carimbo-frescor-blocos-cross-carteira.md` — cada aba cross-carteira (AbaOportunidades/AbaRecomendadas) tem o seu CarimboFrescor.
    5. Pendências abertas, declaradas: build iOS/TestFlight; "mostrar 3 + ver mais" (se o Alex quiser, vira requisito); taxa livre de risco real (deferida no CONTEXT); estado de erro de busca não rastreado em Oportunidades (herdado da Fase 32).
    Commit separado `docs(39): fecha fase 39` depois de conferir `git diff .planning/`.
  </action>
  <acceptance_criteria>
    - `grep -c "NAV-01.*Done" .planning/REQUIREMENTS.md` == 1 e `grep -c "\[ \] \*\*NAV-01" .planning/REQUIREMENTS.md` == 0
    - `grep -n "Phase 39" .planning/ROADMAP.md | head -1` mostra `[x]`
    - `ls .planning/todos/pending/ | grep -c "subaba-operar\|carimbo-frescor"` == 0 e os dois existem em `.planning/todos/resolved/`
    - `git diff .planning/STATE.md` mostra só as seções pretendidas (sem texto de sessão antiga sobrescrito, sem métrica fora de tabela)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2 && git diff --stat .planning/ && grep -c "NAV-01.*Done" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>Documentos refletem a realidade, editados à mão, com os forks e riscos registrados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> produção (Railway) | push publica `server/` e `server/web_dist` juntos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-22 | Tampering (entrega) | version.js -> web_dist | mitigate | merge de origin/main antes do bump.sh; bump.sh antes de publicar-web.sh; carimbo conferido em /api/health |
| T-39-23 | Integrity (front sem backend) | bundle novo lendo meta do piso | mitigate | backend e web_dist no mesmo push; o front já cai no vazio genérico se as chaves de meta faltarem (Plano 03) |
| T-39-24 | Integrity (cliente iOS antigo) | app nativo com bundle anterior | accept | backend mantém `razao` no dict e só acrescenta chaves; o bundle antigo continua funcionando até o TestFlight novo |
| T-39-25 | Repudiation (histórico) | STATE/ROADMAP/REQUIREMENTS, comentário SERVER_BUILD_ID | mitigate | edição à mão + git diff; HISTORICO preservado; mutadores do SDK proibidos |
</threat_model>

<verification>
1. Aprovação humana antes de qualquer push.
2. BUILD_ID novo e chave pisoProbOtm conferidos em produção.
3. `git diff .planning/` revisado antes do commit de docs.
</verification>

<success_criteria>
- Alex aprovou ao vivo (com respostas aos 3 riscos); fase no ar com front e backend juntos.
- NAV-01 Done; Fase 39 fechada à mão; todos resolvidos movidos com nota; pendências declaradas.
</success_criteria>

<output>
Create `.planning/phases/39-reestrutura-o-de-navega-o-da-aba-op-es/39-06-SUMMARY.md` when done
</output>
