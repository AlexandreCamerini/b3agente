---
phase: 01-auditoria-diagn-stica-consolidada
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-GATE.md
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03]

must_haves:
  truths:
    - "Cada um dos 3 hooks de plan.py (can_add_ticker, can_analyze, requires_subscription) tem veredito sobre o que falta para funcionar de verdade, com arquivo:linha"
    - "Os 3 passos técnicos de ativação do ADR-010 estão avaliados um a um: se a estrutura atual os suporta sem reescrita"
    - "A distinção entre cota física da brapi (compartilhada, 15k/mês) e cap comercial (por conta) está rastreada até o que a UI mostra ao usuário quando cada uma dispara"
    - "As features candidatas a tier pago estão mapeadas com o ponto de código onde o gate entraria e o esforço de ativação — sem decidir preço ou quantidade"
  artifacts:
    - path: ".planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-GATE.md"
      provides: "Achados brutos da dimensão gating de monetização"
      contains: "### F-GATE-"
  key_links:
    - from: "FINDINGS-GATE.md"
      to: "docs/adr/010-planos-e-cap-gratuito.md"
      via: "cada achado se posiciona contra o que o ADR já decidiu (técnico) vs. o que é pendente (comercial)"
      pattern: "ADR-010"
    - from: "FINDINGS-GATE.md"
      to: "REPORT-01.md (plano 01-06)"
      via: "formato de achado padronizado F-GATE-NN"
      pattern: "Severidade"
---

<objective>
Auditar a dimensão **arquitetura de gating de monetização** (`server/app/plan.py`,
`server/app/metering.py`, `server/app/managed.py`, `server/app/brapi_budget.py`) e
produzir achados brutos com severidade, evidência arquivo:linha e recomendação.

Purpose: hoje 100% do produto está liberado para 100% dos usuários. O ADR-010 diz que
isso é deliberado (a parte comercial depende do Alex), não uma lacuna de engenharia. A
pergunta desta auditoria é outra: **quando a decisão comercial vier, a estrutura atual
aguenta ligar o gate sem reescrita, ou o "estruturalmente pronto" é otimista?**

Output: `.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-GATE.md`

**Fora de escopo (PROJECT.md / REQUIREMENTS.md Out of Scope):** decidir preço, limite
numérico, o que entra em cada tier, ou modelo de loja. Achado que sugira número
comercial está fora do escopo — a saída é o mapa técnico e o esforço, não a decisão.

**Verificação:** código + docs é suficiente para esta dimensão (D-01). Exercitar a API
local é permitido e agrega confiança onde couber.

**Esta fase é READ-ONLY sobre o produto.** Nenhum arquivo em `server/`, `web/` ou
`web-admin/` pode ser modificado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-auditoria-diagn-stica-consolidada/01-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/ARCHITECTURE.md
</context>

<severity_rubric>
Régua fechada pelo usuário em 01-CONTEXT.md. Aplicar literalmente:

- **Crítico (D-02)** — viola um dos 10 princípios obrigatórios do `CLAUDE.md` do repo
  OU o guardrail CVM.
- **Alto (D-03)** — já causou incidente real documentado OU **bloqueia uma decisão de
  negócio pendente** (esta segunda metade é a que mais se aplica a GATE: gating
  estruturalmente pronto mas inerte é o exemplo literal citado em D-03).
- **Médio (D-04)** — risco real, ainda não materializado em incidente.
- **Baixo (D-05)** — polimento/consistência sem risco de produto.

Calibração desta dimensão: "o gate está desligado" NÃO é achado por si só — é decisão
documentada do ADR-010. Achado é o que **impede ou encarece ligar o gate quando a
decisão vier**, ou o que confunde o usuário hoje (transparência de cota).
</severity_rubric>

<finding_format>
Formato OBRIGATÓRIO e idêntico nos 5 planos da wave 1 — o plano 01-06 consolida por
parsing mecânico. Não alterar rótulos nem ordem.

```
### F-GATE-01 — {título de uma linha}
- **Requisito:** GATE-01
- **Severidade:** Alto — {qual regra da régua se aplica, citando D-02/D-03/D-04/D-05}
- **Evidência:** {arquivo:linha} — {o que está lá}
- **Verificação:** {código/docs | API real}
- **Impacto:** {consequência concreta — o que trava ou encarece quando o gate for ligado}
- **Recomendação:** {ação técnica — nunca um número comercial}
```
</finding_format>

<tasks>

<task type="auto">
  <name>Task 1: GATE-01 — a estrutura de plan.py/metering.py aguenta o escalonamento free→pago?</name>

  <files>.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-GATE.md</files>

  <read_first>
    - `docs/adr/010-planos-e-cap-gratuito.md` (105 linhas) — seções "O que é técnico
      (decidido aqui)", "O que é comercial (pendente — decisão do Alex)", "Consequências"
    - `server/app/plan.py` (86 linhas, arquivo inteiro) — `PLAN_FREE` (19), `PLAN_PRO`
      (25), `ACTIVE_PLAN` (35), `current_plan` (41), `plan_at_least` (52),
      `can_add_ticker` (63), `can_analyze` (73), `requires_subscription` (83)
    - `server/app/metering.py` (134 linhas, arquivo inteiro) — `check` (66), `consume`
      (114), `snapshot` (130), `SECTION`/`GLOBAL_SECTION` (19-20)
    - `server/app/managed.py` (135 linhas) — `managed_config` (64), `global_daily_cap`
      (86), `daily_quota` (120), `rate_per_min` (127), `is_available` (134)
    - `.planning/codebase/CONCERNS.md` — seção "Missing Critical Features" (gating
      pronto e intencionalmente desativado)
    - `server/app/main.py:385-397` — `GET /api/ai/quota` (o que o front recebe hoje)
    - Uso real dos hooks: `grep -rn "can_add_ticker\|can_analyze\|requires_subscription\|plan_at_least\|current_plan" server/app/ web/src/`
  </read_first>

  <action>
**1a. Criar `.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-GATE.md`**
com estas seções, nesta ordem:
  - `# Achados — Dimensão GATE (gating de monetização)` + data
  - `## Método de verificação` — o que foi lido, o que foi exercitado por API, o que não
  - `## Estado dos hooks de gating` — tabela (ver 1b)
  - `## Os 3 passos de ativação do ADR-010` — tabela (ver 1c)
  - `## Achados`
  - `## Verificado e conforme` (fechada na task 2)
  - `## Cobertura de requisitos` (fechada na task 2)

**1b. Tabela `## Estado dos hooks de gating`** com colunas
`| Hook | Linha | Retorna hoje | Quem chama (arquivo:linha) | O que falta para funcionar | Reescrita necessária? |`
e **uma linha para cada** de: `current_plan`, `plan_at_least`, `can_add_ticker`,
`can_analyze`, `requires_subscription`. "Quem chama" precisa vir do grep real — se
nenhuma rota chama, a célula é `NINGUÉM` e isso é evidência de que o mecanismo é inerte
(o próprio `plan.py:54` já registra "nenhuma rota usa isto ainda"). "Reescrita
necessária?" ∈ {`não — só ligar`, `parcial — {o quê}`, `sim — {o quê}`}.

**1c. Tabela `## Os 3 passos de ativação do ADR-010`** com colunas
`| Passo (ADR-010) | O que já existe (arquivo:linha) | O que falta | Depende de decisão comercial? | Esforço |`
e exatamente **3 linhas**, uma por passo especificado no ADR:
  1. resolver `ACTIVE_PLAN` por usuário via recibo de loja validado server-side
  2. alimentar `can_analyze` com contador real no padrão de `server/app/metering.py`
  3. `requires_subscription` checar o recibo em vez de sempre `False`

Para o passo 2 especificamente: `metering.py` JÁ tem contador real
(`check`/`consume`/`snapshot`, com quota por usuário, rate por minuto e cap global).
A pergunta a responder com evidência é: **`can_analyze` e `metering.check` são duas
implementações concorrentes do mesmo conceito?** Se sim, ligar o plano exige escolher
uma e migrar a outra — e isso é achado de arquitetura, não de configuração.

**1d. Verificar `ACTIVE_PLAN` como constante de módulo** (`plan.py:35`): é global de
processo, não resolvida por requisição. Avaliar o que acontece num cenário multi-usuário
onde contas têm planos diferentes: o caminho `current_plan(user)` lê `users.plan` do
banco, mas quem chama de fato? Se a maioria do código lê `ACTIVE_PLAN` direto, isso é
uma reescrita escondida atrás da fachada de "só ligar". Evidenciar com grep.

**1e. Exercitar a API real** (opcional, mas preferível): com o backend local no ar
(subir só se `curl -fsS http://localhost:8787/api/health` não responder; NUNCA usar
`scripts/executar.sh`/`run.sh`, cujo `free_port` mata o servidor dos planos paralelos),
chamar `GET /api/ai/quota` com e sem sessão e registrar o payload real — é o que o front
usa para mostrar cota ao usuário, insumo direto de GATE-02.
  </action>

  <verify>
    <automated>F=.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-GATE.md; test -f "$F" && grep -q '^## Estado dos hooks de gating' "$F" && grep -q '^## Os 3 passos de ativação do ADR-010' "$F" && for h in current_plan plan_at_least can_add_ticker can_analyze requires_subscription; do grep -q "$h" "$F" || exit 1; done && grep -qE '^- \*\*Requisito:\*\* GATE-01' "$F"</automated>
  </verify>

  <acceptance_criteria>
    - `FINDINGS-GATE.md` existe com as 7 seções na ordem especificada
    - `## Estado dos hooks de gating` tem 5 linhas (uma por hook), cada uma com número
      de linha real em `plan.py`, o que retorna hoje, quem chama (arquivo:linha ou
      `NINGUÉM`, vindo de grep) e veredito de reescrita
    - `## Os 3 passos de ativação do ADR-010` tem exatamente 3 linhas, cada uma com
      o que já existe (arquivo:linha) e o que falta
    - A relação entre `can_analyze` e `metering.check` (duplicação conceitual ou
      complementaridade) tem veredito explícito com evidência
    - O comportamento de `ACTIVE_PLAN` como constante de módulo em cenário multi-usuário
      tem veredito com evidência de grep
    - GATE-01 aparece em >= 1 achado `### F-GATE-NN`
    - Nenhum achado sugere preço, limite numérico ou modelo de loja
    - `git status --porcelain server web web-admin` vazio
  </acceptance_criteria>

  <done>O estado real dos hooks e o custo de ativação dos 3 passos do ADR-010 estão evidenciados, com veredito explícito sobre reescrita estrutural.</done>
</task>

<task type="auto">
  <name>Task 2: GATE-02 (transparência cota brapi × cap comercial) e GATE-03 (mapa de features candidatas) + tabela de cobertura</name>

  <files>.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-GATE.md</files>

  <read_first>
    - `.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-GATE.md`
    - `docs/adr/008-fonte-de-cotacoes-selecionavel.md` — brapi master com orçamento;
      a fonte de dados NÃO é diferencial de plano pago (ADR-010, decisão 3)
    - `server/app/brapi_budget.py` (300 linhas) — `cota_mes` (46), `teto_dia` (53),
      `fatia_limite` (57), `pode_gastar` (139), `degradado` (163), `snapshot` (170),
      `projecao` (261); as frações `_FRACOES` (31) e `_SOFT` (32)
    - `server/app/metering.py` — `check` (66) e o cap global vs. quota por usuário
    - `server/app/main.py:385-397` (`GET /api/ai/quota`), `:470-500`
      (`GET /api/obs/usage`, `GET/POST /api/obs/brapi/projecao`)
    - Front: `web/src/plan.js` (35 linhas) e as faixas de `web/src/App.jsx` achadas por
      `grep -n "quota\|cota\|limite\|degradad\|orçament\|orcament" web/src/App.jsx`
    - `docs/adr/010-planos-e-cap-gratuito.md` — seção "O que é comercial (pendente)"
  </read_first>

  <action>
**GATE-02 — as duas cotas são coisas diferentes; a UI diz isso?** Existem dois limites de
naturezas completamente distintas:
  - **cota física da brapi** — compartilhada por TODA a base (15k requisições/mês,
    `brapi_budget.py`), que quando estoura degrada a fonte de dados para todo mundo;
  - **cap comercial de IA** — por conta (`metering.py` + `managed.py`), que quando
    estoura limita só aquele usuário.

Produzir a seção `## Cota física × cap comercial` com a tabela
`| Eixo | Onde é calculado (arquivo:linha) | Escopo | Rota que expõe | O que o usuário vê quando estoura | Confunde com o outro? |`
e **2 linhas** (uma por eixo). A coluna "O que o usuário vê" precisa de evidência do
front (arquivo:linha em `App.jsx`/`plan.js`) ou, melhor, do texto real retornado pela
API. Se o usuário vir a mesma mensagem genérica nos dois casos, isso é achado — e cruza
com o princípio 4 do CLAUDE.md (não inventar valor / mostrar o estado correto) e o
princípio 3 (transparência de fonte e natureza do dado): avaliar se a confusão chega a
violar princípio (**Crítico**, D-02) ou fica em risco de comunicação (**Médio/Alto**).

Verificar também o estado `degradado` (`brapi_budget.py:163`): quando a fatia de
orçamento se esgota e o produto passa a servir dado mais velho ou de fonte backup, a UI
avisa? Princípio 3 exige dizer se o dado é tempo real, atrasado ou histórico.

**GATE-03 — mapa técnico de features candidatas a tier pago.** Produzir a seção
`## Features candidatas a tier pago` com a tabela
`| Feature | Onde vive (arquivo:linha) | Ponto exato onde o gate entraria | Mecanismo disponível hoje | Esforço de ativação | Bloqueio |`.
Cobrir no mínimo as 4 citadas no requisito GATE-03, e acrescentar as que o código
revelar:
  1. IA gerenciada com cota maior (`managed.py`, `metering.py`)
  2. ajuste de intervalo (`brapi_budget.set_spot_intervalo`, linha 244)
  3. alvo dinâmico (`server/app/agent.py`, `avaliar_alvo_dinamico`)
  4. recorte de eficiência (aba "Eficiência da IA", `analytics`/`ai_activity.py`)

"Esforço de ativação" em custo de contexto/alcance (ex.: "1 decorator numa rota" vs.
"novo campo persistido + sync nos dois stores + recibo de loja"), NUNCA em horas. A
coluna "Bloqueio" registra o que impede hoje: decisão comercial pendente, mecanismo
inexistente, ou paridade `deviceStore`/`serverStore` a replicar.

Regra dura: NÃO propor preço, limite numérico, nome de tier ou empacotamento comercial —
isso é Out of Scope declarado. A entrega é o mapa técnico.

**Fechar o arquivo** com `## Cobertura de requisitos`: tabela `| Requisito | Achados |
Status |` com exatamente 3 linhas (GATE-01, GATE-02, GATE-03), status ∈
{`com achados`, `conforme`}. E preencher `## Verificado e conforme` — em especial o que
o ADR-010 já resolveu bem e não precisa virar item de roadmap.
  </action>

  <verify>
    <automated>F=.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-GATE.md; grep -q '^## Cota física × cap comercial' "$F" && grep -q '^## Features candidatas a tier pago' "$F" && grep -q '^## Cobertura de requisitos' "$F" && grep -q '^## Verificado e conforme' "$F" && for r in GATE-01 GATE-02 GATE-03; do grep -q "$r" "$F" || exit 1; done</automated>
  </verify>

  <acceptance_criteria>
    - `## Cota física × cap comercial` tem 2 linhas (brapi e cap de IA), cada uma com
      arquivo:linha do cálculo, rota que expõe e o texto real visto pelo usuário quando
      estoura (com evidência de front ou de payload de API)
    - O estado `degradado` da brapi tem veredito sobre o que a UI comunica, cruzado com
      os princípios 3 e 4 do CLAUDE.md
    - `## Features candidatas a tier pago` tem >= 4 linhas, cada uma com o ponto exato
      de código onde o gate entraria e o esforço em custo de contexto/alcance
    - Nenhuma célula ou recomendação contém preço, limite numérico proposto, nome de
      tier comercial ou modelo de loja
    - `## Cobertura de requisitos` tem 3 linhas (GATE-01..03), todas com status
    - `## Verificado e conforme` não está vazia
    - `git status --porcelain server web web-admin` vazio
  </acceptance_criteria>

  <done>A distinção entre as duas cotas está rastreada até o que o usuário vê, e as features candidatas têm ponto de gate e esforço mapeados sem nenhuma decisão comercial embutida.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhuma nova | Fase read-only: não altera código de produção, não adiciona rota, não é deployada. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Information Disclosure | FINDINGS-GATE.md | mitigate | `plan.py` e `managed.py` tocam credenciais de LLM gerenciada: citar só o NOME das variáveis (`B3_MANAGED_LLM_*`, `BRAPI_TOKEN`), nunca o valor lido do ambiente |
| T-01-10 | Tampering | Código de produção | mitigate | Read-only: acceptance criteria exige `git status --porcelain server web web-admin` vazio |
| T-01-11 | Elevation of Privilege | Auditoria de gating | accept | O plano apenas LÊ a lógica de gate; não altera `ACTIVE_PLAN`, papel de usuário nem cota persistida |
| T-01-SC | Tampering | npm/pip installs | accept | Nenhum pacote novo instalado |
</threat_model>

<verification>
- `FINDINGS-GATE.md` existe com as 7 seções na ordem especificada
- Todo achado segue `### F-GATE-NN` com os 6 campos de rótulo exato e severidade D-0X
- Nenhum achado propõe número comercial (Out of Scope)
- `git status --porcelain server web web-admin` vazio
</verification>

<success_criteria>
- GATE-01..GATE-03 na tabela `## Cobertura de requisitos` com status explícito
- Veredito claro sobre reescrita estrutural: o gating "estruturalmente pronto" do
  ADR-010 se confirma ou não, com evidência
- Nenhuma correção implementada
</success_criteria>

<output>
Create `.planning/phases/01-auditoria-diagn-stica-consolidada/01-04-SUMMARY.md` when done
</output>
