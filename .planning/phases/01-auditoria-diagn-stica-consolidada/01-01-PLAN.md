---
phase: 01-auditoria-diagn-stica-consolidada
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-STORY.md
autonomous: true
requirements: [STORY-01, STORY-02, STORY-03, STORY-04]

must_haves:
  truths:
    - "O relatório bruto de storyline registra a jornada REAL observada (não inferida) do primeiro acesso até a primeira ordem simulada, passo a passo contra os 8 passos da Experiência Principal do CLAUDE.md"
    - "Cada um dos 4 requisitos STORY tem cobertura explícita: pelo menos um achado OU uma declaração 'conforme' com evidência"
    - "Todo achado carrega severidade da régua D-02..D-05, evidência (arquivo:linha ou tela/fluxo), método de verificação e recomendação"
    - "O arquivo declara honestamente qual nível de verificação ao vivo foi alcançado (browser real / API real / só código)"
  artifacts:
    - path: ".planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-STORY.md"
      provides: "Achados brutos da dimensão storyline pedagógico"
      contains: "### F-STORY-"
  key_links:
    - from: "FINDINGS-STORY.md"
      to: "CLAUDE.md (Experiência Principal, Camada educacional)"
      via: "cada achado cita a regra violada"
      pattern: "CLAUDE\\.md"
    - from: "FINDINGS-STORY.md"
      to: "REPORT-01.md (plano 01-06)"
      via: "formato de achado padronizado F-STORY-NN"
      pattern: "\\*\\*Severidade:\\*\\*"
---

<objective>
Auditar a dimensão **storyline pedagógico** (Modo Estudo → Modo Operador) do Boris+ e
produzir achados brutos com severidade, evidência e recomendação.

Purpose: o Core Value do produto é "o usuário leigo sai do Modo Estudo entendendo de
verdade como o mercado funciona". Se a jornada pedagógica não convence, nada mais no
produto importa. Esta é a dimensão de maior peso do relatório.

Output: `.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-STORY.md`

**Esta fase é READ-ONLY sobre o produto.** Nenhum arquivo em `server/`, `web/` ou
`web-admin/` pode ser modificado. O único arquivo escrito por este plano é o
FINDINGS-STORY.md. Correção de achado é fase futura (PROJECT.md Out of Scope:
"Corrigir achados nesta fase").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-auditoria-diagn-stica-consolidada/01-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/codebase/ARCHITECTURE.md
</context>

<severity_rubric>
Régua fechada pelo usuário em 01-CONTEXT.md. Aplicar literalmente, sem inventar níveis:

- **Crítico (D-02)** — viola um dos 10 princípios obrigatórios do `CLAUDE.md` do repo
  OU o guardrail CVM (manchete do card de decisão só do motor determinístico).
- **Alto (D-03)** — já causou incidente real documentado OU bloqueia uma decisão de
  negócio pendente.
- **Médio (D-04)** — risco real, ainda não materializado em incidente.
- **Baixo (D-05)** — polimento/consistência sem risco de produto.
</severity_rubric>

<finding_format>
Formato OBRIGATÓRIO e idêntico nos 5 planos da wave 1 — o plano 01-06 consolida por
parsing mecânico deste formato. Não alterar rótulos nem ordem dos campos.

```
### F-STORY-01 — {título de uma linha}
- **Requisito:** STORY-01
- **Severidade:** Alto — {qual regra da régua se aplica, em uma frase, citando D-03}
- **Evidência:** {arquivo:linha} — {o que está lá} | {tela/fluxo navegado} — {o que apareceu}
- **Verificação:** {ao vivo | API real | código/docs}
- **Impacto:** {consequência concreta pro usuário ou pro negócio}
- **Recomendação:** {ação — uma frase se Baixo/Médio, um parágrafo se Alto/Crítico}
```
</finding_format>

<tasks>

<task type="auto">
  <name>Task 1: Subir o stack local e percorrer ao vivo os 8 passos da Experiência Principal</name>

  <files>.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-STORY.md</files>

  <read_first>
    - `CLAUDE.md` (raiz do repo) — seções "Experiência principal" (os 8 passos),
      "Princípios obrigatórios" (os 10), "Camada educacional"
    - `scripts/run.sh` linhas 34-48 (`free_port`) — entender POR QUE `executar.sh`
      não pode ser usado aqui
    - `web/vite.config.js` (bloco `server:`) — porta 5174, `strictPort`, proxy
      `/api` → `localhost:8787`
    - `server/app/main.py:204-276` — payload exato de `/api/auth/register`,
      `/api/auth/login`, `/api/auth/me` (não adivinhar os nomes dos campos)
    - `server/app/main.py:786-798` — `/api/state` e `PUT /api/config` (é por aqui
      que se troca `appMode` entre `estudo` e `operador`)
  </read_first>

  <action>
**1a. Subir o backend compartilhado (idempotente).**

PROIBIDO rodar `bash scripts/executar.sh`, `bash executar.sh` ou `bash scripts/run.sh`
nesta fase: `run.sh:free_port()` MATA processos do projeto nas portas 8787/5174 e
derrubaria o servidor que os planos 01-02 (UX) e 01-05 (ADMIN) estiverem usando em
paralelo na mesma wave. Subir o uvicorn direto, com guarda:

```bash
curl -fsS http://localhost:8787/api/health >/dev/null 2>&1 || \
  ( cd server && B3_ADMIN_EMAILS=auditoria-admin@local.test \
    nohup ./.venv/bin/python -m uvicorn app.main:app --host 127.0.0.1 --port 8787 \
    >/tmp/b3-audit-backend.log 2>&1 & )
```

Usar o Bash tool com `run_in_background: true`. Se `server/.venv/bin/python` não
existir neste worktree, usar o venv do clone principal:
`/Users/acamerini/dev/bolsia/b3-agente/server/.venv/bin/python`.
Depois, aguardar saúde: repetir `curl -fsS http://localhost:8787/api/health` até
responder (máx. 40 tentativas × 0,5s). Se outro plano ganhou a corrida e o segundo
uvicorn falhou com "address already in use", isso é o resultado ESPERADO — basta o
health responder. NUNCA rodar `--stop` ao final: outro plano da wave pode estar usando.

**1b. Subir o Vite deste plano na porta 5174** (a padrão de `web/vite.config.js`):
`cd web && npm run dev -- --host` em background. `web/node_modules` já existe neste
worktree. O plano 01-02 (UX) usa a 5176 e o 01-05 (ADMIN) a 5175 — não há colisão.

**1c. Criar uma conta isolada para esta auditoria** via `POST /api/auth/register` com
e-mail `auditoria-story@local.test` (o escopo `user_id` isola o estado deste plano do
estado do plano de UX, que roda em paralelo no mesmo backend). Guardar o Bearer token.

**1d. Escolher o nível de navegação ao vivo (escada, do melhor pro pior).** D-01 exige
verificação ao vivo para STORY; registrar honestamente qual nível foi alcançado:
  - **Nível 1 — browser dirigível:** ferramentas `mcp__claude-in-chrome__*`, se
    conectadas. Navegação + clique no DOM. Preferir sempre.
  - **Nível 2 — browser observável:** `mcp__computer-use__*` (`request_access` no
    browser, `open_application`, `screenshot`). Browsers são tier "read": dá pra VER a
    tela renderizada mas NÃO clicar — combinar com o Nível 3 para mudar de estado.
  - **Nível 3 — API real + código:** dirigir o backend real por `curl`
    (`POST /api/buy`, `POST /api/sell`, `PUT /api/config` para trocar `appMode`,
    `GET /api/state`, `GET /api/quotes`, `POST /api/analyze/{ticker}`,
    `GET /api/conceitos`, `POST /api/assistente`) e ler o caminho de render
    correspondente em `web/src/App.jsx`. Sempre disponível.
Não insistir num nível indisponível: descer um degrau e seguir.

**1e. Percorrer os 8 passos do CLAUDE.md, nos DOIS modos** (`appMode: "estudo"` e
`appMode: "operador"`, trocado por `PUT /api/config` ou pela tela Perfil → Modo de
trabalho): (1) escolher ativo → (2) visualizar dados e horário da atualização →
(3) analisar contexto e risco → (4) enviar ordem virtual → (5) acompanhar execução
simulada → (6) visualizar resultado → (7) receber explicação educacional →
(8) registrar o aprendizado e comparar com o benchmark.

Usar um ticker líquido real (ex.: `PETR4`) e uma ordem de compra pequena.

**1f. Criar `.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-STORY.md`**
com exatamente estas seções, nesta ordem:
  - `# Achados — Dimensão STORY (storyline pedagógico)` + data
  - `## Método de verificação` — qual nível da escada (1/2/3) foi alcançado, como o
    stack foi subido, qual conta/ticker foi usado, e o que NÃO pôde ser verificado ao
    vivo (limitação explícita, não omissão)
  - `## Roteiro navegado` — uma tabela `| # | Passo (CLAUDE.md) | Modo | Tela/rota
    observada | O que apareceu | Método |` com **16 linhas** (8 passos × 2 modos).
    Se um passo não existe no produto, a linha existe mesmo assim com "AUSENTE" em
    "Tela/rota observada" — ausência é o achado.
  - `## Achados` (vazia por ora — preenchida nas tasks 2 e 3)
  - `## Verificado e conforme` (vazia por ora)
  - `## Cobertura de requisitos` (vazia por ora)

Ao ler `web/src/App.jsx` (7599 linhas), NUNCA ler o arquivo inteiro: usar `grep -n`
para achar a faixa relevante e só então `Read` com `offset`/`limit`.
  </action>

  <verify>
    <automated>test -f .planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-STORY.md && grep -q '^## Método de verificação' .planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-STORY.md && test "$(grep -cE '^\| *[1-8] *\|' .planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-STORY.md)" -ge 16</automated>
  </verify>

  <acceptance_criteria>
    - `FINDINGS-STORY.md` existe no diretório da fase
    - Contém a seção `## Método de verificação` declarando qual nível da escada (1, 2
      ou 3) foi alcançado e listando explicitamente o que não pôde ser verificado ao vivo
    - `## Roteiro navegado` tem ≥ 16 linhas de tabela (8 passos × 2 modos), cada uma
      com tela/rota observada (ou "AUSENTE") e método de verificação
    - Nenhum arquivo sob `server/`, `web/` ou `web-admin/` foi modificado
      (`git status --porcelain server web web-admin` vazio)
  </acceptance_criteria>

  <done>O roteiro real dos 8 passos × 2 modos está registrado com evidência observada, e o método de verificação está declarado honestamente.</done>
</task>

<task type="auto">
  <name>Task 2: Achados STORY-01 (jornada vs. 8 passos) e STORY-02 (transição Estudo→Operador)</name>

  <files>.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-STORY.md</files>

  <read_first>
    - `.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-STORY.md` (o
      roteiro navegado da task 1 é a base de evidência)
    - `CLAUDE.md` — "Experiência principal" (8 passos)
    - `web/src/copy.js` (162 linhas, `COPY.estudo` / `COPY.operador`) — vocabulário por modo
    - `web/src/disclaimers.js` (41 linhas)
    - `server/app/skill_ref.py` (310 linhas) — `vocab["educacional"]`, `vocab["operador"]`,
      `TIMING[modo][estado]`, Princípio 1
    - `docs/plano-operador-entrada-e-modos.md` — decisões fechadas em 2026-08-07 sobre a
      separação Estudo/Operador; verificar se as Fases A–D foram implementadas ou seguem
      pendentes (a seção "Status" e o código mandam, não a intenção do documento)
    - `web/src/App.jsx` — só as faixas do `ModoTrabalhoCard` (~1779) e do onboarding;
      localizar por `grep -n "appMode\|ModoTrabalhoCard\|Modo de trabalho" web/src/App.jsx`
      e ler apenas as faixas encontradas, nunca o arquivo inteiro
  </read_first>

  <action>
Preencher a seção `## Achados` de `FINDINGS-STORY.md` com os achados de STORY-01 e
STORY-02, usando o formato `### F-STORY-NN` definido em `<finding_format>` e a régua
D-02..D-05 de `<severity_rubric>`.

**STORY-01 — jornada real vs. os 8 passos.** A partir da tabela `## Roteiro navegado`,
registrar um achado para cada passo que: está AUSENTE do produto; existe mas exige que
o usuário descubra sozinho onde fica; existe mas quebra a ordem narrativa; ou está
presente num modo e ausente no outro sem explicação. Passos 7 ("receber explicação
educacional") e 8 ("registrar o aprendizado e comparar com o benchmark") são os de maior
risco pro Core Value — auditar com atenção redobrada e não aceitar "existe a tela" como
suficiente: a pergunta é se o usuário leigo sai entendendo o raciocínio.

**STORY-02 — transição Estudo→Operador tem gatilho e narrativa, ou é um toggle mudo?**
Responder com evidência a estas quatro perguntas, cada uma virando achado ou entrada em
`## Verificado e conforme`:
  1. O produto diz ao usuário **o que muda** ao trocar de modo (vocabulário, execução
     automática, ferramentas)?
  2. O produto diz **por que** e **quando** ele deveria migrar — existe algum critério
     de prontidão, ou é livre a qualquer momento?
  3. A troca acontece onde o usuário procuraria (Perfil → Modo de trabalho) ou está
     enterrada? Conferir contra o achado já documentado em
     `.planning/codebase/CONCERNS.md` ("Dois nomes 'Operador' sem link entre si") —
     este plano avalia o LADO NARRATIVO/PEDAGÓGICO; o lado de dívida técnica do mesmo
     ponto pertence ao plano 01-03 (CODE). Se ambos os planos tocarem o mesmo fato,
     registrar aqui a faceta pedagógica e marcar `**Possível duplicata:** CODE` na
     linha de Impacto, para o plano 01-06 deduplicar.
  4. O que `docs/plano-operador-entrada-e-modos.md` decidiu (Fase A — "Modo Estudo nunca
     executa") está de fato implementado? Verificar no código e, se possível, ao vivo.

Achado que só existe como hipótese ("provavelmente confunde") NÃO entra — só entra o que
foi observado ao vivo, exercitado por API, ou lido no código com arquivo:linha.
  </action>

  <verify>
    <automated>test "$(grep -cE '^### F-STORY-[0-9]+' .planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-STORY.md)" -ge 1 && grep -qE '^- \*\*Requisito:\*\* STORY-01' .planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-STORY.md && grep -qE '^- \*\*Requisito:\*\* STORY-02' .planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-STORY.md</automated>
  </verify>

  <acceptance_criteria>
    - STORY-01 e STORY-02 aparecem cada um em ≥ 1 achado `### F-STORY-NN` OU em uma
      entrada de `## Verificado e conforme` com evidência arquivo:linha
    - Todo achado tem os 6 campos do formato (Requisito, Severidade, Evidência,
      Verificação, Impacto, Recomendação), nesta ordem, com os rótulos exatos
    - Toda severidade cita a regra aplicada (D-02, D-03, D-04 ou D-05)
    - As 4 perguntas de STORY-02 estão todas respondidas com evidência
    - Nenhum achado é hipotético: cada um tem arquivo:linha, tela navegada ou chamada
      de API como evidência
  </acceptance_criteria>

  <done>STORY-01 e STORY-02 estão cobertos com achados evidenciados e classificados pela régua D-02..D-05.</done>
</task>

<task type="auto">
  <name>Task 3: Achados STORY-03 (cobertura didática) e STORY-04 (violações de promessa) + tabela de cobertura</name>

  <files>.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-STORY.md</files>

  <read_first>
    - `CLAUDE.md` — seção "Camada educacional" (a lista dos 13 conceitos) e as 6
      proibições da IA ("não deve: garantir que uma operação dará lucro; ...")
    - `server/app/conceitos.py` (479 linhas) — catálogo determinístico de conceitos
    - `server/app/kb.py` e `server/app/mercado_ref.py` — verbetes de mecânica da B3
    - `server/app/skill_ref.py` (310 linhas) — vocabulário canônico e `DIDATICA`
    - `server/app/defaults.py` — texto dos prompts default enviados à LLM
    - `web/src/copy.js`, `web/src/catalog.js` — espelho do vocabulário no front
    - `docs/didatica-inventario.md` (98 linhas) — inventário do que o app AFIRMA sem
      explicar; é insumo, não conclusão: confirmar se ainda vale hoje
    - `docs/adr/006-camada-de-entendimento.md`, `docs/adr/007-assistente-e-push-do-gatilho.md`
    - `.claude/skills/didatica-boris/SKILL.md`
  </read_first>

  <action>
**STORY-03 — cobertura real da camada educacional.** Montar em `## Achados` (antes dos
achados de STORY-03) uma tabela `| Conceito (CLAUDE.md) | Onde é ensinado hoje | Como
(afordância) | Profundidade | Lacuna |` com **13 linhas**, uma por conceito da lista do
CLAUDE.md: tendência; momentum; valor; qualidade; volatilidade; suporte e resistência;
rompimentos; reversão à média; diversificação; risco-retorno; drawdown; expectativa
matemática; diferença entre taxa de acerto e rentabilidade.

Para cada conceito, a coluna "Onde é ensinado hoje" recebe arquivo:linha
(`conceitos.py`, `kb.py`, `skill_ref.py`, `copy.js`, ou a região `SetorAlvo` em
`App.jsx`) ou "NÃO ENSINADO". "Profundidade" classifica em: `definição` (só diz o que é)
/ `correlação` (liga a outros indicadores) / `decisão` (liga à decisão do usuário) —
o padrão-alvo do Modo Estudo é `decisão` (memória do projeto: "Estudo ensina
indicador→correlação→decisão"). Cada conceito com lacuna vira um achado ou uma linha
agregada num achado de cobertura, com severidade pela régua.

**STORY-04 — inventário de violações REAIS de promessa.** As proibições do CLAUDE.md:
garantir lucro; afirmar 100% de chance/confluência; inventar estatística; ocultar perdas;
transformar simulação em recomendação financeira; executar operação real. E a obrigação:
dizer explicitamente "Não há dados suficientes para concluir" quando faltar evidência.

Auditar as TRÊS superfícies onde uma violação pode nascer, com evidência arquivo:linha:
  1. **Texto determinístico** — `skill_ref.py`, `copy.js`, `disclaimers.js`,
     `conceitos.py`, `kb.py`: existe alguma frase que promete, garante ou afirma certeza?
  2. **Prompt enviado à LLM** — `server/app/defaults.py` e os prompts em
     `server/app/llm.py` / `assistente.py` / `scan_deep.py`: o prompt INSTRUI a IA a
     nunca prometer e a declarar dado insuficiente? Existe guardião de teste travando
     isso (`server/tests/test_auditoria_prompts.py`, `test_m3_format_pede_null_nunca_zero`)?
     Um prompt sem essa instrução é a violação — não é preciso a IA responder para o
     achado ser real.
  3. **Saída real da IA** — só se houver chave BYOK configurada no ambiente local
     (`GET /api/ai/models`, `POST /api/analyze/{ticker}`, `POST /api/assistente`).
     Se não houver chave, NÃO inventar: registrar em `## Método de verificação` que a
     superfície 3 não foi exercitada e que as superfícies 1 e 2 foram.

Também verificar o guardrail CVM: a **manchete do card de decisão** vem só do motor
determinístico? Se a manchete puder vir da LLM em algum caminho, é achado **Crítico**
(D-02). Localizar por `grep -n "manchete\|headline\|veredito" server/app/*.py web/src/App.jsx`.

**Fechar o arquivo** com a seção `## Cobertura de requisitos`: tabela
`| Requisito | Achados | Status |` com exatamente 4 linhas (STORY-01, STORY-02,
STORY-03, STORY-04), onde Status é `com achados` ou `conforme` — nenhuma linha pode
ficar vazia ou ausente. E preencher `## Verificado e conforme` com o que foi auditado e
está OK (distinguir "auditado e conforme" de "não auditado" é obrigatório).
  </action>

  <verify>
    <automated>F=.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-STORY.md; grep -q '^## Cobertura de requisitos' $F && for r in STORY-01 STORY-02 STORY-03 STORY-04; do grep -q "$r" $F || exit 1; done && test "$(grep -cE '^\| *(tend|moment|valor|qualidade|volatil|suporte|rompiment|revers|diversif|risco|drawdown|expectativa|taxa)' $F)" -ge 13</automated>
  </verify>

  <acceptance_criteria>
    - Tabela de cobertura didática tem 13 linhas, uma por conceito do CLAUDE.md, cada
      uma com arquivo:linha ou "NÃO ENSINADO" e uma classificação de profundidade
      (`definição` / `correlação` / `decisão`)
    - STORY-04 audita as 3 superfícies (texto determinístico, prompt, saída real da IA);
      se a superfície 3 não foi exercitada, isso está declarado em
      `## Método de verificação`, não omitido
    - O guardrail CVM (manchete só do motor determinístico) foi verificado
      explicitamente, com arquivo:linha, e classificado
    - `## Cobertura de requisitos` tem 4 linhas (STORY-01..04), todas com status
    - `## Verificado e conforme` não está vazia
    - Nenhum arquivo sob `server/`, `web/` ou `web-admin/` foi modificado
  </acceptance_criteria>

  <done>Os 4 requisitos STORY estão cobertos, a cobertura didática dos 13 conceitos está mapeada com evidência, e o inventário de violações de promessa é factual (não hipotético).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhuma nova | Esta fase não altera código de produção, não adiciona rota, não recebe input de usuário final e não é deployada. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | FINDINGS-STORY.md | mitigate | Não copiar segredo de ambiente (`BRAPI_TOKEN`, chaves de LLM, tokens de sessão) para dentro do relatório; citar só o NOME da variável |
| T-01-02 | Tampering | Código de produção | mitigate | Plano é read-only: acceptance criteria exige `git status --porcelain server web web-admin` vazio ao fim de cada task |
| T-01-03 | Denial of Service | Backend local :8787 compartilhado na wave | accept | Risco local de dev; mitigado por não usar `run.sh --stop`/`free_port` e por contas isoladas por escopo |
| T-01-SC | Tampering | npm/pip installs | accept | Nenhum pacote novo é instalado por este plano (`web/node_modules` já existe no worktree; `server/.venv` já existe) |
</threat_model>

<verification>
- `FINDINGS-STORY.md` existe, com as 6 seções na ordem especificada
- Todo achado segue o formato `### F-STORY-NN` com os 6 campos de rótulo exato
- Toda severidade é uma de Crítico/Alto/Médio/Baixo e cita D-02..D-05
- `git status --porcelain server web web-admin` retorna vazio (nada de produção tocado)
</verification>

<success_criteria>
- STORY-01, STORY-02, STORY-03 e STORY-04 aparecem na tabela `## Cobertura de requisitos`
  com status explícito
- O método de verificação ao vivo está declarado com honestidade, incluindo limitações
- Nenhuma correção de código foi implementada
</success_criteria>

<output>
Create `.planning/phases/01-auditoria-diagn-stica-consolidada/01-01-SUMMARY.md` when done
</output>
