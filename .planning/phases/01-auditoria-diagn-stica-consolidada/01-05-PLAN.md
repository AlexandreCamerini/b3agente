---
phase: 01-auditoria-diagn-stica-consolidada
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-ADMIN.md
autonomous: true
requirements: [ADMIN-01, ADMIN-02, ADMIN-03]

must_haves:
  truths:
    - "As 10 abas do web-admin estão mapeadas contra a permissão RBAC que as gateia no BACKEND, não só no front"
    - "O incidente real do kill-switch (2,5 dias) foi replayado contra o portal atual: o que ele teria mostrado, quando, e o que ainda falta para detectar"
    - "O handoff mobile do ADR-014 tem veredito de usabilidade com evidência de código ou de execução"
    - "Cada um dos 3 requisitos ADMIN tem cobertura explícita: achado ou 'conforme' com evidência"
  artifacts:
    - path: ".planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-ADMIN.md"
      provides: "Achados brutos da dimensão portal de administração/observabilidade"
      contains: "### F-ADMIN-"
  key_links:
    - from: "FINDINGS-ADMIN.md"
      to: "docs/adr/013-rbac-papeis-e-entitlements.md"
      via: "tabela aba → permissão → rota → gate de backend"
      pattern: "ADR-013"
    - from: "FINDINGS-ADMIN.md"
      to: "REPORT-01.md (plano 01-06)"
      via: "formato de achado padronizado F-ADMIN-NN"
      pattern: "Severidade"
---

<objective>
Auditar a dimensão **portal de administração e observabilidade** (`web-admin/`) contra o
RBAC do ADR-013 e o handoff mobile do ADR-014, e produzir achados brutos com severidade,
evidência arquivo:linha e recomendação.

Purpose: o incidente real mais caro registrado no projeto foi operacional, não de código
— o kill-switch ligado sem querer parou a execução de TODA a base por 2,5 dias. A
pergunta central desta dimensão é se o portal de hoje teria detectado isso e permitido
agir rápido.

Output: `.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-ADMIN.md`

**Verificação:** código + docs é suficiente (D-01). Verificação ao vivo do `web-admin/`
é desejável mas condicionada: `web-admin/node_modules` NÃO existe neste worktree. Se
`npm install` em `web-admin/` funcionar sem rede bloqueada, subir o portal em :5175 e
verificar ao vivo; se falhar, seguir só com código e **declarar a limitação** em
`## Método de verificação` — a estrutura da fase autoriza explicitamente código-only
para esta dimensão, desde que a limitação seja anotada.

**Esta fase é READ-ONLY sobre o produto.** Nenhum arquivo em `server/`, `web/` ou
`web-admin/` pode ser modificado (`web-admin/node_modules/` gerado por `npm install`
não é alteração de código-fonte e é ignorado pelo git).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-auditoria-diagn-stica-consolidada/01-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/ARCHITECTURE.md
</context>

<severity_rubric>
Régua fechada pelo usuário em 01-CONTEXT.md. Aplicar literalmente:

- **Crítico (D-02)** — viola um dos 10 princípios obrigatórios do `CLAUDE.md` do repo
  OU o guardrail CVM.
- **Alto (D-03)** — **já causou incidente real documentado** (o kill-switch de 2,5 dias
  é o exemplo literal citado em D-03) OU bloqueia decisão de negócio pendente.
- **Médio (D-04)** — risco real, ainda não materializado em incidente.
- **Baixo (D-05)** — polimento/consistência sem risco de produto.

Calibração desta dimensão: rota admin sem `require_permission` no BACKEND é escalada de
privilégio real — tratar como Alto no mínimo, independentemente de o front esconder o
botão (o anti-padrão "Esconder admin só no front" está documentado em
`.planning/codebase/ARCHITECTURE.md`).
</severity_rubric>

<finding_format>
Formato OBRIGATÓRIO e idêntico nos 5 planos da wave 1 — o plano 01-06 consolida por
parsing mecânico. Não alterar rótulos nem ordem.

```
### F-ADMIN-01 — {título de uma linha}
- **Requisito:** ADMIN-01
- **Severidade:** Alto — {qual regra da régua se aplica, citando D-02/D-03/D-04/D-05}
- **Evidência:** {arquivo:linha} — {o que está lá} | {tela do portal} — {o que apareceu}
- **Verificação:** {código/docs | API real | ao vivo}
- **Impacto:** {consequência concreta para quem opera o produto}
- **Recomendação:** {ação — uma frase se Baixo/Médio, um parágrafo se Alto/Crítico}
```
</finding_format>

<tasks>

<task type="auto">
  <name>Task 1: ADMIN-01 — cobertura das 10 abas contra os grupos RBAC do ADR-013 (front × backend)</name>

  <files>.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-ADMIN.md</files>

  <read_first>
    - `docs/adr/013-rbac-papeis-e-entitlements.md` — seções "Decisões do Alex
      incorporadas (2026-08-16)", "1. Modelo de papel/plano", "2. Mapeamento
      função→permissão", "3. Onde a checagem é aplicada", "4. web-admin/ como central de
      administração", "Pendente (decisão do Alex)"
    - `server/app/rbac.py` (101 linhas, arquivo inteiro) — `GRUPOS` (21), `ROLE_ADMIN`
      (33), `permissoes_do_papel` (40), `permissions_for_user` (49), `grant_role` (61),
      `_is_admin_bootstrap` (79), `ensure_bootstrap_role` (91)
    - `web-admin/src/App.jsx:1100-1180` — a constante `VIEWS` (10 abas, cada uma com
      `perm`) e o filtro `visiveis` (1176)
    - `server/app/main.py:440-760` — as rotas `/api/obs/*`, `/api/analytics/*`,
      `/api/admin/*` e seus `Depends(require_permission(...))`
    - `server/tests/test_adr013_cobertura_rotas.py` — o guardião que trava a cobertura
      de rotas admin
  </read_first>

  <action>
**1a. Criar `.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-ADMIN.md`**
com estas seções, nesta ordem:
  - `# Achados — Dimensão ADMIN (portal de administração/observabilidade)` + data
  - `## Método de verificação` — o que foi lido, se o portal subiu ao vivo (e em que
    porta) ou não, e a limitação declarada caso `npm install` em `web-admin/` falhe
  - `## Aba × permissão × rota × gate de backend` — tabela (ver 1b)
  - `## Achados`
  - `## Verificado e conforme` (fechada na task 2)
  - `## Cobertura de requisitos` (fechada na task 2)

**1b. Tabela `## Aba × permissão × rota × gate de backend`** com colunas
`| Aba (VIEWS) | perm no front | Rotas que consome | Gate no backend (arquivo:linha) | Grupo RBAC do ADR-013 | Divergência |`
e **10 linhas**, uma por aba de `web-admin/src/App.jsx:1100-1110`: Visão Geral, Custos,
Comportamento do Usuário, Eficiência da IA, Automação, Mudança de LLM, Fontes de dados,
Prompts, Usuários e papéis, Auditoria.

Pontos de atenção que a leitura precisa resolver com evidência, não com suposição:
  - A aba **Auditoria** (`App.jsx:1110`) aparece SEM campo `perm` — o filtro
    `visiveis` (1176) usa `!v.perm || perms.includes(v.perm)`, ou seja, ela é visível
    para qualquer um que abra o portal. Verificar qual é o gate da rota
    `GET /api/admin/audit` (`main.py:741`): se o backend gateia, é achado de coerência
    de front (Médio); se NÃO gateia, é escalada de privilégio (Alto ou mais).
  - Para cada aba, confirmar que **toda** rota consumida tem `require_permission` ou
    `require_any_admin_permission` no backend. Levantar com:
    `grep -n "require_permission\|require_any_admin_permission" server/app/main.py`
    e cruzar com a lista de rotas `/api/admin/*`, `/api/obs/*`, `/api/analytics/*`.
  - Confrontar com o guardião `server/tests/test_adr013_cobertura_rotas.py`: ele cobre
    todas as rotas de hoje, ou existem rotas novas fora da allowlist dele?

**1c. Avaliar os grupos contra cenários reais de operação.** Os `GRUPOS` de
`rbac.py:21` foram desenhados por macro função. Testar a régua contra 4 cenários e
registrar veredito por cenário (achado ou linha em `## Verificado e conforme`):
  1. alguém que só precisa OLHAR custo e uso, sem poder mudar nada
  2. alguém que precisa desligar o kill-switch numa emergência, sem acesso a prompts
     nem a dados de usuário
  3. alguém que edita prompt/didática, sem acesso a custo nem a gestão de papéis
  4. o próprio Alex (dono), que precisa de tudo — e o bootstrap aditivo
     (`ensure_bootstrap_role`, `_is_admin_bootstrap`) garante que ele nunca se tranque
     para fora
Se um cenário não tem grupo que o sirva sem dar permissão a mais, isso é achado.

**1d. Subir o portal ao vivo, se possível** (opcional, aumenta a confiança):
backend compartilhado — subir só se `curl -fsS http://localhost:8787/api/health` não
responder, e NUNCA por `scripts/executar.sh`/`run.sh` (o `free_port` deles mata o
servidor dos planos 01-01 e 01-02, que rodam em paralelo):

```bash
curl -fsS http://localhost:8787/api/health >/dev/null 2>&1 || \
  ( cd server && B3_ADMIN_EMAILS=auditoria-admin@local.test \
    nohup ./.venv/bin/python -m uvicorn app.main:app --host 127.0.0.1 --port 8787 \
    >/tmp/b3-audit-backend.log 2>&1 & )
cd web-admin && npm install && npm run dev   # porta 5175 (web-admin/vite.config.js)
```

`B3_ADMIN_EMAILS=auditoria-admin@local.test` é o que permite registrar essa conta e
receber o papel de admin por bootstrap. Se `npm install` falhar (rede bloqueada,
registry indisponível), NÃO insistir: registrar a limitação em `## Método de
verificação` e seguir com auditoria de código — a estrutura da fase autoriza isso
explicitamente para esta dimensão.
  </action>

  <verify>
    <automated>F=.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-ADMIN.md; test -f "$F" && grep -q '^## Aba × permissão × rota × gate de backend' "$F" && for a in "Visão Geral" "Custos" "Comportamento" "Eficiência da IA" "Automação" "LLM" "Fontes de dados" "Prompts" "Usuários" "Auditoria"; do grep -q "$a" "$F" || exit 1; done && grep -qE '^- \*\*Requisito:\*\* ADMIN-01' "$F"</automated>
  </verify>

  <acceptance_criteria>
    - `FINDINGS-ADMIN.md` existe com as 6 seções na ordem especificada
    - A tabela tem 10 linhas, uma por aba de `VIEWS`, cada uma com a permissão do front,
      as rotas consumidas e o gate de backend com arquivo:linha (ou `SEM GATE`)
    - O caso da aba Auditoria sem `perm` no front tem veredito explícito, com o gate
      real de `GET /api/admin/audit` verificado no backend
    - Os 4 cenários de operação têm veredito individual (grupo que serve, ou achado)
    - `## Método de verificação` declara se o portal subiu ao vivo e, se não subiu, por quê
    - ADMIN-01 aparece em >= 1 achado `### F-ADMIN-NN` ou em `## Verificado e conforme`
    - `git status --porcelain server web web-admin` vazio (ignorando `node_modules/`,
      que é gitignored)
  </acceptance_criteria>

  <done>As 10 abas estão rastreadas até o gate de backend real, e os grupos RBAC têm veredito contra 4 cenários concretos de operação.</done>
</task>

<task type="auto">
  <name>Task 2: ADMIN-02 (replay do incidente do kill-switch) e ADMIN-03 (handoff mobile) + tabela de cobertura</name>

  <files>.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-ADMIN.md</files>

  <read_first>
    - `.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-ADMIN.md`
    - `.planning/codebase/CONCERNS.md` — "Kill-switch é controle manual sem expiração
      automática" e "Kill-switch/heartbeat — corrigido, mas o padrão de bug pode se repetir"
    - `server/app/agent.py:126-206` (kill-switch, padrão memória→DB→env) e
      `server/app/agent.py:874-960` (`scheduler_loop`, heartbeat ANTES dos gates)
    - `server/app/timing_watch.py:39,58-59` — o SEGUNDO kill-switch (push do gatilho,
      `B3_TIMING_PUSH_KILL`)
    - `server/app/brapi_budget.py:170` (`snapshot`) e `server/app/candle_provider.py:98-142`
      (`snapshot()`/`alerta`) — o que alimenta o painel
    - `server/app/main.py:440-500` — `/api/obs/logs`, `/api/obs/usage`,
      `/api/obs/brapi/projecao`; `:600-677` — `/api/admin/summary` e o kill-switch
    - `web-admin/src/App.jsx` — o KPI do kill-switch (~linha 96) e os componentes
      `VisaoGeral`, `Custos`, `Automacao`
    - `docs/adr/014-administracao-mobile.md` (207 linhas) — seções "Fatos levantados
      (não redescubra)", "1. Arquitetura de entrega", "2. Tratamento por superfície",
      "Mapeamento tela → permissão", "O que fica de fora desta rodada", "Pendente"
    - `server/app/main.py:752-780` — `POST /api/admin/mobile-handoff` e
      `/mobile-handoff/exchange`
  </read_first>

  <action>
**ADMIN-02 — replay do incidente real, não avaliação genérica.** O incidente: o
kill-switch foi ligado sem querer e parou a execução automática de TODA a base por
2,5 dias; o heartbeat continuava batendo (ficava ANTES do portão) e mascarava o estado.

Produzir a seção `## Replay do incidente do kill-switch` com a tabela
`| Momento | Sinal disponível no portal hoje | Onde apareceria (aba + arquivo:linha) | Alguém seria notificado? | Lacuna |`
e no mínimo **4 linhas**: (T0) kill-switch é ligado; (T0+1h) primeiro pregão sem nenhuma
execução; (T0+1 dia) segundo dia sem execução; (T0+2,5 dias) descoberta manual.

Responder com evidência, por linha: o dado existe no backend? é exposto por alguma rota
`/api/obs/*` ou `/api/admin/*`? aparece em alguma aba do portal? é **passivo** (só
aparece se alguém abrir a tela) ou **ativo** (push/e-mail/alerta)? A distinção
passivo × ativo é o núcleo do achado: o CONCERNS.md já registra que o KPI do kill-switch
está sempre visível no dashboard (`web-admin/src/App.jsx:96`) — isso resolve
visibilidade, não detecção. Um incidente de 2,5 dias exige que alguém OLHE.

Cobrir também, cada um com achado ou linha em `## Verificado e conforme`:
  - **dois kill-switches independentes** (`agent.kill_switch_on` e
    `timing_watch.kill_switch_on`): o portal mostra os DOIS? Se mostra só um, o outro
    pode estar ligado sem ninguém saber — achado.
  - **ausência de TTL/expiração** e de alerta "ligado há X horas em horário de pregão"
  - **orçamento da brapi** (`brapi_budget.snapshot`) e o modo de falha silencioso do
    provedor (HTTP 200 com zero velas, `candle_provider.py:44-52`): o painel mostra o
    alerta certo, ou um alarme que só conta não-200 ficaria cego?
  - **métricas de IA** (`ai_activity.py`, `metering.py`, aba Eficiência da IA): dá para
    perceber um gasto anômalo antes da fatura?

Severidade: o kill-switch é o exemplo literal de D-03 (**Alto** — já causou incidente
real documentado). Não rebaixar por já existir KPI no dashboard: o incidente aconteceu
COM o portal no ar.

**ADMIN-03 — usabilidade do handoff mobile e do fluxo de quem administra.** Avaliar,
com evidência de código (`main.py:752-780`, `web/src/App.jsx` na faixa achada por
`grep -n "handoff\|mobile-handoff\|admin" web/src/App.jsx`) e, se o portal subiu ao
vivo, também na prática:
  - quantos passos até um admin, no iPhone, chegar da tela do app ao painel do portal
  - o que acontece se o token de handoff expirar ou for reusado (é single-use? tem TTL?)
  - quais das 10 abas são utilizáveis num viewport de telefone e quais não são
    (ADR-014 seção "2. Tratamento por superfície" declara a intenção — verificar se
    bate com o código)
  - o que o ADR-014 deixou como "fica de fora desta rodada" e "Pendente" ainda está
    pendente hoje

**Fechar o arquivo** com `## Cobertura de requisitos`: tabela `| Requisito | Achados |
Status |` com exatamente 3 linhas (ADMIN-01, ADMIN-02, ADMIN-03), status ∈
{`com achados`, `conforme`}. E preencher `## Verificado e conforme`.
  </action>

  <verify>
    <automated>F=.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-ADMIN.md; grep -q '^## Replay do incidente do kill-switch' "$F" && grep -q '^## Cobertura de requisitos' "$F" && grep -q '^## Verificado e conforme' "$F" && grep -q 'timing_watch' "$F" && for r in ADMIN-01 ADMIN-02 ADMIN-03; do grep -q "$r" "$F" || exit 1; done</automated>
  </verify>

  <acceptance_criteria>
    - `## Replay do incidente do kill-switch` tem >= 4 linhas de linha do tempo, cada uma
      declarando se o sinal é passivo (precisa alguém olhar) ou ativo (notifica)
    - Os DOIS kill-switches (`agent.py` e `timing_watch.py`) têm veredito sobre
      exposição no portal, com arquivo:linha
    - O modo de falha silencioso do provedor de dados (HTTP 200 com zero velas) tem
      veredito sobre o que o painel mostraria
    - ADMIN-03 responde os 4 pontos (número de passos do handoff, expiração/reuso do
      token, abas utilizáveis em viewport de telefone, pendências do ADR-014 ainda abertas)
    - `## Cobertura de requisitos` tem 3 linhas (ADMIN-01..03), todas com status
    - `## Verificado e conforme` não está vazia
    - `git status --porcelain server web web-admin` vazio (ignorando `node_modules/`)
  </acceptance_criteria>

  <done>O incidente do kill-switch foi replayado contra o portal atual com lacunas nomeadas, e o handoff mobile tem veredito de usabilidade com evidência.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhuma nova | Fase read-only: não altera código de produção, não adiciona rota, não é deployada. A auditoria LÊ o gate de RBAC; não concede nem revoga papel em ambiente real. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Elevation of Privilege | Conta admin local de auditoria | mitigate | `B3_ADMIN_EMAILS=auditoria-admin@local.test` só existe no processo local em 127.0.0.1; nunca setar essa variável em Railway nem commitar |
| T-01-13 | Information Disclosure | FINDINGS-ADMIN.md | mitigate | Achado de rota sem gate descreve a ROTA e a permissão faltante; não incluir token de sessão, e-mail de usuário real nem payload com dado de conta |
| T-01-14 | Tampering | Código de produção | mitigate | Read-only: acceptance criteria exige `git status --porcelain server web web-admin` vazio |
| T-01-SC | Tampering | npm install em web-admin/ | accept | `npm install` apenas restaura as dependências já declaradas em `web-admin/package.json` (React, Vite, @uiw/react-codemirror, @codemirror/*, @lezer/*) — nenhum pacote NOVO é adicionado, nenhum `package.json` é editado |
</threat_model>

<verification>
- `FINDINGS-ADMIN.md` existe com as 6 seções na ordem especificada
- Todo achado segue `### F-ADMIN-NN` com os 6 campos de rótulo exato e severidade D-0X
- O método de verificação declara se o portal subiu ao vivo e, se não, a limitação
- `git status --porcelain server web web-admin` vazio
</verification>

<success_criteria>
- ADMIN-01..ADMIN-03 na tabela `## Cobertura de requisitos` com status explícito
- Toda rota admin sem gate de backend (se houver) está registrada como achado de
  severidade Alto ou superior
- Nenhuma correção implementada
</success_criteria>

<output>
Create `.planning/phases/01-auditoria-diagn-stica-consolidada/01-05-SUMMARY.md` when done
</output>
