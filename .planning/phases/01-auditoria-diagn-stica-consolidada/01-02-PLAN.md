---
phase: 01-auditoria-diagn-stica-consolidada
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-UX.md
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04]

must_haves:
  truths:
    - "Os 10 princípios obrigatórios do CLAUDE.md foram auditados um a um contra telas reais, cada um com veredito e evidência"
    - "Os 9 estados exigidos pelo princípio 9 (carregamento, vazio, erro, mercado fechado, dado atrasado, ordem rejeitada, ordem parcial, operação concluída, e o estado normal) foram procurados nas telas principais"
    - "Cada um dos 4 requisitos UX tem cobertura explícita: achado ou 'conforme' com evidência"
    - "O relatório declara qual nível de verificação ao vivo foi alcançado e o que ficou fora"
  artifacts:
    - path: ".planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-UX.md"
      provides: "Achados brutos da dimensão UX/UI"
      contains: "### F-UX-"
  key_links:
    - from: "FINDINGS-UX.md"
      to: "CLAUDE.md (10 princípios obrigatórios)"
      via: "tabela princípio → veredito → evidência"
      pattern: "Princípio"
    - from: "FINDINGS-UX.md"
      to: "REPORT-01.md (plano 01-06)"
      via: "formato de achado padronizado F-UX-NN"
      pattern: "\\*\\*Severidade:\\*\\*"
---

<objective>
Auditar a dimensão **UX/UI** do Boris+ contra os 10 princípios obrigatórios do
`CLAUDE.md` do repo, com verificação ao vivo, e produzir achados brutos com severidade,
evidência e recomendação.

Purpose: os 10 princípios são a régua normativa do produto (transparência de dado
fictício, transparência de fonte/horário, estados completos, acessibilidade, copy sem
promessa). Violação de princípio é **Crítico** por definição (D-02) — esta dimensão é a
que mais produz achados críticos.

Output: `.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-UX.md`

**Esta fase é READ-ONLY sobre o produto.** Nenhum arquivo em `server/`, `web/` ou
`web-admin/` pode ser modificado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-auditoria-diagn-stica-consolidada/01-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/codebase/ARCHITECTURE.md
</context>

<severity_rubric>
Régua fechada pelo usuário em 01-CONTEXT.md. Aplicar literalmente:

- **Crítico (D-02)** — viola um dos 10 princípios obrigatórios do `CLAUDE.md` do repo
  OU o guardrail CVM (manchete do card de decisão só do motor determinístico).
- **Alto (D-03)** — já causou incidente real documentado OU bloqueia decisão de negócio pendente.
- **Médio (D-04)** — risco real, ainda não materializado em incidente.
- **Baixo (D-05)** — polimento/consistência sem risco de produto.

Atenção: em UX, "o princípio está formalmente cumprido mas o usuário não percebe"
(ex.: o disclaimer existe mas fora da dobra) é **Médio/Alto**, não Crítico — Crítico é
reservado para violação real do princípio.
</severity_rubric>

<finding_format>
Formato OBRIGATÓRIO e idêntico nos 5 planos da wave 1 — o plano 01-06 consolida por
parsing mecânico. Não alterar rótulos nem ordem.

```
### F-UX-01 — {título de uma linha}
- **Requisito:** UX-01
- **Severidade:** Crítico — {qual regra da régua se aplica, citando D-02/D-03/D-04/D-05}
- **Evidência:** {arquivo:linha} — {o que está lá} | {tela/fluxo navegado} — {o que apareceu}
- **Verificação:** {ao vivo | API real | código/docs}
- **Impacto:** {consequência concreta pro usuário}
- **Recomendação:** {ação — uma frase se Baixo/Médio, um parágrafo se Alto/Crítico}
```
</finding_format>

<tasks>

<task type="auto">
  <name>Task 1: Subir o ambiente e auditar os 10 princípios obrigatórios contra telas reais</name>

  <files>.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-UX.md</files>

  <read_first>
    - `CLAUDE.md` (raiz do repo) — a lista numerada completa dos 10 princípios obrigatórios
    - `scripts/run.sh` linhas 34-48 (`free_port`) — por que `executar.sh` é proibido aqui
    - `web/vite.config.js` (bloco `server:`) — `strictPort`, `PORT` env, proxy `/api` → 8787
    - `server/app/main.py:204-276` — payload de `/api/auth/register` e `/api/auth/login`
    - `server/app/main.py:786-798` — `/api/state` e `PUT /api/config` (troca de `appMode`)
    - `server/app/main.py:1501-1537` — `POST /api/buy` / `POST /api/sell` e o erro
      `HTTPException(502, "Sem cotacao para ...")` (é o caminho de "fonte falhou")
    - `web/src/disclaimers.js` (41 linhas) — textos de risco/dinheiro fictício
    - `web/src/copy.js` (162 linhas) — vocabulário por modo
  </read_first>

  <action>
**1a. Subir o backend compartilhado (idempotente).** PROIBIDO rodar
`scripts/executar.sh` / `executar.sh` / `scripts/run.sh`: `run.sh:free_port()` MATA
processos do projeto nas portas 8787/5174 e derrubaria o servidor dos planos 01-01 e
01-05, que rodam em paralelo nesta mesma wave. Subir o uvicorn direto, com guarda:

```bash
curl -fsS http://localhost:8787/api/health >/dev/null 2>&1 || \
  ( cd server && B3_ADMIN_EMAILS=auditoria-admin@local.test \
    nohup ./.venv/bin/python -m uvicorn app.main:app --host 127.0.0.1 --port 8787 \
    >/tmp/b3-audit-backend.log 2>&1 & )
```
(Bash tool com `run_in_background: true`. Fallback de venv:
`/Users/acamerini/dev/bolsia/b3-agente/server/.venv/bin/python`.) Aguardar
`/api/health` responder. Se o segundo uvicorn falhar com "address already in use", é o
resultado ESPERADO — outro plano subiu primeiro. NUNCA rodar `--stop`.

**1b. Subir o Vite DESTE plano na porta 5176** (não a 5174, que é do plano 01-01):
`cd web && PORT=5176 npm run dev -- --host` em background. `vite.config.js` respeita
`process.env.PORT`; `strictPort: true` garante que não há fallback silencioso.

**1c. Conta isolada:** registrar `auditoria-ux@local.test` via `POST /api/auth/register`
(escopo `user_id` separado do plano de STORY, que roda em paralelo no mesmo backend).

**1d. Escada de verificação ao vivo** (D-01 exige ao vivo para UX; registrar o nível
alcançado): **Nível 1** `mcp__claude-in-chrome__*` (navegar + clicar); **Nível 2**
`mcp__computer-use__*` (`request_access` no browser → `open_application` → `screenshot`;
browsers são tier "read": VER sim, clicar não — combinar com Nível 3 para mudar estado);
**Nível 3** `curl` na API real + leitura do caminho de render em `web/src/App.jsx`.
Não insistir num nível indisponível.

**1e. Criar `FINDINGS-UX.md`** com estas seções, nesta ordem:
  - `# Achados — Dimensão UX/UI` + data
  - `## Método de verificação` — nível alcançado, como o stack subiu, conta/ticker
    usados, e o que NÃO pôde ser verificado ao vivo (limitação explícita)
  - `## Auditoria dos 10 princípios` — tabela `| # | Princípio (CLAUDE.md) | Veredito |
    Onde foi verificado | Evidência |` com **10 linhas**, uma por princípio, na ordem do
    CLAUDE.md. Veredito ∈ {`conforme`, `parcial`, `violado`, `não verificável`}.
    `não verificável` exige justificativa na coluna Evidência.
  - `## Achados` (preenchida nas tasks 2 e 3)
  - `## Verificado e conforme` (preenchida na task 3)
  - `## Cobertura de requisitos` (preenchida na task 3)

Ao ler `web/src/App.jsx` (7599 linhas), NUNCA ler o arquivo inteiro: `grep -n` para
achar a faixa e só então `Read` com `offset`/`limit`.
  </action>

  <verify>
    <automated>F=.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-UX.md; test -f $F && grep -q '^## Auditoria dos 10 princípios' $F && test "$(grep -cE '^\| *(10|[1-9]) *\|' $F)" -ge 10</automated>
  </verify>

  <acceptance_criteria>
    - `FINDINGS-UX.md` existe com as 6 seções na ordem especificada
    - `## Auditoria dos 10 princípios` tem exatamente 10 linhas de tabela, uma por
      princípio, cada uma com veredito ∈ {conforme, parcial, violado, não verificável}
      e uma coluna Evidência não vazia
    - `## Método de verificação` declara o nível da escada alcançado e as limitações
    - `git status --porcelain server web web-admin` vazio
  </acceptance_criteria>

  <done>Os 10 princípios estão auditados um a um contra tela/código real, com veredito e evidência, e o método está declarado.</done>
</task>

<task type="auto">
  <name>Task 2: Achados UX-01 (estados completos e transparência de dado) e UX-02 (consistência Estudo × Operador)</name>

  <files>.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-UX.md</files>

  <read_first>
    - `.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-UX.md` (tabela dos
      10 princípios é a base)
    - `CLAUDE.md` — princípios 1 (saldo fictício visível), 3 (fonte + horário + se é
      tempo real/atrasado/histórico), 4 (fonte falhou → não inventar), 9 (estados completos)
    - `docs/adr/001-fonte-de-dados-intraday.md` e `docs/adr/008-fonte-de-cotacoes-selecionavel.md`
      — o que o produto se comprometeu a exibir sobre origem e atraso do dado
    - `server/app/candle_provider.py` linhas 40-145 — o modo de falha silenciosa real de
      31/07/2026 (HTTP 200 com zero velas) e o `snapshot()`/`alerta`
    - `web/src/copy.js`, `web/src/disclaimers.js`
    - `web/src/App.jsx` — só as faixas achadas por
      `grep -n "carregando\|Carregando\|vazio\|erro\|mercado fechado\|atrasad\|rejeitad\|parcial\|fonte\|atualizad" web/src/App.jsx`
  </read_first>

  <action>
**UX-01 — matriz de estados.** Montar em `## Achados` uma tabela
`| Estado (CLAUDE.md princípio 9) | Ativo | Carteira | Operador IA | Perfil |` com
**8 linhas**: carregamento, vazio, erro, mercado fechado, dado atrasado, ordem
rejeitada, ordem parcialmente executada, operação concluída. Cada célula recebe
`OK (arquivo:linha)`, `AUSENTE`, ou `PARCIAL — {o quê falta}`.

Provocar os estados de verdade sempre que possível, não só ler o código:
  - **vazio** — conta recém-criada (`auditoria-ux@local.test`) antes de qualquer compra
  - **erro / fonte indisponível** — `GET /api/quotes?tickers=TICKERINEXISTENTE` e
    `POST /api/buy` de um ticker sem cotação (deve dar 502 "Sem cotacao para ..."):
    a tela mostra o erro real ou inventa/zera o número? Princípio 4 é inegociável
  - **mercado fechado / dado atrasado** — `GET /api/quotes` e `GET /api/timing/{ticker}`
    fora do pregão: o carimbo de horário e a ressalva de atraso aparecem na tela?
  - **ordem rejeitada** — `POST /api/buy` com quantidade que estoura o caixa virtual
  - **operação concluída** — compra válida e depois `POST /api/sell`
  - **ordem parcialmente executada** — se o modelo de dados não suporta execução
    parcial, isso é o achado (o CLAUDE.md exige o estado): registrar com evidência de
    que não existe caminho para ele, não como "não verificado"

Cobrir também, com achado ou linha em `## Verificado e conforme`:
  - saldo fictício **sempre visível** (princípio 1) — em quais telas aparece, em quais some
  - fonte + horário da última atualização + natureza do dado (tempo real / atrasado /
    histórico) exibidos junto do número (princípio 3)

**UX-02 — consistência visual e hierarquia Estudo × Operador.** Responder com evidência:
o usuário sabe, olhando a tela, em qual modo está? Onde essa indicação aparece e onde
some? A troca de modo muda tema/vocabulário de forma coerente (`COPY.estudo` vs
`COPY.operador`, Brand Book v2 — tema × modo são eixos independentes)? Há tela em que os
dois modos são indistinguíveis? Comparar capturas/estados equivalentes nos dois `appMode`.
  </action>

  <verify>
    <automated>F=.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-UX.md; grep -qE '^- \*\*Requisito:\*\* UX-01' $F && grep -qE '^- \*\*Requisito:\*\* UX-02' $F && test "$(grep -cEi '^\| *(carregamento|vazio|erro|mercado fechado|dado atrasado|ordem rejeitada|ordem parcial|operação concluída)' $F)" -ge 8</automated>
  </verify>

  <acceptance_criteria>
    - Matriz de estados tem 8 linhas × 4 telas, sem célula vazia (cada célula é
      `OK (arquivo:linha)`, `AUSENTE` ou `PARCIAL — ...`)
    - Pelo menos os estados "vazio", "erro/fonte indisponível" e "ordem rejeitada" foram
      provocados de verdade via API real, não só lidos no código — e isso está registrado
      na coluna `**Verificação:**` dos achados correspondentes
    - Princípios 1, 3 e 4 têm veredito explícito com evidência
    - UX-01 e UX-02 aparecem cada um em ≥ 1 achado ou entrada de `## Verificado e conforme`
    - Todo achado tem os 6 campos do formato com rótulos exatos e severidade citando D-0X
  </acceptance_criteria>

  <done>A matriz de estados está completa e provocada na prática onde possível; a consistência Estudo × Operador tem veredito com evidência.</done>
</task>

<task type="auto">
  <name>Task 3: Achados UX-03 (responsivo + acessibilidade) e UX-04 (copy) + tabela de cobertura</name>

  <files>.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-UX.md</files>

  <read_first>
    - `.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-UX.md`
    - `CLAUDE.md` — princípio 10 (acessibilidade, linguagem clara, responsividade mobile)
      e a proibição de linguagem de enriquecimento rápido / promessa / garantia de lucro
    - `web/src/copy.js`, `web/src/disclaimers.js`, `web/src/catalog.js`
    - `server/app/skill_ref.py`, `server/app/conceitos.py` (textos servidos pelo backend)
    - `.planning/codebase/CONCERNS.md` — item "Gate 'Executar' mudo em toque"
      (`web/src/App.jsx:3596-3610`, `:3745-3749`): este plano registra a **faceta de
      acessibilidade/UX** (controle desabilitado sem feedback ao toque, dependência de
      `title` que não existe em touch); a faceta de dívida técnica/blast radius pertence
      ao plano 01-03 (CODE-03)
    - `web/src/App.jsx` — faixas achadas por `grep -n "aria-\|role=\|alt=\|tabIndex\|@media\|minWidth\|maxWidth" web/src/App.jsx`
  </read_first>

  <action>
**UX-03 — responsivo e acessibilidade básica nas 4 telas principais (Ativo, Operador IA,
Carteira, Perfil).** Verificar, com evidência:
  - **Responsivo:** renderizar em viewport móvel (se Nível 1/2 disponível, redimensionar
    para ~390×844; senão, auditar as regras de layout no código e declarar o método).
    Procurar: overflow horizontal, texto cortado, tabela que não cabe, modal maior que a
    tela, botão fora da área alcançável pelo polegar.
  - **Alvo de toque:** controles com área menor que ~44×44pt — o app roda em WKWebView
    no iPhone, onde não existe hover.
  - **Leitura de tela / semântica:** presença de `aria-label`/`role`/`alt` em controles
    que só comunicam por ícone ou cor; `grep -c` em `App.jsx` dá a ordem de grandeza,
    mas o achado precisa apontar controles específicos com linha.
  - **Contraste:** conferir os pares de cor do Brand Book v2 (tema claro E escuro) nos
    textos de menor tamanho e nos estados negativos (prejuízo, erro, alerta). Se não
    houver como medir contraste ao vivo, calcular a razão a partir dos hex do tema e
    declarar o método.
  - **Feedback ao toque em controle desabilitado** — o caso do gate "Executar":
    registrar aqui a faceta de acessibilidade e marcar
    `**Possível duplicata:** CODE-03` na linha de Impacto, para o plano 01-06 deduplicar.

**UX-04 — copy contra as proibições do CLAUDE.md.** Varrer TODO texto visível ao
usuário (front: `copy.js`, `disclaimers.js`, `catalog.js`, strings literais em
`App.jsx`; backend: `skill_ref.py`, `conceitos.py`, `kb.py`, `mercado_ref.py`,
`defaults.py`) procurando linguagem de enriquecimento rápido, promessa de acerto,
garantia de lucro, certeza sobre resultado futuro, ou minimização de risco. Comandos de
partida (ajustar/ampliar conforme achar):

```bash
grep -rniE "garant|lucro certo|com certeza|100%|sempre (ganha|acerta)|enriquec|dinheiro rápido|infalível|nunca (perde|erra)|assegur|promet" \
  web/src/copy.js web/src/disclaimers.js web/src/catalog.js web/src/App.jsx \
  server/app/skill_ref.py server/app/conceitos.py server/app/kb.py \
  server/app/mercado_ref.py server/app/defaults.py
```

Cada ocorrência precisa de julgamento, não de contagem: "100%" numa fórmula de
percentual de carteira não é violação; "100% de chance" é. Registrar tanto as violações
quanto os falsos positivos descartados (estes vão para `## Verificado e conforme`).
Verificar também o lado positivo: a frase "Não há dados suficientes para concluir"
(ou equivalente canônico) existe e é usada? Onde?

**Fechar o arquivo** com `## Cobertura de requisitos`: tabela `| Requisito | Achados |
Status |` com exatamente 4 linhas (UX-01, UX-02, UX-03, UX-04), status ∈
{`com achados`, `conforme`}. E preencher `## Verificado e conforme`.
  </action>

  <verify>
    <automated>F=.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-UX.md; grep -q '^## Cobertura de requisitos' $F && for r in UX-01 UX-02 UX-03 UX-04; do grep -q "$r" $F || exit 1; done && test "$(grep -cE '^### F-UX-[0-9]+' $F)" -ge 1 && grep -q '^## Verificado e conforme' $F</automated>
  </verify>

  <acceptance_criteria>
    - UX-03 cobre as 4 telas principais (Ativo, Operador IA, Carteira, Perfil) nos 4
      eixos (responsivo, alvo de toque, semântica/leitura de tela, contraste), cada um
      com veredito e método declarado
    - UX-04 lista as ocorrências encontradas pelo grep com julgamento individual
      (violação vs. falso positivo), e verifica se a frase canônica de dado insuficiente
      existe e onde
    - `## Cobertura de requisitos` tem 4 linhas (UX-01..04), todas com status
    - `## Verificado e conforme` não está vazia
    - `git status --porcelain server web web-admin` vazio
  </acceptance_criteria>

  <done>Os 4 requisitos UX estão cobertos com achados evidenciados e classificados; nenhum princípio ficou sem veredito.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhuma nova | Fase read-only: não altera código de produção, não adiciona rota, não é deployada. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | FINDINGS-UX.md | mitigate | Screenshots e trechos colados não podem conter token de sessão, e-mail real ou chave de API; citar só o nome da variável de ambiente |
| T-01-05 | Tampering | Código de produção | mitigate | Read-only: acceptance criteria exige `git status --porcelain server web web-admin` vazio |
| T-01-06 | Denial of Service | Backend local :8787 compartilhado | accept | Dev local; mitigado por não usar `free_port`/`--stop` e por contas isoladas por escopo |
| T-01-SC | Tampering | npm/pip installs | accept | Nenhum pacote novo instalado (`web/node_modules` e `server/.venv` já existem no worktree) |
</threat_model>

<verification>
- `FINDINGS-UX.md` existe com as 6 seções na ordem especificada
- Tabela dos 10 princípios completa, sem veredito vazio
- Todo achado segue `### F-UX-NN` com os 6 campos de rótulo exato e severidade D-0X
- `git status --porcelain server web web-admin` vazio
</verification>

<success_criteria>
- UX-01..UX-04 na tabela de cobertura com status explícito
- Os 10 princípios com veredito individual e evidência
- Método de verificação ao vivo declarado com limitações
- Nenhuma correção implementada
</success_criteria>

<output>
Create `.planning/phases/01-auditoria-diagn-stica-consolidada/01-02-SUMMARY.md` when done
</output>
