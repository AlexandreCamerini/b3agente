---
phase: 05-corre-o-m-dio-c-digo-gate-admin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/tests/test_fase5_rejeicao_rotas.py
  - server/tests/test_fase5_recompra_reponderacao.py
autonomous: true
requirements: [FIX-C25, FIX-C26]

must_haves:
  truths:
    - "Cada caminho de rejeição de /api/buy e /api/sell tem asserção em nível HTTP em algum arquivo de server/tests/"
    - "A sequência compra → venda parcial → recompra tem teste que trava o preço médio reponderado"
    - "A normalização de lote de 100 na venda parcial está travada por teste (pedir 40 não vende 40)"
  artifacts:
    - path: "server/tests/test_fase5_rejeicao_rotas.py"
      provides: "Guardiões TestClient dos caminhos de rejeição ainda descobertos + mapa de onde cada caminho é coberto"
      contains: "TestClient"
    - path: "server/tests/test_fase5_recompra_reponderacao.py"
      provides: "Guardião da reponderação de preço médio na recompra após venda parcial"
      contains: "def test_"
  key_links:
    - from: "server/tests/test_fase5_rejeicao_rotas.py"
      to: "server/app/main.py (/api/buy, /api/sell)"
      via: "TestClient(main.app).post"
      pattern: "post\\(\"/api/(buy|sell)\""
    - from: "server/tests/test_fase5_recompra_reponderacao.py"
      to: "server/app/store.py (buy/sell)"
      via: "chamada direta ao motor determinístico"
      pattern: "store\\.(buy|sell)\\("
---

<objective>
Fechar FIX-C25 e FIX-C26: os caminhos de rejeição de ordem em `/api/buy` e
`/api/sell` passam a ter teste de rota HTTP dedicado, e a recompra após venda
parcial ganha o teste de reponderação de preço médio que nunca existiu.

Purpose: são os dois pontos de dinheiro (mesmo virtual) sem rede de proteção
em nível de rota — regressão silenciosa aqui muda saldo e preço médio do
usuário sem nenhum teste gritar.
Output: 2 arquivos novos em `server/tests/`. **Zero mudança em código de
produção** — se algum teste exigir mudar `main.py`/`store.py` para passar, é
achado a reportar no SUMMARY, não a "consertar" aqui.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-CONTEXT.md
@CLAUDE.md
</context>

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código em 2026-08-23. -->

Rejeições de `POST /api/buy` (server/app/main.py:1800-1880), na ordem em que
o código as avalia:
- `len(_normalize_ticker(t)) < 4` → `HTTPException(400, "Ticker invalido.")`
- cotação ausente/`price is None` → `HTTPException(502, "Sem cotacao para " + t)`
- em pregão, `qty_normalizada * price > cash` → `HTTPException(400, "Caixa insuficiente.")`
- fora do pregão, `pending_orders.CaixaInsuficiente` → `HTTPException(400, str(e))`
- fora do pregão e `scope is None` → `HTTPException(401, ...)`

Rejeições de `POST /api/sell` (server/app/main.py:1881-1925). **Atenção: não
existe "Ticker invalido" em `/api/sell`** — sem posição é a primeira barreira:
- sem posição no ticker → `HTTPException(400, "Sem posicao em " + t)`
- cotação ausente → `HTTPException(502, "Sem cotacao para " + t)`
- `qty` não-inteiro → `HTTPException(400, "Quantidade inválida.")`
- `qty <= 0` → `HTTPException(400, "Quantidade inválida.")`

Toda rejeição de escopo LOGADO grava histórico via
`store.registrar_rejeicao(conn, tipo, t, qty, price, motivo, user_id=, origem=)`
(FIX-C02, Fase 4) — escopo anônimo NUNCA grava.

Normalização de lote (server/app/store.py):
- `buy`: `qty = max(100, round(qty / 100) * 100)`
- `sell`: `sold = min(pos["qty"], max(100, round(qty / 100) * 100))`
- reponderação em `buy` com posição existente (store.py:597-600):
  `existing["avg"] = round((existing["avg"] * existing["qty"] + price * qty) / total, 2)`
- `sell` parcial NÃO mexe em `avg` (só reduz `qty`)

Padrão de isolamento TestClient da casa (server/tests/test_fase3_gate_plano.py:38-70
e server/tests/test_rotas_fase4.py): fixture autouse que restaura
`sys.modules["app.main"]`, `B3_DB_PATH` em `tempfile.mkdtemp()`,
`sys.modules.pop("app.main")` + `importlib.import_module("app.main")`,
`TestClient(main.app)`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Guardião de rota dos caminhos de rejeição (FIX-C25)</name>
  <files>server/tests/test_fase5_rejeicao_rotas.py</files>
  <read_first>
    - server/app/main.py — ler APENAS os intervalos das duas rotas: linhas 1800-1880 (`/api/buy`) e 1881-1960 (`/api/sell`), mais a definição de `_normalize_ticker` (grep pelo nome para achar a linha)
    - server/tests/test_rotas_fase4.py — seção "FIX-C02" (linhas ~254-378): padrão `_client_isolado`, `_registrar`, `_quote_fake`, patch de `pregao_mod.in_market_hours`; é a cobertura JÁ EXISTENTE de buy/caixa e buy/sem-cotação
    - server/tests/test_ordens_pendentes_rotas.py — linhas 1-120 (docstring + `_client`) e ~255-275 (cobertura existente de venda sem posição)
    - server/tests/test_fase3_get_quote_erro_limpo.py — linhas ~115-145 (cobertura existente de buy/502)
  </read_first>
  <action>
    Antes de escrever qualquer teste, fazer o INVENTÁRIO de cobertura: para
    cada um dos 7 caminhos de rejeição listados no bloco `<interfaces>`,
    localizar (grep por `api/buy`, `api/sell`, `status_code == 400`,
    `status_code == 502` em `server/tests/`) se já existe asserção em nível
    HTTP. A auditoria que originou C-25 é de 2026-08-18, ANTES da Fase 4 —
    parte da lacuna já foi fechada por `test_rotas_fase4.py` (FIX-C02). NÃO
    duplicar teste existente.

    Criar `server/tests/test_fase5_rejeicao_rotas.py` com docstring de
    cabeçalho contendo uma TABELA "caminho de rejeição → arquivo que cobre",
    com as 7 linhas do inventário (as já cobertas apontando para o arquivo
    existente, as novas apontando para este). Essa tabela é o entregável de
    documentação de C-25 — é o que impede a próxima auditoria de reabrir o
    achado sem saber onde olhar.

    Implementar neste arquivo APENAS os caminhos que o inventário mostrar
    descobertos. Pela leitura de 2026-08-23, os descobertos são:
      (a) `/api/buy` com ticker curto (ex.: body `{"t": "AB", "qty": 100}`)
          → 400 e `detail == "Ticker invalido."`;
      (b) `/api/sell` com cotação indisponível → 502 e
          `detail == "Sem cotacao para PETR4"` (exige posição comprada antes;
          montar a posição com a cotação boa e só então trocar o
          `candle_provider.get_quote` por um que devolve `None`);
      (c) `/api/sell` com `qty` não-inteiro (`{"qty": "abc"}`) → 400 e
          `detail == "Quantidade inválida."`;
      (d) `/api/sell` com `qty` zero (`{"qty": 0}`) → 400 e
          `detail == "Quantidade inválida."` — este caso tem regressão
          conhecida (F10-20260819: `0` falsy virava venda TOTAL silenciosa),
          citar isso no docstring do teste.
    Confirmar o inventário antes: se algum destes já estiver coberto, pular e
    registrar na tabela; se algum dos que parecem cobertos NÃO estiver, incluir.

    Em cada teste novo de conta LOGADA, assertar também o efeito colateral de
    FIX-C02: `GET /api/state` traz `history[0]["status"] == "rejeitada"` com
    `price` coerente (`None` quando não houve cotação — nunca `0`/`0.0`, regra
    CLAUDE.md item 4). Incluir um teste de escopo ANÔNIMO em pelo menos um dos
    caminhos novos, assertando `history == []` (balde compartilhado nunca grava).

    Isolamento: reusar verbatim o padrão de `test_rotas_fase4.py`
    (`_client_isolado`, fixture autouse restaurando `sys.modules["app.main"]`,
    `B3_DB_PATH` temporário). Fixar `pregao_mod.in_market_hours` com
    `monkeypatch.setattr(..., lambda now=None: True)` em todo teste cujo
    caminho dependa do ramo em-pregão — nunca deixar o resultado depender da
    hora real em que a suíte roda.

    Não tocar em `server/app/`. Se um teste falhar por comportamento do
    produto (e não por erro do teste), parar e registrar como achado no
    SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/bolsia/b3-agente && bash scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/test.sh` termina com exit 0 e sem nenhum teste novo em xfail/skip
    - `grep -c "def test_" server/tests/test_fase5_rejeicao_rotas.py` retorna >= 3
    - O docstring do arquivo contém a tabela de inventário com as 7 linhas de caminho de rejeição, cada uma nomeando um arquivo de teste existente
    - `grep -n 'in_market_hours' server/tests/test_fase5_rejeicao_rotas.py` mostra patch explícito em todo teste que depende do ramo em-pregão
    - `git diff --name-only` NÃO lista nenhum arquivo sob `server/app/`
  </acceptance_criteria>
  <done>Os caminhos de rejeição descobertos de `/api/buy` e `/api/sell` têm asserção HTTP, e a tabela de inventário no docstring aponta onde cada um dos 7 caminhos é coberto.</done>
</task>

<task type="auto">
  <name>Task 2: Guardião da reponderação na recompra após venda parcial (FIX-C26)</name>
  <files>server/tests/test_fase5_recompra_reponderacao.py</files>
  <read_first>
    - server/app/store.py — `def buy` (linhas 590-624) e `def sell` (linhas 626-700), com atenção à normalização de lote e à linha 599 (fórmula da reponderação)
    - server/tests/test_fase2_portfolio.py — teste existente de venda parcial isolada (padrão de setup de banco/`db.connect`/`store.ensure_defaults` a reusar)
  </read_first>
  <action>
    Criar `server/tests/test_fase5_recompra_reponderacao.py` exercitando o
    motor determinístico direto (`store.buy`/`store.sell`, sem TestClient — a
    conta é do motor, não da rota), num banco temporário no mesmo padrão de
    `test_fase2_portfolio.py`.

    ATENÇÃO — corrigir o exemplo do 05-CONTEXT.md: ele propõe
    `buy(100@30) → sell(qty=40) → buy(60@40)`, que NÃO funciona no motor real
    porque `sell` e `buy` normalizam para lotes de 100 (`max(100, round(qty/100)*100)`):
    `qty=40` viraria 100 (venda TOTAL de uma posição de 100) e `qty=60` viraria
    100. O 05-CONTEXT.md já manda validar a conta contra o `store.py` real antes
    de fixar o assert — a sequência correta, com a MESMA aritmética, é ×10:

      1. caixa suficiente antes (gravar `cash` alto o bastante via `db.kv_set`
         ou `store` — 10.000 do default não paga 1.000 ações a 30);
      2. `store.buy(qty=1000, price=30.0)` → posição `qty=1000`, `avg=30.00`;
      3. `store.sell(qty=400, price=<qualquer>)` → parcial: `qty=600`,
         `avg` INALTERADO em `30.00`;
      4. `store.buy(qty=600, price=40.0)` → `qty=1200` e
         `avg == 35.00` — (30×600 + 40×600) / 1200.

    Assertar `avg == 35.00` E assertar explicitamente `avg != 33.75`, com
    comentário dizendo que 33,75 é o resultado ERRADO que sairia se a
    reponderação usasse as 1.000 cotas originais em vez das 600 remanescentes
    (é essa a regressão que o teste existe para pegar).

    Assertar também, no mesmo arquivo:
      • o PnL realizado da venda parcial é proporcional às 400 cotas vendidas
        (`(preco_venda - 30.00) * 400`, arredondado a 2 casas), e o `cash`
        final bate com a soma das 3 operações;
      • `history` tem 3 entradas, todas com `status == "executada"`;
      • a normalização de lote: `store.sell(qty=40)` sobre uma posição de 100
        vende 100 (venda total), não 40 — teste separado, com comentário de
        que é essa regra que invalida o exemplo original do achado.

    Não tocar em `server/app/`.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/bolsia/b3-agente && bash scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/test.sh` termina com exit 0
    - `grep -n "35.0\|33.75" server/tests/test_fase5_recompra_reponderacao.py` mostra AS DUAS asserções (valor certo e valor errado)
    - `grep -c "def test_" server/tests/test_fase5_recompra_reponderacao.py` retorna >= 3
    - `git diff --name-only` NÃO lista nenhum arquivo sob `server/app/`
  </acceptance_criteria>
  <done>A sequência compra → venda parcial → recompra está travada com o preço médio reponderado correto (35,00), com o valor errado (33,75) explicitamente rejeitado, e a regra de lote de 100 documentada em teste.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente HTTP → `/api/buy` e `/api/sell` | corpo JSON não confiável (`t`, `qty`) atravessa aqui e move saldo/posição |
| suíte de teste → banco real | um teste sem `B3_DB_PATH` temporário escreveria no banco de desenvolvimento |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `/api/buy` com `qty`/`t` fora do contrato | mitigate | Task 1 trava em nível HTTP que ticker curto é 400 antes de qualquer escrita de estado |
| T-05-02 | Tampering | `/api/sell` com `qty` não-inteiro ou `0` | mitigate | Task 1 trava os dois ramos de "Quantidade inválida", incluindo a regressão conhecida de `0` falsy virar venda total |
| T-05-03 | Information disclosure | histórico de rejeição no balde anônimo compartilhado | mitigate | Task 1 assere `history == []` para escopo anônimo em pelo menos um caminho novo |
| T-05-04 | Tampering | reponderação de preço médio em recompra | mitigate | Task 2 trava `avg == 35.00` e rejeita `33.75` explicitamente |
| T-05-05 | Denial of service | teste escrevendo no banco real da máquina | mitigate | `B3_DB_PATH` em `tempfile.mkdtemp()` + restauração de `sys.modules["app.main"]`, padrão já usado na suíte |
| T-05-SC | Tampering | instalação de pacote (npm/pip/cargo) | accept | Nenhum pacote novo é instalado neste plano — só arquivos de teste usando `pytest`/`fastapi.testclient` já presentes em `server/requirements.txt` |
</threat_model>

<verification>
- `bash scripts/test.sh` (suíte backend) passa com os 2 arquivos novos
- Nenhuma linha de `server/app/` no diff
- A tabela de inventário do Task 1 cobre os 7 caminhos de rejeição
</verification>

<success_criteria>
- FIX-C25: cada caminho de rejeição de `/api/buy`/`/api/sell` tem asserção HTTP rastreável, e a tabela de inventário diz onde
- FIX-C26: recompra após venda parcial trava `avg == 35.00`, rejeita `33.75`, e a regra de lote de 100 está documentada em teste
- Zero mudança em código de produção
</success_criteria>

<output>
Create `.planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-01-SUMMARY.md` when done
</output>
