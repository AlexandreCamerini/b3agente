---
phase: 05-corre-o-m-dio-c-digo-gate-admin
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/catalog.js
  - web/src/persistence.js
  - server/tests/test_auditoria_prompts.py
  - web/tests/test_fase5_skill_migracao_legado.mjs
autonomous: true
requirements: [FIX-C22]

must_haves:
  truths:
    - "O texto de skill padrão do Modo Estudo é byte-idêntico entre servidor e app nativo"
    - "O texto de skill padrão do Modo Operador é byte-idêntico entre servidor e app nativo"
    - "Um guardião falha se qualquer um dos dois divergir por um único byte"
    - "Aparelho que já tem o texto legado gravado sobe para o canônico; texto EDITADO pelo usuário é intocável"
  artifacts:
    - path: "web/src/catalog.js"
      provides: "Literais byte-exatos das duas skills + lista de textos legados para migração"
      contains: "defaultSkillText"
    - path: "server/tests/test_auditoria_prompts.py"
      provides: "Guardião de paridade byte-exata estendido para o par de skills"
      contains: "default_skill_text"
    - path: "web/tests/test_fase5_skill_migracao_legado.mjs"
      provides: "Guardião da migração no aparelho (legado sobe, edição fica)"
  key_links:
    - from: "server/tests/test_auditoria_prompts.py"
      to: "web/src/catalog.js"
      via: "leitura do código-fonte + comparação byte a byte com defaults.default_skill_text()"
      pattern: "default_skill_text\\(\\)"
    - from: "web/src/persistence.js (ensureShape)"
      to: "web/src/catalog.js (LEGACY_SKILL_TEXTS)"
      via: "upgrade de default legado no doc do aparelho"
      pattern: "LEGACY_SKILL_TEXTS"
---

<objective>
Fechar FIX-C22: o par `default_skill_text()` (servidor) /
`defaultSkillText()` (app nativo) ganha guardião de paridade byte-exata, no
mesmo padrão do par `carteiraStopAlvo*` que já é travado.

Purpose: a divergência NÃO é hipotética — a verificação de 2026-08-23 mostra
que os dois textos já divergiram por completo. O servidor compõe da fonte
canônica (`skill_ref.PERSONA_BASE` + `PRINCIPIOS` + vocabulário de decisão +
contrato de saída); o `catalog.js` carrega um texto de geração anterior, sem
acentos, sem os 11 princípios e SEM o contrato de saída. Como o `deviceStore`
manda `doc.skill` no corpo da requisição, o iPhone opera hoje com uma persona
diferente da web. Um guardião sozinho não fecha o achado — ele só passaria a
gritar. Este plano reconcilia primeiro, depois trava.
Output: `catalog.js` com os dois literais canônicos + migração de legado no
aparelho; guardião Python estendido; guardião `.mjs` da migração.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-CONTEXT.md
@CLAUDE.md
@.claude/skills/didatica-boris/SKILL.md
</context>

<interfaces>
<!-- Estado verificado em 2026-08-23. -->

Servidor (`server/app/defaults.py`):
- `default_skill_text()` (linha 22) = `"\n".join([...])` compondo
  `skill_ref.PERSONA_BASE`, `skill_ref.PRINCIPIOS`,
  `skill_ref.decisoes_txt("educacional")` e `_CONTRATO_SAIDA`
- `default_skill_text_operador()` (linha 43) = mesma composição, com a função
  de mesa e `decisoes_txt("operador")`

App nativo (`web/src/catalog.js`):
- `defaultSkillTextOperador()` (linha 35) e `defaultSkillText()` (linha 58)
  devolvem `[...].join("\n")` de um texto de geração ANTERIOR — sem acentos,
  sem os 11 princípios, sem `# Contrato de saída`

Guardião a estender (`server/tests/test_auditoria_prompts.py:255`):
```python
def test_a8ii_paridade_defaults_carteira_com_catalog_js():
    # lê web/src/catalog.js como TEXTO e compara, byte a byte, o conteúdo do
    # template literal `chave: \`...\`` com defaults.default_llm_prompts()[chave]
    m = re.search(chave + r":\s*`([^`]*)`", src)
    assert m.group(1) == prompts[chave]
```

Consumo do texto no aparelho (`web/src/persistence.js`):
- linha ~285-315 `ensureShape`: garante a forma do doc e já faz backfill de
  `doc.skillOperador` quando ausente (`defaultSkillTextOperador()`)
- linha ~643-644 `restore`: `doc.skill.text = defaultSkillText()`
- linha ~734: `const skillAtiva = doc.config.appMode === "operador" ? doc.skillOperador : doc.skill;`
  → vai no corpo de `api.analyzeTechnical`, ou seja, o texto do aparelho é o
  que a LLM recebe

Precedente de migração de default legado (servidor, `server/app/store.py:33`):
`_eh_default_antigo(conn, chave, texto)` compara o sha256 do texto com uma
lista de gerações anteriores conhecidas (`defaults.LEGACY_PROMPT_SHA256`);
default antigo SOBE, texto editado pelo usuário é INTOCÁVEL. É esse contrato
que o aparelho precisa espelhar.

Guardiões que tocam esses textos e podem reagir à mudança (rodar todos):
`web/tests/test_copy_theme.mjs`, `web/tests/test_didatica_parity.mjs`,
`web/tests/test_rr_min_fonte_unica.mjs`, `web/tests/test_api_parity.mjs`,
`web/tests/test_fase3_paridade_stores_generica.mjs`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Reconciliar os literais de skill em catalog.js com o servidor</name>
  <files>web/src/catalog.js</files>
  <read_first>
    - server/app/defaults.py — linhas 1-63 (`_CONTRATO_SAIDA`, `default_skill_text`, `default_skill_text_operador`)
    - server/app/skill_ref.py — `PERSONA_BASE` (linha 16), `RR_MIN`/`RR_MIN_TXT` (linhas 30-34), `PRINCIPIOS` (linha 38), `decisoes_txt` (linha 365) e o dict `vocab`
    - web/src/catalog.js — linhas 1-100 (os dois `defaultSkillText*` e o comentário de paridade do `carteiraStopAlvo`)
  </read_first>
  <action>
    Gerar o texto canônico REAL executando o servidor, não transcrevendo à mão:
    rodar `python -c` a partir de `server/` imprimindo
    `defaults.default_skill_text()` e `defaults.default_skill_text_operador()`
    (usar o mesmo interpretador que `scripts/test.sh` resolve — o `.venv` do
    clone principal). Copiar a saída byte a byte.

    Reescrever `web/src/catalog.js` para expor os dois textos como CONSTANTES
    de template literal, no mesmo formato que o guardião de `carteiraStopAlvo`
    já sabe casar (`NOME: \`...\`` não serve aqui porque são funções — usar
    `const SKILL_TEXT_ESTUDO = \`...\`;` / `const SKILL_TEXT_OPERADOR = \`...\`;`
    e manter `defaultSkillText()` / `defaultSkillTextOperador()` como funções
    que só devolvem a constante). Preservar as assinaturas exportadas —
    `persistence.js` e os testes chamam as funções, não as constantes.

    Regras rígidas do literal (é isto que o guardião vai comparar):
      • bytes idênticos ao stdout do Python, INCLUSIVE acentuação, `⇒`, `≥`,
        `—`, e a quebra de linha final (ou ausência dela);
      • ZERO interpolação `${...}` — o comentário existente no arquivo
        (linhas 30-34 e 85-91) já explica que interpolar quebra a paridade
        byte-a-byte; o mesmo vale aqui;
      • nenhum backtick dentro do texto (conferir; se houver, escapar e
        registrar no SUMMARY, porque isso obriga a ajustar a regex do guardião).

    Atualizar o comentário de cabeçalho das duas constantes explicando: o
    texto é ESPELHO de `server/app/defaults.py`, travado byte a byte por
    `test_a8ii_paridade_defaults_skill_com_catalog_js`, e a fonte de verdade
    é o servidor (mudou lá, muda aqui). Citar a data e FIX-C22.

    Acrescentar, no mesmo arquivo, `export const LEGACY_SKILL_TEXTS = [...]`
    contendo os DOIS textos que estão sendo substituídos agora (o
    `defaultSkillText()` e o `defaultSkillTextOperador()` de hoje, copiados
    verbatim ANTES da edição) — é a lista que o Task 2 usa para migrar
    aparelhos. Comentar que a lista só CRESCE: geração futura acrescenta,
    nunca remove (mesmo contrato de `defaults.LEGACY_PROMPT_SHA256`).

    Rodar a suíte web inteira depois: a mudança de texto pode reagir com
    `test_copy_theme.mjs` (checa presença de substring), `test_didatica_parity.mjs`
    e `test_rr_min_fonte_unica.mjs` (checa que todo literal `\d,\d:1` bate com
    `RR_MIN_TXT` — o texto canônico traz "Mínimo 1,5:1", então deve passar).
    Se algum guardião existente falhar, ANALISAR antes de mexer nele: se a
    expectativa antiga descrevia o texto legado, atualizar o guardião COM NOTA
    datada explicando a reversão deliberada (guardrail CLAUDE.md); nunca
    apagar asserção.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/bolsia/b3-agente && bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx vite build` conclui sem erro (o literal não quebrou a sintaxe do módulo)
    - `grep -n "SKILL_TEXT_ESTUDO\|SKILL_TEXT_OPERADOR\|LEGACY_SKILL_TEXTS" web/src/catalog.js` casa nas 3
    - `grep -c '\${' web/src/catalog.js` não aumentou em relação ao valor anterior à edição (nenhuma interpolação nova)
    - Comparação byte a byte confirmada por um one-liner que importa `defaults` no Python e casa com o literal extraído do fonte (é exatamente o que o guardião do Task 2 automatiza)
    - `bash scripts/executar.sh --testes` exit 0
  </acceptance_criteria>
  <done>`catalog.js` carrega os dois textos de skill byte-idênticos ao servidor, sem interpolação, e guarda a lista de textos legados para migração.</done>
</task>

<task type="auto">
  <name>Task 2: Guardião de paridade byte-exata + migração do legado no aparelho</name>
  <files>server/tests/test_auditoria_prompts.py, web/src/persistence.js, web/tests/test_fase5_skill_migracao_legado.mjs</files>
  <read_first>
    - server/tests/test_auditoria_prompts.py — linhas 233-300 (o guardião `test_a8ii_paridade_defaults_carteira_com_catalog_js` e os testes de migração de default legado que o cercam)
    - web/src/persistence.js — linhas 280-350 (`ensureShape`, incluindo o backfill de `skillOperador` na linha ~313) e 630-650 (`restore`)
    - web/src/catalog.js — o resultado do Task 1
    - server/app/store.py — linhas 30-100 (`_eh_default_antigo` e como `ensure_defaults` decide subir um default antigo)
  </read_first>
  <action>
    (a) Guardião de paridade, em `server/tests/test_auditoria_prompts.py`:
    acrescentar `test_a8ii_paridade_defaults_skill_com_catalog_js`, imediatamente
    abaixo do guardião de carteira já existente, no MESMO padrão (ler
    `web/src/catalog.js` como texto, extrair o template literal por regex,
    comparar byte a byte). Comparar `SKILL_TEXT_ESTUDO` com
    `defaults.default_skill_text()` e `SKILL_TEXT_OPERADOR` com
    `defaults.default_skill_text_operador()`. Mensagem de assert nomeando o
    par divergente e dizendo que a fonte de verdade é `defaults.py`.
    Docstring citando FIX-C22 e o fato de que o texto do aparelho vai no corpo
    da requisição de análise — divergência aqui = persona diferente no iPhone.

    (b) Migração no aparelho, em `web/src/persistence.js`: dentro de
    `ensureShape` (onde o backfill de `skillOperador` já mora), acrescentar o
    upgrade de default legado — se `doc.skill.text` for byte-idêntico a
    QUALQUER entrada de `LEGACY_SKILL_TEXTS`, substituir por
    `defaultSkillText()`; idem para `doc.skillOperador.text` com
    `defaultSkillTextOperador()`. Texto que não casa com nenhum legado é
    edição do usuário e é INTOCÁVEL — mesma regra de `store._eh_default_antigo`.
    Comentar citando o precedente do servidor e FIX-C22.
    Confirmar que a mudança fica no `deviceStore`/`ensureShape` e NÃO exige
    método novo no `serverStore` (paridade de MÉTODO, guardrail CLAUDE.md, não
    é violada porque nenhum método público novo é criado — registrar isso
    explicitamente no SUMMARY).

    (c) Guardião da migração, `web/tests/test_fase5_skill_migracao_legado.mjs`,
    no padrão dos `.mjs` da casa (ver `web/tests/test_fase3_fonte_technicals.mjs`
    para o formato `ok(nome, cond)` + `process.exit(fails === 0 ? 0 : 1)`).
    Cobrir:
      • doc com `skill.text` legado ⇒ depois de `ensureShape`, vira o canônico;
      • doc com `skill.text` EDITADO (string arbitrária) ⇒ inalterado;
      • idem para `skillOperador`;
      • `LEGACY_SKILL_TEXTS` não contém o texto canônico atual (senão a
        migração reescreveria o default vigente em laço — é o mesmo sanity
        check que `test_hashes_legados_existem_e_nao_incluem_o_default_atual`
        faz no servidor).
    Se `ensureShape` não for exportável/exercitável isoladamente, usar
    inspeção estática de fonte (padrão já usado por vários `.mjs` do projeto)
    para travar a existência e a forma da migração, e declarar essa limitação
    no cabeçalho do teste.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/bolsia/b3-agente && bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/executar.sh --testes` exit 0 (as DUAS suítes)
    - `grep -n "test_a8ii_paridade_defaults_skill_com_catalog_js" server/tests/test_auditoria_prompts.py` casa
    - Prova negativa: alterar um único caractere de `SKILL_TEXT_ESTUDO` em `catalog.js` faz `bash scripts/test.sh` FALHAR; reverter e confirmar que volta a passar (executar essa prova e registrar no SUMMARY)
    - `grep -n "LEGACY_SKILL_TEXTS" web/src/persistence.js` casa dentro de `ensureShape`
    - `cd web && node tests/test_fase5_skill_migracao_legado.mjs` exit 0
    - `cd web && npx vite build` conclui sem erro
  </acceptance_criteria>
  <done>A divergência entre servidor e aparelho passa a ser impossível de introduzir sem um teste falhar, e os aparelhos que já carregam o texto legado sobem para o canônico sem tocar em edição do usuário.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app nativo → `/api/technical/analyze` | o aparelho MANDA o texto de skill no corpo; o servidor o usa como persona da LLM |
| doc local do aparelho → `ensureShape` | texto persistido em geração anterior atravessa aqui |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Tampering | persona divergente entre servidor e aparelho | mitigate | Guardião byte-exato no padrão do par `carteiraStopAlvo*`, com prova negativa executada |
| T-05-14 | Tampering | migração sobrescrevendo texto EDITADO pelo usuário | mitigate | Upgrade só quando o texto casa byte a byte com uma entrada de `LEGACY_SKILL_TEXTS`; guardião `.mjs` cobre o caso de edição |
| T-05-15 | Tampering | `LEGACY_SKILL_TEXTS` contendo o default vigente (laço de reescrita) | mitigate | Asserção dedicada no guardião `.mjs`, espelhando o sanity check já existente no servidor |
| T-05-16 | Repudiation | guardião existente apagado para "fazer passar" | mitigate | Regra do CLAUDE.md: reversão deliberada atualiza o guardião COM nota datada, nunca apaga |
| T-05-SC | Tampering | instalação de pacote (npm/pip/cargo) | accept | Nenhum pacote novo; os `.mjs` usam só `fs`/`path` do Node e o build usa o Vite já instalado |
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` passa (backend + web)
- `cd web && npx vite build` passa
- Prova negativa do guardião executada e registrada
</verification>

<success_criteria>
- FIX-C22: par `default_skill_text`/`defaultSkillText` (e a versão Operador) travado byte a byte, no padrão do par já protegido
- A divergência real encontrada foi reconciliada, não só detectada
- Aparelhos com texto legado migram; edição do usuário permanece intocável
</success_criteria>

<output>
Create `.planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-03-SUMMARY.md` when done
</output>
