---
phase: 05-corre-o-m-dio-c-digo-gate-admin
plan: 06
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - web-admin/src/App.jsx
  - web/tests/test_fase5_alerta_gasto_admin.mjs
  - web/tests/test_fase5_auditoria_perm.mjs
autonomous: true
requirements: [FIX-C38, FIX-C39]

must_haves:
  truths:
    - "A aba Custos mostra o estado do alerta preventivo de gasto de IA, com tom âmbar quando o limiar é cruzado"
    - "Alerta não configurado e alerta sem base de comparação aparecem como estados próprios — nunca como 'dentro do normal'"
    - "O admin configura limiar e janela no mesmo formulário auditado que já existe, sem permissão nova"
    - "A entrada de Auditoria na lista de abas declara sua regra de acesso explicitamente, sem estreitar o acesso real"
  artifacts:
    - path: "web-admin/src/App.jsx"
      provides: "Linha de alerta na aba Custos, 2 campos no formulário de IA, sentinela PERM_ANY na lista de abas"
      contains: "Alerta de gasto (IA gerenciada)"
    - path: "web/tests/test_fase5_alerta_gasto_admin.mjs"
      provides: "Guardião estático do painel e do formulário do portal admin"
    - path: "web/tests/test_fase5_auditoria_perm.mjs"
      provides: "Guardião de que a regra de acesso da aba Auditoria não estreitou"
  key_links:
    - from: "web-admin/src/App.jsx (Custos)"
      to: "payload alertaGastoIA de /api/obs/usage"
      via: "leitura de data.alertaGastoIA numa linha Kv"
      pattern: "alertaGastoIA"
    - from: "web-admin/src/App.jsx (VIEWS)"
      to: "filtro de abas visíveis"
      via: "sentinela PERM_ANY tratada como 'qualquer permissão administrativa'"
      pattern: "PERM_ANY"
---

<objective>
Fechar a metade FRONT de FIX-C38 (a aba Custos passa a mostrar o alerta
preventivo de gasto de IA que o backend calcula, e o admin passa a configurar
limiar e janela) e FIX-C39 (a entrada "Auditoria" da lista de abas ganha
declaração explícita de regra de acesso).

Purpose: o hard stop de gasto de IA já existe e funciona (`metering.check`,
`cap_global`), mas só se manifesta cortando — o painel não avisa antes. C-39 é
consistência de modelo de dado: a aba Auditoria é a única das 10 sem `perm`
declarado, então a regra dela ("qualquer permissão administrativa") só existe
implicitamente, no `!v.perm ||` do filtro.
Output: 3 mudanças pequenas em `web-admin/src/App.jsx` + 2 guardiões estáticos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-CONTEXT.md
@.planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-UI-SPEC.md
@.planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-02-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
<!-- Estado verificado em 2026-08-23. `web-admin/` é projeto npm SEPARADO,
     sem framework de teste (package.json só tem dev/build/preview). -->

**Decisão de escopo de teste, tomada no planejamento**: NÃO se introduz
framework de teste no `web-admin/`. Já existe precedente na casa de guardião
`.mjs` que mora em `web/tests/` e faz inspeção ESTÁTICA do fonte de
`web-admin/src/App.jsx` — `web/tests/test_fase3_custos_falha_brapi.mjs`
(C-36), `test_admin_ui.mjs`, `test_fase3_admin_duracao.mjs`,
`test_fase3_admin_timing_watch.mjs`, `test_editor_texto_byte_exato.mjs`.
Esses arquivos rodam dentro da suíte canônica e usam só `fs`/`path`. Os
guardiões deste plano seguem esse padrão. Verificação visual do portal
continua sendo `npx vite build` + checkpoint humano (Plano 05-08).

Contrato vindo do Plano 05-02 (`_usage_snapshot()` → `/api/obs/usage`):
```
data.alertaGastoIA = {
  configurado: bool, avaliavel: bool, motivo: str|null,
  hoje: number, media: number|null, desvioPct: number|null,
  limiarPct: number|null, janelaDias: number|null, acima: bool
}
```
Campos novos de `/api/admin/config/ia`: `llmAlertaGastoPct`, `llmAlertaJanelaDias`.

`web-admin/src/App.jsx`:
- `Kv({ label, value, tone })` (linha ~24): `tone` aceita `"positive"`,
  `"negative"`, `"warn"`; qualquer outro valor cai em `T.text`
- `Custos()` (linha 139): `useFetch(() => api.obsUsage())` em `data`; card
  "Uso de IA" nas linhas 154-168; a última linha do card é o `map` de
  `data.tokens?.porModelo`
- Precedente de linha de alerta com limiar (linha ~191, card de orçamento
  brapi): `value={data.candles.alerta ? "⚠ acima do limiar de " + (...) + "%" : "dentro do normal"}`
- `MudancaDeLLM({ user })` (linha 627): `podeEditar` = permissão
  `llm.configurar`; helper `campo(chave, label, placeholder, numerico)`
  (linha 641); os 5 `campo(...)` existentes nas linhas 686-690; a função
  `salvar` converte `""`/`null` em `null` e números via `Number(...)`
- `VIEWS` (linhas 1198-1209): 9 entradas com `perm`, a de `auditoria` sem
- filtro (linha 1274): `const visiveis = VIEWS.filter((v) => !v.perm || perms.includes(v.perm));`
- backend da aba Auditoria já é gateado por `require_any_admin_permission()`
  — o filtro do front é conveniência, não controle de acesso

Contrato de cópia (05-UI-SPEC.md, aprovado):
- rótulo: `"Alerta de gasto (IA gerenciada)"`
- acima: `"⚠ gasto de hoje X% acima da média dos últimos N dias"` (tom `warn`)
- abaixo: `"dentro do normal"` (tom `positive`)
- sem limiar: `"não configurado"` (sem tom de alerta)
- campos: `"Limiar de alerta de gasto (%)"` / placeholder `"vazio = sem alerta preventivo"`;
  `"Janela de comparação (dias)"` / placeholder `"vazio = usa Ndias padrão"`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Linha de alerta na aba Custos + 2 campos no formulário de IA (FIX-C38)</name>
  <files>web-admin/src/App.jsx</files>
  <read_first>
    - web-admin/src/App.jsx — linhas 1-60 (token `T`, `Kv`, `Card`), 139-200 (componente `Custos`, incluindo o precedente de alerta com limiar na linha ~191) e 620-706 (`MudancaDeLLM`, helper `campo`, função `salvar`)
    - .planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-UI-SPEC.md — seção "FIX-C38", contratos de leitura e de escrita
    - .planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-02-SUMMARY.md — o formato exato do dict `alertaGastoIA` como foi implementado
  </read_first>
  <action>
    (a) Leitura — no card "Uso de IA" de `Custos()`, acrescentar UMA linha `Kv`
    logo depois do `EventoComSerie` de "Tokens/dia" (linha ~161). Mesmo card,
    mesmo ritmo visual, sem contêiner novo e sem card novo.
      • `label="Alerta de gasto (IA gerenciada)"`;
      • valor e tom derivados de `data.alertaGastoIA`, nesta ordem de teste:
        `!a || !a.configurado` ⇒ `"não configurado"`, tom `"faint"`;
        `!a.avaliavel` ⇒ `"sem base de comparação"` + o `motivo` entre
        parênteses quando houver, tom `"faint"`;
        `a.acima` ⇒ `"⚠ gasto de hoje " + a.desvioPct + "% acima da média dos últimos " + a.janelaDias + " dias"`, tom `"warn"`;
        senão ⇒ `"dentro do normal"`, tom `"positive"`.
      • NUNCA usar tom `"negative"` aqui: negativo fica reservado para o hard
        stop, que é sinal separado e já existente (`analisesGerenciadas` no
        teto). Alerta preventivo e hard stop são severidades diferentes e não
        se fundem — comentar isso no código.
      • O estado "não configurado" e o "sem base de comparação" NÃO podem cair
        no verde de `positive`: um alerta desligado ou sem histórico não é um
        "tudo certo" (CLAUDE.md item 4).

    (b) Para o tom `"faint"` acima, acrescentar um único termo ao ternário de
    cor de `Kv` (linha ~24): `tone === "faint" ? T.faint : ...`. É uma
    adição a uma expressão existente, não um componente novo. Não mudar mais
    nada em `Kv`.

    (c) Escrita — em `MudancaDeLLM`, acrescentar dois `campo(...)` logo depois
    do `campo("llmGlobalDailyCap", ...)` (linha ~690), com `numerico={true}`
    (teclado numérico no iPhone, requisito do ADR-014), reusando o mesmo
    `disabled={!podeEditar}` e o mesmo botão "Salvar (auditado)":
      • `campo("llmAlertaGastoPct", "Limiar de alerta de gasto (%)", "vazio = sem alerta preventivo", true)`
      • `campo("llmAlertaJanelaDias", "Janela de comparação (dias)", "vazio = usa Ndias padrão", true)`
    Incluir as duas chaves no laço numérico de `salvar` (o que hoje itera
    `["llmDailyQuota","llmRatePerMin","llmGlobalDailyCap"]`) para que campo
    vazio vire `null` explícito e limpe o override, igual aos outros.
    NENHUMA permissão nova — `llm.configurar` já gateia este card.

    Fora de escopo (05-CONTEXT.md): push/e-mail do alerta. Só indicador
    visual, mesmo precedente reduzido de C-37 na Fase 3.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/bolsia/b3-agente/web-admin && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web-admin && npx vite build` conclui sem erro
    - `grep -c "Alerta de gasto (IA gerenciada)" web-admin/src/App.jsx` retorna 1
    - `grep -n "não configurado\|sem base de comparação\|dentro do normal" web-admin/src/App.jsx` mostra os 3 estados na região do card "Uso de IA"
    - `grep -n 'tone === "faint"' web-admin/src/App.jsx` casa dentro de `Kv`
    - `grep -c "llmAlertaGastoPct" web-admin/src/App.jsx` retorna >= 2 (o `campo(...)` e o laço de `salvar`)
    - Nenhuma ocorrência de `"negative"` na expressão de tom da nova linha de alerta
    - `git diff web-admin/src/App.jsx` não introduz nenhuma permissão nova nem novo `Card`
  </acceptance_criteria>
  <done>A aba Custos mostra os quatro estados do alerta preventivo com os tons certos, e o admin configura limiar e janela no formulário auditado que já existia.</done>
</task>

<task type="auto">
  <name>Task 2: Regra de acesso explícita na aba Auditoria (FIX-C39)</name>
  <files>web-admin/src/App.jsx</files>
  <read_first>
    - web-admin/src/App.jsx — linhas 1190-1215 (array `VIEWS`) e 1265-1290 (o filtro `visiveis` e o render de `Cabecalho`)
    - .planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-UI-SPEC.md — seção "FIX-C39", que confirma que `perm` NUNCA é renderizado (o único consumidor é o filtro) e que o checker não deve esperar elemento visual novo
    - server/app/rbac.py — `require_any_admin_permission` (grep pelo nome), para confirmar que o backend já gateia a rota de auditoria independentemente do filtro do front
  </read_first>
  <action>
    Aplicar exatamente o contrato de 5 itens do UI-SPEC:

    1. Declarar, junto de `VIEWS`, a sentinela `const PERM_ANY = "*";` com o
       comentário de uma linha: nenhuma permissão específica é exigida —
       qualquer permissão administrativa basta, que é a regra de HOJE.
    2. Entrada de auditoria passa a
       `{ id: "auditoria", label: "Auditoria", C: Auditoria, perm: PERM_ANY }`.
    3. Filtro passa a tratar a sentinela como equivalente a "sem perm":
       `VIEWS.filter((v) => !v.perm || v.perm === PERM_ANY || perms.includes(v.perm))`.
    4. **NÃO** atribuir uma permissão real (ex.: `"observabilidade.ver"`) à
       aba Auditoria — isso seria mudança de regra de acesso, proibida
       explicitamente pela decisão de C-39 no 05-CONTEXT.md. O backend já
       gateia com `require_any_admin_permission()`; o filtro do front é
       conveniência.
    5. Nenhuma mudança visual, nenhuma cópia nova: `perm` não é renderizado
       em lugar nenhum do portal.

    A verificação é de REGRESSÃO, não de estado novo: a aba tem de continuar
    aparecendo para toda combinação de permissão administrativa em que
    aparecia antes.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/bolsia/b3-agente/web-admin && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web-admin && npx vite build` conclui sem erro
    - `grep -n 'const PERM_ANY = "\*"' web-admin/src/App.jsx` casa
    - `grep -n 'id: "auditoria"' web-admin/src/App.jsx` mostra `perm: PERM_ANY` na mesma linha
    - `grep -n 'v.perm === PERM_ANY' web-admin/src/App.jsx` casa no filtro `visiveis`
    - Nenhuma das 10 entradas de `VIEWS` mudou de `perm` além da de auditoria: `git diff web-admin/src/App.jsx` mostra alteração em UMA linha do array
    - A aba Auditoria não passou a exigir permissão real: `grep -n 'id: "auditoria"' web-admin/src/App.jsx` NÃO casa com `observabilidade.ver` nem com nenhuma outra string de permissão
  </acceptance_criteria>
  <done>A entrada de Auditoria declara sua regra de acesso como as outras 9, sem estreitar o acesso real e sem mudança visual.</done>
</task>

<task type="auto">
  <name>Task 3: Guardiões estáticos do portal admin</name>
  <files>web/tests/test_fase5_alerta_gasto_admin.mjs, web/tests/test_fase5_auditoria_perm.mjs</files>
  <read_first>
    - web/tests/test_fase3_custos_falha_brapi.mjs (arquivo inteiro) — o guardião de referência: mora em `web/tests/`, lê `web-admin/src/App.jsx` por `readFileSync`, roda sem build
    - web-admin/src/App.jsx — o resultado dos Tasks 1 e 2
    - .planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-UI-SPEC.md — os literais de cópia que devem aparecer 1× cada
  </read_first>
  <action>
    Criar dois guardiões estáticos em `web/tests/`, no formato de
    `test_fase3_custos_falha_brapi.mjs` (`readFileSync` do fonte do
    `web-admin/`, `ok(nome, cond)`, `process.exit(fails === 0 ? 0 : 1)`),
    para que rodem dentro da suíte canônica sem exigir infraestrutura nova no
    projeto admin. Documentar essa escolha no cabeçalho de cada arquivo,
    citando o precedente e o fato de que `web-admin/` não tem suíte própria.

    (a) `test_fase5_alerta_gasto_admin.mjs` — FIX-C38:
      • cada literal do Copywriting Contract aparece exatamente 1×:
        `"Alerta de gasto (IA gerenciada)"`, `"dentro do normal"` na região
        nova (atenção: a string já existe no card de brapi — assertar a
        CONTAGEM esperada considerando a ocorrência pré-existente, não
        assumir 1), `"não configurado"`, `"sem base de comparação"`,
        `"Limiar de alerta de gasto (%)"`, `"Janela de comparação (dias)"`;
      • `data.alertaGastoIA` é LIDO do payload;
      • os 4 estados existem e o `acima` usa tom `"warn"` — nunca `"negative"`
        (fundir alerta preventivo com hard stop é a regressão que este teste
        existe para pegar);
      • `Kv` ganhou o tom `"faint"`;
      • os dois campos novos usam `numerico` e entram no laço numérico de
        `salvar`;
      • nenhuma permissão nova aparece no diff do card (o guardião assere que
        `llm.configurar` continua sendo a única permissão citada em
        `MudancaDeLLM`).

    (b) `test_fase5_auditoria_perm.mjs` — FIX-C39:
      • `PERM_ANY` está declarado e vale `"*"`;
      • a entrada `id: "auditoria"` carrega `perm: PERM_ANY`;
      • o filtro trata `PERM_ANY` explicitamente;
      • **asserção de não-regressão de acesso**: a entrada de auditoria NÃO
        cita nenhuma das 7 permissões reais usadas pelas outras abas
        (`observabilidade.ver`, `operador_ia.ver`, `execucao_automatica.ver`,
        `llm.configurar`, `fontes_dados.configurar`, `prompts.editar`,
        `usuarios.gerenciar`) — é o jeito de travar que ninguém "consertou"
        C-39 estreitando o acesso;
      • as outras 9 entradas continuam com suas permissões originais (lista
        explícita no teste).
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/bolsia/b3-agente/web && node tests/test_fase5_alerta_gasto_admin.mjs && node tests/test_fase5_auditoria_perm.mjs && cd .. && bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - Os dois `.mjs` novos saem com exit 0 rodando isoladamente
    - Os dois `.mjs` rodam sem `web/node_modules` (usam só `fs`/`path`): confirmar com `node --input-type=module -e "import('fs')"`-equivalente ou simplesmente checando que não há import de `web/src/`
    - Prova negativa registrada no SUMMARY: trocar o tom do alerta de `"warn"` para `"negative"` faz o guardião FALHAR; reverter
    - Prova negativa de C-39: trocar `perm: PERM_ANY` por `perm: "observabilidade.ver"` faz o guardião FALHAR; reverter
    - `bash scripts/executar.sh --testes` exit 0
    - `cd web-admin && npx vite build` exit 0
  </acceptance_criteria>
  <done>As duas correções do portal admin passam a ser protegidas por guardião dentro da suíte canônica, sem instalar nada novo no `web-admin/`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin autenticado → aba Custos | dado de FinOps do servidor inteiro é exibido aqui |
| filtro de abas do front → controle de acesso real | o filtro NÃO é o portão; o portão é `require_permission`/`require_any_admin_permission` no backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-24 | Spoofing | alerta mostrando "dentro do normal" sem base de comparação | mitigate | Quatro estados distintos, com "não configurado" e "sem base de comparação" em tom `faint`, nunca `positive` |
| T-05-25 | Tampering | alerta preventivo fundido com o hard stop numa severidade só | mitigate | Tom `warn` reservado ao preventivo; guardião assere que `negative` não aparece na expressão de tom da linha nova |
| T-05-26 | Elevation of privilege | "consertar" C-39 atribuindo permissão real e estreitando o acesso | mitigate | Sentinela `PERM_ANY` + guardião que assere ausência das 7 permissões reais na entrada de auditoria |
| T-05-27 | Elevation of privilege | confiar no filtro do front como controle de acesso | accept | Backend gateia com `require_any_admin_permission()` independentemente; o filtro é conveniência de UI (documentado no comentário da sentinela) |
| T-05-28 | Repudiation | mudança de limiar de FinOps sem rastro | mitigate | Reusa o fluxo `PUT /api/admin/config/ia`, que audita toda escrita (ADR-013) — nenhum caminho de escrita novo |
| T-05-SC | Tampering | instalação de pacote (npm/pip/cargo) | mitigate | Decisão explícita: NENHUM framework de teste entra no `web-admin/`; guardiões reusam o padrão `.mjs` de `web/tests/` com `fs`/`path`. `git diff` de `web-admin/package.json` tem de ficar vazio |
</threat_model>

<verification>
- `cd web-admin && npx vite build` passa
- `bash scripts/executar.sh --testes` passa com os 2 guardiões novos
- `git diff web-admin/package.json` vazio (nenhuma dependência nova)
- As duas provas negativas executadas e registradas
</verification>

<success_criteria>
- FIX-C38 (front): quatro estados do alerta preventivo na aba Custos, com tom âmbar no cruzamento do limiar, e configuração no formulário auditado existente
- FIX-C39: regra de acesso da aba Auditoria declarada explicitamente, acesso real inalterado
- Zero infraestrutura nova no `web-admin/`
</success_criteria>

<output>
Create `.planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-06-SUMMARY.md` when done
</output>
