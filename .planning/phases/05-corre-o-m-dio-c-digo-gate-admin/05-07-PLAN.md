---
phase: 05-corre-o-m-dio-c-digo-gate-admin
plan: 07
type: execute
wave: 2
depends_on: ["05-02", "05-03", "05-05"]
files_modified:
  - web/src/persistence.js
  - web/src/App.jsx
  - web/tests/test_fase5_gate_mensal_front.mjs
  - web/tests/test_fase5_c34_orcamento_nao_vaza.mjs
autonomous: true
requirements: [FIX-C33, FIX-C34]

must_haves:
  truths:
    - "O gate de análises do front recebe a contagem real do mês, obtida do ledger do servidor — não o literal 0"
    - "Os DOIS stores (deviceStore e serverStore) expõem o método novo com o mesmo contrato"
    - "Quando a contagem não pode ser obtida, o front degrada de forma explícita e documentada, e o gate do servidor continua sendo o autoritativo"
    - "O aviso de dado degradado continua visível ao usuário final sem revelar número de orçamento, cota, limite ou percentual"
  artifacts:
    - path: "web/src/persistence.js"
      provides: "Método analisesNoMes() nos dois stores, lendo o ledger do servidor"
      contains: "analisesNoMes"
    - path: "web/src/App.jsx"
      provides: "Call site de canAnalyze com a contagem real"
      contains: "canAnalyze"
    - path: "web/tests/test_fase5_gate_mensal_front.mjs"
      provides: "Guardião de que o literal 0 não volta e de que os dois stores têm o método"
    - path: "web/tests/test_fase5_c34_orcamento_nao_vaza.mjs"
      provides: "Guardião de confirmação de FIX-C34 sobre o comportamento já entregue em FIX-C30"
  key_links:
    - from: "web/src/App.jsx (A.analyze)"
      to: "store.analisesNoMes()"
      via: "argumento de canAnalyze"
      pattern: "canAnalyze\\("
    - from: "web/src/persistence.js"
      to: "GET /api/ai/quota (monthUsed)"
      via: "api.aiQuota() nos dois stores"
      pattern: "monthUsed"
---

<objective>
Fechar a metade FRONT de FIX-C33 (o espelho do gate no cliente também chamava
`canAnalyze(0)` com o comentário "FUTURO: passar contagem do mês") e FIX-C34
(medidor de orçamento brapi — **fechamento por confirmação**, não por feature
nova).

Purpose: C-33 no front é a mesma lacuna estrutural do backend — no dia em que
o número comercial do ADR-010 for populado, o pré-check do cliente compararia
`0 >= limite` e nunca avisaria. C-34 foi resolvido no 05-CONTEXT.md por
verificação direta contra `03-01-SUMMARY.md`: o fix de FIX-C30 (Fase 3) já
entrega o que o achado ORIGINAL recomenda, e o Copywriting Contract da Fase 3
decidiu deliberadamente não expor orçamento/cota/limite ao usuário final.
Construir um medidor agora contradiria decisão de produto já shippada.
Output: método novo nos dois stores, call site corrigido, 2 guardiões.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-CONTEXT.md
@.planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-UI-SPEC.md
@.planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-02-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
<!-- Estado verificado em 2026-08-23. -->

Espelho do gate no cliente (`web/src/plan.js`, espelho de `server/app/plan.py`):
```js
export const PLAN_FREE = { id: "free", maxWatchlist: null, maxAnalysesPerMonth: null };
export function canAddTicker(count, plan = ACTIVE_PLAN)      // -> { ok, reason? }
export function canAnalyze(usedThisMonth, plan = ACTIVE_PLAN) // -> { ok, reason? }
```

Call sites em `web/src/App.jsx`:
- linha ~7161, dentro de `A.analyze`: `const gate = canAnalyze(0); // FUTURO: passar contagem do mês`
- linha ~7618: `const gate = canAddTicker((data.watchlist || []).length);`
  — **já passa o estado real**, nada a mudar

Contrato vindo do Plano 05-02 — `GET /api/ai/quota` passa a devolver, em nível
raiz: `monthUsed` (inteiro para conta logada, `null` para escopo anônimo) e
`monthLimit` (hoje `null`).

Os DOIS stores de `web/src/persistence.js` (guardrail do CLAUDE.md: método
novo entra nos DOIS, com o mesmo contrato):
- `serverStore()` (linha ~97): já expõe `aiQuota: () => api.aiQuota()` (linha ~220)
- `deviceStore()` (linha ~214): já expõe `async aiQuota() { ensure(); return api.aiQuota(); }` (linha ~997)
- seleção em runtime: `export const store = isNative ? deviceStore() : serverStore();` (linha 1171)
- guardiões de paridade que vão avaliar o método novo:
  `web/tests/test_api_parity.mjs` e `web/tests/test_fase3_paridade_stores_generica.mjs`

Estado de FIX-C30 (Fase 3), que é o objeto da confirmação de C-34:
- `GET /api/technicals/{ticker}` devolve `degradado` (bool) — `server/app/main.py:1171`,
  rota SEM gate de permissão (usuário final alcança)
- `web/src/App.jsx:1589-1592`: qualificador âmbar na linha de fonte do
  `TechnicalModal` — `data.degradado && <span style={{ color: T.warn }}> · dado pode estar mais desatualizado que o habitual</span>`
- `web/src/App.jsx:5646-5650`: linha "estado do orçamento" em `FonteDadosScreen`
- **fato relevante confirmado por leitura**: `FonteDadosScreen` obtém `orc`
  de `store.adminSummary()` → `/api/admin/summary`, que exige a permissão
  `observabilidade.ver`; conta não-admin cai em `adminDenied` e a seção
  inteira some. Ou seja, os NÚMEROS de orçamento continuam admin-only mesmo
  dentro do app consumidor — e é assim que o Copywriting Contract da Fase 3
  quis: *"o qualificador de degradado deliberadamente não menciona
  orçamento/cota/mês/limite ao usuário final — só o efeito (dado mais velho),
  nunca a causa."*
- guardião existente de C-30 no front: `web/tests/test_fase3_fonte_technicals.mjs`
  (não apagar, não reescrever)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: analisesNoMes() nos DOIS stores + call site do gate (FIX-C33 front)</name>
  <files>web/src/persistence.js, web/src/App.jsx</files>
  <read_first>
    - web/src/persistence.js — linhas 1-30 (cabeçalho que explica os dois stores), 210-230 (`aiQuota` do `serverStore`), 990-1010 (`aiQuota` do `deviceStore`) e 1165-1175 (a seleção em runtime)
    - web/src/plan.js (arquivo inteiro, ~35 linhas)
    - web/src/App.jsx — linhas 7150-7175 (o call site `A.analyze`) e 7610-7625 (o call site de `canAddTicker`, referência do formato correto)
    - web/tests/test_api_parity.mjs e web/tests/test_fase3_paridade_stores_generica.mjs — como a paridade é verificada, para não introduzir assimetria
    - .planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-02-SUMMARY.md — contrato de `monthUsed`/`monthLimit`
  </read_first>
  <action>
    (a) `web/src/persistence.js`: acrescentar `analisesNoMes()` ao `serverStore`
    E ao `deviceStore`, com contrato IDÊNTICO —
    `async analisesNoMes(): Promise<number|null>`, devolvendo o `monthUsed` da
    resposta de `api.aiQuota()` quando for número, e `null` em qualquer outro
    caso (escopo anônimo, campo ausente, resposta inesperada). O `deviceStore`
    chama `ensure()` antes, como já faz em `aiQuota()`.

    Ponto de desenho a comentar no código: o aparelho NÃO mantém contador
    próprio. O `plan.py` do servidor é explícito — a contagem tem de derivar
    do ledger de `metering`, "nunca de um segundo contador paralelo" — então
    os dois stores leem o MESMO endpoint. É por isso que o local-first do
    iPhone abre exceção aqui, igual já abre para `aiQuota`.

    (b) `web/src/App.jsx`, dentro de `A.analyze` (linha ~7161): substituir
    `canAnalyze(0)` por uma leitura real:
      • obter a contagem com `await store.analisesNoMes()`;
      • envolver em `try/catch`; em erro ou `null`, seguir com `0` e comentar
        POR QUE isso é aceitável e não é o hardcode que C-33 corrige: o gate
        AUTORITATIVO é o do servidor (`_gate_analise`), que roda na mesma
        requisição de análise; este é um pré-check de UX, e falhar-aberto num
        pré-check é preferível a bloquear análise por causa de uma leitura de
        cota que não respondeu. Citar FIX-C33 e o contrato de `plan.py`;
      • apagar o comentário `// FUTURO: passar contagem do mês` — deixou de
        ser verdade.
    Manter o resto de `A.analyze` intacto (a função já é `async`, então o
    `await` novo não muda a forma dela).

    (c) NÃO tocar no call site de `canAddTicker` (linha ~7618): já passa
    `(data.watchlist || []).length`, que é o estado real. Registrar no SUMMARY
    que essa metade de FIX-C33 já estava correta antes da fase, nos dois lados.

    (d) NÃO ligar limite comercial: `web/src/plan.js` continua com
    `maxAnalysesPerMonth: null` nos dois planos (ADR-010 é decisão de negócio
    pendente — 05-CONTEXT.md, Deferred).
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/bolsia/b3-agente/web && npx vite build && cd .. && bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx vite build` conclui sem erro
    - `grep -c "analisesNoMes" web/src/persistence.js` retorna >= 2 (um por store)
    - `grep -v '^\s*//' web/src/App.jsx | grep -c "canAnalyze(0)"` retorna 0
    - `grep -n "FUTURO: passar contagem do mês" web/src/App.jsx` não casa
    - `grep -c "maxAnalysesPerMonth: null" web/src/plan.js` retorna 2 (nenhum limite ligado)
    - `cd web && node tests/test_api_parity.mjs && node tests/test_fase3_paridade_stores_generica.mjs` saem com exit 0
    - `bash scripts/executar.sh --testes` exit 0
  </acceptance_criteria>
  <done>O pré-check de análises do front usa a contagem real do mês vinda do ledger do servidor, presente nos dois stores com o mesmo contrato, e a degradação está documentada no código.</done>
</task>

<task type="auto">
  <name>Task 2: Guardião do gate no front + confirmação de FIX-C34</name>
  <files>web/tests/test_fase5_gate_mensal_front.mjs, web/tests/test_fase5_c34_orcamento_nao_vaza.mjs</files>
  <read_first>
    - web/tests/test_fase3_fonte_technicals.mjs (arquivo inteiro) — o guardião de C-30 que este plano CONFIRMA (e não altera)
    - web/src/App.jsx — linhas 1580-1600 (qualificador de degradado no `TechnicalModal`) e 5525-5560 (`FonteDadosScreen`, incluindo a origem admin-gated de `orc`)
    - .planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-CONTEXT.md — a decisão de C-34 ("Tensão resolvida por verificação direta")
    - .planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-UI-SPEC.md — a tabela "No-UI requirements", que lista C-34 como verificação, sem UI
  </read_first>
  <action>
    (a) `web/tests/test_fase5_gate_mensal_front.mjs` — guardião estático de
    FIX-C33 no front, no formato `readFileSync` + regex + `ok(nome, cond)` +
    `process.exit(fails === 0 ? 0 : 1)` (ver `test_fase3_fonte_technicals.mjs`).
    Filtrar linhas de comentário antes de contar. Assertar:
      • o fonte de `App.jsx`, sem comentários, NÃO contém `canAnalyze(0`
        (fecha a CLASSE do erro, não a instância);
      • em `A.analyze`, `canAnalyze` é chamado com um identificador, não com
        literal numérico;
      • `analisesNoMes` está definido nos DOIS stores de `persistence.js` —
        assertar pela contagem de definições, não por uma só ocorrência;
      • `web/src/plan.js` continua com `maxAnalysesPerMonth: null` nos dois
        planos (o teste falha se alguém ligar limite comercial sem passar
        pelo ADR-010);
      • `canAddTicker` continua recebendo o tamanho real da watchlist.

    (b) `web/tests/test_fase5_c34_orcamento_nao_vaza.mjs` — guardião de
    CONFIRMAÇÃO de FIX-C34. O cabeçalho tem de explicar, para quem ler daqui a
    seis meses, POR QUE este arquivo não constrói nada:
      • o texto ORIGINAL do achado C-34 diz que "não é necessário expor
        orçamento bruto ao usuário final... mesma mudança de C-30 resolve as
        duas questões";
      • o Copywriting Contract da Fase 3 decidiu, deliberadamente, que o
        qualificador de degradado não menciona orçamento/cota/mês/limite —
        só o efeito, nunca a causa;
      • portanto o escopo de C-34 nesta fase é CONFIRMAR que esse
        comportamento segue valendo. O Success Criteria 4 do ROADMAP (escrito
        em 2026-08-18, antes da entrega de FIX-C30) pede um "medidor visível
        ao usuário final" e foi SUPERADO por essa decisão de produto —
        registrar a supersessão aqui e no SUMMARY, para que a verificação da
        fase não a trate como lacuna. Reabrir a decisão (expor medidor de
        verdade) é escolha do Alex, explícita, não inferência do ROADMAP.
    Asserções:
      • o qualificador `"dado pode estar mais desatualizado que o habitual"`
        continua no fonte, exatamente 1×, condicionado a `data.degradado` e
        usando `T.warn` (as mesmas invariantes que `test_fase3_fonte_technicals.mjs`
        trava — a duplicação é DELIBERADA: se o guardião da Fase 3 for um dia
        relaxado, este ainda grita);
      • a vizinhança do qualificador (janela de ~200 caracteres em torno da
        ocorrência) NÃO contém as palavras `orçamento`, `cota`, `limite`, nem
        o caractere `%` — é a asserção de não-vazamento propriamente dita;
      • os números de orçamento em `FonteDadosScreen` continuam vindo de
        `adminSummary`/`admin.usoIA` e a seção continua condicionada a
        `adminDenied` — ninguém expôs medidor ao usuário final por acidente
        enquanto mexia nesta fase.
    Este arquivo não altera `test_fase3_fonte_technicals.mjs` em nada.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/bolsia/b3-agente/web && node tests/test_fase5_gate_mensal_front.mjs && node tests/test_fase5_c34_orcamento_nao_vaza.mjs && node tests/test_fase3_fonte_technicals.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Os dois `.mjs` novos e o `test_fase3_fonte_technicals.mjs` saem com exit 0
    - Prova negativa registrada no SUMMARY: reintroduzir `canAnalyze(0)` em `App.jsx` faz o guardião de C-33 FALHAR; reverter
    - Prova negativa registrada no SUMMARY: acrescentar a palavra `cota` ao lado do qualificador de degradado faz o guardião de C-34 FALHAR; reverter
    - `git diff web/tests/test_fase3_fonte_technicals.mjs` vazio (o guardião da Fase 3 não foi tocado)
    - O cabeçalho de `test_fase5_c34_orcamento_nao_vaza.mjs` registra explicitamente a supersessão do Success Criteria 4 do ROADMAP
    - `bash scripts/executar.sh --testes` exit 0
  </acceptance_criteria>
  <done>Reintroduzir o hardcode do gate ou vazar número de orçamento para o usuário final passa a quebrar a suíte, e a resolução de C-34 fica registrada no próprio guardião.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| front → gate de plano | o pré-check do cliente NÃO é o portão; `_gate_analise` no servidor é |
| servidor → usuário final (painel técnico) | detalhe de infraestrutura (orçamento/cota) pode vazar por copy nesta borda |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-29 | Elevation of privilege | contagem enviada pelo cliente virar a fonte do gate | accept | O front é pré-check de UX; a decisão vinculante roda no servidor (`_gate_analise`, Plano 05-02). Documentado no comentário do call site |
| T-05-30 | Tampering | segundo contador de análises nascendo no aparelho | mitigate | Os dois stores leem `monthUsed` do MESMO endpoint; guardião assere que `analisesNoMes` existe nos dois e nenhum acumula localmente |
| T-05-31 | Denial of service | leitura de cota falhando bloquear a análise | mitigate | `try/catch` com falha-aberta explícita e comentada; gate autoritativo do servidor continua ativo |
| T-05-32 | Information disclosure | número de orçamento/cota vazando para o usuário final | mitigate | Guardião de vizinhança do qualificador proíbe `orçamento`/`cota`/`limite`/`%` ao lado do aviso de degradado |
| T-05-33 | Repudiation | decisão de C-34 se perder e alguém "consertar" construindo o medidor | mitigate | A supersessão do Success Criteria 4 fica escrita no cabeçalho do guardião e no SUMMARY |
| T-05-SC | Tampering | instalação de pacote (npm/pip/cargo) | accept | Nenhum pacote novo; os `.mjs` usam só `fs`/`path` e o build usa o Vite já instalado |
</threat_model>

<verification>
- `cd web && npx vite build` passa (obrigatório: front editado)
- `bash scripts/executar.sh --testes` passa, incluindo os dois guardiões de paridade de store
- As duas provas negativas executadas e registradas
- `test_fase3_fonte_technicals.mjs` intocado
</verification>

<success_criteria>
- FIX-C33 (front): pré-check usa a contagem real do mês, com paridade entre os dois stores e degradação documentada
- FIX-C34: fechado por confirmação — o comportamento de FIX-C30 segue valendo e não vaza número de orçamento; nenhuma UI nova construída; supersessão do ROADMAP registrada
- Nenhum limite comercial ativado
</success_criteria>

<output>
Create `.planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-07-SUMMARY.md` when done
</output>
