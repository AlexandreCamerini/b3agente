---
phase: 05-corre-o-m-dio-c-digo-gate-admin
plan: 08
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05", "05-06", "05-07"]
files_modified:
  - web/src/version.js
  - server/web_dist
  - server/admin_dist
autonomous: false
requirements: [FIX-C21, FIX-C22, FIX-C23, FIX-C33, FIX-C34, FIX-C38, FIX-C39]

must_haves:
  truths:
    - "O que foi corrigido no front do app consumidor está publicado em server/web_dist com carimbo novo"
    - "O que foi corrigido no portal admin está publicado em server/admin_dist"
    - "O Alex confirmou ao vivo o toggle desabilitado e a linha de alerta de gasto"
  artifacts:
    - path: "web/src/version.js"
      provides: "BUILD_ID novo carimbando a entrega da Fase 5"
      contains: "BUILD_ID"
    - path: "server/web_dist"
      provides: "Bundle do app consumidor publicado (única árvore que o Railway enxerga)"
    - path: "server/admin_dist"
      provides: "Bundle do portal admin publicado"
  key_links:
    - from: "web/src/version.js"
      to: "server/web_dist"
      via: "scripts/bump.sh seguido de scripts/publicar-web.sh"
      pattern: "BUILD_ID"
---

<objective>
Publicar o que a Fase 5 mudou no front — app consumidor (`web/src/App.jsx`,
`persistence.js`, `catalog.js`) e portal admin (`web-admin/src/App.jsx`) — e
fechar com verificação humana ao vivo.

Purpose: sem este plano a fase termina testada e mergeada, mas nada do front
chega ao ar. É o modo de falha já registrado no projeto ("fase sem plano de
publicação front": o plano toca `web/src/`, o merge passa, e a correção nunca
sai do repositório). Backend vai junto no mesmo push (Railway redeploya).
Output: `web_dist` e `admin_dist` atualizados, carimbo novo, checkpoint humano.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-CONTEXT.md
@CLAUDE.md
</context>

<interfaces>
<!-- Fluxo de publicação da casa. Verificado em 2026-08-23. -->

Ordem obrigatória (guardrail do CLAUDE.md): `scripts/bump.sh` ANTES de
`scripts/publicar-web.sh`; nunca editar `server/web_dist` à mão.

- `scripts/bump.sh` — avança o `BUILD_ID` em `web/src/version.js`. É o único
  carimbo editado à mão; o `SERVER_BUILD_ID` de `/api/health` deriva dele.
  Esquecer o bump faz o dist novo sair com o carimbo do dia anterior — a
  assinatura de "bundle amputado" que `test_ios_assets.mjs` pega.
- `scripts/publicar-web.sh` — builda `web/` e publica em `server/web_dist`
  (única árvore que o Railway enxerga; `rootDirectory=/server`).
- `scripts/publicar-admin.sh` — builda `web-admin/` e publica em
  `server/admin_dist` (servido em `/admin/*`). SEM carimbo de versão: a
  prova de publicação é o hash do arquivo mudar no diff.

Regra de processo herdada da Fase 8 (registrada no STATE.md): fase com
checkpoint humano bloqueante não leva `git push` de nenhuma wave antes da
aprovação. Nesta fase, o push acontece DENTRO deste plano, imediatamente
antes do checkpoint — que é a verificação do que acabou de subir. Nenhuma
wave anterior é empurrada isoladamente.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Bump, build e publicação das duas árvores de front</name>
  <files>web/src/version.js, server/web_dist, server/admin_dist</files>
  <read_first>
    - scripts/bump.sh — linhas 1-25 (o contrato do carimbo)
    - scripts/publicar-web.sh — linhas 1-40 (o que builda e o que copia, e as opções `--so-build`/`--so-publicar`)
    - scripts/publicar-admin.sh — linhas 1-25
    - CLAUDE.md — seção "Guardrails do repositório", item de publicação
  </read_first>
  <action>
    Executar, nesta ordem exata:

    1. `bash scripts/executar.sh --testes` — a suíte canônica INTEIRA tem de
       estar verde antes de qualquer build. Se falhar, parar: publicar por
       cima de suíte vermelha é o oposto do que esta fase entrega.
    2. `bash scripts/bump.sh` — carimbo novo do dia. Registrar o `BUILD_ID`
       gerado no SUMMARY.
    3. `bash scripts/publicar-web.sh` — build + publicação em `server/web_dist`.
    4. `bash scripts/publicar-admin.sh` — build + publicação em `server/admin_dist`.
    5. Revisar o diff das duas árvores: confirmar que `server/web_dist` carrega
       o `BUILD_ID` novo e que `server/admin_dist` mudou de hash (senão o
       portal não foi republicado e a linha de alerta de gasto não está no ar).
    6. Commit único desta publicação, mensagem citando a fase e o `BUILD_ID`.
    7. `git push` — o Railway redeploya sozinho, levando junto as mudanças de
       `server/app/` das waves anteriores.
    8. Depois do deploy, confirmar `/api/health` respondendo com o
       `SERVER_BUILD_ID` derivado do carimbo novo.

    Não editar `server/web_dist` nem `server/admin_dist` à mão em nenhuma
    hipótese — os scripts são o único caminho.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/bolsia/b3-agente && bash scripts/executar.sh --testes && git status --porcelain server/web_dist server/admin_dist</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/executar.sh --testes` exit 0 ANTES do build
    - `git diff HEAD~1 --stat -- server/web_dist` mostra arquivos alterados
    - `git diff HEAD~1 --stat -- server/admin_dist` mostra arquivos alterados
    - O `BUILD_ID` de `web/src/version.js` aparece no bundle publicado em `server/web_dist` (`grep -rl "<BUILD_ID>" server/web_dist | head -1` casa)
    - `curl -s https://<host de produção>/api/health` devolve o `SERVER_BUILD_ID` derivado do carimbo novo
    - Nenhum arquivo sob `server/web_dist`/`server/admin_dist` foi editado fora dos scripts (o diff é inteiramente de saída de build)
  </acceptance_criteria>
  <done>As duas árvores de front estão publicadas e no ar, com carimbo novo verificável em `/api/health`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificação ao vivo das duas mudanças visíveis da fase</name>
  <files>—</files>
  <read_first>
    - .planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-UI-SPEC.md — as seções de FIX-C23 e FIX-C38, para conferir o que foi contratado
  </read_first>
  <action>
    Apresentar ao Alex o bloco `<what-built>` e o roteiro de `<how-to-verify>`,
    parar e aguardar. Não seguir, não corrigir nada, não fechar a fase antes do
    sinal de retomada. Ao receber a resposta, transcrever o resultado dos 7
    passos no SUMMARY, um a um, incluindo os que o Alex não conseguiu executar
    (ex.: verificação no iPhone sem build novo de TestFlight) — "não
    verificado" é resultado válido e tem de ficar registrado como tal, nunca
    marcado como aprovado por inferência.
  </action>
  <what-built>
    A Fase 5 fechou 11 achados Médio. Só DOIS mudam pixel:
    • FIX-C23 — o toggle "Entrar automaticamente" (Operador IA) agora é um
      controle HTML desabilitado de verdade fora do Modo Operador, esmaecido a
      60% e com cursor de "não permitido", no mesmo padrão do slider de
      alocação e do botão Executar que já eram assim.
    • FIX-C38 — a aba Custos do portal admin ganhou a linha "Alerta de gasto
      (IA gerenciada)", com quatro estados (não configurado / sem base de
      comparação / dentro do normal / ⚠ acima do limiar), e o formulário de
      "Mudança de LLM" ganhou dois campos: limiar (%) e janela (dias).
    Os outros 9 achados são teste, guardião, migração de leitura e documentação
    — sem pixel. FIX-C34 foi fechado por confirmação: o aviso de dado
    degradado continua aparecendo sem revelar número de orçamento/cota, que é
    a decisão de produto tomada na Fase 3.
  </what-built>
  <how-to-verify>
    1. Abrir o app (web ou iPhone) em **Modo Estudo** e ir em Operador IA.
       O toggle "Entrar automaticamente" tem de aparecer apagado, não
       clicável, e o texto abaixo continua dizendo "Disponível no Modo
       Operador…". Tentar clicar: nada acontece. Tentar chegar nele por
       Tab: o foco deve pular o controle.
    2. Trocar para **Modo Operador** (Perfil → Modo de trabalho). O mesmo
       toggle volta a ficar nítido e clicável. NÃO precisa ligar a entrada
       automática — só confirmar que o controle responde.
    3. Abrir o portal admin → aba **Custos** → card "Uso de IA". Confirmar a
       linha "Alerta de gasto (IA gerenciada)". Sem limiar configurado, ela
       tem de dizer "não configurado" em cinza — nunca verde de "dentro do
       normal".
    4. Portal admin → aba **Mudança de LLM**: os dois campos novos aparecem
       depois de "Teto global/dia". Preencher limiar = 30 e janela = 7,
       "Salvar (auditado)", recarregar e confirmar que os valores persistiram.
    5. Voltar à aba Custos: a linha agora deve sair de "não configurado" para
       "dentro do normal" ou "sem base de comparação" (depende de já haver
       histórico diário gravado). "sem base de comparação" NÃO é defeito — é o
       estado correto quando ainda não há dias suficientes na série.
    6. Portal admin: confirmar que a aba **Auditoria** continua aparecendo
       normalmente (FIX-C39 não podia estreitar o acesso).
    7. App, qualquer modo: abrir o painel técnico de um ativo e confirmar que
       a linha de fonte segue como antes — se aparecer o aviso âmbar de dado
       desatualizado, ele NÃO pode citar orçamento, cota, limite nem
       percentual (FIX-C34).
  </how-to-verify>
  <acceptance_criteria>
    - Os 7 passos acima verificados pelo Alex, com o resultado de cada um registrado no SUMMARY
    - Qualquer divergência vira achado nomeado no SUMMARY (não é corrigida em silêncio dentro deste plano)
  </acceptance_criteria>
  <verify>
    <human-check>Os 7 passos de `<how-to-verify>` executados pelo Alex no app publicado e no portal admin publicado, com resultado item a item.</human-check>
  </verify>
  <done>O Alex respondeu ao checkpoint e o resultado dos 7 passos está transcrito no SUMMARY, inclusive os itens não verificados.</done>
  <resume-signal>Digite "aprovado" ou descreva o que divergiu, item por item</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repositório → produção (Railway) | o `git push` deste plano publica app consumidor, portal admin e backend de uma vez |
| build local → `server/web_dist` | árvore versionada que o Railway serve; edição manual aqui contorna todo o pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-34 | Tampering | `server/web_dist` editado à mão | mitigate | Publicação só por `scripts/publicar-web.sh`; critério de aceite exige que o diff seja inteiramente saída de build |
| T-05-35 | Spoofing | dist novo com carimbo antigo (assinatura de bundle amputado) | mitigate | `bash scripts/bump.sh` ANTES do publicar; `BUILD_ID` conferido dentro de `server/web_dist` e em `/api/health` |
| T-05-36 | Denial of service | publicar por cima de suíte vermelha | mitigate | `bash scripts/executar.sh --testes` verde é pré-condição declarada do Task 1 |
| T-05-37 | Repudiation | portal admin não republicado, alerta novo invisível em produção | mitigate | Diff de `server/admin_dist` é critério de aceite explícito, além da conferência ao vivo no checkpoint |
| T-05-SC | Tampering | supply chain no build de produção | mitigate | Build a partir dos `node_modules` já instalados dos manifestos versionados; nenhuma dependência nova entrou nesta fase (critério de aceite dos Planos 05-04 e 05-06) |
</threat_model>

<verification>
- Suíte canônica verde ANTES do build
- `BUILD_ID` novo presente em `server/web_dist` e em `/api/health`
- `server/admin_dist` com hash alterado
- Checkpoint humano aprovado, com os 7 passos registrados
</verification>

<success_criteria>
- As correções de front da Fase 5 estão no ar (app consumidor e portal admin)
- Carimbo de entrega novo e verificável
- Verificação ao vivo aprovada pelo Alex, com divergências (se houver) registradas como achado, não corrigidas em silêncio
</success_criteria>

<output>
Create `.planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-08-SUMMARY.md` when done
</output>
