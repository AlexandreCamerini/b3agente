---
phase: 40-continuidade-da-aba-op-es
plan: 02
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - web/src/version.js
  - server/web_dist
  - server/app/main.py
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [ESTADO-01]

must_haves:
  truths:
    - "O Alex viu, rodando, os cenários A, A2, B, B2, C, D, E, F e G do 40-UI-SPEC e aprovou ESTADO-01"
    - "O Alex confirmou ou rejeitou EXPLICITAMENTE, por nome, DP-1 (deep-link de Posições troca só a aba e mantém o ticker lembrado) — não por inferência a partir dos cenários"
    - "A fase está NO AR no PWA (BUILD_ID novo conferido em /api/health de produção) e nenhum push ocorreu antes do aprovado"
    - "ESTADO-01 Done e Fase 40 fechada em ROADMAP/STATE/REQUIREMENTS, editados À MÃO"
  artifacts:
    - path: "web/src/version.js"
      provides: "BUILD_ID novo carimbando a entrega da Fase 40"
      contains: "F10-"
    - path: ".planning/REQUIREMENTS.md"
      provides: "ESTADO-01 Done"
      contains: "ESTADO-01"
  key_links:
    - from: "web/src/version.js (BUILD_ID)"
      to: "server/web_dist + SERVER_BUILD_ID em /api/health"
      via: "scripts/bump.sh seguido de scripts/publicar-web.sh"
      pattern: "F10-[0-9]{8}-[0-9]{2}"
---

<objective>
Fechar a Fase 40: verificação humana ao vivo dos cenários do UI-SPEC (inclusive a
premissa P-1 nomeada como DP-1), publicação do front e documentos de planejamento
fechados à mão.

Purpose: plano que toca `web/src/` sem bump+publicação fica testado e nunca vai ao ar
(defeito de processo conhecido); o checkpoint humano segura o push da fase inteira.
Output: build no ar, ESTADO-01 Done, STATE/ROADMAP/REQUIREMENTS coerentes.
</objective>

<repo_guardrail>
- Não chamar mutador de estado do `gsd-sdk` (`state.advance-plan`, `state.record-session`, `state.record-metric`, `state.add-decision`) — corrompem `.planning/STATE.md` (decisão do Alex, 2026-09-11). Editar à mão com Edit; `git diff .planning/` antes do commit.
- `git fetch origin && git merge origin/main` ANTES do `scripts/bump.sh` (bump.sh deriva do valor LOCAL — sessão paralela colide); `scripts/bump.sh` antes de `scripts/publicar-web.sh`; nunca editar `server/web_dist` à mão; conflito em `web_dist` se resolve REGERANDO.
- Histórico não se reescreve: o comentário de `SERVER_BUILD_ID` em main.py ganha a entrada nova no topo e preserva o texto anterior como `HISTORICO (entrega anterior, F10-20260924-01)`.
- Nenhum push antes do "aprovado" da Task 1 com resposta explícita a DP-1.
</repo_guardrail>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-continuidade-da-aba-op-es/40-CONTEXT.md
@.planning/phases/40-continuidade-da-aba-op-es/40-UI-SPEC.md
@.planning/phases/40-continuidade-da-aba-op-es/40-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: verificação humana ao vivo — cenários A–G do UI-SPEC + DP-1 nomeada</name>
  <what-built>
    - Memória em sessão (App.jsx, `opcoesMemoria`) do ticker + aba ativa da tela Opções, gravada a cada mudança e lida no mount por inicializador lazy (40-01).
    - Precedência: deep-link de Posições > memória > "Oportunidades"; ticker só restaurado se ainda estiver na carteira.
    - Troca de conta/logout zera a memória e remonta a tela (key de escopo).
    - Nada visual novo: sem toast, sem texto, sem animação (D-05).
  </what-built>
  <how-to-verify>
    Subir backend + Vite locais (`bash scripts/executar.sh`; se travar em `lsof`/`ps`, subir backend e Vite diretamente), com uma conta que tenha pelo menos 2 ativos em carteira. DevTools aberto na aba Rede, viewport 375px.

    1. **A — sair e voltar**: em Opções, ir a "Montar", escolher um ativo (ex. PETR4), abrir o sheet de Vigias, fechar, expandir "Ver outros vencimentos". Tocar Carteira no menu e voltar a Opções: abre em "Montar", chip do ativo marcado, Comparar RECOLHIDO, tese/vencimento/alvo/stop vazios, sheet fechado. Nenhum frame com "Oportunidades" ativa nem com "Escolha um ativo…" (repetir 3×).
    2. **A2** — em "Destacadas" sem ativo, sair e voltar: abre em "Destacadas".
    3. **F** — em "Oportunidades", tocar "Montar", sair e voltar: abre em "Montar".
    4. **G** — tocar de novo o chip ativo (desseleciona), sair e voltar: sem ativo.
    5. **B / B2** — com memória em "Montar" + PETR4, ir a Posições e tocar a linha "N estruturas destacadas…": abre em "Destacadas". Tocar "Montar": PETR4 continua escolhido. Sair por outra aba e voltar: abre em "Montar" (última aba vista). Depois tocar Opções direto pelo menu sem deep-link: não força Destacadas (one-shot da 39-02 intacto).
    6. **E** — com memória em "Montar" + um ativo, vender a posição inteira desse ativo na Carteira, voltar a Opções: "Montar" sem ativo, com o aviso "Escolha um ativo…" já existente; nenhum chip do ativo vendido.
    7. **C** — com memória em "Montar" + ativo, sair da conta (Perfil) e entrar com OUTRA conta; abrir Opções: "Oportunidades", sem ativo, nada do ativo anterior visível.
    8. **D** — recarregar a página: Opções abre em "Oportunidades" sem ativo (esperado — D-01, sem persistência entre sessões).
    9. **Rede** — nos itens 1-5, voltar a Opções com ativo lembrado NÃO dispara `/api/options/mcp/*` nem leitura paga; só `opcoes/tecnico` (custo 0). O botão da leitura paga aparece no estado pré-clique.
    10. **Modo Estudo** — repetir o item 1 em Estudo: mesmo comportamento, sem nenhum texto novo.

    **Decisão do planner a confirmar POR NOME (não vale aprovação por inferência a partir dos itens 1-10):**

    - **DP-1 — deep-link preserva o ticker lembrado (premissa P-1 do UI-SPEC, leitura do planner sobre D-03).** O D-03 diz que o toque em Posições "sempre vence" o estado lembrado. O plano aplicou isso só à ABA: o deep-link abre em Destacadas, mas o ativo lembrado continua escolhido, e aparece ao ir para Montar (item 5). Motivo: Destacadas não depende de ativo, então as duas coisas não competem. A alternativa (deep-link zera também o ativo) muda uma linha de `tickerInicialOpcoes`/do inicializador e o teste correspondente. Frase a apresentar ao Alex, sem parafrasear: **"Quando você toca a linha de estruturas em Posições, Opções abre em Destacadas mas mantém o ativo que você tinha escolhido em Montar. Aprova, ou prefere que esse toque também limpe o ativo?"** Rejeição = plano de gap (mudar o inicializador do ticker para ignorar a memória quando `ctx.opcoesAbaInicial` é válido + atualizar test_opcoes_continuidade_ui.mjs) ANTES de publicar.

    Divergência nos itens 1, 5, 6, 7 ou 9, ou rejeição de DP-1, é BLOQUEANTE para a publicação.
  </how-to-verify>
  <read_first>
    - .planning/phases/40-continuidade-da-aba-op-es/40-UI-SPEC.md (tabela "Cenários observáveis" e "Sem salto perceptível")
    - .planning/phases/40-continuidade-da-aba-op-es/40-01-SUMMARY.md (provas negativas, nota de C2, limitação declarada)
  </read_first>
  <action>
    Subir o ambiente local, apresentar ao Alex o roteiro de 10 itens + DP-1 como pergunta própria (frase em negrito exatamente como escrita) e PARAR. Mencionar também, como informação e não pergunta, a limitação registrada no 40-01-SUMMARY (no nativo, a carteira da conta anterior pode aparecer por um instante entre o reset e o loadState — pré-existente, não é a memória desta fase). Não publicar, não dar push, não seguir para a Task 2 antes da resposta.
  </action>
  <acceptance_criteria>
    - O Alex responde "aprovado" com resposta EXPLÍCITA a DP-1 (aprova ou rejeita), ou descreve divergências. Um "aprovado" sem menção a DP-1 não basta: perguntar de novo, por nome. Nenhum push/publicação antes disso.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — verificação visual/interativa por desenho; substitutos automatizados: `cd web && node tests/test_opcoes_continuidade_ui.mjs && node tests/test_opcoes_analisar_ui.mjs && node tests/test_logout_reset.mjs`</automated>
  </verify>
  <resume-signal>Digite "aprovado" com a resposta a DP-1 (aprova/rejeita) — ou descreva o que divergiu</resume-signal>
  <done>Aprovação humana registrada com DP-1 respondida, ou lista de divergências a corrigir (plano de gap) antes de publicar.</done>
</task>

<task type="auto">
  <name>Task 2: publicar o front e conferir em produção</name>
  <files>web/src/version.js, server/web_dist, server/app/main.py</files>
  <read_first>
    - scripts/bump.sh (linhas 1-40)
    - scripts/publicar-web.sh (inteiro)
    - server/app/main.py (linha `SERVER_BUILD_ID = ...` ~1662 e o formato da entrada com HISTORICO)
    - CLAUDE.md — "Publicação" e "Três esquemas de versão"
  </read_first>
  <action>
    Ordem obrigatória:
    1. `git fetch origin && git merge origin/main`. Conflito em `server/web_dist` se resolve regerando.
    2. Suíte canônica pré-bump: `bash scripts/executar.sh --testes` (fora do sandbox se o artefato TLS aparecer), números no SUMMARY, comparados com o 40-01-SUMMARY.
    3. `bash scripts/bump.sh`, depois `bash scripts/publicar-web.sh`. Se `npm ci` der EPERM de sandbox, rodar só o passo de build fora do sandbox e registrar — nenhum código de produto tocado para contornar.
    4. Comentário do `SERVER_BUILD_ID` em main.py: entrada nova no topo descrevendo a Fase 40 (ESTADO-01: ticker + aba da tela Opções lembrados em memória no App.jsx, `opcoesMemoria`, sem tocar os stores — D-01; só ticker+aba, resto volta default — D-02; deep-link de Posições vence na aba e mantém o ticker — D-03/DP-1 com a resposta do Alex; limpeza em `_resetScopeState` + key `escopoOpcoes` remontando a tela na troca de conta — D-04/SC#4/C2; restauração silenciosa por inicializador lazy — D-05; funções puras em web/src/opcoes/memoriaOpcoes.js; guardião test_opcoes_continuidade_ui.mjs com prova negativa; 3 guardiões reconciliados com nota datada; números da suíte) e o texto anterior preservado integralmente como `HISTORICO (entrega anterior, F10-20260924-01)`.
    5. Commit + push em `v2/interacao-estrutural` e fast-forward de `origin/main` (padrão das Fases 35/38/39).
    6. Conferir no ar (redeploy ~3-6 min): `curl -s https://boris.semente.dev/api/health` mostrando o `SERVER_BUILD_ID` novo; colar a saída no SUMMARY.
    Limitação a declarar, não resolver: o app iOS carrega bundle local — a continuidade da aba Opções só chega ao iPhone num build novo de TestFlight; nenhum backend mudou nesta fase, então o bundle antigo segue funcionando como antes.
  </action>
  <acceptance_criteria>
    - `grep -E "F10-[0-9]{8}-[0-9]{2}" web/src/version.js` com a data real de hoje
    - `grep -c "HISTORICO (entrega anterior, F10-20260924-01)" server/app/main.py` == 1
    - `/api/health` de produção com o `SERVER_BUILD_ID` novo (saída no SUMMARY)
    - `git log origin/main -1 --format=%H` == `git rev-parse HEAD`
  </acceptance_criteria>
  <verify>
    <automated>grep -E "F10-[0-9]{8}-[0-9]{2}" /Users/acamerini/dev/borisv2/web/src/version.js && curl -s https://boris.semente.dev/api/health</automated>
  </verify>
  <done>Fase 40 no ar no PWA, carimbo conferido em produção, pendência iOS registrada.</done>
</task>

<task type="auto">
  <name>Task 3: fechar os documentos de planejamento à mão</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (seção "Continuidade UX" ~46-56 e tabela de rastreio ~85-100)
    - .planning/ROADMAP.md (item da Fase 40 no topo ~linha 22 e a seção "### Phase 40" ~274-294)
    - .planning/STATE.md (frontmatter, "Current Position", formato das entradas "Posição anterior")
  </read_first>
  <action>
    Com Edit (nunca mutador do gsd-sdk):
    1. REQUIREMENTS.md: `- [x] **ESTADO-01**`; `Pending` -> `Done` na linha ESTADO-01 da tabela; linha "Last updated" nova (2026-09-25 ou data real) citando Fase 40 fechada, BUILD_ID publicado e a resposta a DP-1.
    2. ROADMAP.md: `- [x] Phase 40: Continuidade da aba Opções (2/2 plans) — completed <data>`; na seção "### Phase 40", `**Plans**: 2 plans` e a lista `- [x] 40-01-PLAN.md — memória em sessão (ticker+aba) + reset de escopo + guardião` / `- [x] 40-02-PLAN.md — verificação humana, publicação, fechamento`.
    3. STATE.md: frontmatter (`status`, `stopped_at`, `last_updated`, `last_activity`, `progress` — `completed_phases`/`total_plans`/`completed_plans`/`percent` coerentes entre si e com a barra, contando a Fase 40 e mantendo a Fase 41 como não iniciada); "Current Position" reescrito para a Fase 40 fechada: o que foi entregue, BUILD_ID, e as decisões que o próximo leitor não deve redescobrir — `opcoesMemoria` em memória no App.jsx (não nos stores, D-01); `lembrarOpcoes` é eco contínuo, `opcoesAbaInicial` segue one-shot independente; write-back sem cleanup de unmount (C2); `escopoOpcoes` key incrementada em `_resetScopeState`; default normaliza para null; funções puras em memoriaOpcoes.js; resposta do Alex a DP-1 com data. Próximo passo: Fase 41 (TELAS-01). A posição anterior vai para "Posição anterior", texto da época preservado.
    4. Pendências declaradas: build iOS/TestFlight; persistência entre reloads segue deferida (CONTEXT, Deferred Ideas); limitação do nativo registrada no 40-01-SUMMARY.
    Conferir `git diff .planning/` (sem texto antigo sobrescrito, sem métrica fora de tabela). Commit separado `docs(40): fecha fase 40` e push junto do fast-forward de origin/main.
  </action>
  <acceptance_criteria>
    - `grep -c "ESTADO-01.*Done" .planning/REQUIREMENTS.md` == 1 e `grep -c "\[ \] \*\*ESTADO-01" .planning/REQUIREMENTS.md` == 0
    - `grep -n "Phase 40" .planning/ROADMAP.md | head -1` mostra `[x]`
    - `grep -c "DP-1" .planning/STATE.md` ≥ 1
    - `git diff .planning/STATE.md` mostra só as seções pretendidas
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2 && git diff --stat HEAD~1 -- .planning/ && grep -c "ESTADO-01.*Done" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>Documentos refletem a realidade, editados à mão, com DP-1 e pendências registradas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> produção (Railway) | push publica `server/` e `server/web_dist` juntos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-07 | Tampering (entrega) | version.js -> web_dist | mitigate | merge de origin/main antes do bump.sh; bump.sh antes de publicar-web.sh; carimbo conferido em /api/health |
| T-40-08 | Information Disclosure | publicação sem validar vazamento entre contas | mitigate | cenário C (item 7) é bloqueante no checkpoint antes de qualquer push |
| T-40-09 | Integrity (cliente iOS antigo) | app nativo com bundle anterior | accept | nenhuma mudança de backend/contrato nesta fase; bundle antigo segue funcionando sem a continuidade até o TestFlight novo |
| T-40-10 | Repudiation (histórico) | STATE/ROADMAP/REQUIREMENTS, comentário SERVER_BUILD_ID | mitigate | edição à mão + git diff; HISTORICO preservado; mutadores do SDK proibidos |
</threat_model>

<verification>
1. Aprovação humana antes de qualquer push, com resposta explícita a DP-1.
2. BUILD_ID novo conferido em produção.
3. `git diff .planning/` revisado antes do commit de docs.
</verification>

<success_criteria>
- Alex aprovou ao vivo os cenários A–G e respondeu DP-1 por nome; fase no ar.
- ESTADO-01 Done; Fase 40 fechada à mão; pendências declaradas.
</success_criteria>

<output>
Create `.planning/phases/40-continuidade-da-aba-op-es/40-02-SUMMARY.md` when done
</output>
