---
phase: 30-curadoria-ia-melhores-estruturas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/opcoes_curadoria.py
  - server/tests/test_opcoes_curadoria.py
autonomous: true
requirements: [SC-1, SC-4, SC-5]
must_haves:
  truths:
    - "Dada uma cadeia já em memória, o motor devolve N candidatos de venda coberta de STRIKES diferentes do MESMO vencimento, sem tocar rede"
    - "A ordem das 4 melhores é função só de (razao, premioUnitario, contractSymbol) — embaralhar a entrada não muda a saída"
    - "Nenhum candidato de put/collar entra no universo"
    - "Um candidato com perda_maxima None ou <= 0 é DESCARTADO, nunca vira razao infinita"
    - "A camada de narração tem como recusar entrada não-rankeada (exigir_ranking levanta ValueError)"
  artifacts:
    - path: "server/app/opcoes_curadoria.py"
      provides: "candidatos_da_posicao, rankear, exigir_ranking, narrativa_system, narrativa_user, TOPO, STRIKES_POR_POSICAO"
      contains: "def rankear"
    - path: "server/tests/test_opcoes_curadoria.py"
      provides: "guardiões unitários do motor puro de curadoria"
      contains: "def test_"
  key_links:
    - from: "server/app/opcoes_curadoria.py"
      to: "server/app/opcoes_motor.py"
      via: "rastrear/avaliar/perna_de_acao/perna_de_contrato"
      pattern: "opcoes_motor\\.(rastrear|avaliar|perna_de_)"
    - from: "server/app/opcoes_curadoria.py"
      to: "server/app/options_quant.py"
      via: "piso de liquidez D5"
      pattern: "LIQUIDEZ_NEGOCIAVEL"
---

<objective>
Motor PURO da curadoria: enumerar vários STRIKES de venda coberta dentro do
vencimento único que a cadeia já traz, avaliar cada um por `avaliar()`, e
ordenar por razão prêmio/perda máxima com desempate total e determinístico.

Purpose: a escolha das 4 estruturas é 100% aritmética já existente
(`rastrear()`/`avaliar()`), nunca de LLM (CLAUDE.md princípio 5, 30-CONTEXT
"Guardrail não-negociável"). Este plano cria o único lugar do repositório onde
essa ordem é decidida — todos os planos seguintes consomem, nenhum recalcula.
Output: `server/app/opcoes_curadoria.py` (módulo puro, sem rede/banco/LLM/
relógio interno) + guardiões unitários.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-curadoria-ia-melhores-estruturas/30-CONTEXT.md
@CLAUDE.md

NÃO leia `.planning/STATE.md` — não é necessário para este plano.
Baseline da fase medida em 2026-09-13 no commit `ff91fea`:
`bash scripts/executar.sh --testes` sai com código 0 (verde). Qualquer
saída != 0 no fim de uma task é regressão.

<interfaces>
Contratos que este módulo consome (extraídos do código, não explore o
repositório para redescobri-los):

`server/app/opcoes_motor.py`
- `rastrear(cadeia, filtros) -> list[dict]` — filtros aceitos: `tipo`
  ("call"|"put"), `referencia` (spot, float), `relacao` ("acima" |
  "abaixo_ou_igual"), `criterio` ("min"|"max", default "min"),
  `liquidez_minima` (override: UMA passada nesse piso), `n` (default 1).
  Sem `liquidez_minima` faz DUAS passadas (NEGOCIÁVEL 55, depois DIFÍCIL 30).
  Devolve os contratos ADR-004 ORIGINAIS, intactos, ordenados por strike
  ascendente e cortados em `n`. `[]` em toda porta fechada, nunca exceção.
- `perna_de_acao(ticker, preco, quantidade) -> dict` — levanta ValueError se
  preco/quantidade não positivos.
- `perna_de_contrato(contrato, lado, quantidade=1) -> dict` — levanta
  ValueError se `optionType` não é call/put ou `lastPrice` não é > 0.
- `avaliar(pernas) -> dict` — delega a `opcoes_payoff.perfil_da_estrutura`.

`server/app/opcoes_payoff.perfil_da_estrutura` devolve exatamente estas
chaves: `pernas`, `custo_liquido`, `fluxo` ("debito"|"credito"|"neutro"),
`ganho_maximo`, `perda_maxima`, `ganho_ilimitado`, `perda_ilimitada`,
`breakevens`, `delta_total`, `curva`, `unidade`. Para venda coberta
(ACAO +1 e CALL vendida -1, inclinação direita 0) `ganho_maximo` e
`perda_maxima` são SEMPRE números finitos; `perda_maxima` é
`round(-min(resultados), 4)` quando o pior ponto é negativo, e `0.0` quando
nenhum ponto da curva é negativo.

`server/app/options_quant.py` — `LIQUIDEZ_NEGOCIAVEL = 55`,
`LIQUIDEZ_DIFICIL = 30`, `FAIXA_NEGOCIAVEL = "NEGOCIÁVEL"`,
`liquidity_score(volume, openInterest, bid, ask) -> {"score", "spreadPct", ...}`,
`faixa_de_liquidez(score) -> str`.

`server/app/opcoes_lastreadas.py` — `_PRAZO_MIN_DIAS = 15`,
`_PRAZO_MAX_DIAS = 60`, `_dias_ate(expiration, hoje) -> int|None`,
`_bloco_liquidez(contrato, liq, modo) -> dict` com chaves
`score`/`faixa`/`volume`/`spreadPct`/`aviso`.

`server/app/store.qty_livre(pos) -> int` (server/app/store.py:631).

`server/app/skill_ref.py` — `opcoes_lastreadas_txt(modo, chave, **dados)`
(chave "call_coberta" espera os dados `n`, `ticker`, `strike`,
`premioTotal`, `qtyAcoes`), `num_br(x)`, `PRINCIPIOS`,
`PRINCIPIO_DADOS_SEM_PACOTE`, `DISCLAIMER`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: candidatos_da_posicao — vários strikes, um vencimento, zero rede</name>
  <files>server/app/opcoes_curadoria.py, server/tests/test_opcoes_curadoria.py</files>
  <read_first>
    - server/app/opcoes_motor.py:30-117 (rastrear e _candidato_valido — contrato de filtros e das duas passadas de liquidez)
    - server/app/opcoes_motor.py:127-204 (perna_de_contrato, perna_de_acao, avaliar)
    - server/app/opcoes_lastreadas.py:1-60 (docstring de pureza, _PRAZO_MIN_DIAS/_PRAZO_MAX_DIAS, _dias_ate, _bloco_liquidez)
    - server/app/opcoes_lastreadas.py:220-292 (portas fechadas de propor() e os args EXATOS de rastrear no ramo call_coberta — é este o que se espelha com n maior)
    - server/app/opcoes_lastreadas.py:322-392 (montagem do dict de proposta: nomes de campo a espelhar)
    - server/tests/test_opcoes_motor.py (primeiras ~60 linhas: como as fixtures de cadeia ADR-004 são montadas nos testes deste domínio — reuse a mesma forma, não invente outra)
  </read_first>
  <behavior>
    - Cadeia com 6 calls líquidas acima do spot, `n=5` → devolve 5 candidatos, todos com `expiration` igual ao `chain["expiration"]` (um vencimento só) e `strike` distintos, em ordem de strike ascendente.
    - Cadeia com `providerStatus` != "ok" → `[]`.
    - `posicao` com `qty_livre` 99 → `[]`; com 100 → `contratos == 1`, `qtyAcoes == 100`.
    - `spot` None / 0 / negativo / bool → `[]` (nunca TypeError).
    - `expiration` a 10 dias ou a 90 dias de `hoje` → `[]` (fora de 15..60).
    - Contrato com score de liquidez 40 (DIFÍCIL) e nenhum NEGOCIÁVEL na cadeia → `[]`: o piso `LIQUIDEZ_NEGOCIAVEL` é EXPLÍCITO, então a segunda passada DIFÍCIL de `rastrear` não roda (D5).
    - Candidato cuja estrutura devolve `perda_maxima` 0.0 → descartado da lista; nenhum candidato devolvido tem `razao` infinita, NaN ou None.
    - Cada candidato devolvido tem `razao == round(premioUnitario / estrutura["perda_maxima"], 6)`.
    - `manchete` de cada candidato é byte-igual a `skill_ref.opcoes_lastreadas_txt(modo, "call_coberta", n=..., ticker=..., strike=..., premioTotal=..., qtyAcoes=...)` — o teste compara com a chamada direta, não com um literal.
    - Nenhuma chamada de rede: o teste passa um dict de cadeia em memória e o módulo não importa `options_provider`, `httpx`, `candle_provider` nem `db`.
  </behavior>
  <action>
Crie `server/app/opcoes_curadoria.py` com docstring declarando: módulo PURO
(sem rede, sem banco, sem LLM, sem relógio interno — `hoje` entra por
argumento), mesma disciplina de `opcoes_motor.py`/`opcoes_lastreadas.py`; e
declarando os dois cercos da Fase 30: universo SÓ venda coberta (D1 —
put/collar não entram) e vários STRIKES dentro do vencimento ÚNICO que a
cadeia já traz (D3 — jamais múltiplos vencimentos, porque isso exigiria uma
chamada a `options_provider.get_options` por vencimento e destruiria a
propriedade de custo de rede zero desta fase).

Constantes de módulo: `TOPO = 4`, `STRIKES_POR_POSICAO = 5`,
`PISO_LIQUIDEZ = LIQUIDEZ_NEGOCIAVEL` (importado de `.options_quant`, nunca
o literal 55).

Implemente `candidatos_da_posicao(underlying, chain, spot, posicao, modo,
hoje, *, n=STRIKES_POR_POSICAO) -> list[dict]`. Devolve SEMPRE lista, `[]`
em toda porta fechada, nunca `None` e nunca exceção (mesma postura de
`rastrear`). Portas fechadas, na ordem, espelhando `opcoes_lastreadas.propor`
(opcoes_lastreadas.py:220-237 e 260-262): `chain` não dict ou
`chain.get("providerStatus") != "ok"`; `posicao` não dict ou
`store.qty_livre(posicao) < 100`; `spot` não numérico, bool, ou <= 0; `dias
= _dias_ate(chain.get("expiration"), hoje)` None ou fora de
`_PRAZO_MIN_DIAS..._PRAZO_MAX_DIAS`. Importe `_dias_ate`,
`_PRAZO_MIN_DIAS`, `_PRAZO_MAX_DIAS` e `_bloco_liquidez` de
`.opcoes_lastreadas` — NÃO reimplemente nenhum deles (duas cópias do prazo
elegível divergem na primeira manutenção).

Deliberadamente NÃO há porta de setup/plano técnico aqui, ao contrário de
`propor()` (que exige `decisao`/`lado` e devolve `sem_setup`): D1 define
elegibilidade como "comprado, com lote >= 100 ações livres", e só. Escreva
esse porquê em comentário citando D1, com a consequência nomeada — uma
posição que o motor técnico lê como alta PODE aparecer neste ranking e não
apareceria na proposta única da mesma tela. Não "corrija" isso adicionando
um gate técnico: é decisão de produto fechada, não descuido.

Seleção dos candidatos: UMA chamada
`opcoes_motor.rastrear(chain, {"tipo": "call", "referencia": spot,
"relacao": "acima", "criterio": "min", "n": n, "liquidez_minima":
PISO_LIQUIDEZ})`. O `liquidez_minima` EXPLÍCITO é o ponto de D5 e precisa de
comentário: sem ele `rastrear` faz a segunda passada em `LIQUIDEZ_DIFICIL`
(opcoes_motor.py:104-109) e um contrato DIFÍCIL entraria num ranking que o
usuário vai ler como "as 4 melhores para gerar receita" — o piso desta fase
é NEGOCIÁVEL e só.

Para cada contrato selecionado, monte as pernas com
`opcoes_motor.perna_de_acao(underlying, spot, quantidade=1)` e
`opcoes_motor.perna_de_contrato(contrato, "venda", quantidade=1)` (a perna
ACAO é obrigatória: sem ela `perfil_da_estrutura` vê só a call vendida,
inclinação -1, e classifica uma venda COBERTA como perda ilimitada), chame
`estrutura = opcoes_motor.avaliar(pernas)` e envolva o par
`perna_de_contrato`/`avaliar` em `try/except ValueError: continue` — um
contrato defeituoso individual não pode derrubar a curadoria da carteira
inteira.

Descarte o candidato quando `estrutura["perda_maxima"]` é None ou <= 0, com
comentário: razão sobre zero não é "razão infinita", é razão indefinida, e
publicar `inf`/`None` como se fosse mérito violaria o princípio 4 do
CLAUDE.md. Caso contrário `razao = round(premio_unitario /
estrutura["perda_maxima"], 6)`.

Campos EXATOS de cada candidato (espelhando os nomes já publicados em
`opcoes_lastreadas.propor`, opcoes_lastreadas.py:364-392, para o front não
aprender um segundo vocabulário): `tipo` fixo em `"call_coberta"`,
`ticker`, `contractSymbol`, `optionType`, `strike`, `expiration`
(de `chain`), `diasParaVencimento`, `contratos` (`qty_livre // 100`),
`qtyAcoes` (`contratos * 100`), `premioUnitario` (`round(premio, 2)`),
`premioTotal` (`round(premio * qtyAcoes, 2)`), `liquidez`
(`_bloco_liquidez(contrato, liq, modo)`), `estrutura`, `razao`, `manchete`,
`didatica` (a mesma frase com modo `"educacional"`, igual `propor`),
`precoObjeto` (`round(float(spot), 2)`).

`manchete`/`didatica` vêm SÓ de `skill_ref.opcoes_lastreadas_txt(modo,
"call_coberta", ...)`, com os números passando por `skill_ref.num_br` como
em `propor` — este módulo NÃO compõe frase nova (guardrail CVM).

Crie `server/tests/test_opcoes_curadoria.py` cobrindo todos os casos de
`<behavior>`, no estilo de `server/tests/test_opcoes_motor.py`.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes > /tmp/f30-01-t1.txt 2>&1; echo "exit=$?"; tail -5 /tmp/f30-01-t1.txt</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/executar.sh --testes` sai com código 0 (baseline de `ff91fea` preservada).
    - `python -c "import ast,sys; src=open('server/app/opcoes_curadoria.py').read(); assert 'options_provider' not in src and 'httpx' not in src and 'candle_provider' not in src"` passa — o módulo é puro por inspeção, não só por intenção.
    - `grep -c 'LIQUIDEZ_NEGOCIAVEL' server/app/opcoes_curadoria.py` >= 1 e `grep -c '\"liquidez_minima\"' server/app/opcoes_curadoria.py` >= 1.
    - Nenhuma ocorrência de `"put"` como `tipo` de filtro de `rastrear` no arquivo (`grep -v '^ *#' server/app/opcoes_curadoria.py | grep -c '"tipo": "put"'` == 0).
    - Todos os testes novos de `server/tests/test_opcoes_curadoria.py` passam individualmente: `server/.venv/bin/python -m pytest server/tests/test_opcoes_curadoria.py -q`.
  </acceptance_criteria>
  <done>Enumeração de N strikes de venda coberta sobre uma cadeia em memória, com piso NEGOCIÁVEL explícito, razão calculada e candidatos indefinidos descartados.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: rankear + exigir_ranking — ordem total, determinística, auditável</name>
  <files>server/app/opcoes_curadoria.py, server/tests/test_opcoes_curadoria.py</files>
  <read_first>
    - server/app/opcoes_curadoria.py (o arquivo inteiro, como ficou na Task 1)
    - server/app/opcoes_lastreadas.py:393-402 (precedente da regra "proposta é SEMPRE candidatos[0], nunca recalculado à parte" — a mesma disciplina vale aqui)
  </read_first>
  <behavior>
    - `rankear` com 9 candidatos devolve exatamente 4 (TOPO), em ordem decrescente de `razao`.
    - Embaralhar a lista de entrada (qualquer permutação) produz saída IDÊNTICA — o teste roda >= 20 permutações (random com seed fixa) e compara a sequência de `contractSymbol`.
    - Empate de `razao`: desempata por `premioUnitario` decrescente; empate nos dois: por `contractSymbol` ascendente. O teste monta um empate triplo exato e fixa a ordem esperada.
    - Cada item devolvido tem `posicaoNoRanking` 1..N, na ordem, e `rankear` não altera nenhum outro campo do candidato (teste compara os dicts ignorando só a chave nova).
    - `rankear([])` → `[]`.
    - `exigir_ranking` aceita a saída de `rankear`; levanta `ValueError` para: lista com 5+ itens; lista com `razao` crescente; `posicaoNoRanking` fora de 1..N ou fora de ordem; item sem `razao`; entrada que não é lista.
    - `narrativa_user(top, modo)` chama `exigir_ranking` ANTES de montar texto: passar uma lista fora de ordem levanta ValueError, não devolve prompt.
    - `narrativa_user` inclui o `contractSymbol` e a `razao` de cada item na ordem recebida, e o texto contém a instrução literal de não reordenar.
    - `narrativa_system("operador")` e `narrativa_system("estudo")` contêm `skill_ref.PRINCIPIOS` e `skill_ref.DISCLAIMER` (comparação por `in`, contra as constantes, não contra literais).
  </behavior>
  <action>
Em `server/app/opcoes_curadoria.py`, acrescente:

`rankear(candidatos, *, topo=TOPO) -> list[dict]` — ordena com chave
`(-c["razao"], -c["premioUnitario"], c["contractSymbol"] or "")`, corta em
`topo` e devolve cópias rasas (`{**c, "posicaoNoRanking": i}`) com `i`
1-based. Comentário obrigatório explicando por que o desempate termina em
`contractSymbol`: sem um último critério TOTAL, dois candidatos de razão e
prêmio iguais ficariam na ordem de chegada, e a ordem de chegada depende da
ordem das posições da carteira — a mesma carteira produziria rankings
diferentes entre requisições, e "as 4 melhores" deixaria de ser uma
afirmação verificável. Declare também, no mesmo comentário, que a chave de
ordenação é a ÚNICA fonte da ordem: sem peso configurável, sem entrada de
usuário, sem qualquer campo vindo de LLM (D2 + princípio 5 do CLAUDE.md).

`exigir_ranking(top) -> None` — validação que a camada de narração chama
antes de qualquer coisa. Levanta `ValueError` com mensagem nomeando o
defeito quando: `top` não é lista; `len(top) > TOPO`; algum item não é dict
ou não tem `razao` numérica; `posicaoNoRanking` não é exatamente
`1..len(top)` na ordem; a sequência de `razao` não é monotonicamente
não-crescente. Docstring: esta função existe para que a etapa de IA seja
INCAPAZ de receber um pool não rankeado — o guardrail vira comportamento do
código, não promessa de comentário (30-CONTEXT, guardrail não-negociável).

`narrativa_system(modo) -> str` — prefixo estável por modo, montado de
`skill_ref.PRINCIPIOS`, `skill_ref.PRINCIPIO_DADOS_SEM_PACOTE` e
`skill_ref.DISCLAIMER` (mesmo padrão de `assistente.system_prefixo`,
server/app/assistente.py:189-196), mais as regras desta superfície: a ordem
das estruturas já foi decidida pelo motor determinístico; descreva na ordem
recebida; é proibido reordenar, sugerir outra ordem, acrescentar estrutura
que não está na lista, inventar número que não está na lista, prometer
lucro ou tratar o texto como recomendação. Nada variável (timestamp, id,
ticker) dentro do prefixo — variável invalida o cache de prompt em silêncio.

`narrativa_user(top, modo) -> str` — PRIMEIRA linha do corpo é
`exigir_ranking(top)`. Depois serializa os 4 itens NA ORDEM recebida, cada
um com `posicaoNoRanking`, `ticker`, `contractSymbol`, `strike`,
`diasParaVencimento`, `premioUnitario`, `premioTotal`, `razao`,
`estrutura["ganho_maximo"]`, `estrutura["perda_maxima"]`,
`estrutura["breakevens"]`, `liquidez["faixa"]` — e nada além disso (não
mande `curva` inteira: é ruído caro em tokens). Fecha com a instrução de
escrever um parágrafo curto por estrutura, na ordem dada.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes > /tmp/f30-01-t2.txt 2>&1; echo "exit=$?"; tail -5 /tmp/f30-01-t2.txt</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/executar.sh --testes` sai com código 0.
    - Teste de permutação presente e verde: `server/.venv/bin/python -m pytest server/tests/test_opcoes_curadoria.py -q -k permuta` reporta >= 1 teste passando.
    - Teste de recusa presente e verde: `-k exigir_ranking` reporta >= 4 casos de `pytest.raises(ValueError)`.
    - `grep -v '^ *#' server/app/opcoes_curadoria.py | grep -c 'def exigir_ranking'` == 1 e `narrativa_user` chama `exigir_ranking` (verificável por `grep -A3 'def narrativa_user' server/app/opcoes_curadoria.py | grep -c exigir_ranking` >= 1).
  </acceptance_criteria>
  <done>A ordem das 4 é uma função pura e total dos números do motor; a porta de entrada da narração recusa qualquer lista que não seja o top já ordenado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cadeia do provider → motor de curadoria | dado de mercado externo (mydata/Yahoo) entra como dict não confiável: campos podem faltar, vir `None` ou não numéricos |
| motor determinístico → camada de IA | fronteira do princípio 5: só o top já ordenado atravessa, nunca um pool |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | `rankear` | mitigate | chave de ordenação é `(-razao, -premioUnitario, contractSymbol)` e nada mais: sem peso configurável, sem parâmetro de usuário, sem campo de LLM. Teste de permutação (>=20 embaralhamentos) prova que a saída não depende da ordem de entrada |
| T-30-02 | Elevation of Privilege | `narrativa_user` / `exigir_ranking` | mitigate | `exigir_ranking` levanta ValueError para lista com >TOPO itens, `razao` crescente ou `posicaoNoRanking` fora de ordem; `narrativa_user` a chama na primeira linha. A IA fica estruturalmente incapaz de receber pool não rankeado |
| T-30-03 | Information Disclosure | `narrativa_user` | mitigate | o prompt carrega uma lista fechada de campos (sem `curva`, sem dado de conta, sem saldo, sem id de sessão) — enumerada no código, não "tudo que houver no dict" |
| T-30-04 | Spoofing | dado de mercado degradado | mitigate | `perda_maxima` None/<=0 descarta o candidato em vez de publicar razão infinita; `spot`/`lastPrice` não numéricos fecham a porta com `[]` (princípio 4 do CLAUDE.md: nunca inventar valor) |
| T-30-05 | Denial of Service | rede do provider | accept | este módulo é puro e não toca rede: `STRIKES_POR_POSICAO` multiplica só aritmética em memória sobre uma cadeia JÁ buscada (D3). O risco de rede pertence ao Plano 02 |
| T-30-SC | Tampering | npm/pip/cargo installs | mitigate | nenhuma dependência nova neste plano (só stdlib + módulos internos). Se alguma instalação aparecer, para e exige checkpoint humano de legitimidade |
</threat_model>

<verification>
1. `bash scripts/executar.sh --testes` → exit 0 (baseline `ff91fea` verde).
2. `server/.venv/bin/python -m pytest server/tests/test_opcoes_curadoria.py -q` → todos verdes.
3. Pureza por inspeção: `server/app/opcoes_curadoria.py` não menciona
   `options_provider`, `httpx`, `candle_provider`, `db`, `llm`, `mcp`.
4. `grep -v '^ *#' server/app/opcoes_curadoria.py | grep -c 'get_options'` == 0.
</verification>

<success_criteria>
- SC-1 (parcial): a ordem das 4 é decidida por `rankear`, função pura e total dos números de `avaliar()`; nenhuma LLM participa.
- SC-4: universo só venda coberta — nenhum filtro `tipo: "put"`, nenhum collar, nenhuma referência a MCP no módulo.
- SC-5: suíte canônica exit 0 ao fim de cada task.
</success_criteria>

<output>
Create `.planning/phases/30-curadoria-ia-melhores-estruturas/30-01-SUMMARY.md` when done
</output>

<source_audit>
## Multi-Source Coverage Audit (Fase 30 — os 4 planos)

Auditoria feita antes de finalizar o conjunto. Nenhum item MISSING.

### GOAL (ROADMAP.md, Phase 30)
| Item do goal | Plano | Status |
|---|---|---|
| Bloco novo em Posições, ao lado de "Oportunidades de opções" | 30-04 | COVERED |
| 4 melhores propostas de venda coberta da carteira INTEIRA | 30-01 (rankear/TOPO) + 30-02 (varredura cross-posição) | COVERED |
| Ordenadas por razão prêmio/perda máxima | 30-01 (chave de ordenação) | COVERED |
| Escolha 100% determinística via rastrear()/avaliar() | 30-01 | COVERED |
| IA só narra o que o número decidiu | 30-03 (exigir_ranking + narrativa_*) + 30-04 (bloco rotulado e separado) | COVERED |
| Custo ZERO do serviço MCP por arquitetura | 30-02 (guardião estático de MCP) | COVERED |

### REQ (Success Criteria do ROADMAP, citados como SC-1..SC-5)
| ID | Plano(s) | Status |
|---|---|---|
| SC-1 (as 4 são as de maior razão; nenhuma LLM decide ou reordena) | 30-01, 30-02, 30-03, 30-04 | COVERED |
| SC-2 (nenhuma chamada nova ao MCP; uma busca por posição elegível) | 30-02 | COVERED |
| SC-3 (narração na MESMA cota de /api/analyze) | 30-03, 30-04 (verificação ao vivo no portal admin) | COVERED |
| SC-4 (put/collar/MCP fora do ranking) | 30-01 (universo), 30-02 (rota), 30-04 (copy/tela) | COVERED |
| SC-5 (suíte canônica sem regressão + vite build verde) | todos (verify de cada task) | COVERED |

### RESEARCH
Sem `30-RESEARCH.md` nesta fase (verificado: o diretório da fase só tem
`30-CONTEXT.md`). Os achados de código que substituiriam a pesquisa estão no
próprio CONTEXT ("O que já existe hoje", achados 1-4) e foram distribuídos:
achado 1 (`filtros["n"]`) → 30-01; achado 2 (`avaliar` delega a
`perfil_da_estrutura`) → 30-01; achado 3 (custo MCP zero por arquitetura) →
30-02; achado 4 (não estender a lista `candidatos` da Fase 19/28) → 30-04
(bloco NOVO, irmão, não fork da tira existente). Nenhum achado sem plano.

### CONTEXT (decisões D1-D6)
| ID | Decisão | Plano(s) | Status |
|---|---|---|---|
| D1 | Só venda coberta, toda a carteira elegível (comprado, lote livre >= 100) | 30-01 (universo + elegibilidade), 30-02 (partição antes do fetch) | COVERED |
| D2 | Ranking prêmio ÷ perda máxima, com piso de liquidez | 30-01 (`razao` + chave de ordenação) | COVERED |
| D3 | Vários STRIKES dentro do vencimento único já buscado | 30-01 (`n` sobre a cadeia em memória), 30-02 (`get_options` sem `expiration`, contagem exata testada) | COVERED |
| D4 | Bloco em Posições/Portfólio, irmão de `OportunidadesOpcoes` | 30-04 (render após App.jsx:4515, mesmo guard) | COVERED |
| D5 | Piso `LIQUIDEZ_NEGOCIAVEL` | 30-01 (`liquidez_minima` EXPLÍCITO, desliga a passada DIFÍCIL) | COVERED |
| D6 | Mesma cota de `/api/analyze` via `_gate_analise` | 30-03 (gate na rota, sem teto paralelo), 30-04 (narração só por toque) | COVERED |

### Exclusões declaradas (não são lacunas)
Put de proteção e collar no ranking (D1); estruturas via MCP/b-mcp (D1);
múltiplos vencimentos por posição (D3); estender a lista `candidatos` da
Fase 19/28; migrar `OpcoesCamada` para "Operar"; polish de UX pós-uso real
(Fase 31); publicação (`bump.sh` + `publicar-web.sh` + `cap sync`) — passo
humano separado, nomeado como pendência no 30-04.
</source_audit>
