---
phase: 30-curadoria-ia-melhores-estruturas
plan: 03
type: execute
wave: 3
depends_on: ["30-02"]
files_modified:
  - server/app/curadoria_narrativa.py
  - server/app/main.py
  - server/tests/test_opcoes_curadoria_narrativa.py
autonomous: true
requirements: [SC-1, SC-3, SC-5]
must_haves:
  truths:
    - "A narração da IA consome a MESMA cota de /api/analyze, pelo mesmo _gate_analise, sem orçamento paralelo"
    - "A função de narração recebe SÓ o top-4 já ordenado e recusa qualquer outra entrada com ValueError"
    - "O cliente não consegue enviar candidatos: a rota recomputa o ranking no servidor e ignora qualquer lista do corpo"
    - "Cota gasta apenas em narração bem-sucedida; top vazio não chama LLM e não gasta cota"
  artifacts:
    - path: "server/app/curadoria_narrativa.py"
      provides: "narrar() — camada fina de LLM sobre o top já rankeado"
      contains: "exigir_ranking"
    - path: "server/app/main.py"
      provides: "POST /api/options/curadoria/narrativa com _gate_analise"
      contains: "/api/options/curadoria/narrativa"
    - path: "server/tests/test_opcoes_curadoria_narrativa.py"
      provides: "guardiões do gate de cota e da fronteira determinístico→IA"
      contains: "def test_"
  key_links:
    - from: "server/app/main.py"
      to: "server/app/curadoria_narrativa.py"
      via: "narrar(top já rankeado)"
      pattern: "curadoria_narrativa\\.narrar"
    - from: "server/app/main.py"
      to: "_gate_analise"
      via: "mesma cota de /api/analyze (D6)"
      pattern: "_gate_analise\\(scope, config\\)"
    - from: "server/app/curadoria_narrativa.py"
      to: "server/app/opcoes_curadoria.py"
      via: "exigir_ranking + narrativa_system + narrativa_user"
      pattern: "opcoes_curadoria\\.(exigir_ranking|narrativa_)"
---

<objective>
A etapa de IA: um parágrafo por estrutura, sobre as 4 que o número JÁ
escolheu. A IA nunca vê um pool, nunca reordena, nunca acrescenta estrutura —
e gasta a mesma cota mensal de `/api/analyze` (D6), não um orçamento paralelo.

Purpose: SC-3 e a metade "IA só narra" de SC-1. O risco real desta fase é o
usuário ler o bloco como "a IA recomenda isto para ganhar dinheiro"; o
contrapeso é código que torna a reordenação IMPOSSÍVEL, não um comentário
dizendo que ela não acontece.
Output: `server/app/curadoria_narrativa.py` (camada fina) + rota
`POST /api/options/curadoria/narrativa` + guardiões.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-curadoria-ia-melhores-estruturas/30-CONTEXT.md
@.planning/phases/30-curadoria-ia-melhores-estruturas/30-02-SUMMARY.md
@CLAUDE.md
@.claude/skills/didatica-boris/SKILL.md

NÃO leia `.planning/STATE.md`. Baseline: `bash scripts/executar.sh --testes`
exit 0 em `ff91fea`. NÃO leia `server/app/main.py` inteiro.

<interfaces>
`server/app/opcoes_curadoria.py` (Plano 01):
- `exigir_ranking(top) -> None` — ValueError para lista >TOPO, `razao`
  crescente, `posicaoNoRanking` fora de ordem, item sem `razao`.
- `narrativa_system(modo) -> str`, `narrativa_user(top, modo) -> str`
  (este último chama `exigir_ranking` na primeira linha).

`server/app/main.py` (Plano 02): `async def _curadoria_top(scope, modo)`
→ `(top, meta)`.

`server/app/main.py:678` `_gate_analise(scope, config, custo=1)` →
`(config_efetiva, consume)`. Precedência: BYOK (`llm.resolve_key`) →
plano mensal (`plan.can_analyze` com contagem real de `metering`) →
metering diário dentro de `_ai_apply_managed`. Levanta `HTTPException(402)`
quando a cota acabou. `consume()` é chamado SÓ no sucesso.

`server/app/llm.py`:
- `resolve_key(config) -> str`
- `collect_usage()` — context manager que acumula os usos
- `_call_llm(config, key, system, user, max_tokens) -> str` (texto, não JSON
  do provedor; já aplica `_system_cacheavel` por dentro). Precedente de uso
  fora de `llm.py`: `server/app/assistente.py:320-321`.
- `public_error(exc) -> dict`

`server/app/ai_activity.registrar_uso(conn, *, scope, ticker, tipo, usos)`
(ai_activity.py:105).
`server/app/kpi.normalize_markdown(texto)` — reduz a saída ao dialeto que o
front renderiza.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: curadoria_narrativa.narrar — a IA só recebe o top ordenado</name>
  <files>server/app/curadoria_narrativa.py</files>
  <read_first>
    - server/app/assistente.py:280-349 (corpo de `responder` — o precedente EXATO de camada fina de LLM: collect_usage, _call_llm, registrar_uso em try/except, normalize_markdown; e `system_prefixo` em assistente.py:189-196)
    - server/app/opcoes_curadoria.py (arquivo inteiro, do Plano 01)
    - server/app/ai_activity.py:100-125 (assinatura de registrar_uso)
  </read_first>
  <behavior>
    - `narrar` com o top-4 válido e um `_call_llm` fake → devolve `{"texto": <markdown normalizado>}` e registra uso com `tipo="Curadoria"`.
    - `narrar` com lista de 5 itens → `ValueError` ANTES de qualquer chamada ao LLM (o fake de `_call_llm` registra 0 chamadas).
    - `narrar` com `razao` crescente (top fora de ordem) → `ValueError`, 0 chamadas ao LLM.
    - `narrar` com `top=[]` → `ValueError` (a decisão de não chamar a IA quando não há estrutura pertence à rota, e a camada não aceita entrada vazia disfarçada de válida).
    - `registrar_uso` levantando exceção NÃO derruba a resposta (contabilidade nunca quebra a narração — mesmo precedente de `assistente.responder`).
    - O módulo não importa `opcoes_motor`, não chama `candidatos_da_posicao` nem `rankear`, e não toca `options_provider` — verificado por inspeção de fonte.
  </behavior>
  <action>
Crie `server/app/curadoria_narrativa.py`, camada FINA espelhando
`assistente.responder` (server/app/assistente.py:280-342).

Docstring do módulo declarando a fronteira: este módulo é o ÚNICO ponto onde
a IA toca a curadoria, e ele recebe o top-4 JÁ ORDENADO por parâmetro.
Escreva, nomeando: é PROIBIDO este módulo importar `opcoes_motor`, chamar
`opcoes_curadoria.candidatos_da_posicao`/`rankear`, ou aceitar um pool — se
uma manutenção futura precisar de mais candidatos, quem os ordena é
`rankear` no chamador, nunca aqui. O motivo é o guardrail não-negociável da
fase (30-CONTEXT) e o princípio 5 do CLAUDE.md.

Constantes: `TIPO = "Curadoria"` (o rótulo do ledger de `ai_activity`) e
`MAX_TOKENS = 900` (4 parágrafos curtos; comentário dizendo por que este
número e não o do assistente).

`async def narrar(conn, config, scope, modo, top) -> dict`:
1. PRIMEIRA linha do corpo: `opcoes_curadoria.exigir_ranking(top)`. Segunda
   guarda: `if not top: raise ValueError(...)`.
2. `system = opcoes_curadoria.narrativa_system(modo)`,
   `user = opcoes_curadoria.narrativa_user(top, modo)` — o prompt inteiro
   nasce no módulo PURO, nunca é composto aqui (é o que faz o prompt
   testável sem rede).
3. `key = llm.resolve_key(config)`; dentro de `with llm.collect_usage() as
   usos:` chame `await llm._call_llm(config, key, system, user, MAX_TOKENS)`.
   Comentário citando o precedente `assistente.py:320-321` para o uso de
   `_call_llm` fora de `llm.py`.
4. `ai_activity.registrar_uso(conn, scope=scope, ticker="-", tipo=TIPO,
   usos=usos)` dentro de `try/except Exception` com `print` — contabilidade
   nunca derruba a resposta. `ticker="-"` porque a narração é
   cross-posição: não pertence a um ativo (mesmo sentinela que a rota de
   análise usa quando não há ticker).
5. Devolva `{"texto": kpi.normalize_markdown((texto or "").strip()),
   "estruturas": [it["contractSymbol"] for it in top]}` — a lista de
   símbolos NA ORDEM narrada viaja na resposta de propósito: é o que permite
   ao cliente (e ao teste) provar que o texto fala das MESMAS 4 estruturas
   que a tela mostra, na mesma ordem.

Não implemente teto de custo em R$ próprio (o `teto_dia_brl` do assistente).
D6 é explícito: a cota é a de `/api/analyze`, pelo `_gate_analise` — que
mora na rota, Task 2. Um segundo freio aqui seria o orçamento paralelo que
D6 proíbe; escreva isso em comentário para a próxima manutenção não
"melhorar" acrescentando um.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes > /tmp/f30-03-t1.txt 2>&1; echo "exit=$?"; tail -5 /tmp/f30-03-t1.txt</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/executar.sh --testes` exit 0.
    - `grep -A4 'async def narrar' server/app/curadoria_narrativa.py | grep -c 'exigir_ranking'` >= 1 — a validação é a primeira coisa do corpo.
    - `grep -v '^ *#' server/app/curadoria_narrativa.py | grep -cE 'opcoes_motor|candidatos_da_posicao|rankear|options_provider|get_options'` == 0.
    - `grep -v '^ *#' server/app/curadoria_narrativa.py | grep -c 'teto_dia_brl'` == 0 — nenhum orçamento paralelo (D6).
    - `grep -c 'metering' server/app/curadoria_narrativa.py` == 0 — a cota é decidida na rota, não aqui.
  </acceptance_criteria>
  <done>Existe exatamente uma porta de entrada da IA na curadoria, e ela recusa qualquer coisa que não seja o top-4 ordenado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /api/options/curadoria/narrativa sob _gate_analise</name>
  <files>server/app/main.py, server/tests/test_opcoes_curadoria_narrativa.py</files>
  <read_first>
    - server/app/main.py:2386-2410 (call site de `_gate_analise` na rota `/api/technical/analyze/{ticker}` — o template de fiação BYOK→plano→metering)
    - server/app/main.py:2660-2675 (o par `ai_activity.registrar_uso` + `_consume_ai()` "conta a cota só no sucesso")
    - server/app/main.py:678-720 (docstring e precedência de `_gate_analise`)
    - server/app/main.py (só o bloco `_curadoria_top`/`options_curadoria` criado no Plano 02 — localize com `grep -n curadoria server/app/main.py`)
    - server/tests/test_plan_gate_byok.py (como os testes deste repositório exercitam o gate de cota: os três degraus da precedência)
  </read_first>
  <behavior>
    - POST com cota disponível e `_call_llm` fake → 200 com `texto` preenchido, `top` presente, e `consume` chamado EXATAMENTE uma vez.
    - `_gate_analise` levantando 402 → a rota devolve 402 (a mensagem de cota do plano chega ao cliente) e `_call_llm` NÃO é chamado.
    - Carteira sem estrutura elegível (`top == []`) → 200 com `texto: None` e `motivo: "sem_estrutura"`, `_call_llm` NÃO chamado e `consume` NÃO chamado (não se cobra cota por não ter o que narrar).
    - `_call_llm` levantando → 502 com `llm.public_error`, e `consume` NÃO chamado.
    - Corpo da requisição contendo `{"top": [...adulterado...]}` ou `{"candidatos": [...]}` → IGNORADO: a resposta `top` é o que o servidor recomputou, e `estruturas` (da narração) casa com ele. Teste envia um `top` de 9 itens em ordem invertida e afirma que a resposta tem <= 4 itens na ordem do motor.
    - `estruturas` da resposta é igual, na ordem, a `[it["contractSymbol"] for it in resposta["top"]]`.
    - Guardião estático: o corpo da rota lê do body SÓ `config` — nenhuma leitura de `body.get("top")`/`body.get("candidatos")`/`body.get("estruturas")` existe no arquivo.
  </behavior>
  <action>
Em `server/app/main.py`, imediatamente após a rota `options_curadoria` do
Plano 02, acrescente
`@app.post("/api/options/curadoria/narrativa")` →
`async def options_curadoria_narrativa(body: dict = Body(default={}), scope:
Optional[str] = Depends(current_scope))`.

Docstring declarando as três decisões e o porquê de cada uma:
1. O corpo aceita SOMENTE `config` (a config do handset, mesmo padrão de
   `/api/technical/analyze`). A rota NÃO aceita estruturas por parâmetro: ela
   RECOMPUTA o ranking chamando `_curadoria_top` e narra o que o servidor
   ordenou. Escreva a consequência: se o cliente pudesse mandar a lista, um
   cliente adulterado (ou só desatualizado) faria a IA descrever uma ordem
   que o motor nunca produziu, e a promessa "a escolha é determinística"
   viraria uma promessa sobre o cliente, não sobre o sistema.
2. A cota é a MESMA de `/api/analyze`, pelo MESMO `_gate_analise` (D6) —
   sem orçamento paralelo, sem gate novo.
3. Custo de rede: a recomputação reusa o cache de 300 s do
   `options_provider`, então na prática ela não dobra o consumo do provider
   quando o usuário narra logo após ver o bloco. Uma chamada por posição
   elegível segue sendo o teto (D3).

Corpo, na ordem:
- `config = (body or {}).get("config") or store.get(_conn, "config",
  user_id=scope)`; `modo = (config or {}).get("appMode") or "estudo"`
  (capture `modo` ANTES do gate, que pode recriar a config — mesma armadilha
  documentada em main.py:2394).
- `config, _consume_ai = _gate_analise(scope, config)` SEM try/except de
  402. Comentário: ao contrário de `/api/technical/analyze` (que converte
  402 em `iaIndisponivel` e cai num fallback determinístico), aqui o 402
  PROPAGA — o fallback determinístico desta tela já está na tela: é o
  próprio bloco de `GET /api/options/curadoria`, que não gasta cota. Mascarar
  o 402 num 200 sem texto esconderia do usuário que a cota acabou.
- `top, meta = await _curadoria_top(scope, modo)`.
- `if not top:` devolva `{"texto": None, "motivo": "sem_estrutura", "top":
  [], "modo": modo, "at": now_str(), **meta}` SEM chamar LLM e SEM chamar
  `_consume_ai()`.
- `try: resultado = await curadoria_narrativa.narrar(_conn, config, scope,
  modo, top)` / `except HTTPException: raise` / `except Exception as e:
  raise HTTPException(502, llm.public_error(e))` — mesmo tratamento do
  precedente em main.py:2381-2382.
- `_consume_ai()` DEPOIS do sucesso, com o comentário "conta a cota só no
  sucesso" que é a convenção do arquivo.
- Devolva `{"texto": resultado["texto"], "estruturas":
  resultado["estruturas"], "top": top, "motivo": "narrado", "modo": modo,
  "fonte": "ia-sobre-ranking-deterministico", "at": now_str(), **meta}`.

Crie `server/tests/test_opcoes_curadoria_narrativa.py` cobrindo todos os
casos de `<behavior>`, no estilo de `server/tests/test_plan_gate_byok.py`
para a parte de gate e de `test_opcoes_curadoria_rota.py` (Plano 02) para a
montagem de carteira/cadeia. Substitua `llm._call_llm` por um fake que conta
chamadas e devolve texto fixo; substitua/instrumente o `consume` devolvido
pelo gate para contar chamadas.

Os dois guardiões que o CONTEXT pede explicitamente, cada um com comentário
nomeando o que defende:
- `test_ia_nunca_recebe_pool_nao_rankeado`: captura os argumentos com que
  `curadoria_narrativa.narrar` foi chamada e afirma que o 5º argumento é
  EXATAMENTE o objeto devolvido por `rankear` (mesma sequência de
  `contractSymbol`, <= TOPO itens, `razao` não-crescente); e afirma que
  passar uma lista de 9 itens desordenados direto a `narrar` levanta
  ValueError.
- `test_corpo_da_requisicao_nao_influencia_a_ordem`: POST com
  `{"top": [...9 itens invertidos...], "candidatos": [...]}` e asserção de
  que a resposta é idêntica à do POST com corpo vazio (mesma sequência de
  `contractSymbol`), mais o guardião estático de que `body.get("top")` não
  existe em `server/app/main.py` (linhas de comentário filtradas antes de
  contar).
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes > /tmp/f30-03-t2.txt 2>&1; echo "exit=$?"; tail -5 /tmp/f30-03-t2.txt</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/executar.sh --testes` exit 0.
    - `server/.venv/bin/python -m pytest server/tests/test_opcoes_curadoria_narrativa.py -q` → >= 8 testes verdes, incluindo os dois nomeados acima.
    - `grep -A30 'def options_curadoria_narrativa' server/app/main.py | grep -c '_gate_analise(scope, config)'` == 1.
    - `grep -v '^ *#' server/app/main.py | grep -cE 'body\.get\("(top|candidatos|estruturas)"\)'` == 0.
    - `grep -A60 'def options_curadoria_narrativa' server/app/main.py | grep -c '_consume_ai()'` == 1 — cota gasta num único ponto, o do sucesso.
    - Nenhum `metering.consume` novo escrito nesta fase (`git diff --stat` não toca `server/app/metering.py`; a cota vem inteira do `_gate_analise` existente).
  </acceptance_criteria>
  <done>A narração existe, gasta a cota de `/api/analyze` uma vez por sucesso, e é estruturalmente incapaz de mudar a ordem que o motor decidiu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente HTTP → rota de narrativa | corpo da requisição é dado NÃO CONFIÁVEL: um handset desatualizado ou adulterado pode mandar qualquer lista |
| rota → `_gate_analise` | fronteira de dinheiro: além dela, a chave do servidor paga tokens |
| motor determinístico → LLM | fronteira do princípio 5: só o top-4 ordenado atravessa |
| LLM → usuário | texto gerado será lido como "a IA recomenda isto para ganhar dinheiro" (risco nomeado no 30-CONTEXT) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-12 | Tampering | corpo da requisição de narrativa | mitigate | a rota lê do body SÓ `config` e recomputa `_curadoria_top` no servidor; guardião estático proíbe `body.get("top"/"candidatos"/"estruturas")` e teste funcional prova que um `top` adulterado de 9 itens não muda a resposta |
| T-30-13 | Elevation of Privilege | LLM reordenando/ampliando a lista | mitigate | `exigir_ranking` na primeira linha de `narrar` e de `narrativa_user`; `narrativa_system` proíbe reordenar/acrescentar/inventar número; `estruturas` volta na resposta para o cliente conferir a ordem narrada contra a ordem exibida |
| T-30-14 | Denial of Service (bolso) | cota de IA | mitigate | `_gate_analise(scope, config)` na entrada (BYOK→plano mensal→metering diário, D6); `_consume_ai()` só no sucesso, num único ponto; `top` vazio não chama LLM nem cobra cota; `MAX_TOKENS = 900` limita a resposta |
| T-30-15 | Repudiation | gasto de IA sem rastro | mitigate | `ai_activity.registrar_uso(tipo="Curadoria")` em toda narração; o rótulo próprio separa este gasto dos demais no portal admin |
| T-30-16 | Information Disclosure | vazamento de estado da conta no prompt | mitigate | o prompt é montado só por `narrativa_user`, com lista FECHADA de campos (Plano 01, T-30-03): sem saldo, sem caixa, sem id de sessão, sem token |
| T-30-17 | Spoofing | texto de IA passando por decisão do motor | mitigate | `fonte: "ia-sobre-ranking-deterministico"` na resposta e `texto` numa chave separada de `top`; a manchete de cada item continua vindo só de `skill_ref` (guardrail CVM) — o Plano 04 garante a separação visual |
| T-30-SC | Tampering | npm/pip/cargo installs | mitigate | nenhuma dependência nova neste plano. Qualquer instalação necessária interrompe a task e exige checkpoint humano de legitimidade de pacote |
</threat_model>

<verification>
1. `bash scripts/executar.sh --testes` → exit 0.
2. `server/.venv/bin/python -m pytest server/tests/test_opcoes_curadoria_narrativa.py server/tests/test_opcoes_curadoria_rota.py server/tests/test_opcoes_curadoria.py -q` → verde.
3. Exercício do endpoint real (memória do repositório): servidor local +
   `curl -s -X POST localhost:8787/api/options/curadoria/narrativa -H
   'content-type: application/json' -d '{}'` → 200 com `texto` (ou 402 com a
   mensagem de cota, se a conta local estiver sem cota — as duas respostas
   são sucesso do teste; 500 não é).
4. Nenhum I/O síncrono no event loop: a chamada ao LLM é `await`.
</verification>

<success_criteria>
- SC-1 (metade "IA só narra"): a IA recebe exclusivamente o top-4 já ordenado; reordenar é impossível por construção, com dois guardiões provando.
- SC-3: a narração passa por `_gate_analise`, a mesma cota de `/api/analyze`; nenhum contador novo, nenhum `metering.consume` novo.
- SC-5: suíte canônica exit 0 ao fim de cada task.
</success_criteria>

<output>
Create `.planning/phases/30-curadoria-ia-melhores-estruturas/30-03-SUMMARY.md` when done
</output>
