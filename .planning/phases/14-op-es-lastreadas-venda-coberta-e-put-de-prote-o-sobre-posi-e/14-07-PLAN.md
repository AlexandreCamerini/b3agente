---
phase: 14-opcoes-lastreadas
plan: 07
type: execute
wave: 6
depends_on: ["14-06"]
files_modified:
  - web/src/App.jsx
  - web/src/copy.js
  - web/tests/test_carteira_lastro_ui.mjs
autonomous: true
requirements: []

must_haves:
  truths:
    - "Na Carteira, a posição que lastreia uma CALL coberta mostra quantas ações estão travadas"
    - "O modal de venda só deixa vender a quantidade livre, e diz por que o resto não está disponível"
    - "Uma call liquidada por vencimento aparece na tela como estado do sistema, sem CTA e sem dramatização"
    - "O Patrimônio Total exibido inclui as pernas lastreadas"
  artifacts:
    - path: "web/src/App.jsx"
      provides: "Badge de trava, limite de venda por quantidade livre, aviso de liquidação forçada, patrimônio com pernas lastreadas"
      contains: "travada"
    - path: "web/tests/test_carteira_lastro_ui.mjs"
      provides: "Guardião da trava e do patrimônio na Carteira"
  key_links:
    - from: "web/src/App.jsx SellModal"
      to: "positions[].qtyTravada"
      via: "limite superior do seletor de quantidade"
      pattern: "qtyTravada"
    - from: "web/src/App.jsx"
      to: "web/src/finance.js portfolioMetrics"
      via: "passagem de optionPositions ao agregado"
      pattern: "portfolioMetrics\\([^)]*optionPositions"
---

<objective>
Fechar o laço na Carteira: a **trava visível** (badge + limite de venda), o
**aviso de liquidação forçada**, e o **Patrimônio Total** passando a exibir o
número que o `finance.js` já sabe calcular desde o plano 14-05.

Purpose: a trava existe no motor desde o plano 14-01; se ela não aparecer na
tela, o usuário descobre a restrição batendo nela — o oposto da transparência
que o produto promete (CLAUDE.md, estados completos).
Output: badge de trava, seletor de venda limitado, banner de liquidação, seis
call sites de `portfolioMetrics` atualizados, guardião de UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-op-es-lastreadas-venda-coberta-e-put-de-prote-o-sobre-posi-e/14-CONTEXT.md
@.planning/phases/14-op-es-lastreadas-venda-coberta-e-put-de-prote-o-sobre-posi-e/14-UI-SPEC.md
@.planning/phases/14-op-es-lastreadas-venda-coberta-e-put-de-prote-o-sobre-posi-e/14-06-SUMMARY.md
@CLAUDE.md

<interfaces>
Estado (planos 14-01/14-02):
- `positions[].qtyTravada` (int, ausente = 0)
- `optionPositions[].side` (`"vendida"`|`"comprada"`) e `optionPositions[].lastro` (`{t, qty}`)
- histórico de liquidação: `{type:"COMPRA", kind:"opcao", acao:"fechar", motivo:"vencimento", origem:"sistema", price:<intrínseco>, pnl}`

Agregado (plano 14-05, fonte `web/src/finance.js`):
`portfolioMetrics(positions, quotes, cash, reservado, optionPositions, optionQuotes)` →
`{posVal, cost, openPnL, openPct, dayVal, patr, cash, reservado, opcoesVal, opcoesPnL}`

UI existente (fonte `web/src/App.jsx`):
- `PosPill({qty})` (linha ~1023) — a forma de pílula que o badge de trava reusa, trocando `T.accent` por `T.negative`
- `SellModal` (linha ~6786): `qty = Math.min(pos.qty, Math.max(100, sellModal.qty))`, `step(dir)` também limitado por `pos.qty`, botão "Vender tudo (N)"
- `A.openSell(t)` (linha ~7382) monta `{t, qty: pos.qty}`
- card de posição na Carteira (linhas ~3760-3830): resumo, compras da posição, botões "Stop/alvo (IA)" e `cp.btnVender`
- seis chamadas de `portfolioMetrics` (linhas 1640, 1802, 3659, 7933, 8106, 8129), todas com quatro argumentos hoje
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trava visível — badge na Carteira e venda limitada à quantidade livre</name>
  <files>web/src/App.jsx, web/src/copy.js</files>
  <read_first>
    - web/src/App.jsx, `PosPill` (linha ~1023) e o card de posição da Carteira (linhas ~3745-3830)
    - web/src/App.jsx, `SellModal` (linhas ~6786-6858) e `A.openSell`/`A.confirmSell` (linhas ~7382-7407)
    - .planning/phases/14-op-es-lastreadas-venda-coberta-e-put-de-prote-o-sobre-posi-e/14-UI-SPEC.md (linha do badge de trava: `"{qty} travada(s) · lastro de CALL"`, cor `T.negative` porque comunica restrição)
    - CLAUDE.md (guardrail: stop/alvo nunca são vetados — a trava de lastro NÃO pode desabilitar os controles de proteção da posição, só a venda)
  </read_first>
  <action>
1) Componente `TravaPill({ qty })` em `web/src/App.jsx`, ao lado de `PosPill`: mesma forma (pílula, `borderRadius 999px`, 10.5px/800), cor `T.negative` sobre `T.negativeTint10`, texto vindo de `cp.badgeTravada(qty)`.

2) `copy.js`, chave nova nos DOIS modos — `badgeTravada(qty)`: Operador `"{qty} travada(s) · lastro de CALL"`; Estudo com o mesmo sentido sem vocabulário de ordem, por exemplo `"{qty} travada(s) · lastro da call coberta"` (o guardião de vocabulário rejeita a palavra "vender"/"comprar", não "call coberta"). Verificar o resultado rodando o guardião, não por inspeção.

3) Renderizar `TravaPill` em duas superfícies, sempre que `pos.qtyTravada > 0`:
   - card de posição da Carteira (junto do resumo "em carteira · N cotas · PM"),
   - `AtivoCard`, ao lado do `PosPill` já existente.

4) `SellModal`: introduzir `livre = pos.qty - (pos.qtyTravada || 0)` e usar `livre` como teto em TODOS os pontos onde hoje se usa `pos.qty`: cálculo de `qty`, `step`, botão "Vender tudo", e a definição de `total` (venda total agora significa "vender tudo o que está livre"). Quando `pos.qtyTravada > 0`, exibir uma linha explicativa em `T.textMuted` dizendo quantas ações estão travadas e que elas voltam a ficar disponíveis quando a call for recomprada ou vencer — texto de `copy.js` (chave `avisoTravaNaVenda(qty)` nos dois modos). Com `livre === 0`, desabilitar o botão de confirmar e mostrar a mesma frase (a recusa do servidor continua existindo como segunda camada; a UI não é a única guarda).

5) `A.openSell(t)`: montar `{t, qty: livre}` em vez de `pos.qty`.

6) NÃO tocar nos botões de stop/alvo da posição travada — guardrail do repositório: proteção nunca é vetada. Registrar isso em comentário de uma linha ao lado da mudança do modal, para que uma leitura futura não "uniformize" o bloqueio.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx vite build &amp;&amp; node tests/test_copy_theme.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function TravaPill" web/src/App.jsx` retorna 1
    - `grep -c "qtyTravada" web/src/App.jsx` retorna 4 ou mais (badge ×2, modal, openSell)
    - `grep -c "badgeTravada" web/src/copy.js` retorna 2 (uma chave por modo)
    - `cd web && npx vite build` conclui sem erro
    - Os botões de stop/alvo continuam habilitados na posição travada: `grep -c "disabled.*qtyTravada" web/src/App.jsx` retorna 0
  </acceptance_criteria>
  <done>A trava aparece antes de o usuário esbarrar nela: badge nas duas superfícies e modal de venda que só oferece o que é vendável, explicando o resto.</done>
</task>

<task type="auto">
  <name>Task 2: Aviso de liquidação forçada e Patrimônio com as pernas lastreadas</name>
  <files>web/src/App.jsx, web/src/copy.js</files>
  <read_first>
    - web/src/App.jsx, as seis chamadas de `portfolioMetrics` (linhas 1640, 1802, 3659, 7933, 8106, 8129) e o que cada tela faz com `m.patr`/`m.openPnL`
    - web/src/finance.js (assinatura estendida do plano 14-05 e os campos `opcoesVal`/`opcoesPnL`)
    - web/src/App.jsx, o bloco de aviso "análise de outro momento" (linhas ~3188-3194) — o formato de banner informativo já usado no card, que o aviso de liquidação segue
    - .planning/phases/14-op-es-lastreadas-venda-coberta-e-put-de-prote-o-sobre-posi-e/14-UI-SPEC.md (o aviso de liquidação: informativo, sem CTA, `T.negative` só no ícone/borda e texto em `T.textSecondary` — nunca o texto inteiro em vermelho)
  </read_first>
  <action>
1) Aviso de liquidação forçada: componente `AvisoLiquidacao({ evento })` renderizado no card da posição de ação cujo `t` bate com o `underlying` da liquidação, quando existir no `data.history` uma entrada com `kind === "opcao"`, `motivo === "vencimento"`, `origem === "sistema"` e `acao === "fechar"` nas últimas entradas. Texto: `cp.avisoLiquidacaoForcada(ticker, valor)`, chave nova nos dois modos com o texto verbatim do UI-SPEC (`"Esta call de {TICKER} venceu dentro do dinheiro e não foi fechada a tempo — liquidada em dinheiro pelo valor intrínseco (R$ {valor}). Sua posição em ações não foi alterada."`), e a variante de vencimento FORA do dinheiro (`valor === 0`) dizendo que a call expirou sem valor, o prêmio integral ficou com quem vendeu e o lastro foi liberado. Sem botão, sem link — é estado, não ação.

2) Patrimônio: atualizar as SEIS chamadas de `portfolioMetrics` para passar `data.optionPositions` como quinto argumento (o sexto, `optionQuotes`, fica `undefined` nesta fase — a marcação cai no prêmio de abertura, decisão registrada no plano 14-05 por causa do orçamento do mydata). Onde a tela discrimina composição do patrimônio (Carteira/Evolução), exibir a linha da perna de opção com `m.opcoesVal`, rotulada em `copy.js` (chave `linhaPatrimonioOpcoes` nos dois modos) e com a ressalva de que a perna está marcada pelo prêmio de abertura — dado sem cotação ao vivo é DITO, nunca apresentado como se fosse ao vivo (CLAUDE.md princípio 3).

3) Conferir que `concentracaoMaxima` continua recebendo só `positions` — o aviso de concentração fala de ativo, não de perna de opção (comentário já existente em `finance.js` diz isso; não alterar).
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx vite build &amp;&amp; node tests/test_finance.mjs &amp;&amp; node tests/test_concentracao_carteira.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "portfolioMetrics(" web/src/App.jsx` retorna 6 e `grep -c "portfolioMetrics([^)]*optionPositions" web/src/App.jsx` retorna 6 (nenhum call site ficou para trás)
    - `grep -c "avisoLiquidacaoForcada" web/src/copy.js` retorna 2
    - `grep -c "opcoesVal" web/src/App.jsx` retorna 1 ou mais
    - `cd web && npx vite build` conclui sem erro
  </acceptance_criteria>
  <done>O patrimônio exibido conta a perna lastreada com a ressalva de marcação, e a liquidação por vencimento aparece na tela como estado do sistema, sem dramatização e sem CTA.</done>
</task>

<task type="auto">
  <name>Task 3: Guardião da Carteira lastreada</name>
  <files>web/tests/test_carteira_lastro_ui.mjs</files>
  <read_first>
    - web/tests/test_fase2_portfolio.mjs e web/tests/test_concentracao_carteira.mjs (padrão de guardião de tela desta base)
    - web/src/App.jsx (o código dos Tasks 1 e 2)
  </read_first>
  <action>
Criar `web/tests/test_carteira_lastro_ui.mjs` (leitura estática de `App.jsx` + `copy.js`, no estilo dos guardiões existentes) verificando:
- `TravaPill` existe e é renderizada em duas superfícies distintas (contagem de usos >= 2).
- O `SellModal` não usa mais `pos.qty` como teto: asserção negativa contra `Math.min(pos.qty` e positiva sobre a variável `livre`.
- `A.openSell` monta a quantidade a partir da livre, não da total.
- Os botões de stop/alvo NÃO são desabilitados por trava (asserção negativa) — o guardrail "stop/alvo nunca são vetados" fica travado por teste.
- As seis chamadas de `portfolioMetrics` passam `optionPositions`.
- O aviso de liquidação não tem botão: no bloco do `AvisoLiquidacao` não existe `<button`.
- O texto do aviso vem de `copy.js`, não hardcodado no JSX.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && node tests/test_carteira_lastro_ui.mjs` passa com 7 ou mais asserções
    - `bash scripts/executar.sh --testes` sem falhas
    - `cd web && node tests/test_opcoes_proposta_ui.mjs` continua passando
  </acceptance_criteria>
  <done>Um guardião quebra se alguém voltar a oferecer a venda da ação travada, esconder a trava, vetar stop/alvo por causa dela, ou deixar um call site de patrimônio para trás.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI→API de venda | a quantidade pedida é montada na tela e revalidada no servidor |
| histórico→UI | o aviso de liquidação é derivado de entradas de histórico gravadas pelo servidor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-26 | Tampering | venda de ação travada pela UI | mitigate | teto do modal e de `openSell` na quantidade livre; a guarda do servidor (plano 14-01) continua como segunda camada — a UI nunca é a única |
| T-14-27 | Repudiation | liquidação forçada invisível | mitigate | aviso derivado do histórico (`motivo="vencimento"`, `origem="sistema"`), exibido no card da posição afetada |
| T-14-28 | Information Disclosure | patrimônio marcado sem cotação ao vivo apresentado como ao vivo | mitigate | ressalva textual obrigatória na linha da perna de opção (princípio 3 do CLAUDE.md) |
| T-14-29 | Denial of Service | trava bloqueando proteção | mitigate | teste negativo garante que stop/alvo nunca são desabilitados por `qtyTravada` |
| T-14-SC | Tampering | npm installs | accept | nenhuma dependência nova nesta fase |
</threat_model>

<verification>
- `cd web && npx vite build` verde
- `bash scripts/executar.sh --testes` verde
- `cd web && node tests/test_copy_theme.mjs` verde (chaves espelhadas + ramo estudo sem vocabulário de ordem)
</verification>

<success_criteria>
- Trava visível e respeitada em toda superfície de venda de ação
- Liquidação forçada comunicada como estado, sem CTA e sem manipulação visual
- Patrimônio Total inclui a perna lastreada, com a marcação declarada
- Guardrail "stop/alvo nunca são vetados" preservado e travado por teste
</success_criteria>

<output>
Create `.planning/phases/14-op-es-lastreadas-venda-coberta-e-put-de-prote-o-sobre-posi-e/14-07-SUMMARY.md` when done
</output>
