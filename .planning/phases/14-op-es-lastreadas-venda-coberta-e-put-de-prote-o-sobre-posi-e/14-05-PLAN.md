---
phase: 14-opcoes-lastreadas
plan: 05
type: execute
wave: 4
depends_on: ["14-03", "14-04"]
files_modified:
  - web/src/api.js
  - web/src/persistence.js
  - web/src/finance.js
  - web/tests/test_opcoes_lastreadas_stores.mjs
  - web/tests/test_finance.mjs
autonomous: true
requirements: []

must_haves:
  truths:
    - "Web e iPhone falam com a mesma mecânica lastreada pelos mesmos nomes de método"
    - "No iPhone sem sessão, abrir e fechar uma operação lastreada altera caixa, trava e histórico locais exatamente como o servidor faria"
    - "Ação travada não é vendida pelo caminho local do iPhone"
    - "A quantidade livre é calculada num lugar só no front (finance.js), consumido por persistence.js e App.jsx"
    - "Posição de opção lastreada entra no Patrimônio Total e no P&L da Carteira; posição de opção antiga continua fora"
  artifacts:
    - path: "web/src/persistence.js"
      provides: "optionsProposta / optionsAbrirLastreada / optionsFecharLastreada nos DOIS stores"
      contains: "optionsAbrirLastreada"
    - path: "web/src/finance.js"
      provides: "qtyLivre() (fonte única da quantidade vendável no front) + portfolioMetrics considerando pernas lastreadas"
      exports: ["qtyLivre", "portfolioMetrics"]
      contains: "optionPositions"
    - path: "web/tests/test_opcoes_lastreadas_stores.mjs"
      provides: "Guardião do espelho deviceStore × store.py da mecânica lastreada e da fonte única de qtyLivre"
  key_links:
    - from: "web/src/persistence.js deviceStore"
      to: "server/app/store.py"
      via: "espelho da aritmética de abrir/fechar/travar"
      pattern: "espelho de store\\.py"
    - from: "web/src/persistence.js"
      to: "web/src/finance.js qtyLivre"
      via: "import da fonte única da quantidade vendável (nunca recalcula a subtração)"
      pattern: "import \\{[^}]*qtyLivre[^}]*\\} from \"\\./finance\\.js\""
    - from: "web/src/finance.js qtyLivre"
      to: "server/app/store.py qty_livre"
      via: "gêmeo entre camadas, mesmo nome e mesma semântica"
      pattern: "store\\.py"
    - from: "web/src/finance.js portfolioMetrics"
      to: "optionPositions com lastro"
      via: "soma da perna de opção no patrimônio"
      pattern: "lastro"
---

<objective>
A camada de dados do front: os três métodos novos nos **DOIS stores** (paridade
obrigatória do repo), o cliente HTTP, a **fonte única da quantidade livre**
(`qtyLivre` em `finance.js`, gêmeo de `store.qty_livre`) e o **Patrimônio Total**
passando a incluir as pernas lastreadas (D-6).

Purpose: sem paridade, o iPhone e o navegador divergem em silêncio — é o modo de
falha que este repositório já pagou duas vezes (carteira nativa sem sync,
`initialBudget` sem sync). Sem D-6, a operação aparece só dentro do card e o
patrimônio mente. E sem `qtyLivre` num módulo só, a subtração do travado vira
três cópias (backend, store local, modal de venda) que envelhecem separadas — a
regra que o `store.py` chama de fonte única deixaria de valer justamente na
camada onde o usuário aperta o botão.
Output: `api.js` com três chamadas, `finance.js` com `qtyLivre` exportada,
`persistence.js` com os três métodos nos dois stores (o `deviceStore` com a
aritmética espelhada, importando `qtyLivre`), `finance.js` com as pernas
lastreadas no agregado, dois arquivos de teste.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-op-es-lastreadas-venda-coberta-e-put-de-prote-o-sobre-posi-e/14-CONTEXT.md
@.planning/phases/14-op-es-lastreadas-venda-coberta-e-put-de-prote-o-sobre-posi-e/14-03-SUMMARY.md
@.planning/phases/14-op-es-lastreadas-venda-coberta-e-put-de-prote-o-sobre-posi-e/14-04-SUMMARY.md
@CLAUDE.md

<interfaces>
Rotas criadas no plano 14-03 (fonte `server/app/main.py`):
- `GET /api/options/proposta/{ticker}` → `{ticker, providerStatus, modo, proposta, motivo, motivoTexto, putSemLastro}`
  onde `proposta` é `null` ou `{tipo:"call_coberta"|"put_protecao", contractSymbol, optionType, strike, expiration, diasParaVencimento, contratos, qtyAcoes, premioUnitario, premioTotal, lastro:{t,qtyLivre}, liquidez:{score,label}, manchete, didatica, chips:[{k,v}]}`
- `POST /api/options/lastreada/abrir` corpo `{underlying, contractSymbol, expiration, contratos}` → `public_state` + `priceUsed`
- `POST /api/options/lastreada/fechar` corpo `{contractSymbol, contratos}` → `public_state` + `priceUsed`
- 403 com "Modo Estudo não executa ordens…" fora do Modo Operador; 502 com a frase do ADR-004 quando a cadeia está degradada

Formato de estado (planos 14-01/14-02):
- `positions[].qtyTravada` (int, ausente = 0)
- `optionPositions[].side` (`"vendida"`|`"comprada"`, ausente = modelo antigo) e `optionPositions[].lastro` (`{t, qty}`, ausente = modelo antigo)

Fonte única da quantidade vendável (plano 14-01, `server/app/store.py`):
`qty_livre(pos) = max(0, qty - qtyTravada)`, com docstring dizendo que a
subtração não se recalcula em outro módulo. Este plano cria o gêmeo do front,
`qtyLivre(pos)` em `web/src/finance.js`.

Front existente (fonte `web/src/persistence.js`):
- `serverStore` delega em uma linha (`optionsBuy: (body) => api.optionsBuy(body)`)
- `deviceStore` tem, para cada operação: ramo `sync.hasSession()` que chama a API e absorve por `_adotarCarteiraDoServidor(r)`, e ramo local que replica a aritmética de `store.py` sobre `doc`
- helpers locais: `_sellOptionLocal(pos, price, qty, motivo)`, `_intrinsecoOpcao(pos, spot)`, `_registrarRejeicaoLocal(tipo, t, qty, price, motivo)`, `now()`, `write()`, `pub()`
- `persistence.js` ainda NÃO importa `finance.js`; `finance.js` não importa nada (módulo puro, sem risco de ciclo)
- guardião de paridade: `web/tests/test_fase3_paridade_stores_generica.mjs` compara os nomes de método dos dois `return {}` — método novo em um lado sem o outro QUEBRA a suíte

Precedente de fonte única entre camadas (fonte `web/src/finance.js`, cabeçalho de
`RR_MIN`): constante mora em `finance.js`, o gêmeo do backend é nomeado no
comentário e um teste (`web/tests/test_rr_min_fonte_unica.mjs`) prende os dois.
`qtyLivre` segue exatamente esse padrão.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cliente HTTP e delegação do serverStore</name>
  <files>web/src/api.js, web/src/persistence.js</files>
  <read_first>
    - web/src/api.js (linhas ~270-285: `optionsChain`, `optionsGate`, `optionsBuy`, `optionsSell`, `putOptionPosition` — o padrão `req(METHOD, path, body, timeout)` e `enrichErrorMessage`)
    - web/src/persistence.js, bloco `serverStore` (linhas ~254-261: as delegações de opção existentes)
    - web/tests/test_fase3_paridade_stores_generica.mjs (o guardião que vai cobrar o método do outro lado)
  </read_first>
  <action>
Em `web/src/api.js`, acrescentar três chamadas ao lado das de opção já existentes, com o mesmo estilo de uma linha:
- `optionsProposta: (t) => req("GET", "/api/options/proposta/" + encodeURIComponent(t), undefined, 30000)` — o mesmo timeout de 30s de `optionsChain`/`optionsGate`, pela mesma razão (a rota busca cadeia + candles).
- `optionsAbrirLastreada: (body) => req("POST", "/api/options/lastreada/abrir", body)`
- `optionsFecharLastreada: (body) => req("POST", "/api/options/lastreada/fechar", body)`

Em `web/src/persistence.js`, bloco `serverStore`, acrescentar as três delegações diretas (`optionsProposta`, `optionsAbrirLastreada`, `optionsFecharLastreada`) junto das demais chamadas de opção. Abrir/fechar mexem em caixa real, então são chamada DIRETA — nunca `sync.mutate`/outbox (mesma decisão já registrada para `buy`/`sell`/`cancelPendingOrder`: reaplicar de fila offline devolveria caixa duas vezes). Registrar isso em comentário de uma linha.
  </action>
  <verify>
    <automated>cd web &amp;&amp; node tests/test_api_parity.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "optionsProposta\|optionsAbrirLastreada\|optionsFecharLastreada" web/src/api.js` retorna 3
    - `grep -c "sync.mutate(\"optionsAbrirLastreada\"" web/src/persistence.js` retorna 0 (nunca na fila otimista)
    - `cd web && node tests/test_api_parity.mjs` passa
  </acceptance_criteria>
  <done>O web fala com as três rotas novas pelo mesmo caminho que já usa para as demais operações de opção.</done>
</task>

<task type="auto">
  <name>Task 2: qtyLivre em finance.js e deviceStore — espelho local da mecânica lastreada e da trava</name>
  <files>web/src/finance.js, web/src/persistence.js, web/tests/test_opcoes_lastreadas_stores.mjs</files>
  <read_first>
    - web/src/finance.js: cabeçalho de definições adotadas e o bloco de `RR_MIN` (o padrão exato de fonte única do front nomeando o gêmeo do backend — `qtyLivre` entra escrito assim), mais `markPrice` (estilo das funções puras defensivas do módulo)
    - web/src/persistence.js, bloco `deviceStore` inteiro: `optionsBuy`/`optionsSell`/`putOptionPosition` (linhas ~1100-1205), `buy`/`sell` (linhas ~1271-1400), `_sellOptionLocal`/`_intrinsecoOpcao`/`_registrarRejeicaoLocal` (linhas ~472-514), `cycle` (linhas ~1434-1506)
    - web/src/persistence.js, linhas 17-30 (bloco de imports — `finance.js` ainda não está lá e vai entrar)
    - server/app/store.py — `abrir_call_coberta`, `fechar_call_coberta`, `comprar_put_protecao`, `liquidar_lastreada_vencida`, `qty_livre` (a aritmética a ser espelhada byte a byte no comportamento, não no texto)
    - web/tests/test_rr_min_fonte_unica.mjs (o formato de guardião de fonte única desta base — o teste novo repete o padrão para `qtyLivre`)
    - CLAUDE.md, guardrail de paridade dos dois stores
  </read_first>
  <action>
1) `web/src/finance.js` — criar a fonte ÚNICA da quantidade vendável do front, antes de qualquer consumidor:
`export function qtyLivre(pos)` devolvendo `Math.max(0, (Number(pos && pos.qty) || 0) - (Number(pos && pos.qtyTravada) || 0))`, defensiva a `pos` nulo no mesmo estilo de `markPrice`.
Documentar no cabeçalho do arquivo, junto das demais definições adotadas: quantidade livre = quantidade total menos a travada como lastro de CALL coberta aberta; é o gêmeo de `store.qty_livre` (`server/app/store.py`, plano 14-01), MESMO nome e MESMA semântica; nenhum outro módulo do front recalcula a subtração — `persistence.js` e `App.jsx` importam esta função. Citar no comentário o precedente `RR_MIN` (fonte única do front amarrada ao backend por teste), para que a regra apareça como padrão da casa e não como preferência isolada.

2) `web/src/persistence.js` — acrescentar `qtyLivre` ao bloco de imports (`import { qtyLivre } from "./finance.js";` — `finance.js` é módulo puro sem imports, não há ciclo) e usá-la diretamente. NÃO criar helper privado `_qtyLivre` nem qualquer outra cópia da subtração: uma terceira aritmética de "livre" é exatamente o que a fonte única existe para impedir.

3) Acrescentar ao `deviceStore` os três métodos com a MESMA assinatura do `serverStore` e, no ramo sem sessão, o espelho local da aritmética do `store.py`.

3a) `optionsProposta(t)`: sempre delega (`api.optionsProposta(t)`) — proposta depende de cadeia e de análise técnica, que são dado de mercado e nunca se duplicam no aparelho (mesma regra já aplicada a `optionsGate`/`optionsChain`).

3b) `optionsAbrirLastreada(body)`:
- Com sessão: chama a API, `_adotarCarteiraDoServidor(r)`, `write()`, devolve `pub()` com `priceUsed` — cópia exata do padrão de `optionsBuy`.
- Sem sessão: busca a cadeia, recusa com a frase existente se `providerStatus !== "ok"`, localiza o contrato, valida prêmio numérico > 0, calcula `qty = contratos * 100`, valida `qtyLivre(pos) >= qty` (a função importada de `finance.js`, espelho de `store.qty_livre`), e então: CALL → CREDITA `qty*price` no `doc.cash`, cria a posição com `side:"vendida"` e `lastro:{t,qty}`, incrementa `pos.qtyTravada`; PUT → DEBITA, valida caixa, cria com `side:"comprada"` e `lastro`, sem tocar `qtyTravada`. Histórico com `kind:"opcao"` e `acao:"abrir"`, mesmos campos do backend.
- Recusa local grava `_registrarRejeicaoLocal` antes de lançar o erro, com a mesma frase do backend.

3c) `optionsFecharLastreada(body)`: com sessão delega; sem sessão, `side === "vendida"` → DEBITA `qty*price`, `pnl = (avg - price) * qty`, decrementa `qtyTravada` com piso 0, remove/reduz a posição e grava histórico `acao:"fechar"`; `side === "comprada"` → reusa `_sellOptionLocal`.

4) Trava no caminho local de venda de AÇÃO (`deviceStore.sell`, ramo sem sessão): usar `qtyLivre(pos)` (a importada) no lugar de `pos.qty` para limitar a quantidade vendida; com livre 0, gravar rejeição com a frase de lastro e lançar o erro — espelho da guarda criada em `store.sell` no plano 14-01. Sem isto, o iPhone offline vende ação travada e diverge do servidor no próximo sync.

5) Ciclo local (`deviceStore.cycle`): no laço de `optionPositions`, tratar posição com `lastro` num ramo próprio antes do bloco de stop/alvo — vencida liquida pela regra nova (call vendida: debita `qty*intrinseco`, `pnl = (avg - intrinseco)*qty`, destrava; put comprada: `_sellOptionLocal(pos, intrinseco, null, "vencimento")`), não vencida faz `continue`. Espelho de `agent._avaliar_opcoes` (plano 14-04).

Teste `web/tests/test_opcoes_lastreadas_stores.mjs` — no estilo dos guardiões existentes deste repositório (leitura estática de `web/src/persistence.js` e `web/src/finance.js` + asserções sobre o texto, como fazem `test_fase3_paridade_stores_generica.mjs` e `test_rr_min_fonte_unica.mjs`), verificando:
- os três métodos aparecem nos DOIS blocos de store;
- o ramo local de `optionsAbrirLastreada` contém tanto o crédito (`doc.cash = +(doc.cash + `) quanto o débito, e a escrita de `qtyTravada`;
- FONTE ÚNICA (o ponto do teste, não um detalhe): `finance.js` exporta `qtyLivre`; `persistence.js` a IMPORTA de `./finance.js`; `persistence.js` a usa em pelo menos dois pontos (venda de ação e abertura lastreada); e `persistence.js` NÃO contém nenhuma subtração crua do travado — asserção negativa contra o padrão `qty` menos `qtyTravada` escrito à mão (regex tolerante a espaço e a `|| 0`), e asserção negativa contra a existência de um helper privado `_qtyLivre`;
- o ciclo local tem ramo `lastro` antes das checagens de stop/alvo;
- nenhum dos três métodos passa por `sync.mutate`.
  </action>
  <verify>
    <automated>cd web &amp;&amp; node tests/test_opcoes_lastreadas_stores.mjs &amp;&amp; node tests/test_finance.mjs &amp;&amp; node tests/test_fase3_paridade_stores_generica.mjs &amp;&amp; node tests/test_carteira_nativa_sincroniza.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && node tests/test_fase3_paridade_stores_generica.mjs` passa com 0 assimetrias novas
    - `grep -c "optionsAbrirLastreada" web/src/persistence.js` retorna 2 ou mais (um por store)
    - `grep -c "export function qtyLivre" web/src/finance.js` retorna 1
    - `grep -c "qtyLivre" web/src/persistence.js` retorna 3 ou mais (import + venda + abertura)
    - `grep -c "_qtyLivre" web/src/persistence.js` retorna 0 (nenhuma cópia privada da subtração)
    - `grep -c "qtyTravada || 0" web/src/persistence.js` retorna 0 fora do ramo que INCREMENTA/DECREMENTA a trava (a leitura da quantidade livre passa só por `qtyLivre`)
    - `grep -c "espelho de store.py" web/src/persistence.js` aumenta (a nova aritmética declara de quem é espelho)
  </acceptance_criteria>
  <done>Os dois stores expõem os mesmos três métodos; o iPhone offline produz exatamente o mesmo caixa, trava e histórico que o servidor produziria; e a quantidade livre tem UMA implementação no front (`finance.js`), importada em vez de reescrita.</done>
</task>

<task type="auto">
  <name>Task 3: Patrimônio Total inclui as pernas lastreadas</name>
  <files>web/src/finance.js, web/tests/test_finance.mjs</files>
  <read_first>
    - web/src/finance.js (`markPrice`, `portfolioMetrics`, `concentracaoMaxima`, o `qtyLivre` recém-criado no Task 2 e o cabeçalho com as definições adotadas — o novo comportamento entra ali como definição escrita, não como código sem explicação)
    - web/tests/test_finance.mjs (asserções existentes de `portfolioMetrics`, que não podem regredir)
    - .planning/phases/14-op-es-lastreadas-venda-coberta-e-put-de-prote-o-sobre-posi-e/14-CONTEXT.md (D-6 e a discricionariedade sobre retroatividade)
  </read_first>
  <action>
Estender `portfolioMetrics` de forma ADITIVA: `portfolioMetrics(positions, quotes, cash, reservado, optionPositions, optionQuotes)`. Os dois parâmetros novos são opcionais — chamada antiga com quatro argumentos produz exatamente o resultado de hoje (nenhum call site quebra).

Regras (documentar no cabeçalho do arquivo, junto das demais definições adotadas e logo abaixo da definição de `qtyLivre` escrita no Task 2):
- Só entram posições de opção com `lastro` presente. Posição de opção do modelo antigo continua FORA do agregado — a mudança de D-6 vale para a mecânica lastreada, e nenhuma migração de dado foi decidida (registrar essa escolha em comentário).
- Marcação: prêmio ao vivo de `optionQuotes[contractSymbol]` quando for número > 0; senão `avg` (mesmo piso estável de `markPrice`, nunca 0 — CLAUDE.md item 4).
- `side === "comprada"` (put de proteção): soma `+qty * preço` em `posVal`, `+qty * avg` em `cost`, `+(preço - avg) * qty` em `openPnL`.
- `side === "vendida"` (call coberta): soma `-qty * preço` em `posVal` (é um passivo de recompra; o prêmio recebido já está dentro de `cash`) e `+(avg - preço) * qty` em `openPnL`. NÃO soma nada em `cost` — não houve desembolso.
- `dayVal` não muda: continua contando apenas variação diária de AÇÃO (não há `change` diário confiável de contrato de opção nesta fase).
- Devolver dois campos novos no objeto de retorno: `opcoesVal` (soma líquida das pernas lastreadas) e `opcoesPnL`, para a UI poder discriminar sem recalcular.
- `posVal` de AÇÃO continua contando a posição INTEIRA (`pos.qty`), travada ou não: a trava restringe a venda, não retira o ativo do patrimônio. Registrar em comentário para que ninguém confunda `qtyLivre` (o que pode ser vendido) com o que é marcado a mercado.

Marcação ao vivo de contrato NÃO é buscada nesta fase (a Carteira não dispara chamada de cadeia por posição): `optionQuotes` chega vazio por padrão e a perna é marcada pelo prêmio de abertura. Motivo explícito a registrar em comentário: cada cadeia é uma requisição contra o orçamento medido do mydata (ADR-020), que é justamente o que está travando a virada de produção — gastar requisição por posição na tela de Carteira compraria um número mais fresco ao preço de agravar o bloqueio da fase.

Acrescentar em `web/tests/test_finance.mjs`: chamada com 4 argumentos devolve o mesmo resultado de antes (guardião de compatibilidade); put lastreada com prêmio ao vivo maior que o `avg` aumenta `patr` e `openPnL`; call coberta vendida com prêmio ao vivo MENOR que o `avg` aumenta `openPnL` (a call barateou = lucro do vendedor) e reduz `posVal`; posição de opção SEM `lastro` não altera nenhum número; sem `optionQuotes`, a perna é marcada pelo `avg` e o `openPnL` da perna é 0; e a bateria unitária de `qtyLivre` (sem a chave devolve `qty`; `qty=300`/`qtyTravada=200` devolve 100; travada maior que a total devolve 0, nunca negativo; `pos` nulo devolve 0), espelhando os casos do `test_lastro_trava.py` do backend.
  </action>
  <verify>
    <automated>cd web &amp;&amp; node tests/test_finance.mjs &amp;&amp; node tests/test_concentracao_carteira.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && node tests/test_finance.mjs` passa, incluindo o teste de compatibilidade de 4 argumentos e os casos de `qtyLivre`
    - `grep -c "opcoesVal" web/src/finance.js` retorna 1 ou mais
    - `grep -v '^//' web/src/finance.js | grep -c "lastro"` retorna 1 ou mais (o filtro do modelo novo está no código, não só no comentário)
    - `bash scripts/executar.sh --testes` sem falhas
  </acceptance_criteria>
  <done>O Patrimônio Total e o P&L da Carteira passam a contar a perna lastreada com o sinal certo (put soma, call vendida é passivo), sem mexer no que já existia.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| armazenamento local do aparelho→app | `localStorage` do WKWebView é editável fora do app; o doc local não é fonte de verdade quando há sessão |
| app→API | corpo das chamadas de abrir/fechar é montado no cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-18 | Tampering | `deviceStore` local sem sessão | mitigate | trava de lastro replicada no caminho local pela `qtyLivre` importada de `finance.js` — o aparelho não produz um estado que o servidor recusaria; com sessão, o servidor executa e o aparelho só absorve |
| T-14-19 | Tampering | fila offline reaplicando ordem | mitigate | abrir/fechar são chamada direta, nunca `sync.mutate`/outbox (evita creditar prêmio duas vezes) |
| T-14-20 | Information Disclosure | `optionQuotes` | accept | prêmio de contrato é dado público de mercado |
| T-14-21 | Denial of Service | uma cadeia por posição na Carteira | mitigate | decisão explícita de NÃO buscar cotação por posição nesta fase (orçamento do mydata, ADR-020); marcação cai no prêmio de abertura |
| T-14-22 | Tampering | cópias divergentes da aritmética de "livre" | mitigate | uma implementação só no front (`finance.js qtyLivre`), importada por `persistence.js` e `App.jsx`; guardião com asserção negativa contra subtração crua e contra helper privado |
| T-14-SC | Tampering | npm installs | accept | nenhuma dependência nova nesta fase |
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` sem falhas (backend + web)
- `cd web && node tests/test_fase3_paridade_stores_generica.mjs` sem assimetria nova
- `grep -rn "qtyTravada" web/src/ | grep -v finance.js` não mostra nenhuma subtração de quantidade livre fora de `finance.js` (só incremento/decremento da trava)
- Nenhum arquivo de `web/src/*.jsx` foi tocado neste plano (`git diff --name-only | grep -c jsx` = 0) — a UI é dos planos 14-06/14-07
</verification>

<success_criteria>
- Três métodos novos, presentes e simétricos nos dois stores
- `qtyLivre` existe uma vez só no front (`finance.js`), é importada por `persistence.js` e fica pronta para o `App.jsx` do plano 14-07
- iPhone offline reproduz a aritmética do servidor, inclusive a trava e a liquidação por vencimento
- Patrimônio Total inclui as pernas lastreadas e ignora as antigas, com teste de compatibilidade protegendo os call sites atuais
</success_criteria>

<output>
Create `.planning/phases/14-op-es-lastreadas-venda-coberta-e-put-de-prote-o-sobre-posi-e/14-05-SUMMARY.md` when done
</output>
