---
phase: 14-opcoes-lastreadas
plan: 08
type: execute
wave: 7
depends_on: ["14-07"]
files_modified:
  - web/src/version.js
  - server/web_dist
  - docs/adr/023-opcoes-lastreadas.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "A mecânica lastreada roda ponta a ponta contra o provedor mock, sem mydata em produção"
    - "A fase está no ar dormente: nada aparece nem executa enquanto a cadeia real não responder"
    - "A decisão da liquidação forçada e a da não-retroatividade estão registradas em ADR, não só em código"
    - "O front construído nesta fase foi efetivamente publicado, não apenas mergeado"
  artifacts:
    - path: "docs/adr/023-opcoes-lastreadas.md"
      provides: "Registro das decisões desta fase (lastro, sem atribuição, fórmula de liquidação, dormência)"
      min_lines: 40
    - path: "web/src/version.js"
      provides: "Carimbo BUILD_ID da entrega"
  key_links:
    - from: "web/src/version.js"
      to: "server/web_dist"
      via: "scripts/publicar-web.sh"
      pattern: "BUILD_ID"
---

<objective>
Entregar a fase: registrar as decisões em ADR, verificar a mecânica ponta a ponta
contra o provedor **mock**, publicar o front (regra do repositório: plano que
toca `web/src/` sem publicar fica testado e nunca vai ao ar), e confirmar com o
desenvolvedor que a fase está **dormente** em produção.

Purpose: a fase é construída para ficar guardada até a virada do mydata. Se a
dormência não for verificada ao vivo, o modo de falha é o pior possível — uma
superfície nova aparecendo com cadeia degradada para todo mundo.
Output: ADR-023, build publicado com carimbo novo, checkpoint humano aprovado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-op-es-lastreadas-venda-coberta-e-put-de-prote-o-sobre-posi-e/14-CONTEXT.md
@.planning/notes/opcoes-mecanica-lastreada-decisoes.md
@.planning/phases/14-op-es-lastreadas-venda-coberta-e-put-de-prote-o-sobre-posi-e/14-07-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ADR-023 + verificação ponta a ponta contra o mock + publicação do front</name>
  <files>docs/adr/023-opcoes-lastreadas.md, web/src/version.js, server/web_dist</files>
  <read_first>
    - docs/adr/021-ponte-gatilho-put.md e docs/adr/022-ciclo-de-vida-da-sugestao-de-put.md (formato do ADR desta base e o mecanismo de sombra que esta fase NÃO estende — o ADR novo precisa dizer isso explicitamente)
    - docs/adr/004-fonte-de-opcoes-na-v2.md e docs/adr/020-centralizacao-de-dados-no-mydata.md (o contrato `providerStatus` e o estado da virada — o ADR novo não reabre nenhum dos dois)
    - scripts/bump.sh e scripts/publicar-web.sh (cabeçalhos: por que o bump é manual e por que `server/web_dist` nunca é editado à mão)
    - .planning/notes/opcoes-mecanica-lastreada-decisoes.md (D-1..D-8, a fonte das decisões a registrar)
  </read_first>
  <action>
1) Escrever `docs/adr/023-opcoes-lastreadas.md` no formato dos ADRs existentes, registrando: escopo restrito a duas operações lastreadas; nunca simular atribuição/exercício e a FÓRMULA exata de liquidação forçada definida no plano 14-04 (recompra sintética ao valor intrínseco, débito de `qty × intrínseco`, `pnl = (avg − intrínseco) × qty`, ações intocadas, lastro destravado); a trava de lastro (`positions[].qtyTravada`) como campo novo e `store.qty_livre` como fonte única; a decisão de NÃO-retroatividade (posições de opção sem `lastro` seguem fora da trava e fora do Patrimônio — nenhuma migração de dado); a PUT de proteção não travar ações e o estado "sem lastro" ser DERIVADO; a entrada de `B3_OPTIONS_PROVIDER=mock` como provedor de desenvolvimento com default de produção inalterado; e a dormência (a fase só desperta quando a cadeia real responder, sem flag nova — o gate de liquidez já existente é o interruptor). Fechar com a lista do que NÃO foi reaberto: ADR-001, ADR-004, ADR-008, ADR-020, ADR-021, ADR-022.

2) Verificação ponta a ponta contra o mock, com o servidor local: subir o backend com `B3_OPTIONS_PROVIDER=mock` e exercitar, por `curl`, a sequência completa — `GET /api/options/proposta/PETR4` sem posição (espera `proposta: null`, `motivo: "sem_lastro"`), comprar ações, `GET` de novo (espera proposta com `contratos >= 1`), `POST /api/options/lastreada/abrir` em Modo Estudo (espera 403), trocar para Operador e abrir (espera caixa creditado e `qtyTravada` preenchido no `public_state`), tentar `POST /api/sell` da posição inteira (espera 400 com a frase de lastro), `POST /api/options/lastreada/fechar` (espera destrava) e, por fim, `B3_OPTIONS_MOCK_STATUS=degraded` com `GET` da proposta (espera `motivo: "degradado"`). Registrar as saídas observadas no SUMMARY — é esta a evidência de que a fase é testável sem mydata.

3) Confirmar a dormência com o provedor DEFAULT: com `B3_OPTIONS_PROVIDER` ausente (yahoo), `GET /api/options/gate/PETR4` devolve `liquida: false` e `GET /api/options/proposta/PETR4` devolve `proposta: null` com `motivo: "degradado"` — nenhuma superfície nova aparece.

4) Publicar o front: `bash scripts/bump.sh` e depois `bash scripts/publicar-web.sh`, nessa ordem (nunca editar `server/web_dist` à mão). Rodar `bash scripts/executar.sh --testes` DEPOIS da publicação e conferir que os guardiões de carimbo/bundle passam.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `ls docs/adr/023-opcoes-lastreadas.md` existe e o arquivo cita a fórmula de liquidação e a decisão de não-retroatividade
    - `git diff --name-only | grep -c "server/web_dist"` maior que 0 e `git diff web/src/version.js | grep -c "^+.*BUILD_ID"` igual a 1
    - As saídas de `curl` da sequência do item 2 estão transcritas no SUMMARY, incluindo o 403 de Modo Estudo e o 400 de lastro travado
    - `bash scripts/executar.sh --testes` sem falhas
  </acceptance_criteria>
  <done>As decisões estão registradas em ADR, a mecânica foi exercitada de ponta a ponta sem mydata, e o front foi publicado com carimbo novo.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificação humana — proposta, trava e dormência</name>
  <files>nenhum — verificação ao vivo, sem edição de arquivo</files>
  <read_first>
    - .planning/phases/14-op-es-lastreadas-venda-coberta-e-put-de-prote-o-sobre-posi-e/14-08-SUMMARY.md (as saídas de curl registradas na Task 1 — a verificação humana confirma na tela o que o curl já provou no servidor)
    - .planning/phases/14-op-es-lastreadas-venda-coberta-e-put-de-prote-o-sobre-posi-e/14-UI-SPEC.md (o contrato visual aprovado, contra o qual a tela é conferida)
  </read_first>
  <action>Pausar a execução e apresentar ao desenvolvedor o roteiro de verificação abaixo. Não prosseguir, não commitar e não marcar a fase como concluída até receber o sinal de retomada. Se o desenvolvedor apontar divergência, registrar cada uma no SUMMARY com tela, modo e passo antes de encerrar.</action>
  <what-built>
Mecânica de opções lastreadas completa e dormente: motor (trava de lastro, abrir/fechar CALL coberta, comprar PUT de proteção, liquidação forçada no vencimento sem atribuição), motor de proposta determinística por modo, três rotas HTTP, paridade dos dois stores do front, Patrimônio Total incluindo as pernas lastreadas, card de proposta com cadeia expansível, badge de trava e aviso de liquidação na Carteira. Provedor `mock` disponível para exercitar tudo sem mydata; default de produção continua `yahoo`, e é por isso que nada disso aparece em produção hoje.
  </what-built>
  <how-to-verify>
1. Suba o app local com o backend em `B3_OPTIONS_PROVIDER=mock` (`bash scripts/executar.sh` com a variável exportada) e abra a Watchlist.
2. Com uma posição comprada em PETR4, confirme no card do ativo: a proposta aparece ACIMA da cadeia, com uma manchete única, chips e a linha do contrato; a cadeia completa continua acessível pelo acordeão abaixo.
3. Em **Modo Estudo**: a proposta deve explicar a operação em linguagem condicional ("Se você tivesse…") e NÃO deve haver botão de executar.
4. Troque para **Modo Operador**: o CTA aparece. Toque nele e confirme que o aviso de confirmação diz quantas ações ficam travadas.
5. Vá à Carteira: a posição de PETR4 deve mostrar o badge de ações travadas; abra "Vender" e confirme que o seletor não deixa passar da quantidade livre e explica o motivo.
6. Confirme que os botões de stop/alvo da mesma posição continuam funcionando (a trava restringe venda, nunca proteção).
7. Feche a call pelo CTA e confirme que o badge some e a venda total volta a ser oferecida.
8. Por fim, reinicie o backend SEM `B3_OPTIONS_PROVIDER` (default yahoo) e confirme que nenhuma linha de opções e nenhuma proposta aparecem em card nenhum — a fase está dormente.
  </how-to-verify>
  <resume-signal>Responda "aprovado" ou descreva o que divergiu (tela, modo e passo)</resume-signal>
  <verify>
    <automated>MISSING — verificação visual e de modo por natureza; a perna automatizável já roda na Task 1 (`bash scripts/executar.sh --testes` e a sequência de curl contra o provedor mock)</automated>
    <human-check>Os 8 passos de `how-to-verify` conferidos pelo desenvolvedor no app local</human-check>
  </verify>
  <acceptance_criteria>
    - Desenvolvedor respondeu "aprovado", ou toda divergência apontada foi registrada no SUMMARY com tela, modo e passo
    - O passo 8 (dormência com provedor default) foi conferido explicitamente — é o que impede a fase de aparecer em produção antes da virada do mydata
  </acceptance_criteria>
  <done>Checkpoint aprovado pelo desenvolvedor, ou divergências registradas e encaminhadas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repositório→produção | `server/web_dist` é a única árvore que o Railway publica |
| env de produção→feature | `B3_OPTIONS_PROVIDER` decide se a fase acorda |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-30 | Tampering | publicação sem bump | mitigate | `bump.sh` antes de `publicar-web.sh`; o guardião de carimbo/bundle da suíte quebra se os elos divergirem |
| T-14-31 | Elevation of Privilege | fase despertando sem querer em produção | mitigate | default `yahoo` inalterado + checkpoint humano do passo 8 verificando ausência total de superfície |
| T-14-32 | Repudiation | decisões de produto só no código | mitigate | ADR-023 registra fórmula de liquidação, não-retroatividade e dormência |
| T-14-SC | Tampering | npm/pip installs | accept | nenhuma dependência nova nesta fase |
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` verde depois da publicação
- `curl` da sequência ponta a ponta com `B3_OPTIONS_PROVIDER=mock` reproduzida no SUMMARY
- `GET /api/options/proposta/PETR4` com provedor default devolve `proposta: null`
- Checkpoint humano aprovado
</verification>

<success_criteria>
- ADR-023 escrito, com as decisões que o CONTEXT.md deixou para o planejamento
- Mecânica exercitada ponta a ponta sem mydata em produção
- Front publicado com carimbo novo (a fase existe no ar, dormente)
- Desenvolvedor confirmou proposta, split de modo, trava e dormência ao vivo
</success_criteria>

<output>
Create `.planning/phases/14-op-es-lastreadas-venda-coberta-e-put-de-prote-o-sobre-posi-e/14-08-SUMMARY.md` when done
</output>
