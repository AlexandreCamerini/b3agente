---
phase: 19-motor-multi-candidato
plan: 04
type: execute
wave: 4
depends_on: ["19-03"]
files_modified:
  - web/src/version.js
  - server/app/main.py
  - server/web_dist
autonomous: false
requirements: [MULTI-01, MULTI-02]

must_haves:
  truths:
    - "O motor multi-candidato e a UI de N candidatos estão publicados — não ficam testados no repo e ausentes do ar"
    - "O carimbo de entrega é novo e coerente entre web/src/version.js, server/web_dist e SERVER_BUILD_ID"
    - "Um humano confirmou, com o app rodando e mercado aberto, que os dois candidatos aparecem lado a lado, que cada CTA abre a estrutura certa e que aceitar um impede aceitar o irmão"
    - "O humano decidiu conscientemente sobre publicar junto os fluxos das Fases 17 e 18, que seguem sem checkpoint aprovado"
  artifacts:
    - path: "web/src/version.js"
      provides: "BUILD_ID novo desta entrega"
      contains: "BUILD_ID"
    - path: "server/web_dist"
      provides: "bundle publicado com o ramo de N candidatos"
  key_links:
    - from: "web/src/version.js"
      to: "server/web_dist"
      via: "scripts/bump.sh seguido de scripts/publicar-web.sh"
      pattern: "BUILD_ID"
    - from: "web/src/version.js"
      to: "server/app/main.py:SERVER_BUILD_ID"
      via: "sincronização automática dentro de publicar-web.sh"
      pattern: "SERVER_BUILD_ID"
---

<objective>
Publicar a Fase 19 e submetê-la à verificação humana com o mercado aberto.

Purpose: dois riscos conhecidos deste repositório convergem aqui. (1) "Fase sem
plano de publicação front" — plano que toca `web/src/` sem bump/publicação fica
mergeado, testado e nunca vai ao ar. (2) Esta fase muda o que o motor OFERECE e
o que o usuário PODE ACEITAR: nenhuma asserção estática prova que dois cards
cabem na tela do iPhone, que a manchete de cada candidato é legível sem corte,
nem que a rejeição do candidato irmão aparece de forma compreensível. Além
disso, esta fase mexe em backend E front — a publicação precisa carregar os
dois carimbos coerentes.

Output: carimbo novo, `server/web_dist` republicado, `SERVER_BUILD_ID`
sincronizado, suíte canônica verde após a publicação e a decisão humana
registrada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-motor-multi-candidato/19-CONTEXT.md
@.planning/phases/19-motor-multi-candidato/19-03-SUMMARY.md
@CLAUDE.md

<interfaces>
`scripts/bump.sh` — avança o `BUILD_ID` em `web/src/version.js`. Sem argumento,
gera o próximo do dia (`F10-AAAAMMDD-NN`). É comando MANUAL de propósito: o
carimbo marca ENTREGA, não build.

`scripts/publicar-web.sh` — builda `web/` e publica em `server/web_dist`, a ÚNICA
árvore que o Railway enxerga (`rootDirectory=/server`). Ele TAMBÉM sincroniza
`SERVER_BUILD_ID` em `server/app/main.py` a partir do carimbo local
(`publicar-web.sh:68-71`) — por isso, com front sendo publicado, NÃO se edita
`SERVER_BUILD_ID` à mão. Nunca editar `server/web_dist` à mão. Ordem
obrigatória: `bump.sh` ANTES de `publicar-web.sh`.

`bash scripts/executar.sh --testes` — suíte canônica (pytest + `web/tests/*.mjs`).
Roda DEPOIS da publicação: há guardiões de carimbo/bundle que só têm o que medir
com o `web_dist` novo no lugar.

Carimbo vigente antes desta fase: `F10-20260903-02` (em `web/src/version.js` e
em `server/app/main.py:1013`).
</interfaces>

<risco_herdado>
**A publicação é cumulativa e carrega DUAS fases não aprovadas junto.**
`.planning/STATE.md` registra:

- **Fase 17** (`17-06-PLAN.md`, Task 2): checkpoint humano ADIADO, NÃO
  APROVADO — o mercado estava fechado e o roteiro de 10 passos nunca foi
  exercitado; push para `origin` segue não feito.
- **Fase 18** (`18-05-PLAN.md`, Task 2): checkpoint humano PENDENTE, exercitado
  só PARCIALMENTE no iPhone em 2026-09-03 — a tira e o estado vazio foram
  confirmados com dado real, mas tira COM item, aceite em Modo Operador, Modo
  Estudo sem CTA e collar por caixa insuficiente seguem sem verificação, porque
  nenhuma posição real do Alex tinha proposta ativa no dia.

Publicar a Fase 19 põe no ar, no mesmo bundle, os dois fluxos ainda não
confirmados ao vivo — e a Fase 19 os AMPLIA (o mesmo detalhe de posição agora
pode oferecer duas estruturas aceitáveis em vez de uma). O ROADMAP registra a
decisão explícita do Alex de PLANEJAR a Fase 19 mesmo assim; planejar não é
publicar.

Isso NÃO é decisão do executor. A Task 2 apresenta o fato ao Alex e espera a
decisão dele antes de qualquer coisa ir para `origin`.
</risco_herdado>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump do carimbo e publicação de front + backend</name>
  <read_first>
    - scripts/bump.sh (cabeçalho — por que o bump é manual e o que quebra sem ele)
    - scripts/publicar-web.sh:55-80 (a sincronização automática de `SERVER_BUILD_ID`)
    - web/src/version.js (carimbo atual)
    - server/app/main.py:1010-1021 (`SERVER_BUILD_ID` e `/api/health`)
    - .planning/phases/19-motor-multi-candidato/19-03-SUMMARY.md (estado da suíte antes da publicação)
  </read_first>
  <files>web/src/version.js, server/app/main.py, server/web_dist</files>
  <action>
Nesta ordem exata, sem pular passo:
1. `bash scripts/bump.sh` — sem argumento (próximo carimbo do dia).
2. `bash scripts/publicar-web.sh` — builda `web/` e publica em `server/web_dist`;
   ele sincroniza `SERVER_BUILD_ID` sozinho a partir do carimbo local.
3. `bash scripts/executar.sh --testes` — DEPOIS da publicação, para que os
   guardiões de carimbo/bundle meçam o `web_dist` novo.
4. Conferir os TRÊS elos do carimbo: `web/src/version.js`,
   `server/app/main.py:SERVER_BUILD_ID` e o bundle dentro de `server/web_dist`
   carregam o MESMO `BUILD_ID`.

Não editar `server/web_dist` à mão. Não editar `SERVER_BUILD_ID` à mão (o
script sincroniza; edição manual só existe em deploy só-backend, que não é o
caso — esta fase publica front). Não dar push para `origin`: a Task 2 é
bloqueante e a decisão sobre o risco herdado é do Alex.

Registrar no SUMMARY o carimbo gerado e a contagem de testes das duas suítes.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `git diff web/src/version.js` mostra um `BUILD_ID` novo no formato `F10-AAAAMMDD-NN`, diferente de `F10-20260903-02`.
    - `git status --porcelain server/web_dist` mostra arquivos modificados (a publicação de fato aconteceu).
    - Os três elos batem: `grep -o 'F10-[0-9]*-[0-9]*' web/src/version.js | head -1` é igual ao valor de `sed -n 's/.*SERVER_BUILD_ID = "\([^"]*\)".*/\1/p' server/app/main.py`, e `grep -rl "$(grep -o 'F10-[0-9]*-[0-9]*' web/src/version.js | head -1)" server/web_dist | head -1` retorna ao menos um arquivo.
    - `bash scripts/executar.sh --testes` sai com código 0 DEPOIS da publicação (as duas suítes).
    - `git diff --stat web/package.json web/package-lock.json server/requirements.txt server/requirements-prod.txt` sai vazio.
    - `git log origin/main..HEAD --oneline | wc -l` > 0 e nenhum `git push` foi executado (verificável por `git status -sb` mostrando commits à frente de `origin`).
  </acceptance_criteria>
  <done>Carimbo novo nos três elos, `server/web_dist` republicado, suíte canônica verde após a publicação, nada empurrado para `origin`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificação humana do motor multi-candidato no app rodando</name>
  <what-built>
O motor de proposta deixou de escolher UMA estrutura por posição. Quando a
leitura técnica é de queda (VENDER/baixa) e TANTO a put de proteção QUANTO a
trava protetora cabem no caixa e no lastro, os dois candidatos passam a ser
oferecidos ao mesmo tempo, lado a lado, dentro do detalhe da posição em
Posições — cada um com a manchete do motor, o payoff próprio e o próprio botão
de aceite. Quando só uma estrutura cabe (o caso de hoje), a tela é exatamente a
mesma de antes.

O padrão visual é o MESMO da tira "Oportunidades de opções" da Fase 18 —
nenhuma cor, tamanho ou frase nova foi criada nesta fase. Nos dois branches em
que o motor já era de candidato único (venda coberta no viés de prêmio; nenhuma
estrutura no viés de alta), nada mudou.

No backend, aceitar um candidato passa a impedir a execução do irmão sobre a
mesma posição — por dois caminhos que já existiam: o lastro travado pela call
vendida e a regra "posição com lastreada aberta recebe proposta de fechamento,
não proposta nova".
  </what-built>
  <how-to-verify>
Rodar o app local (`bash scripts/executar.sh` ou o fluxo de dev habitual), com
mercado ABERTO — a cadeia de opções ao vivo é o que faz existir um segundo
candidato. Se nenhuma posição real tiver leitura de VENDER/baixa no dia, usar
uma conta/ativo em que o Radar esteja indicando queda; sem isso, os passos 1-5
não são exercitáveis e o checkpoint fica parcialmente pendente (registrar,
não fingir aprovação).

1. **Dois candidatos aparecem.** Em Posições, abrir o detalhe de opções de uma
   posição cuja leitura técnica seja de queda e cujo caixa comporte a put
   isolada. Esperado: DOIS cards lado a lado, roláveis com o dedo — put de
   proteção primeiro (à esquerda), trava protetora depois.
2. **Cada manchete é a do motor.** Ler as duas manchetes. Esperado: são
   DIFERENTES entre si e nenhuma delas parece montada com pedaços (nada de
   frase truncada com "..."). Comparar a manchete do primeiro card com a que
   aparece para o mesmo ativo na Watchlist: TEXTO IDÊNTICO. Divergência é bug de
   guardrail CVM — reprovar.
3. **Cada card tem payoff próprio.** Esperado: ganho máximo, perda máxima,
   breakeven e caixa DIFERENTES entre os dois cards (são estratégias
   diferentes); nenhum campo mostrando "R$ 0,00" onde deveria estar vazio/"—".
4. **Um candidato só continua igual.** Abrir o detalhe de uma posição em que só
   uma estrutura é elegível. Esperado: o card único de sempre, sem linha de
   rolagem, visualmente idêntico ao de antes desta fase.
5. **Aceite em Modo Operador.** Com o app em Modo Operador, tocar o CTA do
   SEGUNDO card (a trava protetora). Esperado: a confirmação cita a trava do
   lastro; a operação abre as DUAS pernas juntas; enquanto está em voo, o botão
   do outro card também fica desabilitado.
6. **O irmão não executa depois.** Logo em seguida, tocar o CTA do primeiro card
   (a put). Esperado: NÃO abre nada — aparece a mensagem de erro no rodapé
   ("Lastro insuficiente..."). A carteira continua com as duas pernas do collar,
   sem uma terceira posição de opção.
7. **Ordem inversa.** Em outra posição (ou depois de encerrar as pernas),
   aceitar primeiro a put e depois tentar a trava protetora. Esperado: a
   segunda tentativa é recusada com a mensagem de que já existe operação
   lastreada aberta naquela posição.
8. **Modo Estudo.** Trocar para Modo Estudo e reabrir o detalhe com dois
   candidatos. Esperado: os dois cards continuam aparecendo, cada um com a
   frase didática, e NENHUM botão de executar em nenhum dos dois.
9. **iPhone.** Repetir 1, 2 e 5 no aparelho. Esperado: os dois cards rolam
   horizontalmente com o dedo, os botões são tocáveis sem precisar mirar, e a
   manchete não sai cortada em nenhum dos dois.
10. **Risco herdado — decisão sua.** Publicar/empurrar a Fase 19 leva junto os
    fluxos das Fases 17 e 18, que seguem sem checkpoint aprovado (detalhe em
    `<risco_herdado>` deste plano e em `.planning/STATE.md`). Escolher:
    (a) aprovar e empurrar as três fases juntas; (b) aprovar a Fase 19 e segurar
    o push até fechar 17/18 ao vivo; (c) reprovar e listar o que corrigir antes.
  </how-to-verify>
  <action>
Pausar a execução e apresentar ao Alex o roteiro de verificação acima, com o
app já publicado localmente. Não prosseguir, não dar push para `origin` e não
marcar a fase como completa antes da resposta. Registrar, passo a passo, o que
o Alex reportou — inclusive os passos que ele não conseguiu exercitar e o
motivo (mercado fechado, nenhuma posição com leitura de queda no dia, aparelho
indisponível), sem transformar "não testado" em "passou".
  </action>
  <resume-signal>Responder "aprovado" (com a escolha a/b/c do passo 10) ou descrever os problemas encontrados, indicando quais passos falharam.</resume-signal>
  <acceptance_criteria>
    - Os 10 passos foram exercitados com o mercado aberto e o resultado de cada um registrado no SUMMARY (passou / falhou / não aplicável, com o motivo).
    - O passo 2 (manchete de cada candidato idêntica à do motor, e a do primeiro card idêntica à da Watchlist) passou — divergência aqui é reprovação automática, sem exceção.
    - Os passos 6 e 7 (as duas ordens de aceite do irmão) foram exercitados; se a segunda aceitação tiver SUCEDIDO em qualquer das ordens, é reprovação — MULTI-02 critério 3 não foi cumprido.
    - A decisão sobre o risco herdado das Fases 17/18 (a/b/c) está registrada literalmente no SUMMARY e em `.planning/STATE.md`.
    - Nenhum push para `origin` aconteceu antes da resposta do humano.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repositório → `server/web_dist` | Única árvore que o Railway enxerga; um bundle publicado com carimbo velho vira "a fase não funcionou" sem sintoma óbvio. |
| repositório local → `origin` | Push é o que efetivamente leva a mudança para produção; esta fase NÃO empurra sem decisão humana. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-17 | Tampering | `server/web_dist` editado à mão | mitigate | Publicação exclusivamente por `scripts/publicar-web.sh`; critério de aceite confere os três elos do carimbo, que só ficam coerentes pelo caminho do script. |
| T-19-18 | Repudiation | bundle no ar sem carimbo distinguível | mitigate | `bump.sh` antes de `publicar-web.sh`; `SERVER_BUILD_ID` sincronizado pelo script e conferido contra `web/src/version.js` e contra o conteúdo de `server/web_dist`. |
| T-19-19 | Elevation of privilege | publicar ao ar fluxo de aceite nunca verificado ao vivo (Fases 17/18) | mitigate | Checkpoint BLOQUEANTE com decisão explícita a/b/c antes de qualquer push; nenhum push é executado pelo executor. Risco nomeado em `<risco_herdado>` e em `.planning/STATE.md`. |
| T-19-20 | Denial of service | deploy só-backend com carimbo divergente | accept | Não se aplica: esta fase publica front, e `publicar-web.sh` sincroniza `SERVER_BUILD_ID` sozinho. O critério de aceite trava a coerência. |
| T-19-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote novo — o build usa as dependências já instaladas; critério de aceite exige diff vazio em `web/package.json`, `web/package-lock.json` e nos dois `requirements*.txt`. |
</threat_model>

<verification>
- Três elos do carimbo coerentes.
- `bash scripts/executar.sh --testes` verde DEPOIS da publicação.
- Nenhum push para `origin` antes da decisão humana.
</verification>

<success_criteria>
- Fase 19 publicada localmente com carimbo novo e rastreável.
- Roteiro de 10 passos exercitado com mercado aberto (ou o que ficou pendente registrado explicitamente, sem fingir aprovação).
- Decisão a/b/c sobre o risco herdado das Fases 17/18 registrada em `.planning/STATE.md`.
</success_criteria>

<output>
Create `.planning/phases/19-motor-multi-candidato/19-04-SUMMARY.md` when done
</output>
