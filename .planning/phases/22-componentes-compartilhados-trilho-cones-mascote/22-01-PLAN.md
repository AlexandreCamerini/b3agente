---
phase: 22-componentes-compartilhados-trilho-cones-mascote
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/App.jsx
  - web/tests/test_fase22_componentes_compartilhados.mjs
autonomous: true
requirements: [SYS-01]

must_haves:
  truths:
    - "Existe UM único padrão de rolagem horizontal no app: todos os quatro trilhos passam pelo mesmo helper, nenhum define overflowX/scroll-snap por conta própria"
    - "Os três trilhos que hoje rolam solto (filtro MODELO DE ANÁLISE, tira de oportunidades de opções, linha de candidatos da proposta) passam a assentar em item ao parar de rolar"
    - "Nenhum dos três ganhou a largura de 84% do carrossel da home — o filtro segue escaneável com ~3 chips e os candidatos de opção seguem comparáveis lado a lado (Decisão 1 do 22-UI-SPEC)"
    - "Um guardião estático trava o padrão único: um trilho novo que redefina overflowX inline em vez de usar o helper faz a suíte canônica falhar"
  artifacts:
    - path: "web/src/App.jsx"
      provides: "carouselTrackStyle/carouselItemStyle como constantes de módulo, consumidos pelos 4 trilhos"
      contains: "carouselTrackStyle"
    - path: "web/tests/test_fase22_componentes_compartilhados.mjs"
      provides: "guardião da Fase 22, Seção A (SYS-01)"
      contains: "carouselTrackStyle"
  key_links:
    - from: "web/src/App.jsx (4 containers de rolagem horizontal)"
      to: "carouselTrackStyle / carouselItemStyle"
      via: "chamada do helper com overrides de call site"
      pattern: "carouselTrackStyle\\("
    - from: "web/tests/test_fase22_componentes_compartilhados.mjs"
      to: "web/src/App.jsx"
      via: "readFileSync + regex (guardião estático — App.jsx importa @capacitor/core e não é importável fora do build)"
      pattern: "readFileSync"
---

<objective>
Fazer os quatro trilhos horizontais do app passarem por um único mecanismo de
rolagem: um helper compartilhado que carrega `overflowX`/`scrollSnapType`, e
call sites que só declaram o que é legitimamente diferente entre eles
(espaçamento, largura do item, alinhamento do snap).

Purpose: SYS-01 pede "um único padrão de carrossel horizontal usado em todo
lugar". A varredura do `22-UI-SPEC.md` achou 4 trilhos (não 2): um já com
snap (o da home) e três rolando solto. Copiar valores em cada call site
resolveria hoje e divergiria de novo no próximo trilho — por isso o helper,
não o copy-paste.

Output: `App.jsx` com `carouselTrackStyle`/`carouselItemStyle` no nível de
módulo e nenhum `overflowX: "auto"` inline fora deles, mais um guardião novo
que falha se alguém reintroduzir um trilho solto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-componentes-compartilhados-trilho-cones-mascote/22-CONTEXT.md
@.planning/phases/22-componentes-compartilhados-trilho-cones-mascote/22-UI-SPEC.md
@.planning/phases/22-componentes-compartilhados-trilho-cones-mascote/22-PATTERNS.md
@CLAUDE.md

<interfaces>
Constantes de módulo já existentes em `App.jsx`, para colocar os helpers novos
ao lado (verificado ao vivo em 2026-09-05, zero drift):

- `App.jsx:292` — `const card = { background: T.bgCard, border: ..., borderRadius: "12px" };`
- `App.jsx:293` — `const kicker = { fontSize: "10px", color: T.textFaint, letterSpacing: "0.06em" };`

`bash scripts/executar.sh --testes` — suíte canônica (pytest + TODOS os
`web/tests/*.mjs`, descobertos por glob). Um `.mjs` novo em `web/tests/` entra
na suíte automaticamente, sem registro em lugar nenhum.

`cd web && npx vite build` — obrigatório após editar o front: grep e teste
estático NÃO pegam erro de sintaxe JSX (CLAUDE.md, Validação obrigatória).
</interfaces>

<estado_verificado_ao_vivo>
Confirmado por `grep -n` no working tree em 2026-09-05, imediatamente antes de
escrever este plano. Os números do `22-UI-SPEC.md`/`22-PATTERNS.md` continuam
válidos — mas **RE-VERIFIQUE com `grep -n "overflowX" web/src/App.jsx` antes de
editar**, porque este repositório já registrou line drift entre pesquisa e
execução.

`grep -n "overflowX" web/src/App.jsx` → 5 linhas:
- **1949** — HERO-CARROSSEL (setups da home). JÁ tem `scrollSnapType: "x mandatory"`.
- **3792** — filtro `TECH_MODELS` "MODELO DE ANÁLISE" (Watchlist). Sem snap.
- **4001** — tira `OportunidadesOpcoes` (Posições). Sem snap.
- **4133** — linha de candidatos de `PropostaDaPosicao`. Sem snap.
- **9013** — `<main>`, `overflowX: "hidden"`. **NÃO é trilho** — é o shell de
  rolagem vertical da página, e é travado pelo guardião da Fase 20
  (`test_fase20_fundacao_visual.mjs:59`, FIX-01). Não tocar.

`grep -n "scrollSnapType"` → 1 (linha 1949). `grep -n "scrollSnapAlign"` → 1
(linha 1951). `grep -n 'flex: "0 0 84%"'` → 1 (linha 1951).

**Nenhum guardião existente assenta sobre o estilo destes trilhos.** Varredura
feita sobre os 116 arquivos de `web/tests/`: `test_carteira_opcoes_tira.mjs` e
`test_opcoes_multi_candidato_ui.mjs` cobrem o CONTEÚDO/comportamento da tira e
do `CandidatoOpcao` (manchete, CTA, cálculo), zero asserção sobre `style`/
`flex`/`minWidth`/`overflow`. `test_fase20_fundacao_visual.mjs:59` é a única
asserção de `overflowX` e mira exclusivamente `<main>`. Este plano só ADICIONA
guardião — não precisa reescrever nenhum.
</estado_verificado_ao_vivo>

<decisao_do_planner_hero_pelo_helper>
O `22-PATTERNS.md` deixa explícito que rotear ou não o HERO-CARROSSEL pelo
helper é decisão do planner. **Decisão: rotear.** Razões:

1. SYS-01 é literalmente "um único padrão usado em todo lugar". Deixar o
   trilho de referência como o único que NÃO usa o helper deixaria o requisito
   3/4 satisfeito e daria ao próximo agente o exemplo errado para copiar.
2. Permite a asserção mais forte que este guardião pode fazer:
   `overflowX: "auto"` passa a existir em UM lugar só (a definição do helper).
   Sem isso, a única verificação possível é contar ocorrências — que não
   distingue "usou o helper" de "copiou os valores de novo".

O comportamento do HERO **não muda em nada**: ele passa `"x mandatory"` e
`"center"` como override explícito no call site, e mantém
`flex: "0 0 84%"`/`maxWidth: "330px"`/`margin: "0 -18px"`. O estilo computado
sai idêntico ao de hoje. A taxonomia da Decisão 1 do `22-UI-SPEC.md` (peek de
84% SÓ no HERO; `proximity` nos outros três) fica preservada e, agora, visível
como override deliberado em vez de divergência acidental.
</decisao_do_planner_hero_pelo_helper>

<scrollbarwidth_nao_mexer>
O `22-UI-SPEC.md` marca como opcional ("skip if it risks scope creep") remover
`scrollbarWidth: "none"` das linhas 4001 e 4133. **Decisão: NÃO remover, e NÃO
adicionar aos outros dois.** Removê-lo faria aparecer uma barra de rolagem no
Firefox desktop dentro de dois cards onde hoje ela não aparece — uma mudança
visual que ninguém pediu, no plano cujo objetivo é justamente não introduzir
padrão visual novo. O helper carrega só o que era divergência real (snap); os
dois `scrollbarWidth` seguem como override de call site. Registrar essa
não-mudança no SUMMARY, deliberada, para não virar "esqueceram".
</scrollbarwidth_nao_mexer>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guardião da Fase 22, Seção A (SYS-01), escrito antes da mudança (RED)</name>
  <read_first>
    - web/tests/test_fase21_dedup_consolidacao.mjs:1-40 (guardião irmão da fase anterior — copiar a forma: cabeçalho datado explicando o escopo, `readFileSync` de App.jsx, helper `ok(nome, cond)`, `process.exit(fails)`)
    - web/tests/test_fase3_c19_card_status.mjs:20-33 (padrão da casa para ISOLAR o corpo de um componente e ABORTAR se o marcador sumir, em vez de virar no-op silencioso)
    - web/src/App.jsx:1946-1955 (HERO-CARROSSEL: container 1949, item 1951 — o padrão de referência)
    - web/src/App.jsx:3790-3800 (container do TECH_MODELS + o botão-chip no `.map()`)
    - web/src/App.jsx:3999-4018 (container da tira OportunidadesOpcoes + o botão no `.map()`)
    - web/src/App.jsx:4131-4137 e :4156-4172 (container de PropostaDaPosicao + a `<div>` interna de `CandidatoOpcao`, onde o item vive)
    - web/src/App.jsx:290-295 (as constantes de módulo `card`/`kicker` — vizinhança onde os helpers novos entram)
  </read_first>
  <files>web/tests/test_fase22_componentes_compartilhados.mjs</files>
  <behavior>
    Com o `App.jsx` AINDA NÃO editado, o guardião novo deve FALHAR nestas
    asserções (prova de que tem dentes e não é no-op):
    - "existe `const carouselTrackStyle`" → falha, ainda não existe
    - "existe `const carouselItemStyle`" → falha, ainda não existe
    - "`overflowX: \"auto\"` aparece exatamente 1× em App.jsx" → falha, hoje há 4
    - "os 4 containers de trilho chamam `carouselTrackStyle(`" → falha, 0 chamadas
    E deve PASSAR nestas, que descrevem o que NÃO muda:
    - "`flex: \"0 0 84%\"` aparece exatamente 1× (só o HERO)"
    - "`<main>` mantém `overflowX: \"hidden\"` (FIX-01, Fase 20, fora do escopo do helper)"
  </behavior>
  <action>
Criar `web/tests/test_fase22_componentes_compartilhados.mjs` seguindo o padrão
de guardião estático da casa (regex sobre `App.jsx` lido com `readFileSync` —
o módulo importa `@capacitor/core` e não é importável fora do build; ver o
cabeçalho de `test_fase21_dedup_consolidacao.mjs`).

Cabeçalho do arquivo: comentário datado dizendo que este é o guardião ÚNICO da
Fase 22 (v1.5, Redesenho de UI), que a Seção A cobre SYS-01 (plano 22-01), e
que os planos 22-02 e 22-03 vão **ESTENDER ESTE MESMO ARQUIVO** com as Seções
B (SYS-02, ícones), C (SYS-02, Radar + tier dot + varredura de emoji zero) e D
(SYS-03, sombra do PetFab) — para o próximo agente não criar um arquivo
paralelo. Organizar o corpo com um separador de seção explícito
(`// --- Seção A — SYS-01 ...`), porque outros dois planos vão escrever aqui
depois.

Asserções obrigatórias da Seção A:

1. `const carouselTrackStyle` existe em `App.jsx` e sua definição contém
   `overflowX: "auto"`, `display: "flex"`, `scrollSnapType: "x proximity"` e
   `WebkitOverflowScrolling: "touch"`. Isolar a definição com o padrão da casa
   (do `const carouselTrackStyle` até o próximo `\nconst `), ABORTANDO com
   `process.exit(1)` e mensagem explícita se o marcador não for encontrado.
2. `const carouselItemStyle` existe e sua definição contém `scrollSnapAlign`.
3. **A asserção central do padrão único:**
   `(app.match(/overflowX: "auto"/g) || []).length === 1` — depois desta fase,
   `overflowX: "auto"` só pode existir DENTRO da definição do helper. Um trilho
   novo que redefina inline quebra aqui. Mensagem de falha deve dizer isso em
   texto: "trilho horizontal novo deve usar carouselTrackStyle(), não redefinir
   overflowX inline".
4. `(app.match(/carouselTrackStyle\(/g) || []).length >= 5` — a definição mais
   os 4 call sites.
5. `(app.match(/carouselItemStyle\(/g) || []).length >= 5` — a definição mais os
   4 itens (o item de `PropostaDaPosicao` mora dentro de `CandidatoOpcao`, não
   no `.map()`).
6. Presença nomeada de cada um dos 4 call sites, cada um isolado por uma
   assinatura textual estável do próprio site (NÃO por número de linha):
   - o recorte que contém `TECH_MODELS.map(` chama `carouselTrackStyle(`;
   - o recorte de `function OportunidadesOpcoes` chama `carouselTrackStyle(`;
   - o recorte de `function PropostaDaPosicao` chama `carouselTrackStyle(`;
   - o recorte de `function EvolucaoScreen` (onde vive o HERO-CARROSSEL) chama
     `carouselTrackStyle(`.
   Usar o padrão de isolamento por `function Xxx(` → próximo `\nfunction `,
   abortando explicitamente se qualquer marcador sumir. Para o `TECH_MODELS`,
   isolar pela linha que contém `TECH_MODELS.map(` e a linha imediatamente
   anterior que abre o container.
7. **Taxonomia preservada (Decisão 1 do 22-UI-SPEC):**
   `(app.match(/flex: "0 0 84%"/g) || []).length === 1` — o peek de 84% segue
   exclusivo do HERO. Mensagem de falha: "peek de 84% é do HERO-CARROSSEL
   apenas — chips de filtro e candidatos de opção precisam ficar comparáveis
   lado a lado".
8. `"x mandatory"` aparece exatamente 1× e `scrollSnapAlign: "center"` (ou
   `carouselItemStyle("center")`) aparece exatamente 1× — o sub-variante de foco
   único é só do HERO.
9. `/<main[^>]*overflowX: "hidden"/` continua verdadeiro — a Fase 20 (FIX-01)
   não foi atropelada pelo helper.

Rodar `node web/tests/test_fase22_componentes_compartilhados.mjs` e CONFIRMAR
que ele falha nas asserções 1, 2, 3, 4, 5 e 6 (e passa nas 7, 8, 9). Registrar
a saída literal no SUMMARY — é a prova de que o guardião tem dentes.

NÃO editar `App.jsx` nesta task.
  </action>
  <verify>
    <automated>node web/tests/test_fase22_componentes_compartilhados.mjs; test $? -ne 0 && echo "RED confirmado (esperado nesta task)"</automated>
  </verify>
  <acceptance_criteria>
    - `web/tests/test_fase22_componentes_compartilhados.mjs` existe e roda com `node` sem build.
    - Rodado contra o `App.jsx` NÃO editado, o script sai com código diferente de 0.
    - A saída contém `FALHOU` para as asserções de `carouselTrackStyle`, `carouselItemStyle` e para a contagem de `overflowX: "auto"`.
    - A saída contém `ok ` para a asserção de `flex: "0 0 84%"` valer exatamente 1× e para a de `<main>` com `overflowX: "hidden"`.
    - O arquivo aborta com mensagem explícita (nunca passa em silêncio) se qualquer marcador de isolamento (`function EvolucaoScreen(`, `function OportunidadesOpcoes(`, `function PropostaDaPosicao(`, `TECH_MODELS.map(`) não for encontrado.
    - O cabeçalho do arquivo nomeia as Seções B, C e D como responsabilidade dos planos 22-02 e 22-03, no MESMO arquivo.
    - `git status --porcelain web/src/App.jsx` sai VAZIO (nenhuma edição de App.jsx nesta task).
  </acceptance_criteria>
  <done>O guardião da Fase 22 existe, entrou na suíte por glob, e provou ter dentes falhando contra o código atual pelas razões certas.</done>
</task>

<task type="auto">
  <name>Task 2: Helper compartilhado e os 4 trilhos roteados por ele (GREEN)</name>
  <read_first>
    - web/src/App.jsx:290-295 (onde `card`/`kicker` vivem — os helpers novos entram logo depois)
    - web/src/App.jsx:1946-1955 (HERO-CARROSSEL — RE-VERIFICAR com `grep -n "overflowX" web/src/App.jsx` antes de editar)
    - web/src/App.jsx:3790-3800 (TECH_MODELS)
    - web/src/App.jsx:3999-4018 (OportunidadesOpcoes)
    - web/src/App.jsx:4131-4137 e :4156-4172 (PropostaDaPosicao + CandidatoOpcao)
    - .planning/phases/22-componentes-compartilhados-trilho-cones-mascote/22-PATTERNS.md (o "AFTER (shape)" de cada site já está escrito lá, literal — copiar de lá, não reinventar)
  </read_first>
  <files>web/src/App.jsx</files>
  <action>
**Passo 0 — re-verificar.** `grep -n "overflowX" web/src/App.jsx` deve devolver
as 5 linhas listadas em `<estado_verificado_ao_vivo>`. Se os números tiverem
mudado, seguir pelo CONTEÚDO (as assinaturas textuais), nunca pelo número.

**Passo 1 — definir os dois helpers**, imediatamente após `const kicker = ...`
(linha ~293), no mesmo estilo de constante de módulo:

    // Fase 22 (SYS-01): padrão ÚNICO de rolagem horizontal do app. Todo trilho
    // passa por aqui — `overflowX: "auto"` não deve existir inline em nenhum
    // outro lugar de App.jsx (o guardião da Fase 22 trava isso).
    // `extra` é mesclado por último: gap/margin/padding/scrollbarWidth e, no
    // caso do HERO-CARROSSEL, o override de scrollSnapType.
    const carouselTrackStyle = (extra) => ({
      display: "flex",
      overflowX: "auto",
      scrollSnapType: "x proximity",
      WebkitOverflowScrolling: "touch",
      ...extra,
    });
    // `align`: "start" para trilhos de navegar/comparar (o padrão); "center"
    // só para o HERO-CARROSSEL, único trilho de foco em um card dominante.
    const carouselItemStyle = (align = "start") => ({ scrollSnapAlign: align });

`proximity` (e não `mandatory`) é deliberado: os três trilhos novos são linhas
de escaneio/comparação com muitos itens curtos, e `mandatory` brigaria com a
rolagem livre. Está registrado na Decisão 1 do `22-UI-SPEC.md`.

**Passo 2 — HERO-CARROSSEL (linha ~1949/1951).** Rotear pelo helper mantendo o
comportamento byte a byte (ver `<decisao_do_planner_hero_pelo_helper>`):
- container: `style={carouselTrackStyle({ gap: "12px", scrollSnapType: "x mandatory", margin: "0 -18px", padding: "2px 18px 6px" })}`
- item: `style={{ ...card, ...carouselItemStyle("center"), flex: "0 0 84%", maxWidth: "330px", borderLeft: \`3px solid ${T.accent}\`, padding: "15px 16px", textAlign: "left", cursor: "pointer" }}`
Nada mais muda no HERO: `flex: "0 0 84%"`, `maxWidth: "330px"` e o
`margin: "0 -18px"` que sangra até a borda do viewport ficam exatamente como
estão.

**Passo 3 — TECH_MODELS (linha ~3792).**
- container: `style={carouselTrackStyle({ gap: "8px", paddingBottom: "2px" })}`
- item (o `<button key={id}>` dentro do `.map()`): acrescentar
  `...carouselItemStyle("start")` no INÍCIO do objeto de estilo, antes de
  `minWidth: "118px"`.
- **`minWidth: "118px"` NÃO muda.** Nenhum `flex`/`maxWidth` novo. É o ponto
  central da Decisão 1: são 8 chips que o usuário escaneia; forçar 84% de
  largura mostraria ~1,2 chip por vez e transformaria um filtro em um swipe
  item a item.

**Passo 4 — OportunidadesOpcoes (linha ~4001).**
- container: `style={carouselTrackStyle({ gap: "10px", scrollbarWidth: "none", paddingBottom: "2px" })}`
  (o `scrollbarWidth: "none"` PERMANECE — ver `<scrollbarwidth_nao_mexer>`).
- item (o `<button key={p.t}>` dentro do `.map()`): acrescentar
  `...carouselItemStyle("start")` antes de `flex: "0 0 auto"`.
- `flex: "0 0 auto"` e `minWidth: "210px"` NÃO mudam.

**Passo 5 — PropostaDaPosicao (linha ~4133) + CandidatoOpcao (linha ~4170).**
- container em ~4133: `style={carouselTrackStyle({ marginTop: "11px", gap: "10px", scrollbarWidth: "none", paddingBottom: "2px" })}`.
- **Atenção — o item NÃO está no `.map()` aqui.** A `<div>` do item mora dentro
  da própria função `CandidatoOpcao` (linha ~4170). O
  `...carouselItemStyle("start")` entra NAQUELA `<div>`, antes de
  `flex: "0 0 auto"`. Adicionar no call site do `.map()` (~4134) não teria
  efeito nenhum e passaria despercebido.
- `flex: "0 0 auto"` e `minWidth: "210px"` NÃO mudam — os candidatos precisam
  continuar visíveis lado a lado (é o que a Fase 19 construiu, MULTI-01/02).

**Regras que valem para os 5 passos:**
- Estilo 100% inline com spread das constantes de módulo. Zero CSS Modules,
  zero Tailwind, zero classe nova, zero dependência nova.
- Nenhum valor de `gap`/`padding`/`margin`/`minWidth` é renumerado: todos os
  valores de espaçamento de hoje são preservados literalmente
  (`22-UI-SPEC.md`, Spacing Scale — os quatro valores estão assinados).
- `<main>` (linha ~9013) NÃO é trilho e NÃO é tocado.

Depois das edições, nesta ordem:
1. `node web/tests/test_fase22_componentes_compartilhados.mjs` — agora tudo passa.
2. `cd web && npx vite build` — obrigatório: grep e teste estático não pegam
   erro de sintaxe JSX (CLAUDE.md).
3. `bash scripts/executar.sh --testes` — suíte canônica, as DUAS suítes.

Se a suíte acusar falha em QUALQUER teste que não seja o guardião novo, PARAR e
reportar: a varredura deste plano concluiu que nenhum outro guardião assenta
sobre o estilo destes trilhos, então uma falha inesperada significa que a
varredura errou e a premissa precisa ser reaberta, não contornada.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'overflowX: "auto"' web/src/App.jsx` retorna exatamente `1` (só a definição do helper).
    - `grep -c 'carouselTrackStyle(' web/src/App.jsx` retorna pelo menos `5` (definição + 4 call sites).
    - `grep -c 'carouselItemStyle(' web/src/App.jsx` retorna pelo menos `5` (definição + 4 itens).
    - `grep -c 'flex: "0 0 84%"' web/src/App.jsx` retorna exatamente `1`, e `grep -n 'flex: "0 0 84%"' web/src/App.jsx` aponta para o item do HERO-CARROSSEL.
    - `grep -c '"x mandatory"' web/src/App.jsx` retorna exatamente `1` (o override do HERO).
    - `grep -c 'minWidth: "118px"' web/src/App.jsx` retorna `1` (chip do TECH_MODELS intacto) e `grep -c 'minWidth: "210px"' web/src/App.jsx` retorna `2` (tira + CandidatoOpcao intactos).
    - `grep -n 'carouselItemStyle' web/src/App.jsx` mostra uma ocorrência DENTRO do corpo de `function CandidatoOpcao` (não apenas no call site do `.map()` de `PropostaDaPosicao`).
    - `grep -n '<main' web/src/App.jsx` continua mostrando `overflowX: "hidden"` (FIX-01 da Fase 20 intacto).
    - `node web/tests/test_fase22_componentes_compartilhados.mjs` sai com código 0.
    - `cd web && npx vite build` conclui com código 0.
    - `bash scripts/executar.sh --testes` sai com código 0 — as DUAS suítes.
    - `git diff --stat` lista apenas `web/src/App.jsx` e `web/tests/test_fase22_componentes_compartilhados.mjs`; `git diff web/package.json web/package-lock.json` sai vazio.
    - Nenhum `git push` foi executado.
  </acceptance_criteria>
  <done>Os quatro trilhos horizontais do app passam pelo mesmo helper, os três que rolavam solto assentam em item, e o guardião trava a regra de que nenhum trilho futuro redefine `overflowX` inline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helper compartilhado → call sites | O helper é mesclado ANTES do `extra`; um override de call site pode silenciosamente anular o snap. O guardião mede o resultado no arquivo, não a intenção. |
| unificação de estilo → UX existente | Aplicar o peek de 84% fora do HERO regride duas interações já entregues (filtro de 8 chips; comparação lado a lado da Fase 19). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | Um trilho novo redefinindo `overflowX` inline e divergindo de novo do padrão | mitigate | Asserção 3 do guardião: `overflowX: "auto"` só pode existir 1× em App.jsx, dentro do helper |
| T-22-02 | Denial of service | Aplicar `flex: "0 0 84%"` ao TECH_MODELS ou aos candidatos de opção, escondendo conteúdo que o usuário precisa comparar | mitigate | Asserção 7 do guardião (`flex: "0 0 84%"` === 1) + critérios de aceite que travam `minWidth: "118px"` e `minWidth: "210px"` |
| T-22-03 | Repudiation | `scrollSnapAlign` do `PropostaDaPosicao` adicionado no `.map()` (sem efeito) em vez de dentro de `CandidatoOpcao` | mitigate | Passo 5 nomeia o erro explicitamente; critério de aceite exige a ocorrência DENTRO do corpo de `CandidatoOpcao` |
| T-22-04 | Denial of service | Erro de sintaxe JSX passando por grep e teste estático e quebrando o app inteiro | mitigate | `npx vite build` obrigatório, com código 0 como critério de aceite |
| T-22-05 | Tampering | Helper genérico demais engolir o `overflowX: "hidden"` do `<main>` e quebrar FIX-01 da Fase 20 | mitigate | Asserção 9 do guardião + critério de aceite explícito sobre `<main>` |
| T-22-SC | Tampering | instalações npm/pip/cargo | mitigate | Zero pacote novo neste plano; `git diff` de `web/package.json`/`package-lock.json` vazio como critério de aceite |
</threat_model>

<verification>
- `overflowX: "auto"` com exatamente 1 ocorrência (a definição do helper).
- Os 4 trilhos chamando `carouselTrackStyle(`; os 4 itens chamando `carouselItemStyle(`.
- `flex: "0 0 84%"` e `"x mandatory"` exclusivos do HERO-CARROSSEL.
- Larguras de item dos outros 3 trilhos inalteradas (118px / 210px×2).
- `<main>` com `overflowX: "hidden"` intacto.
- Guardião novo verde e provado com dentes (falhou antes, passa depois).
- `npx vite build` verde; `scripts/executar.sh --testes` verde nas duas suítes.
</verification>

<success_criteria>
O critério 1 do ROADMAP da Fase 22 está satisfeito no código e travado por
guardião: todo trilho horizontal do app rola pelo mesmo mecanismo, assenta em
item, e nenhum deles perdeu a densidade de conteúdo que já tinha.
</success_criteria>

<output>
Create `.planning/phases/22-componentes-compartilhados-trilho-cones-mascote/22-01-SUMMARY.md` when done
</output>
