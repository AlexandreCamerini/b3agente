---
phase: 04-corre-o-m-dio-storyline-ux
plan: 07
type: execute
wave: 4
depends_on: ["04-01", "04-06"]
files_modified:
  - web/src/finance.js
  - web/src/copy.js
  - web/src/App.jsx
  - web/tests/test_finance.mjs
  - web/tests/test_concentracao_carteira.mjs
autonomous: true
requirements: [FIX-C05]
must_haves:
  truths:
    - "Quando um ativo passa de 50% do patrimônio simulado, a Carteira avisa — sem bloquear nada"
    - "O aviso leva ao verbete de diversificação, que explica o conceito com o número daquela carteira"
    - "O conceito de diversificação deixa de estar ausente do produto (13º da lista obrigatória do CLAUDE.md)"
  artifacts:
    - path: "web/src/finance.js"
      provides: "concentracaoMaxima() — maior posição em % do patrimônio"
      exports: ["concentracaoMaxima"]
    - path: "web/src/copy.js"
      provides: "copy de concentração nos DOIS modos (chaves espelhadas)"
      contains: "concentracao"
    - path: "web/src/App.jsx"
      provides: "card de aviso T.warn no CarteiraScreen + link saiba mais para o verbete"
      contains: "CONCENTRAÇÃO ALTA"
  key_links:
    - from: "web/src/App.jsx CarteiraScreen"
      to: "conceito diversificacao"
      via: "abrirVerbete('diversificacao', {ticker, pct}) -> ConceitoSheet -> POST /api/conceito/diversificacao"
      pattern: "diversificacao"
    - from: "web/src/App.jsx CarteiraScreen"
      to: "finance.concentracaoMaxima"
      via: "posValue/patr com as mesmas cotações do portfolioMetrics"
      pattern: "concentracaoMaxima"
---

<objective>
Fechar FIX-C05 no front: a tela de Carteira passa a avisar quando um único ativo
responde por mais de 50% do patrimônio simulado, e o aviso abre o verbete
`diversificacao` criado no Plano 04-01.

Purpose: "diversificação" é um dos 13 conceitos obrigatórios da seção "Camada
educacional" do `CLAUDE.md` e tinha ZERO ocorrência no produto inteiro. O
backend já ganhou o conceito; sem este plano ele continua inalcançável pelo
usuário.

Output: aritmética pura de concentração, copy por modo, card de aviso e link de
drill-down.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-corre-o-m-dio-storyline-ux/04-CONTEXT.md
@.planning/phases/04-corre-o-m-dio-storyline-ux/04-UI-SPEC.md
@.planning/phases/04-corre-o-m-dio-storyline-ux/04-01-SUMMARY.md
@CLAUDE.md
@.claude/skills/didatica-boris/SKILL.md
</context>

<interfaces>
<!-- Citações verificadas contra o fonte vivo em 2026-08-21. RE-GREP mesmo assim. -->

`web/src/finance.js`:
- `markPrice(quote, position)` linhas 31-36 — cotação > 0, senão cai no `avg`.
- `portfolioMetrics(positions, quotes, cash, reservado)` linhas 38-59 → devolve
  `{posVal, cost, openPnL, openPct, dayVal, patr, cash, reservado}`. `patr` inclui
  `cash + reservado + posVal`. NÃO devolve valor POR POSIÇÃO — é isso que falta.

`web/src/copy.js`:
- `COPY.estudo` (linha 19) e `COPY.operador` (linha ~160). Guardião
  `web/tests/test_copy_theme.mjs:27-28` compara `Object.keys` dos dois modos e
  FALHA se divergirem — chave nova entra nos DOIS.
- Vocabulário de ordem (comprar/vender) é PROIBIDO no ramo `estudo` (guardião).
- `copyFor(mode)` linha 265; `ctx.cp` no App.jsx já é o dicionário do modo ativo.

`web/src/App.jsx`:
- `function CarteiraScreen({ ctx })` linha 3493. Destructuring na linha 3499
  (`const { data, quotes, analysis, A, goMercado, cp } = ctx;`), `m = portfolioMetrics(...)`
  linha 3502, `total = m.patr` linha 3504, helper `kpi(...)` 3507-3513, grid de
  KPIs 3526-3531, ramo de portfólio vazio 3533-3540, lista de posições a partir de 3542.
- `const SUBLINHADO = {...}` linha 2295; `function ConceitoSheet(...)` linha 2394 —
  abre via estado `conceitoAberto` (`{cid, dados, trilha, setor?, viaProativa?}`),
  declarado na linha 6597 (`const [conceitoAberto, setConceitoAberto] = useState(null)`),
  renderizado nas linhas 7946-7954.
- Ações existentes que abrem a folha: `A.openConceito(cid, dados)` linha 7285 —
  MARCA `viaProativa: true` e dispara `track("coach_tip_shown")`; `A.abrirSetor(...)`
  linha 7293 — conta `gestoUso`. NENHUMA das duas serve para um link comum.
- Cabeçalho com ícone-em-círculo a reusar: `AnalysisView` "FATOS RELEVANTES",
  linhas 1202-1205 (`<svg width="15" height="15" viewBox="0 0 24 24">` com
  `<circle r="9.5">` + `<path d="M12 11v5M12 7.5h.01">`).
- Caixa de aviso `T.warn` estabelecida: `BuyModal` `statusIndisponivel`, 6471-6474.
- `ctx.didatica` — objeto com `.ligada`; a camada de conceitos pode estar
  desligada por config do servidor (`conceitos.didatica_ligada()`).
- Regra de cor (comentário na linha 723): "T.positive/T.negative/T.warn — NUNCA
  T.accent: o acento muda de matiz [Estudo↔Operador], sinal não pode mudar com o modo".

Backend disponível (Plano 04-01): conceito `diversificacao` em
`conceitos.CONCEITOS`, com `campos = ("ticker", "pct")`, servido por
`POST /api/conceito/diversificacao` com corpo `{dados: {ticker, pct}}` —
o mesmo caminho que `ConceitoSheet` já usa (`store.conceito(cid, {dados})`).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: concentracaoMaxima() — aritmética pura da concentração (finance.js)</name>
  <files>web/src/finance.js, web/tests/test_finance.mjs</files>
  <behavior>
    - `concentracaoMaxima(positions, quotes, patr)` devolve `{t, valor, pct}` da MAIOR posição, ou `null`.
    - Usa `markPrice(quote, position)` — exatamente o mesmo preço de marcação de `portfolioMetrics`, para o `pct` bater com o patrimônio exibido na tela.
    - `patr <= 0`, `positions` vazio/não-array, ou nenhuma posição com `qty > 0` → `null`.
    - `pct` é `valor / patr * 100`, sem arredondamento na função (a UI formata).
    - Empate entre duas posições de mesmo valor → devolve a primeira em ordem de `positions` (determinístico, sem `sort` instável).
    - Posição com `qty` ou `avg` não numérico é tratada como 0, nunca `NaN`.
  </behavior>
  <read_first>
    - web/src/finance.js (linhas 28-60: `markPrice` e `portfolioMetrics` — a fonte da aritmética que precisa ser espelhada)
    - web/tests/test_finance.mjs (padrão `ok`/`near` e os casos de `portfolioMetrics`)
    - .planning/phases/04-corre-o-m-dio-storyline-ux/04-UI-SPEC.md (seção "FIX-C05" item 1: `posValue(p) / total` com `total = m.patr`)
    - CLAUDE.md (princípio 5: cálculo determinístico, nunca pela IA)
  </read_first>
  <action>
Escreva PRIMEIRO os casos em `web/tests/test_finance.mjs`, depois implemente.

Exporte `concentracaoMaxima(positions, quotes, patr)` em `web/src/finance.js`,
junto de `portfolioMetrics` (é a mesma família de cálculo de carteira).

- Percorre `positions` uma vez, calculando `valor = qty * markPrice(quotes[p.t], p)`.
- Guarda o maior `valor` e devolve `{t, valor, pct: (valor / patr) * 100}`.
- Guard clauses no topo, padrão da base: `patr` não numérico ou `<= 0` → `null`;
  `positions` não-array ou vazio → `null`.
- `Number(p.qty) || 0` e a mesma defensiva de `portfolioMetrics` para os campos.
- Função PURA. Não toca em `cash`, `reservado` nem em `optionPositions` (posição
  de opção é coleção própria, ADR-003, e o aviso do UI-SPEC fala de ativo).
- NÃO aplique o limiar de 50% aqui: a função devolve o número, quem decide o
  corte é a UI. Assim o limiar fica visível num lugar só e testável.

Não altere `portfolioMetrics`, `equityCurve`, `benchmarkSerie` nem `markPrice`.
  </action>
  <verify>
    <automated>cd web && node tests/test_finance.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Duas posições (PETR4 100@33 = 3300; VALE3 50@54 = 2700) com `patr = 10000` → `{t: "PETR4", valor: 3300, pct: 33}` (comparação `near` até 1e-9).
    - `concentracaoMaxima([], {}, 10000)` === `null`; `concentracaoMaxima(pos, q, 0)` === `null`; `concentracaoMaxima(null, null, null)` === `null`.
    - Posição sem cotação usa `avg` (mesmo resultado que `markPrice` devolveria) — verificado com um caso em que `quotes` está vazio.
    - `qty: "abc"` não produz `NaN` em `valor` nem em `pct`.
    - Empate exato de valor → devolve a PRIMEIRA da lista (caso explícito no teste).
    - A soma dos `valor` de todas as posições calculada pelo mesmo laço bate com `portfolioMetrics(...).posVal` (consistência entre as duas funções, testada num caso).
    - Todos os casos antigos de `test_finance.mjs` continuam passando.
  </acceptance_criteria>
  <done>`concentracaoMaxima` devolve a maior posição em % do patrimônio usando exatamente o mesmo preço de marcação da tela, sem limiar embutido e sem `NaN`.</done>
</task>

<task type="auto">
  <name>Task 2: Aviso de concentração alta na Carteira, com link para o verbete (FIX-C05, UI)</name>
  <files>web/src/copy.js, web/src/App.jsx, web/tests/test_concentracao_carteira.mjs</files>
  <read_first>
    - .planning/phases/04-corre-o-m-dio-storyline-ux/04-UI-SPEC.md (seção "FIX-C05" INTEIRA — focal point, os 4 itens do contrato visual e a copy dos DOIS modos, verbatim)
    - web/src/App.jsx (linhas 3493-3545: `CarteiraScreen` até o fim do grid de KPIs e o ramo de portfólio vazio)
    - web/src/App.jsx (linhas 1195-1215: o cabeçalho ícone-em-círculo de "FATOS RELEVANTES" a reusar)
    - web/src/App.jsx (linhas 6465-6480: a caixa `T.warn` do BuyModal)
    - web/src/App.jsx (linhas 7280-7325: `openConceito`, `abrirSetor`, `trocarConceito`, `voltarConceito`, `closeConceito` — para adicionar a ação nova no mesmo bloco, com o mesmo estilo)
    - web/src/App.jsx (linhas 6595-6600 e 7940-7960: declaração e render de `conceitoAberto`)
    - web/src/copy.js (linhas 18-120 do ramo `estudo` e o ramo `operador` correspondente; `subtituloPortfolio`/`vazioPortfolio` como referência de registro de voz)
    - web/tests/test_copy_theme.mjs (linhas 20-60: o guardião de chaves espelhadas e de vocabulário de ordem proibido no Estudo)
    - .planning/phases/04-corre-o-m-dio-storyline-ux/04-01-SUMMARY.md (o id do conceito e os `campos` aceitos)
  </read_first>
  <action>
Implementa FIX-C05 no front per decisão do CONTEXT.md ("aviso na tela de Carteira
quando a concentração num único ativo passar de 50% do patrimônio") e o contrato
visual do UI-SPEC.

1. `web/src/copy.js` — chaves NOVAS nos DOIS modos (o guardião compara
   `Object.keys` e falha se divergirem):
   - `concentracaoTitulo`: `"Concentração alta"` (igual nos dois — é rótulo, não voz).
   - `concentracaoCorpo`: função `(ticker, pct) => string`, com a copy VERBATIM do
     UI-SPEC:
     - estudo: `{ticker} sozinho responde por {pct}% do seu patrimônio simulado. Diversificação reduz o quanto um único evento negativo pode derrubar a carteira inteira — vale estudar o conceito antes de aumentar ainda mais essa posição.`
     - operador: `{ticker} concentra {pct}% da carteira. Acima disso, um único stop ruim carrega peso desproporcional no resultado — considere o tamanho antes do próximo aporte no papel.`
   - `concentracaoLink`: `"saiba mais"` nos dois.
   Cuidado com o guardião de vocabulário: o ramo `estudo` não pode conter verbo
   de ordem de operação. As frases acima já respeitam isso — não as reescreva.

2. `web/src/App.jsx` — ação nova de drill-down. No mesmo bloco de `A` onde vivem
   `openConceito`/`abrirSetor` (linhas ~7285-7300), adiciona:
   `abrirVerbete: (cid, dados) => setConceitoAberto({ cid, dados: dados || null, trilha: [] }),`
   Por que não reusar as existentes: `openConceito` marca `viaProativa: true` e
   dispara `track("coach_tip_shown")` (é a via do gatilho, one-shot por conceito);
   `abrirSetor` incrementa `config.gestoUso`, a medição do sublinhado pontilhado.
   Um link comum não é nenhuma das duas — contaminar essas métricas é defeito de
   medição. Deixe um comentário curto explicando isso, no estilo do arquivo.

3. `web/src/App.jsx` — card de aviso em `CarteiraScreen`, ENTRE o grid de KPIs
   (fecha na linha ~3531) e o ramo de portfólio vazio (linha ~3533):
   - `const conc = concentracaoMaxima(data.positions, quotes, m.patr);`
     (importa `concentracaoMaxima` de `./finance.js`, na linha de import 15).
   - `const LIMIAR_CONCENTRACAO = 50;` — constante nomeada, no topo do módulo
     junto das outras constantes de regra, não número mágico inline.
   - Renderiza somente quando `conc && conc.pct > LIMIAR_CONCENTRACAO`.
   - Caixa: `padding: "13px 14px"`, `borderRadius: "11px"`,
     `background: "color-mix(in srgb, " + T.warn + " 12%, transparent)"`,
     `border: 1px solid ${T.warn}` — mesma gramática de aviso soft já usada no
     BuyModal e no aviso de prontidão do Plano 04-03. NÃO invente caixa nova.
   - Cabeçalho: ícone-em-círculo copiado do padrão de "FATOS RELEVANTES"
     (linhas 1202-1205) com `stroke={T.warn}` no lugar de `T.textSecondary`, e
     rótulo `CONCENTRAÇÃO ALTA` em 11px, `fontWeight: 800`,
     `letterSpacing: "0.05em"`, `color: T.warn`.
   - Corpo: `cp.concentracaoCorpo(conc.t, Math.round(conc.pct))` em 12.5px,
     `T.textPrimary`, `lineHeight: 1.5`.
   - Link: `<button type="button">` com `cp.concentracaoLink`, estilo de link
     inline (`background: "transparent"`, `border: "none"`, `padding: 0`,
     `color: T.accent`, `fontWeight: 700`, `fontSize: "12px"`,
     `textDecoration: "none"`), chamando
     `A.abrirVerbete("diversificacao", { ticker: conc.t, pct: Math.round(conc.pct) })`.
     Renderiza o link SOMENTE quando `ctx.didatica && ctx.didatica.ligada` — com a
     camada desligada o backend devolve `null` e a folha mostraria erro; o aviso
     em si continua aparecendo (ele é determinístico e independente da camada).
   - `T.accent` aqui é legítimo e NÃO viola a regra da linha 723: é um LINK/CTA,
     não a cor do sinal. O sinal do aviso é `T.warn` na caixa e no rótulo.
   - Não bloqueia nada: nenhuma confirmação, nenhum botão de compra desabilitado,
     nenhuma posição escondida. É aviso educacional (CONTEXT.md), não hard stop.
   - Hierarquia: o grid de KPIs continua sendo o primeiro elemento lido; o card
     não pode ter tipografia maior nem animação.

Cria `web/tests/test_concentracao_carteira.mjs`: guardião estático travando
(a) `LIMIAR_CONCENTRACAO = 50` e a condição de render `> LIMIAR_CONCENTRACAO`,
(b) a caixa usa `T.warn` e NÃO usa `T.negative` (não é erro), (c) o rótulo
`CONCENTRAÇÃO ALTA` existe, (d) o link chama `abrirVerbete("diversificacao", ...)`
com `ticker` e `pct`, (e) o card NÃO desabilita nem esconde nada (nenhum
`disabled` novo no `CarteiraScreen`), (f) as 3 chaves de copy existem nos DOIS
modos de `copy.js`, com o texto verbatim do UI-SPEC.
  </action>
  <verify>
    <automated>cd web && node tests/test_concentracao_carteira.mjs && node tests/test_copy_theme.mjs && node tests/test_conceito_ui.mjs && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CONCENTRAÇÃO ALTA" web/src/App.jsx` == 1 e `grep -c "LIMIAR_CONCENTRACAO" web/src/App.jsx` >= 2 (declaração + uso).
    - A condição de render é `conc.pct > LIMIAR_CONCENTRACAO` (estritamente maior — 50% exatos NÃO dispara, per "passar de 50%").
    - No bloco do card, `T.warn` aparece e `T.negative` NÃO aparece.
    - `A.abrirVerbete` existe, NÃO chama `track(` e NÃO toca em `gestoUso` (verificado por regex no guardião).
    - A chamada do link passa `{ ticker: ..., pct: ... }` — os únicos dois `campos` que o conceito aceita.
    - `Object.keys(COPY.estudo)` === `Object.keys(COPY.operador)` (test_copy_theme.mjs verde) e as 3 chaves novas estão nos dois.
    - O corpo do modo `estudo` é idêntico byte a byte à copy do UI-SPEC (comparação de string literal no guardião).
    - Nenhum `disabled` novo foi introduzido em `CarteiraScreen` (`git diff` do bloco não adiciona `disabled=`).
    - `npx vite build` em `web/` conclui sem erro.
  </acceptance_criteria>
  <done>A Carteira avisa sobre concentração acima de 50% com a gramática `T.warn` já estabelecida, na voz do modo ativo, com "saiba mais" abrindo o verbete `diversificacao` ancorado no ticker e no percentual reais — sem bloquear nenhuma ação.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| estado da carteira → cálculo exibido | `positions`/`quotes` vêm do servidor ou do doc local e viram um percentual apresentado como fato financeiro |
| link do front → allowlist de conceitos do backend | `cid`/`dados` vão para `POST /api/conceito/{cid}` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-22 | Tampering | `dados` enviados a `/api/conceito/diversificacao` | mitigate | `conceitos._valores` só interpola as chaves da allowlist `campos = ("ticker","pct")` — chave fora dela é ignorada e não reescreve vocabulário (guardião `test_chamador_nao_reescreve_o_vocabulario_canonico`) |
| T-04-23 | Spoofing | percentual exibido divergindo do patrimônio da tela | mitigate | `concentracaoMaxima` usa o MESMO `markPrice` e o MESMO `m.patr` de `portfolioMetrics`; teste de consistência entre as duas funções |
| T-04-24 | Elevation of Privilege | aviso virando bloqueio de operação | mitigate | guardião assere ausência de `disabled` novo em `CarteiraScreen`; CONTEXT.md classifica como aviso, não hard stop |
| T-04-25 | Denial of Service | cálculo por render em carteira grande | accept | laço O(n) sobre `positions` (dezenas de itens no pior caso realista), mesma ordem de grandeza de `portfolioMetrics`, que já roda no mesmo render |
| T-04-SC | Tampering | instalação de pacotes | accept | nenhum pacote novo em `web/package.json` |
</threat_model>

<verification>
- `cd web && npx vite build` — obrigatório.
- `bash scripts/executar.sh --testes` — suíte canônica verde antes de fechar o plano.
- Verificação ao vivo recomendada: carteira com uma posição acima de 50% mostra o card e o "saiba mais" abre a folha do conceito com o ticker e o percentual certos; carteira equilibrada não mostra nada.
</verification>

<success_criteria>
- `concentracaoMaxima` existe, é pura e consistente com `portfolioMetrics`.
- As 3 chaves de copy existem nos dois modos, com o texto do UI-SPEC.
- O card aparece acima de 50%, usa `T.warn`, não bloqueia nada e abre o verbete `diversificacao`.
- "diversificação" deixa de ter zero ocorrência no produto (backend no 04-01, front aqui).
- Suíte canônica verde e `npx vite build` verde.
</success_criteria>

<output>
Create `.planning/phases/04-corre-o-m-dio-storyline-ux/04-07-SUMMARY.md` when done
</output>
