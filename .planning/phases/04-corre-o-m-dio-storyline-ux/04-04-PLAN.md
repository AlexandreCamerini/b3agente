---
phase: 04-corre-o-m-dio-storyline-ux
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - server/app/main.py
  - server/tests/test_rotas_fase4.py
autonomous: true
requirements: [FIX-C01, FIX-C02, FIX-C03]
must_haves:
  truths:
    - "Usuário grátis sem chave de IA recebe uma explicação determinística no Passo 7 em vez de HTTP 502"
    - "Toda rejeição de /api/buy e /api/sell fica gravada no histórico antes do erro voltar ao cliente"
    - "O cliente consegue buscar a série do Ibovespa e recebe erro claro quando o Yahoo falha"
  artifacts:
    - path: "server/app/main.py"
      provides: "fallback determinístico nas 2 rotas de análise, registro de rejeição em buy/sell, rota GET /api/benchmark/ibov"
      contains: "benchmark"
    - path: "server/tests/test_rotas_fase4.py"
      provides: "guardião das 3 fiações de rota"
  key_links:
    - from: "server/app/main.py"
      to: "server/app/explicacao_det.py"
      via: "fallback quando _gate_analise ou llm.analyze_structured falham"
      pattern: "explicacao_det\\.montar"
    - from: "server/app/main.py"
      to: "server/app/store.py"
      via: "registrar_rejeicao antes de cada HTTPException de buy/sell"
      pattern: "registrar_rejeicao"
    - from: "server/app/main.py"
      to: "server/app/benchmark.py"
      via: "rota GET /api/benchmark/ibov"
      pattern: "serie_ibov"
---

<objective>
Ligar os motores dos Planos 04-01 e 04-02 nas rotas HTTP: o Passo 7 deixa de
devolver 502 quando não há IA (FIX-C01), `/api/buy` e `/api/sell` param de
descartar a tentativa rejeitada (FIX-C02), e o cliente ganha acesso à série do
Ibovespa (FIX-C03).

Purpose: este é o único plano da fase que toca `server/app/main.py` — por isso
concentra a fiação dos três achados de backend. Os contratos de resposta já estão
FIXADOS (ver `<interfaces>`) e os planos de front dependem deles literalmente.

Output: 2 rotas de análise com fallback, 2 rotas de carteira com rastro de
rejeição, 1 rota nova de benchmark, 1 guardião.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-corre-o-m-dio-storyline-ux/04-CONTEXT.md
@.planning/phases/04-corre-o-m-dio-storyline-ux/04-01-SUMMARY.md
@.planning/phases/04-corre-o-m-dio-storyline-ux/04-02-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
<!-- Citações verificadas contra o fonte vivo em 2026-08-21. RE-GREP mesmo assim. -->

`server/app/main.py`:
- `@app.post("/api/technical/analyze/{ticker}")` linha 1400, handler `analyze_technical_model` — a rota que os DOIS stores realmente usam (`persistence.js` → `api.analyzeTechnical`)
- `@app.post("/api/analyze/{ticker}")` linha 1554, handler `analyze` — rota legada (contador `LEGACY_ANALYZE`, linha 1551); corrigir também, o CONTEXT.md cita as duas
- `_gate_analise(scope, config, custo=1)` linha 435 — levanta `HTTPException(402, reason)` ANTES de qualquer chamada de LLM quando plano/cota bloqueiam; devolve `(config, consume)`
- `_ai_apply_managed` linha 406 — mesma família, também levanta 402
- ponto de falha da IA em `analyze_technical_model`: linhas 1462-1466
  (`except Exception as e: raise HTTPException(502, llm.public_error(e))`)
- ponto de falha da IA em `analyze`: linhas 1590-1594 (mesma forma)
- `llm.public_error(exc) -> dict` (`server/app/llm.py:148`) — payload PÚBLICO já sanitizado; `LLMUserError` carrega `code` (ex.: `"missing_key"`, `llm.py:497`)
- snapshot indisponível: `except ValueError: raise HTTPException(502, "Sem historico para " + t)` (linhas 1430-1431 e 1581-1582) — este 502 PERMANECE (sem snapshot não há nada determinístico a dizer)
- `store.set_analysis(_conn, t, payload, user_id=scope)` linhas 1542-1543 / 1612-1613 — só quando `not body.get("config")`
- `prompt_fp = llm.prompt_fingerprint(...)` linha 1449 e o reuso por `promptFp` linhas 1455-1461
- `@app.post("/api/buy")` linha 1696 e `@app.post("/api/sell")` linha 1753 — os pontos de rejeição de HOJE:
  - buy: `len(t) < 4` → 400 "Ticker invalido." (1702-1703); sem cotação → 502 (1707-1708);
    `qty*price > cash` dentro de `store.ORDER_LOCK` → 400 "Caixa insuficiente." (1725-1726);
    escopo anônimo fora do pregão → 401 (1734-1739); `pending_orders.CaixaInsuficiente` → 400 (1742-1743)
  - sell: sem posição → 400 (1757-1759); sem cotação → 502 (1761-1762); qty inválida → 400 (1771-1776);
    sem posição na releitura sob trava → 400 (1786-1787); escopo anônimo → 401 (1796-1798);
    `pending_orders.PosicaoInsuficiente` → 400 (1801-1802)
- `@app.get("/api/history/{ticker}")` linha 1067 — modelo de rota GET simples com `candles_mod.period_to_range(period)`
- `_normalize_ticker(s)` linha 1080; `current_scope` dependência de escopo linha ~94

Do Plano 04-01: `explicacao_det.montar(ticker, snap, modo, quote) -> {"markdown", "fonte", "verbetes", "semDados", "asOf"}`
Do Plano 04-02: `store.registrar_rejeicao(conn, tipo, t, qty, price, motivo, user_id, origem) -> dict`
                `benchmark.serie_ibov(period) -> dict` / `benchmark.BenchmarkIndisponivel`

## CONTRATOS DE RESPOSTA FIXADOS (os planos de front 04-05 e 04-06 dependem deles)

Rotas de análise, campos ADITIVOS no payload (nenhum campo existente muda de nome ou tipo):
```
fonte:          "ia" | "deterministico"      # sempre presente a partir daqui
iaIndisponivel: null | {"code": str|null, "mensagem": str}
verbetes:       list[str]                    # só no caminho determinístico
semDados:       bool                         # só no caminho determinístico
```
Caminho determinístico devolve HTTP 200 (nunca 502) e preenche `markdown`.

Histórico (já definido no Plano 04-02):
```
status: "executada" | "rejeitada"   # ausente = legado = tratar como executada
motivo: str                         # só em rejeitada
```

Nova rota:
```
GET /api/benchmark/ibov?period=<periodo>
200 -> {"t":"^BVSP","nome":"Ibovespa","fonte":"yahoo","candles":[{"date","close"}],"asOf"}
502 -> {"detail": "Comparação com o Ibovespa indisponível agora."}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fallback determinístico nas duas rotas de análise (FIX-C01)</name>
  <files>server/app/main.py, server/tests/test_rotas_fase4.py</files>
  <read_first>
    - server/app/main.py (linhas 1400-1545: `analyze_technical_model` inteiro; linhas 1554-1615: `analyze` inteiro; linhas 406-455: `_ai_apply_managed` e `_gate_analise`)
    - server/app/llm.py (linhas 95-160: `LLMUserError`, `_cfg_payload`, `public_error`; linha 497: o ponto que produz `code="missing_key"`)
    - server/app/explicacao_det.py (o módulo entregue pelo Plano 04-01 — assinatura e retorno)
    - .planning/phases/04-corre-o-m-dio-storyline-ux/04-01-SUMMARY.md
    - .planning/phases/04-corre-o-m-dio-storyline-ux/04-CONTEXT.md (decisão C-01)
    - server/tests/test_assistente_kb.py (padrão de teste de rota com TestClient nesta base)
  </read_first>
  <action>
Implementa a fiação de FIX-C01 per decisão do CONTEXT.md: "quando a IA não
estiver disponível, montar uma explicação mínima determinística — nunca um card
vazio ou só o erro técnico".

Em `analyze_technical_model` (rota `/api/technical/analyze/{ticker}`) e em
`analyze` (rota legada `/api/analyze/{ticker}`), aplica o MESMO padrão:

1. Envolve a chamada de `_gate_analise(scope, config)` em try/except
   `HTTPException`. Se `e.status_code == 402` (plano/cota esgotados), guarda
   `ia_indisponivel = {"code": "quota", "mensagem": str(e.detail)}` e SEGUE em
   frente com `config` original e `_consume_ai = lambda: None`. Qualquer outro
   status é re-levantado sem alteração.
2. O bloco do snapshot (`except ValueError: raise HTTPException(502, "Sem historico...")`)
   NÃO muda: sem snapshot não existe nada determinístico a dizer, e um 502 aqui é
   a resposta honesta.
3. Envolve a chamada de LLM. Se `ia_indisponivel` já está setado, PULA a chamada
   inteira (não gastar rede nem log de atividade por uma cota que já negou).
   Se a chamada levantar exceção, `ia_indisponivel = {"code": (public.get("code")), "mensagem": (public.get("message") or public.get("detail"))}`
   derivado EXCLUSIVAMENTE de `llm.public_error(e)` — nunca `str(e)`, nunca
   `repr(e)`, nunca o traceback. Ver `<threat_model>` T-04-01.
4. Quando `ia_indisponivel` está setado:
   - chama `explicacao_det.montar(t, snap, modo=("operador" if modo == "operador" else "educacional"), quote=quote)`
   - monta o payload de resposta com os MESMOS nomes de campo que o caminho de
     IA já usa (`markdown`, `snapshotId`, `snapshotAt`, `at`, `candles`,
     `setupsRadar`, `technicalContext` onde aplicável), acrescido de
     `fonte: "deterministico"`, `iaIndisponivel`, `verbetes`, `semDados`.
     Campos que só a IA produz (`kpis`, `detail`, `proposal`, `text`,
     `confiancaFinal`, `rebaixadoPorFundamento`, `fundamento`) vão como `None` —
     nunca com valor sintético.
   - NÃO chama `_consume_ai()` (não houve consumo), NÃO chama
     `ai_activity.registrar_uso`, NÃO chama `analysis_outcomes.registrar` (não há
     proposta de stop/alvo para medir assertividade — registrar aqui contaminaria
     a instrumentação da Fase 6).
   - NÃO chama `store.set_analysis(...)`. Persistir o fallback com `promptFp`
     faria o reuso por fingerprint devolver a explicação sem-IA PARA SEMPRE,
     mesmo depois de a chave voltar. O fallback é efêmero por desenho.
   - Devolve HTTP 200.
5. No caminho de sucesso da IA, adiciona ao payload `fonte: "ia"` e
   `iaIndisponivel: None`. Só isso — o resto do payload não muda.

Cria/estende `server/tests/test_rotas_fase4.py` com os casos de C-01 (TestClient
+ monkeypatch de `llm.analyze_structured` e de `_gate_analise`): IA lançando
`LLMUserError(code="missing_key")` → 200 com `fonte == "deterministico"` e
`markdown` não vazio; gate levantando 402 → 200 determinístico e `llm` NUNCA
chamado; snapshot ausente → segue 502; IA OK → `fonte == "ia"`; caminho
determinístico NÃO grava em `analyses`.
  </action>
  <verify>
    <automated>cd server && ./.venv/bin/python -m pytest -q tests/test_rotas_fase4.py</automated>
  </verify>
  <acceptance_criteria>
    - `POST /api/technical/analyze/PETR4` com `llm.analyze_structured` lançando `LLMUserError(..., code="missing_key")` → status 200, `body["fonte"] == "deterministico"`, `body["markdown"] != ""`, `body["iaIndisponivel"]["code"] == "missing_key"`.
    - Mesma requisição: `body["iaIndisponivel"]["mensagem"]` NÃO contém "Traceback", "api_key", "sk-", nem o nome do host do provedor.
    - `_gate_analise` levantando `HTTPException(402, ...)` → status 200 e um espião em `llm.analyze_structured` registra ZERO chamadas.
    - Depois de uma resposta determinística, `store.get(conn, "analyses")` NÃO contém o ticker (fallback não persiste).
    - Espiões confirmam ZERO chamadas de `ai_activity.registrar_uso` e de `analysis_outcomes.registrar` no caminho determinístico.
    - Snapshot indisponível (`technical_snapshot.get` lançando `ValueError`) → continua 502.
    - Caminho de IA bem-sucedido → `body["fonte"] == "ia"` e `body["iaIndisponivel"] is None`; todos os campos que existiam antes continuam presentes.
    - `POST /api/analyze/PETR4` (rota legada) apresenta o mesmo comportamento nos 3 casos principais.
  </acceptance_criteria>
  <done>Nenhum usuário sem chave/sem cota recebe 502 no Passo 7: as duas rotas devolvem 200 com explicação determinística, sem consumir cota, sem poluir instrumentação e sem vazar erro cru do provedor.</done>
</task>

<task type="auto">
  <name>Task 2: Registro da tentativa rejeitada em /api/buy e /api/sell (FIX-C02)</name>
  <files>server/app/main.py, server/tests/test_rotas_fase4.py</files>
  <read_first>
    - server/app/main.py (linhas 1690-1810: `/api/buy` e `/api/sell` completos, incluindo os comentários de T-02-35 sobre a trava e de F10-20260819 sobre a normalização de lote)
    - server/app/store.py (a função `registrar_rejeicao` entregue pelo Plano 04-02 e o `ORDER_LOCK`)
    - server/app/pending_orders.py (`CaixaInsuficiente`, `PosicaoInsuficiente` — o texto das exceções vira `motivo`)
    - .planning/phases/04-corre-o-m-dio-storyline-ux/04-02-SUMMARY.md
    - .planning/phases/04-corre-o-m-dio-storyline-ux/04-UI-SPEC.md (seção "FIX-C02": o tom exigido da copy de `motivo`)
    - server/tests/test_ordens_pendentes_rotas.py (padrão de teste de rota de carteira nesta base)
  </read_first>
  <action>
Implementa a fiação de FIX-C02 per decisão do CONTEXT.md: "toda tentativa de
ordem (aceita OU rejeitada) grava `status` e `motivo`, mesmo sem abrir posição —
o caso mais educativo precisa ficar revisável no histórico".

Em `/api/buy` e `/api/sell`, ANTES de cada `raise HTTPException` que representa
uma REJEIÇÃO DE ORDEM, chama `store.registrar_rejeicao(...)` com o motivo em
linguagem de usuário. Pontos exatos (re-grep para confirmar as linhas):

buy — `Ticker invalido` (400), `Sem cotacao` (502), `Caixa insuficiente` (400,
DENTRO da trava), anônimo fora do pregão (401), `pending_orders.CaixaInsuficiente` (400).
sell — `Sem posicao` (400, as duas ocorrências), `Sem cotacao` (502),
`Quantidade inválida` (400, as duas), anônimo fora do pregão (401),
`pending_orders.PosicaoInsuficiente` (400).

Regras de implementação:
- `scope is None` (anônimo) → NÃO registra. O balde anônimo é COMPARTILHADO entre
  todos os anônimos (mesma razão de T-02-07 que já barra ordem pendente
  anônima): gravar rejeição lá vazaria a tentativa de um usuário na tela de
  outro. O 401 continua igual.
- A chamada de `Caixa insuficiente` do caminho imediato acontece DENTRO do
  `with store.ORDER_LOCK` já existente — mesma trava, mesma escrita de kv.
- `price` é o preço já obtido quando existe; `None` quando a rejeição foi
  exatamente por não haver cotação. Nunca 0.0.
- `qty` é a quantidade JÁ NORMALIZADA para lote de 100 quando a normalização
  já ocorreu naquele ponto; a quantidade crua do corpo quando ainda não.
- `t` é sempre o resultado de `_normalize_ticker` — nunca a string crua do corpo
  (ver `<threat_model>` T-04-03).

Copy de `motivo`, com o tom fixado pelo UI-SPEC (direto, específico, sem jargão,
espelhando `"Caixa insuficiente. Disponível: {money}"` que o BuyModal já usa):
- caixa: `Caixa insuficiente para {qty} {ticker} a R$ {preço} — disponível: R$ {valor}.`
- sem cotação: `Sem cotação disponível para {ticker} no momento do pedido.`
- ticker inválido: `{ticker} não é um ativo reconhecido pelo simulador.`
- sem posição: `Você não tem posição em {ticker} para vender.`
- quantidade inválida: `Quantidade inválida: {valor}. A venda usa lotes de 100.`
- posição reservada insuficiente: o texto da própria `PosicaoInsuficiente`.

A `HTTPException` continua sendo levantada com a MESMA mensagem e o MESMO status
de hoje — nenhum contrato de erro existente muda. O registro é aditivo.

Estende `server/tests/test_rotas_fase4.py` com os casos de C-02.
  </action>
  <verify>
    <automated>cd server && ./.venv/bin/python -m pytest -q tests/test_rotas_fase4.py tests/test_ordens_pendentes_rotas.py tests/test_ciclo_imediato_apos_carteira.py</automated>
  </verify>
  <acceptance_criteria>
    - `POST /api/buy {"t":"PETR4","qty":100000}` com caixa insuficiente → status 400 (inalterado) E `store.get(conn,"history",user_id=scope)[0]["status"] == "rejeitada"` com `motivo` contendo "Caixa insuficiente".
    - `POST /api/sell` de ticker sem posição → 400 (inalterado) E entrada `status="rejeitada"`, `type="VENDA"`, `pnl is None`.
    - Rejeição por falta de cotação → entrada com `price is None` (não 0, não 0.0).
    - Requisição anônima (`scope is None`) rejeitada → `store.get(conn,"history",user_id=None)` NÃO ganhou entrada nenhuma.
    - Compra bem-sucedida → `history[0]["status"] == "executada"` e `cash`/`positions` corretos (nenhuma regressão de aritmética).
    - Após uma rejeição, `store.get(conn,"cash")` é idêntico ao valor de antes da requisição.
    - `t` gravado é o normalizado: `POST /api/buy {"t":"petr4"}` inválido grava `"PETR4"`, nunca `"petr4"`.
    - `tests/test_ordens_pendentes_rotas.py` continua verde (fluxo de pendentes intacto).
  </acceptance_criteria>
  <done>Toda rejeição de ordem de conta logada fica gravada com status/motivo/preço honestos, o contrato de erro HTTP não mudou, nada de dinheiro se moveu e o balde anônimo continua limpo.</done>
</task>

<task type="auto">
  <name>Task 3: Rota GET /api/benchmark/ibov (FIX-C03)</name>
  <files>server/app/main.py, server/tests/test_rotas_fase4.py</files>
  <read_first>
    - server/app/main.py (linhas 1060-1078: `/api/history/{ticker}` — modelo de rota GET com `period_to_range`; linha ~94: `current_scope`)
    - server/app/benchmark.py (o módulo entregue pelo Plano 04-02)
    - server/app/candles.py (procure `period_to_range` e `normalize_period`)
    - .planning/phases/04-corre-o-m-dio-storyline-ux/04-02-SUMMARY.md
    - .planning/phases/04-corre-o-m-dio-storyline-ux/04-UI-SPEC.md (seção "FIX-C03" item 5: o modo de falha — nunca esconder o gráfico, nunca fabricar linha reta)
  </read_first>
  <action>
Implementa a fiação de FIX-C03 per decisão do CONTEXT.md: expor a série do
Ibovespa para o Passo 8 comparar o retorno da carteira com um índice real.

Adiciona a rota:

`@app.get("/api/benchmark/ibov")` com handler
`async def benchmark_ibov(period: Optional[str] = None, scope: Optional[str] = Depends(current_scope))`.

- Sem parâmetro de ticker. O símbolo é constante em `benchmark.SIMBOLO` — a rota
  NÃO aceita símbolo do cliente (ver `<threat_model>` T-04-04).
- Chama `await benchmark.serie_ibov(period)` e devolve o dict como está.
- `except benchmark.BenchmarkIndisponivel:` → `raise HTTPException(502, "Comparação com o Ibovespa indisponível agora.")`.
  Essa frase é a MESMA que o UI-SPEC manda o front exibir — texto único, tom
  informativo e não alarmante (CLAUDE.md princípio 4).
- Não exige escopo: a curva é dado público de mercado, e a rota já é
  cacheada/limitada no módulo. Mantém a dependência `current_scope` só pela
  consistência com as outras rotas (não usa o valor para decidir nada).
- Posicionamento no arquivo: junto das outras rotas de dado de mercado (perto de
  `/api/history/{ticker}`), NUNCA depois do `app.mount()` do catch-all `/` — a
  ordem de montagem importa nesta base.

Estende `server/tests/test_rotas_fase4.py` com os casos de C-03.
  </action>
  <verify>
    <automated>cd server && ./.venv/bin/python -m pytest -q tests/test_rotas_fase4.py tests/test_benchmark_ibov.py</automated>
  </verify>
  <acceptance_criteria>
    - `GET /api/benchmark/ibov` com `serie_ibov` monkeypatchado → 200 e corpo com as chaves `t`, `nome`, `fonte`, `candles`, `asOf`.
    - `serie_ibov` lançando `BenchmarkIndisponivel` → 502 com `detail == "Comparação com o Ibovespa indisponível agora."` (comparação de string exata).
    - A mensagem de 502 NÃO contém nome de host, "crumb", "cookie" nem `str` da exceção original.
    - A assinatura do handler NÃO tem nenhum parâmetro chamado `ticker`, `t` ou `symbol` (`grep` no bloco da rota).
    - `GET /api/benchmark/ibov` duas vezes seguidas com o mesmo `period` → o fake de `_yfetch` foi invocado uma única vez (o cache do módulo está de fato no caminho).
    - `cd server && ./.venv/bin/python -m pytest -q` verde — nenhuma rota existente quebrou e o catch-all continua montado por último.
  </acceptance_criteria>
  <done>`GET /api/benchmark/ibov` serve a série diária do índice, degrada com uma frase única e honesta quando o Yahoo falha, e não aceita símbolo do cliente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → API | corpo/query de `/api/buy`, `/api/sell`, `/api/*analyze`, `/api/benchmark/ibov` são entrada não confiável |
| provedor de IA → resposta do usuário | exceção do provedor pode carregar chave/URL/stack |
| servidor → Yahoo | rota nova é o único caminho novo para rede externa nesta fase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | fallback de C-01 nas rotas de análise | mitigate | `iaIndisponivel.mensagem` sai SÓ de `llm.public_error(e)`; teste assere ausência de "Traceback"/"api_key"/"sk-"/host do provedor |
| T-04-03 | Tampering | `motivo`/`t` gravados no histórico | mitigate | `t` sempre via `_normalize_ticker`; `motivo` montado por f-string do servidor com valores já validados; truncado em 200 chars no `store` |
| T-04-04 | SSRF / Denial of Service | `GET /api/benchmark/ibov` | mitigate | sem parâmetro de símbolo (constante `^BVSP`); `period` só passa por `period_to_range` (allowlist); cache TTL 900s no módulo |
| T-04-12 | Information Disclosure | rejeição gravada no balde anônimo compartilhado | mitigate | `scope is None` não registra nada — mesma razão de T-02-07 (Fase 2) |
| T-04-13 | Denial of Service | fallback determinístico gastando CPU por request | accept | `explicacao_det` é composição de string sobre um snapshot já em memória; custo desprezível e menor que o caminho de LLM que ele substitui |
| T-04-14 | Repudiation | fallback poluindo `analysis_outcomes` | mitigate | caminho determinístico não chama `analysis_outcomes.registrar` nem `ai_activity.registrar_uso`; espiões no teste garantem zero chamadas |
| T-04-SC | Tampering | instalação de pacotes | accept | nenhum pacote novo; só módulos internos entregues pelos Planos 04-01/04-02 |
</threat_model>

<verification>
- `cd server && ./.venv/bin/python -m pytest -q` — suíte backend inteira verde.
- `bash scripts/executar.sh --testes` — suíte canônica verde antes de fechar o plano.
- Exercitar as rotas de verdade (não só unitário): subir `bash scripts/run.sh` e conferir `GET /api/benchmark/ibov` e um `POST /api/buy` que rejeita, confirmando o histórico via `GET /api/state`.
</verification>

<success_criteria>
- As duas rotas de análise devolvem 200 + `fonte: "deterministico"` quando a IA não está disponível, sem consumir cota nem persistir a análise.
- Toda rejeição de ordem de conta logada aparece no histórico com `status`/`motivo`; contrato de erro HTTP inalterado.
- `GET /api/benchmark/ibov` existe, cacheia e degrada com a frase única.
- Nenhum arquivo de `web/` tocado (a UI é dos Planos 04-05/04-06).
</success_criteria>

<output>
Create `.planning/phases/04-corre-o-m-dio-storyline-ux/04-04-SUMMARY.md` when done
</output>
