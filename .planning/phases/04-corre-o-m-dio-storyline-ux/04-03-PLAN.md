---
phase: 04-corre-o-m-dio-storyline-ux
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/App.jsx
  - web/tests/test_brand_book_v2_tokens.mjs
  - web/tests/test_copy_theme.mjs
  - web/tests/test_acessibilidade_acordeao.mjs
  - web/tests/test_prontidao_operador.mjs
autonomous: true
requirements: [FIX-C16, FIX-C15, FIX-C04]
must_haves:
  truths:
    - "textFaint atinge 4.5:1 em TODAS as superfícies onde renderiza, nos 2 temas e nos 2 modos"
    - "Usuário por teclado consegue abrir e fechar o acordeão de opções com Enter e Espaço"
    - "Quem nunca abriu uma análise no Estudo vê um aviso pedagógico antes de ativar o Operador — e consegue ativar mesmo assim"
  artifacts:
    - path: "web/src/App.jsx"
      provides: "4 hex de textFaint corrigidos, onKeyDown/aria-expanded nos 2 toggles, painel de prontidão em ModoTrabalhoCard"
      contains: "aria-expanded"
    - path: "web/tests/test_acessibilidade_acordeao.mjs"
      provides: "guardião do teclado no acordeão"
    - path: "web/tests/test_prontidao_operador.mjs"
      provides: "guardião do aviso de prontidão (soft, nunca bloqueio)"
  key_links:
    - from: "web/src/App.jsx ModoTrabalhoCard"
      to: "escolher('operador')"
      via: "painel inline entre o segmented control e a descrição; TermoOperadorModal segue intacto depois"
      pattern: "TermoOperadorModal"
    - from: "web/tests/test_brand_book_v2_tokens.mjs"
      to: "PALETTE/MODE_OPERADOR textFaint"
      via: "loop de contraste AA passa a incluir textFaint nas 3 superfícies"
      pattern: "textFaint"
---

<objective>
Fechar os três achados de front que NÃO dependem de nenhuma mudança de backend:
contraste WCAG AA de `textFaint` (FIX-C16), acordeão acessível por teclado
(FIX-C15) e o aviso pedagógico de prontidão Estudo→Operador (FIX-C04).

Purpose: são os três achados de menor acoplamento da fase — rodam na Wave 1 em
paralelo com o backend porque nenhum plano da Wave 1 toca `App.jsx`.

Output: `App.jsx` com 4 hex corrigidos, 2 toggles acessíveis e 1 painel novo;
2 guardiões existentes atualizados com nota e 2 guardiões novos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-corre-o-m-dio-storyline-ux/04-CONTEXT.md
@.planning/phases/04-corre-o-m-dio-storyline-ux/04-UI-SPEC.md
@CLAUDE.md
</context>

<interfaces>
<!-- Citações verificadas contra o fonte vivo em 2026-08-21. RE-GREP mesmo assim:
     App.jsx tem 7997 linhas e cresce a cada fase. -->

`web/src/App.jsx`:
- `const BRAND = {` linha 40 · `const PALETTE = {` linha 61 · `const MODE_OPERADOR = {` linha 114
- `PALETTE.dark.textFaint` linha 70 (`"#6f7797"`) · `PALETTE.light.textFaint` linha 87 (`"#7a8099"`)
- `MODE_OPERADOR.dark.textFaint` linha 126 (`"#5b6890"`) · `MODE_OPERADOR.light.textFaint` linha 143 (`"#7a8a85"`)
- superfícies: dark `bgBase #10121a` / `bgPanel #161927` / `bgCard #1b1f2e` (linha 67);
  light `bgBase #f7f8fc` / `bgPanel #eef0f7` / `bgCard #ffffff` (linha 84);
  Operador dark `#0a0c12`/`#10131c`/`#141926` (linha 124); Operador light `#f2f6f4`/`#e9efec`/`#ffffff` (linha 141)
- foco global: `.b3 :focus-visible{ outline:2px solid ${T.accent}; outline-offset:2px; border-radius:4px; }` linha 296
- `function OpcaoContrato(` linha 2747; seu toggle: `<div onClick={onToggle} role="button" tabIndex={0} ...>` linha 2760
- `function OpcoesCamada(` linha 2816; seu toggle: mesma forma, linha 2827; o painel expandido abre na linha 2834 (`borderRadius:"11px"`, SEM `overflow:hidden` — verificar antes de mexer)
- `function ModoTrabalhoCard({ ctx })` linha 1879: `escolher` linha 1884-1905, segmented control linha 1910-1913, texto descritivo linha 1914-1918, `{termoOpen && <TermoOperadorModal .../>}` linha 1919
- `function TermoOperadorModal(` linha 1924 — NÃO alterar: o gate legal continua exatamente como está
- padrão de caixa de aviso soft (`T.warn`) já estabelecido: `BuyModal` `statusIndisponivel`, linhas 6471-6474 —
  `padding:"9px 11px", borderRadius:"9px", background:"color-mix(in srgb, "+T.warn+" 12%, transparent)", border:"1px solid "+T.warn`
- estado disponível em `ctx.data`: `data.analyses` (dict `ticker -> payload`, presente nos DOIS stores —
  `serverStore` via `public_state`, `deviceStore` via `doc.analyses`)

Guardiões que hoje travam os hex ANTIGOS (têm que ser atualizados com nota, nunca apagados):
- `web/tests/test_brand_book_v2_tokens.mjs` linhas 93-99 (`NEUTROS.dark.textFaint`/`NEUTROS.light.textFaint`)
  e o loop de contraste da seção 5 (linhas ~152-166) que hoje NÃO inclui `textFaint` — essa omissão É o bug C-16
- `web/tests/test_copy_theme.mjs` linha 167 (`textFaint: "#5b6890"` do Operador escuro)
- `web/tests/test_mode_operador_light_palette.mjs` só checa PRESENÇA das chaves, sem hex — confirmar que segue verde

Comandos: teste isolado `cd web && node tests/test_x.mjs`; build `cd web && npx vite build`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: textFaint acima de 4.5:1 nos 4 pares tema × modo (FIX-C16)</name>
  <files>web/src/App.jsx, web/tests/test_brand_book_v2_tokens.mjs, web/tests/test_copy_theme.mjs</files>
  <read_first>
    - .planning/phases/04-corre-o-m-dio-storyline-ux/04-UI-SPEC.md (seção "FIX-C16" — método, tabela de valores atuais, tabela de valores novos, e a nota de escopo sobre o par do Modo Operador)
    - web/src/App.jsx (linhas 61-150: PALETTE e MODE_OPERADOR completos, incluindo o comentário de metodologia de acento nas linhas 88-93)
    - web/tests/test_brand_book_v2_tokens.mjs (arquivo inteiro, 190 linhas: cabeçalho que documenta a divergência deliberada, `scheme()`, `contrast()`, seção 2 `NEUTROS`, seção 5 do loop de AA)
    - web/tests/test_copy_theme.mjs (linha 167 e o contexto do bloco R3)
    - web/tests/test_mode_operador_light_palette.mjs (arquivo inteiro, 40 linhas)
    - CLAUDE.md (guardrail "Guardiões de teste não se apagam — reversão deliberada atualiza o guardião com nota")
  </read_first>
  <action>
Implementa FIX-C16 com os hex FINAIS já calculados e verificados no UI-SPEC.
NÃO recalcule nem aproxime — use exatamente estes quatro valores:

| Onde | Linha (re-grep) | De | Para |
|---|---|---|---|
| `PALETTE.dark.textFaint` | 70 | `#6f7797` | `#7f86a2` |
| `PALETTE.light.textFaint` | 87 | `#7a8099` | `#666c85` |
| `MODE_OPERADOR.dark.textFaint` | 126 | `#5b6890` | `#7581a8` |
| `MODE_OPERADOR.light.textFaint` | 143 | `#7a8a85` | `#616e6a` |

Os dois pares do `MODE_OPERADOR` entram no MESMO commit por decisão explícita do
UI-SPEC: é o mesmo token, o mesmo call site (`T.textFaint`), o mesmo texto de
usuário, medindo 3,10–3,62:1 — pior que os valores que a auditoria mediu. Fechar
C-16 só nos dois primeiros deixaria o bug vivo em toda sessão em Modo Operador.

Deixa um comentário curto acima de cada hex novo dizendo por que ele mudou
(WCAG AA 4.5:1, achado C-16 do REPORT-01) e citando a razão de contraste do pior
caso — o arquivo já usa esse estilo de comentário-decisão (ver linhas 88-93).

NÃO mexa em `P.textFaint` usado como STROKE do placeholder tracejado do
`CapitalCurve` (`App.jsx:1627`): é uso não-textual, a regra de contraste de texto
não se aplica, e o UI-SPEC marca explicitamente "no change needed".

NÃO mexa em `textDim` do tema claro (`#6b7288`, linha 86). O UI-SPEC identificou
que ele mede 4,20:1 e também reprova AA, mas marcou como FORA DE ESCOPO desta
fase (backlog). Registre esse achado no SUMMARY do plano em vez de corrigi-lo.

Guardiões (a regra da casa é atualizar com nota, nunca apagar):

1. `web/tests/test_brand_book_v2_tokens.mjs`
   - Atualiza `NEUTROS.dark.textFaint` para `"#7f86a2"` e `NEUTROS.light.textFaint`
     para `"#666c85"`, com um comentário de 2-3 linhas explicando que o Brand
     Book v2 especificava `--muted-2 #6f7797`/`#7a8099`, que esses valores
     reprovavam AA (3,43–4,24:1 conforme a superfície) e que C-16 os substitui
     pelo mesmo tom com luminosidade ajustada — mesma metodologia que a seção 3
     do próprio arquivo já usa para justificar o dourado claro `#8a6c1c`.
   - Estende a seção 5: inclui `textFaint` na lista de tokens auditados E troca a
     superfície única (`bgCard`) por um laço nas TRÊS superfícies (`bgBase`,
     `bgPanel`, `bgCard`) para esse token. O pior caso é `bgCard` no escuro e
     `bgPanel` no claro — testar só o card deixaria o claro passar por engano.
     Essa omissão é literalmente o bug que C-16 fecha; o guardião tem que passar
     a cobri-la.
2. `web/tests/test_copy_theme.mjs` linha 167: troca `textFaint: "#5b6890"` por
   `textFaint: "#7581a8"`, com comentário citando C-16.
3. `web/tests/test_mode_operador_light_palette.mjs`: rode e confirme que segue
   verde (ele só checa presença de chave). Se falhar, ajuste com nota.
  </action>
  <verify>
    <automated>cd web && node tests/test_brand_book_v2_tokens.mjs && node tests/test_copy_theme.mjs && node tests/test_mode_operador_light_palette.mjs && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '#6f7797\|#7a8099\|#5b6890\|#7a8a85' web/src/App.jsx` retorna 0 (nenhum dos quatro hex antigos sobrou).
    - `grep -c '#7f86a2' web/src/App.jsx` == 1, idem para `#666c85`, `#7581a8`, `#616e6a`.
    - `test_brand_book_v2_tokens.mjs` audita `textFaint` contra `bgBase`, `bgPanel` E `bgCard` nos 4 pares (Estudo/Operador × dark/light) e todas as 12 razões medem ≥ 4.5 — valores esperados de referência: dark base pior caso 4.551, light base 4.555, Operador dark 4.564, Operador light 4.566.
    - `test_copy_theme.mjs` verde com o hex novo.
    - `App.jsx:1627` continua usando `P.textFaint` como `stroke` do placeholder (não foi alterado).
    - `grep -n 'textDim: "#6b7288"' web/src/App.jsx` ainda retorna 1 linha (fora de escopo, não tocado).
    - `npx vite build` em `web/` conclui sem erro.
  </acceptance_criteria>
  <done>Os 4 valores de `textFaint` passam AA (≥4.5:1) contra as 3 superfícies onde renderizam, o guardião de marca passa a auditar esse token, e nenhum guardião foi apagado.</done>
</task>

<task type="auto">
  <name>Task 2: Acordeão de opções responde a teclado e anuncia estado (FIX-C15)</name>
  <files>web/src/App.jsx, web/tests/test_acessibilidade_acordeao.mjs</files>
  <read_first>
    - .planning/phases/04-corre-o-m-dio-storyline-ux/04-UI-SPEC.md (seção "FIX-C15")
    - web/src/App.jsx (linhas 2747-2850: `OpcaoContrato` e `OpcoesCamada` completos, com os dois `role="button"` e o painel expandido)
    - web/src/App.jsx (linha 296: a regra global `:focus-visible`)
    - web/tests/test_conceito_ui.mjs (padrão de teste estático de UI que lê `App.jsx` como texto e valida por regex — modelo para o guardião novo)
  </read_first>
  <action>
Implementa FIX-C15 per decisão do CONTEXT.md e do UI-SPEC.

RE-GREP primeiro: `grep -n 'role="button"' web/src/App.jsx`. Na verificação de
2026-08-21 há exatamente DOIS pontos (linhas 2760 e 2827). Se o grep revelar
mais, TODOS entram nesta correção — o CONTEXT.md diz "em todo ponto que usa esse
padrão (re-grep obrigatório)".

Em cada `<div onClick={onToggle} role="button" tabIndex={0} ...>` adiciona:
- `onKeyDown={(e) => { if (e.key === "Enter" || e.key === " ") { e.preventDefault(); onToggle(); } }}`
  O `e.preventDefault()` no Espaço não é opcional: sem ele a página rola junto.
- `aria-expanded={isOpen}` em `OpcaoContrato` e `aria-expanded={open}` em
  `OpcoesCamada` (são os nomes das props de estado de cada um — confira no fonte).
  O UI-SPEC pede isso junto: já existe `role="button"`/`tabIndex={0}`, falta só o
  anúncio do estado, e é a mesma categoria de correção.

Mantém `<div>` (a alternativa `<button>` nativo do CONTEXT.md exigiria refazer o
layout flex e o reset de estilo — não há ganho de acessibilidade sobre
`role`+`tabIndex`+`onKeyDown`+`aria-expanded`, e o guardrail "não reescreva sem
necessidade" vale).

Não muda cor, copy nem estilo. Confere que o painel expandido de `OpcoesCamada`
(linha ~2834) NÃO tem `overflow:hidden` em nenhum ancestral do toggle — se tiver,
o anel de foco global (`outline-offset:2px`, linha 296) fica cortado; nesse caso
a correção é `overflow: visible` na linha do toggle, NUNCA um estilo de foco novo.

Cria `web/tests/test_acessibilidade_acordeao.mjs`: guardião estático (lê
`App.jsx` como texto, como os outros testes de UI desta base) que trava — para
cada ocorrência de `role="button"` no arquivo — a presença de `onKeyDown` com
`Enter`, com `" "`, com `preventDefault` e de `aria-expanded` no mesmo elemento.
O teste tem que FALHAR se alguém adicionar um `role="button"` novo sem teclado:
conte as ocorrências de `role="button"` e exija o mesmo número de `onKeyDown`
adjacentes.
  </action>
  <verify>
    <automated>cd web && node tests/test_acessibilidade_acordeao.mjs && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'role="button"' web/src/App.jsx` é igual ao número de elementos que também trazem `onKeyDown` e `aria-expanded` (o guardião novo verifica isso e falha se divergir).
    - Cada `onKeyDown` novo contém as três coisas: `e.key === "Enter"`, `e.key === " "` e `e.preventDefault()`.
    - `OpcaoContrato` tem `aria-expanded={isOpen}` e `OpcoesCamada` tem `aria-expanded={open}` (nomes das props conferidos no fonte).
    - Nenhuma mudança de `color`, `background`, `fontSize` ou copy nas duas linhas alteradas (diff só adiciona atributos).
    - `npx vite build` em `web/` conclui sem erro.
  </acceptance_criteria>
  <done>Os dois toggles ativam por Enter e Espaço sem rolar a página, anunciam `aria-expanded`, e um guardião novo impede que o próximo `role="button"` nasça sem teclado.</done>
</task>

<task type="auto">
  <name>Task 3: Aviso pedagógico de prontidão Estudo→Operador (FIX-C04)</name>
  <files>web/src/App.jsx, web/tests/test_prontidao_operador.mjs</files>
  <read_first>
    - .planning/phases/04-corre-o-m-dio-storyline-ux/04-CONTEXT.md (decisão C-04: critério SOFT, aviso e não bloqueio; e o item de "Deferred Ideas" que proíbe mexer no critério LEGAL)
    - .planning/phases/04-corre-o-m-dio-storyline-ux/04-UI-SPEC.md (seção "FIX-C04": posição do painel, tratamento visual, os dois botões e a copy exata)
    - web/src/App.jsx (linhas 1879-1925: `ModoTrabalhoCard` completo; linhas 1924-1990: `TermoOperadorModal` — só para confirmar que segue intacto)
    - web/src/App.jsx (linhas 6465-6480: a caixa `statusIndisponivel` do BuyModal — o padrão `T.warn` que deve ser reusado literalmente)
    - web/src/App.jsx (linha 723: o comentário-regra "T.positive/T.negative/T.warn — NUNCA T.accent")
    - web/src/copy.js (registro de voz `estudo.*`: `welcomeCorpo`, `vazioPortfolio` — o tom de professor que a copy nova tem que ter)
    - web/src/persistence.js (linhas 330-345 e 985-1000: `analyses` presente nos dois stores)
  </read_first>
  <action>
Implementa FIX-C04 per decisão do CONTEXT.md: "critério mínimo de prontidão SOFT
(aviso, não bloqueio duro)". O gate LEGAL (`!c.operadorTermo` →
`TermoOperadorModal`) continua EXATAMENTE como está — mexer nele está listado em
Deferred Ideas.

Sinal de prontidão, em `ModoTrabalhoCard`:
`const analises = ctx.data && ctx.data.analyses;`
`const prontidaoConhecida = analises && typeof analises === "object";`
`const nuncaAnalisou = prontidaoConhecida && Object.keys(analises).length === 0;`

Fail-open explícito (CLAUDE.md princípio 4, reforçado pelo UI-SPEC): se
`analyses` for `undefined`/`null` (sinal DESCONHECIDO), o aviso NÃO aparece.
Só um zero MEDIDO dispara o nudge — nunca a ausência do dado.

Estado local: `const [nudgeOperador, setNudgeOperador] = useState(false);` e
`const [nudgeDispensado, setNudgeDispensado] = useState(false);`

Em `escolher(m)`: antes do `if (m === "operador" && !c.operadorTermo)`, insere
`if (m === "operador" && nuncaAnalisou && !nudgeDispensado) { setNudgeOperador(true); return; }`.
Ordem importa: o nudge aparece PRIMEIRO; se a pessoa seguir, o fluxo legal
existente roda inalterado depois.

Painel (não é modal): renderiza `{nudgeOperador && (...)}` ENTRE o segmented
control (linha ~1913) e o texto descritivo (linha ~1914). Estilo reusado
literalmente da caixa `statusIndisponivel` do BuyModal: `padding: "9px 11px"`,
`borderRadius: "9px"`, `background: "color-mix(in srgb, " + T.warn + " 12%, transparent)"`,
`border: 1px solid ${T.warn}`. `T.warn` — NUNCA `T.accent` (o acento muda de
matiz entre modos; sinal não pode mudar com o modo) e NUNCA `T.negative` (isto é
convite pedagógico, não erro).

Copy VERBATIM do UI-SPEC (voz de professor: a pessoa ainda está no Estudo):
- Título (11.5px, 700, `T.warn`): `Você ainda não abriu nenhuma análise no Estudo.`
- Corpo (11.5px, `T.textSecondary`, lineHeight 1.4): `O Modo Operador libera decisões diretas — mas ele parte do que você já entende. Vale a pena estudar pelo menos um ativo antes de ativar.`

Dois botões, mesma linha:
- Recomendado (tratamento de acento, porque é o caminho recomendado):
  rótulo `Fazer uma análise no Estudo primeiro`, `border: 1px solid ${T.accent}`,
  `background: T.accentTint`, `color: T.accent`, `fontWeight: 700`, `fontSize: "12px"`.
  Ação: `setNudgeOperador(false)` e navega para a watchlist do Estudo pela ação
  já existente no ctx (`ctx.goMercado` — confirme o nome exato por grep antes de
  usar; é a mesma ação do botão "Ir à watchlist →" do portfólio vazio).
- Prosseguir (link de texto): rótulo `Ativar mesmo assim`,
  `background: "transparent"`, `border: "none"`, `color: T.textMuted`,
  `fontWeight: 700`. Ação: `setNudgeDispensado(true)`, `setNudgeOperador(false)`
  e chama `escolher("operador")` de novo — que agora passa direto pro gate legal.
  `nudgeDispensado` é estado de COMPONENTE, não persistido: o aviso não volta na
  mesma sessão (nudge, não interstício recorrente) e não é gravado em config
  nenhuma.

Ambos os botões com `minHeight: "40px"` (alvo de toque) e `type="button"`.

Cria `web/tests/test_prontidao_operador.mjs`: guardião estático provando que (a)
o painel usa `T.warn` e não `T.accent` no fundo/borda, (b) a copy é a do
UI-SPEC, palavra por palavra, (c) existe o caminho "Ativar mesmo assim" — o
achado seria REINTRODUZIDO ao contrário se alguém transformasse isso em bloqueio,
(d) o gate legal segue presente (`!c.operadorTermo` e `TermoOperadorModal`
continuam em `ModoTrabalhoCard`), (e) o sinal falha aberto (a condição exige
`typeof analyses === "object"` antes de concluir zero).
  </action>
  <verify>
    <automated>cd web && node tests/test_prontidao_operador.mjs && node tests/test_decisao_modo.mjs && node tests/test_auditoria_modo_link_restart.mjs && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `ModoTrabalhoCard` contém a string literal `Ativar mesmo assim` e a string literal `Fazer uma análise no Estudo primeiro`.
    - O bloco do painel contém `T.warn` e NÃO contém `T.accent` no `background`/`border` da caixa (o acento só aparece no botão recomendado).
    - `grep -c "operadorTermo" web/src/App.jsx` não diminuiu em relação ao HEAD anterior, e `TermoOperadorModal` continua sendo renderizado por `ModoTrabalhoCard`.
    - A condição de disparo exige `typeof analyses === "object"` antes de checar `length === 0` (fail-open verificado por regex no guardião).
    - `nudgeDispensado` não aparece em nenhuma chamada de `saveConfig`/`putConfig` (não é persistido).
    - `test_decisao_modo.mjs` e `test_auditoria_modo_link_restart.mjs` continuam verdes (o fluxo de troca de modo e o reload não regrediram).
    - `npx vite build` em `web/` conclui sem erro.
  </acceptance_criteria>
  <done>Quem tem zero análises MEDIDAS vê o aviso antes de ativar o Operador, consegue prosseguir num toque, o gate legal continua intacto, e o aviso não aparece quando o sinal é desconhecido.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| estado do cliente → UI de gate | `data.analyses` vem do servidor (web) ou do doc local (iOS); é entrada não confiável para decisão de UI |
| UI → ativação do Modo Operador | a transição de modo tem consequência jurídica (aceite de termo) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Elevation of Privilege | `ModoTrabalhoCard.escolher` | mitigate | o nudge é UI-only e adicional; o gate legal (`operadorTermo`) continua no cliente E no servidor (`store.set_config`) — guardião assere que `!c.operadorTermo`/`TermoOperadorModal` seguem no fluxo |
| T-04-09 | Spoofing | sinal de prontidão vindo de `data.analyses` | accept | um cliente adulterado poderia forjar `analyses` para pular o aviso; o aviso é pedagógico e dismissível por desenho (CONTEXT.md), então forjá-lo não dá nenhum acesso que "Ativar mesmo assim" já não dê |
| T-04-10 | Denial of Service | painel de prontidão | mitigate | `nudgeDispensado` é estado de componente e some no reload; não persiste em `config`, então não há como travar um usuário fora do Operador por estado corrompido |
| T-04-11 | Information Disclosure | mudança de `textFaint` | accept | mudança puramente cromática; nenhum dado novo exposto — ao contrário, texto antes ilegível passa a ser legível |
| T-04-SC | Tampering | instalação de pacotes | accept | nenhum pacote novo — nenhuma dependência adicionada em `web/package.json` |
</threat_model>

<verification>
- `cd web && npx vite build` — obrigatório: grep e teste estático não pegam erro de sintaxe JS.
- `bash scripts/executar.sh --testes` — suíte canônica (pytest + `web/tests/*.mjs`) verde antes de fechar o plano.
- Confirmar que nenhum arquivo de `server/` foi tocado (este plano é 100% front).
</verification>

<success_criteria>
- Os 4 hex de `textFaint` passam AA contra as 3 superfícies, com o guardião de marca cobrindo o token.
- Os toggles com `role="button"` respondem a Enter/Espaço e anunciam `aria-expanded`.
- O aviso de prontidão aparece só com zero medido, nunca bloqueia, e não toca no gate legal.
- Dois guardiões existentes atualizados COM NOTA, dois guardiões novos criados, nenhum apagado.
- `npx vite build` verde e suíte canônica verde.
</success_criteria>

<output>
Create `.planning/phases/04-corre-o-m-dio-storyline-ux/04-03-SUMMARY.md` when done
</output>
