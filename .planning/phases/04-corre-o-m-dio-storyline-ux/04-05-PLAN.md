---
phase: 04-corre-o-m-dio-storyline-ux
plan: 05
type: execute
wave: 2
depends_on: ["04-03"]
files_modified:
  - web/src/App.jsx
  - web/src/persistence.js
  - CLAUDE.md
  - web/tests/test_disclaimer_trade_modal.mjs
  - web/tests/test_fonte_explicacao.mjs
  - web/tests/test_historico_rejeitada.mjs
autonomous: true
requirements: [FIX-C13, FIX-C14, FIX-C01, FIX-C02]
must_haves:
  truths:
    - "O disclaimer de operação simulada aparece junto do botão de confirmar, na compra e na venda"
    - "O usuário lê, no momento da ordem, que a simulação executa por completo ou não executa"
    - "Explicação sem IA é rotulada como automática do app — nunca como conteúdo de IA"
    - "Ordem rejeitada aparece no histórico com o motivo, em vez de sumir com o toast"
  artifacts:
    - path: "web/src/App.jsx"
      provides: "DISCLAIMERS.trade nos 2 modais, nota de fonte na AnalysisView, linha rejeitada no HistoricoScreen, refresh de estado no catch de compra/venda"
      contains: "DISCLAIMERS.trade"
    - path: "web/src/persistence.js"
      provides: "paridade do rastro de rejeição no ramo local do deviceStore"
      contains: "rejeitada"
    - path: "CLAUDE.md"
      provides: "declaração formal de simulação tudo-ou-nada na seção Modelo de simulação"
      contains: "tudo-ou-nada"
  key_links:
    - from: "web/src/App.jsx AnalysisView"
      to: "an.fonte do backend"
      via: "AiNote source-aware / DeterministicNote"
      pattern: "an\\.fonte|source"
    - from: "web/src/App.jsx HistoricoScreen"
      to: "h.status/h.motivo"
      via: "pill neutra + sub-linha T.warn"
      pattern: "h\\.status"
    - from: "web/src/App.jsx confirmBuy/confirmSell"
      to: "store.getState()"
      via: "refresh no catch para o histórico rejeitado aparecer"
      pattern: "catch"
---

<objective>
Fechar a camada de transparência no ponto de decisão (FIX-C13, FIX-C14) e ligar
na UI os dois contratos de backend da Wave 2: rótulo de fonte da explicação
(FIX-C01, lado front) e rastro da ordem rejeitada (FIX-C02, lado front).

Purpose: `DISCLAIMERS.trade` existe desde sempre em `disclaimers.js` e NENHUMA
ocorrência de `DISCLAIMERS.` em `App.jsx` usa `.trade` — é um defeito de
renderização puro. E de nada adianta o backend gravar `status`/`motivo` se o
histórico não mostra e o `AiNote` rotular texto determinístico como "conteúdo de
IA" (violação de transparência que o UI-SPEC levantou no mesmo caminho de render).

Output: 2 modais com disclaimer, `AnalysisView` ciente da fonte, histórico com
linha de rejeição, paridade nos dois stores, `CLAUDE.md` declarando tudo-ou-nada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-corre-o-m-dio-storyline-ux/04-CONTEXT.md
@.planning/phases/04-corre-o-m-dio-storyline-ux/04-UI-SPEC.md
@CLAUDE.md
@.claude/skills/didatica-boris/SKILL.md
</context>

<interfaces>
<!-- Citações verificadas contra o fonte vivo em 2026-08-21. RE-GREP mesmo assim. -->

`web/src/disclaimers.js` linhas 24-26 — a string existe e NÃO deve ser alterada:
`trade: "Operação SIMULADA (paper trading). Nenhuma ordem real é enviada a uma corretora."`

`web/src/App.jsx`:
- `const AiNote = ({ at }) => (...)` linhas 344-349 — `fontSize:"10px"`, `color:T.textFaint`,
  `marginTop:"10px"`, glifo `ⓘ` com `fontWeight:700`; texto
  `"Conteúdo educacional de IA · não é recomendação" + (at ? " · " + at : "")`.
  Usada em DOIS lugares: linha 1219 (`AnalysisView`) e linha 2382.
- `function AnalysisView({ an })` linhas 1189-1221: `if (an.error) return <div style={{color:T.negative,...}}>{an.error}</div>`
  (linha 1191) descarta TODO o resto; `const body = an.markdown || d.resumo || an.text || an.analysis || ""` (linha 1193);
  fallback de corpo vazio na linha 1199 (`<div style={{color:T.textMuted,fontSize:"13px"}}>`)
- `function BuyModal({ ctx })` linhas 6423-6483: nota secundária 11px `T.textFaint` linha 6469;
  caixa `statusIndisponivel` 6471-6474; linha do botão `<div style={{display:"flex",gap:"8px",marginTop:"16px"}}>` linha 6476
- `function SellModal({ ctx })` linhas 6487-6553: nota secundária linha 6539; linha do botão 6546
- `function HistoricoScreen({ ctx })` linhas 3708-3806: sub-linha de erro de pendente
  (o padrão a copiar) linhas 3759-3763 — `padding:"0 15px 9px",fontSize:"11px",color:T.warn,lineHeight:1.4`;
  `ordemPendentePill` linha 3746 — `padding:"3px 9px",borderRadius:"999px",fontSize:"10.5px",fontWeight:800,color:T.warn,background:"color-mix(in srgb, "+T.warn+" 14%, transparent)"`;
  cabeçalho da tabela concluída linha 3789; linha da tabela 3792-3802 (pill de TIPO em 3796, RESULTADO em 3801)
- `confirmSell` linhas 7052-7064 e `confirmBuy` linhas 7102-7125 — hoje o `catch` só faz
  `flash("Compra: " + e.message)`, sem nenhum refresh de estado
- `store.getState()` existe nos DOIS stores (`persistence.js:112` serverStore via `sync.readState()`,
  `persistence.js:474` deviceStore, que quando logado busca `api.getState()` e adota)

`web/src/persistence.js`:
- `deviceStore.buy` linhas 1005-1048: ramo logado (servidor executa) × ramo local
  (`throw new Error("Caixa insuficiente.")` na linha 1025; entrada de histórico na 1040)
- `deviceStore.sell` linhas 1049-1083: `throw new Error("Sem posicao em " + t)` linha 1062;
  entrada de histórico linha 1077
- guardião de paridade: `web/tests/test_api_parity.mjs`

## CONTRATO DE BACKEND (fixado pelos Planos 04-02/04-04 — implemente contra ele)
Payload das rotas de análise: `fonte: "ia" | "deterministico"`,
`iaIndisponivel: null | {code, mensagem}`, `verbetes: string[]`, `semDados: bool`.
Entrada de histórico: `status: "executada" | "rejeitada"` (AUSENTE = legado =
tratar como executada), `motivo: string` só em rejeitada, `price` pode ser `null`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Disclaimer de operação simulada e declaração tudo-ou-nada nos modais (FIX-C13, FIX-C14)</name>
  <files>web/src/App.jsx, CLAUDE.md, web/tests/test_disclaimer_trade_modal.mjs</files>
  <read_first>
    - .planning/phases/04-corre-o-m-dio-storyline-ux/04-UI-SPEC.md (seções "FIX-C13" e "FIX-C14" — posição exata, tipografia e o que NÃO fazer)
    - .planning/phases/04-corre-o-m-dio-storyline-ux/04-CONTEXT.md (decisão C-14: NÃO implementar fill parcial; é declaração de escopo)
    - web/src/disclaimers.js (arquivo inteiro, 41 linhas)
    - web/src/App.jsx (linhas 6423-6553: BuyModal e SellModal completos)
    - web/src/App.jsx (grep `DISCLAIMERS\.` para ver como as outras chaves já são renderizadas nesta base)
    - CLAUDE.md (seção "## Modelo de simulação", a lista "O sistema mantém (e toda mudança preserva)")
  </read_first>
  <action>
FIX-C13 — renderiza `DISCLAIMERS.trade` em `BuyModal` e `SellModal`,
imediatamente ACIMA da linha de botões (`<div style={{display:"flex",gap:"8px",marginTop:"16px"}}>`,
linha 6476 no Buy e 6546 no Sell). É "perto do botão de confirmação" per decisão
do CONTEXT.md: o instante de maior atenção, não enterrado acima do stepper.

Estilo (UI-SPEC): `fontSize: "10.5px"`, `color: T.textFaint`, `lineHeight: 1.4`,
`marginTop: "10px"`. Um passo mais discreto que a nota secundária logo acima
(11px) — lê como rodapé de conformidade fixo, não como status dinâmico.
Placement e estilo IDÊNTICOS nos dois modais, sem variação por modo (a própria
string não bifurca por modo).

NÃO altere a string em `disclaimers.js` — este achado é 100% de renderização.
Importe/consuma `DISCLAIMERS` do jeito que `App.jsx` já faz para as outras chaves.

FIX-C14 — declaração formal de que a simulação é tudo-ou-nada por desenho.
NÃO implemente fill parcial: está em Deferred Ideas e nos Success Criteria do
ROADMAP como decisão de produto fechada. Dois lugares, nada além disso:

1. UI: acrescenta uma SEGUNDA FRASE à nota secundária que já existe sob o
   stepper (linha 6469 no Buy, 6539 no Sell — mesmo `<div>`, mesmo estilo 11px
   `T.textFaint`, sem caixa nova, sem badge novo):
   `Esta simulação executa por completo ou não executa — não há preenchimento parcial de ordem.`
   Anexa como segunda sentença ao ramo em que o mercado está ABERTO e também ao
   ramo `fechado` (a regra vale nos dois casos). Nada muda no `HistoricoScreen`:
   um status que estruturalmente não pode ocorrer não ganha coluna.
2. Documentação: na seção `## Modelo de simulação` do `CLAUDE.md` da raiz,
   acrescenta um item à lista "O sistema mantém (e toda mudança preserva)":
   execução tudo-ou-nada por desenho — `store.buy`/`store.sell` executam 100% ou
   rejeitam inteiro; "ordem parcialmente executada" NÃO existe no modelo de
   dados, e o motivo é o princípio 5 (cálculo determinístico, sem estado que o
   motor nunca produz). Cite o achado C-14 do REPORT-01 e a decisão do ROADMAP
   v1.1 para quem ler daqui a seis meses saber que foi deliberado.

Cria `web/tests/test_disclaimer_trade_modal.mjs`: guardião estático que trava
(a) `DISCLAIMERS.trade` renderizado nos dois modais, (b) posicionado ANTES da
linha de botões em cada um (comparação de índice de string dentro do bloco de
cada função), (c) a frase de tudo-ou-nada presente nos dois modais, (d) a string
de `disclaimers.js` inalterada byte a byte, (e) `CLAUDE.md` contendo a
declaração.
  </action>
  <verify>
    <automated>cd web && node tests/test_disclaimer_trade_modal.mjs && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DISCLAIMERS.trade" web/src/App.jsx` == 2 (uma no BuyModal, uma no SellModal).
    - Em cada modal, o índice da ocorrência de `DISCLAIMERS.trade` é MENOR que o índice de `A.confirmBuy` / `A.confirmSell` respectivamente (fica acima do botão).
    - `grep -c "não há preenchimento parcial de ordem" web/src/App.jsx` >= 2.
    - `grep -c "tudo-ou-nada" CLAUDE.md` >= 1 e a nova linha cita C-14.
    - `web/src/disclaimers.js` sem diff (`git diff --stat web/src/disclaimers.js` vazio).
    - Nenhum badge, modal ou coluna nova foi criada para fill parcial (`grep -ci "parcialmente executada" web/src/App.jsx` == 0 fora de comentário).
    - `npx vite build` em `web/` conclui sem erro.
  </acceptance_criteria>
  <done>Os dois modais mostram a garantia de operação simulada junto do botão de confirmar e declaram a execução tudo-ou-nada; `CLAUDE.md` registra a decisão; zero código de fill parcial foi escrito.</done>
</task>

<task type="auto">
  <name>Task 2: AnalysisView e AiNote cientes da fonte da explicação (FIX-C01, front)</name>
  <files>web/src/App.jsx, web/tests/test_fonte_explicacao.mjs</files>
  <read_first>
    - .planning/phases/04-corre-o-m-dio-storyline-ux/04-UI-SPEC.md (seção "FIX-C01" — os 3 itens do contrato visual e a copy exata da nota determinística)
    - web/src/App.jsx (linhas 335-350: `InfoDot` e `AiNote`; linhas 1189-1222: `AnalysisView`; linha 2382: o segundo uso de `AiNote`)
    - web/src/persistence.js (linhas 700-725: `deviceStore.analyze` — quais campos da resposta o device guarda; `fonte` precisa sobreviver aí também)
    - .planning/phases/04-corre-o-m-dio-storyline-ux/04-04-PLAN.md (contrato de resposta das rotas de análise)
    - CLAUDE.md (princípio 6/7: a IA informa quando usa dado histórico/insuficiente; a manchete é do motor)
  </read_first>
  <action>
Implementa o lado front de FIX-C01 contra o contrato fixado pelo Plano 04-04
(`fonte`, `iaIndisponivel`, `verbetes`, `semDados`).

1. `AiNote` ganha uma prop `source`, com default `"ia"` (as duas chamadas atuais
   continuam funcionando sem mudança de comportamento):
   - `source === "ia"` → texto atual, byte a byte:
     `Conteúdo educacional de IA · não é recomendação{at ? " · " + at : ""}`
   - `source === "deterministico"` → 
     `Explicação automática do app (sem IA) · baseada no setup/indicador detectado{at ? " · " + at : ""}`
   Mesmo tratamento visual nos dois casos (10px, `T.textFaint`, glifo `ⓘ`,
   `marginTop: 10px`) — a nota é rótulo de status do app, não voz de persona, e
   por isso NÃO bifurca por modo Estudo/Operador.
   Isto não é polimento: hoje `AiNote` afirma incondicionalmente que o conteúdo é
   de IA. Injetar texto determinístico no mesmo `an.markdown` sem trocar a nota
   rotularia conteúdo não-IA como IA — violação de transparência direta do
   princípio 7 do CLAUDE.md.

2. `AnalysisView`:
   - Passa a renderizar `<AiNote at={an.at} source={an.fonte === "deterministico" ? "deterministico" : "ia"} />`.
   - Quando `an.fonte === "deterministico"`, o corpo renderiza pelo MESMO caminho
     `<Markdown text={body} />` já usado para a IA — mesma tipografia, mesmo
     chrome do card. NÃO crie contêiner "degradado": explicação determinística é
     resultado educacional de primeira classe, não estado de erro.
   - Acima do corpo, quando `an.iaIndisponivel` existe, renderiza UMA linha 11px
     `T.textFaint` (não `T.negative`, não caixa de alerta):
     `IA indisponível agora — mostrando a explicação automática do app, sem IA.`
     Copy verbatim do UI-SPEC.
   - `an.semDados === true` (ou `body` vazio no caminho determinístico) →
     reusa o padrão da linha 1199, `<div style={{color:T.textMuted,fontSize:"13px"}}>`,
     com a frase MANDATÓRIA do CLAUDE.md, verbatim:
     `Não há dados suficientes para uma explicação agora.`
   - O ramo `if (an.error)` da linha 1191 CONTINUA existindo (erro real de rede/
     cliente ainda precisa aparecer), mas deixa de ser o caminho normal do
     usuário sem chave: esse agora chega como 200 com `fonte: "deterministico"`.

3. `web/src/persistence.js`: no `deviceStore.analyze` (linhas ~706-722), inclui
   `fonte`, `iaIndisponivel`, `verbetes` e `semDados` nos campos guardados em
   `doc.analyses[t]`. Sem isso o app nativo perde o rótulo ao reabrir o card e
   volta a mostrar "Conteúdo educacional de IA" sobre texto determinístico.
   Confira se o ramo de reaproveitamento (`reaproveitada`) preserva os campos.

Cria `web/tests/test_fonte_explicacao.mjs`: guardião estático travando (a) as
duas variantes de texto do `AiNote`, (b) que `AnalysisView` escolhe a variante
por `an.fonte`, (c) a frase "Não há dados suficientes para uma explicação agora."
presente verbatim, (d) a linha de "IA indisponível agora" verbatim, (e) que o
caminho determinístico usa `<Markdown` e não um contêiner de erro, (f) que
`persistence.js` persiste `fonte` em `doc.analyses`.
  </action>
  <verify>
    <automated>cd web && node tests/test_fonte_explicacao.mjs && node tests/test_api_parity.mjs && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Explicação automática do app (sem IA)" web/src/App.jsx` == 1.
    - `grep -c "Conteúdo educacional de IA · não é recomendação" web/src/App.jsx` == 1 (a string original não foi duplicada nem alterada).
    - `grep -c "Não há dados suficientes para uma explicação agora." web/src/App.jsx` == 1 (frase mandatória do CLAUDE.md, verbatim).
    - `grep -c "IA indisponível agora — mostrando a explicação automática do app, sem IA." web/src/App.jsx` == 1.
    - `AiNote` tem `source` com default `"ia"`; a chamada da linha ~2382 continua sem passar `source` e mantém o texto de IA.
    - `grep -c "fonte" web/src/persistence.js` aumentou e `doc.analyses[t]` inclui `fonte`.
    - `test_api_parity.mjs` continua verde.
    - `npx vite build` em `web/` conclui sem erro.
  </acceptance_criteria>
  <done>Explicação determinística renderiza no mesmo card da IA, rotulada corretamente como conteúdo automático do app, com a frase mandatória no caso sem dados — e o rótulo sobrevive ao reload no app nativo.</done>
</task>

<task type="auto">
  <name>Task 3: Ordem rejeitada visível no histórico, nos dois stores (FIX-C02, front)</name>
  <files>web/src/App.jsx, web/src/persistence.js, web/tests/test_historico_rejeitada.mjs</files>
  <read_first>
    - .planning/phases/04-corre-o-m-dio-storyline-ux/04-UI-SPEC.md (seção "FIX-C02" — os 3 itens do contrato visual e a razão de NÃO usar T.negative na pill)
    - web/src/App.jsx (linhas 3708-3806: `HistoricoScreen` completo, com a sub-linha `ultimoErro` que é o precedente a copiar)
    - web/src/App.jsx (linhas 7040-7130: `confirmSell` e `confirmBuy` — os `catch` que hoje só dão `flash`)
    - web/src/persistence.js (linhas 97-200: superfície do `serverStore`; linhas 1000-1085: `deviceStore.buy`/`deviceStore.sell`, com o ramo logado e o ramo local)
    - web/tests/test_api_parity.mjs (o guardião de paridade dos dois stores)
    - .planning/phases/04-corre-o-m-dio-storyline-ux/04-02-PLAN.md (contrato da entrada de histórico)
    - CLAUDE.md (guardrail de paridade `deviceStore` ↔ `serverStore`)
  </read_first>
  <action>
Implementa o lado front de FIX-C02 contra o contrato dos Planos 04-02/04-04.

1. `HistoricoScreen`, tabela de operações concluídas (linhas ~3792-3802):
   - `const rejeitada = h.status === "rejeitada";`. Entrada LEGADA (sem `status`)
     é tratada como executada — histórico não se reescreve.
   - Pill de TIPO: quando `rejeitada`, o rótulo continua sendo `h.type`
     (`COMPRA`/`VENDA`) mas as cores passam a `color: T.textFaint` sobre
     `background: T.bgPanel`. NÃO use `T.negative`: nessa tabela vermelho já
     significa "venda EXECUTADA" (cor adjacente a P&L) e reusá-lo afirmaria
     visualmente que houve execução.
   - Ao lado da pill, badge `REJEITADA` com a MESMA forma do `ordemPendentePill`
     (linha 3746): `padding: "3px 9px"`, `borderRadius: "999px"`,
     `fontSize: "10.5px"`, `fontWeight: 800`, `color: T.warn`,
     `background: "color-mix(in srgb, " + T.warn + " 14%, transparent)"`.
   - Coluna RESULTADO: `—` (em dash) para rejeitada. Nunca `R$ 0,00` — o estado
     real é "não existe resultado" (CLAUDE.md item 4).
   - Coluna PREÇO: `—` quando `h.price == null` (rejeição por falta de cotação).
   - Sub-linha abaixo da linha, na MESMA posição/estilo do `ultimoErro`
     (linhas 3759-3763): `padding: "0 15px 9px"`, `fontSize: "11px"`,
     `color: T.warn`, `lineHeight: 1.4`, texto `Rejeitada: {h.motivo}`.
     Isso exige quebrar o `map` atual (que hoje devolve um único `<div>` de
     linha) num fragmento com a linha + a sub-linha condicional — mesma estrutura
     que a lista de pendentes já usa logo acima.

2. `confirmBuy` e `confirmSell` (linhas ~7052-7064 e ~7102-7125): no `catch`,
   ANTES do `flash(...)`, atualiza o estado para que a entrada rejeitada recém-
   gravada pelo servidor apareça:
   `try { setData(await store.getState()); } catch { /* refresh best-effort: o toast do erro já informou */ }`
   Sem isso, o servidor grava a rejeição mas a tela só a mostraria no próximo
   boot. O `flash` do erro continua igual.

3. `web/src/persistence.js` — PARIDADE (guardrail do CLAUDE.md: método/campo novo
   entra nos DOIS stores). No `deviceStore`, ramo LOCAL (sem sessão) de `buy` e
   de `sell`:
   - antes de cada `throw new Error(...)` de rejeição (caixa insuficiente na
     linha ~1025; sem posição na linha ~1062), insere em `doc.history` a entrada
     `{ date: now(), type: "COMPRA"|"VENDA", t, qty, price: <preço ou null>, pnl: null, status: "rejeitada", motivo: <mesma frase do backend>, origem: "manual" }`,
     chama `write()` e SÓ ENTÃO lança o erro. Espelha `store.py.registrar_rejeicao`
     — inclusive o cap de 100 rejeições, mesma constante, mesma poda que preserva
     todas as executadas (comentário "espelho de store.py", como os vizinhos).
   - nas entradas de sucesso do ramo local (linhas ~1040 e ~1077), acrescenta
     `status: "executada"`.
   - o ramo LOGADO não muda: o servidor já executou e `_adotarCarteiraDoServidor`
     traz o `history` dele; a rejeição vem pelo refresh do item 2.

Cria `web/tests/test_historico_rejeitada.mjs`: guardião estático travando (a) a
pill neutra + badge `REJEITADA` com `T.warn` e a AUSÊNCIA de `T.negative` no ramo
rejeitado, (b) `—` em RESULTADO e em PREÇO nulo, (c) a sub-linha `Rejeitada:`
com `T.motivo`, (d) o refresh no `catch` de `confirmBuy` e `confirmSell`, (e) a
paridade: `deviceStore` grava `status: "rejeitada"` antes de lançar, nos dois
métodos, e `status: "executada"` no sucesso.
  </action>
  <verify>
    <automated>cd web && node tests/test_historico_rejeitada.mjs && node tests/test_api_parity.mjs && node tests/test_fase2_portfolio.mjs && node tests/test_fase3_paridade_stores_generica.mjs && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `HistoricoScreen` contém `h.status === "rejeitada"` e a string literal `REJEITADA`.
    - No bloco da linha rejeitada, `T.negative` NÃO aparece; `T.warn` e `T.textFaint` aparecem.
    - `grep -c "Rejeitada: " web/src/App.jsx` >= 1 e a sub-linha usa `color: T.warn` com `fontSize: "11px"`.
    - Linha rejeitada rende `—` em RESULTADO (nenhuma chamada de `moneySigned` nesse ramo) e `—` quando `h.price == null`.
    - Entrada legada sem `status` continua caindo no ramo executado (o guardião verifica que a condição é `=== "rejeitada"`, nunca `!== "executada"`).
    - `confirmBuy` e `confirmSell` chamam `store.getState()` dentro do `catch`, ANTES do `flash`.
    - `deviceStore.buy` e `deviceStore.sell` gravam entrada com `status: "rejeitada"` antes de cada `throw` do ramo local, e `status: "executada"` no ramo de sucesso.
    - `test_api_parity.mjs`, `test_fase2_portfolio.mjs` e `test_fase3_paridade_stores_generica.mjs` continuam verdes.
    - `npx vite build` em `web/` conclui sem erro.
  </acceptance_criteria>
  <done>Uma ordem rejeitada aparece no histórico com pill neutra, badge REJEITADA, motivo em `T.warn` e traço em vez de zero fabricado — na web (via refresh) e no app nativo (via espelho local), com o histórico legado intocado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resposta da API → render | `an.markdown`, `h.motivo` e `an.iaIndisponivel.mensagem` vêm do servidor e vão pra tela |
| doc local do iOS → render | `deviceStore` grava histórico local que a mesma tela renderiza |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-15 | Tampering | `h.motivo` renderizado no histórico | mitigate | renderizado como TEXTO em `{h.motivo}` (React escapa por padrão) — nunca `dangerouslySetInnerHTML`; guardião verifica ausência dessa API no bloco |
| T-04-16 | Information Disclosure | `an.markdown` determinístico passando por `<Markdown>` | accept | mesmo componente que já renderiza saída de LLM há fases; superfície de render inalterada, e a origem agora é o PRÓPRIO servidor (menos confiável antes, mais agora) |
| T-04-01 | Information Disclosure | `an.iaIndisponivel.mensagem` na tela | mitigate | o servidor já sanitiza via `llm.public_error` (Plano 04-04 T-04-01); o front exibe sem concatenar `e.stack` nem detalhes de rede |
| T-04-02 | Denial of Service | histórico local de rejeições no iOS | mitigate | `deviceStore` espelha o `CAP_REJEICOES = 100` do `store.py`; `localStorage` não cresce sem limite |
| T-04-17 | Spoofing | rótulo de fonte da explicação | mitigate | `source` deriva SÓ de `an.fonte` do servidor; o front nunca decide sozinho que um texto é de IA |
| T-04-SC | Tampering | instalação de pacotes | accept | nenhum pacote novo em `web/package.json` |
</threat_model>

<verification>
- `cd web && npx vite build` — obrigatório antes de declarar qualquer task pronta.
- `bash scripts/executar.sh --testes` — suíte canônica (pytest + `web/tests/*.mjs`) verde antes de fechar o plano.
- Conferir com `git diff --stat` que `web/src/disclaimers.js` não foi tocado.
</verification>

<success_criteria>
- `DISCLAIMERS.trade` renderiza nos dois modais, acima do botão de confirmar.
- A execução tudo-ou-nada está declarada na UI e no `CLAUDE.md`; nenhum código de fill parcial foi escrito.
- Explicação determinística nunca é rotulada como IA; caso sem dados usa a frase mandatória.
- Ordem rejeitada aparece no histórico nos dois stores, sem zero fabricado e sem reescrever histórico legado.
- Suíte canônica verde e `npx vite build` verde.
</success_criteria>

<output>
Create `.planning/phases/04-corre-o-m-dio-storyline-ux/04-05-SUMMARY.md` when done
</output>
