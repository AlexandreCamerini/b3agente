---
phase: 23-motion-com-proposito-e-ilustracao-unificada
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/pet/BorisFlat.jsx
  - web/src/pet/BorisIntro.jsx
  - web/tests/test_boris_intro.mjs
autonomous: true
requirements: [ILUS-01]

must_haves:
  truths:
    - "A arte do modal 'Este é o Boris' é flat/vetorial, no mesmo vocabulário do LogoMark — zero sombreado semi-realista, zero PNG"
    - "Lado a lado com o LogoMark/ícone do app é inequivocamente o MESMO personagem: mesmo azul-marinho de corpo, mesmos óculos redondos âmbar, mesmo bico âmbar"
    - "Os óculos e o bico são âmbar de marca em QUALQUER tema e QUALQUER modo — nunca seguem o acento de Estudo/Operador"
    - "PetFab, web/src/pet/Boris.jsx e web/src/assets/boris.png continuam exatamente como estavam — a troca de arte se limita ao modal de introdução"
    - "O guardião test_boris_intro.mjs foi ATUALIZADO (não apagado) e continua travando a ordem do conteúdo, os botões, o portão de overlay e os 3 lugares de borisIntroVisto"
    - "A ilustração nova não pôde nem tentou importar nada de App.jsx — o import circular que BorisIntro.jsx já contorna continua contornado"
  artifacts:
    - path: "web/src/pet/BorisFlat.jsx"
      provides: "ilustração flat/cartoon do Boris (SVG inline, meio-corpo), export default com prop size"
      contains: "export default function BorisFlat"
    - path: "web/src/pet/BorisIntro.jsx"
      provides: "modal de introdução com a arte flat no lugar do PNG semi-realista"
      contains: "BorisFlat"
    - path: "web/tests/test_boris_intro.mjs"
      provides: "guardião atualizado: passa a travar o import de BorisFlat e as travas de marca da ilustração"
      contains: "BorisFlat"
  key_links:
    - from: "web/src/pet/BorisIntro.jsx"
      to: "web/src/pet/BorisFlat.jsx"
      via: "import default relativo, mesmo padrão do import de Boris.jsx que ele substitui"
      pattern: "import BorisFlat from \"./BorisFlat.jsx\""
    - from: "web/src/pet/BorisFlat.jsx"
      to: "LogoMark (web/src/App.jsx)"
      via: "cópia literal de cor e geometria de rosto (hex hardcoded — import de App.jsx criaria ciclo)"
      pattern: "#2a3a6b"
---

<objective>
Trocar a arte do modal "Este é o Boris" — hoje `<Boris size={110}/>`, que
renderiza um PNG 490×655 com sombreado e textura de pena — por uma ilustração
flat/vetorial nova, desenhada no MESMO vocabulário visual do `LogoMark`
(corpo azul-marinho fixo, óculos redondos âmbar, bico âmbar, olhos brancos).

Purpose: ILUS-01. É a única inconsistência de estilo que sobra no mascote. Em
40px (`PetFab`) o sombreado do PNG não é legível; em 110px ele é, e o modal de
apresentação do assistente — a primeira vez que a pessoa vê o Boris de perto —
mostra um personagem quase-fotorrealista ao lado de uma marca inteiramente
flat.

Output: `web/src/pet/BorisFlat.jsx` novo, um `BorisIntro.jsx` que o consome, e
o guardião `test_boris_intro.mjs` atualizado (não apagado) para travar o import
novo e as regras de marca da ilustração. `Boris.jsx`, `boris.png`, `PetFab` e o
ícone publicado do app ficam intocados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/23-motion-com-proposito-e-ilustracao-unificada/23-CONTEXT.md
@.planning/phases/23-motion-com-proposito-e-ilustracao-unificada/23-UI-SPEC.md
@.planning/phases/23-motion-com-proposito-e-ilustracao-unificada/23-PATTERNS.md
@CLAUDE.md

<interfaces>
`bash scripts/executar.sh --testes` — suíte canônica (pytest + TODOS os
`web/tests/*.mjs`, por glob). `scripts/test.sh` sozinho é meia baseline e NÃO
conta como validação (CLAUDE.md).

`cd web && npx vite build` — obrigatório após editar o front: grep e teste
estático NÃO pegam erro de sintaxe JSX (CLAUDE.md, Validação obrigatória).

Este plano NÃO publica. O bump + `publicar-web.sh` são do plano 23-04.
</interfaces>

<estado_verificado_ao_vivo>
Lido diretamente no working tree em 2026-09-06. **RE-VERIFIQUE por conteúdo
antes de editar.**

`web/src/pet/BorisIntro.jsx` (94 linhas):
- linha 27: `import Boris from "./Boris.jsx";` — **o único import do arquivo**.
- linhas 23-25 (dentro do docstring): *"Mesmos tokens de tema (`var(--x)`) que
  `BorisChat.jsx` já usa, pelo mesmo motivo: importar `T`/`card` de `App.jsx`
  criaria import circular (App.jsx importa este arquivo)."*
- linhas 29-32: reconstrói `T` a partir de `var(--x)` em vez de importar de
  `App.jsx` — o contorno já existente do ciclo.
- linha 42: `<Boris size={110} />`, dentro da coluna flex centralizada
  (linhas 41-49), imediatamente acima do título "Este é o **Boris**".

`web/src/App.jsx:18` importa `Boris` de `./pet/Boris.jsx` para o `PetFab` —
portanto **remover o import de `Boris` de `BorisIntro.jsx` NÃO deixa
`Boris.jsx` órfão**; o componente segue vivo e usado.

`LogoMark` (`web/src/App.jsx:201-231`) — o molde de cor e geometria. Cores
literais, todas já assinadas no `23-UI-SPEC.md` (seção Color):
`#161927` (fundo do badge + anel do selo), `#2a3a6b` (tufos de orelha, rosto),
`BRAND.amber` = `#f2a93b` (óculos, bico, selo), `#eef1f8` (olhos).
Geometria do rosto, em `viewBox="0 0 64 64"`:

    <path d="M14 16 L22 27 L10 27 Z" fill="#2a3a6b" />      {/* tufo esquerdo */}
    <path d="M50 16 L42 27 L54 27 Z" fill="#2a3a6b" />      {/* tufo direito */}
    <circle cx="32" cy="34" r="26" fill="#2a3a6b" />        {/* rosto */}
    <circle cx="22" cy="32" r="10" fill="none" stroke="#f2a93b" strokeWidth="3.2" />
    <circle cx="42" cy="32" r="10" fill="none" stroke="#f2a93b" strokeWidth="3.2" />
    <path d="M30 32 Q32 29 34 32" fill="none" stroke="#f2a93b" strokeWidth="3.2" />
    <circle cx="22" cy="32" r="4" fill="#eef1f8" />
    <circle cx="42" cy="32" r="4" fill="#eef1f8" />
    <path d="M32 42 L27.5 49 L36.5 49 Z" fill="#f2a93b" />  {/* bico */}

O comentário de marca em `App.jsx:202-205` é regra travada e continua valendo
para a ilustração nova: *"nunca recolorir os óculos fora do âmbar da marca"* —
até a Fase 3 os óculos/bico/selo seguiam o acento do MODO; a v2 fechou essa
porta.

`web/tests/test_boris_intro.mjs` (113 linhas) — guardião estático que assenta
sobre `BorisIntro.jsx`. A asserção que ESTE plano quebra é uma só:

    ok("BorisIntro.jsx importa Boris (mesmo componente animado da F1)",
       /import Boris from ["']\.\/Boris\.jsx["'];/.test(introSrc));

As outras 20 asserções do arquivo (ordem D8 do conteúdo, frases de disclaimer,
`onConversar`/`onDepois`, portão de overlay, `borisIntroVisto` nos 3 lugares)
**não são tocadas por este plano e não podem ser afrouxadas**. Em particular,
a asserção de ordem usa `introSrc.slice(introSrc.indexOf("export default
function"))` para ignorar o docstring — o docstring pode ser editado à
vontade, o corpo não.

Nenhum outro `web/tests/*.mjs` cita `BorisIntro`.
</estado_verificado_ao_vivo>

<restricao_de_import_circular>
`BorisFlat.jsx` **não pode importar nada de `web/src/App.jsx`** — nem `BRAND`,
nem `T`, nem `LogoMark`. `App.jsx:18-21` importa `Boris`, `BorisChat` e
`BorisIntro` de `./pet/*.jsx`; qualquer import na direção contrária recria o
ciclo que o próprio `BorisIntro.jsx` documenta ter contornado (linhas 23-25).

Consequência prática: os hex de marca entram **literais** no SVG novo
(`"#2a3a6b"`, `"#f2a93b"`, `"#eef1f8"`), exatamente como o `LogoMark` já os
escreve literais para tudo menos o `BRAND.amber`. Isso NÃO é "hex solto fora
do token system" — é a mesma exceção que o `BorisIntro.jsx` já pratica ao
reconstruir `T` a partir de `var(--x)`. Registrar essa razão num comentário no
topo do arquivo novo, com a referência a `App.jsx:201-231` como fonte, para
que o próximo agente não "corrija" o literal para um token e quebre o build.

Pelo mesmo motivo, a ilustração **não é temável e não é dependente de modo**:
não wire nada a `var(--accent)`. Corpo/rosto sempre `#2a3a6b`; óculos/bico
sempre `#f2a93b`.
</restricao_de_import_circular>

<risco_de_contraste_medido>
**Medido, não estimado** (WCAG relative luminance, calculado em 2026-09-06):

| Par | Contraste |
|---|---|
| corpo `#2a3a6b` × `bgCard` do tema ESCURO (`#1b1f2e`) | **1,49:1** |
| corpo `#2a3a6b` × `bgCard` do tema CLARO (`#ffffff`) | 10,96:1 |
| óculos/bico `#f2a93b` × `bgCard` escuro | 8,20:1 |
| óculos/bico `#f2a93b` × corpo `#2a3a6b` | 5,49:1 |
| olhos `#eef1f8` × corpo `#2a3a6b` | 9,69:1 |
| (referência) corpo `#2a3a6b` × fundo do próprio `LogoMark` (`#161927`) | 1,59:1 |

Leitura honesta disso: **a silhueta azul-marinho quase não se separa do card
no tema escuro.** Não é regressão introduzida por este plano — o `LogoMark`
tem exatamente o mesmo par (1,59:1) e é o padrão de marca aprovado; quem
carrega o reconhecimento são o âmbar e os olhos, que medem 8,20:1 e 9,69:1. Só
que a 32px isso lê como um ícone, e a 110px pode ler como um borrão escuro com
óculos flutuando.

A ilustração é `aria-hidden` (decorativa), então **não há requisito WCAG de
contraste aqui** — o requisito é de PRODUTO: o critério 4 do ROADMAP exige que
seja "inequivocamente o mesmo Boris" lado a lado com o ícone do app.

**Portanto:**
1. Não tentar resolver isso por palpite durante a implementação.
2. Entregar o item ao orquestrador como verificação ao vivo obrigatória
   (`<environment_limitation_known_upfront>`, item 2).
3. **Remédio pré-autorizado, a aplicar SOMENTE se a checagem ao vivo no tema
   escuro reprovar:** contorno de 1 unidade de `viewBox` em `#eef1f8` no path
   do rosto/corpo (`stroke="#eef1f8" strokeWidth="1"`). `#eef1f8` já está na
   lista de cores assinadas do `23-UI-SPEC.md` (é o branco dos olhos), então
   isso **não introduz cor nova**. Nenhum outro remédio (fundo novo, gradiente,
   recolorir o corpo, amarrar ao acento do tema) está autorizado — todos
   quebrariam a trava de marca.
4. Se o remédio for aplicado, registrar no SUMMARY: o veredito do tema escuro,
   o valor aplicado e o motivo.
</risco_de_contraste_medido>

<fronteiras_de_escopo>
Travas do kickoff do milestone, registradas no `ROADMAP.md` e no
`23-CONTEXT.md`. Não reabrir, não "aproveitar a viagem":

- `web/src/pet/Boris.jsx` e `web/src/assets/boris.png` — **não tocar**.
- `PetFab` e qualquer outro call site de `<Boris .../>` — **não tocar**. O FAB
  continua com o PNG a 40px.
- Ícone do app (`resources/ios/`, `server/ios_dist`) — **não tocar**. Já está
  publicado no TestFlight/App Store; é o critério 5 do ROADMAP.
- Nenhum texto do modal muda: o título "Este é o **Boris**", os três blocos de
  conteúdo e os dois botões seguem byte a byte. `copy.js` não é tocado.
- Nenhuma dependência nova (biblioteca de ícone, de SVG, de animação).
</fronteiras_de_escopo>

<environment_limitation_known_upfront>
**Precedente confirmado nos 12 planos das Fases 20, 21 e 22**: subagentes
spawnados via Task **NÃO herdam as ferramentas MCP de navegador** do
orquestrador (`mcp__computer-use__*`, `mcp__claude-in-chrome__*`) — bug
upstream anthropics/claude-code#13898.

Este plano é o mais dependente de olho humano da fase inteira: "é reconhecível
como o mesmo personagem" não é mensurável por regex. Faça tudo o que é
automatizável (arquivo novo, swap, guardião atualizado, `vite build`, suíte) e
entregue o julgamento visual ao orquestrador.

**Como fazer o modal aparecer para conferir** (o modal só monta uma vez por
conta — `borisIntroVisto`): a intro abre quando `data.config.borisIntroVisto`
é falso, a aba é `mercado`, `didatica.ligada` é verdadeiro e nenhum outro
overlay está aberto (guardião `test_boris_intro.mjs`, seções 3 e 4). Para
reabrir numa conta que já viu, zerar `borisIntroVisto` na config (via a
própria UI de perfil se houver caminho, ou reset de conta local) — **descrever
no SUMMARY o caminho exato que você identificou**, para o orquestrador não ter
que descobrir sozinho.

Seção obrigatória no SUMMARY — **"Pendente de verificação ao vivo
(orquestrador)"**, roteiro numerado no formato de `21-04-SUMMARY.md`:
1. Abrir o modal "Este é o Boris" e conferir, **lado a lado com o `LogoMark`
   do topo da tela e com o ícone do app na home do iPhone**, que é o mesmo
   personagem: mesmo azul de corpo, mesmos óculos redondos âmbar, mesmo bico
   triangular âmbar (critério 4 do ROADMAP).
2. **Tema ESCURO — o item de risco desta fase**: a silhueta se separa do card
   (`bgCard` `#1b1f2e`), ou lê como borrão escuro com óculos flutuando?
   Contraste medido do corpo contra o card: **1,49:1**. Se reprovar, aplicar o
   remédio pré-autorizado de `<risco_de_contraste_medido>` (contorno `#eef1f8`
   de 1 unidade), rodar `npx vite build` + a suíte de novo, e registrar o
   valor final.
3. **Tema CLARO**: mesma conferência (`bgCard` `#ffffff`, contraste 10,96:1 —
   esperado passar folgado); confirmar que o âmbar não "estoura" sobre o
   branco.
4. **Modo Operador**: reabrir o modal com o app em Operador e confirmar que
   óculos e bico continuam ÂMBAR, sem seguir o acento do modo — é a regra de
   marca travada em `App.jsx:202-205`.
5. Confirmar que o `PetFab` (coruja flutuante, 40px, canto inferior direito)
   **continua com a arte antiga**, e que os dois conviverem lado a lado não
   parece defeito.
6. Confirmar que os dois botões ("Conversar agora" / "Depois") seguem
   funcionando: "Conversar agora" abre o mesmo chat do FAB, "Depois" só fecha.

Item não medido é item registrado como ABERTO no SUMMARY, nunca aproximado
nem declarado verificado.
</environment_limitation_known_upfront>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BorisFlat.jsx — a ilustração flat, no vocabulário do LogoMark</name>
  <read_first>
    - web/src/App.jsx:201-231 (LogoMark inteiro — a fonte literal de cor e geometria; copiar de lá, não redesenhar o rosto)
    - web/src/App.jsx:40-44 (BRAND — para conferir que `#f2a93b` é mesmo `BRAND.amber`)
    - web/src/pet/BorisIntro.jsx:1-35 (o docstring que explica o contorno do import circular, e o padrão de reconstruir tokens em vez de importar de App.jsx)
    - web/src/pet/Boris.jsx:1-40 (a forma de um componente do diretório `pet/`: export default, prop `size`, sem dependência de App.jsx)
    - .planning/phases/23-motion-com-proposito-e-ilustracao-unificada/23-UI-SPEC.md (Illustration Contract — o que é travado e o que é discricionário)
  </read_first>
  <files>web/src/pet/BorisFlat.jsx</files>
  <action>
Criar `web/src/pet/BorisFlat.jsx`: um componente de apresentação puro, SVG
inline, sem estado, sem efeito, sem I/O.

**Assinatura:** `export default function BorisFlat({ size = 110 })`.

**Docstring no topo do arquivo** (mesma densidade dos outros arquivos de
`pet/`), cobrindo:
- que é a arte do modal de introdução (ILUS-01, Fase 23), e que o `PetFab`
  segue com o PNG de propósito (decisão de escopo do kickoff);
- que os hex são LITERAIS porque importar `BRAND`/`T` de `App.jsx` criaria
  import circular — mesma razão que `BorisIntro.jsx` já documenta (linhas
  23-25). Apontar `App.jsx:201-231` (`LogoMark`) como a fonte de verdade dos
  valores, para que qualquer mudança de marca seja feita nos dois lugares;
- que óculos e bico são âmbar em QUALQUER tema/modo — a regra travada de
  `App.jsx:202-205` ("nunca recolorir os óculos fora do âmbar da marca"),
  fechada na v2 do Brand Book.

**Estrutura obrigatória do SVG:**

1. `viewBox` que preserve as coordenadas do `LogoMark` no terço superior, para
   que o rosto seja copiado **verbatim** em vez de redesenhado. Recomendado:
   `viewBox="0 0 64 92"` — a cabeça do `LogoMark` ocupa y≈8..60 sem nenhuma
   transformação, e sobram ~32 unidades para o corpo. `width={size}`,
   `height={Math.round(size * 92 / 64)}`.
2. **Rosto copiado literalmente** do `LogoMark` (os 9 elementos listados em
   `<estado_verificado_ao_vivo>`), com os mesmos `cx`/`cy`/`r`/`d`/`strokeWidth`.
   Não recalcular, não "melhorar" proporção do rosto.
3. **Corpo novo, abaixo** (a única parte inventada): forma arredondada simples
   em `#2a3a6b` — elipse ou path arredondado — nascendo atrás do rosto
   (desenhada ANTES dele na ordem do documento, para o rosto ficar por cima) e
   descendo até o pé do `viewBox`. Flat: `fill` chapado, zero gradiente, zero
   `filter`, zero sombreado.
4. **Sem o fundo `#161927` e sem o selo "+" do `LogoMark`.** Os dois são
   elementos de BADGE, não do personagem; o fundo do modal (`T.bgCard` do
   `BorisIntro`) é o fundo. Fundo transparente.
5. **Discricionário** (`23-CONTEXT.md`/`23-UI-SPEC.md`): proporção do corpo,
   presença de "gravata" (o PNG atual tem uma), pés visíveis, asas. Se incluir
   gravata, usar `#f2a93b` (não uma cor nova). Critério único: o resultado tem
   que ser inequivocamente o mesmo personagem do `LogoMark`.
6. **Acessibilidade:** `aria-hidden role="img"`, exatamente como o `LogoMark`
   (`App.jsx:210`). **Não** adicionar `aria-label` — o título "Este é o
   **Boris**" logo abaixo já identifica o personagem, e um rótulo aqui criaria
   anúncio duplicado (mesma regra que a Fase 22 estabeleceu para ícones com
   rótulo textual adjacente).
7. `style={{ display: "block", flex: "none" }}` como no `LogoMark`, para não
   herdar o baseline de inline nem encolher dentro do flex do modal.

**Proibições explícitas:**
- nenhum `import` de `../App.jsx` (nem de nada em `web/src/` fora de `pet/`);
- nenhum `var(--...)`, nenhum token de tema — a arte é fixa;
- nenhum `<image>`/`<img>`/`boris.png`;
- nenhuma animação, nenhum `@keyframes`, nenhum `<style>` — a ilustração é
  estática (motion desta fase é dos planos 23-01/23-03, em outro arquivo).

Ao final desta task: `cd web && npx vite build` deve concluir com código 0
(o arquivo novo ainda não é importado por ninguém, mas o build valida a
sintaxe JSX). **Não editar `BorisIntro.jsx` nesta task** — o swap é a Task 2,
para que o guardião quebre e seja consertado num passo só, visível no diff.
  </action>
  <verify>
    <automated>cd web && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/pet/BorisFlat.jsx` existe e contém `export default function BorisFlat`.
    - `grep -c 'from "\.\./App' web/src/pet/BorisFlat.jsx` retorna `0`, e `grep -c "import" web/src/pet/BorisFlat.jsx` retorna `0` (o componente não importa nada — nem React, seguindo o JSX runtime automático já usado no projeto; se o projeto exigir import de React, então a ÚNICA linha de import permitida é a de `react`).
    - `grep -c '#2a3a6b' web/src/pet/BorisFlat.jsx` é >= 3 (tufos, rosto, corpo) e `grep -c '#f2a93b' web/src/pet/BorisFlat.jsx` é >= 4 (dois aros, ponte, bico).
    - `grep -c '#eef1f8' web/src/pet/BorisFlat.jsx` é >= 2 (os dois olhos).
    - `grep -c 'var(--' web/src/pet/BorisFlat.jsx` retorna `0` — a arte não é temável.
    - `grep -Ec 'linearGradient|radialGradient|filter=|feGaussian|<image|boris\.png' web/src/pet/BorisFlat.jsx` retorna `0` — flat de verdade.
    - `grep -c 'aria-hidden' web/src/pet/BorisFlat.jsx` retorna `1` e `grep -c 'aria-label' web/src/pet/BorisFlat.jsx` retorna `0`.
    - Os quatro `d`/`cx`/`cy` do rosto batem com o `LogoMark`: `grep -c 'M14 16 L22 27 L10 27 Z' web/src/pet/BorisFlat.jsx`, `grep -c 'M32 42 L27.5 49 L36.5 49 Z'`, `grep -c 'cx="22" cy="32"'` e `grep -c 'cx="42" cy="32"'` retornam todos >= 1.
    - `cd web && npx vite build` conclui com código 0.
    - `git status --porcelain web/src/pet/BorisIntro.jsx web/tests/test_boris_intro.mjs` sai VAZIO (nada editado ainda).
    - `git status --porcelain web/src/pet/Boris.jsx web/src/assets/boris.png` sai VAZIO.
  </acceptance_criteria>
  <done>Existe uma ilustração flat do Boris, meio-corpo, com o rosto copiado literalmente do LogoMark e um corpo novo na mesma linguagem, sem importar nada de App.jsx e sem cor fora da lista assinada.</done>
</task>

<task type="auto">
  <name>Task 2: Swap no BorisIntro + guardião test_boris_intro.mjs atualizado</name>
  <read_first>
    - web/src/pet/BorisIntro.jsx (arquivo inteiro, 94 linhas — o import na 27 e o call site na 42)
    - web/tests/test_boris_intro.mjs (arquivo inteiro, 113 linhas — em especial a asserção "BorisIntro.jsx importa Boris (mesmo componente animado da F1)", a única que este plano invalida)
    - web/src/pet/BorisFlat.jsx (o arquivo criado na Task 1 — a assinatura e a prop `size`)
    - CLAUDE.md (guardrail "Guardiões de teste não se apagam — reversão deliberada atualiza o guardião com nota")
  </read_first>
  <files>web/src/pet/BorisIntro.jsx, web/tests/test_boris_intro.mjs</files>
  <action>
**Passo 1 — `BorisIntro.jsx`, duas linhas e um comentário.**
- Linha 27: `import Boris from "./Boris.jsx";` → `import BorisFlat from "./BorisFlat.jsx";`
  **Remover o import de `Boris`**, não deixar os dois — `Boris` deixa de ser
  usado neste arquivo, e import morto é exatamente o rastro que faz o próximo
  agente achar que a troca foi parcial. (`Boris.jsx` continua vivo: `App.jsx:18`
  o importa para o `PetFab`.)
- Linha 42: `<Boris size={110} />` → `<BorisFlat size={110} />`.
- Acrescentar ao docstring do topo um parágrafo curto de decisão (não
  descrição): que a arte da intro é a flat de `BorisFlat.jsx` desde a Fase 23
  (ILUS-01), que o `PetFab` **segue com o PNG de propósito** (decisão de escopo
  do kickoff do milestone v1.5) e que, portanto, ver as duas artes no mesmo app
  não é defeito.
- **Nada mais muda.** Nenhuma linha de layout, nenhum texto, nenhum estilo. A
  coluna flex centralizada das linhas 41-49 fica como está — a ilustração nova
  entra no mesmo slot com a mesma largura de 110px.

**Passo 2 — atualizar `web/tests/test_boris_intro.mjs`.** Regra do repositório
(CLAUDE.md): guardião não se apaga, se ATUALIZA com nota.

Substituir a asserção:

    ok("BorisIntro.jsx importa Boris (mesmo componente animado da F1)",
       /import Boris from ["']\.\/Boris\.jsx["'];/.test(introSrc));

por um bloco novo, precedido de um comentário datado explicando a REVERSÃO
DELIBERADA (a arte da intro mudou na Fase 23, ILUS-01; a asserção antiga
travava o PNG semi-realista e não pode simplesmente sumir do histórico do
arquivo). O bloco novo trava, no mínimo:

1. `BorisIntro.jsx` importa `BorisFlat` de `./BorisFlat.jsx`
   (`/import BorisFlat from ["']\.\/BorisFlat\.jsx["'];/`).
2. `BorisIntro.jsx` **NÃO** importa mais `Boris` de `./Boris.jsx`
   (`!/import Boris from ["']\.\/Boris\.jsx["']/.test(introSrc)`) — a asserção
   de AUSÊNCIA é a que prova que a troca foi completa; só a de presença
   passaria com os dois imports convivendo.
3. O call site é `<BorisFlat size={110} />` e **não existe mais `<Boris `** no
   corpo do componente (usar o mesmo recorte `introBody` que o arquivo já
   calcula a partir de `export default function`, para o docstring — que
   MENCIONA `Boris` e `PetFab` em prosa — não autoinvalidar a asserção).
4. `web/src/pet/BorisFlat.jsx` existe, exporta `BorisFlat` por default, e é
   flat: contém `#2a3a6b`, `#f2a93b`, `#eef1f8`, e **não** contém
   `linearGradient`, `radialGradient`, `<image`, `boris.png` nem `filter=`.
5. **Trava de marca:** `BorisFlat.jsx` não contém `var(--` — os óculos e o
   corpo não podem virar temáveis nem seguir o acento do modo
   (regra de `App.jsx:202-205`, fechada na v2 do Brand Book).
6. **Trava de fronteira de escopo:** `web/src/App.jsx` continua importando
   `Boris` de `./pet/Boris.jsx` (`/import Boris from ["']\.\/pet\/Boris\.jsx["']/`)
   — o `PetFab` não foi arrastado junto. Mensagem de falha em texto: "a troca
   de arte se limita ao modal de introdução (kickoff do milestone v1.5)".
7. Acessibilidade: `BorisFlat.jsx` tem `aria-hidden` e **não** tem
   `aria-label` (o título do modal já identifica o personagem).

**Não afrouxar nem remover nenhuma das outras asserções do arquivo** — ordem
D8 do conteúdo, frases de disclaimer, `onConversar`/`onDepois`, `abrirPet`
como fonte única de `setPetOpen(true)`, portão de overlay, `borisIntroVisto`
nos 3 lugares. Elas continuam válidas e este plano não as toca.

Depois das edições, nesta ordem:
1. `node web/tests/test_boris_intro.mjs` — verde, com as asserções novas.
2. `cd web && npx vite build` — obrigatório (CLAUDE.md).
3. `bash scripts/executar.sh --testes` — suíte canônica, as DUAS suítes.

Se qualquer teste que não seja `test_boris_intro.mjs` falhar, PARAR e
reportar: a varredura deste plano concluiu que nenhum outro `web/tests/*.mjs`
cita `BorisIntro`, então falha inesperada significa que a varredura errou.

**Não dar `git push`** — Fases 17/18/19 do v1.4 seguem com checkpoints humanos
pendentes; a decisão de enviar a `origin` é do Alex.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'import BorisFlat from "./BorisFlat.jsx"' web/src/pet/BorisIntro.jsx` retorna `1`.
    - `grep -c 'import Boris from "./Boris.jsx"' web/src/pet/BorisIntro.jsx` retorna `0`.
    - `grep -c '<BorisFlat size={110} />' web/src/pet/BorisIntro.jsx` retorna `1` e `grep -c '<Boris size={110} />' web/src/pet/BorisIntro.jsx` retorna `0`.
    - `git diff --stat web/src/pet/BorisIntro.jsx` mostra um diff pequeno (import, call site e o parágrafo do docstring) — nenhuma linha de layout, texto de conteúdo ou estilo alterada.
    - `grep -c 'import Boris from "./pet/Boris.jsx"' web/src/App.jsx` retorna `1` — o `PetFab` não foi arrastado.
    - `git status --porcelain web/src/pet/Boris.jsx web/src/assets/boris.png resources/ios server/ios_dist` sai VAZIO.
    - `web/tests/test_boris_intro.mjs` tem MAIS asserções do que antes (nenhuma removida sem substituta) e contém um comentário datado explicando a reversão deliberada de ILUS-01.
    - O guardião tem a asserção de AUSÊNCIA de `import Boris from "./Boris.jsx"` em `BorisIntro.jsx`, não só a de presença de `BorisFlat`.
    - `node web/tests/test_boris_intro.mjs` sai com código 0.
    - `cd web && npx vite build` conclui com código 0.
    - `bash scripts/executar.sh --testes` sai com código 0 — as DUAS suítes.
    - `git diff --stat` lista apenas `web/src/pet/BorisFlat.jsx`, `web/src/pet/BorisIntro.jsx` e `web/tests/test_boris_intro.mjs`; `git diff web/package.json web/package-lock.json` sai VAZIO.
    - O SUMMARY contém a seção "Pendente de verificação ao vivo (orquestrador)" com os 6 itens de `<environment_limitation_known_upfront>`, o caminho para reabrir o modal numa conta que já viu, e os números de contraste medidos de `<risco_de_contraste_medido>`.
    - Nenhum `git push` foi executado.
  </acceptance_criteria>
  <done>O modal "Este é o Boris" mostra a ilustração flat nova, o PNG semi-realista saiu daquele slot (e só dele), e o guardião foi atualizado com nota — travando tanto a troca quanto a fronteira de escopo que a troca não pode ultrapassar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `pet/*.jsx` → `App.jsx` | Import na direção `pet/ → App.jsx` recria o ciclo que `BorisIntro.jsx` documenta ter contornado; quebra o build de forma difícil de ler. |
| troca de arte → fronteira de escopo do kickoff | "Unificar o Boris" convida a arrastar o `PetFab` e o ícone do app junto — o ícone já está publicado no TestFlight/App Store. |
| guardião antigo → mudança deliberada | Apagar a asserção que a mudança invalidou destrói o registro de que ela existia e por quê. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-07 | Denial of service | `BorisFlat.jsx` importar `BRAND`/`T` de `App.jsx` e criar import circular | mitigate | `<restricao_de_import_circular>` + critério de aceite `grep -c 'from "../App'` = 0 + `npx vite build` obrigatório |
| T-23-08 | Tampering | Troca parcial: import de `BorisFlat` adicionado e import de `Boris` deixado para trás | mitigate | Guardião com asserção de AUSÊNCIA (item 2 do Passo 2), não só de presença |
| T-23-09 | Elevation of privilege | Arrastar `PetFab`/`Boris.jsx`/ícone do app para a troca de arte, além da fronteira do kickoff | mitigate | `<fronteiras_de_escopo>` + asserção 6 do guardião (App.jsx segue importando Boris) + critério de aceite com `git status` vazio nos caminhos protegidos |
| T-23-10 | Tampering | Óculos/bico virarem temáveis ou seguirem o acento do modo, reabrindo a regressão que a v2 fechou | mitigate | Asserção 5 do guardião (`var(--` ausente em BorisFlat.jsx) + item 4 do roteiro de verificação ao vivo (Modo Operador) |
| T-23-11 | Repudiation | Apagar a asserção antiga do `test_boris_intro.mjs` em vez de atualizá-la com nota | mitigate | Critério de aceite exige contagem de asserções MAIOR que antes e comentário datado de reversão deliberada |
| T-23-12 | Spoofing | Declarar "é o mesmo personagem" e "separa do fundo no escuro" sem ter olhado | mitigate | `<risco_de_contraste_medido>` traz os números reais (1,49:1 no escuro) e o remédio pré-autorizado; `<environment_limitation_known_upfront>` proíbe estimativa |
| T-23-SC | Tampering | instalações npm/pip/cargo | mitigate | Zero pacote novo (SVG escrito à mão); `git diff` de `web/package.json`/`package-lock.json` vazio como critério de aceite |
</threat_model>

<verification>
- `BorisFlat.jsx` existe, é flat, sem import de `App.jsx`, sem `var(--`, sem gradiente/filtro/PNG.
- Rosto com a geometria literal do `LogoMark`; corpo novo em `#2a3a6b`.
- `BorisIntro.jsx` importa e renderiza `BorisFlat`; o import de `Boris` saiu.
- `App.jsx` segue importando `Boris` (PetFab intocado); `Boris.jsx`, `boris.png`, `resources/ios`, `server/ios_dist` sem diff.
- `test_boris_intro.mjs` atualizado com nota, com mais asserções do que antes e com a asserção de ausência.
- `npx vite build` verde; `scripts/executar.sh --testes` verde nas duas suítes.
- Roteiro de verificação ao vivo (6 itens) + números de contraste entregues ao orquestrador no SUMMARY.
</verification>

<success_criteria>
Os critérios 4 e 5 do ROADMAP da Fase 23 estão satisfeitos no código: a arte do
modal "Este é o Boris" é flat/cartoon no mesmo vocabulário do `LogoMark` e do
ícone do app, e o ícone já publicado não foi tocado — com o julgamento visual
("é inequivocamente o mesmo personagem", "separa do fundo no tema escuro")
entregue ao orquestrador como roteiro medido, não como afirmação.
</success_criteria>

<output>
Create `.planning/phases/23-motion-com-proposito-e-ilustracao-unificada/23-02-SUMMARY.md` when done
</output>
