---
phase: 34-navega-o-hub-workspace
plan: 03
type: execute
wave: 3
depends_on: ["34-02"]
files_modified:
  - web/src/opcoes/OpcoesScreen.jsx
  - web/tests/test_opcoes_hub_workspace_ui.mjs
  - web/tests/test_opcoes_analisar_ui.mjs
  - web/tests/test_opcoes_criar_setup_ui.mjs
autonomous: true
requirements: [NAV-02, NAV-05]

must_haves:
  truths:
    - "Dentro do workspace, três pills alternam entre Analisar, Comparar e Setups salvos [NAV-02, D-02]"
    - "Trocar de pill NÃO dispara nenhuma leitura paga: os três jobs continuam compartilhando a mesma leitura MCP de 3 chamadas [NAV-05]"
    - "A pill ativa é a única coisa que recebe accent; todas têm alvo de toque de 44px e aria-pressed"
    - "O estado da pill é local do orquestrador e ortogonal ao `subaba` (Setups × Operar) — nenhum dos dois é reusado pelo outro"
  artifacts:
    - path: "web/src/opcoes/OpcoesScreen.jsx"
      provides: "Pill row de 3 abas do workspace + gate do ramo DADOS por aba"
      contains: "abaWorkspace"
    - path: "web/tests/test_opcoes_hub_workspace_ui.mjs"
      provides: "Asserções de NAV-05 (troca de aba não paga) e da pill row"
      contains: "abaWorkspace"
  key_links:
    - from: "web/src/opcoes/OpcoesScreen.jsx (pill row)"
      to: "setAbaWorkspace"
      via: "onClick puro de useState — nunca um disparador de chamada"
      pattern: "onClick=\\{\\(\\) => setAbaWorkspace\\("
    - from: "web/src/opcoes/OpcoesScreen.jsx (ramo DADOS)"
      to: "SecaoAnalisar / SecaoComparar / SecaoSetups"
      via: "gate por abaWorkspace, com as MESMAS props de hoje"
      pattern: "abaWorkspace === \"(analisar|comparar|setups)\""
---

<objective>
Acrescentar ao workspace a pill row de 3 abas (Analisar / Comparar / Setups
salvos — D-02) e gatear o ramo "4. DADOS" da cascata por ela. A leitura paga
continua sendo pedida UMA vez, no orquestrador, acima da pill row: trocar de
aba é `useState` puro, nunca um novo pedido.

Purpose: NAV-05 (os três jobs compartilham a mesma leitura de 3 chamadas —
custo atual preservado, decisão confirmada com o Alex) e NAV-02 (o workspace é
o lugar do ticker, com os três trabalhos dele à mão sem rolagem).
Output: um seletor de 3 pills e um gate de render; zero chamada nova.
</objective>

<context_deviation>
Este plano assume a resolução do conflito D-01 registrada no
`34-02-PLAN.md` (seção `blocking_finding`): a 3ª aba é **"Setups salvos"** e
renderiza `<SecaoSetups>` INTEIRO (listagem ticker-scoped "SETUPS GRAVADOS" +
bloco "CRIAR UM SETUP"), porque a listagem cross-ticker do hub é
`<SecaoVigias` (mesma entidade, custo zero) e `l.setups` só existe com ticker
+ leitura paga.

Se o Alex reverter para a leitura literal de D-01, muda só esta task 2:
a 3ª aba renderiza `<SecaoSetups modo="criar">`, `SecaoSetups.jsx` ganha a
prop `modo` (a partição está mapeada em `34-PATTERNS.md`, seção
`SecaoSetups.jsx`), o rótulo volta a `cp.opcoesAbaCriarSetup` = "Criar Setup",
e o hub ganha `<SecaoSetups modo="lista">` — aceitando que ele renderize
sempre vazio. Nada mais da fase muda.
</context_deviation>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-navega-o-hub-workspace/34-CONTEXT.md
@.planning/phases/34-navega-o-hub-workspace/34-UI-SPEC.md
@.planning/phases/34-navega-o-hub-workspace/34-PATTERNS.md
@.planning/phases/34-navega-o-hub-workspace/34-02-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
<!-- Contrato do estado novo e do gate. Nada disto deve ser re-derivado. -->

Estado novo, no orquestrador (`OpcoesScreen.jsx`), declarado junto de `subaba`:

  const [abaWorkspace, setAbaWorkspace] = useState("analisar");

- ids das abas: "analisar" | "comparar" | "setups"
- **NÃO reusar `subaba`** — `subaba` significa "Setups × Operar" (Fase 28,
  28-02) e é ortogonal. Reusá-lo faria a aba interna do workspace mudar a
  sub-aba da tela.
- rótulos: `cp.opcoesAbaAnalisar`, `cp.opcoesAbaComparar`,
  `cp.opcoesAbaSetupsSalvos` (as três chaves criadas no 34-01, nos dois modos)

Gate do ramo "4. DADOS":
  abaWorkspace === "analisar" → <SecaoAnalisar ... />           (props idênticas às de hoje)
  abaWorkspace === "comparar" → temLeitura ? <SecaoComparar/> : null   (gate `temLeitura` PRESERVADO)
  abaWorkspace === "setups"   → <SecaoSetups ... />             (props idênticas às de hoje)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: pill row de 3 abas do workspace</name>
  <files>web/src/opcoes/OpcoesScreen.jsx</files>
  <read_first>
    - web/src/opcoes/OpcoesScreen.jsx — linhas 593-617 (`subabas`: o padrão de
      pill a copiar verbatim, incluindo o comentário que declara por que ele
      reusa a régua do `seletor`), 243-249 (`ticker`/`subaba`), e o
      `workspaceTopo` criado no 34-02
    - .planning/phases/34-navega-o-hub-workspace/34-PATTERNS.md — seção
      "Core pill-row pattern to copy verbatim"
    - .planning/phases/34-navega-o-hub-workspace/34-UI-SPEC.md — seções
      "Workspace composition", Color (accent reservado à pill ativa) e Mobile
      375px
  </read_first>
  <action>
    Declarar `const [abaWorkspace, setAbaWorkspace] = useState("analisar");`
    imediatamente após o `const [subaba, setSubaba] = useState("setups");`
    (linha ~249), com comentário dizendo: é estado de NAVEGAÇÃO local do
    workspace, ortogonal a `subaba`, e nasce em "analisar" porque é o job que
    a leitura paga entrega primeiro.

    Declarar `const workspacePillRow = (...)` copiando a estrutura de
    `subabas` (linhas 600-617) **verbatim**, mudando apenas: o array de abas
    (3 entradas: `{ id: "analisar", rotulo: cp.opcoesAbaAnalisar || "Analisar" }`,
    `{ id: "comparar", rotulo: cp.opcoesAbaComparar || "Comparar" }`,
    `{ id: "setups", rotulo: cp.opcoesAbaSetupsSalvos || "Setups salvos" }`),
    o `onClick` (`() => setAbaWorkspace(a.id)`) e o `aria-pressed`
    (`abaWorkspace === a.id`). Todos os valores de estilo iguais aos do
    original: `gap: "8px"`, `margin: "10px 0 4px"`, `minHeight: "44px"`,
    `padding: "8px 14px"`, `borderRadius: "11px"`, accent só no ativo,
    `fontWeight: 700`, `fontSize: "13px"`. Nenhum valor de espaçamento novo
    (contrato de Spacing do UI-SPEC); sem `role="tab"` (este app não usa ARIA
    de tab em lugar nenhum — ver `BottomNav` em `App.jsx`).

    Renderizar `{workspacePillRow}` como ÚLTIMO elemento de `workspaceTopo`,
    logo depois de `{podePedirLeitura ? blocoLeituraDoServico : null}` — ou
    seja, ACIMA da cascata. É essa posição que faz NAV-05 ser estrutural e não
    uma promessa: lastro, leitura interna e convite pago são compartilhados
    pelas três abas e são pedidos uma vez, fora do gate de aba.

    Proibições explícitas, com razão:
    - O `onClick` da pill chama SÓ `setAbaWorkspace`. Nada de
      `abrirLeitura`/`abrirCadeia`/`abrirOperaveis`/`montarProposta`/
      `verPossibilidades`/`atualizarVigias` — qualquer um deles ali cobraria
      cota por trocar de aba, o oposto exato de NAV-05 e do §3.3 do ADR-027
      ("custo de MCP só em clique explícito com custo declarado").
    - Nenhum `useEffect` novo reagindo a `abaWorkspace`: efeito que dispara
      chamada na troca de aba é a MESMA classe de defeito que a Fase 27
      (27-05) fechou quando tirou a leitura do efeito de troca de ticker.
    - Não declarar custo na pill (`CUSTO_NO_BOTAO`): declarar custo onde não
      há custo mente para o outro lado, e o guardião
      `test_opcoes_custo_declarado.mjs` proíbe.
  </action>
  <acceptance_criteria>
    - `grep -c "const \[abaWorkspace, setAbaWorkspace\] = useState(\"analisar\")" web/src/opcoes/OpcoesScreen.jsx` retorna 1
    - `grep -c "setAbaWorkspace" web/src/opcoes/OpcoesScreen.jsx` retorna 2
      (declaração + o único `onClick`)
    - A fatia de `workspacePillRow` (de `const workspacePillRow` até o `);`
      que o fecha) contém `minHeight: "44px"` e `aria-pressed` e NÃO contém
      nenhum de `abrirLeitura|abrirCadeia|abrirOperaveis|montarProposta|verPossibilidades|atualizarVigias|CUSTO_NO_BOTAO`
    - `grep -c "useEffect" web/src/opcoes/OpcoesScreen.jsx` inalterado em
      relação ao 34-02 (nenhum efeito novo)
    - `cd web && npx vite build` verde
  </acceptance_criteria>
  <verify>
    <automated>cd web && npx vite build && cd .. && node web/tests/test_opcoes_custo_declarado.mjs</automated>
  </verify>
  <done>A pill row existe com 3 abas, reusa a régua visual do `subabas` sem valor novo, vive acima da cascata e seu clique é `useState` puro.</done>
</task>

<task type="auto">
  <name>Task 2: gatear o ramo 4 (DADOS) por aba, preservando props e gates de hoje</name>
  <files>web/src/opcoes/OpcoesScreen.jsx</files>
  <read_first>
    - web/src/opcoes/OpcoesScreen.jsx — o ramo 4 inteiro como está depois do
      34-02 (hoje linhas 769-859): `<SecaoAnalisar>` com 21 props,
      `{temLeitura ? <SecaoComparar> : null}` com 17 props, `<SecaoSetups>`
      com 14 props, e os comentários de fase que acompanham cada um
    - web/src/opcoes/SecaoAnalisar.jsx — o `useEffect([ticker])` interno que
      fecha o painel na troca de ativo (a desmontagem por troca de aba tem de
      ser avaliada contra ele)
    - web/tests/test_opcoes_analisar_ui.mjs — as asserções que ancoram em
      `<SecaoAnalisar` e na ordem dentro do ramo DADOS
    - web/tests/test_opcoes_criar_setup_ui.mjs — as asserções que ancoram em
      `<SecaoSetups`/`CriarSetup`
  </read_first>
  <action>
    Envolver cada um dos três renders do ramo 4 no gate da aba, sem tocar
    NENHUMA prop:

    - `{abaWorkspace === "analisar" ? <SecaoAnalisar ...todas as props de hoje... /> : null}`
    - `{abaWorkspace === "comparar" && temLeitura ? <SecaoComparar ...todas as props de hoje... /> : null}`
      — o `temLeitura` de hoje NÃO sai: sem leitura não há de onde a tese
      sair, e o comentário de 33-04 que diz isso fica.
    - `{abaWorkspace === "setups" ? <SecaoSetups ...todas as props de hoje... /> : null}`

    Manter os comentários de fase de cada bloco (eles registram a posição
    EXATA de origem de cada job) e acrescentar em cada um uma linha datada
    dizendo que a partir da Fase 34 o bloco é gateado por `abaWorkspace`, sem
    mudança de prop.

    **Consequência a registrar no SUMMARY, não a "corrigir":** trocar de aba
    DESMONTA a seção anterior, então o estado LOCAL dela reseta —
    `painel` (cadeia/operáveis aberto) em `SecaoAnalisar.jsx` e o equivalente
    em `SecaoComparar.jsx`. Isso é consistente com D-03 (o workspace não
    guarda estado escondido) e NÃO custa chamada: os dados de
    `cadeia`/`operaveis`/`possibilidades` vivem nos trios do hook, no
    orquestrador, e continuam lá enquanto o ticker não muda — reabrir o painel
    mostra o dado já pago, sem novo pedido. Conferir isso lendo o hook antes
    de afirmar, e escrever a conferência no SUMMARY (é o coração de NAV-05).

    Se a conferência mostrar que reabrir o painel REPETE uma chamada (ou seja,
    `abrirCadeia`/`abrirOperaveis` é chamado no mount de `SecaoAnalisar` em
    vez de por clique), PARAR e reportar: seria um custo novo introduzido pela
    troca de aba, o que NAV-05 proíbe. Não contornar com cache novo — isso
    seria mecanismo de dado novo, fora do escopo desta fase.

    Nada de `<SubAbaOperar>`, `subabas`, `seletor`, `cabecalho`, ramos 1-3 da
    cascata ou `disclaimer` muda nesta task.
  </action>
  <acceptance_criteria>
    - `grep -c "abaWorkspace === \"analisar\"\|abaWorkspace === \"comparar\"\|abaWorkspace === \"setups\"" web/src/opcoes/OpcoesScreen.jsx` retorna 3
    - `grep -c "temLeitura ?" web/src/opcoes/OpcoesScreen.jsx` — o gate de
      `SecaoComparar` continua presente (conferir por `git diff` que a
      condição `temLeitura` não foi removida, só combinada)
    - `git diff web/src/opcoes/OpcoesScreen.jsx` não mostra NENHUMA linha de
      prop removida ou renomeada nos três renders (só as linhas de gate
      acrescentadas ao redor)
    - `node web/tests/test_opcoes_analisar_ui.mjs && node web/tests/test_opcoes_criar_setup_ui.mjs` verdes
      (reapontando âncoras se e só se alguma ficar inerte — nunca afrouxando)
    - `cd web && npx vite build` verde
  </acceptance_criteria>
  <verify>
    <automated>cd web && npx vite build && cd .. && node web/tests/test_opcoes_analisar_ui.mjs && node web/tests/test_opcoes_criar_setup_ui.mjs</automated>
  </verify>
  <done>Os três jobs do workspace renderizam um por vez pela aba ativa, com as props e os gates de hoje intactos; nenhuma chamada nova nasce da troca de aba.</done>
</task>

<task type="auto">
  <name>Task 3: guardião de NAV-05 + suíte canônica verde</name>
  <files>web/tests/test_opcoes_hub_workspace_ui.mjs</files>
  <read_first>
    - web/tests/test_opcoes_hub_workspace_ui.mjs — as asserções do 34-01 e do
      34-02, para acrescentar seguindo o mesmo estilo
    - web/tests/test_opcoes_analisar_ui.mjs — a asserção existente de "escolher
      ticker não dispara leitura paga" (é o analog direto da asserção de
      NAV-05 abaixo: mesma classe de defeito, um nível mais fundo)
    - web/src/opcoes/OpcoesScreen.jsx — a fatia da pill row e o ramo 4 já
      gateado
  </read_first>
  <action>
    Acrescentar ao guardião novo, sempre sobre a fonte sem comentário:

    1. **NAV-05, o núcleo:** a fatia da pill row (`const workspacePillRow`
       até o `);` de fechamento) não contém nenhum de
       `abrirLeitura|abrirCadeia|abrirOperaveis|montarProposta|verPossibilidades|atualizarVigias|compilarSetup|confirmarSetup`.
       Trocar de aba não pode pagar.
    2. Nenhum `useEffect` do arquivo lista `abaWorkspace` no array de
       dependências — efeito que reage à troca de aba é a porta pela qual o
       custo voltaria em silêncio. Asserção por regex sobre cada
       `useEffect(..., [ ... ])` do arquivo.
    3. `abaWorkspace` e `subaba` são estados distintos: `setSubaba` não
       aparece dentro da fatia da pill row, e `setAbaWorkspace` não aparece
       dentro da fatia de `subabas`.
    4. A pill row tem exatamente 3 abas e as 3 usam chave de copy (nenhum
       rótulo literal solto): a fatia contém `cp.opcoesAbaAnalisar`,
       `cp.opcoesAbaComparar` e `cp.opcoesAbaSetupsSalvos`, uma vez cada.
    5. Alvo de toque e afordância: a fatia contém `minHeight: "44px"` e
       `aria-pressed`.
    6. O gate de `temLeitura` de `SecaoComparar` sobreviveu: a fatia do render
       de `<SecaoComparar` é precedida por `temLeitura` na mesma expressão.
    7. Os três renders do ramo 4 são mutuamente exclusivos: existem
       exatamente 3 comparações `abaWorkspace === "..."` no arquivo, e os três
       ids são `analisar`, `comparar`, `setups` (nenhum id órfão que nunca
       vira pill, nenhuma pill sem destino).

    Nas asserções que contam ocorrências, filtrar comentário ANTES de contar
    (`semComentario`) — este arquivo ganhou comentários citando
    `abrirLeitura` e `abaWorkspace` para explicar as proibições.

    Fechar a onda com a suíte canônica e o build.
  </action>
  <acceptance_criteria>
    - `node web/tests/test_opcoes_hub_workspace_ui.mjs` verde com as 7
      asserções novas (13+ no total do arquivo)
    - Prova negativa REAL, mínimo 2: (a) inserir `abrirLeitura()` no `onClick`
      da pill → asserção 1 reprova; (b) acrescentar `abaWorkspace` às
      dependências de um `useEffect` → asserção 2 reprova. Reverter com
      `git checkout -- web/src/opcoes/OpcoesScreen.jsx` e registrar no SUMMARY.
    - `bash scripts/executar.sh --testes` na baseline (2923 pytest / 0 failed;
      `.mjs` só com a falha ambiental `test_ios_assets.mjs`)
    - `cd web && npx vite build` verde
    - `git diff web/src/App.jsx` vazio
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <done>NAV-05 é travado por guardião (não por promessa), com prova negativa; suíte canônica na baseline; build verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhuma nova | Estado de navegação local e gate de render. Nenhuma chamada, rota ou dado novo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-06 | Denial of service (consumo de cota do usuário) | pill row do workspace | mitigate | Asserções 1 e 2 do guardião: a fatia da pill row não pode conter nenhum disparador de chamada, e nenhum `useEffect` pode depender de `abaWorkspace`. É o vetor pelo qual trocar de aba passaria a cobrar 3 chamadas por toque (NAV-05, §3.3 do ADR-027). |
| T-34-07 | Repudiation (custo não declarado) | pill row | mitigate | A pill NÃO declara custo porque não tem custo; `test_opcoes_custo_declarado.mjs` reprova custo declarado onde não há cobrança, e a asserção 1 garante que não há cobrança. |
| T-34-SC | Tampering (supply chain) | npm install | accept | Zero pacote novo nesta fase. |
</threat_model>

<verification>
1. `bash scripts/executar.sh --testes` — as DUAS suítes, baseline da Fase 33.
2. `cd web && npx vite build` verde.
3. Conferência escrita no SUMMARY de que reabrir cadeia/operáveis depois de
   trocar de aba NÃO repete chamada (leitura do hook, não suposição).
4. Provas negativas por injeção registradas (mínimo 2).
</verification>

<success_criteria>
- Três pills no workspace, ativa recebendo accent, 44px, `aria-pressed`
  [NAV-02, D-02].
- Trocar de pill não dispara leitura paga — travado por guardião [NAV-05].
- Props e gates dos três jobs idênticos aos de hoje.
- Suíte canônica na baseline; `App.jsx` intocado.
</success_criteria>

<output>
Create `.planning/phases/34-navega-o-hub-workspace/34-03-SUMMARY.md` when done.
</output>
