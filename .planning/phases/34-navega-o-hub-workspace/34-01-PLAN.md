---
phase: 34-navega-o-hub-workspace
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/copy.js
  - web/src/opcoes/WorkspaceHeader.jsx
  - web/tests/test_opcoes_hub_workspace_ui.mjs
autonomous: true
requirements: [NAV-03]

must_haves:
  truths:
    - "Existe um componente de header de workspace que mostra o ticker escolhido e um único botão de voltar (NAV-03), sem breadcrumb e sem histórico"
    - "As 4 chaves de copy novas existem nos DOIS modos (COPY.estudo e COPY.operador) — nenhum modo abre com rótulo vazio"
    - "O guardião novo reprova a suíte se o header passar a ler store/hook, renderizar `.manchete`, ou perder o alvo de toque de 44px"
  artifacts:
    - path: "web/src/opcoes/WorkspaceHeader.jsx"
      provides: "Header fixo do workspace (nome do ticker + botão Voltar) — D-05"
      contains: "export default function WorkspaceHeader"
    - path: "web/tests/test_opcoes_hub_workspace_ui.mjs"
      provides: "Guardião da fase 34 (parte 1: header + paridade de copy)"
      contains: "WorkspaceHeader.jsx"
    - path: "web/src/copy.js"
      provides: "opcoesVoltarAoHub / opcoesAbaAnalisar / opcoesAbaComparar / opcoesAbaSetupsSalvos nos dois modos"
      contains: "opcoesVoltarAoHub"
  key_links:
    - from: "web/tests/test_opcoes_hub_workspace_ui.mjs"
      to: "web/src/copy.js"
      via: "import { COPY } e checagem de paridade estudo × operador"
      pattern: "COPY\\.(estudo|operador)"
    - from: "web/src/opcoes/WorkspaceHeader.jsx"
      to: "CSS custom properties de :root"
      via: "espelho declarado VARKEY/TOKENS/T local, zero import de App.jsx"
      pattern: "const VARKEY"
---

<objective>
Criar a fundação ISOLADA do split hub/workspace: as 4 chaves de copy novas
(nos dois modos) e o componente `WorkspaceHeader.jsx` (D-05: nome do ticker +
um único botão "Voltar"), mais a primeira metade do guardião novo da fase.

Este plano NÃO toca `OpcoesScreen.jsx` — por desenho. É a única fatia da fase
que pode ser escrita e verificada sem reestruturar a tela, e tirá-la do
caminho antes do split reduz o raio de explosão do plano 34-02 (que vai mexer
no arquivo que 21 guardiões leem).

Purpose: separar "criar peça nova" de "reorganizar a tela" — a lição da Fase
33 é que cada onda fecha com a suíte canônica verde antes de a próxima
começar (D-03 do `33-CONTEXT.md`).
Output: 1 componente novo, 4 chaves de copy × 2 modos, 1 arquivo de guardião
novo.
</objective>

<context_deviation>
**Resolvido, não é mais pendência.** O planner levantou um conflito factual
entre a leitura literal original de D-01 e o modelo de dados (detalhado no
plano 34-02). O orquestrador verificou por leitura direta
(`OpcoesScreen.jsx:320`, `useOpcoesMcp.js`) e o Alex confirmou a resolução
em 2026-09-20: `34-CONTEXT.md` (D-01/D-02) e `34-UI-SPEC.md` já foram
emendados (commit `55594fd`) para bater com o que este plano sempre usou:

- Chave `cp.opcoesAbaSetupsSalvos` = "Setups salvos" / "Setups" — não
  `cp.opcoesAbaCriarSetup` (nome descartado, nunca chega a existir no código).
- A 3ª aba do workspace contém `SecaoSetups` INTEIRA (listagem ticker-scoped
  "SETUPS GRAVADOS" **e** o bloco de criação), não só a criação.

Nenhuma ação pendente aqui — este plano já implementa a versão emendada.
</context_deviation>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-navega-o-hub-workspace/34-CONTEXT.md
@.planning/phases/34-navega-o-hub-workspace/34-UI-SPEC.md
@.planning/phases/34-navega-o-hub-workspace/34-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
<!-- Contrato que o 34-02 vai consumir. Definido AQUI, não descoberto lá. -->

Assinatura do componente novo (props-only, zero hook, zero store):

  WorkspaceHeader({ ticker, onVoltar, cp })

- `ticker`: string não vazia (o chamador só o renderiza quando `ticker` é truthy)
- `onVoltar`: callback sem argumento; em `OpcoesScreen.jsx` será
  `() => escolherTicker(ticker)` (plano 34-02) — o reset de tese/vencimento/
  alvo/stop já mora dentro de `escolherTicker`, não se escreve um segundo
  caminho de reset (D-03)
- `cp`: o dicionário de copy do modo ativo; o componente lê
  `cp.opcoesVoltarAoHub` com fallback literal `"Voltar"`

Chaves de copy criadas aqui e consumidas no 34-02/34-03:
  opcoesVoltarAoHub, opcoesAbaAnalisar, opcoesAbaComparar, opcoesAbaSetupsSalvos
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 4 chaves de copy novas, nos DOIS modos</name>
  <files>web/src/copy.js</files>
  <read_first>
    - web/src/copy.js — ler as duas vizinhanças de inserção: linhas 375-390
      (bloco `COPY.estudo`, onde vivem `opcoesSubabaSetups`/`opcoesSubabaOperar`)
      e linhas 1005-1020 (as MESMAS chaves em `COPY.operador`). Ler também
      as linhas 105-115 e 805-815 (`opcoesSetupsTitulo`) e 325-330 / 955-962
      (`opcoesCriarTitulo`) para ver o padrão de divergência de tom entre os
      dois modos.
    - .planning/phases/34-navega-o-hub-workspace/34-PATTERNS.md — seção
      "`web/src/copy.js` (config, dictionary)" e "Locale-parity for every new
      `cp.X` key"
    - .claude/skills/didatica-boris/SKILL.md — a skill se aplica a qualquer
      texto que o usuário lê; confirmar que rótulo estrutural de navegação não
      exige vocabulário didático novo e que nada aqui promete resultado
  </read_first>
  <action>
    Inserir 4 chaves em CADA um dos dois blocos de `COPY`, logo APÓS
    `opcoesSubabaOperar` (linha ~384 em `COPY.estudo`, linha ~1013 em
    `COPY.operador`) — mesma vizinhança das irmãs de navegação, não no fim do
    objeto.

    Valores, seguindo a divergência de tom que o arquivo já pratica (estudo
    mais explícito, operador mais curto — ver `opcoesSubabaOperar`:
    "Operação" × "Operar"; `opcoesCriarTitulo`: "CRIAR UM SETUP" × "CRIAR
    SETUP"):

    - `COPY.estudo`: opcoesVoltarAoHub: "Voltar", opcoesAbaAnalisar:
      "Analisar", opcoesAbaComparar: "Comparar", opcoesAbaSetupsSalvos:
      "Setups salvos"
    - `COPY.operador`: opcoesVoltarAoHub: "Voltar", opcoesAbaAnalisar:
      "Analisar", opcoesAbaComparar: "Comparar", opcoesAbaSetupsSalvos:
      "Setups"

    `opcoesVoltarAoHub` fica "Voltar" (não "Voltar ao hub") por decisão
    registrada no UI-SPEC: o botão vive DENTRO do header, colado ao nome do
    ticker, e todo botão de volta/cancelar deste diretório usa verbo único.

    Os três rótulos de aba são curtos de propósito: são 3 pills numa linha a
    375px de largura (contrato de mobile do UI-SPEC) — "Comparar vencimentos"
    estouraria a linha.

    NÃO renomear, reordenar nem deduplicar nenhuma chave existente.
    Em particular `opcoesSetupsTitulo`/`opcoesCriarTitulo` ficam intocadas
    (o UI-SPEC proíbe renomear explicitamente).
  </action>
  <acceptance_criteria>
    - `node -e "import('./web/src/copy.js').then(({COPY})=>{const ks=['opcoesVoltarAoHub','opcoesAbaAnalisar','opcoesAbaComparar','opcoesAbaSetupsSalvos'];const f=ks.filter(k=>!COPY.estudo[k]||!COPY.operador[k]);if(f.length){console.log('FALTAM',f);process.exit(1)}console.log('ok paridade')})"`
      imprime `ok paridade` e sai 0
    - `git diff --stat web/src/copy.js` mostra só linhas ADICIONADAS (8
      inserções, 0 remoções)
    - `grep -c "opcoesVoltarAoHub" web/src/copy.js` retorna exatamente 2
  </acceptance_criteria>
  <verify>
    <automated>node -e "import('./web/src/copy.js').then(({COPY})=>{const ks=['opcoesVoltarAoHub','opcoesAbaAnalisar','opcoesAbaComparar','opcoesAbaSetupsSalvos'];const f=ks.filter(k=>!COPY.estudo[k]||!COPY.operador[k]);if(f.length){console.log('FALTAM',f);process.exit(1)}console.log('ok paridade')})"</automated>
  </verify>
  <done>As 4 chaves existem nos dois modos, com os valores acima, sem remover nem renomear nenhuma chave existente.</done>
</task>

<task type="auto">
  <name>Task 2: WorkspaceHeader.jsx — header fixo do workspace (D-05)</name>
  <files>web/src/opcoes/WorkspaceHeader.jsx</files>
  <read_first>
    - web/src/opcoes/SecaoVigias.jsx — linhas 1-60: é o analog de forma
      inteira (doc-comment de cabeçalho explicando fase/plano, espelho
      `VARKEY`/`TOKENS`/`T` local, constante `BOTAO` local, assinatura
      props-only)
    - web/src/opcoes/OpcoesScreen.jsx — linhas 122-126 (constante `BOTAO`,
      o estilo exato do botão) e linhas 411-412 (a caixa do `cabecalho`, o
      estilo exato do container)
    - .planning/phases/34-navega-o-hub-workspace/34-PATTERNS.md — seção
      "`web/src/opcoes/WorkspaceHeader.jsx` (NEW component)"
    - .planning/phases/34-navega-o-hub-workspace/34-UI-SPEC.md — seções
      Typography, Color e Spacing Scale (os valores são contrato, não sugestão)
  </read_first>
  <action>
    Criar `web/src/opcoes/WorkspaceHeader.jsx` com `export default function
    WorkspaceHeader({ ticker, onVoltar, cp })`.

    Estrutura: uma caixa `<div>` com o estilo EXATO do `cabecalho`
    (`border: 1px solid T.borderSubtle`, `borderRadius: "14px"`, `padding:
    "12px 14px"`, `background: T.bgPanel`) contendo uma linha flex
    (`alignItems: "center"`, `justifyContent: "space-between"`, `gap: "10px"`)
    com: à esquerda um `<span>` do ticker (`fontSize: "15px"`, `fontWeight:
    800`, `color: T.textPrimary`); à direita um `<button type="button">` com
    o estilo `BOTAO` declarado localmente (`minHeight: "44px"`, `padding:
    "10px 14px"`, `borderRadius: "11px"`, `border: 1px solid T.borderSubtle`,
    `background: "transparent"`, `color: T.textSecondary`, `fontWeight: 700`,
    `fontSize: "13px"`), `onClick={onVoltar}`, rótulo
    `cp.opcoesVoltarAoHub || "Voltar"`.

    Nenhum token novo: declarar o trio local `VARKEY`/`TOKENS`/`T` com APENAS
    as chaves usadas (`borderSubtle`, `bgPanel`, `textPrimary`,
    `textSecondary`) — mesmo espelho declarado de todos os irmãos da pasta,
    ZERO import de `App.jsx` (ADR-027 Decisão 3, isolamento de duas vias,
    verificado por varredura de diretório em
    `test_opcoes_subabas_ui.mjs`).

    Proibições, cada uma com razão:
    - Sem `useState`/`useEffect`/qualquer hook e sem ler `ctx`/`store`: o
      orquestrador é o único dono de estado compartilhado (REORG-03/04 da
      Fase 33, invariante que esta fase não pode quebrar). Um guardião de
      diretório da 33-04 já reprova `useState` de `tese`/`ticker`/
      `vencimento`/`lote` em componente de seção.
    - Sem `.manchete` e sem qualquer render de veredito/manchete: o guardrail
      CVM varre este diretório inteiro e a allowlist
      `RENDERIZADORES_DE_MANCHETE` (`web/tests/test_opcoes_subabas_ui.mjs`,
      seção final) NÃO muda nesta fase.
    - Sem `T.accent`: o botão de voltar é navegação, não a ação primária da
      tela (Color do UI-SPEC reserva accent só para a pill ativa).
    - Sem `position: fixed`/`sticky`: "header fixo no topo" em D-05 significa
      "primeiro elemento do workspace, sempre presente", não elemento grudado
      na viewport — nenhum bloco deste diretório usa posicionamento fixo e
      introduzir um mudaria a rolagem da tela inteira.

    Abrir o arquivo com um doc-comment `/** ... */` no padrão dos irmãos,
    dizendo: fase/plano (34-01), que existe para D-05/NAV-03 (um único
    caminho de volta, sem breadcrumb nem histórico), e que o reset de estado
    NÃO mora aqui (mora em `escolherTicker`, no orquestrador).
  </action>
  <acceptance_criteria>
    - `grep -c "from \"../App.jsx\"\|from '../App.jsx'" web/src/opcoes/WorkspaceHeader.jsx` retorna 0
    - `grep -Ec "useState|useEffect|useMemo|useRef|\.manchete|position: *\"(fixed|sticky)\"|T\.accent" web/src/opcoes/WorkspaceHeader.jsx` retorna 0
    - `grep -c "minHeight: \"44px\"" web/src/opcoes/WorkspaceHeader.jsx` retorna 1
    - `grep -c "export default function WorkspaceHeader" web/src/opcoes/WorkspaceHeader.jsx` retorna 1
    - `grep -c "cp.opcoesVoltarAoHub" web/src/opcoes/WorkspaceHeader.jsx` retorna 1
  </acceptance_criteria>
  <verify>
    <automated>cd web && npx vite build</automated>
  </verify>
  <done>O arquivo existe, compila no build do Vite, é props-only, não importa App.jsx, não renderiza manchete e respeita os valores exatos de Typography/Spacing/Color do UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 3: guardião novo (parte 1) + suíte canônica verde</name>
  <files>web/tests/test_opcoes_hub_workspace_ui.mjs</files>
  <read_first>
    - web/tests/test_opcoes_subabas_ui.mjs — arquivo INTEIRO (279 linhas): é o
      analog de forma completa. Copiar o padrão de carregamento de fonte
      (`readFileSync` + `join(dirOpcoes, ...)`), o `semComentario`, o helper
      `ok(nome, cond)`, a checagem de paridade de `cp.X` (seção 9), a
      checagem de 44px/`aria-pressed` (seção 10) e o bloco de saída
      (`process.exit(1)`)
    - web/src/opcoes/WorkspaceHeader.jsx — o arquivo criado na Task 2
    - .planning/phases/34-navega-o-hub-workspace/34-PATTERNS.md — seção
      "`web/tests/test_opcoes_hub_workspace_ui.mjs` (NEW guardian test)",
      assertivas 4, 6 e 7
  </read_first>
  <action>
    Criar `web/tests/test_opcoes_hub_workspace_ui.mjs` com as asserções que
    já são verificáveis nesta onda (as do split em si entram no 34-02/34-03,
    por acréscimo ao MESMO arquivo):

    1. `WorkspaceHeader.jsx` existe e exporta `WorkspaceHeader` como default.
    2. Ele é props-only: zero `useState|useEffect|useMemo|useRef`, zero
       `ctx.`/`store.`, zero import de `App.jsx` (varredura na fonte sem
       comentário).
    3. Ele não renderiza manchete: zero match de `/\.manchete\b/`.
    4. O botão de voltar carrega `minHeight: "44px"` e usa
       `cp.opcoesVoltarAoHub`.
    5. Ele NÃO usa `T.accent` (o accent é reservado à pill ativa — Color do
       UI-SPEC).
    6. Paridade de locale: para cada uma das 4 chaves novas
       (`opcoesVoltarAoHub`, `opcoesAbaAnalisar`, `opcoesAbaComparar`,
       `opcoesAbaSetupsSalvos`), a chave existe em `COPY.estudo` E em
       `COPY.operador` e nenhuma das duas é string vazia.
    7. `SecaoSetups.jsx` continua com `cp.opcoesSetupsTitulo` E
       `cp.opcoesCriarTitulo` (guarda contra o "não renomear" do UI-SPEC —
       a asserção nasce aqui porque é o 34-03 que mexe no arredor dela).

    Usar `semComentario` antes de TODA asserção que faz grep de termo que
    também aparece em comentário explicativo (as asserções 2, 3 e 5 são
    exatamente esse caso — o doc-comment do componente menciona
    `useState`/manchete ao explicar por que não os usa). Um `grep -c` sobre
    fonte com comentário é auto-invalidante.

    Cabeçalho do arquivo: doc-comment no padrão do analog, dizendo o que cada
    seção trava e por que (cada asserção é um defeito que ESTA fase poderia
    reintroduzir em silêncio).

    Não é necessário registrar o arquivo em `scripts/executar.sh`: o runner
    itera `for t in web/tests/*.mjs` (linha 33) — o arquivo entra na suíte
    pelo nome.
  </action>
  <acceptance_criteria>
    - `node web/tests/test_opcoes_hub_workspace_ui.mjs` sai 0 e imprime
      "todos os testes passaram"
    - Prova negativa real (não só "o teste passa"): injetar
      temporariamente `const [x, setX] = useState("")` em
      `WorkspaceHeader.jsx`, rodar o teste, confirmar que ele REPROVA, e
      reverter a injeção com `git checkout -- web/src/opcoes/WorkspaceHeader.jsx`.
      Registrar o resultado no SUMMARY.
    - `bash scripts/executar.sh --testes` — baseline idêntica à da Fase 33:
      2923 pytest passed / 0 failed e 152/153 `.mjs` (a única falha aceita é
      `test_ios_assets.mjs`, ambiental e pré-existente, `web/ios/` é
      gitignored — `CLAUDE.md:236`; o total de `.mjs` sobe de 152 para 153
      por causa deste arquivo novo)
    - `cd web && npx vite build` verde
  </acceptance_criteria>
  <verify>
    <automated>node web/tests/test_opcoes_hub_workspace_ui.mjs && bash scripts/executar.sh --testes</automated>
  </verify>
  <done>Guardião novo verde, com prova negativa registrada; suíte canônica na baseline da Fase 33 (nenhuma falha nova); build do Vite verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhuma nova | Este plano cria um componente de apresentação puro e entradas de dicionário. Nenhuma rota, nenhum parser de entrada externa, nenhum novo consumo de dado do servidor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Information disclosure | `WorkspaceHeader.jsx` | mitigate | O componente só renderiza `ticker` (string já visível na tela) e uma string de copy. Guardião trava `ctx.`/`store.` dentro do arquivo — ele não tem acesso a estado de conta. |
| T-34-02 | Tampering (regulatório, guardrail CVM) | `WorkspaceHeader.jsx` | mitigate | Asserção 3 do guardião novo + varredura de diretório existente: zero `.manchete`; allowlist `RENDERIZADORES_DE_MANCHETE` não muda. |
| T-34-SC | Tampering (supply chain) | npm install | accept | Nenhum pacote novo é instalado nesta fase (zero dependência nova em `web/package.json`). Sem instalação, sem superfície de supply chain — não se aplica. |
</threat_model>

<verification>
1. `node web/tests/test_opcoes_hub_workspace_ui.mjs` verde, com prova
   negativa por injeção registrada no SUMMARY.
2. `bash scripts/executar.sh --testes` na baseline da Fase 33 (as DUAS
   suítes; `scripts/test.sh` sozinho não conta).
3. `cd web && npx vite build` verde — grep e teste estático não pegam erro de
   sintaxe JSX.
4. `git diff --stat` mostra exatamente 3 arquivos: `copy.js` (só adições),
   `WorkspaceHeader.jsx` (novo), `test_opcoes_hub_workspace_ui.mjs` (novo).
   `OpcoesScreen.jsx` NÃO aparece no diff deste plano.
</verification>

<success_criteria>
- `WorkspaceHeader.jsx` existe, props-only, com os valores exatos de
  Typography/Spacing/Color do UI-SPEC e alvo de toque de 44px.
- 4 chaves de copy novas nos DOIS modos.
- Guardião novo verde com prova negativa; suíte canônica sem falha nova.
- `OpcoesScreen.jsx` intocado (este plano não reestrutura nada).
</success_criteria>

<output>
Create `.planning/phases/34-navega-o-hub-workspace/34-01-SUMMARY.md` when done.
</output>
