---
phase: 34-navega-o-hub-workspace
plan: 04
type: execute
wave: 4
depends_on: ["34-03"]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - web/src/version.js
  - server/web_dist
autonomous: false
requirements: [NAV-01, NAV-02, NAV-03, NAV-04, NAV-05, NAV-06]

must_haves:
  truths:
    - "O Alex viu a navegação nova rodando e aprovou — hub abre sem ticker, escolher ticker entra no workspace, um botão volta [NAV-01..03]"
    - "A navegação nova está NO AR para o usuário do PWA (não só verde na suíte local)"
    - "NAV-01..06 estão marcados Done em REQUIREMENTS.md e a Fase 34 fechada em ROADMAP.md/STATE.md, editados à MÃO"
  artifacts:
    - path: "web/src/version.js"
      provides: "BUILD_ID novo carimbando a entrega das Fases 33+34"
      contains: "F10-"
    - path: ".planning/REQUIREMENTS.md"
      provides: "NAV-01..06 marcados Done"
      contains: "NAV-06"
  key_links:
    - from: "web/src/version.js (BUILD_ID)"
      to: "server/web_dist"
      via: "scripts/bump.sh seguido de scripts/publicar-web.sh"
      pattern: "F10-[0-9]{8}-[0-9]{2}"
---

<objective>
Fechar a fase: verificação humana da navegação nova, publicação do front (as
Fases 33 e 34 juntas — a 33 nunca foi publicada) e atualização à MÃO dos
documentos de planejamento.

Purpose: sem este plano a fase fica testada e nunca vai ao ar — o defeito de
processo registrado em `.planning` como "Fase sem plano de publicação front".
Output: build carimbado no ar + docs coerentes com a realidade.
</objective>

<repo_guardrail>
**Não chamar mutador de estado do `gsd-sdk` neste plano.** `state.advance-plan`,
`state.record-session`, `state.record-metric`, `state.add-decision` e
`roadmap.annotate-dependencies` corromperam o `STATE.md`/`ROADMAP.md` deste
repositório em 3 sessões anteriores (sobrescrevem `stopped_at`/`status` com
texto de sessão antiga, erram o contador de planos, colam métrica fora da
tabela). Editar `STATE.md`/`ROADMAP.md`/`REQUIREMENTS.md` com a ferramenta
Edit e conferir com `git diff .planning/` ANTES do commit. Os verbos de
LEITURA do SDK seguem confiáveis.
</repo_guardrail>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-navega-o-hub-workspace/34-CONTEXT.md
@.planning/phases/34-navega-o-hub-workspace/34-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: verificação humana da navegação hub × workspace</name>
  <what-built>
    A sub-aba "Setups" da aba Opções deixou de ser uma rolagem com 5 trabalhos
    empilhados. Agora ela tem dois modos, decididos pelo ticker escolhido (o
    MESMO estado de antes, sem rota e sem histórico):

    - **hub** (nenhum ativo escolhido): frase-ponte + Bloco A + Bloco B
      (descoberta cross-carteira) → SEUS VIGIAS (a listagem dos setups
      salvos, cross-ticker, custo zero) → os chips de ativo da carteira.
    - **workspace** (ativo escolhido): um header no topo com o nome do ativo e
      um botão "Voltar" → lastro da posição → leitura técnica interna
      (grátis) → convite da leitura paga → três pills: Analisar / Comparar /
      Setups salvos.

    Os avisos de "consultando…" e de erro do serviço continuam no MESMO lugar
    de antes, valendo para os dois modos. Nenhuma chamada nova foi
    introduzida: trocar de pill é troca de tela, não novo pedido.
  </what-built>
  <how-to-verify>
    Rodar local: `bash scripts/executar.sh` (ou o dev server de sempre) e
    abrir a aba Opções, sub-aba "Setups".

    1. **Hub** — sem escolher ativo nenhum: confirmar que aparecem, nesta
       ordem: frase-ponte, Bloco A, Bloco B, SEUS VIGIAS, chips da carteira.
       Confirmar que NÃO aparece nada de análise de ativo (nem lastro, nem
       leitura, nem pills).
    2. **Entrar no workspace** — tocar num chip de ativo: confirmar que os
       blocos de descoberta cross-carteira DESAPARECEM e que o header com o
       nome do ativo + "Voltar" aparece no topo.
    3. **Voltar** — tocar "Voltar": confirmar que volta ao hub, que os chips
       reaparecem, e que reentrar no MESMO ativo começa limpo (tese,
       vencimento, alvo e stop vazios — D-03: resetar é o comportamento
       correto, não um defeito).
    4. **Três pills** — dentro do workspace, alternar Analisar → Comparar →
       Setups salvos → Analisar. Confirmar que cada aba mostra o conteúdo
       dela, que a pill ativa é a única destacada, e — o item mais importante
       — que o contador de consultas do serviço NÃO sobe ao trocar de aba
       (conferir no cabeçalho/observabilidade: 3 chamadas da leitura, uma
       vez, não 3 por aba).
    5. **Erro visível nos dois modos** — com o serviço fora do ar (ou com a
       cota estourada), confirmar que o aviso aparece tanto no hub quanto no
       workspace, e que no hub os vigias e as oportunidades continuam na tela
       (é conteúdo de custo zero; ele não pode sumir junto com o erro).
    6. **375px** — reduzir a janela a 375px de largura: as 3 pills cabem numa
       linha e o header não quebra.

    Se algo divergir, descrever o que viu — em particular qualquer chamada
    nova no contador (item 4) é bloqueante: seria NAV-05 quebrado.
  </how-to-verify>
  <read_first>
    - .planning/phases/34-navega-o-hub-workspace/34-03-SUMMARY.md
    - .planning/phases/34-navega-o-hub-workspace/34-CONTEXT.md — D-01 a D-05
  </read_first>
  <action>
    Subir o ambiente local, abrir a sub-aba Setups, apresentar ao Alex o
    roteiro de 6 itens acima e PARAR. Não publicar, não commitar código novo e
    não seguir para a Task 2 antes da resposta dele — o item 4 (o contador de
    consultas não subir ao trocar de pill) é a única medição de NAV-05 que a
    suíte estática não consegue fazer, e é bloqueante.
  </action>
  <acceptance_criteria>
    - O Alex responde "aprovado" ou descreve divergências. Nenhuma publicação
      acontece antes disso (checkpoint bloqueante segura o push da FASE
      INTEIRA, não só desta task).
  </acceptance_criteria>
  <verify>
    <automated>MISSING — verificação visual/interativa por desenho; o substituto automatizado é `node web/tests/test_opcoes_hub_workspace_ui.mjs` (34-03), que cobre a estrutura mas não o contador de consultas ao vivo</automated>
  </verify>
  <resume-signal>Digite "aprovado" ou descreva o que divergiu</resume-signal>
  <done>Aprovação humana registrada, ou lista de divergências a corrigir antes de publicar.</done>
</task>

<task type="auto">
  <name>Task 2: publicar o front (Fases 33 + 34 juntas)</name>
  <files>web/src/version.js, server/web_dist</files>
  <read_first>
    - scripts/bump.sh — linhas 1-25 (por que o carimbo é comando próprio e o
      que acontece se for esquecido)
    - scripts/publicar-web.sh — o fluxo inteiro, para saber o que ele faz e o
      que ele NÃO faz
    - CLAUDE.md — seção "Publicação" e "Três esquemas de versão" (não confundir
      BUILD_ID com tag de milestone nem com versão pública)
  </read_first>
  <action>
    Publicar a entrega combinada das Fases 33 e 34 (a 33 fechou verificada e
    nunca foi ao ar — publicar as duas num único carimbo é a decisão
    registrada no ROADMAP da Fase 33).

    Ordem obrigatória, sem atalho:
    1. Mesclar `origin/main` ANTES de carimbar — `bump.sh` deriva o próximo
       BUILD_ID do valor LOCAL, e sessão paralela que já carimbou hoje faz os
       dois colidirem. `server/web_dist` se REGERA, não se mescla: em conflito
       nele, resolver regerando.
    2. `bash scripts/bump.sh` (sem argumento: pega o próximo do dia).
    3. `bash scripts/publicar-web.sh`.
    4. Conferir no ar: o `BUILD_ID` servido é o novo e `/api/health` responde
       com o `SERVER_BUILD_ID` derivado dele.

    Nunca editar `server/web_dist` à mão (guardrail do `CLAUDE.md`).

    **Limitação a declarar no SUMMARY, não a resolver aqui:** esta publicação
    atinge o PWA/web. O app do iPhone carrega bundle LOCAL (sem `server.url`),
    então a navegação nova só chega ao aparelho num build novo de TestFlight
    (`scripts/ios-testflight.sh`/`ios-bump-build.sh`). Isso está FORA do escopo
    desta fase — registrar como pendência para o Alex decidir, não executar por
    conta própria.

    Se algum comando de rede falhar por sandbox (TLS/proxy), é problema
    ambiental conhecido: reportar o erro exato e o passo em que parou, sem
    inventar contorno.
  </action>
  <acceptance_criteria>
    - `grep "BUILD_ID" web/src/version.js` mostra um carimbo no formato
      `F10-AAAAMMDD-NN` com a data real de hoje
    - `git diff --stat server/web_dist | tail -1` mostra o dist regenerado
    - Resposta de `/api/health` em produção contém o `SERVER_BUILD_ID`
      derivado do novo BUILD_ID (colar a saída no SUMMARY)
    - `node web/tests/test_ios_assets.mjs` — se ainda falhar, confirmar que a
      falha é a ambiental conhecida (`web/ios/` gitignored) e NÃO "mesmo
      carimbo com chunks diferentes", que é a assinatura de bump esquecido
  </acceptance_criteria>
  <verify>
    <automated>grep -E "F10-[0-9]{8}-[0-9]{2}" web/src/version.js</automated>
  </verify>
  <done>BUILD_ID novo carimbado, dist publicado, carimbo conferido no ar, pendência do iOS registrada.</done>
</task>

<task type="auto">
  <name>Task 3: fechar os documentos de planejamento à mão</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — linhas 20-30 (NAV-01..06) e 60-70 (a tabela
      de rastreio por fase)
    - .planning/ROADMAP.md — linha 142 (o checkbox da Fase 34), linhas 187-217
      (o detalhe da fase, incluindo `**Plans**: TBD`) e a tabela de Progresso
      a partir da linha 219
    - .planning/STATE.md — frontmatter e as seções "Current Position" /
      "Project Reference"
  </read_first>
  <action>
    Editar os três arquivos com a ferramenta **Edit** (nunca mutador do
    `gsd-sdk`, ver `repo_guardrail` acima):

    1. `REQUIREMENTS.md`: marcar `- [x]` em NAV-01..NAV-06 (linhas 24-29) e
       trocar `Pending` → `Done` nas 6 linhas da tabela de rastreio (linhas
       64-69).
    2. `ROADMAP.md`: marcar `- [x]` no item da Fase 34 (linha 142); substituir
       `**Plans**: TBD` (linha 216) pela contagem real (4 planos, 4 ondas
       sequenciais) e pela lista dos 4 planos com um resumo de uma linha cada
       e a data de conclusão, no MESMO formato que a Fase 33 usa (linhas
       177-184); acrescentar a Fase 34 à tabela de Progresso com a data real.
    3. `STATE.md`: atualizar `status`, `stopped_at`, `last_updated`,
       `last_activity` e o bloco `progress` no frontmatter; reescrever
       "Current Position" para a Fase 34 fechada (o que foi entregue, o
       BUILD_ID publicado, as pendências abertas) e mover o texto da posição
       anterior para "Posição anterior". Registrar explicitamente, como
       decisão da fase: o conflito D-01 × modelo de dados e como foi
       resolvido (a listagem cross-ticker de setups salvos é o índice de
       vigias, custo zero; `l.setups` é ticker-scoped e vive no workspace),
       porque é exatamente o tipo de achado que o próximo leitor vai querer
       não redescobrir.

    Registrar também as pendências que esta fase deixa abertas, sem fingir que
    foram feitas: build iOS/TestFlight não feito; backlog B2 (estado não
    sobrevive à troca de seção) segue aberto por decisão explícita (D-03);
    2 todos já executados na Fase 33 ainda esquecidos em
    `.planning/todos/pending/`; avisos de lint acumulados na pasta
    `web/src/opcoes/`.

    Commit dos docs separado do commit de código (padrão do repositório:
    `docs(34): ...`), depois de conferir `git diff .planning/`.
  </action>
  <acceptance_criteria>
    - `grep -c "NAV-0.*Done" .planning/REQUIREMENTS.md` retorna 6
    - `grep -c "\[ \] \*\*NAV" .planning/REQUIREMENTS.md` retorna 0
    - A linha `**Plans**: TBD` não existe mais na seção da Fase 34 (conferir
      por `grep -n "Phase 34" -A 35 .planning/ROADMAP.md`)
    - `git diff .planning/STATE.md` mostra APENAS as seções pretendidas — sem
      texto de sessão antiga sobrescrito, sem linha de métrica fora da tabela,
      sem contador de planos errado (a assinatura da corrupção que os
      mutadores do SDK causam neste repo)
    - `bash scripts/executar.sh --testes` ainda na baseline (docs não podem
      quebrar teste, mas a suíte fecha a fase)
  </acceptance_criteria>
  <verify>
    <automated>git diff --stat .planning/ && grep -c "NAV-0.*Done" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>Os três documentos refletem a realidade da fase, editados à mão, conferidos por diff, com as pendências abertas declaradas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → produção (Railway) | `publicar-web.sh` empurra o dist para o serviço que serve o app. É a única fronteira desta fase, e é pré-existente. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-08 | Tampering (entrega) | `web/src/version.js` → `server/web_dist` | mitigate | `bump.sh` ANTES de `publicar-web.sh`; conferência do carimbo servido e do `SERVER_BUILD_ID` em `/api/health`. Bump esquecido produz "mesmo carimbo com chunks diferentes" — a assinatura de bundle amputado de 2026-08-06. |
| T-34-09 | Repudiation (histórico) | `.planning/STATE.md`, `.planning/ROADMAP.md` | mitigate | Edição à mão + `git diff` antes do commit; mutadores do `gsd-sdk` proibidos por guardrail do repositório (corromperam estes arquivos 3 vezes). Histórico (`qa/`, `ESTADO-*`, RELEASES) não se reescreve. |
| T-34-10 | Elevation of privilege | nenhum | accept | Nenhuma mudança de RBAC, permissão ou rota nesta fase. `podeCriarSetup` continua conveniência de UI sobre um 403 que o backend já dá sozinho (ADR-013). |
| T-34-SC | Tampering (supply chain) | npm install | accept | Zero pacote novo na fase inteira. |
</threat_model>

<verification>
1. Aprovação humana registrada antes de qualquer publicação.
2. BUILD_ID novo conferido no ar (`/api/health`).
3. `git diff .planning/` revisado linha a linha antes do commit de docs.
4. `bash scripts/executar.sh --testes` na baseline.
</verification>

<success_criteria>
- Alex viu e aprovou a navegação nova.
- Navegação no ar no PWA, com carimbo novo conferido.
- NAV-01..06 Done; Fase 34 fechada em ROADMAP/STATE, editados à mão.
- Pendências abertas declaradas (iOS/TestFlight, backlog B2, todos
  esquecidos, lint).
</success_criteria>

<output>
Create `.planning/phases/34-navega-o-hub-workspace/34-04-SUMMARY.md` when done.
</output>
