---
phase: 13-uso-real-vis-vel-na-interface-enforcement-no-ios
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - web/src/App.jsx
  - web/tests/test_fase13_contadores_ui.mjs
autonomous: true
requirements: [CAP-06]

must_haves:
  truths:
    - "Usuário free vê 'ativos: X/10' no subtítulo da Watchlist sem precisar abrir nenhum modal"
    - "Usuário free vê o par uso/limite dentro do CatalogModal, no momento em que a ação pode ser bloqueada"
    - "Usuário free vê 'análises deste mês: X/30' na tela Atividade da IA"
    - "Quando o backend não confirma o número, a tela mostra '—' e nunca um número estimado"
    - "Usuário pro não vê contador nenhum — nem X/∞, nem 'ilimitado'"
  artifacts:
    - path: "web/src/App.jsx"
      provides: "helper QuotaSeg + 3 pontos de exibição + estado wlQuota no App()"
      contains: "QuotaSeg"
    - path: "web/tests/test_fase13_contadores_ui.mjs"
      provides: "guardião estático dos 5 estados e do tratamento tipográfico"
      min_lines: 50
  key_links:
    - from: "web/src/App.jsx::App()"
      to: "web/src/persistence.js::store.watchlistQuota"
      via: "carregamento do estado wlQuota"
      pattern: "store\\.watchlistQuota\\(\\)"
    - from: "web/src/App.jsx::MercadoScreen"
      to: "web/src/App.jsx::QuotaSeg"
      via: "segmento do subtítulo da Watchlist"
      pattern: "<QuotaSeg"
    - from: "web/src/App.jsx::AtividadeIAScreen"
      to: "web/src/persistence.js::store.aiQuota"
      via: "monthUsed/monthLimit da tela de IA"
      pattern: "store\\.aiQuota\\(\\)"
---

<objective>
Tornar o uso real visível (CAP-06): três pontos de exibição do par uso/limite,
exatamente onde o mockup aprovado pelo Alex definiu — subtítulo da Watchlist
(visão passiva), CatalogModal (ponto de ação) e Atividade da IA (análises do
mês) — com os 5 estados do contrato de UI: normal, quase-no-limite, no-limite,
plano sem teto (omite) e indisponível ("—", nunca número estimado).

Purpose: hoje o usuário só descobre o limite ao esbarrar nele. D-01/D-02
escolheram a opção dual (visibilidade proativa + número exato no momento da
ação) e o `13-UI-SPEC.md` travou tipografia, cor e copy — este plano só executa.
Output: helper `QuotaSeg` reutilizado nos 3 locais, estado `wlQuota` no `App()`,
guardião estático dos 5 estados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-uso-real-vis-vel-na-interface-enforcement-no-ios/13-CONTEXT.md
@.planning/phases/13-uso-real-vis-vel-na-interface-enforcement-no-ios/13-UI-SPEC.md
@.planning/phases/13-uso-real-vis-vel-na-interface-enforcement-no-ios/13-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contratos e âncoras de código. Não explorar o arquivo de 7900 linhas atrás disso. -->

Entregue pelos planos 13-01/13-02:
  store.watchlistQuota() -> Promise<{ count: number, limit: number|null, planId: "free"|"pro" }>
  store.aiQuota()        -> Promise<{ ..., monthUsed: number|null, monthLimit: number|null }>   (JÁ EXISTIA)

Tokens de estilo já existentes em App.jsx:
  MONO  (linha 236) = "ui-monospace,'SF Mono',Menlo,Consolas,monospace"
  SANS  (linha 241)
  T.warn / T.textMuted / T.textFaint  (tema ativo, linhas 69-167)

Âncoras EXATAS dos 3 pontos de exibição (estado atual):

  1. web/src/App.jsx:3427-3429, dentro de `function MercadoScreen({ ctx })` (linha 3376):
     <p style={{ margin: "0 0 12px", color: T.textMuted, fontSize: "13px", maxWidth: "560px", lineHeight: 1.55 }}>
       {cp.subtituloWatchlist}{quotesAt ? "  ·  cotações " + quotesAt : ""}
     </p>

  2. web/src/App.jsx:6618, dentro de `function CatalogModal({ ctx })` (linha 6603):
     <div style={{ fontSize: "12.5px", color: T.textMuted, marginTop: "3px" }}>{catalogSel.length} de {data.catalog.length} selecionados</div>
     // desestruturação atual: const { data, catalogSel, setCatalogSel, addState, setAddState, A } = ctx;

  3. web/src/App.jsx:4948-4995, dentro de `function AtividadeIAScreen({ ctx })`:
     const [ati, setAti] = useState(null); const [err, setErr] = useState("");
     const carregar = useCallback(async () => {
       try { setAti(await store.aiActivity()); setErr(""); } catch (e) { setErr(...); }
     }, []);
     useEffect(() => { carregar(); }, [carregar]);
     // o card "CUSTO ESTIMADO" termina em </div> após o bloco {ati && (<>...</>)}

Âncoras no componente App() (linha 6817+):
  const [data, setData] = useState(null);            // linha 6818
  ...
  useEffect(() => { if (data) refreshQuotes(); },    // linha ~7219
    [data && Array.isArray(data.watchlist) ? data.watchlist.join(",") : ""]);
  const ctx = { ... data, quotes, analysis, ..., catalogSel, setCatalogSel, ... };   // linha 7916

Helpers presentacionais de módulo (onde o helper novo se encaixa):
  function Spinner(...)   // linha 338
  function IconBtn(...)   // linha 343
  const InfoDot = ...     // linha 355

Analogs de estado visual já no arquivo:
  App.jsx:5712-5723  -> fragmento "X/Y" com <span style={cond ? { color: T.warn } : undefined}>
  App.jsx:5566-5570  -> estado indisponível com `?? "—"` (semântica "não sei agora")
                        CONTRASTADO com `?? "ilimitado"` (semântica "sei que não há teto") —
                        a Fase 13 NUNCA usa "ilimitado": plano sem teto OMITE o segmento (D-03)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helper QuotaSeg (5 estados) + estado wlQuota no App()</name>
  <read_first>
    - web/src/App.jsx (linhas 230-250 para `MONO`/`SANS`; 335-360 para o vizinho `IconBtn`/`InfoDot` onde o helper entra; 5560-5575 e 5705-5725 para os dois analogs de estado visual; 6815-6840 para as declarações de estado do `App()`; 7215-7225 para o efeito que reage a mudança de watchlist; 7910-7940 para a montagem do `ctx`)
    - .planning/phases/13-uso-real-vis-vel-na-interface-enforcement-no-ios/13-UI-SPEC.md (tabelas Typography, Color e Estados — tratamento não renegociável)
    - .planning/phases/13-uso-real-vis-vel-na-interface-enforcement-no-ios/13-PATTERNS.md (seção "Padrão X/Y com 5 estados" — esqueleto do helper)
  </read_first>
  <files>web/src/App.jsx</files>
  <action>
    (a) Criar no escopo de MÓDULO, logo após `const InfoDot` (linha ~358), o
    componente `function QuotaSeg({ quota, count, prefix, suffix })`, único
    renderizador do par uso/limite nos 3 locais. Regras (contrato do 13-UI-SPEC):
    - `quota === undefined` (ainda carregando) → `return null` (segmento inteiro
      omitido, sem flash de "—").
    - `quota` objeto com `quota.limit == null` (plano sem teto / pro) → `return null`.
      NUNCA renderizar "ilimitado" nem "X/∞" (D-03, critério de sucesso 5).
    - `quota === null` (falha ao confirmar) OU `count == null` → renderizar
      `{prefix}—{suffix}` com o travessão em texto normal (sem MONO/700), mesmo
      idioma do `?? "—"` já usado em `App.jsx:5566`. Nunca um número estimado nem
      um valor antigo em cache (princípio 4 do CLAUDE.md, critério de sucesso 3).
    - Caso normal → `{prefix}<span style={{ fontFamily: MONO, fontWeight: 700, color: cor }}>{count}/{quota.limit}</span>{suffix}`.
      `cor` = `T.warn` quando `count / quota.limit >= 0.9` (cobre também
      `count === limit` e `count > limit`), senão `"inherit"`. O limiar 0.9 é o
      piso travado pelo UI-SPEC — não afrouxar.
    - O `<span>` envolve SÓ os dígitos e a barra. `prefix`/`suffix` (ex.:
      `" · ativos: "`, `" do plano free"`) ficam FORA do span, em `SANS` herdado.
      Não aplicar `fontSize` próprio: o fragmento herda o tamanho do pai.
    Comentário de cabeçalho do helper deve registrar: por que MONO só nos dígitos
    (dígitos tabulares evitam reflow quando 7/10 vira 8/10; MONO na frase inteira
    importaria o registro visual do painel de admin para uma tela de leigo) e por
    que `T.warn` e não `T.negative`/`T.positive` (verde/vermelho são reservados a
    P&L neste app — usá-los aqui faria uma contagem parecer resultado financeiro).

    (b) No `App()` (linha ~6818), declarar `const [wlQuota, setWlQuota] = useState(undefined);`
    com os 3 valores semânticos documentados em comentário: `undefined` = ainda
    não carregou, `null` = não foi possível confirmar, objeto = dado real.

    (c) Criar `const refreshWlQuota = useCallback(async () => { try { setWlQuota(await store.watchlistQuota()); } catch { setWlQuota(null); } }, []);`
    — o `catch` seta `null` (estado indisponível explícito), nunca mantém o valor
    anterior nem chuta um número.

    (d) Chamar `refreshWlQuota()` DENTRO do efeito já existente que reage à
    watchlist (linha ~7219, `useEffect(() => { if (data) refreshQuotes(); }, [...watchlist.join(",")])`),
    ao lado de `refreshQuotes()`. Isso cobre boot, adição de ticker e salvamento
    do catálogo sem criar um segundo efeito. Não introduzir polling nem cache.

    (e) Expor `wlQuota` no objeto `ctx` (linha ~7936, na mesma linha de
    `data, quotes, analysis, ...`) para que `MercadoScreen` e `CatalogModal` leiam
    de uma fonte única — não recalcular em nenhum dos dois.
  </action>
  <verify>
    <automated>cd web && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function QuotaSeg" web/src/App.jsx` retorna 1, e a definição está no escopo de módulo (coluna 0), não dentro de outro componente
    - `grep -n "useState(undefined)" web/src/App.jsx` inclui a linha de `wlQuota`, e `grep -c "setWlQuota(null)" web/src/App.jsx` retorna 1 (caminho de falha explícito)
    - `grep -c "ilimitado" web/src/App.jsx` não aumenta em relação ao estado anterior (nenhuma ocorrência nova) — plano sem teto omite, não escreve "ilimitado"
    - O corpo de `QuotaSeg` contém `fontFamily: MONO` e `fontWeight: 700` exatamente uma vez, e contém `T.warn`; NÃO contém `T.negative` nem `T.positive`
    - `grep -c "refreshWlQuota" web/src/App.jsx` retorna 3 ou mais (declaração + uso no efeito + eventual dependência), e existe apenas UMA chamada a `store.watchlistQuota()` no arquivo
    - `cd web && npx vite build` conclui sem erro
  </acceptance_criteria>
  <done>Helper único dos 5 estados existe, `wlQuota` carrega junto com as cotações e está disponível no `ctx`.</done>
</task>

<task type="auto">
  <name>Task 2: Aplicar QuotaSeg nos 3 pontos de exibição</name>
  <read_first>
    - web/src/App.jsx (linhas 3376-3432 — `MercadoScreen` e o `<p>` do subtítulo; 6603-6622 — `CatalogModal` e a linha de selecionados; 4948-4998 — `AtividadeIAScreen`, `carregar` e o card CUSTO ESTIMADO)
    - .planning/phases/13-uso-real-vis-vel-na-interface-enforcement-no-ios/13-UI-SPEC.md (tabela "Copywriting Contract" — texto exato de cada local em cada estado; e "Fonte dos números — contrato de dados")
    - .planning/phases/13-uso-real-vis-vel-na-interface-enforcement-no-ios/13-CONTEXT.md (mockup aprovado, seção `<specifics>`)
  </read_first>
  <files>web/src/App.jsx</files>
  <action>
    Ponto 1 — subtítulo da Watchlist (`MercadoScreen`, `<p>` da linha ~3427):
    acrescentar, DEPOIS do trecho de `cotações {quotesAt}`, o segmento
    `<QuotaSeg quota={ctx.wlQuota} count={(data.watchlist || []).length} prefix="  ·  ativos: " />`.
    Numerador é `data.watchlist.length` (estado SALVO), denominador é
    `wlQuota.limit` — contrato de dados do UI-SPEC. Adicionar `wlQuota` à
    desestruturação de `ctx` no topo de `MercadoScreen` se o componente já
    desestrutura; caso contrário usar `ctx.wlQuota`. Não alterar `cp.subtituloWatchlist`
    nem `copy.js`/`skill_ref.py` — o segmento concatena neutro nos dois modos.

    Ponto 2 — `CatalogModal`, linha ~6618: acrescentar após "selecionados"
    `<QuotaSeg quota={ctx.wlQuota} count={catalogSel.length} prefix=" · " suffix={" do plano " + ((ctx.wlQuota && ctx.wlQuota.planId) || "free")} />`.
    Numerador é `catalogSel.length` (seleção LOCAL em edição, ainda não salva) —
    NÃO trocar por `data.watchlist.length`: são semanticamente diferentes por
    desenho, e ler o salvo faria a contagem divergir da seleção em progresso.
    Incluir `wlQuota` na desestruturação `const { data, catalogSel, ... } = ctx;`.

    Ponto 3 — `AtividadeIAScreen`, card CUSTO ESTIMADO:
    - Adicionar `const [aiq, setAiq] = useState(undefined);` com a mesma semântica
      de 3 valores de `wlQuota`.
    - Dentro de `carregar`, buscar a cota em um try/catch SEPARADO do de
      `aiActivity` (`try { setAiq(await store.aiQuota()); } catch { setAiq(null); }`),
      para que a falha de uma não apague a outra — o contador de análises tem que
      aparecer mesmo se o custo estimado falhar, e vice-versa.
    - Renderizar a nova linha como IRMÃ do bloco `{ati && (<>...</>)}`, ainda
      dentro do card, para não ficar refém de `ati`:
      `<div style={{ fontSize: "13px", color: T.textFaint, marginTop: "9px" }}>análises deste mês: <QuotaSeg .../></div>`
      com `quota={aiq === undefined ? undefined : (aiq === null ? null : { limit: aiq.monthLimit })}`
      e `count={aiq && typeof aiq.monthUsed === "number" ? aiq.monthUsed : null}`.
      Quando `monthLimit` é `null` (conta pro, ou escopo anônimo em que
      `/api/ai/quota` devolve ambos `null`), `QuotaSeg` devolve `null` e a linha
      inteira some — envolver o `<div>` numa condição que também some com o rótulo
      "análises deste mês:", para não deixar um rótulo órfão sem número.

    Em nenhum dos 3 pontos: criar componente visual novo, mudar padding/gap,
    introduzir ícone, ou alterar o tamanho de fonte do texto ao redor. A fase
    estende strings dentro de containers já posicionados.
  </action>
  <verify>
    <automated>cd web && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<QuotaSeg" web/src/App.jsx` retorna exatamente 3
    - O `<QuotaSeg` do `CatalogModal` recebe `count={catalogSel.length}` (não `data.watchlist.length`) — verificável por `grep -n "QuotaSeg" web/src/App.jsx` + inspeção da linha
    - O `<QuotaSeg` do subtítulo recebe `count={(data.watchlist || []).length}`
    - `AtividadeIAScreen` tem DOIS try/catch independentes dentro de `carregar` — a falha de `aiActivity` não zera `aiq`
    - Nenhuma das 3 chamadas passa um literal numérico como `quota` ou `count`: nenhum `10`/`30` novo aparece em `git diff web/src/App.jsx`
    - `cd web && npx vite build` conclui sem erro
    - `bash scripts/executar.sh --testes` continua verde
  </acceptance_criteria>
  <done>Os 3 contadores renderizam nos locais do mockup aprovado, com as fontes de número corretas por local.</done>
</task>

<task type="auto">
  <name>Task 3: Guardião estático dos 5 estados e do tratamento tipográfico</name>
  <read_first>
    - web/src/App.jsx (o helper `QuotaSeg` e os 3 call sites recém-criados)
    - web/tests/test_fase5_gate_mensal_front.mjs (padrão de asserção por presença/ausência de string)
    - web/tests/test_device_budget_sync.mjs (helper `bodyOf()` de chaves balanceadas)
    - web/tests/test_brand_book_v2_tokens.mjs (padrão de guardião de token de cor/tipografia, se existir asserção reaproveitável)
  </read_first>
  <files>web/tests/test_fase13_contadores_ui.mjs</files>
  <action>
    Criar o guardião no estilo da casa (inspeção estática de fonte com
    `readFileSync` + `bodyOf()`, sem Vitest/Jest, sem importar `App.jsx`).
    Asserções mínimas, cada uma travando uma decisão do 13-UI-SPEC:
    - existe exatamente 1 definição de `function QuotaSeg` e exatamente 3 usos `<QuotaSeg`;
    - o corpo de `QuotaSeg` contém `fontFamily: MONO` e `fontWeight: 700`;
    - o corpo de `QuotaSeg` contém `T.warn` e NÃO contém `T.negative` nem
      `T.positive` (guardrail: verde/vermelho são reservados a P&L);
    - o corpo de `QuotaSeg` contém o travessão `—` (estado indisponível) e NÃO
      contém a palavra `ilimitado` (D-03: plano sem teto omite o segmento);
    - o corpo contém o limiar `0.9` (quase-no-limite não pode ser afrouxado sem
      quebrar o teste);
    - a chamada dentro de `CatalogModal` usa `catalogSel.length` e a do subtítulo
      usa `data.watchlist` — provar que as duas fontes de numerador NÃO foram
      unificadas (são diferentes por desenho);
    - `App.jsx` não ganhou literal de limite: nenhuma linha não-comentada do diff
      desta fase introduz `/10` ou `/30` como texto fixo.
    Cabeçalho do arquivo deve explicar o que o guardião previne: alguém
    "simplificar" o helper mostrando "ilimitado" para pro, ou trocar `T.warn` por
    vermelho, reabre uma decisão de produto travada (D-03 e o guardrail de cor).
  </action>
  <verify>
    <automated>node web/tests/test_fase13_contadores_ui.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node web/tests/test_fase13_contadores_ui.mjs` sai com código 0 e imprime 0 linhas iniciadas por `FALHOU`
    - O teste falha (código != 0) se `T.warn` for trocado por `T.negative` no helper — validar invertendo temporariamente e revertendo, e registrar o resultado no SUMMARY
    - `bash scripts/executar.sh --testes` roda as DUAS suítes e sai verde, com o arquivo novo incluído no laço `for t in web/tests/*.mjs`
  </acceptance_criteria>
  <done>Guardião trava os 5 estados, a cor e a tipografia; a suíte canônica está verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `store.watchlistQuota()`/`store.aiQuota()` → render React | Resposta de rede (possivelmente ausente, parcial ou malformada) vira texto exibido ao usuário |
| exibição de número de cota → percepção do usuário | Um número errado aqui induz decisão errada; o CLAUDE.md proíbe valor inventado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | Spoofing (de dado) | `QuotaSeg` | mitigate | Falha, `undefined` ou `count`/`limit` não numérico nunca vira número: `undefined` omite, falha renderiza `—`. Nenhum fallback numérico, nenhum cache reexibido como atual (princípio 4 do CLAUDE.md). Travado pelo guardião da Task 3 |
| T-13-10 | Information Disclosure | contador em conta pro | mitigate | `limit == null` omite o segmento inteiro; o guardião assere ausência da string "ilimitado", que revelaria semântica de plano onde D-03 pediu silêncio |
| T-13-11 | Tampering | numerador do CatalogModal | accept | `catalogSel.length` é estado local em edição — divergir do salvo é o comportamento DESEJADO (ponto de decisão × status ambiente). O gate autoritativo continua no `PUT /api/watchlist` (Fase 12) e no `deviceStore` (plano 13-02); a UI aqui é informativa |
| T-13-12 | Denial of Service | efeito de carregamento | accept | `refreshWlQuota` roda no mesmo efeito de `refreshQuotes`, disparado por mudança de watchlist — sem polling, sem intervalo, sem chamada por render. Uma requisição leve extra por mudança de lista |
| T-13-SC | Tampering (supply chain) | npm installs | accept | Nenhuma dependência nova; nenhum componente de registry externo (o projeto não usa shadcn/Radix — ver Registry Safety do 13-UI-SPEC) |
</threat_model>

<verification>
- `cd web && npx vite build` sem erro (grep e teste estático não pegam erro de sintaxe JSX)
- `bash scripts/executar.sh --testes` verde (as DUAS suítes)
- Inspeção do diff: 3 usos de `QuotaSeg`, nenhum componente visual novo, nenhum padding/gap novo
</verification>

<success_criteria>
- Critérios 1, 2, 3 e 5 do ROADMAP satisfeitos na UI (contador de ativos, contador de análises, estado indisponível, plano pro sem número fabricado)
- Tratamento tipográfico e de cor exatamente como o 13-UI-SPEC travou
- Guardião estático protege as decisões contra "simplificação" futura
</success_criteria>

<output>
Create `.planning/phases/13-uso-real-vis-vel-na-interface-enforcement-no-ios/13-03-SUMMARY.md` when done
</output>
