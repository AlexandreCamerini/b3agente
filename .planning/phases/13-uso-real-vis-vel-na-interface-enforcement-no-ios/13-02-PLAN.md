---
phase: 13-uso-real-vis-vel-na-interface-enforcement-no-ios
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - web/src/api.js
  - web/src/plan.js
  - web/src/persistence.js
  - web/tests/test_fase13_watchlist_quota_ios.mjs
autonomous: true
requirements: [CAP-12, CAP-07, CAP-06]

must_haves:
  truths:
    - "Usuário free no app iOS com 10 ativos não consegue gravar o 11º — deviceStore bloqueia ANTES do write()"
    - "Se o limite real não puder ser confirmado (offline/endpoint fora), a adição é bloqueada com mensagem específica, não gravada em silêncio"
    - "deviceStore e serverStore expõem watchlistQuota() com o mesmo nome e contrato (paridade)"
    - "Nenhum número de limite (10/30) aparece hardcodado no front — o valor vem sempre do endpoint"
    - "web/src/plan.js não contém mais a frase de CTA 'Faça upgrade para adicionar mais.'"
  artifacts:
    - path: "web/src/api.js"
      provides: "wrapper HTTP watchlistQuota"
      contains: "/api/watchlist/quota"
    - path: "web/src/persistence.js"
      provides: "watchlistQuota() nos dois stores + gate fail-closed no deviceStore"
      contains: "watchlistQuota"
    - path: "web/src/plan.js"
      provides: "canAddTicker sem CTA + canGrowWatchlistTo espelhando plan.py"
      contains: "canGrowWatchlistTo"
    - path: "web/tests/test_fase13_watchlist_quota_ios.mjs"
      provides: "guardião estático do gate fail-closed e da ausência de CTA"
      min_lines: 50
  key_links:
    - from: "web/src/persistence.js::deviceStore.addWatchlistTicker"
      to: "web/src/api.js::watchlistQuota"
      via: "chamada de rede antes do write()"
      pattern: "api\\.watchlistQuota\\(\\)"
    - from: "web/src/persistence.js::deviceStore.putWatchlist"
      to: "web/src/plan.js::canGrowWatchlistTo"
      via: "checagem de crescimento em massa"
      pattern: "canGrowWatchlistTo"
---

<objective>
Fechar o CR-01 (bypass do cap no app iOS nativo) e alinhar o espelho comercial
do front. Hoje `deviceStore.putWatchlist`/`addWatchlistTicker` gravam direto no
`localStorage` sem gate nenhum — o limite de 10 ativos da Fase 12 só vale no
web/PWA. Este plano porta a checagem para o cliente nativo, lendo o limite REAL
do endpoint criado no plano 13-01, com política fail-closed (D-04).

Purpose: para watchlist no iOS NÃO existe gate autoritativo no servidor (o
aparelho é a fonte da verdade, local-first) — esta checagem local é a ÚNICA
linha de defesa. Errar aqui reabre um bypass real, não um bug de UX.
Output: `watchlistQuota()` nos dois stores, gate fail-closed no deviceStore,
`plan.js` sem CTA e com o espelho `canGrowWatchlistTo`, guardião estático.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-uso-real-vis-vel-na-interface-enforcement-no-ios/13-CONTEXT.md
@.planning/phases/13-uso-real-vis-vel-na-interface-enforcement-no-ios/13-PATTERNS.md
@.planning/phases/13-uso-real-vis-vel-na-interface-enforcement-no-ios/13-UI-SPEC.md
@.planning/phases/12-limites-do-plano-gratuito-ativos/12-REVIEW.md
@CLAUDE.md

<interfaces>
<!-- Contratos que o executor precisa. Não explorar o codebase para descobri-los. -->

CONTRATO DO ENDPOINT (entregue pelo plano 13-01):
  GET /api/watchlist/quota -> { "count": <int>, "limit": <int|null>, "planId": "free"|"pro" }
  limit === null  => plano sem teto (pro) => nunca bloquear

web/src/api.js (analogs exatos a copiar):
  putWatchlist: (tickers) => req("PUT", "/api/watchlist", { tickers }),           // linha 202
  addWatchlistTicker: (ticker) => req("POST", "/api/watchlist/add", { ticker }),  // linha 203
  aiQuota: () => req("GET", "/api/ai/quota"),                                     // linha 303

web/src/persistence.js — serverStore (linhas ~157-158, 284, 289-292):
  putWatchlist: (tickers) => sync.mutate("putWatchlist", [tickers], (cur) => ({...})),
  addWatchlistTicker: (ticker) => api.addWatchlistTicker(ticker),
  aiQuota: () => api.aiQuota(),
  analisesNoMes: async () => { const q = await api.aiQuota(); return (q && typeof q.monthUsed === "number") ? q.monthUsed : null; },

web/src/persistence.js — deviceStore (linhas 791-838, 1140-1152):
  async putWatchlist(tickers) { ensure(); const set = new Set(tickers);
    doc.watchlist = knownTickers().filter((t) => set.has(t)); write(); _agendarSyncPrefs(); return pub(); }
  async addWatchlistTicker(ticker) { ... valida no Yahoo via api.validateTicker/api.getQuotes ...
    if (!doc.watchlist.includes(info.t)) doc.watchlist = [...doc.watchlist, info.t];
    write(); ... return out; }
  async aiQuota() { ensure(); return api.aiQuota(); }
  async analisesNoMes() { ensure(); const q = await api.aiQuota(); return (...) ? q.monthUsed : null; }

web/src/plan.js (estado ATUAL, a corrigir):
  export const PLAN_FREE = { id: "free", maxWatchlist: null, maxAnalysesPerMonth: null };
  export function canAddTicker(count, plan = ACTIVE_PLAN) {
    if (plan.maxWatchlist != null && count >= plan.maxWatchlist)
      return { ok: false, reason: `O plano ${plan.id} permite até ${plan.maxWatchlist} ativos. Faça upgrade para adicionar mais.` };
    return { ok: true }; }
  export function canAnalyze(usedThisMonth, plan = ACTIVE_PLAN) { ... }   // já sem CTA, NÃO mexer

server/app/plan.py (fonte de verdade do TOM da frase, Fase 12/CAP-07):
  can_add_ticker      -> f"Voce atingiu o limite de {limit} ativos do plano {plan['id']}."      (bloqueia em >=)
  can_grow_watchlist_to -> f"Voce atingiu o limite de {limit} ativos do plano {plan['id']}."    (bloqueia em >)

web/tests/test_fase3_paridade_stores_generica.mjs:
  guardião genérico — compara os conjuntos de nomes de método dos dois `return {}`;
  qualquer método novo precisa existir nos DOIS stores ou o teste quebra.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: watchlistQuota() em api.js e nos dois stores (paridade)</name>
  <read_first>
    - web/src/api.js (linhas 195-210 e 295-310 — padrão `req()` e vizinhança de `aiQuota`)
    - web/src/persistence.js (linhas 150-165 e 280-295 — bloco de retorno do `serverStore`; linhas 1135-1155 — `deviceStore.aiQuota`/`analisesNoMes`)
    - web/tests/test_fase3_paridade_stores_generica.mjs (como o guardião fatia os dois `return {}` — o método novo tem que cair dentro dos blocos que ele lê)
    - .planning/phases/13-uso-real-vis-vel-na-interface-enforcement-no-ios/13-CONTEXT.md (D-05: leitura ao vivo, sem cache local)
  </read_first>
  <files>web/src/api.js, web/src/persistence.js</files>
  <action>
    (a) `web/src/api.js`: adicionar `watchlistQuota: () => req("GET", "/api/watchlist/quota"),`
    imediatamente após `aiQuota` (linha 303) — mesma forma de GET sem parâmetro,
    mesmo `req` helper. Não criar cliente HTTP novo.

    (b) `web/src/persistence.js` / `serverStore`: adicionar `watchlistQuota: () => api.watchlistQuota(),`
    ao lado de `aiQuota` (linha 284) — delegação de uma linha, sem cache, sem
    otimismo, sem passar por `sync.mutate` (é leitura, não mutação).

    (c) `web/src/persistence.js` / `deviceStore`: adicionar
    `async watchlistQuota() { ensure(); return api.watchlistQuota(); }` ao lado de
    `aiQuota` (linha ~1140) — MESMO formato de `deviceStore.aiQuota`.

    Comentário obrigatório em (c) explicando por que o local-first do iPhone abre
    exceção aqui: `max_watchlist` é dado server-authoritative (D-05, mesma
    justificativa já escrita para `aiQuota`/`analisesNoMes`), e o aparelho NÃO
    mantém contador nem cópia do limite — nenhum `10` existe no front.

    Os dois métodos precisam ficar DENTRO dos respectivos blocos `return {}` (o
    guardião genérico de paridade fatia a partir do `return {`) e com o MESMO nome
    exato `watchlistQuota` nos dois lados — guardrail "Paridade obrigatória" do
    CLAUDE.md.
  </action>
  <verify>
    <automated>node web/tests/test_fase3_paridade_stores_generica.mjs && node web/tests/test_api_parity.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "watchlistQuota" web/src/persistence.js` retorna 2 ou mais, com pelo menos 1 definição em cada store (verificável rodando o guardião genérico de paridade, que sai com código 0)
    - `grep -n "/api/watchlist/quota" web/src/api.js` retorna exatamente 1 linha
    - `node web/tests/test_fase3_paridade_stores_generica.mjs` sai 0 e não reporta `watchlistQuota` como assimetria
    - `grep -vn '^\s*//' web/src/persistence.js | grep -c "maxWatchlist\s*=\s*10\|limit\s*=\s*10"` retorna 0 — nenhum limite hardcodado
    - Nenhum dos dois métodos novos persiste o resultado em `doc` ou em variável de módulo: `grep -A3 "watchlistQuota" web/src/persistence.js` não contém `write()` nem atribuição a `doc.`
  </acceptance_criteria>
  <done>`watchlistQuota` existe com mesmo nome nos dois stores, delega ao endpoint, e o guardião de paridade passa.</done>
</task>

<task type="auto">
  <name>Task 2: plan.js — remover CTA e adicionar o espelho canGrowWatchlistTo</name>
  <read_first>
    - web/src/plan.js (arquivo inteiro, 36 linhas)
    - server/app/plan.py (linhas 74-101 — `can_add_ticker` e `can_grow_watchlist_to`, a fonte do tom fato+motivo já corrigida na Fase 12/CAP-07)
    - .planning/phases/13-uso-real-vis-vel-na-interface-enforcement-no-ios/13-UI-SPEC.md (linhas 150-165 — contrato de copy: não inventar terceira variante de tom)
  </read_first>
  <files>web/src/plan.js</files>
  <action>
    (a) Em `canAddTicker`, trocar a `reason` atual
    (`` `O plano ${plan.id} permite até ${plan.maxWatchlist} ativos. Faça upgrade para adicionar mais.` ``)
    por `` `Você atingiu o limite de ${plan.maxWatchlist} ativos do plano ${plan.id}.` `` —
    mesma estrutura fato+motivo de `can_add_ticker` em `plan.py` e mesmo padrão da
    `canAnalyze` que já existe logo abaixo. A divergência de acentuação em relação
    ao Python ("Voce" sem acento no backend, por convenção ASCII do arquivo `.py`)
    é deliberada: o texto do front é o que o usuário lê, e vai acentuado. Registrar
    isso em comentário de uma linha.

    (b) Adicionar `export function canGrowWatchlistTo(finalSize, plan = ACTIVE_PLAN)`
    espelhando `can_grow_watchlist_to` do Python: bloqueia quando
    `plan.maxWatchlist != null && finalSize > plan.maxWatchlist` (operador `>`,
    NÃO `>=` — é o tamanho FINAL de uma troca em massa, não "quantos existem antes
    de uma adição"), com a MESMA `reason` do item (a). Comentário deve explicar a
    diferença de semântica entre os dois hooks, igual faz o docstring do Python
    (WR-02 do 12-REVIEW.md).

    (c) NÃO alterar `canAnalyze` (já está sem CTA) nem `requiresSubscription`.

    (d) NÃO trocar `maxWatchlist: null` / `maxAnalysesPerMonth: null` por 10/30 em
    `PLAN_FREE`. Decisão de planejamento, explicitada aqui para não ser
    re-litigada: o critério de sucesso 6 do ROADMAP proíbe `10` hardcodado no
    front (contrato C-32/C-33, fonte única). O limite real chega SEMPRE do
    endpoint em runtime e é injetado nestes hooks como objeto de plano
    (`{ id: quota.planId, maxWatchlist: quota.limit }`, ver Task 3). Atualizar o
    comentário do topo do arquivo para dizer isso: os campos `null` aqui são o
    fallback "sem limite conhecido localmente", e a fonte de verdade é
    `server/app/plan.py` via `GET /api/watchlist/quota`.
  </action>
  <verify>
    <automated>node -e "import('./web/src/plan.js').then(m=>{const r=m.canAddTicker(10,{id:'free',maxWatchlist:10});if(r.ok||/upgrade/i.test(r.reason))throw new Error('canAddTicker errado: '+JSON.stringify(r));const g=m.canGrowWatchlistTo(11,{id:'free',maxWatchlist:10});if(g.ok)throw new Error('canGrowWatchlistTo nao bloqueou 11');const g2=m.canGrowWatchlistTo(10,{id:'free',maxWatchlist:10});if(!g2.ok)throw new Error('canGrowWatchlistTo bloqueou 10 (deveria permitir)');const p=m.canGrowWatchlistTo(999,{id:'pro',maxWatchlist:null});if(!p.ok)throw new Error('pro nao pode ser bloqueado');console.log('plan.js ok')})"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Faça upgrade" web/src/plan.js` retorna 0
    - `grep -c "export function canGrowWatchlistTo" web/src/plan.js` retorna 1
    - `canGrowWatchlistTo(10, {id:'free',maxWatchlist:10})` retorna `{ok:true}` e `canGrowWatchlistTo(11, ...)` retorna `ok:false` (operador `>`, não `>=`) — coberto pelo comando de verify
    - `canAddTicker(10, {id:'free',maxWatchlist:10}).reason` é exatamente `"Você atingiu o limite de 10 ativos do plano free."`
    - `grep -n "maxWatchlist:" web/src/plan.js` continua mostrando `null` nas duas constantes `PLAN_FREE`/`PLAN_PRO` — nenhum 10/30 hardcodado
    - `canAnalyze` permanece byte-idêntica ao estado anterior: `git diff web/src/plan.js` não mostra nenhuma linha alterada dentro do corpo de `canAnalyze`
  </acceptance_criteria>
  <done>plan.js sem CTA, com `canGrowWatchlistTo` espelhando o Python, e sem número de limite hardcodado.</done>
</task>

<task type="auto">
  <name>Task 3: Gate fail-closed no deviceStore + guardião estático</name>
  <read_first>
    - web/src/persistence.js (linhas 791-798 `putWatchlist`, 799-838 `addWatchlistTicker` — ler o corpo inteiro dos dois antes de editar)
    - web/src/plan.js (após a Task 2 — `canAddTicker`/`canGrowWatchlistTo` e como importá-los; conferir se `persistence.js` já importa de `./plan.js`)
    - .planning/phases/13-uso-real-vis-vel-na-interface-enforcement-no-ios/13-PATTERNS.md (seção "deviceStore (iOS): gate fail-closed", com o pseudocódigo e o ANTI-PATTERN a não copiar)
    - .planning/phases/12-limites-do-plano-gratuito-ativos/12-REVIEW.md (CR-01 — o achado que este gate fecha)
    - web/tests/test_device_budget_sync.mjs (helper `bodyOf()` de chaves balanceadas e o padrão de asserção por ORDEM de índice dentro do corpo do método)
    - web/tests/test_fase5_gate_mensal_front.mjs (padrão de asserção de ausência de string e de contagem de definições por store)
  </read_first>
  <files>web/src/persistence.js, web/tests/test_fase13_watchlist_quota_ios.mjs</files>
  <action>
    (a) `deviceStore.addWatchlistTicker`: inserir o gate DEPOIS da validação do
    ticker (o bloco que resolve `info` via `api.validateTicker`/`api.getQuotes`) e
    ANTES de qualquer mutação de `doc` — isto é, antes de `doc.custom = [...]` e
    antes de `doc.watchlist = [...doc.watchlist, info.t]`/`write()`. Curto-circuito:
    se `doc.watchlist.includes(info.t)` (ticker já está na lista), NÃO é
    crescimento — pular o gate e seguir o caminho atual (adicionar duas vezes o
    mesmo ativo não pode virar erro de limite).
    Sequência do gate:
    1. `let quota; try { quota = await api.watchlistQuota(); } catch { throw new Error("Não foi possível confirmar o limite do plano agora. Tente de novo."); }`
       — fail-closed (D-04). O `catch` NUNCA pode seguir para a escrita.
    2. Resposta inválida (não-objeto, ou `typeof quota.count !== "number"`) recebe
       o MESMO tratamento do item 1 — dado inválido bloqueia (princípio 4 do
       CLAUDE.md), não é fail-open.
    3. `const r = canAddTicker(quota.count, { id: quota.planId || "free", maxWatchlist: quota.limit });`
       `if (!r.ok) throw new Error(r.reason);` — a frase vem de `plan.js`
       (Task 2), que espelha `plan.py`; não escrever frase nova aqui.
       `quota.limit === null` (pro) faz `canAddTicker` devolver `ok: true` — pro
       nunca é bloqueado.

    (b) `deviceStore.putWatchlist`: mesma política, com a semântica de MASSA
    (espelho de `can_grow_watchlist_to`). Calcular primeiro
    `const final = knownTickers().filter((t) => set.has(t));` e comparar com o
    estado atual: só buscar a quota e aplicar o gate se
    `final.length > (doc.watchlist || []).length` (só CRESCIMENTO é barrado —
    remoção e reordenação nunca falham, nem offline; grandfather clause D-04 da
    Fase 12 preservada). Havendo crescimento: mesma sequência 1→3 da alínea (a),
    trocando o hook por `canGrowWatchlistTo(final.length, { id: quota.planId || "free", maxWatchlist: quota.limit })`.
    Só depois: `doc.watchlist = final; write(); _agendarSyncPrefs(); return pub();`

    (c) Importar `canAddTicker`/`canGrowWatchlistTo` de `./plan.js` no topo de
    `persistence.js` (conferir se já existe import desse módulo antes de criar um
    segundo). NÃO duplicar a lógica de comparação dentro do store.

    (d) Comentário obrigatório em cada gate, citando CR-01 (12-REVIEW.md) e D-04:
    por que aqui é fail-CLOSED enquanto `A.analyze` (App.jsx:7257) e
    `analisesNoMes` são fail-open — para análises o servidor é a autoridade
    (`_gate_analise` barra de qualquer jeito); para watchlist no iOS o cliente é a
    ÚNICA defesa, então falha-aberta reabriria o bypass. Este comentário é o
    guardião humano da decisão; não omitir.

    (e) Criar `web/tests/test_fase13_watchlist_quota_ios.mjs` no estilo da casa —
    inspeção ESTÁTICA de fonte (`readFileSync` + `bodyOf()` de chaves
    balanceadas), sem Vitest/Jest e sem executar `fetch`. Asserções mínimas:
    - `bodyOf(src, "async addWatchlistTicker(ticker)")` contém `watchlistQuota` e o
      índice dessa ocorrência é MENOR que o índice de `write()` e menor que o de
      `doc.watchlist = [...doc.watchlist`;
    - o mesmo para `bodyOf(src, "async putWatchlist(tickers)")`: `watchlistQuota`
      antes de `write()`;
    - o `catch` que envolve `api.watchlistQuota()` nos dois corpos contém `throw`
      (fail-closed) — provar que NÃO existe ali um `catch { }`/`catch { /* ... */ }`
      vazio equivalente ao de `analisesNoMes`;
    - `putWatchlist` contém a comparação de crescimento (`final.length >` contra o
      tamanho atual) — remoção não dispara rede;
    - `web/src/plan.js` não contém a string `"Faça upgrade"`;
    - `web/src/persistence.js` não contém nenhum literal de limite: nenhuma linha
      não-comentada casando `maxWatchlist` seguido de número.
    Cabeçalho do arquivo deve explicar o incidente que ele previne (CR-01) e por
    que a asserção é de ORDEM (rede antes da escrita), não de resultado.
  </action>
  <verify>
    <automated>node web/tests/test_fase13_watchlist_quota_ios.mjs && node web/tests/test_fase3_paridade_stores_generica.mjs && cd web && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `node web/tests/test_fase13_watchlist_quota_ios.mjs` sai com código 0 e imprime `ok ` em todas as asserções (0 `FALHOU`)
    - No corpo de `deviceStore.addWatchlistTicker`, o índice de `api.watchlistQuota()` é menor que o índice de `write()` — asserção verificada pelo próprio teste
    - No corpo de `deviceStore.putWatchlist`, a busca de quota só ocorre sob a condição de crescimento: o teste assere a presença do comparador de tamanho antes da chamada de rede
    - A mensagem de indisponibilidade é exatamente `"Não foi possível confirmar o limite do plano agora. Tente de novo."` (`grep -c` retorna >= 1 em persistence.js)
    - Nenhum `catch` do gate está vazio: `grep -A2 "api.watchlistQuota()" web/src/persistence.js` mostra `throw new Error` no caminho de erro
    - `cd web && npx vite build` conclui sem erro (erro de sintaxe JS não é pego por grep/teste estático)
    - `bash scripts/executar.sh --testes` continua verde (as DUAS suítes)
  </acceptance_criteria>
  <done>iOS bloqueia o 11º ativo e bloqueia quando não consegue confirmar o limite; guardião estático trava a ordem rede→escrita.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `deviceStore` (iOS, local-first) → `localStorage` | O aparelho é a fonte da verdade; NÃO há gate server-side depois dessa escrita |
| `deviceStore` → `GET /api/watchlist/quota` | Rede não confiável (offline, timeout, resposta malformada, proxy) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Elevation of Privilege | `deviceStore.addWatchlistTicker`/`putWatchlist` | mitigate | Gate lê `count`/`limit` do endpoint e chama `canAddTicker`/`canGrowWatchlistTo` ANTES de qualquer `write()`. Guardião estático `test_fase13_watchlist_quota_ios.mjs` trava a ORDEM (rede antes da escrita) — a classe do erro, não só a instância |
| T-13-06 | Tampering | resposta de `/api/watchlist/quota` interceptada/forjada | mitigate | Resposta inválida ou não-numérica cai no MESMO caminho de bloqueio da falha de rede (fail-closed, princípio 4 do CLAUDE.md). Nenhum default permissivo: ausência de `limit` numérico nunca vira "sem teto" — só `limit === null` explícito libera, e mesmo assim via `plan.js`, não por branch ad hoc |
| T-13-07 | Denial of Service (self-inflicted) | usuário offline não consegue REMOVER ativo | mitigate | `putWatchlist` só busca quota quando `final.length > atual.length`; remoção/reordenação nunca tocam a rede e nunca falham offline. Grandfather clause (D-04 da Fase 12) preservada: conta acima do limite não perde ativos |
| T-13-08 | Repudiation | recusa sem motivo legível | mitigate | Duas frases distintas e específicas: "não foi possível confirmar o limite" (rede) × "Você atingiu o limite de N ativos do plano X" (limite real), ambas com fato+motivo e sem CTA (CAP-07) |
| T-13-SC | Tampering (supply chain) | npm installs | accept | Nenhuma dependência nova em `web/package.json`; o guardião novo roda como script Node puro, sem framework de teste adicional |
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` verde (pytest + todos os `web/tests/*.mjs`)
- `cd web && npx vite build` sem erro
- Inspeção manual do diff de `persistence.js`: nenhuma chamada de rede depois de `write()`, nenhum `catch` silencioso no gate
</verification>

<success_criteria>
- CAP-12 fechado: iOS respeita `max_watchlist` real, lido do endpoint, sem hardcode
- Fail-closed comprovado por guardião estático (ordem rede→escrita + `throw` no catch)
- Paridade `deviceStore`↔`serverStore` mantida para `watchlistQuota`
- CAP-07 do front fechado: `plan.js` sem frase de CTA
</success_criteria>

<output>
Create `.planning/phases/13-uso-real-vis-vel-na-interface-enforcement-no-ios/13-02-SUMMARY.md` when done
</output>
