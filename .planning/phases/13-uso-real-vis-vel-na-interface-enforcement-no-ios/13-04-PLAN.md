---
phase: 13-uso-real-vis-vel-na-interface-enforcement-no-ios
plan: 04
type: execute
wave: 4
depends_on: ["13-03"]
files_modified: []
autonomous: false
requirements: [CAP-06, CAP-12]

must_haves:
  truths:
    - "O fragmento X/Y em estado quase-no-limite/no-limite é legível no TEMA CLARO (T.warn = #a16207) em 10.5-13px — verificado com renderização real, não por leitura de código"
    - "Os 3 contadores aparecem nos locais do mockup aprovado, com números reais"
    - "O nome do app exibido no App Store Connect/TestFlight está correto (Boris+), confirmado pelo Alex"
  artifacts:
    - path: ".planning/phases/13-uso-real-vis-vel-na-interface-enforcement-no-ios/13-04-SUMMARY.md"
      provides: "registro do veredito humano dos dois checkpoints"
  key_links:
    - from: "13-UI-SPEC.md (ação pendente de contraste)"
      to: "verificação renderizada no tema claro"
      via: "checkpoint humano bloqueante"
      pattern: "T\\.warn"
---

<objective>
Fechar as duas verificações que NENHUM agente pode fazer sozinho: (1) o
contraste real do `T.warn` no tema claro (`#a16207`) em 10.5-13px, que o
`13-UI-SPEC.md` deixou explicitamente pendente porque os specialists revisaram
só código-fonte, sem renderizar; e (2) o nome do app no App Store Connect/
TestFlight (critério de sucesso 9 do ROADMAP), que vive no portal da Apple e
não no repositório.

Purpose: os planos 01-03 entregam código verificável por teste; estes dois itens
são verificáveis só por olho humano e por acesso a um portal externo. Deixá-los
implícitos é como a fase "passa" no automático e falha na realidade.
Output: veredito registrado no SUMMARY, com correções aplicadas se reprovar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-uso-real-vis-vel-na-interface-enforcement-no-ios/13-UI-SPEC.md
@.planning/phases/13-uso-real-vis-vel-na-interface-enforcement-no-ios/13-CONTEXT.md
@TESTFLIGHT.md

<interfaces>
Ambiente de dev (scripts/run.sh, invocado por scripts/dev.sh):
  backend FastAPI -> http://localhost:8787   (uvicorn app.main:app --reload)
  front Vite      -> http://localhost:5174   (proxy /api -> 8787)

Endpoint de conferência (plano 13-01):
  GET /api/watchlist/quota -> { count, limit, planId }

Estados a forçar (13-UI-SPEC, tabela "Estados"):
  normal            -> count/limit < 0.9        (ex.: 5/10)
  quase no limite   -> count/limit >= 0.9       (ex.: 9/10)  -> color: T.warn
  no limite         -> count === limit          (ex.: 10/10) -> color: T.warn
  indisponível      -> endpoint fora            -> "—"
  plano sem limite  -> limit === null (pro)     -> segmento OMITIDO

Tokens de cor sob suspeita (13-UI-SPEC, "Ação pendente antes de fechar a fase"):
  T.warn tema escuro = #fbbf24   (risco baixo)
  T.warn tema claro  = #a16207   (MAIOR risco de contraste baixo contra T.bgBase)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preparar o ambiente de verificação ao vivo (automatizar tudo antes do olho humano)</name>
  <read_first>
    - scripts/run.sh (linhas 1-30 e 95-120 — portas 8787/5174 e como backend e Vite sobem)
    - .planning/phases/13-uso-real-vis-vel-na-interface-enforcement-no-ios/13-UI-SPEC.md (tabela "Estados" e a nota de ação pendente de contraste)
    - server/app/db.py (assinatura de `set_user_plan`, para forçar o estado "plano sem limite")
  </read_first>
  <files>(nenhum arquivo do repo é modificado — task de preparação de ambiente)</files>
  <action>
    Deixar a tela PRONTA para o Alex olhar, sem pedir a ele nenhum passo que uma
    CLI possa executar:
    1. Subir o ambiente de dev em background: `bash scripts/dev.sh` (backend 8787
       + Vite 5174). Confirmar que ambos responderam antes de seguir.
    2. Criar/usar uma conta de teste local e semear a watchlist em 9 ativos
       (estado "quase no limite") via `PUT /api/watchlist` com tickers do
       catálogo padrão. Confirmar por `curl -s localhost:8787/api/watchlist/quota`
       que a resposta é `count: 9, limit: 10, planId: "free"`.
    3. Registrar no output do executor os 4 comandos/URLs exatos que o Alex vai
       usar (não descrições vagas): a URL da Watchlist, como abrir o
       CatalogModal, como chegar em Perfil → Atividade da IA, e como alternar
       tema claro/escuro na própria UI.
    4. Preparar os comandos de troca de estado para o checkpoint, prontos para
       colar: (a) subir para 10 ativos (estado "no limite"); (b) derrubar só o
       backend para ver o estado "—"; (c) `set_user_plan(..., "pro")` para ver o
       segmento omitido.
    NÃO pedir ao humano nada que caiba num comando. O checkpoint da Task 2 é só
    para o que exige olho: legibilidade e julgamento visual.
  </action>
  <verify>
    <automated>curl -s localhost:8787/api/watchlist/quota | grep -q '"limit"' && curl -s -o /dev/null -w "%{http_code}" localhost:5174 | grep -q 200</automated>
  </verify>
  <acceptance_criteria>
    - `curl -s localhost:8787/api/watchlist/quota` devolve JSON com `count`, `limit` e `planId`
    - `curl -s -o /dev/null -w "%{http_code}" localhost:5174` retorna 200
    - A watchlist da conta de teste tem exatamente 9 tickers no momento do checkpoint (`count: 9`)
    - O output do executor lista os comandos de troca de estado (10/10, backend fora, plano pro) prontos para colar, sem placeholders
  </acceptance_criteria>
  <done>Ambiente no ar, estado 9/10 semeado, comandos de troca de estado documentados.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Alex verifica os 3 contadores e o contraste do T.warn no tema claro</name>
  <read_first>
    - .planning/phases/13-uso-real-vis-vel-na-interface-enforcement-no-ios/13-UI-SPEC.md (tabelas Typography, Color, Estados e Copywriting Contract — é contra elas que o veredito é dado)
  </read_first>
  <files>(nenhum arquivo do repo é modificado — verificação visual)</files>
  <action>
    Pausar a execução e apresentar ao Alex o roteiro de `<how-to-verify>` abaixo,
    junto com os comandos de troca de estado preparados na Task 1 (10/10, backend
    fora, conta pro). Não seguir para o plano 13-05 sem o sinal de retomada. Se o
    veredito for reprovação de contraste, aplicar a correção DENTRO do que o
    13-UI-SPEC permite (ajuste do token de cor de alerta ou do peso do fragmento —
    nunca trocar para `T.negative`/`T.positive`, reservados a P&L) e repetir o
    checkpoint do zero.
  </action>
  <what-built>
    Três contadores de uso/limite, todos alimentados por número real do backend:
    subtítulo da Watchlist ("· ativos: 9/10"), CatalogModal ("N de 40
    selecionados · 9/10 do plano free") e Atividade da IA ("análises deste mês:
    X/30"). Fragmento numérico em fonte monoespaçada, peso 700; cor escala para
    T.warn a partir de 90% do limite; "—" quando o backend não confirma; segmento
    inteiro omitido no plano sem teto. Ambiente já no ar com a watchlist em 9/10.
  </what-built>
  <how-to-verify>
    1. Abra http://localhost:5174 → aba Watchlist. Confirme no subtítulo:
       "· ativos: 9/10", com os dígitos em fonte monoespaçada e negrito, e a cor
       ÂMBAR (não vermelho, não verde).
    2. Toque em ✎ (Editar watchlist). Confirme a segunda linha do cabeçalho do
       modal: "N de 40 selecionados · 9/10 do plano free".
    3. Vá em Perfil → Atividade da IA. Confirme a linha "análises deste mês:
       X/30" dentro do card CUSTO ESTIMADO.
    4. **O ponto crítico desta verificação**: troque para o TEMA CLARO e repita
       os passos 1-3. O T.warn no claro é `#a16207` em 10.5-13px — nunca foi
       renderizado nem medido. Diga se está LEGÍVEL contra o fundo claro ou se
       parece apagado/ilegível. Este é o único item que reprova a fase sozinho.
    5. Rode o comando (a) que o executor deixou pronto para ir a 10/10 e confirme
       que a cor continua âmbar (não vira vermelho).
    6. Rode o comando (b) (derruba o backend) e confirme que os 3 pontos mostram
       "—", nunca um número velho ou zero.
    7. Rode o comando (c) (conta vira "pro") e confirme que os 3 segmentos SOMEM
       por completo — sem "ilimitado", sem "X/∞", sem "0/0".
  </how-to-verify>
  <acceptance_criteria>
    - Alex confirma legibilidade do fragmento âmbar no TEMA CLARO, ou reprova com a descrição do problema
    - Os 3 contadores exibem o número real (9/10 na watchlist), não um placeholder
    - Estado "—" confirmado com o backend fora
    - Estado "plano sem teto" confirmado: segmento ausente, sem a palavra "ilimitado"
    - Se algum item reprovar, a correção é aplicada e o checkpoint é repetido antes de seguir — não se registra "parcialmente ok"
  </acceptance_criteria>
  <verify>
    <automated>node web/tests/test_fase13_contadores_ui.mjs</automated>
    <human-check>Alex aprova a legibilidade do fragmento âmbar no tema claro e os 5 estados ao vivo</human-check>
  </verify>
  <done>Veredito do Alex registrado; se reprovou, correção aplicada e checkpoint repetido até aprovar.</done>
  <resume-signal>Digite "aprovado" ou descreva o que ficou errado (especialmente o contraste no tema claro)</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Alex confere o nome do app no App Store Connect/TestFlight</name>
  <read_first>
    - TESTFLIGHT.md (checklist de publicação iOS já existente no repo)
    - .planning/ROADMAP.md (Fase 13, critério de sucesso 9)
  </read_first>
  <files>(nenhum arquivo do repo é modificado — configuração vive no portal da Apple)</files>
  <action>
    Pausar e apresentar ao Alex o roteiro de `<how-to-verify>` abaixo. Não tentar
    nenhuma automação: não existe CLI para o nome exibido no App Store Connect, e
    qualquer tentativa de "resolver no repo" levaria a mexer no bundle id, que é
    invariante. Após o sinal de retomada, registrar o veredito literal do Alex
    (nome encontrado, nome final, houve correção sim/não) no `13-04-SUMMARY.md`.
  </action>
  <what-built>
    Nada — este item é deliberadamente fora do alcance de qualquer agente. O nome
    exibido do app é configuração no portal da Apple (App Store Connect), não
    existe no repositório e não há CLI para alterá-lo. O bundle id
    `com.alexandrecamerini.bolsia` NÃO muda (guardrail do repo: trocá-lo publica
    outro app e quebra o login SIWA) — o que se verifica aqui é só o NOME
    EXIBIDO, resíduo do rename BolsIA→Boris+.
  </what-built>
  <how-to-verify>
    1. Abra App Store Connect → Meus Apps → o app do bundle
       `com.alexandrecamerini.bolsia`.
    2. Confira o nome exibido do app e o nome que aparece no TestFlight para os
       testadores.
    3. Se ainda estiver como "BolsIA" (ou qualquer variante antiga), corrija para
       "Boris+" ali mesmo.
    4. Responda com o estado encontrado (já correto × corrigido agora) para que o
       SUMMARY registre o fato — o critério de sucesso 9 só fecha com o veredito
       do Alex, não com uma suposição.
  </how-to-verify>
  <acceptance_criteria>
    - O veredito do Alex está registrado no SUMMARY: nome encontrado, nome final, e se houve correção
    - Nenhuma alteração de `com.alexandrecamerini.bolsia` foi feita ou sugerida
    - Nenhum arquivo do repositório foi tocado por esta task
  </acceptance_criteria>
  <verify>
    <human-check>Alex reporta o nome exibido encontrado no App Store Connect/TestFlight e se corrigiu</human-check>
  </verify>
  <done>Critério de sucesso 9 do ROADMAP fechado com veredito escrito, não com suposição.</done>
  <resume-signal>Descreva o nome encontrado e se corrigiu (ex.: "já estava Boris+" ou "estava BolsIA, corrigi")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ambiente de dev local → olho humano | O veredito visual é o único gate para uma decisão de acessibilidade não medida |
| repositório → App Store Connect (portal externo Apple) | Configuração fora do controle de versão; nenhuma automação atravessa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-13 | Repudiation | veredito humano não registrado | mitigate | Ambos os checkpoints exigem o resultado escrito no `13-04-SUMMARY.md` (nome encontrado/corrigido; legibilidade aprovada/reprovada) — a fase não fecha com "assumo que está ok" |
| T-13-14 | Tampering | identidade do app iOS | mitigate | A task diz explicitamente que só o NOME EXIBIDO muda; `com.alexandrecamerini.bolsia` é invariante do repo (trocar publica outro app e quebra SIWA). Acceptance criteria assere que nada do bundle id foi alterado |
| T-13-15 | Information Disclosure | conta de teste semeada no dev | accept | Dados de teste locais (`B3_DB_PATH` de dev), sem PII e sem chave real; não sai da máquina |
| T-13-SC | Tampering (supply chain) | npm/pip installs | accept | Nenhuma instalação; `scripts/run.sh` pode rodar `npm install` a partir do lockfile já versionado, sem dependência nova |
</threat_model>

<verification>
- Veredito dos dois checkpoints registrado no SUMMARY, com o estado exato encontrado
- Se o contraste reprovar: correção aplicada (ajuste de token/peso dentro do que o 13-UI-SPEC permite) e checkpoint repetido
- Ambiente de dev derrubado ao final (sem processo órfão em 8787/5174)
</verification>

<success_criteria>
- Ação pendente de contraste do 13-UI-SPEC resolvida com renderização real, não por leitura de código
- Critério de sucesso 9 do ROADMAP fechado com veredito do Alex
- Os 5 estados de UI confirmados ao vivo (normal, quase-limite, limite, indisponível, sem teto)
</success_criteria>

<output>
Create `.planning/phases/13-uso-real-vis-vel-na-interface-enforcement-no-ios/13-04-SUMMARY.md` when done
</output>
