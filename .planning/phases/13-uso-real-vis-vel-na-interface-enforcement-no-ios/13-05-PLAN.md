---
phase: 13-uso-real-vis-vel-na-interface-enforcement-no-ios
plan: 05
type: execute
wave: 5
depends_on: ["13-04"]
files_modified:
  - web/src/version.js
  - server/web_dist
autonomous: true
requirements: [CAP-06, CAP-12]

must_haves:
  truths:
    - "A versão web em produção reflete o front desta fase — os contadores não ficam só no repo"
    - "O carimbo BUILD_ID foi avançado antes da publicação, sem editar server/web_dist à mão"
    - "O bundle iOS local está sincronizado com o front novo, e o passo de TestFlight está explicitamente registrado como pendência do Alex"
  artifacts:
    - path: "web/src/version.js"
      provides: "BUILD_ID novo desta entrega"
      contains: "BUILD_ID"
    - path: "server/web_dist"
      provides: "árvore publicada que o Railway serve"
  key_links:
    - from: "web/src/version.js"
      to: "server/web_dist"
      via: "scripts/publicar-web.sh"
      pattern: "BUILD_ID"
---

<objective>
Levar a fase ao ar. Sem este plano, os 3 contadores e o gate do iOS ficam
testados no repositório e nunca chegam ao usuário — o padrão de falha já
registrado no histórico do projeto ("fase sem plano de publicação front").

Purpose: `server/web_dist` é a ÚNICA árvore que o Railway enxerga
(`rootDirectory=/server`); o front em `web/` fica fora do build context. A
publicação é um passo manual deliberado, com carimbo (`BUILD_ID`) que prova
qual entrega está no ar.
Output: `BUILD_ID` novo, `server/web_dist` republicado, bundle iOS sincronizado
e a pendência de TestFlight registrada no SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@TESTFLIGHT.md

<interfaces>
Pipeline de publicação (guardrail do repo — ordem obrigatória):
  bash scripts/bump.sh              # avança BUILD_ID em web/src/version.js (SEMPRE antes)
  bash scripts/publicar-web.sh      # npm ci + vite build + publica em server/web_dist + sincroniza carimbo
  # NUNCA editar server/web_dist direto

Formato do carimbo (scripts/bump.sh): prefixo de fase + data de 8 dígitos +
sequencial, ex.: F10-20260829-01. Esquecer o bump faz o dist novo sair com o
carimbo do dia anterior — assinatura de "bundle amputado" que o
web/tests/test_ios_assets.mjs pega.

Bundle iOS (Capacitor 8): `web/ios/` existe no repo; `npx cap sync ios` copia
`web/dist` para dentro do projeto nativo. O archive/upload para TestFlight
exige Xcode e é passo manual do Alex (TESTFLIGHT.md), fora do alcance do agente.

Suíte canônica: `bash scripts/executar.sh --testes` (pytest + web/tests/*.mjs).
`scripts/test.sh` sozinho é meia baseline e NÃO conta como validação.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump do carimbo e publicação do front</name>
  <read_first>
    - scripts/bump.sh (linhas 1-40 — formato do BUILD_ID e por que o bump é comando separado)
    - scripts/publicar-web.sh (linhas 1-45 — as 3 etapas e as flags `--so-build`/`--so-publicar`)
    - web/src/version.js (BUILD_ID atual)
    - CLAUDE.md (seção "Publicação" dos guardrails)
  </read_first>
  <files>web/src/version.js, server/web_dist</files>
  <action>
    1. Rodar `bash scripts/executar.sh --testes` ANTES de publicar e confirmar
       verde nas DUAS suítes. Se falhar, parar — não publicar suíte vermelha.
    2. `bash scripts/bump.sh` (sem argumento: pega o próximo carimbo do dia).
       Confirmar que `web/src/version.js` mudou e que o novo `BUILD_ID` segue o
       padrão `<prefixo>-<AAAAMMDD>-<NN>`.
    3. `bash scripts/publicar-web.sh` (sem flags: builda, publica e sincroniza o
       carimbo). NÃO editar `server/web_dist` manualmente em nenhuma hipótese.
    4. Revisar o diff de `server/web_dist`: deve conter os assets novos com o
       carimbo novo. Se o diff vier vazio, algo falhou — investigar em vez de
       seguir.
    5. Registrar no SUMMARY o `BUILD_ID` publicado (é o que prova qual entrega
       está no ar) e lembrar que o deploy do Railway acontece no push.
    Este plano NÃO faz deploy só-backend, então não é necessário bump manual de
    `SERVER_BUILD_ID` — ele é derivado do `BUILD_ID` pelo pipeline normal.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes && grep -q "$(sed -n 's/.*BUILD_ID = "\([^"]*\)".*/\1/p' web/src/version.js)" server/web_dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/executar.sh --testes` sai com código 0 (as DUAS suítes)
    - `git diff web/src/version.js` mostra o `BUILD_ID` avançado, no formato `<prefixo>-<AAAAMMDD>-<NN>`
    - O `BUILD_ID` de `web/src/version.js` aparece dentro de `server/web_dist` (carimbo propagado) — coberto pelo comando de verify
    - `git status --porcelain server/web_dist` lista arquivos alterados (a publicação de fato produziu dist novo)
    - Nenhum arquivo sob `server/web_dist` foi editado por Edit/Write — só por `scripts/publicar-web.sh`
  </acceptance_criteria>
  <done>Carimbo avançado, `server/web_dist` republicado a partir do build real, suíte canônica verde.</done>
</task>

<task type="auto">
  <name>Task 2: Sincronizar o bundle iOS e registrar a pendência de TestFlight</name>
  <read_first>
    - TESTFLIGHT.md (checklist de publicação iOS — quais passos exigem Xcode e são do Alex)
    - scripts/instalar-iphone.sh (linhas 50-75 — o papel do `npx cap sync ios` no pipeline)
    - web/capacitor.config.ts (confirma que o app nativo carrega bundle LOCAL, sem `server.url` — por isso o iOS só recebe o código novo com build novo)
  </read_first>
  <files>web/ios (sincronização de bundle; sem edição manual de fonte)</files>
  <action>
    O gate do CAP-12 vive no `deviceStore`, que só executa no app nativo — e o app
    nativo carrega o bundle EMBUTIDO no binário (`web/capacitor.config.ts` não usa
    `server.url`). Consequência que precisa ficar registrada, não implícita:
    **publicar a web NÃO fecha o CAP-12 para o usuário do iPhone**; só um build
    novo distribuído por TestFlight fecha.

    1. Rodar `npx cap sync ios` a partir de `web/` para copiar o `dist` recém
       buildado para dentro do projeto nativo. Se o comando falhar por ausência de
       ambiente (sem Xcode/CocoaPods na máquina), NÃO improvisar: registrar a
       falha e o motivo no SUMMARY e seguir — a sincronização vira parte do passo
       manual do Alex.
    2. Registrar no SUMMARY, como pendência explícita e nomeada:
       "CAP-12 está no código e coberto por teste, mas só vale no iPhone do
       usuário depois de um build novo no TestFlight (passo manual do Alex,
       TESTFLIGHT.md: `scripts/ios-bump-build.sh` → `scripts/ios-testflight.sh` →
       archive/upload no Xcode)."
    3. NÃO rodar archive/upload, NÃO tocar em `com.alexandrecamerini.bolsia`, NÃO
       mexer em entitlements de APNs (o flip `--apns-prod` é coordenado com o
       Railway e não é escopo desta fase).
  </action>
  <verify>
    <automated>test -d web/ios/App && (cd web && npx cap sync ios --help >/dev/null 2>&1 || true) && echo "sync avaliado"</automated>
  </verify>
  <acceptance_criteria>
    - O SUMMARY contém a pendência de TestFlight escrita nominalmente, com os scripts do caminho manual (`ios-bump-build.sh`, `ios-testflight.sh`)
    - `grep -c "com.alexandrecamerini.bolsia" web/capacitor.config.ts` inalterado e `git diff web/capacitor.config.ts` vazio
    - `git diff web/ios/App/App/App.entitlements` vazio (nenhum flip de APNs)
    - Se `npx cap sync ios` falhou, o SUMMARY registra o erro exato e o motivo — não há alegação de sincronização que não aconteceu
  </acceptance_criteria>
  <done>Bundle iOS sincronizado (ou falha registrada com motivo) e a pendência de TestFlight nomeada no SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repositório → produção (Railway serve `server/web_dist`) | Publicação move código para o ambiente que o usuário real usa |
| `web/dist` → bundle nativo iOS | Código que roda no aparelho do usuário, fora do alcance de correção server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-16 | Tampering | `server/web_dist` | mitigate | Publicação só via `scripts/publicar-web.sh` (build real a partir de `web/`), nunca edição manual do dist — guardrail do repo; acceptance criteria assere isso |
| T-13-17 | Repudiation | qual entrega está no ar | mitigate | `scripts/bump.sh` antes da publicação; o `BUILD_ID` viaja version.js → dist → web_dist e é registrado no SUMMARY. Sem bump, dist novo sai com carimbo velho (assinatura de bundle amputado que `test_ios_assets.mjs` pega) |
| T-13-18 | Elevation of Privilege | CAP-12 assumido como fechado no iOS | mitigate | O plano declara explicitamente que o gate do `deviceStore` só chega ao usuário via build novo de TestFlight, e obriga o registro dessa pendência no SUMMARY — evita a fase ser marcada completa com o bypass ainda vivo nos aparelhos em campo |
| T-13-19 | Tampering | identidade do app / APNs | mitigate | Task proíbe alterar `com.alexandrecamerini.bolsia` e entitlements de push; acceptance criteria assere diff vazio nesses arquivos |
| T-13-SC | Tampering (supply chain) | `npm ci` dentro de `publicar-web.sh` | accept | Instalação determinística a partir do `package-lock.json` já versionado; nenhuma dependência nova entrou nesta fase (planos 01-03 não tocaram `package.json` nem `requirements.txt`) |
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` verde antes da publicação
- `BUILD_ID` novo presente em `web/src/version.js` e propagado para `server/web_dist`
- Diffs de `capacitor.config.ts` e `App.entitlements` vazios
</verification>

<success_criteria>
- Front desta fase publicado em `server/web_dist` com carimbo novo
- Pendência de TestFlight (única via para o CAP-12 valer nos aparelhos em campo) registrada nominalmente, não implícita
- Nenhum guardrail de publicação violado
</success_criteria>

<output>
Create `.planning/phases/13-uso-real-vis-vel-na-interface-enforcement-no-ios/13-05-SUMMARY.md` when done
</output>
