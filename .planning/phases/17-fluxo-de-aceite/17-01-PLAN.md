---
phase: 17-fluxo-de-aceite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/store.py
  - server/tests/test_opcoes_collar_execucao.py
autonomous: true
requirements: [FLOW-03]

must_haves:
  truths:
    - "Uma trava protetora (collar) de 2 pernas é executada pelo motor de ordens de store.py — as duas pernas escrevem juntas ou nenhuma escreve"
    - "Uma perna inválida (tipo errado, lastro insuficiente, caixa insuficiente) deixa caixa, positions e optionPositions exatamente como estavam"
    - "O mesmo lote-lastro serve as duas pernas: qty_livre == contratos*100 é suficiente para o collar inteiro"
  artifacts:
    - path: "server/app/store.py"
      provides: "abrir_collar — execução de 2 pernas numa única aquisição de ORDER_LOCK"
      contains: "def abrir_collar"
    - path: "server/tests/test_opcoes_collar_execucao.py"
      provides: "Guardiões de atomicidade estrutural e de não-composição das funções single-leg"
  key_links:
    - from: "server/app/store.py::abrir_collar"
      to: "store.ORDER_LOCK"
      via: "um único `with ORDER_LOCK:` cobrindo validação das duas pernas + escrita"
      pattern: "with ORDER_LOCK:"
    - from: "server/tests/test_opcoes_collar_execucao.py"
      to: "abrir_collar"
      via: "inspect.getsource — prova que abrir_collar NÃO chama abrir_call_coberta/comprar_put_protecao"
      pattern: "inspect.getsource"
---

<objective>
Criar `store.abrir_collar` — o caminho de EXECUÇÃO de uma estrutura de duas
pernas que a Fase 16 deixou explicitamente pendente (`server/app/main.py:2451-2453`:
"O collar continua sem caminho de EXECUÇÃO até a Fase 17").

Purpose: hoje `store.abrir_call_coberta` e `store.comprar_put_protecao`
executam UMA perna por chamada. Encadeá-las não funciona nem no caminho feliz:
`abrir_call_coberta` incrementa `qtyTravada` em `qty`, e a chamada seguinte a
`comprar_put_protecao` vê `qty_livre` já reduzido e recusa por lastro
insuficiente — com o prêmio da call JÁ creditado e a posição vendida JÁ
gravada. O usuário ficaria com metade da trava protetora, exposto a um risco
DIFERENTE do apresentado na tela. O invariante "tudo-ou-nada" do CLAUDE.md
aplicado a nível de ESTRUTURA exige uma função própria.

Output: `store.abrir_collar` + `server/tests/test_opcoes_collar_execucao.py`.
Nenhuma rota é tocada neste plano (Plano 17-03 liga a rota).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-fluxo-de-aceite/17-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código real. Não explorar o repo. -->

server/app/store.py:
- `ORDER_LOCK = threading.RLock()` (linha 25) — trava única de ordem; reentrante,
  MAS reentrar não salva atomicidade: escrever dentro de uma chamada aninhada já
  persistiu estado antes da validação seguinte.
- `qty_livre(pos: dict) -> int` (linha 601) — ações livres (qty - qtyTravada).
- `registrar_rejeicao(conn, tipo, t, qty, price, motivo, user_id=None, origem="manual") -> dict`
  (linha 730) — `tipo` só aceita "COMPRA" | "VENDA"; grava entrada de history com
  `status: "rejeitada"`; NÃO toca cash/positions.
- `abrir_call_coberta(conn, contract, contratos, price, user_id=None, meta=None, origem="manual")`
  (linha 886) — modelo de referência: valida optionType=="call", posição no
  underlying, `qty_livre >= qty`; credita `qty*price`; grava optionPosition com
  `side:"vendida"`, `lastro:{"t","qty"}`, `stop:None`, `alvo:None`,
  `abertaEm: now_str()`, `ivEntrada`/`deltaEntrada`/`hv21Entrada` do `contract`;
  incrementa `pos_acao["qtyTravada"]`; insere history
  `{"date","type":"VENDA","kind":"opcao","acao":"abrir","t":cid,"underlying","qty","price","pnl":None,"origem"}`;
  4 `db.kv_set` no fim (optionPositions, positions, cash, history).
- `comprar_put_protecao(conn, contract, contratos, price, user_id=None, meta=None, origem="manual")`
  (linha 1021) — mesmo padrão, `side:"comprada"`, DEBITA `qty*price`, NUNCA toca
  `qtyTravada`, e checa `qty*price > cash` antes de escrever; 3 `db.kv_set`
  (optionPositions, cash, history — não escreve positions porque não trava).
- Merge de posição pré-existente (nos dois): `existing = next((p for p in opts if
  p["id"] == cid and p.get("side") == <side>), None)`; se existe, recalcula `avg`
  ponderado, soma `qty` e soma `lastro["qty"]`.
- `contract` recebido tem as chaves: `id` (contractSymbol), `underlying`,
  `optionType`, `strike`, `expiration`, `ivEntrada` (e opcionalmente
  `deltaEntrada`, `hv21Entrada`).

server/app/opcoes_lastreadas.py (`_propor_collar`, linha 41) — a proposta que
esta função executa: perna de CALL vendida (`lado: "venda"`) + perna de PUT
comprada (`lado: "compra"`), `contratos` inteiro, `qtyAcoes = contratos*100`,
`caixa.custoLiquidoUnitario` = `premio_put - premio_call` por ação (positivo =
débito, negativo/zero = crédito financiado pelo lastro).

server/tests/test_opcoes_lastreadas_store.py — padrão de teste da casa:
`_fresh_db()` com SQLite temporário por teste (`db.connect` + `store.ensure_defaults`),
sem conftest; helpers `_contract_call(...)` / `_contract_put(...)` devolvendo o
dict de `contract`; `store.buy(conn, "PETR4", 300, 38.0)` para semear lastro.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: store.abrir_collar — validação das duas pernas antes de qualquer escrita</name>
  <files>server/app/store.py, server/tests/test_opcoes_collar_execucao.py</files>
  <read_first>
    - server/app/store.py (linhas 1-40 para ORDER_LOCK/imports; 596-610 qty_livre; 725-765 registrar_rejeicao; 886-965 abrir_call_coberta; 1021-1107 comprar_put_protecao)
    - server/tests/test_opcoes_lastreadas_store.py (linhas 1-60 — fixtures e helpers a reusar)
    - server/app/opcoes_lastreadas.py (linhas 41-155 — `_propor_collar`, forma da proposta que esta função executa)
  </read_first>
  <behavior>
    - Caminho feliz: `abrir_collar` com lastro livre de EXATAMENTE `contratos*100`
      ações abre as DUAS pernas (a composição sequencial das funções single-leg
      falharia aqui — a call travaria o lote e a put recusaria por lastro).
    - Caixa: `cash` final == `cash_inicial + qty*premio_call - qty*premio_put`,
      arredondado a 2 casas.
    - `optionPositions` ganha 2 entradas: a da call com `side=="vendida"` e a da
      put com `side=="comprada"`, ambas com `lastro == {"t": underlying, "qty": qty}`.
    - `qtyTravada` da posição de ação é incrementado em `qty` UMA vez só (a perna
      da call trava; a perna da put não trava).
    - `history` ganha 2 entradas de abertura: uma `type=="VENDA"` com `t` ==
      contractSymbol da call e uma `type=="COMPRA"` com `t` == contractSymbol da put.
    - Rejeição por tipo de contrato trocado (call na posição da put, ou vice-versa):
      levanta `ValueError`, registra rejeição, e `cash`/`positions`/`optionPositions`
      ficam idênticos ao estado anterior.
    - Rejeição por lastro insuficiente (`qty_livre < qty`): mesmo contrato de
      "nada escrito".
    - Rejeição por caixa insuficiente no DÉBITO líquido (`qty*(premio_put -
      premio_call) > cash`): mesmo contrato de "nada escrito". Com crédito líquido
      (`premio_call >= premio_put`) não há porta de caixa — o lastro financia.
    - `contratos < 1`, prêmio não-numérico ou <= 0, `underlying` divergente entre as
      duas pernas, e `contract_call["id"] == contract_put["id"]` levantam `ValueError`
      sem escrever nada.
    - Reabertura sobre posição já existente do MESMO contrato faz merge de `avg`
      ponderado e soma de `qty`/`lastro["qty"]` nas duas pernas (mesma aritmética
      das funções single-leg).
  </behavior>
  <action>
Escrever primeiro `server/tests/test_opcoes_collar_execucao.py` cobrindo os itens
de `<behavior>` (ciclo RED), depois implementar (GREEN).

Implementação: acrescentar a `server/app/store.py`, IMEDIATAMENTE APÓS
`comprar_put_protecao` (que termina na linha 1106) e ANTES de
`liquidar_lastreada_vencida` (linha 1109), a função:

`abrir_collar(conn, contract_call: dict, contract_put: dict, contratos: int,
premio_call: float, premio_put: float, user_id=None, origem: str = "manual") -> None`

Ordem obrigatória dentro da função:
1. Fora da trava (mesmo lugar onde `abrir_call_coberta` faz): `contratos < 1` →
   `ValueError("Número de contratos precisa ser pelo menos 1.")`. Prêmios: cada um
   precisa ser `int`/`float` (recusar `bool`) e `> 0`, senão
   `ValueError("Prêmio inválido para a trava protetora.")`. `qty = int(contratos) * 100`.
2. `with ORDER_LOCK:` — TUDO daqui pra baixo dentro desta única aquisição.
3. Validações, nesta ordem, cada uma com `registrar_rejeicao` antes do `ValueError`
   quando o motivo é estado de carteira (tipo de contrato, lastro, caixa) — use
   `tipo="VENDA"` e o `id` da call para falhas da perna vendida, `tipo="COMPRA"` e o
   `id` da put para as demais:
   a. `contract_call.get("optionType") != "call"` → "Trava protetora exige um contrato de CALL na perna vendida."
   b. `contract_put.get("optionType") != "put"` → "Trava protetora exige um contrato de PUT na perna comprada."
   c. `contract_call.get("underlying") != contract_put.get("underlying")` → `ValueError("As duas pernas da trava protetora precisam ser do mesmo ativo-objeto.")` (sem `registrar_rejeicao`: é defeito de chamador, não estado de carteira).
   d. `contract_call.get("id") == contract_put.get("id")` → `ValueError("As duas pernas da trava protetora precisam ser contratos diferentes.")`.
   e. Sem posição no `underlying` → "Sem posição em {underlying} para lastrear a trava protetora."
   f. `qty_livre(pos_acao) < qty` → "Lastro insuficiente: {livre} ação(ões) livres de {underlying}, {qty} necessárias para {contratos} contrato(s) de trava protetora." — checagem ÚNICA: o mesmo lote lastreia as duas pernas (a call trava, a put registra o mesmo lote como lastro sem travar, exatamente como `comprar_put_protecao` faz hoje).
   g. `custo_liquido_total = round(qty * (premio_put - premio_call), 2)`; se
      `custo_liquido_total > cash` → "Caixa insuficiente para a trava protetora: custo líquido R$ {custo_liquido_total} — disponível: R$ {cash}." A porta de caixa é sobre o LÍQUIDO porque as duas pernas liquidam no mesmo instante atômico; cobrar o prêmio bruto da put recusaria estruturas que de fato cabem.
4. Só depois de TODAS as validações passarem: montar/mergear as duas entradas de
   `optionPositions` (schema byte a byte igual ao de `abrir_call_coberta` para a call
   e ao de `comprar_put_protecao` para a put, incluindo `stop:None`, `alvo:None`,
   `abertaEm: now_str()`, `ivEntrada`/`deltaEntrada`/`hv21Entrada` lidos do respectivo
   `contract`), aplicar `cash = round(cash + qty*premio_call - qty*premio_put, 2)`,
   `pos_acao["qtyTravada"] = int(pos_acao.get("qtyTravada") or 0) + qty` (uma vez só),
   e inserir as duas entradas de `history` (a da call com `type:"VENDA"`, a da put com
   `type:"COMPRA"`, ambas `kind:"opcao"`, `acao:"abrir"`, `pnl:None`, `origem=origem`).
5. Escrita: exatamente quatro `db.kv_set` no fim — `optionPositions`, `positions`,
   `cash`, `history`. Nenhum `db.kv_set` antes disso.

PROIBIDO: chamar `abrir_call_coberta` ou `comprar_put_protecao` de dentro de
`abrir_collar`. `ORDER_LOCK` é reentrante, então a composição não travaria — mas
`abrir_call_coberta` já teria persistido caixa e posição antes de a validação da put
rodar. Escrever o docstring nomeando esta proibição e a razão.

NÃO adicionar campo novo às entradas de `history` (a estrutura é registrada como duas
ordens porque `history` é livro de ORDENS; a identidade da estrutura vive na proposta).
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/test_opcoes_collar_execucao.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def abrir_collar" server/app/store.py` == 1
    - `grep -n "with ORDER_LOCK" server/app/store.py` mostra exatamente UMA ocorrência dentro do corpo de `abrir_collar`
    - `cd server && .venv/bin/python -m pytest tests/test_opcoes_collar_execucao.py -q` passa com >= 9 testes
    - `cd server && .venv/bin/python -m pytest tests/test_opcoes_lastreadas_store.py tests/test_opcoes_lastreadas_vencimento.py -q` continua verde (nenhuma regressão nas funções single-leg)
    - Teste com `qty_livre` == `contratos*100` exato passa (prova que a composição sequencial não é o que foi implementado)
  </acceptance_criteria>
  <done>`store.abrir_collar` existe, abre as duas pernas juntas e não escreve nada quando qualquer perna é inválida.</done>
</task>

<task type="auto">
  <name>Task 2: Guardiões de atomicidade estrutural e de não-composição</name>
  <files>server/tests/test_opcoes_collar_execucao.py</files>
  <read_first>
    - server/app/store.py (a função `abrir_collar` escrita na Task 1)
    - server/tests/test_opcoes_collar_execucao.py (arquivo da Task 1)
    - server/tests/test_opcoes_collar_vocab.py (padrão de guardião estrutural por leitura de fonte já usado na Fase 16)
  </read_first>
  <action>
Acrescentar a `server/tests/test_opcoes_collar_execucao.py` três guardiões que medem
ESTRUTURA, não só comportamento:

1. `test_abrir_collar_nao_compoe_as_funcoes_single_leg`: usa `inspect.getsource(store.abrir_collar)`
   e afirma que o texto NÃO contém `"abrir_call_coberta("` nem `"comprar_put_protecao("`.
   Docstring explicando: `ORDER_LOCK` é `RLock`, logo a composição aninhada não deadlocka
   — ela grava caixa/posição da primeira perna antes de a segunda ser validada, que é
   exatamente a execução de meia estrutura que esta fase existe para impedir.

2. `test_abrir_collar_valida_tudo_antes_de_qualquer_kv_set`: `inspect.getsource` de
   `abrir_collar`; afirma que o índice da PRIMEIRA ocorrência de `"db.kv_set("` é maior
   que o índice da ÚLTIMA ocorrência de `"raise ValueError("`. Guardião de ordem de
   código — pega o dia em que alguém mover uma validação para depois de uma escrita.

3. `test_abrir_collar_rejeicao_de_caixa_nao_move_nada`: cenário completo com espião —
   semeia posição, fixa `cash` abaixo do débito líquido, chama `abrir_collar` esperando
   `ValueError`, e afirma que `store.get(conn,"cash")`, `store.get(conn,"positions")` e
   `store.get(conn,"optionPositions")` são iguais aos snapshots tirados ANTES da chamada
   (comparação de igualdade do objeto inteiro, não campo a campo) — e que existe
   exatamente uma entrada de `history` com `status == "rejeitada"`.

Cabeçalho do arquivo (docstring de módulo) precisa registrar: qual invariante do
CLAUDE.md estes guardiões protegem ("execução tudo-ou-nada por desenho", aplicado a
nível de ESTRUTURA) e que apagar/afrouxar qualquer um deles exige nota explícita
(guardrail "guardiões de teste não se apagam").
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/test_opcoes_collar_execucao.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "inspect.getsource" server/tests/test_opcoes_collar_execucao.py` >= 2
    - Os 3 testes nomeados na ação existem e passam
    - Injeção de falha registrada no SUMMARY: trocando temporariamente o corpo de `abrir_collar` por chamadas encadeadas a `abrir_call_coberta`/`comprar_put_protecao`, pelo menos 2 testes reprovam; após `git checkout -- server/app/store.py`, todos passam
    - `bash scripts/executar.sh --testes` verde (suíte canônica completa: pytest + web/tests/*.mjs)
  </acceptance_criteria>
  <done>A suíte falha se `abrir_collar` voltar a compor as funções single-leg ou se qualquer escrita anteceder uma validação.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rota HTTP → `store.abrir_collar` | `contract_call`/`contract_put`/prêmios chegam de fora de `store.py`; esta camada não pode assumir que o chamador validou |
| requisições concorrentes → estado kv | duas aberturas simultâneas sobre o mesmo lastro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | `abrir_collar` — escrita parcial | mitigate | Todas as validações das duas pernas dentro de UMA aquisição de `ORDER_LOCK`, antes de qualquer `db.kv_set`; guardião de ordem de código (Task 2, guardião 2) |
| T-17-02 | Tampering | composição de funções single-leg | mitigate | Guardião por `inspect.getsource` proibindo `abrir_call_coberta(`/`comprar_put_protecao(` no corpo (Task 2, guardião 1) |
| T-17-03 | Elevation of Privilege | perna de tipo trocado (put onde se espera call) | mitigate | Validação explícita de `optionType` das DUAS pernas + `underlying` idêntico + `id` distinto, antes de qualquer escrita |
| T-17-04 | Information Disclosure / Repudiation | tentativa recusada some em silêncio | mitigate | `registrar_rejeicao` em todas as portas de estado de carteira (tipo, lastro, caixa), com motivo nomeado |
| T-17-05 | Denial of Service | lost update entre duas aberturas concorrentes sobre o mesmo lastro | mitigate | Leitura de `positions`/`cash` DENTRO da mesma aquisição de `ORDER_LOCK` da escrita — mesmo padrão de T-14-06 |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhuma dependência nova neste plano — `git diff --stat server/requirements.txt server/requirements-prod.txt` precisa sair vazio |
</threat_model>

<verification>
- `cd server && .venv/bin/python -m pytest tests/test_opcoes_collar_execucao.py -q` verde.
- `bash scripts/executar.sh --testes` verde (suíte canônica; `scripts/test.sh` sozinho NÃO conta).
- `git status --porcelain web/` vazio — este plano é backend-only.
- `git diff --stat server/requirements.txt server/requirements-prod.txt` vazio.
- Nenhuma alteração em `server/app/main.py` (a rota é do Plano 17-03).
</verification>

<success_criteria>
- A suíte prova, por comportamento, que uma perna inválida não move caixa, posições nem optionPositions.
- A suíte prova que `qty_livre == contratos*100` é suficiente para o collar inteiro.
- A suíte falha se `abrir_collar` for reescrito como composição das funções single-leg.
- `abrir_call_coberta`/`comprar_put_protecao` continuam intocadas (diff só acrescenta função nova).
</success_criteria>

<output>
Create `.planning/phases/17-fluxo-de-aceite/17-01-SUMMARY.md` when done.
Registrar para o Plano 17-03: assinatura exata de `abrir_collar` e a lista de
`ValueError` que a rota precisa converter em 400.
</output>
