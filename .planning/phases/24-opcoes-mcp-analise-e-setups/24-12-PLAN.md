---
phase: 24-opcoes-mcp-analise-e-setups
plan: 12
type: execute
wave: 6
depends_on: ["24-11"]
gap_closure: true
files_modified:
  - server/app/options_mcp_api.py
  - server/tests/test_options_mcp_api.py
  - web/src/opcoes/OpcoesScreen.jsx
  - web/src/copy.js
  - web/tests/test_opcoes_mcp_aba_ui.mjs
autonomous: true
requirements: [achado ao vivo 2026-09-11 — "dado em dia" com dois pregões faltando]

must_haves:
  truths:
    - "A tela diz quantos pregões separam o dado exibido do último pregão fechado"
    - "Com pregão faltando, o chip para de dizer 'dado em dia' — a afirmação passa a ser a distância real"
    - "O dia corrente nunca conta como atraso: o dado dele só existe depois do fechamento"
    - "Nada é bloqueado por causa disto — o carimbo informa, quem bloqueia continua sendo o frescor do serviço"
  artifacts:
    - path: "server/app/options_mcp_api.py"
      provides: "_atraso_em_pregoes — distância em pregões, pelo calendário da B3"
      contains: "def _atraso_em_pregoes"
  key_links:
    - from: "server/app/options_mcp_api.py"
      to: "pregao.is_trading_day"
      via: "contagem de pregões entre o dado e hoje"
      pattern: "is_trading_day"
---

<objective>
Achado ao vivo em 2026-09-11, medido com `scripts/diagnostico-leitura-opcoes.sh`:
a aba mostrava "pregão de 2026-09-08" numa **sexta, 11/09**, com o chip dizendo
**"dado em dia"**. O serviço estava coerente com o próprio contrato —
`negociacao_b3` com idade de 49,58 h contra um SLA de 96 h — mas dois pregões
(quarta 09 e quinta 10) não estavam na base.

Purpose: repassar o veredito do fornecedor como se fosse nosso faz o app
afirmar "em dia" sobre dado de dois pregões atrás. O princípio 3 do
`CLAUDE.md` pede o oposto: dizer se o dado é de tempo real, atrasado ou
histórico. O Boris tem calendário de pregão próprio (`server/app/pregao.py`,
com feriados fixos, móveis e as exceções por ofício da B3) e pode afirmar a
distância REAL em vez de herdar um SLA de quatro dias.

Output: helper puro que conta pregões entre o dado e hoje, o campo no envelope
e o chip da tela dizendo a distância quando ela existe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-opcoes-mcp-analise-e-setups/24-CONTEXT.md
@.planning/phases/24-opcoes-mcp-analise-e-setups/24-11-SUMMARY.md
@CLAUDE.md

<o_que_ja_existe>
`server/app/pregao.py` — NÃO reimplementar nada disto:
- `feriados(ano) -> set` — fixos + móveis (Páscoa, Carnaval, Sexta-feira
  Santa, Corpus Christi) + exceções por ofício + extras de env;
- `is_holiday(d: date) -> bool`;
- `is_trading_day(d: date = None) -> bool` — dia útil E não feriado;
- `BRT` como fuso do módulo.

Medido no dia do achado: 2026-09-07 foi **segunda e feriado** (Independência),
e é por isso que o `last_failure` do MyData mostra um 404 do
`COTAHIST_D07092026.ZIP` — a B3 não publica arquivo de dia sem pregão. Esse
erro é esperado e NÃO entra nesta conta.
</o_que_ja_existe>

<guardrails>
- **O dia corrente NUNCA conta como atraso.** O COTAHIST de um pregão sai à
  noite; contar hoje faria o app acusar atraso a manhã inteira, todo dia. A
  contagem é dos pregões ESTRITAMENTE entre o `trading_date` e hoje.
- **Isto NÃO bloqueia nada.** Quem decide bloquear veredito e criação de setup
  continua sendo o frescor do serviço (ADR-027, Decisão 8). Mudar o critério
  de bloqueio seria outra decisão, e não é esta.
- **Não reimplementar calendário.** `pregao.is_trading_day` é a fonte única —
  ele já conhece Carnaval, Corpus Christi e as exceções por ofício.
- Data inválida, ausente ou no futuro → `pregoes: None`, e a tela volta ao
  comportamento de hoje. Nunca 0 por falta de informação: 0 significa "está no
  último pregão fechado", que é uma afirmação forte.
- Texto novo nas DUAS vozes de `copy.js`, chaves idênticas; `didatica-boris`
  obrigatória.
- Nenhuma chamada de tool nova: o custo declarado das rotas não muda.
- **Não chamar os mutadores de estado do `gsd-sdk`** (`CLAUDE.md`).
</guardrails>
</context>

<tasks>

<task type="auto">
  <name>Task 1: _atraso_em_pregoes no backend</name>
  <read_first>
    - server/app/pregao.py (INTEIRO — `is_trading_day`, `feriados`, `BRT`)
    - server/app/options_mcp_api.py (helpers puros e o envelope de `/status` e `/leitura`)
  </read_first>
  <files>server/app/options_mcp_api.py</files>
  <action>
```python
def _atraso_em_pregoes(data_do_dado, _hoje=None) -> dict:
    """Quantos pregões separam o dado exibido do último pregão fechado.

    Existe porque o SLA da fonte e a pergunta do usuário são coisas
    diferentes. Medido em 2026-09-11: `negociacao_b3` com 49,58 h de idade
    contra um SLA de 96 h é `em_dia` pelo contrato do fornecedor, e ainda
    assim faltavam DOIS pregões na base — a tela dizia "dado em dia" sobre
    uma cotação de terça, numa sexta. Repassar o veredito de quem publica,
    em vez de medir a distância, é o que produz esse tipo de afirmação.

    **O dia corrente não entra na conta**, e isso não é conservadorismo: o
    COTAHIST de um pregão só sai depois do fechamento, então contar hoje
    faria o app acusar atraso todas as manhãs, sobre um dado que ainda não
    poderia existir.

    `pregoes: None` para data ausente, malformada ou no futuro — nunca 0,
    que afirmaria "está no último pregão" sem ter como saber.
    """
```

Corpo: parse ISO de `data_do_dado` (`date.fromisoformat`, `ValueError`/
`TypeError` → `None`); `hoje = (_hoje or datetime.now(BRT).date())`; data
posterior a hoje → `None`; conta
`sum(1 for d in <dias entre data+1 e hoje-1 inclusive> if pregao.is_trading_day(d))`.

Retorna `{"pregoes": int|None, "referencia": hoje.isoformat(), "fonte": "calendario_b3"}`.
`fonte` é o que permite à tela dizer de onde vem a contagem sem raspar texto.

`_hoje` existe só para o teste fixar o relógio — mesmo padrão de `_dia_sp`.

Import: `from . import pregao` no topo (módulo puro, sem I/O, sem ciclo).

**Onde entra:** `/status` e `/leitura` ganham `"atraso": _atraso_em_pregoes(<o
mesmo valor que já vai em `pregao`>)`. As duas já calculam esse valor; reusar a
variável, não recalcular. Nenhuma chamada de tool nova.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/server && .venv/bin/python -m pytest tests/test_options_mcp_api.py tests/test_options_mcp_leitura.py tests/test_mcp_guardioes.py -q</automated>
  </verify>
  <acceptance_criteria>
    - O caso do achado, com relógio fixo: dado `2026-09-08`, hoje `2026-09-11` → `pregoes == 2` (quarta 09 e quinta 10).
    - Dado de ontem (`2026-09-10`, hoje `2026-09-11`) → `pregoes == 0`: hoje não conta.
    - Fim de semana não infla: dado `2026-09-11` (sexta), hoje `2026-09-14` (segunda) → `pregoes == 0` (sábado e domingo não são pregão, e segunda é hoje).
    - Feriado não infla: um intervalo que contenha `2026-09-07` (Independência, segunda) não o conta.
    - Data ausente, `"banana"` e data futura → `pregoes is None`.
    - `atraso` presente em `/status` e `/leitura`; custo do `_cap_check` inalterado nas duas.
  </acceptance_criteria>
  <done>O backend afirma a distância real em pregões, pelo calendário da B3, sem contar o dia corrente.</done>
</task>

<task type="auto">
  <name>Task 2: o chip da tela diz a distância</name>
  <read_first>
    - web/src/opcoes/OpcoesScreen.jsx (o bloco `let chip = ...` e as três variantes de frescor; o cabeçalho com "Pregão: …")
    - web/src/copy.js (`opcoesFrescorEmDia`, `opcoesFrescorAtrasado`, `opcoesFrescorNaoMedido`)
  </read_first>
  <files>web/src/opcoes/OpcoesScreen.jsx, web/src/copy.js</files>
  <action>
O chip ganha uma QUARTA variante, com precedência sobre "dado em dia":

```
atraso.pregoes >= 1   → cp.opcoesAtrasoPregoes(n)     ex.: "2 pregões atrás"
atraso.pregoes === 0  → comportamento de hoje (em dia / atrasado / não medido)
atraso.pregoes == null→ comportamento de hoje
```

A precedência é o ponto do plano: com pregão faltando, "dado em dia" é
afirmação falsa mesmo quando o fornecedor a assina. O que o chip diz passa a
ser o que se pode medir — a distância.

Quando o frescor do serviço TAMBÉM bloqueia (`frescor.bloqueia`), o chip
mantém o tom de alerta que já tem hoje e acrescenta a distância; as duas
informações não competem, dizem coisas diferentes (idade da carga × pregões
faltando).

Chaves novas nos DOIS modos:
- `opcoesAtrasoPregoes: (n) => ...` — "1 pregão atrás" / "N pregões atrás",
  com o singular correto;
- `opcoesAtrasoAjuda` — uma frase explicando que a contagem é de pregões
  fechados pelo calendário da B3 e que o pregão de hoje só entra depois de
  publicado. Vai no rodapé do cabeçalho, no mesmo tom discreto do resto.

Nada de verde/vermelho no chip por causa disto: é informação de data, não P&L.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/web && npx vite build && node tests/test_opcoes_mcp_aba_ui.mjs && node tests/test_opcoes_analisar_ui.mjs && node tests/test_copy_theme.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx vite build` verde.
    - Com `atraso.pregoes >= 1`, o fonte NÃO permite que `opcoesFrescorEmDia` seja escolhido: a checagem do atraso vem ANTES na cadeia.
    - Singular correto em 1.
    - Chaves nos dois modos, conjunto `opcoes*` idêntico.
    - `atraso` ausente → a tela se comporta exatamente como antes (nenhuma regressão nas três variantes existentes).
  </acceptance_criteria>
  <done>Quem olha a tela vê "2 pregões atrás" onde antes lia "dado em dia".</done>
</task>

<task type="auto">
  <name>Task 3: guardiões</name>
  <read_first>
    - web/tests/test_opcoes_mcp_aba_ui.mjs (as asserções do chip de frescor nas três variantes — elas NÃO podem ser afrouxadas)
    - server/tests/test_options_mcp_api.py
  </read_first>
  <files>server/tests/test_options_mcp_api.py, web/tests/test_opcoes_mcp_aba_ui.mjs</files>
  <action>
**Backend:** os seis casos do aceite da Task 1, todos com relógio fixo por
`_hoje` (nenhum teste que dependa da data em que a suíte roda — teste que muda
de resultado em novembro é pior que teste nenhum).

**Front:** o guardião das três variantes do chip ganha a quarta, e mais duas
asserções que travam a regra central:
1. a checagem de `atraso.pregoes` aparece no fonte ANTES da que escolhe
   `opcoesFrescorEmDia`;
2. sanidade: a regex detecta a ordem invertida numa string de teste.

As asserções existentes do chip continuam valendo byte a byte — se alguma
precisar mudar de expectativa, a mudança vem COM nota explicando a reversão
deliberada (guardião não se apaga).
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2 && bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - Testes novos provados RED antes da correção; prova no SUMMARY.
    - Nenhum teste novo depende da data de execução.
    - Suíte canônica sem regressão sobre a baseline que o 24-11 deixar.
  </acceptance_criteria>
  <done>A regra "distância medida vence SLA herdado" está travada nos dois lados.</done>
</task>

</tasks>

<verification>
1. `bash scripts/executar.sh --testes` FORA do sandbox (`dangerouslyDisableSandbox: true`).
2. `cd web && npx vite build`.
3. Saída colada no SUMMARY.
</verification>

<success_criteria>
- "2 pregões atrás" no lugar de "dado em dia", no caso medido.
- Dia corrente nunca conta; fim de semana e feriado não inflam.
- Nada passou a ser bloqueado por causa disto.
- Calendário vem de `pregao.py`, sem segunda implementação.
</success_criteria>

<output>
Create `.planning/phases/24-opcoes-mcp-analise-e-setups/24-12-SUMMARY.md` when done
</output>
