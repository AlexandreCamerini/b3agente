---
phase: 24-opcoes-mcp-analise-e-setups
plan: 03
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - server/app/mcp_client.py
  - server/app/options_mcp_api.py
  - server/app/main.py
  - server/app/rbac.py
  - server/tests/test_opcoes_dsl.py
  - server/tests/test_guardrail_imperativo.py
  - server/tests/test_adr013_rbac.py
autonomous: true
requirements: [PLANO Fase 5 — backend]

must_haves:
  truths:
    - "Uma descrição em português vira um setup declarativo validado pelo próprio serviço, sem vocabulário copiado para dentro do Boris"
    - "Nada é gravado sem o usuário ver a interpretação e o backtest primeiro"
    - "Só quem tem a permissão opcoes.criar_setup cria, confirma ou desativa — e a recusa é do backend, não do botão escondido"
    - "Os problemas de validação voltam item a item, do jeito que o serviço os descreveu"
    - "Criar e desativar setup ficam no log de auditoria"
  artifacts:
    - path: "server/app/mcp_client.py"
      provides: "list_tools() — tools/list é protocolo, não conta no teto"
      contains: "async def list_tools"
    - path: "server/app/options_mcp_api.py"
      provides: "rotas /setups/compilar, /setups/confirmar, /setups/{name}/desativar e o compilador NL->DSL"
      contains: "@router.post(\"/setups/compilar\")"
    - path: "server/app/rbac.py"
      provides: "entidade de auditoria da escrita de setup"
      contains: "opcoes.criar_setup"
    - path: "server/tests/test_opcoes_dsl.py"
      provides: "compilação, dry-run, confirmação, permissão e frescor bloqueante"
      contains: "def test_"
  key_links:
    - from: "server/app/options_mcp_api.py (compilar)"
      to: "inputSchema de create_setup + resource mydata://tools/create_setup"
      via: "system montado em RUNTIME"
      pattern: "mydata://tools/create_setup"
    - from: "server/app/options_mcp_api.py (confirmar/desativar)"
      to: "audit.record"
      via: "entidade opcoes_setup"
      pattern: "audit.record"
---

<objective>
Fase 5 do `docs/PLANO-aba-opcoes.md`: criar setup técnico por descrição em
português, com ensaio antes de gravar e entitlement de verdade.

Purpose: hoje os setups que a aba mostra foram criados FORA do Boris, por
algum cliente MCP chamando `create_setup` direto. A aba só lê. Esta fase traz
a criação para dentro — e é a razão de o Alex ter pedido a Fase 3 antes: sem
cadeia e vencimentos na tela, escrever um setup é escrever no escuro.

Output: `list_tools` no cliente; três rotas gateadas por permissão; o
compilador cujo `system` é montado em runtime do schema do serviço; auditoria;
testes. **A Fase 4 (veredito) não entra** — mas a fiação de LLM que ela usaria
nasce aqui (D-24.5 do CONTEXT), pronta para ser reusada, não duplicada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-opcoes-mcp-analise-e-setups/24-CONTEXT.md
@.planning/phases/24-opcoes-mcp-analise-e-setups/24-01-SUMMARY.md
@CLAUDE.md

<interfaces>
O 24-CONTEXT.md traz o schema de `Setup`, o shape de `create_setup`
(dry-run/confirm/problems), o de `deactivate_setup` e o do `backtest`. Não
redescobrir.

De `server/app/main.py` (a ler antes de escrever):
- `require_permission(perm)` (linha ~159) — factory de dependency que faz
  `ensure_bootstrap_role` e levanta 403 com o nome da permissão.
- `_gate_analise(scope, config, custo=1)` (linha ~507) — ponto ÚNICO de gate
  de análise: plano mensal (402) + IA gerenciada; devolve `(config, consume)`,
  e `consume()` é chamado só no sucesso.
- `options_mcp_api.configure(_conn, require_user)` (linha ~181) — a fiação a
  estender.

De `server/app/llm.py`:
- `resolve_key(config) -> str`
- `async _call_llm(config, key, system, user, max_tokens) -> str` (devolve
  TEXTO; levanta `LLMUserError` com `code` para chave/modelo ausente)
- `_parse_json_loose(raw) -> obj | None` (tolerante a cercas)

De `server/app/ai_activity.py`: `registrar_uso(conn, *, scope, ticker, tipo,
usos)` — o padrão usado em `main.py:1837` e irmãos.

De `server/app/audit.py`: `record(conn, actor_id, entity, entity_id, field,
old_value, new_value)`.

De `server/app/rbac.py`: `GRUPOS["opcoes"] = {"opcoes.criar_setup"}` já existe
(Fase 1); `ENTIDADES_POR_PERMISSAO` tem a nota dizendo que a entrada de
`opcoes.criar_setup` entra "na Fase 5, junto com a rota de escrita que a
gravará" — é agora.
</interfaces>

<guardrails>
- **`with _cap_check(uid, n) as cap:` em TODA rota nova** (guardião AST IV).
  Custos: compilar 1 (`create_setup(confirm=false)`), confirmar 1, desativar 1.
  `tools/list` e `resources/read` são protocolo e **não** contam — nem no teto
  do serviço, nem no cap.
- **ENG-06 continua verde: nenhuma DSL portada.** Proibido criar
  `opcoes_dsl.py`, proibido listar indicadores, operadores ou padrões como
  constante no Boris. O `system` do compilador é montado EM RUNTIME do
  `inputSchema` de `create_setup` (via `tools/list`) e do resource
  `mydata://tools/create_setup`. `test_opcoes_gatilho.py` guarda isso.
- **Validação local é só de FORMA.** É objeto? tem `name`, `ticker`,
  `description`, `conditions` (lista não vazia)? Ponto. A semântica quem
  valida é `create_setup(confirm=false)`, e os `problems` voltam item a item,
  verbatim.
- **`description` = as palavras ORIGINAIS do usuário.** Se a LLM reescrever,
  o backend restaura o texto que a pessoa digitou antes de mandar ao serviço.
  É o campo que prova o que ela pediu.
- **Nada é gravado sem dry-run visto.** `/setups/confirmar` recebe o setup que
  o usuário viu; não recompila, não chama LLM.
- **Frescor bloqueante (ADR-027, Decisão 8):** `negociacao_b3` fora de
  `em_dia`, `warning` presente ou erro da tool → **409** `dado_atrasado` com
  `idadeHoras/slaHoras` ou `motivo: "nao_medido"`. Criar setup sobre dado que
  ninguém mediu é fabricar confiança.
- **Dois 402 distintos:** `plano_analises`/`ia_gerenciada` (gate de análise, de
  `_gate_analise`) e `mcp_cota` (cap da aba). Textos próprios da aba — nunca o
  copy de BYOK do `metering`.
- **Sem literal de segredo, sem `"fixture"`, sem URL fora do contrato**
  (guardiões i, ii, iii). O `mcp_client` continua o ÚNICO importador de
  `mcp`/`httpx2`.
- **Sem dependência nova.**
</guardrails>
</context>

<tasks>

<task type="auto">
  <name>Task 1: list_tools no cliente e a fiação de permissão e LLM no configure</name>
  <read_first>
    - server/app/mcp_client.py linhas 584-682 (`call_tool`/`get_prompt`/`read_resource`: o padrão de cache, obslog e `_chamar`)
    - server/app/options_mcp_api.py linhas 60-100 (`configure` e `require_user`)
    - server/app/main.py linhas 155-185 (`require_permission` e a chamada de `configure`)
  </read_first>
  <files>server/app/mcp_client.py, server/app/options_mcp_api.py, server/app/main.py</files>
  <action>
**1. `mcp_client.list_tools()`** — mesma forma de `get_prompt`/`read_resource`,
com `TTL_ESTATICO_S` (1 h) e o comentário dizendo por que é de graça:
`tools/list` é protocolo (como `initialize` e `ping`), não `tools/call`, então
não conta no teto de 2.000/dia do serviço nem no cap por usuário.

```python
async def list_tools() -> ResultadoTool:
    """`tools/list`. NÃO conta no teto do serviço (é protocolo), então também
    não entra no cap por usuário. Existe para a Fase 5 montar o `system` do
    compilador a partir do `inputSchema` REAL de `create_setup` — copiar o
    vocabulário para dentro do Boris é exatamente o que o ENG-06 proíbe."""
```

Executor: `await sessao.list_tools()`. Cachear o objeto do SDK, como
`get_prompt` faz.

**2. `configure()` estendido** — assinatura nova, com os extras OPCIONAIS e
default `None` (para não quebrar nenhum chamador de teste existente):

```python
def configure(conn, require_user_dep, *, require_permission_dep=None,
              gate_analise=None, config_do_usuario=None) -> None:
```

- `require_permission_dep`: a factory `require_permission` de `main.py`.
- `gate_analise`: `_gate_analise` de `main.py`, chamado como
  `gate_analise(uid, config)` → `(config_efetiva, consume)`.
- `config_do_usuario`: `lambda uid: store.get(_conn, "config", user_id=uid)`.

Os três nascem em `main.py` e vir daqui seria import circular — a mesma razão
já documentada para `require_user`. `main.py:181` passa a chamar
`options_mcp_api.configure(_conn, require_user,
require_permission_dep=require_permission, gate_analise=_gate_analise,
config_do_usuario=lambda uid: store.get(_conn, "config", user_id=uid))`.

**3. `require_criar_setup()`** em `options_mcp_api.py` — delegação local, pelo
MESMO motivo documentado em `require_user` (o guardião de cobertura de rotas
reconhece o nome como gate):

```python
def require_criar_setup(authorization: Optional[str] = Header(default=None)) -> dict:
    """Sessão + `opcoes.criar_setup` (ADR-013, grupo `opcoes`). Delegação ao
    `require_permission` de main.py, injetado por `configure()`. Sem a
    fiação, 503 `mcp_nao_configurado` — nunca um 200 permissivo: falhar
    ABERTO numa rota de escrita é pior que não ter a rota."""
```

Implementação: `_require_permission("opcoes.criar_setup")` produz a dependency;
como `Depends` já foi resolvido, chamar a função interna com o header — mesmo
padrão do `require_user` local. Se `_require_permission` for `None`, levantar
503 `mcp_nao_configurado`.

**4. `rbac.ENTIDADES_POR_PERMISSAO`** ganha
`"opcoes.criar_setup": {"opcoes_setup"}`, substituindo a nota que dizia "entra
na Fase 5" por uma linha registrando que entrou e por quê (o contrato do mapa
é "cobre toda `entity` HOJE gravada", e a partir deste plano `opcoes_setup` é
gravada). Atualizar `test_adr013_rbac.py` se ele contar entradas do mapa.
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/test_mcp_client.py tests/test_mcp_guardioes.py tests/test_adr013_rbac.py tests/test_adr013_cobertura_rotas.py tests/test_opcoes_fronteira.py tests/test_opcoes_gatilho.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "async def list_tools" server/app/mcp_client.py` retorna 1, e a função usa `TTL_ESTATICO_S`.
    - `grep -n "require_permission_dep\|gate_analise\|config_do_usuario" server/app/main.py` mostra os três passados na chamada de `configure`.
    - `grep -n "opcoes_setup" server/app/rbac.py` retorna a entrada nova.
    - `python -m pytest tests/test_adr013_rbac.py tests/test_adr013_cobertura_rotas.py -q` verde.
    - `python -m pytest tests/test_opcoes_fronteira.py -q` verde — `mcp`/`httpx2` seguem importados só por `mcp_client`.
    - Chamar `configure(conn, require_user)` sem os kwargs continua funcionando (compatibilidade dos testes existentes).
  </acceptance_criteria>
  <done>`tools/list` disponível e de graça; permissão, gate de LLM e leitura de config injetados sem import circular; auditoria de setup declarada no RBAC.</done>
</task>

<task type="auto">
  <name>Task 2: o compilador NL→DSL e as três rotas de setup</name>
  <read_first>
    - server/app/options_mcp_api.py — tudo que os planos 24-01 e este já acrescentaram
    - server/app/llm.py linhas 488-513 (`_call_llm`) e 852-881 (`_parse_json_loose`)
    - server/app/assistente.py em volta da linha 296 (padrão de `resolve_key` + `_call_llm` + tratamento de erro)
  </read_first>
  <files>server/app/options_mcp_api.py</files>
  <action>
**1. `_schema_do_create_setup()` — o `system` montado em RUNTIME.**

```python
async def _material_do_compilador() -> tuple:
    """Schema vivo + texto para humanos de `create_setup`, as DUAS fontes do
    `system` do compilador. Montar em runtime é o que impede o vocabulário
    (indicadores, operadores, padrões) de virar constante dentro do Boris —
    cópia que envelhece em silêncio quando o serviço acrescenta um indicador,
    e é exatamente o que o ENG-06 proíbe.

    `tools/list` e `resources/read` são protocolo: de graça no teto e fora do
    cap. Cache de 1 h no `mcp_client`."""
```

Retorna `(input_schema: dict, texto_humano: str)`. `input_schema` sai da
entrada de nome `create_setup` em `list_tools()`; `texto_humano`, de
`read_resource("mydata://tools/create_setup")`. Qualquer um dos dois ausente
→ `McpErroDeTool` tratado por `_erro_http` (422): compilar sem o contrato
vivo seria compilar de memória.

O `system` é composto assim (função pura `_system_compilador(schema, texto)`,
testável sem rede):
- uma instrução curta do Boris dizendo o papel ("você compila a descrição do
  usuário para o objeto abaixo; não inventa campo; não reescreve a
  descrição; responde só o JSON");
- o `inputSchema` serializado;
- o texto do resource, verbatim.

Este texto do Boris é fixo → entra em `FONTES` do
`test_guardrail_imperativo.py` (Task 3), como constante de módulo
`SYSTEM_COMPILADOR_CABECALHO`.

**2. `POST /setups/compilar` — `Depends(require_criar_setup)`, custo 1 + LLM.**

Corpo: `{descricao, ticker}`. `descricao` não vazia (422
`descricao_ausente`); `ticker` obrigatório (422) — o serviço exige `ticker` no
setup e adivinhá-lo do texto seria inventar.

Ordem das etapas, e ela importa:
1. **Frescor bloqueante ANTES de tudo** (Decisão 8): `check_data_freshness`
   (cache 600 s, custo 1 se tocar a rede — reservar 2 no total desta rota) →
   `_frescor(sc, erro)`; `bloqueia` → **409** `{"code": "dado_atrasado",
   "message": ..., "idadeHoras"|"motivo": "nao_medido", "slaHoras": ...}`.
   Nada de LLM e nada de gravação sobre dado que ninguém mediu.
2. **Gate de análise** (`gate_analise(uid, config)`) → 402 `plano_analises`
   ou `ia_gerenciada` conforme o `reason`, com texto da aba. Converter a
   `HTTPException` do gate: ela vem com o copy de BYOK do metering, que aqui
   mandaria a pessoa configurar uma chave que não resolve nada (§3.2).
3. `_material_do_compilador()` (de graça).
4. `llm._call_llm(config, key, system, user, max_tokens)`; `user` = a
   descrição + o ticker. `LLMUserError` com `code` (`missing_key`,
   `missing_model`) → 400 com `code` e `action` preservados (o front já sabe
   renderizar "Como corrigir:").
5. `llm._parse_json_loose(texto)` → `None` ou não-dict → **422**
   `{"code": "compilacao_invalida", "message": ..., "cru": texto}` — o texto
   cru vai junto, rotulado, para a pessoa ver o que a IA respondeu. Nunca um
   setup fabricado para "salvar" a resposta.
6. **Validação de FORMA apenas**: `name`, `ticker`, `description`,
   `conditions` (lista não vazia). Faltou → 422 `forma_invalida` com a lista
   dos campos ausentes.
7. `description` é SOBRESCRITA com a descrição original do usuário, sempre.
   Comentar a razão: é o campo que prova o que foi pedido, e a paráfrase da
   LLM apagaria isso.
8. `ticker` do corpo vence o que a LLM escreveu (mesma razão).
9. `create_setup` com `{"setup": setup, "confirm": False}` →
   `_chamada_com_cap(cap, ...)`.
   - `{"error", "problems"}` vem como `McpErroDeTool` (o `mcp_client` levanta
     ao ver `error`): capturar ESTE caso especificamente e responder **422**
     `{"code": "setup_invalido", "message": <error>, "problems": [...]}`,
     com os problemas item a item, verbatim. Reescrevê-los tiraria a única
     informação acionável que a pessoa tem.
   - sucesso → **200** `{"status": "dry_run", "setup": setup_as_interpreted,
     "backtest": ..., "descricao": <original>, "pregao", "fonte", "at",
     "frescor", "cap"}`.
10. `consume()` do gate + `ai_activity.registrar_uso(..., tipo="Opções ·
    setup", ticker=<ticker>, usos=...)` só no sucesso.

**3. `POST /setups/confirmar` — `Depends(require_criar_setup)`, custo 1.**

Corpo: `{setup}` — o objeto que o usuário VIU no dry-run. Sem LLM, sem
recompilação. Mesma validação de forma; frescor bloqueante de novo (o dry-run
pode ter sido visto há uma hora). `create_setup` com `confirm=True`.
Sucesso → 200 com `{status, name, backtest, ...envelope}` e
`audit.record(_conn, uid, "opcoes_setup", <name>, "status", None, "ativo")`.

**4. `POST /setups/{name}/desativar` — `Depends(require_criar_setup)`, custo 1.**

`deactivate_setup`. Sem frescor bloqueante: desativar é PARAR de afirmar, e
travar isso por dado velho deixaria um setup errado vigiando porque a carga
atrasou. Comentar essa assimetria — ela é deliberada.
Sucesso → `audit.record(_conn, uid, "opcoes_setup", name, "status", "ativo",
"inativo")`. `{"error", "known_setups"}` → 422 com a lista, verbatim.

**Nota de implementação sobre `{name}`**: mesma limitação já documentada em
`/setups/{name}/grafico` — nome com `/` não resolve. Repetir o comentário
apontando para lá, não reimplementar conversor `path`.
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/test_mcp_guardioes.py tests/test_mcp_cap.py tests/test_options_mcp_leitura.py tests/test_options_mcp_api.py tests/test_adr013_cobertura_rotas.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@router.post" server/app/options_mcp_api.py` retorna 5 (2 do 24-01 + 3 aqui).
    - Toda rota nova aparece com `_cap_check` no corpo — `python -m pytest tests/test_mcp_guardioes.py -q` verde.
    - `grep -rn "indicator\s*==\|crosses_above\|bullish_engulfing\|rel_volume" server/app/options_mcp_api.py` NÃO retorna nada — nenhum vocabulário da DSL copiado.
    - `python -m pytest tests/test_opcoes_gatilho.py -q` verde (ENG-06).
    - `grep -c "audit.record" server/app/options_mcp_api.py` retorna 2 (confirmar e desativar).
    - `/setups/compilar` sobrescreve `description` com o original: o fonte contém a atribuição e o comentário da razão.
    - Os três códigos de 402 existem no arquivo: `mcp_cota` (já existia), `plano_analises` e `ia_gerenciada`.
  </acceptance_criteria>
  <done>Compilar → dry-run → confirmar → desativar, tudo gateado por permissão, com frescor bloqueante onde afirma e auditoria onde grava; zero vocabulário de DSL dentro do Boris.</done>
</task>

<task type="auto">
  <name>Task 3: testes da Fase 5 e FONTES do guardião imperativo</name>
  <read_first>
    - server/tests/test_options_mcp_leitura.py (esqueleto e espião)
    - server/tests/test_guardrail_imperativo.py linhas 1-60 (o dict `FONTES` e o que ele varre)
    - server/tests/test_adr013_rbac.py (como uma permissão é provada ponta a ponta)
  </read_first>
  <files>server/tests/test_opcoes_dsl.py, server/tests/test_guardrail_imperativo.py</files>
  <action>
**A. `FONTES` ganha** `"options_mcp_api.SYSTEM_COMPILADOR_CABECALHO":
options_mcp_api.SYSTEM_COMPILADOR_CABECALHO` — texto fixo novo entra na
varredura na fase que o cria ([R-12] do PLANO). Comentar por que: é texto que
vai para uma LLM e volta como setup; imperativo de pressão ali contamina o
que a pessoa grava.

**B. `server/tests/test_opcoes_dsl.py`**, offline, mesmo esqueleto do irmão de
leitura. Espião de `mcp_client.call_tool` por nome de tool + monkeypatch de
`llm._call_llm` (a LLM FALSA devolve uma string que o teste controla).

Testes obrigatórios:

1. **Sem permissão → 403 e ZERO chamadas.** Usuário comum registrado (sem
   `role_admin`) em `/setups/compilar`, `/setups/confirmar` e
   `/setups/{n}/desativar`. É o coração do aceite: a recusa é do backend.
2. Anônimo → 401 nas três.
3. **PT → DSL → dry-run**: LLM falsa devolve JSON válido; a rota responde 200
   com `status: "dry_run"`, `backtest.disparos`,
   `backtest.disparos_por_100_pregoes_avaliaveis` e
   `backtest.retorno_apos_disparo["d+5"]`/`["d+10"]` presentes e VERBATIM.
4. **`description` é a original**: LLM falsa devolve `description:
   "REESCRITO PELA IA"`; o que chega em `create_setup` (inspecionar os args do
   espião) é o texto que o usuário digitou.
5. **`problems` item a item**: `create_setup` devolve
   `{"error": ..., "problems": ["conditions[0].operator: ...", "..."]}` → 422
   `setup_invalido` com a lista igual, byte a byte, na mesma ordem.
6. **LLM devolve lixo** (texto sem JSON) → 422 `compilacao_invalida` com `cru`
   preenchido; ZERO chamada a `create_setup`.
7. **Forma incompleta** (LLM devolve `{"name": "x"}`) → 422 `forma_invalida`
   listando os campos ausentes; ZERO chamada a `create_setup`.
8. **Frescor bloqueante** → 409 `dado_atrasado`: (a) `negociacao_b3`
   `atrasada` → `idadeHoras` presente; (b) classe crítica ausente → `motivo:
   "nao_medido"`. Nos dois casos ZERO LLM e ZERO `create_setup`.
9. **Confirmar não chama LLM**: monkeypatch de `_call_llm` que FALHA o teste
   se for chamado; `/setups/confirmar` responde 200 e grava.
10. **Auditoria**: depois de confirmar, `audit.recent(conn)` tem uma linha com
    `entity == "opcoes_setup"` e o nome do setup; depois de desativar, outra
    com `old_value="ativo"`, `new_value="inativo"`.
11. **Desativar não é bloqueado por frescor** — com dado atrasado, responde
    200. A assimetria é deliberada e um teste a fixa.
12. **Cota cheia → 402 `mcp_cota`** antes de qualquer rede.
13. **Gate de análise nega → 402** com `plano_analises` (ou `ia_gerenciada`), e
    o texto NÃO contém o copy de BYOK do metering ("chave", "Configurações →
    Modelo de IA" etc. do caminho antigo) — asserção negativa explícita.
14. **`tools/list` e `resources/read` não consomem cap**: `usado` antes e
    depois de uma compilação que só usou schema + resource + um
    `create_setup` difere de exatamente 2 (o `check_data_freshness` e o
    `create_setup`), nunca 4.
15. **`_system_compilador(schema, texto)` é puro**: chamado direto, sem rede,
    contém o `inputSchema` serializado e o texto do resource, e NÃO contém
    nenhum indicador hardcodado (asserção: o nome `rsi` só aparece se estiver
    no schema/texto passados).
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/test_opcoes_dsl.py tests/test_guardrail_imperativo.py tests/test_mcp_guardioes.py tests/test_adr013_rbac.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_opcoes_dsl.py -q` verde, com pelo menos 15 testes.
    - `python -m pytest tests/test_guardrail_imperativo.py -q` verde com a fonte nova incluída.
    - O teste 1 falha se `Depends(require_criar_setup)` for trocado por `Depends(require_user)` — provar e reverter, registrando no SUMMARY.
    - O teste 14 falha se `list_tools`/`read_resource` passarem a consumir cap.
    - Nenhum teste depende de rede nem de `MCP_CLIENT_SECRET`.
  </acceptance_criteria>
  <done>Fase 5 coberta: permissão, compilação, forma, semântica do serviço, frescor, auditoria e o custo real do cap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| texto do usuário → LLM → objeto gravado | é o caminho por onde uma paráfrase viraria registro permanente |
| usuário logado → armazém de setups do serviço | setups são SEM DONO (ADR-027, Decisão 7): quem grava, grava para todos |
| gate de análise × cap da aba | dois tetos distintos na mesma requisição, com recusas de significado diferente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-10 | Elevation of Privilege | rotas de escrita de setup | mitigate | `require_criar_setup` no backend; teste 1 prova o 403; falhar FECHADO (503) sem a fiação |
| T-24-11 | Tampering | `description` do setup | mitigate | sobrescrita com o original; teste 4 |
| T-24-12 | Spoofing | resposta da LLM | mitigate | validação de forma + `create_setup(confirm=false)` como validador semântico; lixo vira 422 com o cru rotulado, nunca setup fabricado |
| T-24-13 | Repudiation | quem criou/desativou | mitigate | `audit.record` com entidade `opcoes_setup`; entrada no `ENTIDADES_POR_PERMISSAO` para o admin enxergar |
| T-24-14 | Information Disclosure | erro de LLM | mitigate | `LLMUserError` já sanitizada por `llm.public_error`; nenhuma chave em mensagem |
| T-24-15 | Tampering | dado velho virando setup | mitigate | 409 `dado_atrasado` antes da LLM; desativar é a exceção declarada |
| T-24-SC | Tampering | pip installs | mitigate | nenhum pacote novo |
</threat_model>

<verification>
1. `bash scripts/executar.sh --testes` FORA do sandbox (`dangerouslyDisableSandbox: true`) — ver a nota do sandbox no 24-CONTEXT.
2. Saída colada no SUMMARY com a contagem.
3. `git diff --stat server/requirements.txt server/requirements-prod.txt` vazio.
</verification>

<success_criteria>
- Três rotas de setup, todas com permissão real no backend e cap com `with`.
- `system` do compilador montado em runtime; nenhum vocabulário da DSL dentro do Boris; ENG-06 verde.
- Dry-run obrigatório antes de gravar; `problems` item a item; `description` original preservada.
- Frescor bloqueia criar, não bloqueia desativar.
- Auditoria gravada e visível pelo mapa de entidades do ADR-013.
</success_criteria>

<output>
Create `.planning/phases/24-opcoes-mcp-analise-e-setups/24-03-SUMMARY.md` when done
</output>
