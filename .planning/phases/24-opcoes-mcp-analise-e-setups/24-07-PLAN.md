---
phase: 24-opcoes-mcp-analise-e-setups
plan: 07
type: execute
wave: 3
depends_on: ["24-06"]
gap_closure: true
files_modified:
  - server/app/options_mcp_api.py
  - server/tests/test_mcp_cap.py
  - server/tests/test_options_mcp_api.py
  - web/src/copy.js
  - web/src/opcoes/OpcoesScreen.jsx
autonomous: true
requirements: [24-VERIFICATION F-04]

must_haves:
  truths:
    - "Chamada que a tool recusou debita o cap do usuário, porque a viagem aconteceu e o teto compartilhado já foi cobrado"
    - "Falha em que a viagem NÃO aconteceu (sem credencial) ou que o próprio serviço declara não cobrada (teto atingido) continua sem debitar"
    - "A pessoa vê que a tentativa recusada consumiu uma chamada da cota dela"
  artifacts:
    - path: "server/app/options_mcp_api.py"
      provides: "consumo de cap na recusa de tool, em UM ponto só"
      contains: "McpErroDeTool"
  key_links:
    - from: "server/app/options_mcp_api.py (_chamada_com_cap)"
      to: "cap.consome"
      via: "except McpErroDeTool → consome → re-levanta"
      pattern: "except mcp_client.McpErroDeTool"
---

<objective>
Fechar o achado F-04 do `24-VERIFICATION.md` com a decisão que o Alex tomou em
2026-09-11: **cobrar a viagem**.

Purpose: o serviço MCP conta toda `tools/call` no teto de 2.000/dia ANTES de
executar a tool. Quando a tool recusa o pedido, o Boris hoje não debita nada —
nem do cap do usuário, nem do contador global. Com o fan-out de até 6
vencimentos do `/possibilidades` sobre um ticker cuja cadeia não precifica,
~330 toques esgotam a cota de TODA a base sem mover os 60/dia de ninguém. O
cap existe exatamente para isso não acontecer.

O critério passa a ser literal: **tocou a rede, debitou**.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-opcoes-mcp-analise-e-setups/24-VERIFICATION.md
@.planning/phases/24-opcoes-mcp-analise-e-setups/24-CONTEXT.md
@CLAUDE.md

<guardrails>
- **Só `McpErroDeTool` passa a cobrar.** As outras falhas NÃO cobram, e cada
  uma por um motivo próprio, que precisa estar comentado no código:
  · `McpNaoConfigurado` — não houve viagem (falta credencial no servidor);
  · `McpNaoAutorizado` — o porteiro recusa antes do contador de tools;
  · `McpTetoAtingido` — o próprio contrato do serviço diz que "a chamada
    recusada não conta";
  · `McpIndisponivel` — timeout/5xx: não há prova de que o serviço contou, e
    cobrar por indisponibilidade puniria o usuário pela queda do fornecedor.
  Cobrar o que não se sabe se foi cobrado é o mesmo erro de origem, invertido.
- **Um ponto só.** A regra vive em `_chamada_com_cap`; qualquer rota que use
  outro caminho para chamar tool passa a usá-lo, ou herda o mesmo `try`. Duas
  cópias desta regra divergem na primeira manutenção.
- **NÃO cachear a recusa.** O Alex escolheu só cobrar; cache negativo era a
  outra opção e foi descartada nesta rodada.
- `with _cap_check(...)` em toda rota (guardião AST) continua valendo.
- Baseline a não regredir: a que o 24-06 deixar (cole-a no SUMMARY).
</guardrails>
</context>

<tasks>

<task type="auto">
  <name>Task 1: cobrar a recusa de tool, em um ponto só</name>
  <read_first>
    - .planning/phases/24-opcoes-mcp-analise-e-setups/24-VERIFICATION.md (achado F-04 inteiro, com o cenário medido)
    - server/app/options_mcp_api.py (`_chamada_com_cap` e a docstring dela, que hoje DOCUMENTA o comportamento antigo — ela precisa mudar junto)
    - server/app/options_mcp_api.py (a rota `/status`, que chama `mcp_client.call_tool` direto e trata `McpErroDeTool` como estado, respondendo 200)
  </read_first>
  <files>server/app/options_mcp_api.py</files>
  <action>
**1. `_chamada_com_cap`:**

```python
try:
    r = await mcp_client.call_tool(nome, args)
except mcp_client.McpErroDeTool:
    # A viagem ACONTECEU: o serviço conta toda `tools/call` no porteiro,
    # antes de executar a tool — então o teto compartilhado de 2.000/dia já
    # foi debitado quando a tool disse não. Não cobrar aqui deixava a
    # recusa de graça para o usuário e cara para a base inteira: seis
    # `evaluate` sobre uma cadeia que não precifica custavam 6 do teto e 0
    # do cap (achado F-04; decisão do Alex em 2026-09-11: cobrar a viagem).
    cap.consome(1)
    raise
```

As demais exceções continuam SEM consumir, cada uma pelo motivo listado no
`<guardrails>` — escreva os quatro motivos no comentário, porque a próxima
pessoa que ler isto vai querer "uniformizar" e precisa saber por que não.

Atualize a docstring de `_chamada_com_cap`: hoje ela afirma que "chamada que
termina em exceção não consome — inclusive `McpErroDeTool` […] é uma decisão a
avalizar". A decisão foi tomada; a docstring precisa dizer o que o código faz
agora, e registrar que a avalização aconteceu e qual foi.

**2. `/status`:** ela chama `mcp_client.call_tool` diretamente (não passa por
`_chamada_com_cap`) e trata `McpErroDeTool` como ESTADO, respondendo 200 com
`frescor.bloqueia`. A viagem aconteceu igual — então o `except
mcp_client.McpErroDeTool` dela passa a `cap.consome(1)` antes de seguir.
Comentar apontando para a mesma razão, sem repetir o texto inteiro.

**3. Transparência (`_erro_http`):** o detail do 422 `mcp_erro_de_tool` ganha
`"cobrado": True` e a constante nova `AVISO_RECUSA_COBRADA`:

```python
AVISO_RECUSA_COBRADA = (
    "Esta tentativa consumiu uma chamada da sua cota do dia: o serviço cobra "
    "a consulta mesmo quando recusa o pedido."
)
```

Texto fixo novo → acrescente a `AVISOS` (o guardião imperativo varre
`options_mcp_api.AVISOS`). Cobrar sem dizer que cobrou é a parte do defeito
que o usuário enxerga.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/server && .venv/bin/python -m pytest tests/test_mcp_cap.py tests/test_options_mcp_api.py tests/test_opcoes_dsl.py tests/test_mcp_guardioes.py tests/test_guardrail_imperativo.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cap.consome(1)" server/app/options_mcp_api.py` cobre os caminhos de sucesso E de recusa.
    - Nenhuma das outras quatro exceções consome cap — provado por teste, não por leitura.
    - `AVISO_RECUSA_COBRADA` está em `AVISOS` e `test_guardrail_imperativo.py` segue verde.
    - A docstring de `_chamada_com_cap` descreve o comportamento NOVO e registra a decisão.
  </acceptance_criteria>
  <done>Recusa de tool debita 1; as quatro falhas que não viajaram (ou que o serviço declara não cobradas) seguem sem debitar; o usuário é avisado.</done>
</task>

<task type="auto">
  <name>Task 2: testes do novo critério e o cenário do F-04</name>
  <read_first>
    - server/tests/test_mcp_cap.py (espião que conta chamadas e o padrão de asserção sobre `metering.used`)
    - server/tests/test_options_mcp_api.py (o teste de `McpErroDeTool` em um vencimento do `/possibilidades`)
  </read_first>
  <files>server/tests/test_mcp_cap.py, server/tests/test_options_mcp_api.py</files>
  <action>
Testes obrigatórios, cada um provado RED antes da correção (rode contra o
código anterior com `git stash`/`git checkout --` ou revertendo a mão, veja
vermelho, restaure — e registre a prova no SUMMARY):

1. **`McpErroDeTool` debita 1**: `metering.used` sobe em 1 depois de uma rota
   cuja tool recusou.
2. **As outras quatro não debitam**: um teste por exceção
   (`McpNaoConfigurado`, `McpNaoAutorizado`, `McpIndisponivel`,
   `McpTetoAtingido`) provando `used` inalterado.
3. **O cenário do F-04, medido**: `/possibilidades` com 6 vencimentos onde o
   `propose` vem do cache e os 6 `evaluate` recusam → `used` sobe **6**, não
   0. É o teste que mede o defeito relatado, e é o que não pode voltar.
4. **Devolução de reserva continua correta**: com 6 recusas sobre uma reserva
   de 12 (2×6), o saldo devolvido é 6 — cobrar a recusa não pode fazer a
   contabilidade da `_Reserva` devolver a mais nem a menos.
5. **`/status` com erro de tool**: responde 200 (segue sendo estado) E debita
   1.
6. **O 422 carrega `cobrado: true`** e a nota.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/server && .venv/bin/python -m pytest tests/test_mcp_cap.py tests/test_options_mcp_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Os 6 testes existem e passam.
    - Cada um foi provado RED contra o código de antes — prova registrada no SUMMARY.
    - Nenhum teste existente de cap foi afrouxado; se algum precisou mudar de expectativa, a mudança está comentada com a razão e a data (guardião não se apaga).
  </acceptance_criteria>
  <done>O defeito do F-04 tem um teste que o mede e falha sem a correção.</done>
</task>

<task type="auto">
  <name>Task 3: a tela diz que a tentativa recusada consumiu cota</name>
  <read_first>
    - web/src/opcoes/OpcoesScreen.jsx (o ramo de erro `mcp_erro_de_tool`, que hoje mostra `erro.message` cru com pre-wrap)
    - web/src/copy.js (as duas vozes)
  </read_first>
  <files>web/src/copy.js, web/src/opcoes/OpcoesScreen.jsx</files>
  <action>
Chave nova `opcoesRecusaCobrada` nos DOIS modos (mesma substância, vozes
diferentes): a consulta foi recusada pelo serviço e ainda assim consumiu uma
chamada da cota do dia, porque o serviço cobra a viagem.

No ramo de erro com `code === "mcp_erro_de_tool"`, quando o detail trouxer
`cobrado: true`, exibir essa linha abaixo da mensagem da tool, discreta
(`T.textMuted`), sem alarme. O texto continua vindo do backend na mensagem
principal; esta é a única frase que o front acrescenta, e ela é do front
porque é sobre a COTA DA PESSOA, não sobre o pedido.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/web && npx vite build && node tests/test_opcoes_analisar_ui.mjs && node tests/test_copy_theme.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx vite build` verde.
    - Chave presente nos dois modos; conjunto de chaves `opcoes*` segue idêntico entre `estudo` e `operador`.
    - A linha só aparece com `cobrado: true` — sem o campo, a tela não afirma nada sobre cota.
  </acceptance_criteria>
  <done>Quem tomou a recusa sabe que ela custou uma chamada.</done>
</task>

</tasks>

<verification>
1. `bash scripts/executar.sh --testes` FORA do sandbox (`dangerouslyDisableSandbox: true`).
2. `cd web && npx vite build`.
3. Saída colada no SUMMARY, sem regressão sobre a baseline deixada pelo 24-06.
</verification>

<success_criteria>
- Recusa de tool debita o cap; as quatro falhas sem viagem provada não debitam.
- O cenário do F-04 tem teste que o mede.
- O usuário é informado de que a recusa consumiu cota.
- Cache negativo NÃO foi implementado (não era a decisão).
</success_criteria>

<output>
Create `.planning/phases/24-opcoes-mcp-analise-e-setups/24-07-SUMMARY.md` when done
</output>
