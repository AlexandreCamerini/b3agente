---
phase: 24-opcoes-mcp-analise-e-setups
plan: 05
type: execute
wave: 4
depends_on: ["24-02", "24-04"]
files_modified:
  - web/src/version.js
  - server/app/main.py
  - server/web_dist
autonomous: false
requirements: [publicação]

must_haves:
  truths:
    - "O que foi implementado nesta fase está no ar, e o carimbo do front bate com o do servidor"
    - "A publicação só acontece depois do OK explícito do Alex"
---

<objective>
Levar a Fase 24 ao ar: bump de carimbo, build, publicação do front e
verificação em produção.

**Este plano NÃO roda automaticamente.** `autonomous: false` é literal: ele
publica para todos os usuários e precisa do OK explícito do Alex, do mesmo
jeito que o 19-04 (que segue pendente pela mesma razão).

Purpose: `server/web_dist` é a única árvore que o Railway serve. Mesclar PR
não publica nada — sem esta etapa, a fase fica testada e invisível. É a
armadilha que já custou dois dias de divergência entre `BUILD_ID` e
`SERVER_BUILD_ID` em setembro.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-opcoes-mcp-analise-e-setups/24-CONTEXT.md
@CLAUDE.md

<guardrails>
- **`bump.sh` sem argumento pode REBAIXAR produção.** Ele deriva do `BUILD_ID`
  LOCAL; com `SERVER_BUILD_ID` à frente (deploy só-backend), o
  `publicar-web.sh` iguala para BAIXO (compara `!=`, não ordem). Passar o
  carimbo à mão e reescrever o comentário da linha — o script preserva o texto
  antigo, que vira mentira.
- **Nunca editar `server/web_dist` direto.** Conflito ali se resolve
  rebuildando, não mesclando.
- **Ordem obrigatória:** `scripts/bump.sh <carimbo>` → `npx vite build` →
  `scripts/publicar-web.sh`. Deploy só-backend exige bump manual de
  `SERVER_BUILD_ID`.
- **Conferir por TAMANHO** (`ls -laS`) ao validar o bundle — já se confundiu um
  chunk de 271 bytes com o bundle de 827 KB.
- **`railway logs` sem `--environment production` mente** (a CLI fica linkada
  no staging).
</guardrails>
</context>

<tasks>

<task type="human">
  <name>Task 1: OK do Alex para publicar</name>
  <action>
Apresentar: o que a Fase 24 entrega, o resultado da suíte, o que NÃO foi
verificado ao vivo (tudo que depende de `MCP_CLIENT_SECRET`) e o carimbo
proposto. Só seguir com um sim explícito.
  </action>
  <done>Aprovação registrada no SUMMARY, com data e a frase do Alex.</done>
</task>

<task type="auto">
  <name>Task 2: bump, build e publicação</name>
  <files>web/src/version.js, server/app/main.py, server/web_dist</files>
  <action>
1. `git fetch origin && git merge origin/main` ANTES do bump (o `bump.sh`
   deriva do valor local; sessão paralela colide).
2. `bash scripts/bump.sh F10-AAAAMMDD-NN` com o carimbo escolhido; conferir
   que `SERVER_BUILD_ID` em `server/app/main.py` e `BUILD_ID` em
   `web/src/version.js` ficaram IGUAIS e que o comentário da linha descreve
   esta entrega.
3. `cd web && npx vite build`.
4. `bash scripts/publicar-web.sh`.
5. Verificar em produção: `curl -s https://boris.semente.dev/api/health` →
   `build` igual ao carimbo; o rodapé do Perfil mostrando os dois carimbos.
  </action>
  <verify>
    <automated>curl -s https://boris.semente.dev/api/health</automated>
  </verify>
  <acceptance_criteria>
    - `/api/health` responde o carimbo novo.
    - `BUILD_ID` == `SERVER_BUILD_ID`.
    - `ls -laS server/web_dist/assets | head` mostra o bundle principal com tamanho plausível.
  </acceptance_criteria>
  <done>Front e servidor no mesmo carimbo, em produção.</done>
</task>

<task type="human">
  <name>Task 3: checkpoint no iPhone</name>
  <action>
Roteiro mínimo, no aparelho, com o app apontando para produção:
1. Aba Opções → escolher PETR4 → leitura aparece com pregão e frescor.
2. "Analisar" → tese de alta → lote 100 → estrutura montada, custo em reais.
3. "Possibilidades" → conferir que o custo em chamadas aparece ANTES do toque;
   disparar; conferir um cartão por vencimento e o payoff desenhado.
4. Breakeven em preço (não em reais) — conferir a olho.
5. Com a conta admin: "Criar setup" → descrição em português → dry-run com
   backtest → confirmar → o setup aparece na lista do ticker.
6. Com conta comum: a seção "Criar setup" não existe.
7. 375 px sem scroll horizontal; dois temas; `prefers-reduced-motion`.
  </action>
  <done>Roteiro percorrido; defeitos encontrados viram quick task, não emenda neste plano.</done>
</task>

</tasks>

<verification>
1. `bash scripts/executar.sh --testes` verde ANTES do bump.
2. `/api/health` depois da publicação.
3. Checkpoint humano no iPhone.
</verification>

<success_criteria>
- Publicado só com OK explícito; carimbos iguais; checkpoint percorrido.
</success_criteria>

<output>
Create `.planning/phases/24-opcoes-mcp-analise-e-setups/24-05-SUMMARY.md` when done
</output>
