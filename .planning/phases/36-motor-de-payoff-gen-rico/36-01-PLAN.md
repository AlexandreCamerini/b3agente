---
phase: 36-motor-de-payoff-gen-rico
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/opcoes_payoff.py
  - server/tests/test_opcoes_payoff.py
autonomous: true
requirements: [PAYOFF-02]

must_haves:
  truths:
    - "D-01: nenhum arquivo novo é criado nesta onda — todo o trabalho cai em `server/app/opcoes_payoff.py` e `server/tests/test_opcoes_payoff.py`, e `perfil_da_estrutura()` é ESTENDIDA, não reescrita"
    - "D-02: a convenção de unidade fica intacta — `quantidade` continua número de contratos, nenhum fator de lote entra no módulo, breakeven continua PREÇO (nenhuma multiplicação nova por lote em lugar nenhum)"
    - "D-03: uma perna pode carregar `vencimento` opcional; com vencimentos iguais ou ausentes o resultado é idêntico ao de hoje; com vencimentos divergentes a função devolve estado explícito com motivo declarado, sem curva e sem ganho/perda máxima"
    - "D-04: entrada degenerada (custo líquido zero E curva inteira em zero E inclinação da cauda zero) é recusada com `ValueError` de mensagem clara; fora dela, `_breakevens` deixa de reportar `S=0` como cruzamento quando a curva só encosta no zero sem sair dele"
    - "D-07: os casos-limite que o motor já resolvia mas ninguém travava (straddle, strangle, borboleta, condor com platô central, box de resultado constante não-zero, ratio spread com quantidades assimétricas) viram teste nomeado"
    - "Os 4 consumidores (`opcoes_motor.py`, `opcoes_lastreadas.py`, `opcoes_curadoria.py`, `options_mcp_api.py`) continuam funcionando sem uma linha alterada — nenhum deles passa `vencimento` hoje"
  artifacts:
    - path: "server/app/opcoes_payoff.py"
      provides: "campo `vencimento` por perna, estado de vencimentos divergentes, guarda de entrada degenerada, `_breakevens` corrigido"
      contains: "vencimento"
    - path: "server/tests/test_opcoes_payoff.py"
      provides: "cobertura nova de D-03, D-04 e dos casos-limite de D-07"
      contains: "def test_perfil_calendario"
  key_links:
    - from: "server/tests/test_opcoes_payoff.py"
      to: "server/app/opcoes_payoff.py"
      via: "from app import opcoes_payoff as m"
      pattern: "from app import opcoes_payoff"
    - from: "server/app/opcoes_motor.py"
      to: "server/app/opcoes_payoff.py"
      via: "avaliar() delega direto a perfil_da_estrutura(), sem renomear chave"
      pattern: "return perfil_da_estrutura\\(pernas\\)"
---

<objective>
Corrigir e endurecer o motor de payoff que já está em produção: aceitar
`vencimento` por perna e degradar com honestidade quando os vencimentos
divergem (D-03), recusar entrada degenerada e parar de inventar breakeven em
`S=0` (D-04), e travar em teste nomeado os casos-limite que o motor já
resolvia sem ninguém provar (D-07 / PAYOFF-02).

Purpose: a Fase 37 desenha o gráfico e escreve a explicação a partir DESTE
motor. Um breakeven espúrio em `S=0` viraria uma marca no eixo e uma frase
("empate em R$ 0,00") que o usuário lê como fato. Corrigir aqui é corrigir na
única fonte de verdade (D-01), antes de haver consumidor de desenho.

Output: `server/app/opcoes_payoff.py` com campo novo, guarda nova e bugfix;
`server/tests/test_opcoes_payoff.py` com os casos de D-03/D-04 e os
casos-limite de PAYOFF-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-motor-de-payoff-gen-rico/36-CONTEXT.md
@.planning/phases/36-motor-de-payoff-gen-rico/36-PATTERNS.md
@CLAUDE.md

<interfaces>
Contrato ATUAL de `server/app/opcoes_payoff.py` (medido por execução direta em
2026-09-21, não suposto — use como verdade de partida):

`_validar_perna(perna, i) -> dict` devolve as chaves `contrato`, `tipo`,
`lado`, `sinal`, `strike`, `premio`, `quantidade`, `delta`. `delta` é lido com
`_numero(perna.get("delta"))` — permissivo, vira `None` em silêncio quando
falta. `strike`/`premio`/`quantidade`/`tipo`/`lado` levantam `ValueError`
citando `perna {i}` e o campo.

`perfil_da_estrutura(pernas) -> dict` devolve `pernas`, `custo_liquido`,
`fluxo`, `ganho_maximo`, `perda_maxima`, `ganho_ilimitado`,
`perda_ilimitada`, `breakevens`, `delta_total`, `curva`, `unidade`.
`inclinacao_direita` é LOCAL (não sai no dicionário) e vale
`sum(sinal * quantidade)` só das pernas `CALL`/`ACAO`.

`_delta_total(...)` é o padrão de "nulo tem motivo": devolve
`{"valor": ..., "pernas_sem_delta": n, "motivo": str|None}`.

Saídas reais medidas nesta sessão (rodando o módulo, não deduzidas):
- Caso golden (CALL compra 49.17 prêmio 0.40 + CALL venda 49.67 prêmio 0.15):
  `custo_liquido` 0.25, `breakevens` [49.42], `ganho_maximo` 0.25,
  `perda_maxima` 0.25, `curva` [(0.0, -0.25), (49.17, -0.25), (49.67, 0.25)].
- Degenerada (CALL compra 50 prêmio 1 + CALL venda 50 prêmio 1):
  `custo_liquido` 0.0, `curva` [(0.0, 0.0), (50.0, 0.0)],
  `breakevens` [0.0, 50.0] ← os dois espúrios, é o defeito de D-04.
- CALL comprada strike 50 prêmio 0: mesma `curva` achatada em zero, mas
  `inclinacao_direita` 1 → `breakevens` [0.0, 50.0]; o correto é [50.0].
- Ratio 1x2 (CALL compra 50 prêmio 3 + CALL venda 55 prêmio 1.5 qtd 2):
  `custo_liquido` 0.0, `curva` [(0.0, 0.0), (50.0, 0.0), (55.0, 5.0)],
  `perda_ilimitada` True, `ganho_maximo` 5.0, `breakevens` hoje
  [0.0, 50.0, 60.0].
- Box travado em zero (CALL compra 50 pr. 6, CALL venda 55 pr. 3, PUT compra
  55 pr. 4, PUT venda 50 pr. 2): `custo_liquido` 5.0, curva inteira em 0.0,
  `breakevens` hoje [0.0, 50.0, 55.0] — todos espúrios, mas NÃO é degenerada
  (custo ≠ 0).
- Box de resultado constante +0.5 (mesmo acima com PUT compra 55 prêmio 3.5):
  `custo_liquido` 4.5, curva inteira em 0.5, `breakevens` [] ,
  `ganho_maximo` 0.5, `perda_maxima` 0.0.
- Borboleta (CALL compra 45 pr. 6, CALL venda 50 pr. 3 qtd 2, CALL compra 55
  pr. 1.5): custo 1.5, `breakevens` [46.5, 53.5], ganho 3.5, perda 1.5.
- Condor (CALL compra 45 pr. 6, CALL venda 50 pr. 3, CALL venda 55 pr. 1.5,
  CALL compra 60 pr. 0.5): custo 2.0, `breakevens` [47.0, 58.0], ganho 3.0,
  perda 2.0, curva [(0,-2),(45,-2),(50,3),(55,3),(60,-2)] — o platô central
  são os pontos 50 e 55 com o mesmo resultado 3.0.
- Straddle comprado (CALL 50 pr. 1 + PUT 50 pr. 1): `breakevens` [48.0, 52.0].
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `vencimento` por perna e degradação honesta de calendário/diagonal (D-03)</name>
  <files>server/app/opcoes_payoff.py, server/tests/test_opcoes_payoff.py</files>
  <read_first>
    - server/app/opcoes_payoff.py (arquivo inteiro, 277 linhas — leia de uma vez, não em pedaços)
    - server/tests/test_opcoes_payoff.py (arquivo inteiro, 222 linhas)
    - .planning/phases/36-motor-de-payoff-gen-rico/36-CONTEXT.md (D-03 e a seção `canonical_refs`)
    - .planning/phases/36-motor-de-payoff-gen-rico/36-PATTERNS.md (seções "Verified retrocompatibility", "Analog A/B/C" de D-03, "Guard ordering")
    - server/app/opcoes_motor.py linhas 120-205 (`perna_de_contrato`, `perna_de_acao`, `avaliar` — os adaptadores que NÃO podem precisar de mudança)
  </read_first>
  <behavior>
    - `_validar_perna({"tipo": "CALL", "lado": "compra", "strike": 40, "premio": 1})` devolve dict com chave `vencimento` valendo `None` (campo ausente não é erro).
    - `_validar_perna({..., "vencimento": "2026-10-16"})` devolve `vencimento` exatamente `"2026-10-16"` — string preservada verbatim, sem parse, sem `_numero`, sem comparação com data de hoje.
    - `perfil_da_estrutura` com TODAS as pernas sem `vencimento` devolve exatamente o mesmo resultado de hoje (curva, breakevens, ganho/perda máxima idênticos), acrescido da chave nova `vencimentos` com `divergentes=False` e `motivo=None`.
    - `perfil_da_estrutura` com as duas pernas no MESMO `vencimento` devolve resultado idêntico ao da mesma estrutura sem `vencimento` nenhum (comparar os dois dicionários campo a campo, exceto `pernas`).
    - `perfil_da_estrutura` com uma perna `vencimento="2026-10-16"` e outra sem `vencimento` NÃO é divergência (D-03: omitido = mesmo vencimento implícito) — calcula normalmente.
    - `perfil_da_estrutura` com `vencimento="2026-10-16"` e `vencimento="2026-11-20"` devolve `vencimentos["divergentes"] is True`, `vencimentos["motivo"]` contendo a expressão "vencimentos diferentes", `curva == []`, `breakevens == []`, `ganho_maximo is None`, `perda_maxima is None`, `ganho_ilimitado is False`, `perda_ilimitada is False`, e `custo_liquido` ainda calculado (a soma dos prêmios continua verdadeira).
  </behavior>
  <action>
    Escreva PRIMEIRO os testes do bloco `<behavior>` (nomes em `snake_case`
    seguindo `test_<funcao_ou_conceito>_<cenario>`, mesmo padrão dos 25 testes
    existentes), rode e confirme que falham, só então implemente.

    Em `_validar_perna`: acrescente `"vencimento": perna.get("vencimento")` ao
    dicionário devolvido, logo depois da chave `"delta"`. Leia como `delta` é
    lido — passthrough permissivo, ausência não é erro. NÃO passe por
    `_numero` (uma data em string viraria `None`) e NÃO valide formato:
    `vencimento` é dado OPACO neste módulo, comparado só por igualdade entre
    pernas. Isso é obrigação do docstring do módulo (linhas 1-4: "sem leitura
    de relógio") e o motivo pelo qual `prazo_em_pregoes` foi deliberadamente
    não portado (linhas 19-25) — aritmética de calendário é problema do
    chamador, nunca daqui.

    Em `perfil_da_estrutura`: depois de `normalizadas` ser construída (linha
    atual 177) e ANTES de `avaliar`/`curva` serem montadas (linhas atuais
    184-189), colete os valores de `vencimento` distintos e não-`None`, em
    ordem de PRIMEIRA APARIÇÃO — não ordene a lista, `vencimento` é opaco e
    `sorted()` sobre tipos misturados levanta `TypeError`. Se houver mais de
    um valor distinto, faça curto-circuito e devolva o dicionário degradado
    sem construir curva nenhuma: construir curva para pernas de vencimentos
    diferentes É a "aproximação linear" que D-03 proíbe.

    O dicionário degradado tem exatamente as mesmas chaves do normal, com
    `pernas`, `custo_liquido`, `fluxo`, `delta_total` e `unidade` calculados
    normalmente e `curva=[]`, `breakevens=[]`, `ganho_maximo=None`,
    `perda_maxima=None`, `ganho_ilimitado=False`, `perda_ilimitada=False`.

    Acrescente a chave nova `vencimentos` aos DOIS retornos (forma estável,
    o consumidor nunca precisa de `.get` defensivo), sub-dicionário na mesma
    forma de `_delta_total` (linhas 260-276): `{"divergentes": bool,
    "distintos": [...], "motivo": str|None}`. `motivo` é `None` quando não há
    divergência e, quando há, é uma frase PT-BR direta no registro das
    mensagens já existentes do módulo, dizendo que os vencimentos são
    diferentes e que o resultado depende do valor restante da outra ponta —
    nunca hedging, nunca `0.0` no lugar de desconhecido (convenção "nulo
    sempre tem motivo", docstring linhas 13-17).

    Atualize o docstring de `perfil_da_estrutura` declarando o caso de
    vencimentos divergentes e por que ele não desenha curva.

    NÃO toque em `opcoes_motor.py`, `opcoes_lastreadas.py`,
    `opcoes_curadoria.py` nem `options_mcp_api.py`: o `36-PATTERNS.md` já
    rastreou os 4 e nenhum monta perna com `vencimento` hoje — o campo
    opcional com default `None` é seguro com ZERO alteração neles. Enfiar
    `vencimento` nos adaptadores é escopo de fase futura, não desta.
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/test_opcoes_payoff.py -x -q</automated>
    <automated>cd server && grep -rn "vencimento" app/opcoes_motor.py app/opcoes_lastreadas.py app/opcoes_curadoria.py | grep -v "^.*#" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_opcoes_payoff.py` passa com os 25 testes antigos intactos (nenhum alterado) mais os 6 novos do bloco `<behavior>`.
    - `git diff --stat` mostra exatamente 2 arquivos alterados: `server/app/opcoes_payoff.py` e `server/tests/test_opcoes_payoff.py`. Nenhum arquivo novo criado (D-01).
    - `grep -n "vencimento" server/app/opcoes_payoff.py | grep -v "^[0-9]*: *#"` mostra o campo em `_validar_perna`, a checagem de divergência em `perfil_da_estrutura` e a chave `vencimentos` nos dois retornos.
    - Comando de retrocompatibilidade roda limpo: `cd server && .venv/bin/python -c "import sys; sys.path.insert(0,'.'); from app import opcoes_motor as om; print(om.avaliar([{'tipo':'ACAO','lado':'compra','premio':30},{'tipo':'CALL','lado':'venda','strike':32,'premio':1.5}])['breakevens'])"` imprime `[28.5]` — o mesmo valor de antes da mudança.
    - O módulo continua sem `import` de rede/banco/LLM/relógio: `grep -n "^from\|^import" server/app/opcoes_payoff.py` mostra apenas `from __future__` e `from typing`.
  </acceptance_criteria>
  <done>Perna aceita `vencimento` opcional; vencimentos divergentes devolvem estado explícito com motivo, sem curva; estruturas de vencimento único ou omitido calculam exatamente como antes; os 4 consumidores intactos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: recusar entrada degenerada e parar de inventar breakeven em S=0 (D-04)</name>
  <files>server/app/opcoes_payoff.py, server/tests/test_opcoes_payoff.py</files>
  <read_first>
    - server/app/opcoes_payoff.py (já lido na Task 1 — releia apenas se a Task 1 mudou a região; não releia o arquivo inteiro de novo)
    - .planning/phases/36-motor-de-payoff-gen-rico/36-CONTEXT.md (D-04, incluindo o parágrafo sobre box/resultado constante NÃO-zero)
    - .planning/phases/36-motor-de-payoff-gen-rico/36-PATTERNS.md (seções "D-04.1 Degenerate-entry rejection" e "D-04.2 `_breakevens` bugfix", incluindo o "Explicit non-example")
  </read_first>
  <behavior>
    - Degenerada: `perfil_da_estrutura([CALL compra 50 prêmio 1, CALL venda 50 prêmio 1])` levanta `ValueError` cuja mensagem contém "sem exposição" (ou a expressão exata escolhida, travada no teste por substring, nunca por igualdade de string inteira).
    - Sobrevive (NÃO é degenerada): `CALL comprada strike 50 prêmio 0` — `custo_liquido` 0.0 e curva achatada em zero, mas `inclinacao_direita` 1. Resultado: `breakevens == [50.0]` (hoje devolve `[0.0, 50.0]`), `ganho_ilimitado is True`.
    - Sobrevive (NÃO é degenerada): box travado em ZERO com `custo_liquido` 5.0 (CALL compra 50 pr. 6, CALL venda 55 pr. 3, PUT compra 55 pr. 4, PUT venda 50 pr. 2) — curva inteira em 0.0 mas custo ≠ 0. Resultado: `breakevens == []` (hoje devolve `[0.0, 50.0, 55.0]`).
    - Sobrevive: ratio 1x2 com `custo_liquido` 0.0 (CALL compra 50 pr. 3 + CALL venda 55 pr. 1.5 qtd 2) — curva não é toda zero. Resultado: `breakevens == [50.0, 60.0]` (hoje devolve `[0.0, 50.0, 60.0]`), `perda_ilimitada is True`.
    - Precedência de guarda: estrutura simultaneamente degenerada E com vencimentos divergentes DEVOLVE o dicionário degradado de D-03 (não levanta `ValueError`) — a divergência é fato sobre a entrada, a degeneração é fato sobre o resultado calculado.
    - Regressão: `breakevens` continua `[28.5]` na venda coberta, `[42.0]` na call seca comprada, `[46.5, 53.5]` na borboleta e `[47.0, 58.0]` no condor.
  </behavior>
  <action>
    Escreva PRIMEIRO os testes do bloco `<behavior>`, confirme que falham nos
    valores esperados (rodando, não por leitura), e só então implemente.

    **Guarda de entrada degenerada (D-04.1).** Insira em
    `perfil_da_estrutura` imediatamente DEPOIS de `inclinacao_direita` ser
    calculada (linhas atuais 198-200) e antes de `resultados` /
    `ganho_ilimitado`. A condição é de TRÊS partes, todas obrigatórias:
    `custo == 0` E todo ponto de `curva` com `resultado == 0` E
    `inclinacao_direita == 0`. Duas partes não bastam — com duas partes o box
    travado em zero (custo 5, curva zerada, cauda plana) seria recusado por
    engano, e ele é uma estrutura legítima. Levante `ValueError` seguindo a
    convenção de erro de escopo-de-estrutura já usada na linha 175 (sem
    prefixo `perna {i}:`, frase PT-BR direta, sem hedging), dizendo que não há
    exposição: custo líquido e resultado são zero em qualquer preço, não há o
    que analisar. A redação exata é sua (Claude's Discretion em D-04), o
    conteúdo semântico não é.

    Não mova a construção de `curva` nem o cálculo de `custo` — a guarda
    precisa dos dois e construir a curva antes de recusar é aritmética pura e
    barata, sem I/O.

    **Correção de `_breakevens` (D-04.2).** A regra que fica valendo, e que
    deve estar escrita no docstring da função: um ponto de resultado zero só é
    breakeven quando a curva DEIXA o zero indo para a direita. Em concreto,
    duas mudanças cirúrgicas e nada mais:
    1. No laço `for anterior, atual in zip(curva, curva[1:])`, a condição
       `if y0 == 0:` passa a exigir também que o próximo ponto seja não-zero
       (`y1 != 0`). É isso que apaga o `0.0` espúrio da call de prêmio zero,
       do ratio e do box travado em zero, preservando o `50.0` do ratio (onde
       a curva de fato sai do zero).
    2. Na cauda, `if ultimo["resultado"] == 0:` só acrescenta
       `ultimo["preco_objeto"]` quando `inclinacao_direita` for diferente de
       zero. Com inclinação zero a curva continua em zero para sempre à
       direita, e o último strike não é cruzamento nenhum. O ramo `elif
       inclinacao_direita:` que calcula `adiante` fica INTACTO.

    NÃO toque no ramo de interpolação (linhas atuais 246-247): é ele que
    produz `[49.42]` do caso golden e `[48.0, 52.0]` do straddle, está certo.
    Não reescreva a função, não troque o algoritmo por outro, não introduza
    varredura por intervalos — a correção é de condição, não de arquitetura.

    **Precedência das guardas.** A checagem de vencimentos divergentes da
    Task 1 já devolve antes da curva ser montada, e a guarda degenerada roda
    depois — então a precedência exigida (divergência primeiro) já é
    consequência da posição no código. Não a deixe implícita: escreva o teste
    que a prova e um comentário de uma linha no código dizendo por que a ordem
    é essa.
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/test_opcoes_payoff.py -x -q</automated>
    <automated>cd server && .venv/bin/python -m pytest tests/ -q -k "opcoes or payoff"</automated>
  </verify>
  <acceptance_criteria>
    - Os 6 comportamentos do bloco `<behavior>` passam como testes nomeados.
    - Prova negativa REAL (executada, não descrita): reverta à mão a condição `y1 != 0` da correção 1, rode `pytest tests/test_opcoes_payoff.py` e confirme que os testes de call-prêmio-zero / ratio / box falham nomeando os breakevens espúrios; desfaça a reversão e confirme verde de novo. Registre as duas saídas no SUMMARY.
    - `cd server && .venv/bin/python -m pytest tests/ -q -k "opcoes"` passa inteiro — nenhum teste de `opcoes_motor`/`opcoes_lastreadas`/`opcoes_curadoria`/`options_mcp_api` regride.
    - `git diff` no módulo mostra alteração apenas nas duas condições descritas dentro de `_breakevens` e na guarda nova de `perfil_da_estrutura` — o ramo de interpolação aparece inalterado no diff.
    - Nenhum arquivo novo criado (D-01).
  </acceptance_criteria>
  <done>Estrutura sem exposição é recusada com mensagem clara; breakeven espúrio em `S=0` some sem derrubar nenhum breakeven legítimo; divergência de vencimento tem precedência provada por teste.</done>
</task>

<task type="auto">
  <name>Task 3: travar em teste os casos-limite de PAYOFF-02 e rodar a suíte canônica</name>
  <files>server/tests/test_opcoes_payoff.py</files>
  <read_first>
    - server/tests/test_opcoes_payoff.py (estado após as Tasks 1-2)
    - .planning/REQUIREMENTS.md (PAYOFF-02 — a lista de casos-limite é o checklist desta task)
    - .planning/phases/36-motor-de-payoff-gen-rico/36-CONTEXT.md (D-07)
    - CLAUDE.md seção "Validação obrigatória"
  </read_first>
  <action>
    Acrescente um bloco de testes nomeados "Parte 3 — casos-limite de
    PAYOFF-02", seguindo o padrão de nome e de asserção multi-campo já usado
    em `test_perfil_collar_travado_dos_dois_lados`. Use os números já medidos
    na seção `<interfaces>` deste plano — eles saíram de execução direta do
    módulo, não de dedução; se algum divergir na sua execução, PARE e reporte
    em vez de ajustar o teste ao valor observado.

    Um teste nomeado por caso, sem parametrizar (a lista de PAYOFF-02 é um
    checklist auditável — um `pytest.mark.parametrize` esconderia qual caso
    caiu):
    - Straddle comprado (CALL 50 prêmio 1 + PUT 50 prêmio 1): `breakevens`
      `[48.0, 52.0]`, dois breakevens, `ganho_ilimitado is True`.
    - Strangle comprado (PUT compra 45 prêmio 1 + CALL compra 55 prêmio 1):
      dois breakevens, um abaixo de 45 e outro acima de 55.
    - Borboleta (CALL compra 45 pr. 6, CALL venda 50 pr. 3 quantidade 2, CALL
      compra 55 pr. 1.5): `custo_liquido` 1.5, `breakevens` `[46.5, 53.5]`,
      `ganho_maximo` 3.5, `perda_maxima` 1.5, nenhum lado ilimitado.
    - Condor (CALL compra 45 pr. 6, CALL venda 50 pr. 3, CALL venda 55 pr.
      1.5, CALL compra 60 pr. 0.5): `breakevens` `[47.0, 58.0]`,
      `ganho_maximo` 3.0, `perda_maxima` 2.0, e o platô central provado pelos
      pontos de `curva` em 50.0 e 55.0 terem o MESMO resultado 3.0.
    - Box de resultado constante NÃO-zero (CALL compra 50 pr. 6, CALL venda 55
      pr. 3, PUT compra 55 pr. 3.5, PUT venda 50 pr. 2): `custo_liquido` 4.5,
      todo ponto de `curva` com o mesmo `resultado` 0.5, `breakevens == []`,
      `ganho_maximo` 0.5, `perda_maxima` 0.0, nenhum lado ilimitado. Este é o
      caso que D-04 nomeia como diferente do degenerado — o teste existe para
      que a guarda de três partes nunca degenere para duas.
    - Quantidades assimétricas entre pernas (ratio 1x2 já usado na Task 2, mas
      aqui asserindo o efeito da quantidade): `custo_liquido` 0.0 com prêmios
      3 e 1.5×2, `perda_ilimitada is True`, `ganho_maximo` 5.0 — a quantidade
      é número de CONTRATOS e nenhum fator de lote entra no módulo (D-02).
    - Trava de baixa com puts (PUT compra 50 prêmio 3 + PUT venda 45 prêmio 1):
      nenhum lado ilimitado, `ganho_maximo` e `perda_maxima` finitos e
      somando o span menos/mais o custo, um breakeven só.
    - Venda descoberta (CALL venda 40 prêmio 2): já coberta por
      `test_perfil_call_seca_vendida_perda_ilimitada` — NÃO duplique, cite o
      teste existente num comentário do bloco novo para o checklist de
      PAYOFF-02 ficar rastreável inteiro.

    Acrescente também um comentário de cabeçalho no bloco mapeando cada teste
    ao item de PAYOFF-02 que ele trava, para a Fase 37 e o verificador
    conseguirem auditar cobertura por leitura.

    Depois disso, rode a suíte canônica INTEIRA do repositório (as duas
    suítes, pytest e `.mjs`) e registre as contagens. A baseline conhecida é
    2923 passed / 5 skipped / 3 xfailed / 0 failed no pytest e 152 arquivos
    `.mjs` (com a falha ambiental pré-existente conhecida de
    `test_ios_assets.mjs` quando `web/ios/` não existe neste worktree —
    documentada em `CLAUDE.md`, não é regressão). Esta fase não toca uma
    linha de `web/`, então o lado `.mjs` tem de ficar IDÊNTICO à baseline; o
    pytest sobe exatamente pelo número de testes novos das Tasks 1-3.

    Se `bash scripts/executar.sh --testes` travar por causa do achado
    ambiental deste host (`lsof`/`ps` pendurados, registrado no STATE.md da
    Fase 35), NÃO substitua silenciosamente por meia baseline: rode as duas
    suítes à mão (`cd server && .venv/bin/python -m pytest tests/ -q` e cada
    `node web/tests/*.mjs`), e declare no SUMMARY que o wrapper canônico
    travou e qual contorno foi usado.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - Cada item de PAYOFF-02 tem um teste nomeado correspondente em `server/tests/test_opcoes_payoff.py`, ou um comentário citando nominalmente o teste pré-existente que já o cobre. Auditável por: `grep -c "^def test_" server/tests/test_opcoes_payoff.py` maior que 25 (baseline atual) e o cabeçalho do bloco "Parte 3" listando os itens.
    - `bash scripts/executar.sh --testes` termina com exit 0, `0 failed` no pytest, e a contagem de passed é exatamente a baseline 2923 mais o número de testes novos desta onda (declare o número no SUMMARY).
    - O lado `.mjs` fica idêntico à baseline (nenhum arquivo de `web/` foi tocado: `git diff --stat -- web/ | wc -l` devolve 0).
    - Nenhum `NaN` sai do motor em nenhum caso-limite: `cd server && .venv/bin/python -c "import sys,math; sys.path.insert(0,'.'); from app import opcoes_payoff as m; r=m.perfil_da_estrutura([{'tipo':'CALL','lado':'compra','strike':45,'premio':6},{'tipo':'CALL','lado':'venda','strike':50,'premio':3,'quantidade':2},{'tipo':'CALL','lado':'compra','strike':55,'premio':1.5}]); assert not any(isinstance(v,float) and math.isnan(v) for v in [r['custo_liquido'], r['ganho_maximo'], r['perda_maxima']] if v is not None); print('ok')"` imprime `ok` (PAYOFF-02: nunca `NaN`).
  </acceptance_criteria>
  <done>Todos os casos-limite de PAYOFF-02 travados em teste nomeado; suíte canônica verde com o lado `.mjs` idêntico à baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chamador → `perfil_da_estrutura` | pernas chegam de rota HTTP (`options_mcp_api`) via adaptadores de `opcoes_motor`; conteúdo é entrada não confiável |
| nenhuma outra | módulo puro: sem rede, sem banco, sem LLM, sem relógio, sem escrita em disco |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering | `_validar_perna` — campo `vencimento` novo | mitigate | `vencimento` é passthrough opaco, nunca usado em `eval`/parse/comparação com relógio; só igualdade entre pernas. Nenhum caminho de código o interpreta como data |
| T-36-02 | Denial of Service | `perfil_da_estrutura` — curva cresce com o número de pernas | accept | curva é O(n) sobre strikes distintos; as rotas que chamam já limitam o número de pernas a montagens de até 4; risco baixo, sem alvo de valor |
| T-36-03 | Information disclosure | mensagens de `ValueError` | mitigate | mensagens citam índice de perna e campo, nunca dado de conta/usuário — convenção já existente em `_validar_perna`, preservada |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | zero instalação de pacote nesta fase; nenhuma dependência nova (só stdlib/`typing`), auditável por `grep -n "^from\|^import" server/app/opcoes_payoff.py` |
</threat_model>

<verification>
1. `bash scripts/executar.sh --testes` (suíte canônica, AS DUAS suítes) com exit 0 e `0 failed`.
2. `cd server && .venv/bin/python -m pytest tests/ -q -k "opcoes"` verde — nenhum dos 4 consumidores regride.
3. `git diff --stat` lista exatamente 2 arquivos, ambos previstos no frontmatter; nenhum arquivo novo (D-01).
4. `git diff --stat -- web/` vazio (fase Python-only).
5. Prova negativa da correção de `_breakevens` executada e registrada no SUMMARY (reverter, ver falhar nomeando, restaurar, ver passar).
</verification>

<success_criteria>
- `vencimento` aceito por perna, opcional, default `None`, sem mudança de comportamento para os 4 consumidores atuais (D-03).
- Vencimentos divergentes devolvem estado explícito com `motivo` declarado e sem curva (D-03).
- Entrada degenerada recusada com `ValueError` de mensagem clara; box travado em zero e call de prêmio zero continuam aceitos (D-04.1).
- `breakevens` não reporta mais `S=0` espúrio, e nenhum breakeven legítimo some (D-04.2).
- Casos-limite de PAYOFF-02 travados em testes nomeados (D-07, parte 1).
- Suíte canônica verde, lado `.mjs` idêntico à baseline.
</success_criteria>

<output>
Create `.planning/phases/36-motor-de-payoff-gen-rico/36-01-SUMMARY.md` when done.
NÃO edite `.planning/STATE.md` nem `.planning/ROADMAP.md` — guardrail do
repositório: quem edita esses dois é o orquestrador, à mão, depois da onda.
</output>
