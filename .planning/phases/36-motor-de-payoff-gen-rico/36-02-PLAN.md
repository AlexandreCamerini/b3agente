---
phase: 36-motor-de-payoff-gen-rico
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - server/app/opcoes_payoff.py
  - server/tests/test_opcoes_payoff.py
autonomous: true
requirements: [PAYOFF-01, PAYOFF-03]

must_haves:
  truths:
    - "D-01: nenhum arquivo novo é criado — as duas funções novas nascem dentro de `server/app/opcoes_payoff.py`, ao lado de `perfil_da_estrutura()`, e os testes dentro de `server/tests/test_opcoes_payoff.py`"
    - "D-05: existe função que devolve domínio X (min/max strike com margem especificada, spot sempre dentro) e domínio Y (inclui zero, 15% além do extremo finito), sinalizando lado ilimitado com `None` em vez de desenhar teto/piso falso"
    - "D-06: existe função que devolve a lista de segmentos da curva, da esquerda para a direita, com `de`/`ate`/`inclinacao`/`e_plato`, cortando SÓ nos strikes (nunca nos breakevens) e com a cauda direita como segmento de `ate` nulo"
    - "D-07: domínio e segmentos estão travados em teste para pelo menos 3 estruturas diferentes (unária ilimitada, travada dos dois lados, 2+ breakevens), e o caso golden do Alex é teste de regressão NOMEADO travando breakeven 49,42, ganho/perda máx 0,25 por unidade (R$ 25,00 no lote de 100), 3 segmentos e nenhum lado ilimitado"
    - "Nenhum ponto do motor decide o cálculo pelo nome da estratégia — auditável por leitura direta e provado por teste de independência de nome (PAYOFF-01)"
    - "D-02: o lote continua multiplicando FORA do módulo; breakeven continua preço e nunca é multiplicado — o teste golden prova as duas coisas"
  artifacts:
    - path: "server/app/opcoes_payoff.py"
      provides: "dominio_da_curva() e segmentos_da_curva(), funções puras novas"
      contains: "def segmentos_da_curva"
    - path: "server/tests/test_opcoes_payoff.py"
      provides: "regressão nomeada do caso golden + cobertura de domínio e segmentos"
      contains: "def test_golden_trava_de_alta"
  key_links:
    - from: "server/app/opcoes_payoff.py::segmentos_da_curva"
      to: "server/app/opcoes_payoff.py::perfil_da_estrutura"
      via: "consome o dicionário já calculado (curva + ganho_ilimitado/perda_ilimitada), não recalcula aritmética"
      pattern: "perfil\\[.curva.\\]"
    - from: "server/tests/test_opcoes_payoff.py"
      to: "server/app/opcoes_payoff.py"
      via: "from app import opcoes_payoff as m"
      pattern: "from app import opcoes_payoff"
---

<objective>
Entregar os dois dados novos que a Fase 37 consome — domínio X/Y do gráfico
(D-05) e a lista estruturada de segmentos da curva (D-06) — como funções puras
ao lado de `perfil_da_estrutura()`, e fechar a fase com o caso golden do Alex
travado como teste de regressão NOMEADO (PAYOFF-03).

Purpose: a Fase 37 desenha eixos e escreve uma frase por segmento. Se ela
calcular o domínio ou segmentar a curva por conta própria, nasce a segunda
fonte de verdade que D-01 existe para evitar (precedentes reais neste repo:
`RR_MIN` na Fase 6, CTA de collar na Fase 32). Aqui o cálculo fica no motor; lá
fica só o traço.

Output: `dominio_da_curva()` e `segmentos_da_curva()` em
`server/app/opcoes_payoff.py`; cobertura de D-05/D-06/D-07 e o teste golden em
`server/tests/test_opcoes_payoff.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-motor-de-payoff-gen-rico/36-CONTEXT.md
@.planning/phases/36-motor-de-payoff-gen-rico/36-PATTERNS.md
@.planning/phases/36-motor-de-payoff-gen-rico/36-01-SUMMARY.md
@CLAUDE.md

<interfaces>
Estado do módulo APÓS a onda 1 (leia o `36-01-SUMMARY.md` para confirmar):
`perfil_da_estrutura(pernas)` devolve `pernas`, `custo_liquido`, `fluxo`,
`ganho_maximo`, `perda_maxima`, `ganho_ilimitado`, `perda_ilimitada`,
`breakevens`, `delta_total`, `curva`, `unidade`, `vencimentos`.

Ponto crítico de desenho: `inclinacao_direita` é variável LOCAL de
`perfil_da_estrutura`, NÃO sai no dicionário. As funções novas desta onda
recebem o dicionário pronto — a inclinação da cauda se lê dos booleanos que já
existem: `ganho_ilimitado is True` significa cauda positiva,
`perda_ilimitada is True` significa cauda negativa, nenhum dos dois significa
cauda plana. Não acrescente `inclinacao_direita` ao retorno nem recalcule a
soma de sinais — D-06 diz explicitamente "não recalcula nada que a função já
sabe".

Cada item de `perfil["pernas"]` traz `contrato`, `tipo`, `lado`, `quantidade`,
`strike`, `premio`. A perna `ACAO` tem `strike` 0.0 — que é constante de
cálculo, NÃO um strike de verdade (ver comentário nas linhas 81-87 do módulo).

Números já medidos por execução direta nesta sessão (verdade de partida —
se sua execução divergir, PARE e reporte, não ajuste o teste):
- Golden (CALL compra 49.17 pr. 0.40 + CALL venda 49.67 pr. 0.15):
  `custo_liquido` 0.25, `breakevens` [49.42], `ganho_maximo` 0.25,
  `perda_maxima` 0.25, ambos ilimitados `False`,
  `curva` [(0.0, -0.25), (49.17, -0.25), (49.67, 0.25)].
- CALL seca comprada 40 prêmio 2: `curva` [(0.0, -2.0), (40.0, -2.0)],
  `ganho_ilimitado` True, `perda_maxima` 2.0.
- Venda coberta (ACAO pr. 30 + CALL venda 32 pr. 1.5): `curva`
  [(0.0, -28.5), (32.0, 3.5)], `ganho_maximo` 3.5, `perda_maxima` 28.5,
  nenhum lado ilimitado.
- Straddle comprado (CALL 50 pr. 1 + PUT 50 pr. 1): `curva`
  [(0.0, 48.0), (50.0, -2.0)], `breakevens` [48.0, 52.0],
  `ganho_ilimitado` True.
- Condor (CALL compra 45 pr. 6, CALL venda 50 pr. 3, CALL venda 55 pr. 1.5,
  CALL compra 60 pr. 0.5): `curva`
  [(0,-2),(45,-2),(50,3),(55,3),(60,-2)], nenhum lado ilimitado.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `dominio_da_curva()` — domínio X e Y para o gráfico (D-05)</name>
  <files>server/app/opcoes_payoff.py, server/tests/test_opcoes_payoff.py</files>
  <read_first>
    - server/app/opcoes_payoff.py (arquivo inteiro no estado pós-onda-1, leitura única)
    - server/tests/test_opcoes_payoff.py (arquivo inteiro no estado pós-onda-1)
    - .planning/phases/36-motor-de-payoff-gen-rico/36-CONTEXT.md (D-05 e a seção "Claude's Discretion")
    - .planning/phases/36-motor-de-payoff-gen-rico/36-PATTERNS.md (seção "D-05 — Domain X/Y function", que traz a aritmética da margem já conferida à mão)
    - .planning/phases/36-motor-de-payoff-gen-rico/36-01-SUMMARY.md
  </read_first>
  <behavior>
    - Golden com `spot=49.40`: `margem` 1.976 (vence o termo de 4% do spot sobre os 12% do span de 0.50, que dariam 0.06), `x_min` 47.194, `x_max` 51.646, `y_max` 0.2875, `y_min` -0.2875, spot já dentro (sem expansão).
    - Golden com `spot=55.0`: `margem` 2.2, domínio base [46.97, 51.87] e `x_max` expandido para exatamente 55.0 (`x_min` segue 46.97) — cláusula "expandir o lado que falta até incluir o spot".
    - Golden com `spot=None`: `margem` 0.06 (só o termo do span), `x_min` 49.11, `x_max` 49.73, e `motivo` não-nulo declarando que o domínio foi calculado sem spot.
    - CALL seca comprada 40 prêmio 2, `spot=41`: strike único → `margem` 4.0 (10% do strike), `x_min` 36.0, `x_max` 44.0, `y_max is None` (ganho ilimitado — nunca um teto inventado), `y_min` -2.3.
    - Venda coberta (ACAO + CALL venda 32), `spot=31`: `x_min` 28.8 e `x_max` 35.2 — prova que o `strike` 0.0 da perna ACAO NÃO entra no cálculo de span/mínimo (se entrasse, `x_min` colaria em 0.0).
    - Straddle comprado (2 breakevens), `spot=50`: domínio X calculado sobre o strike único 50 e `y_max is None`, `y_min` finito.
    - Estrutura só com perna ACAO (prêmio 30) e sem spot: âncora é o preço do papel, `x_min` 27.0, `x_max` 33.0.
    - Perfil com `vencimentos["divergentes"] is True` (calendário da onda 1): devolve `x_min`/`x_max`/`y_min`/`y_max` todos `None` e `motivo` citando a divergência — nunca um domínio inventado sobre curva vazia.
  </behavior>
  <action>
    Escreva PRIMEIRO os testes do bloco `<behavior>` com os números literais
    acima, rode, confirme falha, só então implemente.

    Crie `dominio_da_curva(perfil, spot=None)` como função de topo em
    `server/app/opcoes_payoff.py`, logo depois de `perfil_da_estrutura`.
    Recebe o dicionário JÁ calculado mais o spot — não remonta `normalizadas`,
    não chama `_validar_perna`, não reconstrói curva. Devolve dicionário com
    `x_min`, `x_max`, `y_min`, `y_max`, `margem`, `spot`, `ganho_ilimitado`,
    `perda_ilimitada`, `motivo`.

    Regras, todas obrigatórias e travadas em teste:
    1. Guarda de divergência primeiro: se `perfil["vencimentos"]["divergentes"]`
       for verdadeiro, devolva tudo `None` com `motivo` citando a divergência.
       Sem isso, `ganho_maximo`/`perda_maxima` nulos da degradação seriam lidos
       como "ilimitado" e o gráfico desenharia uma seta onde não há curva.
    2. Strikes considerados são só os das pernas de `tipo` `CALL` ou `PUT`. A
       perna `ACAO` tem `strike` 0.0 por convenção de cálculo, não por ser um
       strike — incluí-la ancoraria o eixo em zero e esmagaria qualquer venda
       coberta contra a margem esquerda.
    3. Com dois ou mais strikes distintos: `span` é máximo menos mínimo e
       `margem` é `max(0.12 * span, 0.04 * spot)` quando `spot` é número, ou só
       `0.12 * span` quando não é (e aí `motivo` declara a degradação).
    4. Com exatamente um strike de opção: `margem` é `0.10 * strike` (D-05,
       caso "perna única, sem span"), sem combinar com o termo do spot.
    5. Sem nenhum strike de opção (estrutura só de ACAO): âncora é o `spot`
       quando numérico, senão o maior `premio` entre as pernas `ACAO` (que é o
       preço do papel, ver comentário das linhas 93-95 do módulo); domínio é
       âncora ±10%, e `motivo` declara qual âncora foi usada.
    6. `x_min` é `min(strike) - margem` limitado em baixo por 0.0 — preço do
       objeto negativo não existe neste módulo (`resultado_no_vencimento`
       recusa `s < 0`). `x_max` é `max(strike) + margem`.
    7. Se `spot` for numérico e cair fora, expanda o lado que falta até
       EXATAMENTE o spot (`x_min = min(x_min, spot)`, `x_max = max(x_max,
       spot)`), sem margem extra além dele — D-05 diz "até incluí-lo".
    8. `y_max` é `None` quando `ganho_ilimitado`, senão
       `max(ganho_maximo, 0.0) * 1.15`. `y_min` é `None` quando
       `perda_ilimitada`, senão `-max(perda_maxima, 0.0) * 1.15`. O zero fica
       sempre dentro por construção (um lado é não-negativo, o outro
       não-positivo). Nunca devolva `0.0` no lugar de "sem teto" — é
       literalmente o guardrail do docstring do módulo (linhas 13-17) e o
       mesmo do guardião `test_m3_format_pede_null_nunca_zero`.
    9. Todo float que sai passa por `round(x, 4)`, como todo o resto do módulo
       (linhas 122, 158, 186-187, 206, 211).

    Docstring da função explicando por que ela é separada de
    `perfil_da_estrutura`: domínio é pergunta de quem DESENHA, e misturá-la na
    função de cálculo financeiro puro é o que o CONTEXT proíbe — a separação é
    o desenho, não descuido.
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/test_opcoes_payoff.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Os 8 comportamentos do bloco `<behavior>` passam como testes nomeados distintos (`test_dominio_<cenario>`), cobrindo ao menos 3 formas de estrutura diferentes: unária ilimitada, travada dos dois lados e 2+ breakevens (D-07).
    - `cd server && .venv/bin/python -c "import sys; sys.path.insert(0,'.'); from app import opcoes_payoff as m; p=m.perfil_da_estrutura([{'tipo':'CALL','lado':'compra','strike':40,'premio':2}]); d=m.dominio_da_curva(p, spot=41); print(d['x_min'], d['x_max'], d['y_max'], d['y_min'])"` imprime `36.0 44.0 None -2.3`.
    - `grep -n "def dominio_da_curva" server/app/opcoes_payoff.py` encontra a função em `server/app/opcoes_payoff.py` — e `git status --porcelain` não lista nenhum arquivo novo (D-01).
    - O módulo continua puro: `grep -n "^from\|^import" server/app/opcoes_payoff.py` mostra só `from __future__` e `from typing`.
    - `cd server && .venv/bin/python -m pytest tests/ -q -k "opcoes"` verde — nenhum consumidor regride.
  </acceptance_criteria>
  <done>`dominio_da_curva()` devolve domínio X com spot sempre dentro e domínio Y que inclui zero e sinaliza lado ilimitado com `None`, travado em teste para 3+ estruturas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `segmentos_da_curva()` — leitura da curva segmento a segmento (D-06)</name>
  <files>server/app/opcoes_payoff.py, server/tests/test_opcoes_payoff.py</files>
  <read_first>
    - server/app/opcoes_payoff.py (estado após a Task 1; releia só a região nova se já leu o arquivo)
    - .planning/phases/36-motor-de-payoff-gen-rico/36-CONTEXT.md (D-06 e o bloco `<specifics>`, que traz o exemplo do golden JÁ CORRIGIDO — segmentos cortam só nos strikes, nunca nos breakevens)
    - .planning/phases/36-motor-de-payoff-gen-rico/36-PATTERNS.md (seção "D-06 — Segments function", incluindo a aritmética recomputada à mão)
  </read_first>
  <behavior>
    - Golden: exatamente 3 segmentos — `{"de": 0.0, "ate": 49.17, "inclinacao": "zero", "e_plato": True}`, `{"de": 49.17, "ate": 49.67, "inclinacao": "positiva", "e_plato": False}`, `{"de": 49.67, "ate": None, "inclinacao": "zero", "e_plato": True}`. A fronteira é 49.17, NÃO 49.42: o breakeven cai DENTRO do segundo segmento e continua sendo reportado só por `breakevens`.
    - CALL seca comprada 40 prêmio 2 (unária ilimitada): 2 segmentos — `(0.0 → 40.0, "zero", platô)` e `(40.0 → None, "positiva", não-platô)`.
    - Condor (2 breakevens + platô central): 5 segmentos, com o terceiro sendo `{"de": 50.0, "ate": 55.0, "inclinacao": "zero", "e_plato": True}` e o último `{"de": 60.0, "ate": None, "inclinacao": "zero", "e_plato": True}`.
    - Straddle comprado: 2 segmentos — `(0.0 → 50.0, "negativa")` e `(50.0 → None, "positiva")` — apesar dos 2 breakevens, porque breakeven não corta segmento.
    - Estrutura só com perna ACAO: 1 segmento só, `{"de": 0.0, "ate": None, "inclinacao": "positiva", "e_plato": False}` (curva tem um ponto só, não há par).
    - Perfil com `vencimentos["divergentes"] is True`: devolve `[]` — não há curva, e inventar segmento sobre curva vazia seria a aproximação que D-03 proíbe.
    - Primeiro segmento sempre começa em `de` 0.0 e nunca em `None`: preço do objeto negativo não existe neste módulo (assimetria proposital com `ate`).
  </behavior>
  <action>
    Escreva PRIMEIRO os testes do bloco `<behavior>` com os dicionários
    literais acima, rode, confirme falha, só então implemente.

    Crie `segmentos_da_curva(perfil)` como função de topo em
    `server/app/opcoes_payoff.py`, depois de `dominio_da_curva`. Devolve lista
    de dicionários com as chaves `de`, `ate`, `inclinacao` e `e_plato`, na
    ordem da esquerda para a direita.

    Mecânica, seguindo o mesmo formato de caminhada de `_breakevens` (percorre
    `curva` aos pares com `zip(curva, curva[1:])`):
    1. Um segmento por par adjacente de pontos de `curva`: `de` é o
       `preco_objeto` do anterior, `ate` o do atual, `inclinacao` é
       `"positiva"`, `"negativa"` ou `"zero"` conforme o sinal de
       `resultado` atual menos anterior, e `e_plato` é verdadeiro exatamente
       quando `inclinacao` é `"zero"`.
    2. Um segmento final de cauda, sempre presente quando há ao menos um ponto
       de curva: `de` é o `preco_objeto` do último ponto, `ate` é `None`
       (sem teto), e a inclinação vem dos booleanos que já existem —
       `ganho_ilimitado` verdadeiro é `"positiva"`, `perda_ilimitada`
       verdadeiro é `"negativa"`, nenhum dos dois é `"zero"`. NÃO recalcule a
       soma de sinais nem exponha `inclinacao_direita` no retorno de
       `perfil_da_estrutura`.
    3. NÃO funda segmentos adjacentes de mesmo sinal. Duas calls compradas de
       strikes diferentes produzem dois trechos positivos com inclinações
       aritméticas diferentes (+1 e depois +2), e fundi-los apagaria a
       dobra — além de quebrar a contagem de 3 segmentos que PAYOFF-03 exige
       do caso golden.
    4. NÃO corte segmento em breakeven. Esta é a leitura fechada em
       `36-CONTEXT.md` (bloco `<specifics>`, corrigido depois do pattern-map):
       a curva só dobra nos strikes; no breakeven só o SINAL cruza zero, dentro
       de um trecho que continua reto. A Fase 37 usa `breakevens` como frase de
       PONTO ("empate em 49,42"), separada da frase de segmento.
    5. `perfil["curva"]` vazia devolve lista vazia.
    6. Todo float que sai passa por `round(x, 4)`.

    Docstring declarando a regra de corte (só strikes) e o motivo — é
    exatamente o ponto em que um implementador futuro tentaria "melhorar"
    cortando no breakeven e quebraria PAYOFF-03.
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/test_opcoes_payoff.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Os 7 comportamentos do bloco `<behavior>` passam como testes nomeados (`test_segmentos_<cenario>`), cobrindo as mesmas 3+ formas de estrutura exigidas por D-07.
    - `cd server && .venv/bin/python -c "import sys; sys.path.insert(0,'.'); from app import opcoes_payoff as m; p=m.perfil_da_estrutura([{'tipo':'CALL','lado':'compra','strike':49.17,'premio':0.40},{'tipo':'CALL','lado':'venda','strike':49.67,'premio':0.15}]); import json; print(json.dumps(m.segmentos_da_curva(p)))"` imprime exatamente 3 segmentos com fronteiras 49.17 e 49.67 e nenhuma fronteira em 49.42.
    - Prova negativa REAL (executada, não descrita): acrescente à mão um corte em breakeven na função, rode `pytest tests/test_opcoes_payoff.py` e confirme que o teste do golden falha apontando 4 segmentos; desfaça e confirme verde. Registre as duas saídas no SUMMARY.
    - `git status --porcelain` não lista arquivo novo (D-01).
  </acceptance_criteria>
  <done>`segmentos_da_curva()` devolve a leitura segmento a segmento, cortando só nos strikes, com cauda de `ate` nulo, travada em teste para 3+ estruturas e com prova negativa do corte em breakeven.</done>
</task>

<task type="auto">
  <name>Task 3: caso golden como regressão nomeada (PAYOFF-03), auditoria de PAYOFF-01 e suíte canônica</name>
  <files>server/tests/test_opcoes_payoff.py</files>
  <read_first>
    - server/tests/test_opcoes_payoff.py (estado após as Tasks 1-2)
    - .planning/REQUIREMENTS.md (PAYOFF-01 e PAYOFF-03, literais)
    - .planning/phases/36-motor-de-payoff-gen-rico/36-CONTEXT.md (bloco `<specifics>`, o caso golden com lote 100; D-02 sobre lote multiplicar fora)
    - server/app/options_mcp_api.py linhas 1274-1300 (`_vezes_lote` e o comentário de por que breakeven NÃO é convertido — é o padrão que o teste golden precisa espelhar)
    - CLAUDE.md seção "Validação obrigatória"
  </read_first>
  <action>
    **1. Teste golden nomeado (PAYOFF-03).** Crie um único teste chamado
    `test_golden_trava_de_alta_49_17_49_67_debito_025` (nome que se lê como
    especificação, padrão do arquivo) travando, num só lugar, tudo o que o
    requisito exige da trava de alta com calls, strikes 49,17 e 49,67, débito
    0,25: `custo_liquido` 0.25 e `fluxo` "debito"; `breakevens` exatamente
    `[49.42]`; `ganho_maximo` 0.25 e `perda_maxima` 0.25 por unidade do objeto;
    `ganho_ilimitado` e `perda_ilimitada` ambos `False`; `segmentos_da_curva`
    devolvendo exatamente 3 segmentos com as fronteiras 49.17 e 49.67.

    No mesmo teste, prove as duas metades de D-02 sobre o lote de 100, que é o
    que o requisito quer dizer com "R$ 25,00": multiplique `ganho_maximo` e
    `perda_maxima` por 100 DENTRO DO TESTE (o módulo nunca faz isso) e asserte
    25.0 nos dois; e asserte que `breakevens[0]` continua 49.42 — preço, nunca
    multiplicado. Comente no teste que a multiplicação é responsabilidade de
    `_vezes_lote` em `options_mcp_api.py` e que breakeven fica de propósito
    fora de `_em_reais` (o comentário das linhas 1283-1289 de lá explica por
    quê); assim o teste documenta a fronteira em vez de só medir um número.

    Se qualquer um desses valores divergir ao rodar, PARE e reporte — eles
    vieram de execução direta do módulo, não de dedução.

    **2. Auditoria de PAYOFF-01 (nenhuma lógica por nome de estratégia).**
    Acrescente `test_motor_nao_decide_pelo_nome_da_estrutura`: monte a mesma
    trava de alta duas vezes, uma com `contrato` valendo nomes neutros e outra
    com nomes que parecem nome de estratégia (por exemplo `"BORBOLETA"` e
    `"STRADDLE"`), e asserte que todo campo do resultado é idêntico exceto o
    passthrough `contrato` dentro de `pernas`. Um motor que ramificasse por
    nome falharia aqui.

    **3. Suíte canônica.** Rode `bash scripts/executar.sh --testes` — AS DUAS
    suítes, pytest e `web/tests/*.mjs`. `scripts/test.sh` sozinho é meia
    baseline e não conta. Esta fase não toca uma linha de `web/`, então o lado
    `.mjs` tem de ficar idêntico à baseline (152 arquivos, com a falha
    ambiental pré-existente de `test_ios_assets.mjs` quando `web/ios/` não
    existe neste worktree — documentada no `CLAUDE.md`, não é regressão). O
    pytest sobe exatamente pelo número de testes novos das duas ondas; declare
    a contagem final e a diferença contra a baseline 2923 no SUMMARY.

    Se `bash scripts/executar.sh --testes` travar pelo achado ambiental deste
    host (`lsof`/`ps` pendurados, registrado no STATE.md da Fase 35), NÃO
    substitua silenciosamente por meia baseline: rode as duas suítes à mão e
    declare no SUMMARY que o wrapper travou e qual contorno foi usado.

    **4. Não publique nada.** Esta fase é backend puro, sem efeito de front e
    sem rota nova — nenhum `bump.sh`, nenhum `publicar-web.sh`, nenhum push
    para `origin/main`. Publicação só quando a Fase 37 entregar o consumidor.
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/test_opcoes_payoff.py -q</automated>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def test_golden_trava_de_alta" server/tests/test_opcoes_payoff.py` encontra exatamente uma definição — PAYOFF-03 exige teste de regressão NOMEADO, não asserção diluída em teste genérico.
    - O teste golden asserta, no mesmo corpo: breakeven 49.42, ganho e perda máximos 0.25 por unidade, 25.0 após multiplicar por 100 no teste, 3 segmentos, nenhum lado ilimitado.
    - `test_motor_nao_decide_pelo_nome_da_estrutura` passa, e a auditoria de leitura confirma ausência de ramificação por nome: `grep -in "estrategia\|strategy\|borboleta\|straddle\|condor" server/app/opcoes_payoff.py | grep -v '^[0-9]*: *#' | grep -v '"""'` não devolve nenhuma linha de código executável (ocorrências só em comentário/docstring são aceitáveis e devem ser listadas no SUMMARY se houver).
    - `bash scripts/executar.sh --testes` termina com exit 0 e `0 failed` no pytest.
    - `git diff --stat -- web/ | wc -l` devolve 0 e `git status --porcelain` não lista arquivo novo fora de `.planning/` (D-01).
    - Nenhum `BUILD_ID`/`SERVER_BUILD_ID` alterado, nenhum push para `origin/main` nesta fase.
  </acceptance_criteria>
  <done>Caso golden travado como regressão nomeada com as 5 propriedades de PAYOFF-03; independência de nome de estratégia provada por teste e por auditoria de leitura; suíte canônica verde com `.mjs` idêntico à baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chamador → `dominio_da_curva` / `segmentos_da_curva` | recebem o dicionário de perfil já validado mais um `spot` vindo de cotação externa (não confiável) |
| nenhuma outra | funções puras: sem rede, sem banco, sem LLM, sem relógio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-04 | Tampering | `spot` malformado (string, bool, `None`) em `dominio_da_curva` | mitigate | `spot` só é usado quando é número; a checagem segue `_numero` (que recusa `bool` explicitamente, linhas 44-52), e a ausência vira `motivo` declarado em vez de `0.0` |
| T-36-05 | Repudiation / integridade de exibição | domínio Y com lado ilimitado | mitigate | `y_max`/`y_min` viram `None` quando o lado é ilimitado; teto/piso numérico inventado seria a tela afirmando um limite que não existe (princípio 4 do CLAUDE.md) — travado por teste |
| T-36-06 | Information disclosure | `motivo` das degradações | accept | texto fixo em PT-BR sem dado de conta/usuário; mesmo registro das mensagens já existentes |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | zero instalação de pacote nesta fase; nenhuma dependência nova, auditável por `grep -n "^from\|^import" server/app/opcoes_payoff.py` |
</threat_model>

<verification>
1. `bash scripts/executar.sh --testes` (AS DUAS suítes) com exit 0 e `0 failed`.
2. `cd server && .venv/bin/python -m pytest tests/ -q -k "opcoes"` verde — os 4 consumidores intactos.
3. Caso golden reproduzido por execução direta do módulo, com 3 segmentos e fronteiras 49.17/49.67.
4. Prova negativa do corte em breakeven executada e registrada no SUMMARY.
5. `git diff --stat` lista exatamente 2 arquivos de produto, ambos previstos no frontmatter; nenhum arquivo novo (D-01); `web/` intocado.
</verification>

<success_criteria>
- `dominio_da_curva()` e `segmentos_da_curva()` existem dentro de `opcoes_payoff.py`, puras, sem dependência nova (D-05, D-06, D-01).
- Domínio X inclui o spot sempre; domínio Y inclui zero e devolve `None` no lado ilimitado (D-05).
- Segmentos cortam só nos strikes, cauda com `ate` nulo, golden com exatamente 3 (D-06, PAYOFF-03).
- Caso golden é teste de regressão nomeado travando as 5 propriedades do requisito (PAYOFF-03).
- Nenhuma decisão de cálculo por nome de estratégia, provado por teste e por auditoria de leitura (PAYOFF-01).
- Suíte canônica verde, lado `.mjs` idêntico à baseline, nada publicado.
</success_criteria>

<output>
Create `.planning/phases/36-motor-de-payoff-gen-rico/36-02-SUMMARY.md` when done.
NÃO edite `.planning/STATE.md` nem `.planning/ROADMAP.md` — guardrail do
repositório: quem edita esses dois é o orquestrador, à mão, depois da onda.
</output>
