---
phase: 25-planos-comerciais
plan: 03
type: execute
wave: 3
depends_on: ["25-02"]
files_modified:
  - server/app/plan.py
  - server/tests/test_catalogo_planos.py
autonomous: true
requirements: [Fase 2 do 25-CONTEXT — catálogo de planos]

must_haves:
  truths:
    - "Cada plano declara os limites de cada ponto de controle e as funções que libera"
    - "Sem nada configurado, free e pro se comportam EXATAMENTE como hoje"
    - "Limite ausente é sem limite; zero é bloqueio — nunca se confundem"
    - "O valor vigente diz de onde veio: painel, variável de ambiente ou padrão"
  artifacts:
    - path: "server/app/plan.py"
      provides: "catálogo com limites por ponto de controle, funções liberadas e precedência memória → kv → env → default"
      contains: "def limites_do_plano"
    - path: "server/tests/test_catalogo_planos.py"
      provides: "guardião de que nada mudou sem configuração"
      contains: "def test_"
---

<objective>
Fase 2 do `25-CONTEXT.md`. `plan.py:31-42` é hoje o **único** bloco de limites
100% literal do app — todos os outros já têm env, e três já têm kv + admin +
auditoria.

Purpose: o plano precisa deixar de ser dois dicts fixos e virar um catálogo
configurável, para que a Fase 3 (gates) e a Fase 4 (portal) tenham o que ler
e escrever.

**Este plano NÃO muda comportamento.** Ele cria a estrutura e prova, por
teste, que sem configuração tudo segue idêntico. Quem passa a LER o catálogo
é o 25-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-planos-comerciais/25-CONTEXT.md
@.planning/phases/25-planos-comerciais/25-02-SUMMARY.md
@CLAUDE.md

<pontos_de_controle_mapeados>
Da varredura completa. **Entram no catálogo** (limite POR USUÁRIO, é produto):

| chave sugerida | o que limita | hoje vem de | default de hoje |
|---|---|---|---|
| `max_analyses_per_month` | análises/mês | `plan.py:34` (literal) | free 30, pro `None` |
| `max_watchlist` | ativos na watchlist | `plan.py:33` (literal) | free 10, pro `None` |
| `ia_gerenciada_dia` | análises/dia na chave do servidor | `managed.daily_quota()` — kv `llmDailyQuota` → env `B3_MANAGED_DAILY_QUOTA` → 20 | 20, igual para todos |
| `opcoes_chamadas_dia` | chamadas de tool/dia da aba Opções | `options_mcp_api.cota_usuario_dia()` — kv → env → 60 | 60, igual para todos |
| `assistente_brl_dia` | teto de custo do assistente, em R$/dia | `assistente.teto_dia_brl()` — env `B3_ASSISTENTE_TETO_BRL` → 1.0 | 1.0, igual para todos |

**NÃO entram** (teto FÍSICO ou proteção, ADR-010 decisão 2 — *"um usuário pago
consome da MESMA cota física"*):
- `managed.global_daily_cap()`, `options_mcp_api.cota_global_dia()`,
  `TETO_SERVICO_DIA` (2.000/dia do serviço), `brapi_budget.cota_mes()`,
  `mydata_budget` — protegem o app de cair, não o modelo de negócio;
- **rate por minuto** (`managed.rate_per_min`, `options_mcp_api.rate_min`) —
  é proteção contra flood. Vender "mais requisições por minuto" seria vender
  o direito de derrubar o serviço.

**Funções liberadas:** lista de chaves. A primeira é `opcoes.criar_setup`
(decisão **D2**: RBAC = administração, plano = produto). Candidatas do ADR-010
(`:518-527`) ficam registradas, não implementadas.
</pontos_de_controle_mapeados>

<guardrails>
- **Compatibilidade de chave:** `can_analyze` lê `max_analyses_per_month` e
  `can_add_ticker`/`can_grow_watchlist_to` leem `max_watchlist`
  (`plan.py:79-119`). **Mantenha esses dois nomes.** Renomear quebraria os
  chamadores, e este plano não pode mudar comportamento.
- **Precedência memória → kv → env → default**, copiando
  `options_mcp_api.py:481-554` (`_valor_e_origem`) — é a mais completa das
  quatro implementações do padrão no repo: valida por camada, recusa `bool` e
  `<= 0`, expõe a `origem` e tem `reset_*_cache()` para os testes. **Não
  invente a quinta variação.**
- **Ausente ≠ zero.** `None` = sem limite. `0` = bloqueia. As duas coisas
  precisam ser representáveis e distinguíveis — `main.py:790` já recusa zero
  na cota de Opções por essa razão, e `plan.py` usa `None` para ilimitado hoje.
- **Sem configuração, nada muda.** É o critério mais importante deste plano e
  precisa de teste dedicado: `free` e `pro` com kv vazio e env ausente devolvem
  exatamente os valores de hoje.
- **`plan.py` não importa `managed` nem `options_mcp_api`.** O catálogo declara
  os limites; quem os aplica é o gate (Fase 3). Import ao contrário criaria
  ciclo — `options_mcp_api` já é fiado por injeção justamente por isso.
- `plan_at_least` (`plan.py:68-75`) é órfão e cita um `require_plan()` que não
  existe. **Decida**: implementar o gate de tier ou remover a função e a
  menção. Não deixe o docstring mentindo. Se remover, confira que nada importa.
- **Não chamar os mutadores de estado do `gsd-sdk`.** STATE à mão.
- Commit por heredoc com delimitador entre aspas.
- **Não tocar** em `server/web_dist`, `server/admin_dist`, `web/src/version.js`
  nem na linha `SERVER_BUILD_ID`.
</guardrails>
</context>

<tasks>

<task type="auto">
  <name>Task 1: o catálogo com limites e funções</name>
  <read_first>
    - server/app/plan.py INTEIRO (125 linhas)
    - server/app/options_mcp_api.py linhas 440-620 (o padrão `_valor_e_origem`, `LIMITES`, `limites()`, `_env_int`, `_kv_int`, `reset_limites_cache`)
    - server/app/db.py (`kv_get`/`kv_set` e `admin_config_get`/`set` — escolha a camada que o padrão copiado usa)
  </read_first>
  <files>server/app/plan.py</files>
  <action>
Estruturar `PLAN_FREE`/`PLAN_PRO` como catálogo, preservando as duas chaves
que já têm chamador (`max_analyses_per_month`, `max_watchlist`) e
acrescentando as três novas da tabela em `<pontos_de_controle_mapeados>`, mais
uma lista `funcoes`.

Acrescente:
- `LIMITES_DE_PLANO`: tupla declarativa `(chave, kv, env, default_por_plano)`,
  no espírito do `LIMITES` de `options_mcp_api.py:585-589` — é ela que a rota
  admin da Fase 4 vai iterar, e é o que impede uma lista paralela na UI.
- `limites_do_plano(plano_id) -> dict` com `{chave: {"valor": x, "origem":
  "kv"|"env"|"default"}}`. A **origem** não é enfeite: sem ela o admin muda
  pelo painel, a env continua diferente, e ninguém sabe qual manda.
- `funcoes_do_plano(plano_id) -> set` — as funções que o plano libera.
- `set_limite_do_plano(plano_id, chave, valor)` persistindo no kv, com
  invalidação de cache, no mesmo formato do `_set_limite`
  (`options_mcp_api.py:543-554`).
- `reset_cache_planos()` para os testes.

As chaves de kv devem incluir o id do plano (ex.: `planoFree.analisesMes`),
senão dois planos disputam o mesmo registro.

`can_analyze`, `can_add_ticker` e `can_grow_watchlist_to` continuam
funcionando **sem alteração de assinatura** — eles recebem o dict de plano e
leem a chave. Se o catálogo passar a resolver valores dinamicamente, o dict
que `current_plan` devolve precisa continuar tendo as chaves que eles esperam.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/server && .venv/bin/python -m pytest tests/ -q -k "plan or watchlist or quota or metering"</automated>
  </verify>
  <acceptance_criteria>
    - `limites_do_plano("free")` sem kv e sem env devolve `max_analyses_per_month=30`, `max_watchlist=10` e os três novos com os defaults de hoje, todos com `origem: "default"`.
    - `limites_do_plano("pro")` devolve `None` onde hoje é `None`.
    - kv vence env, env vence default; valor torto em qualquer camada cai para a de baixo sem derrubar.
    - `None` (sem limite) e `0` (bloqueia) são distinguíveis no ida e volta pelo kv.
    - `funcoes_do_plano` devolve conjunto; `opcoes.criar_setup` está declarado (mesmo que ainda não seja lido por ninguém — quem lê é o 25-04).
    - `plan.py` não importa `managed` nem `options_mcp_api`.
  </acceptance_criteria>
  <done>O catálogo existe, resolve por precedência e diz a origem de cada valor.</done>
</task>

<task type="auto">
  <name>Task 2: o guardião de que nada mudou</name>
  <read_first>
    - server/tests/ — procure os testes que hoje exercitam `can_analyze`, `can_add_ticker` e `/api/watchlist/quota`, e siga o estilo
  </read_first>
  <files>server/tests/test_catalogo_planos.py</files>
  <action>
O teste mais importante do plano: **sem configuração, o comportamento é
idêntico ao de antes**.

1. Para cada plano e cada chave, o valor resolvido sem kv e sem env é igual ao
   literal que `plan.py` tinha antes desta mudança. Escreva os valores
   esperados **explicitamente no teste** (30, 10, `None`, 20, 60, 1.0) — aqui
   a duplicação é o ponto: se alguém mudar o default no código, o teste
   precisa gritar, não acompanhar.
2. `can_analyze(29, free)` permite e `can_analyze(30, free)` nega, com a mesma
   mensagem de hoje (byte a byte — é texto que o usuário lê).
3. `can_add_ticker` e `can_grow_watchlist_to` idem, com os limites de hoje.
4. Precedência: kv > env > default, com um teste por degrau.
5. `None` ≠ `0`: um plano com `0` numa chave bloqueia; com `None`, libera.
6. Valor torto no kv (texto, `bool`, negativo) cai para a camada de baixo e
   **não** desliga o limite — desligar por dado corrompido seria o pior modo
   de falhar.
7. As funções do plano incluem `opcoes.criar_setup` em quem deve ter.

Prove RED onde fizer sentido e diga no SUMMARY quais casos passavam antes — um
teste que passa nos dois estados prova não-regressão, não a mudança, e a
distinção importa aqui porque o objetivo DESTE plano é justamente não mudar
nada.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2 && bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `test_catalogo_planos.py` verde com os 7 blocos.
    - Suíte canônica sem regressão sobre `2590 passed, 5 skipped, 3 xfailed` + 132 `.mjs`.
    - Nenhum teste existente de plano/watchlist/quota precisou mudar. Se algum precisou, a mudança vem com nota datada explicando por quê.
  </acceptance_criteria>
  <done>Está provado que a refatoração não virou mudança de política.</done>
</task>

</tasks>

<verification>
1. `bash scripts/executar.sh --testes` FORA do sandbox (`dangerouslyDisableSandbox: true`).
2. Saída no SUMMARY, com a decisão sobre `plan_at_least` registrada.
</verification>

<success_criteria>
- Catálogo com limites por ponto de controle, funções por plano e origem de cada valor.
- Comportamento idêntico sem configuração, provado por teste.
- Tetos físicos e rate/min fora do catálogo, com a razão escrita.
</success_criteria>

<output>
Create `.planning/phases/25-planos-comerciais/25-03-SUMMARY.md` when done
</output>
