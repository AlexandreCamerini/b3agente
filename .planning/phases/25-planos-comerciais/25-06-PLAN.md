---
phase: 25-planos-comerciais
plan: 06
type: execute
wave: 6
depends_on: ["25-05"]
files_modified:
  - server/app/main.py
  - server/tests/test_fase12_cap_watchlist.py
  - web/src/App.jsx
  - web/src/copy.js
  - web/src/plan.js
  - web/tests/test_plano_ui.mjs
autonomous: true
requirements: [Fase 5 do 25-CONTEXT — plano visível no app]

must_haves:
  truths:
    - "O usuário vê em que plano está e o que ele libera, num lugar que não disputa espaço com o dia a dia"
    - "A recusa por limite de watchlist chega estruturada ao cliente, não como frase crua"
    - "Nenhuma linguagem de upgrade, nenhum badge global, nenhuma cor fora de T.warn"
    - "Os textos são idênticos nos dois modos — é estado de conta, não voz de professor ou mesa"
  artifacts:
    - path: "web/src/App.jsx"
      provides: "tile Plano no grupo Conta do PerfilHub, PlanoScreen, banner de limite compartilhado"
      contains: "function PlanoScreen"
    - path: "server/app/main.py"
      provides: "os dois gates de watchlist com 402 estruturado"
      contains: "COD_WATCHLIST"
---

<objective>
Fase 5 do `25-CONTEXT.md`, a última do backend/frontend antes da publicação
conjunta. Delegada a um subagente de UX (`UX Researcher`) por instrução do
CONTEXT aprovado — a recomendação dele é a base deste plano.

Purpose: hoje **nada** no app lê `authUser.plan`. O único traço de plano na
tela é um sufixo discreto num modal (`App.jsx:7615`). Depois desta fase, a
pessoa tem onde ver o que o plano dela libera, e a recusa por limite chega
como informação, não como frase crua que o backend nunca escreveu para ser
lida por um usuário.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-planos-comerciais/25-CONTEXT.md
@.planning/phases/25-planos-comerciais/25-04-SUMMARY.md
@.planning/phases/25-planos-comerciais/25-05-SUMMARY.md
@CLAUDE.md

<recomendacao_do_ux_researcher>
Achados que fixam a decisão (não redescubra):

- `QuotaSeg` (`App.jsx:459-472`) já tem 3 estados (`undefined`=carregando/omite,
  `null`=falhou/travessão, objeto=mostra) e convenção de cor: `T.warn` a partir
  de 90%, **nunca** `T.negative`/`T.positive` — reservados a P&L. Herdar, não
  reinventar.
- **Tile "Plano" no grupo Conta do `PerfilHub`** (`App.jsx:2541-2544`, logo
  abaixo do tile de login) — não em "IA e desempenho", não badge global.
  Razão: plano muda raríssimo (só atribuição manual do dono hoje), e um badge
  permanente para um dado quase nunca consultado é o custo de carga cognitiva
  pago em TODA tela pela estética de freemium agressivo que o ADR-010 (decisão
  4) proíbe.
- **Não empilhar em `AtividadeIAScreen`** (é sobre custo/histórico, não
  entitlement) **nem em `AiConfigScreen`** (é sobre modelo/BYOK, eixo
  ortogonal ao plano).
- **A recusa por limite merece um componente compartilhado**, mas nem modal
  nem `flash()` (toast de 2,6s — curto demais para algo que a pessoa precisa
  entender). Extrair a caixa `T.warn` já repetida inline (`App.jsx:1939, 2251,
  4604, 7734-7736`), renderizada **inline no ponto da tentativa** (dentro do
  formulário, dentro do botão), nunca como toast/modal.
- **Correção sobre o escopo dos gates** (verificado nesta sessão, não estava
  no relatório do UX Researcher): só os DOIS gates de watchlist
  (`can_add_ticker` em `main.py:1918`, `can_grow_watchlist_to` em
  `main.py:1882`) emitem 402 **cru** ao cliente. O gate de análise
  (`can_analyze`) tem o 402 capturado e convertido em **200** com fallback
  determinístico antes de chegar ao front (FIX-C01, `main.py:2351+`) — nunca
  aparece como erro. Este plano estrutura **só os dois de watchlist**.
- `api.js` já extrai `err.detail`/`err.code` (`api.js:176-181`) e o comentário
  do próprio código diz que **nenhum consumidor lê isso hoje** — infraestrutura
  pronta e ociosa.
</recomendacao_do_ux_researcher>

<anti_padroes_a_evitar>
Do próprio UX Researcher, específicos deste produto:
- Nenhuma contagem regressiva ansiosa, nenhum "só resta 1!".
- **Nenhum CTA de upgrade** — não existe loja/IAP; um botão assim promete o
  que não se cumpre. Nem "Em breve".
- Nenhum badge "FREE"/"PRO" fixo em toda tela.
- Nenhum modal bloqueante para limite atingido — é estado esperado, não
  catastrófico.
- Nenhuma tabela comparativa Free × Pro com checkmarks.
- **`T.negative` é só para P&L.** Usar em aviso de limite faria "bati o teto"
  parecer prejuízo.
- Não misturar erro técnico (fonte fora do ar) com recusa de plano no mesmo
  componente — categorias diferentes (princípio 4 do CLAUDE.md).
- Não exibir a `reason` do backend verbatim — ela é ASCII sem acento, convenção
  de log Python, não copy de produto. Passa por `copy.js`.
</anti_padroes_a_evitar>

<guardrails>
- **Texto idêntico nos dois modos.** É estado de conta, não voz de professor
  vs mesa — mesmo precedente de `curvaPoucosDias` (afirmação factual, não
  enquadramento de decisão). Simplifica o guardião de paridade de chaves.
- **`/api/watchlist/add` e `PUT /api/watchlist` mudam de contrato**: `detail`
  deixa de ser string, vira dict (`code`, `message`, `limite`, `usado`).
  `message` carrega o texto de hoje **verbatim** — é o que preserva os testes
  existentes com uma mudança de caminho de acesso (`.json()["detail"]` →
  `.json()["detail"]["message"]"`), não de conteúdo.
- **Atualizar os testes existentes que quebram, com nota datada** — não
  apagar o guardião. `test_fase12_cap_watchlist.py:106,156,201,218,247` leem
  `r.json()["detail"]` como string hoje.
- **`plan.js` continua sem número hardcoded** — o comentário de topo
  (`plan.js:1-12`) proíbe `10`/`30` no front; a fonte é sempre o endpoint.
- **Composição de 2-3 chamadas na tela nova** (`authMe`/`aiQuota`/
  `watchlistQuota`) é custo real, nomeado pelo UX Researcher — não é grátis,
  não pule.
- **Não chamar os mutadores de estado do `gsd-sdk`.** STATE à mão.
- Commit por heredoc com delimitador entre aspas.
- **Não tocar** em `server/web_dist`, `server/admin_dist`, `web/src/version.js`
  nem na linha `SERVER_BUILD_ID`. Publicação é etapa separada — a última desta
  fase, e ela publica as CINCO fases juntas.
</guardrails>
</context>

<tasks>

<task type="auto">
  <name>Task 1: os dois gates de watchlist com 402 estruturado</name>
  <read_first>
    - server/app/main.py linhas 1855-1920 (as duas rotas de watchlist inteiras, e como `can_grow_watchlist_to`/`can_add_ticker` são chamados)
    - server/app/options_mcp_api.py linhas 2380-2400 (`marcar_o_limite`/`codigo_do_limite` — o mecanismo de carimbo já existente; NÃO reimplementar, mas notar que ele é server-internal e não serializa no JSON — o `detail` que chega ao cliente hoje é só a string `reason`)
  </read_first>
  <files>server/app/main.py</files>
  <action>
Constante nova perto das rotas de watchlist: `COD_WATCHLIST = "watchlist_limite"`.

Nas duas rotas (`main.py:1882` e `1918`), trocar
`raise HTTPException(402, reason)` por
`raise HTTPException(402, {"code": COD_WATCHLIST, "message": reason, "limite": plan.get("max_watchlist"), "usado": <tamanho relevante>})`.

`plan` já está em escopo (é o dict devolvido por `_plano_efetivo`/`can_grow_watchlist_to`). `usado` é `len(final)` na rota PUT e `len(atual)+1`(tentativa) ou o tamanho pré-adição na rota POST — use o que a rota já calcula, não introduza uma consulta nova.

`message` continua **exatamente** a string que `plan.can_grow_watchlist_to`/
`can_add_ticker` produzem hoje — nenhuma reescrita, é o que preserva o
conteúdo dos testes existentes.

Comentar por que existe: infraestrutura de `err.detail`/`err.code` já existe
no `api.js` e está ociosa; isso é o primeiro consumidor real fora da aba
Opções, e é o par (código, número) que o front vai usar para renderizar o
banner sem adivinhar pela frase.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/server && .venv/bin/python -m pytest tests/test_fase12_cap_watchlist.py tests/test_fase3_gate_plano.py -q</automated>
  </verify>
  <acceptance_criteria>
    - As duas rotas devolvem 402 com `detail` dict contendo `code == "watchlist_limite"`, `message` (a frase de hoje, verbatim) e `limite`.
    - Os testes existentes ajustados para ler `.json()["detail"]["message"]`, com nota datada explicando a mudança de forma (não de conteúdo).
    - Nenhuma outra rota de plano mudou de forma — `can_analyze` continua indo por `_gate_analise`/FIX-C01, sem 402 cru ao cliente.
  </acceptance_criteria>
  <done>As duas rotas que hoje devolvem 402 cru ao cliente passam a devolver o par código+número que o front precisa para não adivinhar pela frase.</done>
</task>

<task type="auto">
  <name>Task 2: banner compartilhado de limite, e o tile + PlanoScreen</name>
  <read_first>
    - web/src/App.jsx linhas 459-472 (`QuotaSeg` — o precedente de estados e cor)
    - web/src/App.jsx linhas 1930-1945, 2245-2255 (dois dos banners `T.warn` repetidos — o padrão visual a extrair)
    - web/src/App.jsx linhas 2509-2612 (`PerfilHub` inteiro, o grupo Conta e o padrão de `ProfileTile`)
    - web/src/App.jsx linhas 5716-5888 (`AiConfigScreen` — o molde de subtela de Perfil a seguir para `PlanoScreen`)
    - web/src/App.jsx linha 7869 e 9342-9358 (roteamento de `perfilView`)
    - web/src/api.js linhas 170-182 (`err.detail`/`err.code`, hoje sem consumidor)
  </read_first>
  <files>web/src/App.jsx</files>
  <action>
**1. `LimiteAtingido` — componente compartilhado.** Extrai a caixa `T.warn`
repetida (fundo `color-mix(..., 12%, transparent)`, borda 1px, raio 9-11px)
num componente `function LimiteAtingido({ erro, cp })` que:
- só renderiza quando `erro?.code === "watchlist_limite"` (ou outros códigos
  de limite que já existem, como `mcp_cota`/`plano_analises` — mas sem
  duplicar o que a aba Opções já resolve sozinha; este componente é para os
  pontos que HOJE não têm tratamento nenhum, começando pela watchlist);
- mostra a mensagem (de `cp.*`, nunca `erro.message` cru — ver Task 3),
  opcionalmente o número (`limite`/`usado`) quando vierem;
- cor `T.warn`, nunca `T.negative`;
- renderizado **inline**, no ponto onde a tentativa aconteceu (dentro do
  formulário de adicionar ticker, por exemplo) — nunca toast, nunca modal.

Montar no(s) ponto(s) onde hoje `flash(gate.reason)` ou equivalente já captura
a recusa de watchlist no cliente (pré-check local existente) e onde o 402 do
servidor chega (`A.addTicker`/fluxo de PUT em massa) — troque o tratamento
cru pelo componente.

**2. Tile "Plano"** no grupo Conta do `PerfilHub` (`App.jsx:2541`, logo após
o tile de conta existente): `<ProfileTile onClick={() => onOpen("plano")}
title={cp.planoRotulo} sub={...um resumo de uma linha, de cp.*...} icon={...}
/>`.

**3. `perfilView`** ganha `"plano"` na lista (`App.jsx:7869`) e o roteamento
em `App.jsx:9342-9358`.

**4. `function PlanoScreen({ ctx })`**, no molde de `AiConfigScreen`: compõe
`ctx.authUser.plan` (já vem de `/api/auth/me`) + `store.aiQuota()` +
`store.watchlistQuota()` numa única tela. Mostra: nome do plano
(`cp.planoNome`), e uma linha por entitlement usando a MESMA semântica de
3-estados de `QuotaSeg` (`cp.planoEntitlementAnalises`,
`cp.planoEntitlementWatchlist` — omite quando `limit == null`, mostra número
quando existir). Nenhuma cor de P&L. Nenhum CTA. Nenhuma comparação de planos.

O sufixo existente do modal de catálogo (`App.jsx:7615`) continua — é
complementar, não redundante.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/web && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `npx vite build` verde.
    - `LimiteAtingido` renderiza inline (não em toast, não em modal) e só com `code` reconhecido.
    - Cor do banner é `T.warn` em todo caminho — `grep -c "T.negative"` dentro do bloco do componente novo retorna 0.
    - Tile "Plano" no grupo Conta, abre `PlanoScreen`.
    - `PlanoScreen` compõe as 2-3 chamadas sem duplicar lógica de `QuotaSeg`.
    - Nenhum texto de upgrade, nenhum badge fixo fora do tile do hub.
  </acceptance_criteria>
  <done>A pessoa vê o que o plano libera num lugar que não disputa espaço com o dia a dia, e a recusa de watchlist aparece como informação, no ponto da tentativa.</done>
</task>

<task type="auto">
  <name>Task 3: copy.js e o guardião</name>
  <read_first>
    - web/src/copy.js (os dois blocos, e o comentário sobre `curvaPoucosDias` como precedente de texto idêntico nos dois modos)
    - web/tests/test_copy_theme.mjs (o guardião de paridade de chaves — o padrão a seguir para o novo)
  </read_first>
  <files>web/src/copy.js, web/tests/test_plano_ui.mjs</files>
  <action>
Chaves novas, **texto idêntico** nos dois blocos (`estudo` e `operador`):
`planoRotulo`, `planoNome` (função `(planId) => ...`), `planoResumoTile`
(função), `planoTituloTela`, `planoDescricao`, `planoEntitlementWatchlist`
(função `(limit) => ...`, retorna `null`/vazio quando `limit == null`),
`planoEntitlementAnalises` (idem), `planoAvisoLimiteWatchlist` (função
`(limite) => ...` — substitui a exibição de `erro.message` cru no banner).

Nenhuma chave de CTA de upgrade — de propósito, não esquecimento.

`test_plano_ui.mjs` (estático, no estilo dos guardiões existentes em
`web/tests/`):
- as chaves novas existem nos dois modos com o MESMO texto (paridade mais
  forte que "mesma chave" — aqui é "mesmo conteúdo", com asserção explícita);
- nenhuma chave de upgrade/CTA existe (`grep` negativo por padrões como
  "assine", "upgrade", "vire pro");
- `LimiteAtingido` não usa `T.negative`;
- `PlanoScreen` não importa nem referencia comparação entre planos (sem
  tabela, sem "veja o que você ganha").
</action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2 && bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `test_plano_ui.mjs` verde.
    - Guardião de paridade de chaves (`test_copy_theme.mjs`) continua verde com as chaves novas.
    - Suíte canônica sem regressão sobre a baseline do 25-05.
  </acceptance_criteria>
  <done>Os textos existem, são idênticos nos dois modos, e o guardião prova a ausência de linguagem de upgrade.</done>
</task>

</tasks>

<verification>
1. `bash scripts/executar.sh --testes` FORA do sandbox (`dangerouslyDisableSandbox: true`).
2. `cd web && npx vite build`.
3. Saída no SUMMARY.
</verification>

<success_criteria>
- Tile "Plano" no grupo Conta do hub, com subtela que compõe o estado real da conta.
- Banner de limite compartilhado, inline, em `T.warn`, alimentado pelo código estruturado.
- Watchlist devolve 402 estruturado; análise continua degradando para 200 (inalterado).
- Zero linguagem de upgrade, zero badge global, zero cor fora da convenção.
</success_criteria>

<output>
Create `.planning/phases/25-planos-comerciais/25-06-SUMMARY.md` when done
</output>
