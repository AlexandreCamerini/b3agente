---
phase: 25-planos-comerciais
plan: 04
type: execute
wave: 4
depends_on: ["25-03"]
files_modified:
  - server/app/main.py
  - server/app/managed.py
  - server/app/options_mcp_api.py
  - server/app/assistente.py
  - server/tests/test_gates_por_plano.py
autonomous: true
requirements: [Fase 3 do 25-CONTEXT — gates leem o plano (D3)]

must_haves:
  truths:
    - "O limite que barra uma conta é o do plano dela, não mais um número igual para todos"
    - "Quem não configurar plano nenhum continua exatamente onde estava"
    - "O owner nunca é barrado pelo cap comercial, e continua sujeito ao teto físico"
    - "A recusa diz QUAL limite bateu, por código, não por texto raspado"
  artifacts:
    - path: "server/app/main.py"
      provides: "gates de limite resolvidos pelo plano da conta; owner isento do cap comercial"
      contains: "limites_do_plano"
    - path: "server/tests/test_gates_por_plano.py"
      provides: "guardião de que o limite vem do plano e o teto físico não"
      contains: "def test_"
---

<objective>
Fase 3 do `25-CONTEXT.md`. O catálogo existe desde o 25-03 e **ninguém o lê**.
Aqui os gates passam a ler.

Purpose: hoje a cota diária da IA gerenciada, a cota da aba Opções e o teto em
R$ do assistente são um número só para toda a base. Depois desta fase, são o
número do plano da conta — e a decisão **D3** do Alex entra: o `owner` nunca é
barrado pelo cap comercial.

**Escopo deliberadamente reduzido (decisão do Alex, 2026-09-12: "da forma mais
segura e rápida"):** a migração de `opcoes.criar_setup` do RBAC para o plano
(decisão D2) **NÃO acontece aqui**. Hoje só quem tem `role_admin` cria setup;
liberar por plano ampliaria o acesso — qualquer conta passaria a gravar no
armazém compartilhado do serviço MCP. A infraestrutura (`funcoes_do_plano`)
está pronta desde o 25-03 e fica aguardando os planos comerciais reais.
Registre isso no SUMMARY como pendência nomeada, não como esquecimento.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-planos-comerciais/25-CONTEXT.md
@.planning/phases/25-planos-comerciais/25-03-SUMMARY.md
@CLAUDE.md

<a_conciliacao_com_os_overrides_globais>
O 25-03 deixou uma lacuna nomeada: `plan.py` não pode importar `managed` nem
`options_mcp_api` (ciclo), então os overrides **globais** que já existem
(`llmDailyQuota` no admin_config, `mcpCotaUsuarioDia` no kv) ficaram fora da
cadeia do catálogo.

**A regra de conciliação, e é a que menos quebra:**

```
limite do PLANO (kv por plano → env por plano)
  → override GLOBAL de hoje (admin_config / kv, que o portal já escreve)
    → env global
      → default
```

Quem nunca configurar plano nenhum cai no override global e continua
exatamente onde está. Configurar o plano é que passa a ter precedência.

A conciliação mora no **chamador** (`main.py`, `options_mcp_api.py`), nunca em
`plan.py` — é o que evita o ciclo e mantém `plan.py` como declaração.
</a_conciliacao_com_os_overrides_globais>

<guardrails>
- **Sem configuração de plano, NADA muda.** É o mesmo critério do 25-03 e
  continua sendo o mais importante. Teste dedicado.
- **D3 — o owner pula o cap COMERCIAL, nunca o físico.** Ele não pode escapar
  de `global_daily_cap`, `cota_global_dia`, `TETO_SERVICO_DIA`, brapi ou
  mydata: esses tetos existem porque o serviço externo corta para a base
  INTEIRA, e ignorá-los não cria capacidade — só transfere a recusa para outro
  usuário, com uma mensagem que não explica isso.
- **Rate por minuto continua global** (proteção contra flood, não produto).
- **NÃO migrar `opcoes.criar_setup`.** Ver `<objective>`. O gate de função
  fica como está; esta fase é só de LIMITE.
- **O código de erro deixa de ser texto raspado.** Hoje
  `options_mcp_api.py:2326` distingue `plano_analises` de `ia_gerenciada`
  procurando a substring `"analises/mes"` na mensagem — muda a frase, quebra a
  classificação. Faça o gate devolver um código estruturado e o tradutor ler o
  código. O texto ao usuário não muda.
- **A mensagem de recusa nomeia o limite e o plano**, como
  `plan.can_analyze` já faz (`"limite de {limit} analises/mes do plano
  {plan['id']}"`). Não invente linguagem de upgrade — o ADR-010 (decisão 4)
  proíbe "assine e resolve na hora".
- **Não chamar os mutadores de estado do `gsd-sdk`.** STATE à mão.
- Commit por heredoc com delimitador entre aspas.
- **Não tocar** em `server/web_dist`, `server/admin_dist`, `web/src/version.js`
  nem na linha `SERVER_BUILD_ID`.
</guardrails>
</context>

<tasks>

<task type="auto">
  <name>Task 1: os gates de limite resolvem pelo plano</name>
  <read_first>
    - server/app/plan.py (o catálogo do 25-03: `LIMITES_DE_PLANO`, `limites_do_plano`, `configure_db`)
    - server/app/main.py linhas 495-571 (`_ai_apply_managed` e `_gate_analise`)
    - server/app/managed.py linhas 86-135 (`daily_quota`, `rate_per_min`, `global_daily_cap`)
    - server/app/options_mcp_api.py linhas 555-612 (`cota_usuario_dia`, `limites`)
    - server/app/assistente.py linhas 41-68 (`teto_dia_brl`, `custo_hoje`)
  </read_first>
  <files>server/app/main.py, server/app/managed.py, server/app/options_mcp_api.py, server/app/assistente.py</files>
  <action>
Para cada um dos cinco limites do catálogo, o valor efetivo passa a ser
resolvido pela cadeia de `<a_conciliacao_com_os_overrides_globais>`, **no
chamador**:

1. `max_analyses_per_month` — já vem do plano (`_gate_analise`); só confirme
   que agora lê o catálogo e não o literal.
2. `max_watchlist` — idem, nas rotas de watchlist.
3. `ia_gerenciada_dia` — `_ai_apply_managed` (`main.py:508`) passa a resolver
   por plano antes de cair em `managed.daily_quota()`.
4. `opcoes_chamadas_dia` — `_cap_check` (`options_mcp_api.py:725`) idem, antes
   de `cota_usuario_dia()`.
5. `assistente_brl_dia` — o teto do assistente idem, antes de `teto_dia_brl()`.

**`global_daily_cap`, `cota_global_dia`, `TETO_SERVICO_DIA` e os rate/min
ficam exatamente como estão.** Nenhuma linha deles muda.

Cada ponto ganha um comentário curto dizendo por que o plano vem antes do
override global, e que o global continua valendo para quem não configurou.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/server && .venv/bin/python -m pytest tests/ -q -k "plan or quota or metering or mcp_cap or assistente or watchlist"</automated>
  </verify>
  <acceptance_criteria>
    - Sem valor por plano no kv: os cinco limites resolvem exatamente como hoje (override global, env, default nessa ordem).
    - Com valor por plano: ele vence o override global.
    - Os tetos globais e os rate/min não mudaram de valor nem de origem — provado por teste, não por leitura.
    - `plan.py` continua sem importar `managed` e `options_mcp_api`.
  </acceptance_criteria>
  <done>O limite que barra uma conta é o do plano dela, e quem não configurou não sentiu nada.</done>
</task>

<task type="auto">
  <name>Task 2: D3 — o owner e o cap comercial, e o erro por código</name>
  <read_first>
    - server/app/rbac.py (o `OWNER` do 25-02 e `roles_for_user`)
    - server/app/main.py (`_gate_analise`, e como o 402 é montado)
    - server/app/options_mcp_api.py linhas 2320-2360 (`_gate_de_analise` e o `_MARCA_DO_GATE_MENSAL`, a raspagem de string)
  </read_first>
  <files>server/app/main.py, server/app/options_mcp_api.py</files>
  <action>
**1. D3.** Em `_gate_analise`, o `owner` não é barrado pelo cap comercial —
mesma posição lógica do BYOK, que já pula o gate mensal (`main.py:560-561`).
Comente que é decisão D3 do Alex, com a data, e que **não** vale para o teto
físico: o `_ai_apply_managed` continua rodando normalmente para ele.

Cuidado: resolver papel a cada requisição custa uma consulta. `rbac` não tem
cache por decisão explícita do ADR-013 (revogação imediata > latência) —
respeite isso, não introduza cache aqui.

**2. O código estruturado.** `options_mcp_api.py:2326` usa
`_MARCA_DO_GATE_MENSAL = "analises/mes"` para descobrir, pelo TEXTO da
mensagem, se o 402 veio do gate mensal ou da cota diária. Mudar a frase quebra
a classificação em silêncio.

Faça o gate devolver o código junto (ex.: a `HTTPException` carrega
`detail.code` ou uma exceção de domínio distingue os dois), e o tradutor ler o
código em vez da substring. **Mantenha o guardião que existe** — se ele testa
a raspagem, atualize-o para testar o código, com nota datada.

O texto que chega ao usuário não muda.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/server && .venv/bin/python -m pytest tests/ -q -k "gate or owner or mcp or plan"</automated>
  </verify>
  <acceptance_criteria>
    - Owner com o mês estourado: passa. Owner com o teto FÍSICO estourado: é barrado.
    - Conta comum com o mês estourado: continua barrada, com a mesma mensagem.
    - `plano_analises` e `ia_gerenciada` são distinguidos por código; mudar a frase da mensagem NÃO muda a classificação — provado por teste que altera o texto.
    - Nenhuma consulta de papel nova fora do caminho do gate.
  </acceptance_criteria>
  <done>O dono não esbarra no próprio cap comercial, e a classificação do erro para de depender de uma frase.</done>
</task>

<task type="auto">
  <name>Task 3: guardiões</name>
  <files>server/tests/test_gates_por_plano.py</files>
  <action>
1. **Sem configuração, nada muda** — para os cinco limites, com kv vazio.
   É o teste que impede esta fase de virar mudança de política.
2. Com limite por plano configurado, o gate usa o do plano (um teste por
   limite, nomeando qual).
3. **Tetos físicos intactos:** `global_daily_cap`, `cota_global_dia` e
   `TETO_SERVICO_DIA` não mudam com plano nenhum configurado.
4. **D3:** owner passa o comercial, é barrado pelo físico.
5. **Erro por código:** mudar o texto da mensagem do gate mensal não altera a
   classificação `plano_analises` × `ia_gerenciada`.
6. Conta sem plano resolvido (anônimo, ou falha de leitura) cai no menos
   privilegiado — o fail-closed de `_plano_do_escopo` continua.

Prove RED antes e diga no SUMMARY quais casos passavam nos dois estados.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2 && bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `test_gates_por_plano.py` verde com os 6 blocos.
    - Suíte canônica sem regressão sobre `2621 passed, 5 skipped, 3 xfailed` + 132 `.mjs`.
  </acceptance_criteria>
  <done>O que o plano controla, o que ele não controla e o que o owner pula estão todos travados por teste.</done>
</task>

</tasks>

<verification>
1. `bash scripts/executar.sh --testes` FORA do sandbox (`dangerouslyDisableSandbox: true`).
2. Saída no SUMMARY, com a pendência da D2 (`opcoes.criar_setup`) nomeada.
</verification>

<success_criteria>
- Cinco limites resolvidos pelo plano, com o global como camada de baixo.
- Tetos físicos e rate/min intocados.
- Owner isento do comercial, sujeito ao físico.
- Classificação de erro por código, não por frase.
- `opcoes.criar_setup` deliberadamente não migrado, registrado como pendência.
</success_criteria>

<output>
Create `.planning/phases/25-planos-comerciais/25-04-SUMMARY.md` when done
</output>
