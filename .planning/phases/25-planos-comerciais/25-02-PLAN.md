---
phase: 25-planos-comerciais
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - server/app/rbac.py
  - server/app/main.py
  - server/app/agent.py
  - server/tests/test_owner.py
  - server/tests/test_adr013_rbac.py
  - web-admin/src/App.jsx
autonomous: true
requirements: [Fase 1 do 25-CONTEXT — papel owner (decisão D1)]

must_haves:
  truths:
    - "O owner tem toda permissão que existe hoje e toda que vier a existir, sem lista para manter"
    - "Nenhuma rota consegue revogar o owner, e se ele sumir por outro caminho volta sozinho"
    - "Quem não é owner não concede owner"
    - "O portal não oferece um botão que revoga o owner"
  artifacts:
    - path: "server/app/rbac.py"
      provides: "OWNER, permissões por união dinâmica, revoke recusado, bootstrap por e-mail"
      contains: "OWNER"
    - path: "server/tests/test_owner.py"
      provides: "guardião da irrevogabilidade e da reconciliação"
      contains: "def test_"
---

<objective>
Fase 1 do `25-CONTEXT.md`, decisão **D1** do Alex: papel `owner` com todas as
permissões, imune a revogação, ancorado em e-mail.

Purpose: hoje o papel máximo (`role_admin`) depende de `B3_ADMIN_EMAILS` ou,
sem ela, da conta mais antiga do banco — e **nenhum** papel é de fato
irrevogável: `revoke_role` é um `DELETE` cru, e o que salva é a reconcessão
automática no request seguinte. O dono do produto precisa de um papel que não
dependa de ordem de criação de conta nem de ninguém lembrar de configurar uma
lista.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-planos-comerciais/25-CONTEXT.md
@.planning/phases/25-planos-comerciais/25-01-SUMMARY.md
@CLAUDE.md

<como_o_rbac_funciona_hoje>
Mapeado, com linha real — não redescubra:

- `rbac.py:47-53` `permissoes_do_papel`: `ROLE_ADMIN` é **derivado**, a união
  dinâmica de `GRUPOS`. Grupo novo dá permissão nova ao admin sem migração.
  **O `owner` usa o MESMO mecanismo** — é o que cumpre "toda permissão futura".
- `rbac.py:68-77`: `grant_role` valida o nome do papel; **`revoke_role` não
  valida nada e não tem nenhuma condição de bloqueio** — é `DELETE` cru.
- `rbac.py:137-146` `_is_admin_bootstrap`: `B3_ADMIN_EMAILS` OU, se vazia, a
  conta mais antiga (`ORDER BY created_at ASC LIMIT 1`) — o achado A-12.
- `rbac.py:149-159` `ensure_bootstrap_role`: nunca revoga, só concede;
  idempotente; chamado em 7 pontos (`main.py:169`, `:206`, `:255`, `:353`,
  `:406`, `:1222`, `:1270`).
- `main.py:1145-1146`: o ramo `revogar` da rota de papéis, **sem verificação
  nenhuma**. O freio de escalação (`main.py:1153`) existe só no ramo
  `conceder` e compara por nome literal com `ROLE_ADMIN`.
- `db.py:180-190`: tabela `user_roles`, sem `CHECK` no valor de `role` — a
  validação de nome é só em Python.
</como_o_rbac_funciona_hoje>

<guardrails>
- **Permissões por união dinâmica de `GRUPOS`**, como `ROLE_ADMIN` já faz.
  NUNCA uma lista literal de permissões para o `owner` — ela envelheceria no
  primeiro grupo novo, e "nunca perde a função" viraria mentira silenciosa.
- **Defesa em profundidade (D1), as duas camadas:** `revoke_role` recusa, E o
  bootstrap reconcede. Uma sem a outra deixa buraco: só recusar não cobre
  escrita direta no banco; só reconceder deixa janela entre a revogação e o
  próximo request.
- **Sem fallback de "primeira conta"** para o `owner`. Se `B3_OWNER_EMAIL` não
  bate em nenhuma conta, não há owner — e isso é melhor que eleger um por
  ordem de criação, que é o A-12.
- **Não remova o `_is_admin_bootstrap` nem mexa no `role_admin`.** Três testes
  dependem do comportamento atual (`test_adr013_rbac.py:86-99`, `:113-119`,
  `test_opcoes_dsl.py:83-86`) — é contrato testado. Fechar o A-12 de vez é
  decisão separada.
- **O e-mail não é segredo, mas a âncora é configurável:** `B3_OWNER_EMAIL`,
  default `alexandre.camerini@gmail.com`. Comparação case-insensitive e com
  `strip`, como `_is_admin_bootstrap` já faz.
- **Não chamar os mutadores de estado do `gsd-sdk`** (`CLAUDE.md`).
- Commit por heredoc com delimitador entre aspas.
- **Não tocar** em `server/web_dist`, `server/admin_dist`, `web/src/version.js`
  nem na linha `SERVER_BUILD_ID`.
</guardrails>
</context>

<tasks>

<task type="auto">
  <name>Task 1: o papel owner no rbac</name>
  <read_first>
    - server/app/rbac.py INTEIRO (159 linhas)
    - server/app/db.py linhas 180-190 e 742-758 (tabela e funções de papel)
  </read_first>
  <files>server/app/rbac.py</files>
  <action>
1. `OWNER = "owner"` ao lado de `ROLE_ADMIN` (`rbac.py:40`), com comentário
   explicando a diferença entre os dois: `role_admin` é bootstrap de
   governança e pode ser concedido/revogado normalmente; `owner` é a âncora do
   dono do produto e não sai.

2. `permissoes_do_papel` (`rbac.py:47-53`): `OWNER` cai no MESMO ramo de
   `ROLE_ADMIN` — união de `GRUPOS`. Escreva de forma que acrescentar um grupo
   novo continue bastando (sem enumerar).

3. `grant_role`: aceitar `OWNER` como nome válido (hoje só `ROLE_ADMIN` e as
   chaves de `GRUPOS` passam, `rbac.py:70`).

4. **`revoke_role` recusa `OWNER`** — levanta uma exceção de domínio própria
   (ex.: `PapelIrrevogavel(ValueError)`) com mensagem explicando. É a primeira
   regra do repositório que diz "este papel não sai"; comente o porquê e cite
   a decisão D1 com data.

5. `_is_owner(conn, user)`: lê `B3_OWNER_EMAIL` (default
   `alexandre.camerini@gmail.com`), normaliza e compara com `user["email"]`.
   **Sem** fallback de primeira conta — e comente que a ausência do fallback é
   deliberada, apontando para o A-12.

6. `ensure_bootstrap_role` passa a reconciliar `OWNER` também: quem bate no
   e-mail recebe `owner` se não tiver. Mantém a propriedade "nunca revoga" e a
   idempotência.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/server && .venv/bin/python -m pytest tests/test_adr013_rbac.py tests/test_adr013_cobertura_rotas.py tests/test_opcoes_dsl.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `permissoes_do_papel("owner")` == `permissoes_do_papel("role_admin")` == união de `GRUPOS`.
    - Acrescentar um grupo fictício a `GRUPOS` num teste faz as permissões do owner crescerem sozinhas.
    - `revoke_role(conn, uid, "owner")` levanta; `revoke_role` de qualquer outro papel continua funcionando.
    - Conta cujo e-mail bate em `B3_OWNER_EMAIL` recebe `owner` no `ensure_bootstrap_role`; conta que não bate, não.
    - Sem `B3_OWNER_EMAIL` e sem e-mail correspondente: **nenhum** owner (não elege a primeira conta).
    - Os testes existentes do ADR-013 continuam verdes — em especial os que cravam 9 permissões e o bootstrap de `role_admin`.
  </acceptance_criteria>
  <done>O papel existe, deriva as permissões, não se revoga e se reconcilia sozinho.</done>
</task>

<task type="auto">
  <name>Task 2: a rota recusa, e o portal não oferece o botão</name>
  <read_first>
    - server/app/main.py linhas 1113-1161 (`GET /api/admin/users` e a rota de papéis inteira)
    - server/app/agent.py linhas 330-340 (a varredura que itera `GRUPOS`)
    - web-admin/src/App.jsx linhas 1034-1080 (o render por usuário, com um botão por papel)
  </read_first>
  <files>server/app/main.py, server/app/agent.py, web-admin/src/App.jsx</files>
  <action>
1. **`POST /api/admin/users/{id}/roles`**, ramo `revogar` (`main.py:1145`):
   capturar a exceção do `revoke_role` e responder **403** com a razão. Hoje
   esse ramo não tem verificação nenhuma.

2. **Freio de concessão:** só quem é `owner` concede `owner` — mesmo formato
   do freio de `role_admin` em `main.py:1153`. Note que aquele compara por
   nome literal; escreva o novo de modo que os dois fiquem legíveis lado a
   lado, sem transformar num `if` gigante.

3. **`gruposDisponiveis`** (`main.py:1124`) é `sorted(GRUPOS) + [ROLE_ADMIN]`,
   e o portal renderiza **um botão de toggle por item**
   (`web-admin/src/App.jsx:1065-1078`). Se `owner` entrar nessa lista, vira um
   botão que tenta revogar e toma 403. Decida entre: manter `owner` fora da
   lista, ou incluí-lo num campo separado que a UI renderiza como estado, sem
   toggle. Qualquer que seja, o portal **mostra** que a conta é owner — some-lo
   da tela faria o admin achar que a conta não tem o papel.

4. **`agent.py:334-336`** monta os destinatários do push de kill-switch
   iterando `rbac.GRUPOS` mais `ROLE_ADMIN`. Papel fora de `GRUPOS` fica
   invisível: o owner não receberia o push. Incluir `OWNER` na varredura.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/server && .venv/bin/python -m pytest tests/ -q -k "rbac or role or admin_users or kill_switch" ; cd /Users/acamerini/dev/borisv2/web-admin && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - Tentar revogar `owner` pela rota → 403 com razão legível; o papel continua lá depois.
    - Quem não é owner tentando conceder `owner` → 403.
    - Owner concedendo owner → 200.
    - O portal mostra que a conta é owner e NÃO oferece um botão que tente revogá-lo.
    - O owner entra na lista de destinatários do push de kill-switch.
    - `npx vite build` do web-admin verde.
  </acceptance_criteria>
  <done>A recusa é real na rota, o portal não mente sobre o estado e o owner não some das varreduras por papel.</done>
</task>

<task type="auto">
  <name>Task 3: guardiões do owner</name>
  <read_first>
    - server/tests/test_adr013_rbac.py (o estilo, e os números cravados nas linhas 94-99, 119, 144)
  </read_first>
  <files>server/tests/test_owner.py, server/tests/test_adr013_rbac.py</files>
  <action>
`test_owner.py`, pelo caminho HTTP completo onde fizer sentido:

1. Owner tem TODAS as permissões — comparado com a união de `GRUPOS`,
   **calculada no teste**, nunca uma lista escrita à mão (senão o teste
   envelhece junto com o defeito que deveria pegar).
2. **Permissão futura entra sozinha:** com um grupo fictício acrescentado a
   `GRUPOS` via monkeypatch, as permissões do owner crescem.
3. Revogar owner pela rota → 403, e o papel permanece.
4. Revogar direto por `rbac.revoke_role` → levanta.
5. **Reconciliação:** removendo o papel direto no banco (`db.revoke_role` ou
   SQL), o próximo request administrativo o devolve. É a segunda camada da D1,
   e sem teste ninguém sabe se ela funciona.
6. Idempotência: passar duas vezes pelo bootstrap não duplica papel.
7. Sem e-mail correspondente → nenhum owner, **e a primeira conta NÃO vira
   owner** (o A-12 não se repete no papel novo).
8. Não-owner não concede owner; owner concede.

Se algum teste existente do ADR-013 precisar mudar de número (as 9 permissões,
a lista exata de papéis), a mudança vem **com nota datada** explicando —
guardião não se afrouxa em silêncio.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2 && bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `test_owner.py` verde, com os 8 casos.
    - Cada um provado RED antes da correção; no SUMMARY, quais falharam e quais passavam nos dois estados.
    - Suíte canônica sem regressão sobre `2576 passed, 5 skipped, 3 xfailed` + 131 `.mjs`.
    - Nenhum guardião existente afrouxado sem nota.
  </acceptance_criteria>
  <done>As duas camadas da D1 têm teste, e "toda permissão futura" é verificado, não prometido.</done>
</task>

</tasks>

<verification>
1. `bash scripts/executar.sh --testes` FORA do sandbox (`dangerouslyDisableSandbox: true`).
2. `cd web-admin && npx vite build`.
3. Saída no SUMMARY.
</verification>

<success_criteria>
- Owner com permissões por união dinâmica, irrevogável nas duas camadas, ancorado em e-mail sem fallback de primeira conta.
- Portal mostra o estado sem oferecer ação impossível.
- Owner visível para as varreduras que iteram papéis.
</success_criteria>

<output>
Create `.planning/phases/25-planos-comerciais/25-02-SUMMARY.md` when done
</output>
