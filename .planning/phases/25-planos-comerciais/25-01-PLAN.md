---
phase: 25-planos-comerciais
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/main.py
  - server/app/metering.py
  - server/tests/test_mcp_guardioes.py
  - server/tests/test_contador_mensal.py
autonomous: true
requirements: [Fase 0 do 25-CONTEXT — consertar o contador antes de configurá-lo]

must_haves:
  truths:
    - "Telemetria de uso do app não consome mais a cota mensal de análises de IA"
    - "Toda rota que gasta IA conta no mesmo lugar — nenhuma escapa do gate mensal"
    - "O guardião de month_section passa a varrer main.py, que foi por onde o defeito entrou"
  artifacts:
    - path: "server/app/main.py"
      provides: "month_section própria para analytics; as três rotas órfãs no gate mensal"
      contains: "analyticsEventsMonth"
    - path: "server/tests/test_contador_mensal.py"
      provides: "guardião de que o contador mede análise, e só análise"
      contains: "def test_"
---

<objective>
Fase 0 do `25-CONTEXT.md`. O contador mensal que o gate comercial lê está
errado nos DOIS sentidos, e configurá-lo por plano antes de consertar daria
controle fino sobre uma medição errada.

Purpose: o limite de 30 análises/mês do plano free hoje (a) é consumido por
telemetria que não é análise nenhuma e (b) ignora três rotas que gastam IA de
verdade. Um usuário pode ser barrado sem ter analisado nada, e outro pode
rodar `/api/scan/deep` dez vezes sem mover o contador.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-planos-comerciais/25-CONTEXT.md
@CLAUDE.md

<os_dois_defeitos_medidos>
**1. Telemetria consome cota de análise.** `server/app/main.py:861-863`:

```python
metering.consume(_conn, user["id"], custo=result["accepted"],
                 section="analyticsEvents", global_section="analyticsEventsGlobal")
```

Sem `month_section`. O default é `MONTH_SECTION = "aiUsageMonth"`
(`metering.py:98`, assinatura em `metering.py:290`) — exatamente o ledger que
`plan.can_analyze` lê via `metering.month_used` (`main.py:567`). E `custo` é
`result["accepted"]`, o tamanho do LOTE: 50 eventos somam 50.

**2. Três rotas de IA pulam o gate mensal**, chamando `_ai_apply_managed`
direto em vez de `_gate_analise`:
- `main.py:1817` — `POST /api/scan/deep`, com `custo=n` (topN, até 10)
- `main.py:2224` — `POST /api/carteira-stopalvo/{ticker}`
- `main.py:3562` — `POST /api/assistente`

Passam pelo gate: `main.py:1926` (`/api/technical/analyze`), `:2131`
(`/api/analyze`) e `options_mcp_api.py:2622` (`/setups/compilar`).

**3.** `metering.snapshot()` (`metering.py:352-358`) aceita `section` e o
ignora ao chamar `_load_month(conn, user_id)` — sempre lê `aiUsageMonth`.
</os_dois_defeitos_medidos>

<guardrails>
- **Não mude o que o gate DECIDE, só o que ele MEDE.** Os limites continuam os
  de hoje (30/mês no free). Este plano não toca `plan.py`.
- **`/api/scan/deep` reserva `custo=n`** (até 10 tickers). Ao passar pelo gate
  mensal, o mesmo `custo` tem de ir junto — contar 1 onde gasta 10 seria
  trocar um defeito de medição por outro.
- **O assistente tem freio próprio em R$** (`assistente.py:41-46`,
  `B3_ASSISTENTE_TETO_BRL`, default 1.0) e BYOK o dispensa
  (`assistente.py:287`). Acrescentar o gate mensal NÃO remove esse freio: são
  camadas distintas, como o ADR-010 separa físico de comercial.
- **Mudança de comportamento declarada:** ligar o gate mensal em três rotas
  que hoje não o têm PODE barrar quem antes passava. Antes de ativar, meça:
  quantas contas têm `month_used` acima do limite hoje. Se a amostra for
  relevante, PARE e relate — ativar pode virar decisão do Alex, não sua.
- Nenhum contador novo, nenhuma seção nova além da de analytics.
- **Não chamar os mutadores de estado do `gsd-sdk`** (`CLAUDE.md`). STATE à mão.
- Commit por heredoc com delimitador entre aspas; `-m "…"` com crases faz o
  shell executar o conteúdo.
- **Não tocar** em `server/web_dist`, `server/admin_dist`, `web/src/version.js`
  nem na linha `SERVER_BUILD_ID`.
</guardrails>
</context>

<tasks>

<task type="auto">
  <name>Task 1: telemetria para de consumir cota de análise</name>
  <read_first>
    - server/app/main.py linhas 835-866 (a rota de analytics inteira, incluindo `_analytics_quota_dia`/`_analytics_rate_min`)
    - server/app/metering.py linhas 88-100 (as constantes de seção) e 289-320 (`consume`)
  </read_first>
  <files>server/app/main.py</files>
  <action>
Acrescentar `month_section="analyticsEventsMonth"` ao `metering.consume` da
rota de analytics (`main.py:861-863`), com comentário registrando o achado:
o default `aiUsageMonth` é o ledger do gate comercial, e sem a seção própria
cada lote de telemetria descontava do limite mensal de análises do usuário —
`custo` ali é o tamanho do lote, não 1.

Se existir uma constante de módulo para as seções de analytics, use-a; hoje os
nomes são literais inline na rota. Extraí-los para constantes ao lado das
outras (`SECTION`/`GLOBAL_SECTION` de `metering.py`) é bem-vindo, desde que os
valores existentes **não mudem** — mudar o nome da seção perderia o histórico
já gravado no kv.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/server && .venv/bin/python -m pytest tests/ -q -k "analytics or metering or quota or plan"</automated>
  </verify>
  <acceptance_criteria>
    - Um lote de N eventos de analytics NÃO move `metering.month_used(conn, uid)`.
    - O contador diário de analytics (`analyticsEvents`) continua subindo — o rate limit da rota não pode ter sido desligado por engano.
    - O valor das seções diária e global de analytics não mudou (histórico preservado).
  </acceptance_criteria>
  <done>Telemetria conta no ledger dela; a cota de análise volta a medir análise.</done>
</task>

<task type="auto">
  <name>Task 2: as três rotas órfãs entram no gate mensal</name>
  <read_first>
    - server/app/main.py linhas 495-571 (`_ai_apply_managed` e `_gate_analise`, com a precedência BYOK → plano → metering)
    - server/app/main.py linhas 1806-1855 (`/api/scan/deep`, e como `custo=n` é calculado)
    - server/app/main.py linhas 2217-2290 (`/api/carteira-stopalvo`)
    - server/app/main.py linhas 3519-3580 (`/api/assistente`, e o freio em R$ que já existe)
  </read_first>
  <files>server/app/main.py</files>
  <action>
**ANTES de mudar, meça.** Escreva um diagnóstico de leitura (pode ser um teste
temporário ou um trecho no SUMMARY) que responda: com os limites de hoje,
quantas contas teriam sido barradas nessas três rotas no mês corrente? Se a
resposta for "várias", PARE e relate — a ativação passa a ser decisão do Alex.

Com a medição feita, trocar `_ai_apply_managed(...)` por `_gate_analise(...)`
nas três, preservando o `custo`:
- `/api/scan/deep` (`main.py:1817`): `custo=n`, o mesmo que já reserva.
- `/api/carteira-stopalvo` (`:2224`) e `/api/assistente` (`:3562`): custo 1.

Comentar em cada uma por que passou a ter o gate: o contador mensal é o que o
plano comercial lê, e rota que gasta IA sem contar nele fura o cap por
omissão. O `_gate_analise` já resolve BYOK antes do plano (`main.py:560-561`),
então quem usa chave própria continua passando.

**Preservar:** o freio em R$ do assistente e o `custo=n` do scan. Nenhum dos
dois é substituído pelo gate mensal.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/server && .venv/bin/python -m pytest tests/ -q -k "scan or assistente or carteira or gate or plan"</automated>
  </verify>
  <acceptance_criteria>
    - As três rotas passam por `_gate_analise` — provado por teste que estoura o mês e espera 402 (ou o fallback determinístico, onde a rota já o tem).
    - `/api/scan/deep` passa `custo=n` ao gate, não 1.
    - Com BYOK, as três continuam passando mesmo com o mês estourado.
    - O freio em R$ do assistente continua funcionando, independente do gate.
    - A medição de impacto está no SUMMARY.
  </acceptance_criteria>
  <done>Toda rota que gasta IA conta no mesmo lugar.</done>
</task>

<task type="auto">
  <name>Task 3: guardiões — o defeito não volta por onde entrou</name>
  <read_first>
    - server/tests/test_mcp_guardioes.py linhas 250-300 (o guardião de `month_section`, que hoje só varre `options_mcp_api.py`)
  </read_first>
  <files>server/tests/test_mcp_guardioes.py, server/tests/test_contador_mensal.py</files>
  <action>
**1. Estender o guardião existente.** `test_mcp_guardioes.py:258` resolve o
valor de `month_section` por AST e exige `"mcpUsageMonth"` — mas só em
`options_mcp_api.py`. Generalizar para varrer também `main.py`, com a regra:
toda chamada a `metering.consume` declara `month_section` **explicitamente**.
O valor exigido passa a ser "não é o default implícito", não um literal único
— cada módulo tem a sua seção.

Se generalizar quebrar a asserção específica do MCP, mantenha as duas: a geral
(todo consume declara) e a específica (a da aba Opções é `mcpUsageMonth`).
Guardião não se afrouxa.

**2. `test_contador_mensal.py`** — novo, o guardião do que o contador SIGNIFICA:
- lote de analytics não move `month_used`;
- cada uma das rotas de IA move `month_used` (uma por teste, nomeando a rota);
- `/api/scan/deep` move `n`, não 1;
- BYOK não move `month_used` em nenhuma delas (é o ledger da chave do
  servidor, e BYOK não usa a chave do servidor).

Cada um provado RED antes da correção.

**3.** `metering.snapshot()` (`metering.py:352-358`): propagar `section` para
`_load_month` ou remover o parâmetro. Escolha uma e justifique no código —
parâmetro que existe e não faz nada é armadilha para o próximo leitor.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2 && bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - O guardião generalizado FALHA se alguém acrescentar um `metering.consume` sem `month_section` em `main.py` — provar injetando e revertendo.
    - `test_contador_mensal.py` verde, com os casos acima.
    - Suíte canônica sem regressão sobre `2565 passed, 5 skipped` + 131 `.mjs`.
  </acceptance_criteria>
  <done>O caminho por onde o defeito entrou está fechado, e o contador tem um teste que diz o que ele significa.</done>
</task>

</tasks>

<verification>
1. `bash scripts/executar.sh --testes` FORA do sandbox (`dangerouslyDisableSandbox: true`).
2. Saída no SUMMARY, com a medição de impacto da Task 2.
</verification>

<success_criteria>
- Telemetria não consome cota de análise.
- Nenhuma rota de IA fora do gate mensal.
- Guardião cobre `main.py`.
- Zero mudança nos limites — este plano conserta a medição, não a política.
</success_criteria>

<output>
Create `.planning/phases/25-planos-comerciais/25-01-SUMMARY.md` when done
</output>
