---
phase: 20-funda-o-estrutural-e-tipogr-fica
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/App.jsx
  - web/tests/test_fase20_fundacao_visual.mjs
autonomous: true
requirements: [FIX-01, FIX-02, SYS-04]

must_haves:
  truths:
    - "Num viewport de 375px, a raiz do app (.b3-shell) e o container de conteúdo (<main>) não rolam para o lado: scrollWidth === clientWidth nos dois"
    - "A mensagem de status de mercado trunca com reticência visível em vez de empurrar conteúdo para fora do viewport, nos dois call sites (Topbar e home)"
    - "Em janela de 1280px o conteúdo principal fica contido em 720px, alinhado com a barra inferior que já respeita esse teto"
    - "Os trilhos horizontais intencionais (ticker/marquee, carrosséis) continuam roláveis dentro do próprio container"
    - "O teto de largura vive numa única constante compartilhada — o 720px não fica duplicado em dois literais"
  artifacts:
    - path: "web/src/App.jsx"
      provides: ".b3-shell com overflow-x:hidden, minWidth:0 nos containers pai do MarketStatusBadge, constante CONTENT_MAX_WIDTH aplicada nos dois call sites"
      contains: "CONTENT_MAX_WIDTH"
    - path: "web/tests/test_fase20_fundacao_visual.mjs"
      provides: "guardião estático de FIX-01/FIX-02/SYS-04 lendo web/src/App.jsx como texto (padrão dos 114 testes .mjs existentes)"
      min_lines: 40
  key_links:
    - from: "web/src/App.jsx (const CONTENT_MAX_WIDTH)"
      to: "BottomNav + wrapper de conteúdo pós-login"
      via: "referência à mesma constante em vez de dois literais 720px/1060px"
      pattern: "maxWidth: CONTENT_MAX_WIDTH"
    - from: "web/tests/test_fase20_fundacao_visual.mjs"
      to: "web/src/App.jsx"
      via: "readFileSync + asserção de substring, sem build/JSDOM"
      pattern: "readFileSync"
---

<objective>
Fechar a contenção horizontal do esqueleto: a raiz para de vazar lateralmente
(FIX-01), a mensagem de status de mercado trunca em vez de empurrar (FIX-02) e
o conteúdo principal ganha o mesmo teto de 720px que a barra inferior já
respeita (SYS-04).

Purpose: os três defeitos são da MESMA camada (contenção de largura no shell) e
os três são pré-requisito das Fases 21/22/23 — qualquer componente novo herda o
vazamento se ele continuar aberto. Além disso, o defeito FIX-02 foi documentado
em `20-UI-SPEC.md` e `20-PATTERNS.md` como NÃO confirmado por leitura estática
(4 dos 5 ancestrais do badge já têm `minWidth:0`); este plano mede ao vivo antes
de escrever a correção, exatamente como o achado original de FIX-01 foi
produzido (`scrollWidth` 504px vs `clientWidth` 375px).

Output: `web/src/App.jsx` com as três correções e um guardião estático novo em
`web/tests/` que impede a regressão das três.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-funda-o-estrutural-e-tipogr-fica/20-CONTEXT.md
@.planning/phases/20-funda-o-estrutural-e-tipogr-fica/20-UI-SPEC.md
@.planning/phases/20-funda-o-estrutural-e-tipogr-fica/20-PATTERNS.md
@CLAUDE.md

<interfaces>
Constantes e regras que este plano consome ou cria (extraídas do arquivo real,
verificadas linha a linha em 2026-09-05 — zero drift em relação ao UI-SPEC):

`web/src/App.jsx:236-243` — bloco de constantes compartilhadas, o único
precedente do arquivo para "valor declarado uma vez, consumido por referência":
`const MONO = "ui-monospace,'SF Mono',Menlo,Consolas,monospace";`
`const SANS = "'Nunito', -apple-system, system-ui, 'Segoe UI', Helvetica, Arial, sans-serif";`
`const DISPLAY = "'Fredoka', " + SANS;`
É aqui que `CONTENT_MAX_WIDTH` entra (idioma `const NOME = valor;` com
comentário curto em português explicando a decisão, no estilo das linhas 237-242).

`web/src/App.jsx:281-326` — `GlobalStyle()`, única autoridade de CSS "de
verdade" do app: uma função que devolve `<style>{...}</style>` com template
string, regras chapadas, uma por linha, sem aninhamento e sem pré-processador.
Linha 290 hoje: `.b3-shell{ height:100vh; height:100dvh; }`.

`web/src/App.jsx:8920` — style inline do MESMO elemento `.b3-shell`, que já
declara `overflow: "hidden"` nos dois eixos e mesmo assim não fecha o defeito
medido. A regra de classe é aditiva de propósito, não duplicada.

`web/src/App.jsx:875` — `BottomNav`, fonte do valor 720px:
`<div style={{ display: "flex", maxWidth: "720px", margin: "0 auto", padding: "5px 6px" }}>`

`web/src/App.jsx:8967` — wrapper de conteúdo pós-login, hoje com o literal
`maxWidth: "1060px"` (NÃO 720px — o texto do `20-CONTEXT.md` descreve o ponto de
inserção certo mas não nomeia o valor pré-existente):
`<div style={{ maxWidth: "1060px", margin: "0 auto", padding: "24px 18px 34px", transform: pullY ? ... }}>`

`web/src/App.jsx:795` — linha externa do `Topbar`, único ancestral do
`MarketStatusBadge` SEM `minWidth: 0` na leitura estática:
`<div style={{ display: "flex", alignItems: "center", gap: "12px", padding: "10px 16px", borderBottom: ..., background: T.bgPanel, flex: "none" }}>`
Os demais ancestrais (802, 803, 804, 827) já carregam `minWidth: 0`. O bloco
irmão da direita (linha 832) é `flex: "none"` com `whiteSpace: "nowrap"` — ele
NÃO encolhe por desenho, então é candidato a empurrar a linha.

`web/src/App.jsx:1871` — call site da home, este confirmado genuinamente sem a
propriedade: `<div style={{ marginTop: "4px" }}>` envolvendo `<MarketStatusBadge>`.

`bash scripts/executar.sh` — sobe backend em :8787 e Vite em :5173 (modo DEV).
`bash scripts/executar.sh --stop` encerra e libera as portas.
`bash scripts/executar.sh --testes` — suíte canônica (pytest + `web/tests/*.mjs`,
descobertos por glob: um arquivo novo em `web/tests/` entra na suíte sozinho,
sem registro em lugar nenhum).
`npx vite build` dentro de `web/` — obrigatório depois de editar `web/src/`
(grep e teste estático não pegam erro de sintaxe JSX).
</interfaces>

<decisao_do_planner>
**FIX-02 — a causa raiz NÃO está decidida por leitura estática, e este plano
não finge que está.** `20-UI-SPEC.md` (seção FIX-02) e `20-PATTERNS.md`
(seção 2) registram explicitamente que a leitura do código não encontrou o
ancestral faltando: 4 dos 5 já têm `minWidth: 0`. `App.jsx:795` é o candidato,
não a conclusão. A Task 1 mede antes; a Task 2 corrige o que a medição apontar.
Escrever a correção a partir do palpite é o anti-padrão que este projeto já
pagou caro (ver "3 defeitos que só a verificação ao vivo pegou", MEMORY).

**Verificação ao vivo é do executor, não de um checkpoint humano.** Esta fase é
puramente visual/CSS, sem dependência de dado de mercado ao vivo nem de
aparelho físico: uma conta local criada no backend local (`:8787`, SQLite
vazio) renderiza Topbar, H1s, BottomNav e estados vazios — suficiente para
medir largura, truncamento e teto. Não há motivo para empurrar isso para o
Alex.
</decisao_do_planner>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Medir ao vivo onde a largura vaza e se o badge trunca</name>
  <read_first>
    - web/src/App.jsx:281-326 (`GlobalStyle()` inteiro — todas as regras que já existem)
    - web/src/App.jsx:774-843 (`MarketStatusBadge` + a cadeia completa de ancestrais no `Topbar`)
    - web/src/App.jsx:1860-1878 (call site da home)
    - web/src/App.jsx:8915-8975 (shell pós-login: style inline do `.b3-shell`, `<main>`, wrapper de conteúdo, `BottomNav`)
    - .planning/phases/20-funda-o-estrutural-e-tipogr-fica/20-PATTERNS.md (seções 1 e 2 — estado verificado da cadeia de ancestrais)
    - scripts/executar.sh:1-60 (como subir DEV e como a suíte web é descoberta)
  </read_first>
  <files>nenhum arquivo editado nesta task (só medição); o achado é registrado no SUMMARY</files>
  <action>
Subir o app em DEV e medir, não deduzir.

1. Subir `bash scripts/executar.sh` em background (backend :8787 + Vite :5173).
   Aguardar a porta 5173 responder antes de seguir.
2. Abrir `http://localhost:5173` no navegador via a MCP do Chrome
   (`mcp__claude-in-chrome__*`). Se a extensão não estiver conectada, usar
   `mcp__computer-use__*` para abrir o Chrome e o DevTools. Se NENHUM dos dois
   estiver disponível, PARAR e reportar ao orquestrador — não substituir
   medição por leitura estática, é justamente o que este plano existe para
   evitar.
3. O app exige login. Criar uma conta local de teste pelo próprio fluxo de
   e-mail/senha contra o backend local (SQLite local nasce vazio; conta nova
   nasce limpa, sem posições-demo). Registrar no SUMMARY que a conta é local e
   descartável.
4. Com o viewport em **375 × 812** (largura do achado original), avaliar no
   contexto da página e anotar os números crus:
   - `document.querySelector('.b3-shell')` → `scrollWidth` e `clientWidth`
   - `document.querySelector('main')` → `scrollWidth` e `clientWidth`
   - `document.documentElement` e `document.body` → `scrollWidth` e `clientWidth`
   - varrer todos os elementos e listar os que têm
     `scrollWidth > clientWidth` OU `getBoundingClientRect().right > 375`,
     devolvendo para cada um `tagName`, `className`, os primeiros ~60
     caracteres do atributo `style` e a `right` medida. Essa lista é o
     resultado principal da task.
5. Separar dessa lista os trilhos horizontais INTENCIONAIS (o marquee do
   ticker `.tt-track` e qualquer carrossel com `overflowX` próprio) — eles
   devem continuar vazando dentro do próprio container; não são defeito.
6. Para FIX-02, medir o badge diretamente: pegar o `<span>` de texto dentro do
   `MarketStatusBadge` do Topbar e comparar `scrollWidth` vs `clientWidth`, com
   o Modo Estudo ativo (a frase longa "Mercado fechado, abre..." é a que
   estoura). Se a frase renderizada no estado atual for curta, forçar o pior
   caso alterando o texto do nó no DOM ao vivo (`textContent`) para uma frase
   longa equivalente e medir de novo — a medição é sobre o layout, não sobre o
   conteúdo. Subir a cadeia de ancestrais a partir do badge e anotar, para cada
   um, `computedStyle.minWidth`, `flexShrink` e `overflow`. Concluir QUAL
   ancestral impede o encolhimento.
7. Repetir o passo 4 (só shell e main) com viewport **1280 × 900**, anotando a
   largura renderizada do wrapper de conteúdo — o número "antes" do SYS-04
   (esperado: ~1060px ou a largura da janela).
8. Encerrar com `bash scripts/executar.sh --stop`.

Registrar no SUMMARY, em texto: os números medidos em 375px e 1280px, a lista
de elementos que vazam, o ancestral culpado do FIX-02 e se ele é ou não o
`App.jsx:795` candidatado pelo UI-SPEC. Se for outro, nomear qual (arquivo:linha
e o trecho do style).
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
    <human-check>não aplicável — a medição é feita pelo próprio executor via MCP do navegador</human-check>
  </verify>
  <acceptance_criteria>
    - O SUMMARY contém os quatro pares `scrollWidth`/`clientWidth` medidos em 375px (`.b3-shell`, `main`, `documentElement`, `body`), com números inteiros reais, não estimativas.
    - O SUMMARY contém a lista de elementos com `right > 375`, cada um identificado por `tagName` + trecho do `style`, e a marcação de quais são trilhos intencionais.
    - O SUMMARY nomeia UM ancestral específico (arquivo:linha) como causa do não-truncamento do badge, com os valores de `minWidth`/`flexShrink` que sustentam a conclusão — ou declara explicitamente que o badge JÁ trunca no estado atual e que FIX-02 se reduz ao call site da home (`App.jsx:1871`).
    - O SUMMARY registra a largura renderizada do wrapper de conteúdo em 1280px (o valor "antes" do SYS-04).
    - `bash scripts/executar.sh --testes` sai com código 0 (baseline intocada — esta task não edita código).
    - `git status --porcelain web/src` sai vazio (nenhuma edição de fonte nesta task).
  </acceptance_criteria>
  <done>A causa de cada um dos três defeitos está medida e nomeada por arquivo:linha, com números crus registrados. Nenhuma linha de código foi alterada ainda.</done>
</task>

<task type="auto">
  <name>Task 2: Aplicar contenção horizontal, truncamento e teto de 720px</name>
  <read_first>
    - O SUMMARY parcial / anotações da Task 1 (o ancestral culpado e a lista de elementos que vazam)
    - web/src/App.jsx:236-243 (idioma de declaração de constante compartilhada)
    - web/src/App.jsx:281-326 (`GlobalStyle()` — para editar a regra `.b3-shell` no lugar certo)
    - web/src/App.jsx:861-889 (`BottomNav` — o literal 720px a substituir pela constante)
    - .planning/phases/20-funda-o-estrutural-e-tipogr-fica/20-PATTERNS.md (seções 1, 2 e 3 — trechos exatos "antes/depois")
  </read_first>
  <files>web/src/App.jsx</files>
  <action>
Quatro edições em `web/src/App.jsx`, todas in-place, nenhum arquivo novo,
nenhuma dependência nova. Localizar cada ponto pelo CONTEÚDO citado abaixo, não
pelo número de linha isolado — números vão andar conforme as edições entram.

**(a) FIX-01 — contenção na raiz.** Dentro de `GlobalStyle()`, na regra
`.b3-shell{ height:100vh; height:100dvh; }`, acrescentar `overflow-x:hidden;`
como terceira propriedade da MESMA regra (não criar regra separada). Decisão
travada em `20-CONTEXT.md`; o `overflow:"hidden"` inline já existente em
`App.jsx:8920` não substitui isto — a auditoria mediu o vazamento com ele no
lugar.

**(b) FIX-01 — contenção adicional SE a Task 1 apontou outro elemento.** Se a
medição mostrou `scrollWidth > clientWidth` em `<main>` ou no wrapper de
conteúdo, aplicar a contenção mínima também lá (`overflowX: "hidden"` no style
inline do elemento medido). Duas restrições: (1) NÃO aplicar contenção em
elemento que seja trilho intencional; (2) `<main>` já tem `overflowY: "auto"`,
então acrescentar `overflowX: "hidden"` ali não muda o eixo vertical. Se a Task
1 mostrou que só o shell vazava, não fazer nada aqui e registrar isso.

**(c) FIX-02 — permitir o encolhimento.** Acrescentar `minWidth: 0` como chave
irmã no MESMO objeto de style inline do ancestral que a Task 1 nomeou (idioma
já usado 4 vezes na própria cadeia: `App.jsx:802, 803, 804, 827`). Se o
ancestral for o candidato do UI-SPEC, é a linha externa do `Topbar`
(`display:"flex", alignItems:"center", gap:"12px", padding:"10px 16px"`, com
`flex:"none"` e `borderBottom`). Independentemente do que a Task 1 apontou,
aplicar TAMBÉM no call site da home, que está confirmado como faltando:
`<div style={{ marginTop: "4px" }}>` envolvendo `<MarketStatusBadge>` passa a
`<div style={{ marginTop: "4px", minWidth: 0 }}>`. Não tocar em
`MarketStatusBadge` por dentro — ele já declara `whiteSpace:"nowrap"`,
`overflow:"hidden"`, `textOverflow:"ellipsis"` e `minWidth:0` no próprio span.

**(d) SYS-04 — teto único de largura.** Declarar, no bloco de constantes junto
de `MONO`/`SANS`/`DISPLAY`, com um comentário curto em português explicando a
decisão (no estilo dos comentários das linhas 237-242):
`CONTENT_MAX_WIDTH` com o valor `"720px"`. Comentário deve dizer, em uma ou
duas linhas, que 720px é o teto que o `BottomNav` já praticava e que a área de
conteúdo passa a respeitar o MESMO valor para não haver deriva entre os dois
(SYS-04, Fase 20). Depois: substituir o literal `maxWidth: "720px"` do
`BottomNav` por `maxWidth: CONTENT_MAX_WIDTH`, e o literal `maxWidth: "1060px"`
do wrapper de conteúdo pós-login por `maxWidth: CONTENT_MAX_WIDTH`. Ao terminar,
não pode sobrar nenhum literal `"720px"` nem `"1060px"` nesses dois pontos.

Não reordenar nem remover chave existente de nenhum objeto de style. Não mexer
em espaçamento interno de tela (é escopo da Fase 21/22). Não instalar nada.
  </action>
  <verify>
    <automated>cd web && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'overflow-x:hidden' web/src/App.jsx` ≥ 1 e a ocorrência está dentro da regra `.b3-shell` (verificável por `grep -n '\.b3-shell{' web/src/App.jsx` mostrando `overflow-x` na mesma linha).
    - `grep -n 'const CONTENT_MAX_WIDTH' web/src/App.jsx` retorna exatamente 1 linha, e o valor é `"720px"`.
    - `grep -c 'maxWidth: CONTENT_MAX_WIDTH' web/src/App.jsx` retorna 2.
    - `grep -c '"1060px"' web/src/App.jsx` retorna 0.
    - `grep -c 'maxWidth: "720px"' web/src/App.jsx` retorna 0.
    - `grep -n 'marginTop: "4px", minWidth: 0' web/src/App.jsx` retorna ≥ 2 linhas (o call site do Topbar que já tinha + o da home, agora corrigido).
    - `cd web && npx vite build` sai com código 0.
    - `git diff --stat web/src/App.jsx` mostra apenas o arquivo `App.jsx` alterado, e `git diff web/package.json web/package-lock.json` sai vazio.
  </acceptance_criteria>
  <done>As três correções estão no fonte, o build do Vite passa e o 720px existe em uma única constante referenciada nos dois lugares.</done>
</task>

<task type="auto">
  <name>Task 3: Guardião estático novo + remedição ao vivo em 375px e 1280px</name>
  <read_first>
    - web/tests/test_brand_book_v2_tokens.mjs:1-40 (padrão do guardião: cabeçalho explicando POR QUE existe, `readFileSync` de `../src/App.jsx`, helper `ok(name, cond)`, `process.exit(fails)`)
    - web/tests/test_hero_reconciliado.mjs (segundo exemplo do mesmo padrão, para confirmar a convenção de saída)
    - scripts/executar.sh:19-46 (como a suíte web descobre `web/tests/*.mjs` por glob)
    - web/src/App.jsx (estado após a Task 2 — as strings exatas que o guardião vai travar)
  </read_first>
  <files>web/tests/test_fase20_fundacao_visual.mjs</files>
  <action>
**Parte 1 — criar o guardião.** Novo arquivo
`web/tests/test_fase20_fundacao_visual.mjs`, seguindo o padrão dos 114 testes
`.mjs` existentes: sem build, sem JSDOM, sem dependência — lê
`web/src/App.jsx` como texto com `readFileSync`, usa o helper
`ok(nome, condicao)` que imprime `ok `/`FALHOU ` e conta falhas, e termina com
`process.exit(fails)`. Rodável isolado por `node web/tests/test_fase20_fundacao_visual.mjs`
a partir de `web/`.

Cabeçalho em comentário deve explicar POR QUE o guardião existe (convenção do
repositório: guardião carrega história de decisão, não repete o código): a
Fase 20 é a fundação estrutural do v1.5 e as Fases 21/22/23 editam o MESMO
arquivo de 9k linhas; sem trava, uma dessas fases reintroduz o vazamento
horizontal sem ninguém perceber, porque o sintoma só aparece em 375px.

Asserções desta parte (as de tipografia e movimento entram nos planos 20-02 e
20-03, no MESMO arquivo):
- FIX-01: a regra `.b3-shell` dentro de `GlobalStyle()` contém `overflow-x:hidden`.
- FIX-02: existe pelo menos DOIS containers com `marginTop: "4px", minWidth: 0`
  (Topbar e home) e o `<span>` de rótulo do `MarketStatusBadge` continua com
  `textOverflow: "ellipsis"` — a correção depende das duas metades.
- SYS-04: existe exatamente uma declaração `const CONTENT_MAX_WIDTH`, seu valor
  é `"720px"`, há exatamente duas referências `maxWidth: CONTENT_MAX_WIDTH`, e
  não sobrou nenhum literal `"1060px"` no arquivo.
- Anti-regressão de escopo: o arquivo NÃO passou a importar Tailwind, shadcn,
  styled-components ou qualquer CSS-in-JS (asserção de ausência de `import`
  desses nomes) — a Fase 20 é explicitamente proibida de introduzi-los pelo
  `20-UI-SPEC.md`.

Ao contar ocorrências, filtrar linhas de comentário antes de contar (o próprio
cabeçalho do guardião e os comentários em português do `App.jsx` mencionam os
mesmos termos; contagem crua se autoinvalida).

**Parte 2 — remedir ao vivo.** Subir `bash scripts/executar.sh` de novo, logar
com a conta local da Task 1 e repetir as medições:
- Em **375 × 812**: `.b3-shell` e `<main>` com `scrollWidth === clientWidth`;
  nenhum elemento com `getBoundingClientRect().right > 375` além dos trilhos
  intencionais identificados na Task 1.
- Em **375 × 812**, badge do Topbar em Modo Estudo com a frase longa: o
  `<span>` de rótulo tem `scrollWidth > clientWidth` (o texto não cabe) E
  `getComputedStyle(span).textOverflow === "ellipsis"` E a reticência é visível
  no screenshot — ou seja, ele TRUNCA em vez de empurrar. O bloco da direita
  (patrimônio) continua inteiro na tela.
- Em **1280 × 900**: o wrapper de conteúdo mede 720px de largura e está
  horizontalmente centrado; a largura da barra inferior bate com a do conteúdo
  (mesmo teto, alinhados).
- Tirar screenshot das duas larguras e descrever no SUMMARY o que aparece.
- Regressão de largura intermediária: em **820 × 1000** (tablet, faixa 768-1059px
  que ANTES usava até 1060px), navegar por Acompanhar, Watchlist e Portfólio e
  registrar se alguma tela quebra visivelmente (corte, sobreposição, texto
  espremido) com o teto novo de 720px. Narrowing é o resultado correto do
  SYS-04, mas quebra visível é regressão desta fase e precisa ser registrada
  explicitamente no SUMMARY (mesmo que o conserto pertença à Fase 21/22).
- Encerrar com `bash scripts/executar.sh --stop`.

**Parte 3.** Rodar a suíte canônica inteira.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `web/tests/test_fase20_fundacao_visual.mjs` existe e `cd web && node tests/test_fase20_fundacao_visual.mjs` sai com código 0.
    - O guardião de fato trava: revertendo temporariamente `overflow-x:hidden` do `.b3-shell` o teste falha (verificar e desfazer a reversão; não deixar a reversão commitada).
    - `bash scripts/executar.sh --testes` sai com código 0 (as DUAS suítes — pytest + web).
    - O SUMMARY registra, com números medidos, que em 375px `.b3-shell.scrollWidth === .b3-shell.clientWidth` e `main.scrollWidth === main.clientWidth`.
    - O SUMMARY registra que o `<span>` do badge tem `scrollWidth > clientWidth` com `textOverflow: ellipsis` computado, e que nenhum conteúdo do Topbar saiu do viewport de 375px.
    - O SUMMARY registra a largura medida do wrapper de conteúdo em 1280px = 720px e o alinhamento com o `BottomNav`.
    - O SUMMARY registra o resultado da varredura em 820px nas 3 telas, nomeando qualquer quebra visível (ou afirmando que não houve).
  </acceptance_criteria>
  <done>Guardião novo na suíte (verde e comprovadamente sensível), suíte canônica verde, e as três verdades de FIX-01/FIX-02/SYS-04 medidas ao vivo com números registrados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhuma nova | Esta fase edita apenas CSS/estilo inline no bundle do front. Não cria rota, não lê entrada do usuário, não toca `server/app/*.py`, não muda contrato de API, não instala pacote. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | `web/src/App.jsx` — edição num arquivo de 9k linhas compartilhado com as Fases 17/18/19 não publicadas | mitigate | Editar por âncora de conteúdo, nunca por número de linha isolado; `npx vite build` obrigatório; `git diff --stat` limitado a `App.jsx` como critério de aceite |
| T-20-02 | Denial of Service | Contenção horizontal ampla demais quebrando trilhos roláveis (ticker, carrosséis) | mitigate | A Task 1 classifica os trilhos intencionais ANTES da correção; a Task 3 remede e confirma que continuam roláveis |
| T-20-03 | Information disclosure | Conta local de teste criada no backend local | accept | SQLite local descartável, sem dado real, sem segredo; nada é commitado |
| T-20-SC | Tampering | instalações npm/pip/cargo | mitigate | Zero pacote novo nesta fase (`20-UI-SPEC.md`, Registry Safety: nenhum registry usado). Critério de aceite exige `git diff web/package.json web/package-lock.json` vazio |
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` verde (duas suítes).
- `cd web && npx vite build` sem erro.
- Medição ao vivo em 375px: shell e main sem rolagem horizontal.
- Medição ao vivo em 1280px: conteúdo em 720px, alinhado ao BottomNav.
- Badge trunca com reticência em vez de empurrar.
</verification>

<success_criteria>
FIX-01, FIX-02 e SYS-04 verificados por medição ao vivo (não por leitura de
código), travados por guardião estático novo na suíte canônica, com o 720px
existindo numa única constante compartilhada.
</success_criteria>

<output>
Create `.planning/phases/20-funda-o-estrutural-e-tipogr-fica/20-01-SUMMARY.md` when done
</output>
