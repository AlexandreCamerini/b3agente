---
phase: 41-consolida-o-de-registros-de-tela
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - web/src/App.jsx
  - web/src/telas.js
  - web/tests/test_telas_registro.mjs
  - web/tests/test_pet_opcoes.mjs
  - web/tests/test_operador_ia_subtela.mjs
  - web/tests/test_copy_theme.mjs
  - web/tests/test_radar.mjs
  - web/tests/test_pet_ui.mjs
  - web/tests/test_tour_opcoes.mjs
autonomous: true
requirements: [TELAS-01]

must_haves:
  truths:
    - "BottomNav, tourPassos, ajudaSecoes e o petTela/petSnapshot leem a LISTA de telas de web/src/telas.js — não existe mais `const defs = [[`, nem a ternária de petTela, nem lista de telas embutida no tour/ajuda (SC#1)"
    - "Rótulos da barra nos dois modos, os 6 passos do tour, as 11 seções da ajuda, a derivação de petTela e o corpo do switch do petSnapshot são idênticos ao fixture pré-refactor (D-04, SC#2)"
    - "Um `case` novo no switch do petSnapshot sem entrada no registro, ou uma entrada `snapshot: \"switch\"` sem `case`, reprova em teste (D-03)"
    - "`git diff web/src/copy.js docs/AJUDA.md server/app/` vazio ao fim do plano (D-04)"
    - "Todo guardião que pinava a forma antiga foi reconciliado com nota datada, nenhum apagado, e nenhum perdeu cobertura (asserção textual trocada por asserção comportamental equivalente ou mais forte)"
    - "O passo-a-passo de adicionar uma tela hipotética está escrito (cabeçalho de telas.js + SUMMARY), com a contagem de arquivos tocados (SC#4), e os achados de inconsistência estão registrados sem correção (D-04)"
  artifacts:
    - path: "web/src/App.jsx"
      provides: "4 consumidores religados ao registro"
      contains: "from \"./telas.js\""
    - path: "web/tests/test_telas_registro.mjs"
      provides: "PARTE D (fiação: nenhuma lista paralela sobrou) + PARTE E (case do switch ↔ registro) + equivalência tour/ajuda executando App.jsx"
      contains: "idsComSnapshotNoSwitch"
    - path: "web/src/telas.js"
      provides: "Cabeçalho 'Como adicionar uma tela' conferido contra o código religado"
      contains: "Como adicionar uma tela"
  key_links:
    - from: "web/src/App.jsx BottomNav"
      to: "defsDaBarra(cp)"
      via: "import de ./telas.js"
      pattern: "defsDaBarra\\(cp\\)"
    - from: "web/src/App.jsx petTela"
      to: "telaDoAssistente(tab, carteiraView)"
      via: "import de ./telas.js"
      pattern: "const petTela = telaDoAssistente\\(tab, carteiraView\\);"
    - from: "web/src/App.jsx tourPassos/ajudaSecoes"
      to: "telasDoTour()/telasDaAjuda()"
      via: "textos indexados por id dentro das próprias funções (D-02)"
      pattern: "telasDaAjuda\\(\\)"
---

<objective>
Religar os 4 consumidores de tela do App.jsx ao registro `web/src/telas.js` criado na 41-01,
sem nenhuma mudança visível, e reconciliar os guardiões que pinavam a forma antiga.

Purpose: fecha SC#1 (uma lista, não quatro) e SC#4 (tela nova = um ponto no front + espelho no
backend), com SC#2 provado pelo fixture pré-refactor em vez de por inspeção visual.
Output: App.jsx religado; test_telas_registro.mjs estendido (fiação + switch); 6 guardiões
reconciliados com nota datada; suíte canônica + `npx vite build` verdes; SUMMARY com o
passo-a-passo de tela nova e os achados registrados.
</objective>

<repo_guardrail>
- D-04: refactor puro. Qualquer diff visível é bug. `web/src/copy.js`, `docs/AJUDA.md`, `server/app/` não mudam. Strings de tour/ajuda não mudam de texto — só de lugar DENTRO da própria função (D-02).
- D-03: o corpo do switch do petSnapshot (de `switch (petTela) {` até `}, [petTela, data, quotes, wlScan]);`) não muda um byte — o sha256 do fixture trava.
- Guardiões não se apagam: cada asserção textual que deixar de casar é SUBSTITUÍDA por uma equivalente/mais forte, com nota datada "REVERSÃO DELIBERADA (<data real>, Fase 41, TELAS-01): …" explicando o que mudou e por que a cobertura não caiu. Comentários históricos existentes ficam.
- Qualquer outro guardião que ficar vermelho na suíte canônica e não estiver listado aqui segue a mesma regra e entra no SUMMARY.
- Front editado → `cd web && npx vite build` antes de declarar pronto. Suíte canônica `bash scripts/executar.sh --testes`, fora do sandbox se aparecer o artefato TLS.
- `eval` direto dentro de módulo ESM enxerga os imports do módulo: os testes que avaliam `tourPassos`/`ajudaSecoes` extraídas do App.jsx precisam importar `telasDoTour`/`telasDaAjuda` de ../src/telas.js para o eval resolver os nomes.
</repo_guardrail>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-consolida-o-de-registros-de-tela/41-CONTEXT.md
@.planning/phases/41-consolida-o-de-registros-de-tela/41-PATTERNS.md
@.planning/phases/41-consolida-o-de-registros-de-tela/41-01-SUMMARY.md
@CLAUDE.md

<interfaces>
De web/src/telas.js (41-01): `TELAS`, `idsDasTelas()`, `defsDaBarra(cp)`, `telasDoTour()`, `telasDaAjuda()`, `telaDoAssistente(tab, carteiraView)`, `idsComSnapshotNoSwitch()`. Campos por entrada: `id, barra, rotuloCp, rotuloPadrao, tour, ajuda, snapshot, subtelaDe, carteiraView`.

Asserções textuais que HOJE pinam a forma antiga (a reconciliar):
- test_pet_opcoes.mjs:42-49 — regex `const defs = \[\[…\]\];` para derivar `abasDaBarra`.
- test_operador_ia_subtela.mjs:39-47 — bloco `defs` com 5 itens, `["opcoes", (cp && cp.tabOpcoes)`, `["mercado", (cp && cp.tituloWatchlist)`, `["radar", (cp && cp.tabRadar)`; :98-99 — regex literal da ternária de petTela.
- test_copy_theme.mjs:48 e :55 — `app.includes('["mercado", (cp && cp.tituloWatchlist)')` e `app.includes('["radar", (cp && cp.tabRadar)')`.
- test_radar.mjs:44 — regex `["radar", (cp && cp.tabRadar) || "Radar"]`.
- test_pet_ui.mjs:177-178 — regex literal da ternária de petTela; :167 `ABAS_PET` escrita à mão.
- test_tour_opcoes.mjs:61-65 — eval de tourPassos/ajudaSecoes (passará a precisar dos imports do registro).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BottomNav e petTela leem do registro + reconciliação dos guardiões da barra e do petTela</name>
  <files>web/src/App.jsx, web/tests/test_pet_opcoes.mjs, web/tests/test_operador_ia_subtela.mjs, web/tests/test_copy_theme.mjs, web/tests/test_radar.mjs, web/tests/test_pet_ui.mjs</files>
  <read_first>
    - web/src/telas.js (inteiro)
    - web/src/App.jsx (imports do topo; BottomNav ~1023-1055; bloco petTela ~9125-9133 — localizar com grep)
    - web/tests/test_pet_opcoes.mjs, web/tests/test_operador_ia_subtela.mjs, web/tests/test_copy_theme.mjs (linhas 40-60), web/tests/test_radar.mjs (linhas 38-48), web/tests/test_pet_ui.mjs (linhas 155-190)
    - web/tests/fixtures/telas_baseline_41.json
  </read_first>
  <action>
    App.jsx: adicionar `import { defsDaBarra, telasDoTour, telasDaAjuda, telaDoAssistente } from "./telas.js";` junto dos imports locais. No BottomNav, trocar o literal `const defs = [[…]];` por `const defs = defsDaBarra(cp);` — o `defs.map(([id, label]) => …)` e todo o JSX ficam idênticos. Preservar os comentários históricos do BottomNav e somar uma nota datada "Fase 41 (TELAS-01, <data>): a lista de abas, a ordem e o fallback literal de cada rótulo moram em web/src/telas.js; o TEXTO por modo continua em copy.js via rotuloCp (D-02)". Trocar a linha do petTela por `const petTela = telaDoAssistente(tab, carteiraView);`, preservando o comentário F4/aba-opcoes F2 acima dela e somando nota datada de que as subtelas (agente/historico) são declaradas no registro por `subtelaDe`/`carteiraView`. NÃO tocar o useMemo do petSnapshot nesta task.
    Guardiões (cada um com nota "REVERSÃO DELIBERADA (<data>, Fase 41, TELAS-01)" explicando: a lista literal saiu do App.jsx para o registro; a asserção passou de texto para comportamento, com cobertura igual ou maior):
    - test_pet_opcoes.mjs: derivar `abasDaBarra` de `defsDaBarra(COPY.estudo).map(([id]) => id)` importado de ../src/telas.js (e COPY de ../src/copy.js); acrescentar `ok` de que o BottomNav chama `defsDaBarra(cp)` no App.jsx. Todo o resto (loop contra PET_TELAS e `case`, ramo backend de opcoes, princípio 4) fica igual. Manter o cabeçalho histórico e acrescentar que a consolidação C3 chegou (Fase 41) e o guardião central agora é test_telas_registro.mjs — este permanece como verificação da aba Opções.
    - test_operador_ia_subtela.mjs seção 1: substituir as asserções do bloco `defs` por: BottomNav usa `defsDaBarra(cp)`; `defsDaBarra(COPY.estudo)` tem 5 itens; 5º é `["opcoes", COPY.estudo.tabOpcoes]`; entradas radar/mercado do registro declaram `rotuloCp` "tabRadar"/"tituloWatchlist". Seção 7: trocar a regex da ternária por `const petTela = telaDoAssistente(tab, carteiraView);` + tabela-verdade (carteira+historico→historico, carteira+agente→agente, carteira+main→carteira).
    - test_copy_theme.mjs:48 e :55: trocar `app.includes(...)` por comportamento — rótulo de mercado em `defsDaBarra(COPY.estudo)`/`(COPY.operador)` igual a `COPY.<modo>.tituloWatchlist`, rótulo do radar igual a `COPY.<modo>.tabRadar` e diferente entre os modos; mais `ok` de que o BottomNav chama `defsDaBarra(cp)`.
    - test_radar.mjs:44: trocar a regex por: entrada radar do registro com `rotuloCp === "tabRadar"` e `rotuloPadrao === "Radar"`, e `defsDaBarra(null)` devolvendo "Radar" para radar.
    - test_pet_ui.mjs:177-178: trocar a regex da ternária pela linha nova + a mesma tabela-verdade; em :167 manter `ABAS_PET` e acrescentar `ok` de que `new Set(ABAS_PET)` é igual ao conjunto de `idsDasTelas()` (D-01).
    Rodar os 5 guardiões + test_telas_registro.mjs e `npx vite build`.
  </action>
  <acceptance_criteria>
    - `grep -c "const defs = \[\[" web/src/App.jsx` == 0 e `grep -c "const defs = defsDaBarra(cp);" web/src/App.jsx` == 1
    - `grep -c 'carteiraView === "historico" ? "historico"' web/src/App.jsx` == 0 e `grep -c "const petTela = telaDoAssistente(tab, carteiraView);" web/src/App.jsx` == 1
    - `grep -c "REVERSÃO DELIBERADA" ` em cada um dos 5 guardiões ≥ 1 (contagem nova, comparada com `git show HEAD:<arquivo> | grep -c`)
    - `cd web && for t in test_pet_opcoes test_operador_ia_subtela test_copy_theme test_radar test_pet_ui test_telas_registro; do node tests/$t.mjs || exit 1; done` sai 0
    - `cd web && npx vite build` sai 0
    - `git diff --quiet web/src/copy.js docs/AJUDA.md server/app/`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/web && for t in test_pet_opcoes test_operador_ia_subtela test_copy_theme test_radar test_pet_ui test_telas_registro; do node tests/$t.mjs >/dev/null || { echo "FALHOU $t"; exit 1; }; done && npx vite build >/dev/null && git diff --quiet src/copy.js ../docs/AJUDA.md ../server/app/ && echo ok</automated>
  </verify>
  <done>Barra e petTela leem do registro; guardiões reconciliados sem perda de cobertura; build verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tourPassos e ajudaSecoes iteram os ids do registro; switch do petSnapshot amarrado ao registro por teste</name>
  <files>web/src/App.jsx, web/tests/test_telas_registro.mjs, web/tests/test_tour_opcoes.mjs</files>
  <read_first>
    - web/src/App.jsx (ajudaSecoes+tourPassos ~2488-2580; useMemo do petSnapshot ~9134-9240 — só leitura para o teste)
    - web/tests/test_telas_registro.mjs (estado da 41-01)
    - web/tests/test_tour_opcoes.mjs (inteiro)
    - web/tests/fixtures/telas_baseline_41.json
  </read_first>
  <behavior>
    - PARTE C estendida: `tourPassos(COPY.<modo>)` e `ajudaSecoes(COPY.<modo>, op)` extraídas do App.jsx ATUAL (extrair()+eval, com os imports do registro no escopo) deepEqual o fixture nas 2 e 4 combinações
    - PARTE D (fiação, SC#1): App.jsx não contém `const defs = [[` nem a ternária antiga; BottomNav chama `defsDaBarra(cp)`; `tourPassos` chama `telasDoTour()`; `ajudaSecoes` chama `telasDaAjuda()`; petTela usa `telaDoAssistente`
    - PARTE E (D-03): o conjunto de `case "<id>":` do bloco do switch do petSnapshot é igual ao conjunto `idsComSnapshotNoSwitch()`; todo `case` é id do registro; o sha256 do bloco do switch é igual ao do fixture
  </behavior>
  <action>
    RED: estender test_telas_registro.mjs com as PARTES C (tour/ajuda executando o App.jsx), D e E acima; importar `telasDoTour`/`telasDaAjuda` no topo para o eval resolver; reusar a função `extrair` (copiar de test_tour_opcoes.mjs, com comentário de origem). A regex do bloco do switch é a mesma do gerador da 41-01 (de `switch (petTela) {` até `}, [petTela, data, quotes, wlScan]);`). Mensagens de causa-raiz: "case sem entrada no registro → tela que o assistente explica sem ser tela registrada"; "entrada snapshot:'switch' sem case → cai no default: return {} e o Boris fala sem dado (achado A1 da Fase 26)". Rodar: PARTE D reprova no tour/ajuda (ainda não religados).
    GREEN (App.jsx, D-02 — textos ficam nas funções):
    - `tourPassos(cp)`: manter os 2 passos de introdução literais no topo do array; os 4 passos de tela passam para um objeto local `porTela` indexado por id (radar, mercado, carteira, opcoes) com os MESMOS pares [titulo, corpo], strings idênticas byte a byte (inclusive "1 · ", "2 · "… e o `cp.tituloOpcoes || "Opções"`); retorno = introdução seguida de `telasDoTour().map((id) => porTela[id])`. Preservar o comentário histórico de 2026-09-12 (Fase 26, A4) e somar nota datada Fase 41.
    - `ajudaSecoes(cp, operador)`: as 2 seções institucionais do topo e as 3 do fim ficam literais onde estão; as 6 seções de tela vão para um objeto local `porTela` (evolucao, radar, mercado, carteira, opcoes, agente) com títulos e parágrafos idênticos — os comentários internos (Fase 26 A4 sobre Opções; 2026-09-10 aba-opcoes F2 sobre o Operador IA) acompanham a entrada correspondente; retorno = topo + `telasDaAjuda().map((id) => porTela[id])` + fim. Nota datada Fase 41 no comentário qa/38 do topo.
    - O useMemo/switch do petSnapshot NÃO é editado (sha256 trava). Acrescentar só um comentário ACIMA de `const petSnapshot = useMemo(` (fora do bloco hasheado) dizendo que os `case` deste switch são amarrados às entradas `snapshot: "switch"` de telas.js por test_telas_registro.mjs (PARTE E) e que "mercado" é `snapshot: "petSheet"` no registro.
    test_tour_opcoes.mjs: acrescentar `import { telasDoTour, telasDaAjuda } from "../src/telas.js";` com nota "REVERSÃO DELIBERADA (<data>, Fase 41, TELAS-01): tourPassos/ajudaSecoes passaram a iterar os ids do registro; o eval precisa desses nomes no escopo do módulo. Nenhuma asserção mudou." — todas as asserções existentes ficam como estão.
    Rodar até verde + `npx vite build`.
  </action>
  <acceptance_criteria>
    - `cd web && node tests/test_telas_registro.mjs && node tests/test_tour_opcoes.mjs && node tests/test_ajuda_help.mjs` saem 0
    - `grep -c "telasDoTour()" web/src/App.jsx` ≥ 1 e `grep -c "telasDaAjuda()" web/src/App.jsx` ≥ 1
    - PARTE E compara o sha256 do bloco do switch com o do fixture e passa (switch intocado)
    - `cd web && npx vite build` sai 0
    - `git diff --quiet web/src/copy.js docs/AJUDA.md server/app/`
    - Prova negativa registrada no SUMMARY: (a) acrescentar temporariamente `case "fantasma": { return {}; }` no switch → PARTE E reprova; (b) trocar temporariamente um caractere num título de passo do tour → PARTE C reprova; reverter ambos
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/web && node tests/test_telas_registro.mjs && node tests/test_tour_opcoes.mjs && node tests/test_ajuda_help.mjs && npx vite build >/dev/null && echo ok</automated>
  </verify>
  <done>As 4 listas leem do registro; tour e ajuda idênticos ao fixture; switch amarrado ao registro por teste.</done>
</task>

<task type="auto">
  <name>Task 3: suíte canônica, zero-diff visível, passo-a-passo de tela nova (SC#4) e achados registrados</name>
  <files>web/src/telas.js</files>
  <read_first>
    - web/src/telas.js e web/src/App.jsx (já religados)
    - .planning/phases/41-consolida-o-de-registros-de-tela/41-PATTERNS.md (seção "Achados registrados")
    - web/src/copy.js (só as chaves tabRadar/tituloRadar/tituloWatchlist/tituloPortfolio/tabOpcoes/tituloOpcoes/tituloOperadorIA nos dois modos — grep)
  </read_first>
  <action>
    1. Cabeçalho de telas.js: acrescentar a seção "Como adicionar uma tela" descrevendo o procedimento REAL após a religação, conferido contra o código: (a) entrada nova em TELAS (único ponto da LISTA no front); (b) espelho em `conceitos.PET_TELAS` (backend) — a paridade reprova nos dois lados se faltar; (c) conteúdo que depende da natureza da tela e que os testes EXIGEM quando o campo pede: `case` no switch do petSnapshot se `snapshot: "switch"` (PARTE E reprova sem ele), texto em `porTela` de tourPassos se `tour` não-nulo, texto em `porTela` de ajudaSecoes se `ajuda: true` (a equivalência/undefined reprova), path no `NavIcon` se `barra` não-nulo, chave em copy.js se `rotuloCp`, o componente da tela e o ramo de render em App.jsx, e o ramo de resumo em main.py se a tela precisar de resumo próprio. Ser honesto: SC#4 é "a LISTA mora num ponto só e o resto é exigido por teste", não "uma linha e pronto".
    2. Walkthrough no SUMMARY: simular (sem commitar) uma tela hipotética `"estudos"` na barra, sem tour, com ajuda e snapshot "switch": aplicar SÓ a entrada em TELAS, rodar test_telas_registro.mjs e pytest test_telas_paridade.py, e registrar quais asserções reprovam e apontam cada arquivo que falta; contar os arquivos tocados no total (registro + PET_TELAS + conteúdo exigido) e comparar com o antes (4 listas no App.jsx + PET_TELAS, nenhuma amarrada). Reverter com `git checkout -- <arquivos>` e conferir `git status` limpo.
    3. Achados registrados, NÃO corrigidos (D-04), listados no SUMMARY com evidência (arquivo:linha e valores dos dois modos): ajudaSecoes tem 11 seções e só 6 são tela; historico e perfil sem seção de ajuda; tour cobre 4 telas (agente/historico/perfil sem passo; evolucao só no passo-zero de introdução); rótulo da aba do Radar na barra (`tabRadar`: Radar/Mesa) difere do título usado no tour/ajuda (`tituloRadar`: Radar de mercado/Mesa de oportunidades); seção "Operador IA" da ajuda é literal enquanto o BackHeader usa `cp.tituloOperadorIA`; "Acompanhar" é literal nos três lugares (barra, tour, ajuda "Acompanhar (início)") sem chave de copy; qualquer outro descasamento visto durante a execução.
    4. Verificação final: `bash scripts/executar.sh --testes` (fora do sandbox se o artefato TLS aparecer — registrar os dois números) comparado ao baseline do 41-01-SUMMARY; `cd web && npx vite build`; `git diff --stat HEAD~N -- web/src/copy.js docs/AJUDA.md server/app/` vazio para todos os commits da fase.
    Limitação a declarar: nada visível mudou, então o iOS com bundle antigo segue igual; o registro só chega ao binário num build novo de TestFlight, sem efeito para o usuário.
  </action>
  <acceptance_criteria>
    - `grep -c "Como adicionar uma tela" web/src/telas.js` == 1
    - SUMMARY contém: walkthrough da tela hipotética com as asserções que reprovaram e a contagem de arquivos; lista de achados com arquivo:linha; números da suíte canônica fora do sandbox (0 failed pytest; .mjs só com a falha ambiental conhecida test_ios_assets.mjs, se aparecer)
    - `git status --porcelain` limpo após reverter a simulação
    - `git diff 1d4f54a -- web/src/copy.js docs/AJUDA.md server/app/ | wc -l` == 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2 && grep -c "Como adicionar uma tela" web/src/telas.js && test "$(git diff 1d4f54a -- web/src/copy.js docs/AJUDA.md server/app/ | wc -l | tr -d ' ')" = "0" && (cd web && npx vite build >/dev/null) && echo ok</automated>
  </verify>
  <done>SC#1–#4 demonstrados por teste e por walkthrough; achados registrados; suíte canônica e build verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| front (tela ativa) → /api/assistente, /api/pet/resumo | `petTela` vira `tela: "pet:<id>"`; backend valida contra PET_TELAS |
| registro ↔ consumidores do App.jsx | 4 consumidores passam a depender de um módulo só |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-05 | Integrity (mudança visível) | BottomNav/tour/ajuda religados | mitigate | equivalência deepEqual com o fixture pré-refactor em test_telas_registro.mjs (PARTE C) + checkpoint humano da 41-03 |
| T-41-06 | Denial of Service (assistente mudo/errado) | telaDoAssistente substituindo a ternária | mitigate | tabela-verdade de 35 combinações contra o fixture; PARTE E amarra case↔registro |
| T-41-07 | Repudiation (perda de guardião) | 6 guardiões reconciliados | mitigate | nota datada em cada um, asserção textual trocada por comportamental equivalente/mais forte; nenhum arquivo de teste apagado |
| T-41-08 | Tampering (conteúdo regulatório) | textos da ajuda/tour (avisos "nenhuma ordem", "simulado") | mitigate | strings não mudam (deepEqual byte a byte); git diff de copy.js/AJUDA.md vazio |
| T-41-09 | Elevation of Privilege | allowlist do backend | accept | server/app/ intocado; validação server-side inalterada |
</threat_model>

<verification>
1. PARTES A–E de test_telas_registro.mjs verdes, com provas negativas registradas.
2. 6 guardiões reconciliados verdes, cada um com nota datada.
3. `npx vite build` verde; suíte canônica fora do sandbox sem regressão.
4. Diff de copy.js, docs/AJUDA.md e server/app/ vazio desde 1d4f54a.
</verification>

<success_criteria>
- SC#1: nenhuma lista paralela de telas no App.jsx (PARTE D).
- SC#2: comportamento idêntico ao fixture (PARTE C) — confirmação humana na 41-03.
- SC#3: paridade em dois pontos (41-01), intacta.
- SC#4: walkthrough da tela hipotética no SUMMARY com contagem de arquivos.
</success_criteria>

<output>
Create `.planning/phases/41-consolida-o-de-registros-de-tela/41-02-SUMMARY.md` when done
</output>
