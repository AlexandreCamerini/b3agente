---
phase: 41-consolida-o-de-registros-de-tela
plan: 03
type: execute
wave: 3
depends_on: ["41-02"]
files_modified:
  - web/src/version.js
  - server/web_dist
  - server/app/main.py
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [TELAS-01]

must_haves:
  truths:
    - "O Alex viu, rodando, as 5 abas nos dois modos, o tour inteiro, a Ajuda aberta e o assistente respondendo em 2–3 telas (incluindo agente ou histórico), e aprovou que nada mudou visivelmente (D-05)"
    - "O Alex tomou ciência, por nome, de que os achados de inconsistência registrados na 41-02 NÃO foram corrigidos nesta fase (D-04)"
    - "A fase está NO AR no PWA (BUILD_ID novo conferido em /api/health de produção) e nenhum push ocorreu antes do aprovado"
    - "TELAS-01 Done e Fase 41 fechada em ROADMAP/STATE/REQUIREMENTS, editados À MÃO"
  artifacts:
    - path: "web/src/version.js"
      provides: "BUILD_ID novo carimbando a entrega da Fase 41"
      contains: "F10-"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TELAS-01 Done"
      contains: "TELAS-01"
  key_links:
    - from: "web/src/version.js (BUILD_ID)"
      to: "server/web_dist + SERVER_BUILD_ID em /api/health"
      via: "scripts/bump.sh seguido de scripts/publicar-web.sh"
      pattern: "F10-[0-9]{8}-[0-9]{2}"
---

<objective>
Fechar a Fase 41: verificação humana curta e bloqueante de que o refactor não mudou nada
visível (D-05), publicação do front e documentos de planejamento fechados à mão.

Purpose: plano que toca `web/src/` sem bump+publicação fica testado e nunca vai ao ar; o
checkpoint humano segura o push da fase inteira (padrão das Fases 39/40).
Output: build no ar, TELAS-01 Done, STATE/ROADMAP/REQUIREMENTS coerentes.
</objective>

<repo_guardrail>
- Não chamar mutador de estado do `gsd-sdk` (`state.advance-plan`, `state.record-session`, `state.record-metric`, `state.add-decision`) nem `roadmap.annotate-dependencies` — editar à mão com Edit; `git diff .planning/` antes do commit.
- `git fetch origin && git merge origin/main` ANTES do `scripts/bump.sh` (bump.sh deriva do valor LOCAL); `scripts/bump.sh` antes de `scripts/publicar-web.sh`; nunca editar `server/web_dist` à mão; conflito em `web_dist` se resolve REGERANDO.
- Histórico não se reescreve: o comentário de `SERVER_BUILD_ID` em main.py ganha a entrada nova no topo e preserva o texto anterior como `HISTORICO (entrega anterior, F10-20260925-01)` (ou o carimbo que estiver lá após o merge de origin/main).
- Nenhum push antes do "aprovado" da Task 1.
</repo_guardrail>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-consolida-o-de-registros-de-tela/41-CONTEXT.md
@.planning/phases/41-consolida-o-de-registros-de-tela/41-02-SUMMARY.md
@.planning/phases/40-continuidade-da-aba-op-es/40-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: verificação humana curta — nada visível mudou (D-05) + ciência dos achados</name>
  <what-built>
    - Registro único `web/src/telas.js` com as 8 telas do assistente; BottomNav, tour, ajuda e petTela/petSnapshot leem a lista dele (41-02).
    - Paridade front↔`conceitos.PET_TELAS` testada nos dois lados (41-01).
    - Por desenho, NADA visível mudou (D-04): rótulos, ordem, passos do tour, seções da ajuda, resposta do assistente.
  </what-built>
  <how-to-verify>
    Subir backend + Vite locais (`bash scripts/executar.sh`; se travar em `lsof`/`ps`, subir backend e Vite diretamente), conta com pelo menos 1 posição na carteira, viewport 375px.

    1. **Barra, Modo Estudo**: 5 abas na ordem Acompanhar · Radar · Watchlist · Portfólio · Opções; tocar cada uma abre a tela certa, ícone e destaque de ativa como antes.
    2. **Barra, Modo Operador** (Perfil → Modo de trabalho): Acompanhar · Mesa · Monitoramento · Posições · Opções; tocar cada uma.
    3. **Tour**: Perfil → Como funciona → "Ver o tour de novo": 6 passos, na ordem Bem-vindo (Acompanhar) → O que o Boris+ é → 1 · Descubra no Radar de mercado/Mesa de oportunidades → 2 · Acompanhe na … → 3 · Simule no … → 4 · Estude estruturas em Opções; "Pular" fecha.
    4. **Ajuda**: 11 seções na ordem O que é o Boris+ · Os dois modos · Acompanhar (início) · <Radar> · <Watchlist> · <Portfólio> · Opções · Operador IA · Fundamento (A/B/C) · Eficiência da IA · Avisos importantes; abrir 2 delas.
    5. **Assistente (pet)** em 3 telas: Portfólio, Opções e uma subtela (Portfólio → "Abrir o Operador IA" OU "Ver histórico de operações"): o Boris abre falando daquela tela (não da Watchlist), sem erro "Tela desconhecida.".
    6. Repetir o item 5 numa tela em Modo Estudo e noutra em Modo Operador.

    **Ciência a confirmar POR NOME (não vale por inferência dos itens 1-6):** apresentar a lista de achados do 41-02-SUMMARY (ex.: historico/perfil sem seção de ajuda; tour sem passo para agente/historico/perfil; aba "Mesa" na barra × "Mesa de oportunidades" no tour/ajuda; "Operador IA" literal na ajuda; "Acompanhar" sem chave de copy) com a frase, sem parafrasear: **"Estes achados ficaram registrados e NÃO foram corrigidos nesta fase, porque a decisão foi refactor sem nenhuma mudança visível. Concorda em deixá-los para depois?"** Se o Alex quiser corrigir algum agora, isso vira fase/quick separada DEPOIS da publicação — não entra nesta.

    **SC#4 a confirmar POR NOME (adicionado 2026-09-25, warning do gsd-plan-checker):** apresentar o resultado do walkthrough de "tela hipotética" do 41-02-SUMMARY (quantos arquivos uma tela nova tocava ANTES × DEPOIS da fase, e quais peças continuam exigidas fora do registro — case do switch do petSnapshot, textos em tourPassos/ajudaSecoes/copy.js, ícone, render, ramo do main.py quando aplicável) com a frase, sem parafrasear: **"O ROADMAP dizia que uma tela nova exigiria editar um único ponto do front. O que a fase entrega é: a LISTA de telas mora num ponto só e os testes passam a exigir e apontar cada peça que falta — mas o conteúdo (textos, ícone, snapshot) ainda mora nos seus lugares, por decisão D-02. Aceita essa leitura do critério #4?"** Rejeição = divergência BLOQUEANTE (reabre D-02; plano de gap antes de publicar).

    Divergência em qualquer item 1–6, ou rejeição da leitura do SC#4, é BLOQUEANTE para a publicação (vira plano de gap).
  </how-to-verify>
  <read_first>
    - .planning/phases/41-consolida-o-de-registros-de-tela/41-02-SUMMARY.md (achados, walkthrough, números da suíte)
    - web/src/copy.js (rótulos tabRadar/tituloRadar/tituloWatchlist/tituloPortfolio nos dois modos, para conferir os nomes esperados do roteiro)
  </read_first>
  <action>
    Subir o ambiente local, apresentar ao Alex o roteiro de 6 itens + a frase de ciência dos achados, e PARAR. Não publicar, não dar push, não seguir para a Task 2 antes da resposta.
  </action>
  <acceptance_criteria>
    - O Alex responde "aprovado" com resposta EXPLÍCITA à frase dos achados E à frase do SC#4, ou descreve divergências. Um "aprovado" sem menção aos dois não basta: perguntar de novo, por nome. Nenhum push antes disso.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — verificação visual/interativa por desenho; substitutos automatizados: `cd web && node tests/test_telas_registro.mjs && node tests/test_tour_opcoes.mjs && node tests/test_pet_ui.mjs && node tests/test_pet_opcoes.mjs`</automated>
  </verify>
  <resume-signal>Digite "aprovado" com a resposta sobre os achados e sobre a leitura do SC#4 — ou descreva o que divergiu</resume-signal>
  <done>Aprovação humana registrada com ciência dos achados, ou lista de divergências (plano de gap) antes de publicar.</done>
</task>

<task type="auto">
  <name>Task 2: publicar o front e conferir em produção</name>
  <files>web/src/version.js, server/web_dist, server/app/main.py</files>
  <read_first>
    - scripts/bump.sh (linhas 1-40)
    - scripts/publicar-web.sh (inteiro)
    - server/app/main.py (linha `SERVER_BUILD_ID = ...` ~1662 e o formato da entrada com HISTORICO)
    - CLAUDE.md — "Publicação" e "Três esquemas de versão"
  </read_first>
  <action>
    Ordem obrigatória:
    1. `git fetch origin && git merge origin/main`. Conflito em `server/web_dist` se resolve regerando.
    2. Suíte canônica pré-bump: `bash scripts/executar.sh --testes` (fora do sandbox se o artefato TLS aparecer), números no SUMMARY comparados com o 41-02-SUMMARY.
    3. `bash scripts/bump.sh`, depois `bash scripts/publicar-web.sh`. Se `npm ci` der EPERM de sandbox, rodar só o passo de build fora do sandbox e registrar — nenhum código de produto tocado para contornar.
    4. Comentário do `SERVER_BUILD_ID` em main.py: entrada nova no topo descrevendo a Fase 41 (TELAS-01: registro único `web/src/telas.js` com as 8 telas do assistente — D-01; BottomNav/tour/ajuda/petTela leem a lista dele, textos continuam em tourPassos/ajudaSecoes/copy.js — D-02; switch do petSnapshot intocado e amarrado ao registro por teste — D-03; refactor puro, equivalência provada por fixture pré-refactor e confirmada ao vivo pelo Alex — D-04/D-05; paridade com conceitos.PET_TELAS em dois pontos, test_telas_registro.mjs + test_telas_paridade.py; guardiões reconciliados com nota datada; achados registrados e não corrigidos, com a resposta do Alex; números da suíte) e o texto anterior preservado integralmente como `HISTORICO (entrega anterior, <carimbo anterior>)`.
    5. Commit + push em `v2/interacao-estrutural` e fast-forward de `origin/main` (padrão das Fases 39/40).
    6. Conferir no ar (redeploy ~3-6 min): `curl -s https://boris.semente.dev/api/health` mostrando o `SERVER_BUILD_ID` novo; colar a saída no SUMMARY.
    Limitação a declarar, não resolver: o app iOS carrega bundle local — o registro só chega ao iPhone num build novo de TestFlight; como nada visível nem de contrato mudou, o bundle antigo segue idêntico para o usuário.
  </action>
  <acceptance_criteria>
    - `grep -E "F10-[0-9]{8}-[0-9]{2}" web/src/version.js` com a data real de hoje
    - `grep -c "HISTORICO (entrega anterior, F10-20260925-01)" server/app/main.py` == 1 (ou o carimbo anterior real após o merge)
    - `/api/health` de produção com o `SERVER_BUILD_ID` novo (saída no SUMMARY)
    - `git log origin/main -1 --format=%H` == `git rev-parse HEAD`
  </acceptance_criteria>
  <verify>
    <automated>grep -E "F10-[0-9]{8}-[0-9]{2}" /Users/acamerini/dev/borisv2/web/src/version.js && curl -s https://boris.semente.dev/api/health</automated>
  </verify>
  <done>Fase 41 no ar no PWA, carimbo conferido em produção, pendência iOS registrada.</done>
</task>

<task type="auto">
  <name>Task 3: fechar os documentos de planejamento à mão</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (seção "Consolidação técnica" ~60-66 e tabela de rastreio ~90-100)
    - .planning/ROADMAP.md (item da Fase 41 no topo ~linha 23 e a seção "### Phase 41")
    - .planning/STATE.md (frontmatter, "Current Position", formato das entradas "Posição anterior")
  </read_first>
  <action>
    Com Edit (nunca mutador do gsd-sdk):
    1. REQUIREMENTS.md: `- [x] **TELAS-01**` com a nota "*Done em <data>* (Fase 41, `<BUILD_ID>`): registro `web/src/telas.js`, paridade em dois pontos, zero mudança visível confirmada ao vivo"; `Pending` → `Done` na linha TELAS-01 da tabela; linha "Last updated" nova citando Fase 41 fechada e o BUILD_ID.
    2. ROADMAP.md: `- [x] Phase 41: Consolidação de registros de tela (3/3 plans) — completed <data>`; na seção "### Phase 41", marcar os 3 planos `[x]`. Se todas as fases do v1.8 estiverem fechadas, NÃO fechar o milestone aqui (isso é `/gsd-complete-milestone`, decisão do Alex) — só registrar no STATE como próximo passo.
    3. STATE.md: frontmatter (`status`, `stopped_at`, `last_updated`, `last_activity`, `progress` — contadores coerentes entre si e com a barra, contando a Fase 41 e os 3 planos); "Current Position" reescrito para a Fase 41 fechada: o que foi entregue, BUILD_ID, e as decisões que o próximo leitor não deve redescobrir — registro em `web/src/telas.js` (8 telas, D-01), textos fora do registro (D-02), switch do petSnapshot amarrado por teste e hash (D-03), fixture `web/tests/fixtures/telas_baseline_41.json` é verdade congelada e NÃO se regenera, "Como adicionar uma tela" no cabeçalho de telas.js, achados registrados e a resposta do Alex. Próximo passo: decidir com o Alex o fechamento do milestone v1.8. A posição anterior vai para "Posição anterior", texto da época preservado.
    4. Pendências declaradas: build iOS/TestFlight; os achados de inconsistência (lista curta, apontando o 41-02-SUMMARY).
    Conferir `git diff .planning/` (sem texto antigo sobrescrito, sem métrica fora de tabela). Commit separado `docs(41): fecha fase 41` e push junto do fast-forward de origin/main.
  </action>
  <acceptance_criteria>
    - `grep -c "TELAS-01.*Done" .planning/REQUIREMENTS.md` ≥ 1 e `grep -c "\[ \] \*\*TELAS-01" .planning/REQUIREMENTS.md` == 0
    - `grep -n "Phase 41" .planning/ROADMAP.md | head -1` mostra `[x]`
    - `grep -c "telas_baseline_41" .planning/STATE.md` ≥ 1
    - `git diff .planning/STATE.md` mostra só as seções pretendidas
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2 && git diff --stat HEAD~1 -- .planning/ && grep -c "TELAS-01.*Done" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>Documentos refletem a realidade, editados à mão, com achados e pendências registrados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → produção (Railway) | push publica `server/` e `server/web_dist` juntos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-10 | Tampering (entrega) | version.js → web_dist | mitigate | merge de origin/main antes do bump.sh; bump.sh antes de publicar-web.sh; carimbo conferido em /api/health |
| T-41-11 | Integrity (regressão visível em produção) | nav/tour/ajuda/assistente | mitigate | checkpoint bloqueante (itens 1–6) antes de qualquer push |
| T-41-12 | Integrity (cliente iOS antigo) | app nativo com bundle anterior | accept | nenhuma mudança de backend/contrato nem visível; bundle antigo idêntico para o usuário |
| T-41-13 | Repudiation (histórico) | STATE/ROADMAP/REQUIREMENTS, comentário SERVER_BUILD_ID | mitigate | edição à mão + git diff; HISTORICO preservado; mutadores do SDK proibidos |
</threat_model>

<verification>
1. Aprovação humana antes de qualquer push, com ciência explícita dos achados.
2. BUILD_ID novo conferido em produção.
3. `git diff .planning/` revisado antes do commit de docs.
</verification>

<success_criteria>
- Alex aprovou ao vivo que nada visível mudou e tomou ciência dos achados; fase no ar.
- TELAS-01 Done; Fase 41 fechada à mão; pendências declaradas.
</success_criteria>

<output>
Create `.planning/phases/41-consolida-o-de-registros-de-tela/41-03-SUMMARY.md` when done
</output>
