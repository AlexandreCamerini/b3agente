---
phase: 41-consolida-o-de-registros-de-tela
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/tests/fixtures/gerar_telas_baseline_41.mjs
  - web/tests/fixtures/telas_baseline_41.json
  - web/src/telas.js
  - web/tests/test_telas_registro.mjs
  - server/tests/test_telas_paridade.py
autonomous: true
requirements: [TELAS-01]

must_haves:
  truths:
    - "Existe um registro único `TELAS` com as 8 telas que o assistente conhece (evolucao, radar, mercado, carteira, opcoes, agente, historico, perfil), cada uma com a flag/ordem de barra, posição no tour, presença na ajuda e origem do snapshot (D-01, D-02)"
    - "Um teste do front (web/tests) e um teste do backend (server/tests) comparam os ids do registro com `conceitos.PET_TELAS` 8 com 8, sem exceção, e ambos REPROVAM quando os dois lados divergem (SC#3)"
    - "O comportamento de hoje (rótulos da barra por modo, passos do tour, seções da ajuda, derivação de `petTela`, corpo do switch do `petSnapshot`) está congelado num fixture gerado do App.jsx ANTES de qualquer edição, e as funções do registro reproduzem exatamente esse fixture (D-04)"
    - "Nenhum arquivo de produto existente é tocado neste plano (App.jsx, copy.js, conceitos.py intactos) — o registro nasce sem consumidor, como rede de segurança"
  artifacts:
    - path: "web/src/telas.js"
      provides: "Registro único TELAS + funções puras de leitura (defsDaBarra, telasDoTour, telasDaAjuda, telaDoAssistente, idsComSnapshotNoSwitch)"
      contains: "export const TELAS"
    - path: "web/tests/fixtures/telas_baseline_41.json"
      provides: "Saídas do código PRÉ-refactor (barra × 4 cps, tour × 2 modos, ajuda × 4 combinações, tabela-verdade de petTela, sha256 do corpo do switch)"
      contains: "_origem"
    - path: "web/tests/test_telas_registro.mjs"
      provides: "Guardião de paridade registro↔PET_TELAS + equivalência registro↔baseline"
      contains: "PET_TELAS"
    - path: "server/tests/test_telas_paridade.py"
      provides: "Segundo ponto testado: pytest lê web/src/telas.js como texto e compara com conceitos.PET_TELAS"
      contains: "PET_TELAS"
  key_links:
    - from: "web/tests/test_telas_registro.mjs"
      to: "server/app/conceitos.py"
      via: "leitura do fonte como texto + regex (nunca import cross-language)"
      pattern: "PET_TELAS = \\("
    - from: "server/tests/test_telas_paridade.py"
      to: "web/src/telas.js"
      via: "open() + re.findall dos ids do bloco TELAS"
      pattern: "telas\\.js"
    - from: "web/tests/test_telas_registro.mjs"
      to: "web/tests/fixtures/telas_baseline_41.json"
      via: "deepEqual entre a saída das funções do registro e o fixture pré-refactor"
      pattern: "telas_baseline_41\\.json"
---

<objective>
Criar a rede de segurança da Fase 41 ANTES de mexer em qualquer consumidor: (1) congelar num
fixture o comportamento atual das 4 listas de tela do App.jsx; (2) criar o registro único
`web/src/telas.js` com as 8 telas (D-01) e funções puras que reproduzem, byte a byte, o que as
4 listas produzem hoje; (3) testes de paridade nos DOIS pontos (web/tests e server/tests) contra
`conceitos.PET_TELAS`.

Purpose: a ROADMAP marca esta como a fase mais arriscada do v1.8 (nav+tour+ajuda+assistente de
uma vez). Com o fixture e a paridade verdes antes da religação (41-02), qualquer mudança visível
introduzida pelo refactor reprova em teste, não no checkpoint humano.
Output: telas.js (sem consumidor ainda), fixture + gerador, test_telas_registro.mjs,
test_telas_paridade.py — todos verdes; App.jsx/copy.js/conceitos.py intocados.
</objective>

<repo_guardrail>
- Refactor puro, zero mudança visível (D-04): este plano NÃO edita App.jsx, copy.js, conceitos.py, main.py nem docs/AJUDA.md.
- Paridade sem import cross-language (CLAUDE.md, padrão defaults.py × catalog.js): JS lê `.py` como texto; Python lê `.js` como texto. Nunca subprocess, nunca import do outro lado.
- Suíte canônica é `bash scripts/executar.sh --testes` (pytest + web/tests/*.mjs); dentro do sandbox ~27 testes de backend falham por artefato TLS — rodar fora do sandbox e registrar os dois números.
- `web/tests/fixtures/` é subdiretório: o runner só executa `web/tests/*.mjs` do topo, então o gerador NÃO roda na suíte (é ferramenta de auditoria, não teste).
</repo_guardrail>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-consolida-o-de-registros-de-tela/41-CONTEXT.md
@.planning/phases/41-consolida-o-de-registros-de-tela/41-PATTERNS.md
@CLAUDE.md

<interfaces>
Fontes ATUAIS das 4 listas (App.jsx, HEAD 1d4f54a — confirmar linhas com grep antes de ler):
- `BottomNav` ~1023; a lista é `const defs = [["evolucao", "Acompanhar"], ["radar", (cp && cp.tabRadar) || "Radar"], ["mercado", (cp && cp.tituloWatchlist) || "Watchlist"], ["carteira", (cp && cp.tituloPortfolio) || "Portfólio"], ["opcoes", (cp && cp.tabOpcoes) || "Opções"]];` (termina em `]];`). Ícone vem de `<NavIcon id={id} />` (o id É a chave do ícone).
- `ajudaSecoes(cp, operador)` ~2492: 11 seções `[titulo, [paragrafos]]`. Ordem: "O que é o Boris+", "Os dois modos", "Acompanhar (início)" (=evolucao), tRadar (=radar), tWl (=mercado), tPort (=carteira), tOpc (=opcoes), "Operador IA" (=agente), "Fundamento (A/B/C)", "Eficiência da IA", "Avisos importantes". historico e perfil NÃO têm seção.
- `tourPassos(cp)` ~2569: 6 passos `[titulo, corpo]`: 2 de introdução (não-tela, ficam FORA do registro por D-02) + "1 · Descubra no <tituloRadar>" (=radar), "2 · Acompanhe na <tituloWatchlist>" (=mercado), "3 · Simule no <tituloPortfolio>" (=carteira), "4 · Estude estruturas em <tituloOpcoes>" (=opcoes).
- `petTela` ~9132: `const petTela = tab === "carteira" ? (carteiraView === "historico" ? "historico" : carteiraView === "agente" ? "agente" : "carteira") : tab;`
- `petSnapshot` ~9138: `useMemo(() => { if (!data) return {}; switch (petTela) { case "carteira": … case "evolucao": … case "radar": … case "agente": … case "historico": … case "opcoes": … case "perfil": … default: return {}; } }, [petTela, data, quotes, wlScan]);` — "mercado" SEM case de propósito (snapshot dele vem do resumo dentro do PetSheet).

Backend (NÃO editar): `server/app/conceitos.py:565` → `PET_TELAS = ("mercado", "carteira", "evolucao", "radar", "agente", "historico", "perfil", "opcoes")`.

`web/src/copy.js` exporta `COPY` (`COPY.estudo`, `COPY.operador`) — importável em node (os testes .mjs já fazem `import { COPY } from "../src/copy.js"`).

Técnica de extração/avaliação de função pura do App.jsx sem build (copiar de `web/tests/test_tour_opcoes.mjs:44-65`): `extrair(nome, fonte)` por contagem de chaves + `eval("(" + fonte + ")")`.

Contrato do registro a criar (web/src/telas.js) — tabela, uma linha por entrada, NESTA ordem de array (a ordem do array é a ordem da ajuda):

| id | barra (ordem, null = fora) | rotuloCp (chave de cp) | rotuloPadrao (fallback literal) | tour (posição no funil, null = sem passo) | ajuda (tem seção) | snapshot | subtela |
|---|---|---|---|---|---|---|---|
| evolucao | 1 | null | "Acompanhar" | null | true | "switch" | — |
| radar | 2 | "tabRadar" | "Radar" | 1 | true | "switch" | — |
| mercado | 3 | "tituloWatchlist" | "Watchlist" | 2 | true | "petSheet" | — |
| carteira | 4 | "tituloPortfolio" | "Portfólio" | 3 | true | "switch" | — |
| opcoes | 5 | "tabOpcoes" | "Opções" | 4 | true | "switch" | — |
| agente | null | — | — | null | true | "switch" | subtelaDe "carteira", carteiraView "agente" |
| historico | null | — | — | null | false | "switch" | subtelaDe "carteira", carteiraView "historico" |
| perfil | null | — | — | null | false | "switch" | — |

Funções puras exportadas (assinaturas — o executor implementa):
- `defsDaBarra(cp)` → array `[[id, rotulo], …]` das entradas com `barra` não-nulo, ordenadas por `barra`; rótulo = `(cp && t.rotuloCp && cp[t.rotuloCp]) || t.rotuloPadrao` (mesma semântica de `(cp && cp.X) || "literal"`; evolucao ignora cp).
- `telasDoTour()` → ids com `tour` não-nulo, ordenados por `tour` (["radar","mercado","carteira","opcoes"]).
- `telasDaAjuda()` → ids com `ajuda === true`, na ordem do array (["evolucao","radar","mercado","carteira","opcoes","agente"]).
- `telaDoAssistente(tab, carteiraView)` → se `tab === "carteira"`, o id da entrada com `subtelaDe === "carteira"` e `carteiraView === carteiraView`, senão "carteira"; para qualquer outro `tab`, devolve `tab` inalterado (inclusive valor desconhecido — mesma semântica da ternária atual).
- `idsComSnapshotNoSwitch()` → ids com `snapshot === "switch"`.
- `idsDasTelas()` → os 8 ids na ordem do array.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: congelar o comportamento atual das 4 listas num fixture (antes de qualquer edição)</name>
  <files>web/tests/fixtures/gerar_telas_baseline_41.mjs, web/tests/fixtures/telas_baseline_41.json</files>
  <read_first>
    - web/src/App.jsx (só os trechos: BottomNav ~1023-1040, ajudaSecoes+tourPassos ~2488-2580, petTela+petSnapshot ~9125-9240 — localizar com grep antes)
    - web/tests/test_tour_opcoes.mjs (linhas 1-65: técnica extrair()+eval, import de COPY)
    - web/tests/test_pet_opcoes.mjs (linhas 40-60: regex do bloco defs e leitura de PET_TELAS)
  </read_first>
  <action>
    Criar `web/tests/fixtures/gerar_telas_baseline_41.mjs` (node ESM, sem dependência nova) com comentário de cabeçalho datado ("Fase 41, plano 41-01, 2026-09-25 ou data real") dizendo: gera o baseline do comportamento PRÉ-refactor das 4 listas de tela; roda uma vez sobre o App.jsx antes da religação da 41-02; rodar de novo DEPOIS da 41-02 falha por desenho (o bloco `const defs` e a ternária deixam de existir) — o JSON gerado é a verdade congelada, não se regenera. Aceitar um argumento opcional com o caminho do App.jsx (default `web/src/App.jsx`) para permitir auditoria contra `git show <sha>:web/src/App.jsx > arquivo`.
    O gerador produz `web/tests/fixtures/telas_baseline_41.json` com:
    - `_origem`: sha de `git rev-parse HEAD` no momento da geração + data + a frase "NÃO regenerar após a 41-02 (D-04)";
    - `barra`: para cada cp em {estudo: COPY.estudo, operador: COPY.operador, nulo: null, vazio: {}}, a lista `[[id, rotulo]]` obtida avaliando a expressão do array `defs` extraída do BottomNav (regex `const defs = (\[\[[\s\S]*?\]\]);` e `new Function("cp", "return " + expr)`);
    - `tour`: `tourPassos(COPY.estudo)` e `tourPassos(COPY.operador)` (extrair()+eval);
    - `ajuda`: `ajudaSecoes` para as 4 combinações (estudo,false), (estudo,true), (operador,false), (operador,true);
    - `petTela`: tabela-verdade avaliando a expressão RHS da linha `const petTela = …;` (extraída por regex, avaliada com `new Function("tab","carteiraView", "return " + expr)`) para tab ∈ {evolucao, radar, mercado, carteira, opcoes, perfil, "desconhecida"} × carteiraView ∈ {"main", "historico", "agente", undefined → serializar como "__undefined__", "outra"};
    - `switchPetSnapshot`: `casos` = ids de `case "<id>":` dentro do bloco que vai de `switch (petTela) {` até `}, [petTela, data, quotes, wlScan]);`, e `sha256` desse bloco inteiro (node:crypto) — o corpo do switch não pode mudar um byte (D-03).
    Rodar `cd web && node tests/fixtures/gerar_telas_baseline_41.mjs` e commitar o JSON gerado. Conferir à mão no JSON: barra estudo = Acompanhar/Radar/Watchlist/Portfólio/Opções; barra operador = Acompanhar/Mesa/Monitoramento/Posições/Opções; tour com 6 passos; ajuda com 11 seções; casos do switch = carteira, evolucao, radar, agente, historico, opcoes, perfil (7, sem mercado).
    Não editar App.jsx.
  </action>
  <acceptance_criteria>
    - `web/tests/fixtures/telas_baseline_41.json` existe e `node -e` com JSON.parse mostra: `barra.estudo.length === 5`, `barra.operador[1][1] === "Mesa"`, `tour.estudo.length === 6`, `ajuda` com 11 seções em cada combinação, `switchPetSnapshot.casos.length === 7` sem "mercado", `switchPetSnapshot.sha256` com 64 hex, `_origem` contendo o sha de HEAD
    - `git diff --quiet web/src/` (nenhum arquivo de produto alterado)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/web && node tests/fixtures/gerar_telas_baseline_41.mjs && node -e 'const b=JSON.parse(require("fs").readFileSync("tests/fixtures/telas_baseline_41.json","utf8"));if(b.barra.estudo.length!==5||b.tour.estudo.length!==6||b.switchPetSnapshot.casos.length!==7||b.switchPetSnapshot.casos.includes("mercado"))process.exit(1);console.log("ok")' && git diff --quiet src/</automated>
  </verify>
  <done>Fixture pré-refactor commitado, com origem rastreável, cobrindo as 4 listas + corpo do switch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: registro único web/src/telas.js + guardião de paridade e equivalência no front</name>
  <files>web/src/telas.js, web/tests/test_telas_registro.mjs</files>
  <read_first>
    - web/src/opcoes/memoriaOpcoes.js (formato de módulo puro: cabeçalho-contrato, funções pequenas, guard clause)
    - web/tests/test_pet_opcoes.mjs (inteiro — padrão `ok()`, leitura de PET_TELAS por regex, mensagem de causa-raiz na falha)
    - web/tests/fixtures/telas_baseline_41.json (gerado na Task 1)
    - server/app/conceitos.py (linha 565, PET_TELAS — só leitura)
  </read_first>
  <behavior>
    - idsDasTelas() tem exatamente 8 ids, sem duplicata, e o conjunto é igual ao de conceitos.PET_TELAS lido como texto (8×8, sem exceção — D-01)
    - defsDaBarra(cp) deepEqual baseline.barra para cp = COPY.estudo, COPY.operador, null e {}
    - telasDoTour() === ["radar","mercado","carteira","opcoes"]; telasDaAjuda() === ["evolucao","radar","mercado","carteira","opcoes","agente"]
    - telaDoAssistente(tab, view) reproduz a tabela-verdade baseline.petTela para as 35 combinações
    - idsComSnapshotNoSwitch() tem o mesmo conjunto de baseline.switchPetSnapshot.casos (mercado é a única com snapshot "petSheet")
    - toda entrada com barra não-nula tem rotuloPadrao string não-vazia e ordens de barra 1..5 contíguas; toda entrada com tour tem posição única 1..4
    - o módulo não importa nada (sem React, sem store, sem copy.js)
  </behavior>
  <action>
    Primeiro (RED) criar `web/tests/test_telas_registro.mjs` com cabeçalho datado ("Fase 41, plano 41-01 — TELAS-01") explicando: o registro é a fonte única da LISTA de telas (achado C3 da Fase 26 / A1 — "opcoes" entrou na barra e ficou fora do assistente); este guardião absorve o papel de rede de segurança que `test_pet_opcoes.mjs` se autodeclarava ter "até a Fase 41"; a paridade com PET_TELAS segue o padrão de dois pontos testados (este arquivo lê conceitos.py como texto; `server/tests/test_telas_paridade.py` lê telas.js como texto). Estrutura: PARTE A (paridade com PET_TELAS, regex idêntica à de test_pet_opcoes.mjs), PARTE B (integridade do registro: 8 ids, sem duplicata, ordens contíguas), PARTE C (equivalência com o fixture: deepEqual por JSON.stringify para barra, tour-ids, ajuda-ids, petTela, snapshot-ids). Cada `ok()` com mensagem de causa-raiz (ex.: "id fora de PET_TELAS → /api/assistente responde 400 'Tela desconhecida.'"; "rótulo da barra divergiu do baseline → mudança visível, viola D-04"). Rodar: tem que falhar (módulo inexistente).
    Depois (GREEN) criar `web/src/telas.js`: cabeçalho-contrato no padrão de memoriaOpcoes.js (Fase 41, TELAS-01: registro único da ESTRUTURA das telas — D-02: nenhum texto de tour/ajuda aqui, eles continuam em tourPassos/ajudaSecoes/copy.js; D-03: o snapshot é runtime e fica no switch do App.jsx, o registro só declara quem tem `case`; os 2 passos de introdução do tour não são tela e ficam fora; espelho no backend = conceitos.PET_TELAS, travado por test_telas_registro.mjs + test_telas_paridade.py; zero import). `export const TELAS = Object.freeze([...])` com as 8 entradas da tabela em <interfaces> (cada entrada também `Object.freeze`), campos exatamente `id, barra, rotuloCp, rotuloPadrao, tour, ajuda, snapshot, subtelaDe, carteiraView` (null quando não se aplica — nunca omitir campo, para o teste Python casar por regex de forma estável; um objeto por linha, com `id: "<id>"` como primeiro campo). Exportar as 6 funções puras descritas em <interfaces>. Não adicionar seção de ajuda para historico/perfil nem passo de tour para agente/historico/perfil (D-04 — achados, não correções).
    Rodar até verde. Não importar telas.js em App.jsx neste plano.
  </action>
  <acceptance_criteria>
    - `cd web && node tests/test_telas_registro.mjs` sai 0 com "todos os testes passaram"
    - `grep -c "^import" web/src/telas.js` == 0
    - `grep -c 'id: "' web/src/telas.js` == 8
    - `git diff --quiet web/src/App.jsx web/src/copy.js server/app/conceitos.py`
    - Prova negativa registrada no SUMMARY: trocar temporariamente o id "perfil" por "perfil2" em telas.js → o teste reprova nomeando a PARTE A; reverter → verde
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/web && node tests/test_telas_registro.mjs && node tests/test_pet_opcoes.mjs && node tests/test_tour_opcoes.mjs</automated>
  </verify>
  <done>Registro com as 8 telas reproduz exatamente o comportamento congelado; paridade front↔PET_TELAS reprova em divergência.</done>
</task>

<task type="auto">
  <name>Task 3: segundo ponto testado — pytest lê telas.js e compara com PET_TELAS</name>
  <files>server/tests/test_telas_paridade.py</files>
  <read_first>
    - server/tests/test_auditoria_prompts.py (linhas ~250-270: test_a8ii_paridade_defaults_carteira_com_catalog_js — Python lendo .js como texto)
    - server/tests/test_pet_todas_telas.py (linhas 1-70: docstring de guardião e asserção sobre PET_TELAS)
    - web/src/telas.js (criado na Task 2)
  </read_first>
  <action>
    Criar `server/tests/test_telas_paridade.py` com docstring datada (Fase 41, TELAS-01): segundo ponto do par registro-front × PET_TELAS, mesmo desenho de test_a8ii (lê `web/src/telas.js` como texto via caminho relativo a `__file__`, nunca subprocess node). Testes:
    `test_registro_de_telas_do_front_bate_com_pet_telas` — isola o bloco entre `export const TELAS` e o `]);` que o fecha, extrai os ids com `re.findall(r'id:\s*"([a-z]+)"', bloco)`, afirma len == 8, sem duplicata, e `set(ids) == set(conceitos.PET_TELAS)` com mensagem citando a diferença simétrica e a consequência ("tela no front fora da allowlist → 400 'Tela desconhecida.'; tela na allowlist fora do front → resumo sem tela que o chame").
    `test_pet_telas_nao_tem_duplicata` — len(PET_TELAS) == len(set(PET_TELAS)) == 8.
    Não editar conceitos.py nem main.py.
    Prova negativa: acrescentar temporariamente "fantasma" ao final da tupla PET_TELAS num patch local (não commitado) OU remover uma entrada de telas.js → os DOIS testes (mjs e pytest) reprovam; reverter; registrar a saída no SUMMARY.
    Rodar a suíte canônica `bash scripts/executar.sh --testes` (fora do sandbox se aparecer o artefato TLS) e registrar no SUMMARY: pytest passed/skipped/xfailed/failed e .mjs ok/total, comparando com o baseline do 40-02-SUMMARY (3048 passed/5 skipped/3 xfailed; única falha .mjs ambiental test_ios_assets.mjs). Esperado: +2 pytest e +1 .mjs, zero regressão.
  </action>
  <acceptance_criteria>
    - `cd server && .venv/bin/python -m pytest tests/test_telas_paridade.py -q` → 2 passed (usar o python da venv do projeto; se o caminho diferir, o do scripts/executar.sh)
    - `grep -c "subprocess" server/tests/test_telas_paridade.py` == 0
    - `git diff --quiet server/app/` (backend de produto intocado)
    - SUMMARY registra prova negativa nos dois lados e os números da suíte canônica
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/server && .venv/bin/python -m pytest tests/test_telas_paridade.py tests/test_pet_todas_telas.py -q</automated>
  </verify>
  <done>Paridade travada nos dois pontos; suíte canônica sem regressão.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| front (tela ativa) → /api/assistente, /api/pet/resumo | o front manda `tela: "pet:<id>"`; o backend só aceita ids de PET_TELAS |
| registro do front ↔ allowlist do backend | duas listas mantidas em linguagens diferentes, sem import |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Tampering (deriva silenciosa) | web/src/telas.js × conceitos.PET_TELAS | mitigate | paridade 8×8 nos dois pontos (test_telas_registro.mjs + test_telas_paridade.py), com prova negativa registrada |
| T-41-02 | Denial of Service (assistente mudo) | tela no front fora da allowlist → 400 | mitigate | mesma paridade; mensagem da falha nomeia a consequência |
| T-41-03 | Integrity (mudança visível não-intencional) | futuras consumidoras do registro | mitigate | fixture pré-refactor com rótulos/tour/ajuda/petTela/sha256 do switch; equivalência exigida em teste |
| T-41-04 | Elevation of Privilege | allowlist do backend | accept | backend não muda nesta fase; PET_TELAS continua sendo a fronteira server-side (a paridade não relaxa a validação) |
</threat_model>

<verification>
1. `cd web && node tests/test_telas_registro.mjs` verde; prova negativa registrada.
2. `pytest tests/test_telas_paridade.py` verde; prova negativa registrada.
3. `git diff` do plano não toca App.jsx, copy.js, conceitos.py, main.py, docs/AJUDA.md.
4. Suíte canônica fora do sandbox sem regressão contra o baseline do 40-02.
</verification>

<success_criteria>
- Registro único com as 8 telas existe e reproduz o comportamento atual (fixture).
- SC#3 atendido: paridade front↔PET_TELAS em dois pontos testados, reprova em divergência.
- Nenhuma mudança de produto neste plano.
</success_criteria>

<output>
Create `.planning/phases/41-consolida-o-de-registros-de-tela/41-01-SUMMARY.md` when done
</output>
