---
phase: 37-gr-fico-de-payoff-e-explica-o-confi-veis
plan: 04
type: execute
wave: 2
depends_on: ["37-02"]
files_modified:
  - web/src/opcoes/PayoffChart.jsx
  - web/tests/test_payoff_responsivo.mjs
autonomous: true
requirements: [CHART-01, CHART-02, CHART-03]

must_haves:
  truths:
    - "O eixo Y mostra 3 marcas numericas (topo/zero/base) com 2 casas decimais, nunca inteiro arredondado a 0"
    - "A linha do zero tem o rotulo visivel R$ 0"
    - "Todo strike e o spot sao marcados no eixo, reusando o mesmo algoritmo de supressao por colisao que breakevens ja usam"
    - "Um lado ilimitado nunca mostra tick numerico nem plato falso — so a seta + rotulo curto (sem teto/sem piso) junto dela"
    - "Quando dominio.xMin/xMax/yMin/yMax existem (nao-null), o SVG usa esses valores; quando ausentes, cai no calculo local useMemo existente, sem quebrar os 3 consumidores atuais"
  artifacts:
    - path: "web/src/opcoes/PayoffChart.jsx"
      provides: "eixo Y com 2 casas, PAD_E=48, marcas de strike/spot mescladas na colisao de breakeven, bloco Hoje/No vencimento"
  key_links:
    - from: "PayoffChart.jsx"
      to: "props dominio/segmentos/valorHoje (Plano 37-01/37-03)"
      via: "props opcionais, fallback local quando ausentes"
      pattern: "dominio\\.(xMin|xMax|yMin|yMax|spot)"
---

<objective>
Fecha CHART-01 (eixo Y com escala visível, 2 casas decimais, zero rotulado
"R$ 0", legenda "multiplique pelo lote"), CHART-02 (strike E spot marcados
no eixo, reusando o algoritmo de colisão já existente) e CHART-03 (seta de
lado ilimitado com rótulo curto "sem teto"/"sem piso" junto dela, nunca um
platô falso) em `PayoffChart.jsx` — o mesmo componente que os 3 consumidores
reais (`SecaoAnalisar.jsx`, `SecaoComparar.jsx`, `CuradoriaEstruturas.jsx`,
D-08) já usam, então a correção chega aos 3 automaticamente (D-09), sem
tocar nenhum deles neste plano.

Também entrega a metade FRONTEND de CHART-05: o par de `Kicker`s "No
vencimento"/"Hoje · valor de mercado" dentro do `cabecalho` existente,
lendo a prop nova `valorHoje` (Plano 37-03) — mas SÓ a renderização; a
conexão real com dados vivos (passar `valorHoje`/`dominio`/`segmentos` como
props desde `SecaoAnalisar.jsx`/`SecaoComparar.jsx`) é o Plano 37-05.

Purpose: hoje o eixo Y não tem nenhuma marca numérica, e o domínio X/Y é
calculado LOCALMENTE por percentual fixo (`PayoffChart.jsx:100-160`), não
pela margem `max(12% do span, 4% do spot)` que `dominio_da_curva()` (Fase
36) já implementa. Sem essa correção, CHART-04 (Plano 37-01) fica
computado no backend mas nunca desenhado.
Output: `PayoffChart.jsx` estendido, sem quebrar nenhum dos 3 consumidores
existentes quando `dominio`/`segmentos`/`valorHoje` não são passados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-gr-fico-de-payoff-e-explica-o-confi-veis/37-CONTEXT.md
@.planning/phases/37-gr-fico-de-payoff-e-explica-o-confi-veis/37-UI-SPEC.md
@.planning/phases/37-gr-fico-de-payoff-e-explica-o-confi-veis/37-PATTERNS.md
</context>

<interfaces>
Três props NOVAS, opcionais, cada uma independentemente `null`-safe (Plano
37-01/37-03 já produzem este formato exato — camelCase):

`dominio`: `{ xMin, xMax, yMin, yMax, margem, spot, ganhoIlimitado,
perdaIlimitada, motivo }` — qualquer campo pode ser `null`.

`segmentos`: `[{ de, ate, inclinacao, ePlato }]` — este plano NÃO consome
`segmentos` diretamente (é o `ExplicacaoPayoff.jsx` do Plano 37-02 que
consome); ignorar esta prop no componente `PayoffChart`.

`valorHoje`: `{ erro: {...} } | { dados: { porAcao, emReais } } |
undefined` — `undefined`/ausente = bloco "Hoje" não renderiza (não
tentado); presente = renderiza carregando... NÃO EXISTE (Plano 37-03 já
resolve síncrono no backend — a prop SEMPRE chega já resolvida como `erro`
ou `dados`, nunca precisa de estado de loading neste componente).

Chaves de copy já criadas pelo Plano 37-02, todas em `cp`:
`opcoesEixoZeroRotulo`, `opcoesEixoVerticalLoteAjuda`,
`opcoesNoVencimentoTitulo`, `opcoesHojeTitulo`, `opcoesHojeAjuda`,
`opcoesPerdaIlimitadaCurta`, `opcoesHojePrefixoEixo` (nota: `opcoesHojeCarregando`
NÃO existe — o Plano 37-02 não a cria, porque o Plano 37-03 fechou a
arquitetura como síncrona; ver nota acima).
</interfaces>

<read_first>
- `web/src/opcoes/PayoffChart.jsx` (arquivo INTEIRO, 349 linhas — já lido
  nesta sessão de planejamento; ler de novo para confirmar estado atual
  antes de editar, especialmente linhas 31-48 constantes de token/geometria,
  100-160 `desenho = useMemo`, 176-216 `cabecalho`/`caixa`, 267-301
  zero-line+breakevens, 303-328 cenários, 330-337 setas)
- `web/src/opcoes/uiOpcoes.jsx` linhas 37-51 (`Kicker`, `Aviso` — import
  novo necessário em `PayoffChart.jsx`, hoje ausente)
- `.planning/phases/37-gr-fico-de-payoff-e-explica-o-confi-veis/37-UI-SPEC.md`
  §1 inteira (contrato de geometria completo: fórmula do `PAD_E=48`, 3
  ticks, regra de dedup quando extremo é zero, threshold de âncora de texto,
  fórmula de merge strike+breakeven, prioridade spot vs. cenário, arithmetic
  do row-clip que prova "merge, não segunda linha") e §2 (estrutura exata do
  bloco Hoje/No vencimento, incluindo o texto de `opcoesHojeAjuda`)
- `web/tests/test_payoff_responsivo.mjs` (arquivo inteiro — entender o que
  já está travado por regex/geometria antes de estender)
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Geometria — eixo Y (CHART-01), strike+spot no eixo (CHART-02), setas rotuladas (CHART-03), domínio do backend com fallback (CHART-04 rendering)</name>
  <files>web/src/opcoes/PayoffChart.jsx</files>
  <action>
Alterar `PAD_E` de `10` para `48` (linha 41). Ajustar o threshold de âncora
de texto de breakeven (linha 291, `x < 28 ? "start" : ...`) para
`x < PAD_E + 18 ? "start" : ...` (preserva a margem de ~18px perto da
borda, agora ancorada ao `PAD_E` novo).

Em `desenho = useMemo(...)` (linhas 100-160): quando as 4 props
`dominio?.xMin`, `dominio?.xMax`, `dominio?.yMin`, `dominio?.yMax` são TODAS
não-`null`/não-`undefined`, usar esses valores para `x0`, `x1`, `ymin`,
`ymax` em vez do cálculo local atual (o bloco `foco`/`base`/`x0`/`x1` de
janela e o `extentOf([...])` de `ymin`/`ymax`) — MAS preservar o `recortar`/
interpolação da curva dentro dessa janela (a lógica de "curva ainda
funciona, só a JANELA muda de fonte"). Quando qualquer uma das 4 é `null`/
ausente, manter o `useMemo` EXATAMENTE como está hoje (fallback local) —
isto é o que mantém `CuradoriaEstruturas.jsx` funcionando sem edição nesta
fase (D-09). Adicionar `dominio` às dependências do `useMemo`.

Substituir a linha única do zero (linha 267-269) por um grupo de até 3
marcas no eixo Y, `x = PAD_E - 4 = 44`, `textAnchor="end"`,
`dominantBaseline="central"`, tick de 4px de `x=44` a `x=PAD_E=48`,
`fontSize={FONTE_MIN}`, `fill={T.textMuted}`: (a) topo — `sy(dominio?.yMax
?? ymax)`, label = `fmt(dominio?.yMax ?? ymax)` (2 casas, `fmt()` já
existente já faz isso) — OMITIR esta marca quando `dominio?.ganhoIlimitado`
é `true` (a seta já é o sinal honesto); (b) zero — `yZero` (já existe),
label = `cp.opcoesEixoZeroRotulo || "R$ 0"`; (c) base — `sy(dominio?.yMin ??
ymin)`, label = `fmt(dominio?.yMin ?? ymin)` — OMITIR quando
`dominio?.perdaIlimitada` é `true`. Regra de dedup: se a marca de topo ou
base cair a menos de 3px de `yZero`, suprimir essa marca extrema e mostrar
só "R$ 0". A linha do zero tracejada existente continua desenhada
independente das marcas (ela é a LINHA, as marcas são o EIXO).

Strikes no eixo (CHART-02): extrair os strikes únicos de `e.legs` (filtrar
`kind === "CALL" || kind === "PUT"`, `strike` numérico) e MESCLAR num único
array ordenado por `sx()` junto com `marcas` (breakevens, já existente)
ANTES do loop de colisão de 44px (linhas 284-301) — cada item do array
mesclado carrega um discriminador (`{ valor, tipoMarca: "breakeven" |
"strike" }`) para o `<line>` escolher `strokeDasharray="2 4"` +
`stroke={T.textMuted}` (breakeven, como já é) OU `strokeDasharray="1 3"` +
`stroke={T.borderFaint}` (strike, novo) — quando dois valores landam no
MESMO slot de 44px, o breakeven GANHA a exibição do texto (a linha do
strike continua sempre desenhada, só o texto perde), critério: ordenar por
`sx()` e, em empate de slot, breakeven vem primeiro no array de entrada do
loop.

Spot no eixo (CHART-02): quando `dominio?.spot` é número, desenhar uma
linha cheia (não tracejada) `stroke={T.textPrimary}` de `PAD_T` a
`H-PAD_B` em `sx(dominio.spot)`, mais um círculo `<circle r="3"
fill={T.textPrimary}/>` na mesma posição X, Y = `sy(resultado no spot)` se
computável via a mesma interpolação `entre()` já existente na curva, senão
`yZero`. Rótulo: `(cp.opcoesHojePrefixoEixo || "hoje") + " " + fmt(dominio.spot)`
— sem prefixo de moeda. Esta marca entra no MESMO array de colisão que
`cenarios` já usa (linhas 307-328, círculos 52×14), e GANHA prioridade
sobre um `cenario` colidente (spot sempre desenha o texto; se um cenário
colide com o spot, o cenário perde o texto, não o contrário) — inverter a
ordem de inserção no array `desenhados` para que o spot seja processado
PRIMEIRO no loop.

Setas rotuladas (CHART-03): ao lado de cada seta existente (linhas
332-337), adicionar um `<text>` imediatamente à esquerda, mesma linha,
`textAnchor="end"`, `x = (x da seta) - 16`, `fontSize={FONTE_MIN}`, cor
igual à seta (`corLucro`/`corPerda`): lado de ganho usa
`cp.opcoesGanhoIlimitado || "sem teto"` (chave já existente, sem mudança);
lado de perda usa a chave NOVA `cp.opcoesPerdaIlimitadaCurta || "sem piso"`
(NÃO a `opcoesPerdaIlimitada` longa, que continua só na legenda abaixo do
SVG).

Legenda "multiplique pelo lote" (CHART-01): na `<div>` de legenda abaixo do
SVG (linhas 340-345), após a frase `"Eixo vertical: resultado " +
opcoesPorAcaoRotulo + "."`, adicionar uma frase nova:
`" " + (c.opcoesEixoVerticalLoteAjuda || "Multiplique pelo lote para o
valor total.")`.
  </action>
  <acceptance_criteria>
    <criterion>`grep -n "const PAD_E = 48" web/src/opcoes/PayoffChart.jsx` existe</criterion>
    <criterion>`grep -n "opcoesEixoZeroRotulo\|opcoesPerdaIlimitadaCurta\|opcoesHojePrefixoEixo\|opcoesEixoVerticalLoteAjuda" web/src/opcoes/PayoffChart.jsx` mostra as 4 chaves em uso</criterion>
    <criterion>`grep -n "dominio" web/src/opcoes/PayoffChart.jsx` mostra a prop desestruturada da assinatura da função E usada dentro do `useMemo`</criterion>
    <criterion>`npx vite build` (dentro de `web/`) sai com exit 0</criterion>
    <criterion>Leitura direta confirma: quando `dominio` não é passado (undefined), o componente renderiza IDENTICO ao comportamento anterior a este plano para os 3 consumidores existentes — nenhuma prop nova é obrigatória</criterion>
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Bloco "No vencimento"/"Hoje · valor de mercado" (CHART-05 frontend) + testes de geometria</name>
  <files>web/src/opcoes/PayoffChart.jsx, web/tests/test_payoff_responsivo.mjs</files>
  <action>
Em `PayoffChart.jsx`, adicionar `import { Kicker, Aviso, ErroDoMcp } from
"./uiOpcoes.jsx";` (novo — hoje o arquivo não importa `uiOpcoes.jsx`,
comentário do próprio arquivo em `estruturaParaPayoff.js` já nomeia essa
lacuna). Envolver o `cabecalho` existente (linhas 176-203) com um
`<Kicker>{c.opcoesNoVencimentoTitulo || "No vencimento"}{vencimento ? " · "
+ vencimento : ""}</Kicker>` ANTES do conteúdo atual (nome/custo/ganho/
perda/breakeven inalterados). Depois do `cabecalho` (ainda dentro de
`caixa`, antes do `filho`/SVG — ou depois do SVG, decisão de leiaute
discricionária dentro do que o UI-SPEC §2 mostra: o diagrama lista o bloco
"Hoje" DEPOIS do bloco "No vencimento" mas ANTES da curva SVG é uma leitura
válida do diagrama — posicionar o bloco novo imediatamente após o
`cabecalho`, antes do SVG, mantendo `caixa(filho)` como está), adicionar:
SE `valorHoje` é `undefined`/`null`, NADA renderiza (ausente, não
degradado). SE `valorHoje.erro` existe, renderizar
`<Kicker>{c.opcoesHojeTitulo || "Hoje · valor de mercado"}</Kicker>` seguido
de `<ErroDoMcp erro={valorHoje.erro} cp={cp} />`. SE `valorHoje.dados`
existe, renderizar o mesmo Kicker seguido de duas linhas no MESMO estilo de
`cabecalho` (`fontSize: "11.5px"`, `color: T.textSecondary`,
`fontVariantNumeric: "tabular-nums"`): `(c.opcoesPorAcaoRotulo || "por
ação") + ": " + moeda(valorHoje.dados.porAcao)` e, se
`valorHoje.dados.emReais` é número, `(c.opcoesEmReaisRotulo || "em reais") +
": " + moeda(valorHoje.dados.emReais)`; seguido de uma linha de ajuda
(mesmo estilo de `AJUDA`/linha de breakeven-ajuda já existente, linha
199-201): `c.opcoesHojeAjuda || ""`.

Estender `web/tests/test_payoff_responsivo.mjs` com asserções de geometria
estática (mesmo padrão do arquivo — regex/leitura de fonte, sem DOM):
`PAD_E === 48`; a string `"opcoesEixoZeroRotulo"` aparece no fonte;
`strokeDasharray="1 3"` aparece (marca de strike nova, distinta de `"2 4"`
de breakeven); `opcoesPerdaIlimitadaCurta` aparece perto de uma das duas
setas (`aria-hidden`); `Kicker` é importado de `./uiOpcoes.jsx`.
  </action>
  <acceptance_criteria>
    <criterion>`node web/tests/test_payoff_responsivo.mjs` sai com exit 0</criterion>
    <criterion>`grep -n "import.*Kicker.*uiOpcoes" web/src/opcoes/PayoffChart.jsx` existe</criterion>
    <criterion>`grep -n "opcoesNoVencimentoTitulo\|opcoesHojeTitulo\|opcoesHojeAjuda" web/src/opcoes/PayoffChart.jsx` mostra as 3 chaves em uso</criterion>
    <criterion>`npx vite build` (dentro de `web/`) sai com exit 0</criterion>
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nenhuma nova nesta plano | `PayoffChart.jsx` continua SVG puro, sem I/O — consome props já validadas a montante |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-08 | Tampering (visual, não segurança) | Domínio Y com `yMax`/`yMin` ambos muito próximos de zero (estrutura de risco/ganho quase nulo) desenhando um gráfico "achatado" que parece platô falso | accept | `dominio_da_curva()` (Fase 36) já aplica `*1.15` de folga sobre o extremo real — este plano não introduz cálculo novo, só desenha o que o backend já validou; risco visual residual (curva muito achatada é honesta, não um bug) |
| T-37-09 | Repudiation | Spot desenhado incorretamente por erro de sinal/posição, levando o usuário a uma leitura errada da estrutura | mitigate | `dominio.spot` chega já validado/arredondado por `dominio_da_curva()` (Plano 37-01); este componente só posiciona (`sx(dominio.spot)`), nunca recalcula o valor — testes de geometria (Task 2) cobrem a presença dos elementos, revisão visual humana cobre a leitura correta (checkpoint fora deste plano, ver Plano 37-05) |
</threat_model>

<verification>
- `node web/tests/test_payoff_responsivo.mjs` — exit 0
- `bash scripts/executar.sh --testes` — exit 0, sem regressão em `test_opcoes_analisar_ui.mjs`/`test_estrutura_para_payoff.mjs` (este plano não toca `estruturaParaPayoff.js`/`SecaoAnalisar.jsx`)
- `npx vite build` (dentro de `web/`) — exit 0
</verification>

<success_criteria>
- `PayoffChart.jsx` renderiza eixo Y com 2 casas decimais, strike+spot marcados, setas rotuladas, sem quebrar os 3 consumidores existentes
- Bloco "Hoje"/"No vencimento" renderiza quando `valorHoje` é passado, ausente quando não
</success_criteria>

<output>
Create `.planning/phases/37-gr-fico-de-payoff-e-explica-o-confi-veis/37-04-SUMMARY.md` when done
</output>
