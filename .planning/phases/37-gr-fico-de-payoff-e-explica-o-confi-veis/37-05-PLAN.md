---
phase: 37-gr-fico-de-payoff-e-explica-o-confi-veis
plan: 05
type: execute
wave: 3
depends_on: ["37-01", "37-02", "37-03", "37-04"]
files_modified:
  - web/src/opcoes/SecaoAnalisar.jsx
  - web/src/opcoes/SecaoComparar.jsx
  - server/app/main.py
  - web/src/version.js
  - server/web_dist
autonomous: false

requirements: [CHART-01, CHART-02, CHART-03, CHART-04, CHART-05, EXPL-01, EXPL-02, EXPL-03]

must_haves:
  truths:
    - "Em Analisar (SecaoAnalisar.jsx), o gráfico mostra eixo Y com escala, strike/spot marcados, e abaixo dele aparece a explicação por segmento e (quando disponível) o bloco Hoje/valor de mercado"
    - "Em Comparar (SecaoComparar.jsx), cada candidato mostra o gráfico corrigido e a explicação; só o 1o candidato da lista mostra o bloco Hoje (os demais nao tentam a busca)"
    - "CuradoriaEstruturas.jsx continua funcionando sem nenhuma edição nesta fase — recebe o gráfico corrigido de graça (fallback local), sem os blocos novos (D-09)"
    - "A suíte canônica completa (pytest + .mjs) e o build do front passam sem regressão"
    - "A fase está NO AR com carimbo próprio (BUILD_ID/SERVER_BUILD_ID novos), confirmada por HTTP em produção, e a publicação só rodou DEPOIS do 'aprovado' explícito do Alex no checkpoint da Task 2 — nunca antes"
  artifacts:
    - path: "web/src/opcoes/SecaoAnalisar.jsx"
      provides: "PayoffChart com dominio/segmentos/valorHoje + ExplicacaoPayoff"
    - path: "web/src/opcoes/SecaoComparar.jsx"
      provides: "idem, por item da lista, valorHoje só no índice 0"
    - path: "web/src/version.js"
      provides: "Carimbo de entrega desta fase"
      contains: "BUILD_ID"
    - path: "server/app/main.py"
      provides: "SERVER_BUILD_ID com o histórico da entrega preservado"
      contains: "SERVER_BUILD_ID"
  key_links:
    - from: "SecaoAnalisar.jsx"
      to: "ExplicacaoPayoff.jsx (Plano 37-02)"
      via: "import + render após RazaoGanhoPerda"
      pattern: "<ExplicacaoPayoff"
    - from: "SecaoComparar.jsx"
      to: "possibilidades.dados.possibilidades[].valorHoje (Plano 37-03)"
      via: "item.valorHoje passthrough, sem lógica de índice no front (backend já gateia)"
      pattern: "valorHoje=\\{item\\.valorHoje\\}"
    - from: "checkpoint humano aprovado (Task 2)"
      to: "scripts/bump.sh + scripts/publicar-web.sh (Task 3)"
      via: "ordem obrigatória — bump ANTES de publicar, nunca editar server/web_dist à mão, e nada roda antes do 'aprovado'"
      pattern: "SERVER_BUILD_ID"
---

<objective>
Onda de fechamento: conecta o gráfico corrigido (Plano 37-04) e a
explicação (Plano 37-02) aos dois consumidores reais que a Fase 35 acabou de
polir — `SecaoAnalisar.jsx` (Analisar) e `SecaoComparar.jsx` (Comparar),
D-09 — passando as props novas (`dominio`, `segmentos`, `valorHoje`, já
produzidas pelo backend nos Planos 37-01/37-03) e renderizando
`<ExplicacaoPayoff/>`. É este plano que torna CHART-01..05/EXPL-01..03
OBSERVÁVEIS de ponta a ponta — os planos anteriores construíram o mecanismo
(backend + componentes), este os liga.

`CuradoriaEstruturas.jsx` NÃO é tocado (D-09: recebe o gráfico corrigido de
graça via fallback local em `PayoffChart.jsx`, sem os blocos novos).

Purpose: fechar as duas regressões confirmadas em produção por screenshot
(CHART-05: "hoje"/"no vencimento" confundidos; EXPL-03: razão "1:1,00" vs.
"1:0,67") no lugar onde um usuário real as vê.
Output: `SecaoAnalisar.jsx`/`SecaoComparar.jsx` wired; checkpoint humano ao
vivo confirmando a leitura visual (Task 2); e, só depois do "aprovado"
explícito nessa mesma conversa, publicação em produção com carimbo próprio
(Task 3) — mesmo padrão travado em `35-03-PLAN.md` para fase que toca
`web/src/`: checkpoint bloqueante segura a publicação da FASE INTEIRA, não
fica em aberto como decisão futura fora do plano.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-gr-fico-de-payoff-e-explica-o-confi-veis/37-CONTEXT.md
@.planning/phases/37-gr-fico-de-payoff-e-explica-o-confi-veis/37-UI-SPEC.md
@.planning/phases/35-jornada-guiada-do-workspace/35-03-PLAN.md
</context>

<interfaces>
Envelope de `proposta()` (Plano 37-01/37-03): `proposta.dados.dominio`,
`proposta.dados.segmentos`, `proposta.dados.valorHoje` — chaves irmãs de
`proposta.dados.emReais`/`proposta.dados.razaoGanhoPerda` já consumidas
hoje em `SecaoAnalisar.jsx:391-397`.

Envelope de `possibilidades()`: cada `item` de
`possibilidades.dados.possibilidades` ganha `item.dominio`/`item.segmentos`
sempre que `item.estrutura` existe, e `item.valorHoje` SÓ no item de
índice 0 (chave ausente nos demais — backend já decide isso, Plano 37-03;
o front só repassa `item.valorHoje` como está, sem lógica de índice
própria).

`ExplicacaoPayoff` (Plano 37-02): `<ExplicacaoPayoff segmentos={...}
spot={dominio && dominio.spot} razao={...} cp={cp} />`.
</interfaces>

<read_first>
- `web/src/opcoes/SecaoAnalisar.jsx` linhas 355-400 (bloco atual de
  `PayoffChart`/`RazaoGanhoPerda`/`Pernas` — ler o estado ATUAL do arquivo
  após os Planos 37-01..37-04, não o texto deste documento)
- `web/src/opcoes/SecaoComparar.jsx` linhas 180-230 (bloco atual de
  `.map((item) => ...)`, `PayoffChart`/`RazaoGanhoPerda`/`Cenarios`)
- `web/src/opcoes/ExplicacaoPayoff.jsx` (produzido pelo Plano 37-02 — ler o
  arquivo real, confirmar a assinatura exata de props antes de conectar)
- `web/src/opcoes/PayoffChart.jsx` (produzido/estendido pelo Plano 37-04 —
  confirmar os nomes exatos das props novas aceitas)
- `web/tests/test_opcoes_analisar_ui.mjs` (arquivo inteiro — guardião
  existente que varre `web/src/opcoes/*.{js,jsx}` dinamicamente via
  `readdirSync`; confirmar que nenhuma regra dele quebra com os novos
  imports/props antes de considerar a task completa)
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Wire dominio/segmentos/valorHoje + ExplicacaoPayoff em SecaoAnalisar.jsx e SecaoComparar.jsx</name>
  <files>web/src/opcoes/SecaoAnalisar.jsx, web/src/opcoes/SecaoComparar.jsx</files>
  <action>
Em `SecaoAnalisar.jsx`, adicionar `import ExplicacaoPayoff from
"./ExplicacaoPayoff.jsx";` ao topo (junto dos outros imports de
`./uiOpcoes.jsx`/`./PayoffChart.jsx`). No bloco `proposta.dados ? (<>...`
(linhas ~389-397), adicionar as props novas ao `<PayoffChart>` existente:
`dominio={proposta.dados.dominio}`, `segmentos={proposta.dados.segmentos}`,
`valorHoje={proposta.dados.valorHoje}` (simples passthrough, sem
transformação — os nomes já batem 1:1 com o que o backend devolve, Plano
37-01/37-03). Logo após `<RazaoGanhoPerda razao={proposta.dados.razaoGanhoPerda}
cp={cp} />` (linha 396), adicionar `<ExplicacaoPayoff
segmentos={proposta.dados.segmentos} spot={proposta.dados.dominio &&
proposta.dados.dominio.spot} razao={proposta.dados.razaoGanhoPerda} cp={cp}
/>` — MESMO nível de indentação, mesma sequência de irmãos que
`<Pernas .../>` já usa.

Em `SecaoComparar.jsx`, adicionar o mesmo import. No `.map((item) => (...))`
(linha ~210), trocar a assinatura do callback para `.map((item) => (` →
manter (não precisa de índice — o backend já decide quem recebe
`valorHoje`, D-02 já resolvido no Plano 37-03; o front NÃO reintroduz gate
por índice). No bloco de sucesso (linhas ~219-225, dentro do `) : ( <>
...`), adicionar `dominio={item.dominio}`, `segmentos={item.segmentos}`,
`valorHoje={item.valorHoje}` ao `<PayoffChart>` existente, e adicionar
`<ExplicacaoPayoff segmentos={item.segmentos} spot={item.dominio &&
item.dominio.spot} razao={item.razaoGanhoPerda} cp={cp} />` logo após
`<RazaoGanhoPerda razao={item.razaoGanhoPerda} cp={cp} />` (linha 222),
antes de `<Cenarios .../>` (mesmo `gap: "10px"` do grid pai já cuida do
espaçamento entre os três).

`CuradoriaEstruturas.jsx` NÃO é tocado (D-09) — confirmar por
`git diff --stat` ao final da task que este arquivo não aparece no diff.
  </action>
  <acceptance_criteria>
    <criterion>`grep -n "import ExplicacaoPayoff" web/src/opcoes/SecaoAnalisar.jsx web/src/opcoes/SecaoComparar.jsx` mostra o import nos dois arquivos</criterion>
    <criterion>`grep -n "dominio={" web/src/opcoes/SecaoAnalisar.jsx web/src/opcoes/SecaoComparar.jsx` mostra a prop passada ao `PayoffChart` nos dois</criterion>
    <criterion>`grep -n "<ExplicacaoPayoff" web/src/opcoes/SecaoAnalisar.jsx web/src/opcoes/SecaoComparar.jsx` mostra o componente renderizado nos dois</criterion>
    <criterion>`git diff --stat` (contra o HEAD anterior a este plano) NÃO lista `CuradoriaEstruturas.jsx`</criterion>
    <criterion>`npx vite build` (dentro de `web/`) sai com exit 0</criterion>
    <criterion>`bash scripts/executar.sh --testes` (suíte canônica completa, pytest + `.mjs`) sai com exit 0, sem falha nova em relação à baseline registrada em STATE.md antes desta fase</criterion>
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: verificação ao vivo — gráfico corrigido, explicação por segmento e bloco Hoje/valor de mercado</name>
  <what-built>
Gráfico de payoff corrigido (eixo Y com escala real, strike/spot marcados,
setas de risco ilimitado rotuladas, domínio sem cortar platô) e explicação
por segmento, visíveis em Analisar e em Comparar. Bloco "Hoje · valor de
mercado" separado de "No vencimento" (CHART-05). Rótulo "Pontuação de
curadoria" no lugar da antiga "razão" ambígua (D-04).
  </what-built>
  <how-to-verify>
1. Suba o backend e o Vite localmente (per achado de ambiente já registrado
   em STATE.md: `bash scripts/executar.sh` sozinho trava neste host — suba
   backend e Vite diretos, não o launcher).
2. Abra a aba Opções, workspace, pill Analisar. Escolha um ticker com
   opções (ex. PETR4), monte uma tese (ex. alta, CALL), clique "Ver
   possibilidades"/monte a estrutura.
3. Confira no gráfico: (a) eixo Y à esquerda mostra 3 números com vírgula e
   2 casas (nunca "0" sozinho no topo/base quando o resultado é sub-R$1);
   (b) a linha tracejada do zero tem o rótulo "R$ 0"; (c) cada strike tem
   uma linha pontilhada fina, o spot tem uma linha sólida rotulada "hoje
   {preço}"; (d) se a estrutura tiver lado ilimitado, a seta tem "sem
   teto"/"sem piso" escrito ao lado dela, sem platô desenhado além da seta.
4. Abaixo do gráfico, confira o bloco "Como ler esta estrutura" — o texto
   descreve a faixa onde o spot está PRIMEIRO, depois as demais faixas, sem
   usar as palavras strike/prêmio/delta/theta/exercício/ITM/OTM/ATM/perna.
5. Se a busca de valor de mercado atual funcionar (depende do serviço
   MCP estar no ar), confira o bloco "Hoje · valor de mercado" — separado
   visualmente de "No vencimento", com o aviso "pode mudar a qualquer
   momento, diferente do resultado no vencimento acima".
6. Vá para a pill Comparar, rode "Ver possibilidades" com 2+ vencimentos —
   confirme que SÓ o primeiro candidato da lista mostra o bloco "Hoje" (os
   demais mostram só o gráfico + explicação, sem esse bloco).
7. No Modo Operador, confira a curadoria (se acessível) — o rótulo antes
   chamado "Razão"/"prêmio sobre perda máxima" agora diz "Pontuação de
   curadoria", e o gráfico ali também está com eixo Y/strike/spot
   corrigidos (mesmo componente, sem os blocos novos).
  </how-to-verify>
  <action>
Antes de chamar o Alex, colher a evidência automática e deixá-la colada na
mensagem do checkpoint: `bash scripts/executar.sh --testes` (suíte canônica
inteira) e `cd web && npx vite build`, ambos já rodados e verdes pela Task 1
— reconfirmar aqui, não assumir que o estado ainda é o mesmo se houve
qualquer edição entre as duas tasks. Subir backend + Vite diretos (achado de
ambiente já registrado em STATE.md: o launcher `bash scripts/executar.sh`
sem argumentos trava neste host) e confirmar que a porta do Vite responde
ANTES de apresentar o roteiro. Apresentar o roteiro de `<how-to-verify>` na
íntegra, numerado. PARAR aqui — não editar nada, não fazer bump nem
publicar nesta task; a Task 3 cuida da publicação, e só roda depois do
"aprovado" explícito registrado nesta conversa (checkpoint bloqueante segura
a publicação da FASE INTEIRA, não só desta task). Se o Alex reportar
divergência, registrar literalmente o que ele disse e devolver ao
orquestrador sem corrigir por conta própria.
  </action>
  <acceptance_criteria>
    - O passo 3 (eixo Y, zero rotulado, strike/spot, seta rotulada) foi confirmado com veredito explícito, não um "parece ok" genérico
    - O passo 4 (explicação por segmento, spot primeiro, sem vocabulário banido) foi lido e confirmado
    - O passo 6 (só o 1o candidato de Comparar mostra o bloco Hoje) foi conferido literalmente contando quantos candidatos mostram o bloco
    - `bash scripts/executar.sh --testes` e `cd web && npx vite build` verdes, com saídas coladas na mensagem do checkpoint ANTES de pedir aprovação
  </acceptance_criteria>
  <resume-signal>Digite "aprovado" ou descreva o que precisa de ajuste — sem esta resposta explícita, a Task 3 (publicação) NÃO roda e a fase não é considerada pronta para o gsd-verifier</resume-signal>
  <verify>
    <human-check>Os 7 passos do roteiro acima, reportados pelo Alex um a um</human-check>
    <automated>bash scripts/executar.sh --testes && cd web && npx vite build</automated>
  </verify>
  <done>O Alex viu o gráfico corrigido e a explicação rodando em Analisar e Comparar, confirmou o gate do 1o candidato em Comparar, e a aprovação (ou a lista de correções) está registrada.</done>
</task>

<task type="auto">
  <name>Task 3: bump, publicação e confirmação em produção</name>
  <files>web/src/version.js, server/app/main.py, server/web_dist</files>
  <read_first>
    - CLAUDE.md — seção "Guardrails do repositório": `scripts/bump.sh` ANTES de
      `publicar-web.sh`, nunca editar `server/web_dist` direto, e os três
      esquemas de versão (esta task mexe só no `BUILD_ID`/`SERVER_BUILD_ID`, o
      carimbo de ENTREGA — não em `package.json` nem `MARKETING_VERSION`)
    - `.planning/phases/35-jornada-guiada-do-workspace/35-03-PLAN.md` (Task 2)
      — o precedente exato deste passo neste repositório: mesma sequência
      merge → bump → publicar → suíte → push → confirmação HTTP
    - `server/app/main.py` — linha ~1662, o comentário do `SERVER_BUILD_ID`
      atual (`F10-20260921-01`, publicação da Fase 35 — a entrega anterior
      tem de virar "HISTÓRICO", não ser apagada; Fase 36 NÃO publicou o
      front, então a entrega imediatamente anterior a esta segue sendo a da
      Fase 35)
    - scripts/bump.sh e scripts/publicar-web.sh — ler antes de rodar
  </read_first>
  <action>
    **Só rodar depois do "aprovado" da Task 2.** Se a aprovação não existir
    nesta conversa, parar e reportar — não publicar antes da aprovação em
    nenhuma hipótese (checkpoint bloqueante segura a publicação da FASE
    INTEIRA, não só da própria Task 2).

    1. Mesclar `origin/main` ANTES de qualquer carimbo — `bump.sh` deriva o
       número do valor LOCAL, e sessão paralela já causou colisão neste repo
       (memória `bump-colide-em-sessao-paralela.md`). `server/web_dist` se
       REGENERA, não se mescla: em conflito ali, resolver rodando
       `publicar-web.sh` depois do merge.
    2. `bash scripts/bump.sh` → novo `BUILD_ID` em `web/src/version.js`.
    3. `bash scripts/publicar-web.sh` → regenera `server/web_dist` e atualiza
       `SERVER_BUILD_ID` em `server/app/main.py`. Reescrever à mão o
       comentário do `SERVER_BUILD_ID` com o resumo desta fase (gráfico de
       payoff corrigido: eixo Y com escala real e zero rotulado, strikes e
       spot marcados, seta de risco ilimitado rotulada "sem teto"/"sem piso";
       explicação por segmento 100% determinística reusando literalmente a
       mesma variável de `RazaoGanhoPerda`, nunca recalculada — D-05/D-06;
       bloco "Hoje · valor de mercado" só no 1o candidato de Comparar — D-02;
       rótulo "Razão" da curadoria renomeado para "Pontuação de curadoria" —
       D-04) e rebaixar a entrega anterior (`F10-20260921-01`, Fase 35) a
       "HISTORICO", preservando o texto dela (guardrail: histórico não se
       reescreve).
    4. Repetir a suíte canônica DEPOIS do bump (`bash scripts/executar.sh
       --testes`) — o carimbo entra em arquivo que a suíte lê; confirmar que
       nada caiu.
    5. Commit, push em `v2/interacao-estrutural` E em `origin/main` (conferir
       que é fast-forward, não merge cego).
    6. Confirmar por HTTP: `curl -s https://boris.semente.dev/api/health` até
       responder o `SERVER_BUILD_ID` novo (o Railway pode levar alguns minutos
       servindo o anterior — registrar os dois carimbos observados e o tempo).

    Proibições, com razão:
    - NÃO editar `.planning/STATE.md` nem `.planning/ROADMAP.md`: guardrail
      deste repositório (2026-09-11) — os mutadores do `gsd-sdk` corrompem
      esses arquivos e o orquestrador os edita à mão depois da onda.
    - NÃO tocar `package.json` nem `MARKETING_VERSION`: cadência pública é
      decisão separada do Alex.
    - NÃO editar `server/web_dist` à mão em nenhuma hipótese.
  </action>
  <acceptance_criteria>
    - `grep -c "BUILD_ID = \"F10-" web/src/version.js` retorna 1, com data de hoje e sufixo novo
    - `grep -o 'SERVER_BUILD_ID = "[^"]*"' server/app/main.py` bate exatamente com o `BUILD_ID` de `web/src/version.js`
    - O comentário do `SERVER_BUILD_ID` contém o resumo desta fase E a entrega anterior (`F10-20260921-01`, Fase 35) marcada como "HISTORICO" (texto anterior preservado)
    - `bash scripts/executar.sh --testes` verde DEPOIS do bump
    - `git log --oneline -1` mostra o commit de publicação; `git status` limpo; `HEAD` igual a `origin/main` (fast-forward confirmado)
    - `curl -s https://boris.semente.dev/api/health` responde o carimbo novo (registrar a saída e, se houver atraso de deploy, os dois carimbos observados)
    - `git diff .planning/STATE.md .planning/ROADMAP.md` vazio (o orquestrador fecha os docs à mão)
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/executar.sh --testes && curl -s https://boris.semente.dev/api/health</automated>
  </verify>
  <done>O gráfico de payoff corrigido e a explicação estão em produção com carimbo próprio, confirmados por HTTP, com o histórico do `SERVER_BUILD_ID` preservado, os documentos de planejamento intocados pelo executor, e a publicação só ocorreu após o "aprovado" da Task 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wiring puro | Passthrough de props já validadas pelos Planos 37-01/37-02/37-03/37-04 |
| repositório → produção (Railway) | `publicar-web.sh` + push em `origin/main` publicam o bundle servido a todos os usuários (Task 3). Única fronteira real de publicação nesta fase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-10 | Tampering | `SecaoComparar.jsx` reintroduzindo um gate de índice próprio (`i === 0`) que diverge do gate real do backend (Plano 37-03), mostrando/escondendo o bloco Hoje no candidato errado | mitigate | Este plano NÃO implementa lógica de índice no front — só repassa `item.valorHoje` como está; a fonte única do gate é o backend (chave ausente vs. presente), eliminando a possibilidade de dois gates divergentes |
| T-37-11 | Tampering (publicar sem autorização) | `publicar-web.sh` / push em `origin/main` (Task 3) | mitigate | Checkpoint bloqueante: a Task 3 só roda com o "aprovado" explícito do Alex na Task 2, nesta mesma conversa (guardrail do repositório, fase por fase; mesmo padrão de 35-03-PLAN.md) |
| T-37-12 | Tampering (carimbo colidido) | `bump.sh` | mitigate | Merge com `origin/main` ANTES do bump — `bump.sh` deriva do valor local e sessão paralela já colidiu (memória `bump-colide-em-sessao-paralela.md`) |
| T-37-13 | Repudiation (histórico de entrega apagado) | comentário do `SERVER_BUILD_ID` | mitigate | Entrega anterior (`F10-20260921-01`, Fase 35) rebaixada a "HISTORICO" com o texto preservado; histórico não se reescreve (guardrail) |
| T-37-SC | Tampering (supply chain) | npm/pip install | accept | Zero pacote novo — nenhum install nesta fase |
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` — exit 0 (antes E depois do bump)
- `npx vite build` (dentro de `web/`) — exit 0
- Checkpoint humano ao vivo aprovado (roteiro da Task 2)
- `curl -s https://boris.semente.dev/api/health` servindo o carimbo novo
- `git diff .planning/STATE.md .planning/ROADMAP.md` vazio
</verification>

<success_criteria>
- CHART-01..05 e EXPL-01..03 observáveis de ponta a ponta em Analisar e Comparar
- `CuradoriaEstruturas.jsx` intocado, ainda funcional (fallback local)
- Checkpoint humano aprovado
- A fase está no ar com carimbo próprio, confirmado por HTTP, e a publicação só ocorreu depois da aprovação explícita — nunca antes
- Nenhum documento de planejamento foi editado pelo executor
</success_criteria>

<output>
Create `.planning/phases/37-gr-fico-de-payoff-e-explica-o-confi-veis/37-05-SUMMARY.md` when done
</output>
