---
phase: 37-gr-fico-de-payoff-e-explica-o-confi-veis
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/copy.js
  - web/src/opcoes/uiOpcoes.jsx
  - web/src/opcoes/ExplicacaoPayoff.jsx
  - web/tests/test_explicacao_payoff.mjs
autonomous: true
requirements: [EXPL-01, EXPL-02, EXPL-03]

must_haves:
  truths:
    - "ExplicacaoPayoff(segmentos, spot, razao, cp) descreve TODOS os segmentos de segmentos_da_curva(), o segmento onde o spot está primeiro, os demais na ordem espacial esquerda->direita restante"
    - "O texto nunca usa strike/prêmio/delta/theta/volatilidade implícita/exercício/rolagem/ITM/OTM/ATM/perna (vocabulário banido, EXPL-02)"
    - "A razão G/P, quando citada no texto, usa a MESMA função de formatação que RazaoGanhoPerda já usa (formatarRazao), nunca uma segunda formatação independente"
    - "cp.curadoriaRazaoRotulo deixa de ler como resultado financeiro e passa a se identificar como pontuação de ranking (D-04)"
  artifacts:
    - path: "web/src/opcoes/ExplicacaoPayoff.jsx"
      provides: "Componente puro, novo, que renderiza a explicação por segmento"
      min_lines: 40
    - path: "web/src/opcoes/uiOpcoes.jsx"
      provides: "formatarRazao() extraído e exportado"
    - path: "web/src/copy.js"
      provides: "chaves novas de copy (CHART-01/02/03/05 + explicação EXPL-01..03), pareadas Estudo/Operador"
    - path: "web/tests/test_explicacao_payoff.mjs"
      provides: "guardião do vocabulário banido + prova de reuso de formatarRazao"
  key_links:
    - from: "ExplicacaoPayoff.jsx"
      to: "uiOpcoes.jsx formatarRazao()"
      via: "import { formatarRazao } from './uiOpcoes.jsx'"
      pattern: "import\\s*\\{[^}]*formatarRazao[^}]*\\}\\s*from\\s*[\"']\\./uiOpcoes\\.jsx[\"']"
---

<objective>
Entrega a camada explicativa determinística (EXPL-01/02/03) como componente
puro, ainda NÃO conectado às telas reais (a conexão é o Plano 37-05, wave 3
— este plano cria o componente e o vocabulário, testável isoladamente).
Também fecha D-04 (renomear `curadoriaRazaoRotulo` para deixar claro que é
pontuação de ranking, não resultado financeiro) e prepara TODAS as chaves de
copy que os Planos 37-04 (geometria do gráfico) e 37-05 (wiring) vão
consumir — consolidado aqui, num plano só, para que nenhum outro plano deste
fase precise tocar `web/src/copy.js` em paralelo (evita conflito de arquivo
entre ondas).

Purpose: EXPL-03 é a regressão confirmada em produção (razão exibida
"1:1,00" vs. texto dizendo "1:0,67") — dois formatadores independentes do
mesmo número divergindo. A extração de `formatarRazao()` em `uiOpcoes.jsx`
torna essa classe de bug estruturalmente impossível: só existe UM lugar que
formata a razão, e tanto `RazaoGanhoPerda` quanto `ExplicacaoPayoff` chamam
essa mesma função.
Output: `ExplicacaoPayoff.jsx` (componente puro), `formatarRazao()`
exportado de `uiOpcoes.jsx`, chaves de copy novas em `web/src/copy.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-gr-fico-de-payoff-e-explica-o-confi-veis/37-CONTEXT.md
@.planning/phases/37-gr-fico-de-payoff-e-explica-o-confi-veis/37-UI-SPEC.md
@.planning/phases/37-gr-fico-de-payoff-e-explica-o-confi-veis/37-PATTERNS.md
</context>

<interfaces>
`segmentos` prop (formato exato produzido pelo Plano 37-01, já camelCase):
`[{ de: number, ate: number|null, inclinacao: "positiva"|"negativa"|"zero", ePlato: boolean }]`,
ordenado esquerda→direita, último item sempre com `ate: null` (cauda).

`razao` prop: `{ valor: number|null, motivo: string|null }` — MESMO shape
que `RazaoGanhoPerda` já consome (`web/src/opcoes/uiOpcoes.jsx:75-93`), vindo
de `_razao_ganho_perda()` (`server/app/options_mcp_api.py:1325`).

`spot` prop: `number|null` — `dominio.spot`, produzido pelo Plano 37-01.
</interfaces>

<read_first>
- `web/src/opcoes/uiOpcoes.jsx` (arquivo inteiro, 152 linhas — já lido nesta
  sessão de planejamento; ler de novo para confirmar estado atual antes de
  editar) especialmente linhas 75-93 (`RazaoGanhoPerda`, a formatação
  `"1 : " + fmt(razao.valor)` a extrair) e linhas 37-51 (`Kicker`, `Aviso`,
  a reusar verbatim por `ExplicacaoPayoff`)
- `web/src/finance.js` ou `web/src/plan.js` — ler UM dos dois como exemplo
  de "função pura, zero I/O, single-purpose" (disciplina a seguir em
  `ExplicacaoPayoff.jsx`)
- `web/src/copy.js` linhas 170-330 (bloco Estudo — `opcoesCarregando`,
  `opcoesSemEstrutura`, `opcoesBreakevenRotulo/Ajuda`, `opcoesRazaoRotulo/Ajuda`,
  `opcoesPorAcaoRotulo`, `opcoesEmReaisRotulo`, `opcoesGanhoIlimitado`)
- `web/src/copy.js` linhas 880-990 (bloco Operador — mesmas chaves, redação
  mais terse)
- `web/src/copy.js` linha 712 (`curadoriaRazaoRotulo`, bloco Estudo) e linha
  1300 (mesmo, bloco Operador) — os dois sites do rename D-04
- `web/tests/test_estrutura_para_payoff.mjs` linhas 107-135 (técnica de
  guardião estático por regex, modelo para o guardião do vocabulário banido)
- `.planning/phases/37-gr-fico-de-payoff-e-explica-o-confi-veis/37-UI-SPEC.md`
  §3 (tabela completa de 11 templates de frase por caso
  segmento-posição×inclinação — copiar as 11 linhas literalmente para as
  chaves de copy, não reformular)
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: Chaves de copy novas (CHART-01/02/03/05 + EXPL-01..03) e rename D-04</name>
  <files>web/src/copy.js</files>
  <action>
No bloco Estudo (linhas ~170-330), adicionar as chaves novas seguindo
EXATAMENTE o padrão vizinho (mesma indentação, mesmo estilo de comentário
quando a chave vizinha já tem um): `opcoesEixoZeroRotulo: "R$ 0"`,
`opcoesEixoVerticalLoteAjuda: "Multiplique pelo lote para o valor total."`,
`opcoesNoVencimentoTitulo: "No vencimento"`, `opcoesHojeTitulo: "Hoje ·
valor de mercado"`, `opcoesHojeAjuda: "Preço de mercado agora — pode mudar a
qualquer momento, diferente do resultado no vencimento acima."`,
(NÃO adicionar `opcoesHojeCarregando` — o UI-SPEC propôs um estado de
"carregando" para o bloco "hoje", mas o Plano 37-03 fechou a arquitetura
como síncrona: `valorHoje` sempre chega ao front já resolvido no mesmo
round-trip, nunca precisa de estado de loading. Uma chave nunca consumida
por nenhum componente é dead code — não criar.)
`opcoesPerdaIlimitadaCurta: "sem piso"`, `opcoesHojePrefixoEixo: "hoje"`,
`opcoesComoLerTitulo: "Como ler esta estrutura"`. Repetir a MESMA lista de
chaves no bloco Operador (linhas ~880-990), com os MESMOS valores literais
(nenhuma delas tem variante Operador diferenciada no UI-SPEC — só as 11
chaves de template de frase abaixo e o rename D-04 diferem por modo).

Adicionar as 11 chaves de template de frase da tabela `37-UI-SPEC.md` §3
(uma por linha da tabela: spot positiva, spot negativa, spot platô, spot
cauda ganho ilimitado, spot cauda perda ilimitada, outro segmento positiva,
outro segmento negativa, outro segmento platô, outra cauda platô, outra
cauda ganho ilimitado, outra cauda perda ilimitada) — nomear
`opcoesExplicSpotPositiva`, `opcoesExplicSpotNegativa`,
`opcoesExplicSpotPlato`, `opcoesExplicSpotCaudaGanho`,
`opcoesExplicSpotCaudaPerda`, `opcoesExplicOutroPositiva`,
`opcoesExplicOutroNegativa`, `opcoesExplicOutroPlato`,
`opcoesExplicOutroCaudaPlato`, `opcoesExplicOutroCaudaGanho`,
`opcoesExplicOutroCaudaPerda` — valor literal EXATO do texto da coluna
"Template" do UI-SPEC (placeholders `{spot}`/`{de}`/`{ate}` viram, no texto
final, `"{" + valor formatado + "}"` interpolado pelo componente, não pela
chave de copy — a chave guarda o template com os literais `{spot}`/`{de}`/
`{ate}` como estão na tabela). Mesmas 11 chaves nos dois blocos (Estudo e
Operador) — redação idêntica é aceitável aqui: o UI-SPEC não pede variação
por modo para estas 11 (só a citação da razão, se usada, reusa
`formatarRazao`, tratado pelo componente, não pela chave de copy).

Rename D-04: alterar SÓ o VALOR de `curadoriaRazaoRotulo` na linha 712
(bloco Estudo) para `"Pontuação de curadoria (ordena a lista — não é o
resultado da estrutura)"` e na linha 1300 (bloco Operador) para
`"Pontuação de curadoria"`. Não tocar `cand.razao` em
`CuradoriaEstruturas.jsx` (fora de escopo deste documento, per UI-SPEC §4) —
nenhuma outra linha muda.
  </action>
  <acceptance_criteria>
    <criterion>`grep -c "opcoesComoLerTitulo" web/src/copy.js` retorna 2 (uma por bloco Estudo/Operador)</criterion>
    <criterion>`grep -c "opcoesExplicSpotPositiva\|opcoesExplicSpotNegativa\|opcoesExplicSpotPlato\|opcoesExplicSpotCaudaGanho\|opcoesExplicSpotCaudaPerda\|opcoesExplicOutroPositiva\|opcoesExplicOutroNegativa\|opcoesExplicOutroPlato\|opcoesExplicOutroCaudaPlato\|opcoesExplicOutroCaudaGanho\|opcoesExplicOutroCaudaPerda" web/src/copy.js` retorna 22 (11 chaves × 2 blocos)</criterion>
    <criterion>`grep -n "curadoriaRazaoRotulo" web/src/copy.js` mostra "Pontuação de curadoria" nas duas ocorrências (712, 1300), nenhuma mais lendo "prêmio sobre perda máxima"/"prêmio / perda máxima"</criterion>
    <criterion>`npx vite build` (dentro de `web/`) sai com exit 0 — sintaxe JS válida</criterion>
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: formatarRazao() extraído + ExplicacaoPayoff.jsx (componente puro)</name>
  <files>web/src/opcoes/uiOpcoes.jsx, web/src/opcoes/ExplicacaoPayoff.jsx, web/tests/test_explicacao_payoff.mjs</files>
  <behavior>
    - Segmento do spot com `inclinacao: "positiva"` gera a frase de
      `cp.opcoesExplicSpotPositiva` com `{spot}` substituído pelo valor
      formatado (2 casas, vírgula) de `spot`, listada PRIMEIRO na saída
    - Demais segmentos (não-spot) aparecem depois, na ordem original do
      array (esquerda→direita), pulando o índice do segmento do spot
    - Segmento cuja `ate === null` e `inclinacao === "positiva"` usa o
      template de cauda ganho ilimitado (`opcoesExplicSpotCaudaGanho`/
      `opcoesExplicOutroCaudaGanho`), NUNCA o template finito
    - `formatarRazao({valor: 0.67, motivo: null})` retorna a mesma string
      que `RazaoGanhoPerda` hoje produz inline (`"1 : 0,67"`) — chamado por
      AMBOS os componentes, nunca formatado duas vezes
    - `ExplicacaoPayoff({segmentos: [], ...})` (array vazio ou não-array)
      retorna `null` (sem crash, sem caixa vazia)
    - Nenhuma palavra do vocabulário banido (strike, prêmio, delta, theta,
      volatilidade implícita, exercício, rolagem, ITM, OTM, ATM, "perna")
      aparece em nenhum template de copy nem no texto composto pelo
      componente
  </behavior>
  <action>
Em `uiOpcoes.jsx`, extrair de `RazaoGanhoPerda` (linhas 75-93) a expressão
`"1 : " + fmt(razao.valor)` para uma função nova exportada
`formatarRazao(razao)` que retorna essa string quando `ehNum(razao?.valor)`
é verdadeiro, e `razao?.motivo || "—"` caso contrário — MESMA lógica que já
existe inline, só nomeada e exportada. Alterar `RazaoGanhoPerda` para chamar
`formatarRazao(razao)` em vez de repetir a expressão inline.

Criar `web/src/opcoes/ExplicacaoPayoff.jsx`, componente puro (zero `fetch`,
zero `useEffect`, zero `useState`), seguindo o formato de guarda-cláusula +
`Aviso` de `RazaoGanhoPerda`: `export default function
ExplicacaoPayoff({ segmentos, spot, razao, cp })`. Importa `{ Kicker, Aviso,
formatarRazao }` de `./uiOpcoes.jsx`. Se `segmentos` não é array ou está
vazio, retorna `null`. Uma função auxiliar pura `montarTexto(segmentos,
spot, razao, cp)` (exportada nomeadamente, para ser testável em isolamento
pelo `.mjs`) localiza o índice do segmento cujo intervalo `[de, ate]`
contém `spot` (ou cujo `ate === null` e `spot >= de`, para a cauda),
monta a frase desse segmento PRIMEIRO usando as chaves `cp.opcoesExplicSpot*`
(9 combinações: positiva/negativa/ePlato/cauda-ganho/cauda-perda), depois
percorre os demais segmentos na ordem original (pulando o índice do spot)
usando as chaves `cp.opcoesExplicOutro*`, junta as frases com `"\n"`. Se a
função recebe `razao` com `ehNum(razao?.valor)` verdadeiro, a ÚLTIMA linha
do texto cita a razão chamando `formatarRazao(razao)` — nunca formata
`razao.valor` de novo dentro deste arquivo. O componente renderiza
`<Kicker>{cp.opcoesComoLerTitulo || "Como ler esta estrutura"}</Kicker>`
seguido de `<Aviso>{texto}</Aviso>` (mesma composição visual de
`RazaoGanhoPerda`/blocos existentes — `whiteSpace: "pre-wrap"` já em
`Aviso`, então `"\n"` vira quebra de linha visível sem CSS novo).

Criar `web/tests/test_explicacao_payoff.mjs` (sem build, `node` puro,
mesmo padrão de `test_estrutura_para_payoff.mjs`): importa `montarTexto`
de `ExplicacaoPayoff.jsx` e `formatarRazao` de `uiOpcoes.jsx`, roda os casos
do bloco `<behavior>` acima como asserções `ok(...)`, e inclui o guardião
estático de vocabulário banido: lê o código-fonte de `web/src/copy.js` (só
as chaves `opcoesExplic*`) e de `ExplicacaoPayoff.jsx`, reprova por regex
case-insensitive a presença de `strike|prêmio|premio|delta|theta|volatilidade
implícita|exercício|rolagem|\bITM\b|\bOTM\b|\bATM\b|perna` fora de
comentários.
  </action>
  <acceptance_criteria>
    <criterion>`node web/tests/test_explicacao_payoff.mjs` sai com exit 0, todos os `ok()` verdadeiros</criterion>
    <criterion>`grep -n "export function formatarRazao" web/src/opcoes/uiOpcoes.jsx` existe</criterion>
    <criterion>`grep -c '"1 : " + fmt(razao.valor)' web/src/opcoes/uiOpcoes.jsx` retorna 1 (só dentro de `formatarRazao`, `RazaoGanhoPerda` não repete a expressão)</criterion>
    <criterion>`grep -n "formatarRazao" web/src/opcoes/ExplicacaoPayoff.jsx` mostra o import E ao menos uma chamada — nenhuma ocorrência de `razao.valor` seguida de `+`/`toFixed` fora dessa chamada</criterion>
    <criterion>`npx vite build` (dentro de `web/`) sai com exit 0</criterion>
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nenhuma nova nesta plano | `ExplicacaoPayoff.jsx` é função pura sobre props já validadas a montante (Plano 37-01); zero I/O, zero rede |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-03 | Information Disclosure (regulatório, não segurança de dados) | `ExplicacaoPayoff.jsx` citando `razao`/segmentos de forma que soe recomendação | mitigate | Templates de copy (37-UI-SPEC §3) descrevem MECÂNICA da curva ("se o preço subir, seu resultado melhora"), nunca direção/probabilidade/conselho — mesma disciplina do guardrail CVM já aplicado a manchete; guardião de vocabulário banido cobre jargão técnico, não substitui revisão humana do tom |
| T-37-04 | Tampering | Divergência entre o número exibido por `RazaoGanhoPerda` e o citado por `ExplicacaoPayoff` (classe de bug EXPL-03) | mitigate | `formatarRazao()` única função, importada por ambos — impossível por construção ter duas formatações divergentes desde que nenhum dos dois arquivos reformate `razao.valor` inline (guardião do acceptance_criteria acima cobre isso) |
</threat_model>

<verification>
- `node web/tests/test_explicacao_payoff.mjs` — exit 0
- `bash scripts/executar.sh --testes` — exit 0, sem regressão na suíte `.mjs` existente (`test_estrutura_para_payoff.mjs`, `test_payoff_responsivo.mjs`, `test_opcoes_analisar_ui.mjs` continuam verdes — este plano não toca `PayoffChart.jsx`/`SecaoAnalisar.jsx`/`SecaoComparar.jsx`)
- `npx vite build` (dentro de `web/`) — exit 0
</verification>

<success_criteria>
- `ExplicacaoPayoff` existe como componente puro, testável sem DOM/build
- `formatarRazao()` é a ÚNICA função de formatação da razão G/P no diretório `web/src/opcoes/`
- Todas as chaves de copy que os Planos 37-04/37-05 vão consumir já existem em `web/src/copy.js`, nos dois modos
- `curadoriaRazaoRotulo` não lê mais como resultado financeiro (D-04 fechado)
</success_criteria>

<output>
Create `.planning/phases/37-gr-fico-de-payoff-e-explica-o-confi-veis/37-02-SUMMARY.md` when done
</output>
