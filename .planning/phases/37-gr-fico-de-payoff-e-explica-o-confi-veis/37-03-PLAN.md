---
phase: 37-gr-fico-de-payoff-e-explica-o-confi-veis
plan: 03
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - server/app/options_mcp_api.py
  - server/tests/test_options_mcp_api.py
autonomous: true
requirements: [CHART-05]

must_haves:
  truths:
    - "POST /api/options/mcp/proposta com uma única estrutura sempre tenta buscar o valor de mercado atual (valorHoje) via get_option_chain, somando prêmio atual x lado x quantidade por perna"
    - "POST /api/options/mcp/possibilidades só tenta a busca de valorHoje para o PRIMEIRO item da lista (índice 0); os demais itens nunca recebem a chave valorHoje preenchida"
    - "Falha na busca de valorHoje NUNCA aborta a rota inteira — o resto do envelope (estrutura, emReais, dominio, segmentos) continua presente e correto"
    - "valorHoje chega ao front já resolvido no mesmo round-trip: {erro} ou {dados}, nunca um estado carregando client-side"
    - "chamadasPrevistas em possibilidades() reflete a chamada extra de get_option_chain (gated ao 1o candidato), sem o número mentir sobre o que a rota realmente consome"
  artifacts:
    - path: "server/app/options_mcp_api.py"
      provides: "_valor_hoje() (busca+soma por perna via get_option_chain) e wiring em proposta()/possibilidades()"
  key_links:
    - from: "options_mcp_api.py: proposta()/possibilidades()"
      to: "mcp_client.call_tool('get_option_chain', ...)"
      via: "_chamada_com_cap(cap, 'get_option_chain', args) dentro de _cap_check"
      pattern: "get_option_chain"
---

<objective>
Fecha CHART-05 (metade backend): o campo `valorHoje` no envelope de
`POST /api/options/mcp/proposta` e `POST /api/options/mcp/possibilidades`,
somando o prêmio ATUAL de cada perna (via `get_option_chain`, D-01 de
`37-CONTEXT.md`) × lado × quantidade — corrigindo a regressão confirmada em
produção onde "hoje" e "no vencimento" se confundiam no mesmo número.

Decisão de arquitetura (exercício da discretion já delegada por
`37-CONTEXT.md` — "nome exato do campo... e da função/endpoint que faz a
busca via `get_option_chain`" — confirmada por revisão antes deste plano):
a busca roda SÍNCRONA, dentro do mesmo handler `proposta()`/`possibilidades()`,
mesmo padrão de `_chamada_com_cap` já usado por toda chamada MCP deste
arquivo — um único round-trip HTTP. `valorHoje` chega ao front como
`null`/`undefined` (não tentado — D-02, candidatos 2+ de `possibilidades()`)
OU `{erro: {...}}` OU `{dados: {porAcao, emReais}}` — NUNCA
`{carregando: true}`: por definição o JSON só existe depois que o fetch já
resolveu de um jeito ou de outro. Não há round-trip separado, não há estado
de loading client-side específico para este campo.

Purpose: D-01 aceita o custo de cota extra por ser um número real de
marcação a mercado. D-02 limita esse custo em `possibilidades()` ao 1º
candidato da lista já ordenada — evita multiplicar chamadas.
Output: `valorHoje` no envelope dos dois endpoints; `chamadasPrevistas`
atualizado em `possibilidades()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-gr-fico-de-payoff-e-explica-o-confi-veis/37-CONTEXT.md
@.planning/phases/37-gr-fico-de-payoff-e-explica-o-confi-veis/37-PATTERNS.md
</context>

<interfaces>
Resposta de `get_option_chain` (tool MCP, já em uso na rota `GET /cadeia/{ticker}`,
`options_mcp_api.py:2054-2108`): `dados.get("options")` é uma lista de dicts
PT-KEYED (inconsistência pré-existente do serviço, não introduzida por este
plano): `{contrato, tipo, strike, premio, delta, total_negocios,
dt_vencimento, dt_pregao, preco_objeto}` (ver fixture `_CADEIA` em
`server/tests/test_options_mcp_api.py:107-122`). O campo `contrato` é a
MESMA string que `legs[].contract` (EN) da resposta de
`evaluate_option_structure`/`propose_option_setups` (ver
`server/tests/fixtures/mcp_evaluate_petr4.json`) — é assim que o código
casa uma perna avaliada com sua cotação atual na cadeia.

`legs[].side` é `"buy"|"sell"` (não `"compra"|"venda"` como o motor local) —
sinal: `"buy"` → `+1`, `"sell"` → `-1`, mesma convenção de sinal de
`opcoes_payoff._validar_perna` (`sinal = 1.0 if lado == "compra" else -1.0`),
só com os literais em inglês.
</interfaces>

<read_first>
- `server/app/options_mcp_api.py` linhas 2054-2108 (`cadeia()` — o padrão
  EXATO de chamada de `get_option_chain` com `_cap_check`/`_chamada_com_cap`
  a reusar, incluindo tratamento de erro `except (mcp_client.McpErro,
  ValueError)`)
- `server/app/options_mcp_api.py` linhas 763-840 (`_cap_check`, `_erro_http`
  — entender o contrato exato do context manager antes de aninhar um novo)
- `server/app/options_mcp_api.py` linhas 1131-1185 (`_chamada_com_cap`)
- `server/app/options_mcp_api.py` linhas 1274-1323 (`_vezes_lote`, `_em_reais`
  — disciplina "lote multiplica fora", a seguir para `valorHoje.dados.emReais`)
- `server/app/options_mcp_api.py` (após Plano 37-01: `proposta()` já tem
  `dominio`/`segmentos` — ler o estado ATUAL do arquivo, não o texto deste
  plano, para saber onde inserir o `valorHoje` sem conflitar)
- `server/app/options_mcp_api.py` linhas 2273-2441 (`possibilidades()`
  completo — especialmente o `with _cap_check(uid, 2 * len(escolhidos)) as
  cap2:` de duas etapas, D-24.1, e o comentário sobre por que a reserva é
  feita em duas fases)
- `server/tests/test_options_mcp_api.py` linhas 107-227 (`_CADEIA`, `_setup`,
  `_AVALIACAO`, `_roteador`, `_espiao` — helpers existentes)
</read_first>

<tasks>

<task type="auto">
  <name>Task 1: _valor_hoje() + wiring em proposta() (sempre) e possibilidades() (só o 1o candidato)</name>
  <files>server/app/options_mcp_api.py</files>
  <action>
Criar `async def _valor_hoje(cap, ticker: str, legs: list, ...) -> dict`
posicionada perto de `_em_reais`/`_razao_ganho_perda` (linha ~1366) OU como
função de rota-nível logo antes de `proposta()` (linha ~2199) — decisão de
posicionamento é discricionária, mas a assinatura deve aceitar o `_Reserva`
(`cap`) já aberto pelo `with _cap_check(...)` do chamador (mesmo padrão de
`_chamada_com_cap(cap, nome, args)` — esta função NÃO abre seu próprio `with
_cap_check`, o chamador já reservou). Corpo: monta `args = {"ticker": alvo}`
(sem `expiration`/`kind` — precisa ver CALL e PUT de todos os vencimentos
presentes nas pernas); chama `dados, _cache = await _chamada_com_cap(cap,
"get_option_chain", args)` DENTRO de um `try/except (mcp_client.McpErro,
ValueError) as e`; em caso de exceção, devolve `{"erro": {"code": ...,
"message": str(e)}}` (mesmo shape de erro que `_erro_http`/`ErroDoMcp` no
front já sabem interpretar — ler `_erro_http` para replicar a forma exata
do dict de erro, NÃO inventar um shape novo). Em caso de sucesso, monta um
índice `{opcao.get("contrato"): opcao.get("premio") for opcao in
dados.get("options") or [] if isinstance(opcao, dict)}`; para cada perna em
`legs` (lista EN, `{contract, side, quantity}` mínimo), busca o prêmio atual
pelo índice via `opcao.get("contract")`; se ALGUMA perna não é encontrada no
índice (contrato ausente do resultado — cadeia truncada ou vencimento sem
negócio), devolve `{"erro": {"code": "valor_hoje_incompleto", "message":
"O serviço não devolveu cotação atual para todas as pernas desta
estrutura."}}` — NUNCA soma parcial calada. Caso todas as pernas sejam
encontradas, soma `sinal * quantidade * premio_atual` por perna (sinal:
`"buy"`→`+1`, `"sell"`→`-1`, mesma convenção de `opcoes_payoff._validar_perna`)
para obter o valor por ação, e devolve `{"dados": {"porAcao": round(valor,
4), "emReais": None}}` — `emReais` fica `None` aqui de propósito: quem
multiplica pelo lote é o CHAMADOR (`proposta()`/`possibilidades()`, que já
tem `lote` em escopo), reusando `_vezes_lote(valor, lote)` — mesma
disciplina "lote multiplica fora" de `_em_reais`, nunca uma segunda
implementação da multiplicação dentro de `_valor_hoje`.

Em `proposta()`: quando `len(estruturas) == 1` E `lote` foi informado (só
faz sentido mostrar `emReais` se há lote — mesma regra de `em_reais`),
abrir um `with _cap_check(uid, 1) as cap_hoje:` ANINHADO dentro do `with
_cap_check(uid, 1) as cap:` já existente (mesmo padrão de duas reservas
aninhadas que `possibilidades()` já usa, D-24.1) e chamar `valor_hoje =
await _valor_hoje(cap_hoje, alvo, estruturas[0].get("legs") or [])`; se
`valor_hoje` tem chave `"dados"`, completar `valor_hoje["dados"]["emReais"]
= _vezes_lote(valor_hoje["dados"]["porAcao"], lote)`. Quando `len(estruturas)
!= 1` ou sem `lote`, `valor_hoje = None` (chave ausente do dict de saída ou
`None` explícito — escolher UM padrão e ser consistente com como `emReais`
já trata "sem lote": ver linha 2240). Adicionar `"valorHoje": valor_hoje`
ao dict de retorno, chave irmã de `"dominio"`/`"segmentos"` (Plano 37-01).

Em `possibilidades()`: só para o item de índice 0 do loop `for vencimento in
escolhidos` (usar `enumerate(escolhidos)` — a estrutura hoje usa `for
vencimento in escolhidos` sem índice, trocar para `for i, vencimento in
enumerate(escolhidos)`), quando esse item tiver `estrutura` não-nula, abrir
um `with _cap_check(uid, 1) as cap_hoje:` e chamar `_valor_hoje(cap_hoje,
alvo, estrutura.get("legs") or [])`, completando `emReais` com
`_vezes_lote(...)` da mesma forma. Para os demais índices (`i != 0`) e para
o item sem `estrutura`, `valorHoje` NÃO entra no dict do item (chave
ausente, não `None` — D-02: "não tentado" é estado diferente de "tentado e
falhou", e o front (Plano 37-05) distingue os dois pela PRESENÇA da chave).
Atualizar `envelope["chamadasPrevistas"]` (linha ~2340) de `2 * len(escolhidos)
+ 1` para `2 * len(escolhidos) + 2` — o `+1` extra é a chamada de
`get_option_chain` do 1º candidato, constante (não escala com N, porque só
o índice 0 é tentado).
  </action>
  <acceptance_criteria>
    <criterion>`grep -n "async def _valor_hoje" server/app/options_mcp_api.py` existe, uma vez</criterion>
    <criterion>`grep -n '"valorHoje"' server/app/options_mcp_api.py` aparece em `proposta()` e em `possibilidades()`</criterion>
    <criterion>`grep -n "2 \* len(escolhidos) + 2" server/app/options_mcp_api.py` existe (fórmula atualizada) e `grep -c "2 \* len(escolhidos) + 1" server/app/options_mcp_api.py` retorna 0 (fórmula antiga não sobrou em nenhum lugar)</criterion>
    <criterion>`grep -n "enumerate(escolhidos)" server/app/options_mcp_api.py` existe dentro de `possibilidades()`</criterion>
    <criterion>Leitura direta confirma: nenhuma exceção de `_valor_hoje`/`get_option_chain` propaga para fora do `try/except` local — só o `except (mcp_client.McpErro, ValueError)` do LOOP PRINCIPAL de `possibilidades()` (que aborta a rota inteira) continua reservado para falha do SERVIÇO como um todo, não para a falha pontual do fetch de valor_hoje</criterion>
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Testes de valor_hoje — sucesso, contrato ausente, falha do serviço, gate do 1o candidato</name>
  <files>server/tests/test_options_mcp_api.py</files>
  <action>
Estender `server/tests/test_options_mcp_api.py` com um bloco de testes (novo
separador de seção) cobrindo, via `_espiao`/`_roteador` (estender `_roteador`
para aceitar um `get_option_chain` customizável por teste — hoje ele sempre
devolve `_CADEIA` fixo; adicionar parâmetro opcional para o teste sobrepor):
(1) `proposta()` com estrutura única e lote informado, `get_option_chain`
devolvendo prêmio atual para TODAS as pernas — `valorHoje.dados.porAcao` é a
soma assinada esperada (calcular manualmente no teste a partir de `_CADEIA`
ou de um fixture local com valores conhecidos) e `valorHoje.dados.emReais`
igual a `porAcao * lote`; (2) mesma estrutura, mas `get_option_chain`
devolve uma cadeia SEM um dos contratos das pernas — `valorHoje == {"erro":
{"code": "valor_hoje_incompleto", ...}}`, e o resto do envelope
(`dominio`/`segmentos`/`estruturas`) continua presente e correto (a falha
de `valorHoje` não contamina o resto da resposta, nem o status HTTP, que
continua 200); (3) `get_option_chain` levanta `mcp_client.McpErro` — mesmo
resultado do caso (2), `valorHoje.erro` presente, resto da resposta intacto,
status 200 (a rota NÃO vira 502 por causa de `valor_hoje` — só o loop
principal de `possibilidades()`/a chamada de `propose_option_setups` em
`proposta()` abortam a rota); (4) `possibilidades()` com 2+ vencimentos
resolvidos — só o item de índice 0 tem a chave `"valorHoje"` presente no
dict (mesmo que como `{"erro":...}`), os demais itens NÃO têm a chave
`"valorHoje"` (usar `"valorHoje" not in item` na asserção, não
`item.get("valorHoje") is None` — são estados diferentes); (5)
`envelope["chamadasPrevistas"] == 2 * len(escolhidos) + 2` para uma chamada
com N vencimentos escolhidos conhecido no teste.
  </action>
  <acceptance_criteria>
    <criterion>`cd server && python -m pytest tests/test_options_mcp_api.py -k "valor_hoje" -v` — todos os testes novos passam</criterion>
    <criterion>O teste do caso (4) usa `assert "valorHoje" not in item` (não `is None`) para os itens 1+ — confirmado por leitura do teste antes de considerar a task completa</criterion>
    <criterion>`bash scripts/executar.sh --testes` (suíte canônica completa) sai com exit 0, sem falha nova</criterion>
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente autenticado → rota `/proposta`/`/possibilidades` | Requisição pode ser feita diretamente (não só pelo botão da UI), disparando o fetch extra de `get_option_chain` a cada chamada |
| `options_mcp_api.py` → serviço MCP externo (`get_option_chain`) | Chamada de rede nova por request, consumindo cota compartilhada (ADR-013/`plan.py`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigate/Accept |
|-----------|----------|-----------|-------------|-----------------|
| T-37-05 | Denial of Service (esgotamento de cota) | Cliente chamando `/proposta`/`/possibilidades` repetidamente para drenar a cota diária mais rápido via o fetch extra de `valorHoje` | mitigate | `_cap_check`/`_chamada_com_cap` já aplicam o MESMO teto diário por usuário (`plan.py`, ADR-013) a esta chamada como a qualquer outra do arquivo — não há bypass possível chamando a rota HTTP diretamente, porque o gate é server-side, não client-side (D-02 do CONTEXT.md é sobre CUSTO típico de uso normal, não sobre controle de acesso — o teto de cota é a mitigação real contra abuso deliberado) |
| T-37-06 | Information Disclosure | Mensagem de erro de `get_option_chain` (`erro.message`) exposta verbatim ao cliente | accept | Mesmo padrão JÁ aceito em toda a rota (`_erro_http`, `ErroDoMcp` no front) — mensagens do serviço MCP não carregam segredo por construção do `mcp_client` (comentário já existente no arquivo, linha ~2394) |
| T-37-07 | Repudiation/Integrity | `_valor_hoje` soma prêmios de forma incorreta se o sinal de `side` for invertido silenciosamente | mitigate | Testes do Task 2 fixam o sinal esperado (`buy`→+1, `sell`→-1) contra valores conhecidos; nenhuma perna sem contrato encontrado soma como 0 (erro explícito em vez de soma parcial silenciosa) |
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` — exit 0
- `grep -n "chamadasPrevistas" server/app/options_mcp_api.py` confirma a fórmula nova
</verification>

<success_criteria>
- `valorHoje` aparece resolvido (nunca `carregando`) no mesmo round-trip de `proposta()`/`possibilidades()`
- Falha do fetch de valor_hoje nunca aborta a rota
- `possibilidades()` só tenta o fetch para o candidato de índice 0
- `chamadasPrevistas` reflete o custo real
</success_criteria>

<output>
Create `.planning/phases/37-gr-fico-de-payoff-e-explica-o-confi-veis/37-03-SUMMARY.md` when done
</output>
