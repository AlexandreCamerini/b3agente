---
phase: 03-corre-o-cr-tico-alto
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - web/tests/test_fase3_paridade_stores_generica.mjs
  - web/src/App.jsx
  - web/tests/test_fase3_c19_card_status.mjs
  - web/tests/test_auditoria_status_strip.mjs
autonomous: true
requirements: [FIX-C20, FIX-C19]

must_haves:
  truths:
    - "Um método adicionado a só um dos dois stores (`deviceStore` ou `serverStore`) faz a suíte falhar, sem depender de o autor lembrar de escrever um teste pontual"
    - "As assimetrias intencionais entre os stores estão declaradas numa allowlist explícita, com justificativa, não implícitas no silêncio do teste"
    - "Ao abrir a tela Operador IA, o usuário vê num ÚNICO bloco, antes de qualquer controle, o estado atual dos 3 interruptores que decidem se uma ordem dispara: Modo do app, Operador no servidor e Executar/sinalizar"
    - "Cada badge exibe o valor derivado da fonte canônica que a própria tela já usa (`ctx.operador`, `ag.serverEnabled` com a mesma guarda de login do card-herói, `modoEfetivo`) — o card nunca contradiz o card-herói logo abaixo"
    - "O card é leitura, não controle: nenhum badge alterna estado; a troca acontece pelo link direto para o Perfil"
    - "Existe UMA tira de status na tela, não duas — a tira parcial anterior (só Modo do app) foi absorvida pelo card, não duplicada"
  artifacts:
    - path: "web/tests/test_fase3_paridade_stores_generica.mjs"
      provides: "guardião genérico exaustivo de paridade deviceStore × serverStore (extrai as chaves dos DOIS e compara conjuntos)"
      min_lines: 40
    - path: "web/src/App.jsx"
      provides: "card de status único (3 badges + link para o Perfil) como primeiro elemento do return de AgenteScreen"
      contains: "Operador no servidor"
    - path: "web/tests/test_fase3_c19_card_status.mjs"
      provides: "guardião estático do card C-19: existe, vem antes de qualquer controle, lê as 3 fontes canônicas, é read-only e não duplica a tira antiga"
      min_lines: 40
  key_links:
    - from: "web/tests/test_fase3_paridade_stores_generica.mjs"
      to: "web/src/persistence.js"
      via: "readFileSync + extração das chaves dos dois blocos de store"
      pattern: "persistence\\.js"
    - from: "web/src/App.jsx (card C-19)"
      to: "ctx.operador / ag.serverEnabled / modoEfetivo"
      via: "leitura das variáveis canônicas já derivadas no topo de AgenteScreen"
      pattern: "modoEfetivo === \"executar\""
    - from: "web/src/App.jsx (card C-19)"
      to: "tela Perfil"
      via: "A.go(\"perfil\") no link \"Trocar modo →\""
      pattern: "A\\.go\\(\"perfil\"\\)"
    - from: "web/tests/test_fase3_c19_card_status.mjs"
      to: "web/src/App.jsx"
      via: "readFileSync + isolamento do corpo de AgenteScreen"
      pattern: "App\\.jsx"
---

<objective>
Fechar dois achados Alto de natureza diferente, ambos instâncias do
padrão-classe "estado que muda num lugar que outro não vê":

- **C-20 (Task 1):** barreira estrutural contra "método existe num store e não
  no outro" — guardião genérico de paridade `deviceStore` × `serverStore`.
- **C-19 (Task 2):** o **card de status único** no topo da tela "Operador IA",
  ANTES de qualquer controle, resumindo os 3 interruptores que decidem se uma
  ordem dispara (Modo do app · Operador no servidor · Executar/sinalizar), com
  o estado atual de cada um e link direto pra trocar.

Purpose: hoje os stores estão simétricos (58 métodos de cada lado, 0
assimetrias de nome), mas 28 dos 58 não têm NENHUMA referência em teste — a
paridade depende do autor lembrar de escrever um teste pontual. Os dois
incidentes reais (carteira nativa não sincronizava, F10-20260807-05;
`initialBudget` sem sync device→servidor, F10-20260809-05) são instâncias
dessa mesma classe. Do lado de C-19, o usuário precisa hoje visitar DUAS telas
(Operador IA e Perfil → Modo de trabalho) para saber se uma ordem dispararia —
a tela atual mostra só 1 dos 3 interruptores, e essa lacuna foi a causa raiz
registrada de "não me deixa selecionar Executar".
Output: um guardião genérico no padrão estático da casa (`readFileSync` +
regex) e uma mudança de UI real em `web/src/App.jsx`, com seu próprio guardião
estático.

**Fronteira de escopo (D-02 revisado do CONTEXT.md):** C-19 é *visibilidade
estrutural*, não teste de regressão — a decisão original desta linha (guardião
de contagem de leituras de `appMode`) estava ERRADA, confundia C-19 com C-21.
Este plano NÃO migra os pontos de leitura redundante de `appMode` em
`App.jsx` — essa migração mecânica é C-21 (Médio, fase 5), requisito
diferente, e permanece intocada aqui.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-corre-o-cr-tico-alto/03-CONTEXT.md
@.planning/phases/03-corre-o-cr-tico-alto/03-UI-SPEC.md
@.planning/codebase/TESTING.md
@CLAUDE.md

<interfaces>
<!-- Estrutura real dos alvos. Use direto; não reexplore o repositório. -->

`web/src/persistence.js` (1251 linhas) — dois stores, ESTILOS DIFERENTES de
declaração de método, e é justamente por isso que um regex único não serve:
```javascript
function serverStore() { ... return {          // linha 97; objeto literal com propriedades-seta
  isNative: false,
  getState: () => sync.readState(),
  putConfig: (patch) => sync.mutate("putConfig", [patch], optConfig(patch)),
  syncPushPrefs: async () => { ... },
}; }

function deviceStore() { ... return {          // linha 214; corpo do objeto começa na linha 441
  async syncPushPrefs() { ensure(); ... },     // método-atalho (shorthand)
  putConfig(patch) { ... },
}; }

export const store = isNative ? deviceStore() : serverStore();   // linha 1171
```

Assimetria INTENCIONAL conhecida (única, documentada no REPORT-01): a
assinatura de `_setDeviceScope` difere entre os dois lados.

`web/src/App.jsx` — `AgenteScreen` (função na linha 3688). As TRÊS fontes que
o card de C-19 consome já existem no topo dessa função, prontas:
```javascript
function AgenteScreen({ ctx }) {
  const { data, A, cycleBusy } = ctx;
  const ag = data.agent;                      // linha 3690
  const logged = !!ctx.authUser;              // linha 3692
  const { operador } = ctx;                   // linha 3698  (fonte única de appMode)
  const modoEfetivo = operador ? (ag.mode || (ag.autonomous ? "executar" : "sinalizar")) : "sinalizar";  // linha 3702
```

Início do `return` de `AgenteScreen` (o card entra ANTES da linha 3732):
```jsx
  return (
    <div>
      <div style={{ display: "flex", alignItems: "center", gap: "2px" }}>          {/* 3732 */}
        <h1 style={{ margin: 0, fontSize: "22px", fontWeight: 700 }}>Operador IA</h1>
        <InfoDot onClick={A.openAbout} />
      </div>
      <p style={{ ... }}>Seu operador autônomo da carteira SIMULADA: ...</p>       {/* 3736-3738 */}
```

A tira PARCIAL de hoje (linhas 3740-3756) — mostra só 1 dos 3 interruptores e
é o bloco que o card ABSORVE (o container/estilo dela é o padrão a reusar):
```jsx
      {/* qa/audit-2026-08-07 (itens 3+4): resumo do modo do app, ANTES de ... */}
      <div style={{ marginTop: "14px", display: "flex", alignItems: "center", justifyContent: "space-between", gap: "10px", padding: "10px 14px", borderRadius: "10px", background: T.bgBase, border: `1px solid ${T.borderFaint}` }}>
        <div style={{ fontSize: "12px", color: T.textSecondary }}>
          Modo do app: <b style={{ color: operador ? T.accent : T.textPrimary }}>{operador ? "📈 Operador" : "🎓 Estudo"}</b>
          <span style={{ color: T.textFaint }}> — {operador ? "decisões diretas liberadas" : "só orienta, nunca decide sozinho"}</span>
        </div>
        <button onClick={() => A.go("perfil")} style={{ background: "transparent", border: "none", padding: 0, color: T.accent, fontWeight: 800, fontSize: "11.5px", textDecoration: "underline", flex: "none" }}>
          Trocar →
        </button>
      </div>
```

Card-herói logo abaixo (linhas 3761-3803) — o card de status NÃO pode
contradizê-lo: ele só diz ATIVO quando `(ag.serverEnabled && logged)`, e exibe
`modoEfetivo === "executar" ? "Executar" : "Apenas sinalizar"`.

Guardião JÁ EXISTENTE da tira atual — `web/tests/test_auditoria_status_strip.mjs`
(house rule: guardião não se apaga; reversão deliberada atualiza com nota):
```javascript
ok("a tira \"Modo do app\" existe em AgenteScreen, ANTES do card OPERADOR NO SERVIDOR",
   screen.indexOf("Modo do app:") > 0
   && screen.indexOf("Modo do app:") < screen.indexOf("OPERADOR NO SERVIDOR"));
ok("mostra qual dos dois modos está ativo (Operador × Estudo), não um rótulo fixo",
   /\{operador \? "📈 Operador" : "🎓 Estudo"\}/.test(screen));
ok("tem o link direto pra Perfil (mesmo destino do link de dentro dos cards)",
   /Modo do app:[\s\S]{0,400}A\.go\("perfil"\)/.test(screen));
```

Padrão de guardião estático já usado no repositório (`web/tests/test_deep_parity.mjs`):
```javascript
const src = readFileSync(new URL("../src/persistence.js", import.meta.url), "utf8");
const hits = (src.match(new RegExp("\\b" + m + "\\s*[:(]", "g")) || []).length;
assert.ok(hits >= 2, m + " deve existir nos DOIS stores");
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guardião genérico exaustivo de paridade deviceStore × serverStore (C-20)</name>
  <files>web/tests/test_fase3_paridade_stores_generica.mjs</files>

  <read_first>
    - `web/src/persistence.js:97-213` — corpo de `serverStore()` inteiro (estilo `nome: (args) => ...`)
    - `web/src/persistence.js:214-250` e `:435-450` — cabeça de `deviceStore()` e o início do seu `return {` (estilo `async nome(args) {` / `nome(args) {`)
    - `web/src/persistence.js:1160-1172` — fim de `deviceStore()` e `export const store = ...`
    - `web/tests/test_deep_parity.mjs` — o padrão vigente (contagem `>= 2`), que este guardião COMPLEMENTA, não substitui
    - `web/tests/test_didatica_parity.mjs` — segundo exemplo do mesmo padrão
    - `.planning/codebase/TESTING.md` — "Web Test Framework" (Pattern A/B, exit code), "Static source inspection", "Named parity guardians" e a house rule "guardiões de teste não se apagam"
    - `.planning/milestones/v1.0-phases/01-auditoria-diagn-stica-consolidada/REPORT-01.md` — seção `#### C-20` (linha 544) e a lista dos 28 métodos sem referência em teste (linhas 512-524)
  </read_first>

  <action>
    Criar `web/tests/test_fase3_paridade_stores_generica.mjs`, Pattern A
    (checker booleano `ok(nome, cond)`, `process.exit(fails === 0 ? 0 : 1)`),
    inspecionando `web/src/persistence.js` estaticamente via `readFileSync`
    (o módulo importa `@capacitor/core` e não é importável fora do build —
    mesma razão dos guardiões existentes).

    Algoritmo, explícito:
    1. Localizar os limites dos dois blocos por índice de string:
       `iServer = src.indexOf("function serverStore()")`,
       `iDevice = src.indexOf("function deviceStore()")`,
       `iExport = src.indexOf("export const store")`.
       Falhar com mensagem clara se qualquer um for `-1` (o guardião precisa
       gritar se o arquivo for reestruturado, não passar em silêncio).
       `blocoServer = src.slice(iServer, iDevice)`,
       `blocoDevice = src.slice(iDevice, iExport)`.
    2. Extrair os nomes de método de cada bloco com regexes ESPECÍFICAS do
       estilo de cada store, aplicadas linha a linha e ignorando linhas de
       comentário (`linha.trim().startsWith("//")` ou `*`):
       - serverStore (propriedade-seta / valor): `^\s{4}([A-Za-z_$][\w$]*)\s*:\s*`
       - deviceStore (método-atalho, com ou sem `async`): `^\s{4}(?:async\s+)?([A-Za-z_$][\w$]*)\s*\(`
         MAIS o mesmo padrão de propriedade-seta (`^\s{4}([A-Za-z_$][\w$]*)\s*:\s*`),
         porque `deviceStore` mistura os dois estilos.
       A indentação de 4 espaços é o que restringe a extração ao NÍVEL do
       objeto retornado, sem capturar funções aninhadas — validar essa
       premissa contra o arquivo real e ajustar o número de espaços se
       necessário, mas manter a âncora de indentação (não usar regex global
       solta, que capturaria helpers internos).
    3. Declarar no topo do arquivo, como constante, a allowlist de assimetrias
       intencionais: `const ASSIMETRIAS_INTENCIONAIS = { "_setDeviceScope": "assinatura difere de propósito (escopo é conceito do aparelho) — REPORT-01 C-20" };`
       — objeto nome→justificativa, nunca uma lista nua.
    4. Assertivas:
       (a) `blocoServer` e `blocoDevice` foram localizados e cada um rendeu
       pelo menos 40 nomes (sanidade da extração — se a regex quebrar por uma
       mudança de formatação, o guardião falha em vez de virar no-op);
       (b) `soNoDevice = [...device].filter(m => !server.has(m) && !(m in ASSIMETRIAS_INTENCIONAIS))` é vazio,
       com mensagem listando os nomes;
       (c) `soNoServer` idem;
       (d) todo nome da allowlist realmente existe em pelo menos um dos dois
       lados (impede a allowlist virar lixo acumulado depois de uma remoção);
       (e) a contagem total de cada lado é igual (`server.size === device.size`
       descontadas as assimetrias declaradas).
    5. Mensagem de falha acionável, no tom do repositório, por exemplo:
       `"`+"`"+`X`+"`"+` existe só em deviceStore. Método novo entra nos DOIS stores no MESMO commit (CLAUDE.md, guardrail de paridade). Se a assimetria for intencional, declare-a em ASSIMETRIAS_INTENCIONAIS com a justificativa."`

    Cabeçalho do arquivo no padrão "guardião" da casa: bloco de comentário
    citando C-20 do REPORT-01, os DOIS incidentes reais que a lacuna causou
    (carteira nativa não sincronizava, F10-20260807-05; `initialBudget` sem
    sync device→servidor, F10-20260809-05) e a nota de que este teste
    COMPLEMENTA os guardiões pontuais existentes (`test_deep_parity.mjs`,
    `test_didatica_parity.mjs`, ...) — nenhum deles é removido.

    NÃO alterar `web/src/persistence.js` neste plano. Se a extração revelar
    uma assimetria REAL hoje (o REPORT-01 diz que não há), parar e reportar em
    vez de "corrigir" o store — isso seria um achado novo, fora do escopo.
  </action>

  <acceptance_criteria>
    - `node web/tests/test_fase3_paridade_stores_generica.mjs` sai 0.
    - O teste é comprovadamente sensível: ao inserir temporariamente um método fictício em um só dos stores, o teste sai não-zero e nomeia o método na saída (executar essa verificação e REVERTER a inserção antes de concluir a task).
    - Cada lado extraiu ≥ 40 nomes de método (a extração não virou no-op silencioso).
    - Nenhum arquivo em `web/src/` foi modificado.
    - Nenhum guardião existente foi apagado ou enfraquecido: `ls web/tests/test_deep_parity.mjs web/tests/test_didatica_parity.mjs` continua listando os dois, com conteúdo inalterado (`git diff --stat web/tests/test_deep_parity.mjs web/tests/test_didatica_parity.mjs` vazio).
  </acceptance_criteria>

  <verify>
    <automated>node web/tests/test_fase3_paridade_stores_generica.mjs</automated>
  </verify>

  <done>Existe um guardião único que compara os conjuntos de métodos dos dois stores e falha em qualquer assimetria não declarada, com allowlist justificada e sensibilidade comprovada.</done>
</task>

<task type="auto">
  <name>Task 2: Card de status único no topo de "Operador IA" — 3 interruptores visíveis (C-19)</name>
  <files>web/src/App.jsx, web/tests/test_fase3_c19_card_status.mjs, web/tests/test_auditoria_status_strip.mjs</files>

  <read_first>
    - `.planning/phases/03-corre-o-cr-tico-alto/03-UI-SPEC.md` — seção `### C-19 — status card único, topo de AgenteScreen` (Phase-Specific Component Notes) — é o contrato visual desta task: placement, hierarquia, cor, tipografia, espaçamento, os 3 badges e o link. Ler ANTES de escrever JSX.
    - `.planning/phases/03-corre-o-cr-tico-alto/03-CONTEXT.md` — decisão **D-02 (revisado)**: C-19 é card de visibilidade, NÃO teste de leitura de `appMode`; a migração dos pontos redundantes segue sendo C-21 (fase 5).
    - `web/src/App.jsx:3688-3760` — `AgenteScreen`: as 3 fontes (`operador` :3698, `ag.serverEnabled` via `ag` :3690 + `logged` :3692, `modoEfetivo` :3702), o início do `return` (:3731-3738) e a tira parcial atual (:3740-3756)
    - `web/src/App.jsx:3761-3803` — card-herói: os rótulos que o card de status NÃO pode contradizer (`(ag.serverEnabled && logged) ? "ATIVO" : "INATIVO"`, `modoEfetivo === "executar" ? "Executar" : "Apenas sinalizar"`)
    - `web/src/App.jsx:7214-7220` — definição canônica de `ctx.operador` e o comentário "Novo código deve ler `ctx.operador`" (o card OBEDECE essa convenção; não redevira de `data.config.appMode`)
    - `web/tests/test_auditoria_status_strip.mjs` — guardião VIVO da tira atual; as 4 assertivas que precisam continuar verdes (ou ser atualizadas com nota, nunca apagadas)
    - `web/tests/test_agente_modo_estudo_ui.mjs` — segundo guardião que já lê `AgenteScreen` estaticamente (padrão de isolamento `indexOf("function AgenteScreen(")` → próximo `\nfunction `)
    - `.planning/milestones/v1.0-phases/01-auditoria-diagn-stica-consolidada/REPORT-01.md` — seção `#### C-19` (linha 536)
    - `docs/auditoria-controle-ordens-parametros.md` — prioridade 3 (a recomendação original do card, e os 3 bugs históricos que motivaram)
    - `CLAUDE.md` — "Front editado → `npx vite build` antes de declarar ok"
  </read_first>

  <action>
    Implementar em `web/src/App.jsx`, dentro de `AgenteScreen`, o card de
    status único descrito no 03-UI-SPEC.md (seção C-19) — **implementa
    FIX-C19 / decisão D-02 revisada**.

    **Posição:** primeiro elemento do `return` de `AgenteScreen`, ANTES do
    bloco do `<h1>Operador IA</h1>` (:3732) — "no topo, ANTES de qualquer
    controle" (`docs/auditoria-controle-ordens-parametros.md`, prioridade 3).

    **Absorção da tira parcial (decisão explícita, não opcional):** a tira
    atual de :3740-3756 mostra 1 dos 3 interruptores e tem exatamente o mesmo
    papel. O requisito é card ÚNICO — o novo card SUBSTITUI aquele bloco
    (removê-lo do JSX), não convive com ele. Preservar no comentário do novo
    card o histórico de decisão do bloco absorvido (referência
    `qa/audit-2026-08-07 itens 3+4` e a causa raiz "não me deixa selecionar
    Executar"), no estilo de comentário da casa, acrescentando a referência
    C-19/REPORT-01. Duas tiras de status na mesma tela = defeito, não entrega.

    **Os 3 badges** (rótulo curto + ponto colorido, texto puro, sem componente
    novo), cada um lendo a fonte canônica que a tela JÁ derivou — nenhum lê
    `data.config.appMode`:
    1. "Modo do app" → `operador`. Manter LITERALMENTE a expressão de exibição
       existente `{operador ? "📈 Operador" : "🎓 Estudo"}` e o rótulo com o
       prefixo `Modo do app:` — isso mantém verde o guardião existente
       `test_auditoria_status_strip.mjs` sem reescrevê-lo.
    2. "Operador no servidor" → `Ligado` / `Desligado`, derivado de
       `!!(ag.serverEnabled && logged)`. O UI-SPEC cita `ag.serverEnabled`; a
       guarda `&& logged` é a MESMA que o card-herói logo abaixo aplica
       (:3765, :3768) — sem ela o card diria "Ligado" enquanto o herói diz
       "INATIVO". Regra do produto: a tela não exibe estado que o resto da
       tela contradiz.
    3. "Executar/sinalizar" → `modoEfetivo === "executar" ? "Executar" :
       "Apenas sinalizar"` (:3702). Nunca `ag.mode` cru — em Modo Estudo o
       `modoEfetivo` já força "sinalizar".

    **Read-only:** nenhum badge é clicável/alternável. O único elemento
    interativo do card é o link final "Trocar modo →", reusando o padrão
    existente `onClick={() => A.go("perfil")}` (:3752, :3798) com o mesmo
    estilo inline daquele botão-link (transparente, `T.accent`, 800, 11.5px,
    sublinhado) — o link é ação, mantém accent; os badges são passivos e não.
    Não criar deep-link para sub-view do Perfil (não existe alvo de
    `setPerfilView` para isso): o hub do Perfil basta.

    **Cor (03-UI-SPEC Dimension 3):** valor "ativo/ligado" em `T.positive`,
    "inativo/desligado" em `T.textFaint`. **Nenhum `T.accent` nos badges** —
    accent fica reservado ao `Toggle` do card-herói e ao link. Isso muda a
    cor do valor de "Modo do app" em relação à tira antiga (era `T.accent` /
    `T.textPrimary`) — mudança deliberada, prevista no UI-SPEC.

    **Tipografia (Dimension 4):** texto dos badges 11px, peso 400. Nenhum peso
    novo. O link mantém o peso 800 do padrão existente (exceção de código
    estabelecido já registrada no force-approval do UI-SPEC).

    **Espaçamento (Dimension 5):** reusar exatamente o container da tira
    absorvida (`padding: "10px 14px"`, `borderRadius: "10px"`, `background:
    T.bgBase`, `border: 1px solid ${T.borderFaint}`, `display: flex`,
    `justifyContent: "space-between"`, `gap: "10px"`) — zero valor de
    espaçamento novo. Ajustar só o `marginTop` para caber como primeiro
    elemento (o `marginTop: "14px"` da tira existia por vir depois do `<p>`;
    como primeiro elemento, usar `0`/omitir). Os 3 badges numa linha com o
    mesmo `gap` já usado em linhas de badge desta tela; em telas estreitas
    permitir `flexWrap: "wrap"` (padrão já usado :3789), sem media query nova.

    **Peso visual (Dimension 2):** o card fica ABAIXO do card-herói em peso —
    sem borda accent, sem CTA de peso, texto menor. O herói continua o foco.

    Sem mudança de backend: os 3 valores já chegam em `data`/`ctx`.

    Em seguida, criar `web/tests/test_fase3_c19_card_status.mjs` (Pattern A,
    `readFileSync` sobre `App.jsx`, isolando o corpo de `AgenteScreen` pelo
    mesmo `indexOf` dos guardiões existentes), com cabeçalho citando C-19,
    REPORT-01 e D-02 revisado, e as assertivas:
    (a) o card existe e vem ANTES do `<h1>` "Operador IA" e antes de
        "OPERADOR NO SERVIDOR" (índices de string comparados);
    (b) os 3 rótulos aparecem no bloco do card: `Modo do app`,
        `Operador no servidor`, `Executar/sinalizar` (ou os rótulos exatos
        que o JSX usar — registrar no comentário do teste);
    (c) cada badge lê a fonte canônica: casar
        `{operador ? "📈 Operador" : "🎓 Estudo"}`,
        `ag.serverEnabled && logged` (ou a constante derivada dela) e
        `modoEfetivo === "executar"` dentro do bloco do card;
    (d) o bloco do card NÃO contém `config.appMode` (a convenção "novo código
        lê `ctx.operador`" vale para o código novo desta task);
    (e) read-only: dentro do bloco do card não há `putAg(`, `setServer(` nem
        `<Toggle`; o único `onClick` é `A.go("perfil")`;
    (f) sem accent nos badges: `T.accent` aparece no máximo 1× no bloco (o
        link);
    (g) tira única: a string `Modo do app:` aparece exatamente 1× no corpo de
        `AgenteScreen` (impede reintroduzir a tira antiga em paralelo).
    Delimitar "o bloco do card" por marcadores estáveis (do comentário-âncora
    do card até o `<div style={{ display: "flex", alignItems: "center", gap:
    "2px" }}>` do `<h1>`), e falhar com mensagem clara se os marcadores não
    forem encontrados — o guardião grita em vez de virar no-op.

    Rodar `node web/tests/test_auditoria_status_strip.mjs`. Ele DEVE continuar
    verde sem edição — as 3 primeiras assertivas seguem satisfeitas pelo card.
    Se a 3ª (`Modo do app:` … `A.go("perfil")` dentro de 400 caracteres) falhar
    só porque os 2 badges novos aumentaram a distância, ATUALIZAR a janela do
    regex nesse guardião acrescentando uma nota datada explicando que C-19
    expandiu a tira em card de 3 badges, preservando a INTENÇÃO da assertiva
    (link para o Perfil junto do status). Nunca apagar o arquivo nem a
    assertiva (house rule: "guardiões de teste não se apagam").

    NÃO migrar nenhuma leitura existente de `data.config.appMode` fora do
    bloco removido — isso é C-21, fase 5.
  </action>

  <acceptance_criteria>
    - `node web/tests/test_fase3_c19_card_status.mjs` sai 0, com as 7 assertivas (a)-(g) presentes e verdes.
    - `node web/tests/test_auditoria_status_strip.mjs` sai 0 (inalterado, ou alterado apenas na janela do regex com nota datada — `git diff web/tests/test_auditoria_status_strip.mjs` não pode conter remoção de assertiva).
    - `node web/tests/test_agente_modo_estudo_ui.mjs` sai 0 (a trava de Modo Estudo do botão "Executar" não foi afetada).
    - `cd web && npx vite build` passa (CLAUDE.md: grep não pega erro de sintaxe JSX).
    - Sensibilidade comprovada: trocar temporariamente a leitura do badge 3 para `ag.mode` cru faz `test_fase3_c19_card_status.mjs` sair não-zero (executar e REVERTER antes de concluir).
    - Grep confirma tira única: `grep -c "Modo do app:" web/src/App.jsx` retorna `1`.
    - Nenhuma leitura de `data.config.appMode` foi adicionada ou migrada: a contagem de ocorrências de `config.appMode` em `App.jsx` não aumenta e só diminui pela remoção do bloco absorvido, se ele contiver alguma (`git diff web/src/App.jsx` revisado linha a linha).
    - Nenhum arquivo de backend alterado (`git status --porcelain server/` vazio).
  </acceptance_criteria>

  <verify>
    <automated>node web/tests/test_fase3_c19_card_status.mjs && node web/tests/test_auditoria_status_strip.mjs && node web/tests/test_agente_modo_estudo_ui.mjs && cd web && npx vite build</automated>
    <human-check>Abrir a tela "Operador IA": o card aparece no topo, antes do título, com os 3 estados corretos; trocar o Modo do app no Perfil e voltar muda o badge 1 e o badge 3 juntos; ligar/desligar o Operador no servidor muda o badge 2 e o card-herói na mesma direção (nunca em direções opostas).</human-check>
  </verify>

  <done>A tela "Operador IA" abre com um card de status único no topo mostrando os 3 interruptores que decidem se uma ordem dispara, cada um lido da fonte canônica e coerente com o card-herói; o card é somente leitura, com link direto para o Perfil; a tira parcial anterior foi absorvida (não duplicada); o build passa e nenhum ponto de `appMode` foi migrado (C-21 segue intacto para a fase 5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| desenvolvedor/agente → código do cliente | mudança que introduz assimetria de store ou estado de modo exibido de forma divergente atravessa para produção |
| `deviceStore` (localStorage, aparelho) × `serverStore` (servidor) | mesma interface, dois backends — divergência = perda silenciosa de dado do usuário |
| UI (card de status) → decisão do usuário sobre operar | o que o card afirma orienta a ação do usuário; afirmação errada induz erro operacional |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering (integridade de dado do usuário) | método adicionado a só um dos dois stores | mitigate | guardião genérico compara os conjuntos de chaves dos dois blocos e falha em qualquer assimetria não declarada; allowlist exige justificativa escrita. |
| T-03-10 | Spoofing (de estado do sistema para o usuário) | card de status afirmando "Ligado"/"Executar" divergente do estado real | mitigate | os 3 badges leem as MESMAS variáveis já derivadas e exibidas pelo card-herói (`operador`, `ag.serverEnabled && logged`, `modoEfetivo`), nunca `ag.mode` cru nem `data.config.appMode`; assertiva (c) do guardião trava as três fontes. |
| T-03-11 | Elevation of Privilege | card virar (ou parecer) o interruptor de execução | mitigate | card é read-only por construção (assertiva (e): sem `putAg`/`setServer`/`Toggle` no bloco); a trava REAL de execução continua no backend (`agent.py:559-570`, dupla trava escrita+leitura), que este plano não toca — a UI nunca é o único gate. |
| T-03-12 | Repudiation | guardião apagado para "fazer o teste passar" (em especial `test_auditoria_status_strip.mjs`, cuja tira é absorvida) | mitigate | house rule do repositório (CLAUDE.md: "guardiões de teste não se apagam") + critério de aceite que proíbe remoção de assertiva e exige nota datada em caso de ajuste de janela do regex. |
| T-03-13 | Denial of Service (de desenvolvimento) | regex de guardião quebrar e virar no-op silencioso | mitigate | assertivas de sanidade: extração precisa render ≥ 40 nomes por store; os marcadores do bloco do card precisam ser encontrados, senão o teste falha em vez de passar vazio. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | nenhum pacote novo é instalado neste plano — testes em `node` puro e build com o Vite já instalado. |
</threat_model>

<verification>
- `node web/tests/test_fase3_paridade_stores_generica.mjs` e `node web/tests/test_fase3_c19_card_status.mjs` — ambos verdes.
- `node web/tests/test_auditoria_status_strip.mjs` e `node web/tests/test_agente_modo_estudo_ui.mjs` — guardiões vizinhos da mesma tela, verdes.
- `cd web && npx vite build` — front editado, build obrigatório antes de declarar ok (CLAUDE.md).
- `bash scripts/executar.sh --testes` — suíte canônica (pytest + `web/tests/*.mjs`) sem falha nova.
- `git status --porcelain server/ web/src/persistence.js` vazio — este plano não toca backend nem os stores.
</verification>

<success_criteria>
- O guardião genérico de paridade dos stores existe, passa, e comprovadamente falha quando a assimetria é reintroduzida.
- A tela "Operador IA" mostra, no topo e antes de qualquer controle, um card ÚNICO com os 3 interruptores que decidem se uma ordem dispara, cada um lido da fonte canônica e coerente com o card-herói, com link direto pro Perfil.
- O card é somente leitura: nenhum badge altera estado.
- Nenhum guardião existente foi removido ou enfraquecido; ajustes deliberados carregam nota datada.
- Nenhum ponto de leitura de `appMode` foi migrado (fronteira D-02 revisado respeitada; C-21 continua sendo trabalho da fase 5).
</success_criteria>

<output>
Create `.planning/phases/03-corre-o-cr-tico-alto/03-03-SUMMARY.md` when done
</output>

