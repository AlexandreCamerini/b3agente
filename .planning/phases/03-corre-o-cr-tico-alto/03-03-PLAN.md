---
phase: 03-corre-o-cr-tico-alto
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - web/tests/test_fase3_paridade_stores_generica.mjs
  - web/tests/test_fase3_appmode_estrutural.mjs
autonomous: true
requirements: [FIX-C20, FIX-C19]

must_haves:
  truths:
    - "Um método adicionado a só um dos dois stores (`deviceStore` ou `serverStore`) faz a suíte falhar, sem depender de o autor lembrar de escrever um teste pontual"
    - "As assimetrias intencionais entre os stores estão declaradas numa allowlist explícita, com justificativa, não implícitas no silêncio do teste"
    - "Uma tela nova que leia `data.config.appMode` diretamente, em vez de `ctx.operador`, faz a suíte falhar"
    - "A definição canônica `ctx.operador` não pode ser removida sem quebrar a suíte"
  artifacts:
    - path: "web/tests/test_fase3_paridade_stores_generica.mjs"
      provides: "guardião genérico exaustivo de paridade deviceStore × serverStore (extrai as chaves dos DOIS e compara conjuntos)"
      min_lines: 40
    - path: "web/tests/test_fase3_appmode_estrutural.mjs"
      provides: "guardião estrutural: leituras diretas de data.config.appMode não podem crescer; ctx.operador não pode sumir"
      min_lines: 30
  key_links:
    - from: "web/tests/test_fase3_paridade_stores_generica.mjs"
      to: "web/src/persistence.js"
      via: "readFileSync + extração das chaves dos dois blocos de store"
      pattern: "persistence\\.js"
    - from: "web/tests/test_fase3_appmode_estrutural.mjs"
      to: "web/src/App.jsx"
      via: "readFileSync + contagem de leituras diretas de config.appMode"
      pattern: "App\\.jsx"
---

<objective>
Substituir a disciplina manual por barreira estrutural nos dois padrões-classe
que já causaram incidente documentado neste repositório: "método existe num
store e não no outro" (C-20) e "estado de modo lido de forma independente em
vez da fonte única" (C-19).

Purpose: hoje os stores estão simétricos (58 métodos de cada lado, 0
assimetrias de nome), mas 28 dos 58 não têm NENHUMA referência em teste — a
paridade depende do autor lembrar de escrever um teste pontual. Os dois
incidentes reais (carteira nativa não sincronizava, F10-20260807-05;
`initialBudget` sem sync device→servidor, F10-20260809-05) são instâncias
dessa mesma classe. Para `appMode`, a convenção "novo código lê `ctx.operador`"
existe só como comentário — nenhum teste a força.
Output: dois guardiões genéricos, no padrão estático já usado no repositório
(`readFileSync` + regex), que falham na CLASSE do erro e não só no sintoma já
corrigido.

**Fronteira de escopo (D-02 do CONTEXT.md):** este plano NÃO migra os pontos
de leitura redundante de `appMode` em `App.jsx` — essa migração mecânica é
C-21 (Médio, fase 5), requisito diferente. Aqui se constrói só a barreira que
impede o problema de CRESCER.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-corre-o-cr-tico-alto/03-CONTEXT.md
@.planning/codebase/TESTING.md
@CLAUDE.md

<interfaces>
<!-- Estrutura real dos alvos. Use direto; não reexplore o repositório. -->

`web/src/persistence.js` (1251 linhas) — dois stores, ESTILOS DIFERENTES de
declaração de método, e é justamente por isso que um regex único não serve:
```javascript
function serverStore() { ... return {          // linha 97; objeto literal com propriedades-seta
  isNative: false,
  getState: () => sync.readState(),
  putConfig: (patch) => sync.mutate("putConfig", [patch], optConfig(patch)),
  syncPushPrefs: async () => { ... },
}; }

function deviceStore() { ... return {          // linha 214; corpo do objeto começa na linha 441
  async syncPushPrefs() { ensure(); ... },     // método-atalho (shorthand)
  putConfig(patch) { ... },
}; }

export const store = isNative ? deviceStore() : serverStore();   // linha 1171
```

Assimetria INTENCIONAL conhecida (única, documentada no REPORT-01): a
assinatura de `_setDeviceScope` difere entre os dois lados.

`web/src/App.jsx` — a definição canônica de modo (linha ~7217-7220):
```javascript
    // Novo código deve ler `ctx.operador`, não redevirar de data.config.appMode.
    operador: !!(data && data.config && data.config.appMode === "operador"),
```

Leituras diretas de `config.appMode` hoje (inventário do REPORT-01, verificado
por grep): linhas 1624, 1828, 2018, 3188, 4224, 5606, 5756, 6319, 6501, 6862,
7220, 7411. Além disso há ESCRITAS (`A.saveConfig({ appMode: m })`, linhas
1834 e 1897) e comentários — que NÃO contam como leitura.

Padrão de guardião estático já usado no repositório (`web/tests/test_deep_parity.mjs`):
```javascript
const src = readFileSync(new URL("../src/persistence.js", import.meta.url), "utf8");
const hits = (src.match(new RegExp("\\b" + m + "\\s*[:(]", "g")) || []).length;
assert.ok(hits >= 2, m + " deve existir nos DOIS stores");
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guardião genérico exaustivo de paridade deviceStore × serverStore (C-20)</name>
  <files>web/tests/test_fase3_paridade_stores_generica.mjs</files>

  <read_first>
    - `web/src/persistence.js:97-213` — corpo de `serverStore()` inteiro (estilo `nome: (args) => ...`)
    - `web/src/persistence.js:214-250` e `:435-450` — cabeça de `deviceStore()` e o início do seu `return {` (estilo `async nome(args) {` / `nome(args) {`)
    - `web/src/persistence.js:1160-1172` — fim de `deviceStore()` e `export const store = ...`
    - `web/tests/test_deep_parity.mjs` — o padrão vigente (contagem `>= 2`), que este guardião COMPLEMENTA, não substitui
    - `web/tests/test_didatica_parity.mjs` — segundo exemplo do mesmo padrão
    - `.planning/codebase/TESTING.md` — "Web Test Framework" (Pattern A/B, exit code), "Static source inspection", "Named parity guardians" e a house rule "guardiões de teste não se apagam"
    - `.planning/milestones/v1.0-phases/01-auditoria-diagn-stica-consolidada/REPORT-01.md` — seção `#### C-20` (linha 544) e a lista dos 28 métodos sem referência em teste (linhas 512-524)
  </read_first>

  <action>
    Criar `web/tests/test_fase3_paridade_stores_generica.mjs`, Pattern A
    (checker booleano `ok(nome, cond)`, `process.exit(fails === 0 ? 0 : 1)`),
    inspecionando `web/src/persistence.js` estaticamente via `readFileSync`
    (o módulo importa `@capacitor/core` e não é importável fora do build —
    mesma razão dos guardiões existentes).

    Algoritmo, explícito:
    1. Localizar os limites dos dois blocos por índice de string:
       `iServer = src.indexOf("function serverStore()")`,
       `iDevice = src.indexOf("function deviceStore()")`,
       `iExport = src.indexOf("export const store")`.
       Falhar com mensagem clara se qualquer um for `-1` (o guardião precisa
       gritar se o arquivo for reestruturado, não passar em silêncio).
       `blocoServer = src.slice(iServer, iDevice)`,
       `blocoDevice = src.slice(iDevice, iExport)`.
    2. Extrair os nomes de método de cada bloco com regexes ESPECÍFICAS do
       estilo de cada store, aplicadas linha a linha e ignorando linhas de
       comentário (`linha.trim().startsWith("//")` ou `*`):
       - serverStore (propriedade-seta / valor): `^\s{4}([A-Za-z_$][\w$]*)\s*:\s*`
       - deviceStore (método-atalho, com ou sem `async`): `^\s{4}(?:async\s+)?([A-Za-z_$][\w$]*)\s*\(`
         MAIS o mesmo padrão de propriedade-seta (`^\s{4}([A-Za-z_$][\w$]*)\s*:\s*`),
         porque `deviceStore` mistura os dois estilos.
       A indentação de 4 espaços é o que restringe a extração ao NÍVEL do
       objeto retornado, sem capturar funções aninhadas — validar essa
       premissa contra o arquivo real e ajustar o número de espaços se
       necessário, mas manter a âncora de indentação (não usar regex global
       solta, que capturaria helpers internos).
    3. Declarar no topo do arquivo, como constante, a allowlist de assimetrias
       intencionais: `const ASSIMETRIAS_INTENCIONAIS = { "_setDeviceScope": "assinatura difere de propósito (escopo é conceito do aparelho) — REPORT-01 C-20" };`
       — objeto nome→justificativa, nunca uma lista nua.
    4. Assertivas:
       (a) `blocoServer` e `blocoDevice` foram localizados e cada um rendeu
       pelo menos 40 nomes (sanidade da extração — se a regex quebrar por uma
       mudança de formatação, o guardião falha em vez de virar no-op);
       (b) `soNoDevice = [...device].filter(m => !server.has(m) && !(m in ASSIMETRIAS_INTENCIONAIS))` é vazio,
       com mensagem listando os nomes;
       (c) `soNoServer` idem;
       (d) todo nome da allowlist realmente existe em pelo menos um dos dois
       lados (impede a allowlist virar lixo acumulado depois de uma remoção);
       (e) a contagem total de cada lado é igual (`server.size === device.size`
       descontadas as assimetrias declaradas).
    5. Mensagem de falha acionável, no tom do repositório, por exemplo:
       `"`+"`"+`X`+"`"+` existe só em deviceStore. Método novo entra nos DOIS stores no MESMO commit (CLAUDE.md, guardrail de paridade). Se a assimetria for intencional, declare-a em ASSIMETRIAS_INTENCIONAIS com a justificativa."`

    Cabeçalho do arquivo no padrão "guardião" da casa: bloco de comentário
    citando C-20 do REPORT-01, os DOIS incidentes reais que a lacuna causou
    (carteira nativa não sincronizava, F10-20260807-05; `initialBudget` sem
    sync device→servidor, F10-20260809-05) e a nota de que este teste
    COMPLEMENTA os guardiões pontuais existentes (`test_deep_parity.mjs`,
    `test_didatica_parity.mjs`, ...) — nenhum deles é removido.

    NÃO alterar `web/src/persistence.js` neste plano. Se a extração revelar
    uma assimetria REAL hoje (o REPORT-01 diz que não há), parar e reportar em
    vez de "corrigir" o store — isso seria um achado novo, fora do escopo.
  </action>

  <acceptance_criteria>
    - `node web/tests/test_fase3_paridade_stores_generica.mjs` sai 0.
    - O teste é comprovadamente sensível: ao inserir temporariamente um método fictício em um só dos stores, o teste sai não-zero e nomeia o método na saída (executar essa verificação e REVERTER a inserção antes de concluir a task).
    - Cada lado extraiu ≥ 40 nomes de método (a extração não virou no-op silencioso).
    - Nenhum arquivo em `web/src/` foi modificado.
    - Nenhum guardião existente foi apagado ou enfraquecido: `ls web/tests/test_deep_parity.mjs web/tests/test_didatica_parity.mjs` continua listando os dois, com conteúdo inalterado (`git diff --stat web/tests/test_deep_parity.mjs web/tests/test_didatica_parity.mjs` vazio).
  </acceptance_criteria>

  <verify>
    <automated>node web/tests/test_fase3_paridade_stores_generica.mjs</automated>
  </verify>

  <done>Existe um guardião único que compara os conjuntos de métodos dos dois stores e falha em qualquer assimetria não declarada, com allowlist justificada e sensibilidade comprovada.</done>
</task>

<task type="auto">
  <name>Task 2: Guardião estrutural de appMode — leituras diretas não podem crescer (C-19)</name>
  <files>web/tests/test_fase3_appmode_estrutural.mjs</files>

  <read_first>
    - `web/src/App.jsx:7210-7225` — a definição canônica `ctx.operador` e o comentário "Novo código deve ler `ctx.operador`"
    - `web/src/App.jsx` linhas 1624, 1828, 2018, 3188, 4224, 5606, 5756, 6319, 6501, 6862, 7411 — as leituras diretas de hoje (contar antes de escrever a baseline; NÃO alterá-las)
    - `web/src/App.jsx:1834` e `:1897` — as duas ESCRITAS (`saveConfig({ appMode: ... })`), que não são leitura e não entram na contagem
    - `.planning/phases/03-corre-o-cr-tico-alto/03-CONTEXT.md` — decisão **D-02** (fronteira C-19 × C-21): só o teste estrutural, sem migração
    - `.planning/milestones/v1.0-phases/01-auditoria-diagn-stica-consolidada/REPORT-01.md` — seção `#### C-19` (linha 536) e o mapa de recomputação de `appMode` (linhas 486-510)
    - `.planning/codebase/TESTING.md` — "Guardian Tests — Regression Locks" e a house rule de reversão deliberada com nota
    - `web/tests/test_deep_parity.mjs` — padrão estático de referência
  </read_first>

  <action>
    Criar `web/tests/test_fase3_appmode_estrutural.mjs`, Pattern A, lendo
    `web/src/App.jsx` via `readFileSync`.

    Algoritmo:
    1. Percorrer o arquivo linha a linha, descartando linhas cujo `trim()`
       comece por `//` ou `*` (comentários NÃO contam — sem isso o próprio
       cabeçalho do guardião se auto-invalidaria).
    2. Contar as linhas restantes que casem com a regex de LEITURA:
       `/\.config\s*(?:\|\|\s*\{\})?\s*\)?\s*\.appMode|config\s*&&\s*[\w.]*config\.appMode/`
       — ajustar a regex contra o arquivo real até que ela case exatamente com
       as linhas do inventário de leitura acima e com NENHUMA das duas linhas
       de escrita (`saveConfig({ appMode: ... })`). Registrar em comentário a
       lista de números de linha que a regex captura no momento da escrita.
    3. Declarar `const BASELINE_LEITURAS_DIRETAS = <n>;` com o valor real
       apurado, e um bloco de comentário listando cada linha capturada e por
       que ela ainda existe (dívida de C-21, fase 5).
    4. Assertivas:
       (a) `total <= BASELINE_LEITURAS_DIRETAS`, com mensagem:
       `"Leitura nova de data.config.appMode detectada (N > BASELINE). Novo código lê ctx.operador (App.jsx, definição canônica). Se esta leitura for realmente necessária, atualize BASELINE com uma nota explicando por quê — guardião não se apaga (CLAUDE.md)."`;
       (b) se `total < BASELINE`, imprimir aviso explícito instruindo a baixar
       a baseline no mesmo commit (é o que C-21 fará na fase 5) — sem falhar,
       para não travar a melhoria;
       (c) a âncora canônica continua viva: o fonte contém
       `operador: !!(data && data.config && data.config.appMode === "operador")`
       exatamente 1 vez;
       (d) o comentário de convenção continua no arquivo: a string
       `Novo código deve ler` aparece pelo menos 1 vez (a convenção não pode
       ser apagada em silêncio);
       (e) sensibilidade: as duas ESCRITAS conhecidas não são contadas como
       leitura (assertiva sobre a regex aplicada às linhas 1834/1897 lidas
       diretamente do fonte).
    5. Cabeçalho no padrão "guardião": citar C-19, os 3 bugs históricos
       (`docs/auditoria-controle-ordens-parametros.md`) e a CLASSE que o teste
       tranca ("estado que muda num lugar que outro não vê"), além da nota
       explícita de que a migração dos pontos redundantes é C-21 (fase 5) e
       NÃO é feita aqui — decisão D-02.

    NÃO alterar `web/src/App.jsx`. Nenhuma leitura existente é migrada neste
    plano.
  </action>

  <acceptance_criteria>
    - `node web/tests/test_fase3_appmode_estrutural.mjs` sai 0.
    - Sensibilidade comprovada: ao acrescentar temporariamente uma linha `const x = (data.config && data.config.appMode) === "operador";` em `web/src/App.jsx`, o teste sai não-zero (executar e REVERTER a inserção antes de concluir a task; `git diff --stat web/src/App.jsx` deve ficar vazio ao fim).
    - `BASELINE_LEITURAS_DIRETAS` está declarada com o valor real apurado e acompanhada da lista comentada dos pontos capturados.
    - As duas linhas de escrita de `appMode` não são contadas (assertiva (e) presente e verde).
    - Nenhum arquivo em `web/src/` foi modificado ao final da task.
  </acceptance_criteria>

  <verify>
    <automated>node web/tests/test_fase3_appmode_estrutural.mjs && git diff --quiet web/src/App.jsx</automated>
  </verify>

  <done>Uma leitura nova de `data.config.appMode` fora da definição canônica faz a suíte falhar com mensagem acionável; `ctx.operador` e o comentário de convenção estão travados; nenhum ponto existente foi migrado (C-21 segue intacto para a fase 5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| desenvolvedor/agente → código do cliente | mudança que introduz assimetria de store ou leitura divergente de modo atravessa para produção |
| `deviceStore` (localStorage, aparelho) × `serverStore` (servidor) | mesma interface, dois backends — divergência = perda silenciosa de dado do usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering (integridade de dado do usuário) | método adicionado a só um dos dois stores | mitigate | guardião genérico compara os conjuntos de chaves dos dois blocos e falha em qualquer assimetria não declarada; allowlist exige justificativa escrita. |
| T-03-10 | Elevation of Privilege | leitura divergente de `appMode` habilitando execução automática fora do Modo Operador | mitigate | guardião estrutural impede o crescimento de leituras diretas; a trava REAL de execução continua no backend (`agent.py:559-570`, dupla trava escrita+leitura), que este plano não toca — a UI nunca é o único gate. |
| T-03-11 | Repudiation | guardião apagado para "fazer o teste passar" | mitigate | house rule do repositório (CLAUDE.md: "guardiões de teste não se apagam") mais os critérios de aceite deste plano, que exigem `git diff` vazio nos guardiões existentes. |
| T-03-12 | Denial of Service (de desenvolvimento) | regex do guardião quebrar e virar no-op silencioso | mitigate | assertivas de sanidade: extração precisa render ≥ 40 nomes por store e os índices dos blocos precisam ser encontrados, senão o teste falha em vez de passar vazio. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | nenhum pacote novo é instalado neste plano — os testes rodam em `node` puro, sem runner. |
</threat_model>

<verification>
- `node web/tests/test_fase3_paridade_stores_generica.mjs` e `node web/tests/test_fase3_appmode_estrutural.mjs` — ambos verdes.
- `bash scripts/executar.sh --testes` — suíte canônica sem falha nova.
- `git status --porcelain web/src/` vazio — nenhum fonte de produção alterado por este plano.
</verification>

<success_criteria>
- Dois guardiões genéricos existem, passam, e comprovadamente falham quando o padrão-classe é reintroduzido.
- Nenhum guardião existente foi removido ou enfraquecido.
- Nenhum ponto de leitura de `appMode` foi migrado (fronteira D-02 respeitada; C-21 continua sendo trabalho da fase 5).
</success_criteria>

<output>
Create `.planning/phases/03-corre-o-cr-tico-alto/03-03-SUMMARY.md` when done
</output>
