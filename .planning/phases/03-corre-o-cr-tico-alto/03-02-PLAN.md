---
phase: 03-corre-o-cr-tico-alto
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/candle_provider.py
  - server/tests/test_fase3_get_quote_erro_limpo.py
  - web-admin/src/App.jsx
  - web/tests/test_fase3_custos_falha_brapi.mjs
autonomous: true
requirements: [FIX-C12, FIX-C36]

must_haves:
  truths:
    - "Comprar/vender/adicionar um ticker inválido devolve erro limpo (502 'Sem cotacao para X' ou 404 'Ticker não encontrado'), sem URL, sem parâmetro `crumb`, sem nome de classe de exceção"
    - "Nenhuma resposta de erro de cotação carrega HTTP 500 com traceback de provedor"
    - "O painel de custos do portal admin mostra respostas vazias (200 sem vela) como número PRÓPRIO, separado de `erros`"
    - "O painel de custos mostra a taxa de falha (erros + vazios / requisições) e o estado do alerta do provedor primário"
  artifacts:
    - path: "server/app/candle_provider.py"
      provides: "get_quote (singular) com o mesmo tratamento de falha por ticker que get_quotes (plural) já tem"
      contains: "except Exception"
    - path: "server/tests/test_fase3_get_quote_erro_limpo.py"
      provides: "guardião: exceção crua do provedor não sobe de get_quote nem vaza detalhe técnico na rota"
    - path: "web-admin/src/App.jsx"
      provides: "3 linhas novas no card 'Orçamento brapi (ADR-008)': vazios, taxa de falha, alerta do provedor"
      contains: "Respostas vazias"
    - path: "web/tests/test_fase3_custos_falha_brapi.mjs"
      provides: "guardião estático: o painel lê vazios/taxaFalha/alerta e nunca funde vazios em erros"
  key_links:
    - from: "server/app/main.py (rota /api/buy, já existente)"
      to: "candle_provider.get_quote"
      via: "quote.get('price') is None → HTTPException(502, 'Sem cotacao para ' + t)"
      pattern: "Sem cotacao para"
    - from: "web-admin/src/App.jsx"
      to: "/api/agent/status → candles"
      via: "data.candles.vazios / data.candles.taxaFalha / data.candles.alerta"
      pattern: "data\\.candles\\.(vazios|taxaFalha|alerta)"
---

<objective>
Fechar o par de achados que REPORT-01 encadeia como C-12 → C-36: o modo de
falha do provedor de dados deixa de ser feio para o usuário (erro cru com URL
e `crumb` vazando num HTTP 500) e deixa de ser invisível para o admin
(o painel só mostrava `erros`, sendo que o incidente real de 31/07/2026 foi
HTTP 200 com zero velas — que conta como `vazios`, não como `erro`).

Purpose: C-12 é comportamento reproduzido ao vivo em produção (`POST /api/buy
{"t":"XXXXX9"}` devolveu 500 com a URL do Yahoo e o parâmetro `crumb`); C-36 é
a mesma cegueira do incidente de 31/07 reproduzida na camada de apresentação,
com o dado já pronto no payload e nunca lido.
Output: `candle_provider.get_quote` alinhado ao padrão de `get_quotes`;
3 linhas novas no card "Orçamento brapi (ADR-008)" do portal admin; 2
guardiões.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-corre-o-cr-tico-alto/03-CONTEXT.md
@.planning/phases/03-corre-o-cr-tico-alto/03-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Contratos já existentes. Use direto; não reexplore o repositório. -->

`server/app/candle_provider.py` — o padrão CORRETO, já usado por `get_quotes`
(plural) para uma falha por ticker:
```python
out[t] = {"t": t, "price": None, "change": 0,
          "error": "sem cotação (brapi sem orçamento, backup desligado)"}
```

`server/app/candle_provider.py:365-379` — `get_quote` (singular), o ponto de
C-12: `await yahoo.get_quote(ticker)` sem `try/except` nos DOIS ramos
(provedor brapi com fallback, e provedor não-brapi).

`server/app/tickers.py:32-49` — classificador que a rota de watchlist usa:
```python
def validation_outcome(quote, transient_error=False) -> dict:
    # transient_error=True         -> {"status": "unavailable"}   (main.py -> 503)
    # quote["price"] is not None   -> {"status": "ok", ...}
    # caso contrário               -> {"status": "not_found"}     (main.py -> 404)
```

`server/app/main.py:1508-1510` — a mensagem limpa que HOJE nunca é alcançada:
```python
quote = await candle_provider.get_quote(t)
if not quote or quote.get("price") is None:
    raise HTTPException(502, "Sem cotacao para " + t)
```

`server/app/candle_provider.py:98-141` — `snapshot()` JÁ devolve tudo que
C-36 precisa (nenhum backend a fazer):
```python
"requisicoes": req, "erros": erros, "vazios": vazios, "falhas": falhas,
"taxaFalha": round(taxa, 4), "limiarAlerta": _LIMIAR_ERRO,
"alerta": bool(prim["req"] >= 50 and prim["taxaFalha"] > _LIMIAR_ERRO),
```

`web-admin/src/App.jsx:22-36` — componentes a reusar (não criar novos):
```javascript
function Kv({ label, value, tone })          // tone: "positive" | "negative" | "warn" | undefined
function EventoComSerie({ label, value, serie })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: get_quote (singular) devolve preço nulo em vez de propagar exceção crua do provedor</name>
  <files>server/app/candle_provider.py, server/tests/test_fase3_get_quote_erro_limpo.py</files>

  <read_first>
    - `server/app/candle_provider.py:355-404` — `get_quote` (singular, sem try/except) e `get_quotes` (plural, com o padrão correto), lado a lado
    - `server/app/candle_provider.py:330-352` — `QuoteUnavailable = yahoo.QuoteUnavailable` e `_quote_brapi`
    - `server/app/main.py:855-870` — rota de adicionar ticker: `except yahoo.QuoteUnavailable → 503`, `except Exception → 404`, e `tickers.validation_outcome`
    - `server/app/main.py:1501-1518` — rota `/api/buy` e a mensagem `"Sem cotacao para " + t`
    - `server/app/tickers.py:32-49` — `validation_outcome`
    - `.planning/milestones/v1.0-phases/01-auditoria-diagn-stica-consolidada/REPORT-01.md` — seção `#### C-12` (linha 427)
    - `.planning/codebase/TESTING.md` — "Error Testing" e "Mocking" (fetcher injetável / monkeypatch, nunca mock lib)
  </read_first>

  <behavior>
    - `get_quote("XXXXX9")`, com `yahoo.get_quote` levantando `httpx.HTTPStatusError` (ou qualquer `Exception` que não seja `QuoteUnavailable`), devolve `{"t": "XXXXX9", "price": None, "change": 0, "error": <texto genérico>}` — não levanta.
    - O texto de `error` NÃO contém `http`, `://`, `crumb`, `query1.finance.yahoo.com`, nem o nome da classe da exceção original.
    - `QuoteUnavailable` continua sendo LEVANTADA quando é o contrato legítimo (brapi sem token/orçamento e nenhum backup configurado) — os chamadores mapeiam para 503, e esse comportamento não pode mudar.
    - `POST /api/buy` com ticker inexistente devolve HTTP 502 com corpo `{"detail": "Sem cotacao para XXXXX9"}` — nunca 500, nunca URL, nunca `crumb`.
    - Adicionar um ticker inexistente à watchlist devolve 404 com a mensagem amigável já existente (`validation_outcome` classifica `price=None` como `not_found`).
  </behavior>

  <action>
    Em `server/app/candle_provider.py`, na função `get_quote` (singular),
    envolver as chamadas a `yahoo.get_quote(ticker)` — as DUAS, a do ramo
    brapi-com-fallback e a do ramo provedor-não-brapi — em `try/except`
    replicando o padrão de `get_quotes`. Regras exatas:
    - `except QuoteUnavailable: raise` — reerguer sem alterar (contrato de
      indisponibilidade transitória que os chamadores já tratam como 503).
    - `except Exception:  # noqa: BLE001` — devolver
      `{"t": ticker, "price": None, "change": 0, "error": "sem cotação (falha do provedor de dados)"}`.
      NÃO incluir `str(e)` no payload: é exatamente o vazamento de C-12.
    Se a distinção do detalhe técnico for útil para operação, registrar via
    `print(...)` no padrão de log já usado em `agent.py` (`[candle_provider] ...`)
    — servidor, nunca resposta.
    Manter o `raise QuoteUnavailable(...)` explícito do caso "brapi sem
    token/orçamento e nenhum backup configurado" intacto.
    Comentário no padrão da casa citando C-12 e o achado (500 com URL e
    `crumb` reproduzido ao vivo em `POST /api/buy {"t":"XXXXX9"}`), e a
    assimetria histórica com `get_quotes`.

    Criar `server/tests/test_fase3_get_quote_erro_limpo.py`, cabeçalho de
    guardião explicando o achado. Testes:
    (a) `monkeypatch.setattr(yahoo, "get_quote", <coroutine que levanta Exception("... https://query1.finance.yahoo.com/...crumb=rZaCr5Q9WJs ...")>)`
    + `provider_name` em `"yahoo"` → `asyncio.run(candle_provider.get_quote("XXXXX9"))`
    devolve dict com `price is None` e `error` sem `"http"`, sem `"crumb"`;
    (b) mesmo cenário com `provider_name` em `"brapi"` e `_quote_brapi`
    devolvendo `None` → mesmo resultado limpo;
    (c) `yahoo.get_quote` levantando `yahoo.QuoteUnavailable` → `pytest.raises(candle_provider.QuoteUnavailable)`
    (o contrato de 503 não regride);
    (d) teste de rota (padrão `TestClient` + `B3_DB_PATH` monkeypatchado +
    reimport de `app.main`, ver TESTING.md): `POST /api/buy {"t":"XXXXX9","qty":10}`
    com `yahoo.get_quote` levantando exceção crua → `resp.status_code == 502`
    e `"Sem cotacao para" in resp.json()["detail"]` e
    `"crumb" not in resp.text` e `"http" not in resp.text.lower()`;
    (e) `tickers.validation_outcome({"t":"XXXXX9","price":None,"change":0,"error":"..."}, False)["status"] == "not_found"`
    — trava que a rota de watchlist segue devolvendo 404 amigável, não 500.
  </action>

  <acceptance_criteria>
    - `cd server && ./.venv/bin/python -m pytest -q tests/test_fase3_get_quote_erro_limpo.py` sai 0 com ao menos 5 testes.
    - `grep -n "except Exception" server/app/candle_provider.py` retorna ao menos 1 ocorrência dentro do corpo de `get_quote` (verificar pelo número de linha em relação a `async def get_quote`).
    - O payload de erro não interpola a exceção: `grep -n 'str(e)' server/app/candle_provider.py` não retorna nenhuma ocorrência dentro de `get_quote`.
    - `cd server && ./.venv/bin/python -m pytest -q` (suíte inteira) verde — em especial `tests/test_candle_provider.py` (20+ testes) sem regressão.
    - Nenhuma alteração em `server/app/main.py`, `server/app/yahoo.py` ou `server/app/tickers.py` (a mensagem limpa já existe; só passa a ser alcançada).
  </acceptance_criteria>

  <verify>
    <automated>cd server && ./.venv/bin/python -m pytest -q tests/test_fase3_get_quote_erro_limpo.py && ./.venv/bin/python -m pytest -q</automated>
  </verify>

  <done>Falha do provedor no spot singular vira preço nulo com erro genérico; a rota devolve 502 "Sem cotacao para X" (ou 404 na watchlist), sem URL nem `crumb`; `QuoteUnavailable` segue levantada onde é o contrato.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Painel de custos do portal admin passa a mostrar vazios, taxa de falha e alerta do provedor</name>
  <files>web-admin/src/App.jsx, web/tests/test_fase3_custos_falha_brapi.mjs</files>

  <read_first>
    - `web-admin/src/App.jsx:22-36` — `Kv` (tones `positive`/`negative`/`warn`) e `Card`
    - `web-admin/src/App.jsx:166-184` — o card "Orçamento brapi (ADR-008)" inteiro, incluindo os dois `EventoComSerie` (Requisições, Erros) que este task estende
    - `web-admin/src/App.jsx:14-18` — tokens `T` (dark-only), em especial `T.warn` e `T.negative`
    - `server/app/candle_provider.py:98-141` — `snapshot()`, confirmando que `vazios`, `taxaFalha`, `alerta` e `limiarAlerta` já existem no payload
    - `.planning/phases/03-corre-o-cr-tico-alto/03-UI-SPEC.md` — "Copywriting Contract" (as 3 linhas de C-36, texto literal), "Phase-Specific Component Notes → C-36", "Color / Surface B"
    - `.planning/milestones/v1.0-phases/01-auditoria-diagn-stica-consolidada/REPORT-01.md` — seção `#### C-36` (linha 739), inclusive o incidente de 31/07/2026
    - `web/tests/test_editor_texto_byte_exato.mjs` — precedente de teste em `web/tests/` que inspeciona estaticamente um fonte de `web-admin/src/`
  </read_first>

  <behavior>
    - O card "Orçamento brapi (ADR-008)" ganha, imediatamente após o `EventoComSerie` de "Erros (janela 3 dias)", três linhas `Kv`:
      1. `Respostas vazias (200 sem vela, janela 3 dias)` = `data.candles.vazios`, tom `warn` quando `> 0`, sem tom caso contrário.
      2. `Taxa de falha (erros + vazios / requisições, 3 dias)` = `(data.candles.taxaFalha * 100).toFixed(1) + "%"`, tom `negative` quando `data.candles.alerta === true`.
      3. `Alerta do provedor primário` = `"⚠ acima do limiar de " + (data.candles.limiarAlerta * 100).toFixed(0) + "%"` (tom `negative`) quando `data.candles.alerta`, senão `"dentro do normal"` (tom `positive`).
    - `vazios` permanece um número SEPARADO de `erros` — nunca somado nem substituído (é a razão de ser do achado: o incidente de 31/07 lia "0 erros").
    - Nenhum `Card`, componente, cor ou padding novo é introduzido.
  </behavior>

  <action>
    Em `web-admin/src/App.jsx`, dentro do bloco `{data && data.candles?.orcamentoBrapi && ( ... )}`
    do card "Orçamento brapi (ADR-008)", inserir as três linhas `Kv` descritas
    em `<behavior>`, na ordem indicada, logo depois do `EventoComSerie` de
    "Erros (janela 3 dias)". Usar os rótulos LITERAIS do Copywriting Contract
    do UI-SPEC (não parafrasear).
    Guardas de leitura: os três campos vivem em `data.candles` (não em
    `data.candles.orcamentoBrapi`) — usar `data.candles.vazios ?? "—"` e
    proteger a formatação numérica contra `undefined`
    (`typeof data.candles.taxaFalha === "number" ? (…*100).toFixed(1) + "%" : "—"`),
    pelo princípio 4 do CLAUDE.md: sem dado, mostrar o estado correto, nunca
    um número inventado (nem `0.0%`, que aqui leria como "está tudo bem").
    Comentário curto acima do bloco citando C-36 e o incidente de 31/07/2026
    (HTTP 200 com zero velas por 2 horas de pregão), no padrão de
    comentário-com-histórico do repositório.

    Criar `web/tests/test_fase3_custos_falha_brapi.mjs` no Pattern A (checker
    booleano), lendo `web-admin/src/App.jsx` via `readFileSync` (precedente:
    `web/tests/test_editor_texto_byte_exato.mjs`). Assertivas:
    (a) os três rótulos literais aparecem 1x cada;
    (b) `data.candles.vazios`, `data.candles.taxaFalha`, `data.candles.alerta`
    e `data.candles.limiarAlerta` aparecem no fonte;
    (c) `vazios` NÃO é somado a `erros` — nenhuma ocorrência de regex
    `erros\s*\+\s*.*vazios` nem `vazios\s*\+\s*.*erros`;
    (d) o `EventoComSerie` de "Erros (janela 3 dias)" continua existindo
    (as duas métricas coexistem, a nova não substitui a antiga);
    (e) o tom do alerta usa `"negative"` quando `alerta` e `"positive"` caso
    contrário (regex sobre o trecho).
    Cabeçalho de guardião explicando C-36 e por que fundir `vazios` em `erros`
    reintroduziria a cegueira do incidente.
  </action>

  <acceptance_criteria>
    - `node web/tests/test_fase3_custos_falha_brapi.mjs` sai 0 e imprime "TODOS OS TESTES PASSARAM".
    - `cd web-admin && npx vite build` conclui sem erro.
    - `grep -c "Respostas vazias" web-admin/src/App.jsx` retorna 1.
    - `grep -c "Erros (janela 3 dias)" web-admin/src/App.jsx` retorna 1 (a linha antiga não foi removida nem fundida).
    - Nenhum token de cor novo: `grep -c "#" web-admin/src/App.jsx` inalterado em relação ao HEAD anterior (as linhas novas usam só `tone=`).
  </acceptance_criteria>

  <verify>
    <automated>node web/tests/test_fase3_custos_falha_brapi.mjs && cd web-admin && npx vite build</automated>
  </verify>

  <done>O admin que abrir a aba Custos durante um evento idêntico ao de 31/07/2026 vê "Respostas vazias > 0" e uma taxa de falha diferente de zero, em vez de "Erros: 0".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provedor externo (Yahoo/brapi) → resposta HTTP do app | texto de exceção de terceiro cruzando para o corpo da resposta ao usuário |
| backend → portal admin | métricas de observabilidade cruzando para uma superfície RBAC-gated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | `candle_provider.get_quote` → corpo de erro de `/api/buy`, `/api/sell`, watchlist | mitigate | mensagem de erro FIXA no código (`"sem cotação (falha do provedor de dados)"`), sem interpolar `str(e)`; guardião assert que a resposta não contém `crumb`, `http`, nem host do provedor. É a correção literal de C-12. |
| T-03-06 | Denial of Service | ticker inválido em massa contra `/api/buy` | accept | o caminho passa a terminar em 502/404 barato em vez de 500 com traceback; nenhuma cota de rede nova é gasta (o provedor já falhava antes). Rate limiting global fica fora do escopo deste achado. |
| T-03-07 | Repudiation | perda do detalhe técnico ao esconder a exceção | mitigate | detalhe segue registrado no log do servidor (`print("[candle_provider] ...")`), nunca na resposta — diagnóstico preservado sem vazamento. |
| T-03-08 | Information Disclosure | métricas de falha do provedor no portal admin | accept | `/api/agent/status` e `/api/obs/usage` já são RBAC-gated (`require_permission`); as 3 linhas novas leem campos que a rota já devolvia, sem ampliar o payload nem a permissão. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | nenhum pacote novo é instalado neste plano — não há superfície de supply chain para mitigar. |
</threat_model>

<verification>
- `cd server && ./.venv/bin/python -m pytest -q` — suíte backend verde.
- `node web/tests/test_fase3_custos_falha_brapi.mjs` — guardião do painel verde.
- `bash scripts/executar.sh --testes` — suíte canônica sem falha nova.
- `cd web-admin && npx vite build` — build do portal sem erro.
- Verificação humana (fica para `/gsd:verify-phase`): com o servidor local no ar,
  `curl -X POST localhost:8787/api/buy -d '{"t":"XXXXX9","qty":10}'` devolve 502
  com `Sem cotacao para XXXXX9` e nenhum trecho de URL; a aba Custos do portal
  mostra as três linhas novas.
</verification>

<success_criteria>
- Erro de cotação nunca sai como 500 com detalhe técnico do provedor.
- `vazios`, `taxaFalha` e `alerta` visíveis no card "Orçamento brapi (ADR-008)", com `vazios` separado de `erros`.
- Dois guardiões novos verdes; suíte canônica e os dois builds sem falha nova.
</success_criteria>

<output>
Create `.planning/phases/03-corre-o-cr-tico-alto/03-02-SUMMARY.md` when done
</output>
