---
phase: 03-corre-o-cr-tico-alto
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - server/app/main.py
  - server/app/plan.py
  - server/tests/test_fase3_gate_plano.py
autonomous: true
requirements: [FIX-C31, FIX-C32]

must_haves:
  truths:
    - "Os 3 call sites de gate (`can_add_ticker` em /api/watchlist, `can_analyze` nas duas rotas de análise) resolvem o plano REAL do usuário logado via `current_plan(user)`, não o fallback global `ACTIVE_PLAN`"
    - "Uma conta com `users.plan = 'pro'` recebe `PLAN_PRO` nos hooks de gate; uma conta anônima (sem sessão) continua recebendo `ACTIVE_PLAN`"
    - "Uma falha ao ler o usuário no banco não derruba a rota nem libera mais do que o fallback: cai em `ACTIVE_PLAN` (free), nunca num plano superior"
    - "Existe UM único ponto de decisão de gate de análise por requisição; `plan.can_analyze` não é mais invocado em paralelo a `metering.check` a partir de dois lugares independentes"
    - "`plan.py` declara por escrito que o contador de cota é `metering` e que `can_analyze` jamais mantém contador próprio"
    - "Nenhum comportamento visível ao usuário muda: `PLAN_FREE` e `PLAN_PRO` continuam com todos os limites `None` (ilimitado) — D-03: corrige a arquitetura sem ativar limite comercial"
  artifacts:
    - path: "server/app/main.py"
      provides: "resolução do plano real nos 3 call sites + gate único de análise"
      contains: "current_plan"
    - path: "server/app/plan.py"
      provides: "contrato escrito: metering é o contador único; limites seguem None"
      contains: "metering"
    - path: "server/tests/test_fase3_gate_plano.py"
      provides: "guardião: plano real resolvido, gate único, limites intocados"
  key_links:
    - from: "server/app/main.py"
      to: "server/app/plan.py"
      via: "plan.current_plan(usuario) passado como plan= aos hooks"
      pattern: "plan\\.current_plan\\("
    - from: "server/app/main.py"
      to: "server/app/db.py"
      via: "db.get_user_by_id(_conn, scope) para resolver o dict do usuário a partir do escopo"
      pattern: "get_user_by_id"
---

<objective>
Corrigir a ARQUITETURA do gating comercial sem ativar nenhum limite: os hooks
de plano passam a resolver o plano REAL da conta (`current_plan(user)`, hoje
código órfão — zero chamadas fora da própria definição), e o gate de análise
deixa de existir em duplicidade com `metering` dentro da mesma requisição.

Purpose: hoje `main.py:870,1223,1370` chamam os hooks sem `plan=`, caindo no
fallback global `ACTIVE_PLAN` — quando `PLAN_FREE` ganhar um número, TODAS as
contas (inclusive `'pro'`) cairiam no mesmo limite (REPORT-01 C-31). E
`plan.can_analyze` e `metering.check` coexistem na MESMA chamada de
`/api/analyze/{ticker}` e `/api/technical/analyze/{ticker}`, dois mecanismos
independentes de contagem prontos para divergir no dia da ativação
(REPORT-01 C-32).
Output: os 3 call sites resolvendo o plano real; um gate único de análise;
contrato de contagem escrito em `plan.py`; guardião de backend.

**Fronteira de escopo (D-03 do CONTEXT.md):** `PLAN_FREE` e `PLAN_PRO`
continuam com TODOS os limites `None`. Nada de `set_user_plan`, nada de
validação de recibo de loja, nada de contagem mensal real (isso é C-33, fase
5). Comportamento visível ao usuário: ZERO mudança. O que muda é que o
mecanismo passa a estar correto por construção quando o ADR-010 for ligado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-corre-o-cr-tico-alto/03-CONTEXT.md
@CLAUDE.md
@docs/adr/010-planos-e-cap-gratuito.md

<interfaces>
<!-- Contratos já existentes. Use direto; não reexplore o repositório. -->

`server/app/plan.py` — desenhado desde o início para aceitar `plan=`/`user=`:
```python
ACTIVE_PLAN = PLAN_FREE
PLANOS_POR_ID = {"free": PLAN_FREE, "pro": PLAN_PRO}

def current_plan(user: Optional[dict] = None) -> dict:
    if not user:
        return ACTIVE_PLAN
    return PLANOS_POR_ID.get(user.get("plan") or "free", PLAN_FREE)   # valor inválido -> PLAN_FREE

def can_add_ticker(current_count: int, plan: Optional[dict] = None) -> tuple:   # (permitido, motivo)
def can_analyze(used_this_month: int, plan: Optional[dict] = None) -> tuple:    # (permitido, motivo)
```

`server/app/main.py:94-102` — `current_scope` devolve um `user_id` (str) ou
`None`, NUNCA o dict do usuário:
```python
def current_scope(authorization: Optional[str] = Header(default=None)) -> Optional[str]:
```

`server/app/db.py` — resolução do dict (a coluna `plan` já está no SELECT):
```python
_USER_COLS = "id, email, provider, provider_sub, name, created_at, plan"
def get_user_by_id(conn, user_id: str)   # -> dict | None
```

Os 3 call sites atuais:
```python
# main.py:870  (rota de adicionar ticker à watchlist)
allowed, reason = plan.can_add_ticker(len(store.get(_conn, "watchlist", user_id=scope)))
# main.py:1223 (rota POST /api/technical/analyze/{ticker})
allowed, reason = plan.can_analyze(0)
# main.py:1370 (rota POST /api/analyze/{ticker})
allowed, reason = plan.can_analyze(0)  # FUTURO: passar a contagem do mes do usuario
```

`server/app/main.py:356-380` — `_ai_apply_managed(scope, config, custo=1)`, o
OUTRO gate da mesma requisição: chama `metering.check(...)` e levanta 402.

`server/app/metering.py` — o contador REAL, por usuário, janela DIÁRIA
(não mensal — a divergência de janela é parte do achado C-32):
```python
def check(conn, user_id, *, quota, rate_per_min, custo=1, cap_global=None) -> tuple  # registra rate limit, NÃO consome cota
def consume(conn, user_id, *, custo=1) -> int                                        # conta só no SUCESSO
def snapshot(conn, user_id, quota) -> dict
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Os 3 call sites de gate resolvem o plano real do usuário (C-31)</name>
  <files>server/app/main.py, server/tests/test_fase3_gate_plano.py</files>

  <read_first>
    - `server/app/plan.py` inteiro (86 linhas) — `current_plan`, `can_add_ticker`, `can_analyze`, `ACTIVE_PLAN`
    - `server/app/main.py:94-102` — `current_scope` (devolve id, não dict)
    - `server/app/main.py:855-875` — call site de `can_add_ticker`
    - `server/app/main.py:1218-1226` — call site de `can_analyze` em `/api/technical/analyze/{ticker}`
    - `server/app/main.py:1362-1372` — call site de `can_analyze` em `/api/analyze/{ticker}`
    - `server/app/db.py:313-322` — `_USER_COLS` e `get_user_by_id`
    - `.planning/phases/03-corre-o-cr-tico-alto/03-CONTEXT.md` — decisão **D-03** e a seção "Gating (C-31, C-32)" dos canonical_refs
    - `.planning/milestones/v1.0-phases/01-auditoria-diagn-stica-consolidada/REPORT-01.md` — seção `#### C-31` (linha 678)
    - `docs/adr/010-planos-e-cap-gratuito.md` — decisão 5 (os 3 passos técnicos de ativação)
    - `.planning/codebase/TESTING.md` — padrão de teste de rota com `TestClient` + `B3_DB_PATH`
  </read_first>

  <behavior>
    - Com sessão de um usuário cujo `users.plan == "pro"`, os hooks recebem `PLAN_PRO` (`plano["id"] == "pro"`).
    - Com sessão de um usuário cujo `users.plan == "free"` (ou `NULL`), os hooks recebem `PLAN_FREE`.
    - Sem sessão (escopo anônimo, `scope is None`), os hooks recebem `ACTIVE_PLAN` — comportamento de hoje, inalterado.
    - Com `users.plan` num valor desconhecido (ex.: `"enterprise"`, dado corrompido), os hooks recebem `PLAN_FREE` — o plano MENOS privilegiado, nunca um superior (fail-closed).
    - Se `db.get_user_by_id` levantar exceção, a rota não quebra: o plano resolvido é `ACTIVE_PLAN` e a requisição segue.
    - Todas as rotas continuam devolvendo exatamente o mesmo status/corpo de hoje, porque todos os limites seguem `None`.
  </behavior>

  <action>
    Em `server/app/main.py`, criar UM helper de módulo perto de `current_scope`:
    ```
    def _plano_do_escopo(scope):
    ```
    que devolve `plan.current_plan(db.get_user_by_id(_conn, scope) if scope else None)`,
    com todo o corpo dentro de `try/except Exception` que devolve
    `plan.ACTIVE_PLAN` (comentário: o eixo comercial nunca pode derrubar uma
    rota de dado; e a degradação é para o plano MENOS privilegiado, nunca para
    um superior). Docstring citando C-31 e o fato de `current_plan` ter sido
    código órfão desde o ADR-013.

    Substituir os 3 call sites por chamadas que passam o plano resolvido:
    - `main.py:870`: `plan.can_add_ticker(len(...), plan=_plano_do_escopo(scope))`
    - `main.py:1223` e `main.py:1370`: passar `plan=_plano_do_escopo(scope)`
      (a forma final destes dois é fechada pela Task 2, que os unifica — nesta
      task basta o plano real chegar; NÃO deixar o `0` hardcoded "para depois":
      ele continua exatamente como está, porque a contagem real é C-33, fase
      5, e o comentário `# FUTURO: passar a contagem do mes do usuario` deve
      ser mantido com a referência atualizada para `C-33 (fase 5)`).
    Manter o comentário "GANCHO FREEMIUM (hoje sempre permite)" — segue
    verdadeiro, os limites continuam `None`.

    NÃO tocar em `plan.py::PLAN_FREE`/`PLAN_PRO` (limites seguem `None`), nem
    em `db.set_user_plan` (nunca chamada, fora de escopo — é ativação
    comercial).

    Criar `server/tests/test_fase3_gate_plano.py` (cabeçalho de guardião
    citando C-31/C-32 e a decisão D-03). Testes desta task:
    (a) `plan.current_plan({"plan": "pro"})["id"] == "pro"`;
    (b) `plan.current_plan({"plan": "enterprise"})["id"] == "free"` (fail-closed);
    (c) `plan.current_plan(None) is plan.ACTIVE_PLAN`;
    (d) teste de rota com `TestClient`: criar usuário, gravar
    `db.set_user_plan(conn, uid, "pro")` direto no banco de teste (uso
    programático legítimo — não é o fluxo de recibo), autenticar e chamar a
    rota de watchlist com `plan.can_add_ticker` monkeypatchado por um espião
    que registra o kwarg `plan` recebido → o espião viu `{"id": "pro"}`;
    (e) mesma rota SEM `Authorization` → o espião viu `plan.ACTIVE_PLAN`;
    (f) `db.get_user_by_id` monkeypatchado para levantar → a rota responde o
    mesmo status de sucesso de antes e o espião viu `plan.ACTIVE_PLAN`;
    (g) guardião de fronteira D-03: `plan.PLAN_FREE["max_watchlist"] is None`
    e `plan.PLAN_FREE["max_analyses_per_month"] is None` — se algum dia
    alguém ativar um número aqui sem passar pelo ADR-010, este teste grita.
  </action>

  <acceptance_criteria>
    - `cd server && ./.venv/bin/python -m pytest -q tests/test_fase3_gate_plano.py` sai 0.
    - `grep -v '^\s*#' server/app/main.py | grep -c 'plan\.current_plan('` ≥ 1 (o órfão deixou de ser órfão).
    - Os 3 call sites passam `plan=`: `grep -n 'can_add_ticker(\|can_analyze(' server/app/main.py` mostra `plan=` em todas as ocorrências de chamada.
    - `grep -n 'set_user_plan' server/app/main.py` retorna 0 ocorrências (nenhuma ativação comercial introduzida).
    - `git diff server/app/plan.py` não altera `PLAN_FREE`/`PLAN_PRO`/`ACTIVE_PLAN` nesta task.
    - `cd server && ./.venv/bin/python -m pytest -q` verde (suíte inteira, sem regressão).
  </acceptance_criteria>

  <verify>
    <automated>cd server && ./.venv/bin/python -m pytest -q tests/test_fase3_gate_plano.py && ./.venv/bin/python -m pytest -q</automated>
  </verify>

  <done>Os 3 hooks de gate recebem o plano real da conta logada, degradam para `ACTIVE_PLAN` em ausência de sessão ou falha de banco, nunca para um plano superior, e todos os limites seguem `None`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate único de análise — `can_analyze` deixa de ser concorrente de `metering.check` (C-32)</name>
  <files>server/app/main.py, server/app/plan.py, server/tests/test_fase3_gate_plano.py</files>

  <read_first>
    - `server/app/main.py:356-390` — `_ai_apply_managed`, onde `metering.check` é chamado e o 402 é levantado
    - `server/app/main.py:1218-1240` — rota `/api/technical/analyze/{ticker}`, ordem atual: `can_analyze(0)` → `_ai_apply_managed`
    - `server/app/main.py:1362-1385` — rota `/api/analyze/{ticker}`, mesma ordem
    - `server/app/metering.py:1-135` — docstring do módulo (fluxo check → consume), `check`, `consume`, `snapshot`; observar que a janela é DIÁRIA
    - `server/app/plan.py:62-81` — `can_add_ticker`/`can_analyze` e seus docstrings
    - `.planning/milestones/v1.0-phases/01-auditoria-diagn-stica-consolidada/REPORT-01.md` — seções `#### C-32` (linha 686) e `#### C-33` (linha 694, para saber o que NÃO fazer aqui)
    - `.planning/phases/03-corre-o-cr-tico-alto/03-CONTEXT.md` — decisão **D-03**
    - `docs/adr/010-planos-e-cap-gratuito.md`
  </read_first>

  <behavior>
    - Existe exatamente UMA função em `main.py` que decide se uma análise pode prosseguir; as duas rotas de análise a chamam e nenhuma delas invoca `plan.can_analyze` diretamente.
    - Essa função aplica, nesta ordem e num único ponto: (1) o gate de PLANO (`plan.can_analyze` com o plano real do usuário) e (2) o gate de COTA/rate da IA gerenciada (`metering`, via `_ai_apply_managed`).
    - Só existe UM contador de uso de IA no sistema: o de `metering`. `plan.can_analyze` não lê, escreve nem mantém contador algum.
    - Uma negativa de plano e uma negativa de cota produzem 402 pelo MESMO caminho de código, com a mensagem específica de cada gate preservada.
    - Nenhum status/corpo de resposta muda em relação a hoje, já que `max_analyses_per_month is None`.
    - `plan.py` documenta, em texto, que quando o limite mensal for ativado (ADR-010), a contagem TEM de vir do ledger de `metering` e nunca de um contador paralelo.
  </behavior>

  <action>
    Em `server/app/main.py`, criar `_gate_analise(scope, config, custo: int = 1)`
    — o ponto ÚNICO de decisão — colocada imediatamente antes/junto de
    `_ai_apply_managed` para deixar a adjacência óbvia. Contrato:
    1. resolve `plano = _plano_do_escopo(scope)` (helper da Task 1);
    2. chama `allowed, reason = plan.can_analyze(0, plan=plano)` e levanta
       `HTTPException(402, reason)` se negado — o `0` permanece porque a
       contagem real do mês é C-33 (fase 5); manter o comentário
       `# C-33 (fase 5): contagem real do mês; o ledger é o de metering, nunca um segundo contador`;
    3. chama `config, consume = _ai_apply_managed(scope, config, custo=custo)`
       (que já contém `metering.check` e seu próprio 402);
    4. devolve `(config, consume)` — a mesma tupla que as rotas já consomem.
    Docstring citando C-32: "dois gates coexistiam na mesma requisição
    (`plan.can_analyze` e `metering.check`), cada um com sua própria noção de
    janela — plano é MENSAL, metering é DIÁRIO. Aqui eles passam a ser um
    único ponto de decisão, com precedência explícita: o gate de PLANO decide
    se a conta tem direito ao recurso; o de METERING decide se ainda há cota
    da chave do servidor hoje. O CONTADOR é sempre o de `metering`."

    Nas duas rotas de análise, substituir o par
    `allowed, reason = plan.can_analyze(0)` + `raise 402` + `config, _consume_ai = _ai_apply_managed(scope, config)`
    por uma única chamada a `_gate_analise(...)`, preservando o `custo=` que
    cada rota já usa (atenção: `/api/scan/deep` usa `custo>1` — se ela também
    chamar `_ai_apply_managed`, mantê-la usando `_ai_apply_managed` direto ou
    passar pelo gate com o mesmo `custo`; NÃO alterar a semântica de custo de
    nenhuma rota). Preservar a ordem relativa em que `modo` é capturado ANTES
    do managed em `/api/technical/analyze` (comentário "FASE 8B (B4)"): se a
    unificação mudar essa ordem, a config gerenciada sobrescreve o `appMode` —
    regressão conhecida e documentada no próprio arquivo.

    Em `server/app/plan.py`, acrescentar ao docstring do MÓDULO (e ao de
    `can_analyze`) o contrato de contagem: `metering.py` é o contador único de
    uso de IA; `can_analyze` é o gate de TIER comercial e nunca mantém
    contagem própria; quando o ADR-010 ligar `max_analyses_per_month`, o valor
    passado tem de derivar do ledger de `metering` (C-33, fase 5). Nenhuma
    mudança de código executável em `plan.py` — só o contrato escrito.

    Acrescentar a `server/tests/test_fase3_gate_plano.py`:
    (h) guardião estático de gate único: ler `server/app/main.py`, descartar
    linhas de comentário (`grep -v '^\s*#'` equivalente em Python) e assertar
    que `plan.can_analyze(` aparece EXATAMENTE 1 vez no arquivo inteiro
    (dentro de `_gate_analise`);
    (i) as duas rotas de análise chamam `_gate_analise` (regex sobre o fonte,
    contando ≥ 2 ocorrências de `_gate_analise(`);
    (j) teste de rota: `metering.check` monkeypatchado para devolver
    `(False, "cota estourada")` → a rota de análise devolve 402 com essa
    mensagem;
    (k) teste de rota: `plan.can_analyze` monkeypatchado para devolver
    `(False, "limite do plano free")` → a rota devolve 402 com essa mensagem
    e `metering.check` NÃO foi chamado (espião com contador em 0) — prova de
    precedência determinística, não de dois gates correndo em paralelo;
    (l) `plan.py` contém a palavra `metering` no texto do módulo (o contrato
    escrito não pode ser apagado sem quebrar o guardião).
  </action>

  <acceptance_criteria>
    - `cd server && ./.venv/bin/python -m pytest -q tests/test_fase3_gate_plano.py` sai 0 com ao menos 12 testes (7 da Task 1 + 5 desta).
    - `grep -v '^\s*#' server/app/main.py | grep -c 'plan\.can_analyze('` retorna exatamente 1.
    - `grep -v '^\s*#' server/app/main.py | grep -c '_gate_analise('` ≥ 3 (definição + as duas rotas).
    - `grep -v '^\s*#' server/app/plan.py | grep -c 'metering'` ≥ 1.
    - `cd server && ./.venv/bin/python -m pytest -q` verde — em especial `tests/test_qa42_finops.py` e os testes de `metering`/`managed`, que travam a semântica de `custo`.
    - Nenhum limite ativado: `PLAN_FREE["max_analyses_per_month"] is None` e `PLAN_FREE["max_watchlist"] is None` seguem verdadeiros (assertiva (g) da Task 1 continua verde).
  </acceptance_criteria>

  <verify>
    <automated>cd server && ./.venv/bin/python -m pytest -q tests/test_fase3_gate_plano.py && ./.venv/bin/python -m pytest -q</automated>
  </verify>

  <done>Há um único ponto de decisão de gate de análise por requisição, com precedência explícita e contador único (`metering`); `plan.py` declara o contrato por escrito; nenhum limite comercial foi ativado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sessão (Bearer token) → escopo → plano comercial | valor persistido `users.plan` atravessa para uma decisão de autorização-adjacente |
| requisição do usuário → chave de LLM do servidor (IA gerenciada) | gate de cota protege gasto real com terceiro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Elevation of Privilege | `users.plan` com valor inesperado/corrompido resolvendo para um plano superior | mitigate | `current_plan` já usa `PLANOS_POR_ID.get(..., PLAN_FREE)` — fail-closed para o plano MENOS privilegiado; guardião (b) da Task 1 trava esse comportamento com um valor inválido explícito. |
| T-03-14 | Denial of Service | exceção de `db.get_user_by_id` no caminho quente das rotas de dado/análise | mitigate | `_plano_do_escopo` inteiramente dentro de `try/except Exception → ACTIVE_PLAN`; guardião (f) exercita o caminho de falha e exige resposta de sucesso. |
| T-03-15 | Elevation of Privilege | bypass do gate de cota por caminho alternativo (rota que não passe pelo gate único) | mitigate | guardião estático (h)/(i): `plan.can_analyze(` aparece exatamente 1 vez no `main.py` e as duas rotas chamam `_gate_analise`; qualquer rota nova que copie o padrão antigo quebra o guardião. |
| T-03-16 | Spoofing | cliente afirmando plano no corpo da requisição | mitigate | o plano é resolvido SEMPRE server-side a partir do Bearer token → `users.plan`; nenhum campo de corpo/parâmetro de plano é lido. Nada no cliente influencia `_plano_do_escopo`. |
| T-03-17 | Repudiation | ativação acidental de limite comercial sem passar pelo ADR-010 | mitigate | guardião (g) assere `max_watchlist is None` e `max_analyses_per_month is None`; ligar um número exige alterar o guardião com nota, o que torna a decisão visível no diff. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | nenhum pacote novo é instalado neste plano. |
</threat_model>

<verification>
- `cd server && ./.venv/bin/python -m pytest -q` — suíte backend verde (≈970 testes).
- `bash scripts/executar.sh --testes` — suíte canônica sem falha nova.
- Verificação de não-regressão comportamental: com o servidor local, `POST /api/analyze/{ticker}` e `POST /api/technical/analyze/{ticker}` respondem o mesmo status/corpo de antes do plano (nenhum 402 novo).
- `grep -rn "set_user_plan" server/app/main.py` vazio — nenhuma ativação comercial introduzida.
</verification>

<success_criteria>
- `current_plan(user)` deixa de ser código órfão e alimenta os 3 hooks.
- Um único ponto de decisão de gate de análise, com contador único (`metering`).
- Zero mudança visível ao usuário; `PLAN_FREE`/`PLAN_PRO` intocados.
- Guardião de backend novo verde; suíte canônica sem regressão.
</success_criteria>

<output>
Create `.planning/phases/03-corre-o-cr-tico-alto/03-04-SUMMARY.md` when done
</output>
