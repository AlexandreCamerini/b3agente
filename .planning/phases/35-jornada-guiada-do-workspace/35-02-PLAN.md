---
phase: 35-jornada-guiada-do-workspace
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - web/src/opcoes/OpcoesScreen.jsx
  - web/src/opcoes/SecaoAnalisar.jsx
  - web/src/opcoes/SecaoComparar.jsx
  - web/src/opcoes/SecaoVigias.jsx
  - web/tests/test_opcoes_jornada_ui.mjs
autonomous: true
requirements: [JORN-02, JORN-01]

must_haves:
  truths:
    - "D-03: existe um `BOTAO_PRIMARIO` com a geometria EXATA do `BOTAO` neutro (mesmo minHeight/padding/borderRadius/fontWeight/fontSize), variando só `border: none`, `background: T.accent` e `color: T.onAccent` [JORN-02]"
    - "D-04: exatamente 3 botões do app recebem `BOTAO_PRIMARIO` — 'Ler no serviço de opções', 'Montar estrutura' e 'Ver possibilidades'; nenhum botão de `SecaoSetups.jsx`/`WorkspaceHeader.jsx`/qualquer outro arquivo o recebe [JORN-02]"
    - "D-05: o estado desabilitado do botão preenchido usa `opacity: 0.55` (não o 0.45 do neutro) e o botão NUNCA some — `display: none` continua proibido"
    - "D-07: nenhum texto sobre o preenchimento de accent usa literal `#fff` nem `color-mix` — o único valor é `T.onAccent`, o token que o design system já calibrou (4,94:1 a 8,09:1 nas 4 combinações tema × modo)"
    - "D-06: 'Montar estrutura' e 'Ver possibilidades' NÃO somem nem desabilitam depois do clique — uma marca neutra `✓` em `T.textMuted` aparece entre o botão e o resultado, só quando existe resultado com conteúdo [JORN-01]"
    - "Toda referência `T.<token>` em `web/src/opcoes/*.jsx` está declarada no array `TOKENS` do próprio arquivo — token esquecido vira `undefined` silencioso, não erro"
    - "As cópias locais de cada constante nova são byte a byte idênticas entre arquivos (duplicação aceita pela regra do diretório, divergência não)"
  artifacts:
    - path: "web/src/opcoes/OpcoesScreen.jsx"
      provides: "BOTAO_PRIMARIO + CUSTO_NO_BOTAO_PRIMARIO e o CTA da leitura paga preenchido"
      contains: "BOTAO_PRIMARIO"
    - path: "web/src/opcoes/SecaoAnalisar.jsx"
      provides: "CTA 'Montar estrutura' preenchido + marca de resultado"
      contains: "MARCA_RESULTADO"
    - path: "web/src/opcoes/SecaoComparar.jsx"
      provides: "CTA 'Ver possibilidades' preenchido + marca de resultado"
      contains: "desabilitadoPrimario"
    - path: "web/tests/test_opcoes_jornada_ui.mjs"
      provides: "Varredura de tokens por diretório, fronteira dos 3 CTAs, identidade das cópias locais, regra de cor da marca"
      contains: "onAccent"
  key_links:
    - from: "web/src/opcoes/*.jsx (BOTAO_PRIMARIO)"
      to: "var(--on-accent) / var(--accent)"
      via: "array TOKENS local + Object.fromEntries(VARKEY)"
      pattern: "\"onAccent\""
    - from: "SecaoAnalisar.jsx / SecaoComparar.jsx (MARCA_RESULTADO)"
      to: "T.textMuted"
      via: "estilo de metadado, nunca T.positive/T.negative (reservados a direção financeira)"
      pattern: "MARCA_RESULTADO"
---

<objective>
Dar hierarquia visual real ao próximo passo: os 3 CTAs que movem a jornada
("Ler no serviço de opções", "Montar estrutura", "Ver possibilidades") deixam
de ter o mesmo estilo neutro de qualquer botão exploratório e passam a um
preenchimento sólido de accent com texto em `T.onAccent`. Junto, a marca de
resultado (`✓` discreto) para os dois CTAs que continuam re-clicáveis depois
do primeiro uso.

Purpose: JORN-02 (o botão que revela as estruturas é proeminente e o propósito
é evidente sem explicação externa — resolvido por ESTILO, a redação não muda)
e JORN-01 (a marca de resultado fecha o laço de progresso: o usuário vê que
aquela etapa produziu algo).
Output: 4 constantes de estilo novas (espelho local por arquivo, como manda a
regra do diretório), 3 botões restilizados, 2 marcas de resultado, 1 varredura
de token que fecha uma classe inteira de defeito silencioso.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-jornada-guiada-do-workspace/35-CONTEXT.md
@.planning/phases/35-jornada-guiada-do-workspace/35-UI-SPEC.md
@.planning/phases/35-jornada-guiada-do-workspace/35-PATTERNS.md
@.planning/phases/35-jornada-guiada-do-workspace/35-01-SUMMARY.md
@CLAUDE.md
</context>

<decisao_do_planner>
**Onde as constantes novas moram: espelho local por arquivo, NÃO centralizadas
em `uiOpcoes.jsx`.** `35-CONTEXT.md` deixou a chamada para o planner; a
decisão é seguir a regra que o próprio `uiOpcoes.jsx` declara no seu
docstring (linhas 18-22): "token de tema, constante de estilo e formatador de
UMA linha podem ser espelho declarado local em cada arquivo de
`web/src/opcoes/` — mas qualquer coisa com RAMIFICAÇÃO de lógica vive numa
fonte só, aqui". As 4 constantes desta fase são objetos de estilo puros e um
helper de uma linha, sem ramificação; `BOTAO`/`CAIXA`/`CAMPO`/`AJUDA` já são
espelhos locais nos mesmos 3 arquivos. Centralizar agora seria abrir a
primeira exceção a uma regra escrita, sem o gatilho que a própria regra usa
(2+ consumidores de LÓGICA, como foi com `Linha`/`ErroDoMcp`).

O risco real da duplicação — divergir na primeira manutenção feita só numa
cópia — não é aceito de graça: vira asserção de identidade no guardião
(Task 3). Centralizar `BOTAO`/`CAIXA`/`CAMPO`/`AJUDA` continua sendo dívida
nomeada em `35-CONTEXT.md` `<deferred>`, fora desta fase.

**Declarar só onde usa.** Nenhum arquivo ganha constante que não consome (isso
viraria aviso de lint e, pior, cópia morta que ninguém mantém):
- `OpcoesScreen.jsx`: `BOTAO_PRIMARIO`, `CUSTO_NO_BOTAO_PRIMARIO`
  (o CTA da leitura não tem estado desabilitado hoje — não declarar
  `desabilitadoPrimario` aqui)
- `SecaoAnalisar.jsx`: `BOTAO_PRIMARIO`, `CUSTO_NO_BOTAO_PRIMARIO`,
  `desabilitadoPrimario`, `MARCA_RESULTADO`
- `SecaoComparar.jsx`: `BOTAO_PRIMARIO`, `desabilitadoPrimario`,
  `MARCA_RESULTADO` (o botão "Ver possibilidades" NÃO tem custo dentro dele —
  o custo é linha própria ANTES do botão, `SecaoComparar.jsx:128-130`;
  `35-PATTERNS.md` é explícito que inventar um subtexto de custo ali seria
  escopo novo)
</decisao_do_planner>

<achado_do_planejamento>
**Defeito pré-existente que a varredura de tokens desta fase expõe, e que
precisa ser resolvido dentro deste plano porque o guardião novo não passa com
ele presente:** `SecaoVigias.jsx:90` renderiza
`background: selecionado ? T.accentTint10 : T.bgPanel`, mas `"bgPanel"` NÃO
está no array `TOKENS` do arquivo (linha 17). `T.bgPanel` é, portanto,
`undefined` — o chip de vigia NÃO selecionado renderiza sem fundo nenhum, em
silêncio, desde a Fase 33 (33-01). É exatamente a classe de defeito que o
`T.onAccent` desta fase produziria se o array não fosse estendido.

Medido, não suposto: varredura de `web/src/opcoes/*.jsx` cruzando cada
`T.<token>` referenciado contra o `TOKENS` do próprio arquivo — este é o
ÚNICO caso do diretório.

Tratamento: corrigir (acrescentar `"bgPanel"` ao array, uma palavra) e
registrar como fold-in no SUMMARY, nomeando a consequência visual: chips de
vigia não selecionados passam a ter o fundo de painel que o código sempre
pretendeu. Entra no roteiro do checkpoint ao vivo (plano 35-03) para o Alex
ver. Se ele preferir manter a aparência atual, a reversão é a mesma palavra e
o guardião ganha exceção DATADA E NOMEADA — nunca a regra afrouxada.
</achado_do_planejamento>

<interfaces>
<!-- Contrato fechado. Valores literais já corrigidos pelo UI-SPEC contra os
     hex reais de App.jsx:95-213 — não re-derivar, não "melhorar". -->

```js
const BOTAO_PRIMARIO = {
  minHeight: "44px", padding: "10px 14px", borderRadius: "11px",
  border: "none", background: T.accent, color: T.onAccent,
  fontWeight: 700, fontSize: "13px",
};
const CUSTO_NO_BOTAO_PRIMARIO = {
  display: "block", fontSize: "11px", fontWeight: 600,
  color: T.onAccent, marginTop: "3px",
};
const desabilitadoPrimario = (cond) => (cond ? { opacity: 0.55, cursor: "not-allowed" } : null);
const MARCA_RESULTADO = {
  display: "flex", alignItems: "center", gap: "5px",
  fontSize: "11px", color: T.textMuted, marginTop: "6px",
};
```

Extensões obrigatórias do array `TOKENS` (o `T` é `Object.fromEntries` sobre
ele; token fora do array vira `undefined` em silêncio):
- `OpcoesScreen.jsx:92-93` → acrescentar `"onAccent"`
- `SecaoAnalisar.jsx:42-43` → acrescentar `"onAccent"`
- `SecaoComparar.jsx:47` → acrescentar `"accent"` E `"onAccent"` (o arquivo
  não tem nenhum dos dois hoje)
- `SecaoVigias.jsx:17` → acrescentar `"bgPanel"` (fold-in, ver
  `<achado_do_planejamento>`)

Contraste já medido e travado por guardião pré-existente
(`web/tests/test_brand_book_v2_tokens.mjs:169-170` afirma `onAccent` sobre
`accent` ≥ 4,5:1 nas 4 paletas): Dark·Estudo 5,72:1 · Light·Estudo 4,94:1 ·
Dark·Operador 8,09:1 · Light·Operador 4,94:1. O literal `#fff` reprovaria
(2,10:1–2,90:1 nos dois temas escuros) e o `color-mix(#fff 72%)` reprovaria em
todas.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: BOTAO_PRIMARIO no CTA da leitura paga (OpcoesScreen.jsx)</name>
  <files>web/src/opcoes/OpcoesScreen.jsx, web/src/opcoes/SecaoVigias.jsx</files>
  <read_first>
    - web/src/opcoes/OpcoesScreen.jsx — linhas 89-94 (`VARKEY`/`TOKENS`/`T`),
      125-145 (`BOTAO` com o comentário do alvo de toque de 44px e
      `CUSTO_NO_BOTAO` com o comentário que explica por que o custo fica
      DENTRO do botão), 540-563 (`blocoLeituraDoServico` já com o Kicker de
      "Passo 1 de 2" do plano 35-01)
    - web/src/opcoes/CuradoriaEstruturas.jsx — linhas 264-271 (precedente em
      produção do botão preenchido: geometria + `opacity: 0.55`; NÃO copiar o
      `color: "#fff"` dele — o UI-SPEC provou que reprova AA em 2 das 4
      combinações e que aquela linha não tem comentário justificando)
    - web/src/opcoes/SecaoVigias.jsx — linhas 17 e 90 (o defeito de token do
      `<achado_do_planejamento>`)
    - .planning/phases/35-jornada-guiada-do-workspace/35-UI-SPEC.md — seções
      "D-03/D-04/D-05" e "D-07" (tabelas de contraste computado)
  </read_first>
  <action>
    Em `OpcoesScreen.jsx`:

    1. Acrescentar `"onAccent"` ao array `TOKENS` (linhas 92-93), no fim da
       lista. Comentário de uma linha dizendo por que: `T.onAccent` é o token
       calibrado para texto SOBRE preenchimento de accent, e token fora deste
       array vira `undefined` calado.
    2. Declarar `BOTAO_PRIMARIO` e `CUSTO_NO_BOTAO_PRIMARIO` com os valores
       literais de `<interfaces>`, imediatamente ABAIXO de `BOTAO` (linha 131)
       e de `CUSTO_NO_BOTAO` (linha 145) respectivamente — cada um grudado no
       neutro que espelha, para que a próxima manutenção veja os dois juntos.
       Comentário obrigatório acima de `BOTAO_PRIMARIO` registrando: Fase 35,
       D-03; é a MESMA geometria do `BOTAO` (nenhum valor de espaçamento
       novo), a extensão do "reserved-for" do accent aprovada pelo Alex
       (accent passa a ter duas formas: tint+borda = pill ativa;
       preenchido = CTA do próximo passo); e que `color` é `T.onAccent`, nunca
       `#fff` — com o número do contraste (2,90:1 e 2,10:1 nos temas escuros)
       que torna o literal branco reprovado.
    3. No botão de `blocoLeituraDoServico` (linha 552-560): trocar
       `...BOTAO` por `...BOTAO_PRIMARIO` e `style={CUSTO_NO_BOTAO}` por
       `style={CUSTO_NO_BOTAO_PRIMARIO}`. Nada mais muda: `onClick`,
       `width: "100%"`, `marginTop: "12px"`, o texto `cp.opcoesLerNoServico` e
       a linha de custo `(cp.opcoesCustoChamadas || ...)(CUSTO_DA_ACAO.leitura)`
       ficam byte a byte como estão (o custo declarado é cruzado com o
       `_cap_check` do backend por `test_opcoes_custo_declarado.mjs`).

    Em `SecaoVigias.jsx` (fold-in do `<achado_do_planejamento>`):

    4. Acrescentar `"bgPanel"` ao array `TOKENS` da linha 17, com comentário
       datado: Fase 35 — `T.bgPanel` era referenciado na linha 90 sem estar no
       array desde a Fase 33 (33-01), resolvendo para `undefined`; o chip de
       vigia não selecionado ficava sem fundo. Achado pela varredura de tokens
       desta fase, não por relato.

    Proibições, com razão:
    - Não tocar em `BOTAO`, `CUSTO_NO_BOTAO`, `CAIXA`, `AJUDA` — outros
      controles dependem deles e esta fase não refatora o que já existe
      (`35-CONTEXT.md` `<deferred>`).
    - Não aplicar `BOTAO_PRIMARIO` a nenhum outro botão do arquivo ("Ver a
      cadeia", "Ver as operáveis", botões de carteira vazia, `SubAbaOperar`):
      D-04 é uma lista fechada de 3, e accent em tudo é accent em nada.
    - Nenhum literal `#fff`, `#ffffff`, `white` ou `color-mix(` entra neste
      arquivo.
  </action>
  <acceptance_criteria>
    - `grep -c '"onAccent"' web/src/opcoes/OpcoesScreen.jsx` retorna 1 e `grep -c '"bgPanel"' web/src/opcoes/SecaoVigias.jsx` retorna 1
    - `node -e 'const s=require("fs").readFileSync("web/src/opcoes/OpcoesScreen.jsx","utf8");const m=s.match(/const BOTAO_PRIMARIO = \{[\s\S]*?\};/)[0];console.log(m);process.exit(/border: "none"/.test(m)&&/background: T\.accent/.test(m)&&/color: T\.onAccent/.test(m)&&/minHeight: "44px"/.test(m)&&!/#fff|color-mix/.test(m)?0:1)'` sai 0
    - A fatia do botão de `blocoLeituraDoServico` contém `...BOTAO_PRIMARIO` e `CUSTO_NO_BOTAO_PRIMARIO`, e NÃO contém `...BOTAO,` nem `style={CUSTO_NO_BOTAO}`
    - `grep -c "BOTAO_PRIMARIO" web/src/opcoes/OpcoesScreen.jsx` retorna 2 (declaração + 1 uso)
    - `grep -rn "#fff\|color-mix" web/src/opcoes/OpcoesScreen.jsx web/src/opcoes/SecaoVigias.jsx` não retorna nada
    - `node web/tests/test_opcoes_custo_declarado.mjs && node web/tests/test_opcoes_vigias_ui.mjs && node web/tests/test_opcoes_hub_workspace_ui.mjs` verdes
    - `cd web && npx vite build` verde
  </acceptance_criteria>
  <verify>
    <automated>cd web && npx vite build && cd .. && node web/tests/test_opcoes_custo_declarado.mjs && node web/tests/test_opcoes_vigias_ui.mjs && node web/tests/test_opcoes_hub_workspace_ui.mjs && node web/tests/test_brand_book_v2_tokens.mjs</automated>
  </verify>
  <done>O CTA da leitura paga é o único botão preenchido do arquivo, com texto em `T.onAccent`; o token do chip de vigia deixou de ser `undefined`.</done>
</task>

<task type="auto">
  <name>Task 2: os dois CTAs das seções + marca de resultado re-clicável</name>
  <files>web/src/opcoes/SecaoAnalisar.jsx, web/src/opcoes/SecaoComparar.jsx</files>
  <read_first>
    - web/src/opcoes/SecaoAnalisar.jsx — linhas 40-45 (`TOKENS`), 74-90
      (`AJUDA`/`BOTAO`/`desabilitado`/`CUSTO_NO_BOTAO`, com o comentário
      "Botão desabilitado FICA VISÍVEL, em vez de sumir"), 313-345 (o botão
      "Montar estrutura" e a cascata `carregando → erro → vazio → dados` logo
      abaixo)
    - web/src/opcoes/SecaoComparar.jsx — linhas 45-50 (`TOKENS`), 66-80
      (`AJUDA`/`BOTAO`/`desabilitado`), 120-175 (o bloco de custo ANTES do
      botão, o botão "Ver possibilidades" e a cascata de `possibilidades`)
    - web/src/opcoes/PropostaLastreada.jsx — linha 183 (`const cor = isCall ?
      T.positive : T.negative;` e o comentário que reserva esse par à direção
      financeira — a razão de `MARCA_RESULTADO` ser `T.textMuted`)
    - .planning/phases/35-jornada-guiada-do-workspace/35-PATTERNS.md — seções
      `SecaoAnalisar.jsx` e `SecaoComparar.jsx` (inclusive a divergência
      explícita: o botão de Comparar NÃO tem custo dentro dele)
  </read_first>
  <action>
    Em `SecaoAnalisar.jsx`:

    1. Acrescentar `"onAccent"` ao array `TOKENS` (linhas 42-43).
    2. Declarar `BOTAO_PRIMARIO` (abaixo de `BOTAO`),
       `desabilitadoPrimario` (abaixo de `desabilitado`) e
       `CUSTO_NO_BOTAO_PRIMARIO` (abaixo de `CUSTO_NO_BOTAO`) com os valores
       literais de `<interfaces>` — byte a byte iguais aos de
       `OpcoesScreen.jsx`. Comentário acima de `desabilitadoPrimario`: D-05, o
       0,55 (e não o 0,45 do neutro) porque um preenchido a 0,45 ainda lê como
       vívido sobre fundo escuro; precedente em produção em
       `CuradoriaEstruturas.jsx:268`. A regra de nunca sumir vale igual — só
       `opacity`/`cursor` mudam, nunca `display: none`.
    3. Declarar `MARCA_RESULTADO` com os valores de `<interfaces>`. Comentário:
       D-06 — `T.textMuted`, NUNCA `T.positive`/`T.negative`, porque esse par
       é reservado a direção financeira (`PropostaLastreada.jsx:183`) e reusá-lo
       para "sucesso de UI" colidiria com "alta"/"baixa" no mesmo campo visual.
    4. No botão "Montar estrutura" (linhas 320-329): `...BOTAO` →
       `...BOTAO_PRIMARIO`; `...desabilitado(!temTese || !loteOk)` →
       `...desabilitadoPrimario(!temTese || !loteOk)`; `style={CUSTO_NO_BOTAO}`
       → `style={CUSTO_NO_BOTAO_PRIMARIO}`. O `disabled={!temTese || !loteOk}`
       e o conteúdo dos spans não mudam.
    5. Imediatamente depois do `</button>` (linha 329) e ANTES do
       `<div style={{ marginTop: "10px" }}>` que abre a cascata, acrescentar a
       marca: ternário sobre `proposta.dados && (proposta.dados.estruturas || []).length`
       renderizando um `<div style={MARCA_RESULTADO}>` com um `<span>✓</span>`
       e um `<span>{cp.opcoesEstruturaMontada || "Estrutura montada"}</span>`.
       O gate é resultado COM conteúdo — nunca `carregando`, nunca `erro`,
       nunca vazio-com-motivo: afirmar "montada" sobre uma resposta sem
       estrutura seria afirmar resultado que não existe (princípio 4 do
       `CLAUDE.md`).

    Em `SecaoComparar.jsx`:

    6. Acrescentar `"accent"` E `"onAccent"` ao array `TOKENS` (linha 47) — o
       arquivo não tem nenhum dos dois hoje, e sem os dois o botão preenchido
       ficaria sem fundo E sem cor de texto.
    7. Declarar `BOTAO_PRIMARIO`, `desabilitadoPrimario` e `MARCA_RESULTADO`,
       byte a byte iguais aos de `SecaoAnalisar.jsx`, nas mesmas posições
       relativas (cada um abaixo do neutro que espelha). NÃO declarar
       `CUSTO_NO_BOTAO_PRIMARIO` aqui: o custo deste fluxo é linha própria
       ANTES do botão (linhas 128-130), não subtexto dentro dele — acrescentar
       um subtexto de custo seria escopo novo, e `35-PATTERNS.md` proíbe
       explicitamente.
    8. No botão "Ver possibilidades" (linhas 152-161): `...BOTAO` →
       `...BOTAO_PRIMARIO` e `...desabilitado(...)` →
       `...desabilitadoPrimario(...)`. O conteúdo do botão (uma linha só,
       `cp.opcoesVerPossibilidades`) não muda.
    9. Imediatamente depois do `</button>` (linha 161), ainda DENTRO do
       fragmento do ramo `consultados.length > 0` (para a marca ficar grudada
       no botão a que se refere), acrescentar o mesmo padrão da marca:
       ternário sobre
       `possibilidades.dados && (possibilidades.dados.possibilidades || []).length`
       com `✓` e `{cp.opcoesPossibilidadesVistas || "Possibilidades carregadas"}`.

    Proibições, com razão:
    - Nenhum dos dois botões pode passar a `disabled` depois do clique nem
      sumir: trocar tese/lote/alvo e montar de novo é uso legítimo (comparar
      cenários) — D-06 é explícito.
    - Nenhum literal `#fff`/`white`/`color-mix(` nos dois arquivos.
    - Nenhum outro botão destes arquivos ("Ver a cadeia", "Ver as operáveis")
      recebe `BOTAO_PRIMARIO` — são exploração opcional, D-04.
    - Não renomear nem reescrever `desabilitado` (0,45): ele continua servindo
      os botões neutros dos mesmos arquivos.
  </action>
  <acceptance_criteria>
    - `grep -c "BOTAO_PRIMARIO" web/src/opcoes/SecaoAnalisar.jsx` retorna 2 e `grep -c "BOTAO_PRIMARIO" web/src/opcoes/SecaoComparar.jsx` retorna 2 (declaração + 1 uso em cada)
    - `grep -c "desabilitadoPrimario" web/src/opcoes/SecaoAnalisar.jsx` e o mesmo em `SecaoComparar.jsx` retornam 2 cada; `grep -c "desabilitado(" ` em cada arquivo continua ≥ 1 (o helper neutro sobreviveu)
    - `grep -c "MARCA_RESULTADO" web/src/opcoes/SecaoAnalisar.jsx` e em `SecaoComparar.jsx` retornam 2 cada
    - `grep -n "opcoesEstruturaMontada" web/src/opcoes/SecaoAnalisar.jsx` e `grep -n "opcoesPossibilidadesVistas" web/src/opcoes/SecaoComparar.jsx` retornam 1 linha cada, e em ambas a condição do ternário contém `.length` (gate de resultado com conteúdo, não de `.dados` apenas)
    - `grep -rn "#fff\|color-mix\|display: \"none\"" web/src/opcoes/SecaoAnalisar.jsx web/src/opcoes/SecaoComparar.jsx` não retorna nada
    - `grep -c '"onAccent"' web/src/opcoes/SecaoAnalisar.jsx` retorna 1; em `SecaoComparar.jsx`, `grep -c '"accent", "onAccent"\|"onAccent"' ` confirma os dois tokens no array
    - `node web/tests/test_opcoes_analisar_ui.mjs && node web/tests/test_opcoes_custo_declarado.mjs && node web/tests/test_opcoes_proposta_ui.mjs` verdes
    - `cd web && npx vite build` verde
  </acceptance_criteria>
  <verify>
    <automated>cd web && npx vite build && cd .. && node web/tests/test_opcoes_analisar_ui.mjs && node web/tests/test_opcoes_custo_declarado.mjs && node web/tests/test_opcoes_proposta_ui.mjs && node web/tests/test_opcoes_hub_workspace_ui.mjs</automated>
  </verify>
  <done>"Montar estrutura" e "Ver possibilidades" são preenchidos, continuam re-clicáveis, desabilitam a 0,55 sem sumir, e cada um ganha uma marca neutra quando (e só quando) produziu resultado com conteúdo.</done>
</task>

<task type="auto">
  <name>Task 3: guardião — varredura de tokens, fronteira dos 3 CTAs, identidade das cópias</name>
  <files>web/tests/test_opcoes_jornada_ui.mjs</files>
  <read_first>
    - web/tests/test_opcoes_jornada_ui.mjs — o arquivo criado no 35-01, para
      acrescentar no mesmo estilo (mesmo `ok`, mesmo `semComentario`, mesma
      disciplina de sanidade antes de cada bloco derivado)
    - web/tests/test_opcoes_custo_declarado.mjs — o padrão de varredura de
      diretório com `readdirSync` (a lição do 33-03: lista fixa de arquivos
      para de cobrir no dia em que nasce um arquivo novo)
    - web/tests/test_brand_book_v2_tokens.mjs — linhas 160-175 (a asserção
      pré-existente de contraste `onAccent` × `accent` nas 4 paletas; o
      guardião novo NÃO a reimplementa, apenas depende dela)
    - web/src/opcoes/*.jsx — os 4 arquivos tocados nas Tasks 1-2
  </read_first>
  <action>
    Estender `web/tests/test_opcoes_jornada_ui.mjs` com as asserções abaixo,
    sempre sobre a fonte sem comentários e sempre com sanidade antes de
    qualquer bloco derivado:

    10. **Varredura de tokens por DIRETÓRIO (fecha a classe inteira):** para
        cada `.jsx` de `web/src/opcoes/`, extrair o array `TOKENS` e o conjunto
        de `T.<nome>` referenciados; reprovar se algum token referenciado não
        estiver no array do próprio arquivo. Sanidade obrigatória: a varredura
        tem de achar pelo menos 8 arquivos com `TOKENS` e pelo menos 1
        referência `T.` em cada um (senão a asserção passa por vácuo). Este é
        o bloco que teria pego o `T.bgPanel` de `SecaoVigias.jsx` em 2026-09-20
        — citar isso no comentário do bloco, com data.
    11. **Fronteira D-04 (exatamente 3 CTAs):** `BOTAO_PRIMARIO` só pode
        aparecer em `OpcoesScreen.jsx`, `SecaoAnalisar.jsx` e
        `SecaoComparar.jsx` — varredura de diretório com allowlist desses 3
        nomes; qualquer outro arquivo de `web/src/opcoes/` que o mencione
        reprova. E, em cada um dos 3, exatamente 1 USO (2 ocorrências totais,
        contando a declaração).
    12. **Os 3 usos são os 3 botões certos:** a fatia do `<button ...>` que
        contém `cp.opcoesLerNoServico` usa `BOTAO_PRIMARIO`; idem para
        `cp.opcoesMontarEstrutura` e `cp.opcoesVerPossibilidades`. E a prova
        pelo avesso: as fatias dos botões de `cp.opcoesVerCadeia` e
        `cp.opcoesVerOperaveis` NÃO usam `BOTAO_PRIMARIO` (continuam neutros).
    13. **Identidade das cópias locais (o preço da duplicação aceita):**
        normalizar espaços e comparar, entre arquivos, o corpo de
        `BOTAO_PRIMARIO` (3 cópias), `CUSTO_NO_BOTAO_PRIMARIO` (2),
        `desabilitadoPrimario` (2) e `MARCA_RESULTADO` (2). Divergência
        reprova, nomeando quais arquivos diferem.
    14. **Cópia morta proibida:** nenhum arquivo declara uma dessas 4
        constantes sem usá-la (contagem de ocorrências ≥ 2 sempre que a
        declaração existe).
    15. **D-07, o par proibido:** nenhum dos 3 arquivos contém `#fff`,
        `#ffffff`, `"white"` ou `color-mix(` — o UI-SPEC computou que os dois
        valores propostos originalmente reprovam AA (branco literal: 2,90:1
        Dark·Estudo e 2,10:1 Dark·Operador; `color-mix(#fff 72%)`: 2,20:1 a
        3,38:1). O único valor permitido sobre preenchimento de accent é
        `T.onAccent`. Comentário do bloco cita os números para que ninguém
        "simplifique" o token de volta a um hex.
    16. **D-05:** o corpo de `desabilitadoPrimario` contém `0.55` e
        `cursor: "not-allowed"`, e NÃO contém `display`. Nos dois botões com
        estado desabilitado, o spread usado é `desabilitadoPrimario(`, não
        `desabilitado(`.
    17. **D-06, regra de cor e gate da marca:** as fatias de `MARCA_RESULTADO`
        não contêm `T.positive` nem `T.negative`; e cada render da marca é
        gateado por uma condição que contém `.length` e NÃO contém `.erro` nem
        `.carregando`.
    18. **JORN-03 parity, prova negativa estrutural:** `SecaoSetups.jsx` e
        `WorkspaceHeader.jsx` continuam sem `BOTAO_PRIMARIO`, sem `T.accent` e
        sem `T.onAccent` (a paridade das 3 pills é o Kicker compartilhado do
        plano 35-01, não botão novo dentro delas).

    Fechar com PROVA NEGATIVA REAL, por injeção, mínimo 4 — executada, não
    descrita, com saídas no SUMMARY:
    (a) trocar `color: T.onAccent` por `color: "#fff"` em um dos 3 →
        asserção 15 reprova;
    (b) remover `"onAccent"` do `TOKENS` de `SecaoComparar.jsx` →
        asserção 10 reprova;
    (c) aplicar `BOTAO_PRIMARIO` a um botão de `SecaoSetups.jsx` →
        asserções 11 e 18 reprovam;
    (d) trocar `desabilitadoPrimario(` por `desabilitado(` no botão de
        `SecaoAnalisar.jsx` → asserção 16 reprova.
    Reverter cada injeção com `git checkout -- <arquivo>` e confirmar
    `git diff` limpo antes de commitar.
  </action>
  <acceptance_criteria>
    - `node web/tests/test_opcoes_jornada_ui.mjs` sai 0 com os blocos 10-18 acrescentados, todos com sanidade verde (nenhuma fatia vazia, varredura achando ≥ 8 arquivos)
    - As 4 provas negativas foram EXECUTADAS: cada injeção produziu exit 1 nomeando a asserção; `git diff` pós-reversão vazio — saídas coladas no SUMMARY
    - `node web/tests/test_brand_book_v2_tokens.mjs` verde (evidência automática de contraste `onAccent` × `accent` ≥ 4,5:1 nas 4 combinações tema × modo — a metade automatizável do risco aberto de D-07; a metade visual fica no checkpoint do 35-03)
    - `bash scripts/executar.sh --testes` — suíte canônica COMPLETA, na baseline: 2923 pytest / 0 failed, `.mjs` só com a falha ambiental pré-existente `test_ios_assets.mjs`
    - `cd web && npx vite build` verde
    - `git diff web/src/App.jsx` vazio
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <done>A hierarquia do CTA está travada por guardião em 9 eixos (token, fronteira, identidade, cor proibida, estado desabilitado, gate da marca), com 4 provas negativas reais e a suíte canônica na baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhuma nova | Constantes de estilo e troca de estilo em 3 botões já existentes. Nenhuma rota, nenhum dado, nenhuma chamada nova. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04 | Information disclosure (texto ilegível = informação perdida) | texto sobre preenchimento de accent | mitigate | Asserção 15 proíbe `#fff`/`color-mix`; `test_brand_book_v2_tokens.mjs:169-170` já trava `onAccent`×`accent` ≥ 4,5:1 nas 4 paletas; checkpoint visual no 35-03. |
| T-35-05 | Tampering (token silenciosamente `undefined`) | arrays `TOKENS` locais | mitigate | Asserção 10, varredura por diretório — foi ela que expôs `T.bgPanel` em `SecaoVigias.jsx`. |
| T-35-06 | Repudiation (afirmar resultado inexistente) | marca `✓` de resultado | mitigate | Asserção 17: a marca só renderiza sob condição com `.length`, nunca sob `.erro`/`.carregando` (princípio 4 do CLAUDE.md). |
| T-35-07 | Elevation of privilege (accent vazando para fora da lista fechada) | `BOTAO_PRIMARIO` | mitigate | Asserções 11/18: allowlist de 3 arquivos, e prova negativa em `SecaoSetups.jsx`/`WorkspaceHeader.jsx`. |
| T-35-SC | Tampering (supply chain) | npm/pip install | accept | Zero pacote novo — nenhum install nesta fase. |
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` (pytest + todos os `web/tests/*.mjs`) na baseline
- `cd web && npx vite build` verde
- `node web/tests/test_opcoes_jornada_ui.mjs` verde com 4 provas negativas executadas e revertidas
- `node web/tests/test_brand_book_v2_tokens.mjs` verde (contraste nas 4 combinações)
- `git diff web/src/App.jsx` vazio
</verification>

<success_criteria>
- Os 3 CTAs da jornada são preenchidos com accent e texto `T.onAccent`; nenhum outro botão do app é
- Nenhum literal `#fff`/`color-mix` sobre preenchimento de accent em `web/src/opcoes/`
- Os dois CTAs re-clicáveis continuam clicáveis depois do resultado e ganham marca neutra `✓`
- Toda referência `T.<token>` do diretório está declarada no array do próprio arquivo
- As cópias locais das 4 constantes são idênticas entre arquivos
</success_criteria>

<output>
Create `.planning/phases/35-jornada-guiada-do-workspace/35-02-SUMMARY.md` when done
</output>
