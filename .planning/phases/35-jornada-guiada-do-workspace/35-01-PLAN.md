---
phase: 35-jornada-guiada-do-workspace
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/copy.js
  - web/src/opcoes/OpcoesScreen.jsx
  - web/tests/test_opcoes_jornada_ui.mjs
autonomous: true
requirements: [JORN-01, JORN-03]

must_haves:
  truths:
    - "D-01: o bloco de convite da leitura paga anuncia o estágio ('LEITURA DO ATIVO · Passo 1 de 2') e o carril de pills anuncia o seu ('O QUE FAZER · Passo 2 de 2') — dois estágios, nunca três [JORN-01]"
    - "D-08: com a leitura já feita, uma linha de transição acima do carril diz o que fazer em seguida ('Leitura concluída — escolha Analisar ou Comparar.') [JORN-01]"
    - "D-08: Analisar e Comparar NUNCA são numeradas entre si — nenhum '2 de 3'/'3 de 3' nasce neste plano (princípio 5 do CLAUDE.md: não inventar sequência que o produto não tem)"
    - "D-02: com a pill 'Setups salvos' ativa, o convite de leitura paga NÃO aparece — quem só quer ver/criar setup salvo deixa de ser barrado por um portão de que não precisa [JORN-03]"
    - "D-06: assim que a leitura existe, o lugar do convite passa a exibir 'LEITURA DO ATIVO · leitura já feita' (metadado em T.textMuted, sem celebração e sem cor de direção financeira)"
    - "O rótulo do estágio 2 aparece igual nas três pills — a paridade de JORN-03 é estrutural (Kicker compartilhado acima do carril), não um botão novo dentro de SecaoSetups [JORN-03]"
    - "Nenhuma chamada paga nova nasce deste plano: as linhas acrescentadas leem `temLeitura`/`abaWorkspace`, estado já computado, e nenhuma contém disparador (NAV-05 preservado)"
  artifacts:
    - path: "web/src/copy.js"
      provides: "7 chaves novas de jornada nos DOIS modos (estudo/operador)"
      contains: "opcoesPasso1de2"
    - path: "web/src/opcoes/OpcoesScreen.jsx"
      provides: "Rótulos de estágio, linha de transição, gate D-02 e ramo D-06"
      contains: "opcoesEscolhaTitulo"
    - path: "web/tests/test_opcoes_jornada_ui.mjs"
      provides: "Guardião da jornada (estágios, gate por pill, ramo de leitura já feita, anti-inércia do guardião da Fase 34)"
      contains: "abaWorkspace !== \"setups\""
  key_links:
    - from: "web/src/opcoes/OpcoesScreen.jsx (convite de leitura)"
      to: "abaWorkspace"
      via: "gate de render por pill ativa, sem estado novo"
      pattern: "podePedirLeitura && abaWorkspace !== \"setups\""
    - from: "web/src/opcoes/OpcoesScreen.jsx (linha de transição)"
      to: "temLeitura"
      via: "ternário sobre estado já computado na linha 419"
      pattern: "temLeitura \\? <div style=\\{AJUDA\\}>"
    - from: "web/src/opcoes/OpcoesScreen.jsx"
      to: "web/src/copy.js"
      via: "cp.<chave> || \"default PT-BR\" (convenção de fallback do diretório)"
      pattern: "cp\\.opcoes(Passo1de2|Passo2de2|EscolhaTitulo|LeituraJaFeita|LeituraConcluidaAjuda)"
---

<objective>
Dar ao workspace um indicador de progresso honesto e corrigir o portão que
bloqueia quem não precisa dele. Dois estágios reais são nomeados — a porta
(leitura paga) e o carril de pills —, uma linha de transição aparece quando a
leitura já foi feita, o lugar do convite vira metadado ("leitura já feita") em
vez de buraco, e o convite deixa de aparecer sobre a pill "Setups salvos".

Purpose: JORN-01 (o usuário vê em que etapa está e o que falta) e JORN-03 (as
três pills recebem a MESMA moldura de progresso — paridade estrutural, pelo
Kicker compartilhado acima do carril, não por botão novo dentro de
`SecaoSetups.jsx`).
Output: 7 chaves de copy nos dois modos, 4 edições cirúrgicas em
`OpcoesScreen.jsx`, 1 guardião novo. Zero chamada nova, zero estado novo,
zero mudança de arquitetura de render.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-jornada-guiada-do-workspace/35-CONTEXT.md
@.planning/phases/35-jornada-guiada-do-workspace/35-UI-SPEC.md
@.planning/phases/35-jornada-guiada-do-workspace/35-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
<!-- Contrato fechado. Nada abaixo deve ser re-derivado lendo o código. -->

Estado LIDO (nenhum criado):
- `temLeitura` — `OpcoesScreen.jsx:419`, `!!(behavior && !semCandles)`
- `podePedirLeitura` — `OpcoesScreen.jsx:538-539`
- `abaWorkspace` — `OpcoesScreen.jsx:260`, valores `"analisar" | "comparar" | "setups"`

Estilos LIDOS (nenhum criado neste plano):
- `AJUDA` — `OpcoesScreen.jsx:151`
- `Kicker` — importado de `./uiOpcoes.jsx`, aceita `children` como está

Sequência final dentro de `workspaceTopo` (`OpcoesScreen.jsx:695-707`):

    WorkspaceHeader
    LastroDoAtivo
    LeituraInterna
    podePedirLeitura && abaWorkspace !== "setups"  → blocoLeituraDoServico   (D-02)
    temLeitura                                     → Kicker "· leitura já feita" (D-06)
    Kicker "O QUE FAZER · Passo 2 de 2"            (sempre, sem gate)        (D-08)
    temLeitura                                     → linha AJUDA de transição (D-08)
    workspacePillRow                               (último, posição intocada)

Chaves novas (mesmo valor nos dois modos — nenhuma delas fala de execução ou
dinheiro, então divergir de tom seria invenção; o conjunto de chaves `opcoes*`
tem de ser IGUAL nos dois ramos, asserção já existente em
`test_opcoes_analisar_ui.mjs`):

    opcoesPasso1de2            = "Passo 1 de 2"
    opcoesPasso2de2            = "Passo 2 de 2"
    opcoesEscolhaTitulo        = "O QUE FAZER"
    opcoesLeituraJaFeita       = "leitura já feita"
    opcoesLeituraConcluidaAjuda= "Leitura concluída — escolha Analisar ou Comparar."
    opcoesEstruturaMontada     = "Estrutura montada"
    opcoesPossibilidadesVistas = "Possibilidades carregadas"

As duas últimas são consumidas no plano 35-02 (`MARCA_RESULTADO` em
`SecaoAnalisar.jsx`/`SecaoComparar.jsx`); nascem aqui para que o vocabulário
inteiro da fase entre num commit só e o 35-02 não precise tocar `copy.js`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 7 chaves de jornada em copy.js, nos dois modos</name>
  <files>web/src/copy.js</files>
  <read_first>
    - web/src/copy.js — linhas 98-113 (bloco `opcoesLeituraTitulo`/
      `opcoesLerNoServico`/`opcoesLeituraConvite` do ramo `COPY.estudo`, com o
      comentário de Fase 27 que explica POR QUE o convite existe), 270-276
      (`opcoesMontarEstrutura`/`opcoesVerPossibilidades`, estudo), 386-395
      (bloco de Fase 34: `opcoesVoltarAoHub` + as 3 chaves de pill, estudo),
      e os três blocos espelho do ramo `COPY.operador`: 814-822, 924-929,
      ~1029-1035
    - .planning/phases/35-jornada-guiada-do-workspace/35-PATTERNS.md — seção
      `web/src/copy.js` (tabela de pontos de inserção e defaults exatos)
    - .planning/phases/35-jornada-guiada-do-workspace/35-UI-SPEC.md — seção
      "Copywriting Contract"
  </read_first>
  <action>
    Acrescentar as 7 chaves listadas em `<interfaces>` aos DOIS ramos
    (`COPY.estudo` e `COPY.operador`), com o MESMO valor em cada ramo.

    Pontos de inserção (agrupar por vizinhança semântica, como o arquivo já
    faz — não empilhar as 7 num bloco solto no fim do objeto):
    - `opcoesPasso1de2` e `opcoesLeituraJaFeita`: logo abaixo de
      `opcoesLeituraConvite` (estudo ~linha 110, operador ~linha 820), no
      mesmo bloco da leitura paga.
    - `opcoesEscolhaTitulo`, `opcoesPasso2de2` e
      `opcoesLeituraConcluidaAjuda`: junto do bloco de Fase 34 das pills
      (estudo ~394, operador ~1033), logo depois de
      `opcoesAbaSetupsSalvos`.
    - `opcoesEstruturaMontada` e `opcoesPossibilidadesVistas`: ao lado de
      `opcoesMontarEstrutura`/`opcoesVerPossibilidades` (estudo ~274,
      operador ~927) — são a marca de resultado de cada um desses dois
      botões, consumida no plano 35-02.

    Comentário obrigatório acima de cada grupo novo, no ramo `estudo`,
    dizendo (a) Fase 35 e a decisão que originou (`D-01`/`D-06`/`D-08`), e
    (b) a razão de o valor ser IDÊNTICO nos dois modos: são rótulos de
    progresso/metadado, categoria que já é neutra neste arquivo
    (`opcoesPregaoRotulo`, `opcoesFonteRotulo`, `opcoesConsultadoEmRotulo`
    são iguais nos dois ramos) — divergir de tom aqui seria inventar voz onde
    não há ação nem dinheiro. No ramo `operador`, comentário curto
    cruzando-referência ao do ramo estudo, padrão já usado no arquivo (ver
    linha 817-818).

    Proibições, com razão:
    - Nada de numerar Analisar/Comparar ("Passo 2 de 3", "3 de 3", "etapa
      final"). Os dois jobs são independentes dentro do MESMO estágio; um
      número entre eles afirmaria uma ordem que o produto não tem — princípio
      5 do `CLAUDE.md`, a razão explícita de D-01/D-08.
    - Nada de texto de celebração ("Pronto!", "Muito bem", emoji de festa) em
      `opcoesLeituraJaFeita`: é metadado de estado, e o Kicker que o exibe é
      `T.textMuted`.
    - Não editar nenhuma das chaves já existentes: `opcoesLeituraTitulo`,
      `opcoesLerNoServico`, `opcoesMontarEstrutura`, `opcoesVerPossibilidades`
      e as 3 chaves de pill ficam byte a byte como estão (o UI-SPEC é
      explícito: JORN-02 é resolvido por ESTILO, não por redação nova).
  </action>
  <acceptance_criteria>
    - `grep -c "opcoesPasso1de2\|opcoesPasso2de2\|opcoesEscolhaTitulo\|opcoesLeituraJaFeita\|opcoesLeituraConcluidaAjuda\|opcoesEstruturaMontada\|opcoesPossibilidadesVistas" web/src/copy.js` retorna 14 (7 chaves × 2 ramos)
    - `node -e "import('./web/src/copy.js').then(({COPY})=>{const ks=['opcoesPasso1de2','opcoesPasso2de2','opcoesEscolhaTitulo','opcoesLeituraJaFeita','opcoesLeituraConcluidaAjuda','opcoesEstruturaMontada','opcoesPossibilidadesVistas'];const bad=ks.filter(k=>!COPY.estudo[k]||!COPY.operador[k]||COPY.estudo[k]!==COPY.operador[k]);console.log(bad.length?'FALHOU '+bad:'ok 7/7 iguais nos dois modos');process.exit(bad.length?1:0)})"` sai 0
    - `node web/tests/test_opcoes_analisar_ui.mjs` verde — a asserção "o conjunto de chaves opcoes* é IGUAL nos dois modos" é o gate automático de paridade e reprova se uma chave entrar só num ramo
    - `git diff web/src/copy.js` NÃO mostra alteração em `opcoesLeituraTitulo`, `opcoesLerNoServico`, `opcoesMontarEstrutura`, `opcoesVerPossibilidades`, `opcoesAbaAnalisar`, `opcoesAbaComparar`, `opcoesAbaSetupsSalvos`
  </acceptance_criteria>
  <verify>
    <automated>node web/tests/test_opcoes_analisar_ui.mjs && node web/tests/test_opcoes_mcp_aba_ui.mjs</automated>
  </verify>
  <done>As 7 chaves existem nos dois ramos com valor idêntico, agrupadas por vizinhança semântica e comentadas; nenhuma chave existente mudou.</done>
</task>

<task type="auto">
  <name>Task 2: rótulos de estágio, linha de transição, gate por pill e ramo "leitura já feita"</name>
  <files>web/src/opcoes/OpcoesScreen.jsx</files>
  <read_first>
    - web/src/opcoes/OpcoesScreen.jsx — linhas 526-563 (`servicoIndisponivel`/
      `podePedirLeitura`/`blocoLeituraDoServico`, incluindo o comentário que
      explica por que o convite SOME nos 4 códigos acionáveis), 151 (`AJUDA`),
      255-260 (`abaWorkspace`), 415-420 (`temLeitura`/`nadaParaMostrar`),
      630-655 (`workspacePillRow` e o comentário de NAV-05), 688-707
      (`workspaceTopo`)
    - web/src/opcoes/OpcoesScreen.jsx — linhas 517-536 (comentário da Fase 27/
      ADR-027 Emenda 2 que explica por que o convite NÃO pode morar dentro da
      pill Analisar — a Opção C rejeitada; não reabrir)
    - .planning/phases/35-jornada-guiada-do-workspace/35-UI-SPEC.md — seções
      "D-01/D-08", "D-02", "D-06" (contrato de render exato) e a nota de
      discrição sobre o gate de D-06
    - web/tests/test_opcoes_hub_workspace_ui.mjs — linhas 200-220 (a fatia
      `corpoWorkspaceTopo`, de `<WorkspaceHeader` até o PRIMEIRO `);`) e
      307-316 (contagem de `abaWorkspace === "..."` que tem de continuar 3)
  </read_first>
  <action>
    Quatro edições cirúrgicas, todas dentro de `OpcoesScreen.jsx`, nenhuma
    tocando condição de dado, prop ou chamada.

    1. **D-01, estágio 1.** Na linha 542, trocar
       `<Kicker>{cp.opcoesLeituraTitulo || "LEITURA DO ATIVO"}</Kicker>` pela
       concatenação com o separador `" · "` já convencional deste diretório:
       título com fallback, mais `" · "`, mais `(cp.opcoesPasso1de2 || "Passo 1 de 2")`.
       O `Kicker` não muda — ele já aceita `children`.

    2. **D-02, gate por pill.** Na linha 700, trocar
       `{podePedirLeitura ? blocoLeituraDoServico : null}` por
       `{podePedirLeitura && abaWorkspace !== "setups" ? blocoLeituraDoServico : null}`.
       Usar `!==`, NUNCA uma terceira comparação `abaWorkspace === "setups"`:
       `test_opcoes_hub_workspace_ui.mjs:314-316` exige exatamente 3
       ocorrências de `abaWorkspace === "..."` no arquivo (o gate do ramo 4),
       e uma quarta reprovaria o guardião da Fase 34 por medir outra coisa.
       Comentário datado ao lado: Fase 35, D-02 — `SecaoSetups` nunca leu
       `temLeitura` (confirmado por leitura direta do arquivo), então o
       convite pago acima dela era portão sem porta.

    3. **D-06, o lugar do convite quando a leitura existe.** Logo DEPOIS da
       linha do gate acima, acrescentar um ramo `{temLeitura ? (...) : null}`
       que renderiza um `<div style={{ marginTop: "14px" }}>` (mesmo valor do
       bloco de convite, nenhum espaçamento novo) contendo um `Kicker` com
       título + `" · "` + `(cp.opcoesLeituraJaFeita || "leitura já feita")`.
       Decisão do planner sobre a nota de discrição do UI-SPEC: o gate é
       `temLeitura` SOZINHO, sem `abaWorkspace` — registrar isso em
       comentário com a razão: tudo que fica acima do carril
       (`LastroDoAtivo`, `LeituraInterna`, este metadado) é contexto do
       TICKER, compartilhado pelas três pills; D-02 remove da pill "Setups
       salvos" a AÇÃO paga, não o contexto. Estender o gate de D-06 por
       simetria visual seria ampliar uma decisão travada por conta própria.
       Registrar também, no comentário, a borda conhecida: com
       `leitura.dados` presente mas `behavior` ausente (`sem_candles`), nem o
       convite nem este metadado aparecem — é o comportamento de HOJE,
       preservado de propósito; inventar texto para esse estado seria afirmar
       leitura que o motor não endossou.

    4. **D-08, estágio 2 e transição.** Trocar a linha 705 (`{workspacePillRow}`,
       hoje sozinha) por três linhas, nesta ordem: (a) um `Kicker` com
       `(cp.opcoesEscolhaTitulo || "O QUE FAZER") + " · " + (cp.opcoesPasso2de2 || "Passo 2 de 2")`,
       SEM gate — é o rótulo que dá a JORN-03 paridade estrutural entre as
       três pills; (b) `{temLeitura ? <div style={AJUDA}>{cp.opcoesLeituraConcluidaAjuda || "Leitura concluída — escolha Analisar ou Comparar."}</div> : null}`;
       (c) `{workspacePillRow}` inalterado, continuando o ÚLTIMO elemento de
       `workspaceTopo`.

    Proibições, com razão:
    - Nenhuma das linhas novas pode conter `abrirLeitura`, `abrirCadeia`,
      `abrirOperaveis`, `montarProposta`, `verPossibilidades`,
      `atualizarVigias`, `compilarSetup` ou `confirmarSetup` — rótulo que
      dispara chamada é NAV-05 quebrado (§3.3 do ADR-027).
    - Nenhum `useEffect` novo, nenhum `useState` novo: as 4 edições leem
      estado já computado.
    - Nenhuma sequência `);` pode nascer DENTRO do corpo de `workspaceTopo`.
      O guardião da Fase 34 fatia `corpoWorkspaceTopo` de `<WorkspaceHeader`
      até o primeiro `);` — um `);` no meio truncaria a fatia e deixaria a
      asserção "`seletor` não aparece dentro de workspaceTopo" INERTE
      (passando sem medir nada). As formas de fechamento a usar são `) : null}`
      e `}</Kicker>`, nunca `);` intermediário.
    - Não mexer em `podePedirLeitura`, `servicoIndisponivel`, `temLeitura`,
      `nadaParaMostrar`, no corpo de `blocoLeituraDoServico` (fora o Kicker da
      edição 1) nem no `workspacePillRow`.
    - Não mover o convite para dentro da pill Analisar (Opção C, rejeitada em
      `35-CONTEXT.md` por contrariar ADR-027 Emenda 2 — o comentário das
      linhas 517-536 explica que ali ele seria inalcançável).
  </action>
  <acceptance_criteria>
    - `grep -c 'podePedirLeitura && abaWorkspace !== "setups" ? blocoLeituraDoServico : null' web/src/opcoes/OpcoesScreen.jsx` retorna 1
    - `grep -o 'abaWorkspace === "[a-z]*"' web/src/opcoes/OpcoesScreen.jsx | wc -l` continua 3
    - `grep -c "opcoesPasso1de2\|opcoesPasso2de2\|opcoesEscolhaTitulo\|opcoesLeituraJaFeita\|opcoesLeituraConcluidaAjuda" web/src/opcoes/OpcoesScreen.jsx` retorna 5 (uma ocorrência de cada)
    - `grep -c "cp.opcoesLeituraTitulo" web/src/opcoes/OpcoesScreen.jsx` retorna 2 (convite + metadado D-06) — conferir que nenhum guardião conta essa chave esperando 1: `node web/tests/test_opcoes_mcp_aba_ui.mjs` e `node web/tests/test_opcoes_analisar_ui.mjs` verdes
    - A fatia de `<WorkspaceHeader` até o primeiro `);` (a mesma que `test_opcoes_hub_workspace_ui.mjs:205-210` calcula) contém `{workspacePillRow}` — prova de que nenhum `);` novo truncou a fatia e deixou o guardião da Fase 34 inerte
    - `grep -c "useEffect\|useState" web/src/opcoes/OpcoesScreen.jsx` inalterado em relação ao HEAD anterior (`git stash`-free: comparar com `git show HEAD:web/src/opcoes/OpcoesScreen.jsx | grep -c "useEffect\|useState"`)
    - `node web/tests/test_opcoes_hub_workspace_ui.mjs && node web/tests/test_opcoes_subabas_ui.mjs && node web/tests/test_opcoes_custo_declarado.mjs` verdes
    - `cd web && npx vite build` verde (grep e teste estático não pegam erro de sintaxe JSX)
  </acceptance_criteria>
  <verify>
    <automated>cd web && npx vite build && cd .. && node web/tests/test_opcoes_hub_workspace_ui.mjs && node web/tests/test_opcoes_subabas_ui.mjs && node web/tests/test_opcoes_analisar_ui.mjs && node web/tests/test_opcoes_mcp_aba_ui.mjs && node web/tests/test_opcoes_custo_declarado.mjs</automated>
  </verify>
  <done>Os dois estágios estão nomeados na tela, a transição aparece quando a leitura existe, o convite some na pill "Setups salvos" e o metadado ocupa o lugar dele quando a leitura já foi feita — sem estado novo, sem chamada nova e sem deixar o guardião da Fase 34 inerte.</done>
</task>

<task type="auto">
  <name>Task 3: guardião da jornada (arquivo novo) + prova negativa real</name>
  <files>web/tests/test_opcoes_jornada_ui.mjs</files>
  <read_first>
    - web/tests/test_opcoes_hub_workspace_ui.mjs — o estilo de guardião desta
      família: `semComentario` antes de contar, fatia ANTES de marcador,
      asserção de sanidade para cada fatia (o achado do 34-02: buscar marcador
      no arquivo inteiro casa com homônimo e deixa a asserção inerte)
    - web/tests/test_opcoes_custo_declarado.mjs — o padrão de varredura de
      diretório (`readdirSync` + filtro `Secao*.jsx`) e o padrão de asserção
      de sanidade obrigatória antes de cada bloco derivado
    - web/src/opcoes/OpcoesScreen.jsx — as 4 edições da Task 2
    - web/src/copy.js — as 7 chaves da Task 1
  </read_first>
  <action>
    Criar `web/tests/test_opcoes_jornada_ui.mjs` no mesmo formato dos irmãos
    (import de `readFileSync`/`COPY`, helper `ok(nome, cond)`, contador
    `fails`, `process.exit(fails === 0 ? 0 : 1)`), com cabeçalho explicando o
    defeito que ele fecha (portão de leitura sem passo nomeado, e convite pago
    barrando quem só quer setup salvo) e a regra de que rótulo de progresso
    não pode virar disparador de chamada.

    Ler o fonte SEM comentários (`semComentario`, mesma implementação dos
    irmãos) antes de qualquer contagem — este plano acrescenta comentários que
    citam `abaWorkspace`, `temLeitura` e nomes de chave, e contá-los faria o
    guardião se auto-invalidar.

    Asserções (cada bloco derivado precede de uma asserção de sanidade que
    reprova se a fatia não foi localizada):

    1. **D-02:** existe exatamente 1 ocorrência de
       `podePedirLeitura && abaWorkspace !== "setups"`; e o número de
       `abaWorkspace === "<id>"` continua 3 (o gate do ramo 4 da Fase 34 não
       ganhou um quarto ramo por acidente).
    2. **D-01:** a fatia de `blocoLeituraDoServico` (de
       `const blocoLeituraDoServico = (` até o `);` que a fecha) contém
       `cp.opcoesPasso1de2` e o separador `" · "`.
    3. **D-06:** existe um ramo `temLeitura ?` cuja fatia contém
       `cp.opcoesLeituraJaFeita`, e essa fatia NÃO contém `T.positive` nem
       `T.negative` (par reservado a direção financeira —
       `PropostaLastreada.jsx:183`) nem emoji/“Pronto”/“Parabéns”.
    4. **D-08:** `cp.opcoesEscolhaTitulo` e `cp.opcoesPasso2de2` aparecem 1x
       cada, e ambos ANTES da ocorrência de `{workspacePillRow}` no arquivo
       (o rótulo do estágio precede o carril que ele nomeia);
       `cp.opcoesLeituraConcluidaAjuda` aparece 1x, dentro de um ternário
       sobre `temLeitura`, e usa o estilo `AJUDA` (nenhum estilo novo).
    5. **Princípio 5 (não inventar sequência):** o arquivo NÃO contém nenhuma
       das formas `Passo 2 de 3`, `Passo 3 de 3`, `de 3"`, `etapa 3` — nem em
       `OpcoesScreen.jsx` nem nos valores das chaves novas em `copy.js`.
       Analisar e Comparar nunca são numeradas entre si.
    6. **NAV-05:** a fatia que vai do `Kicker` do estágio 2 até
       `{workspacePillRow}` não contém nenhum de
       `abrirLeitura|abrirCadeia|abrirOperaveis|montarProposta|verPossibilidades|atualizarVigias|compilarSetup|confirmarSetup`.
    7. **Anti-inércia do guardião da Fase 34:** recalcular a MESMA fatia que
       `test_opcoes_hub_workspace_ui.mjs` usa (`<WorkspaceHeader` até o
       primeiro `);`) e exigir que ela contenha `{workspacePillRow}`. Se uma
       edição futura introduzir um `);` no meio de `workspaceTopo`, o guardião
       da Fase 34 passaria a medir uma fatia truncada sem reprovar — esta
       asserção reprova antes.
    8. **Paridade de copy:** as 7 chaves existem em `COPY.estudo` e
       `COPY.operador`, nenhuma vazia, e o valor é idêntico nos dois ramos
       (decisão registrada neste plano; se um dia divergir de propósito, a
       asserção muda junto da decisão, não em silêncio).
    9. **`SecaoSetups.jsx` intocada:** o arquivo não ganhou nenhuma das 7
       chaves novas nem referência a `temLeitura` — JORN-03 é paridade
       estrutural pelo Kicker compartilhado, não tratamento novo dentro dela
       (D-04/UI-SPEC "JORN-03 parity").

    Fechar com PROVA NEGATIVA REAL, por injeção, mínimo 3 — não descrita,
    executada, com a saída registrada no SUMMARY:
    (a) reverter o gate de D-02 para `podePedirLeitura ?` → asserção 1 reprova;
    (b) trocar `cp.opcoesPasso2de2` por um literal `"Passo 2 de 3"` →
        asserções 4 e 5 reprovam;
    (c) inserir `abrirLeitura()` dentro do ternário da linha de transição →
        asserção 6 reprova.
    Reverter cada injeção com `git checkout -- <arquivo>` e confirmar
    `git diff` limpo antes de commitar.
  </action>
  <acceptance_criteria>
    - `node web/tests/test_opcoes_jornada_ui.mjs` sai 0 com no mínimo 9 blocos de asserção e nenhuma fatia vazia (toda asserção de sanidade verde)
    - As 3 provas negativas foram EXECUTADAS: cada injeção produziu exit 1 nomeando a asserção, e o `git diff` pós-reversão é vazio — saídas coladas no SUMMARY
    - `bash scripts/executar.sh --testes` — suíte canônica COMPLETA (pytest + todos os `web/tests/*.mjs`), na baseline: 2923 pytest / 0 failed, `.mjs` só com a falha ambiental pré-existente `test_ios_assets.mjs` (`web/ios/` gitignored, `CLAUDE.md:236`), mais o arquivo novo passando
    - `cd web && npx vite build` verde
    - `git diff web/src/App.jsx` vazio (esta fase não toca o núcleo do app)
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <done>A jornada está travada por guardião com prova negativa real, o guardião da Fase 34 está protegido contra inércia por truncamento de fatia, e a suíte canônica inteira está na baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhuma nova | Rótulo, gate de render e chave de copy. Nenhuma rota, nenhum dado, nenhuma chamada externa nova. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Denial of service (consumo da cota do usuário) | linhas novas em `workspaceTopo` | mitigate | Asserção 6 do guardião: a fatia do estágio 2 não pode conter nenhum disparador de chamada paga; nenhum `useEffect`/`useState` novo (NAV-05, §3.3 do ADR-027). |
| T-35-02 | Repudiation (afirmar etapa que não existe) | copy de progresso | mitigate | Asserção 5: nenhuma forma "de 3" pode aparecer — numerar Analisar/Comparar inventaria sequência (princípio 5 do CLAUDE.md). |
| T-35-03 | Tampering (guardião inerte) | `test_opcoes_hub_workspace_ui.mjs` | mitigate | Asserção 7 recalcula a fatia da Fase 34 e exige que ela ainda contenha `{workspacePillRow}`. |
| T-35-SC | Tampering (supply chain) | npm/pip install | accept | Zero pacote novo nesta fase — nenhum `npm install`/`pip install` é executado por nenhum plano da Fase 35. |
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` (suíte canônica: pytest + `web/tests/*.mjs`) na baseline
- `cd web && npx vite build` verde
- `node web/tests/test_opcoes_jornada_ui.mjs` verde, com as 3 provas negativas executadas e revertidas
- `git diff web/src/App.jsx` vazio
</verification>

<success_criteria>
- O convite de leitura paga anuncia "Passo 1 de 2" e o carril anuncia "Passo 2 de 2"; nada numera Analisar contra Comparar
- Com a pill "Setups salvos" ativa, o convite pago não aparece
- Com a leitura feita, o lugar do convite exibe "LEITURA DO ATIVO · leitura já feita" e uma linha de transição aparece acima do carril
- As 7 chaves existem nos dois modos com o mesmo valor
- Guardião novo verde com prova negativa real; suíte canônica na baseline
</success_criteria>

<output>
Create `.planning/phases/35-jornada-guiada-do-workspace/35-01-SUMMARY.md` when done
</output>
