---
phase: 35-jornada-guiada-do-workspace
plan: 03
type: execute
wave: 3
depends_on: ["35-01", "35-02"]
files_modified:
  - web/src/version.js
  - server/app/main.py
  - server/web_dist
autonomous: false
requirements: [JORN-01, JORN-02, JORN-03]

must_haves:
  truths:
    - "D-07: o contraste do rótulo E do subtexto de custo sobre o preenchimento de accent foi conferido AO VIVO nas 4 combinações reais (tema claro/escuro × Modo Estudo/Operador) — não assumido a partir da tabela computada [JORN-02]"
    - "D-02: com a pill 'Setups salvos' ativa e sem leitura paga, o convite de leitura NÃO aparece e a lista/criação de setup fica alcançável — verificado por clique, não por leitura de código [JORN-03]"
    - "D-06: 'Montar estrutura' e 'Ver possibilidades' continuam clicáveis depois do primeiro resultado, e a marca `✓` aparece só quando há resultado com conteúdo [JORN-01]"
    - "D-01/D-08: os dois rótulos de estágio aparecem em 375px sem estourar linha, e nenhuma tela numera Analisar contra Comparar [JORN-01]"
    - "A jornada está NO AR com carimbo próprio (`BUILD_ID`/`SERVER_BUILD_ID` novos) e confirmada por HTTP em produção — fase que toca `web/src/` sem publicação fica testada e nunca vista"
  artifacts:
    - path: "web/src/version.js"
      provides: "Carimbo de entrega desta fase"
      contains: "BUILD_ID"
    - path: "server/app/main.py"
      provides: "SERVER_BUILD_ID com o histórico da entrega preservado"
      contains: "SERVER_BUILD_ID"
  key_links:
    - from: "checkpoint humano aprovado"
      to: "scripts/bump.sh + scripts/publicar-web.sh"
      via: "ordem obrigatória — bump ANTES de publicar, nunca editar server/web_dist à mão"
      pattern: "SERVER_BUILD_ID"
---

<objective>
Fechar a fase com a verificação que nenhum teste estático pode fazer —
contraste real do botão preenchido nos quatro pares tema × modo, o bug de
D-02 corrigido por clique, e a re-clicabilidade dos dois CTAs — e, só depois
da aprovação explícita do Alex, publicar.

Purpose: encerrar o risco aberto de D-07 com evidência visual (a metade
automatizável já está travada por `test_brand_book_v2_tokens.mjs`), provar
JORN-01/02/03 na tela, e evitar o modo de falha conhecido deste repositório
(fase que muda `web/src/`, passa na suíte, é mesclada e nunca vai ao ar).
Output: aprovação registrada, carimbo novo em produção, confirmação por HTTP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-jornada-guiada-do-workspace/35-CONTEXT.md
@.planning/phases/35-jornada-guiada-do-workspace/35-UI-SPEC.md
@.planning/phases/35-jornada-guiada-do-workspace/35-01-SUMMARY.md
@.planning/phases/35-jornada-guiada-do-workspace/35-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: verificação ao vivo — contraste nos 4 pares tema × modo, D-02 e D-06</name>
  <read_first>
    - .planning/phases/35-jornada-guiada-do-workspace/35-UI-SPEC.md — seções
      "D-07" (tabela de contraste computado, incluindo por que `#fff` e
      `color-mix` foram descartados) e "D-03/D-04/D-05"
    - .planning/phases/35-jornada-guiada-do-workspace/35-CONTEXT.md — D-07
      ("quem executar/verificar esta fase MUST confirmar contraste real nos
      dois temas antes de fechar a fase")
    - .planning/phases/35-jornada-guiada-do-workspace/35-02-SUMMARY.md — em
      especial o registro do fold-in de `SecaoVigias.jsx` (chip de vigia não
      selecionado passou a ter fundo de painel), que entra no roteiro abaixo
      por ser mudança visual fora do workspace
  </read_first>
  <what-built>
    Fase 35 completa, ainda NÃO publicada:
    - "Passo 1 de 2" no bloco de leitura paga e "O QUE FAZER · Passo 2 de 2"
      acima do carril de pills; linha de transição quando a leitura existe
    - O convite de leitura paga não aparece mais sobre a pill "Setups salvos"
    - "LEITURA DO ATIVO · leitura já feita" ocupa o lugar do convite depois da
      leitura
    - Os 3 CTAs da jornada ("Ler no serviço de opções", "Montar estrutura",
      "Ver possibilidades") preenchidos com accent, texto em `T.onAccent`
    - Marca `✓` discreta depois de "Montar estrutura"/"Ver possibilidades"
    - Fold-in: chip de vigia não selecionado voltou a ter fundo de painel
      (`T.bgPanel` era `undefined` desde a Fase 33)

    Evidência automática já coletada (colar as saídas no relato do checkpoint):
    `node web/tests/test_brand_book_v2_tokens.mjs` (contraste `onAccent` ×
    `accent` ≥ 4,5:1 nas 4 paletas), `node web/tests/test_opcoes_jornada_ui.mjs`,
    `bash scripts/executar.sh --testes` na baseline e `cd web && npx vite build`.
  </what-built>
  <how-to-verify>
    Subir o ambiente: `bash scripts/executar.sh` (backend :8787 + Vite :5173) e
    abrir `http://localhost:5173` no navegador, com o DevTools em viewport de
    375×667 (o contrato de densidade mobile desta família de telas).

    **Trocar de tema:** o tema segue o sistema operacional
    (`prefers-color-scheme`, `App.jsx:78`). O caminho rápido é DevTools →
    Rendering → "Emulate CSS prefers-color-scheme" → `light` / `dark`. O
    caminho real é Ajustes do macOS → Aparência. Usar o DevTools para varrer e,
    se algo parecer marginal, confirmar no caminho real.
    **Trocar de modo:** Perfil → Modo Operador (a troca recarrega o app por
    desenho, Fase 34).

    Roteiro (Opções → sub-aba "Setups"):

    1. **Sem ticker (hub).** Confirmar que nada mudou no hub além do chip de
       vigia não selecionado, que agora tem fundo de painel em vez de
       transparente. Dizer se a aparência nova está certa — se preferir a
       anterior, é reversão de uma palavra (está registrado no 35-02).
    2. **Escolher um ticker da carteira (workspace), SEM pedir a leitura.**
       Esperado: bloco "LEITURA DO ATIVO · Passo 1 de 2" com o botão "Ler no
       serviço de opções" PREENCHIDO de accent, o custo em 2ª linha dentro do
       próprio botão; abaixo, "O QUE FAZER · Passo 2 de 2" e o carril de 3
       pills. NÃO deve existir nenhum "de 3" em lugar nenhum.
    3. **Clicar na pill "Setups salvos" ainda sem leitura.** Esperado: o bloco
       do convite de leitura DESAPARECE (é o bug que a fase corrige), o rótulo
       "O QUE FAZER · Passo 2 de 2" permanece, e a lista/criação de setup fica
       acessível. Voltar para "Analisar": o convite reaparece.
    4. **Pedir a leitura** ("Ler no serviço de opções" — custa 3 chamadas da
       cota, é o preço declarado no próprio botão). Esperado depois: o convite
       vira "LEITURA DO ATIVO · leitura já feita" e aparece a linha "Leitura
       concluída — escolha Analisar ou Comparar." acima do carril.
    5. **Analisar → "Montar estrutura".** Esperado: botão preenchido;
       desabilitado (sem tese/lote) ele CONTINUA VISÍVEL, só mais apagado.
       Depois do resultado: aparece "✓ Estrutura montada" em cinza discreto
       entre o botão e o resultado, e o botão CONTINUA clicável — trocar a tese
       e montar de novo tem de funcionar.
    6. **Comparar → "Ver possibilidades".** Mesmo padrão; aqui o custo fica na
       linha ACIMA do botão (não dentro dele — é assim hoje e não mudou).
       Depois do resultado: "✓ Possibilidades carregadas".
    7. **O ponto crítico (D-07): repetir os passos 2, 5 e 6 nas QUATRO
       combinações** — tema escuro × Estudo, tema claro × Estudo, tema escuro ×
       Operador, tema claro × Operador. Em cada uma, dizer se (a) o rótulo do
       botão e (b) o subtexto de custo dentro do botão são confortavelmente
       legíveis sobre o preenchimento. Se qualquer uma das oito leituras estiver
       marginal, dizer QUAL — o ajuste é de token, não de hex solto, e o
       guardião proíbe voltar a `#fff`/`color-mix`.
    8. **Regressão de navegação:** trocar de pill várias vezes e confirmar pela
       aba Network do DevTools que NENHUMA chamada nova sai a cada troca
       (NAV-05, a garantia estrutural da Fase 34 que esta fase não pode
       arranhar).

    Pendência a NÃO tratar como defeito: o app nativo do iPhone continua sem
    esta entrega até um build de TestFlight (o bundle é local, sem
    `server.url`) — pendência herdada, registrada, fora do escopo.
  </how-to-verify>
  <action>
    Antes de chamar o Alex, colher a evidência automática e deixá-la colada na
    mensagem do checkpoint — pedir verificação humana sem ter rodado o que a
    máquina já responde desperdiça o único recurso escasso da fase:
    `bash scripts/executar.sh --testes` (suíte canônica inteira, na baseline),
    `cd web && npx vite build`, `node web/tests/test_brand_book_v2_tokens.mjs`
    (contraste `onAccent` × `accent` nas 4 paletas) e
    `node web/tests/test_opcoes_jornada_ui.mjs`.

    Subir o ambiente com `bash scripts/executar.sh` e confirmar que `:5173`
    responde ANTES de apresentar o roteiro — checkpoint que começa com "o
    servidor não sobe" queima o turno do Alex.

    Apresentar o roteiro de `<how-to-verify>` na íntegra, numerado, deixando
    explícito que o passo 7 (as 8 leituras de contraste) é o que fecha o risco
    aberto de D-07 e que o passo 1 pede veredito sobre uma mudança visual fora
    do workspace (o fold-in do chip de vigia).

    PARAR aqui. Não rodar `bump.sh`, `publicar-web.sh` nem `git push` —
    nenhuma parte da Task 2 começa antes do "aprovado — publicar" explícito.
    Se o Alex reportar divergência, registrar literalmente o que ele disse e
    reportar ao orquestrador sem corrigir por conta própria: correção de
    contraste é troca de token (nunca hex solto) e pode exigir emenda ao
    UI-SPEC.
  </action>
  <acceptance_criteria>
    - As 8 leituras de contraste do passo 7 foram reportadas uma a uma (4 combinações × rótulo/subtexto), com veredito explícito de cada
    - O passo 3 foi executado por CLIQUE e o convite comprovadamente desapareceu na pill "Setups salvos"
    - O passo 5/6 confirmou re-clicabilidade real (segundo clique com tese diferente produziu resultado novo)
    - O passo 8 foi conferido na aba Network: zero requisição nova por troca de pill
    - `node web/tests/test_brand_book_v2_tokens.mjs`, `node web/tests/test_opcoes_jornada_ui.mjs`, `bash scripts/executar.sh --testes` e `cd web && npx vite build` verdes, com saídas colhidas ANTES de pedir a aprovação
  </acceptance_criteria>
  <resume-signal>Responda "aprovado — publicar" para liberar a Task 2, ou descreva o que precisa mudar (inclusive a reversão do fold-in do chip de vigia, se preferir a aparência anterior). Sem esta resposta explícita, a Task 2 NÃO roda.</resume-signal>
  <verify>
    <human-check>As 8 leituras de contraste + os 4 pontos de comportamento acima, reportados pelo Alex</human-check>
    <automated>node web/tests/test_brand_book_v2_tokens.mjs && node web/tests/test_opcoes_jornada_ui.mjs && bash scripts/executar.sh --testes</automated>
  </verify>
  <done>O Alex viu a jornada rodando nas 4 combinações tema × modo, o risco aberto de D-07 está fechado com evidência visual além da computada, e a aprovação (ou a lista de correções) está registrada.</done>
</task>

<task type="auto">
  <name>Task 2: bump, publicação e confirmação em produção</name>
  <files>web/src/version.js, server/app/main.py, server/web_dist</files>
  <read_first>
    - CLAUDE.md — seção "Guardrails do repositório": `scripts/bump.sh` ANTES de
      `publicar-web.sh`, nunca editar `server/web_dist` direto, e os três
      esquemas de versão (esta task mexe só no `BUILD_ID`/`SERVER_BUILD_ID`, o
      carimbo de ENTREGA — não em `package.json` nem `MARKETING_VERSION`)
    - .planning/milestones/v1.6-phases/34-navega-o-hub-workspace/34-04-SUMMARY.md
      — a publicação anterior (`F10-20260920-01`), inclusive o merge de
      segurança com `origin/main` feito ANTES do carimbo
    - server/app/main.py — linha ~1662, o comentário do `SERVER_BUILD_ID`
      atual (a entrega anterior tem de virar "HISTÓRICO", não ser apagada)
    - scripts/bump.sh e scripts/publicar-web.sh — ler antes de rodar
  </read_first>
  <action>
    **Só rodar depois do "aprovado — publicar" da Task 1.** Se a aprovação não
    existir nesta conversa, parar e reportar.

    1. Mesclar `origin/main` ANTES de qualquer carimbo — `bump.sh` deriva o
       número do valor LOCAL, e sessão paralela já causou colisão neste repo
       (memória `bump-colide-em-sessao-paralela.md`). `server/web_dist` se
       REGENERA, não se mescla: em conflito ali, resolver rodando
       `publicar-web.sh` depois do merge.
    2. `bash scripts/bump.sh` → novo `BUILD_ID` em `web/src/version.js`.
    3. `bash scripts/publicar-web.sh` → regenera `server/web_dist` e atualiza
       `SERVER_BUILD_ID` em `server/app/main.py`. Reescrever à mão o
       comentário do `SERVER_BUILD_ID` com o resumo desta fase (jornada guiada
       do workspace: dois estágios nomeados, convite pago não barra mais a pill
       "Setups salvos", 3 CTAs preenchidos com `T.onAccent` — nunca `#fff`, com
       o número do contraste —, marca de resultado re-clicável, fold-in do
       token `bgPanel` em `SecaoVigias.jsx`) e rebaixar a entrega anterior a
       "HISTORICO", preservando o texto dela (guardrail: histórico não se
       reescreve).
    4. Repetir a suíte canônica DEPOIS do bump (`bash scripts/executar.sh
       --testes`) — o carimbo entra em arquivo que a suíte lê; confirmar que
       nada caiu.
    5. Commit, push em `v2/interacao-estrutural` E em `origin/main` (conferir
       que é fast-forward, não merge cego).
    6. Confirmar por HTTP: `curl -s https://boris.semente.dev/api/health` até
       responder o `SERVER_BUILD_ID` novo (o Railway pode levar alguns minutos
       servindo o anterior — registrar os dois carimbos observados e o tempo).

    Proibições, com razão:
    - NÃO editar `.planning/STATE.md` nem `.planning/ROADMAP.md`: guardrail
      deste repositório (2026-09-11) — os mutadores do `gsd-sdk` corrompem
      esses arquivos e o orquestrador os edita à mão depois da onda.
    - NÃO tocar `package.json` nem `MARKETING_VERSION`: cadência pública é
      decisão separada do Alex.
    - NÃO editar `server/web_dist` à mão em nenhuma hipótese.
  </action>
  <acceptance_criteria>
    - `grep -c "BUILD_ID = \"F10-" web/src/version.js` retorna 1, com data de hoje e sufixo novo
    - `grep -o 'SERVER_BUILD_ID = "[^"]*"' server/app/main.py` bate exatamente com o `BUILD_ID` de `web/src/version.js`
    - O comentário do `SERVER_BUILD_ID` contém o resumo desta fase E a entrega anterior marcada como "HISTORICO" (texto anterior preservado)
    - `bash scripts/executar.sh --testes` verde DEPOIS do bump
    - `git log --oneline -1` mostra o commit de publicação; `git status` limpo; `HEAD` igual a `origin/main` (fast-forward confirmado)
    - `curl -s https://boris.semente.dev/api/health` responde o carimbo novo (registrar a saída e, se houver atraso de deploy, os dois carimbos observados)
    - `git diff .planning/STATE.md .planning/ROADMAP.md` vazio (o orquestrador fecha os docs à mão)
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/executar.sh --testes && curl -s https://boris.semente.dev/api/health</automated>
  </verify>
  <done>A jornada guiada está em produção com carimbo próprio, confirmada por HTTP, com o histórico do `SERVER_BUILD_ID` preservado e os documentos de planejamento intocados pelo executor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repositório → produção (Railway) | `publicar-web.sh` + push em `origin/main` publicam o bundle servido a todos os usuários. É a única fronteira real desta fase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-08 | Tampering (publicar sem autorização) | `publicar-web.sh` / push em `origin/main` | mitigate | Checkpoint bloqueante: a Task 2 só roda com o "aprovado — publicar" explícito do Alex nesta conversa (guardrail do repositório, fase por fase). |
| T-35-09 | Tampering (carimbo colidido) | `bump.sh` | mitigate | Merge com `origin/main` ANTES do bump — `bump.sh` deriva do valor local e sessão paralela já colidiu (memória `bump-colide-em-sessao-paralela.md`). |
| T-35-10 | Repudiation (histórico de entrega apagado) | comentário do `SERVER_BUILD_ID` | mitigate | Entrega anterior rebaixada a "HISTORICO" com o texto preservado; histórico não se reescreve (guardrail). |
| T-35-11 | Information disclosure (texto ilegível em produção) | `BOTAO_PRIMARIO` no tema claro | mitigate | Task 1, passo 7: 8 leituras de contraste ao vivo antes de qualquer publicação, além da evidência computada de `test_brand_book_v2_tokens.mjs`. |
| T-35-SC | Tampering (supply chain) | npm/pip install | accept | Zero pacote novo — nenhum install nesta fase. |
</threat_model>

<verification>
- Checkpoint humano aprovado explicitamente, com as 8 leituras de contraste reportadas
- `bash scripts/executar.sh --testes` verde antes E depois do bump
- `cd web && npx vite build` verde
- `curl -s https://boris.semente.dev/api/health` servindo o carimbo novo
- `git diff .planning/STATE.md .planning/ROADMAP.md` vazio
</verification>

<success_criteria>
- O risco aberto de D-07 está fechado com evidência visual nos 4 pares tema × modo, não por inferência
- O bug de D-02 foi visto corrigido por clique
- A jornada está no ar com carimbo próprio, confirmado por HTTP
- Nenhum documento de planejamento foi editado pelo executor
</success_criteria>

<output>
Create `.planning/phases/35-jornada-guiada-do-workspace/35-03-SUMMARY.md` when done
</output>
