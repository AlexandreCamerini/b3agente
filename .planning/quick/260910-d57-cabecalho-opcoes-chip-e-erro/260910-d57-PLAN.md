---
quick_id: 260910-d57
phase: quick-260910-d57
plan: 01
type: quick
wave: 1
depends_on: []
files_modified: [web/src/opcoes/OpcoesScreen.jsx, web/src/copy.js, web/tests/test_opcoes_mcp_aba_ui.mjs]
autonomous: true
---

<objective>
Dois defeitos do cabeçalho da aba Opções, achados no teste ao vivo no iPhone
(2026-09-10, com a Fase 2 instalada contra produção, que só tem a Fase 1).
O cabeçalho é renderizado SEMPRE (`OpcoesScreen.jsx:146`), antes de todo
ramo de estado — então o que ele afirma vale mesmo quando nada respondeu.
</objective>

<context>
Sintoma relatado: a aba mostra "frescor não medido". Investigado: não é o
backend errando a medição. Em produção `/api/options/mcp/leitura/{t}` responde
404 (Fase 2 não deployada) e o objeto de frescor fica nulo; o chip é o valor
INICIAL da variável, não uma medição.

Chaves de copy JÁ existem nos dois modos (`copy.js:61-63` e `333-335`):
`opcoesFrescorEmDia`, `opcoesFrescorAtrasado`, `opcoesFrescorNaoMedido`.
Uma chave nova (para o estado "nada consultado") entra nos DOIS modos, com
o mesmo texto — é afirmação factual sobre dado, não voz de professor/mesa
(mesmo critério já registrado em `copy.js` para payoff/fonte).
</context>

<tasks>
<task type="auto">
  <name>Task 1: chip honesto e erro por acionabilidade</name>
  <files>web/src/opcoes/OpcoesScreen.jsx, web/src/copy.js, web/tests/test_opcoes_mcp_aba_ui.mjs</files>
  <action>
  DEFEITO 1 (`OpcoesScreen.jsx:86-92`) — hoje `let chip = cp.opcoesFrescorNaoMedido || "frescor não medido"` é o default e só muda sob `frescor.medido`. Passar a distinguir TRÊS origens:
    (a) `!frescor` — nada foi consultado: chip NÃO afirma nada sobre frescor. Preferir OMITIR o chip inteiro (o cabeçalho já mostra `Pregão: —`, e um chip vazio/traço solto é ruído); se omitir quebrar o layout, usar "—". Documentar a escolha no comentário.
    (b) `frescor.medido === false` — o serviço respondeu e não mediu: "frescor não medido" continua, e é verdade.
    (c) `frescor.medido === true` — em dia / atrasado com idade, como já está.
  Comentário deve citar a simetria com a regra que o mesmo arquivo já aplica aos setups: "sem avaliação NÃO vira não armado: ausência de leitura não é leitura negativa".
  Conferir `fonte` (`OpcoesScreen.jsx:~110`): o default literal `"mcp.semente.dev"` afirma origem mesmo sem resposta — aplicar a mesma lógica (sem resposta, não afirmar fonte).
  Chave nova em `copy.js` nos DOIS modos se a variante (a) precisar de texto.

  DEFEITO 2 (`OpcoesScreen.jsx:94`) — `const erro = leitura.erro || status.erro` deixa o 404 da leitura mascarar o `mcp_nao_configurado` do status, que é o que diz ao usuário o que fazer. Escolher por ACIONABILIDADE: erro COM `code` conhecido (`mcp_nao_configurado`, `mcp_cota`, `mcp_teto_servico`, `mcp_indisponivel`) vence erro SEM `code`, venha de onde vier; com os dois codificados ou os dois sem código, a leitura vence (é a chamada que o usuário disparou). Extrair um helper puro pequeno e comentar o porquê.

  GUARDIÃO (`test_opcoes_mcp_aba_ui.mjs`, padrão readFileSync+regex; FILTRAR comentários antes de CONTAR ocorrências): (i) o chip não pode ser inicializado com o texto/chave de "não medido" — essa variante só aparece sob teste que prove que houve resposta; (ii) a escolha do erro não pode ser `leitura.erro || status.erro` puro — tem de haver seleção por `code`; (iii) asserção de sanidade que falha se a regex parar de casar (nunca vira no-op).
  </action>
  <verify>bash scripts/executar.sh --testes (inteira) E cd web && npx vite build</verify>
  <done>Suíte verde, build verde, e o guardião novo falha se qualquer um dos dois defeitos voltar (provar revertendo temporariamente e registrando no SUMMARY).</done>
</task>
</tasks>

<success_criteria>
Um commit atômico. Sem tocar backend, web-admin, bump, deploy ou publicação.
Entra na branch `v2/interacao-estrutural` e vai junto no PR #39.
</success_criteria>
