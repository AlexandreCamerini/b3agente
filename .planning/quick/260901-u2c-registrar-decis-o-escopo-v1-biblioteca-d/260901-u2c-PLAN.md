---
phase: quick-260901-u2c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/seeds/opcoes-v2-setups-propostos-via-b-mcp.md
  - .planning/notes/opcoes-v2-b-mcp-exploracao.md
autonomous: true
requirements: [QUICK-260901-U2C]

must_haves:
  truths:
    - "Quem ler o seed vê o escopo do v1 da biblioteca de setups como decisão FECHADA (venda coberta + put de proteção + collar/trava protetora), não como pendência aguardando o Alex"
    - "Quem ler a nota de exploração vê o item 5 ('Escopo do v1 da biblioteca') como DECIDIDO, com o racional completo — inclusão do collar por ser combinação das duas pernas já calculadas"
    - "O racional de exclusão está registrado e distingue os dois motivos: straddle/strangle coberto sai por liquidez (2 pernas opostas num mercado que já é curto pra 1); cash-secured put sai por definição (inicia posição em vez de proteger uma existente)"
    - "A diretriz arquitetural do Alex está registrada junto ao escopo: v1 implementável só com código determinístico do Boris, mas com screening de cadeia e cálculo de estrutura atrás de um limite interno, para que trocar pelas tools do b-mcp (find_tradable_options, evaluate_option_structure) seja extensão limpa e não reescrita"
    - "O texto original que explicava por que o item 5 ficou em aberto continua legível na nota, como contexto histórico abaixo da resolução"
    - "A seção 'Pendências de produto' do seed fica só com o plano comercial"
    - "Nenhum arquivo de código foi tocado — o diff é exclusivamente .planning/"
  artifacts:
    - path: ".planning/seeds/opcoes-v2-setups-propostos-via-b-mcp.md"
      provides: "Escopo do v1 fechado na seção 'Decisões de produto fechadas', com a ressalva de arquitetura; fora da lista de pendências"
      contains: "collar"
    - path: ".planning/notes/opcoes-v2-b-mcp-exploracao.md"
      provides: "Item 5 das decisões da sessão virado de NÃO DECIDIDO para DECIDIDO, com racional de inclusão/exclusão e histórico preservado"
      contains: "trava protetora"
  key_links:
    - from: ".planning/seeds/opcoes-v2-setups-propostos-via-b-mcp.md"
      to: ".planning/notes/opcoes-v2-b-mcp-exploracao.md"
      via: "referência textual ao item 5 da nota para o racional completo"
      pattern: "opcoes-v2-b-mcp-exploracao"
    - from: ".planning/seeds/opcoes-v2-setups-propostos-via-b-mcp.md"
      to: ".planning/todos/pending/opcoes-v2-confirmar-hub-mydata-e-acesso-b-mcp.md"
      via: "a ressalva de MCP futuro aponta o bloqueio (item 2) que a motiva"
      pattern: "opcoes-v2-confirmar-hub-mydata-e-acesso-b-mcp"
---

<objective>
Fechar na documentação de planejamento a decisão do Alex sobre o escopo do v1 da
biblioteca de setups de opções: **venda coberta + put de proteção + collar (trava
protetora)** — e registrar, colada nessa decisão, a ressalva arquitetural do Alex
de **deixar espaço para uso futuro de um MCP**.

Purpose: os dois docs hoje dizem que esse escopo está em aberto ("NÃO DECIDIDO —
deixado em aberto de propósito" na nota; bullet em "Pendências de produto" no
seed). Enquanto disserem isso, um `/gsd-plan-phase` futuro sobre Opções v2 ou
reabre uma decisão já tomada, ou planeja a biblioteca errada. A ressalva de
arquitetura é a parte mais fácil de perder: ela não é decisão de código pra
fechar agora, é diretriz de design pra quem for planejar a fase depois, e só
sobrevive se ficar escrita junto do escopo.

Output: dois arquivos de planejamento atualizados. ZERO código.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/seeds/opcoes-v2-setups-propostos-via-b-mcp.md
@.planning/notes/opcoes-v2-b-mcp-exploracao.md
@CLAUDE.md

<facts>
Decisão a registrar (fonte: Alex, 2026-09-01) — registrar tal e qual, não
reformular o racional:

**Escopo do v1 da biblioteca: venda coberta + put de proteção + collar (trava
protetora).**
- Venda coberta e put de proteção já existem (Fase 14).
- Collar é a combinação das duas mesmas pernas já calculadas — extensão
  natural, zero peça nova no motor.

**Excluído: straddle/strangle coberto.** Motivo = liquidez. Liquidez de opções
B3 fora dos blue-chips já é curta pra 1 perna, pior pra 2 pernas simultâneas de
lados opostos (o `find_tradable_options` do b-mcp tem `min_trades` justamente
por isso).

**Excluído: cash-secured put.** Motivo = definição, NÃO liquidez. Inicia posição
(comprar ação mais barato) em vez de proteger uma que já existe — contradiz a
régua "só sobre cobertura real", que é o próprio enunciado da feature.

**Ressalva arquitetural EXPLÍCITA do Alex — deixar espaço para MCP futuro.** Não
é decisão nova de código a fechar agora; é diretriz de design para quem planejar
/implementar a fase depois. Hoje o acesso server-to-server ao b-mcp
(`~/dev/MCP/servers/mydata/`) ainda é bloqueio aberto (ver
`.planning/todos/pending/opcoes-v2-confirmar-hub-mydata-e-acesso-b-mcp.md`,
item 2). Logo: o v1 tem que ser implementável com código determinístico do
próprio Boris, sem depender do b-mcp para existir — MAS desenhado de um jeito
que, quando o bloqueio cair, trocar/estender a lógica de **screening de cadeia**
e **cálculo de estrutura** pelas tools do b-mcp (`find_tradable_options`,
`evaluate_option_structure`) seja extensão limpa, não reescrita. Concretamente:
essa lógica fica atrás de um limite/interface interno que não amarra no formato
de dado específico do código-hoje-do-Boris.

Guardrail do repo aplicável (CLAUDE.md): "Histórico não se reescreve". O texto
que explica por que o item 5 ficou deliberadamente em aberto é registro de
sessão — não some; a resolução entra POR CIMA dele. Mesmo padrão já aplicado
nesses dois arquivos nas duas quick tasks anteriores desta exploração (260901-r5t
e a decisão de navegação).
</facts>

<project_skills_check>
Skills do projeto: `didatica-boris` (vocabulário/didática voltada ao usuário
final) e `swiftui-pro` (revisão de código SwiftUI). Nenhuma das duas incide sobre
doc interno de planejamento — sem conflito de regra a acomodar.
</project_skills_check>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fechar o escopo do v1 da biblioteca de setups (+ ressalva de MCP futuro) nos dois docs</name>
  <files>.planning/seeds/opcoes-v2-setups-propostos-via-b-mcp.md, .planning/notes/opcoes-v2-b-mcp-exploracao.md</files>
  <action>
Edite os DOIS arquivos com a ferramenta Edit (nunca heredoc/`cat`). São a mesma
unidade de decisão — os dois têm que sair consistentes.

**Arquivo 1 — `.planning/seeds/opcoes-v2-setups-propostos-via-b-mcp.md`:**

(a) A seção "Decisões de produto fechadas:" hoje tem um único bullet (a
navegação, Candidato A, 2026-09-01). Adicione um SEGUNDO bullet, depois dele,
registrando o escopo do v1 da biblioteca:
- Escopo decidido por Alex em 2026-09-01: **venda coberta + put de proteção +
  collar (trava protetora)**.
- Por que o collar entra: é a combinação das duas mesmas pernas já calculadas —
  extensão natural, zero peça nova no motor. Venda coberta e put de proteção já
  existem (Fase 14).
- Excluídos, com os motivos DISTINTOS preservados: **straddle/strangle coberto**
  sai por liquidez (opções B3 fora dos blue-chips já são curtas pra 1 perna,
  pior pra 2 pernas simultâneas de lados opostos — o `find_tradable_options` do
  b-mcp tem `min_trades` justamente por isso); **cash-secured put** sai por
  definição, não por liquidez (inicia posição em vez de proteger uma que já
  existe, contradiz a régua "só sobre cobertura real" que é o enunciado da
  feature).
- **Ressalva arquitetural (Alex), parte da mesma decisão:** o v1 tem que ser
  implementável com código determinístico do próprio Boris, sem depender do
  b-mcp para existir (o acesso server-to-server segue bloqueado — ver
  `.planning/todos/pending/opcoes-v2-confirmar-hub-mydata-e-acesso-b-mcp.md`,
  item 2), MAS desenhado com a lógica de screening de cadeia e cálculo de
  estrutura atrás de um limite/interface interno, de modo que trocar/estender
  por `find_tradable_options` / `evaluate_option_structure` do b-mcp, quando o
  bloqueio cair, seja extensão limpa e não reescrita. Deixe explícito que isso
  é diretriz de design para a fase de implementação, não decisão de código a
  fechar agora.
- Aponte para o item 5 da seção "Decisões tomadas nesta sessão" de
  `.planning/notes/opcoes-v2-b-mcp-exploracao.md` como racional completo.

(b) Na seção "Pendências de produto (não técnicas, aguardando o Alex)", REMOVA o
bullet "Escopo do v1 da biblioteca além de venda coberta/put de proteção." A
lista fica com um item só: o plano comercial (gratuito vs. pago) e sua relação
com o cap da v1.3.

Não altere o frontmatter nem a `trigger_condition` — os bloqueios técnicos do
seed (hub mydata / acesso b-mcp) seguem inalterados por esta decisão. Não mexa
no parágrafo do corpo que diz "V1: biblioteca fixa dos setups mais conhecidos do
mercado" — ele segue verdadeiro; esta decisão apenas nomeia quais são.

**Arquivo 2 — `.planning/notes/opcoes-v2-b-mcp-exploracao.md`:**

O item **5** dentro de "## Decisões tomadas nesta sessão" hoje é:

  `**5. Escopo do v1 da biblioteca além de venda coberta / put de proteção.**`
  seguido de `NÃO DECIDIDO — deixado em aberto de propósito. ...`

Reestruture esse item (e SÓ esse item — não toque nos itens 1, 2, 3, 4, 6, 7)
em três partes, nesta ordem:

1. **Veredito primeiro.** Mantenha a linha do cabeçalho em negrito como está e
   faça a linha IMEDIATAMENTE seguinte começar, literalmente, com
   `DECIDIDO (Alex, 2026-09-01):` — seguida do escopo: venda coberta + put de
   proteção + **collar (trava protetora)**. Use a grafia `collar (trava
   protetora)` ao menos uma vez. Registre o motivo da inclusão do collar
   (combinação das duas mesmas pernas já calculadas; extensão natural, zero peça
   nova no motor).
2. **Racional de exclusão**, com os dois motivos mantidos distintos (não
   colapse num "por liquidez" genérico):
   - straddle/strangle coberto → liquidez: opções B3 fora dos blue-chips já são
     curtas pra 1 perna, pior pra 2 pernas simultâneas de lados opostos; cite
     que o `find_tradable_options` do b-mcp tem `min_trades` justamente por
     isso;
   - `cash-secured put` → definição, não liquidez: inicia posição (comprar a
     ação mais barato) em vez de proteger uma que já existe; contradiz a régua
     "só sobre cobertura real", que é o próprio enunciado da feature. Use a
     grafia `cash-secured` (com hífen).
   E a **ressalva arquitetural do Alex** como parte da mesma decisão: v1
   implementável com código determinístico do Boris (o b-mcp não pode ser
   pré-requisito de existência — bloqueio 2 desta mesma nota segue aberto), mas
   com screening de cadeia e cálculo de estrutura atrás de um limite/interface
   interno que não amarre no formato de dado específico do código-hoje-do-Boris,
   para que a troca por `find_tradable_options` / `evaluate_option_structure`
   seja **extensão limpa, não reescrita**. Diga explicitamente que é diretriz de
   design para quem planejar/implementar a fase, não decisão de código agora.
3. **Contexto histórico**, por último, claramente rotulado como tal: preserve o
   texto original que explicava por que o item ficou em aberto — mantenha a
   expressão "deixado em aberto de propósito" e a substância do argumento (era
   decisão de produto/pedagógica que dependia de critério que só o Alex tinha:
   liquidez real de opções B3 por ticker, o que faz sentido pedagogicamente no
   Modo Estudo; estava registrado como pergunta em aberto, não como bloqueio
   técnico). Não apague esse racional — ele mostra que a pergunta foi respondida
   pelo critério que ela mesma pedia. Guardrail do repo: histórico não se
   reescreve.

Não mexa na seção "## Bloqueios" (o item 2, acesso server-to-server ao b-mcp,
continua aberto e é justamente o que motiva a ressalva). Não mexa na seção
"Ideia original", nem na seção "Navegação", nem em "Fontes consultadas".

**Escopo estrito:** nenhum arquivo fora de `.planning/`. Não abra, não edite e
não crie nada em `web/`, `server/` ou `web-admin/` — a implementação do motor de
setups de opções é tarefa futura, fora de escopo. Não rode build, `cap sync`,
bump ou publicação.
  </action>
  <verify>
    <automated>
cd /Users/acamerini/dev/bolsia/b3-agente && bash -c '
SEED=.planning/seeds/opcoes-v2-setups-propostos-via-b-mcp.md
NOTA=.planning/notes/opcoes-v2-b-mcp-exploracao.md
fail=0
chk()  { if [ "$2" -eq 0 ]; then echo "FAIL: $1"; fail=1; else echo "ok: $1"; fi; }
chkz() { if [ "$2" -ne 0 ]; then echo "FAIL: $1"; fail=1; else echo "ok: $1"; fi; }

# --- extrações de seção (gates escopados, nunca no arquivo inteiro) ---
ITEM5=$(awk "/^\\*\\*5\\. Escopo do v1/,/^\\*\\*6\\./" "$NOTA")
VEREDITO=$(awk "/^\\*\\*5\\. Escopo do v1/{getline; print}" "$NOTA")
FECHADAS=$(awk "/Decis.*es de produto fechadas/,/Pend.*ncias de produto/" "$SEED")
PENDENCIAS=$(awk "/Pend.*ncias de produto/,0" "$SEED")

# 1. Veredito DECIDIDO na posicao certa (linha logo apos o cabecalho do item 5)
chk  "item 5: linha do veredito comeca com DECIDIDO (Alex, 2026-09-01)" "$(printf %s "$VEREDITO" | grep -c "^DECIDIDO (Alex, 2026-09-01)")"
chkz "item 5: veredito nao e mais NAO DECIDIDO"                          "$(printf %s "$VEREDITO" | grep -c "NÃO DECIDIDO")"

# 2. Escopo do v1 nomeado nos DOIS arquivos (collar/trava protetora sao os tokens novos)
chk "nota: item 5 nomeia collar"            "$(printf %s "$ITEM5" | grep -ci "collar")"
chk "nota: item 5 usa trava protetora"      "$(printf %s "$ITEM5" | grep -ci "trava protetora")"
chk "nota: item 5 mantem venda coberta"     "$(printf %s "$ITEM5" | grep -ci "venda coberta")"
chk "nota: item 5 mantem put de protecao"   "$(printf %s "$ITEM5" | grep -ci "put de prote")"
chk "seed: decisoes fechadas nomeiam collar"        "$(printf %s "$FECHADAS" | grep -ci "collar")"
chk "seed: decisoes fechadas usam trava protetora"  "$(printf %s "$FECHADAS" | grep -ci "trava protetora")"
chk "seed: decisoes fechadas carimbam a data"       "$(printf %s "$FECHADAS" | grep -c "2026-09-01")"

# 3. Racional de exclusao, com os DOIS motivos distintos preservados
chk "nota: exclusao de straddle/strangle registrada"      "$(printf %s "$ITEM5" | grep -ci "straddle")"
chk "nota: motivo liquidez citado"                        "$(printf %s "$ITEM5" | grep -ci "liquidez")"
chk "nota: min_trades citado como evidencia"              "$(printf %s "$ITEM5" | grep -c "min_trades")"
chk "nota: exclusao de cash-secured put registrada"       "$(printf %s "$ITEM5" | grep -ci "cash-secured")"
chk "nota: motivo definicao (inicia posicao) citado"      "$(printf %s "$ITEM5" | grep -Eci "inicia posi|cobertura real")"
chk "seed: exclusoes nomeadas (straddle)"                 "$(printf %s "$FECHADAS" | grep -ci "straddle")"
chk "seed: exclusoes nomeadas (cash-secured)"             "$(printf %s "$FECHADAS" | grep -ci "cash-secured")"

# 4. Ressalva arquitetural: espaco pra MCP futuro atras de limite interno
chk "nota: cita find_tradable_options no item 5"         "$(printf %s "$ITEM5" | grep -c "find_tradable_options")"
chk "nota: cita evaluate_option_structure no item 5"     "$(printf %s "$ITEM5" | grep -c "evaluate_option_structure")"
chk "nota: extensao limpa e nao reescrita"               "$(printf %s "$ITEM5" | grep -Eci "reescrita|extensão limpa")"
chk "nota: limite/interface interno"                     "$(printf %s "$ITEM5" | grep -Eci "interface interno|limite/interface|limite interno")"
chk "seed: ressalva cita as tools do b-mcp"              "$(printf %s "$FECHADAS" | grep -Ec "find_tradable_options|evaluate_option_structure")"
chk "seed: ressalva fala em extensao, nao reescrita"     "$(printf %s "$FECHADAS" | grep -Eci "reescrita|extensão limpa")"
chk "seed: ressalva aponta o todo do bloqueio"           "$(printf %s "$FECHADAS" | grep -c "opcoes-v2-confirmar-hub-mydata-e-acesso-b-mcp")"

# 5. Historico preservado (guardrail: historico nao se reescreve)
chk "nota: item 5 preserva o texto de por que ficou em aberto" "$(printf %s "$ITEM5" | grep -ci "deixado em aberto de prop")"

# 6. Pendencias do seed: escopo saiu, plano comercial fica
chkz "seed: escopo do v1 saiu das pendencias"  "$(printf %s "$PENDENCIAS" | grep -ci "escopo do v1")"
chk  "seed: plano comercial segue pendente"    "$(printf %s "$PENDENCIAS" | grep -ci "plano comercial")"

# 7. Escopo do diff: SO .planning/, zero codigo
chkz "nenhum arquivo fora de .planning tocado" "$(git diff --name-only HEAD | grep -cv "^\.planning/")"
chkz "nenhum arquivo de codigo tocado"         "$(git diff --name-only HEAD | grep -Ec "^(web|server|web-admin)/")"

# 8. Os dois arquivos alvo realmente mudaram
chk "seed modificado" "$(git diff --name-only HEAD -- "$SEED" | wc -l | tr -d " ")"
chk "nota modificada" "$(git diff --name-only HEAD -- "$NOTA" | wc -l | tr -d " ")"

[ "$fail" -eq 0 ] && echo "VERIFY OK" || { echo "VERIFY FALHOU"; exit 1; }
'
    </automated>
  </verify>
  <done>
- Seed: "Decisões de produto fechadas" tem 2 itens (navegação + escopo do v1 com
  a ressalva de MCP futuro); "Pendências de produto" tem 1 item (plano comercial).
- Nota: item 5 abre com `DECIDIDO (Alex, 2026-09-01):`, traz o racional de
  inclusão do collar, os dois motivos distintos de exclusão (straddle = liquidez;
  cash-secured put = definição), a diretriz de limite/interface interno para o
  b-mcp futuro, e o contexto histórico preservado ao final.
- `git diff --name-only HEAD` retorna exclusivamente caminhos sob `.planning/`.
- O bloco `<automated>` acima imprime `VERIFY OK`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma) | Edição de dois arquivos markdown de planejamento interno. Sem entrada não confiável, sem superfície de rede, sem execução, sem dependência nova. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-u2c-01 | Tampering | Arquivos de código do app (`web/`, `server/`) alterados por engano numa task de doc | mitigate | Verify gate falha se `git diff --name-only HEAD` listar qualquer caminho fora de `.planning/` |
| T-u2c-02 | Repudiation | Decisão de produto registrada sem autor/data — vira "quem decidiu excluir cash-secured put?" daqui a dois meses | mitigate | Veredito carimbado `DECIDIDO (Alex, 2026-09-01)` na nota e data exigida no bloco de decisões fechadas do seed |
| T-u2c-03 | Information disclosure | Perda do racional histórico (por que o item 5 ficou em aberto) ao sobrescrever o item | mitigate | Guardrail do repo "histórico não se reescreve"; verify gate exige "deixado em aberto de propósito" ainda presente no item 5 |
| T-u2c-04 | Tampering | Colapso dos dois motivos de exclusão num só ("tudo por liquidez"), destruindo a distinção que o Alex fez de propósito | mitigate | Gates separados exigem `straddle`+`liquidez`+`min_trades` E `cash-secured`+`inicia posição/cobertura real` no mesmo bloco |
| T-u2c-05 | Denial of service | Ressalva de MCP futuro se perder e a fase futura nascer amarrada no formato de dado de hoje, exigindo reescrita | mitigate | Gates exigem `find_tradable_options`, `evaluate_option_structure`, "extensão limpa/reescrita" e "limite/interface interno" nos dois arquivos |
| T-u2c-SC | Tampering | Instalação de pacote npm/pip/cargo | accept | Nenhum install nesta task — sem superfície de supply chain |
</threat_model>

<verification>
Rodar o bloco `<automated>` da Task 1 (é a suíte completa desta quick task).

Suíte canônica do repo (`bash scripts/executar.sh --testes`) e `npx vite build`
**não se aplicam**: nada em `server/`, `web/` ou `web-admin/` é tocado. Rodá-las
não valida nada aqui — o gate real é o escopo do diff somado aos gates de
conteúdo escopados por seção.
</verification>

<success_criteria>
1. `VERIFY OK` impresso pelo bloco automatizado.
2. Leitura humana do seed: o escopo do v1 lê-se como decidido (venda coberta +
   put de proteção + collar), com a ressalva de MCP futuro colada nele; a única
   pendência de produto restante é o plano comercial.
3. Leitura humana da nota: item 5 abre com o veredito, segue com inclusão/
   exclusão/ressalva arquitetural, fecha com o contexto histórico rotulado.
4. `git diff --name-only HEAD` = exatamente 2 arquivos, ambos sob `.planning/`.
</success_criteria>

<output>
Create `.planning/quick/260901-u2c-registrar-decis-o-escopo-v1-biblioteca-d/260901-u2c-SUMMARY.md` when done
</output>
