---
phase: 260824-i45-notificacoes-push-deep-link-titulo-com-m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/skill_ref.py
  - server/app/agent.py
  - server/app/pending_orders.py
  - server/app/main.py
  - server/app/radar_daily.py
  - server/tests/test_push_payload.py
  - server/tests/test_push_registro_evento.py
  - server/tests/test_ordens_pendentes_scheduler.py
  - server/tests/test_radar_daily.py
  - web/src/App.jsx
  - web/tests/test_notif_pregao.mjs
autonomous: true
requirements: [PUSH-DEEPLINK, PUSH-TITULO-MARCO, PUSH-PNL-VENDA, PUSH-REGISTRO-EVENTO, NOTIF-LOCAL-GUARDA-PREGAO, RADAR-TEXTO-PREVIA]

must_haves:
  truths:
    - "Um push de execução do Operador (stop, alvo, entrada automática, ordem pendente) chega com `data.t` preenchido e o toque leva ao card do ativo — o mecanismo do app já existe e passa a ser alimentado"
    - "Nenhum push do Operador sai mais com o título literal `Agente Boris+ (simulado)` quando o evento tem marco conhecido: stop, alvo, entrada automática e ordem pendente carregam o marco real no título"
    - "Todo título de push do Operador continua declarando que a operação é simulada (CLAUDE.md princípio 1) — o corpo de entrada automática e de ordem pendente não diz `simulado`, então o título precisa dizer"
    - "O push de stop/alvo mostra o resultado realizado da operação, lido do valor que `store.sell` devolve — nunca recomposto no texto (CLAUDE.md princípio 5), e a proveniência fica travada por âncora de FONTE, não só por igualdade numérica"
    - "O evento de mercado do gatilho e da prévia do Radar aparece em EVENTOS E AVISOS RECENTES, não só a linha de entrega no Diário técnico"
    - "As notificações locais de stop/alvo/variação do front só disparam com o pregão CONFIRMADO aberto pela fonte canônica do backend; com o estado indeterminado (1ª consulta em voo ou falha de rede) não notifica E não consome o `armado`, então o aviso sai na primeira avaliação com o pregão confirmado, em vez de se perder"
    - "O front NÃO ganha nenhuma reimplementação de calendário/feriado da B3 em JS — o único juiz continua sendo `pregao.in_market_hours()` no servidor"
    - "O push do Radar das 08:45 deixa explícito que é prévia PRÉ-ABERTURA, em título e corpo, deixando de ler como alerta fora de hora — e o guardião do título passa a ser capaz de falhar (hoje ele é vácuo)"
  artifacts:
    - path: "server/app/skill_ref.py"
      provides: "PUSH_TITULOS (por modo, por tag do evento) + push_titulo() com degradação definida; PUSH_RADAR (prévia pré-abertura, mode-agnostic)"
      contains: "PUSH_TITULOS"
    - path: "server/app/agent.py"
      provides: "Eventos de saída/entrada com `t`/`tag`/`pnl`; D2 e D3 derivam título e `extra` do evento; `_avisar_gatilhos` registra o evento de mercado em agent.events; `_run_cycle_inner` devolve appMode"
      contains: "push_titulo"
    - path: "server/app/main.py"
      provides: "`_notify(uid, title, body, extra=None)` repassando `extra` a `push.send_to_user`"
      contains: "extra=extra"
    - path: "server/app/pending_orders.py"
      provides: "Campo `t` (ticker) nos 3 eventos de ordem pendente executada/cancelada"
    - path: "server/app/radar_daily.py"
      provides: "Título e corpo de PRÉVIA pré-abertura vindos de skill_ref.PUSH_RADAR + registro do evento em agent.events"
      contains: "PUSH_RADAR"
    - path: "server/tests/test_push_payload.py"
      provides: "Guardiões dos itens 1/2/3: extra com `t` válido, título por tag/modo, P&L com âncora de FONTE (`pnl = store.sell(`) além da igualdade numérica"
    - path: "server/tests/test_push_registro_evento.py"
      provides: "Guardiões dos itens 4/6: gatilho e Radar escrevem em agent.events; texto do Radar diz prévia pré-abertura. Fakes só COLETAM — asserção de conteúdo fora da corrotina"
    - path: "web/src/App.jsx"
      provides: "Guarda de pregão no laço de notificação local (early return literal antes de tocar notifRef) + `mercado` na dep array; recap de boot deixa de emitir banner de sistema"
      contains: "if (!pregaoAberto) return;"
    - path: "web/tests/test_notif_pregao.mjs"
      provides: "Guardião source-grep: a LINHA LITERAL da guarda existe e vem antes de `notifRef.current[p.t]`, `mercado` na dep array, sem calendário em JS, recap de boot sem notify.send"
  key_links:
    - from: "server/app/agent.py D2/D3 (call sites de notify_push)"
      to: "server/app/main.py `_notify` → push.send_to_user(extra=...)"
      via: "kwarg `extra` novo na closure, repassado ao payload fora do `aps`"
      pattern: "extra=extra"
    - from: "server/app/push.py payload extra"
      to: "web/src/notify.js:381-382 (validação /^[A-Z]{4}\\d{1,2}$/) → App.jsx onPushTap"
      via: "campo `t` no payload — o servidor só o emite quando casa o MESMO formato que o cliente valida, senão o cliente descarta em silêncio"
      pattern: "\\^\\[A-Z\\]\\{4\\}"
    - from: "server/app/store.py:650 (`pnl` calculado dentro de sell)"
      to: "server/app/agent.py:879-884 (evento de proteção simulada)"
      via: "valor de RETORNO de store.sell, nunca recomposto de price/avg no agent — travado por âncora de fonte `src.count(\"pnl = store.sell(\") == 1`"
      pattern: "pnl = store\\.sell"
    - from: "server/app/main.py:971-982 (/api/market/status → pregao.in_market_hours)"
      to: "web/src/App.jsx estado `mercado` (6806) → laço de notificação local (7722)"
      via: "estado já existente no root, revalidado a cada 60s/visibilitychange/focus — o front consome, não reimplementa"
      pattern: "mercado\\.aberto"
---

<objective>
Seis defeitos determinísticos nas notificações push do Boris+, todos com causa
confirmada no código (mapeamento verificado contra o HEAD em 2026-08-24):

1. **Deep link** — `_notify` (`main.py:2788`) não aceita `extra`, então todo push
   do funil D chega sem `data.t`; `notify.js:382` descarta em silêncio e o toque
   abre o app sem destino.
2. **Título genérico** — `agent.py:1224` e `:1261` mandam o literal
   `"Agente Boris+ (simulado)"`; o `kind`/`motivo` do evento estruturado existe
   e é jogado fora (só `ev["text"]` viaja).
3. **P&L ausente** — `store.sell` calcula `pnl` (`store.py:650`) e `agent.py:879`
   descarta o retorno; o push de stop/alvo não diz o resultado.
4. **Sem registro** — gatilho e Radar não escrevem em `agent.events`; o único
   rastro do gatilho é a linha de ENTREGA (`agent.py:1034`), que vai só para a
   tela técnica; o Radar é fire-and-forget com `except: pass`.
5. **Notificação local fora do pregão** — o laço de stop/alvo/variação
   (`App.jsx:7722-7744`) dispara a cada mudança de `quotes`, inclusive com
   cotação stale de madrugada; o recap de boot (`:7086`, `:7103`) emite banner de
   sistema a qualquer hora.
6. **Texto do Radar** — o job das 08:45 é prévia pré-abertura por desenho
   (decisão travada D1), mas o texto não diz isso e lê como alerta fora de hora.

Purpose: fechar os cinco sintomas relatados pelo Alex sem tocar no que já
funciona (a navegação do lado do app) e sem re-litigar as decisões travadas
(horário do Radar, granularidade de preferência, central de notificações).

Output: payload com destino, título com o marco real, P&L vindo do motor,
evento de mercado visível em "EVENTOS E AVISOS RECENTES", notificação local
respeitando o pregão e prévia do Radar honesta sobre o próprio horário.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.claude/skills/didatica-boris/SKILL.md

<decisoes_travadas>
NÃO re-litigar, NÃO propor alternativa, NÃO perguntar de novo:

- **D1** — Radar às 08:45 FICA. `B3_RADAR_DAILY_HHMM`, o job e a audiência não
  mudam. `agent.py:1104-1107` documenta a escolha deliberada de
  `is_trading_day` em vez de `in_market_hours`. A correção é SÓ de vocabulário.
- **D2** — Preferências por classe de evento estão FORA deste quick task (vão
  num segundo, logo depois). NÃO tocar `push.PREFS_PADRAO` (`push.py:86`), nem o
  contrato de `register-token` (`main.py:2664-2690`), nem a paridade quebrada de
  `serverStore.syncPushPrefs` (`persistence.js:153-161`).
- **D3** — O "log" fecha em `agent.events` (`store.py:806`), que já existe e já
  tem tela (`App.jsx:4301-4309`). NÃO construir central de notificações
  recebidas — foi explicitamente descartado.

Fora de escopo por decisão: `aps.category`/ações interativas no banner,
roteamento do toque por `kind` (mudaria o contrato de navegação do cliente),
publicação (`bump.sh`, `publicar-web.sh`, `entregar.sh`, `cap sync ios`).
</decisoes_travadas>

<regra_de_teste_transversal>
**Asserção de CONTEÚDO nunca mora dentro de uma corrotina que o código de
produção aguarda sob `except Exception`.** O fake COLETA; o teste assere depois
do `_run(...)`.

Motivo, com prova executada: `radar_daily.py:241-244` faz
`try: await notify_push(...) except Exception: pass`. `AssertionError` é
subclasse de `Exception`, então qualquer `assert` dentro de `fake_push` é
ENGOLIDO — o guardião existente em `test_radar_daily.py:115-119` já é vácuo hoje
por essa razão (e `enviados.append(uid)` roda ANTES do assert, mantendo o teste
verde). O mesmo vale para `agent.py:1036` e `:1225` (`except Exception` em volta
de `notify_push`) e para o `except` novo em volta de `store.push_events`.

Isto vale para TODO fake criado ou editado neste plano. Única exceção legítima:
fake que LEVANTA de propósito para exercitar o caminho de falha — ali o
levantamento é o comportamento sob teste, não uma asserção.
</regra_de_teste_transversal>

<interfaces>
<!-- Verificado por leitura direta em 2026-08-24 contra o HEAD atual. -->

`server/app/push.py:171-205` — canal ÚNICO de saída. Já suporta `extra`; a
correção alimenta o parâmetro, não cria caminho novo:

  async def send_to_user(conn, user_id, title, body, som=True, prioridade="10",
                         client=None, extra: Optional[dict] = None) -> dict:
      ...
      payload = {"aps": aps}
      for k, v in (extra or {}).items():
          if v is not None and k != "aps":
              payload[k] = v

`server/app/main.py:2788-2789` — a closure que ESTRANGULA o `extra` hoje:

  async def _notify(uid, title, body):
      await push.send_to_user(_conn, uid, title, body)

`web/src/notify.js:375-389` — o lado do app, que JÁ FUNCIONA (não reescrever):

  const t = String(d.t || "").trim().toUpperCase().slice(0, 12);
  if (/^[A-Z]{4}\d{1,2}$/.test(t)) handler(t, String(d.kind || ""));

  → `App.jsx:7011-7035`: garante o ticker na watchlist, `setTab("mercado")`,
    reserva o dono proativo e rola até `#ativo-<T>`.

  CONSEQUÊNCIA CONFIRMADA: payload com `kind` e SEM `t` válido nunca chega ao
  handler — o `if` filtra antes. `extra` sem `t` é peso morto, não meia-função.

`server/app/agent.py:1013-1015` — a INTENÇÃO a preservar no caso agregado (o
comentário explica por que `t` é omitido quando há N ativos; não forçar
`tickers[0]`):

  extra = {"kind": "timing"}
  if len(tickers) == 1:
      extra["t"] = tickers[0]

`server/app/agent.py:879-884` — alvo dos itens 2 e 3. `store.sell` DEVOLVE o
`pnl` (`store.py:650`, `return pnl` em `store.py:661`) e o retorno é jogado fora.
Confirmado que este call site vende SEMPRE TOTAL (sem `qty=`), então o `pnl` é
realizado, em R$, com o sinal certo, e venda parcial não existe aqui:

  store.sell(conn, pos["t"], price, user_id=scope, motivo="stop" if breach_stop else "alvo",
             origem="automatico")  # ADR-012 (Fase 3) / ADR15-04
  executed += 1
  _bump_ops(conn, scope, store.get(conn, "agent", user_id=scope) or ag)
  events.append({"time": _now_str(), "kind": "buy",
                 "text": f"Proteção simulada: {pos['t']} vendido ({motivo}) a R$ {price:.2f}."})

`server/app/agent.py:696-699` — evento de entrada automática (sem `t`/`tag`):

  events.append({"time": _now_str(), "kind": "buy",
                 "text": f"Entrada automática: {ticker} comprado ({qty} ações, "
                         f"{par['allocPct']:.0f}% do caixa) a R$ {price:.2f} — gatilho de entrada "
                         f"atingido."})

`server/app/pending_orders.py:269-301` — os 3 eventos JÁ têm `tag`
(`pendente-cancelada` / `pendente-executada`) e NÃO têm `t`. O ticker está na
variável local `t` do laço.

`server/app/agent.py:803-809` + `:904` — o appMode real do usuário já é lido
dentro do ciclo e NÃO sai no retorno:

  cfg = store.get(conn, "config", user_id=scope) or {}
  app_mode = cfg.get("appMode") if isinstance(cfg, dict) else None
  ...
  return {"events": events, "executed": executed}

  ATENÇÃO ao mapeamento de nomes: `config.appMode` vale `"operador"` ou
  `"estudo"`; as chaves de `skill_ref` são `"operador"` e `"educacional"`.
  `run_cycle_for` tem um early-return sem `appMode` (`agent.py:785`) — ler
  sempre com `.get("appMode")`.

`server/app/store.py:806-810` — destino do item 4. Prepend com cap 50
(confirmado por leitura; nada além do cap acontece):

  def push_events(conn, events: list, cap: int = 50, user_id=None) -> dict:
      ag = get(conn, "agent", user_id=user_id)
      ag["events"] = (events + ag.get("events", []))[:cap]
      db.kv_set(conn, "agent", ag, user_id=user_id)
      return ag

`web/src/App.jsx:4301-4309` — a tela que consome `agent.events`. Lê `e.kind`
(`"warn"` → âmbar, `"buy"` → verde, resto → cinza), `e.text` e `e.time`.
Campos extras (`t`, `tag`, `pnl`) são invisíveis para ela: adição é segura.

`server/app/radar_daily.py:201-245` — alvo dos itens 4 e 6. Imports do módulo
hoje: `db, scanner, candles_mod` (`store` ainda NÃO está importado; `store`
importa só `db`/`defaults`/`catalog`, então não há ciclo — import de módulo é
seguro).

`server/app/main.py:971-982` — a FONTE CANÔNICA do pregão que o front já
consome (rota pública, `pregao.in_market_hours()`, janela 10:00–16:55 BRT com
feriados fixos e móveis):

  return {"aberto": pregao.in_market_hours(), "diaDePregao": ..., "abertura": ...}

`web/src/App.jsx:6806-6835` — o estado do root que consome essa rota. É a peça
que o item 5 precisa e que o laço de notificação simplesmente não olha:

  const [mercado, setMercado] = useState(null);
  // null = 1ª consulta em voo; { erro: true } = falha de rede (nunca "chuta")
  ... revalidado a cada 60s + visibilitychange + focus

  `App()` começa em `App.jsx:6750` e o laço de notificação está em `:7722` —
  MESMO escopo, `mercado` está disponível sem prop drilling nem contexto novo.

`web/src/App.jsx:7720-7744` — o laço a corrigir (item 5). `notifRef.current[p.t]`
só é tocado em `:7728`, DENTRO do `for` de `:7725`; um `return` posicionado entre
`:7724` e `:7725` não cria a entrada e não consome o `armado`:

  const notifRef = useRef({});
  useEffect(() => {
    const n = data && data.config && data.config.notif;
    if (!n || !n.enabled) return;
    for (const p of (data.positions || [])) {
      ...
      const st = notifRef.current[p.t] || (notifRef.current[p.t] = { stopArmed: true, alvoArmed: true, varKey: "" });
      ...
    }
  }, [quotes, data]);

`web/src/App.jsx:7074-7108` — o recap de boot (item 5, segundo alvo). Roda no
BOOT, com o app em primeiro plano, e emite `flash` E `notify.send`:

  const agentSummaryDone = useRef(false);
  ... flash(msg); notify.send("Agente Boris+", msg);
  ... flash(e.text); notify.send("Boris+", e.text);

`web/src/notify.js:184-217` — `send()` no nativo agenda o banner E chama
`_emitForeground(title, body)`, que cai no handler registrado em
`App.jsx:6919` (→ `flash`). Ou seja: no recap de boot a mensagem já aparece
in-app por DOIS caminhos; o banner de sistema é o único componente
"fora de hora".

`web/src/copy.js:74-82` e `:211-221` — precedente da casa: texto de notificação
LOCAL (só front) vive em `copy.js` SEM espelho em `skill_ref.py`. Simetricamente,
texto de push do SERVIDOR (só backend, o front nunca renderiza) vive em
`skill_ref.py` SEM espelho em `copy.js`. A disciplina de espelho existe contra
DIVERGÊNCIA entre os dois lados; criar chave morta no outro lado não é espelho,
é lixo. NÃO adicionar as chaves novas a `copy.js` nem a `CHAVES_ESPERADAS` de
`web/tests/test_vocabulario_espelho.mjs` (esse guardião enumera só
HISTORICO/HISTORICO_ROTULO/ENTRADA_AUTO e não é afetado por dicts novos).

ATENÇÃO — não confundir os dois "stop": `copy.js` tem
`notifStopTitulo` = "Stop acionado · X" / "STOP executável · X" para o alerta
LOCAL (o preço TOCOU o stop, nada foi feito). O título novo do backend é para
uma VENDA JÁ EXECUTADA pelo Operador. São eventos diferentes — não unificar.

Guardiões existentes que ESTE plano vai fazer falhar (atualizar com nota,
NUNCA apagar — regra do CLAUDE.md):
  • `server/tests/test_ordens_pendentes_scheduler.py:147,180` — spies
    `async def fake_notify(uid, title, body)` (quebram com o kwarg `extra`) e
    `assert titulos == {"Agente Boris+ (simulado)"}` (:154), que hoje TRAVA
    justamente o defeito do item 2. Este assert está FORA da corrotina (o fake só
    faz `pushes.append(...)`), então é um guardião REAL — ele vai ficar vermelho
    de verdade.
  • `server/tests/test_radar_daily.py:115-119` — guardião VÁCUO (ver
    `<regra_de_teste_transversal>`): NÃO vai ficar vermelho sozinho. Precisa ser
    reescrito para poder falhar. Ver Task 2, passo 6.
  • `server/tests/test_radar_daily.py:187-192`
    (`test_run_daily_usa_o_push_body`) — exige que a linha
    `corpo = push_body(payload)` continue existindo LITERAL, 1×. Preservar.
  • `server/tests/test_radar_daily.py:133-184` — o corpo do Radar precisa
    CONTINUAR contendo `"{n} ativos analisados"`, `"nenhum setup em destaque"`,
    `"{n} ativo(s) analisados"`, o veredito em minúsculas junto do percentual,
    e nenhum imperativo de operação. O texto novo só pode ENVELOPAR isso.

Guardiões de front que cobrem a região da Task 3 e que o checker confirmou NÃO
quebrarem com a mudança — mas que precisam rodar no `<verify>` para o feedback
não chegar tarde:
  • `web/tests/test_status_mercado_ui.mjs:45-79` — trava `store.marketStatus(`
    aparecendo exatamente 1× em `App.jsx` e inspeciona a janela a partir de
    `const [mercado, setMercado] = useState(null);`.
  • `web/tests/test_copy_theme.mjs:120` — exige que `cp.notifStopTitulo(`,
    `cp.notifAlvoTitulo(` e `cp.notifVarTitulo(` continuem sendo CHAMADOS de
    `App.jsx`. São exatamente as três linhas dentro do laço que a guarda passa a
    proteger: mover/apagar qualquer uma delas quebra este guardião.
  • `web/tests/test_logout_reset.mjs:40` — exige `notifRef.current = {}` em
    `_resetScopeState`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: payload com destino, título com o marco real e P&L da venda (itens 1, 2, 3)</name>
  <files>server/app/skill_ref.py, server/app/agent.py, server/app/pending_orders.py, server/app/main.py, server/tests/test_push_payload.py, server/tests/test_ordens_pendentes_scheduler.py</files>
  <read_first>
    - server/app/skill_ref.py linhas 239-273 (`TIMING` + `timing_txt`) — o PADRÃO exato a seguir: dict por modo, helper com degradação definida
    - server/app/agent.py linhas 660-711 (`_avaliar_entradas`, fim) e 838-904 (`_avaliar_saidas` + retorno de `_run_cycle_inner`)
    - server/app/agent.py linhas 1195-1265 (funis D2 e D3 do push no `scheduler_loop`)
    - server/app/pending_orders.py linhas 255-302 (os 3 `eventos.append`)
    - server/app/main.py linhas 2788-2797 (`_notify` + injeção no scheduler)
    - server/tests/test_ordens_pendentes_scheduler.py linhas 140-190 (spies e asserção de título a atualizar)
    - server/tests/test_agent.py linhas 1-45 (convenção de fixture: `_conn`, `_seed`, `_quotes`, `_run`)
    - server/tests/test_radar_daily.py linhas 187-192 (`test_run_daily_usa_o_push_body`) — o IDIOMA de âncora de fonte a reusar no teste de proveniência do P&L
  </read_first>
  <behavior>
    - `skill_ref.push_titulo("operador", "stop", "PETR4")` devolve o título de mesa com o ticker; `push_titulo("educacional", "stop", "PETR4")` devolve a voz de professor. Modo desconhecido cai em `"educacional"` (mesma degradação de `timing_txt`).
    - `skill_ref.push_titulo(modo, None, "PETR4")` e `push_titulo(modo, "tag-que-nao-existe", "PETR4")` devolvem o genérico do modo — nunca `KeyError`, nunca frase com `{t}` cru.
    - `skill_ref.push_titulo(modo, "stop", "")` (evento sem ticker, ex.: contrato de opção filtrado) devolve o genérico — nunca `"Stop acionado (simulado) · "` com o sufixo pendurado.
    - TODO título de `PUSH_TITULOS` declara que é simulado (princípio 1 do CLAUDE.md): os corpos de entrada automática e de ordem pendente NÃO dizem "simulado", então o título é o único lugar que sustenta a declaração.
    - Push de ordem pendente executada chega com `extra["t"]` igual ao ticker da ordem e título nomeando "Pendente executada"; push de cancelamento, com "Pendente cancelada".
    - Push de stop do Operador chega com `extra["t"]` igual ao ticker da posição, título nomeando o stop, e corpo contendo o resultado realizado com o MESMO valor que `store.sell` devolveu.
    - Push de alvo idem, com o título do alvo. Push de entrada automática idem, com o título de entrada.
    - A proveniência do P&L é verificável no CÓDIGO, não só no número: `agent.py` contém exatamente uma ocorrência de `pnl = store.sell(` e nenhuma recomposição de `(price - avg) * qty` no caminho do evento.
    - Evento cujo `t` NÃO casa `^[A-Z]{4}\d{1,2}$` (contrato de opção etc.) sai SEM `extra["t"]` — o cliente descartaria em silêncio e o campo seria peso morto no payload.
    - `_notify` chamado sem `extra` (chamador antigo) continua funcionando — `extra=None` é o default e `send_to_user` já trata `None`.
  </behavior>
  <action>
    1. **`server/app/skill_ref.py`** — logo APÓS `timing_txt` (linha ~273) e ANTES do bloco `HISTORICO` (linha ~275), acrescentar a seção de vocabulário do push do Operador, no mesmo formato de `TIMING`/`timing_txt` (dict por modo + helper). Comentário de cabeçalho registrando: (a) que é texto de saída SÓ do backend, sem espelho em `copy.js` porque o front nunca o renderiza; (b) que o sufixo "(simulado)" é obrigatório porque os corpos de entrada automática e de ordem pendente não o carregam; (c) que estes títulos são de operação JÁ EXECUTADA e não devem ser confundidos com `copy.js notifStopTitulo`, que é o alerta local de preço tocando o nível.

    Chaves (tags) e strings exatas:

      PUSH_TITULOS = {
          "operador": {
              "stop": "STOP executado (simulado) · {t}",
              "alvo": "ALVO atingido (simulado) · {t}",
              "entrada-auto": "ENTRADA automática (simulada) · {t}",
              "pendente-executada": "Pendente executada (simulada) · {t}",
              "pendente-cancelada": "Pendente cancelada (simulada) · {t}",
              "generico": "Operador Boris+ (simulado)",
          },
          "educacional": {
              "stop": "Stop acionado (simulado) · {t}",
              "alvo": "Alvo atingido (simulado) · {t}",
              "entrada-auto": "Entrada simulada · {t}",
              "pendente-executada": "Pendente executada (simulada) · {t}",
              "pendente-cancelada": "Pendente cancelada (simulada) · {t}",
              "generico": "Agente Boris+ (simulado)",
          },
      }

    Helper, com a degradação do `<behavior>` explícita no corpo:

      def push_titulo(modo: str, tag: str = "", ticker: str = "") -> str:
          d = PUSH_TITULOS.get(modo if modo in PUSH_TITULOS else "educacional", PUSH_TITULOS["educacional"])
          frase = d.get(tag or "")
          if not frase or ("{t}" in frase and not ticker):
              return d["generico"]      # tag desconhecida ou sem ticker: título inteiro, nunca frase quebrada
          return frase.replace("{t}", ticker)

    Guardrail de vocabulário: nenhum título carrega verbo imperativo de operação
    (o guardrail regulatório do `push_body` vale aqui também) — descrevem o que o
    SIMULADOR fez, nunca o que a pessoa deve fazer.

    2. **`server/app/pending_orders.py`** — acrescentar `"t": t` aos 3 dicts de
    `eventos.append` (linhas ~269, ~287, ~297). Adição pura: `tag` já existe, o
    consumidor do front (`App.jsx:4301`) ignora campos que não conhece.

    3. **`server/app/agent.py` — evento de saída (linhas 879-884)**: capturar o
    retorno de `store.sell` em `pnl` e enriquecer o evento:

      pnl = store.sell(conn, pos["t"], price, user_id=scope, motivo="stop" if breach_stop else "alvo",
                       origem="automatico")  # ADR-012 (Fase 3) / ADR15-04
      executed += 1
      _bump_ops(conn, scope, store.get(conn, "agent", user_id=scope) or ag)
      _txt = f"Proteção simulada: {pos['t']} vendido ({motivo}) a R$ {price:.2f}."
      if pnl is not None:
          _txt += f" Resultado realizado: R$ {pnl:+.2f}."
      events.append({"time": _now_str(), "kind": "buy", "t": pos["t"],
                     "tag": "stop" if breach_stop else "alvo", "pnl": pnl, "text": _txt})

    Comentário obrigatório na linha do `pnl`: o número vem do RETORNO de
    `store.sell` (`store.py:650`), o motor determinístico — recompor
    `(price - avg) * qty` aqui violaria o princípio 5 do CLAUDE.md. `pnl is None`
    (posição sumiu entre a leitura e a venda) mantém o texto de hoje, sem
    inventar resultado.

    4. **`server/app/agent.py` — evento de entrada automática (linhas 696-699)**:
    acrescentar `"t": ticker, "tag": "entrada-auto"` ao dict, preservando o
    `text` atual byte a byte.

    NÃO tocar os eventos de `_avaliar_opcoes`: contrato de opção não é ticker de
    ação, cai no genérico por desenho (v2 de opções não está em escopo).

    5. **`server/app/agent.py` — retorno do ciclo (linha 904)**: devolver também
    o modo já lido em `:808`, para o funil D3 não precisar de leitura extra de kv:

      return {"events": events, "executed": executed, "appMode": app_mode}

    6. **`server/app/agent.py` — topo do módulo**: acrescentar, junto das demais
    constantes de módulo, o filtro que espelha a validação do cliente:

      # ESPELHO DELIBERADO de `web/src/notify.js:382`: o app valida o ticker do
      # payload contra este MESMO formato e DESCARTA EM SILÊNCIO o que não casar.
      # Mandar `t` fora do formato (contrato de opção, por ex.) é peso morto no
      # payload e ruído no diagnóstico — filtramos na origem.
      _TICKER_PUSH_RE = re.compile(r"^[A-Z]{4}\d{1,2}$")

    Conferir se `re` já está importado no módulo; se não, importar.

    7. **`server/app/agent.py` — funil D2, ordens pendentes (linhas 1197-1226)**:
    derivar título + `extra` do próprio evento. A leitura de kv do modo é
    CONDICIONAL — `executar_pendentes` devolve lista vazia na esmagadora maioria
    dos ticks, e uma leitura de `config` por uid por tick sem nenhum push a
    emitir seria trabalho jogado fora:

      if eventos:
          _modo = "operador" if (store.get(conn, "config", user_id=uid) or {}).get("appMode") == "operador" else "educacional"
      for ev in eventos:
          ...

    e, no lugar da chamada atual:

      _tk = str(ev.get("t") or "")
      _extra = {"kind": "ordem"}
      if _TICKER_PUSH_RE.match(_tk):
          _extra["t"] = _tk
      await notify_push(uid, skill_ref.push_titulo(_modo, ev.get("tag"), _tk), ev["text"], extra=_extra)

    (`skill_ref` já está importado em `agent.py:30`.)

    8. **`server/app/agent.py` — funil D3, ciclo do Operador (linhas 1257-1263)**:
    trocar a lista de textos por iteração sobre os eventos, preservando o filtro
    `kind == "buy"` e o `try/except` best-effort com `_registrar_push_falho()`:

      if notify_push and r["executed"]:
          _modo = "operador" if r.get("appMode") == "operador" else "educacional"
          for e in r["events"]:
              if e.get("kind") != "buy":
                  continue
              _tk = str(e.get("t") or "")
              _extra = {"kind": "operador"}
              if _TICKER_PUSH_RE.match(_tk):
                  _extra["t"] = _tk
              try:
                  await notify_push(uid, skill_ref.push_titulo(_modo, e.get("tag"), _tk), e["text"], extra=_extra)
              except Exception:  # noqa: BLE001 — push é best-effort
                  _registrar_push_falho()

    9. **`server/app/main.py` (linhas 2788-2789)**: alargar a closure, com
    comentário de UMA linha dizendo que sem isto o toque no push não tem destino:

      async def _notify(uid, title, body, extra=None):
          await push.send_to_user(_conn, uid, title, body, extra=extra)

    10. **`server/tests/test_ordens_pendentes_scheduler.py`** — ATUALIZAR (nunca
    apagar) os dois guardiões que travam o comportamento antigo:
      - spies (linhas 147 e 180): assinatura vira
        `async def fake_notify(uid, title, body, extra=None)` e passa a guardar
        `(uid, title, body, extra)`. O fake continua SÓ COLETANDO — nenhuma
        asserção dentro dele (`agent.py:1225` tem `except Exception` em volta,
        engoliria);
      - asserção de título (linha ~154): `assert titulos == {"Agente Boris+ (simulado)"}`
        vira a asserção dos títulos NOVOS, com comentário-nota registrando que o
        literal genérico era o defeito 2 deste quick task e que o guardião mudou
        de lado de propósito;
      - acrescentar, nos dois testes, a asserção de que cada push carrega
        `extra["t"]` com o ticker da ordem (o guardião do item 1 pelo caminho da
        FIAÇÃO real do scheduler).

    11. **`server/tests/test_push_payload.py`** (arquivo novo, seguindo as
    fixtures de `test_agent.py`) — cobrir, cada um falhando antes desta task:
      - `push_titulo` nos dois modos para cada tag; tag desconhecida, tag `None`
        e ticker vazio caindo no genérico; modo inexistente caindo em
        `"educacional"`;
      - todo valor de `PUSH_TITULOS` contém "simulad" (princípio 1) e nenhum
        contém imperativo de operação (mesmos regex de
        `test_push_body_respeita_o_guardrail_regulatorio`);
      - ciclo com stop batido: o evento tem `t`, `tag == "stop"`, `pnl` igual ao
        `pnl` gravado em `history[0]["pnl"]` por `store.sell`, e o `text` contém
        "Resultado realizado";
      - **âncora de PROVENIÊNCIA do P&L** (idioma de `test_run_daily_usa_o_push_body`):
        a igualdade numérica acima NÃO prova origem — como este call site vende
        sempre total, um `round((price - pos["avg"]) * pos["qty"], 2)` recomposto
        daria exatamente o mesmo número e passaria. O princípio 5 precisa de
        âncora de FONTE, dentro da suíte (o `grep` de `<acceptance_criteria>` não
        roda no CI):

          import pathlib
          src = (pathlib.Path(__file__).resolve().parents[1] / "app" / "agent.py").read_text(encoding="utf-8")
          assert src.count("pnl = store.sell(") == 1, "o pnl tem que vir do RETORNO de store.sell"
          assert "Resultado realizado: R$ {pnl:+.2f}" in src, "o texto formata o pnl recebido, não um recomposto"
          assert 'pos["avg"]' not in src.split("pnl = store.sell(")[1].split("events.append")[0], \
              "nada de recompor (price - avg) * qty entre a venda e o evento (CLAUDE.md princípio 5)"

      - ciclo com alvo batido: `tag == "alvo"`;
      - entrada automática: `tag == "entrada-auto"` e `t` preenchido;
      - `scheduler_loop(once=True)` com spy de 4 argumentos que só COLETA: os
        pushes do Operador chegam com título != `"Agente Boris+ (simulado)"` e
        com `extra["t"]` casando `^[A-Z]{4}\d{1,2}$` (asserções DEPOIS do
        `asyncio.run`, nunca dentro do spy);
      - âncora de fonte: `agent.py` NÃO contém mais o literal
        `"Agente Boris+ (simulado)"` em nenhuma chamada de `notify_push`.
  </action>
  <verify>
    <automated>cd server && ./.venv/bin/python -m pytest tests/test_push_payload.py tests/test_ordens_pendentes_scheduler.py tests/test_agent.py tests/test_timing_watch.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Agente Boris+ (simulado)" server/app/agent.py` retorna 0 (o literal migrou para `skill_ref.PUSH_TITULOS["educacional"]["generico"]`).
    - `grep -n "extra=extra" server/app/main.py` retorna 1 linha (dentro de `_notify`).
    - `grep -c "pnl = store.sell(" server/app/agent.py` retorna 1.
    - `grep -n "def push_titulo" server/app/skill_ref.py` retorna 1 linha.
    - `cd server && ./.venv/bin/python -m pytest tests/ -q` verde (suíte inteira, não só os arquivos tocados — o alargamento de assinatura pode atingir spy em teste não previsto).
    - `git diff server/app/push.py` VAZIO (o canal de saída não muda — só passa a ser alimentado).
    - `git diff` não toca `server/app/push.py:86` (`PREFS_PADRAO`), `server/app/main.py:2664-2690` (`register-token`) nem `web/src/persistence.js` — decisão D2.
  </acceptance_criteria>
  <done>`_notify` repassa `extra`; os pushes de D2/D3 chegam com `data.t` no formato que `notify.js` valida e com título derivado do `tag` do evento no vocabulário do modo; o P&L do stop/alvo vem do retorno de `store.sell`, com a proveniência travada por âncora de fonte dentro da suíte; os guardiões que travavam o título genérico foram atualizados com nota, não apagados; suíte pytest inteira verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: evento de mercado em agent.events e prévia pré-abertura do Radar (itens 4, 6)</name>
  <files>server/app/skill_ref.py, server/app/agent.py, server/app/radar_daily.py, server/tests/test_push_registro_evento.py, server/tests/test_radar_daily.py</files>
  <read_first>
    - server/app/agent.py linhas 988-1038 (`_avisar_gatilhos` completo — a closure `_enviar` e a linha de ENTREGA que hoje é o único rastro)
    - server/app/radar_daily.py linhas 185-259 (`_push_audience`, `push_body`, `run_daily`, `maybe_run`)
    - server/app/store.py linhas 806-810 (`push_events`) e linha 169 (`now_str`)
    - server/tests/test_radar_daily.py linhas 105-192 (TODOS os guardiões de `push_body`, o de ancoragem, e o guardião VÁCUO de título a reescrever)
    - server/tests/test_timing_watch.py (convenção de fixture para exercitar `_avisar_gatilhos`/`timing_watch`)
    - o bloco `<regra_de_teste_transversal>` deste plano — vale para todo fake criado ou editado nesta task
  </read_first>
  <behavior>
    - Depois de um aviso de gatilho, `store.get(conn, "agent", user_id=uid)["events"]` contém o evento de mercado com o corpo canônico de `timing_watch.mensagem` — o mesmo texto que foi ao push, sem redação nova.
    - O evento do gatilho é registrado MESMO quando a entrega do push falha (`send_to_user` devolvendo `sent=0`): o evento de mercado aconteceu; a entrega é outra coisa e continua sendo registrada por `push_agent_log` (`agent.py:1034`), que NÃO muda.
    - Aviso de gatilho de UM ativo registra `t` com o ticker; agregado de N ativos registra `t: None` — a mesma intenção de `agent.py:1013-1015`, nunca eleger um ticker arbitrário.
    - Depois da varredura automática do Radar, cada usuário da audiência tem em `agent.events` o evento com o corpo da prévia.
    - Falha ao registrar o evento não impede o push, e falha no push não impede o registro (dois `try/except` independentes) — o `except: pass` de hoje engoliria os dois juntos.
    - Título e corpo do push do Radar dizem, em texto, que é prévia PRÉ-ABERTURA e que o pregão ainda não abriu.
    - O guardião do título do Radar é capaz de FALHAR: uma regressão ao literal `"Radar do dia 📡"` deixa a suíte vermelha. (Hoje não deixaria — ver `<regra_de_teste_transversal>`.)
    - O corpo continua nomeando o top-3 COM o veredito, continua dizendo "{n} ativos analisados", continua dizendo "nenhum setup em destaque" quando não há confluência, e continua sem imperativo de operação — o texto novo ENVELOPA o de hoje, não o substitui.
    - Nenhuma mudança em `should_run`, `B3_RADAR_DAILY_HHMM`, `_push_audience` ou no gate `pregao.is_trading_day()` de `agent.py:1109` (decisão D1).
  </behavior>
  <action>
    1. **`server/app/skill_ref.py`** — logo abaixo de `push_titulo` (Task 1),
    acrescentar o vocabulário do Radar. Comentário de cabeçalho registrando a
    decisão D1 (o horário das 08:45 é deliberado; o defeito era o texto não dizer
    isso) e por que este dict, ao contrário de `PUSH_TITULOS`, NÃO é por modo: a
    audiência do Radar é `radar_daily._push_audience` ("todo mundo com token"),
    não um escopo com `appMode` resolvido, e a prévia é leitura de mercado sem
    verbo de carteira — resolver voz por usuário custaria uma leitura de config
    por usuário por dia sem mudar o conteúdo.

      PUSH_RADAR = {
          "titulo": "Prévia do Radar · pré-abertura 📡",
          "corpo_destaques": "Prévia pré-abertura: maior confluência em {itens}. O pregão ainda não abriu — a abertura pode mudar estes preços. Abra para ver o plano e o risco ({n} ativos analisados).",
          "corpo_vazio": "Prévia pré-abertura: varredura concluída, {n} ativo(s) analisados, nenhum setup em destaque hoje. O pregão ainda não abriu. Abra o Radar para estudar.",
      }

    NÃO acrescentar nada disso a `web/src/copy.js` nem a `CHAVES_ESPERADAS` de
    `web/tests/test_vocabulario_espelho.mjs` — ver a nota no bloco `<interfaces>`.

    2. **`server/app/radar_daily.py`** — importar `skill_ref` e `store` na linha
    de import do pacote (`from . import db, scanner` vira
    `from . import db, scanner, skill_ref, store`; confirmado que não há ciclo).

    3. **`server/app/radar_daily.py` `push_body` (linhas 201-223)** — manter a
    docstring atual (ela carrega o porquê do veredito obrigatório, qa/43) e
    acrescentar um parágrafo sobre a prévia. Trocar SÓ as duas f-strings de
    retorno por formatação sobre `skill_ref.PUSH_RADAR`, preservando a
    composição de `itens` (ticker + percentual + veredito minúsculo) onde está:

      if not destaques:
          return skill_ref.PUSH_RADAR["corpo_vazio"].replace("{n}", str(n))
      itens = " · ".join(...)          # INALTERADO
      return skill_ref.PUSH_RADAR["corpo_destaques"].replace("{itens}", itens).replace("{n}", str(n))

    4. **`server/app/radar_daily.py` `run_daily` (linhas 238-244)** — trocar o
    título literal por `skill_ref.PUSH_RADAR["titulo"]`, registrar o evento e
    separar os dois `try`. A linha `corpo = push_body(payload)` fica LITERAL
    (guardião de ancoragem `test_run_daily_usa_o_push_body` conta essa substring):

      if notify_push and origem == "automática":
          corpo = push_body(payload)   # qa/43: nomeia o top-N em vez de só contar
          for uid in _push_audience(conn):
              # try PRÓPRIO: o registro do evento é o rastro DURÁVEL (a tela
              # "EVENTOS E AVISOS RECENTES"); a entrega do push é efêmera. Um
              # `except` só, como antes, fazia a falha de um matar o outro.
              try:
                  store.push_events(conn, [{"time": store.now_str(), "kind": "info",
                                            "tag": "radar-diario", "text": corpo}], user_id=uid)
              except Exception:  # noqa: BLE001 — registro é best-effort
                  pass
              try:
                  await notify_push(uid, skill_ref.PUSH_RADAR["titulo"], corpo)
              except Exception:  # noqa: BLE001 — push é best-effort
                  pass

    NÃO passar `extra` aqui, e comentar por quê: a prévia nomeia N tickers e não
    tem destino único; eleger `destaques[0]` cometeria o erro de ticker trocado
    que `agent.py:1008-1015` existe para evitar. E `extra` só com `kind`, sem
    `t`, seria peso morto — `notify.js:382` filtra por
    `^[A-Z]{4}\d{1,2}$` ANTES de chamar o handler, então um payload sem `t`
    válido nunca chega a lugar nenhum. Rotear o toque por `kind` exigiria mudar
    o contrato de navegação de `notify.js`, fora de escopo.

    Limite deliberado a registrar em comentário: o evento entra só para
    `_push_audience` (quem tem token), a MESMA audiência do push — escrever para
    toda a base seria um `kv_set` por usuário por dia sem push correspondente.

    5. **`server/app/agent.py` `_avisar_gatilhos._enviar` (linhas 1007-1037)** —
    ANTES do `try` do envio (para o evento sobreviver a uma falha de entrega),
    registrar o evento de mercado, com `try` próprio:

      try:
          store.push_events(conn, [{"time": _now_str(), "kind": "info",
                                    "tag": "timing-gatilho",
                                    "t": tickers[0] if len(tickers) == 1 else None,
                                    "text": corpo}], user_id=uid)
      except Exception as e:  # noqa: BLE001 — registro nunca derruba o aviso
          print(f"[timing_watch] registro do evento para {uid[:8]}… falhou: {e}")

    Comentário obrigatório: até aqui o único rastro era a linha de ENTREGA
    (`Aviso de condição atingida enviado a N/M aparelho(s)`) no `agentLog`, tela
    técnica — ela registra o ENVIO, não o evento de mercado, e continua existindo
    sem alteração. `t` só no caso de um ativo, pela mesma razão de `:1008-1015`.

    NÃO tocar `timing_watch.py`, `push.prefs_for`, o kill-switch nem
    `_usuarios_com_consentimento` — audiência e consentimento não mudam.

    6. **`server/tests/test_radar_daily.py`** — ATUALIZAR (nunca apagar) o
    guardião de título das linhas 115-119.

    **ATENÇÃO: esse guardião já é VÁCUO hoje, e uma atualização in-place
    produziria outro guardião vácuo.** Prova: o `assert "Radar do dia" in title`
    roda DENTRO da corrotina `fake_push`, que `run_daily` aguarda dentro do
    `except Exception: pass` (`radar_daily.py:241-244`); `AssertionError` é
    subclasse de `Exception`, então é engolido. E `enviados.append(uid)` roda
    ANTES do assert, então `assert enviados == ["u1"]` fica verde de qualquer
    jeito. Um assert que nunca pode bater não é guardião — mantê-lo assim seria o
    "afrouxar para asserção vazia" que o CLAUDE.md proíbe.

    A correção é MOVER a asserção para FORA da corrotina: o fake só COLETA, o
    teste assere depois do `_run(...)`:

      async def fake_push(uid, title, body):
          enviados.append((uid, title))     # SÓ COLETA: assert aqui dentro é engolido
      _run(radar_daily.run_daily(c, _fake_fetch, notify_push=fake_push))
      assert [u for u, _t in enviados] == ["u1"]
      titulos = [t for _u, t in enviados]
      assert titulos == [skill_ref.PUSH_RADAR["titulo"]]
      assert "pré-abertura" in titulos[0].lower()

    Comentário-nota obrigatório registrando (a) a decisão D1 e a data
    (2026-08-24), e (b) que a asserção MUDOU DE LUGAR porque a versão
    in-corrotina era engolida pelo `except` do best-effort — defeito latente
    PREEXISTENTE, não introduzido por este plano.

    Confirmar, sem editar, que os guardiões de `push_body` (linhas 133-184)
    continuam verdes com o texto novo; se algum quebrar, o texto novo está errado
    — corrigir o TEXTO, não o guardião.

    7. **`server/tests/test_push_registro_evento.py`** (arquivo novo) — cobrir,
    cada um falhando antes desta task. **Todo fake aqui só COLETA**; as asserções
    de conteúdo ficam depois do `_run(...)`/`asyncio.run(...)`, nunca dentro da
    corrotina (`agent.py:1036` e o `except` novo do `push_events` engoliriam):
      - `run_daily` com audiência de 1 usuário: `agent.events` do usuário passa a
        conter um item com `tag == "radar-diario"` e `text` igual ao `push_body`
        do mesmo payload;
      - `run_daily` com `notify_push` que LEVANTA: o evento continua registrado
        (prova da separação dos dois `try`). Este é o único fake do plano que
        levanta de propósito — o levantamento É o comportamento sob teste, não
        uma asserção engolida;
      - `push_body` (destaques e vazio) contém "pré-abertura" e "não abriu", e
        continua contendo as substrings que os guardiões qa/43 exigem;
      - `PUSH_RADAR["titulo"]` contém "prévia"/"pré-abertura" (case-insensitive);
      - `_avisar_gatilhos` com 1 ativo: `agent.events` ganha item com
        `tag == "timing-gatilho"` e `t` igual ao ticker;
      - `_avisar_gatilhos` com N >= `timing_watch.AGREGA_A_PARTIR_DE` ativos:
        item registrado com `t is None`;
      - com `send_to_user` devolvendo `sent=0`: o evento é registrado assim mesmo
        e a linha de entrega em `agentLog` continua sendo escrita (as duas
        estruturas coexistem);
      - âncora de fonte: `radar_daily.py` não contém mais o literal
        `"Radar do dia 📡"` e `agent.py` chama `store.push_events` dentro de
        `_avisar_gatilhos`.
  </action>
  <verify>
    <automated>cd server && ./.venv/bin/python -m pytest tests/test_push_registro_evento.py tests/test_radar_daily.py tests/test_timing_watch.py tests/test_fase3_timing_watch_kill.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Radar do dia" server/app/radar_daily.py` retorna 0.
    - `grep -n "PUSH_RADAR" server/app/skill_ref.py server/app/radar_daily.py` retorna pelo menos 4 linhas (definição + título + os dois corpos em uso).
    - `grep -c "corpo = push_body(payload)" server/app/radar_daily.py` retorna 1 (guardião de ancoragem preservado).
    - `grep -n "push_events" server/app/agent.py` inclui uma ocorrência dentro de `_avisar_gatilhos`.
    - Prova de que o guardião de título do Radar deixou de ser vácuo: com o título revertido à mão para `"Radar do dia 📡"`, `pytest tests/test_radar_daily.py -q` fica VERMELHO. Desfazer a reversão depois de confirmar.
    - `cd server && ./.venv/bin/python -m pytest tests/ -q` verde.
    - `git diff server/app/timing_watch.py` VAZIO.
    - `git diff server/app/radar_daily.py` não toca `should_run`, `enabled`, `_push_audience` nem a constante de horário (decisão D1).
    - `git diff web/src/copy.js` VAZIO e `git diff web/tests/test_vocabulario_espelho.mjs` VAZIO.
  </acceptance_criteria>
  <done>Gatilho e Radar registram o evento de mercado em `agent.events` (visível em "EVENTOS E AVISOS RECENTES") de forma independente do sucesso da entrega; o push do Radar diz em título e corpo que é prévia pré-abertura, preservando veredito, contagem e guardrail regulatório; o guardião do título passou a ser capaz de falhar (asserção fora da corrotina); horário, audiência e gate do job intactos.</done>
</task>

<task type="auto">
  <name>Task 3: guarda de pregão nas notificações locais do front (item 5)</name>
  <files>web/src/App.jsx, web/tests/test_notif_pregao.mjs</files>
  <read_first>
    - web/src/App.jsx linhas 6800-6840 (estado `mercado`, o que significam `null` e `{ erro: true }`, e a revalidação por 60s/visibilitychange/focus)
    - web/src/App.jsx linhas 7072-7108 (recap de boot: `agentSummaryDone`, `flash` + `notify.send`)
    - web/src/App.jsx linhas 7720-7744 (laço de notificação local: `notifRef`, os 3 ramos, a dep array)
    - web/src/notify.js linhas 184-225 (`send` chama `_emitForeground`; `notifyIfEnabled` só filtra por preferência, não por horário)
    - web/tests/test_notif_central.mjs (convenção de guardião source-grep: leitura de fonte + regex sobre um chunk, sem render)
    - web/tests/test_status_mercado_ui.mjs linhas 40-80 e web/tests/test_copy_theme.mjs linha 120 (os dois guardiões que já cobrem esta região — entender o que NÃO pode ser movido)
  </read_first>
  <action>
    1. **`web/src/App.jsx` — laço de notificação local (linhas 7722-7744)**:
    acrescentar a guarda IMEDIATAMENTE depois de `if (!n || !n.enabled) return;`
    e ANTES do `for (const p of ...)`, com o comentário que justifica cada
    decisão. A forma é `if (!pregaoAberto) return;` — early return, NÃO um
    `continue` dentro do laço (um `continue` depois de
    `const st = notifRef.current[p.t] || (notifRef.current[p.t] = {...})` já
    teria criado a entrada e consumido o `armado`, que é exatamente o invariante
    que esta guarda existe para preservar):

      // Guarda de pregão (quick task 260824-i45, item 5). `mercado` vem de
      // /api/market/status → pregao.in_market_hours(): a fonte CANÔNICA, com
      // feriado fixo e móvel. O front NÃO reimplementa esse calendário — grep
      // confirmou que não existe equivalente em web/src/ e não é para passar a
      // existir. Sem esta guarda, o laço dispara a cada mudança de `quotes`,
      // inclusive com cotação stale de madrugada.
      // `null` (1ª consulta em voo) e `{ erro: true }` (rede falhou) são estado
      // INDETERMINADO: não afirmamos que está aberto (princípio 4) e também não
      // perdemos o evento — o `return` acontece ANTES de tocar
      // `notifRef.current[p.t]`, então o `armado` fica intacto e o aviso sai na
      // primeira avaliação com o pregão confirmado aberto (princípio 9). Um
      // `continue` DENTRO do laço não serviria: a entrada de `notifRef` já
      // teria sido criada.
      const pregaoAberto = !!(mercado && !mercado.erro && mercado.aberto);
      if (!pregaoAberto) return;

    E acrescentar `mercado` à dep array: `}, [quotes, data, mercado]);` — é o que
    faz o efeito rodar de novo quando o estado do pregão RESOLVE, entregando o
    aviso represado em vez de engoli-lo.

    NÃO mexer na ordem dos 3 ramos, nos limiares (`>= 5` da variação), nas
    chamadas `cp.notifStopTitulo(`/`cp.notifAlvoTitulo(`/`cp.notifVarTitulo(`
    (guardião `web/tests/test_copy_theme.mjs:120` exige que continuem sendo
    chamadas de `App.jsx`), em `notifRef.current = {}` do `_resetScopeState`
    (guardião `web/tests/test_logout_reset.mjs:40`) nem nos textos de `copy.js`.

    NÃO acrescentar uma segunda chamada de `store.marketStatus()`: o guardião
    `web/tests/test_status_mercado_ui.mjs:45` exige EXATAMENTE 1 ocorrência em
    `App.jsx`. A guarda consome o estado `mercado` que já existe — não consulta.

    2. **`web/src/App.jsx` — recap de boot (linhas 7083-7104)**: remover as duas
    chamadas `notify.send(...)` (linhas ~7086 e ~7103), PRESERVANDO os `flash(...)`
    correspondentes e todo o resto do bloco (filtro por `lastSeenAt`, filtro por
    `tag === "pendente-cancelada"`, `store.putAgent`). Comentário no lugar:

      // 260824-i45 (item 5): SEM banner de sistema aqui. Este bloco roda no
      // BOOT, com o app em primeiro plano e a qualquer hora — era a segunda
      // fonte de "notificação fora do pregão", e para eventos que já
      // aconteceram horas antes. O recap NÃO se perde: `flash` continua
      // mostrando in-app, e no nativo `notify.send` já caía no mesmo
      // `_emitForeground` → handler de `App.jsx:6919`. Some o banner
      // redundante, fica a informação.

    Gatilho de pregão NÃO se aplica a este bloco de propósito: ele é um RESUMO do
    que já passou, disparado pela própria pessoa ao abrir o app — gatear por
    horário faria quem abre o app às 19h nunca saber que o agente operou.

    NÃO tocar `notify.notifyIfEnabled` em `A.cycle` (linha ~7590): é disparado
    por um clique explícito em "Executar ciclo agora"; banner logo após o próprio
    toque não é notificação fora de hora.

    NÃO tocar `web/src/notify.js` — o item 5 é sobre QUANDO chamar, não sobre o
    mecanismo de envio.

    3. **`web/tests/test_notif_pregao.mjs`** (arquivo novo, padrão source-grep de
    `test_notif_central.mjs`: lê `web/src/App.jsx` como texto, recorta chunks por
    âncoras de comentário e roda regex; sem render, sem build). Falhar
    RUIDOSAMENTE se uma âncora não for encontrada (chunk vazio = teste mudo é
    exatamente a falha silenciosa que este guardião não pode ter). Verificar:
      - chunk do laço (de `"const notifRef = useRef({});"` até
        `"// Tela de abertura"`) é não-vazio;
      - **a guarda é a LINHA LITERAL do early return, e vem antes de qualquer
        toque em `notifRef.current[p.t]`.** Asserção sobre a linha inteira, não
        sobre dois tokens soltos — `indexOf("mercado.aberto") < indexOf("notifRef.current")`
        passaria num refactor que move a guarda para um `continue` dentro do
        laço, destruindo o invariante do `armado`:

          const iGuarda = chunk.indexOf("if (!pregaoAberto) return;");
          const iRef = chunk.indexOf("notifRef.current[p.t]");
          ok("guarda de pregão é um early return literal", iGuarda >= 0);
          ok("o return vem ANTES de tocar notifRef.current[p.t] (preserva o armado)",
             iGuarda >= 0 && iRef > iGuarda);

      - `pregaoAberto` é derivado de `mercado.aberto` (e não de um horário
        local): o chunk contém `mercado.aberto`;
      - a dep array desse efeito inclui `mercado`;
      - o chunk NÃO contém `getHours(`, `getDay(`, nem literal de horário de
        pregão (`10:00`, `16:55`) — o front não reimplementa o calendário da B3;
      - as três chamadas `cp.notifStopTitulo(`, `cp.notifAlvoTitulo(` e
        `cp.notifVarTitulo(` continuam DENTRO do chunk (não foram movidas para
        fora da guarda);
      - o chunk do recap de boot (de `"const agentSummaryDone"` até
        `"// FASE 2: ao abrir, se houver sessão salva"`) NÃO contém `notify.send`
        e CONTINUA contendo dois `flash(`;
      - `notifRef.current = {}` continua existindo em `App.jsx` (não regredir o
        guardião de logout).
  </action>
  <verify>
    <automated>cd web && npx vite build && node tests/test_notif_pregao.mjs && node tests/test_status_mercado_ui.mjs && node tests/test_copy_theme.mjs && node tests/test_logout_reset.mjs && node tests/test_notif_central.mjs && node tests/test_vocabulario_espelho.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pregaoAberto" web/src/App.jsx` retorna pelo menos 2 linhas (definição + guarda).
    - `grep -c "if (!pregaoAberto) return;" web/src/App.jsx` retorna 1.
    - `grep -c "notify.send" web/src/App.jsx` cai de 3 para 1 (sobra só a confirmação de "Notificações ativadas" em `setNotif`, disparada pelo próprio toggle do usuário).
    - `grep -n "}, \[quotes, data, mercado\]);" web/src/App.jsx` retorna 1 linha.
    - `grep -c "store.marketStatus(" web/src/App.jsx` continua retornando 1 (guardião `test_status_mercado_ui.mjs:45`).
    - `cd web && npx vite build` sai com exit code 0 (exigência do CLAUDE.md — grep não pega erro de sintaxe JSX).
    - `node web/tests/test_notif_pregao.mjs` sai com exit code 0.
    - `git diff web/src/notify.js` VAZIO; `git diff web/src/copy.js` VAZIO; `git diff web/src/persistence.js` VAZIO (decisão D2).
    - `git diff web/src/App.jsx` não toca `NotifSection`/`A.openNotifCentral` (decisão D3: nada de central de notificações recebidas).
  </acceptance_criteria>
  <done>O laço de stop/alvo/variação só notifica com o pregão confirmado aberto pela fonte canônica do backend, sem reimplementar calendário em JS, e sem consumir o `armado` quando o estado é indeterminado — invariante travado por asserção sobre a linha literal do early return; o recap de boot deixa de emitir banner de sistema preservando o `flash`; `npx vite build` e os cinco guardiões da região verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| servidor → APNs → aparelho | O payload de push atravessa a Apple e chega como DADO no cliente. Campos novos (`t`, `kind`) entram fora do `aps`, e `notify.js:381-382` já trata o conteúdo como dado (só o ticker é lido, validado contra o formato da B3, e nada mais do payload muda o que o app faz). |
| cliente → /api/market/status | Rota pública, GET sem parâmetro, resposta puramente derivada do relógio do servidor — nenhuma entrada do usuário cruza. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-i45-01 | Information disclosure | `extra["t"]` no payload APNs (ticker de posição do usuário) | accept | O push já expõe ticker, quantidade e preço no CORPO visível na tela de bloqueio (`agent.py:697-699`, `:884`); mover o ticker também para `data` não amplia a exposição. Preferência e opt-out continuam com o usuário. |
| T-i45-02 | Tampering | Payload de push manipulado a caminho do cliente | accept | Canal TLS do APNs, autenticado por JWT ES256 (`push.py`). O cliente já trata o conteúdo como dado: `notify.js:382` valida `t` contra `^[A-Z]{4}\d{1,2}$` e ignora todo o resto; o pior caso é navegar para um card de ativo existente. Sem `aps.category`, não há ação executável a partir do banner. |
| T-i45-03 | Information disclosure | Título do push nomeando o marco (stop/alvo/entrada) na tela de bloqueio | mitigate | Todo título de `PUSH_TITULOS` carrega "(simulado)"/"simulada" — quem olha a tela de bloqueio por cima do ombro vê explicitamente que é simulação, não patrimônio real (CLAUDE.md princípio 1). Guardião no `test_push_payload.py` trava a presença de "simulad" em todos os valores. |
| T-i45-04 | Denial of Service | `store.push_events` novo por usuário no fan-out do gatilho e do Radar | accept | Uma escrita kv por usuário por evento, com cap 50 já embutido (`store.py:808`); o mesmo laço já faz `push_agent_log` por usuário (gatilho) e `store_result` (Radar). O `try/except` próprio garante que a escrita nunca derrube o aviso nem o laço. |
| T-i45-05 | Repudiation | Evento de mercado sem rastro durável | mitigate | É o próprio item 4: gatilho e Radar passam a escrever em `agent.events`, e o registro acontece INDEPENDENTE do sucesso da entrega — a linha de entrega (`agent.py:1034`) permanece como registro separado do envio. |
| T-i45-06 | Elevation of privilege | — | accept | Nenhuma rota, permissão RBAC, entitlement ou audiência muda. `_push_audience`, `_usuarios_com_consentimento`, `prefs_for` e o kill-switch ficam intactos (decisão D2). |
| T-i45-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo em nenhuma das três tasks — só edição de código já existente. Sem gate de legitimidade a executar. |
</threat_model>

<verification>
1. `bash scripts/executar.sh --testes` verde — suíte canônica COMPLETA (pytest do
   backend + `web/tests/*.mjs`). Exigência do CLAUDE.md; `scripts/test.sh` sozinho
   é meia baseline e não conta. Rodar depois das três tasks.
2. `cd web && npx vite build` verde depois da Task 3 (exigência específica do
   CLAUDE.md — grep e teste estático não pegam erro de sintaxe JSX).
3. Atenção especial no passo 1 a guardiões NÃO previstos que o alargamento de
   assinatura de `notify_push` possa atingir: qualquer spy `(uid, title, body)`
   em `server/tests/` precisa virar `(uid, title, body, extra=None)`. Atualizar
   com nota, nunca apagar. E, ao tocar em qualquer fake, aplicar a
   `<regra_de_teste_transversal>`: asserção de conteúdo sai de dentro da
   corrotina.
4. Prova de mutação nos dois guardiões que este plano reescreve — um guardião que
   não pode ficar vermelho não é guardião:
   - reverter o título do Radar à mão para `"Radar do dia 📡"` → `pytest
     tests/test_radar_daily.py -q` VERMELHO; desfazer;
   - trocar `if (!pregaoAberto) return;` por um `continue` dentro do `for` →
     `node web/tests/test_notif_pregao.mjs` VERMELHO; desfazer.
5. Sem verificação em simulador/aparelho neste plano. Mas registrar no SUMMARY,
   como pendência explícita para o Alex, que **um push só é considerado corrigido
   depois de recebido de verdade num iPhone**: título certo, toque levando ao card
   do ativo, e o evento visível em "EVENTOS E AVISOS RECENTES". Os testes cobrem
   o payload e o registro, não a entrega.
6. Publicação FORA deste plano: `scripts/bump.sh` → `scripts/publicar-web.sh`,
   `atualizar.sh --somente-deploy` e `cap sync ios` são passo separado que o Alex
   dispara depois. A mudança de backend (Tasks 1 e 2) vale assim que o Railway
   sobe; a de front (Task 3) exige publicação do web e build novo para o iPhone.
</verification>

<success_criteria>
- Push de execução do Operador (stop, alvo, entrada automática, ordem pendente
  executada/cancelada) chega com `data.t` no formato que `notify.js` valida, e o
  toque cai no card do ativo pelo mecanismo que já existia.
- Nenhum push do Operador com marco conhecido sai com o título genérico; todos os
  títulos declaram que a operação é simulada.
- Push de stop/alvo mostra o resultado realizado, com o número vindo do retorno de
  `store.sell` (motor determinístico), travado por âncora de FONTE dentro da
  suíte — não só por igualdade numérica, que uma recomposição também satisfaria.
- Gatilho e prévia do Radar aparecem em "EVENTOS E AVISOS RECENTES", registrados
  independentemente do sucesso da entrega.
- Notificação local de stop/alvo/variação só dispara com o pregão confirmado
  aberto pela fonte canônica do backend; estado indeterminado não notifica e não
  perde o evento; o front não ganhou nenhuma lógica de calendário da B3.
- Push do Radar diz em título e corpo que é prévia pré-abertura, preservando
  veredito, contagem de ativos e guardrail regulatório.
- Os dois guardiões reescritos por este plano PODEM ficar vermelhos (prova de
  mutação executada) — nenhuma asserção vácua sobrou.
- Decisões travadas intactas: horário/audiência/gate do Radar (D1), `PREFS_PADRAO`
  e contrato de `register-token` (D2), nenhuma central de notificações (D3).
- `bash scripts/executar.sh --testes` e `cd web && npx vite build` verdes.
</success_criteria>

<output>
Create `.planning/quick/260824-i45-notificacoes-push-deep-link-titulo-com-m/260824-i45-SUMMARY.md` when done
</output>
</content>
