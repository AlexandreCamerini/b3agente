---
phase: quick
plan: 260907-x69
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/skill_ref.py
  - server/app/opcoes_lastreadas.py
  - server/tests/test_opcoes_lastreadas_proposta.py
  - web/src/copy.js
  - web/src/App.jsx
  - web/tests/test_opcoes_proposta_ui.mjs
autonomous: true
requirements: [X69-01, X69-02, X69-03]
must_haves:
  truths:
    - "Com uma put de proteção aberta, a manchete do card de fechamento afirma VENDER a put (crédito), não comprar."
    - "Com uma call coberta aberta, a manchete do card de fechamento afirma RECOMPRAR a call (débito)."
    - "O botão e a confirmação de fechamento de uma put nunca mencionam 'call' nem 'destrava'."
    - "A manchete de fechamento nasce em skill_ref (motor determinístico), nunca composta no front."
    - "A aritmética (premioTotal, contratos, rota, store) permanece byte-idêntica ao que já está em produção."
  artifacts:
    - path: "server/app/skill_ref.py"
      provides: "chaves fechar_call_coberta / fechar_put_protecao nos DOIS modos"
      contains: "fechar_put_protecao"
    - path: "server/app/opcoes_lastreadas.py"
      provides: "proposta_fechar seleciona a chave de texto de FECHAMENTO sem tocar em tipo/motivo"
      contains: "fechar_"
    - path: "web/src/copy.js"
      provides: "CTA e confirmação de fechamento parametrizados por lado, nos DOIS ramos"
    - path: "web/tests/test_opcoes_proposta_ui.mjs"
      provides: "guardião de que o texto de fechamento de put não diz 'call'"
  key_links:
    - from: "server/app/opcoes_lastreadas.py:proposta_fechar"
      to: "server/app/skill_ref.py:OPCOES_LASTREADAS"
      via: "opcoes_lastreadas_txt(modo, 'fechar_' + tipo, **dados)"
      pattern: "opcoes_lastreadas_txt\\(modo, chave_texto"
    - from: "web/src/App.jsx:PropostaLastreada"
      to: "web/src/copy.js:ctaFecharLastreada"
      via: "cp.ctaFecharLastreada(price(p.premioTotal), isCall)"
      pattern: "ctaFecharLastreada\\(price\\(p\\.premioTotal\\), isCall\\)"
---

<objective>
Corrigir os textos do card de FECHAMENTO de operação lastreada. Hoje o card
reusa a frase de ABERTURA (`skill_ref.OPCOES_LASTREADAS[modo][tipo]`) e um CTA
com "call" literal, então fechar uma PUT de proteção exibe "Comprar 1 put(s)…",
"Recomprar a call — R$ 65,00" e uma confirmação que fala de destravar ações que
a put nunca travou.

Purpose: o card afirma uma ação, o botão executa outra. Isso é o oposto do
princípio 9 (estados completos e corretos) e do guardrail CVM — a manchete
determinística precisa descrever o que o motor de fato faz.

Output: duas chaves novas de vocabulário canônico no backend, CTA/confirmação
parametrizados por lado no front, guardiões antigos atualizados com nota datada
e guardiões novos cobrindo o lado da put.

**ESCOPO FECHADO — SÓ TEXTO.** Achado verificado ao vivo (staging, iPhone,
2026-09-07, Modo Operador). O dinheiro já está certo: `POST
/api/options/lastreada/fechar` (`server/app/main.py` ~2700) despacha
`side=="comprada"` → `store.sell_option` (VENDE a put, CREDITA o caixa) e
`side=="vendida"` → `store.fechar_call_coberta`. Nenhuma task pode tocar
cálculo, caixa, PnL, schema, rota ou fluxo de execução.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

@server/app/skill_ref.py
@server/app/opcoes_lastreadas.py
@web/src/copy.js

Nota: `.claude/skills/didatica-boris/SKILL.md` está listado no CLAUDE.md mas
NÃO existe neste worktree (verificado por `find`). As regras que ela encapsula
estão aplicadas diretamente neste plano (frase canônica só em `skill_ref`,
vocabulário nos dois modos, guardião não se apaga) — não gastar contexto
procurando o arquivo.
</context>

<decisions>
Decisões tomadas na leitura do código, para o executor NÃO re-litigar:

**D-1 — Nomes das chaves: `fechar_call_coberta` e `fechar_put_protecao`.**
Prefixo `fechar_` sobre a chave de abertura já existente. Alternativa
descartada: um sub-dicionário `FECHAR` separado — `opcoes_lastreadas_txt` tem
um único lookup plano e o guardião `test_opcoes_collar_vocab.py:74`
(`sorted(OPCOES_LASTREADAS["operador"]) == sorted(...["educacional"])`) mede
paridade de chave nesse mesmo nível; chave plana mantém os dois intactos.

**D-2 — `tipo` e `motivo` NÃO mudam.** `proposta_fechar` continua devolvendo
`{"proposta": {..., "tipo": "call_coberta"|"put_protecao"}, "motivo": <tipo>}`.
Verificado: ~9 asserções de igualdade exata dependem disso
(`test_opcoes_lastreadas_proposta.py:364/367/378/381`,
`test_opcoes_lastreadas_rotas.py:134/159/236/256/543/566`) e o front deriva
`isCall` de `p.optionType`, não da chave de texto. A chave de FECHAMENTO é uma
variável LOCAL nova (`chave_texto`), usada só nas duas linhas de
manchete/didática. Mexer em `tipo` seria mudança de contrato — fora de escopo.

**D-3 — Nomes das chaves de copy.js NÃO mudam** (`ctaFecharLastreada`,
`confirmFecharCoberta`). `confirmFecharCoberta` vira um misnomer (agora cobre
os dois lados), mas renomear obrigaria a mexer em `CHAVES_LASTREADAS`
(`test_opcoes_proposta_ui.mjs:21`), na asserção literal da linha 89 e em 4 call
sites — churn sem ganho. Em vez disso, a assinatura ganha um parâmetro de lado
e o comentário acima da chave registra o motivo do nome herdado.

**D-4 — O ramo ESTUDO de copy.js não muda.** `ctaFecharLastreada: () => "Como
esta posição se encerra"` e `confirmFecharCoberta: () => ""` (copy.js:185/187)
já são genéricos e corretos; Estudo nunca renderiza o CTA (`{operador && …}`).
Ignorar os argumentos extras é comportamento válido de JS — a paridade de
CHAVES (`test_copy_theme.mjs:27`) continua satisfeita sem tocar no ramo.

**D-5 — Paridade `defaults.py` ↔ `catalog.js` NÃO é tocada.** Verificado por
grep: nenhuma das duas contém `OPCOES_LASTREADAS`, `opcoes_lastreadas` ou
"Recomprar". Este plano não toca prompt.

**D-6 — Collar sai correto de graça.** `store.abrir_collar` grava as pernas
como posições normais (`side` "vendida" na call, "comprada" na put), e
`proposta_fechar` continua binário por `side` — cada perna de collar fechada
individualmente pega a frase certa sem código específico.
</decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vocabulário canônico de FECHAMENTO no backend</name>
  <files>server/app/skill_ref.py, server/app/opcoes_lastreadas.py, server/tests/test_opcoes_lastreadas_proposta.py</files>
  <behavior>
    - `skill_ref.OPCOES_LASTREADAS["operador"]["fechar_put_protecao"]` existe e a frase começa com "Vender" (o fechamento de put VENDE e credita).
    - `skill_ref.OPCOES_LASTREADAS["operador"]["fechar_call_coberta"]` existe e a frase começa com "Recomprar" (o fechamento de call RECOMPRA e debita).
    - As mesmas duas chaves existem em `["educacional"]`, no registro condicional ("Se você encerrasse…"), sem verbo de ordem.
    - `sorted(OPCOES_LASTREADAS["operador"]) == sorted(OPCOES_LASTREADAS["educacional"])` continua verdadeiro.
    - `proposta_fechar(pos_put, cadeia, "operador", hoje)["proposta"]["manchete"] != skill_ref.opcoes_lastreadas_txt("operador", "put_protecao", **mesmos_dados)` — manchete de fechamento nunca igual à de abertura.
    - `proposta_fechar(...)["proposta"]["tipo"] == "put_protecao"` e `["motivo"] == "put_protecao"` (inalterados, D-2).
  </behavior>
  <action>
Em `server/app/skill_ref.py`, dentro de `OPCOES_LASTREADAS`, acrescentar em
AMBOS os registros (`operador` e `educacional`) as chaves `fechar_call_coberta`
e `fechar_put_protecao`, logo depois das chaves de abertura homônimas, com um
comentário datado (2026-09-07) explicando que a abertura e o fechamento são
frases DISTINTAS porque a mesma posição descreve ações opostas nos dois
momentos, e que o defeito que motivou isso foi achado ao vivo em staging.

Textos (registro de mesa em `operador`, condicional em `educacional` — mesma
distinção já documentada no comentário do bloco). Marcadores de interpolação
disponíveis, e só eles: `{n}`, `{ticker}`, `{strike}`, `{premioTotal}`,
`{qtyAcoes}` — são exatamente os que `proposta_fechar` monta em `dados`.

- operador / `fechar_call_coberta`:
  "Recomprar {n} call(s) de {ticker} strike {strike} por R$ {premioTotal} — destrava {qtyAcoes} ação(ões)."
- operador / `fechar_put_protecao`:
  "Vender {n} put(s) de {ticker} strike {strike} por R$ {premioTotal} — encerra a proteção de {qtyAcoes} ação(ões)."
- educacional / `fechar_call_coberta`:
  "Se você encerrasse esta call coberta agora, pagaria R$ {premioTotal} para recomprá-la e {qtyAcoes} ação(ões) voltariam a ficar livres."
- educacional / `fechar_put_protecao`:
  "Se você encerrasse esta put de proteção agora, receberia R$ {premioTotal} pela venda e {qtyAcoes} ação(ões) deixariam de estar protegidas contra queda abaixo de R$ {strike}."

Nenhuma promessa de resultado, nenhum número inventado — os valores vêm todos
de `dados`, já formatados por `num_br()` pelo chamador.

Em `server/app/opcoes_lastreadas.py`, dentro de `proposta_fechar` (~linha 429):
manter `tipo` exatamente como está e introduzir logo abaixo uma variável local
`chave_texto = "fechar_" + tipo`, usada APENAS nas duas linhas de texto (~443-444):
`manchete = skill_ref.opcoes_lastreadas_txt(modo, chave_texto, **dados)` e
`didatica = skill_ref.opcoes_lastreadas_txt("educacional", chave_texto, **dados)`.
`proposta["tipo"]` e o `return {"proposta": ..., "motivo": tipo}` (linha 471)
ficam INTOCADOS (D-2). Comentar em uma linha por que a chave de texto diverge
do `tipo` (contrato do front + ~9 guardiões de igualdade exata).
Atualizar também o docstring da função, que hoje diz "sucesso tem `motivo` ==
`tipo`" — segue verdade, mas acrescentar que a FRASE agora vem da chave de
fechamento.

Em `server/tests/test_opcoes_lastreadas_proposta.py`:
1. ATUALIZAR os dois guardiões que travam o texto antigo, cada um com nota
   datada explicando a reversão deliberada (guardião não se apaga):
   - linha ~371 `assert p["manchete"].startswith("Vender")` (fechamento de
     CALL) → `startswith("Recomprar")`;
   - linha ~384 `assert p["manchete"].startswith("Comprar")` (fechamento de
     PUT) → `startswith("Vender")`.
   Nota no formato usado no repo: "ATUALIZADO 2026-09-07 (quick 260907-x69):
   a manchete de fechamento deixou de reusar a frase de ABERTURA — o card dizia
   'Comprar 1 put(s)' logo acima de um botão que VENDE. Mudança deliberada, não
   regressão."
2. ACRESCENTAR, na Parte 3 (`proposta_fechar`), testes novos:
   - `test_fechar_put_protecao_manchete_difere_da_abertura`: monta os mesmos
     `dados` e afirma que `manchete` != `opcoes_lastreadas_txt(modo,
     "put_protecao", **dados)` e que a `didatica` também difere da didática de
     abertura;
   - `test_fechar_call_coberta_manchete_difere_da_abertura`: idem para a call;
   - `test_fechar_put_protecao_nao_menciona_call`: `"call" not in
     p["manchete"].lower()` e `"call" not in p["didatica"].lower()`;
   - `test_vocab_fechamento_existe_nos_dois_modos`: as duas chaves novas em
     `operador` e `educacional`, e o registro educacional sem verbo de ordem
     (mesmo padrão das asserções das linhas 25-34).
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/test_opcoes_lastreadas_proposta.py tests/test_opcoes_lastreadas_rotas.py tests/test_opcoes_collar_vocab.py tests/test_opcoes_collar.py -q</automated>
  </verify>
  <done>Fechamento de put produz manchete que começa com "Vender"; fechamento de call, com "Recomprar"; nenhuma delas igual à frase de abertura; `tipo`/`motivo` inalterados e todos os testes de rota/collar/proposta verdes.</done>
</task>

<task type="auto">
  <name>Task 2: CTA e confirmação por lado no front + guardião + suíte canônica</name>
  <files>web/src/copy.js, web/src/App.jsx, web/tests/test_opcoes_proposta_ui.mjs</files>
  <action>
**`web/src/copy.js`, ramo `operador` (linhas 401 e 403) — só ele (D-4).**
Trocar as duas funções por versões parametrizadas pelo lado, mantendo os nomes
das chaves (D-3) e o parâmetro de lado por ÚLTIMO (assim as chamadas antigas
degradariam para o ramo da put em vez de quebrar, e o guardião da linha 42-45
do teste web, que invoca toda função com `("X","Y","Z","W")`, continua
funcionando):

    ctaFecharLastreada: (custo, isCall) => isCall
      ? `Recomprar a call — R$ ${custo}`
      : `Vender a put — recebe R$ ${custo}`,
    confirmFecharCoberta: (custo, qty, ticker, isCall) => isCall
      ? `Recomprar esta call por R$ ${custo} destrava ${qty} ação(ões) de ${ticker} imediatamente. Continuar?`
      : `Vender esta put por R$ ${custo} encerra a proteção de ${qty} ação(ões) de ${ticker} imediatamente. Continuar?`,

Acrescentar comentário datado (2026-09-07, quick 260907-x69) acima do par:
as duas frases foram escritas na Fase 14 para a venda coberta e nunca
generalizadas quando o fechamento da put entrou; `confirmFecharCoberta` mantém
o nome herdado de propósito (chave travada por `CHAVES_LASTREADAS` no guardião
web), apesar de agora cobrir os dois lados.

**`web/src/App.jsx` — 3 call sites, os dois componentes em PARIDADE.**
Divergir em silêncio entre `PropostaLastreada`/`AtivoCard` e o bloco de
`PropostaDaPosicao` já foi causa de defeito neste repo (regressão do `radarVm`,
documentada no próprio guardião web) — as duas cópias mudam juntas.
- linha ~3262 (`PropostaLastreada`): `cp.ctaFecharLastreada(price(p.premioTotal), isCall)` —
  `isCall` já existe no escopo (`const isCall = p.optionType === "call"`).
- linha ~3465 (`onFecharLastreada` em `AtivoCard`):
  `cp.confirmFecharCoberta(price(p.premioTotal), p.qtyAcoes, t, p.optionType === "call")`.
- linha ~4207 (`onFecharLastreada` em `PropostaDaPosicao`): idem, texto igual.
Nada mais muda: `onClick={posAberta ? onFechar : onAbrir}`, `A.fecharLastreada`,
`price()`, `p.premioTotal` — tudo intocado.

**`web/tests/test_opcoes_proposta_ui.mjs` — guardiões novos** (não apagar
nenhum existente; a asserção da linha 89 continua válida). Acrescentar, depois
do bloco da linha 89:
- `COPY.operador.ctaFecharLastreada("65,00", false)` não casa `/call/i` e
  contém "put";
- `COPY.operador.ctaFecharLastreada("65,00", true)` contém "Recomprar" e "call"
  (o lado da call não regrediu);
- `COPY.operador.confirmFecharCoberta("65,00", 100, "ABEV3", false)` não casa
  `/call/i` nem contém "destrava", e contém "ABEV3" e "100";
- `COPY.operador.confirmFecharCoberta("65,00", 100, "ABEV3", true)` contém
  "Recomprar" e "destrava";
- leitura estática de App.jsx: `/ctaFecharLastreada\(price\(p\.premioTotal\), isCall\)/`
  casa, e `(app.match(/confirmFecharCoberta\(price\(p\.premioTotal\), p\.qtyAcoes, t, p\.optionType === "call"\)/g) || []).length === 2`
  (os DOIS call sites em paridade).
Comentário de cabeçalho do bloco novo: origem (achado ao vivo staging/iPhone
2026-09-07, Modo Operador, put ABEV3 strike 30,00) e o que ele trava.
  </action>
  <verify>
    <automated>cd web && npx vite build && node tests/test_opcoes_proposta_ui.mjs && node tests/test_copy_theme.mjs && cd .. && bash scripts/executar.sh --testes</automated>
  </verify>
  <done>`npx vite build` conclui sem erro; guardião web novo passa nos 4 lados (put/call × CTA/confirm); `test_copy_theme.mjs` (paridade de chaves estudo↔operador) verde; suíte canônica completa (pytest + web/tests/*.mjs) verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhuma nova | A mudança é de string estática e de seleção de chave em dicionário local; nenhum input de usuário novo cruza fronteira, nenhuma rota/parâmetro/serialização muda. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-x69-01 | Information Disclosure | `skill_ref.OPCOES_LASTREADAS` | mitigate | Frases novas interpolam SÓ `{n}/{ticker}/{strike}/{premioTotal}/{qtyAcoes}`, já formatados por `num_br()`; nenhum campo de conta/escopo entra na frase. |
| T-x69-02 | Tampering | manchete determinística (guardrail CVM) | mitigate | A frase nasce em `skill_ref` e chega pronta ao front; guardião novo em `test_opcoes_proposta_ui.mjs` reafirma que o front não compõe manchete (asserções das linhas 58-60 permanecem). |
| T-x69-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo é instalado neste plano — nenhuma superfície de supply chain aberta. |
</threat_model>

<verification>
1. `bash scripts/executar.sh --testes` — suíte canônica COMPLETA (pytest do
   backend + `web/tests/*.mjs`). `scripts/test.sh` sozinho não conta.
2. `cd web && npx vite build` — front foi editado; grep e teste estático não
   pegam erro de sintaxe JSX.
3. Cheque manual de escopo antes de fechar: `git diff --stat` deve listar
   EXATAMENTE os 6 arquivos de `files_modified`. Qualquer arquivo de
   `store.py`, `main.py`, `persistence.js`, `finance.js` ou `web_dist` no diff
   significa que o escopo vazou — reverter.
</verification>

<success_criteria>
- Fechamento de PUT de proteção: manchete afirma VENDER e crédito; didática no
  condicional descreve receber o prêmio; CTA "Vender a put — recebe R$ X";
  confirmação fala em encerrar a proteção, nunca em destravar ações.
- Fechamento de CALL coberta: manchete "Recomprar…", CTA e confirmação como
  antes (sem regressão).
- Nenhum texto de fechamento de put contém a palavra "call".
- `tipo`, `motivo`, prêmio, contratos, rota e store byte-idênticos ao anterior.
- Suíte canônica verde e `npx vite build` limpo.
</success_criteria>

<output>
Criar `.planning/quick/260907-x69-corrigir-textos-do-card-de-fechamento-de/260907-x69-SUMMARY.md`
ao terminar.

REGISTRAR NO SUMMARY, obrigatoriamente:

**Publicação PENDENTE — não executada de propósito.** A branch
`v2/interacao-estrutural` está 7 commits atrás de `origin/main` (que publicou o
carimbo `F10-20260907-01`, servido em produção agora) e `scripts/bump.sh`
deriva o próximo carimbo do valor LOCAL — rodá-lo aqui regeneraria
`F10-20260907-01` apontando para código diferente do que produção serve.
`scripts/bump.sh` e `scripts/publicar-web.sh` NÃO foram rodados; `server/web_dist`
não foi editado. Mesclar `origin/main` ANTES de qualquer bump/publicação. Esta
correção só chega ao iPhone depois disso — junto com a pendência idêntica da
quick 260907-w33.
</output>
