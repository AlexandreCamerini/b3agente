---
phase: quick
plan: 260907-x69
subsystem: ui-copy
tags: [opcoes-lastreadas, skill_ref, copy.js, texto-determinista, guardrail-cvm]

requires: []
provides:
  - "Manchete/didática de FECHAMENTO de call coberta / put de proteção nascem de chaves próprias (`fechar_call_coberta`/`fechar_put_protecao`) em `skill_ref.OPCOES_LASTREADAS`, distintas da frase de ABERTURA."
  - "CTA e confirmação de fechamento no front (`copy.js`) parametrizados por lado (`isCall`), em paridade nos 3 call sites de `App.jsx`."
affects: [opcoes-lastreadas, skill_ref, copy.js]

tech-stack:
  added: []
  patterns:
    - "Chave de TEXTO local (`fechar_<tipo>`) divergindo do campo de CONTRATO (`tipo`/`motivo`) quando a frase muda mas o contrato do endpoint não pode — mesmo padrão de degradação já usado em timing_txt/historico_txt (fallback para 'educacional'/'sem_setup')."

key-files:
  created: []
  modified:
    - server/app/skill_ref.py
    - server/app/opcoes_lastreadas.py
    - server/tests/test_opcoes_lastreadas_proposta.py
    - web/src/copy.js
    - web/src/App.jsx
    - web/tests/test_opcoes_proposta_ui.mjs

key-decisions:
  - "tipo/motivo do retorno de proposta_fechar NÃO mudam (D-2 do plano) — ~9 asserções de igualdade exata dependem disso; a chave de texto é variável local nova."
  - "ctaFecharLastreada/confirmFecharCoberta mantêm os nomes herdados (D-3); ganham isCall como último parâmetro para degradar com segurança em chamadas antigas."
  - "Ramo Estudo de copy.js não muda (D-4) — já é genérico e Estudo nunca renderiza o CTA."

patterns-established:
  - "Guardião de texto errado não se apaga: os dois asserts que travavam a manchete antiga foram ATUALIZADOS com nota datada (2026-09-07) explicando a reversão deliberada, não removidos."

requirements-completed: [X69-01, X69-02, X69-03]

duration: ~50min
completed: 2026-09-08
---

# Quick Task 260907-x69: Corrigir textos do card de fechamento de opções lastreadas Summary

**Card de fechamento de put/call lastreada parou de reusar a frase de ABERTURA (que descrevia a ação oposta) — manchete, CTA e confirmação agora nascem de chaves próprias de fechamento, nos dois modos e nos 3 call sites do front.**

## Performance

- **Duration:** ~50min
- **Completed:** 2026-09-08
- **Tasks:** 2/2 completed
- **Files modified:** 6

## Accomplishments
- `skill_ref.OPCOES_LASTREADAS` ganhou `fechar_call_coberta`/`fechar_put_protecao` nos dois modos (operador/educacional); fechamento de put agora afirma "Vender" (crédito), fechamento de call afirma "Recomprar" (débito) — nenhuma das duas igual à frase de abertura.
- `proposta_fechar` seleciona a chave de texto via variável local `chave_texto = "fechar_" + tipo`, sem tocar em `tipo`/`motivo` (contrato do front + 9 guardiões de igualdade exata preservados).
- `copy.js` (`ctaFecharLastreada`, `confirmFecharCoberta`) parametrizados por `isCall`; os 3 call sites em `App.jsx` (`PropostaLastreada`, `AtivoCard.onFecharLastreada`, `PropostaDaPosicao.onFecharLastreada`) passam o lado correto, em paridade.
- Dois guardiões que travavam literalmente o texto antigo foram atualizados com nota datada (não apagados); 4 testes novos no backend e 8 asserções novas no guardião web cobrem os dois lados.

## Task Commits

1. **Task 1: Vocabulário canônico de FECHAMENTO no backend** - `758f630` (fix)
2. **Task 2: CTA e confirmação por lado no front + guardião + suíte canônica** - `a37c98e` (fix)

_Note: escopo fechado — só texto; nenhum arquivo de `store.py`, `main.py`, `persistence.js`, `finance.js` ou `web_dist` no diff (verificado via `git diff --stat` contra a base do plano)._

## Files Created/Modified
- `server/app/skill_ref.py` - novas chaves `fechar_call_coberta`/`fechar_put_protecao` em `OPCOES_LASTREADAS["operador"]` e `["educacional"]`, com comentário datado explicando a distinção abertura×fechamento.
- `server/app/opcoes_lastreadas.py` - `proposta_fechar` usa `chave_texto = "fechar_" + tipo` só para `manchete`/`didatica`; `tipo`/`motivo` inalterados; docstring atualizado.
- `server/tests/test_opcoes_lastreadas_proposta.py` - 2 guardiões atualizados (nota datada) + 4 testes novos (manchete difere da abertura, não menciona "call", chaves existem nos dois modos).
- `web/src/copy.js` - `ctaFecharLastreada`/`confirmFecharCoberta` parametrizados por `isCall` (último argumento), comentário datado sobre o misnomer herdado.
- `web/src/App.jsx` - 3 call sites passam `isCall`/`p.optionType === "call"` (linhas ~3262, ~3465, ~4207).
- `web/tests/test_opcoes_proposta_ui.mjs` - 8 asserções novas cobrindo os 4 lados (put/call × CTA/confirm) e a paridade dos 2 call sites de confirmação.

## Deviations from Plan

None - plano executado exatamente como escrito. Único ajuste operacional: `web/node_modules` estava ausente neste worktree novo (esperado — gitignored) e foi instalado via `npm install` antes de rodar `npx vite build`/os testes web; não altera nenhum arquivo versionado.

## Known Stubs

None.

## Threat Flags

None — mudança de string estática e seleção de chave em dicionário local; nenhuma rota, parâmetro ou serialização nova (ver `<threat_model>` do plano, disposições `mitigate`/`accept` já cobrem a superfície).

## Publicação PENDENTE — não executada de propósito

A branch `v2/interacao-estrutural` está atrás de `origin/main` (que já publicou o carimbo `F10-20260907-01`, servido em produção). `scripts/bump.sh` deriva o próximo carimbo do valor LOCAL — rodá-lo aqui regeneraria `F10-20260907-01` apontando para código diferente do que produção serve. `scripts/bump.sh` e `scripts/publicar-web.sh` NÃO foram rodados; `server/web_dist` não foi editado. Mesclar `origin/main` ANTES de qualquer bump/publicação. Esta correção só chega ao iPhone depois disso — junto com a pendência idêntica da quick 260907-w33.

## Validation Results

**Suíte canônica (`bash scripts/executar.sh --testes`):** VERDE. Exit code 0.
- Backend (pytest): 2034 passed, 1 skipped, 0 failed.
- Web (`web/tests/*.mjs`): todos os arquivos `[OK]`, 0 `[X]` (grep confirmou zero falhas), incluindo `test_ios_assets.mjs` (o aviso conhecido do plano sobre esse teste NÃO se materializou — passou).

**`cd web && npx vite build`:** VERDE. Build concluído em 1.09s, 90 módulos transformados, sem erro de sintaxe. (Aviso pré-existente de chunk >500kB, fora de escopo.)

**Verificação de escopo (`git diff --stat` contra a base do plano):** exatamente os 6 arquivos de `files_modified` — `server/app/opcoes_lastreadas.py`, `server/app/skill_ref.py`, `server/tests/test_opcoes_lastreadas_proposta.py`, `web/src/App.jsx`, `web/src/copy.js`, `web/tests/test_opcoes_proposta_ui.mjs`. Nenhum arquivo de `store.py`, `main.py`, `persistence.js`, `finance.js` ou `web_dist` no diff.

## Self-Check: PASSED

- FOUND: server/app/skill_ref.py contém `fechar_put_protecao` (grep confirmado).
- FOUND: server/app/opcoes_lastreadas.py contém `chave_texto = "fechar_" + tipo`.
- FOUND: web/src/copy.js contém `isCall` nos dois parâmetros de `ctaFecharLastreada`/`confirmFecharCoberta`.
- FOUND: commit `758f630` em `git log --oneline`.
- FOUND: commit `a37c98e` em `git log --oneline`.
- Testes reportados acima rodaram de fato nesta sessão (saída completa capturada, não inferida).
