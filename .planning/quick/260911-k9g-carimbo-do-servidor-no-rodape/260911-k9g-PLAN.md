---
quick_id: 260911-k9g
phase: quick-260911-k9g
plan: 01
type: quick
wave: 1
depends_on: []
files_modified: [web/src/App.jsx, web/src/api.js, web/src/persistence.js, web/src/copy.js, web/tests/test_carimbo_servidor_rodape.mjs]
autonomous: true
---

<objective>
O rodapé do Perfil (`web/src/App.jsx:2589`) mostra só o `BUILD_ID` do FRONT.
Quando o deploy é só-backend — o que aconteceu **cinco vezes** em 10 e 11/09 —
o app parece desatualizado e não há como ver, do aparelho, qual servidor está
respondendo.

Isso confundiu o Alex **três vezes nesta sessão**. O comentário do próprio
código diz "se não bater com a entrega, o aparelho está rodando código antigo",
o que é verdade só para o front e induz ao erro no caso só-backend.

Decisão do Alex: mostrar OS DOIS carimbos, sempre.
</objective>

<context>
`api.js:199` já bate em `/api/health` — mas só dentro de `testServer(base)`,
para validar um endereço digitado; o valor nunca chega à tela. `/api/health`
responde `{"ok": true, "build": "F10-..."}` e **não exige sessão**.

Padrão de estado de dado externo que o app já usa (`QuotaSeg`, `App.jsx:449`):
`undefined` = carregando (não renderiza), `null` = falhou (mostra travessão,
NUNCA um número estimado), objeto = dado real. Vale aqui: se o servidor não
responder, o carimbo dele é travessão, nunca o do front repetido.
</context>

<tasks>
<task type="auto">
  <name>Task 1: rodapé mostra interface e servidor</name>
  <files>web/src/api.js, web/src/persistence.js, web/src/App.jsx, web/src/copy.js, web/tests/test_carimbo_servidor_rodape.mjs</files>
  <action>
  - `api.js`: método novo que lê `/api/health` do servidor ATUAL (sem
    argumento de base; `testServer` é outra coisa e continua como está).
    Timeout curto (o padrão de 15 s é exagero para um carimbo; algo como
    8 s). Falha vira valor nulo, não exceção que estoure a tela.
  - `persistence.js`: o método nos DOIS stores (paridade obrigatória do
    CLAUDE.md), delegação pura, sem persistir nada.
  - `App.jsx`: o rodapé passa a mostrar os dois. Buscar ao montar a tela do
    Perfil (não no boot do app: é diagnóstico, não caminho crítico). Três
    estados: carregando → só o do front, sem piscar travessão; sucesso → os
    dois; falha → o do servidor como travessão.
    Rótulos curtos e sem jargão, algo como `app F10-… · servidor F10-…`;
    decida a forma final lendo o espaço disponível e o tom do rodapé.
  - Atualizar o comentário FASE 8B logo acima: hoje ele afirma que carimbo
    diferente significa aparelho com código antigo, o que é falso quando o
    deploy foi só-backend. O comentário novo deve dizer que os dois carimbos
    divergem POR DESENHO nesse caso.
  - `copy.js`: se precisar de rótulo, entra nos DOIS modos com chaves
    idênticas (guardião `test_copy_theme.mjs`).

  Guardião `web/tests/test_carimbo_servidor_rodape.mjs` (padrão readFileSync +
  regex, filtrando comentários antes de contar): o rodapé referencia o carimbo
  do servidor; o estado de falha não cai no `BUILD_ID` do front (a regressão
  perigosa: repetir o do front faria a tela AFIRMAR que o servidor está numa
  versão que ninguém mediu — o mesmo defeito que esta sessão corrigiu três
  vezes); e asserção de sanidade que falha se a regex parar de casar.
  </action>
  <verify>node do guardião novo; `bash scripts/executar.sh --testes`; `cd web && npx vite build`</verify>
  <done>O rodapé mostra os dois, e falha vira travessão em vez de repetir o do front.</done>
</task>
</tasks>

<success_criteria>
Um commit atômico. Backend NÃO é tocado (`/api/health` já existe e serve).
Esta muda o FRONT: o SUMMARY deve lembrar que só chega ao usuário depois de
`bump.sh` + `publicar-web.sh` + deploy, e que o app iOS precisa de nova
instalação/TestFlight.
</success_criteria>
