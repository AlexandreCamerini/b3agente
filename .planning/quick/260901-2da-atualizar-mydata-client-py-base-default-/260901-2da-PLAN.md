---
phase: quick-260901-2da
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/mydata_client.py
  - server/tests/test_mydata_client.py
  - .planning/todos/pending/opcoes-v2-confirmar-hub-mydata-e-acesso-b-mcp.md
autonomous: true
requirements: [QT-260901-2da]

must_haves:
  truths:
    - "O default do cliente mydata aponta para o domínio canônico mydata.semente.dev"
    - "Nenhuma ocorrência do domínio antigo sobra em server/app/mydata_client.py"
    - "A suíte de testes do cliente mydata continua verde (guardião atualizado, não apagado)"
    - "O todo de acompanhamento registra que a ação decorrente do item 1 foi executada"
  artifacts:
    - path: "server/app/mydata_client.py"
      provides: "BASE_DEFAULT e docstring com o domínio canônico"
      contains: "https://mydata.semente.dev"
    - path: "server/tests/test_mydata_client.py"
      provides: "guardião de base_url() alinhado ao novo default, com nota da troca"
      contains: "mydata.semente.dev"
    - path: ".planning/todos/pending/opcoes-v2-confirmar-hub-mydata-e-acesso-b-mcp.md"
      provides: "registro da ação decorrente como CONCLUÍDA (item 2 segue aberto)"
      contains: "CONCLUÍDA"
  key_links:
    - from: "server/app/mydata_client.py"
      to: "server/tests/test_mydata_client.py"
      via: "assert m.base_url() == BASE_DEFAULT"
      pattern: "mydata\\.semente\\.dev"
---

<objective>
Corrigir o nome canônico do hub mydata no cliente HTTP do backend:
`BASE_DEFAULT` e a docstring de `server/app/mydata_client.py` passam de
`mydata.acamerini.app` para `mydata.semente.dev`.

Purpose: o Alex confirmou `semente.dev` como domínio canônico. Verificação
técnica já feita (mesmo Railway edge `jfk1`, `x-hikari-trace` idêntico
`jfk1.57w5`, `content-length` idêntico) prova que os dois nomes DNS resolvem
para o MESMO serviço — não há risco de apontar para dado/serviço diferente.
Produção depende integralmente desse default (Railway NÃO tem `MYDATA_URL`
setada, só `MYDATA_TOKEN` e `B3_OPTIONS_PROVIDER=mydata`), então a troca no
código É a correção.

Output: duas linhas corrigidas em `mydata_client.py`, o guardião de teste que
fixa o valor antigo atualizado com nota, e o todo de acompanhamento marcado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/notes/opcoes-v2-b-mcp-exploracao.md
@./CLAUDE.md

Estado atual dos pontos a tocar (já levantado — não precisa explorar):

- `server/app/mydata_client.py:1` — docstring abre com
  ``"""Cliente HTTP do hub `mydata.acamerini.app` (~/dev/cvm-financas), Fase 9``
- `server/app/mydata_client.py:21` — `BASE_DEFAULT = "https://mydata.acamerini.app"`
- `server/tests/test_mydata_client.py:98-99` — guardião que fixa o default:
  `def test_base_url_sem_env_usa_default():` / `assert m.base_url() == "https://mydata.acamerini.app"`
- `server/tests/test_mydata_client.py:1` — docstring do arquivo de teste também
  cita `mydata.acamerini.app`

Fora de escopo por decisão explícita do usuário (NÃO tocar):
- `server/app/candle_provider.py:193` — docstring também cita o domínio antigo.
  Escopo estrito pedido é só `mydata_client.py`; candle nem usa o mydata hoje
  (`B3_CANDLE_PROVIDER` continua `brapi` por decisão do Alex, ADR-008/023).
- Variáveis de ambiente do Railway — não foi pedido, não alterar.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trocar o domínio canônico no cliente mydata e no guardião</name>
  <files>server/app/mydata_client.py, server/tests/test_mydata_client.py</files>
  <action>
Em `server/app/mydata_client.py`, substituir as duas ocorrências de
`mydata.acamerini.app` por `mydata.semente.dev`:
1. Linha 1 (docstring do módulo): ``hub `mydata.acamerini.app` `` → ``hub `mydata.semente.dev` ``.
   Preservar todo o resto da docstring intacto (a referência a
   `~/dev/cvm-financas`, a Fase 9, a nota sobre `X-API-Key` não-Bearer).
2. Linha 21: `BASE_DEFAULT = "https://mydata.acamerini.app"` →
   `BASE_DEFAULT = "https://mydata.semente.dev"`.

Acrescentar UM comentário curto acima de `BASE_DEFAULT` registrando o porquê
(convenção da casa: comentário carrega história de decisão, não repete o
código) — algo na linha de: domínio canônico confirmado pelo Alex em
2026-09-01; `mydata.acamerini.app` era alias do MESMO serviço Railway
(mesmo edge/trace), segue vivo mas não é o nome a usar; produção não seta
`MYDATA_URL`, logo depende deste default.

Em `server/tests/test_mydata_client.py`, atualizar o guardião que fixa o valor
antigo — o repo proíbe apagar guardião, exige atualizá-lo com nota:
1. Linha 99: `assert m.base_url() == "https://mydata.acamerini.app"` →
   `assert m.base_url() == "https://mydata.semente.dev"`.
2. Acrescentar comentário de uma linha no corpo de
   `test_base_url_sem_env_usa_default` dizendo que o valor mudou
   deliberadamente em 2026-09-01 (domínio canônico), não é drift.
3. Linha 1 (docstring do arquivo de teste): trocar a citação
   `mydata.acamerini.app` por `mydata.semente.dev` para não deixar o teste
   documentando o nome aposentado.

NÃO alterar mais nada nesses dois arquivos: nem a lógica de `base_url()`
(validação https/localhost, remoção de barra final), nem `TIMEOUT_S`,
`PAGINAS_MAX`, `RANGE_DIAS`, nem qualquer outro teste do arquivo.
  </action>
  <verify>
    <automated>grep -c "mydata\.acamerini\.app" server/app/mydata_client.py; test "$(grep -c 'mydata\.acamerini\.app' server/app/mydata_client.py)" = "0" &amp;&amp; grep -q 'BASE_DEFAULT = "https://mydata\.semente\.dev"' server/app/mydata_client.py &amp;&amp; python3 -m py_compile server/app/mydata_client.py &amp;&amp; (cd server &amp;&amp; python3 -m pytest tests/test_mydata_client.py -q)</automated>
  </verify>
  <done>
`grep -c "mydata.acamerini.app" server/app/mydata_client.py` retorna 0;
`BASE_DEFAULT` é exatamente `"https://mydata.semente.dev"`; `py_compile`
passa sem erro; `pytest tests/test_mydata_client.py` verde (incluindo
`test_base_url_sem_env_usa_default` com o novo valor).
  </done>
</task>

<task type="auto">
  <name>Task 2: Marcar a ação decorrente como concluída no todo de acompanhamento</name>
  <files>.planning/todos/pending/opcoes-v2-confirmar-hub-mydata-e-acesso-b-mcp.md</files>
  <action>
Editar a seção final do todo, hoje intitulada
`## Ação decorrente do item 1 (resolvido) — pendente de confirmação`
(linhas 40-50).

Renomear o título para
`## Ação decorrente do item 1 (resolvido) — CONCLUÍDA 2026-09-01`
e reescrever o corpo no passado, registrando: o Alex confirmou; o
`BASE_DEFAULT` de `server/app/mydata_client.py` foi trocado de
`https://mydata.acamerini.app` para `https://mydata.semente.dev`; a docstring
do módulo e o guardião `test_base_url_sem_env_usa_default` foram atualizados
junto; risco nulo porque os dois nomes DNS respondem no mesmo serviço Railway
(mesmo edge `jfk1`, `x-hikari-trace` `jfk1.57w5` idêntico); nenhuma variável
de ambiente do Railway foi alterada (produção não seta `MYDATA_URL` e continua
não setando — o default do código é a fonte). Referenciar o commit como
"ver commit desta quick task (`.planning/quick/260901-2da-*`)" — NÃO inventar
hash, ele só existe depois do commit.

NÃO mover o arquivo de `todos/pending/` para `todos/resolved/`: o item 2 do
todo (acesso server-to-server ao `b-mcp`) segue em aberto e é o que ainda
bloqueia o planejamento de Opções v2. Deixar os itens 1 e 2 e a seção
"Por que isso bloqueia planejamento" como estão.
  </action>
  <verify>
    <automated>grep -q "CONCLUÍDA 2026-09-01" .planning/todos/pending/opcoes-v2-confirmar-hub-mydata-e-acesso-b-mcp.md &amp;&amp; grep -q "mydata.semente.dev" .planning/todos/pending/opcoes-v2-confirmar-hub-mydata-e-acesso-b-mcp.md &amp;&amp; test -f .planning/todos/pending/opcoes-v2-confirmar-hub-mydata-e-acesso-b-mcp.md &amp;&amp; ! test -f .planning/todos/resolved/opcoes-v2-confirmar-hub-mydata-e-acesso-b-mcp.md</automated>
  </verify>
  <done>
A seção da ação decorrente está marcada como CONCLUÍDA com data 2026-09-01 e
descreve a troca aplicada; o arquivo permanece em `todos/pending/` (item 2
aberto) e não existe cópia em `todos/resolved/`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → hub mydata (HTTPS externo) | O `BASE_DEFAULT` define para qual host o backend envia o `MYDATA_TOKEN` (header `X-API-Key`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2da-01 | Information Disclosure | `mydata_client.base_url()` — token enviado ao novo host | accept | O host novo é comprovadamente o MESMO serviço (mesmo Railway edge `jfk1`, `x-hikari-trace` `jfk1.57w5` e `content-length` idênticos), sob controle do próprio Alex. Não há mudança de destinatário efetivo do `MYDATA_TOKEN`. |
| T-2da-02 | Spoofing | Downgrade para host não-TLS | mitigate | Já mitigado no código existente: `base_url()` levanta `ValueError` para `http://` fora de localhost. O plano não toca essa validação — o teste `test_base_url_http_fora_de_localhost_levanta` continua guardando. |
| T-2da-03 | Tampering | Instalação de pacote | n/a | Nenhum `pip install`/`npm install` neste plano — zero dependências novas. |
</threat_model>

<verification>
1. `grep -rn "mydata.acamerini.app" server/app/mydata_client.py server/tests/test_mydata_client.py` → 0 linhas.
2. `python3 -m py_compile server/app/mydata_client.py` → sem saída, exit 0.
3. `cd server && python3 -m pytest tests/test_mydata_client.py -q` → verde.
4. `git diff --stat` → exatamente 3 arquivos tocados
   (`mydata_client.py`, `test_mydata_client.py`, o `.md` do todo). Qualquer
   arquivo a mais é escopo vazado — reverter.
5. Suíte canônica completa (`bash scripts/executar.sh --testes`) NÃO é
   exigida: mudança de constante de config, sem front (`web/src/` intocado,
   logo sem `vite build`), sem lógica.
</verification>

<success_criteria>
- `BASE_DEFAULT == "https://mydata.semente.dev"` em `server/app/mydata_client.py`.
- Zero ocorrências de `mydata.acamerini.app` em `mydata_client.py` e em
  `test_mydata_client.py`.
- `pytest tests/test_mydata_client.py` verde — guardião atualizado com nota,
  não apagado (guardrail do repo).
- Todo em `pending/` com a ação decorrente marcada CONCLUÍDA 2026-09-01,
  arquivo NÃO movido para `resolved/`.
- Diff limitado a 3 arquivos. `candle_provider.py` e envs do Railway intocados.
</success_criteria>

<output>
Create `.planning/quick/260901-2da-atualizar-mydata-client-py-base-default-/260901-2da-SUMMARY.md` when done
</output>
</content>
</invoke>
