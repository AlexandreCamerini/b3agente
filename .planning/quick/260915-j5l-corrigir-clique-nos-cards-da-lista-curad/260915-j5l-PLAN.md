---
phase: quick-260915-j5l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/opcoes/executarCandidato.js
  - web/src/App.jsx
  - web/src/copy.js
  - web/tests/test_executar_candidato.mjs
  - web/tests/test_curadoria_ui.mjs
autonomous: true
requirements: [QUICK-260915-J5L]

must_haves:
  truths:
    - "Clicar num card do bloco curado abre uma confirmação INLINE dentro do próprio card (não rola a tela, não expande o acordeão antigo PropostaLastreada)"
    - "Cada um dos 4 tipos (call_coberta, put_protecao, collar, opcao_a_descoberto) dispara a rota correta com o corpo que a rota real exige — venda coberta e put em POST /api/options/lastreada/abrir, collar em POST /api/options/lastreada/abrir-collar, a descoberto em POST /api/options/buy"
    - "A confirmação mostra prêmio, perda máxima e breakeven lendo SÓ campos que o candidato já traz (item.premioTotal, item.estrutura.*) — nenhuma chamada nova ao servidor, nenhuma aritmética nova além do helper porLote já usado em PropostaLastreada"
    - "Erro do servidor aparece VERBATIM no card (e.message), inclusive o 400 do gate de liquidez DIFÍCIL e o 403 de Modo Estudo — nunca reescrito, nunca um genérico 'falhou'"
    - "Liquidez DIFÍCIL exige consentimento explícito no próprio card antes de mandar aceitaLiquidezDificil: true — nunca reenvio automático"
    - "Modo Estudo não mostra botão de executar em card nenhum (defesa em UI espelhando o 403 do servidor, mesmo padrão T-14-23 de PropostaLastreada)"
    - "Os três estados existem na confirmação: parado, executando (botão travado), erro (mensagem do servidor) e executado (sem reenvio)"
    - "`bash scripts/executar.sh --testes` verde (pytest + .mjs) e `npx vite build` verde"
  artifacts:
    - path: "web/src/opcoes/executarCandidato.js"
      provides: "corpoDoCandidato() puro (tipo → rota + corpo exato) + executarCandidato() com store injetável"
      exports: ["corpoDoCandidato", "executarCandidato"]
    - path: "web/tests/test_executar_candidato.mjs"
      provides: "4 casos (um por tipo) com store espião: rota chamada + corpo byte a byte; mais liquidez e tipo desconhecido"
    - path: "web/src/App.jsx"
      provides: "CuradoriaEstruturas com confirmação inline + A.executarCandidatoCurado"
      contains: "executarCandidato"
  key_links:
    - from: "web/src/App.jsx::CuradoriaEstruturas"
      to: "web/src/opcoes/executarCandidato.js"
      via: "onExecutar → A.executarCandidatoCurado → executarCandidato(cand, { store })"
      pattern: "executarCandidato"
    - from: "web/src/opcoes/executarCandidato.js"
      to: "store.optionsAbrirLastreada / store.optionsAbrirCollar / store.optionsBuy"
      via: "despacho por item.tipo — os TRÊS métodos já existem nos dois stores (serverStore e deviceStore), nenhum método novo de store"
      pattern: "optionsAbrirCollar|optionsAbrirLastreada|optionsBuy"
---

<objective>
O bloco "AS 4 MELHORES OPORTUNIDADES DE OPÇÕES" (Fase 31) lista as 4 estruturas
do motor, mas **nenhuma delas tem caminho de execução**: `App.jsx:4240` faz
`onClick={() => onAbrir(item.ticker)}`, jogando fora `idCandidato`,
`contractSymbol`, `pernasContratos`, `contratos`, `qtyAcoes`, `expiration` e
`tipo`. `onAbrir` é `abrirOpcoesDe` (`App.jsx:4641`), que só faz
`setOpcoesFor(t)` + `scrollIntoView` no acordeão antigo `PropostaLastreada` —
outro motor, proposta ÚNICA por posição, hoje sempre venda coberta. Clicar em
put de proteção, collar ou a descoberto leva ao mesmo lugar genérico, que não
sabe executar aquilo que o usuário clicou.

**Decisão de produto do Alex (AskUserQuestion, 2026-09-15):** o clique expande
uma confirmação INLINE dentro do próprio card — prêmio, perda máxima,
breakeven (dados que o candidato JÁ traz) e um botão "Executar" que dispara a
rota certa por tipo, sem rolar a tela para outro lugar.

Purpose: fechar a lacuna entre "a varredura acha 4 estruturas" e "o usuário
consegue abrir a que ele escolheu", sem tocar em backend (as três rotas já
existem, já são gateadas e já estão corretas).
Output: módulo puro de despacho + confirmação inline no card + guardiões.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

Leia SÓ os trechos abaixo (App.jsx tem 9.747 linhas — `Grep` antes de `Read`,
nunca o arquivo inteiro):
- `web/src/App.jsx:4190-4310` — `ROTULO_TIPO_CURADORIA` + `CuradoriaEstruturas` (o alvo)
- `web/src/App.jsx:4559-4610` — `useCuradoria` (o hook que alimenta `top`/`meta`)
- `web/src/App.jsx:4620-4650` e `4722-4742` — `CarteiraScreen`: `ctx` (tem `operador`), `abrirOpcoesDe`, o call site de `<CuradoriaEstruturas>`
- `web/src/App.jsx:8770-8817` — `A.buyOption` / `A.abrirLastreada` / `A.abrirCollar` (padrão setData + track + flash, erro repassando `e.message` verbatim)
- `web/src/opcoes/PropostaLastreada.jsx:102-158` — `useAceiteLastreado` (corpos enviados hoje, tratamento de liquidez DIFÍCIL, `busy`)
- `web/src/opcoes/PropostaLastreada.jsx:193-250` — bloco de payoff numérico: helper `porLote` null-safe e as chaves de copy `payoffGanhoMaximo`/`payoffPerdaMaxima`/`payoffBreakeven`/`payoffIlimitado`/`payoffSemDado` (JÁ EXISTEM nos dois modos — reuse, não recrie)
- `web/tests/test_curadoria_ui.mjs:1-120` — padrão "static source inspection" da casa e a lista `CHAVES`

<interfaces>
<!-- Contratos reais, já verificados no código nesta sessão. Não re-derivar. -->

Candidato (item de `top[]`, de `server/app/opcoes_curadoria.py`), campos comuns:
  { tipo, ticker, expiration, diasParaVencimento, contratos, qtyAcoes,
    premioUnitario, premioTotal, liquidez: { faixa, aviso|null, ... },
    estrutura, razao, manchete, didatica, precoObjeto, idCandidato,
    posicaoNoRanking }
  - call_coberta / put_protecao / opcao_a_descoberto: têm `contractSymbol`, `optionType`, `strike`
  - collar: `contractSymbol: null`, tem `strikeCall`/`strikePut` e
    `pernasContratos: [{contractSymbol, optionType, lado: "venda", strike, premioUnitario},
                       {contractSymbol, optionType, lado: "compra", ...}]`
  - opcao_a_descoberto: `contratos === 1`, `qtyAcoes === 100` (CONTRATOS_A_DESCOBERTO = 1)
  - `liquidez.aviso` só é string quando `liquidez.faixa === "DIFÍCIL"`; nas outras faixas é `null`

Rotas reais (server/app/main.py — NÃO tocar nestes arquivos):
  POST /api/options/lastreada/abrir        (main.py:3566)
    body: { underlying, contractSymbol, expiration?, contratos, aceitaLiquidezDificil? }
    - 403 se config.appMode !== "operador"
    - 400 se `tipo === "collar"` ou >1 perna declarada
    - 400 "Liquidez SEM MERCADO (...)" — incondicional, nenhuma flag destrava
    - 400 "Liquidez DIFÍCIL (score N/100) — ... Reenvie com aceitaLiquidezDificil: true ..."
      (a flag é checada por identidade `is not True`)
    - o servidor resolve call→abrir_call_coberta, put→comprar_put_protecao pelo `optionType` do contrato que ELE buscou
  POST /api/options/lastreada/abrir-collar (main.py:3649)
    body: { underlying, pernasContratos: [{contractSymbol, lado}, {contractSymbol, lado}], contratos, expiration?, aceitaLiquidezDificil? }
    - exige EXATAMENTE 2 pernas, `lado` em ("venda","compra"); re-deriva prêmios/strikes no servidor (nunca confia no corpo)
    - 409 se já há lastreada aberta no underlying
  POST /api/options/buy                    (main.py:3017)
    body: { underlying, contractSymbol, expiration?, qty, meta? }
    - `qty` é em COTAS (normalizado pro lote de 100 mais próximo no servidor)
    - SEM gate de liquidez e SEM `aceitaLiquidezDificil` nesta rota (verificado)
    - o gate de `permitirOpcaoADescoberto` vive dentro de `store.buy_option` (Fase 29)

Clientes já existentes (nos DOIS stores — serverStore e deviceStore; não criar método novo):
  store.optionsAbrirLastreada(body)   // persistence.js:323 / 1438
  store.optionsAbrirCollar(body)      // persistence.js:327 / 1619
  store.optionsBuy(body)              // persistence.js:268 / 1225
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: módulo puro de despacho por tipo + 4 casos de teste</name>
  <files>web/src/opcoes/executarCandidato.js, web/tests/test_executar_candidato.mjs</files>
  <behavior>
    - call_coberta → chama `store.optionsAbrirLastreada` com
      `{underlying: ticker, contractSymbol, expiration, contratos}` (+ `aceitaLiquidezDificil: true` só quando consentido)
    - put_protecao → MESMA rota/corpo do item acima (o servidor decide a perna pelo `optionType` que ele resolve)
    - collar → chama `store.optionsAbrirCollar` com
      `{underlying, pernasContratos: [{contractSymbol, lado}, {contractSymbol, lado}], contratos, expiration}`
      — SÓ `contractSymbol` e `lado` por perna, nada de strike/prêmio (o servidor re-deriva, ADR-026 Decisão 2)
    - opcao_a_descoberto → chama `store.optionsBuy` com
      `{underlying, contractSymbol, expiration, qty: cand.qtyAcoes}` e SEM `aceitaLiquidezDificil` (a rota não aceita)
    - tipo desconhecido / candidato nulo → lança erro nomeado, nunca chama store nenhum
    - `aceitaLiquidezDificil` só entra no corpo quando o chamador passa `true` (identidade, não truthiness)
  </behavior>
  <action>
Crie `web/src/opcoes/executarCandidato.js` com DUAS funções exportadas:

1. `corpoDoCandidato(cand, { aceitaLiquidezDificil = false } = {})` — PURA, zero
   I/O: devolve `{ metodo, body }` onde `metodo` é o NOME do método de store
   (`"optionsAbrirLastreada"` | `"optionsAbrirCollar"` | `"optionsBuy"`) e `body`
   é o corpo exato de cada rota conforme o bloco `<interfaces>` acima. Tipo
   desconhecido ou candidato sem os campos obrigatórios do seu tipo → `throw new
   Error(...)` com motivo nomeado (nunca devolver corpo meia-boca: um corpo
   inválido viraria 400 do servidor com mensagem que o usuário não entende).

2. `executarCandidato(cand, { store, aceitaLiquidezDificil })` — resolve o par
   pelo item 1 e faz `return store[metodo](body)`. `store` é INJETADO (é isso
   que torna os 4 casos testáveis sem DOM e sem rede) e o valor devolvido é o
   estado público da carteira que a rota retorna.

Regras não negociáveis deste módulo, com comentário de topo explicando cada uma
(mesmo estilo de `estruturaParaPayoff.js`):
- ZERO aritmética financeira: os números (`contratos`, `qtyAcoes`) viajam como o
  backend os mandou. Nada de `* 100`, nada de recalcular prêmio (princípio 5 do
  CLAUDE.md).
- ZERO composição de mensagem de erro do servidor: este módulo não captura
  exceção nenhuma — deixa a rejeição subir intacta.
- ZERO método novo de store: reusar os três que já existem nos DOIS stores
  preserva a paridade `deviceStore`↔`serverStore` sem tocar em `persistence.js`
  (guardrail do CLAUDE.md).

Crie `web/tests/test_executar_candidato.mjs` no padrão da casa (Node puro, sem
DOM, `import` direto do módulo, contador `fails` + helper `ok`, igual a
`test_estrutura_para_payoff.mjs`). Casos mínimos:
  1. call_coberta → método `optionsAbrirLastreada` + corpo idêntico (comparação
     profunda, chave a chave, incluindo a AUSÊNCIA de `aceitaLiquidezDificil`)
  2. put_protecao → mesma rota, corpo idêntico
  3. collar → `optionsAbrirCollar`, exatamente 2 pernas, cada perna com SOMENTE
     `contractSymbol` e `lado` (assertar `Object.keys(perna).length === 2` —
     mandar strike/prêmio a mais seria o cliente tentando ditar a estrutura)
  4. opcao_a_descoberto → `optionsBuy`, `qty === cand.qtyAcoes`, e assertar que
     a chave `aceitaLiquidezDificil` NÃO existe no corpo
  5. `aceitaLiquidezDificil: true` entra no corpo nos tipos lastreada/collar
  6. tipo desconhecido e candidato `null` → lançam e o store espião não recebe
     chamada nenhuma
O store espião é um objeto literal que registra `{metodo, body}` e devolve um
estado falso — nenhum `fetch` real, nenhum mock de rede.

Escreva os casos ANTES do módulo e meça o RED (rodar o `.mjs` e ver falhar);
registre a saída no SUMMARY.
  </action>
  <verify>
    <automated>node web/tests/test_executar_candidato.mjs</automated>
  </verify>
  <done>Os 6 casos passam; `corpoDoCandidato` é pura (sem import de api/store/fetch no arquivo); nenhum arquivo de backend e nenhum store tocado.</done>
</task>

<task type="auto">
  <name>Task 2: confirmação inline no card + ação com erro verbatim</name>
  <files>web/src/App.jsx, web/src/copy.js</files>
  <action>
**(a) `web/src/copy.js`** — acrescente aos DOIS registros (`COPY.estudo` e
`COPY.operador`, paridade obrigatória) SOMENTE as chaves que ainda não existem:
`curadoriaExecutarCta`, `curadoriaExecutando`, `curadoriaFechar`,
`curadoriaExecutada`, `curadoriaLiquidezConsentir`, `curadoriaEstudoNaoExecuta`,
`curadoriaVerPosicao`. Reuse as chaves de payoff que JÁ EXISTEM
(`payoffTitulo`, `payoffPerdaMaxima`, `payoffBreakeven`, `payoffIlimitado`,
`payoffSemDado`) — não crie sinônimo. Texto em Estudo é professor, em Operador é
mesa (skill `didatica-boris`); nenhuma palavra de promessa/garantia de
resultado; `curadoriaEstudoNaoExecuta` descreve a condição, não convida a trocar
de modo com linguagem de venda. **Cuidado com o guardião CVM**
(`server/tests/test_opcoes_collar_vocab.py` varre o arquivo INTEIRO, inclusive
comentário): não escreva em `copy.js` — nem em comentário — a string-âncora da
manchete do collar; foi exatamente o achado Rule 1 do 31-04.

**(b) `A.executarCandidatoCurado(cand, opts)`** em `App.jsx`, ao lado dos irmãos
(`abrirLastreada` ~8790): chama `executarCandidato(cand, { store,
aceitaLiquidezDificil: opts && opts.aceitaLiquidezDificil })`, e no sucesso faz
`setData(s)` + `track("trade_simulated", { side: "abrir", ticker: cand.ticker,
instrument: "curadoria_" + cand.tipo })` + `flash(...)`. **Divergência
deliberada dos irmãos, comente a razão no código:** o erro NÃO vira `flash` —
é `throw` de volta, porque nesta tela a mensagem do servidor pertence ao card
que o usuário clicou (um toast de 2,6s sobre um card expandido seria a segunda
superfície da mesma falha). Nenhuma reescrita: quem renderiza usa `e.message`
verbatim.

**(c) `CuradoriaEstruturas`** — a confirmação inline:
- Props novas: `operador` e `onExecutar` (mantém `onAbrir`, ver abaixo).
- Estado novo (3 `useState`, todos indexados por `idCandidato` — nunca por
  ticker: dois candidatos do mesmo ticker convivem no top-4):
  `abertoId` (qual card está expandido, `null` = nenhum), `execucao`
  (`{[id]: {busy, erro, ok}}`) e `liquidezOk` (`{[id]: true}` do consentimento).
- O `onClick` do card passa a alternar `abertoId` (clicar de novo fecha).
  **Remova o `onClick={() => onAbrir(item.ticker)}`**; `onAbrir` sobrevive como
  link secundário DENTRO do painel expandido (`cp.curadoriaVerPosicao`), para
  não apagar a navegação da Fase 18.
- Painel expandido (renderizado só para `item.idCandidato === abertoId`,
  abaixo do card, dentro do mesmo container — não pode rolar a tela nem abrir
  modal):
  * `item.manchete` VERBATIM (guardrail CVM — nunca truncar/concatenar);
  * prêmio: `money(item.premioTotal)` — o mesmo campo que o card já mostra;
  * perda máxima e breakeven lendo `item.estrutura`, com o helper `porLote`
    null-safe COPIADO do padrão de `PropostaLastreada.jsx:200` (`(v) => typeof
    v === "number" ? v * (item.qtyAcoes || 0) : null`) e
    `est.perda_ilimitada ? cp.payoffIlimitado : "R$ " + price(porLote(est.perda_maxima))`;
    breakevens via `est.breakevens.map(price).join(" / ")`, `cp.payoffSemDado`
    quando vazio. Nada além disso — nenhum número novo é calculado aqui;
  * **Modo Estudo (`!operador`)**: NADA de botão executar — mostra
    `item.didatica` + `cp.curadoriaEstudoNaoExecuta`. Defesa em UI espelhando o
    403 do servidor (mesmo T-14-23 de `PropostaLastreada`);
  * **Liquidez DIFÍCIL** (`item.liquidez && item.liquidez.faixa === "DIFÍCIL"`):
    o `item.liquidez.aviso` aparece VERBATIM com um checkbox
    (`cp.curadoriaLiquidezConsentir`) que alimenta `liquidezOk[id]`; o botão
    Executar fica desabilitado enquanto não marcado, e só então o corpo leva
    `aceitaLiquidezDificil: true`. Se `faixa === "DIFÍCIL"` e `aviso` vier
    ausente, o botão fica desabilitado com o motivo escrito — nunca mandar
    consentimento que o usuário não leu. **Nada de `window.confirm`** aqui (a
    decisão do Alex é confirmação inline), e nada de reenvio automático quando
    o servidor devolver o 400 de liquidez: esse 400 é exibido como qualquer
    outro erro, verbatim, e a pessoa decide;
  * Botão Executar: `disabled` quando `busy` ou já `ok`; rótulo
    `cp.curadoriaExecutando` enquanto em voo; chama
    `onExecutar(item, { aceitaLiquidezDificil: !!liquidezOk[id] })`, com
    `try/catch/finally` gravando `erro: e.message || String(e)` e `ok: true`;
  * Erro: caixa em `T.warn` (NUNCA `T.negative` — vermelho é de P&L, precedente
    do `LimiteAtingido` da Fase 25) com a mensagem do servidor verbatim;
  * Sucesso: `cp.curadoriaExecutada`, botão some/trava. **Não recarregue a
    curadoria** — refetch gastaria orçamento do mydata e a carteira já
    atualizou via `setData`. Registre isso em comentário.
- Acessibilidade: o card continua `<button>` com `aria-expanded` refletindo
  `abertoId`, `min-height: 44px` preservado, e o painel legível em 375px.

**(d) call site** (`CarteiraScreen`, ~4728): passe `operador={operador}` e
`onExecutar={(cand, o) => A.executarCandidatoCurado(cand, o)}` — mantendo
`onAbrir={abrirOpcoesDe}`.

Não toque em `server/`, em `persistence.js` nem em `PropostaLastreada.jsx`.
  </action>
  <verify>
    <automated>cd web && npx vite build && node tests/test_executar_candidato.mjs</automated>
  </verify>
  <done>Build verde; clicar no card expande painel inline por tipo; Estudo sem botão; erro do servidor exibido verbatim; `git diff --stat` mostra só `App.jsx` e `copy.js`.</done>
</task>

<task type="auto">
  <name>Task 3: guardiões do bloco curado + suíte canônica</name>
  <files>web/tests/test_curadoria_ui.mjs</files>
  <action>
Estenda `test_curadoria_ui.mjs` (não reescreva: as 13 regras existentes ficam —
"guardiões de teste não se apagam"; atualize o comentário de topo com nota
datada 2026-09-15 explicando a mudança, mesmo padrão da nota da Fase 31).

Atualizações obrigatórias:
- `CHAVES`: acrescente as 7 chaves novas de `copy.js` e corrija a contagem no
  texto das asserções e do cabeçalho (18 → 25). A regra "o CONJUNTO de chaves
  curadoria* é idêntico nos dois modos" já pega o resto sozinha.

Regras novas (cada uma com a razão no comentário):
  14. o card do bloco NÃO chama mais `onAbrir(item.ticker)` no `onClick`
      principal — grep pelo padrão antigo tem de dar zero dentro do corpo de
      `CuradoriaEstruturas` (era o bug: o clique descartava o candidato inteiro);
  15. o `onExecutar` recebe o ITEM (`onExecutar(item`), nunca só o ticker;
  16. o despacho por tipo mora em `web/src/opcoes/executarCandidato.js` e é
      importado por `App.jsx` — `CuradoriaEstruturas` não contém nenhum literal
      de rota (`/api/options/`), porque rota escrita na UI é a segunda cópia do
      contrato;
  17. o painel inline lê `item.estrutura`/`item.premioTotal` e NÃO chama
      `store.`/`api.` nenhum dentro do componente (sem rede nova no clique);
  18. `porLote` do painel é null-safe (`typeof v === "number"`) — `null * 100
      === 0` inventaria "perda máxima R$ 0,00", que é justamente o erro que a
      regra "null nunca 0.0" proíbe;
  19. a caixa de erro usa `T.warn` e nunca `T.negative` no bloco da curadoria;
  20. nenhum `window.confirm(` dentro de `CuradoriaEstruturas` (a confirmação é
      inline, decisão do Alex de 2026-09-15);
  21. `aceitaLiquidezDificil` nunca é enviado sem o consentimento do card —
      estaticamente: o identificador só aparece no componente junto do estado
      de consentimento (`liquidezOk`), nunca como literal `true` solto;
  22. Modo Estudo sem CTA: o botão de executar está dentro de um ramo guardado
      por `operador`.

Prova negativa MEDIDA (não descrita), com `git diff` limpo depois de reverter:
  (i) voltar o `onClick` para `onAbrir(item.ticker)` derruba a regra 14;
  (ii) trocar `T.warn` por `T.negative` na caixa de erro derruba a regra 19;
  (iii) trocar `porLote` por multiplicação direta derruba a regra 18.
Registre as saídas reais no SUMMARY.

Depois rode a suíte canônica INTEIRA uma vez só (as duas suítes; `scripts/test.sh`
sozinho não conta). Se aparecerem ~26 falhas de `PermissionError ... ssl.py`,
é o artefato de sandbox já documentado — reexecute com
`dangerouslyDisableSandbox: true` e registre as duas saídas.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <done>Suíte canônica exit 0 (pytest sem falha + todos os `.mjs`, contagem `.mjs` 149 → 150); as 3 provas negativas medidas e revertidas; `npx vite build` verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → `/api/options/*` | corpo montado no cliente atravessa aqui; o servidor já re-valida tudo (cadeia, preço, liquidez, modo, lastro) |
| candidato do servidor → UI | `manchete`/`didatica`/`liquidez.aviso` vêm prontos do motor e são renderizados verbatim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-J5L-01 | Tampering | corpo do collar montado no front | mitigate | enviar só `contractSymbol`+`lado` por perna; a rota re-deriva prêmio/strike (ADR-026 D2) — teste assertando `Object.keys(perna).length === 2` |
| T-J5L-02 | Elevation of Privilege | executar em Modo Estudo | accept (defesa dupla) | UI esconde o CTA sob `operador`; o 403 do servidor continua sendo a defesa real |
| T-J5L-03 | Information Disclosure | mensagem de erro do servidor na tela | mitigate | exibir `e.message` verbatim; nenhuma composição/adivinhação de causa (princípio 4) |
| T-J5L-04 | Repudiation | execução sem consentimento de liquidez DIFÍCIL | mitigate | checkbox no card alimenta `aceitaLiquidezDificil`; sem marca, botão travado; nunca reenvio automático após o 400 |
| T-J5L-05 | Denial of Service | refetch da curadoria após executar | mitigate | proibido por desenho — orçamento do mydata é 15k/mês; estado vem do `setData` da própria rota |
| T-J5L-SC | Tampering | instalação de pacote | mitigate | nenhum pacote novo neste plano (zero `npm install`) |
</threat_model>

<verification>
1. `node web/tests/test_executar_candidato.mjs` — 4 tipos + liquidez + inválidos.
2. `node web/tests/test_curadoria_ui.mjs` — regras 1-13 preservadas + 14-22 novas.
3. `cd web && npx vite build` — grep/teste estático não pega erro de sintaxe JSX.
4. `bash scripts/executar.sh --testes` — as DUAS suítes, exit 0.
5. `git diff --stat` — só os 5 arquivos de `files_modified`; `server/` e
   `persistence.js` intocados.
</verification>

<success_criteria>
- Clicar em cada um dos 4 tipos abre confirmação inline no próprio card, sem rolagem.
- Cada tipo dispara a rota correta com o corpo exato que a rota exige (provado por teste com store espião).
- Prêmio/perda máxima/breakeven vêm dos campos do candidato; nenhuma chamada de rede no clique.
- 403 de Estudo, 400 de liquidez e 409 de collar aparecem verbatim no card.
- Modo Estudo não oferece executar em card nenhum.
- Suíte canônica verde e `vite build` verde.
</success_criteria>

<output>
Crie `.planning/quick/260915-j5l-corrigir-clique-nos-cards-da-lista-curad/260915-j5l-SUMMARY.md` ao terminar.

Pendência operacional a NOMEAR no SUMMARY (não executar): isto toca `web/src/`,
então só chega ao usuário com `scripts/bump.sh` + `scripts/publicar-web.sh`, e
ao iPhone só com `cap sync` + build novo. A Fase 31 também segue não publicada —
as duas entram na mesma janela.
</output>
