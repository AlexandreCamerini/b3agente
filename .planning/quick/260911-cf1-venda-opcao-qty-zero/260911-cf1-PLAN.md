---
quick_id: 260911-cf1
phase: quick-260911-cf1
plan: 01
type: quick
wave: 1
depends_on: []
files_modified: [server/app/main.py, server/tests/test_fase5_rejeicao_rotas.py]
autonomous: true
---

<objective>
Achado D-1, descoberto pelo executor da quick 260911-15a ao corrigir o A-09 na
linha de baixo: `POST /api/options/sell` tem ABERTA a mesma regressão que a
venda de ação corrigiu em agosto (F10-20260819).

`server/app/main.py:2397`:
```python
qty=int(_qty) if _qty else None
```
`qty: 0` é falsy em Python → vira `None` → `store.sell_option` entende
"campo ausente" → **vende a posição INTEIRA**. E `qty: "abc"` levanta
`ValueError` sem tratamento → 500 com o texto cru da exceção.

É a última rota da família ainda aberta: `/api/sell` (F10-20260819),
`/api/buy` (quick 260910-mqs, achado A-00) e `/api/options/buy`
(quick 260911-15a) já foram fechadas.
</objective>

<context>
Alcançabilidade: a interface NÃO manda `qty` nesta rota hoje (`optionsSell`
repassa o corpo montado pela tela). Então só é atingível por chamada direta à
API — o que não a torna inofensiva: é venda total silenciosa de uma posição
real a partir de uma entrada que o usuário poderia crer que vende zero.

O espelho a seguir é `/api/sell` (`main.py:~2196-2215`), que resolveu
exatamente isto: distingue AUSENTE (`None`) de ZERO/NEGATIVO explícito
(`is not None`), converte sob `try/except (TypeError, ValueError)`, grava
`store.registrar_rejeicao` antes de levantar (só com `scope is not None`) e
levanta `HTTPException(400, "Quantidade inválida.")`.

O teste que trava a versão de ação é
`test_sell_qty_zero_400_regressao_f10_20260819`
(`server/tests/test_fase5_rejeicao_rotas.py:173`).
</context>

<tasks>
<task type="auto">
  <name>Task 1: venda de opção distingue ausente de zero</name>
  <files>server/app/main.py, server/tests/test_fase5_rejeicao_rotas.py</files>
  <action>
  Espelhar o padrão de `/api/sell` na rota de venda de opção:
  - `qty` AUSENTE continua significando venda TOTAL (contrato atual da rota —
    LEIA e preserve; a tela depende disso);
  - `qty` ZERO ou NEGATIVO explícito → 400 `"Quantidade inválida."` (mesma
    string das outras três rotas da família, para a UI não ganhar variante);
  - `qty` não numérico → mesmo 400, nunca 500.
  Rejeição de conta logada grava no histórico com `registrar_rejeicao`, no
  padrão que a rota já usa para os outros casos (confira como ela faz hoje e
  qual `tipo` usa — provavelmente "VENDA"; siga o existente, não invente).

  Comentário de decisão citando D-1, a data, e o fato de ser a QUARTA e última
  rota da família — com o ponteiro para a regressão original F10-20260819, que
  é a mesma armadilha do falsy.

  Testes em `test_fase5_rejeicao_rotas.py`, espelhando o
  `test_sell_qty_zero_400_regressao_f10_20260819`: zero → 400 e **posição
  intacta** (a asserção que importa: provar que NÃO virou venda total);
  negativo → 400; não numérico → 400 e não 500; e um guardião do contrato
  preservado: `qty` AUSENTE continua vendendo tudo. Atualizar a tabela do
  docstring do arquivo com nota da auditoria.
  </action>
  <verify>
  RED/GREEN obrigatório: rodar os testes novos contra o código de ANTES
  (`git show HEAD:server/app/main.py` para arquivo temporário FORA do repo —
  NUNCA `git stash`) e confirmar que o de `qty=0` falha mostrando a venda
  total; depois com a correção. Registrar as duas saídas no SUMMARY.
  Depois: `bash scripts/executar.sh --testes`.
  </verify>
  <done>`qty=0` não vende mais a posição inteira, e o teste prova a posição intacta.</done>
</task>
</tasks>

<success_criteria>
Um commit atômico. Front NÃO é editado. Deploy só-backend (bump manual) — NÃO
fazer. Com esta entrega, as quatro rotas da família têm a mesma guarda.
</success_criteria>
