# Quick Task 260820-0hl: Assertividade do motor de recomendação — pesquisa + design

**Modo:** quick (pesquisa, sem implementação)
**Descrição:** Investigar se a taxa de stop >> alvo é real usando dados de
`analysis_outcomes`, mapear o pipeline determinístico de confluência/entrada/
stop/alvo, pesquisar práticas de risk management, avaliar (sem implementar)
scraping do TradingView, e entregar um ADR/memo com diagnóstico numérico,
alternativas com trade-off e recomendação.

## Tarefas

1. **Pesquisa** — spawnar `gsd-phase-researcher` para extrair dados reais de
   `analysis_outcomes` (dev), mapear o pipeline arquivo:linha, pesquisar
   literatura de risk management e confirmar a viabilidade/risco de ToS do
   TradingView. Output: `260820-0hl-RESEARCH.md`.
2. **Verificação em produção** — a pesquisa recomendou (I7) confirmar os
   números contra o banco de produção antes de fechar o ADR. Rodar
   diagnóstico read-only via `railway ssh` (mesmo padrão de
   `scripts/medir-yahoo-na-railway.sh`), com aprovação explícita do usuário
   por tocar produção. Sem escrita, sem deploy.
3. **Escrever o ADR** — `docs/adr/015-assertividade-do-motor-de-recomendacao.md`,
   com diagnóstico (dev + produção), 3 alternativas com trade-off e uma
   recomendação, sinalizando explicitamente que nenhuma alternativa move
   cálculo do motor determinístico para julgamento de IA (Princípio 5 do
   CLAUDE.md).

## Notas de execução

O documento foi escrito diretamente pelo orquestrador (não delegado a
`gsd-executor`) porque a precisão dos números (n=44→n=6, +2,56R→0,00R,
159→66 planos deduplicados) é o conteúdo central do entregável — repassar por
uma camada adicional de agente arriscava parafrasear/perder precisão nos
números citados. Todas as afirmações de código citam arquivo:linha e foram
verificadas por leitura direta (não presumidas do relatório do pesquisador).

## Critério de aceite

- [ ] ADR em `docs/adr/015-*.md` com diagnóstico numérico real (dev + prod)
- [ ] 2-3 alternativas com trade-off explícito
- [ ] Recomendação clara
- [ ] TradingView avaliado e risco de ToS exposto, não escondido
- [ ] Nenhuma alternativa recomendada move cálculo de determinístico para IA
      sem sinalizar isso explicitamente
- [ ] Nenhum código de produção alterado
