---
quick_id: 260909-th4
phase: quick-260909-th4
plan: 01
type: quick
wave: 1
depends_on: []
files_modified: [server/app/main.py]
autonomous: true
---

<objective>
Bump manual do `SERVER_BUILD_ID` para `F10-20260909-01`, preparando o deploy SÓ-backend da correção de fuso (260909-oyu, PR #36 mesclado em `f238b53`). Sem isso `/api/health` continuaria respondendo `F10-20260908-02` depois do deploy e não daria pra provar que o servidor novo subiu.
</objective>

<context>
- Padrão documentado em `server/app/main.py:1013-1017` e na memória do projeto: deploy só-backend não passa pelo `entregar.sh`, então o carimbo NÃO muda sozinho — bump à mão.
- `web/src/version.js` está em `F10-20260908-02` (igual ao servidor). Como o front NÃO é republicado nesta rodada, ele fica como está: é o esperado num deploy só-backend (o carimbo do servidor anda na frente do front).
- Risco conhecido (memória `deploy-backend-carimbo`): se HOJE ainda houver publicação de front, `bash scripts/bump.sh` sem argumento geraria `F10-20260909-01` e COLIDIRIA. Quem publicar front hoje tem que passar o valor explícito: `bash scripts/bump.sh F10-20260909-02`.
- Deploy em si NÃO faz parte desta task: auto-deploy do Railway está desligado desde 2026-09-07; é clique manual do Alex no painel depois do merge.
</context>

<tasks>
<task type="auto">
  <name>Task 1: bump do carimbo</name>
  <files>server/app/main.py</files>
  <action>Trocar `SERVER_BUILD_ID = "F10-20260908-02"` por `"F10-20260909-01"` com comentário curto (data, o quê, que contém o -02 de 08/09). Só essa linha.</action>
  <verify>`python -c "from app import main; assert main.SERVER_BUILD_ID == 'F10-20260909-01'"` de dentro de `server/`; `pytest -q -k health` verde.</verify>
  <done>`/api/health` in-process responde o id novo; nenhuma outra linha alterada.</done>
</task>
</tasks>

<verification>
- Diff de `server/app/main.py` com exatamente 1 linha alterada.
- Import do módulo funciona (sem erro de sintaxe) e o valor é o esperado.
</verification>

<success_criteria>
Commit atômico `chore(server): bump SERVER_BUILD_ID F10-20260909-01 (260909-th4)` pronto pra merge; STATE registra que o deploy fica pendente de clique do Alex e que o próximo bump de front de hoje precisa ser explícito (`-02`).
</success_criteria>
