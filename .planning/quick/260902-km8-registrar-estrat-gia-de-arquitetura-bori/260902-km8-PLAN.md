---
phase: quick-260902-km8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/notes/opcoes-v2-b-mcp-exploracao.md
  - .planning/seeds/opcoes-v2-setups-propostos-via-b-mcp.md
  - .planning/todos/pending/opcoes-v2-confirmar-hub-mydata-e-acesso-b-mcp.md
autonomous: true
requirements: [QUICK-260902-KM8]

must_haves:
  truths:
    - "Quem ler a nota entende que o v1 de Opções NÃO depende do b-mcp em runtime — a dependência foi trocada por adoção pontual de código puro (calculos.py), decidida uma vez, no repo do Boris"
    - "As 5 estratégias levantadas pelo agente de arquitetura (A a E) estão registradas com 1-2 linhas cada, e está explícito que a escolhida foi a B com o refinamento de reaproveitar calculos.py"
    - "O achado operacional crítico está registrado: o Boris já seleciona contrato por liquidity_score >= 40 + strike extremo (opcoes_lastreadas.py, Fase 14) e o estruturas.py do b-mcp seleciona por delta sem filtro de liquidez — réguas incompatíveis, e a decisão é MANTER liquidity_score"
    - "A DSL de setups técnicos (setups.py do b-mcp) está registrada como FORA de escopo do v1, com o racional de risco de sinal do ADR-016/ADR-017 e o fato de que o gatilho técnico já vem do Radar do Boris"
    - "O limite/interface interno está nomeado concretamente — rastrear(cadeia, filtros) e avaliar(pernas) — no vocabulário do contrato ADR-004/mydata_client.py, não num vocabulário novo"
    - "O item 2 (Transporte em produção) e o Bloqueio 2 da nota não se leem mais como trava do v1; o texto anterior continua legível, rotulado como contexto histórico"
    - "plano-mcp-servico.md está registrado nos três arquivos como o gatilho futuro (em avaliação pelo Alex, não aprovado) que habilita a migração para a Estratégia C"
    - "O todo pendente deixou de ser bloqueio de planejamento e virou item de acompanhamento, sem perder o texto original nem o link para a nota e o seed"
    - "Nenhum arquivo de código foi tocado — o diff é exclusivamente .planning/"
  artifacts:
    - path: ".planning/notes/opcoes-v2-b-mcp-exploracao.md"
      provides: "Item 2 e Bloqueio 2 atualizados + nova seção de arquitetura com as 5 estratégias, a escolha, as duas réguas de seleção e o limite rastrear()/avaliar()"
      contains: "liquidity_score"
    - path: ".planning/seeds/opcoes-v2-setups-propostos-via-b-mcp.md"
      provides: "Seção de decisões de arquitetura fechadas com o resumo da estratégia; trigger_condition destravada"
      contains: "calculos.py"
    - path: ".planning/todos/pending/opcoes-v2-confirmar-hub-mydata-e-acesso-b-mcp.md"
      provides: "Item 2 convertido de bloqueio de planejamento em item de acompanhamento ligado a plano-mcp-servico.md"
      contains: "plano-mcp-servico"
  key_links:
    - from: ".planning/seeds/opcoes-v2-setups-propostos-via-b-mcp.md"
      to: ".planning/notes/opcoes-v2-b-mcp-exploracao.md"
      via: "resumo da decisão de arquitetura aponta a seção completa da nota"
      pattern: "opcoes-v2-b-mcp-exploracao"
    - from: ".planning/todos/pending/opcoes-v2-confirmar-hub-mydata-e-acesso-b-mcp.md"
      to: ".planning/notes/opcoes-v2-b-mcp-exploracao.md"
      via: "o item de acompanhamento aponta a seção de arquitetura que o desbloqueou"
      pattern: "opcoes-v2-b-mcp-exploracao"
    - from: ".planning/notes/opcoes-v2-b-mcp-exploracao.md"
      to: "~/dev/MCP/docs/plano-mcp-servico.md"
      via: "gatilho futuro nomeado (Estratégia C) em item 2, Bloqueio 2 e seção de arquitetura"
      pattern: "plano-mcp-servico"
---

<objective>
Registrar a estratégia de arquitetura fechada com o Alex nesta sessão: **o v1 de
Opções do Boris não depende do b-mcp em runtime**. O único acoplamento aceito é
código Python puro (`calculos.py`, matemática de payoff) adotado uma vez por
cópia no repo do Boris; a seleção de contrato continua sendo a régua que já está
em produção (`liquidity_score >= 40` + strike extremo); a DSL de setups técnicos
fica fora do escopo do v1.

Purpose: hoje os três arquivos de planejamento dizem, em coro, que Opções v2
está travada esperando "acesso server-to-server ao b-mcp" — a nota marca o item 2
como BLOQUEADO, o seed condiciona `/gsd-plan-phase` à resolução do todo, e o todo
tem `priority: high` com título "antes de planejar Opções v2". Nada disso é mais
verdade: a estratégia escolhida contorna a necessidade. Enquanto os docs
disserem o contrário, ou a fase não é planejada (espera um bloqueio que não
bloqueia mais), ou é planejada em cima da régua errada — o achado operacional
mais caro desta sessão é que o `estruturas.py` do b-mcp escolhe contrato por
**delta** e o Boris em produção escolhe por **liquidity_score**; convivendo sem
reconciliar, o app proporia venda coberta por critérios diferentes dependendo da
tela.

Output: três arquivos de planejamento atualizados. ZERO código.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/notes/opcoes-v2-b-mcp-exploracao.md
@.planning/seeds/opcoes-v2-setups-propostos-via-b-mcp.md
@.planning/todos/pending/opcoes-v2-confirmar-hub-mydata-e-acesso-b-mcp.md
@CLAUDE.md

<facts>
Achados do agente de arquitetura (leitura direta de código nos dois repos, não de
doc) — registrar como fato, não reformular o racional:

1. **`~/dev/MCP/servers/mydata/estruturas.py`** (arquivo NOVO, 525 linhas + 450 de
   teste) traz um comentário explícito do autor: "Este é o arquivo que o Boris
   porta por cópia". Catálogo de 13 estruturas de opções (inclui covered_call e
   collar), montagem de pernas **por DELTA** (0,25 asa / 0,5 ATM), **sem filtro
   de liquidez**.
2. **`~/dev/MCP/docs/plano-mcp-servico.md`** (datado 2026-09-02) desenha um
   serviço MCP novo em `mcp.semente.dev`, com autenticação por bearer de máquina
   (não a senha do portal humano), teto por cliente e chave própria no hub; a
   Fase 5 desse plano é "contrato para o Boris". Estado: **"aguarda aprovação
   antes de qualquer implementação"** — o Alex conhece o plano, mas ele está em
   AVALIAÇÃO, não aprovado.
3. **O portal público `b-mcp.semente.dev` serve dado SINTÉTICO/fixture em
   produção** (`MYDATA_MODO` vira "meta" na nuvem). Chamá-lo direto violaria o
   princípio 4 do CLAUDE.md (nunca inventar valor) — não é opção viável de
   qualquer forma.
4. **Achado operacional crítico — duas réguas de seleção incompatíveis.** O Boris
   JÁ TEM motor de 1 perna em produção (`server/app/opcoes_lastreadas.py`, Fase
   14) que escolhe o contrato por **strike extremo + `liquidity_score` >= 40**. O
   `estruturas.py` escolhe por **delta** e não filtra liquidez. Se as duas
   convivessem sem reconciliar, o app proporia venda coberta por critérios
   diferentes dependendo da tela.
5. **Dois riscos de classe diferente dentro do b-mcp.** A matemática de payoff
   (`calculos.py` — custo líquido, breakeven, delta somado; pura, sem I/O,
   identidade aritmética) é risco BAIXO de portar. A DSL de setups técnicos
   (`setups.py`, sinal preditivo) é risco ALTO: o Boris já MEDIU (ADR-016) que
   sinal ingênuo de confluência PERDE DINHEIRO, e reconstruiu seleção por peso
   histórico (ADR-017). Portar a DSL sem passar pelo `scripts/backtest_sinal.py`
   reintroduziria um defeito já corrigido. O v1 não precisa dela — o gatilho
   técnico já vem do Radar do Boris.

Decisão fechada com o Alex nesta sessão (discussão socrática, pergunta por
pergunta):

- `plano-mcp-servico.md` está em avaliação, não aprovado → **o v1 NÃO pode
  depender dele**. A estratégia é de INDEPENDÊNCIA do b-mcp até ele ficar pronto
  (projeto paralelo, outro repo).
- **Critério de seleção do contrato: mantém o de produção** — `liquidity_score`
  >= 40 + strike extremo (`server/app/opcoes_lastreadas.py`). NÃO adota o
  critério por delta do `estruturas.py`.
- **Matemática de payoff: reaproveita `calculos.py`** (custo líquido, ganho/perda
  máximos, breakevens, delta somado para N pernas). É identidade aritmética pura,
  sem I/O, zero dependência de runtime do MCP — portar é decisão de código feita
  uma vez (como adotar uma lib), não integração viva.
- **DSL de setups (`setups.py`): FORA de escopo** desta decisão e do v1 inteiro —
  desnecessária (gatilho vem do Radar) e carrega risco de sinal já corrigido
  (ADR-016/017).
- **Limite/interface interno: duas funções** — `rastrear(cadeia, filtros) ->
  [contratos]` (equivalente a `find_tradable_options`) e `avaliar(pernas) ->
  {custo, ganho_max, perda_max, breakevens, delta}` (equivalente a
  `evaluate_option_structure`) — desenhadas no vocabulário que
  `mydata_client.py` / contrato ADR-004 já usa (prêmio/strike/delta/tipo), NÃO um
  vocabulário novo. É isso que faz a troca futura pela chamada MCP (quando
  `plano-mcp-servico.md` for aprovado) ser troca de **corpo de função**, não
  redesenho.
- **Fonte de dado continua `mydata_client.py`** (já existe, já em produção, já
  respeita o lock do WR-01/`mydata_budget.py`) — zero canal de rede novo.

As 5 estratégias levantadas pelo agente de arquitetura, para registro (a escolha
só tem valor se as alternativas descartadas estiverem legíveis):

- **A** — portar `estruturas.py` por cópia (o próprio autor sinaliza isso no
  arquivo).
- **B** — estender o motor próprio do Boris, usando o b-mcp só como
  especificação de referência.
- **C** — aprovar `plano-mcp-servico.md` e integrar via MCP autenticado
  (`mcp.semente.dev`, bearer de máquina).
- **D** — empurrar a conta para o hub REST (`mydata.semente.dev`), estendendo os
  endpoints existentes.
- **E** — núcleo compartilhado versionado entre os dois repos (lib comum).

**ESCOLHIDA: B**, com o refinamento de reaproveitar `calculos.py` (a parte de A
que é matemática pura, sem o critério de seleção por delta que vem junto).

Guardrail do repo (CLAUDE.md): **"histórico não se reescreve"**. O texto que hoje
declara o item 2 / Bloqueio 2 como travado é registro de sessão — não some; a
atualização entra POR CIMA dele, com o anterior rotulado como contexto
histórico. Mesmo padrão já aplicado nesses arquivos nas quick tasks 260901-r5t e
260901-u2c desta mesma exploração.
</facts>

<project_skills_check>
Skills do projeto: `didatica-boris` (vocabulário/didática voltada ao usuário
final do app) e `swiftui-pro` (revisão de código SwiftUI). Nenhuma incide sobre
doc interno de planejamento — sem conflito de regra a acomodar.
</project_skills_check>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registrar a estratégia de independência do b-mcp nos três docs de planejamento</name>
  <files>.planning/notes/opcoes-v2-b-mcp-exploracao.md, .planning/seeds/opcoes-v2-setups-propostos-via-b-mcp.md, .planning/todos/pending/opcoes-v2-confirmar-hub-mydata-e-acesso-b-mcp.md</files>
  <action>
Edite os TRÊS arquivos com a ferramenta Edit (nunca heredoc/`cat`). São a mesma
unidade de decisão — têm que sair consistentes entre si e apontar uns para os
outros.

---

**Arquivo 1 — `.planning/notes/opcoes-v2-b-mcp-exploracao.md`**

(1a) **Item 2 da seção "Decisões tomadas nesta sessão".** A linha hoje é:

  `**2. Transporte em produção.** PARCIALMENTE RESOLVIDO, com um ponto BLOQUEADO`
  `(ver seção Bloqueios). O Boris já tem ...`

Reestruture o item em duas partes, nesta ordem:

1. **Veredito primeiro.** Mantenha o cabeçalho `**2. Transporte em produção.**`
   e faça o texto que vem logo depois, na MESMA linha, começar literalmente com
   `DECIDIDO (2026-09-02):`. Conteúdo do veredito: o v1 **não chama o processo
   nem o serviço b-mcp em runtime**; o transporte de dado de opções continua
   sendo o REST do `mydata_client.py` que já está em produção (Fase 9/14,
   `mydata.semente.dev`), e a lógica de screening/cálculo fica em código
   determinístico do próprio Boris, atrás do limite interno descrito na nova
   seção de arquitetura (aponte-a pelo título). Deixe explícito que a
   dependência de runtime do b-mcp foi trocada por **adoção pontual de código
   puro** (`calculos.py`), decidida uma vez, e que a migração para chamadas MCP
   de verdade só volta à mesa se/quando `plano-mcp-servico.md` for aprovado.
2. **Contexto histórico**, claramente rotulado como tal, preservando o texto
   original do item: a expressão `PARCIALMENTE RESOLVIDO, com um ponto
   BLOQUEADO`, o fato de o Boris já ter `mydata_client.py` e
   `options_provider_mydata.py` em produção desde 31/08, e o racional de nunca
   subir client MCP stdio dentro do processo único do Railway (esse racional
   continua VÁLIDO, não é só histórico — diga isso). Não apague nada.

(1b) **Nova seção de arquitetura.** Insira uma seção nova com o título
`## Arquitetura decidida (2026-09-02) — independência do b-mcp no v1`,
posicionada **imediatamente antes** de `## Bloqueios — precisam do Alex, não
adivinhados`. Ela deve conter, nesta ordem:

- **Achados novos que mudaram o quadro** (registre os 5 do bloco `<facts>`,
  incluindo: o `estruturas.py` com o comentário "Este é o arquivo que o Boris
  porta por cópia"; o `plano-mcp-servico.md` datado de 2026-09-02, Fase 5 =
  "contrato para o Boris", estado **aguardando aprovação**; e o fato de o portal
  `b-mcp.semente.dev` servir dado **sintético/fixture** em produção
  (`MYDATA_MODO` = "meta"), o que por si só já o desqualifica como fonte —
  chamá-lo violaria o princípio 4 do CLAUDE.md, "não invente valor").
- **As 5 estratégias levantadas, A a E**, cada uma com 1-2 linhas, usando
  literalmente os rótulos `Estratégia A`, `Estratégia B`, `Estratégia C`,
  `Estratégia D`, `Estratégia E` (conteúdo no bloco `<facts>`). Marque a
  escolhida de forma inequívoca: **Estratégia B ESCOLHIDA**, com o refinamento
  de reaproveitar `calculos.py`. Registre por que as outras caem: A traz junto o
  critério de seleção por delta (ver achado abaixo); C depende de um plano não
  aprovado; D empurra para o hub uma conta que é pura e não precisa de rede; E
  cria acoplamento de versionamento entre dois repos com ciclos de release
  independentes.
- **Achado operacional crítico — duas réguas de seleção incompatíveis.**
  `server/app/opcoes_lastreadas.py` (Fase 14, produção) escolhe contrato por
  **strike extremo + `liquidity_score` >= 40**; `estruturas.py` escolhe por
  **delta** (0,25 asa / 0,5 ATM) e **não filtra liquidez**. Decisão: **mantém
  `liquidity_score`** — a régua de produção. Registre a consequência que motivou
  a decisão: se as duas convivessem sem reconciliar, o app proporia venda
  coberta por critérios diferentes dependendo da tela.
- **O que se reaproveita e o que não.** REAPROVEITA: `calculos.py` (custo
  líquido, ganho/perda máximos, breakevens, delta somado para N pernas) —
  matemática de identidade pura, sem I/O, zero dependência de runtime do MCP;
  portar é decisão de código feita uma vez, como adotar uma lib. NÃO
  REAPROVEITA: a DSL de setups técnicos (`setups.py`) — **fora de escopo do v1**,
  com os dois motivos explícitos: (i) desnecessária, o gatilho técnico já vem do
  Radar do Boris; (ii) risco de sinal — o Boris já **mediu** (ADR-016) que sinal
  ingênuo de confluência PERDE DINHEIRO e reconstruiu a seleção por peso
  histórico (ADR-017); portar a DSL sem passar pelo `scripts/backtest_sinal.py`
  reintroduziria um defeito já corrigido.
- **Limite/interface interno.** Nomeie as duas funções literalmente:
  `rastrear(cadeia, filtros) -> [contratos]` (equivalente a
  `find_tradable_options`) e `avaliar(pernas) -> {custo, ganho_max, perda_max,
  breakevens, delta}` (equivalente a `evaluate_option_structure`). Registre
  explicitamente que o vocabulário é o do **contrato ADR-004 /
  `mydata_client.py`** (prêmio/strike/delta/tipo), NÃO um vocabulário novo — e
  que é isso que faz a troca futura pela chamada MCP ser **troca de corpo de
  função, não redesenho**. Registre também que a fonte de dado continua
  `mydata_client.py`, que já respeita o lock do WR-01 (`mydata_budget.py`) —
  zero canal de rede novo (isso é o item 3 desta mesma nota, continua valendo).

(1c) **Bloqueio 2 da seção "## Bloqueios — precisam do Alex, não adivinhados".**
Atualize o item 2 (portal protegido por `PORTAL_SENHA` / acesso
server-to-server), sem apagar a pergunta original:
- Comece o item com um marcador inequívoco `**DESBLOQUEADO PARA O V1
  (2026-09-02)**` e explique: não é mais bloqueio ativo porque a estratégia
  escolhida (Estratégia B, ver a seção de arquitetura) contorna a necessidade —
  o Boris não chama o b-mcp em runtime.
- Registre que o plano de resolução existe e tem nome: `plano-mcp-servico.md`
  (em `~/dev/MCP/docs/`, datado 2026-09-02), **em avaliação pelo Alex, não
  aprovado**; quando aprovado, habilita uma migração futura (Estratégia C) que é
  **troca de corpo de função no limite interno**, não redesenho.
- Preserve abaixo o texto original da pergunta (senha do portal, docs tratando o
  server como "pessoal, local, stdio", "presumir multi-tenant seria arquitetura
  inventada") como contexto histórico rotulado. Acrescente o achado que o
  reforça: o portal serve dado sintético em produção, então nem "pedir a senha"
  seria caminho.
- Não mexa nos bloqueios 1 e 3.

(1d) **Seção "## Próximo passo formal".** Hoje ela diz que "os 3 bloqueios acima
(domínio do hub, acesso server-to-server ao b-mcp, rate-limit) são o que trava
`/gsd-plan-phase`". Isso ficou falso. Atualize: o bloqueio 1 está resolvido, o 2
está desbloqueado para o v1 pela estratégia decidida, e o que resta é o
rate-limit (item 3, todo `medir-rate-limit-mydata.md`) mais a pendência de
produto do plano comercial. Diga o que isso implica: `/gsd-plan-phase` de Opções
v2 tem base suficiente para ser desenhado. Preserve a menção ao todo criado na
sessão original.

Não toque nas seções "Ideia original", "Achado principal", "Padrão de
arquitetura já validado", "Navegação", nem nos itens 1, 3, 4, 5, 6 e 7 das
decisões. Em "## Fontes consultadas nesta sessão", acrescente as fontes NOVAS
desta rodada: `~/dev/MCP/servers/mydata/estruturas.py`,
`~/dev/MCP/docs/plano-mcp-servico.md` e `server/app/opcoes_lastreadas.py`.

---

**Arquivo 2 — `.planning/seeds/opcoes-v2-setups-propostos-via-b-mcp.md`**

(2a) Crie uma seção NOVA `Decisões de arquitetura fechadas:`, posicionada
**entre** o bloco `Decisões de produto fechadas:` e o bloco `Pendências de
produto (não técnicas, aguardando o Alex):`. Justificativa do julgamento (siga
esta, não invente outra): as duas decisões já fechadas ali são de **produto**
(navegação, escopo da biblioteca) e foram tomadas pelo Alex por critério de
produto; esta é de **arquitetura/implementação** e tem público diferente (quem
for planejar/executar a fase). Misturá-las diluiria as duas. Não riscar, não
mover nada do que já existe.

Conteúdo da nova seção, como bullets:
- **Independência do b-mcp em runtime no v1** (2026-09-02). O Boris não chama o
  processo nem o serviço b-mcp; o único acoplamento é código Python puro
  adotado uma vez, por cópia, no repo do Boris. Estratégia B (motor próprio do
  Boris, b-mcp como especificação de referência) entre as 5 avaliadas.
- **Critério de seleção do contrato: `liquidity_score` >= 40 + strike extremo** —
  mantém o que já está em produção (`server/app/opcoes_lastreadas.py`, Fase 14).
  NÃO adota o critério por **delta** do `estruturas.py` do b-mcp. Registre o
  motivo: réguas incompatíveis; convivendo sem reconciliar, o app proporia venda
  coberta por critérios diferentes dependendo da tela.
- **Reaproveita `calculos.py`** (custo líquido, ganho/perda máximos, breakevens,
  delta somado) — matemática de identidade pura, sem I/O.
- **Exclui a DSL de setups (`setups.py`)** do escopo do v1 — gatilho técnico já
  vem do Radar do Boris, e portar sinal preditivo sem `backtest_sinal.py`
  reintroduziria o defeito medido no **ADR-016** e corrigido no ADR-017.
- **Limite interno `rastrear()` / `avaliar()`**, no vocabulário do contrato
  **ADR-004** / `mydata_client.py` — a troca futura pela chamada MCP (quando
  `plano-mcp-servico.md` for aprovado) é troca de corpo de função, não redesenho.
- Aponte `.planning/notes/opcoes-v2-b-mcp-exploracao.md`, seção "Arquitetura
  decidida (2026-09-02)", como racional completo.

(2b) **Frontmatter `trigger_condition`.** Hoje ele diz que só quando o todo for
resolvido (identidade do hub + via de acesso server-to-server ao b-mcp) é que
`/gsd-plan-phase` tem base. Isso ficou falso — atualize para refletir que a base
já existe: o v1 é planejável com código determinístico do Boris, sem depender do
b-mcp; o que resta antes de virar fase é a pendência de produto do plano
comercial (e, como acompanhamento, o rate-limit do hub). Preserve a informação
de que o item de identidade do hub foi resolvido.

(2c) **Linha `Bloqueios que precisam resolver antes de planejar:`** no corpo —
reescreva o rótulo para refletir a realidade (ex.: item de acompanhamento /
histórico dos bloqueios), mantendo o link para
`.planning/todos/pending/opcoes-v2-confirmar-hub-mydata-e-acesso-b-mcp.md`. Não
remova o link.

(2d) Não altere a seção `Pendências de produto` — o plano comercial continua em
aberto e nada nesta decisão o toca.

---

**Arquivo 3 — `.planning/todos/pending/opcoes-v2-confirmar-hub-mydata-e-acesso-b-mcp.md`**

Julgamento a aplicar (siga este, não invente outro): o arquivo **continua em
`todos/pending/`** — não vá para `resolved/`, porque o item 2 não foi
respondido, apenas deixou de bloquear. O que muda é a natureza: de **bloqueio de
planejamento** para **item de acompanhamento** ligado à aprovação de
`plano-mcp-servico.md`.

(3a) **Frontmatter:** troque `priority: high` por `priority: medium` e reescreva
o `title` para não afirmar mais "antes de planejar Opções v2" — deve descrever
acompanhamento (ex.: acompanhar a aprovação do serviço MCP autenticado para
eventual migração de Opções v2). O `# título H1` do corpo deve casar com o novo
`title`.

(3b) **Item 2:** abra com `**DESBLOQUEADO PARA O V1 (2026-09-02)**` e registre:
- a estratégia escolhida contorna a necessidade — o Boris não chama o b-mcp em
  runtime (Estratégia B; `liquidity_score` mantido; `calculos.py` portado;
  `setups.py` fora de escopo; limite interno `rastrear()`/`avaliar()`);
- o gatilho de revisita: **quando o Alex aprovar `plano-mcp-servico.md`**
  (`~/dev/MCP/docs/`, datado 2026-09-02, autenticação por bearer de máquina em
  `mcp.semente.dev`, Fase 5 = "contrato para o Boris"), revisitar a decisão de
  migrar o limite interno para chamadas MCP de verdade (**Estratégia C**);
- que o portal `b-mcp.semente.dev` serve dado sintético em produção, então o
  caminho "usar o portal" está descartado independentemente de senha;
- preserve o texto original da pergunta como contexto histórico rotulado.

(3c) **Parágrafo "Por que isso bloqueia planejamento":** ele afirma que sem o
item 2 não dá pra desenhar o transporte. Atualize (preservando o texto anterior
como histórico) para registrar que o transporte foi decidido —
`mydata_client.py` REST, sem canal novo — e que o planejamento da fase deixou de
depender deste todo.

(3d) Adicione links explícitos para
`.planning/notes/opcoes-v2-b-mcp-exploracao.md` (seção "Arquitetura decidida
(2026-09-02)") e para `.planning/seeds/opcoes-v2-setups-propostos-via-b-mcp.md`,
para que o item de acompanhamento não fique órfão da decisão que o desbloqueou.

(3e) Não toque na seção "Ação decorrente do item 1 (resolvido) — CONCLUÍDA
2026-09-01".

---

**Escopo estrito:** nenhum arquivo fora de `.planning/`. Não abra, não edite e
não crie nada em `web/`, `server/` ou `web-admin/` — a implementação do motor de
opções v2 é fase futura, fora de escopo. Não rode build, `cap sync`, bump ou
publicação. Não crie ADR novo: esta é decisão de planejamento; o ADR nasce
quando a fase for implementada.
  </action>
  <verify>
    <automated>
cd /Users/acamerini/dev/bolsia/b3-agente && bash -c '
NOTA=.planning/notes/opcoes-v2-b-mcp-exploracao.md
SEED=.planning/seeds/opcoes-v2-setups-propostos-via-b-mcp.md
TODO=.planning/todos/pending/opcoes-v2-confirmar-hub-mydata-e-acesso-b-mcp.md
fail=0
chk()  { if [ "$2" -eq 0 ]; then echo "FAIL: $1"; fail=1; else echo "ok: $1"; fi; }
chkz() { if [ "$2" -ne 0 ]; then echo "FAIL: $1"; fail=1; else echo "ok: $1"; fi; }

# --- extracoes escopadas por secao (nunca gate no arquivo inteiro) ---
ITEM2=$(awk "/^\\*\\*2\\. Transporte em produ/,/^\\*\\*3\\./" "$NOTA")
HDR2=$(grep -m1 "^\\*\\*2\\. Transporte em produ" "$NOTA")
ARQ=$(awk "/^## Arquitetura decidida/,/^## Bloqueios/" "$NOTA")
BLOQ=$(awk "/^## Bloqueios/,/^## Pr/" "$NOTA")
BLOQ2=$(printf %s "$BLOQ" | awk "/^2\\./,/^3\\./")
PROX=$(awk "/^## Pr.ximo passo formal/,0" "$NOTA")
ARQFECH=$(awk "/Decis.*es de arquitetura fechadas/,/Pend.*ncias de produto/" "$SEED")
PRODFECH=$(awk "/Decis.*es de produto fechadas/,/Decis.*es de arquitetura fechadas/" "$SEED")
TRIG=$(awk "/^trigger_condition:/,/^planted_date:/" "$SEED")

# ===== 1. NOTA — item 2 virou veredito, historico preservado =====
chk "nota/item2: cabecalho carrega DECIDIDO (2026-09-02)" "$(printf %s "$HDR2" | grep -c "DECIDIDO (2026-09-02)")"
chk "nota/item2: diz que nao chama o b-mcp em runtime"    "$(printf %s "$ITEM2" | grep -Eci "runtime")"
chk "nota/item2: transporte segue mydata_client.py"       "$(printf %s "$ITEM2" | grep -c "mydata_client")"
chk "nota/item2: aponta plano-mcp-servico como gatilho"   "$(printf %s "$ITEM2" | grep -c "plano-mcp-servico")"
chk "nota/item2: historico anterior preservado"           "$(printf %s "$ITEM2" | grep -c "PARCIALMENTE RESOLVIDO")"
chk "nota/item2: rotulo de contexto historico presente"   "$(printf %s "$ITEM2" | grep -Eci "hist.rico")"

# ===== 2. NOTA — nova secao de arquitetura existe e esta completa =====
chk "nota: secao Arquitetura decidida existe"             "$(grep -c "^## Arquitetura decidida" "$NOTA")"
chk "nota/arq: Estrategia A registrada"                   "$(printf %s "$ARQ" | grep -c "Estratégia A")"
chk "nota/arq: Estrategia B registrada"                   "$(printf %s "$ARQ" | grep -c "Estratégia B")"
chk "nota/arq: Estrategia C registrada"                   "$(printf %s "$ARQ" | grep -c "Estratégia C")"
chk "nota/arq: Estrategia D registrada"                   "$(printf %s "$ARQ" | grep -c "Estratégia D")"
chk "nota/arq: Estrategia E registrada"                   "$(printf %s "$ARQ" | grep -c "Estratégia E")"
chk "nota/arq: escolha marcada como ESCOLHIDA"            "$(printf %s "$ARQ" | grep -c "ESCOLHIDA")"
chk "nota/arq: achado estruturas.py"                      "$(printf %s "$ARQ" | grep -c "estruturas.py")"
chk "nota/arq: achado plano-mcp-servico (nao aprovado)"   "$(printf %s "$ARQ" | grep -c "plano-mcp-servico")"
chk "nota/arq: registra que o plano aguarda aprovacao"    "$(printf %s "$ARQ" | grep -Eci "aprova")"
chk "nota/arq: portal serve dado sintetico/fixture"       "$(printf %s "$ARQ" | grep -Eci "sint.tico|fixture")"

# ===== 3. NOTA/arq — as duas reguas de selecao e a escolha =====
chk "nota/arq: liquidity_score citado"                    "$(printf %s "$ARQ" | grep -c "liquidity_score")"
chk "nota/arq: strike extremo citado"                     "$(printf %s "$ARQ" | grep -ci "strike extremo")"
chk "nota/arq: regua por delta do b-mcp citada"           "$(printf %s "$ARQ" | grep -ci "delta")"
chk "nota/arq: motor em producao nomeado"                 "$(printf %s "$ARQ" | grep -c "opcoes_lastreadas")"
chk "nota/arq: incompatibilidade registrada"              "$(printf %s "$ARQ" | grep -Eci "incompat|réguas|reguas")"

# ===== 4. NOTA/arq — reaproveita calculos.py, exclui setups.py com racional =====
chk "nota/arq: calculos.py reaproveitado"                 "$(printf %s "$ARQ" | grep -c "calculos.py")"
chk "nota/arq: matematica pura sem I/O"                   "$(printf %s "$ARQ" | grep -Eci "pura|identidade")"
chk "nota/arq: setups.py registrado"                      "$(printf %s "$ARQ" | grep -c "setups.py")"
chk "nota/arq: setups.py fora de escopo"                  "$(printf %s "$ARQ" | grep -Eci "fora de escopo")"
chk "nota/arq: racional ADR-016"                          "$(printf %s "$ARQ" | grep -c "ADR-016")"
chk "nota/arq: racional ADR-017"                          "$(printf %s "$ARQ" | grep -c "ADR-017")"
chk "nota/arq: backtest_sinal citado"                     "$(printf %s "$ARQ" | grep -c "backtest_sinal")"
chk "nota/arq: gatilho vem do Radar do Boris"             "$(printf %s "$ARQ" | grep -ci "radar")"

# ===== 5. NOTA/arq — limite interno nomeado no vocabulario do ADR-004 =====
chk "nota/arq: funcao rastrear() nomeada"                 "$(printf %s "$ARQ" | grep -c "rastrear(")"
chk "nota/arq: funcao avaliar() nomeada"                  "$(printf %s "$ARQ" | grep -c "avaliar(")"
chk "nota/arq: equivalencia find_tradable_options"        "$(printf %s "$ARQ" | grep -c "find_tradable_options")"
chk "nota/arq: equivalencia evaluate_option_structure"    "$(printf %s "$ARQ" | grep -c "evaluate_option_structure")"
chk "nota/arq: vocabulario do contrato ADR-004"           "$(printf %s "$ARQ" | grep -c "ADR-004")"
chk "nota/arq: troca de corpo de funcao, nao redesenho"   "$(printf %s "$ARQ" | grep -Eci "corpo de fun")"
chk "nota/arq: fonte segue mydata_client.py"              "$(printf %s "$ARQ" | grep -c "mydata_client")"

# ===== 6. NOTA — Bloqueio 2 desbloqueado, historico preservado =====
chk "nota/bloq2: marcado DESBLOQUEADO"                    "$(printf %s "$BLOQ2" | grep -c "DESBLOQUEADO")"
chk "nota/bloq2: aponta plano-mcp-servico"                "$(printf %s "$BLOQ2" | grep -c "plano-mcp-servico")"
chk "nota/bloq2: registra que esta em avaliacao"          "$(printf %s "$BLOQ2" | grep -Eci "avalia|não aprovado|nao aprovado")"
chk "nota/bloq2: preserva a pergunta original (PORTAL_SENHA)" "$(printf %s "$BLOQ2" | grep -c "PORTAL_SENHA")"
chk "nota/bloq1: item 1 intacto (RESOLVIDO)"              "$(printf %s "$BLOQ" | grep -c "RESOLVIDO 2026-09-01")"
chk "nota/bloq3: item 3 intacto (rate-limit)"             "$(printf %s "$BLOQ" | grep -ci "rate-limit")"

# ===== 7. NOTA — proximo passo formal nao afirma mais 3 bloqueios travando =====
chkz "nota/proximo: nao diz mais que os 3 bloqueios travam" "$(printf %s "$PROX" | grep -c "os 3 bloqueios acima")"
chk  "nota/proximo: registra o que resta (rate-limit)"      "$(printf %s "$PROX" | grep -Eci "rate-limit")"
chk  "nota/proximo: registra pendencia comercial"           "$(printf %s "$PROX" | grep -ci "comercial")"

# ===== 8. SEED — nova secao de arquitetura, produto intacto =====
chk "seed: secao Decisoes de arquitetura fechadas existe" "$(grep -Ec "Decis.*es de arquitetura fechadas" "$SEED")"
chk "seed/arq: liquidity_score"                           "$(printf %s "$ARQFECH" | grep -c "liquidity_score")"
chk "seed/arq: calculos.py"                               "$(printf %s "$ARQFECH" | grep -c "calculos.py")"
chk "seed/arq: setups.py excluido"                        "$(printf %s "$ARQFECH" | grep -c "setups.py")"
chk "seed/arq: racional ADR-016"                          "$(printf %s "$ARQFECH" | grep -c "ADR-016")"
chk "seed/arq: limite rastrear/avaliar"                   "$(printf %s "$ARQFECH" | grep -Ec "rastrear\\(|avaliar\\(")"
chk "seed/arq: vocabulario ADR-004"                       "$(printf %s "$ARQFECH" | grep -c "ADR-004")"
chk "seed/arq: aponta a nota como racional completo"      "$(printf %s "$ARQFECH" | grep -c "opcoes-v2-b-mcp-exploracao")"
chk "seed/arq: carimba a data"                            "$(printf %s "$ARQFECH" | grep -c "2026-09-02")"
chk "seed/produto: decisao de navegacao intacta"          "$(printf %s "$PRODFECH" | grep -ci "candidato a")"
chk "seed/produto: escopo v1 (collar) intacto"            "$(printf %s "$PRODFECH" | grep -ci "collar")"
chk "seed: plano comercial segue pendente"                "$(awk "/Pend.*ncias de produto/,0" "$SEED" | grep -ci "plano comercial")"

# ===== 9. SEED — trigger_condition destravada =====
chkz "seed/trigger: nao condiciona mais ao todo do b-mcp" "$(printf %s "$TRIG" | grep -c "opcoes-v2-confirmar-hub-mydata-e-acesso-b-mcp")"
chk  "seed/trigger: registra a nova condicao"             "$(printf %s "$TRIG" | grep -Eci "comercial|determin")"
chk  "seed: link para o todo preservado no corpo"         "$(grep -c "opcoes-v2-confirmar-hub-mydata-e-acesso-b-mcp" "$SEED")"

# ===== 10. TODO — virou item de acompanhamento, sem perder historico =====
chk  "todo: priority rebaixada para medium"               "$(grep -c "^priority: medium" "$TODO")"
chkz "todo: title nao afirma mais bloqueio de planejamento" "$(grep -m1 "^title:" "$TODO" | grep -ci "antes de planejar")"
chk  "todo: item 2 marcado DESBLOQUEADO"                  "$(grep -c "DESBLOQUEADO" "$TODO")"
chk  "todo: aponta plano-mcp-servico como gatilho"        "$(grep -c "plano-mcp-servico" "$TODO")"
chk  "todo: nomeia a Estrategia C como migracao futura"   "$(grep -c "Estratégia C" "$TODO")"
chk  "todo: preserva a pergunta original (PORTAL_SENHA)"  "$(grep -c "PORTAL_SENHA" "$TODO")"
chk  "todo: acao decorrente do item 1 intacta"            "$(grep -c "CONCLUÍDA 2026-09-01" "$TODO")"
chk  "todo: liga de volta na nota"                        "$(grep -c "opcoes-v2-b-mcp-exploracao" "$TODO")"
chk  "todo: liga de volta no seed"                        "$(grep -c "opcoes-v2-setups-propostos-via-b-mcp" "$TODO")"
chk  "todo: segue em todos/pending"                       "$(ls "$TODO" >/dev/null 2>&1 && echo 1 || echo 0)"
chkz "todo: nao foi movido para resolved"                 "$(ls .planning/todos/resolved/opcoes-v2-confirmar-hub-mydata-e-acesso-b-mcp.md 2>/dev/null | wc -l | tr -d " ")"

# ===== 11. Escopo do diff: SO .planning/, zero codigo =====
chkz "nenhum arquivo fora de .planning tocado" "$(git diff --name-only HEAD | grep -cv "^\\.planning/")"
chkz "nenhum arquivo de codigo tocado"         "$(git diff --name-only HEAD | grep -Ec "^(web|server|web-admin)/")"

# ===== 12. Os tres arquivos alvo realmente mudaram =====
chk "nota modificada" "$(git diff --name-only HEAD -- "$NOTA" | wc -l | tr -d " ")"
chk "seed modificado" "$(git diff --name-only HEAD -- "$SEED" | wc -l | tr -d " ")"
chk "todo modificado" "$(git diff --name-only HEAD -- "$TODO" | wc -l | tr -d " ")"

[ "$fail" -eq 0 ] && echo "VERIFY OK" || { echo "VERIFY FALHOU"; exit 1; }
'
    </automated>
  </verify>
  <done>
- Nota: item 2 abre com `DECIDIDO (2026-09-02):` e preserva o texto anterior
  rotulado como histórico; existe a seção "## Arquitetura decidida (2026-09-02)"
  com as Estratégias A-E, a B marcada ESCOLHIDA, as duas réguas de seleção
  (`liquidity_score` mantido vs. delta descartado), `calculos.py` reaproveitado,
  `setups.py` excluído com racional ADR-016/017, e o limite `rastrear()`/
  `avaliar()` no vocabulário do ADR-004; Bloqueio 2 marcado DESBLOQUEADO com a
  pergunta original preservada; "Próximo passo formal" não afirma mais que os 3
  bloqueios travam o planejamento.
- Seed: nova seção "Decisões de arquitetura fechadas" entre produto e pendências,
  com o resumo da estratégia e o link para a nota; `trigger_condition` não
  condiciona mais ao todo do b-mcp; decisões de produto e pendência comercial
  intactas.
- Todo: `priority: medium`, título de acompanhamento, item 2 DESBLOQUEADO com
  gatilho em `plano-mcp-servico.md` (Estratégia C), links de volta para nota e
  seed, arquivo permanece em `todos/pending/`.
- `git diff --name-only HEAD` retorna exclusivamente caminhos sob `.planning/`.
- O bloco `<automated>` acima imprime `VERIFY OK`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma) | Edição de três arquivos markdown de planejamento interno. Sem entrada não confiável, sem superfície de rede, sem execução, sem dependência nova. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-km8-01 | Tampering | Arquivos de código do app (`web/`, `server/`) alterados por engano numa task de doc | mitigate | Verify gate falha se `git diff --name-only HEAD` listar qualquer caminho fora de `.planning/` |
| T-km8-02 | Tampering | Perda do achado das duas réguas de seleção — a fase futura nasce usando o critério por delta do `estruturas.py` e o app passa a propor venda coberta por critérios diferentes por tela | mitigate | Gates exigem `liquidity_score`, `strike extremo`, `delta`, `opcoes_lastreadas` e a palavra de incompatibilidade na seção de arquitetura, e `liquidity_score` também no seed |
| T-km8-03 | Tampering | A DSL de `setups.py` ser portada numa fase futura, reintroduzindo o defeito de sinal já medido e corrigido | mitigate | Gates exigem `setups.py` + "fora de escopo" + `ADR-016` + `ADR-017` + `backtest_sinal` na nota, e `setups.py` + `ADR-016` no seed |
| T-km8-04 | Repudiation | Decisão de arquitetura registrada sem carimbo — daqui a dois meses ninguém sabe se "não usar o b-mcp" foi decisão ou esquecimento | mitigate | Veredito carimbado `DECIDIDO (2026-09-02)` no item 2, título da seção com a data, e data exigida no bloco do seed |
| T-km8-05 | Information disclosure | Perda do racional histórico (por que o item 2 e o Bloqueio 2 eram tratados como trava) ao sobrescrever | mitigate | Guardrail "histórico não se reescreve"; gates exigem `PARCIALMENTE RESOLVIDO` ainda presente no item 2 e `PORTAL_SENHA` no Bloqueio 2 e no todo |
| T-km8-06 | Denial of service | O todo ser arquivado em `resolved/` e a informação do gatilho futuro (aprovação de `plano-mcp-servico.md` → Estratégia C) sumir do radar | mitigate | Gate exige o arquivo ainda em `todos/pending/`, ausente de `todos/resolved/`, com `plano-mcp-servico` e `Estratégia C` presentes |
| T-km8-07 | Elevation of privilege | Alguém ler "o plano do serviço MCP existe" como "está aprovado" e planejar a fase dependendo dele | mitigate | Gates exigem a marcação de aprovação pendente na seção de arquitetura e no Bloqueio 2 ("em avaliação / não aprovado") |
| T-km8-SC | Tampering | Instalação de pacote npm/pip/cargo | accept | Nenhum install nesta task — sem superfície de supply chain |
</threat_model>

<verification>
Rodar o bloco `<automated>` da Task 1 (é a suíte completa desta quick task).

Suíte canônica do repo (`bash scripts/executar.sh --testes`) e `npx vite build`
**não se aplicam**: nada em `server/`, `web/` ou `web-admin/` é tocado. Rodá-las
não valida nada aqui — o gate real é o escopo do diff somado aos gates de
conteúdo escopados por seção.
</verification>

<success_criteria>
1. `VERIFY OK` impresso pelo bloco automatizado.
2. Leitura humana da nota: um planejador que abra o arquivo entende, sem
   perguntar, que o v1 é implementável já, quais 5 estratégias foram avaliadas,
   por que a B venceu, qual régua de seleção vale (`liquidity_score`), o que se
   porta (`calculos.py`) e o que não se porta (`setups.py`, e por quê).
3. Leitura humana do seed: as decisões de arquitetura estão separadas das de
   produto, e a `trigger_condition` não manda mais esperar um bloqueio que não
   bloqueia.
4. Leitura humana do todo: lê-se como item de acompanhamento com gatilho claro
   (aprovação de `plano-mcp-servico.md` → revisitar Estratégia C), não como
   trava de planejamento.
5. `git diff --name-only HEAD` = exatamente 3 arquivos de conteúdo, todos sob
   `.planning/`.
</success_criteria>

<output>
Create `.planning/quick/260902-km8-registrar-estrat-gia-de-arquitetura-bori/260902-km8-SUMMARY.md` when done
</output>
