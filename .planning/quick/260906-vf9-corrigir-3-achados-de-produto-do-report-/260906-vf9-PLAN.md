---
phase: quick-260906-vf9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/App.jsx
  - web/src/copy.js
  - web/src/finance.js
  - web/tests/test_c07_modo_operador_nomeia_operador_ia.mjs
  - web/tests/test_c09_drawdown_alerta.mjs
  - web/tests/test_c06_resumo_operacao.mjs
  - .planning/STATE.md
  - .planning/PROJECT.md
autonomous: true
requirements: [C-06, C-07, C-09]
must_haves:
  truths:
    - "Quem lê a tela de Modo de Trabalho sabe que ligar o Modo Operador também habilita o Operador IA (agente que pode vender sozinho)"
    - "Com drawdown acima de 15%, o card de patrimônio mostra um aviso não-bloqueante com sugestão de ação"
    - "Com drawdown de 15% ou menos, nenhum aviso de drawdown aparece"
    - "Cada operação EXECUTADA do histórico traz uma frase em português simples descrevendo o que aconteceu"
    - "Operação rejeitada continua com a linha 'Rejeitada: ...' e NÃO ganha frase de resumo"
    - "Suíte canônica (pytest + web/tests) verde e `npx vite build` limpo em web/"
  artifacts:
    - path: "web/src/finance.js"
      provides: "resumoOperacao(h) — função pura, determinística, sem inventar dado"
      contains: "export function resumoOperacao"
    - path: "web/src/copy.js"
      provides: "drawdownAlertaTitulo/drawdownAlertaCorpo nos DOIS modos"
      contains: "drawdownAlertaCorpo"
    - path: "web/src/App.jsx"
      provides: "LIMIAR_DRAWDOWN_ALERTA, card de aviso em CapitalCurve, frase de resumo em HistoricoScreen, menção a Operador IA em ModoTrabalhoCard"
      contains: "LIMIAR_DRAWDOWN_ALERTA"
    - path: "web/tests/test_c06_resumo_operacao.mjs"
      provides: "guardião de C-06"
    - path: "web/tests/test_c07_modo_operador_nomeia_operador_ia.mjs"
      provides: "guardião de C-07"
    - path: "web/tests/test_c09_drawdown_alerta.mjs"
      provides: "guardião de C-09"
  key_links:
    - from: "web/src/App.jsx (HistoricoScreen)"
      to: "web/src/finance.js (resumoOperacao)"
      via: "import nomeado na linha 15"
      pattern: "resumoOperacao"
    - from: "web/src/App.jsx (CapitalCurve)"
      to: "web/src/copy.js (cp.drawdownAlertaCorpo)"
      via: "ctx.cp já desestruturado em CapitalCurve"
      pattern: "cp\\.drawdownAlertaCorpo"
---

<objective>
Fechar 3 achados de PRODUTO do REPORT-01 (C-06, C-07, C-09) com escopo e
limiares já decididos pelo orquestrador — o executor implementa, não reabre
decisão.

Purpose: os 3 são gaps de comunicação com o usuário (link causal entre modos,
drawdown sem sugestão de ação, log técnico sem tradução), não bugs de motor.
Nenhum toca cálculo financeiro nem contrato de API.

Output: 1 frase nova em `ModoTrabalhoCard`, 1 card de aviso em `CapitalCurve`,
1 função pura + 1 linha por operação executada em `HistoricoScreen`, 2 chaves
novas em `copy.js` (nos dois modos), 3 guardiões novos, STATE.md/PROJECT.md
atualizados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/PROJECT.md

Arquivos-alvo (ler o trecho, não o arquivo inteiro — App.jsx tem ~8k linhas):
- `web/src/App.jsx` linhas 270-290 (formatadores + `LIMIAR_CONCENTRACAO`)
- `web/src/App.jsx` linhas 1777-1920 (`CapitalCurve`)
- `web/src/App.jsx` linhas 2205-2230 (`ModoTrabalhoCard`)
- `web/src/App.jsx` linhas 4461-4477 (card de concentração — padrão visual a copiar)
- `web/src/App.jsx` linhas 4762-4790 (`.map` do histórico)
- `web/src/copy.js` linhas 155-164 (bloco Estudo) e 371-378 (bloco Operador)
- `web/src/finance.js` linhas 133-163 (`concentracaoMaxima`, vizinhança da função nova)
- `web/tests/test_concentracao_carteira.mjs` (formato de guardião estático a espelhar)
</context>

<interfaces>
<!-- Contratos já verificados no código em 2026-09-06. O executor NÃO precisa explorar. -->

web/src/App.jsx:15 — import atual de finance.js (linha única, adicionar aqui):
`import { portfolioMetrics, dayReturnPct, equityCurve, markPrice, sizingPlano, RR_MIN_TXT, historicoEstado, historicoDesatualizado, benchmarkSerie, concentracaoMaxima, qtyLivre } from "./finance.js";`

web/src/App.jsx:276-278 — formatadores (MÓDULO-LOCAL de App.jsx, NÃO exportados,
NÃO existem em finance.js):
`const nf2 = new Intl.NumberFormat("pt-BR", { minimumFractionDigits: 2, maximumFractionDigits: 2 });`
`const price = (n) => (n == null || isNaN(n) ? "—" : nf2.format(n));`

web/src/App.jsx:284 — `const LIMIAR_CONCENTRACAO = 50;`

web/src/App.jsx:1783 — `CapitalCurve` já desestrutura `cp`:
`const { data, quotes, cp } = ctx;`
web/src/App.jsx:1798 — `const dd = ec.drawdown;` (percentual, já pronto)
web/src/App.jsx:1896-1908 — ramo `hasSeries ? (<> ... </>)`: grid de stats (1898-1903)
seguido do bloco condicional de `ibovErro` (1904-1908). O card novo entra DEPOIS
desse bloco de ibovErro, ainda dentro do fragmento `<>`.

web/src/App.jsx:4466-4474 — gramática visual do card de aviso (copiar):
`style={{ padding: "13px 14px", borderRadius: "11px", background: "color-mix(in srgb, " + T.warn + " 12%, transparent)", border: \`1px solid ${T.warn}\`, marginBottom: "14px" }}`
ícone: `<svg width="15" height="15" viewBox="0 0 24 24" aria-hidden><circle cx="12" cy="12" r="9.5" fill="none" stroke={T.warn} strokeWidth="1.8" /><path d="M12 11v5M12 7.5h.01" stroke={T.warn} strokeWidth="2" strokeLinecap="round" /></svg>`
kicker: `fontSize: "11px", fontWeight: 800, color: T.warn, letterSpacing: "0.05em"`
corpo: `fontSize: "12.5px", color: T.textPrimary, lineHeight: 1.5`

web/src/App.jsx:4766 e 4783-4787 — histórico:
`const rejeitada = h.status === "rejeitada";`
`{rejeitada && h.motivo && (<div style={{ padding: "0 15px 9px", fontSize: "11px", color: T.warn, lineHeight: 1.4 }}>Rejeitada: {h.motivo}</div>)}`

web/src/copy.js — `COPY.estudo` / `COPY.operador`, par de referência:
`concentracaoTitulo: "Concentração alta"` + `concentracaoCorpo: (ticker, pct) => \`...\``

Rótulo canônico da aba, verbatim (App.jsx:965): `"Operador IA"`
</interfaces>

<guardioes_existentes_a_respeitar>
Verificado em 2026-09-06 — nenhum destes precisa ser reescrito se o plano for
seguido, mas TODOS rodam na suíte e podem quebrar se o executor desviar:

1. `web/tests/test_copy_theme.mjs:27-31` — exige que `Object.keys(COPY.estudo)`
   e `Object.keys(COPY.operador)` sejam IDÊNTICOS. As 2 chaves novas entram nos
   DOIS blocos, obrigatoriamente. Esse teste também chama TODA função de copy
   com `v("X","Y","Z")` e `v(null,0,0)` — `drawdownAlertaCorpo` não pode lançar
   com esses argumentos — e faz grep case-insensitive por
   `/registrar entrada|registrar saída|execute a saída|COMPRAR|VENDER/i` no
   ramo ESTUDO: o corpo do modo Estudo NÃO pode conter "comprar"/"vender" em
   nenhuma caixa.
2. `web/tests/test_benchmark_curva.mjs:62-64` — isola por regex
   `<div[^>]*>\s*Comparação com o Ibovespa indisponível agora\.` e exige
   `T.textFaint` sem `T.negative` nesse bloco. Por isso o card novo entra
   DEPOIS do bloco de `ibovErro`, nunca antes nem entre a `<div>` e a frase.
3. `web/tests/test_chart_colors_theme_aware.mjs:48-54` — em `CapitalCurve`,
   proíbe `fill={T.` e `stroke={T.` (atributo SVG cru). O ícone de alerta novo
   usa `stroke={T.warn}` em SVG — **use `P.warn`** (a função já tem
   `const P = usePalette();` na linha 1781) OU mantenha o SVG fora do padrão
   proibido. Decisão: usar `stroke={P.warn}` no SVG e `T.warn` nos estilos CSS
   (`style={{...}}`), que é exatamente a disciplina que o guardião codifica.
4. `web/tests/test_fase21_dedup_consolidacao.mjs:145-165` — isola `CapitalCurve`
   de `indexOf("function CapitalCurve(")` até o próximo `\nfunction ` e exige
   `ec.days >= 3` e `const { data, quotes, cp } = ctx;`. Não mexer nessas duas
   linhas.
5. `web/tests/test_concentracao_carteira.mjs:40` — exige que a string
   `CONCENTRAÇÃO ALTA` apareça EXATAMENTE 1x em App.jsx. O kicker do card novo
   precisa de texto diferente (ex.: `DRAWDOWN ALTO`).
6. `web/tests/test_prontidao_operador.mjs:51` — exige verbatim a frase do nudge
   `"O Modo Operador libera decisões diretas — mas ele parte do que você já
   entende. Vale a pena estudar pelo menos um ativo antes de ativar."`
   (App.jsx:2215). NÃO é a linha do C-07 (essa é a 2225). Não tocar na 2215.

Se algum destes quebrar, a regra do CLAUDE.md vale: guardião não se apaga —
atualiza com nota datada explicando a reversão deliberada.
</guardioes_existentes_a_respeitar>

<tasks>

<task type="auto">
  <name>Task 1: C-07 — nomear "Operador IA" na tela de Modo de Trabalho</name>
  <files>web/src/App.jsx, web/tests/test_c07_modo_operador_nomeia_operador_ia.mjs</files>
  <action>
Ler `web/src/App.jsx` linhas 2205-2230 e confirmar a estrutura de
`ModoTrabalhoCard` (o ternário `mode === "operador" ? <>...</> : <>...</>` no
bloco de descrição, linhas 2222-2226).

Acrescentar UMA frase à descrição existente que nomeie explicitamente a aba
`Operador IA` e diga o que ela faz de diferente — que o agente pode vender
sozinho conforme as regras configuradas. Direção (adaptar ao texto real, não
copiar literal se não encaixar): "Inclui a aba Operador IA — o agente que pode
vender sozinho conforme as regras que você configurar."

Regras de escopo:
- A frase precisa aparecer nos DOIS ramos do ternário OU num trecho comum aos
  dois — quem está em Estudo é justamente quem precisa saber o que vai ligar,
  então o ramo `estudo` é obrigatório; o ramo `operador` é desejável para
  simetria. Escolha a forma mais limpa no texto real e registre a escolha no
  guardião.
- NÃO reescrever o resto do texto da tela. Só adicionar.
- NÃO tocar em `web/src/App.jsx:4908-4915` (`AgenteScreen`) — o sentido inverso
  do link já está correto lá.
- NÃO tocar na linha 2215 (frase de nudge travada por
  `test_prontidao_operador.mjs`).
- Texto inline mesmo, seguindo o padrão já existente desse bloco (essa
  descrição não vem de `cp.*` hoje; não migrar agora — fora de escopo).
- Tom direto, sem promessa de resultado, sem vocabulário de ordem de operação
  além do necessário para descrever o agente.

Criar `web/tests/test_c07_modo_operador_nomeia_operador_ia.mjs` no formato
estático de `test_concentracao_carteira.mjs` (readFileSync de App.jsx + helper
`ok`), verificando:
- isolar o corpo de `ModoTrabalhoCard` (`indexOf("function ModoTrabalhoCard(")`
  até `indexOf("function TermoOperadorModal(")`) e assertar que o recorte não
  veio vazio;
- o recorte contém a string `Operador IA`;
- o recorte contém a frase nova (verbatim, o texto que de fato foi escrito);
- a frase nova aparece no ramo lido por quem está em Estudo (assertar que ela
  está no recorte E que o recorte segue contendo o texto original
  `"Carteira simulada e leitura didática"`, provando que a adição foi aditiva);
- a frase de nudge da linha 2215 continua verbatim no arquivo (não-regressão do
  guardião vizinho).
  </action>
  <verify>
    <automated>node web/tests/test_c07_modo_operador_nomeia_operador_ia.mjs &amp;&amp; node web/tests/test_prontidao_operador.mjs</automated>
  </verify>
  <done>A tela de Modo de Trabalho nomeia "Operador IA" e explica que ele pode vender sozinho; guardião novo verde; `test_prontidao_operador.mjs` segue verde.</done>
</task>

<task type="auto">
  <name>Task 2: C-09 — aviso de drawdown acima de 15% no card de patrimônio</name>
  <files>web/src/copy.js, web/src/App.jsx, web/tests/test_c09_drawdown_alerta.mjs</files>
  <action>
Limiar decidido pelo orquestrador: **15%**. Não reabrir.

(a) `web/src/copy.js` — adicionar, nos DOIS blocos (Estudo, junto às linhas
155-164; Operador, junto às linhas 371-378), logo depois do trio
`concentracao*`, com comentário citando "C-09 (REPORT-01)" e o limiar
`LIMIAR_DRAWDOWN_ALERTA` em App.jsx:
- `drawdownAlertaTitulo: "Drawdown alto"` — IDÊNTICO nos dois modos (é rótulo,
  não voz; mesma disciplina de `concentracaoTitulo`);
- `drawdownAlertaCorpo: (pct) => ...` — corpo forka por modo.
  Estudo, tom explicativo/didático, algo como: "Sua carteira já caiu {pct}%
  desde o pico. Quedas grandes pedem mais cautela: considere reduzir o tamanho
  das próximas posições até recuperar confiança no plano."
  Operador, tom de mesa/direto, adaptado ao estilo dos outros pares do arquivo
  (ver `concentracaoCorpo` do bloco operador como referência de voz).

  Restrição dura do guardião `test_copy_theme.mjs`: o corpo do modo ESTUDO não
  pode conter "comprar"/"vender" em nenhuma caixa, e a função não pode lançar
  quando chamada como `v("X","Y","Z")` ou `v(null,0,0)`.

(b) `web/src/App.jsx` — junto de `LIMIAR_CONCENTRACAO` (linha 284), adicionar
`const LIMIAR_DRAWDOWN_ALERTA = 15;` com comentário no mesmo estilo (constante
nomeada, não número mágico; citar C-09/REPORT-01 e "aviso educacional, não
bloqueio").

(c) `web/src/App.jsx` — dentro de `CapitalCurve`, no ramo `hasSeries`, DEPOIS do
bloco condicional de `ibovErro` (linhas 1904-1908) e ainda dentro do fragmento
`<>`, adicionar:
`{dd > LIMIAR_DRAWDOWN_ALERTA && ( ...card... )}`
O card replica a gramática do card de concentração (ver `<interfaces>`):
container com `background: "color-mix(in srgb, " + T.warn + " 12%, transparent)"`
e `border: \`1px solid ${T.warn}\``, linha de cabeçalho com o SVG de alerta +
kicker em `T.warn`, e o corpo em `T.textPrimary` lendo
`cp.drawdownAlertaCorpo(Math.round(dd))`.

Diferenças obrigatórias em relação ao card de concentração:
- kicker com texto PRÓPRIO (ex.: `DRAWDOWN ALTO`) — a string
  `CONCENTRAÇÃO ALTA` tem que continuar aparecendo exatamente 1x em App.jsx;
- SEM link "saiba mais" / `abrirVerbete` — não existe verbete de "drawdown" e
  criá-lo está FORA DE ESCOPO desta task;
- SVG usa `stroke={P.warn}` (não `T.warn`), por causa de
  `test_chart_colors_theme_aware.mjs`; `P` já existe na linha 1781;
- espaçamento adaptado ao contexto (`marginTop`, não `marginBottom` — o card
  fica no fim do bloco de stats, não antes de outra seção).
- O texto vem SÓ de `cp.*`. Nada de string literal de conteúdo no JSX (o kicker
  é rótulo estrutural, pode ficar inline, mesma escolha do card de
  concentração).
- Passar `Math.round(dd)` (mesmo padrão do `pctArred` do card de concentração);
  o stat acima segue com `dd.toFixed(1)`, intocado.
- Aviso NÃO-BLOQUEANTE: nenhum `disabled=` novo, nenhuma alteração no que já
  é renderizado.

(d) Criar `web/tests/test_c09_drawdown_alerta.mjs`, formato estático espelhando
`test_concentracao_carteira.mjs`, verificando:
- `const LIMIAR_DRAWDOWN_ALERTA = 15;` declarado e usado (≥2 ocorrências);
- condição de render é `dd > LIMIAR_DRAWDOWN_ALERTA` (15% exatos NÃO dispara —
  provar por grep da condição estrita, mais os dois casos abaixo em unidade);
- isolando o corpo de `CapitalCurve` (`indexOf("function CapitalCurve(")` até o
  próximo `\nfunction `): o recorte contém o bloco novo, usa `T.warn`, e NÃO
  usa `T.negative` dentro do bloco do card;
- o card NÃO chama `abrirVerbete`;
- `CONCENTRAÇÃO ALTA` segue com exatamente 1 ocorrência em App.jsx;
- `CapitalCurve` não introduz nenhum `disabled=`;
- as 2 chaves novas existem nos DOIS modos de `COPY`;
- `drawdownAlertaTitulo` é idêntico nos dois modos;
- `drawdownAlertaCorpo(23)` de cada modo devolve string contendo `23`
  (verbatim byte a byte contra o texto escrito, mesma disciplina do guardião de
  concentração);
- corpo do modo Estudo não usa vocabulário de ordem de operação
  (`!/comprar|vender/i`);
- unidade da CONDIÇÃO: importar `equityCurve` de `../src/finance.js`, montar
  snapshots que produzam drawdown > 15 e drawdown <= 15, e assertar
  `ec.drawdown > 15` / `ec.drawdown <= 15` respectivamente — prova o gate no
  dado, já que o render em si é checado por grep.
  </action>
  <verify>
    <automated>node web/tests/test_c09_drawdown_alerta.mjs &amp;&amp; node web/tests/test_copy_theme.mjs &amp;&amp; node web/tests/test_concentracao_carteira.mjs &amp;&amp; node web/tests/test_benchmark_curva.mjs &amp;&amp; node web/tests/test_chart_colors_theme_aware.mjs &amp;&amp; node web/tests/test_fase21_dedup_consolidacao.mjs</automated>
  </verify>
  <done>Com `dd > 15` o card de patrimônio mostra aviso não-bloqueante com título+corpo vindos de `cp.*`; com `dd <= 15` nada aparece; guardião novo verde e os 5 guardiões vizinhos de `CapitalCurve`/copy seguem verdes.</done>
</task>

<task type="auto">
  <name>Task 3: C-06 — frase em linguagem simples por operação executada no Histórico</name>
  <files>web/src/finance.js, web/src/App.jsx, web/tests/test_c06_resumo_operacao.mjs</files>
  <action>
Escopo REDUZIDO por decisão do orquestrador: uma frase por operação executada
dentro do `HistoricoScreen` já existente. NÃO criar tela, aba, artefato ou
endpoint novo. Não reabrir.

(a) `web/src/finance.js` — criar função pura exportada `resumoOperacao(h)`,
posicionada perto de `concentracaoMaxima` (linhas 133-156), com comentário
citando "C-06 (REPORT-01), escopo reduzido" e a regra "não inventa dado que não
esteja em `h`" (CLAUDE.md princípio 4/5).

`finance.js` NÃO tem formatador de moeda (os de App.jsx — `nf2`/`price` —
são módulo-locais e não exportados; importar de App.jsx criaria ciclo). Definir
um `Intl.NumberFormat("pt-BR", { minimumFractionDigits: 2, maximumFractionDigits: 2 })`
módulo-local em finance.js, com comentário explicando por que a duplicação é
deliberada (função pura não pode depender da camada de UI). Confirmado que o
Node do repo formata `32.5` → `"32,50"` e `1234.5` → `"1.234,50"`.

Contrato de `resumoOperacao(h)` — devolve `string` ou `null`:
- `h.status === "rejeitada"` → retorna `null` (a linha "Rejeitada: ..." já
  cobre esse caso; nunca duplicar). Entrada legada sem `status` conta como
  executada (mesma regra de App.jsx:4766: a condição é SEMPRE
  `=== "rejeitada"`, nunca `!== "executada"`).
- COMPRA executada → `"Comprou {qty} {ticker} a R$ {price}."`
- VENDA executada com `h.pnl` não-nulo →
  `"Vendeu {qty} {ticker} a R$ {price}"` + sufixo de motivo (`" no stop"` para
  `'stop'`, `" no alvo"` para `'alvo'`, `" no vencimento"` para
  `'vencimento'`; nada para `'manual'`, `undefined` ou valor desconhecido) +
  `", com lucro de R$ {abs(pnl)}."` se `pnl >= 0`, `", com prejuízo de
  R$ {abs(pnl)}."` se `pnl < 0`.
- VENDA executada com `h.pnl` nulo (defensivo — o motor não deveria produzir)
  → `"Vendeu {qty} {ticker} a R$ {price}."` com o sufixo de motivo, sem a parte
  de resultado.
- `h` ausente/inválido, ou `h.price` nulo/NaN, ou `h.qty` ausente → retorna
  `null` (não fabricar "—" dentro de uma frase em prosa; sem dado, sem frase).
- `h.type` diferente de `"COMPRA"`/`"VENDA"` → retorna `null` (guard clause,
  padrão do arquivo).

(b) `web/src/App.jsx` — adicionar `resumoOperacao` ao import da linha 15 (mesma
linha, mesma lista nomeada).

(c) `web/src/App.jsx` — no `.map` de `data.history` (linhas 4762-4790), DEPOIS
do bloco `{rejeitada && h.motivo && (...)}`, adicionar bloco mutuamente
exclusivo para executadas:
`{!rejeitada && resumoOperacao(h) && (<div style={{ padding: "0 15px 9px", fontSize: "11px", color: T.textMuted, lineHeight: 1.4 }}>{resumoOperacao(h)}</div>)}`
(ou com a chamada memoizada numa const dentro do callback do `.map`, se ficar
mais limpo — o executor escolhe, desde que não chame a função duas vezes por
render sem necessidade).
Cor `T.textMuted`, NÃO `T.warn` — isto é frase pedagógica, não aviso.
Comentário citando C-06 e o motivo do escopo reduzido.

(d) Criar `web/tests/test_c06_resumo_operacao.mjs` com as DUAS metades:
- unidade (importando `resumoOperacao` de `../src/finance.js`), no mínimo:
  COMPRA executada; VENDA executada com lucro; VENDA executada com prejuízo E
  `motivo: "stop"`; VENDA com `motivo: "alvo"`; entrada rejeitada → `null`;
  entrada legada sem `status` → frase gerada; `price` nulo → `null`. Assertar
  as strings VERBATIM (byte a byte), incluindo a formatação pt-BR com vírgula
  decimal;
- estático (readFileSync de App.jsx): `resumoOperacao` está no import da
  linha de `./finance.js`; o bloco novo está dentro de `HistoricoScreen`
  (isolar de `indexOf("function HistoricoScreen(")` até o próximo
  `\nfunction `); o bloco usa `T.textMuted` e NÃO usa `T.warn`; o bloco é
  condicionado a `!rejeitada`; a linha `Rejeitada: {h.motivo}` continua
  presente e intocada.
  </action>
  <verify>
    <automated>node web/tests/test_c06_resumo_operacao.mjs &amp;&amp; node web/tests/test_finance.mjs &amp;&amp; node web/tests/test_historico_ui.mjs &amp;&amp; node web/tests/test_ordens_pendentes_ui.mjs</automated>
  </verify>
  <done>Cada operação executada do histórico mostra uma frase determinística em português simples; rejeitadas continuam só com "Rejeitada: ..."; `resumoOperacao` é pura e testada em 7 casos.</done>
</task>

<task type="auto">
  <name>Task 4: Validação canônica, build do front e atualização de STATE/PROJECT</name>
  <files>.planning/STATE.md, .planning/PROJECT.md</files>
  <action>
(a) Rodar a suíte canônica COMPLETA da raiz do repo:
`bash scripts/executar.sh --testes` — as DUAS suítes (pytest do backend +
`web/tests/*.mjs`, glob auto-descoberto, então os 3 guardiões novos entram
sozinhos). Exigir código de saída 0. `scripts/test.sh` sozinho não conta.

(b) `web/src/App.jsx` foi editado → rodar `npx vite build` dentro de `web/` e
confirmar build limpo (regra do CLAUDE.md: grep e teste estático não pegam erro
de sintaxe JSX).

(c) Se algum guardião pré-existente quebrar: NUNCA apagar. Atualizar o guardião
com nota datada (2026-09-06) explicando a reversão deliberada e o achado que a
motivou. Se a quebra indicar erro de implementação (e não decisão nova),
corrigir a implementação, não o guardião.

(d) Só depois da suíte verde, `.planning/STATE.md` — na tabela "Deferred
Items", a linha de Backlog hoje diz "6 achados Baixo do REPORT-01 (C-06, C-07,
C-09, C-10, C-17, C-29) ...". Reescrever para refletir que C-06, C-07 e C-09
saíram do backlog nesta quick task (260906-vf9), preservando a menção às
correções anteriores (C-08/C-18 em 260906-ugb, C-28 reverificado), com nota
explícita de (i) escopo reduzido do C-06 — frase no Histórico existente, não
tela nova — e (ii) limiar de 15% decidido para o C-09. Deixar claro que restam
C-10, C-17 e C-29 sem fase mapeada. Adicionar também a linha desta task na
tabela "Quick Tasks Completed".

(e) `.planning/PROJECT.md` — mover a menção aos 3 achados para `### Validated`,
citando o que mudou em cada um, o limiar de 15% do C-09 e o escopo reduzido do
C-06. Adicionar UMA linha nova em `### Active` (ou seção de backlog
equivalente) registrando o achado colateral: "CLAUDE.md exige 'drawdown' como
conceito didático obrigatório (lista de conceitos da camada educacional), mas
não existe nenhum verbete para ele em conceitos.py/kb.py — só 'diversificação'
tem link 'saiba mais' funcional hoje. Não corrigido nesta task (mecanismo de
conceitos.py é maior que o de kb.py/_SETUPS, fora de escopo)."

FORA DE ESCOPO, não fazer: `scripts/bump.sh`, `scripts/publicar-web.sh`,
qualquer deploy; editar `.planning/milestones/v1.1-REQUIREMENTS.md` (arquivado);
criar verbete de "drawdown" em `conceitos.py`/`kb.py`; tocar no card de
drawdown de `EficienciaIAScreen` (App.jsx:6013-6017, R-múltiplo, métrica
diferente).
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes &amp;&amp; (cd web &amp;&amp; npx vite build) &amp;&amp; grep -q "260906-vf9" .planning/STATE.md &amp;&amp; grep -qi "drawdown" .planning/PROJECT.md</automated>
  </verify>
  <done>Suíte canônica com exit 0, `npx vite build` limpo, STATE.md sem C-06/C-07/C-09 no backlog e com a quick task registrada, PROJECT.md com os 3 em Validated + o achado colateral do verbete de drawdown em Active.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| motor determinístico → UI | `h` (histórico) e `ec.drawdown` já são produzidos pelo motor; a UI só formata |
| copy.js → tela | texto estático versionado, sem entrada de usuário |

Nenhuma fronteira nova é criada: zero rota HTTP, zero entrada de terceiro,
zero pacote novo (npm/pip), zero mudança de contrato de API.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-vf9-01 | Tampering | `resumoOperacao(h)` em finance.js | mitigate | Função pura, sem I/O e sem estado de módulo; retorna `null` quando falta dado em vez de fabricar valor (CLAUDE.md princípio 4). Guardião unitário trava as strings byte a byte. |
| T-vf9-02 | Information disclosure | frase de resumo no Histórico | accept | Só reexibe dado que a mesma linha já mostra em colunas (qty/price/pnl); nenhum campo novo é exposto. |
| T-vf9-03 | Repudiation | card de aviso de drawdown | accept | Aviso não-bloqueante e derivado de `ec.drawdown` já exibido no stat acima; não altera saldo, ordem ou histórico. |
| T-vf9-04 | Elevation of privilege | frase sobre "Operador IA" | accept | Texto informativo; o gate real da troca de modo (`operadorTermo`, `escolher()`) permanece intocado. |
| T-vf9-SC | Tampering | npm/pip/cargo installs | N/A | Nenhuma instalação de pacote nesta task — `npx vite build` usa dependência já presente em `web/package.json`. |
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` → exit 0 (pytest + todos os `web/tests/*.mjs`, incluindo os 3 guardiões novos).
- `cd web && npx vite build` → build limpo.
- Guardiões pré-existentes de maior risco, individualmente verdes: `test_copy_theme.mjs`, `test_concentracao_carteira.mjs`, `test_benchmark_curva.mjs`, `test_chart_colors_theme_aware.mjs`, `test_fase21_dedup_consolidacao.mjs`, `test_prontidao_operador.mjs`, `test_historico_ui.mjs`.
- `git diff --stat` mostra exatamente os arquivos de `files_modified` — nenhum arquivo de `server/`, nenhum `web_dist`.
</verification>

<success_criteria>
- [ ] C-07: `ModoTrabalhoCard` nomeia "Operador IA" e explica que ele pode vender sozinho; `AgenteScreen` (4908-4915) intocado
- [ ] C-09: `LIMIAR_DRAWDOWN_ALERTA = 15` declarado; card de aviso renderiza só com `dd > 15`; texto vindo de `cp.drawdownAlerta*` nos dois modos; sem link de verbete
- [ ] C-06: `resumoOperacao(h)` pura em finance.js, importada e renderizada em `HistoricoScreen` só para executadas, em `T.textMuted`
- [ ] 3 guardiões novos criados; nenhum guardião existente apagado
- [ ] Suíte canônica exit 0 e `npx vite build` limpo
- [ ] STATE.md e PROJECT.md atualizados (3 achados fora do backlog + achado colateral do verbete de drawdown registrado)
- [ ] Nenhum bump/publish; nenhum verbete novo em conceitos.py/kb.py
</success_criteria>

<output>
Create `.planning/quick/260906-vf9-corrigir-3-achados-de-produto-do-report-/260906-vf9-SUMMARY.md` when done
</output>
</content>
</invoke>
