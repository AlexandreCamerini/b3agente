---
phase: quick-260905-1gb
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/App.jsx
  - web/tests/test_historico_cp_escopo.mjs
  - web/src/version.js
  - server/web_dist/
autonomous: false
requirements: [QUICK-260905-1gb]

must_haves:
  truths:
    - "Com >=1 ordem pendente e histórico vazio, Portfólio → Ver histórico de operações renderiza sem cair no ErrorBoundary"
    - "O estado vazio mostra o texto de vocabulário correto do modo ativo (Estudo: 'Suas compras e vendas simuladas aparecerão aqui.'; Operador: 'Suas entradas e saídas registradas aparecerão aqui.')"
    - "Uma regressão futura que reintroduza referência livre a `cp` dentro de HistoricoScreen quebra a suíte canônica"
  artifacts:
    - path: "web/src/App.jsx"
      provides: "HistoricoScreen sem variável livre `cp`"
      contains: "ctx.cp.vazioHistorico"
    - path: "web/tests/test_historico_cp_escopo.mjs"
      provides: "Guardião: nenhuma referência livre a `cp` em HistoricoScreen"
      min_lines: 25
  key_links:
    - from: "web/src/App.jsx (HistoricoScreen)"
      to: "web/src/copy.js (COPY.*.vazioHistorico)"
      via: "ctx.cp injetado pelo ctx do App"
      pattern: "ctx\\.cp\\.vazioHistorico"
    - from: "web/tests/test_historico_cp_escopo.mjs"
      to: "web/src/App.jsx"
      via: "leitura estática do bloco HistoricoScreen"
      pattern: "function HistoricoScreen\\("
---

<objective>
Corrigir o `ReferenceError: cp is not defined` que derruba a tela de histórico
no ErrorBoundary global ("Algo saiu do lugar") quando existe pelo menos 1 ordem
pendente e nenhuma operação executada — o estado real de quem compra com o
mercado fechado.

Purpose: é o caminho normal do usuário novo (primeira compra fora do pregão →
abrir histórico). Hoje ele não vê a ordem pendente que acabou de criar; vê uma
tela de erro. Fere o princípio 9 (estados completos) do CLAUDE.md.

Output: uma linha corrigida em `web/src/App.jsx`, um guardião de teste que
impede a regressão, e a correção publicada em `server/web_dist` com carimbo
novo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

Arquivos-fonte relevantes:
@web/src/App.jsx  (bloco `function HistoricoScreen({ ctx })`, linha ~4539)
@web/src/copy.js  (chaves `vazioHistorico`, linhas 72 e 292)
@web/tests/test_historico_rejeitada.mjs  (padrão de guardião estático a copiar)

<diagnostico_ja_feito>
NÃO re-diagnosticar. A causa foi localizada por leitura de código e confirmada
por reprodução manual:

- `web/src/App.jsx:4617` usa a expressão livre `cp.vazioHistorico` dentro de
  `function HistoricoScreen({ ctx })`.
- A linha 4540 desestrutura só `const { data, A } = ctx;` — `cp` NUNCA é
  desestruturado nesse componente.
- Não existe binding de `cp` em escopo de módulo em `App.jsx`. Toda outra
  ocorrência de `cp` no arquivo é (a) parâmetro/prop desestruturada, ou (b)
  `ctx.cp`. Confirmado: `const cp = copyFor(...)` só existe DENTRO de outras
  funções (linhas 6546 e 7659), nunca no topo do módulo.
- No MESMO componente a referência correta já é usada: linha 4577,
  `ctx.cp.ordemPendentePill`.
- O bug só dispara no branch `data.history.length === 0` (linhas 4614-4618),
  único lugar do componente que toca a variável livre. Por isso passa
  despercebido em contas com histórico.

Repro confirmada: criar ordem de compra fora do horário de pregão (fica
"pendente") → Portfólio → "Ver histórico de operações" → ErrorBoundary.
</diagnostico_ja_feito>

<interfaces>
Contrato de vocabulário já existente (nada a criar):

web/src/copy.js
  COPY.estudo.vazioHistorico   = "Suas compras e vendas simuladas aparecerão aqui."
  COPY.operador.vazioHistorico = "Suas entradas e saídas registradas aparecerão aqui."

O `ctx` recebido por HistoricoScreen já carrega `ctx.cp` (resultado de
`copyFor(appMode)`) — é o mesmo objeto que a linha 4577 consome hoje sem
problema. Nenhuma mudança de contrato: o fix é de escopo léxico, não de dado.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guardião de escopo + fix de uma linha em HistoricoScreen</name>
  <files>web/tests/test_historico_cp_escopo.mjs, web/src/App.jsx</files>
  <behavior>
    Guardião novo `web/tests/test_historico_cp_escopo.mjs` (roda sem build, é
    varrido automaticamente pelo glob `web/tests/*.mjs` do
    `scripts/executar.sh`). Contrato a asseverar, sobre o SLICE do arquivo
    entre `function HistoricoScreen(` e a próxima declaração `function ` de
    topo (mesmo recorte usado por `test_historico_rejeitada.mjs`, que fatia até
    `function BuyModal(`):

    - Teste 1 (RED antes do fix): o slice de HistoricoScreen NÃO contém
      nenhuma referência livre a `cp` — isto é, toda ocorrência de `cp.` no
      slice é precedida por `.` (forma `ctx.cp.`) ou faz parte de um
      identificador maior. Regex sugerida sobre o slice já SEM comentários:
      `/(^|[^.\w])cp\s*\./` deve NÃO casar.
    - Teste 2: o slice contém literalmente `ctx.cp.vazioHistorico`.
    - Teste 3 (sanidade do recorte, evita guardião vazio que sempre passa): o
      slice foi localizado (comprimento > 0) e contém `ctx.cp.ordemPendentePill`
      (referência correta pré-existente, prova que o recorte pegou o bloco
      certo).

    Higiene obrigatória do guardião: ANTES de aplicar a regex do Teste 1,
    remova do slice as linhas de comentário (`//`) e os blocos `/* */` — o
    próprio bloco tem comentários e um comentário futuro citando `cp.algo`
    invalidaria o teste sem que o código estivesse errado. O cabeçalho do
    arquivo de teste também não deve ser varrido (o teste lê `App.jsx`, nunca
    a si mesmo).
  </behavior>
  <action>
    Passo 1 (RED): criar `web/tests/test_historico_cp_escopo.mjs` copiando a
    forma de `web/tests/test_historico_rejeitada.mjs` — `readFileSync` de
    `../src/App.jsx`, helper `ok(name, cond)`, contador `fails`, `process.exit`
    diferente de zero quando `fails > 0`. Cabeçalho em comentário explicando
    POR QUE o guardião existe: `cp` não é binding de módulo em `App.jsx`, então
    referência livre dentro de um componente que não desestrutura `cp` é
    ReferenceError em runtime, e só aparece no branch de estado vazio (achado
    de 2026-09-05, tela de histórico com ordem pendente e zero operações).
    Rodar `node web/tests/test_historico_cp_escopo.mjs` e CONFIRMAR que o
    Teste 1 falha — se passar de primeira, o recorte do slice está errado e o
    guardião é inútil; ajustar o recorte antes de seguir.

    Passo 2 (GREEN): em `web/src/App.jsx`, linha 4617, trocar
    `{cp.vazioHistorico}` por `{ctx.cp.vazioHistorico}`. Somente isso. NÃO
    alterar a desestruturação da linha 4540, NÃO reescrever o componente, NÃO
    tocar em nenhum outro trecho do arquivo — o restante do bloco já usa a
    forma `ctx.cp.` (linha 4577) e fica consistente. Rodar o guardião de novo e
    confirmar verde.

    Passo 3: validação canônica do repo. `bash scripts/executar.sh --testes`
    (as DUAS suítes — pytest do backend + `web/tests/*.mjs`; `scripts/test.sh`
    sozinho não conta) e, porque o front foi editado, `npx vite build` dentro
    de `web/` — grep e teste estático não pegam erro de sintaxe JS.
  </action>
  <verify>
    <automated>node web/tests/test_historico_cp_escopo.mjs && bash scripts/executar.sh --testes && (cd web && npx vite build)</automated>
  </verify>
  <done>
    `web/tests/test_historico_cp_escopo.mjs` existe, falhava antes do fix e
    passa depois; `web/src/App.jsx:4617` usa `ctx.cp.vazioHistorico`; a suíte
    canônica está verde (contagem de pytest igual ou maior que a última
    registrada em STATE.md — 2021 passed, 1 skipped — e web ok); `npx vite
    build` conclui sem erro. Nenhum outro arquivo de `web/src/` foi alterado.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Publicar carimbo novo e reproduzir o cenário exato ao vivo</name>
  <what-built>
    Antes do checkpoint, executar (automatizado, nesta ordem):
    1. `bash scripts/bump.sh` — carimbo novo em `web/src/version.js`
       (guardrail: bump SEMPRE antes de publicar; esquecer publica dist novo
       com carimbo velho, assinatura de bundle amputado que o
       `test_ios_assets.mjs` pega).
    2. `bash scripts/publicar-web.sh` — builda e publica em `server/web_dist`.
       NUNCA editar `server/web_dist` à mão.
    3. `bash scripts/executar.sh --testes` de novo, DEPOIS da publicação —
       os testes de carimbo/assets só fazem sentido com o dist já sincronizado.
    4. Informar ao Alex o BUILD_ID gerado e como subir o app local para o teste.

    NÃO dar `git push` para `origin`. STATE.md registra checkpoints humanos
    pendentes das Fases 17, 18 e 19 nesta mesma branch; o push segue segurado
    até aquela decisão, e este fix não a destrava.
  </what-built>
  <how-to-verify>
    Roteiro de reprodução do bug original (o mesmo que quebrou hoje):

    1. Subir o app local com o build novo (ou instalar no iPhone via
       `scripts/instalar-iphone.sh`, se preferir validar no aparelho).
    2. Confirmar no rodapé/health que o carimbo é o BUILD_ID recém-gerado — se
       for o anterior, o dist não subiu e o teste não vale.
    3. Usar uma conta SEM operações executadas no histórico (conta nova serve;
       conta nova nasce limpa por guardrail).
    4. Com o mercado FECHADO, criar uma ordem de compra de qualquer ativo —
       ela deve ficar "pendente".
    5. Ir em Portfólio → "Ver histórico de operações".

    Esperado agora:
    - A tela ABRE. Nada de "Algo saiu do lugar" (ErrorBoundary), nada de
      `ReferenceError: cp is not defined` no console.
    - A seção "Pendentes" aparece com a ordem criada, pill "PENDENTE" em âmbar,
      e a linha de caixa reservado.
    - Logo abaixo, o estado vazio "Nenhuma operação ainda" com o texto do modo
      ativo: em Modo Estudo, "Suas compras e vendas simuladas aparecerão
      aqui."; em Modo Operador, "Suas entradas e saídas registradas aparecerão
      aqui.". Trocar de modo e reabrir a tela confirma que o texto acompanha o
      modo (é exatamente essa a ligação que estava quebrada).
    - Console do navegador/Safari Web Inspector limpo de erro nessa navegação.

    Contraprova (opcional, 30s): cancelar a ordem pendente e reabrir a tela —
    sem pendentes e sem histórico, o estado vazio segue renderizando sozinho,
    sem erro.
  </how-to-verify>
  <resume-signal>Responda "aprovado" ou descreva o que apareceu de diferente (com o texto exato do erro, se houver)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhum novo) | O fix é escopo léxico dentro de um componente de render; não move dado entre confiança, não lê entrada do usuário, não chama rede |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1gb-01 | Denial of Service (auto-infligido) | `HistoricoScreen` em `web/src/App.jsx` | mitigate | O próprio bug É a negação de serviço: um caminho normal do usuário derruba a tela inteira no ErrorBoundary. Mitigado pelo fix + guardião estático que impede reintrodução |
| T-1gb-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo é instalado. `publicar-web.sh` roda `npm ci` sobre o lockfile já versionado — sem superfície de supply chain nova |
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` verde (backend pytest + `web/tests/*.mjs`), rodado DUAS vezes: depois do fix e depois da publicação.
- `npx vite build` em `web/` sem erro.
- `node web/tests/test_historico_cp_escopo.mjs` falha no código pré-fix e passa no pós-fix (comprovar a ordem, não só o estado final).
- Reprodução manual ao vivo do cenário exato (ordem pendente + histórico vazio) sem ErrorBoundary — Task 2.
- `git diff --stat` mostra apenas: `web/src/App.jsx` (1 linha), `web/tests/test_historico_cp_escopo.mjs` (novo), `web/src/version.js` (bump) e a árvore `server/web_dist/`.
</verification>

<success_criteria>
- Portfólio → Ver histórico de operações abre com ordem pendente e zero
  operações executadas, mostrando "Pendentes" + estado vazio, sem crash.
- O texto do estado vazio vem de `ctx.cp` e muda com o modo (Estudo × Operador).
- Guardião no lugar: reintroduzir `cp` livre em `HistoricoScreen` quebra a
  suíte canônica.
- Carimbo novo publicado em `server/web_dist` pelo caminho oficial
  (`bump.sh` → `publicar-web.sh`), com os elos de carimbo coerentes.
- Nenhum push para `origin` (checkpoints humanos das Fases 17/18/19 seguem
  pendentes na branch).
</success_criteria>

<output>
Create `.planning/quick/260905-1gb-corrigir-crash-cp-is-not-defined-em-hist/260905-1gb-SUMMARY.md` when done
</output>

