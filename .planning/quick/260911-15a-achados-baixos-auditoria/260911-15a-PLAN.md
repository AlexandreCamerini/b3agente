---
quick_id: 260911-15a
phase: quick-260911-15a
plan: 01
type: quick
wave: 1
depends_on: []
files_modified: [server/app/main.py, server/app/candle_provider.py, server/app/scan_deep.py, server/app/brapi_budget.py, server/app/technical_snapshot.py, docs/adr/008-fonte-de-cotacoes-selecionavel.md, docs/adr/001-fonte-de-dados-intraday.md, web/tests/test_fase3_paridade_stores_generica.mjs, server/tests/test_fase5_rejeicao_rotas.py, server/tests/test_brapi_budget.py]
autonomous: true
---

<objective>
Fechar os achados de severidade baixa e média-documental da auditoria de
2026-09-10, mais o resto de escopo que a correção do A-00 deixou. Quatro
tasks, agrupadas por natureza. Dois achados ficam FORA porque pedem decisão
do Alex (ver no fim).
</objective>

<tasks>
<task type="auto">
  <name>Task 1 (A-09 + resto do A-00b): rotas de opção param de mentir sucesso</name>
  <files>server/app/main.py, server/tests/test_fase5_rejeicao_rotas.py</files>
  <action>
  **A-09** (`main.py:2346`): `store.sell_option` devolvendo `None` (posição já
  vendida por outro caminho) vira HTTP 200 com `priceUsed` — sucesso falso. A
  rota de ação faz o oposto e levanta 400, e o comentário dela diz que era "a
  única exceção silenciosa". Opções ficou de fora. Espelhar: 400 quando o
  retorno for `None`, com a mensagem no padrão da venda de ação (LEIA qual é e
  reuse; não invente string nova).

  **Resto do A-00b** (deixado fora de propósito na quick 260910-mqs para não
  contaminar aquele commit): `/api/options/buy` tem o mesmo vazamento de
  `ValueError` quando `qty` não é número — `int(body.get("qty") or 0)` derruba
  para 500 com o texto cru da exceção. O `qty <= 0` de lá JÁ barra negativo,
  então só falta o tratamento do não-numérico. Espelhar o padrão que a quick
  260910-mqs pôs em `/api/buy`.

  Testes em `test_fase5_rejeicao_rotas.py`, no padrão dos que já existem lá
  (tabela do docstring atualizada com nota da auditoria).
  </action>
  <verify>pytest test_fase5_rejeicao_rotas.py e os testes de opções (test_opcoes_*); suíte inteira</verify>
  <done>Nenhuma rota de opção devolve 200 para operação que não aconteceu, nem 500 para entrada inválida.</done>
</task>

<task type="auto">
  <name>Task 2 (A-10): fuso nos dois módulos que ficaram fora</name>
  <files>server/app/candle_provider.py, server/app/scan_deep.py</files>
  <action>
  `candle_provider.py:61` usa `time.localtime()` e `scan_deep.py:29` usa
  `time.strftime` local como chave de "hoje". Produção roda em UTC, então entre
  21:00 e 23:59 BRT o dia vira adiantado — distorce a janela de
  observabilidade (que é o gatilho declarado de troca de fonte, ADR-008) e a
  chave de cache do scan.
  Ficaram fora da correção de fuso de 2026-09-09 (quick 260909-oyu), que já
  estabeleceu o padrão: `BRT = timezone(timedelta(hours=-3))` local ao módulo,
  com comentário de decisão. Aplicar o mesmo, citando a quick anterior.
  Guardião: espelhar `test_store_now_str_brt.py`, que crava o relógio num
  instante onde UTC e BRT estão em DIAS diferentes — é o caso que prova o dia,
  não só a hora.
  </action>
  <verify>pytest dos testes novos e dos que tocam candle_provider/scan_deep</verify>
  <done>Relógio cravado às 23:30 UTC prova que a chave usa o dia de Brasília.</done>
</task>

<task type="auto">
  <name>Task 3 (A-11): trava preventiva no orçamento de cotações</name>
  <files>server/app/brapi_budget.py, server/tests/test_brapi_budget.py</files>
  <action>
  **Este achado está INERTE hoje** — a verificação adversarial da auditoria
  rodou 50 chamadas concorrentes sem nenhuma corrupção, porque não existe
  ponto de espera entre a checagem e o débito nos call sites reais. Só quebra
  com pausa artificial que o código não tem.
  Corrigir é **defesa em profundidade**, não conserto de bug: o módulo irmão
  (`mydata_budget.py`) já resolveu essa classe com `RLock` + `reservar()`
  atômico depois do incidente WR-01, e a assimetria entre os dois é que faz o
  próximo refactor reintroduzir o problema.
  Portar o mesmo padrão: trava + `reservar(fatia, n)` que faz check+debit sob a
  mesma trava, e trocar os pares `pode_gastar`+`debita` separados nos call
  sites de `candle_provider.py`. PRESERVE o comportamento observável: nenhum
  teste existente de orçamento pode mudar de resultado.
  Se ao ler os call sites a troca exigir mudar a semântica de algum (ex.:
  lugar que checa sem debitar de propósito), PARE e reporte em vez de forçar.
  </action>
  <verify>pytest test_brapi_budget.py e os testes de candle_provider — todos os existentes passam SEM alteração</verify>
  <done>Padrão alinhado com o módulo irmão, comportamento idêntico.</done>
</task>

<task type="auto">
  <name>Task 4 (A-14 + A-15 + A-16): documentação que mente sobre o código</name>
  <files>docs/adr/008-fonte-de-cotacoes-selecionavel.md, docs/adr/001-fonte-de-dados-intraday.md, server/app/technical_snapshot.py, web/tests/test_fase3_paridade_stores_generica.mjs</files>
  <action>
  **A-15**: o ADR-008 descreve a fonte master de um jeito que o ADR-020 depois
  mudou, e não aponta para frente. Acrescentar UMA linha de Status no topo
  referenciando o ADR-020. NÃO tocar o corpo — o guardrail de histórico
  protege o texto da decisão; linha de Status é prática normal de ADR.
  **A-16**: `technical_snapshot.py:32` diz que a chave do cache é par quando é
  tripla desde o ADR-001 (o código está certo, o comentário mente). E o
  ADR-001 mantém `[ ]` num item já implementado (o carimbo de barra na
  interface, `TimingBadge`) — marcar `[x]`, que é fechar checklist, não
  reescrever decisão.
  **A-14**: o comentário do guardião de paridade de stores diz "58 métodos"
  quando são 70. Corrigir o número E registrar, no próprio comentário, que o
  guardião cobre paridade de NOME e não de comportamento, nomeando os métodos
  que escrevem estado e não têm teste pontual (a auditoria listou: `putSkill`,
  `putProfile`, `putSnapshot`, `putLlmPrompts`, `optionsBuy`, `optionsSell`,
  `restoreSkill`). NÃO escreva os testes de comportamento nesta task — é
  escopo próprio; o comentário é o rastro honesto até lá.
  </action>
  <verify>node web/tests/test_fase3_paridade_stores_generica.mjs; suíte inteira</verify>
  <done>Nenhum comentário ou ADR afirma o que o código não faz.</done>
</task>
</tasks>

<fora_de_escopo>
Ficam FORA porque mudam comportamento que o Alex precisa decidir:
- **A-12** (`rbac.py:124`): sem `B3_ADMIN_EMAILS`, a 1ª conta num banco vazio
  vira administradora. Falhar-fechado muda o fluxo de bootstrap; já mapeado em
  `qa/46`.
- **D-01** (`options_mcp_api.py`): recusa por rate limit se apresenta como
  "Cota do dia da aba Opções esgotada." com `usado: 0, limite: 60`. Corrigir
  mexe no contrato que a UI lê.
</fora_de_escopo>

<success_criteria>
Commits atômicos por task. Deploy só-backend (bump manual) — NÃO fazer.
Task 4 é só documentação/comentário: nenhum comportamento muda.
</success_criteria>
