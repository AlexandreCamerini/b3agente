---
phase: quick-260901-r5t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/seeds/opcoes-v2-setups-propostos-via-b-mcp.md
  - .planning/notes/opcoes-v2-b-mcp-exploracao.md
autonomous: true
requirements: [QUICK-260901-R5T]

must_haves:
  truths:
    - "Quem ler o seed vê a colocação na navegação como decisão FECHADA (Candidato A, aba própria 'Opções'), não como pendência aguardando o Alex"
    - "Quem ler a nota de exploração vê que a recomendação provisória (Candidato B, dentro de Posições) foi superada por decisão do Alex em 2026-09-01"
    - "O raciocínio original de por que o B tinha sido recomendado continua legível na nota, como contexto histórico"
    - "Os dois trade-offs do Candidato A (barra de 4→5 itens; aba pode abrir vazia sem cobertura na carteira) estão registrados como riscos conscientemente aceitos"
    - "Nenhum arquivo de código foi tocado — o diff é exclusivamente .planning/"
  artifacts:
    - path: ".planning/seeds/opcoes-v2-setups-propostos-via-b-mcp.md"
      provides: "Decisão de navegação fechada (Candidato A) fora da lista de pendências"
      contains: "Candidato A"
    - path: ".planning/notes/opcoes-v2-b-mcp-exploracao.md"
      provides: "Seção de navegação atualizada para decisão fechada, com histórico preservado e trade-offs aceitos"
      contains: "Candidato A"
  key_links:
    - from: ".planning/seeds/opcoes-v2-setups-propostos-via-b-mcp.md"
      to: ".planning/notes/opcoes-v2-b-mcp-exploracao.md"
      via: "referência textual à seção de navegação da nota"
      pattern: "opcoes-v2-b-mcp-exploracao"
---

<objective>
Fechar na documentação de planejamento a decisão do Alex sobre a colocação de
Opções v2 na navegação: **Candidato A — aba própria "Opções" na barra inferior**
(Mesa/Radar, Monitoramento/Watchlist, Posições, Opções [nova], Perfil).

Purpose: o mockup publicado registrou o Candidato B ("dentro de Posições") como
recomendação provisória, explicitamente marcada como "palpite, não veredito" que
pedia validação. O Alex validou e escolheu outro caminho. Enquanto os dois docs
disserem "não decidido / recomendação provisória por B", qualquer
`/gsd-plan-phase` futuro sobre Opções v2 vai planejar a arquitetura de navegação
errada.

Output: dois arquivos de planejamento atualizados. ZERO código.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/seeds/opcoes-v2-setups-propostos-via-b-mcp.md
@.planning/notes/opcoes-v2-b-mcp-exploracao.md
@CLAUDE.md

<facts>
Decisão a registrar (fonte: Alex, 2026-09-01):
- Escolhido: **Candidato A** — aba própria "Opções" na barra de navegação
  inferior. Barra resultante: Mesa/Radar, Monitoramento/Watchlist, Posições,
  Opções (nova), Perfil.
- Preterido: **Candidato B** — contextual dentro de "Posições" (era a
  recomendação provisória da sessão `/gsd-explore` de 2026-09-01).
- Mockup dos 3 candidatos (referência, permanece válida como artefato):
  https://claude.ai/code/artifact/f9a3690f-94f9-46ed-9209-ef934ccaae21

Trade-offs do Candidato A já documentados no mockup como "contra" — NÃO são
achados novos, precisam apenas ficar registrados como riscos conscientemente
aceitos (não ignorados):
  (i) a barra de navegação vai de 4 para 5 itens — aperta no mobile;
  (ii) a aba pode abrir vazia quando não houver cobertura de opção na carteira
       (a feature é estritamente "opções sobre posição que já existe").

Guardrail do repo aplicável (CLAUDE.md): "Histórico não se reescreve". O
raciocínio original pró-B na nota de exploração é registro de sessão — some não;
é emoldurado como contexto histórico superado.
</facts>

<project_skills_check>
Skills do projeto: `didatica-boris` (vocabulário/didática de produto voltado ao
usuário final) e `swiftui-pro` (revisão de código SwiftUI). Nenhuma das duas
incide sobre doc interno de planejamento — sem conflito de regra a acomodar.
</project_skills_check>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fechar a decisão de navegação (Candidato A) nos dois docs de planejamento</name>
  <files>.planning/seeds/opcoes-v2-setups-propostos-via-b-mcp.md, .planning/notes/opcoes-v2-b-mcp-exploracao.md</files>
  <action>
Edite os DOIS arquivos com a ferramenta Edit (nunca heredoc/`cat`). São a mesma
unidade de decisão — os dois têm que sair consistentes.

**Arquivo 1 — `.planning/seeds/opcoes-v2-setups-propostos-via-b-mcp.md`:**

Na seção "Pendências de produto (não técnicas, aguardando o Alex)", o bullet
"Colocação na navegação — 3 candidatos levantados ... pede validação do Alex ..."
deixa de ser pendência. Remova esse bullet da lista de pendências (a lista fica
com 2 itens: escopo do v1 da biblioteca, e plano comercial) e adicione ACIMA
dela uma seção nova "Decisões de produto fechadas" com o registro da escolha:
Candidato A — aba própria "Opções" na barra de navegação inferior, com a barra
resultante nomeada (Mesa/Radar, Monitoramento/Watchlist, Posições, Opções [nova],
Perfil), decidido por Alex em 2026-09-01. Preserve o link do mockup (agora como
referência do comparativo dos 3 candidatos, não como recomendação pendente) e
aponte para a seção de navegação de
`.planning/notes/opcoes-v2-b-mcp-exploracao.md` para o racional completo e os
trade-offs aceitos.

Não altere o frontmatter nem a `trigger_condition` — os bloqueios técnicos do
seed (hub mydata / acesso b-mcp) seguem inalterados por esta decisão.

**Arquivo 2 — `.planning/notes/opcoes-v2-b-mcp-exploracao.md`:**

A seção `## Navegação — recomendação, não decisão fechada` precisa deixar de se
anunciar como aberta. Reescreva o título para algo como
`## Navegação — DECIDIDO: Candidato A (aba própria "Opções")` e reestruture o
corpo em três partes, nesta ordem:

1. **A decisão** (no topo, primeira coisa que se lê): Alex escolheu, em
   2026-09-01, o Candidato A — aba própria "Opções" na barra inferior (nomeie a
   barra resultante de 5 itens). Deixe explícito que isso supera a recomendação
   provisória por (b) que esta mesma sessão havia registrado, e que a
   recomendação estava marcada de origem como palpite pendente de validação —
   ela foi validada e o veredito divergiu.
2. **Riscos conscientemente aceitos**: os dois "contra" do Candidato A já
   documentados no mockup — (i) barra de 4 para 5 itens, aperta no mobile;
   (ii) a aba pode abrir vazia quando não há cobertura de opção na carteira.
   Registre-os como aceitos com conhecimento de causa, não como pendências
   abertas nem como achados novos. Se quiser, anote que mitigá-los (estado vazio
   com propósito pedagógico; densidade da barra em telas pequenas) é trabalho de
   implementação da aba, fora de escopo aqui.
3. **Contexto histórico — por que (b) tinha sido recomendado**: mantenha o
   raciocínio original legível, integralmente em substância (nunca aparece
   vazio porque só existe quando há cobertura real; evita somar mais uma aba a
   um app que já tem Operador + Mesa + Posições; a sessão não tinha acesso à
   aparência real do app nem a critério de gosto de produto). Não apague nada
   desse racional — ele explica por que a recomendação inicial divergiu da
   escolha final. A menção a rodar
   `design-with-claude:navigation-specialist` deixa de ser pendência: ou some da
   lista de ações abertas, ou fica marcada como não mais necessária (a decisão
   humana veio antes).

Não toque no parágrafo da seção "Ideia original" (linhas ~20-23) que diz que a
navegação "ficou deliberadamente em aberto no prompt original" — é registro
fiel do estado daquela sessão, e o guardrail do repo é "histórico não se
reescreve". A seção dedicada abaixo é quem carrega a resolução.

**Escopo estrito:** nenhum arquivo fora de `.planning/`. Não abra, não edite e
não crie nada em `web/`, `server/` ou `web-admin/` — a implementação da aba de
verdade é tarefa futura, fora de escopo. Não rode build, `cap sync`, bump ou
publicação.
  </action>
  <verify>
    <automated>
cd /Users/acamerini/dev/bolsia/b3-agente && bash -c '
SEED=.planning/seeds/opcoes-v2-setups-propostos-via-b-mcp.md
NOTA=.planning/notes/opcoes-v2-b-mcp-exploracao.md
fail=0
chk() { if [ "$2" -eq 0 ]; then echo "FAIL: $1"; fail=1; else echo "ok: $1"; fi; }
chkz() { if [ "$2" -ne 0 ]; then echo "FAIL: $1"; fail=1; else echo "ok: $1"; fi; }

# 1. Decisao fechada presente nos DOIS arquivos
chk "seed cita Candidato A"  "$(grep -ci "candidato a" "$SEED")"
chk "nota cita Candidato A"  "$(grep -ci "candidato a" "$NOTA")"
chk "seed nomeia a aba propria Opcoes" "$(grep -ci "aba pr" "$SEED")"
chk "nota nomeia a aba propria Opcoes" "$(grep -ci "aba pr" "$NOTA")"
chk "seed carimba a data da decisao" "$(grep -c "2026-09-01" "$SEED")"
chk "nota carimba a data da decisao" "$(grep -c "2026-09-01" "$NOTA")"

# 2. Linguagem de pendencia aberta eliminada
chkz "seed nao pede mais validacao do Alex" "$(grep -c "pede valida" "$SEED")"
chkz "seed sem recomendacao provisoria pendente" "$(grep -c "recomenda..o provis" "$SEED")"
chkz "nota sem titulo antigo de nao-decisao" "$(grep -c "^## Navega..o — recomenda..o, n.o decis.o fechada" "$NOTA")"
chk  "nota tem secao de navegacao marcada como decidida" "$(grep -ci "^## Navega.*DECIDIDO" "$NOTA")"

# 3. Trade-offs registrados como aceitos (nao ignorados)
chk "nota registra barra 4->5 itens" "$(grep -Eci "5 itens|4 pra 5|4 para 5" "$NOTA")"
chk "nota registra risco de aba vazia" "$(grep -Eci "abrir vazi|aba vazi|vazia quando" "$NOTA")"

# 4. Raciocinio historico pro-B preservado
chk "nota preserva o racional original (nunca aparece vazio / Posicoes)" "$(grep -Eci "nunca aparec|contextual dentro" "$NOTA")"

# 5. Escopo: diff SO em .planning/, zero codigo
chkz "nenhum arquivo fora de .planning tocado" "$(git diff --name-only HEAD | grep -cv "^\.planning/")"
chkz "nenhum arquivo de codigo tocado" "$(git diff --name-only HEAD | grep -Ec "^(web|server|web-admin)/")"

# 6. Os dois arquivos alvo realmente mudaram
chk "seed modificado"  "$(git diff --name-only HEAD -- "$SEED" | wc -l | tr -d " ")"
chk "nota modificada"  "$(git diff --name-only HEAD -- "$NOTA" | wc -l | tr -d " ")"

[ "$fail" -eq 0 ] && echo "VERIFY OK" || { echo "VERIFY FALHOU"; exit 1; }
'
    </automated>
  </verify>
  <done>
- Seed: "Colocação na navegação" saiu de "Pendências de produto" e virou decisão
  fechada (Candidato A, aba própria, barra de 5 itens nomeada, data + autor).
- Nota: seção de navegação renomeada para decisão fechada, com (a) a decisão no
  topo, (b) os dois trade-offs como riscos aceitos, (c) o racional pró-B
  preservado como contexto histórico.
- `git diff --name-only HEAD` retorna exclusivamente caminhos sob `.planning/`.
- O bloco `<automated>` acima imprime `VERIFY OK`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma) | Edição de dois arquivos markdown de planejamento interno. Sem entrada não confiável, sem superfície de rede, sem execução, sem dependência nova. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-r5t-01 | Tampering | Arquivos de código do app (`web/`, `server/`) alterados por engano numa task de doc | mitigate | Verify gate falha se `git diff --name-only HEAD` listar qualquer caminho fora de `.planning/` |
| T-r5t-02 | Repudiation | Decisão de produto registrada sem autor/data, virando "quem decidiu isso?" depois | mitigate | Decisão carimbada com "Alex, 2026-09-01" nos dois arquivos; verify gate exige a data em ambos |
| T-r5t-03 | Information disclosure | Perda do racional histórico pró-Candidato B ao sobrescrever a seção | mitigate | Guardrail do repo "histórico não se reescreve"; verify gate exige que o racional original siga presente na nota |
| T-r5t-SC | Tampering | Instalação de pacote npm/pip/cargo | accept | Nenhum install nesta task — sem superfície de supply chain |
</threat_model>

<verification>
Rodar o bloco `<automated>` da Task 1 (é a suíte completa desta quick task).

Suíte canônica do repo (`bash scripts/executar.sh --testes`) e `npx vite build`
**não se aplicam**: nada em `server/`, `web/` ou `web-admin/` é tocado. Rodá-las
não valida nada aqui — o gate real é o escopo do diff.
</verification>

<success_criteria>
1. `VERIFY OK` impresso pelo bloco automatizado.
2. Leitura humana do seed: a colocação na navegação lê-se como decidida
   (Candidato A), não como pendência.
3. Leitura humana da nota: decisão no topo, trade-offs aceitos logo abaixo,
   racional histórico pró-B ainda inteligível.
4. `git diff --name-only HEAD` = exatamente 2 arquivos, ambos sob `.planning/`.
</success_criteria>

<output>
Create `.planning/quick/260901-r5t-registrar-decis-o-alex-escolheu-candidat/260901-r5t-SUMMARY.md` when done
</output>

