---
phase: quick-260907-w33
plan: 01
subsystem: ui
tags: [react, flexbox, carousel, css-in-js, opcoes]

requires: []
provides:
  - "Teto de largura (`flex: \"0 0 210px\"`) nos dois trilhos de opções (CandidatoOpcao + item de OportunidadesOpcoes), corrigindo o estouro horizontal quando a manchete de uma proposta (ex.: collar) é longa"
affects: [fase-22-ui-spec, fase-19-multi-candidato]

tech-stack:
  added: []
  patterns:
    - "flex-basis fixo em item de carrossel horizontal (em vez de `flex: 0 0 auto`) para impedir que `max-content` estique o card além da largura pretendida, mantendo `minWidth` como piso pré-existente"

key-files:
  created: []
  modified:
    - web/src/App.jsx

key-decisions:
  - "Opção B (`flex: \"0 0 210px\"`, sem maxWidth) escolhida pelo Alex sobre a recomendação do planner (Opção A, `84%`/`330px`). Motivo: `minWidth:\"210px\"` da Decisão 1 do 22-UI-SPEC sempre foi PISO, nunca teto — `flex:\"0 0 210px\"` cumpre essa decisão pela primeira vez em vez de revertê-la. Guardião de test_fase22_componentes_compartilhados.mjs (asserção 7, `totalPeek84 === 1`) permanece intocado."
  - "Risco aceito conscientemente pelo Alex: a ~184px de caixa útil, linhas de payoff podem quebrar valor para a linha de baixo. Não antecipado nesta correção — se ocorrer, ajuste de largura futuro com dado medido, não estimado."
  - "Publicação (bump + publicar-web.sh) DELIBERADAMENTE fora desta task — duas outras sessões editam web/src/App.jsx em paralelo; publicar agora empacotaria trabalho de terceiros não verificado. Publicação combinada fica pendente."

patterns-established:
  - "Item de carrossel horizontal comparável lado a lado usa `flex: \"0 0 <valor fixo>px\"` (não `auto`) sempre que o conteúdo interno (manchete/texto) pode variar de comprimento e precisa de teto de largura sem truncamento."

requirements-completed: [QUICK-260907-w33]

duration: ~25min
completed: 2026-09-07
---

# Quick Task 260907-w33: Corrigir estouro de largura do card de candidato de opção

**`flex: "0 0 210px"` aplicado nos dois trilhos de opções (CandidatoOpcao + item de OportunidadesOpcoes), corrigindo o estouro sem reverter a Decisão 1 do 22-UI-SPEC — guardião intocado.**

## Performance

- **Duration:** ~25 min
- **Completed:** 2026-09-07
- **Tasks:** 2 executadas (Task 1 já resolvida pelo Alex antes do dispatch)
- **Files modified:** 1 (`web/src/App.jsx`)

## Accomplishments
- Estouro de largura corrigido nos DOIS sites que compartilham o defeito latente (`CandidatoOpcao`, manifesto; item de `OportunidadesOpcoes`, latente desde a Fase 18)
- Comentário de história de decisão registrado em `CandidatoOpcao` (razão do bug, guardrail CVM da manchete, princípio 9 de larguras iguais, referência à Decisão 1 do 22-UI-SPEC) + ponteiro de uma linha no site espelhado
- Guardião `test_fase22_componentes_compartilhados.mjs` verificado e mantido intocado — a asserção 7 (`totalPeek84 === 1`) segue válida porque a Opção B não introduz um terceiro `flex: "0 0 84%"`
- Suíte canônica completa verde: `bash scripts/executar.sh --testes` → pytest `2030 passed, 1 skipped` + todas as suítes `web/tests/*.mjs` em `[OK]`
- `npx vite build` (a partir de `web/`) concluído sem erro

## Task Commits

Cada task foi commitada atomicamente:

1. **Task 1: Decidir o valor do teto de largura** — RESOLVIDA pelo Alex antes do dispatch (checkpoint `status="RESOLVIDO"` no PLAN.md); não re-executada, nenhum commit desta sessão.
2. **Task 2: Aplicar o teto de largura nos DOIS trilhos + comentário de decisão + guardião coerente** — `0e8e1c6` (fix)
3. **Task 3: Validação canônica + build + publicação do front** — sem commit de código (só verificação; publicação deliberadamente suspensa, ver abaixo)

## Files Created/Modified
- `web/src/App.jsx` — `CandidatoOpcao` (linha ~4260) e item de `OportunidadesOpcoes` (linha ~4106): `flex: "0 0 auto"` → `flex: "0 0 210px"` (mantendo `minWidth: "210px"` pré-existente, sem `maxWidth`); comentário de decisão adicionado em `CandidatoOpcao` + ponteiro no site espelhado

## Decisions Made
- **Opção B confirmada e aplicada** (`flex: "0 0 210px"`), conforme resolução prévia do Alex registrada no PLAN.md. Não houve reabertura da discussão nem aplicação da Opção A.
- Guardião de carrossel (`test_fase22_componentes_compartilhados.mjs`) **não foi tocado** — decisão explícita da Opção B, verificado que a asserção 7 segue passando sem alteração.

## Deviations from Plan

None — plano executado exatamente como escrito para as Tasks 2 e 3 (Task 1 já resolvida fora desta execução).

## Issues Encountered

**Divergência de branch do worktree detectada no `<worktree_branch_check>` inicial.** O HEAD do worktree (`worktree-agent-a8fda9eeaa46b99ab`) estava 7 commits à frente do ponto de fork com `v2/interacao-estrutural`, contendo trabalho de uma feature não relacionada ("volume anormal" — indicador, KB, célula no card). Antes de aplicar o `git reset --hard` exigido pela verificação de base, confirmei que esses 7 commits já estavam mesclados em `origin/main` (hash idêntico, `git branch -a --contains <hash>` listou `remotes/origin/main`) — portanto nenhum trabalho foi perdido. Reset aplicado com segurança para `e33b4c33` (tip de `v2/interacao-estrutural`, contém o próprio plano desta task). Também foram necessários dois bypasses pontuais de sandbox (`dangerouslyDisableSandbox: true`) para o `git reset --hard` (arquivo `.claude/launch.json` fora do allowlist de escrita) e para `bash scripts/executar.sh --testes` / `npx vite build` (script tenta escrever log em `/`, fora do allowlist).

## PUBLICAÇÃO PENDENTE — DESTAQUE

**A publicação foi DELIBERADAMENTE suspensa nesta task, por decisão do Alex (2026-09-07).** `web/src/App.jsx` está alterado e commitado (`0e8e1c6`) mas AINDA NÃO publicado. Motivo: há outras DUAS sessões editando `web/src/App.jsx` em paralelo agora (uma no filtro de setup aposentado, outra no destaque do aviso de ordem pendente); publicar agora empacotaria trabalho de terceiros ainda não verificado no mesmo bundle e geraria um carimbo que as outras duas sessões superariam em seguida.

**Comando pendente, a rodar UMA vez depois que as três sessões aterrissarem:**
```bash
bash scripts/bump.sh && bash scripts/publicar-web.sh
```

**Estado do arquivo no momento desta correção** (foto para quando as três forem mescladas):
```
git status --short   → (árvore limpa; App.jsx já commitado em 0e8e1c6)
git diff --stat web/src/App.jsx (vs HEAD) → sem diff pendente, mudança já commitada
```

**Reverificação em staging** (o achado original veio de lá, iPhone): depois da publicação conjunta, usar `scripts/publicar-staging.sh` (exige árvore limpa) — ler o cabeçalho do script antes de rodar, ele NÃO deve vazar para produção.

## User Setup Required

None — nenhuma configuração externa necessária.

## Next Phase Readiness
- Correção pronta e testada, aguardando publicação conjunta com as outras duas sessões que editam `web/src/App.jsx`.
- Nenhum bloqueio técnico. Ação humana pendente: publicar quando as três frentes convergirem, depois reverificar no staging/iPhone com o cenário original (ABEV3, put_protecao + collar).

---
*Phase: quick-260907-w33*
*Completed: 2026-09-07*

## Self-Check: PASSED

- FOUND: `web/src/App.jsx`
- FOUND: `.planning/quick/260907-w33-corrigir-estouro-de-largura-do-card-de-c/260907-w33-SUMMARY.md`
- FOUND: commit `0e8e1c6` in `git log --oneline --all`
- `grep -c 'flex: "0 0 210px"' web/src/App.jsx` → 3 (2 sites de estilo + 1 menção em comentário) — consistente
