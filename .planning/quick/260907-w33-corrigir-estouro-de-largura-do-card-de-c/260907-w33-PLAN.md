---
phase: quick-260907-w33
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/App.jsx
  - web/tests/test_fase22_componentes_compartilhados.mjs
autonomous: false
requirements: [QUICK-260907-w33]
must_haves:
  truths:
    - "Com dois candidatos (put_protecao + collar) no detalhe da posição, a manchete do collar quebra em várias linhas em vez de esticar o card"
    - "Os dois cards de candidato têm a MESMA largura renderizada (nenhuma proposta ganha peso visual sobre a outra — CLAUDE.md princípio 9)"
    - "Os valores do bloco de payoff (ganho máximo, perda máxima, breakeven, caixa) ficam legíveis dentro do card"
    - "A manchete continua verbatim: nenhum truncamento, nenhuma reticência, nenhum texto do motor alterado (guardrail CVM)"
    - "O item de OportunidadesOpcoes e o item de CandidatoOpcao continuam com o MESMO padrão visual (invariante documentado em App.jsx:4238-4245)"
    - "Nenhum terceiro padrão de carrossel foi introduzido: os dois sites continuam usando carouselTrackStyle()/carouselItemStyle('start')"
    - "Nenhum guardião foi apagado; se algum travou valor antigo, foi ATUALIZADO com nota datada"
  artifacts:
    - path: "web/src/App.jsx"
      provides: "teto de largura nos itens dos dois trilhos de opções (CandidatoOpcao + OportunidadesOpcoes), com comentário de história de decisão"
      contains: "CandidatoOpcao"
    - path: "web/tests/test_fase22_componentes_compartilhados.mjs"
      provides: "guardião de taxonomia de carrossel — asserção 7 coerente com a decisão tomada (atualizada com nota datada SE a Opção A for escolhida; intocada se a Opção B for escolhida)"
      contains: "Decisão 1 do 22-UI-SPEC"
  key_links:
    - from: "web/src/App.jsx (CandidatoOpcao)"
      to: "web/src/App.jsx (item de OportunidadesOpcoes)"
      via: "mesmo objeto de estilo inline — invariante 'reusa verbatim o padrão visual'"
      pattern: "carouselItemStyle\\(\"start\"\\)"
    - from: "web/src/App.jsx"
      to: "web/tests/test_fase22_componentes_compartilhados.mjs (asserção 7)"
      via: "contagem de ocorrências de `flex: \"0 0 84%\"`"
      pattern: "totalPeek84"
---

<objective>
Corrigir o estouro de largura do card de candidato de opção no carrossel do
detalhe da posição, achado ao vivo em 2026-09-07 (staging, iPhone, conta real,
dois candidatos: put_protecao + collar).

Purpose: com `flex: "0 0 auto"` e sem `maxWidth`, o item flex dimensiona pelo
`max-content` — a largura do texto SEM quebrar. O `whiteSpace: "normal"` já
presente PERMITE quebrar, mas nada OBRIGA; sem teto de largura o navegador
prefere a linha única. A manchete do collar é longa por desenho, então o card
explode e os valores do payoff ficam ilegíveis. A correção é de LARGURA, nunca
de texto.

Output: teto de largura nos DOIS trilhos de opções (`CandidatoOpcao` e o item
de `OportunidadesOpcoes`, que tem o mesmo defeito latente desde a Fase 18),
com comentário registrando a história da decisão, guardião coerente, suíte
canônica verde, build publicado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

Alvos (localize por CONTEÚDO, nunca por número de linha — há outras DUAS
sessões editando `web/src/App.jsx` em paralelo agora, em regiões diferentes;
as linhas deslocam):

```
grep -n 'carouselItemStyle("start"), flex: "0 0 auto", minWidth: "210px"' web/src/App.jsx
```

Hoje isso retorna DOIS sites, ambos alvos deste plano:
- item de `OportunidadesOpcoes` (um `<button>`, tem `textAlign: "left"` e `cursor: "pointer"`)
- `CandidatoOpcao` (um `<div>`, sem `cursor`)
</context>

<interfaces>
<!-- Estado ATUAL do código, já lido. O executor não precisa explorar. -->

Helpers de carrossel (fonte única, NÃO alterar):
```js
const carouselTrackStyle = (extra) => ({ display: "flex", overflowX: "auto", scrollSnapType: "x proximity", WebkitOverflowScrolling: "touch", ...extra });
const carouselItemStyle = (align = "start") => ({ scrollSnapAlign: align });
```

Estilo ATUAL do item de `OportunidadesOpcoes` (defeito latente, conteúdo curto):
```js
style={{ ...carouselItemStyle("start"), flex: "0 0 auto", minWidth: "210px", minHeight: "44px", textAlign: "left", padding: "11px 12px", borderRadius: "11px", background: T.bgCard, border: `1px solid ${T.borderFaint}`, cursor: "pointer" }}
```

Estilo ATUAL de `CandidatoOpcao` (defeito manifesto):
```js
style={{ ...carouselItemStyle("start"), flex: "0 0 auto", minWidth: "210px", minHeight: "44px", padding: "11px 12px", borderRadius: "11px", background: T.bgCard, border: `1px solid ${T.borderFaint}` }}
```

Referência de produção (HERO-CARROSSEL, `EvolucaoScreen` — NÃO alterar):
```js
style={{ ...card, ...carouselItemStyle("center"), flex: "0 0 84%", maxWidth: "330px", borderLeft: `3px solid ${T.accent}`, ... }}
```

Guardião em jogo — `web/tests/test_fase22_componentes_compartilhados.mjs`,
asserção 7 (a ÚNICA asserção do repo que trava esses valores; `grep -rn '210px\|330px\|84%' web/tests/ scripts/ server/tests/` não retorna mais nada):
```js
// --- 7. Taxonomia preservada (Decisão 1 do 22-UI-SPEC): peek de 84% só HERO -
const totalPeek84 = (app.match(/flex: "0 0 84%"/g) || []).length;
ok(
  "`flex: \"0 0 84%\"` aparece exatamente 1× (peek de 84% é do HERO-CARROSSEL apenas — chips de filtro e candidatos de opção precisam ficar comparáveis lado a lado)",
  totalPeek84 === 1
);
```
</interfaces>

<conflito_encontrado>
**A instrução literal do pedido colide com uma decisão de produto shipada e
documentada.** Isso não é o guardião travando um valor obsoleto por descuido:
a Fase 22 (milestone v1.5, arquivado) modelou EXATAMENTE esta mudança como
ameaça e a mitigou de propósito.

`.planning/milestones/v1.5-phases/22-.../22-UI-SPEC.md:215-220` (Decisão 1):

> `OportunidadesOpcoes` e `PropostaDaPosicao` candidatos são `minWidth:"210px"`
> cards specifically designed to be **compared side-by-side** (Fase 19's whole
> point, MULTI-01/02, is showing put-isolada vs. collar together for the user
> to weigh). Forcing `84%` width would hide the second candidate off-screen by
> default, undermining the comparison this exact requirement was built to
> enable.

E `22-01-PLAN.md:378`, threat register:

> | T-22-02 | Denial of service | Aplicar `flex: "0 0 84%"` ao TECH_MODELS ou
> aos candidatos de opção, escondendo conteúdo que o usuário precisa comparar |
> mitigate | Asserção 7 do guardião |

O pedido (usar `84%`) é literalmente a ameaça T-22-02 que o guardião existe
para bloquear. O diagnóstico do bug está certo e o bug é real; o VALOR pedido
é que conflita. Por isso a Task 1 é um checkpoint bloqueante, e não uma
escolha silenciosa do executor.

Ambas as opções abaixo corrigem o bug, dão largura IGUAL aos dois cards
(princípio 9), não truncam a manchete e não criam um terceiro padrão visual.
</conflito_encontrado>

<tasks>

<task type="checkpoint:decision" gate="blocking" status="RESOLVIDO">
  <name>Task 1: Decidir o valor do teto de largura (reverte ou preserva a Decisão 1 da Fase 22)</name>

  <RESOLUCAO data="2026-09-07">
**ESCOLHIDO: `opcao-b` — `flex: "0 0 210px"`.** Decisão do Alex, tomada com a
Decisão 1 do 22-UI-SPEC na mão. O executor NÃO precisa perguntar nada; siga o
ramo `opcao-b` em todas as tasks abaixo e NÃO toque no guardião.

Razão pela qual a recomendação do planner (opcao-a) foi recusada: a UI-SPEC da
Fase 22 descreve estes cards como "`minWidth:"210px"` cards specifically
designed to be compared side-by-side" — mas `minWidth` é PISO, não teto, e com
`flex-basis: auto` o card sempre cresceu até o conteúdo. Ou seja, o defeito não
é a falta de um teto novo: é que a implementação NUNCA cumpriu o desenho
documentado. `flex: "0 0 210px"` não reverte a Decisão 1 — ela a CUMPRE pela
primeira vez. Aplicar `84%` seria reverter uma decisão shipada para consertar
um bug causado por ela nunca ter sido implementada, o que é a ameaça T-22-02
literal.

Risco aceito conscientemente: a ~184px de caixa útil, as linhas de payoff
(`label` … `R$ x.xxx,xx`) podem quebrar o valor para a linha de baixo. Isso é
verificável no aparelho em segundos — o Alex está com o iPhone na mão e o
cenário montado em staging (ABEV3, dois candidatos). Se quebrar, a largura sobe
num ajuste seguinte, COM o dado medido em vez de estimado. Não antecipe esse
ajuste.
  </RESOLUCAO>

  <decision>Qual teto de largura aplicar aos itens dos dois trilhos de opções.</decision>
  <context>
O bug é a ausência de teto de largura (`flex-basis: auto` → `max-content`).
Qualquer teto o corrige. O que muda entre as opções é o que acontece com a
comparação lado a lado que a Fase 19 (MULTI-01/02) entregou e a Fase 22
protegeu com guardião.

Largura real do trilho no iPhone (390pt): ~310-320px depois do padding da
página e do card. Logo, 84% ≈ 265px por card; a Opção B fixa 210px por card.
  </context>
  <options>
    <option id="opcao-a">
      <name>A — `flex: "0 0 84%", maxWidth: "330px"` (o que o pedido descreve)</name>
      <pros>Valores mais legíveis (card ~265px em vez de 210px — e "prejudica ler os valores" é literalmente o defeito relatado). Único par de valores já validado em produção (HERO). Larguras iguais. O 2º card espia ~45px na borda, sinalizando que há mais.</pros>
      <cons>Reverte a Decisão 1 do 22-UI-SPEC: a comparação put-isolada × collar deixa de ser simultânea e passa a ser por swipe. Exige ATUALIZAR a asserção 7 do guardião (1× → 3×) com nota datada — reversão deliberada, nunca apagamento.</cons>
    </option>
    <option id="opcao-b">
      <name>B — `flex: "0 0 210px"` (mantém `minWidth: "210px"`, sem `maxWidth`)</name>
      <pros>Corrige o bug igualmente (basis definido = o texto quebra). Larguras iguais. Preserva a Decisão 1 e a comparação lado a lado — o 2º card mostra ~100px, mais peek que a Opção A. Guardião fica INTOCADO (nenhuma reversão de decisão shipada). Reusa o valor 210px que já é o vocabulário destes dois trilhos.</pros>
      <cons>Card mais estreito: a manchete longa do collar fica mais alta, e as linhas de payoff (`space-between`, label + valor em 11px, caixa de conteúdo ~166px) podem quebrar o valor para a linha de baixo. Ou seja: risco de resolver o estouro horizontal e degradar em parte a mesma legibilidade que motivou o achado.</cons>
    </option>
  </options>
  <recommendation>
Opção A, com o guardião atualizado (não apagado) e o comentário de código
registrando a reversão. Razão: o defeito relatado é legibilidade de VALOR, e
265px acomoda `label + R$ x.xxx,xx` numa linha só, enquanto 210px não garante
isso — corrigir o estouro e ainda entregar valor quebrado seria trocar um
defeito por outro. O medo de T-22-02 ("esconder o 2º candidato") continua
parcialmente endereçado: com snap `proximity` + peek de ~45px o 2º candidato
segue sinalizado e alcançável por swipe. Se o Alex priorizar a comparação
simultânea sobre a legibilidade da linha de valor, a Opção B é a resposta
certa e não mexe em nada shipado.
  </recommendation>
  <resume-signal>Responda: `opcao-a` ou `opcao-b`</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Aplicar o teto de largura nos DOIS trilhos + comentário de decisão + guardião coerente</name>
  <files>web/src/App.jsx, web/tests/test_fase22_componentes_compartilhados.mjs</files>
  <action>
Localize os dois alvos por CONTEÚDO (nunca por número de linha — duas outras
sessões editam este arquivo agora):
`grep -n 'carouselItemStyle("start"), flex: "0 0 auto", minWidth: "210px"' web/src/App.jsx`
Devem aparecer 2 ocorrências: o `<button>` de `OportunidadesOpcoes` e o
`<div>` de `CandidatoOpcao`. Se aparecerem 0, 1 ou 3+, PARE e reporte — o
arquivo mudou sob os pés e o diagnóstico precisa ser refeito.

Se a Task 1 escolheu `opcao-a`: nos DOIS sites, troque
`flex: "0 0 auto", minWidth: "210px"` por
`flex: "0 0 84%", minWidth: "210px", maxWidth: "330px"`.
Mantenha todo o resto do objeto de estilo byte a byte (`textAlign`/`cursor`
só existem no site de `OportunidadesOpcoes` — preserve a diferença; ela é
pré-existente e correta, o `<div>` não é clicável).

Se a Task 1 escolheu `opcao-b`: nos DOIS sites, troque `flex: "0 0 auto"` por
`flex: "0 0 210px"`, mantendo `minWidth: "210px"` e SEM adicionar `maxWidth`.

Aplique o MESMO valor nos dois sites. O comentário de `App.jsx` em
`CandidatoOpcao` ("reusa verbatim o padrão visual do item de
OportunidadesOpcoes") é um invariante DOCUMENTADO: divergir quebraria. Se ao
inspecionar ficar claro que um dos dois precisa de valor diferente, documente
o porquê num comentário curto — nunca divirja em silêncio.

Adicione UM comentário (imediatamente acima do estilo de `CandidatoOpcao`, e
um ponteiro de uma linha no site de `OportunidadesOpcoes`) no padrão da casa
— história de decisão, não repetição do código. Deve dizer, em PT-BR: que o
teto de largura existe porque `flex-basis: auto` dimensiona pelo `max-content`
e a manchete do collar então estica o card até ficar ilegível (achado ao vivo
2026-09-07, staging/iPhone, dois candidatos); que a manchete NUNCA é truncada
(guardrail CVM — cortar reescreveria a afirmação do motor), por isso a
correção é de largura; e que os dois cards têm largura IGUAL de propósito
(duas propostas disputam a MESMA decisão — larguras diferentes dariam peso
visual diferente, CLAUDE.md princípio 9).

Guardião — SÓ se a Task 1 escolheu `opcao-a`: em
`web/tests/test_fase22_componentes_compartilhados.mjs`, asserção 7, mude
`totalPeek84 === 1` para `totalPeek84 === 3` e reescreva a mensagem para
refletir os 3 sites legítimos (HERO + os dois trilhos de opções). NÃO apague
a asserção nem afrouxe para `>=`: a contagem exata é o que impede um quarto
site pegar carona. Acrescente acima dela uma nota datada explicando que a
Decisão 1 do 22-UI-SPEC foi PARCIALMENTE revertida em 2026-09-07 (só os dois
trilhos de opções; TECH_MODELS segue fora, comparável e escaneável), o motivo
(achado ao vivo: card esticado até o texto, valores ilegíveis) e que a ameaça
T-22-02 segue endereçada pelo peek + snap `proximity`. NÃO edite os arquivos
de `.planning/milestones/` — histórico não se reescreve.

Se a Task 1 escolheu `opcao-b`: não toque no guardião (a asserção 7 continua
válida como está) e registre no SUMMARY que ela foi verificada e mantida.
  </action>
  <verify>
    <automated>grep -c 'flex: "0 0 auto", minWidth: "210px"' web/src/App.jsx</automated>
    <automated>node web/tests/test_fase22_componentes_compartilhados.mjs</automated>
  </verify>
  <done>
`grep -c 'flex: "0 0 auto", minWidth: "210px"' web/src/App.jsx` retorna 0.
`grep -c 'carouselItemStyle("start")' web/src/App.jsx` continua igual ao valor
de antes da edição (nenhum site perdido, nenhum criado).
`node web/tests/test_fase22_componentes_compartilhados.mjs` sai 0 e imprime a
asserção 7 como `ok`.
Nenhum caractere da manchete (`p.manchete` / `pr.manchete`) foi alterado, e
nenhum `textOverflow`/`ellipsis`/`WebkitLineClamp` foi introduzido nesses dois
blocos.
  </done>
</task>

<task type="auto">
  <name>Task 3: Validação canônica + build + publicação do front</name>
  <files>web/src/App.jsx, server/web_dist/</files>
  <action>
1. Suíte canônica COMPLETA — `bash scripts/executar.sh --testes` (roda pytest
   do backend + `web/tests/*.mjs`). `scripts/test.sh` sozinho é meia baseline
   e NÃO conta como validação (CLAUDE.md). Baseline conhecida: ~2021 passed,
   1 skipped + web ok. Qualquer regressão a mais que isso PARA a task.
2. Front foi editado → `npx vite build` (a partir de `web/`) antes de declarar
   ok. Grep e teste estático não pegam erro de sintaxe JSX.
3. **NÃO PUBLICAR NESTA TASK — decisão do Alex, 2026-09-07.** O passo de
   bump + `publicar-web.sh` fica DELIBERADAMENTE de fora. Motivo: há outras
   DUAS sessões editando `web/src/App.jsx` neste momento (uma no filtro de
   setup aposentado, outra no destaque do aviso de ordem pendente). Publicar
   agora empacotaria trabalho de terceiros ainda não verificado no mesmo
   bundle, e produziria um carimbo que as outras duas superariam em seguida —
   três bumps para uma entrega só. A publicação acontece UMA vez, depois que
   as três aterrissarem.

   Isto NÃO é a armadilha registrada em "fase sem plano de publicação front"
   (correção testada que nunca vai ao ar): a publicação está agendada e
   nomeada, só não é desta task. Registre no SUMMARY, em destaque, que
   `web/src/App.jsx` está alterado e AINDA NÃO publicado, e que o passo
   pendente é `bash scripts/bump.sh && bash scripts/publicar-web.sh`.
4. Rode `git status --short` e `git diff --stat web/src/App.jsx` mesmo assim, e
   reporte o resultado no SUMMARY — serve de foto do estado do arquivo no
   momento desta correção, útil quando as três forem mescladas.
5. O achado veio do STAGING (iPhone). A reverificação, depois da publicação
   conjunta, é por `scripts/publicar-staging.sh` (exige árvore limpa) — leia o
   cabeçalho do script antes de rodar; ele NÃO deve vazar para produção.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
    <automated>cd web && npx vite build</automated>
  </verify>
  <done>
Suíte canônica verde nas DUAS suítes (pytest + web/tests), sem regressão
contra a baseline. `npx vite build` conclui sem erro. Publicação NÃO feita, por
decisão — e o SUMMARY diz isso em destaque, com o comando pendente escrito.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| motor determinístico → UI | A manchete atravessa daqui; texto do motor é verbatim (guardrail CVM) |
| sessões concorrentes → `web/src/App.jsx` | Duas outras sessões editam o mesmo arquivo; o diff publicado pode carregar trabalho não verificado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-w33-01 | Tampering | manchete do motor em `CandidatoOpcao` / `OportunidadesOpcoes` | mitigate | Correção é só de largura; critério de aceite da Task 2 proíbe `textOverflow`/`ellipsis`/`WebkitLineClamp` e exige manchete byte a byte intacta |
| T-w33-02 | Information disclosure (viés visual) | dois candidatos concorrendo pela mesma decisão | mitigate | Largura IGUAL obrigatória nos dois cards (princípio 9); ambas as opções da Task 1 garantem isso |
| T-w33-03 | Denial of service (conteúdo escondido) | 2º candidato empurrado para fora da tela — é a T-22-02 herdada | mitigate | Checkpoint bloqueante da Task 1 leva a escolha ao Alex em vez de o executor decidir; se `opcao-a`, o guardião é atualizado com nota datada, nunca apagado |
| T-w33-04 | Tampering | publicação carregando trabalho de outras sessões | mitigate | Task 3 passo 3: inspeção de `git status`/`git diff --stat` antes de publicar, com parada explícita |
| T-w33-SC | Tampering | instalação de pacote | accept | Nenhum pacote novo — mudança é CSS inline em arquivo existente |
</threat_model>

<verification>
- `grep -c 'flex: "0 0 auto", minWidth: "210px"' web/src/App.jsx` → 0
- `node web/tests/test_fase22_componentes_compartilhados.mjs` → exit 0
- `bash scripts/executar.sh --testes` → duas suítes verdes, sem regressão
- `cd web && npx vite build` → sem erro
- Manchete inalterada: `git diff web/src/App.jsx | grep -E '^[-+].*manchete'` só deve mostrar linhas de comentário, nunca de texto do motor
</verification>

<success_criteria>
- Os dois trilhos de opções têm teto de largura e larguras iguais entre cards
- Nenhum texto do motor foi alterado ou truncado
- Nenhum terceiro padrão visual de carrossel foi criado
- Guardião coerente: atualizado com nota datada (opcao-a) ou verificado e mantido (opcao-b) — nunca apagado nem afrouxado
- Suíte canônica verde + `vite build` limpo + bump/publicação feitos (ou suspensos com motivo reportado)
</success_criteria>

<output>
Create `.planning/quick/260907-w33-corrigir-estouro-de-largura-do-card-de-c/260907-w33-SUMMARY.md` when done.
Registre no SUMMARY: qual opção o Alex escolheu e por quê, o carimbo publicado,
e — se `opcao-a` — que a Decisão 1 do 22-UI-SPEC foi parcialmente revertida
(para rastreabilidade quando alguém reler a Fase 22 arquivada).
</output>
