---
phase: quick-260901-1ak
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: true
requirements: [DOC-SYNC-01]

must_haves:
  truths:
    - "A seção Active do PROJECT.md não lista mais a virada de produção do mydata (resolvida em 2026-08-31)"
    - "A seção Active do PROJECT.md não lista mais a race condition WR-01 (resolvida em 2026-08-31, PR #28)"
    - "Os 5 itens genuinamente abertos da seção Active continuam intactos, com o texto original"
    - "Nenhuma outra seção do PROJECT.md foi alterada"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Seção Active sincronizada com a tabela Deferred Items de STATE.md"
      contains: "### Active"
  key_links:
    - from: ".planning/PROJECT.md"
      to: ".planning/STATE.md"
      via: "seção Active reflete a coluna de status da tabela Deferred Items"
      pattern: "### Active"
---

<objective>
Sincronizar a seção `### Active` do `.planning/PROJECT.md` com a fonte de verdade
(`.planning/STATE.md`, tabela "Deferred Items", atualizada em 2026-08-31),
removendo os 2 itens que já foram resolvidos mas ficaram registrados como
pendentes.

Purpose: PROJECT.md é doc viva de estado atual; a última revisão indicada era
2026-08-29 e ele ficou defasado depois do fechamento da Fase 14 (2026-08-31).
Itens stale em "Active" produzem replanejamento errado nas próximas fases.

Output: `.planning/PROJECT.md` com 5 itens em Active (era 7) e a referência de
data em STATE.md atualizada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

Guardrail do repo (`CLAUDE.md`): "Histórico não se reescreve" vale para `qa/`,
`ESTADO-*`, `CHECKOUT-*` e RELEASES. **Não** vale para `.planning/PROJECT.md`,
que é documento vivo de estado atual e deve refletir a realidade de hoje.

Contexto do que foi resolvido (não vai para o arquivo, é só justificativa):
- Virada mydata: `B3_OPTIONS_PROVIDER=mydata` está em produção, verificado ao
  vivo em 2026-08-31 (`GET /api/options/chain/PETR4` → `providerStatus: "ok"`).
  `B3_CANDLE_PROVIDER` segue `brapi` por decisão explícita (ADR-008 intacto).
  Ver `docs/adr/023-opcoes-lastreadas.md` (Nota aditiva).
- WR-01: resolvido com `MYDATA_BUDGET_LOCK` + `reservar()` atômico em
  `options_provider_mydata.py`, PR #28 / commit `c014c88`. Ver
  `.planning/todos/resolved/decidir-wr01-mydata-budget.md`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover os 2 itens resolvidos da seção Active do PROJECT.md</name>
  <files>.planning/PROJECT.md, .planning/STATE.md</files>
  <action>
Em `.planning/PROJECT.md`, dentro da seção `### Active` (começa na linha ~169),
remover exatamente dois blocos de bullet, cada um sendo um item `- [ ]`
multi-linha completo (a linha do bullet mais todas as suas linhas de
continuação indentadas):

1. O bloco que começa com `- [ ] Retomar a virada de produção do mydata (Fase 9,
   checkpoint \`adiar\`)` e termina na linha que referencia
   `docs/MEDICAO-Mydata-2026-08-27.md §6/§8` (linhas ~183-190 no arquivo atual).

2. O bloco que começa com `- [ ] Gate de orçamento das opções tem race condition
   check-then-debit` e termina na linha que referencia
   `.planning/notes/decisoes-autonomas-v1.2.md` (linhas ~191-194 no arquivo
   atual).

Usar a ferramenta Edit com o texto completo de cada bloco como `old_string`
(não usar sed/heredoc, não editar por número de linha — o casamento é por texto
exato, para não decapitar linhas vizinhas).

**Escopo estrito — NÃO tocar em mais nada.** Os 5 itens restantes de Active
ficam com o texto original, byte a byte:
- Item 8 do checkpoint 08-05 (`entradaAuto`)
- 2 human-check da Fase 3
- Backlog dos 9 achados Baixo do REPORT-01
- `textDim` do tema claro / WCAG AA
- CAP-12 / distribuição TestFlight

Nenhuma outra seção do arquivo é alterada (Requirements/Validated, Out of Scope,
Context, Constraints, Key Decisions, Evolution). Não adicionar item novo, não
reordenar, não reescrever bullets que ficam, não inserir nota de "removido em".

Depois, em `.planning/STATE.md`, atualizar apenas a linha ~21
`See: .planning/PROJECT.md (updated 2026-08-29)` para
`See: .planning/PROJECT.md (updated 2026-09-01)`. Nada mais em STATE.md muda —
a tabela "Deferred Items" já está correta e é a fonte de verdade.
  </action>
  <verify>
    <automated>
cd /Users/acamerini/dev/bolsia/b3-agente && \
python3 - <<'PY'
import re, sys, pathlib
p = pathlib.Path('.planning/PROJECT.md')
t = p.read_text(encoding='utf-8')
fail = []

# 1. Os dois itens sumiram
for needle in ["Retomar a virada de produção do mydata",
               "race condition check-then-debit"]:
    if needle in t:
        fail.append(f"AINDA PRESENTE: {needle}")

# 2. Isola a seção Active e conta os bullets
m = re.search(r'^### Active\n(.*?)^### ', t, re.S | re.M)
if not m:
    fail.append("seção '### Active' não encontrada")
else:
    active = m.group(1)
    n = len(re.findall(r'^- \[ \] ', active, re.M))
    if n != 5:
        fail.append(f"Active tem {n} itens, esperado 5")
    # 3. Os 5 sobreviventes seguem lá
    for keep in ["Item 8 do checkpoint 08-05",
                 "2 human-check da Fase 3",
                 "os 9 achados Baixo do REPORT-01",
                 "`textDim` do tema claro",
                 "CAP-12 (bypass do cap de watchlist no iOS)"]:
        if keep not in active:
            fail.append(f"ITEM APAGADO POR ENGANO: {keep}")

# 4. Nenhuma outra seção sumiu
for sec in ["### Out of Scope", "## Requirements", "## Context"]:
    if sec not in t:
        fail.append(f"SEÇÃO SUMIU: {sec}")

# 5. STATE.md com a data nova
s = pathlib.Path('.planning/STATE.md').read_text(encoding='utf-8')
if "PROJECT.md (updated 2026-09-01)" not in s:
    fail.append("STATE.md: referência de data não atualizada")

if fail:
    print("FALHOU:"); [print(" -", f) for f in fail]; sys.exit(1)
print("OK: Active com 5 itens, 2 stale removidos, 5 preservados, STATE.md sincronizado")
PY
    </automated>
    <automated>cd /Users/acamerini/dev/bolsia/b3-agente && git diff --stat -- .planning/PROJECT.md .planning/STATE.md && git diff -- .planning/PROJECT.md | grep -c '^+[^+]' | xargs -I{} test {} -eq 0 && echo "OK: diff é só remoção em PROJECT.md"</automated>
  </verify>
  <done>
`.planning/PROJECT.md` tem 5 itens em `### Active` (era 7); os blocos do mydata
cutover e do WR-01 não aparecem mais em lugar nenhum do arquivo; os 5 itens
abertos estão intactos; o diff de PROJECT.md é puramente de remoção (zero linhas
adicionadas); `.planning/STATE.md` linha ~21 aponta `updated 2026-09-01`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma) | Edição de documento de planejamento local; sem input externo, sem código executável, sem superfície de rede |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | Tampering | `.planning/PROJECT.md` | mitigate | Edição por casamento de texto exato (Edit), não por número de linha; verify automatizado confirma que os 5 itens preservados continuam presentes e que o diff é só remoção |
| T-quick-02 | Repudiation | histórico de planejamento | accept | Conteúdo removido continua rastreável no git e permanece registrado na tabela "Deferred Items" de STATE.md (fonte de verdade) |
| T-quick-SC | Tampering | npm/pip/cargo installs | n/a | Nenhuma instalação de pacote neste plano |
</threat_model>

<verification>
1. `grep -n "### Active" -A 40 .planning/PROJECT.md` mostra 5 bullets `- [ ]`
2. `git diff -- .planning/PROJECT.md` mostra só linhas `-` (nenhum `+`)
3. STATE.md com `PROJECT.md (updated 2026-09-01)`
</verification>

<success_criteria>
- Seção `### Active` do PROJECT.md com exatamente 5 itens abertos
- Zero ocorrências de "Retomar a virada de produção do mydata" e
  "race condition check-then-debit" no PROJECT.md
- Diff de PROJECT.md 100% remoção
- Nenhuma outra seção do PROJECT.md tocada
- STATE.md com a data de referência atualizada (única linha alterada no arquivo)
</success_criteria>

<output>
Create `.planning/quick/260901-1ak-sincronizar-project-md-active-remover-2-/260901-1ak-SUMMARY.md` when done
</output>
