---
phase: 15-motor-de-proposta-arquitetura-interna
plan: 04
type: execute
wave: 3
depends_on: ["15-02", "15-03"]
files_modified:
  - server/tests/test_opcoes_fronteira.py
  - docs/adr/024-limite-interno-rastrear-avaliar.md
autonomous: true
requirements: [ENG-03, ENG-04, ENG-05]

must_haves:
  truths:
    - "Nenhuma chamada de rede sai do motor de opções para o processo ou serviço b-mcp — a suíte reprova o import, a URL e o cliente MCP."
    - "O único módulo do app que fala com o hub mydata continua sendo o par `options_provider_mydata` / `candle_provider`; o motor não abre canal paralelo."
    - "Toda leitura nova de cadeia de opções passa pelo lock de orçamento existente: com `mydata_budget.reservar()` devolvendo False, nenhuma requisição sai."
    - "`rastrear()` e `avaliar()` são trocáveis por chamadas ao b-mcp mexendo só no corpo: a suíte reprova qualquer dependência de carteira, sessão, texto ou relógio dentro delas."
    - "O ponto de troca e as suas pré-condições estão registrados num ADR, não só em comentário."
  artifacts:
    - path: "server/tests/test_opcoes_fronteira.py"
      provides: "Guardião estrutural de ENG-03 (sem rede ao b-mcp), ENG-04 (trocabilidade) e ENG-05 (canal único com orçamento)"
      min_lines: 130
    - path: "docs/adr/024-limite-interno-rastrear-avaliar.md"
      provides: "Registro da fronteira interna e do procedimento de troca quando plano-mcp-servico.md for aprovado"
      contains: "rastrear"
      min_lines: 60
  key_links:
    - from: "server/tests/test_opcoes_fronteira.py"
      to: "server/app/opcoes_motor.py"
      via: "análise sintática (ast) dos imports do módulo"
      pattern: "ast\\.parse"
    - from: "server/tests/test_opcoes_fronteira.py"
      to: "server/app/mydata_budget.py"
      via: "monkeypatch de reservar() provando que sem cota nada sai"
      pattern: "reservar"
---

<objective>
Transformar as três invariantes de fronteira da fase — sem rede ao b-mcp
(ENG-03), limite trocável (ENG-04), canal único com orçamento (ENG-05) — em
suíte que reprova a violação, e registrar o ponto de troca num ADR.

Purpose: CONTEXT ENG-04 é explícito — "o planner deve garantir que ela é
estruturalmente verdadeira, não só documentada". Sem este plano, as três
restrições existem apenas como intenção nos comentários dos Planos 02 e 03, e
a primeira pessoa que precisar de um dado a mais dentro do motor abre um
`httpx.AsyncClient` sem que nada reclame — furando de uma vez o orçamento de
requisições e o princípio 4 do CLAUDE.md (o b-mcp serve dado sintético em
produção).
Output: `server/tests/test_opcoes_fronteira.py` + `docs/adr/024-*.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-motor-de-proposta-arquitetura-interna/15-CONTEXT.md
@CLAUDE.md

@.planning/phases/15-motor-de-proposta-arquitetura-interna/15-01-SUMMARY.md
@.planning/phases/15-motor-de-proposta-arquitetura-interna/15-02-SUMMARY.md
@.planning/phases/15-motor-de-proposta-arquitetura-interna/15-03-SUMMARY.md
@server/app/options_provider_mydata.py
@server/app/mydata_budget.py
</context>

<interfaces>
<!-- Fatos verificados no código, base dos guardiões. -->

Módulos do app que HOJE importam `mydata_client` (grep em `server/app/*.py`):
  candle_provider.py, options_provider_mydata.py, mydata_budget.py

Módulos do app que HOJE importam `mydata_budget`:
  candle_provider.py, mydata_client.py, options_provider_mydata.py

Caminho do lock (WR-01, PR #28) em `server/app/options_provider_mydata.py`:
  `_gate(n)` (linha 128) — pré-filtro otimista, sem lock
  `_debita()` (linha ~160) — `return mydata_budget.reservar(n)`, o COMMIT
    atômico; devolve False quando outra thread já gastou a cota
  `get_options(ticker, expiration)` (linha 163) — chama `_debita()` ANTES de
    `mydata_client.get_vencimentos` e de novo ANTES de
    `mydata_client.get_options_chain`; `False` devolve `_empty_payload(...)`
    com `providerStatus` "degraded" e NUNCA toca a rede

Assinatura de `mydata_budget.reservar` (`server/app/mydata_budget.py:153`):
  reservar(n: int = 1, now: Optional[datetime] = None) -> bool

Módulos novos desta fase, alvos dos guardiões:
  server/app/opcoes_payoff.py    (Plano 15-01)
  server/app/opcoes_gatilho.py   (Plano 15-02)
  server/app/opcoes_motor.py     (Plano 15-03)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Guardião de fronteira — sem rede ao b-mcp, canal único, orçamento respeitado</name>
  <files>server/tests/test_opcoes_fronteira.py</files>
  <read_first>
    - `server/app/options_provider_mydata.py` linhas 128-263 — `_gate`,
      `_debita` e `get_options`, incluindo os comentários que explicam por que
      a recusa por cota é DURA em opções (D-04 proíbe fallback pro Yahoo) e
      por que a recusa por cota não é cacheada (decisão A-07).
    - `server/app/mydata_budget.py` linhas 134-215 — `pode_gastar`, `debita`,
      `reservar`, `snapshot`, `reset`.
    - `server/tests/test_options_provider_mydata.py` e
      `server/tests/test_mydata_budget.py` — o que JÁ é coberto hoje, para
      este guardião não duplicar teste existente e sim cobrir a invariante
      ESTRUTURAL que falta.
    - `server/app/opcoes_motor.py`, `server/app/opcoes_payoff.py`,
      `server/app/opcoes_gatilho.py` — os módulos que o guardião inspeciona.
  </read_first>
  <action>
    Crie `server/tests/test_opcoes_fronteira.py`. Docstring de topo em PT-BR
    citando Fase 15 / Plano 04 e ENG-03/ENG-05, explicando que este arquivo
    guarda invariantes de ARQUITETURA: ele falha quando alguém abre um caminho
    novo, mesmo que o caminho novo funcione.

    Todos os guardiões estruturais usam `ast.parse` sobre o fonte lido com
    `pathlib.Path`, nunca busca textual: comentário citando "b-mcp",
    "semente.dev" ou "httpx" para explicar a proibição é legítimo e não pode
    reprovar a suíte. Escreva um helper local — algo como
    `_imports(caminho) -> set[str]` devolvendo os módulos raiz de `ast.Import`
    e `ast.ImportFrom`, e `_strings(caminho) -> list[str]` devolvendo as
    `ast.Constant` de tipo str — e reuse nos testes.

    GUARDIÃO A (ENG-03, sem rede) — para cada um dos três módulos novos
    (`opcoes_payoff`, `opcoes_gatilho`, `opcoes_motor`), assevere que os
    imports não contêm nenhum de: `httpx`, `requests`, `urllib`, `urllib3`,
    `socket`, `http`, `aiohttp`, `asyncio`, `subprocess`, `mcp`. E que nenhuma
    constante de string contém "b-mcp", "bmcp", "semente.dev" ou "http://" /
    "https://". A mensagem de falha do assert deve nomear o módulo e o import
    ofensor, para o próximo leitor saber o que fazer.

    GUARDIÃO B (ENG-03, sem cliente MCP) — assevere que nenhum arquivo em
    `server/app/` importa `mcp` e que `mcp` não aparece em
    `server/requirements.txt` nem em `server/requirements-prod.txt`. Subir um
    client MCP stdio é a violação que o CONTEXT nomeia explicitamente.

    GUARDIÃO C (ENG-05, canal único) — colete os módulos de `server/app/*.py`
    cujos imports incluem `mydata_client` e assevere que o conjunto é
    exatamente `{"candle_provider", "options_provider_mydata",
    "mydata_budget"}`. O teste é uma allowlist, não uma denylist: qualquer
    módulo NOVO que passe a falar com o hub reprova e obriga uma decisão
    consciente. Deixe isso escrito no comentário do teste, com a razão — o
    orçamento de requisições do mydata (60/min · 2.000/dia) só é defensável
    enquanto houver um caminho só.

    GUARDIÃO D (ENG-05, orçamento respeitado — teste de COMPORTAMENTO, não
    estrutural) — com `monkeypatch`, force `mydata_budget.reservar` a devolver
    `False` e substitua `mydata_client.get_vencimentos` e
    `mydata_client.get_options_chain` por espiões que registram terem sido
    chamados. Chame `options_provider_mydata.get_options("PETR4")` via
    `asyncio.run` (ou o padrão async já usado em
    `test_options_provider_mydata.py`) e assevere: o payload volta com
    `providerStatus` "degraded", e NENHUM dos dois espiões foi chamado.
    Limpe o cache de módulo antes (`options_provider_mydata._cache.clear()`),
    senão um hit de 300s de outro teste mascara a asserção — e chame
    `mydata_budget.reset()` no teardown para não vazar estado entre testes.

    GUARDIÃO E (ENG-05, o commit é `reservar`, não `debita`) — assevere por
    `ast` que o corpo de `options_provider_mydata._debita` referencia
    `reservar` e NÃO referencia `debita` diretamente. É a regressão exata do
    WR-01 (`debita()` sozinho é check-then-act sem lock, corrigido no PR #28);
    escreva a razão no comentário do teste.
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/test_opcoes_fronteira.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && .venv/bin/python -m pytest tests/test_opcoes_fronteira.py -q` sai 0 com pelo menos 5 testes coletados.
    - `cd server && .venv/bin/python -m pytest tests/test_opcoes_fronteira.py -q -k "rede or httpx or b_mcp or bmcp"` coleta e passa pelo menos 1 teste.
    - `cd server && .venv/bin/python -m pytest tests/test_opcoes_fronteira.py -q -k "orcamento or reservar or cota"` coleta e passa pelo menos 2 testes.
    - `grep -q "ast.parse" server/tests/test_opcoes_fronteira.py` — os guardiões estruturais usam análise sintática, não grep.
    - `grep -q "monkeypatch" server/tests/test_opcoes_fronteira.py` — o guardião D é de comportamento, exercitando o caminho real.
    - Injeção de falha (executar, confirmar que reprova, e DESFAZER): acrescentar temporariamente a linha `import httpx` ao topo de `server/app/opcoes_motor.py` faz `cd server && .venv/bin/python -m pytest tests/test_opcoes_fronteira.py -q` sair != 0; `git checkout -- server/app/opcoes_motor.py` restaura e a suíte volta a sair 0. Registrar o resultado das duas execuções no SUMMARY — um guardião que nunca foi visto falhando não é guardião.
    - `cd server && .venv/bin/python -m pytest tests/ -q` sai 0.
  </acceptance_criteria>
  <done>A suíte reprova import de rede, cliente MCP, URL do b-mcp e canal paralelo ao hub mydata nos módulos de opções, e prova por comportamento que sem cota reservada nenhuma requisição sai.</done>
</task>

<task type="auto">
  <name>Task 2: Guardião de trocabilidade do limite interno + ADR-024</name>
  <files>server/tests/test_opcoes_fronteira.py, docs/adr/024-limite-interno-rastrear-avaliar.md</files>
  <read_first>
    - `server/tests/test_opcoes_fronteira.py` — o estado após a Task 1, para
      reusar os helpers `_imports`/`_strings`.
    - `server/app/opcoes_motor.py` — as assinaturas reais de `rastrear` e
      `avaliar` entregues pelo Plano 15-03.
    - `docs/adr/023-opcoes-lastreadas.md` — o formato de ADR do projeto:
      título `# ADR-0NN: <título>`, bloco Status/Data/Decisor/Base, seção
      `## Contexto`, depois as decisões numeradas.
    - `.planning/phases/15-motor-de-proposta-arquitetura-interna/15-CONTEXT.md`
      seções ENG-03/ENG-04/ENG-05 e `<canonical_refs>` — a fonte do racional.
    - `.planning/notes/opcoes-v2-b-mcp-exploracao.md`, seção "Arquitetura
      decidida (2026-09-02)" — as 5 estratégias avaliadas e por que a B venceu.
  </read_first>
  <action>
    (a) Acrescente a `server/tests/test_opcoes_fronteira.py` o guardião de
    ENG-04 — a trocabilidade tem de ser estrutural.

    Assevere, com `inspect.signature`, que `opcoes_motor.rastrear` tem
    exatamente os parâmetros `(cadeia, filtros)` e `opcoes_motor.avaliar`
    exatamente `(pernas)`, nessa ordem. Renomear ou acrescentar parâmetro
    posicional quebra todo chamador na hora da troca pelo b-mcp — é por isso
    que a assinatura está congelada, e a mensagem do assert deve dizer isso.

    Assevere, por `ast`, que os imports de `opcoes_motor` estão contidos na
    allowlist `{"options_quant", "opcoes_payoff", "typing", "__future__"}`.
    Um import fora dela significa que o motor passou a depender de estado do
    Boris (carteira, sessão, texto, relógio) e a troca do corpo por uma
    chamada de rede deixou de ser possível sem redesenho.

    Assevere que `opcoes_motor` não referencia os nomes `datetime`, `date`,
    `time`, `now`, `today` — relógio dentro do limite tornaria a função
    não-determinística e não-reproduzível pelo lado remoto.

    (b) Crie `docs/adr/024-limite-interno-rastrear-avaliar.md`, seguindo o
    formato de `docs/adr/023-opcoes-lastreadas.md`:
      - Cabeçalho: `# ADR-024: Limite interno rastrear()/avaliar() — o motor
        de opções sem dependência de runtime do b-mcp`, Status "Aceito", Data
        2026-09-02, Decisor "Alex (via `/gsd-explore` + discussão de
        arquitetura, 2026-09-02)", Base: ROADMAP v1.4 Fase 15, `15-CONTEXT.md`,
        `.planning/notes/opcoes-v2-b-mcp-exploracao.md`, ADR-004 (contrato de
        cadeia), ADR-020 (centralização no mydata), ADR-023 (opções
        lastreadas).
      - `## Contexto`: o b-mcp existe como repo separado (`~/dev/MCP`) com
        aritmética de payoff e DSL de setups próprias; em produção ele serve
        dado sintético/fixture, então depender dele em runtime violaria o
        princípio 4 do CLAUDE.md. A Estratégia B (portar a matemática, manter
        a fronteira) foi a escolhida entre as 5 avaliadas.
      - `## Decisão 1 — o limite`: `rastrear(cadeia, filtros)` e
        `avaliar(pernas)` em `server/app/opcoes_motor.py`, equivalentes
        internos de `find_tradable_options` e `evaluate_option_structure`;
        vocabulário do ADR-004 (prêmio, strike, delta, tipo CALL/PUT).
      - `## Decisão 2 — o que foi portado e o que não foi`: portada a
        aritmética de `calculos.py` (`opcoes_payoff.py`); NÃO portada a DSL
        de setups (`setups.py` do b-mcp) por ADR-016/017 — sinal ingênuo de
        confluência já foi medido perdendo dinheiro; NÃO adotado o critério
        por delta de `estruturas.py`, porque a régua do Boris é
        `liquidity_score >= 40` + strike extremo, já em produção desde a
        Fase 14.
      - `## Decisão 3 — o canal de dado`: leitura exclusivamente por
        `mydata_client.py`, sempre atrás de `mydata_budget.reservar()`
        (WR-01/PR #28); nenhum canal paralelo.
      - `## Procedimento de troca`: os passos exatos para quando
        `~/dev/MCP/docs/plano-mcp-servico.md` for aprovado — trocar o CORPO
        das duas funções, manter a assinatura, manter as chaves de retorno,
        e quais guardiões de `test_opcoes_fronteira.py` precisam ser
        reescritos (os de proibição de rede) versus quais continuam valendo
        (assinatura, chaves de retorno, canal com orçamento).
      - `## Consequências`: o que se ganha (motor de N pernas próprio,
        testável offline, sem dependência de disponibilidade externa) e o que
        se paga (a matemática vive em dois repos e pode divergir; mitigação:
        `opcoes_payoff.py` tem suíte própria e a fonte externa é read-only).

    Não altere nenhum arquivo em `~/dev/MCP` — é repo separado e só de
    leitura para esta fase.
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/test_opcoes_fronteira.py -q && test -f docs/adr/024-limite-interno-rastrear-avaliar.md</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && .venv/bin/python -m pytest tests/test_opcoes_fronteira.py -q` sai 0 e coleta pelo menos 3 testes a mais que ao fim da Task 1.
    - `cd server && .venv/bin/python -c "import inspect; from app import opcoes_motor as m; print(list(inspect.signature(m.rastrear).parameters), list(inspect.signature(m.avaliar).parameters))"` imprime `['cadeia', 'filtros'] ['pernas']`.
    - `cd server && .venv/bin/python -m pytest tests/test_opcoes_fronteira.py -q -k "assinatura or trocavel or limite"` coleta e passa pelo menos 1 teste.
    - `test -f docs/adr/024-limite-interno-rastrear-avaliar.md` sai 0 e o arquivo contém as seções `## Contexto`, `## Decisão 1`, `## Decisão 2`, `## Decisão 3`, `## Procedimento de troca` e `## Consequências` (`grep -c '^## ' docs/adr/024-*.md` >= 6).
    - `grep -q "find_tradable_options" docs/adr/024-*.md && grep -q "evaluate_option_structure" docs/adr/024-*.md` — o ADR nomeia as duas tools remotas que as funções substituem.
    - `grep -q "mydata_budget" docs/adr/024-*.md` — o ADR registra o canal com orçamento (ENG-05).
    - `git -C ~/dev/MCP status --porcelain | grep -q .; test $? -ne 0` — o repo externo não foi modificado.
    - `bash scripts/executar.sh --testes` sai 0 — suíte canônica COMPLETA (pytest do backend + `web/tests/*.mjs`), conforme CLAUDE.md.
  </acceptance_criteria>
  <done>A suíte congela as assinaturas de `rastrear`/`avaliar` e a allowlist de imports do motor; o ADR-024 registra o limite, o que foi e não foi portado, o canal com orçamento e o procedimento de troca.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo do Boris → serviço b-mcp (`b-mcp.semente.dev`) | Fronteira de rede que deve permanecer FECHADA nesta fase. O serviço serve dado sintético/fixture em produção; consumi-lo produziria número financeiro inventado. |
| repo do Boris → hub mydata (`mydata.semente.dev`) | Fronteira de rede ABERTA e legítima, mas com orçamento finito (60/min · 2.000/dia) protegido por lock. |
| desenvolvedor futuro → módulos de opções | Quem precisar de um dado a mais dentro do motor é o vetor real: um `httpx` novo fura orçamento e princípio de dado de uma vez. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-15 | Spoofing | dado sintético do b-mcp passando por dado real de mercado | mitigate | Guardião A + B (Task 1): imports de rede, cliente `mcp` e strings "b-mcp"/"semente.dev"/URL reprovam a suíte nos três módulos novos. |
| T-15-16 | Denial of service | canal paralelo esgotando a cota do mydata | mitigate | Guardião C: allowlist dos módulos que importam `mydata_client` congelada em `{candle_provider, options_provider_mydata, mydata_budget}`. Guardião D prova por comportamento que `reservar()` False impede a requisição. |
| T-15-17 | Tampering | regressão do WR-01 (`debita` no lugar de `reservar`) | mitigate | Guardião E: `ast` sobre o corpo de `options_provider_mydata._debita` exige a referência a `reservar` e proíbe `debita` direto — check-then-act sem lock é a condição de corrida já fechada no PR #28. |
| T-15-18 | Elevation of privilege | motor ganhando acesso a carteira/sessão e virando caminho de escrita | mitigate | Guardião de ENG-04 (Task 2): allowlist de imports de `opcoes_motor` limitada a `{options_quant, opcoes_payoff, typing, __future__}`; `store`/`db`/`auth` reprovam. |
| T-15-19 | Repudiation | troca futura pelo b-mcp sem registro do que muda | mitigate | ADR-024 documenta o procedimento de troca e quais guardiões são reescritos versus quais permanecem — a decisão fica auditável fora do histórico de chat. |
| T-15-20 | Information disclosure | token do mydata vazando em mensagem de erro do motor | accept | Já mitigado a montante em `mydata_client._fetch_json` (T-09-01: nunca incluir o valor do token na mensagem) e os módulos novos não têm acesso ao token por construção — a allowlist de imports proíbe `mydata_client`. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo — o guardião B inclusive ASSEVERA que `mcp` não aparece em `server/requirements.txt` nem em `requirements-prod.txt`. Verificável: `git diff --stat server/requirements*.txt` vazio. |
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` sai 0 — a suíte canônica do projeto
  (pytest do backend + `web/tests/*.mjs`). `scripts/test.sh` sozinho é meia
  baseline e não conta como validação (CLAUDE.md).
- Injeção de falha registrada no SUMMARY: com `import httpx` acrescentado
  temporariamente a `server/app/opcoes_motor.py`, `test_opcoes_fronteira.py`
  reprova; após `git checkout --`, passa.
- `git diff --stat server/requirements.txt server/requirements-prod.txt` vazio.
- `git -C ~/dev/MCP status --porcelain` vazio — repo externo intocado.
- Nenhum arquivo de `web/` foi tocado nesta fase inteira: esta é uma fase
  backend-only, sem UI (Fase 18) e sem fluxo de aceite (Fase 17), portanto
  não há bump/`publicar-web.sh` a fazer. Um deploy só-backend, se e quando o
  Alex quiser expor isto, exige bump manual de `SERVER_BUILD_ID` — mas nada
  desta fase está exposto em rota, então nem deploy é necessário ainda.
</verification>

<success_criteria>
- A suíte falha se qualquer módulo de opções importar rede, cliente MCP ou
  referenciar o b-mcp por URL/string.
- A suíte falha se um módulo novo passar a importar `mydata_client`.
- A suíte prova, por comportamento, que `mydata_budget.reservar()` devolvendo
  False impede qualquer requisição ao hub.
- A suíte congela as assinaturas `rastrear(cadeia, filtros)` e
  `avaliar(pernas)` e a allowlist de imports de `opcoes_motor`.
- `docs/adr/024-limite-interno-rastrear-avaliar.md` registra a fronteira, o
  que foi e não foi portado, o canal com orçamento e o procedimento de troca.
- `bash scripts/executar.sh --testes` passa inteiro.
</success_criteria>

<output>
Create `.planning/phases/15-motor-de-proposta-arquitetura-interna/15-04-SUMMARY.md` when done
</output>
