---
phase: 29-opcao-a-descoberto-flag-opt-in
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/defaults.py
  - server/app/store.py
  - server/app/main.py
  - server/tests/test_agent_options.py
  - server/tests/test_automacao.py
  - server/tests/test_opcoes_lastreadas_vencimento.py
  - server/tests/test_opcao_descoberto_gate.py
autonomous: true
requirements: [SC-1, SC-2, SC-4]
must_haves:
  truths:
    - "Conta nova nasce com `permitirOpcaoADescoberto = False` — nenhuma conta existente é migrada para ligado"
    - "Conta legada cujo `config` nem tem a chave é tratada como DESLIGADA (fail-closed), nunca como ligada"
    - "`store.buy_option` recusa abrir posição a seco com o flag desligado, gravando rejeição no histórico e sem mover caixa"
    - "`POST /api/options/buy` com o flag desligado devolve 400 com a mensagem do motor — nunca 500, nunca 200"
    - "`store.sell_option` executa normalmente com o flag desligado, inclusive numa posição aberta quando ele estava ligado"
    - "`set_config` recusa ligar o flag sem `descobertoTermo` aceito (versão + timestamp) no config ou no MESMO patch"
  artifacts:
    - path: "server/app/defaults.py"
      provides: "defaults `permitirOpcaoADescoberto: False` e `descobertoTermo: None` em config"
      contains: "permitirOpcaoADescoberto"
    - path: "server/app/store.py"
      provides: "backfill em ensure_defaults, regra de aceite em set_config, gate em buy_option, constante MOTIVO_DESCOBERTO_DESLIGADO"
      contains: "MOTIVO_DESCOBERTO_DESLIGADO"
    - path: "server/app/main.py"
      provides: "tradução ValueError -> HTTPException(400) na rota /api/options/buy"
      contains: "except ValueError"
    - path: "server/tests/test_opcao_descoberto_gate.py"
      provides: "guardião do gate: default off, fail-closed, rejeição registrada, sell_option nunca gateado, rota 400"
      min_lines: 120
  key_links:
    - from: "server/app/store.py buy_option"
      to: "config.permitirOpcaoADescoberto"
      via: "store.get(conn, 'config', user_id=user_id)"
      pattern: "permitirOpcaoADescoberto"
    - from: "server/app/main.py /api/options/buy"
      to: "store.buy_option ValueError"
      via: "except ValueError -> HTTPException(400, str(e))"
      pattern: "except ValueError"
    - from: "server/app/store.py set_config"
      to: "config.descobertoTermo"
      via: "regra de aceite espelhada de operadorTermo/appMode"
      pattern: "descobertoTermo"
---

<objective>
Construir, no backend, os dois campos de config do flag de opção a descoberto e
o gate determinístico que recusa abrir posição a seco sem ele. Lastro
obrigatório passa a ser o padrão real do motor, não uma intenção documentada.

Purpose: hoje `store.buy_option` compra um contrato de opção sem checar lastro
nem flag nenhum — o caminho "a seco" está em produção sem gate (29-CONTEXT.md,
item 1 do inventário). Este plano fecha exatamente esse buraco, no lugar onde
nenhum atalho de cliente passa por cima: dentro do motor.

Output: `permitirOpcaoADescoberto` + `descobertoTermo` em `config`, gate em
`buy_option`, tradução do `ValueError` para 400 na rota, e um guardião novo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/29-opcao-a-descoberto-flag-opt-in/29-CONTEXT.md

NÃO leia `.planning/STATE.md` — nada nele é necessário para este plano. O
29-CONTEXT.md e as citações de linha exata abaixo são a fonte de verdade.

NÃO leia `server/app/store.py` inteiro (1300+ linhas) nem
`server/app/main.py` inteiro. Cada task lista os intervalos exatos.

**Baseline da suíte canônica, medida em 2026-09-13 no início desta fase**
(`bash scripts/executar.sh --testes`, exit 0):
- pytest: **2780 passed, 5 skipped, 3 xfailed**
- web: **144 arquivos `web/tests/*.mjs`, todos `[OK]`**

Qualquer número de `passed` MENOR que 2780 (descontados os testes novos que
este plano adiciona) é regressão e trava a entrega.

<interfaces>
<!-- Contratos que o executor usa direto. Não explore o codebase para achá-los. -->

De `server/app/store.py` (assinaturas já existentes, NÃO mudam):
```python
def get(conn, key, user_id=None)                      # leitura de kv por escopo
def set_config(conn, patch: dict, user_id=None) -> dict   # store.py:192
def registrar_rejeicao(conn, tipo: str, t: str, qty, price, motivo: str,
                       user_id=None, origem: str = "manual") -> dict   # store.py:738
#   tipo ∈ ("COMPRA", "VENDA") — qualquer outro levanta ValueError
#   motivo é truncado em 200 chars; price None é preservado como None (nunca 0.0)
#   NÃO toca cash/positions/pendingOrders
def buy_option(conn, contract: dict, qty: int, price: float, user_id=None,
               meta=None, origem: str = "manual") -> None   # store.py:774
def sell_option(conn, contract_id: str, price: float, user_id=None, qty=None,
                motivo: str = "manual", origem: str = "manual")   # store.py:829
```

Shape do registro de consentimento (espelho EXATO de `operadorTermo`,
`defaults.py:264` / `store.py:276-281`):
```python
{"aceitoEm": "<ISO-8601>", "versao": "<str>"}   # aceitoEm[:40], versao[:10]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Campos de config do flag + regra de aceite do termo</name>

  <files>server/app/defaults.py, server/app/store.py</files>

  <read_first>
  - `server/app/defaults.py:255-275` — bloco "FASE 7 (F7.1) — Modo Operador" dentro do dict `config`; `operadorTermo: None` está na linha 264. É onde os dois campos novos entram.
  - `server/app/store.py:90-105` — cadeia de `cfg.setdefault(...)` em `ensure_defaults`; `cfg.setdefault("operadorTermo", None)` está na linha 98.
  - `server/app/store.py:272-285` — bloco de `set_config` que sanea `operadorTermo` e aplica a regra "appMode='operador' EXIGE termo". É o padrão a espelhar, linha por linha.
  </read_first>

  <action>
  Decisão D3 do 29-CONTEXT.md, fechada aqui: **dois campos**, espelhando
  `appMode`/`operadorTermo` — booleano do flag + registro versionado do aceite.
  Nomes canônicos, usados sem variação em todo o resto da fase:
  - `permitirOpcaoADescoberto` — booleano, default `False`
  - `descobertoTermo` — `{"aceitoEm", "versao"}` ou `None`, default `None`

  **1a. `server/app/defaults.py`** — logo depois de `"operadorTermo": None,`
  (linha 264), no MESMO dict `config`, adicione as duas chaves com comentário
  curto nomeando a fase e a razão (opção a descoberto exige opt-in explícito;
  lastro obrigatório é o padrão). Ordem: o booleano primeiro, o termo depois,
  para casar com a leitura de `appMode`/`operadorTermo` acima.

  **1b. `server/app/store.py` `ensure_defaults`** — logo depois de
  `cfg.setdefault("operadorTermo", None)` (linha 98), adicione:
  `cfg.setdefault("permitirOpcaoADescoberto", False)` e
  `cfg.setdefault("descobertoTermo", None)`. É backfill de doc antigo — conta
  legada ganha o campo DESLIGADO, nunca ligado (SC-1).

  **1c. `server/app/store.py` `set_config`** — depois do bloco de `appMode`
  (que termina antes do bloco `risco`/`vozAtiva`; localize o fim exato lendo
  o span citado), adicione o bloco novo, espelho ESTRUTURAL do de
  `operadorTermo`/`appMode`:

  - se `patch["descobertoTermo"]` é `dict` E tem `aceitoEm` E tem `versao`,
    grava `cfg["descobertoTermo"] = {"aceitoEm": str(...)[:40], "versao": str(...)[:10]}`
  - se `"permitirOpcaoADescoberto" in patch`:
    - se o valor é verdadeiro E `cfg.get("descobertoTermo")` NÃO é `dict`:
      não faz nada (o flag NÃO liga; silencioso e seguro, exatamente como
      `appMode="operador"` sem termo — `store.py:281-282`)
    - caso contrário: `cfg["permitirOpcaoADescoberto"] = bool(patch[...])`

  Duas propriedades que o comentário no código deve declarar por escrito,
  porque são decisão e não acidente:
  1. **Desligar é sempre livre** — não exige termo, não dispara migração
     nenhuma (nada em `agent`/`positions` depende deste flag). Espelha
     "voltar ao Estudo é livre" do Modo Operador.
  2. **`descobertoTermo` NÃO é apagado ao desligar** — religar não pede
     releitura do termo, igual a `operadorTermo`.

  NÃO toque em `buy_option`/`sell_option` nesta task (é a Task 2).
  </action>

  <verify>
    <automated>cd /Users/acamerini/dev/bolsia/b3-agente && bash scripts/executar.sh --testes > /tmp/f29-01-t1.log 2>&1; echo "EXIT=$?"; grep -E "passed|failed|error|\[X\]" /tmp/f29-01-t1.log | tail -8</automated>
  </verify>

  <acceptance_criteria>
  - `grep -c "permitirOpcaoADescoberto" server/app/defaults.py` >= 1 e `grep -c "descobertoTermo" server/app/defaults.py` >= 1
  - `grep -v '^ *#' server/app/store.py | grep -c 'setdefault("permitirOpcaoADescoberto", False)'` == 1
  - Comportamento (verificável num REPL ou pelo teste da Task 3): `store.ensure_defaults(conn)` num DB novo faz `store.get(conn, "config")["permitirOpcaoADescoberto"] is False` e `[...]["descobertoTermo"] is None`
  - Comportamento: `set_config(conn, {"permitirOpcaoADescoberto": True})` SEM termo no config deixa o valor `False`
  - Comportamento: `set_config(conn, {"descobertoTermo": {"aceitoEm": "2026-09-13T00:00:00Z", "versao": "1.0"}, "permitirOpcaoADescoberto": True})` no MESMO patch resulta em `True`
  - Comportamento: com o flag `True`, `set_config(conn, {"permitirOpcaoADescoberto": False})` resulta em `False` e `descobertoTermo` continua sendo `dict`
  - Suíte canônica: pytest >= 2780 passed, 0 failed; todos os 144 `.mjs` `[OK]`; exit 0
  </acceptance_criteria>

  <done>Os dois campos existem nos defaults e no backfill, `set_config` aceita o termo e recusa ligar o flag sem ele, e a suíte canônica segue verde na baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Gate em buy_option, tradução 400 na rota, e reparo dos testes que compram a seco</name>

  <files>server/app/store.py, server/app/main.py, server/tests/test_agent_options.py, server/tests/test_automacao.py, server/tests/test_opcoes_lastreadas_vencimento.py</files>

  <read_first>
  - `server/app/store.py:774-816` — `buy_option` inteira. O gate entra aqui.
  - `server/app/store.py:829-862` — `sell_option`. **Leia para saber o que NÃO tocar**: nenhuma linha dela muda nesta fase.
  - `server/app/store.py:919-945` — porta de lastro de `abrir_call_coberta`: `registrar_rejeicao(...)` seguido de `raise ValueError(<mesma mensagem>)`. É o padrão exato a espelhar.
  - `server/app/main.py:3015-3063` — rota `POST /api/options/buy`; a chamada a `store.buy_option` está na linha 3060.
  - `server/app/main.py:3400-3412` — `try: store.abrir_call_coberta(...) / except ValueError as e: raise HTTPException(400, str(e))`. É a tradução a espelhar.
  - `server/app/main.py:99-107` — handler global de exceção: qualquer `Exception` não tratada vira **500 com o texto cru**. Sem a tradução desta task, o `ValueError` do gate vazaria como 500 (é o mesmo vazamento que a auditoria A-00b fechou em `/api/buy`).
  - `server/tests/test_agent_options.py:74-112` — 4 chamadas diretas a `store.buy_option` (linhas 78, 87, 98, 108).
  - `server/tests/test_automacao.py:60-80` — 2 chamadas (linhas 65, 76).
  - `server/tests/test_opcoes_lastreadas_vencimento.py:105-115` e `:218-228` — 2 chamadas (linhas 110, 223).
  </read_first>

  <action>
  **2a. Constante da mensagem, em `server/app/store.py`.** Declare no nível do
  módulo, perto do cabeçalho da seção `optionPositions` (o comentário em
  `store.py:773`), uma constante única:

  `MOTIVO_DESCOBERTO_DESLIGADO` = `"Operar opções a descoberto está desligado — ligue o flag em Preferências → Operar opções a descoberto. Lastro obrigatório é o padrão desta conta."`

  Esta string é **fonte única** e será espelhada byte a byte em
  `web/src/persistence.js` pelo plano 29-02 (um guardião compara os dois
  arquivos). Ela cabe no truncamento de 200 chars de `registrar_rejeicao`.
  Nenhuma variante, nenhuma segunda redação da mensagem em outro lugar.

  **2b. O gate, dentro de `buy_option`.** Insira imediatamente depois de
  `cid = contract.get("id")` (hoje `store.py:784`) e ANTES de
  `existing = next(...)` — ou seja, antes de qualquer escrita em `opts`,
  `cash` ou `history`:

  - leia `cfg = get(conn, "config", user_id=user_id) or {}`
  - se `not cfg.get("permitirOpcaoADescoberto")`:
    - `registrar_rejeicao(conn, "COMPRA", cid, qty, price, MOTIVO_DESCOBERTO_DESLIGADO, user_id=user_id, origem=origem)`
    - `raise ValueError(MOTIVO_DESCOBERTO_DESLIGADO)`

  Três decisões a declarar em comentário no código, porque cada uma é
  deliberada e um leitor futuro vai querer o motivo:

  1. **Fail-closed por ausência**: `cfg.get(...)` devolve `None` para conta
     legada que ainda não passou pelo backfill da Task 1 — `None` é falsy e o
     gate BARRA. Ausência da chave nunca significa "liberado" (SC-1, nenhuma
     migração silenciosa para ligado).
  2. **Sem `ORDER_LOCK`** (ao contrário de `abrir_call_coberta`,
     `store.py:924`): a trava lá existe porque `qty_livre` é um recurso
     CONSUMÍVEL e duas requisições concorrentes poderiam ler o mesmo saldo
     livre antes de qualquer uma travar. Um flag booleano de config não é
     consumível — não há lost-update a fechar, e pegar a trava aqui mudaria a
     concorrência de uma função que nunca a teve.
  3. **`registrar_rejeicao` roda também para `user_id=None`**, exatamente como
     `abrir_call_coberta` (`store.py:926`, `933`, `938`) já faz — a rejeição de
     escopo anônimo cai no balde kv compartilhado. Espelhar o irmão direto vale
     mais que otimizar o caminho anônimo, que o CLAUDE.md já declara legado
     ("login obrigatório").

  **`sell_option` NÃO recebe gate nenhum.** Escreva isso como comentário de uma
  linha no topo de `sell_option`, nomeando a razão (guardrail do CLAUDE.md,
  "Stop/alvo nunca são vetados" — impedir alguém de SAIR de uma posição a seco
  que já tem seria pior que não ter gate) e o guardião que protege a decisão.

  **2c. Tradução na rota, `server/app/main.py:3060`.** Envolva a chamada
  `store.buy_option(...)` em `try/except ValueError as e: raise HTTPException(400, str(e))`,
  idêntico a `main.py:3405-3410`. Sem isto o gate devolve 500 com texto cru
  (handler global, `main.py:99`), que é o vazamento que a auditoria A-00b já
  fechou em `/api/buy` — não reabra a classe.

  **2d. Reparar os 8 call-sites de teste que a Task 2 quebra.** As 3 suítes
  listadas no `read_first` compram a seco direto no motor e passam a receber
  `ValueError`. Em CADA arquivo, adicione um helper local (não um conftest —
  o padrão da casa é helper por arquivo, ver `test_lastro_trava.py:23-36`):

  ```python
  def _liberar_descoberto(conn, user_id=None):
      """Fase 29: `buy_option` passou a exigir o flag opt-in. Estes testes são
      sobre a ARITMÉTICA da compra a seco, não sobre o gate — o gate tem
      guardião próprio em test_opcao_descoberto_gate.py."""
      store.set_config(conn, {"descobertoTermo": {"aceitoEm": "2026-09-13T00:00:00Z", "versao": "1.0"},
                              "permitirOpcaoADescoberto": True}, user_id=user_id)
  ```

  Chame-o antes de cada `store.buy_option(...)`, com o MESMO `user_id` da
  chamada (atenção: `test_automacao.py:65,76` usa `user_id="u1"`). Não altere
  nenhuma asserção existente — se uma asserção mudar de valor, pare e
  reporte: significa que o gate está vazando para além da abertura a seco.
  </action>

  <verify>
    <automated>cd /Users/acamerini/dev/bolsia/b3-agente && bash scripts/executar.sh --testes > /tmp/f29-01-t2.log 2>&1; echo "EXIT=$?"; grep -E "passed|failed|error|\[X\]" /tmp/f29-01-t2.log | tail -8</automated>
  </verify>

  <acceptance_criteria>
  - Source: `grep -c 'MOTIVO_DESCOBERTO_DESLIGADO' server/app/store.py` >= 3 (declaração + rejeição + raise)
  - Source: `grep -v '^ *#' server/app/main.py | grep -c 'except ValueError'` aumentou em 1 em relação ao HEAD anterior
  - Source assertion (a decisão de SC-4, verificável sem rodar nada): o corpo de `sell_option` (`server/app/store.py`, da linha `def sell_option` até a linha `def close_option_vencida`) NÃO contém a substring `permitirOpcaoADescoberto`
  - Comportamento: com flag desligado, `store.buy_option(...)` levanta `ValueError` cuja `str()` é exatamente `MOTIVO_DESCOBERTO_DESLIGADO`, `optionPositions` continua `[]`, `cash` não muda, e `history[0]` tem `status == "rejeitada"` e `type == "COMPRA"`
  - Comportamento: com flag desligado e `config` SEM a chave (simule com `db.kv_set(conn, "config", {...sem a chave...})`), o gate BARRA (fail-closed)
  - Comportamento: com flag ligado, `store.buy_option(...)` executa como antes (posição criada, caixa debitado) — nenhuma asserção pré-existente das 3 suítes reparadas mudou de valor
  - Comportamento: `POST /api/options/buy` com Bearer válido e flag desligado devolve **400** (nunca 500, nunca 200) e `r.json()["detail"] == MOTIVO_DESCOBERTO_DESLIGADO`
  - Suíte canônica: pytest >= 2780 passed, 0 failed; 144 `.mjs` `[OK]`; exit 0
  </acceptance_criteria>

  <done>O motor recusa abrir a seco sem o flag, a rota devolve 400 com a mensagem do motor, `sell_option` está provadamente intocada, e as 3 suítes que compravam a seco foram reparadas sem mudar asserção nenhuma.</done>
</task>

<task type="auto">
  <name>Task 3: Guardião do gate — default off, fail-closed, e a sequência que prova que fechar nunca é barrado</name>

  <files>server/tests/test_opcao_descoberto_gate.py</files>

  <read_first>
  - `server/tests/test_lastro_trava.py:1-40` — cabeçalho de intenção + `_fresh_db()` com SQLite temporário por teste. É o template de teste de MOTOR desta casa ("sem conftest").
  - `server/tests/test_fase5_rejeicao_rotas.py:109-160` — `_client_isolado(monkeypatch)`, `_registrar(client, email)` e o fixture autouse `_isola_app_main`. Template de teste de ROTA.
  - `server/tests/test_fase5_rejeicao_rotas.py:390-398` — `_chain_opcao(symbol, price)`: cadeia sintética ADR-004 com UM contrato negociável (`volume` 5000 / `openInterest` 3000, para não bater no gate de liquidez).
  - `server/tests/test_fase5_rejeicao_rotas.py:446-473` — exemplo completo de teste de rejeição em `/api/options/buy` com `monkeypatch.setattr(m.options_provider, "get_options", ...)` e asserção de que a rejeição não moveu caixa.
  </read_first>

  <action>
  Crie `server/tests/test_opcao_descoberto_gate.py`. Cabeçalho de intenção no
  padrão da casa (docstring de módulo nomeando o que o arquivo PROTEGE e por
  quê), citando a Fase 29 e os quatro critérios que ele guarda (SC-1, SC-2,
  SC-4 e o fail-closed). Reuse `_fresh_db()` no formato de
  `test_lastro_trava.py` e o `_client_isolado`/`_registrar`/`_chain_opcao` no
  formato de `test_fase5_rejeicao_rotas.py` (copie os helpers para este
  arquivo — a casa não compartilha conftest).

  Testes obrigatórios, um por comportamento, nomes descritivos em snake_case:

  **Grupo A — default e fail-closed (SC-1)**
  1. `test_conta_nova_nasce_com_flag_desligado` — `ensure_defaults` num DB
     novo: `permitirOpcaoADescoberto is False` e `descobertoTermo is None`.
  2. `test_config_sem_a_chave_e_tratado_como_desligado` — escreva um `config`
     legado via `db.kv_set` SEM as duas chaves, chame `buy_option`, espere
     `ValueError`. Ausência nunca é liberação.
  3. `test_ligar_sem_termo_nao_liga` — `set_config({"permitirOpcaoADescoberto": True})`
     sem termo deixa `False`.
  4. `test_termo_no_mesmo_patch_liga` — termo + flag no MESMO patch liga.
  5. `test_desligar_e_livre_e_preserva_o_termo` — desligar não exige termo e
     `descobertoTermo` continua `dict` depois (religar não pede releitura).

  **Grupo B — o gate na compra (SC-2)**
  6. `test_buy_option_com_flag_desligado_levanta_e_nao_move_nada` — asserte as
     QUATRO coisas: `ValueError` com a mensagem exata
     `store.MOTIVO_DESCOBERTO_DESLIGADO`; `optionPositions == []`; `cash`
     idêntico ao de antes; `history[0]["status"] == "rejeitada"` com
     `type == "COMPRA"` e `motivo == store.MOTIVO_DESCOBERTO_DESLIGADO`.
  7. `test_buy_option_com_flag_ligado_executa_como_antes` — posição criada,
     caixa debitado em `qty * price`, `history[0]["status"]` ausente
     (execução não é rejeição).

  **Grupo C — fechar NUNCA é barrado (SC-4, o ponto mais importante)**
  8. `test_sell_option_com_flag_desligado_executa` — ligue o flag, compre a
     seco, DESLIGUE o flag, venda. A venda executa: `pnl` numérico,
     `optionPositions == []`, `cash` creditado. Esta é literalmente a
     sequência que o 29-CONTEXT.md nomeia ("se o flag foi desligado depois de
     aberta").
  9. `test_sell_option_parcial_com_flag_desligado_executa` — mesma sequência,
     com `qty` parcial: a posição remanescente fica com `avg` preservado.
  10. `test_close_option_vencida_com_flag_desligado_executa` — liquidação por
      vencimento (`close_option_vencida`, que delega a `sell_option` com
      `origem="sistema"`) também não é barrada. Expiração é mecânica
      obrigatória, não uma decisão do usuário.
  11. `test_corpo_de_sell_option_nao_menciona_o_flag` — source assertion: leia
      `server/app/store.py`, fatie de `def sell_option` até
      `def close_option_vencida`, asserte que `"permitirOpcaoADescoberto"` NÃO
      aparece nesse trecho. Guardião da DECISÃO, não só do comportamento — se
      alguém um dia gatear o fechamento, este teste cai antes do review.

  **Grupo D — a rota não é atalho (SC-2, superfície HTTP)**
  12. `test_rota_options_buy_flag_desligado_400_com_a_mensagem_do_motor` —
      `_client_isolado` + `_registrar` + `monkeypatch` de
      `m.options_provider.get_options` para `_chain_opcao()`. POST
      `/api/options/buy` com `{"underlying": "PETR4", "contractSymbol": "PETRK30", "qty": 100}`.
      Asserte `status_code == 400`, `status_code != 500` (com mensagem
      "exceção crua nunca chega ao cliente", no estilo de
      `test_fase5_rejeicao_rotas.py:467`), `detail == store.MOTIVO_DESCOBERTO_DESLIGADO`,
      e via `GET /api/state`: `optionPositions == []` e `cash == 10000.0`.
  13. `test_rota_options_sell_nunca_e_400_por_causa_do_flag` — ligue o flag por
      `PUT /api/config` (termo + flag no mesmo corpo), compre pela rota,
      desligue o flag por `PUT /api/config`, venda por `/api/options/sell`:
      `status_code == 200`. Prova a sequência do Grupo C na superfície HTTP,
      não só no motor.

  Não escreva teste de UI aqui (é dos planos 29-02/29-03). Não adicione
  dependência nova — `pytest` + `fastapi.testclient` já estão em
  `server/requirements.txt`.
  </action>

  <verify>
    <automated>cd /Users/acamerini/dev/bolsia/b3-agente && bash scripts/executar.sh --testes > /tmp/f29-01-t3.log 2>&1; echo "EXIT=$?"; grep -E "passed|failed|error|\[X\]" /tmp/f29-01-t3.log | tail -8; python -m pytest server/tests/test_opcao_descoberto_gate.py -q 2>&1 | tail -3</automated>
  </verify>

  <acceptance_criteria>
  - `server/tests/test_opcao_descoberto_gate.py` existe com >= 13 funções `def test_`
  - `python -m pytest server/tests/test_opcao_descoberto_gate.py -q` → 13+ passed, 0 failed
  - O teste 8 (`test_sell_option_com_flag_desligado_executa`) executa a sequência ligar → comprar → desligar → vender e passa
  - O teste 11 é uma source assertion real (lê `server/app/store.py` do disco), não um comentário
  - O teste 12 asserte explicitamente `status_code != 500`
  - Prova negativa exigida: reverta temporariamente o gate da Task 2 (comente as 2 linhas) e confirme que os testes 2, 6 e 12 FALHAM; restaure o gate em seguida. Um guardião que passa com e sem o gate não guarda nada. Registre no SUMMARY que a prova negativa foi feita.
  - Suíte canônica: pytest >= 2793 passed (2780 + 13 novos), 0 failed; 144 `.mjs` `[OK]`; exit 0
  </acceptance_criteria>

  <done>O guardião existe, cobre os quatro critérios, e a prova negativa confirma que ele detecta a remoção do gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente HTTP → `POST /api/options/buy` | corpo não confiável atravessa aqui; qualquer cliente com Bearer token válido pode chamar a rota sem passar pela UI |
| rota FastAPI → `store.buy_option` | fronteira interna que decide onde o gate vive: na rota (contornável por outro chamador) ou no motor (não contornável) |
| `config` em kv → gate | o flag é dado persistido por escopo; ausência da chave é um estado real (conta legada), não um erro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Elevation of Privilege | `POST /api/options/buy` (`server/app/main.py:3015`) — atalho de cliente que ignora `OpcoesCamada` | mitigate | O gate vive DENTRO de `store.buy_option` (`store.py:774`), não na rota nem no front (Task 2b). Qualquer chamador — rota, script, futuro MCP — atravessa a mesma checagem. Guardião: `test_rota_options_buy_flag_desligado_400_com_a_mensagem_do_motor`. |
| T-29-02 | Denial of Service (usuário trancado na própria posição) | `store.sell_option` (`store.py:829`) | mitigate | `sell_option` e `close_option_vencida` NÃO recebem gate. Provado por comportamento (testes 8, 9, 10, 13 da Task 3, incluindo a sequência ligar→abrir→desligar→fechar) E por source assertion (teste 11, que asserte a AUSÊNCIA da chave no corpo da função). |
| T-29-03 | Tampering (grandfathering silencioso para ligado) | `store.ensure_defaults` / `store.set_config` | mitigate | Ausência da chave é falsy → gate barra (fail-closed, Task 2b decisão 1); `ensure_defaults` faz backfill com `False`; `set_config` recusa ligar sem `descobertoTermo`. Guardiões: testes 1, 2, 3 da Task 3. |
| T-29-04 | Information Disclosure / Repudiation | `registrar_rejeicao` com `user_id=None` (escopo anônimo) | accept | A rejeição de escopo anônimo entra no balde kv compartilhado entre todos os anônimos — mesma exposição que `abrir_call_coberta` (`store.py:926`) já tem hoje. Aceito por consistência com o irmão direto e porque o CLAUDE.md declara login obrigatório (o caminho anônimo é legado). Divergir criaria dois comportamentos para a mesma classe de gate. |
| T-29-05 | Tampering | `ValueError` do gate vazando como 500 com texto cru (handler global, `main.py:99`) | mitigate | Tradução explícita `except ValueError → HTTPException(400, str(e))` na rota (Task 2c), espelhando `main.py:3405-3410`. Asserção `status_code != 500` no teste 12. |
| T-29-SC | Tampering (supply chain) | instalação de pacote npm/pip/cargo | mitigate | **Nenhuma dependência nova nesta fase.** O gate usa só stdlib + código do próprio repo; os testes usam `pytest` e `fastapi.testclient`, já presentes em `server/requirements.txt`. Não há pacote a auditar, portanto não há checkpoint de legitimidade. Se o executor concluir que precisa de um pacote novo, PARE e reporte — isso muda o modelo de ameaça da fase. |
</threat_model>

<verification>
1. `bash scripts/executar.sh --testes` (fora do sandbox) — pytest >= 2793 passed, 0 failed; 144 `.mjs` `[OK]`; exit 0.
2. `python -m pytest server/tests/test_opcao_descoberto_gate.py -q` — 13+ passed.
3. Prova negativa da Task 3 executada e registrada no SUMMARY.
4. `git diff --stat` mostra APENAS os 7 arquivos de `files_modified`. Nenhuma linha em `web/`, em `server/app/agent.py`, ou em `web/src/opcoes/` — fence D2 do 29-CONTEXT.md.
5. Nenhum `bump.sh`/`publicar-web.sh`/push a `origin`: publicação é passo humano separado, fora do escopo desta fase (mesma convenção da Fase 28).
</verification>

<success_criteria>
- SC-1: conta nova e conta legada sem a chave são DESLIGADAS; nenhuma migração para ligado.
- SC-2: `buy_option` recusa a seco sem flag, com mensagem apontando Preferências; a rota devolve 400 com a mensagem do motor; a recusa é do backend.
- SC-4: `sell_option`/`close_option_vencida` nunca barrados, provado por comportamento e por source assertion.
- SC-6: suíte canônica sem regressão da baseline 2780/144.
</success_criteria>

<output>
Create `.planning/phases/29-opcao-a-descoberto-flag-opt-in/29-01-SUMMARY.md` when done.
Registre obrigatoriamente: os nomes finais dos dois campos, a string exata de
`MOTIVO_DESCOBERTO_DESLIGADO` (o plano 29-02 vai espelhá-la byte a byte), o
resultado da prova negativa, e os números de `passed` antes/depois.
</output>
