---
phase: 27-aba-opcoes-sobre-a-carteira
plan: 05
type: execute
wave: 4
depends_on: ["27-04"]
files_modified:
  - web/src/opcoes/OpcoesScreen.jsx
  - web/src/opcoes/useOpcoesMcp.js
  - web/src/opcoes/CriarSetup.jsx
  - web/src/copy.js
  - web/tests/test_opcoes_custo_declarado.mjs
  - web/tests/test_opcoes_analisar_ui.mjs
autonomous: false
requirements: [SC-4, D4, G4]
user_setup: []

must_haves:
  truths:
    - "Todo controle que consome cota diz quanto custa, no proprio controle, ANTES do clique"
    - "O numero que o controle mostra e o mesmo que o backend cobra — provado por um teste que le os dois lados"
    - "A leitura paga do ativo sai de um clique, nunca da troca de ativo"
    - "A unica chamada que ainda roda sozinha ao abrir a aba esta declarada na tela e no guardiao, com o custo dela"
  artifacts:
    - path: "web/tests/test_opcoes_custo_declarado.mjs"
      provides: "guardiao que cruza TODO metodo do store que chama o servico contra a declaracao de custo e contra o `_cap_check` do backend"
    - path: "web/src/opcoes/OpcoesScreen.jsx"
      provides: "tabela CUSTO_DA_ACAO (espelho do backend) + rotulo em cada controle + botao de leitura do servico"
      contains: "CUSTO_DA_ACAO"
  key_links:
    - from: "web/src/opcoes/OpcoesScreen.jsx"
      to: "cp.opcoesCustoChamadas / cp.opcoesSemCusto"
      via: "custo declarado no proprio controle"
      pattern: "opcoesCustoChamadas|opcoesSemCusto"
    - from: "web/tests/test_opcoes_custo_declarado.mjs"
      to: "server/app/options_mcp_api.py"
      via: "cruzamento do rotulo do front com o `_cap_check(uid, N)` da rota"
      pattern: "_cap_check"
    - from: "web/src/opcoes/useOpcoesMcp.js"
      to: "store.mcpLeitura"
      via: "acao sob demanda (`abrirLeitura`), fora de qualquer useEffect"
      pattern: "abrirLeitura"
---

<objective>
Fechar a fase pelo criterio 4 do ROADMAP: **nenhum botao da aba gasta cota em
silencio**, e o numero que ele mostra e o mesmo que o backend cobra.

Purpose: sem o rotulo de custo em cada controle, o hibrido do D1 nao se
sustenta — a pessoa evita a tela inteira por medo de gastar, e a leitura de
graca que o 27-03/27-04 construiram nao serve para nada. Hoje o criterio 4 e
falso em sete dos oito controles da aba, e um deles (`mcpLeitura`, custo 3)
nem controle tem: dispara pela troca de ativo.
Output: `CUSTO_DA_ACAO` espelhando o `_cap_check` de cada rota, rotulo em cada
controle, a leitura do servico virando clique explicito, o custo do frescor
declarado no cabecalho, e um guardiao que cruza os DOIS lados.

**Este plano nasceu na revisao de 2026-09-13 (verificacao adversarial).** Ele
era a Task 3 do 27-04; saiu para plano proprio por custo de contexto, depois
que as correcoes C2 e C3 o fizeram crescer (mover `mcpLeitura` para fora do
efeito, criar controle novo, reescrever o guardiao com cruzamento backend x
front sobre treze metodos, tocar `CriarSetup.jsx`). Nada foi reduzido — o
escopo inteiro continua na fase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-aba-opcoes-sobre-a-carteira/27-CONTEXT.md
@.planning/phases/27-aba-opcoes-sobre-a-carteira/27-02-SUMMARY.md
@.planning/phases/27-aba-opcoes-sobre-a-carteira/27-04-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contratos prontos. Nao explorar o repo. -->

**O MAPA COMPLETO — metodo do store x controle na tela x custo real.**
Conferido no fonte em 2026-09-13 (`grep -n "_cap_check(" server/app/options_mcp_api.py`).
E este mapa que a tabela do front espelha e que o guardiao cruza. Nao chute
nenhuma linha; se alguma divergir do fonte na hora de executar, o fonte ganha
e o SUMMARY registra a divergencia.

| metodo (`persistence.js`) | funcao da rota (`options_mcp_api.py`) | `_cap_check` | controle na tela | chave em `CUSTO_DA_ACAO` |
|---|---|---|---|---|
| `mcpLeitura` | `leitura` | **3** | (hoje NENHUM — cria-se nesta task) "Ler no servico" | `leitura` |
| `mcpCadeia` | `cadeia` | 1 | "Ver a cadeia de opcoes" | `cadeia` |
| `mcpOperaveis` | `operaveis` | 1 | "Ver so as opcoes com liquidez" | `operaveis` |
| `mcpProposta` | `proposta` | 1 | "Montar a estrutura" | `proposta` |
| `mcpSetupGrafico` | `setup_grafico` | 1 | "Disparos do setup" | `grafico` |
| `mcpSetupCompilar` | `setup_compilar` | 2 (+1 analise de IA) | "Ver a interpretacao e o ensaio" | `compilar` |
| `mcpSetupConfirmar` | `setup_confirmar` | 2 | "Gravar este setup" | `confirmar` |
| `mcpSetupDesativar` | `setup_desativar` | 1 | "Desativar este setup" | `desativar` |
| `mcpSetupsListar` | `setups_listar` (27-01) | 2 | "Atualizar" (bloco Seus vigias) | `listarVigias` |
| `mcpPossibilidades` | `possibilidades` | **1 + 2xN** | "Comparar os vencimentos" | **fora da tabela** — calculado (`opcoesCustoChamadas(2*N+1)`), ja declarado hoje |
| `mcpStatus` | `status` | **ate 1** | **sem controle** — roda no mount, alimenta o cabecalho de frescor | **fora da tabela** — entra em `SEM_CONTROLE` |
| `mcpVigias` | (rota local em `main.py`) | **0** | sem controle — roda no mount | **proibida na tabela** |
| `opcoesTecnico` | (rota local em `main.py`) | **0** | sem controle — roda na troca de ativo | **proibida na tabela** |

Duas observacoes que o mapa nao pode deixar implicitas:
· `mcpStatus` consome **0 ou 1**: a reserva e 1 e so vira consumo quando a
  chamada sai de verdade (acerto de cache nao gasta — `options_mcp_api.py:1537-1541`).
· `mcpSetupCompilar` tem custo em DOIS eixos: 2 chamadas do cap **e** uma
  analise de IA paga (`consumir_analise()`), que tem cota propria e tela
  propria.

De `web/src/opcoes/useOpcoesMcp.js` (apos o 27-04):
- `useChamadaSobDemanda(tickerRef)` -> `[estado, disparar, limpar]`; o efeito
  de troca de ticker LIMPA os trios e (hoje) ainda dispara `store.mcpLeitura`.
- `leitura`, `recarregarLeitura` (`useCallback` que rechama `mcpLeitura` no
  MESMO ticker, ja existente, usado depois de gravar/desativar setup).
- `PERIODO_DA_LEITURA` (27-04).

De `web/src/opcoes/OpcoesScreen.jsx`:
- `web/src/opcoes/OpcoesScreen.jsx:660-680` — o UNICO controle que hoje declara
  custo (`cp.opcoesCustoChamadas(chamadasPrevistas)`): e o padrao a replicar.
- `N_MAX_VENCIMENTOS` — o precedente de constante espelhada do backend.
- `cp.opcoesSemCusto` (27-04) — o vocabulario unico de "gratis".

De `web/src/opcoes/CriarSetup.jsx`: botao de compilar (`:145-160`), de gravar
(`:390-400`) e de desativar (`:470-485`). O componente recebe TUDO por prop.

De `web/src/copy.js`: `COPY.estudo`/`COPY.operador` com conjunto de chaves
IDENTICO (`web/tests/test_copy_theme.mjs:27`). `opcoesCustoChamadas(n)` e a
forma canonica de declarar custo.

Guardioes que ja olham este codigo: `test_opcoes_analisar_ui.mjs` (secao 8, com
a allowlist `EFEITO_PERMITIDO` que o 27-02 e o 27-04 escreveram),
`test_opcoes_mcp_aba_ui.mjs`, `test_opcoes_criar_setup_ui.mjs`,
`test_vocabulario_opcoes.mjs`, `test_brand_book_v2_tokens.mjs`, e os tres
criados nesta fase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: A leitura do servico vira clique — e o frescor passa a dizer o que custa</name>

  <read_first>
    - `web/src/opcoes/useOpcoesMcp.js:97-131` (o efeito de troca de ticker, que hoje dispara `store.mcpLeitura`) e `:136-175` (`recarregarLeitura`) — as duas portas atuais.
    - `web/tests/test_opcoes_analisar_ui.mjs`, secao 8 (ja reescrita pelo 27-02/27-04) — a allowlist `EFEITO_PERMITIDO` e a lista `METODOS`.
    - `web/src/opcoes/OpcoesScreen.jsx`, bloco "LEITURA DO ATIVO" (`cp.opcoesLeituraTitulo`) e o cabecalho com o chip de frescor/cota.
    - `server/app/options_mcp_api.py:1497-1545` (`status`) — por que o custo dele e 0 OU 1.
  </read_first>

  <files>web/src/opcoes/useOpcoesMcp.js, web/src/opcoes/OpcoesScreen.jsx, web/src/copy.js, web/tests/test_opcoes_analisar_ui.mjs</files>

  <action>
    **(a) `mcpLeitura` sai do `useEffect`.** Hoje escolher um ativo dispara a
    leitura do servico — 3 chamadas — sem nenhum controle dizer isso. Enquanto o
    ticker nascia vazio, o efeito era consequencia indireta de um clique; com o
    bloco "Seus vigias" (27-02), clicar num vigia tambem troca o ticker, e ai a
    pessoa paga 3 por um clique que, na tela, se parece com navegacao. O §3.3 do
    ADR-027 nao fala de `useEffect`, fala de **clique explicito com custo
    declarado** — e navegar nao e pedir a leitura.
    Extrair o corpo que chama `store.mcpLeitura` para um `useCallback`
    `abrirLeitura()`, irmao do `recarregarLeitura` que ja existe (o
    `recarregarLeitura` continua como esta: ele e recarga do MESMO ticker depois
    de gravar/desativar, e nasce de um clique que ja declarou custo).
    O efeito de troca de ticker continua existindo e continua **limpando** o
    trio `leitura` (dado do ativo anterior nao fica na tela) — ele so deixa de
    disparar. Comentario obrigatorio: o efeito apaga, nunca pede.

    **(b) O controle.** Dentro do bloco "LEITURA DO ATIVO", quando ainda nao ha
    `leitura.dados`, mostrar um estado de convite com um botao
    `cp.opcoesLerNoServico` (chave nova) chamando `abrirLeitura`, com o custo no
    proprio controle via `cp.opcoesCustoChamadas(CUSTO_DA_ACAO.leitura)` (a
    tabela e da Task 2 — se esta task rodar antes, use a constante e deixe a
    tabela para a Task 2; nao redigite o `3` solto no JSX). Texto de apoio
    (`cp.opcoesLeituraConvite`, chave nova) dizendo o que a leitura paga traz que
    o bloco interno (gratis, 27-04) nao traz: catalogo de estruturas,
    vencimentos e a avaliacao do dia dos vigias daquele ticker.
    `minHeight: 44px`, tokens existentes, nenhuma cor nova.
    A ordem visual nao muda: o bloco interno (gratis) continua ANTES do bloco do
    servico (pago).

    **(c) O frescor tambem custa — e passa a dizer.** `mcpStatus` roda no mount
    e consome **ate 1** (0 quando o frescor esta em cache). E a unica chamada da
    aba que gasta sem clique, e ela existe por uma razao que nao da para
    remover: o cabecalho precisa dizer a idade do dado com verdade (ADR-027,
    Decisao 8), e um gate de frescor que so aparece depois de um clique nao
    protege nada. A correcao honesta nao e esconder: e **declarar**.
    No cabecalho, ao lado do chip de frescor, uma linha curta
    (`cp.opcoesCustoFrescor`, chave nova) dizendo que abrir a aba consulta o
    frescor do dado e que isso consome ate 1 consulta, so quando o frescor nao
    esta em cache. Registrar no comentario que esta e a UNICA excecao ao
    criterio 4, que ela e pre-existente a esta fase, e por que nao vira clique.
    Todas as chaves novas nos DOIS blocos de `copy.js`, com voz por modo.

    **(d) Guardiao.** Em `test_opcoes_analisar_ui.mjs`, secao 8, com nota datada
    `2026-09-13 (Fase 27)`: **mover `mcpLeitura` de `EFEITO_PERMITIDO` para
    `METODOS`** — no 27-02 ela entrou na allowlist marcada TRANSITORIA,
    justamente esperando esta task; agora ela nao pode mesmo disparar por
    efeito. Apagar a entrada da allowlist e obrigatorio: allowlist com item
    morto e permissao que ninguem revoga. A assercao de que as duas listas sao
    DISJUNTAS (escrita no 27-02) e o que impede a troca pela metade. A
    ancora de sanidade que hoje e
    `efeitos.some(c => c.includes("store.mcpLeitura"))` PRECISA mudar junto,
    senao vira falso: troque por `store.mcpStatus` (que continua dentro de um
    efeito e esta na allowlist) e deixe escrito no comentario por que a ancora
    mudou — sem ela, um fatiador quebrado faria a secao inteira passar por
    vacuidade. Exigir tambem que `abrirLeitura` exista como `useCallback` no
    hook e seja chamado em `OpcoesScreen.jsx`, no mesmo padrao das quatro acoes
    da F3 que a secao 8 ja checa.
  </action>

  <verify>
    <automated>cd /Users/acamerini/dev/borisv2 && node web/tests/test_opcoes_analisar_ui.mjs && node web/tests/test_opcoes_mcp_aba_ui.mjs && node web/tests/test_opcoes_vigias_ui.mjs && node web/tests/test_copy_theme.mjs && node web/tests/test_vocabulario_opcoes.mjs && cd web && npx vite build</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "store.mcpLeitura" web/src/opcoes/useOpcoesMcp.js` devolve 2 (`abrirLeitura` + `recarregarLeitura`), e NENHUMA das duas dentro de `useEffect`.
    - `grep -n "const abrirLeitura = useCallback" web/src/opcoes/useOpcoesMcp.js` casa exatamente uma linha; `grep -n "abrirLeitura" web/src/opcoes/OpcoesScreen.jsx` casa ao menos uma.
    - `node -e "import('./web/src/copy.js').then(m=>{const k=['opcoesLerNoServico','opcoesLeituraConvite','opcoesCustoFrescor'];for(const b of ['estudo','operador'])for(const x of k)if(!(x in m.COPY[b]))throw new Error(b+'.'+x)})"` sai 0.
    - Os cinco `.mjs` do `<verify>` saem 0; `npx vite build` verde.
    - **Nao-vacuidade (dupla)**: (1) devolver a chamada `store.mcpLeitura` para dentro do `useEffect` de troca de ticker faz `test_opcoes_analisar_ui.mjs` FALHAR (agora `mcpLeitura` esta em `METODOS`); (2) apagar a ancora de sanidade nova e quebrar o fatiador (trocar `"}, ["` por um marcador inexistente) faz a secao 8 FALHAR em vez de passar com zero efeitos. Rode as duas injecoes, confirme, reverta.
  </acceptance_criteria>

  <done>Trocar de ativo nao gasta mais nada; pedir a leitura do servico e um clique que diz "3 consultas" antes de sair; e o unico custo que ainda roda sozinho esta escrito na tela.</done>
</task>

<task type="auto">
  <name>Task 2: CUSTO_DA_ACAO — o rotulo de cada controle, cruzado com o `_cap_check` do backend</name>

  <read_first>
    - O **mapa completo** no bloco `<interfaces>` deste plano — e a especificacao, nao um resumo.
    - `server/app/options_mcp_api.py` — confira o `_cap_check(uid, N)` de cada funcao de rota contra o mapa antes de escrever a tabela. O fonte ganha de qualquer tabela.
    - `web/src/opcoes/OpcoesScreen.jsx:660-680` (o unico controle que ja declara custo) e a constante `N_MAX_VENCIMENTOS` (o precedente de espelho do backend).
    - `web/src/opcoes/CriarSetup.jsx:145-160`, `:390-400`, `:470-485` — os tres botoes que recebem tudo por prop.
    - `web/tests/test_perfil_reorg.mjs`, secao 7 — o padrao de guardiao que cruza backend x front em vez de travar cada lado sozinho.
  </read_first>

  <files>web/src/opcoes/OpcoesScreen.jsx, web/src/opcoes/CriarSetup.jsx, web/src/copy.js, web/tests/test_opcoes_custo_declarado.mjs</files>

  <action>
    **(a) A tabela.** No topo de `OpcoesScreen.jsx`, `CUSTO_DA_ACAO` com as NOVE
    chaves do mapa: `leitura: 3, cadeia: 1, operaveis: 1, proposta: 1,
    grafico: 1, compilar: 2, confirmar: 2, desativar: 1, listarVigias: 2`.
    Comentario obrigatorio dizendo: (1) que ela e ESPELHO do `_cap_check` de
    cada rota em `server/app/options_mcp_api.py`, na mesma disciplina de
    `N_MAX_VENCIMENTOS`; (2) por que o numero vive nos dois lados — a tela
    precisa dizer o custo ANTES de perguntar ao servidor, e quem cobra de
    verdade e o backend; (3) que o guardiao cruza os dois, entao divergir e
    reprovar a suite, nao produzir um rotulo errado em producao.
    `possibilidades` **nao** entra: e calculado (`2*N+1`) e ja declarado.
    `CriarSetup.jsx` recebe `custos={CUSTO_DA_ACAO}` por PROP — o componente ja
    recebe tudo por prop, e uma segunda porta de acoplamento seria gratuita.

    **(b) Os rotulos.** Forma canonica unica: `cp.opcoesCustoChamadas(n)`. Nao
    invente uma segunda maneira de dizer a mesma coisa — duas formas divergem na
    primeira manutencao feita so numa delas. Cobrir: "Ver a cadeia de opcoes",
    "Ver so as opcoes com liquidez", "Montar a estrutura", "Disparos do setup",
    "Ver a interpretacao e o ensaio", "Gravar este setup", "Desativar este
    setup". ("Comparar os vencimentos" e "Atualizar" do bloco de vigias ja
    declaram; "Ler no servico" e da Task 1.)
    O botao de compilar, alem do custo em chamadas, diz que gasta **uma analise
    de IA** — e o unico com custo em dois eixos: chave nova
    `cp.opcoesCustoAnaliseIA`, nos dois blocos. Nao invente o numero da cota de
    IA aqui: o texto nomeia o consumo ("consome uma analise da sua cota do
    dia"), o numero real vive no gate do `metering` e ja tem tela propria.
    Gratuidade: quem NAO toca o servico (bloco interno do 27-04, bloco de vigias
    vindo do indice) usa `cp.opcoesSemCusto`. Um so vocabulario para "gratis".

    **(c) O guardiao** `web/tests/test_opcoes_custo_declarado.mjs` — e ele que
    fecha o criterio 4 do ROADMAP, e ele precisa ser NAO-VACUO por construcao.
    Escreva-o com um **mapa explicito** no topo do arquivo, nao com "cruze pelo
    nome" (os nomes nao coincidem: `mcpSetupsListar` -> `listarVigias`,
    `mcpSetupGrafico` -> `grafico`, `mcpSetupCompilar` -> `compilar`):

    ```
    const ACOES = {
      mcpLeitura:        { chave: "leitura",      rota: "leitura" },
      mcpCadeia:         { chave: "cadeia",       rota: "cadeia" },
      mcpOperaveis:      { chave: "operaveis",    rota: "operaveis" },
      mcpProposta:       { chave: "proposta",     rota: "proposta" },
      mcpSetupGrafico:   { chave: "grafico",      rota: "setup_grafico" },
      mcpSetupCompilar:  { chave: "compilar",     rota: "setup_compilar" },
      mcpSetupConfirmar: { chave: "confirmar",    rota: "setup_confirmar" },
      mcpSetupDesativar: { chave: "desativar",    rota: "setup_desativar" },
      mcpSetupsListar:   { chave: "listarVigias", rota: "setups_listar" },
    };
    const CALCULADO   = ["mcpPossibilidades"];   // 2*N+1, declarado no controle
    const SEM_CONTROLE = ["mcpStatus"];          // frescor no mount, ate 1, declarado no cabecalho
    const SEM_CUSTO   = ["mcpVigias", "opcoesTecnico"];  // custo 0 — proibidos na tabela
    ```

    Com esse mapa, as assercoes:
    · **Cobertura**: derive do fonte do hook (comentarios removidos) TODOS os
      `store.<nome>` chamados; cada um precisa estar em `ACOES`, `CALCULADO`,
      `SEM_CONTROLE` ou `SEM_CUSTO`. Chamada nova sem classificacao FALHA — e e
      exatamente o que se quer, porque o modo de falha aqui e silencioso.
      (Esta e a correcao do defeito C3: a versao anterior exigia entrada em
      `CUSTO_DA_ACAO` para todo `store.mcp*`, o que era impossivel de satisfazer
      — sao nove na tabela e treze no hook — e o executor acabaria afrouxando
      justamente o guardiao criado para fechar o criterio 4.)
    · **Front x tabela**: para cada entrada de `ACOES`, a chave existe em
      `CUSTO_DA_ACAO` (lido do fonte de `OpcoesScreen.jsx` por regex do objeto).
    · **Tabela x backend**: leia `server/app/options_mcp_api.py`, extraia por
      regex, para cada `async def <rota>`, o primeiro `_cap_check(uid, N)` do
      corpo, e exija `N === CUSTO_DA_ACAO[chave]`. Divergencia FALHA. Sanidade
      obrigatoria: o extrator precisa achar as NOVE rotas — se achar menos, o
      teste falha por sanidade em vez de passar por vacuidade (rota renomeada
      no backend nao pode virar "nada a conferir").
    · **Custo zero nao se declara**: nenhum nome de `SEM_CUSTO` aparece em
      `CUSTO_DA_ACAO`. Declarar custo onde nao ha custo mente para o outro lado.
    · **`SEM_CONTROLE` e declarado em algum lugar**: `mcpStatus` nao tem botao,
      mas o cabecalho precisa dizer que abrir a aba consulta o frescor — exija
      que `cp.opcoesCustoFrescor` exista e seja usado em `OpcoesScreen.jsx`
      (Task 1). Um custo sem controle E sem declaracao e o pior dos casos.
    · **`CALCULADO` tem rotulo**: `mcpPossibilidades` usa
      `opcoesCustoChamadas(` com expressao, nao com literal.
    · **Todo controle tem rotulo**: para cada chave de `CUSTO_DA_ACAO`, o fonte
      (de `OpcoesScreen.jsx` **ou** de `CriarSetup.jsx`) contem
      `opcoesCustoChamadas(` com aquela chave como argumento.
    Cabecalho do arquivo nomeando o defeito: sete dos oito controles da aba
    gastavam cota sem dizer, e um rotulo redigitado a mao envelheceria em
    silencio no dia em que o `_cap_check` de uma rota mudasse.
  </action>

  <verify>
    <automated>cd /Users/acamerini/dev/borisv2 && node web/tests/test_opcoes_custo_declarado.mjs && node web/tests/test_opcoes_criar_setup_ui.mjs && node web/tests/test_opcoes_analisar_ui.mjs && node web/tests/test_copy_theme.mjs && cd web && npx vite build</automated>
  </verify>

  <acceptance_criteria>
    - `node web/tests/test_opcoes_custo_declarado.mjs` sai 0 e classifica TODOS os `store.*` do hook (nenhum cai em "nao classificado").
    - `grep -n "CUSTO_DA_ACAO" web/src/opcoes/OpcoesScreen.jsx` casa a declaracao e ao menos 6 usos.
    - `grep -c "custos=" web/src/opcoes/OpcoesScreen.jsx` devolve ≥ 1 (a tabela chega ao `CriarSetup` por prop).
    - `node -e "import('./web/src/copy.js').then(m=>{for(const b of ['estudo','operador'])if(!('opcoesCustoAnaliseIA' in m.COPY[b]))throw new Error(b)})"` sai 0.
    - `npx vite build` verde.
    - **Nao-vacuidade (quadrupla)**: (1) mudar um N em `CUSTO_DA_ACAO` sem mudar o `_cap_check` correspondente FALHA; (2) acrescentar `store.mcpAlgoNovo()` no hook sem classifica-lo FALHA; (3) acrescentar `opcoesTecnico: 1` a `CUSTO_DA_ACAO` FALHA (custo zero nao se declara); (4) renomear uma rota no backend (ex.: `cadeia` -> `cadeia_v2`) FALHA pela sanidade do extrator, em vez de passar por vacuidade. Rode as quatro injecoes, confirme, reverta.
  </acceptance_criteria>

  <done>Nenhum botao da aba gasta cota em silencio, e o numero que ele mostra e o mesmo que o backend cobra — provado por um teste que le os dois lados e que nao passa por vacuidade quando um deles some.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: cada clique cobra o que prometeu</name>
  <what-built>
    O criterio 4 do ROADMAP, na tela: tabela de custo espelhada do backend,
    rotulo em cada controle, a leitura do servico virando clique explicito e o
    custo do frescor declarado no cabecalho. O guardiao prova que os NUMEROS
    batem com o `_cap_check` — mas ele nao ve a tela, e nao prova que o rotulo
    esta no controle CERTO nem que o contador se move como o rotulo diz.
    `ui_safety_gate` esta ligado nesta fase.
  </what-built>
  <action>PAUSE. Nao execute nada: apresente o roteiro abaixo ao desenvolvedor e
  aguarde o sinal de retomada. Registre a resposta dele no SUMMARY, verbatim,
  inclusive os numeros de `usado` que ele anotou.</action>
  <how-to-verify>
    1. `cd web && npm run dev`, aba Opcoes, carteira com posicao. Anote o
       `usado` da cota no cabecalho ANTES de qualquer clique.
    2. Abrir a aba: o `usado` sobe **no maximo 1** (frescor), e o cabecalho DIZ
       isso. Se subir mais, algo voltou a disparar sozinho.
    3. Clique num vigia do bloco "Seus vigias": a tela troca de ativo e o
       `usado` **nao se move**. (Antes desta task, subia 3 em silencio — este
       passo e a prova de que o C2 fechou.)
    4. O bloco de leitura interna (gratis) aparece sozinho; o bloco do servico
       mostra o convite com o botao "Ler no servico — 3 consultas". Clique: o
       `usado` sobe **exatamente 3**.
    5. Percorra os controles, um a um, anotando o rotulo e o delta do `usado`:
       cadeia (1), so com liquidez (1), montar estrutura (1), comparar
       vencimentos (2xN+1, confira com o N que a tela mostra), disparos do
       setup (1), atualizar vigias (2). Cada delta tem de ser igual ao rotulo.
       Qualquer divergencia: PARE e anote qual controle.
    6. Com a permissao `opcoes.criar_setup`: "Ver a interpretacao e o ensaio"
       diz 2 consultas **e** que consome uma analise de IA; confira que o
       `usado` sobe 2 e que o contador de analises do dia sobe 1. Depois
       "Gravar este setup" (2) e "Desativar este setup" (1).
    7. Em 375 px: o rotulo de custo cabe no botao sem estourar, e nenhum alvo de
       toque ficou abaixo de 44 px por causa do texto novo.
    8. Modo Operador: os textos de custo mudam de voz e continuam dizendo o
       mesmo numero.
  </how-to-verify>
  <resume-signal>Responda "aprovado" ou descreva a divergencia, com o controle e os dois numeros (rotulo x delta).</resume-signal>
  <done>O desenvolvedor percorreu os controles, cada delta bateu com o rotulo, e a resposta esta registrada no SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tela -> cota do servico MCP | todo controle pode virar chamada paga; o teto de 2.000/dia e do SERVIDOR, compartilhado por toda a base |
| rotulo exibido -> confianca da pessoa | um custo declarado errado e pior que custo nenhum: ele e crivel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-17 | Repudiation | rotulo de custo divergente do cobrado | mitigate | `test_opcoes_custo_declarado.mjs` cruza `CUSTO_DA_ACAO` com os `_cap_check(uid, N)` do backend, com sanidade do extrator; divergencia reprova a suite |
| T-27-22 | Denial of Service | `mcpLeitura` disparada por troca de ticker | mitigate | a chamada sai do `useEffect` e vira `abrirLeitura` sob clique; `mcpLeitura` entra em `METODOS` no guardiao, que le o fonte e reprova a volta |
| T-27-23 | Repudiation | `mcpStatus` (custo ate 1, sem controle) | accept (declarado) | nao vira clique — o gate de frescor precisa existir ao abrir (ADR-027, Decisao 8). Mitigacao: declaracao no cabecalho (`opcoesCustoFrescor`) + `SEM_CONTROLE` nomeado no guardiao, que exige a declaracao existir |
| T-27-24 | Tampering | chamada nova sem custo declarado | mitigate | a cobertura do guardiao e DERIVADA do fonte do hook: metodo novo sem classificacao reprova a suite |
| T-27-SC | Tampering | instalacao de pacote | n/a | nenhum pacote novo nesta fase |
</threat_model>

<verification>
FORA do sandbox (`dangerouslyDisableSandbox: true` — dentro dele ~26 testes
falham falsamente por bloqueio do bundle de CAs):

`bash scripts/executar.sh --testes`

Baseline: **2691 passed, 5 skipped, 3 xfailed, 0 failed + 137/137 `.mjs`** mais
o delta acumulado de 27-01/02/03/04. Front editado -> `cd web && npx vite build`
verde antes de declarar pronto.

Este e o ultimo plano da fase. Depois da aprovacao do checkpoint, a publicacao
(`scripts/bump.sh` + `scripts/publicar-web.sh`) e o deploy do backend continuam
sendo etapa HUMANA separada — sem eles a fase fica testada e nunca vai ao ar
(licao registrada em `fase-sem-plano-de-publicacao-front`).
</verification>

<success_criteria>
- Todo controle que consome cota declara o custo no proprio controle, e o numero declarado e o mesmo que o backend cobra (criterio 4 do ROADMAP, provado por guardiao que cruza os dois lados e que nao passa por vacuidade).
- Trocar de ativo — inclusive clicando num vigia — nao consome cota nenhuma.
- A leitura do servico sai de um clique que diz "3 consultas" antes de sair.
- A unica chamada que ainda roda sozinha (`mcpStatus`, frescor, ate 1) esta declarada no cabecalho e nomeada no guardiao.
- Nenhuma cor, escala ou token fora do Brand Book v2; alvos de toque com 44 px.
- Suite canonica sem regressao; `npx vite build` verde; checkpoint humano aprovado.
</success_criteria>

<output>
Create `.planning/phases/27-aba-opcoes-sobre-a-carteira/27-05-SUMMARY.md` when done
</output>
