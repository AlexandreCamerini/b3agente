---
phase: 28-aba-opcoes-sub-aba-operar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/opcoes/PropostaLastreada.jsx
  - web/src/App.jsx
  - web/tests/test_opcoes_proposta_ui.mjs
  - web/tests/test_opcoes_multi_candidato_ui.mjs
  - web/tests/test_faixa_liquidez_ui.mjs
  - web/tests/test_carteira_opcoes_tira.mjs
  - web/tests/test_opcoes_mcp_aba_ui.mjs
autonomous: true
requirements: [SC-2]

must_haves:
  truths:
    - "O componente que renderiza a proposta lastreada (manchete do motor, payoff numérico, CTA de abrir/fechar) tem UMA definição só, num módulo em web/src/opcoes/"
    - "App.jsx importa esse módulo; o módulo não importa App.jsx (isolamento do ADR-027 preservado nos dois sentidos)"
    - "O caminho de aceite/fechamento (confirm de liquidez → confirm de estrutura → A.abrirLastreada/A.abrirCollar/A.fecharLastreada) tem UMA implementação só, no mesmo módulo"
    - "Watchlist/Radar e o detalhe da posição em Portfólio continuam renderizando a MESMA proposta que renderizavam antes — zero mudança visual, zero mudança de rota"
    - "A suíte canônica fica verde (baseline 2780 pytest + 143 .mjs) e `npx vite build` passa"
  artifacts:
    - path: "web/src/opcoes/PropostaLastreada.jsx"
      provides: "PropostaLastreada (default), FonteDoDadoProposta, ChipDaProposta, useAceiteLastreado"
      exports: ["default", "FonteDoDadoProposta", "ChipDaProposta", "useAceiteLastreado"]
      contains: "const T = Object.fromEntries"
    - path: "web/src/App.jsx"
      provides: "consumidor do módulo (AtivoCard + PropostaDaPosicao), sem definição local dos três componentes"
  key_links:
    - from: "web/src/App.jsx"
      to: "web/src/opcoes/PropostaLastreada.jsx"
      via: "import estático"
      pattern: "from \"\\./opcoes/PropostaLastreada\\.jsx\""
    - from: "web/src/opcoes/PropostaLastreada.jsx"
      to: "A.abrirLastreada / A.abrirCollar / A.fecharLastreada"
      via: "useAceiteLastreado"
      pattern: "A\\.(abrirLastreada|abrirCollar|fecharLastreada)\\("
---

<objective>
Extrair `PropostaLastreada` (+ `FonteDoDadoProposta`, `ChipDaProposta` e o
caminho de aceite/fechamento) de `App.jsx` para um módulo em
`web/src/opcoes/`, e reapontar os dois consumidores atuais de `App.jsx` para
ele — SEM mudar uma linha de comportamento, cor, texto ou rota.

Purpose: é a precondição estrutural da Fase 28 inteira. A aba Opções
(`OpcoesScreen.jsx`) não importa `App.jsx` de propósito (ADR-027, Decisão 3),
então "trazer a operação para dentro da aba sem duplicar código" só é possível
depois que o componente vive num terceiro lugar que os dois lados importam.
Este plano faz a extração e mantém tudo funcionando exatamente como hoje.

Output: `web/src/opcoes/PropostaLastreada.jsx` (novo), `App.jsx` reapontado, e
os guardiões de teste que mediam "está em App.jsx" atualizados para medir "está
no módulo" — com nota datada, nunca apagados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-aba-opcoes-sub-aba-operar/28-CONTEXT.md
@CLAUDE.md

Regra do repositório que este plano exercita duas vezes:
**guardião de teste não se apaga** — quando a asserção deixa de medir a coisa
certa porque o código MUDOU DE LUGAR (não porque a regra foi revogada), a
asserção é REAPONTADA e ganha nota datada explicando o movimento.

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código em 2026-09-13. -->

Assinaturas a preservar BYTE A BYTE (há guardião de igualdade exata sobre a
primeira; as outras duas são consumidas por `CandidatoOpcao`, que FICA em
App.jsx):

  function FonteDoDadoProposta({ r, cp })
  function ChipDaProposta({ c, p, ticker, onVerbeteLiquidez })
  function PropostaLastreada({ r, operador, cp, busy, onAbrir, onFechar, posAberta, onVerbeteLiquidez })

Helpers de módulo que `PropostaLastreada` lê do escopo de App.jsx hoje e que
o módulo novo precisa ter localmente (App.jsx MANTÉM os seus — há guardiões
exigindo `const FONTE_LABEL` definido em App.jsx, em
`web/tests/test_fonte_cotacoes_visivel.mjs:24` e
`web/tests/test_fase3_fonte_technicals.mjs:54`):

  App.jsx:125  const T = Object.fromEntries(Object.keys(PALETTE.dark).map((k) => [k, `var(${VARKEY(k)})`]));
  App.jsx:256  const MONO = "ui-monospace,'SF Mono',Menlo,Consolas,monospace";
  App.jsx:285  const nf2 = new Intl.NumberFormat("pt-BR", { minimumFractionDigits: 2, maximumFractionDigits: 2 });
  App.jsx:286  const price = (n) => (n == null || isNaN(n) ? "—" : nf2.format(n));
  App.jsx:1342 const FONTE_LABEL = (source) => (source === "mydata" ? "MyData" : source === "brapi" ? "brapi" : source === "yahoo" ? "Yahoo" : source);

Padrão JÁ ESTABELECIDO para o bloco de tokens local (é o que o guardião
`web/tests/test_opcoes_mcp_aba_ui.mjs:74-78` exige de todo componente que
desenha dentro de `web/src/opcoes/`): `const VARKEY = ...`, `const TOKENS = [...]`,
`const T = Object.fromEntries(...)`. Copiar o bloco de
`web/src/opcoes/OpcoesScreen.jsx` (logo após os imports, ~linha 38) e
`web/src/opcoes/SetupChart.jsx`.

Corpo do caminho de aceite a extrair VERBATIM (é a versão PARAMETRIZADA, da
Fase 19 — `App.jsx:4352-4394`, dentro de `PropostaDaPosicao`):

  const aceitarCandidato = async (p) => { ... }   // App.jsx:4352-4384
  const onFecharLastreada = async () => { ... }   // App.jsx:4387-4394
  const [busy, setBusy] = useState(false);        // App.jsx:4323

A cópia mais antiga e NÃO parametrizada, em `AtivoCard`
(`App.jsx:3581-3633`, `onAbrirLastreada`/`onFecharLastreada` lendo
`opProposta` do closure), NÃO é a fonte da extração — ela é removida no plano
28-03 junto com o card.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar web/src/opcoes/PropostaLastreada.jsx com os três componentes e o hook de aceite</name>
  <files>web/src/opcoes/PropostaLastreada.jsx</files>
  <read_first>
    - web/src/App.jsx linhas 3267-3424 (FonteDoDadoProposta, ChipDaProposta, PropostaLastreada — os corpos a mover, comentários inclusos)
    - web/src/App.jsx linhas 4319-4394 (PropostaDaPosicao: o `useState(false)` de busy e os dois handlers parametrizados a mover)
    - web/src/App.jsx linhas 120-130, 250-290, 1340-1345 (T/VARKEY, MONO, nf2/price, FONTE_LABEL — as definições a espelhar)
    - web/src/opcoes/OpcoesScreen.jsx linhas 20-60 (o bloco VARKEY/TOKENS/T local e o comentário que explica por que ele existe)
    - web/src/opcoes/SetupChart.jsx (segundo exemplo do mesmo bloco de tokens)
  </read_first>
  <action>
Criar `web/src/opcoes/PropostaLastreada.jsx` com, nesta ordem:

1. Docblock de cabeçalho no estilo dos irmãos de `web/src/opcoes/` explicando
   as três coisas: (a) este módulo existe porque `OpcoesScreen.jsx` não pode
   importar `App.jsx` (ADR-027, Decisão 3) e `App.jsx` não pode importar
   `OpcoesScreen.jsx` (seria ciclo) — a fiação é de UMA VIA para os dois, e é
   isso que a Emenda 3 do ADR-027 registra; (b) o componente é o de sempre,
   movido sem redesenho — manchete VERBATIM do motor determinístico (guardrail
   CVM, CLAUDE.md princípio 5), payoff numérico, CTA só em Modo Operador;
   (c) por que `T`/`MONO`/`price`/`FONTE_LABEL` são declarados localmente em
   vez de importados de `App.jsx`.

2. `import { useState } from "react";`

3. O bloco de tokens local: `const VARKEY = ...`, `const TOKENS = [...]`,
   `const T = Object.fromEntries(...)`. `VARKEY` e a linha do `T` são cópia
   verbatim de `web/src/opcoes/OpcoesScreen.jsx:41,44` (idênticas a
   `App.jsx:123,125` na forma — a diferença é que ali o `T` deriva de
   `PALETTE.dark`, e aqui de `TOKENS`).

   **ATENÇÃO — o `TOKENS` de `OpcoesScreen.jsx:42-43` NÃO serve como está.**
   Ele é `["bgBase", "bgPanel", "borderSubtle", "borderFaint", "textPrimary",
   "textSecondary", "textMuted", "textFaint", "accent", "accentTint10",
   "negative", "scrim"]` — falta `bgCard`, `accentTint` e `positive`, os três
   lidos por `PropostaLastreada` (`T.bgCard` no fundo do card, `T.accentTint`
   no fundo do CTA, `T.positive` na cor da manchete de CALL). Copiar essa lista
   ACRESCENTANDO os três é obrigatório; sem eles `T.bgCard` sai `undefined` e o
   card renderiza sem fundo, em silêncio, sem erro de build. As três chaves são
   reais e existem em `PALETTE` (`App.jsx:69,77,79`) nos dois temas.
   `TOKENS` final, no mínimo: `bgBase`, `bgPanel`, `bgCard`, `borderSubtle`,
   `borderFaint`, `textPrimary`, `textSecondary`, `textMuted`, `textFaint`,
   `accent`, `accentTint`, `accentTint10`, `positive`, `negative`, `scrim`.

4. `const MONO`, `const nf2`, `const price`, `const FONTE_LABEL` — cópia
   CARACTERE A CARACTERE das quatro linhas de `App.jsx` citadas em
   `<interfaces>`. Comentário acima delas dizendo que são espelho declarado de
   `App.jsx:256/285/286/1342`, que os guardiões de `FONTE_LABEL` obrigam a
   definição a continuar existindo em `App.jsx`, e que a Task 3 deste plano
   instala um guardião de igualdade de corpo entre as duas cópias — divergir o
   formatador de dinheiro entre dois lugares é exatamente o tipo de erro que
   ninguém vê até virar número errado na tela.

5. `export function FonteDoDadoProposta({ r, cp })` — corpo verbatim de
   `App.jsx:3272-3278`, comentário de cabeçalho incluso.

6. `export function ChipDaProposta({ c, p, ticker, onVerbeteLiquidez })` —
   corpo verbatim de `App.jsx:3286-3305`, comentário incluso.

7. `export function useAceiteLastreado({ A, cp, ticker })` — hook novo, cujo
   corpo é a fusão verbatim de `App.jsx:4323` + `4352-4394`:
   - `const [busy, setBusy] = useState(false);`
   - `const aceitarCandidato = async (p) => { ... }` com o corpo de
     `App.jsx:4352-4384` SEM NENHUMA alteração de lógica; a única substituição
     permitida é o identificador `t` (que em `PropostaDaPosicao` é a prop do
     ticker) pelo parâmetro `ticker` do hook — nas três ocorrências:
     `cp.confirmAbrirCollar(p.contratos, t, p.qtyAcoes)`,
     `A.abrirCollar({ underlying: t, ... })`,
     `cp.confirmAbrirCoberta(p.contratos, t, p.qtyAcoes)` e
     `A.abrirLastreada({ underlying: t, ... })`.
   - `const fecharLastreada = async (r) => { ... }` com o corpo de
     `App.jsx:4387-4394`, recebendo `r` por parâmetro em vez de ler do closure
     (o `r` do closure em `PropostaDaPosicao` é a prop da proposta) e usando
     `ticker` no lugar de `t` em `cp.confirmFecharCoberta(price(p.premioTotal),
     p.qtyAcoes, t, p.optionType === "call")`.
   - `return { busy, aceitarCandidato, fecharLastreada };`
   Preservar os comentários de `App.jsx:4354-4356`, `4378-4379` e
   `4346-4351` (eles carregam o POR QUÊ do confirm de liquidez vir antes do de
   estrutura e de só a CALL coberta exigir confirmação de trava), adaptando a
   referência de arquivo:linha para dizer que o corpo veio de
   `PropostaDaPosicao` em App.jsx e virou a implementação única.
   Nada de `window.confirm` novo, nada de confirm removido, nenhuma rota nova:
   as três chamadas continuam `A.abrirCollar`, `A.abrirLastreada`,
   `A.fecharLastreada`, com os MESMOS corpos de requisição.

8. `export default function PropostaLastreada({ r, operador, cp, busy, onAbrir, onFechar, posAberta, onVerbeteLiquidez })`
   — corpo verbatim de `App.jsx:3314-3424`, comentários de cabeçalho
   (`App.jsx:3307-3313`) inclusos. Exportar TAMBÉM como named export
   (`export { PropostaLastreada }` ou declarar `export function PropostaLastreada`
   e `export default PropostaLastreada`), para que o guardião de assinatura
   exata continue casando com `function PropostaLastreada({ r, operador, cp, busy, onAbrir, onFechar, posAberta, onVerbeteLiquidez })`.

PROIBIDO neste arquivo: qualquer `import` de `../App.jsx`; qualquer `import`
de `./OpcoesScreen.jsx`; qualquer `fetch`/`store.*`/`useEffect` (o módulo é
apresentação + handlers de clique — quem busca dado é o consumidor, e um
`useEffect` aqui reintroduziria a busca dupla que o hook `useOpcoesPropostas`
existe para evitar); qualquer mudança de cor, tamanho de fonte, texto ou token
(Brand Book v2 travado por `web/tests/test_brand_book_v2_tokens.mjs`).
  </action>
  <verify>
    <automated>cd web && node -e "const s=require('fs').readFileSync('src/opcoes/PropostaLastreada.jsx','utf8'); const need=['export default','FonteDoDadoProposta','ChipDaProposta','useAceiteLastreado','const T = Object.fromEntries','const price =','const FONTE_LABEL =']; const miss=need.filter(n=>!s.includes(n)); if(miss.length) throw new Error('faltando: '+miss.join(', ')); if(/from\s+[\"'][^\"']*App\.jsx[\"']/.test(s)) throw new Error('importa App.jsx'); if(/from\s+[\"'][^\"']*OpcoesScreen\.jsx[\"']/.test(s)) throw new Error('importa OpcoesScreen.jsx'); if(/useEffect|store\.|fetch\(/.test(s)) throw new Error('tem busca de dado'); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `web/src/opcoes/PropostaLastreada.jsx` existe e exporta `default` (PropostaLastreada), `FonteDoDadoProposta`, `ChipDaProposta`, `useAceiteLastreado`
    - `grep -c 'App.jsx"' web/src/opcoes/PropostaLastreada.jsx` retorna 0 para import (a string pode aparecer em comentário; o que não pode é `from ".../App.jsx"`)
    - O arquivo contém a assinatura literal `function PropostaLastreada({ r, operador, cp, busy, onAbrir, onFechar, posAberta, onVerbeteLiquidez })`
    - O arquivo contém `const VARKEY = `, `const TOKENS = [` e `const T = Object.fromEntries`
    - `TOKENS` inclui `bgCard`, `accentTint` e `positive` (sem eles o card perde fundo, CTA e cor da manchete de CALL, sem erro de build). Provar por: `node -e "const s=require('fs').readFileSync('web/src/opcoes/PropostaLastreada.jsx','utf8'); const m=s.match(/const TOKENS = \\[([\\s\\S]*?)\\]/)[1]; for(const k of ['bgCard','accentTint','positive','bgBase','borderFaint','accent','negative','textPrimary','textSecondary','textMuted','textFaint']) if(!m.includes(JSON.stringify(k))) throw new Error('TOKENS sem '+k); console.log('ok')"`
    - Nenhuma referência `T.<chave>` no arquivo aponta para chave ausente de `TOKENS`: extrair todas as ocorrências de `T.` por regex e conferir que cada chave está em `TOKENS`
    - As linhas `const MONO = `, `const nf2 = `, `const price = ` e `const FONTE_LABEL = ` do módulo são idênticas às de App.jsx:256/285/286/1342 (comparação de string, feita pelo guardião da Task 3)
    - `A.abrirLastreada(`, `A.abrirCollar(` e `A.fecharLastreada(` aparecem exatamente 1× cada no módulo
    - Nenhuma ocorrência de `useEffect`, `store.` ou `fetch(` no módulo
  </acceptance_criteria>
  <done>O módulo existe, exporta os quatro símbolos, não importa App.jsx nem OpcoesScreen.jsx, e os corpos são cópia verbatim dos originais (só o closure `t`/`r` virou parâmetro no hook).</done>
</task>

<task type="auto">
  <name>Task 2: Reapontar App.jsx para o módulo e trocar os handlers de PropostaDaPosicao pelo hook</name>
  <files>web/src/App.jsx</files>
  <read_first>
    - web/src/opcoes/PropostaLastreada.jsx (o módulo criado na Task 1 — as assinaturas exatas dos exports)
    - web/src/App.jsx linhas 1-25 (bloco de imports; onde a linha nova entra, ao lado de `import OpcoesScreen from "./opcoes/OpcoesScreen.jsx";` na linha 12)
    - web/src/App.jsx linhas 3267-3425 (as três definições a deletar)
    - web/src/App.jsx linhas 3790-3820 (uso em AtivoCard — FICA neste plano)
    - web/src/App.jsx linhas 4319-4420 (PropostaDaPosicao — os handlers a trocar pelo hook e o uso do componente)
    - web/src/App.jsx linhas 4431-4475 (CandidatoOpcao — consumidor de ChipDaProposta que FICA em App.jsx e passa a ler o import)
  </read_first>
  <action>
1. Adicionar, junto de `import OpcoesScreen from "./opcoes/OpcoesScreen.jsx";`
   (App.jsx:12), a linha:
   `import PropostaLastreada, { FonteDoDadoProposta, ChipDaProposta, useAceiteLastreado } from "./opcoes/PropostaLastreada.jsx";`

2. Deletar de App.jsx as três definições locais — `function FonteDoDadoProposta`
   (3272-3278), `function ChipDaProposta` (3286-3305) e
   `function PropostaLastreada` (3314-3424) — junto dos comentários de
   cabeçalho que pertencem a elas (3267-3271, 3280-3285, 3307-3313), que vão
   para o módulo na Task 1. Deixar no lugar um comentário de UMA linha
   apontando o novo endereço: as três definições agora vivem em
   `web/src/opcoes/PropostaLastreada.jsx`, Fase 28 (28-01), para que
   `OpcoesScreen.jsx` possa usá-las sem importar `App.jsx`.
   Os três consumidores em App.jsx passam a ler o import:
   `<PropostaLastreada` em AtivoCard (3799) e em PropostaDaPosicao (4416);
   `<FonteDoDadoProposta` em PropostaDaPosicao (4413);
   `<ChipDaProposta` em CandidatoOpcao (4472).
   As chamadas de JSX não mudam — nem nome, nem props, nem ordem.

3. Em `PropostaDaPosicao` (a partir de 4319): substituir
   `const [busy, setBusy] = useState(false);` (4323) e os dois handlers locais
   `aceitarCandidato` (4352-4384) e `onFecharLastreada` (4387-4394) por uma
   única chamada do hook:
   `const { busy, aceitarCandidato, fecharLastreada } = useAceiteLastreado({ A, cp, ticker: t });`
   A chamada do hook fica EXATAMENTE onde o `useState` estava — antes do
   `if (!r || !r.proposta) return null;` (4330), pela regra dos hooks; manter o
   comentário de 4320-4322 adaptado (o "em voo" por posição continua sendo do
   hook, uma instância por posição renderizada).
   O ponto de uso `onFechar={onFecharLastreada}` (4416) passa a
   `onFechar={() => fecharLastreada(r)}` (o hook recebe `r` por parâmetro).
   `onAbrir={() => aceitarCandidato(r.proposta)}` e
   `onAceitar={aceitarCandidato}` (4410) NÃO mudam.

4. NÃO tocar em `AtivoCard` neste plano: `onAbrirLastreada` (3581),
   `onFecharLastreada` (3626), `posAberta` (3578) e `opPropostaBusy` (3500)
   continuam como estão, e o card continua renderizado em 3797-3818. A remoção
   é do plano 28-03 — separar os dois movimentos é o que mantém a suíte verde
   entre eles e deixa o diff desta extração legível.

5. NÃO adicionar, remover ou renomear nenhuma chave de `copy.js`, nenhuma rota,
   nenhum método de store. Nenhuma mudança em `persistence.js` (a extração é de
   componente React, não de chamada de API — a paridade
   `deviceStore`↔`serverStore` não é tocada).
  </action>
  <verify>
    <automated>cd web && npx vite build 2>&1 | tail -5 && node -e "const s=require('fs').readFileSync('src/App.jsx','utf8'); const bad=['function FonteDoDadoProposta','function ChipDaProposta','function PropostaLastreada'].filter(n=>s.includes(n)); if(bad.length) throw new Error('definicao ainda em App.jsx: '+bad.join(', ')); if(!s.includes('from \"./opcoes/PropostaLastreada.jsx\"')) throw new Error('sem import do modulo'); if(!/useAceiteLastreado\(\{ A, cp, ticker: t \}\)/.test(s)) throw new Error('PropostaDaPosicao nao usa o hook'); const n=(s.match(/<PropostaLastreada/g)||[]).length; if(n!==2) throw new Error('pontos de uso em App.jsx: '+n); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vite build` termina sem erro (rodado de dentro de `web/`)
    - `grep -c 'function PropostaLastreada' web/src/App.jsx` = 0; idem `function FonteDoDadoProposta` e `function ChipDaProposta`
    - App.jsx contém a linha de import `from "./opcoes/PropostaLastreada.jsx"` com os quatro símbolos
    - `<PropostaLastreada` aparece exatamente 2× em App.jsx (AtivoCard + PropostaDaPosicao) — inalterado em relação a antes do plano
    - `<FonteDoDadoProposta` e `<ChipDaProposta` continuam aparecendo em App.jsx (PropostaDaPosicao e CandidatoOpcao), agora resolvidos pelo import
    - `PropostaDaPosicao` contém `useAceiteLastreado({ A, cp, ticker: t })` e NÃO contém mais `const aceitarCandidato = async (p) => {`
    - `A.abrirLastreada(`, `A.abrirCollar(` e `A.fecharLastreada(` aparecem exatamente 1× cada em App.jsx (só as de `AtivoCard`; as de `PropostaDaPosicao` foram para o módulo). As DEFINIÇÕES `abrirLastreada:`/`abrirCollar:`/`fecharLastreada:` no objeto `A` (App.jsx ~8714-8740) continuam intactas
    - `git diff --stat web/src/persistence.js web/src/copy.js` vazio
  </acceptance_criteria>
  <done>App.jsx não define mais os três componentes nem os handlers de aceite; importa tudo do módulo; build verde; os dois pontos de uso do card continuam renderizando o mesmo card com as mesmas props.</done>
</task>

<task type="auto">
  <name>Task 3: Reapontar os guardiões que mediam "está em App.jsx" e instalar o guardião de igualdade dos formatadores</name>
  <files>web/tests/test_opcoes_proposta_ui.mjs, web/tests/test_opcoes_multi_candidato_ui.mjs, web/tests/test_faixa_liquidez_ui.mjs, web/tests/test_carteira_opcoes_tira.mjs, web/tests/test_opcoes_mcp_aba_ui.mjs</files>
  <read_first>
    - web/tests/test_opcoes_proposta_ui.mjs (inteiro — é o guardião principal; as asserções a reapontar estão nas linhas 53-69, 154-166, 313-315 e 341-342, e as fatias `propostaFn`/`fonteFnBody` alimentam tudo depois da 320)
    - web/tests/test_opcoes_multi_candidato_ui.mjs linhas 100-145 (itens 8-11)
    - web/tests/test_faixa_liquidez_ui.mjs linhas 125-145 (a asserção de ChipDaProposta)
    - web/tests/test_carteira_opcoes_tira.mjs linhas 170-215 (contagem de store.optionsGate/optionsProposta e a asserção de assinatura)
    - web/tests/test_opcoes_mcp_aba_ui.mjs linhas 1-80 (o mapa `brutos`, o loop de isolamento e a exigência do bloco de tokens)
    - web/src/opcoes/PropostaLastreada.jsx (o módulo novo — a fonte que os guardiões passam a ler)
  </read_first>
  <action>
Regra única deste task: **nenhuma asserção é apagada.** Cada uma que hoje lê
`app` (o texto de `App.jsx`) para medir algo que se MOVEU passa a ler o texto
de `web/src/opcoes/PropostaLastreada.jsx`, e ganha comentário
`// ATUALIZADO 2026-09-13 (Fase 28, 28-01): <o que se moveu e por quê>`.
Onde a asserção mede um FATO que deixou de existir, ela é substituída por uma
que mede o MESMO INVARIANTE no lugar novo — nunca por `true`, nunca por
comentário.

`web/tests/test_opcoes_proposta_ui.mjs`:
- Adicionar ao topo, junto de `const app = readFileSync(...)`:
  `const modulo = readFileSync(join(here, "..", "src", "opcoes", "PropostaLastreada.jsx"), "utf8");`
- Linha 53 (assinatura exata de `PropostaLastreada`): trocar `app` por `modulo`.
- Linhas 54-55 (`function PropostaLastreada` antes de `function OpcoesCamada`
  no arquivo): o invariante era "a definição da proposta vem antes da cadeia".
  Com a proposta em outro arquivo, a ordem entre arquivos não existe. Substituir
  por duas asserções que medem o invariante REAL: (a) `function OpcoesCamada`
  continua definido em `app`; (b) `modulo` não menciona `OpcoesCamada` (o
  módulo extraído não arrastou a cadeia legada consigo).
- Linhas 57-59 (`iPL`/`iOC`/`propostaFn`): recalcular a fatia a partir de
  `modulo` — `const iPL = modulo.indexOf("function PropostaLastreada");` e
  `const propostaFn = modulo.slice(iPL);` (o componente é o último do arquivo).
  Todas as asserções que usam `propostaFn` (62, 161-166, e as de payoff/null
  depois da 320) passam a valer sobre o módulo, sem mudar o texto da asserção.
- Linha 63 (`front não compõe a manchete ("Vender " + variável)`) e 64
  (`"Se você tivesse"`): são guardiões GLOBAIS de composição de frase —
  mantê-los sobre `app` E adicionar as mesmas duas sobre `modulo` (a proibição
  segue o código).
- Linhas 68-69 (`<PropostaLastreada` antes de `<OpcoesCamada` no JSX de uso):
  continua válido neste plano (AtivoCard não mudou) — deixar como está. O plano
  28-03 é que reaponta esta.
- Linha 154 (`ctaFecharLastreada(price(p.premioTotal), isCall)`): trocar `app`
  por `modulo`.
- Linhas 156-157 (`confirmFecharCoberta(...)` exatamente 2× em App.jsx): o
  invariante era "os dois call sites em paridade". Com o hook, há UM call site
  só. Substituir por: `confirmFecharCoberta(price(p.premioTotal), p.qtyAcoes, ticker, p.optionType === "call")`
  aparece exatamente 1× em `modulo` (ajustar o identificador do ticker ao que a
  Task 1 usou) e 1× em `app` (o de `AtivoCard`, que o 28-03 remove), com nota
  datada explicando que a paridade virou implementação única.
- Linhas 313-315 (`function FonteDoDadoProposta` antes de `iPL`): trocar `app`
  por `modulo` (a ordem dentro do módulo é a mesma) e recalcular `fonteFnBody`
  como `modulo.slice(iFonte, iPL)`.
- Linhas 341-342 (`FonteDoDadoProposta usa FONTE_LABEL`): passa a valer sobre
  `fonteFnBody` já recalculado — sem mudança de texto.
- **Guardião novo (é o custo declarado da duplicação dos formatadores):**
  extrair de `app` e de `modulo` as linhas `const MONO = `, `const nf2 = `,
  `const price = ` e `const FONTE_LABEL = ` por regex de linha inteira, e
  afirmar que as quatro são IGUAIS caractere a caractere entre os dois
  arquivos. Nome da asserção deve dizer o porquê: o módulo declara os
  formatadores localmente porque não pode importar `App.jsx`, e um
  `price`/`FONTE_LABEL` divergente mostraria número diferente para o mesmo dado
  na mesma tela.

`web/tests/test_opcoes_multi_candidato_ui.mjs`:
- Item (8), asserção "PropostaDaPosicao continua renderizando PropostaLastreada
  no ramo de candidato único": continua valendo (o JSX não mudou) — não tocar.
- Item (9), `<PropostaLastreada` exatamente 2× em App.jsx: continua valendo
  neste plano — não tocar (o 28-03 reaponta).
- Item (10), `const aceitarCandidato = async (p) => {` em `fatiaPDP`: quebra.
  Substituir por três asserções, com nota datada, que medem o MESMO invariante
  ("caminho de aceite único, compartilhado"): (a) `fatiaPDP` contém
  `useAceiteLastreado({ A, cp, ticker: t })`; (b) o módulo contém
  `const aceitarCandidato = async (p) => {` exatamente 1×; (c) `app` NÃO contém
  mais `const aceitarCandidato = async (p) => {` (a implementação é única e
  está fora). A asserção `onAceitar={aceitarCandidato}` em `fatiaPDP` continua
  como está.
- Item (12), guardiões globais de composição de frase: adicionar as mesmas duas
  sobre o texto do módulo.

`web/tests/test_faixa_liquidez_ui.mjs` (linha ~135): a asserção cobre
`PropostaLastreada` E `CandidatoOpcao`. `CandidatoOpcao` ficou em App.jsx e
`PropostaLastreada` foi para o módulo — dividir em duas asserções, cada uma
sobre a sua fonte, com nota datada. Nenhuma das duas perde a exigência de
`<ChipDaProposta` (nada de `<span` cru para os chips).

`web/tests/test_carteira_opcoes_tira.mjs`:
- Linhas ~178-180 (`store.optionsGate(`/`store.optionsProposta(` 2× em
  App.jsx): NÃO tocar — este plano não muda nenhuma das duas contagens.
- Linhas ~208-211 (assinatura de `PropostaLastreada`): trocar a fonte para o
  módulo, com nota datada.

`web/tests/test_opcoes_mcp_aba_ui.mjs`:
- Acrescentar `PropostaLastreada.jsx` ao mapa `brutos` (mesma leitura dos
  irmãos) para que ele entre no loop de isolamento "não importa App.jsx"
  (linha ~68) e na lista de componentes que desenham e por isso precisam do
  bloco `VARKEY/TOKENS/T` local (linha ~74).
- Adicionar uma asserção nova, explícita, do invariante de DUAS VIAS da Emenda
  3: `PropostaLastreada.jsx` não importa `App.jsx` **nem** `OpcoesScreen.jsx`,
  e `App.jsx` não importa `OpcoesScreen.jsx`. Nome da asserção deve citar
  ADR-027/Emenda 3.

Depois de editar: rodar a suíte canônica FORA do sandbox e conferir contra a
baseline (medir a baseline ANTES de qualquer edição, na primeira execução do
plano, e anotá-la no SUMMARY).
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/executar.sh --testes` (rodado com `dangerouslyDisableSandbox: true`) termina com 0 falhas em pytest e 143/143 nos `.mjs` — ou com a contagem de `.mjs` aumentada se asserções novas foram adicionadas ao mesmo arquivo (o número de ARQUIVOS `.mjs` não diminui e nenhum arquivo passa a falhar)
    - Nenhuma asserção foi deletada: `git diff web/tests/` não remove nenhuma linha `ok("` sem substituí-la por outra `ok("` que meça o mesmo invariante, e cada substituição carrega comentário com a data `2026-09-13` e a referência `Fase 28`
    - `web/tests/test_opcoes_proposta_ui.mjs` lê `src/opcoes/PropostaLastreada.jsx` (há uma linha `readFileSync` apontando para ele)
    - O guardião novo de igualdade de formatadores existe e FALHA se `price` ou `FONTE_LABEL` divergirem: provar injetando um espaço na cópia do módulo, rodando `node web/tests/test_opcoes_proposta_ui.mjs` (deve sair com código != 0) e revertendo
    - `web/tests/test_opcoes_mcp_aba_ui.mjs` inclui `PropostaLastreada.jsx` no loop de isolamento e tem asserção nomeando ADR-027 / Emenda 3
  </acceptance_criteria>
  <done>Suíte canônica verde contra a baseline medida no início do plano; todos os guardiões reapontados com nota datada; o guardião de divergência de formatador provado por injeção de defeito.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente React → rotas internas `/api/options/*` | corpo de abrir/fechar operação lastreada cruza aqui; o servidor re-deriva prêmio e strike (T-17-24) e recusa por 403/lastro insuficiente |
| módulo compartilhado ↔ App.jsx / OpcoesScreen.jsx | fronteira de import: o isolamento do ADR-027 (Decisão 3) só vale se a fiação for de uma via para os dois consumidores |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | `useAceiteLastreado` (corpo movido de `PropostaDaPosicao`) | mitigate | O corpo é cópia verbatim; a única substituição permitida é `t`→`ticker` e `r` do closure→parâmetro. Guardião: `A.abrirLastreada(`/`A.abrirCollar(`/`A.fecharLastreada(` exatamente 1× cada no módulo, com os MESMOS corpos de requisição (`underlying`, `contractSymbol`, `expiration`, `contratos`, `aceitaLiquidezDificil`; collar com `pernasContratos` de `{contractSymbol, lado}`). Nenhuma rota nova, nenhum campo novo no corpo |
| T-28-02 | Elevation of Privilege | perda de um `window.confirm` na cópia | mitigate | Os três confirms continuam na mesma ORDEM: liquidez DIFÍCIL (com `liq.aviso` verbatim e abort nomeado quando o motor é mudo) → confirm de collar OU de venda coberta → só então a chamada. A PUT continua sem confirm de trava (T-14-24) e COM o de liquidez. Asserções de `confirmAbrirCollar`/`confirmAbrirCoberta`/`confirmFecharCoberta` em `test_opcoes_proposta_ui.mjs` seguem valendo, agora sobre o módulo |
| T-28-03 | Elevation of Privilege | CTA aparecendo em Modo Estudo | mitigate | `{operador && (<button ...>)}` e `{!operador && (didatica)}` vêm verbatim; guardião de split de modo em `test_opcoes_proposta_ui.mjs` reaponta para o módulo e continua exigindo os dois ramos. Defesa real é o 403 do servidor (T-14-23) — a UI é a segunda camada |
| T-28-04 | Information Disclosure | manchete/didática do motor reescrita na mudança de arquivo | mitigate | `{p.manchete}` renderizado direto; guardiões globais `"Vender " +` e `"Se você tivesse"` passam a valer sobre `app` E sobre o módulo (guardrail CVM, CLAUDE.md princípio 5) |
| T-28-05 | Tampering | divergência silenciosa entre `price`/`FONTE_LABEL` do módulo e de App.jsx | mitigate | Guardião novo de igualdade caractere a caractere das quatro linhas de formatador entre os dois arquivos, provado por injeção de defeito |
| T-28-06 | Tampering | ciclo de import reintroduzido (App.jsx ↔ OpcoesScreen.jsx) | mitigate | Asserção explícita em `test_opcoes_mcp_aba_ui.mjs`: o módulo não importa `App.jsx` nem `OpcoesScreen.jsx`, e `App.jsx` não importa `OpcoesScreen.jsx` — o invariante de DUAS VIAS da Emenda 3, não um comentário |
| T-28-07 | Repudiation | guardião apagado em vez de reapontado | mitigate | Critério de aceite exige que `git diff web/tests/` não remova nenhum `ok("` sem substituto que meça o mesmo invariante, cada um com nota datada |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | Este plano não instala nem atualiza pacote nenhum (`package.json`/`package-lock.json` fora de `files_modified`); se um install aparecer, o plano está errado — parar e replanejar |
</threat_model>

<verification>
1. `cd web && npx vite build` — verde (erro de sintaxe JSX não aparece em grep)
2. `bash scripts/executar.sh --testes` fora do sandbox — 0 falhas, contra a baseline medida no início do plano
3. `grep -c "function PropostaLastreada" web/src/App.jsx` = 0
4. `grep -c "<PropostaLastreada" web/src/App.jsx` = 2 (inalterado)
5. Injeção de defeito no guardião de formatador: alterar `price` no módulo → `node web/tests/test_opcoes_proposta_ui.mjs` sai != 0 → reverter
6. `git diff --stat` mostra 0 linha em `web/src/persistence.js`, `web/src/copy.js`, `web/src/opcoes/OpcoesScreen.jsx`
</verification>

<success_criteria>
- Existe UMA definição de `PropostaLastreada`/`FonteDoDadoProposta`/`ChipDaProposta` no repositório, em `web/src/opcoes/PropostaLastreada.jsx`
- Existe UMA implementação do caminho de aceite/fechamento lastreado, no mesmo módulo
- `App.jsx` consome o módulo por import; o módulo não importa `App.jsx` nem `OpcoesScreen.jsx`
- Watchlist/Radar e o detalhe de posição em Portfólio renderizam a proposta exatamente como antes (nenhuma mudança de props, cor, texto ou rota)
- Suíte canônica verde; `npx vite build` verde
- Nenhum guardião apagado; todos os reapontados com nota datada
</success_criteria>

<output>
Create `.planning/phases/28-aba-opcoes-sub-aba-operar/28-01-SUMMARY.md` when done.
Registrar no SUMMARY: a baseline de testes MEDIDA antes da primeira edição, a
contagem depois, e a lista de asserções reapontadas (arquivo:linha antes → o
que passou a medir).
</output>
