---
phase: 28-aba-opcoes-sub-aba-operar
plan: 03
type: execute
wave: 3
depends_on: ["28-01", "28-02"]
files_modified:
  - web/src/App.jsx
  - web/tests/test_opcoes_proposta_ui.mjs
  - web/tests/test_opcoes_multi_candidato_ui.mjs
  - web/tests/test_carteira_opcoes_tira.mjs
  - docs/adr/027-consumo-do-servico-mcp-autenticado.md
autonomous: false
requirements: [SC-1, SC-3, SC-5]

must_haves:
  truths:
    - "O card de proposta lastreada não existe mais dentro do AtivoCard — abrir/fechar por Watchlist/Radar deixa de ser possível"
    - "A camada de opções legada (OpcoesCamada, cadeia expansível) continua em Watchlist/Radar, intocada"
    - "A tira OportunidadesOpcoes em Posições continua intocada (não usa PropostaLastreada, não dependia da extração)"
    - "Nenhum handler, estado ou derivado órfão do card removido sobra em AtivoCard (nada de busy travado nem confirm inalcançável)"
    - "A Emenda 3 ao ADR-027 está escrita, com a regra de duas vias e o guardião que a mede"
    - "Abrir e fechar venda coberta, put de proteção e collar funciona a partir da sub-aba Operar, verificado no app rodando"
  artifacts:
    - path: "web/src/App.jsx"
      provides: "AtivoCard sem o card de proposta e sem código órfão; OpcoesCamada preservada"
    - path: "docs/adr/027-consumo-do-servico-mcp-autenticado.md"
      provides: "Emenda 3 (Fase 28) — componente de UI compartilhado sob isolamento de duas vias"
      contains: "Emenda 3"
  key_links:
    - from: "web/src/App.jsx (AtivoCard, bloco opGate && opGate.liquida)"
      to: "OpcoesCamada"
      via: "único consumidor restante do bloco"
      pattern: "<OpcoesCamada"
    - from: "web/src/opcoes/OpcoesScreen.jsx (SubAbaOperar)"
      to: "abrir/fechar lastreada"
      via: "único caminho de execução lastreada na UI a partir desta fase (além do detalhe de posição em Portfólio)"
      pattern: "<PropostaLastreada"
---

<objective>
Fechar a fase: remover o card de proposta lastreada de dentro do `AtivoCard`
(Watchlist/Radar), limpar tudo que fica órfão com ele, reapontar os dois
guardiões que mediam a presença do card ali, escrever a Emenda 3 ao ADR-027 e
verificar ponta a ponta — incluindo no app rodando.

Purpose: sem este plano a fase entrega o oposto do pedido. O objetivo declarado
(28-CONTEXT, D1) era reduzir a duplicação de EXPERIÊNCIA: se o card continuar no
card do ativo E existir na sub-aba Operar, o código deixou de duplicar mas a
navegação continua duplicada, e a pessoa segue com dois lugares para a mesma
decisão. A proposta lastreada de um ativo passa a viver na aba Opções.

Output: `AtivoCard` enxuto e sem código morto; guardiões reapontados com nota
datada; `docs/adr/027` com a Emenda 3; suíte canônica e `npx vite build`
verdes; checkpoint humano de verificação ao vivo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-aba-opcoes-sub-aba-operar/28-CONTEXT.md
@.planning/phases/28-aba-opcoes-sub-aba-operar/28-01-SUMMARY.md
@.planning/phases/28-aba-opcoes-sub-aba-operar/28-02-SUMMARY.md
@CLAUDE.md
@docs/adr/027-consumo-do-servico-mcp-autenticado.md

Decisão desta fase que este plano executa (28-CONTEXT.md, **D1**):
o card dentro de `AtivoCard` (Watchlist/Radar) é REMOVIDO; a tira
`OportunidadesOpcoes` em Posições **permanece intocada** — ela é descoberta de
proposta ativa sem abrir a aba (Fase 18/NAV-01), aponta para a POSIÇÃO, e hoje
deliberadamente NÃO renderiza `PropostaLastreada` (`App.jsx:4426-4430`, junto
do guardião de contagem que trava isso). Nada nesta fase muda esse fato.

<interfaces>
<!-- Endereços exatos, verificados em 2026-09-13. Confirmar com grep antes de
     editar: o 28-01 e o 28-02 mexeram em App.jsx e as linhas andaram. -->

O que sai (dentro de `AtivoCard`):

  App.jsx:3797-3818  bloco `{opGate && opGate.liquida && (<> ... </>)}`
                     -> SAI o `<PropostaLastreada ... />` (3799-3803) e o
                        wrapper `<div style={{ marginTop: "24px" }}>` (3806)
                        que existia só para separar o card da cadeia (D-4 do
                        14-UI-SPEC — sem card acima, o espaçador não tem par);
                     -> FICA o `<OpcoesCamada ... />` (3807-3815) com as
                        MESMAS props, inclusive
                        `rotuloFechado={opProposta && opProposta.proposta ? cp.verCadeiaCompleta : null}`

  App.jsx:3500       const [opPropostaBusy, setOpPropostaBusy] = useState(false);   -> SAI
  App.jsx:3576-3580  const posAberta = ...                                          -> SAI
  App.jsx:3581-3625  const onAbrirLastreada = async () => { ... }                   -> SAI
  App.jsx:3626-3633  const onFecharLastreada = async () => { ... }                  -> SAI

O que FICA, e por quê (não remover por parecer órfão):

  App.jsx:3499       const [opProposta, setOpProposta] = useState(null);
  App.jsx:3513-3523  o efeito que chama store.optionsProposta(t, true)
                     -> continuam porque `rotuloFechado` do OpcoesCamada ainda
                        os lê (3814). É comportamento de HOJE, não introduzido
                        aqui; remover a busca mudaria o rótulo da cadeia, que é
                        outra decisão. Registrar em comentário como dívida
                        NOMEADA para a Fase 31 (polish pós-uso real): uma
                        chamada de proposta por card de watchlist só para
                        rotular um acordeão.
  App.jsx:3555       const myOptionPositions = ...
                     -> continua: `myOptionPositionsLegado` (3564) o filtra e
                        o OpcoesCamada o consome.
  App.jsx:3492-3510  const [opGate, setOpGate] = useState(null) e o efeito do gate
                     -> continuam: são o portão de descobribilidade do
                        `OpcoesCamada` (ADR-003/004/005).
  App.jsx:4235-4308  OportunidadesOpcoes  -> INTOCADA
  App.jsx:4319-4421  PropostaDaPosicao    -> INTOCADA (é o ponto de uso que
                                             sobra em App.jsx)
  App.jsx:4431+      CandidatoOpcao       -> INTOCADO
  App.jsx:~8714+     as definições `abrirLastreada:`/`abrirCollar:`/
                     `fecharLastreada:` no objeto `A`  -> INTOCADAS (são a
                     porta de API que o hook do 28-01 chama)

Guardiões que este plano reaponta (os do 28-01 já foram; estes quebram AGORA):

  web/tests/test_opcoes_proposta_ui.mjs:68-69
    "<PropostaLastreada aparece antes de <OpcoesCamada no JSX de uso"
  web/tests/test_opcoes_proposta_ui.mjs:277
    "bloco opGate.liquida referencia PropostaLastreada"
  web/tests/test_opcoes_multi_candidato_ui.mjs:119-122
    "<PropostaLastreada aparece exatamente 2x no fonte inteiro"

Guardião que NÃO precisa mudar (conferir, não editar às cegas):
  web/tests/test_carteira_opcoes_tira.mjs:~178-180
    store.optionsGate( e store.optionsProposta( 2x em App.jsx
    -> os dois call sites de App.jsx continuam existindo (AtivoCard +
       useOpcoesPropostas). Se a contagem tiver mudado, o plano errou e
       removeu busca que devia ficar: PARAR e investigar antes de ajustar o
       número.
  web/tests/test_carteira_opcoes_tira.mjs:~208-211
    -> já foi reapontado para o módulo no 28-01.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover o card de proposta do AtivoCard e o código que fica órfão</name>
  <files>web/src/App.jsx</files>
  <read_first>
    - web/src/App.jsx: o bloco `opGate && opGate.liquida` dentro de `AtivoCard` (localizar por `grep -n "opGate && opGate.liquida"`, hoje ~3797)
    - web/src/App.jsx: o cabeçalho de estados de `AtivoCard` (localizar por `grep -n "const \[opGate"`, hoje ~3492-3523) — os dois efeitos e os quatro estados
    - web/src/App.jsx: `posAberta`/`onAbrirLastreada`/`onFecharLastreada` (localizar por `grep -n "const onAbrirLastreada"`, hoje ~3576-3633)
    - web/src/App.jsx: `OportunidadesOpcoes` e `PropostaDaPosicao` (para confirmar que NÃO são tocados)
    - .planning/phases/28-aba-opcoes-sub-aba-operar/28-CONTEXT.md seção D1 (a razão de produto da remoção)
  </read_first>
  <action>
Em `AtivoCard`, e SÓ em `AtivoCard`:

1. No bloco `{opGate && opGate.liquida && (...)}`: remover o
   `<PropostaLastreada ... />` inteiro e o wrapper
   `<div style={{ marginTop: "24px" }}>` que o separava da cadeia. O bloco
   passa a ser `{opGate && opGate.liquida && (<OpcoesCamada ... />)}` — o
   fragmento `<>...</>` deixa de ser necessário com um filho só; remover.
   As props de `OpcoesCamada` não mudam, `rotuloFechado` incluso.
   Reescrever o comentário de 3804-3805 (que justificava o espaçador de 24px
   por causa do card acima) para registrar a remoção: por que o espaçador saiu
   e para onde o card foi (aba Opções, sub-aba Operar, Fase 28 D1). O
   comentário de 3791-3796 (por que a linha de opções só existe com gate
   líquido) continua válido — preservar.

2. Remover, porque ficam sem leitor nenhum:
   `const [opPropostaBusy, setOpPropostaBusy] = useState(false);`,
   `const posAberta = ...`, `const onAbrirLastreada = async () => {...}` e
   `const onFecharLastreada = async () => {...}`.
   ANTES de remover cada um, confirmar por grep que a última referência era o
   card removido — se algum ainda tiver leitor, PARAR e reportar em vez de
   remover.

3. NÃO remover: `opProposta`/`setOpProposta` e o efeito que o preenche
   (`store.optionsProposta(t, true)`), `opGate`/`setOpGate` e o efeito do gate,
   `myOptionPositions`, `myOptionPositionsLegado`, `doBuyOption`,
   `doSellOption`, `opBusy`, `opChain` e o resto da máquina do `OpcoesCamada`.
   Acima do efeito de `opProposta`, acrescentar comentário datado dizendo o que
   ele serve AGORA que o card saiu: só `rotuloFechado` do acordeão de cadeia
   (`cp.verCadeiaCompleta`) — dívida nomeada, uma chamada de proposta por card
   de watchlist para rotular um acordeão, candidata a corte na Fase 31, e
   deliberadamente NÃO cortada aqui porque mudar o rótulo da cadeia é decisão
   de produto separada.

4. Não tocar em `OportunidadesOpcoes`, `PropostaDaPosicao`, `CandidatoOpcao`,
   nem nas definições de `abrirLastreada`/`abrirCollar`/`fecharLastreada` no
   objeto `A`. Nenhuma mudança em `copy.js` (as chaves `ctaVendaCoberta`,
   `confirmAbrirCoberta`, etc. continuam em uso pelo módulo do 28-01 e pelo
   detalhe de posição — não são órfãs).
  </action>
  <verify>
    <automated>cd web && npx vite build 2>&1 | tail -5 && node -e "const s=require('fs').readFileSync('src/App.jsx','utf8'); const nPL=(s.match(/<PropostaLastreada/g)||[]).length; if(nPL!==1) throw new Error('<PropostaLastreada em App.jsx: '+nPL+' (esperado 1: PropostaDaPosicao)'); for(const n of ['opPropostaBusy','const posAberta =','const onAbrirLastreada','const onFecharLastreada']) if(s.includes(n)) throw new Error('codigo orfao ainda presente: '+n); for(const n of ['<OpcoesCamada','<OportunidadesOpcoes','function PropostaDaPosicao','function CandidatoOpcao','const [opProposta','cp.verCadeiaCompleta']) if(!s.includes(n)) throw new Error('removido por engano: '+n); const g=(s.match(/store\.optionsGate\(/g)||[]).length, p=(s.match(/store\.optionsProposta\(/g)||[]).length; if(g!==2||p!==2) throw new Error('contagem de busca mudou: gate='+g+' proposta='+p); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vite build` verde
    - `<PropostaLastreada` aparece exatamente 1× em `web/src/App.jsx` (só em `PropostaDaPosicao`)
    - `grep -c "opPropostaBusy" web/src/App.jsx` = 0; idem `const posAberta =`, `const onAbrirLastreada`, `const onFecharLastreada`
    - `A.abrirLastreada(`, `A.abrirCollar(` e `A.fecharLastreada(` aparecem 0× em `web/src/App.jsx`; as definições `abrirLastreada:`, `abrirCollar:`, `fecharLastreada:` no objeto `A` continuam presentes (1× cada)
    - `<OpcoesCamada` continua presente e o bloco `opGate && opGate.liquida` continua existindo, com `OpcoesCamada` como único filho
    - `store.optionsGate(` e `store.optionsProposta(` continuam 2× cada em `web/src/App.jsx`
    - `git diff web/src/App.jsx` não altera nenhuma linha dentro de `OportunidadesOpcoes`, `PropostaDaPosicao` ou `CandidatoOpcao`
    - O efeito de `opProposta` carrega comentário datado `2026-09-13` explicando que serve só a `rotuloFechado` e que o corte é candidato da Fase 31
  </acceptance_criteria>
  <done>AtivoCard renderiza a cadeia de opções e nenhum card de proposta; nenhum handler, estado ou derivado do card removido sobra; build verde.</done>
</task>

<task type="auto">
  <name>Task 2: Reapontar os três guardiões da presença do card e escrever a Emenda 3 ao ADR-027</name>
  <files>web/tests/test_opcoes_proposta_ui.mjs, web/tests/test_opcoes_multi_candidato_ui.mjs, web/tests/test_carteira_opcoes_tira.mjs, docs/adr/027-consumo-do-servico-mcp-autenticado.md</files>
  <read_first>
    - web/tests/test_opcoes_proposta_ui.mjs linhas 60-75 e 255-300 (a asserção de ordem no JSX e o IIFE de paridade de `vm` que inspeciona o bloco `opGate.liquida`)
    - web/tests/test_opcoes_multi_candidato_ui.mjs linhas 115-130 (a contagem de 2 pontos de uso)
    - web/tests/test_carteira_opcoes_tira.mjs linhas 170-215 (conferir, não editar às cegas)
    - docs/adr/027-consumo-do-servico-mcp-autenticado.md linhas 210-250 e 305-345 (Emendas 1 e 2 — o formato exato: título com fase e data, o problema, a decisão, a consequência aceita, o guardião)
    - web/src/opcoes/PropostaLastreada.jsx e web/src/opcoes/OpcoesScreen.jsx (os dois lados da fiação que a Emenda registra)
  </read_first>
  <action>
**Guardiões — nenhum apagado; cada um reaponta para o lugar novo do MESMO
invariante, com comentário `// ATUALIZADO 2026-09-13 (Fase 28, 28-03): ...`.**

`web/tests/test_opcoes_proposta_ui.mjs`:
- Linhas 68-69 (`<PropostaLastreada` antes de `<OpcoesCamada` no JSX de uso):
  o invariante era "no card do ativo, a proposta vem ACIMA da cadeia" (D-4 do
  14-UI-SPEC). Com o card fora do `AtivoCard`, a ordem não existe mais ali.
  Substituir por três asserções: (a) `<OpcoesCamada` continua em `app`;
  (b) o bloco `opGate && opGate.liquida` de `AtivoCard` NÃO contém
  `PropostaLastreada`; (c) `<PropostaLastreada` aparece em
  `web/src/opcoes/OpcoesScreen.jsx` — a proposta não desapareceu, mudou de
  tela. Comentário deve dizer isso com a data e citar 28-CONTEXT D1.
- Linha 277 (`bloco opGate.liquida referencia PropostaLastreada`): trocar por
  `opGateBlock.includes("OpcoesCamada")` mais a negativa
  `!opGateBlock.includes("PropostaLastreada")`, com nota datada. O resto do
  IIFE de paridade `vm` (Watchlist × `radarVm`) continua íntegro e segue
  medindo o mesmo: todo campo de `vm` lido dentro do bloco existe nos DOIS call
  sites — agora com `OpcoesCamada` como único conteúdo do bloco. A asserção de
  sanidade `camposUsados.includes("cp")` continua valendo (`rotuloFechado` lê
  `cp.verCadeiaCompleta`); se ela quebrar, é sinal de que a Task 1 removeu
  `rotuloFechado` — PARAR e corrigir a Task 1, não afrouxar o teste.

`web/tests/test_opcoes_multi_candidato_ui.mjs`:
- Linhas 119-122 (`<PropostaLastreada` exatamente 2× no fonte inteiro): o
  invariante era "o componente tem exatamente 2 pontos de uso; o ramo multi não
  criou um 3º". Ele continua verdadeiro, só que os dois pontos agora estão em
  arquivos diferentes. Reescrever para contar sobre os DOIS consumidores:
  exatamente 1× em `App.jsx` (PropostaDaPosicao) e exatamente 1× em
  `OpcoesScreen.jsx` (SubAbaOperar), total 2 — e 0× em qualquer outro arquivo
  de `web/src/` (varrer `web/src/**/*.jsx` e afirmar que só os dois têm a tag).
  Nota datada explicando o movimento e por que o TETO de 2 continua sendo a
  regra: um terceiro ponto de uso é a duplicação de experiência que a Fase 28
  existiu para fechar.

`web/tests/test_carteira_opcoes_tira.mjs`: conferir que nada quebrou. Editar
SOMENTE se uma asserção falhar, e nesse caso investigar primeiro se a falha é
defeito da Task 1 (busca removida por engano) antes de ajustar o teste.

**Emenda 3 ao ADR-027** — acrescentar ao fim de
`docs/adr/027-consumo-do-servico-mcp-autenticado.md`, no MESMO formato das
Emendas 1 e 2 (título `## Emenda 3 (Fase 28, 2026-09-13) — <frase>`, depois: o
problema, a decisão, a consequência aceita, o guardião):

- **O problema.** A Decisão 3 isolou a aba do núcleo: `OpcoesScreen.jsx` não
  importa `App.jsx`. Isso resolveu o ciclo (App.jsx importa a aba) e manteve a
  aba auditável sozinha — mas transformou "trazer a operação lastreada para
  dentro da aba" em escolha entre duas coisas ruins: duplicar o componente
  (duas manchetes de motor divergindo na próxima correção, em silêncio) ou
  quebrar o isolamento.
- **A decisão.** Componente de UI compartilhado entre o núcleo e a aba vive em
  `web/src/opcoes/`, é importado pelos DOIS, e **não importa nenhum dos dois**.
  A fiação é de uma via para cada lado: `App.jsx → módulo` e
  `OpcoesScreen.jsx → módulo`, nunca `módulo → App.jsx` nem
  `App.jsx → OpcoesScreen.jsx`. É a mesma técnica das Emendas 1 e 2, aplicada a
  componente de apresentação em vez de a dado. O primeiro caso é
  `web/src/opcoes/PropostaLastreada.jsx` (Fase 28).
- **A consequência aceita, declarada.** O módulo não pode importar os tokens,
  o `price` nem o `FONTE_LABEL` de `App.jsx`, então os declara localmente —
  mesmo padrão já em uso por `OpcoesScreen.jsx`, `SetupChart.jsx` e
  `pet/BorisChat.jsx`. A cópia é real e o risco é divergir o formatador de
  dinheiro entre duas telas; por isso ela é MEDIDA, não confiada: guardião de
  igualdade caractere a caractere das linhas de formatador entre `App.jsx` e o
  módulo (28-01, Task 3).
- **O guardião.** `web/tests/test_opcoes_mcp_aba_ui.mjs` mede o invariante de
  duas vias (nenhum dos três importa o outro na direção proibida);
  `web/tests/test_opcoes_subabas_ui.mjs` mede que a aba não ganhou caminho de
  execução próprio; `web/tests/test_opcoes_multi_candidato_ui.mjs` mede o teto
  de 2 pontos de uso.
- **O que a Emenda NÃO decide.** Ela não unifica `PropostaLastreada` com
  `PayoffChart.jsx` (o payoff do caminho MCP). São fontes de dado diferentes —
  motor interno × serviço MCP —, a mesma distinção que a Emenda 2 já aceitou
  por escrito para a leitura técnica. A unificação, se vier, é ADR próprio com
  gatilho medido, no mesmo formato da Decisão 3 e da Emenda 2.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/executar.sh --testes` (com `dangerouslyDisableSandbox: true`) verde: 0 falhas em pytest, todos os `.mjs` passando
    - `git diff web/tests/` não remove nenhuma linha `ok("` sem substituí-la por `ok("` medindo o mesmo invariante; cada substituição tem comentário com `2026-09-13` e `Fase 28`
    - `web/tests/test_opcoes_multi_candidato_ui.mjs` conta 1× em `App.jsx` + 1× em `OpcoesScreen.jsx` e afirma 0× em qualquer outro `.jsx` de `web/src/`
    - `web/tests/test_opcoes_proposta_ui.mjs` contém a asserção negativa `!opGateBlock.includes("PropostaLastreada")` e a positiva de `OpcoesCamada`
    - `docs/adr/027-consumo-do-servico-mcp-autenticado.md` contém uma seção iniciada por `## Emenda 3 (Fase 28, 2026-09-13)` com as cinco partes (problema / decisão / consequência aceita / guardião / o que não decide)
    - A Emenda 3 nomeia os três arquivos de guardião e o arquivo do módulo pelo caminho completo
  </acceptance_criteria>
  <done>Suíte canônica verde; os três guardiões medem o invariante no lugar novo com nota datada; Emenda 3 escrita no formato das Emendas 1 e 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação ao vivo — abrir e fechar lastreada pela sub-aba Operar</name>
  <read_first>
    - .planning/phases/28-aba-opcoes-sub-aba-operar/28-02-SUMMARY.md (o que a sub-aba Operar entrega e o que já foi provado por teste estático)
    - CLAUDE.md seção "Validação obrigatória" (a suíte canônica e o `npx vite build`)
  </read_first>
  <action>
Parar a execução e apresentar ao Alex os 9 passos de `<how-to-verify>` abaixo,
na ordem. Nada de push, nada de `scripts/bump.sh`, nada de `publicar-web.sh`
antes da aprovação — fase com checkpoint bloqueante segura o push da FASE
INTEIRA (lição registrada em `checkpoint-bloqueante-nao-push-antes`), e a
publicação do front está explicitamente fora do escopo desta fase
(28-CONTEXT, Guardrails). Se algum passo falhar, registrar o número do passo e
o sintoma observado e reportar, em vez de contornar.
  </action>
  <what-built>
A aba Opções ganhou duas sub-abas. "Setups" é a tela da Fase 27, sem mudança.
"Operar" é nova: escolher uma posição da carteira mostra o lastro livre, a
leitura técnica interna e a proposta lastreada do motor (manchete, payoff
numérico com ganho máximo / perda máxima / breakevens, e CTA de abrir ou
fechar em Modo Operador). O card de proposta que ficava no card do ativo em
Watchlist/Radar foi removido — a proposta de um ativo agora vive na aba.
`PropostaLastreada` e o caminho de aceite passaram a existir num módulo só
(`web/src/opcoes/PropostaLastreada.jsx`), importado por `App.jsx` e por
`OpcoesScreen.jsx`, sem nenhum dos dois importar o outro (Emenda 3 ao ADR-027).

Verificado automaticamente: suíte canônica (pytest + `.mjs`) verde,
`npx vite build` verde, 12+ asserções novas de guardião, 3 injeções de defeito
vermelhas. **Não verificado por teste estático:** que a operação REALMENTE abre
e fecha pela sub-aba nova, e que nada na aba gasta cota do MCP ao abrir — as
duas coisas que só o app rodando mostra (lição registrada em
`camada-de-entendimento` e no STATE da Fase 27: três defeitos que só a
verificação ao vivo pegou).
  </what-built>
  <how-to-verify>
Rodar o app local (`bash scripts/executar.sh` no modo de sempre) com uma conta
em **Modo Operador** e ao menos uma posição real na carteira, num ativo com
cadeia líquida.

1. **Setups não regrediu.** Abrir a aba Opções. Confirmar que ela abre na
   sub-aba "Setups" e que está igual à de ontem: lista de posições, bloco
   "Seus vigias", e nenhum ativo pré-selecionado. Escolher um ativo e conferir
   que o lastro e a leitura interna (tendência, HV, suporte/resistência)
   aparecem como antes.
2. **Custo zero.** Com a aba de rede do navegador aberta (ou o log do
   servidor), alternar para "Operar" e escolher um ativo. Confirmar que as
   ÚNICAS chamadas são `/api/options/tecnico/...`, `/api/options/gate` e
   `/api/options/proposta` — e **nenhuma** `/api/options/mcp/...`. Se aparecer
   alguma `mcp`, é defeito: reportar em vez de aprovar.
3. **Abrir.** Em "Operar", com o ativo escolhido, confirmar que a proposta
   aparece com manchete, payoff numérico (ganho máximo, perda máxima,
   breakevens) e o botão de abrir. Clicar, passar pelos confirms, e conferir
   que a posição lastreada aparece na Carteira com a trava de lastro correta.
4. **Fechar.** Voltar à sub-aba "Operar" no mesmo ativo e conferir que o CTA
   agora é de FECHAR (a proposta casou com a posição aberta). Fechar e
   conferir o saldo e o destravamento do lastro na Carteira.
5. **Collar.** Se o motor propuser collar para alguma posição, repetir 3 e 4
   com ele (é o caminho de 2 pernas, o mais frágil).
6. **Modo Estudo.** Trocar para Modo Estudo e voltar em "Operar": confirmar que
   a MESMA proposta aparece com a explicação e **nenhum botão de executar**.
7. **A remoção.** Ir em Watchlist e em Radar: confirmar que o card de proposta
   lastreada não aparece mais em card nenhum, e que a linha "opções líquidas
   disponíveis" (o acordeão de cadeia) continua abrindo normalmente.
8. **Posições.** Ir no Portfólio: confirmar que a tira "Oportunidades de
   opções" continua ali e que o detalhe de opções dentro do card de uma
   posição continua abrindo e operando como antes.
9. **Sem perder o ativo.** Escolher um ativo em "Operar", alternar para
   "Setups" e voltar: o ativo continua escolhido.

**Não dar push nem publicar antes da aprovação** — regra do repositório para
fase com checkpoint bloqueante, e a publicação do front está explicitamente
fora do escopo desta fase (28-CONTEXT, Guardrails).
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva o que falhou, em qual dos 9 passos.</resume-signal>
  <verify>
    <human-check>Alex confirma os 9 passos, com atenção especial ao passo 2 (nenhuma chamada `/api/options/mcp/` ao abrir a sub-aba ou escolher ativo), ao 4 (o CTA vira FECHAR quando a proposta casa com a posição aberta) e ao 6 (Modo Estudo sem botão de executar)</human-check>
    <automated>bash scripts/executar.sh --testes 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Alex respondeu "aprovado", ou os defeitos encontrados estão registrados por número de passo e sintoma
    - Passo 2 registrado no SUMMARY com as chamadas de rede observadas: o positivo (`/api/options/tecnico`, `/api/options/gate`, `/api/options/proposta`) e o negativo (0 chamadas a `/api/options/mcp/`)
    - Nenhum commit empurrado e nenhum bump/publicação executado antes da aprovação (`git log origin/main..HEAD` mostra os commits da fase ainda locais)
  </acceptance_criteria>
  <done>Alex aprovou a verificação ao vivo dos 9 passos, ou os defeitos estão registrados e a fase segue aberta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Watchlist/Radar → execução lastreada | fronteira que este plano FECHA: deixa de existir caminho de abrir/fechar operação lastreada a partir do card do ativo |
| `AtivoCard` → `/api/options/gate`, `/api/options/proposta` | as duas buscas permanecem, agora servindo só ao rótulo do acordeão de cadeia |
| aba Opções (sub-aba Operar) → `A.abrirLastreada`/`abrirCollar`/`fecharLastreada` | passa a ser, com o detalhe de posição em Portfólio, o único caminho de execução lastreada na UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-16 | Denial of Service | confirm/busy órfão em `AtivoCard` dessincronizando a UI | mitigate | `opPropostaBusy`, `posAberta`, `onAbrirLastreada` e `onFecharLastreada` são REMOVIDOS, não deixados inalcançáveis; critério de aceite é `grep -c` = 0 para os quatro. Um `setOpPropostaBusy(true)` sobrevivente sem caminho de `false` travaria botão de outro fluxo no mesmo card |
| T-28-17 | Denial of Service | remoção excessiva quebrando o `OpcoesCamada` | mitigate | Lista explícita de "o que FICA e por quê" em `<interfaces>`; verificação automatizada afirma presença de `<OpcoesCamada`, `const [opProposta`, `cp.verCadeiaCompleta` e a contagem 2× de `store.optionsGate(`/`store.optionsProposta(` — se a contagem cair, o plano parou |
| T-28-18 | Tampering | rota ou método de store mudando na remoção | mitigate | `A.abrirLastreada(`/`A.abrirCollar(`/`A.fecharLastreada(` vão a 0 CALL SITES em `App.jsx`, mas as DEFINIÇÕES no objeto `A` continuam 1× cada — critério de aceite separa as duas coisas. Nenhuma rota de backend, nenhum corpo de requisição e nenhum método de `persistence.js` é tocado nesta fase |
| T-28-19 | Repudiation | invariante do teto de 2 pontos de uso diluído ao mudar de contagem por arquivo para contagem por repositório | mitigate | O guardião reescrito afirma 1× em `App.jsx` + 1× em `OpcoesScreen.jsx` E 0× em qualquer outro `.jsx` de `web/src/` — o teto fica mais forte, não mais frouxo |
| T-28-20 | Repudiation | Emenda 3 escrita como comentário em vez de decisão registrada | mitigate | A Emenda vai no ADR-027, no formato das Emendas 1 e 2, nomeando os três guardiões e o módulo pelo caminho completo; e declara explicitamente o que NÃO decide (unificação com `PayoffChart.jsx`) |
| T-28-21 | Elevation of Privilege | operação lastreada executando por caminho não verificado ao vivo | mitigate | Checkpoint bloqueante com os 9 passos, incluindo abrir E fechar sobre posição real, collar quando houver, e o negativo de Modo Estudo. Sem push e sem publicação antes da aprovação |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote instalado ou atualizado nesta fase |
</threat_model>

<verification>
1. `cd web && npx vite build` — verde
2. `bash scripts/executar.sh --testes` fora do sandbox — 0 falhas, contra a baseline registrada no 28-01-SUMMARY
3. `grep -c "<PropostaLastreada" web/src/App.jsx` = 1 e `grep -c "<PropostaLastreada" web/src/opcoes/OpcoesScreen.jsx` = 1
4. `grep -c "A.abrirLastreada(" web/src/App.jsx` = 0; `grep -c "abrirLastreada:" web/src/App.jsx` = 1
5. `grep -c "opPropostaBusy\|const posAberta =\|const onAbrirLastreada\|const onFecharLastreada" web/src/App.jsx` = 0
6. `grep -c "Emenda 3" docs/adr/027-consumo-do-servico-mcp-autenticado.md` >= 1
7. Checkpoint humano aprovado (9 passos)
</verification>

<success_criteria>
- O card de proposta lastreada não existe mais em Watchlist/Radar; o acordeão de cadeia continua
- Nenhum handler, estado ou derivado órfão sobrou em `AtivoCard`
- `OportunidadesOpcoes` e `PropostaDaPosicao` intocados
- Teto de 2 pontos de uso de `PropostaLastreada` preservado e medido por arquivo
- Emenda 3 ao ADR-027 escrita, nomeando o módulo e os guardiões
- Suíte canônica e `npx vite build` verdes
- Abrir e fechar venda coberta / put / collar pela sub-aba Operar verificado no app rodando, com custo zero de MCP confirmado na aba de rede
</success_criteria>

<output>
Create `.planning/phases/28-aba-opcoes-sub-aba-operar/28-03-SUMMARY.md` when done.
Registrar no SUMMARY: as contagens de `grep` antes/depois; a lista de guardiões
reapontados; as chamadas de rede observadas no passo 2 do checkpoint (o
positivo E o negativo de `/api/options/mcp/`); e o estado de publicação (fase
fica TESTADA e não publicada — bump + `publicar-web.sh` é passo humano separado,
fora do escopo desta fase).
</output>
