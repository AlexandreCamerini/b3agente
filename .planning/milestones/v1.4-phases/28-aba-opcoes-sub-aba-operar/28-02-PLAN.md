---
phase: 28-aba-opcoes-sub-aba-operar
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - web/src/opcoes/OpcoesScreen.jsx
  - web/src/copy.js
  - web/tests/test_opcoes_subabas_ui.mjs
autonomous: true
requirements: [SC-1, SC-4]

must_haves:
  truths:
    - "A aba Opções tem duas sub-abas visíveis e alternáveis: 'Setups' (o que a Fase 27 entrega) e 'Operar' (nova)"
    - "Em 'Setups', a tela é idêntica à de hoje — mesma ordem, mesmos blocos, mesmos custos declarados"
    - "Em 'Operar', escolher uma posição da carteira mostra a proposta lastreada daquela posição, com manchete do motor, payoff numérico e CTA de abrir/fechar em Modo Operador"
    - "Abrir 'Operar' e escolher um ativo não gasta UMA chamada do cap do MCP (só rotas internas de custo 0)"
    - "Em Modo Estudo a sub-aba Operar mostra a explicação e NENHUM botão de executar"
    - "Trocar de sub-aba não perde o ativo escolhido"
  artifacts:
    - path: "web/src/opcoes/OpcoesScreen.jsx"
      provides: "estado de sub-aba, o alternador e o componente SubAbaOperar"
      contains: "useState(\"setups\")"
    - path: "web/src/copy.js"
      provides: "rótulos e textos da sub-aba Operar nos DOIS modos"
      contains: "opcoesSubabaOperar"
    - path: "web/tests/test_opcoes_subabas_ui.mjs"
      provides: "guardião das duas sub-abas, do custo zero e do split de modo"
  key_links:
    - from: "web/src/opcoes/OpcoesScreen.jsx"
      to: "web/src/opcoes/PropostaLastreada.jsx"
      via: "import estático (módulo do 28-01)"
      pattern: "from \"\\./PropostaLastreada\\.jsx\""
    - from: "SubAbaOperar"
      to: "store.optionsGate / store.optionsProposta"
      via: "useEffect por ticker escolhido"
      pattern: "store\\.options(Gate|Proposta)\\("
    - from: "SubAbaOperar"
      to: "A.abrirLastreada / A.abrirCollar / A.fecharLastreada"
      via: "useAceiteLastreado (módulo do 28-01)"
      pattern: "useAceiteLastreado\\("
---

<objective>
Dividir a aba Opções em duas sub-abas — "Setups" (tudo que a Fase 27 entrega,
intocado) e "Operar" (nova) — e fazer a sub-aba Operar renderizar a proposta
lastreada de uma posição da carteira usando o módulo extraído no 28-01.

Purpose: é a resposta direta a dois pedidos do Alex de uma vez. (1) Trazer a
operação de opções para DENTRO da aba: hoje abrir/fechar venda coberta, put de
proteção e collar só existe no card do ativo em Watchlist/Radar, longe da aba
que fala de opções. (2) A aba está confusa, com informação demais: dividir em
"monitorar" × "decidir/executar" é simplificação estrutural, não polimento.

Output: `OpcoesScreen.jsx` com alternador de sub-aba e `SubAbaOperar`; as
chaves de copy novas nos dois modos; e um guardião novo que trava o custo zero
de abrir a sub-aba, o split de modo e o caminho de aceite único.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-aba-opcoes-sub-aba-operar/28-CONTEXT.md
@.planning/phases/28-aba-opcoes-sub-aba-operar/28-01-SUMMARY.md
@CLAUDE.md
@docs/adr/027-consumo-do-servico-mcp-autenticado.md

Decisões desta fase que este plano executa (28-CONTEXT.md):
- **D2** — "Setups" = a `OpcoesScreen` de hoje INTEIRA. Nada sai dela. "Operar"
  é ADITIVA: reusa a mesma lista de posições e a mesma leitura técnica interna
  (custo zero, já disponível) porque leitura técnica e lastro são a evidência
  que sustenta a decisão de operar — tirá-las de "Operar" forçaria ida e volta
  entre as sub-abas para decidir uma coisa só.
- **D3** — dentro de "Operar": lista de posições → clicar numa → ver a proposta
  daquela posição. MESMO gesto que "Setups" já usa (o seletor de chips), não
  uma tira horizontal no topo.
- Universo = CARTEIRA (`ctx.data.positions`), nunca watchlist — régua da Fase 27
  (D3), sem chamada nova.

Guardrail que decide o desenho, não uma preferência:
**ADR-027 §3.3 — custo de MCP só em clique explícito, nunca ao abrir tela.**
Abrir a sub-aba Operar e escolher um ativo nela não pode tocar
`mcp.semente.dev`. As três rotas que Operar usa são INTERNAS e de custo zero
de cota: `/api/options/gate`, `/api/options/proposta`, `/api/options/tecnico`.

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código em 2026-09-13. -->

Do módulo criado no 28-01 (`web/src/opcoes/PropostaLastreada.jsx`):

  export default function PropostaLastreada({ r, operador, cp, busy, onAbrir, onFechar, posAberta, onVerbeteLiquidez })
  export function FonteDoDadoProposta({ r, cp })
  export function useAceiteLastreado({ A, cp, ticker })
    -> { busy, aceitarCandidato(p), fecharLastreada(r) }

Já existentes em `OpcoesScreen.jsx`, reusáveis VERBATIM de dentro do próprio
arquivo (declarações de função hoistam; os estilos são const de módulo):

  OpcoesScreen.jsx:144  function Kicker({ children })
  OpcoesScreen.jsx:152  function Aviso({ children, tom })
  OpcoesScreen.jsx:162  const BOTAO
  OpcoesScreen.jsx:186  const CAIXA
  OpcoesScreen.jsx:196  const AJUDA
  OpcoesScreen.jsx:1307 function LastroDoAtivo({ pos, cp })
  OpcoesScreen.jsx:1369 function LeituraInterna({ tecnico, cp })

Estado e derivados já existentes em `OpcoesScreen` (`export default function
OpcoesScreen({ ctx })`, linha 270) que a sub-aba Operar COMPARTILHA (não
duplica):

  OpcoesScreen.jsx:271-273  cp / store / palette vindos de ctx
  OpcoesScreen.jsx:288      const carteira = ((ctx && ctx.data && ctx.data.positions) || []).filter((p) => p && p.t)
  OpcoesScreen.jsx:300      const [ticker, setTicker] = useState("")
  OpcoesScreen.jsx:307      tecnico  (do useOpcoesMcp — leitura interna, custo 0)
  OpcoesScreen.jsx:338      const escolherTicker = (t) => { setTicker(t === ticker ? "" : t); ... }
  OpcoesScreen.jsx:531      const posicaoSelecionada = ticker ? carteira.find((p) => p.t === ticker) : null
  OpcoesScreen.jsx:636-649  const seletor = (...)   // o chip de posição: aria-pressed, minHeight 44px, T.accent/T.accentTint10/T.bgPanel
  OpcoesScreen.jsx:651-656  <h1>{cp.tituloOpcoes}</h1> + <p>{cp.subtituloOpcoes}</p>
  OpcoesScreen.jsx:658-676  cabecalho / blocoVigias / LastroDoAtivo / LeituraInterna / blocoLeituraDoServico
  OpcoesScreen.jsx:681-753  cascata carregando / erro / vazio (TUDO da sub-aba Setups)

Padrão visual do alternador (reusar, NÃO inventar): o próprio `seletor` de
`OpcoesScreen.jsx:636-649` já é a régua de pill/chip desta tela —
`minHeight: "44px"`, `padding: "8px 14px"`, `borderRadius: "11px"`,
`border: 1px solid ${ativo ? T.accent : T.borderSubtle}`,
`background: ativo ? T.accentTint10 : T.bgPanel`,
`color: ativo ? T.accent : T.textSecondary`, `fontWeight: 700`,
`fontSize: "13px"`, e `aria-pressed` no botão (mesma afordância que o
`BottomNav` de `App.jsx:1022` usa; este app não tem `role="tab"`/`tablist` em
lugar nenhum — não introduzir um padrão novo de ARIA aqui).

Referência de como a proposta é buscada hoje, a espelhar em Operar
(`App.jsx:3502-3523`, dentro de `AtivoCard`): dois efeitos em cascata — gate
primeiro (`store.optionsGate(t)`), e só com `gate.liquida` a proposta
(`store.optionsProposta(t, true)`); os dois best-effort (`.catch` silencioso),
os dois com flag de invalidação para que resposta de PETR4 nunca pinte a tela
de VALE3. `store.optionsGate`/`store.optionsProposta` existem nos DOIS stores
(`web/src/persistence.js`) — nenhum método novo é necessário, a paridade
`deviceStore`↔`serverStore` não é tocada.

Contrato da resposta de `/api/options/proposta` que `PropostaLastreada`
consome (o `r`): `{ ticker, source, at, providerStatus, proposta, candidatos, motivo, motivoTexto }`.
`posAberta` é derivado pelo consumidor:
`((ctx.data.optionPositions) || []).filter((p) => p.underlying === ticker).find((p) => p.id === r.proposta.contractSymbol) || null`
(é a fórmula de `App.jsx:4341-4344`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chaves de copy da sub-aba Operar nos dois modos</name>
  <files>web/src/copy.js</files>
  <read_first>
    - web/src/copy.js linhas 90-110 e 655-670 (os pares `tabOpcoes`/`tituloOpcoes`/`subtituloOpcoes` nos dois ramos — o padrão de onde as chaves `opcoes*` moram e como o comentário justifica quando o texto é IGUAL nos dois modos)
    - web/tests/test_copy_theme.mjs linhas 20-40 (o guardião de paridade de chaves entre COPY.estudo e COPY.operador, e a chamada cega de toda função com ("X","Y","Z"))
    - web/tests/test_opcoes_mcp_aba_ui.mjs linhas 330-345 (o guardião de paridade das chaves que começam com `opcoes`)
  </read_first>
  <action>
Adicionar as cinco chaves abaixo em `COPY.estudo` E em `COPY.operador`, junto
do bloco de chaves `opcoes*` de cada ramo (paridade de chaves é exigida por
`web/tests/test_copy_theme.mjs`; paridade da fatia `opcoes*` por
`web/tests/test_opcoes_mcp_aba_ui.mjs:336-337`):

- `opcoesSubabaSetups` — rótulo do pill da primeira sub-aba. Estudo e Operador:
  `"Setups"` (é o nome do artefato, não uma ação — igual nos dois modos pela
  mesma razão já documentada em `tituloOpcoes`; registrar essa razão em
  comentário, senão o próximo leitor toma por esquecimento).
- `opcoesSubabaOperar` — rótulo do pill da segunda sub-aba. Estudo:
  `"Operação"` (substantivo: Estudo descreve, não manda). Operador: `"Operar"`
  (imperativo: é o modo que executa). Os dois DIFERENTES de propósito — é a
  regra de vocabulário por modo do repositório (`skill_ref.py` /
  `web/src/copy.js`), e o comentário deve dizer isso.
- `opcoesOperarIntro` — uma frase de contexto no topo da sub-aba. Estudo: diz
  que ali se vê a estrutura lastreada que o motor propõe para cada posição da
  carteira, com ganho máximo, perda máxima e breakevens em número, e que nada
  é enviado a corretora nenhuma. Operador: diz o mesmo sobre o conteúdo e que
  abrir e fechar acontece ali. Nenhuma das duas promete resultado, nenhuma usa
  linguagem de enriquecimento (CLAUDE.md princípio 8).
- `opcoesOperarEscolherPosicao` — estado de convite quando não há posição
  escolhida. Estudo: "Escolha uma posição da carteira para ver a estrutura
  lastreada que o motor propõe para ela." Operador: variação com a mesma
  informação e voz de mesa. É ESTADO, não ação — sem botão (mesmo precedente de
  `cp.opcoesEscolherAtivo`).
- `opcoesOperarSemLiquidez` — função de UM argumento (o ticker), usada quando
  o gate recusou: diz que não há contrato com liquidez confirmada para aquele
  ativo agora, que sem cadeia líquida o motor não monta estrutura e que nada
  foi estimado no lugar (CLAUDE.md princípio 4). Nos dois modos. Precisa
  tolerar ser chamada com `("X","Y","Z")` pelo guardião de `test_copy_theme.mjs`
  — usar só o primeiro parâmetro.

PROIBIDO: qualquer texto novo que componha a manchete ou a didática do motor
(o guardião global `"Se você tivesse"` e `"Vender " +` cobre o repositório
inteiro); qualquer chave que prometa lucro, acerto ou rentabilidade; renomear
ou remover chave existente.
  </action>
  <verify>
    <automated>cd web && node -e "import('./src/copy.js').then(({COPY})=>{const k=['opcoesSubabaSetups','opcoesSubabaOperar','opcoesOperarIntro','opcoesOperarEscolherPosicao','opcoesOperarSemLiquidez']; for(const m of ['estudo','operador']) for(const x of k) if(!(x in COPY[m])) throw new Error(m+' sem '+x); if(COPY.estudo.opcoesSubabaOperar===COPY.operador.opcoesSubabaOperar) throw new Error('opcoesSubabaOperar igual nos dois modos'); if(typeof COPY.estudo.opcoesOperarSemLiquidez!=='function') throw new Error('opcoesOperarSemLiquidez deve ser funcao'); const e=Object.keys(COPY.estudo).sort().join(','), o=Object.keys(COPY.operador).sort().join(','); if(e!==o) throw new Error('paridade de chaves quebrada'); console.log('ok');})"</automated>
  </verify>
  <acceptance_criteria>
    - As cinco chaves existem em `COPY.estudo` e em `COPY.operador`
    - `Object.keys(COPY.estudo).sort()` === `Object.keys(COPY.operador).sort()` (paridade intacta)
    - `COPY.estudo.opcoesSubabaOperar !== COPY.operador.opcoesSubabaOperar`
    - `COPY.estudo.opcoesSubabaSetups === COPY.operador.opcoesSubabaSetups`, com comentário no código dizendo por que é igual
    - `typeof COPY.estudo.opcoesOperarSemLiquidez === "function"` e chamá-la com `("X","Y","Z")` não lança
    - `node web/tests/test_copy_theme.mjs` e `node web/tests/test_opcoes_mcp_aba_ui.mjs` saem com código 0
    - Nenhum valor novo contém "Se você tivesse" nem `"Vender " +`
  </acceptance_criteria>
  <done>As cinco chaves existem nos dois modos, com paridade de chaves intacta e vocabulário por modo respeitado onde a voz muda.</done>
</task>

<task type="auto">
  <name>Task 2: Alternador de sub-aba e SubAbaOperar em OpcoesScreen.jsx</name>
  <files>web/src/opcoes/OpcoesScreen.jsx</files>
  <read_first>
    - web/src/opcoes/OpcoesScreen.jsx linhas 20-45 (bloco de imports — onde a linha do módulo novo entra)
    - web/src/opcoes/OpcoesScreen.jsx linhas 270-360 (assinatura, ctx, carteira, estado de ticker, escolherTicker)
    - web/src/opcoes/OpcoesScreen.jsx linhas 525-560 (posicaoSelecionada e vizinhança)
    - web/src/opcoes/OpcoesScreen.jsx linhas 636-760 (o seletor de chips e o `return (<section>...` inteiro da cascata atual — o que vira o ramo "Setups")
    - web/src/opcoes/OpcoesScreen.jsx linhas 1300-1400 (LastroDoAtivo e LeituraInterna, os dois blocos de custo zero que Operar reusa)
    - web/src/opcoes/PropostaLastreada.jsx (os exports do 28-01 e a assinatura de useAceiteLastreado)
    - web/src/App.jsx linhas 3490-3525 (a cascata gate→proposta a espelhar: ordem, best-effort, invalidação)
    - web/src/App.jsx linhas 4336-4344 (a fórmula de posAberta)
    - docs/adr/027-consumo-do-servico-mcp-autenticado.md seção §3.3 (a regra de custo por clique)
  </read_first>
  <action>
1. **Import.** Adicionar em `OpcoesScreen.jsx`, junto dos imports irmãos
   (`./PayoffChart.jsx`, `./CriarSetup.jsx`):
   `import PropostaLastreada, { FonteDoDadoProposta, useAceiteLastreado } from "./PropostaLastreada.jsx";`
   Nenhum import de `../App.jsx` — o isolamento do ADR-027 é o ponto da fase.

2. **Estado da sub-aba.** Dentro de `OpcoesScreen`, junto do
   `const [ticker, setTicker] = useState("")` (linha 300):
   `const [subaba, setSubaba] = useState("setups");`
   Comentário: nasce em "setups" porque é a tela que a Fase 27 entregou e a que
   abre de graça; e o ticker é COMPARTILHADO entre as duas sub-abas de
   propósito — quem escolheu PETR4 para ler não deve reescolher para operar.

3. **Alternador.** Declarar, ao lado do `seletor` (~linha 636), um
   `const subabas = (...)` com dois `<button type="button">`, um por sub-aba,
   usando SÓ chaves de token que `TOKENS` (`OpcoesScreen.jsx:42-43`) já
   declara — `accent`, `accentTint10`, `bgPanel`, `borderSubtle`,
   `textSecondary`. Nenhuma chave nova entra em `TOKENS` neste plano (uma
   referência a `T.<chave>` fora de `TOKENS` sai `undefined` em silêncio, sem
   erro de build),
   reusando EXATAMENTE a régua visual do `seletor` (ver `<interfaces>`:
   `minHeight: "44px"`, `padding: "8px 14px"`, `borderRadius: "11px"`,
   borda/fundo/cor alternando por `T.accent`/`T.accentTint10`/`T.bgPanel`/
   `T.borderSubtle`/`T.textSecondary`, `fontWeight: 700`, `fontSize: "13px"`),
   com `aria-pressed={subaba === id}`. Rótulos de `cp.opcoesSubabaSetups` e
   `cp.opcoesSubabaOperar`, com o fallback literal de uma linha no mesmo estilo
   defensivo do resto do arquivo (`cp.X || "..."`). Nenhum token, cor, raio ou
   escala de tipo NOVOS — `web/tests/test_brand_book_v2_tokens.mjs` trava isso.

4. **Recompor o `return`.** No `return (<section>...)` da linha 651:
   - `<h1>{cp.tituloOpcoes}</h1>`, o `<p>{cp.subtituloOpcoes}</p>` e `{subabas}`
     ficam ACIMA do ramo (são a identidade da aba, valem nas duas sub-abas).
   - Todo o resto do corpo atual — `{cabecalho}`, `{blocoVigias}`,
     `{carteira.length > 0 ? seletor : null}`, `LastroDoAtivo`,
     `LeituraInterna`, `blocoLeituraDoServico` e a cascata inteira de
     carregando/erro/vazio/dados — passa a ser o ramo `subaba === "setups"`,
     sem mover uma linha de dentro dele, sem reordenar, sem remover. D2 é
     literal: "Setups" = a tela de hoje inteira.
   - O ramo `subaba === "operar"` renderiza `<SubAbaOperar ... />`.
   Estrutura: `{subaba === "setups" ? (<>...tela de hoje...</>) : (<SubAbaOperar ... />)}`.

5. **`function SubAbaOperar({ carteira, ticker, posicaoSelecionada, tecnico, cp, ctx, seletor })`**
   — a troca de ativo NÃO entra por prop de callback: entra pelo próprio
   `{seletor}`, que já carrega `onClick={() => escolherTicker(p.t)}` do escopo
   de `OpcoesScreen`. Nenhuma prop `onEscolherTicker` — ela seria um segundo
   caminho de seleção, divergindo do primeiro na correção seguinte.
   — declarar no MESMO arquivo (depois de `OpcoesScreen`, junto de
   `LastroDoAtivo`/`LeituraInterna`), para reusar `Aviso`, `Kicker`, `CAIXA`,
   `AJUDA`, `LastroDoAtivo` e `LeituraInterna` verbatim, sem exportar nada
   novo. Conteúdo, nesta ordem:
   - `const store = ctx && ctx.store;`, `const A = ctx && ctx.A;`,
     `const operador = !!(ctx && ctx.operador);`
   - `const { busy, aceitarCandidato, fecharLastreada } = useAceiteLastreado({ A, cp, ticker });`
     — chamado ANTES de qualquer `return` condicional (regra dos hooks).
   - Dois estados: `const [gate, setGate] = useState(null);` e
     `const [prop, setProp] = useState(null);`
   - Dois `useEffect` em cascata, espelhando `App.jsx:3502-3523` com a mesma
     disciplina: o primeiro depende de `[store, ticker]`, zera `gate` e chama
     `store.optionsGate(ticker)` (best-effort, `.catch` silencioso, flag
     `vivo` no cleanup — a mesma razão documentada em `useOpcoesMcp.js`:
     resposta de PETR4 nunca pinta a tela de VALE3); o segundo depende de
     `[store, ticker, gate && gate.liquida]` (primitivo no array de deps, não
     o objeto) e só chama `store.optionsProposta(ticker, true)` quando
     `gate && gate.liquida`. Sem ticker, os dois zeram e não chamam nada.
     Comentário obrigatório acima dos efeitos: estas DUAS rotas são internas,
     custo ZERO de cota do MCP (`/api/options/gate`, `/api/options/proposta`),
     e é só por isso que podem sair de efeito em vez de clique — a mesma
     justificativa que o ADR-027 já aceitou por escrito para
     `/api/options/tecnico` (Emenda 2) e `/api/options/vigias` (Emenda 1).
     **Nenhuma chamada a `store.mcp*` neste componente.**
   - `const posAberta = ...` pela fórmula de `App.jsx:4341-4344`, usando
     `ctx.data.optionPositions` e `ticker`.
   - Render: `{cp.opcoesOperarIntro}` em parágrafo de apoio; então, na ordem:
     **carteira vazia** (`carteira.length === 0`) → reusar VERBATIM o bloco que
     a sub-aba Setups já tem para esse estado (`OpcoesScreen.jsx:722-731`):
     `<Aviso>{cp.opcoesCarteiraVazia}</Aviso>` com o botão
     `{cp.opcoesIrParaCarteira}` chamando `ctx.goCarteira()` — chaves já
     existentes, nenhuma nova, e o mesmo motivo de produto (Fase 27 D2: carteira
     vazia tem MOTIVO e CAMINHO, e o destino é a Carteira, nunca a watchlist);
     e nada mais, sem `seletor` (não há o que selecionar).
     **Com carteira**: `{seletor}` (o MESMO seletor de chips que a sub-aba
     Setups usa — recebido por prop, não reimplementado: D3 pede o mesmo
     gesto); sem `ticker`, um
     `<Aviso>{cp.opcoesOperarEscolherPosicao}</Aviso>` e nada mais; com
     `ticker`, `<LastroDoAtivo pos={posicaoSelecionada} cp={cp} />` e
     `<LeituraInterna tecnico={tecnico} cp={cp} />` (as duas evidências de
     custo zero que D2 manda manter ao lado da decisão), e então:
     - `gate` carregado e `!gate.liquida` →
       `<Aviso>{(cp.opcoesOperarSemLiquidez || ((t) => ...))(ticker)}</Aviso>`
     - `gate.liquida` →
       `<PropostaLastreada r={prop} operador={operador} cp={cp} busy={busy}
        onAbrir={() => aceitarCandidato(prop && prop.proposta)}
        onFechar={() => fecharLastreada(prop)} posAberta={posAberta}
        onVerbeteLiquidez={(dados) => A && A.abrirVerbete("liquidez-opcao", dados)} />`
     - `gate` ainda `null` com ticker escolhido → `<Aviso>` de consulta em
       curso, reusando `cp.opcoesCarregando` (chave já existente; não criar
       chave nova para isto).
   - **Fora de escopo deste componente, explicitamente:** o ramo
     multi-candidato (`r.candidatos.length > 1` / `CandidatoOpcao`) NÃO é
     replicado aqui — `CandidatoOpcao` continua exclusivo de `App.jsx`
     (PropostaDaPosicao), e trazê-lo exigiria uma quarta cópia do padrão
     visual. Com multi-candidato, `PropostaLastreada` mostra `r.proposta` (o
     candidato principal que o motor devolve), que é o comportamento de hoje em
     `AtivoCard`. Registrar isso em comentário como decisão desta fase, com o
     endereço onde o multi vive, para a Fase 30 não redescobrir.

6. **Nada mais muda.** Sem método novo em `persistence.js` (as duas rotas já
   existem nos dois stores — se faltar em um, PARAR e reportar: paridade
   `deviceStore`↔`serverStore` é invariante, não improviso). Sem rota nova no
   backend. Sem mudança em `useOpcoesMcp.js`. Sem mudança em `CUSTO_DA_ACAO`
   (nenhuma ação nova de MCP foi criada).
  </action>
  <verify>
    <automated>cd web && npx vite build 2>&1 | tail -5 && node -e "const s=require('fs').readFileSync('src/opcoes/OpcoesScreen.jsx','utf8'); if(/from\s+[\"'][^\"']*App\.jsx[\"']/.test(s)) throw new Error('importa App.jsx'); for(const n of ['from \"./PropostaLastreada.jsx\"','useState(\"setups\")','function SubAbaOperar','useAceiteLastreado(','store.optionsGate(','store.optionsProposta(','cp.opcoesSubabaOperar']) if(!s.includes(n)) throw new Error('faltando: '+n); const body=s.slice(s.indexOf('function SubAbaOperar')); if(/store\.mcp/.test(body)) throw new Error('SubAbaOperar chama store.mcp*'); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vite build` verde
    - `OpcoesScreen.jsx` não contém `from ".../App.jsx"` (isolamento ADR-027 intacto)
    - `OpcoesScreen.jsx` contém `useState("setups")`, `function SubAbaOperar`, `useAceiteLastreado(` e o import de `./PropostaLastreada.jsx`
    - `store.optionsGate(` e `store.optionsProposta(` aparecem exatamente 1× cada em `OpcoesScreen.jsx`
    - O corpo de `SubAbaOperar` contém 0 ocorrências de `store.mcp` (custo zero de cota — ADR-027 §3.3)
    - `<PropostaLastreada` aparece exatamente 1× em `OpcoesScreen.jsx`
    - `{seletor}` é passado por prop para `SubAbaOperar` (não há um SEGUNDO bloco de chips no arquivo): `grep -c 'carteira.map(' web/src/opcoes/OpcoesScreen.jsx` = 2 — o mesmo número de hoje. As duas ocorrências atuais são `OpcoesScreen.jsx:527` (`tickersEmCarteira`, uma projeção de tickers) e `OpcoesScreen.jsx:638` (o `seletor`). Se o número virar 3, a sub-aba Operar reimplementou o seletor em vez de reusá-lo — corrigir, não ajustar o teste
    - `git diff --stat web/src/persistence.js web/src/opcoes/useOpcoesMcp.js` vazio
    - `git diff web/src/opcoes/OpcoesScreen.jsx` não REMOVE nenhum dos blocos da Fase 27 (`cabecalho`, `blocoVigias`, `LastroDoAtivo`, `LeituraInterna`, `blocoLeituraDoServico`, a cascata de carregando/erro/vazio/dados) — eles só ganham indentação dentro do ramo `subaba === "setups"`
  </acceptance_criteria>
  <done>A aba abre em "Setups" idêntica a hoje; o pill "Operar"/"Operação" alterna para a sub-aba nova; escolher uma posição nela mostra lastro, leitura interna e a proposta lastreada, com CTA só em Modo Operador.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Guardião das sub-abas — custo zero, split de modo e caminho de aceite único</name>
  <files>web/tests/test_opcoes_subabas_ui.mjs</files>
  <behavior>
    - O teste FALHA se `OpcoesScreen.jsx` importar `App.jsx` ou se `PropostaLastreada.jsx` importar `OpcoesScreen.jsx` (invariante de duas vias da Emenda 3 ao ADR-027)
    - O teste FALHA se o corpo de `SubAbaOperar` contiver qualquer `store.mcp*` (custo de cota ao abrir sub-aba / escolher ativo — ADR-027 §3.3)
    - O teste FALHA se `store.optionsGate(`/`store.optionsProposta(` aparecerem mais de 1× em `OpcoesScreen.jsx` (busca duplicada)
    - O teste FALHA se a chamada de `store.optionsProposta` não estiver guardada por `gate`/`liquida` (proposta pedida sobre ativo sem cadeia líquida)
    - O teste FALHA se `SubAbaOperar` compuser manchete própria em vez de delegar a `PropostaLastreada` (guardrail CVM): nenhum `{p.manchete}`/`manchete` renderizado dentro do corpo de `SubAbaOperar`
    - O teste FALHA se `SubAbaOperar` declarar seu próprio `window.confirm` ou chamar `A.abrirLastreada`/`A.abrirCollar`/`A.fecharLastreada` direto (o caminho de aceite é o hook único do 28-01)
    - O teste FALHA se `SubAbaOperar` derivar o modo de outro lugar que não `ctx.operador` (fonte única de appMode — precedente `test_fase5_appmode_fonte_unica.mjs`)
    - O teste FALHA se `SubAbaOperar` iterar `watchlist` em vez de `carteira`/`positions` (universo = carteira, Fase 27 D3)
    - O teste FALHA se alguma `cp.X` referenciada em `SubAbaOperar` não existir nos DOIS ramos de `COPY`
    - O teste FALHA se o alternador de sub-aba não tiver `aria-pressed` nos dois botões ou não tiver `minHeight: "44px"` (alvo tátil mínimo do app)
    - O teste FALHA se a sub-aba Setups perder qualquer um dos blocos da Fase 27 (`cabecalho`, `blocoVigias`, `seletor`, `LastroDoAtivo`, `LeituraInterna`, `blocoLeituraDoServico` continuam referenciados no arquivo)
  </behavior>
  <action>
Criar `web/tests/test_opcoes_subabas_ui.mjs` no molde dos irmãos
(`web/tests/test_opcoes_mcp_aba_ui.mjs`, `test_opcoes_leitura_interna_ui.mjs`):
leitura estática de fonte com `readFileSync`, remoção de comentários antes das
asserções negativas (o helper `semComentario` de
`test_opcoes_mcp_aba_ui.mjs:50-57` — copiar o padrão, não inventar outro),
`import { COPY } from "../src/copy.js";`, contador `fails` e
`ok(nome, cond, detalhe)`, `process.exit(1)` no fim se houver falha.

Fontes a ler: `web/src/opcoes/OpcoesScreen.jsx`,
`web/src/opcoes/PropostaLastreada.jsx`, `web/src/App.jsx`.

Fatia de `SubAbaOperar`: de `indexOf("function SubAbaOperar")` até o próximo
`indexOf("\nfunction ", i+1)` (o mesmo recorte por marcador de linha que
`test_fase22_componentes_compartilhados.mjs:235` já usa por ser mais estável
que casar chaves). Afirmar primeiro que a fatia foi localizada e tem corpo
(`> 300` caracteres) — asserção de "parse mudo", como os irmãos fazem — para
que um recorte vazio não faça todas as negativas passarem de graça.

Cada item do bloco `<behavior>` acima vira UMA asserção `ok(...)` nomeada com
o POR QUÊ, não só com o quê. O cabeçalho do arquivo lista, em comentário
numerado, as classes de defeito que o guardião trava — mesmo formato do
cabeçalho de `test_opcoes_mcp_aba_ui.mjs`.

Provar o guardião por injeção de defeito, um por vez, revertendo cada um:
(a) trocar `store.optionsProposta(ticker, true)` por uma chamada sem a guarda
de `gate.liquida` → o teste falha; (b) inserir `store.mcpLeitura(ticker)` no
corpo de `SubAbaOperar` → o teste falha; (c) trocar `ctx.operador` por
`ctx.data.config.appMode === "operador"` → o teste falha. Registrar as três
injeções e os três vermelhos no SUMMARY — guardião que nunca ficou vermelho
não é guardião, é decoração.
  </action>
  <verify>
    <automated>node web/tests/test_opcoes_subabas_ui.mjs && bash scripts/executar.sh --testes 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `node web/tests/test_opcoes_subabas_ui.mjs` sai com código 0 e imprime pelo menos 12 linhas `ok `
    - As três injeções de defeito descritas no `<action>` fazem o teste sair com código != 0, e estão registradas no SUMMARY com a saída observada
    - A asserção de "parse mudo" existe: o teste falha se a fatia de `SubAbaOperar` não for localizada
    - `bash scripts/executar.sh --testes` (com `dangerouslyDisableSandbox: true`) verde: 0 falhas em pytest e todos os `.mjs` passando, contagem de arquivos `.mjs` = baseline + 1
  </acceptance_criteria>
  <done>O guardião existe, cobre os 11 invariantes do bloco behavior, ficou vermelho nas três injeções e a suíte canônica está verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sub-aba Operar → `/api/options/gate`, `/api/options/proposta` | rotas internas, custo 0 de cota; decidem se existe cadeia líquida e qual estrutura o motor propõe |
| sub-aba Operar → `A.abrirLastreada`/`abrirCollar`/`fecharLastreada` | ponto onde uma operação lastreada é ABERTA/FECHADA; o servidor re-deriva prêmio/strike e recusa por lastro insuficiente ou 403 de modo |
| `OpcoesScreen.jsx` ↔ `App.jsx` | fronteira de import do ADR-027 Decisão 3 — a aba não conhece o núcleo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-08 | Elevation of Privilege | CTA de executar aparecendo em Modo Estudo dentro da aba Opções | mitigate | `operador` vem de `ctx.operador` (fonte única) e é repassado a `PropostaLastreada`, que já condiciona o botão a `{operador && ...}`. Guardião novo afirma que `SubAbaOperar` não deriva o modo de outra fonte. Defesa real continua sendo o 403 do servidor (T-14-23) |
| T-28-09 | Denial of Service | consumo de cota do MCP ao abrir a sub-aba ou ao escolher ativo (ADR-027 §3.3) | mitigate | `SubAbaOperar` só chama `store.optionsGate`/`store.optionsProposta` (internas, custo 0) e `0` métodos `store.mcp*`; guardião conta `store.mcp` = 0 na fatia do componente e exige a guarda `gate.liquida` antes da proposta |
| T-28-10 | Spoofing | resposta de um ticker pintando a tela de outro | mitigate | Flag `vivo` no cleanup dos dois efeitos e dependência primitiva `gate && gate.liquida` no array de deps — mesma disciplina já documentada em `useOpcoesMcp.js` e `App.jsx:3511-3523` |
| T-28-11 | Information Disclosure | manchete/payoff recomposto na aba em vez de delegado ao motor | mitigate | `SubAbaOperar` não renderiza `manchete` nenhuma: delega a `PropostaLastreada`. Guardião afirma ausência de `manchete` na fatia do componente (guardrail CVM, CLAUDE.md princípio 5) |
| T-28-12 | Tampering | segunda implementação do caminho de aceite (confirm de liquidez/estrutura duplicado ou incompleto) | mitigate | Aceite e fechamento vêm de `useAceiteLastreado` (28-01). Guardião afirma 0 `window.confirm` e 0 `A.abrirLastreada(`/`A.abrirCollar(`/`A.fecharLastreada(` dentro de `SubAbaOperar` |
| T-28-13 | Tampering | estrutura proposta sobre ativo sem lastro (universo virando watchlist) | mitigate | A lista é `carteira` = `ctx.data.positions` (Fase 27 D3); guardião afirma que `SubAbaOperar` não referencia `watchlist`. Garantia estrutural: quem itera é o `seletor` já existente, que só conhece `carteira` |
| T-28-14 | Denial of Service | regressão silenciosa na sub-aba Setups (bloco da Fase 27 perdido no rewrap do JSX) | mitigate | Critério de aceite: o diff só INDENTA os blocos da Fase 27; guardião afirma que os seis blocos continuam referenciados; `test_opcoes_mcp_aba_ui.mjs`, `test_opcoes_analisar_ui.mjs`, `test_opcoes_leitura_interna_ui.mjs` e `test_opcoes_criar_setup_ui.mjs` seguem verdes sem edição |
| T-28-15 | Repudiation | divergência `deviceStore` × `serverStore` | accept | Nenhum método de store novo é criado; `optionsGate`/`optionsProposta` já existem nos dois (usados por `AtivoCard` e pelo hook `useOpcoesPropostas`). Se faltar em um, o plano PARA e reporta em vez de improvisar num só |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote instalado ou atualizado; `package.json`/`package-lock.json` fora de `files_modified` |
</threat_model>

<verification>
1. `cd web && npx vite build` — verde
2. `node web/tests/test_opcoes_subabas_ui.mjs` — verde, ≥12 `ok`
3. Três injeções de defeito no guardião novo → vermelho cada uma → revertidas
4. `bash scripts/executar.sh --testes` fora do sandbox — 0 falhas; arquivos `.mjs` = baseline + 1
5. `grep -c "carteira.map(" web/src/opcoes/OpcoesScreen.jsx` = 1 (o seletor é reusado, não duplicado)
6. `git diff --stat web/src/persistence.js web/src/opcoes/useOpcoesMcp.js web/src/App.jsx` vazio
</verification>

<success_criteria>
- A aba Opções tem duas sub-abas alternáveis, "Setups" e "Operar"/"Operação"
- "Setups" é a tela da Fase 27, sem bloco removido nem reordenado
- "Operar" mostra, para a posição escolhida: lastro livre, leitura técnica interna e a proposta lastreada do motor com payoff numérico
- Modo Operador vê CTA de abrir/fechar; Modo Estudo vê a explicação e nenhum botão
- Abrir a sub-aba e escolher um ativo não gasta cota do MCP
- Trocar de sub-aba preserva o ativo escolhido
- Suíte canônica verde; `npx vite build` verde
</success_criteria>

<output>
Create `.planning/phases/28-aba-opcoes-sub-aba-operar/28-02-SUMMARY.md` when done.
Registrar no SUMMARY: as três injeções de defeito com a saída observada; a
contagem de arquivos `.mjs` antes/depois; e a decisão registrada em código de
NÃO replicar o ramo multi-candidato em `SubAbaOperar` (com o endereço onde ele
vive), para a Fase 30 não redescobrir.
</output>
