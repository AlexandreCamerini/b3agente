---
phase: 30-curadoria-ia-melhores-estruturas
plan: 04
type: execute
wave: 4
depends_on: ["30-03"]
files_modified:
  - web/src/api.js
  - web/src/persistence.js
  - web/src/copy.js
  - web/src/App.jsx
  - web/tests/test_curadoria_ui.mjs
autonomous: false
requirements: [SC-1, SC-3, SC-4, SC-5]
must_haves:
  truths:
    - "Em Posições, ao lado de Oportunidades de opções, aparece um bloco com as 4 melhores vendas cobertas da carteira"
    - "A manchete de cada uma das 4 é renderizada VERBATIM do motor — o front nunca compõe frase (guardrail CVM)"
    - "A ordem exibida é a do payload determinístico e NÃO muda depois da narração da IA"
    - "A narração só é buscada por ação explícita do usuário; o bloco determinístico existe e é útil sem gastar cota"
    - "Os dois stores (deviceStore e serverStore) têm os dois métodos novos, com o mesmo contrato"
  artifacts:
    - path: "web/src/App.jsx"
      provides: "CuradoriaEstruturas + useCuradoria, renderizados em CarteiraScreen"
      contains: "CuradoriaEstruturas"
    - path: "web/src/copy.js"
      provides: "chaves de copy da curadoria nos DOIS modos"
      contains: "curadoriaTitulo"
    - path: "web/src/persistence.js"
      provides: "opcoesCuradoria e opcoesCuradoriaNarrativa nos DOIS stores"
      contains: "opcoesCuradoria"
    - path: "web/tests/test_curadoria_ui.mjs"
      provides: "guardião estático da tira nova"
      contains: "ok("
  key_links:
    - from: "web/src/App.jsx"
      to: "/api/options/curadoria"
      via: "store.opcoesCuradoria() dentro de useCuradoria"
      pattern: "opcoesCuradoria\\("
    - from: "web/src/App.jsx"
      to: "CarteiraScreen"
      via: "render ao lado de OportunidadesOpcoes, sob data.positions.length > 0"
      pattern: "<CuradoriaEstruturas"
    - from: "web/src/persistence.js"
      to: "web/src/api.js"
      via: "paridade deviceStore/serverStore"
      pattern: "api\\.curadoria"
---

<objective>
O bloco em Posições: as 4 melhores vendas cobertas da carteira, com a manchete
do motor verbatim, e — só por ação explícita — o parágrafo da IA sobre elas.

Purpose: é a superfície que o usuário vai ler como "a IA recomenda isto para
ganhar dinheiro" (risco nomeado no 30-CONTEXT). O desenho da tela é o que
mantém honesta a separação que o backend já garante: número e ordem do motor
num bloco, texto da IA em outro, rotulado.
Output: bloco novo em `CarteiraScreen`, irmão de `OportunidadesOpcoes` (D4),
com paridade dos dois stores e guardião estático.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-curadoria-ia-melhores-estruturas/30-CONTEXT.md
@.planning/phases/30-curadoria-ia-melhores-estruturas/30-03-SUMMARY.md
@CLAUDE.md
@.claude/skills/didatica-boris/SKILL.md

NÃO leia `.planning/STATE.md`. NÃO leia `web/src/App.jsx` inteiro (~9500
linhas): use os intervalos de `<read_first>` e `grep -n` para o resto.
Baseline: `bash scripts/executar.sh --testes` exit 0 em `ff91fea`.

<interfaces>
Contrato das duas rotas (Planos 02 e 03), já no ar no servidor local:

`GET /api/options/curadoria` → `{ top: [...], modo, fonte:
"deterministico", at, avaliadas: [tickers], ignoradas: [tickers],
degradados: [tickers], candidatosAvaliados: n, source }`.
Cada item de `top`: `tipo` ("call_coberta"), `ticker`, `contractSymbol`,
`optionType`, `strike`, `expiration`, `diasParaVencimento`, `contratos`,
`qtyAcoes`, `premioUnitario`, `premioTotal`, `liquidez`
(`score`/`faixa`/`volume`/`spreadPct`/`aviso`), `estrutura`
(`ganho_maximo`/`perda_maxima`/`breakevens`/`custo_liquido`/`fluxo`/...),
`razao`, `manchete`, `didatica`, `precoObjeto`, `posicaoNoRanking` (1..4).

`POST /api/options/curadoria/narrativa` corpo `{ config }` (e SÓ isso) →
200 `{ texto, estruturas: [contractSymbol na ordem narrada], top, motivo:
"narrado" | "sem_estrutura", modo, fonte, at, ... }`; 402 quando a cota
mensal do plano acabou (a mensagem de cota vem em `detail`); 502 em falha
do provedor de LLM.

Padrões do front que este plano reusa:
- `web/src/api.js` — helper `req(method, path, body, timeoutMs)`;
  `TIMEOUT_LLM` é a constante de 90 s para rota que chama IA (as de dado de
  mercado usam 30000).
- `web/src/persistence.js` — `serverStore()` (web) e `deviceStore()`
  (iOS/nativo); `export const store = isNative ? deviceStore() :
  serverStore()` (linha ~1171). Método de dado de mercado delega direto;
  no `deviceStore` a delegação chama `ensure()` primeiro.
- `web/src/copy.js` — `COPY.estudo` e `COPY.operador`, chaves espelhadas.
- `web/src/App.jsx` — `T` (tokens de tema), `MONO`, `carouselTrackStyle`,
  `carouselItemStyle`, `cp` (copy do modo corrente vindo de `ctx`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api.js + paridade dos dois stores + copy nos dois modos</name>
  <files>web/src/api.js, web/src/persistence.js, web/src/copy.js</files>
  <read_first>
    - web/src/api.js:305-340 (bloco das rotas de opções: onde entram as duas novas e qual timeout cada classe usa)
    - web/src/persistence.js:262-280 (bloco de opções do serverStore)
    - web/src/persistence.js:1210-1222 e 1324-1340 (os espelhos correspondentes no deviceStore, incluindo o `ensure()`)
    - web/src/copy.js:580-595 (bloco `tiraOpcoes*` de COPY.estudo — o vizinho direto das chaves novas)
    - web/src/copy.js:1064-1078 (o mesmo bloco em COPY.operador)
  </read_first>
  <action>
Em `web/src/api.js`, no bloco de opções (após `optionsFecharLastreada`,
api.js:331), acrescente:
- `curadoria: () => req("GET", "/api/options/curadoria", undefined, 30000)`
  — classe "dado de mercado", 30 s, igual `optionsProposta`/`optionsGate`.
- `curadoriaNarrativa: (config) => req("POST",
  "/api/options/curadoria/narrativa", { config }, TIMEOUT_LLM)` — classe
  "chama IA", 90 s, igual `analyzeOption`. Comentário obrigatório: o corpo
  manda SÓ `config`; mandar estruturas daqui seria dar ao cliente poder sobre
  a ordem que o servidor decide, exatamente o que o guardião do Plano 03
  proíbe.

Em `web/src/persistence.js`, acrescente `opcoesCuradoria` e
`opcoesCuradoriaNarrativa` nos DOIS stores — é a paridade obrigatória do
CLAUDE.md, e o teste `web/tests/test_api_parity.mjs` trava se um lado
faltar:
- `serverStore` (ao lado de `optionsProposta`, persistence.js:275):
  `opcoesCuradoria: () => api.curadoria()` e
  `opcoesCuradoriaNarrativa: (config) => api.curadoriaNarrativa(config)`.
- `deviceStore` (ao lado do `async optionsProposta`, persistence.js:1328):
  os mesmos dois como `async`, com `ensure()` antes da delegação. Comentário
  seguindo o precedente do vizinho: ranking e narração são dado de mercado +
  IA, nunca se duplicam/cacheiam no aparelho — carimbar pregão velho como se
  fosse do dia é justamente o que o princípio 3 do CLAUDE.md proíbe.

Em `web/src/copy.js`, acrescente as MESMAS chaves em `COPY.estudo` e
`COPY.operador`, ao lado das `tiraOpcoes*`, com vocabulário por modo (Estudo
= professor descrevendo condição; Operador = mesa). Chaves, todas string
literal (não função):
`curadoriaTitulo`, `curadoriaSubtitulo`, `curadoriaCarregando`,
`curadoriaVazio`, `curadoriaRazaoRotulo`, `curadoriaNarrarCta`,
`curadoriaNarrando`, `curadoriaIaRotulo`, `curadoriaIaRessalva`,
`curadoriaCotaEsgotada`, `curadoriaErroNarrar`.

Regras de conteúdo, não negociáveis (skill didatica-boris + princípios 6/8
do CLAUDE.md): nenhuma das frases promete rentabilidade, garante lucro ou
usa linguagem de enriquecimento; `curadoriaSubtitulo` diz de onde vem a
ordem ("ordenadas pelo motor por prêmio sobre perda máxima", sem citar IA);
`curadoriaIaRotulo` deixa explícito que o texto seguinte é explicação da IA
sobre uma lista já decidida pelo motor; `curadoriaIaRessalva` diz que a IA
explica e não escolhe; `curadoriaVazio` é ESTADO (sem CTA) e nomeia o
motivo (nenhuma posição com lote livre >= 100 e call líquida no vencimento
disponível). Nenhuma frase de manchete de estrutura entra em copy.js — essa
vem do backend (`item.manchete`).
  </action>
  <verify>
    <automated>npx vite build --root web > /tmp/f30-04-t1-build.txt 2>&1; echo "build=$?"; bash scripts/executar.sh --testes > /tmp/f30-04-t1.txt 2>&1; echo "exit=$?"; tail -5 /tmp/f30-04-t1.txt</automated>
  </verify>
  <acceptance_criteria>
    - `npx vite build` (no diretório `web/`) termina sem erro.
    - `bash scripts/executar.sh --testes` exit 0 — inclui `web/tests/test_api_parity.mjs` verde, que é o guardião da paridade dos dois stores.
    - `grep -c 'opcoesCuradoria' web/src/persistence.js` >= 4 (dois métodos × dois stores).
    - `node -e "import('./web/src/copy.js').then(m=>{const k=['curadoriaTitulo','curadoriaSubtitulo','curadoriaCarregando','curadoriaVazio','curadoriaRazaoRotulo','curadoriaNarrarCta','curadoriaNarrando','curadoriaIaRotulo','curadoriaIaRessalva','curadoriaCotaEsgotada','curadoriaErroNarrar'];const f=k.filter(x=>!(x in m.COPY.estudo)||!(x in m.COPY.operador));if(f.length)throw new Error('faltam: '+f);console.log('11 chaves nos dois modos: ok')})"` imprime ok.
    - `grep -c 'TIMEOUT_LLM' web/src/api.js` aumentou em 1 em relação ao baseline (a rota de narrativa usa a classe de timeout de IA, não a de 30 s).
  </acceptance_criteria>
  <done>As duas rotas são alcançáveis dos dois stores com o mesmo contrato, e o vocabulário dos dois modos existe sem nenhuma promessa de lucro.</done>
</task>

<task type="auto">
  <name>Task 2: CuradoriaEstruturas + useCuradoria em CarteiraScreen</name>
  <files>web/src/App.jsx, web/tests/test_curadoria_ui.mjs</files>
  <read_first>
    - web/src/App.jsx:4080-4167 (OportunidadesOpcoes COMPLETO — o irmão que este bloco acompanha: cabeçalho fixo nos três estados, manchete verbatim, estado vazio obrigatório, estilos de item)
    - web/src/App.jsx:4355-4407 (useOpcoesPropostas — o padrão de hook de busca best-effort, com chave primitiva de dependência e `alive`)
    - web/src/App.jsx:4409-4430 (topo de CarteiraScreen: de onde vêm `ctx`, `cp`, `A`, `data`, e como o hook irmão é chamado)
    - web/src/App.jsx:4508-4520 (o ponto EXATO de render, dentro de `data.positions.length > 0 &&`, logo após `<OportunidadesOpcoes .../>`)
    - web/tests/test_carteira_opcoes_tira.mjs:1-60 (estilo do guardião estático deste domínio, incluindo a higiene de filtrar comentários antes de CONTAR)
  </read_first>
  <action>
Em `web/src/App.jsx`:

1. `function useCuradoria()` — logo após `useOpcoesPropostas`
   (App.jsx:4407). Sem parâmetro: a rota é cross-posição e decide as
   elegíveis no servidor. Estado: `top` (array), `meta`, `carregando`,
   `erro`, e, separados, `narrativa` (`{texto, estruturas}`),
   `narrando`, `erroNarrativa`. UMA busca por montagem (`useEffect` com
   dependência `[]` mais o padrão `alive` do hook irmão; comentário citando
   a razão da chave primitiva ali). `catch` em todo caminho — best-effort,
   igual `useOpcoesPropostas`: falha de rede só deixa o bloco sem item, nunca
   quebra a tela.
   `narrar()` é uma função devolvida pelo hook, NUNCA chamada dentro do
   `useEffect`: o parágrafo da IA custa cota (D6) e só sai por toque
   explícito. Comentário obrigatório dizendo isso — buscar narração no mount
   gastaria cota de quem só abriu a aba Posições.
   `narrar()` trata 402 separadamente (a mensagem de cota do plano) de erro
   genérico, guardando qual dos dois em `erroNarrativa`.

2. `function CuradoriaEstruturas({ top, meta, carregando, erro, narrativa,
   narrando, erroNarrativa, onNarrar, cp, onAbrir })` — logo após
   `OportunidadesOpcoes` (App.jsx:4167). Regras de render:
   - Cabeçalho FIXO nos três estados (itens / carregando / vazio), igual à
     tira irmã (NAV-03): a seção nunca desaparece em silêncio quando há
     posições. `cp.curadoriaTitulo` + `cp.curadoriaSubtitulo`.
   - Itera `top` NA ORDEM RECEBIDA, sem nenhum `sort`/`reverse`/`filter` que
     mude ordem. Comentário obrigatório: a ordem é do motor
     (`posicaoNoRanking`); reordenar no front recriaria no cliente
     exatamente o poder que o backend nega à IA. Mostre `posicaoNoRanking`
     como o número do item.
   - `item.manchete` renderizada VERBATIM, sem truncar, sem reticências, sem
     concatenar — guardrail CVM do CLAUDE.md. É PROIBIDO compor frase a
     partir de `strike`/`contratos`/`premioTotal`/`optionType` neste
     componente.
   - Chips com números do payload: `cp.curadoriaRazaoRotulo` +
     `item.razao` formatado, `item.premioTotal`, `item.diasParaVencimento`,
     `item.liquidez.faixa`. Nenhum número recalculado no front.
   - Botão de narração: `cp.curadoriaNarrarCta` (ou `cp.curadoriaNarrando`
     enquanto em voo, desabilitado), `minHeight: 44px` como o resto dos
     alvos de toque. Só aparece quando `top.length > 0`.
   - Bloco de narrativa SEPARADO dos itens e rotulado
     (`cp.curadoriaIaRotulo` + `cp.curadoriaIaRessalva`), renderizado só
     quando `narrativa && narrativa.texto`. A lista de itens NÃO muda quando
     a narrativa chega — o render dos itens lê `top`, nunca
     `narrativa.estruturas`. Comentário obrigatório com esse porquê.
   - Erros: 402 → `cp.curadoriaCotaEsgotada`; outro → `cp.curadoriaErroNarrar`.
     Os dois são ESTADO (texto, sem CTA), no lugar do bloco de narrativa —
     nunca apagam os itens determinísticos.
   - Estado vazio: `cp.curadoriaVazio`, sem botão, mesmo precedente da tira.
   - Item clicável abre o detalhe da posição correspondente via
     `onAbrir(item.ticker)` — reuse `abrirOpcoesDe`, o MESMO handler que
     `OportunidadesOpcoes` já recebe (não crie um segundo caminho de
     navegação).

3. Em `CarteiraScreen`: chame `useCuradoria()` ao lado de
   `useOpcoesPropostas` (App.jsx:4426) e renderize `<CuradoriaEstruturas
   ... />` imediatamente APÓS `<OportunidadesOpcoes ... />`
   (App.jsx:4515), DENTRO do mesmo `data.positions.length > 0 &&` — é o
   irmão que D4 pede, e a mesma razão vale: com carteira vazia, o estado
   vazio do portfólio já explica tudo, e duas mensagens para a mesma
   ausência é ruído.

Crie `web/tests/test_curadoria_ui.mjs`, guardião estático no padrão de
`web/tests/test_carteira_opcoes_tira.mjs` (readFileSync de App.jsx + import
de COPY, sem build e sem DOM), filtrando linhas de comentário antes de
QUALQUER contagem. Asserções, cada uma com o comentário do que defende:
   (1) as 11 chaves de copy existem nos dois modos e são string literal;
   (2) `CuradoriaEstruturas` renderiza `.manchete` e NÃO contém
       `.strike` concatenado em template string de frase, nem `slice(`/
       `substring(`/`…` aplicado à manchete (guardrail CVM);
   (3) o componente não contém `.sort(`, `.reverse(` nem comparação de
       `razao` — a ordem é do servidor (o guardião central do 30-CONTEXT
       traduzido para o front);
   (4) `narrar` NÃO é chamado dentro de `useEffect` em `useCuradoria`
       (cota só por toque, D6);
   (5) a lista de itens é renderizada a partir de `top`, e
       `narrativa.estruturas` não aparece em nenhum `map(` de render;
   (6) `<CuradoriaEstruturas` aparece exatamente uma vez e dentro do
       bloco `data.positions.length > 0`;
   (7) toda chamada de rede do hook tem `.catch(` no próprio encadeamento
       (best-effort, igual ao guardião 4 da tira irmã);
   (8) nenhuma frase de copy da curadoria contém as palavras proibidas
       ("garantido", "lucro garantido", "sem risco", "certeza") —
       verificação sobre os valores importados de COPY, não sobre App.jsx.
  </action>
  <verify>
    <automated>npx vite build --root web > /tmp/f30-04-t2-build.txt 2>&1; echo "build=$?"; bash scripts/executar.sh --testes > /tmp/f30-04-t2.txt 2>&1; echo "exit=$?"; tail -5 /tmp/f30-04-t2.txt</automated>
  </verify>
  <acceptance_criteria>
    - `npx vite build` verde (grep/teste estático não pega erro de sintaxe JSX — regra do CLAUDE.md).
    - `bash scripts/executar.sh --testes` exit 0, com `web/tests/test_curadoria_ui.mjs` entre os `[OK]`.
    - `node web/tests/test_curadoria_ui.mjs` isolado → 8 asserções, 0 falhas.
    - `grep -c '<CuradoriaEstruturas' web/src/App.jsx` == 1.
    - No corpo de `CuradoriaEstruturas`: `.sort(` e `.reverse(` ausentes (contagem sobre a fonte sem comentários == 0).
    - Regressão visual zero na tira irmã: `node web/tests/test_carteira_opcoes_tira.mjs` continua verde.
  </acceptance_criteria>
  <done>O bloco existe em Posições, exibe as 4 na ordem do motor com manchete verbatim, e a narração da IA é um bloco rotulado, opcional e separado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: verificação ao vivo do bloco e do gasto de cota</name>
  <files>(nenhum — checkpoint de verificação, não escreve arquivo)</files>
  <read_first>
    - .planning/phases/30-curadoria-ia-melhores-estruturas/30-04-PLAN.md (só este bloco de task)
  </read_first>
  <action>
NÃO edite código nesta task. Toda a automação possível já foi feita nas
Tasks 1 e 2 (suíte canônica + `npx vite build`); o que resta exige olhos
humanos na tela e no portal admin. Suba o ambiente local, apresente ao Alex
o roteiro de `<how-to-verify>` na ordem, e PARE aguardando o sinal de
retomada. Se o Alex apontar defeito, ele volta como task de correção nova
(com `<verify>` automatizado próprio), nunca como ajuste silencioso aqui.
  </action>
  <verify>
    <human-check>Os 7 pontos de `<how-to-verify>` confirmados pelo Alex, incluindo o débito da cota com rótulo `Curadoria` no portal admin</human-check>
  </verify>
  <what-built>
    Backend: `GET /api/options/curadoria` (determinístico, custo zero de IA) e
    `POST /api/options/curadoria/narrativa` (sob a cota de `/api/analyze`).
    Front: bloco "as 4 melhores" em Posições, ao lado de "Oportunidades de
    opções", com manchete do motor verbatim e narração da IA num bloco
    rotulado e separado, disparada só por toque.
    Suíte canônica verde e `npx vite build` verde.
  </what-built>
  <how-to-verify>
    1. Suba o servidor local e o front (`bash scripts/executar.sh` no padrão
       do repositório) e abra a aba **Posições** com uma carteira que tenha
       ao menos uma posição de 100+ ações livres num ticker com opções
       líquidas.
    2. Confirme que o bloco novo aparece LOGO ABAIXO de "Oportunidades de
       opções", com título, subtítulo e até 4 itens numerados de 1 a 4.
    3. Confirme que a ordem dos 4 é decrescente pelo número exibido de razão
       (prêmio sobre perda máxima) — e que ela NÃO muda ao recarregar a
       página.
    4. Toque no botão de explicação da IA. Confirme: (a) o texto aparece num
       bloco rotulado como explicação da IA, separado dos 4 itens; (b) a
       ORDEM e os números dos 4 itens continuam exatamente os mesmos depois
       do texto chegar; (c) o texto não promete lucro nem sugere uma ordem
       diferente da exibida.
    5. Abra o portal admin (aba de uso/custo de IA) e confirme que o gasto
       apareceu com o rótulo **Curadoria**, e que o contador da cota mensal
       de análises subiu 1 — o mesmo contador de `/api/analyze`, não um
       segundo balde (D6).
    6. Estado vazio: com uma carteira sem posição elegível (nenhum lote livre
       de 100), confirme que o bloco ainda mostra cabeçalho + a frase de
       estado vazio nomeando o motivo, em vez de desaparecer.
    7. Modo: troque Estudo ↔ Operador em Perfil e confirme que o vocabulário
       do bloco muda (professor × mesa) sem mudar nenhum número.

    **Pendência operacional a declarar no SUMMARY (não é falha):** este plano
    NÃO publica. O front só chega ao ar com `scripts/bump.sh` seguido de
    `scripts/publicar-web.sh` (nunca editando `server/web_dist` direto), e o
    app nativo (iPhone) só reflete a fase após `cap sync` + build novo. Passo
    humano separado, fora do escopo autônomo.
  </how-to-verify>
  <resume-signal>Digite "approved" ou descreva o que ficou errado (com print da tela e/ou a linha do log)</resume-signal>
  <acceptance_criteria>
    - O Alex confirmou os 7 pontos acima, ou apontou defeitos que voltaram como task de correção.
    - A pendência de publicação (bump + publicar-web + cap sync) está nomeada no SUMMARY.
  </acceptance_criteria>
  <done>Comportamento verificado no navegador, não só na suíte: ordem estável, narração rotulada e separada, e cota debitada no balde de `/api/analyze`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resposta do backend → render | o payload é confiável quanto à ordem, mas o front pode traí-la reordenando/recompondo frase |
| toque do usuário → cota de IA | cada toque no botão de narração gasta uma análise do plano comercial |
| texto da IA → leitura do usuário | o parágrafo será lido como recomendação financeira se não estiver rotulado e separado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-18 | Tampering | `CuradoriaEstruturas` | mitigate | render itera `top` na ordem recebida; guardião estático proíbe `.sort(`/`.reverse(`/comparação de `razao` no componente. Reordenar no front seria devolver ao cliente o poder que o backend nega à IA |
| T-30-19 | Spoofing | manchete recomposta no front | mitigate | `item.manchete` verbatim, sem truncar; guardião proíbe `slice(`/`substring(`/template string de frase sobre `strike`/`premioTotal` (guardrail CVM do CLAUDE.md) |
| T-30-20 | Elevation of Privilege | texto da IA confundido com a decisão | mitigate | narrativa em bloco separado, rotulado por `curadoriaIaRotulo` + ressalva "a IA explica, não escolhe"; a lista de itens lê `top` e nunca `narrativa.estruturas` (guardião 5) |
| T-30-21 | Denial of Service (bolso do usuário) | cota de IA | mitigate | `narrar()` nunca é chamado no `useEffect` (guardião 4): o bloco determinístico é útil sem gastar nada, e a cota só sai por toque. 402 vira texto de estado, não loop de retry |
| T-30-22 | Information Disclosure | payload no cliente | accept | `top` traz só dado de mercado e da própria posição do escopo, já visível na mesma tela; nenhum segredo atravessa (as chaves de provider ficam no servidor, guardrail do CLAUDE.md) |
| T-30-23 | Repudiation | divergência entre stores | mitigate | os dois métodos entram em `deviceStore` E `serverStore`; `web/tests/test_api_parity.mjs` é o guardião que trava se um lado faltar (paridade obrigatória do CLAUDE.md) |
| T-30-24 | Spoofing | copy prometendo lucro | mitigate | guardião 8 varre os valores de COPY contra as palavras proibidas ("garantido", "sem risco", "certeza") — princípios 6 e 8 do CLAUDE.md como teste, não como intenção |
| T-30-SC | Tampering | npm/pip/cargo installs | mitigate | nenhuma dependência nova neste plano (React/Vite já instalados). Qualquer `npm install` necessário interrompe a task e exige checkpoint humano de legitimidade de pacote |
</threat_model>

<verification>
1. `npx vite build` (em `web/`) → sem erro, nas duas tasks de front.
2. `bash scripts/executar.sh --testes` → exit 0 ao fim de cada task.
3. `node web/tests/test_curadoria_ui.mjs` → 8 asserções, 0 falhas.
4. `node web/tests/test_api_parity.mjs` e `node web/tests/test_carteira_opcoes_tira.mjs` → verdes (nenhuma regressão nos vizinhos).
5. Verificação ao vivo no navegador (Task 3), incluindo o débito da cota no portal admin — a memória do repositório é explícita: defeito de fiação de UI só aparece no teste ao vivo.
</verification>

<success_criteria>
- SC-1: a tela exibe as 4 na ordem do payload determinístico e a narração não altera nem a ordem nem os números.
- SC-3: a narração consome a cota de `/api/analyze`, verificado no portal admin com o rótulo `Curadoria`.
- SC-4: só venda coberta na tela (o payload não tem outro `tipo`; nenhum texto de put/collar entra na copy da curadoria).
- SC-5: suíte canônica exit 0 e `npx vite build` verde.
</success_criteria>

<output>
Create `.planning/phases/30-curadoria-ia-melhores-estruturas/30-04-SUMMARY.md` when done
</output>
