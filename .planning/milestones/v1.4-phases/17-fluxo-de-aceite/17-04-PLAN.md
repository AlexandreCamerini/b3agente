---
phase: 17-fluxo-de-aceite
plan: 04
type: execute
wave: 2
depends_on: ["17-02"]
files_modified:
  - web/src/App.jsx
  - web/src/copy.js
  - web/tests/test_opcoes_proposta_ui.mjs
autonomous: true
requirements: [FLOW-01, FLOW-04]

must_haves:
  truths:
    - "O usuário vê ganho máximo, perda máxima e breakeven(s) da proposta antes de decidir"
    - "Valor que o motor não calcula aparece como 'ilimitado' ou '—', nunca como R$ 0,00"
    - "Todo card de proposta — inclusive o estado vazio — declara a fonte e o horário do dado"
  artifacts:
    - path: "web/src/App.jsx"
      provides: "bloco de payoff em PropostaLastreada + linha de fonte/horário"
      contains: "p.estrutura"
    - path: "web/src/copy.js"
      provides: "rótulos de payoff e de frescor nos DOIS modos"
      contains: "payoffGanhoMaximo"
    - path: "web/tests/test_opcoes_proposta_ui.mjs"
      provides: "guardiões de null-nunca-zero e de frescor declarado"
  key_links:
    - from: "web/src/App.jsx::PropostaLastreada"
      to: "proposta.estrutura / proposta.caixa"
      via: "leitura direta dos campos que a Fase 16 já produz"
      pattern: "p\\.estrutura"
    - from: "web/src/App.jsx::PropostaLastreada"
      to: "r.source / r.at"
      via: "FONTE_LABEL (App.jsx:1185), mesmo padrão de App.jsx:1655"
      pattern: "FONTE_LABEL"
---

<objective>
FLOW-01 e FLOW-04 no card: `PropostaLastreada` passa a exibir o payoff completo
que a Fase 16 já calcula (`proposta.estrutura` e `proposta.caixa`) e a declarar
a fonte e o horário do dado.

Purpose: hoje o componente mostra manchete, chips e CTA, mas descarta
silenciosamente `estrutura` (ganho máximo, perda máxima, breakevens, se o lado
é ilimitado) e `caixa` (custo/crédito líquido) — os campos que o Plano 16-01
adicionou explicitamente "prontos para a Fase 17 (FLOW-01) exibir". Sem eles o
usuário decide sem ver o risco. E o card não diz de onde veio o dado nem quando
foi lido (princípio 3 do CLAUDE.md).

Output: bloco de payoff + linha de frescor em `PropostaLastreada`, chaves novas
em `copy.js` nos dois modos, guardiões em `test_opcoes_proposta_ui.mjs`.
Este plano NÃO toca collar (Plano 17-05) — só melhora o card que já existe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-fluxo-de-aceite/17-CONTEXT.md
@CLAUDE.md
@.claude/skills/didatica-boris/SKILL.md

<interfaces>
<!-- Contratos reais. Não explorar o repo. -->

Resposta de `GET /api/options/proposta/{ticker}` (após o Plano 17-02):
`{ticker, providerStatus, modo, proposta, motivo, motivoTexto, putSemLastro,
source, at}` — `source` é "yahoo" | "brapi" | "mydata" | "mock" | `null`;
`at` é string `"DD/MM/AAAA HH:MM"`.

`proposta.estrutura` (de `server/app/opcoes_payoff.py::perfil_da_estrutura`),
TUDO **por unidade do objeto (uma ação)**:
`{pernas: [...], custo_liquido: float, fluxo: "debito"|"credito"|"neutro",
ganho_maximo: float|null, perda_maxima: float|null, ganho_ilimitado: bool,
perda_ilimitada: bool, breakevens: float[], delta_total: {...}, curva: [...],
unidade: "por unidade do objeto (uma ação); multiplique pelo lote se precisar"}`.
`ganho_maximo` é `null` SE E SOMENTE SE `ganho_ilimitado` é `true`; idem para
`perda_maxima`/`perda_ilimitada`. `breakevens` pode ser lista vazia.

`proposta.caixa`: `{custoLiquidoUnitario: float, custoLiquidoTotal: float,
fluxo: "debito"|"credito"|"neutro"}` — `custoLiquidoTotal` JÁ é o valor do lote
(unitário × `qtyAcoes`), positivo = débito, negativo = crédito.

`proposta.qtyAcoes` (int) e `proposta.precoObjeto` (float, spot usado na perna
de ação da estrutura).

`web/src/App.jsx`:
- `const price = (n) => (n == null || isNaN(n) ? "—" : nf2.format(n));` (linha 251).
  ATENÇÃO: `null * 100 === 0` em JS — multiplicar antes de checar `null` transforma
  "não aplicável" em "R$ 0,00", exatamente o defeito que a regra "null nunca 0.0"
  do repositório proíbe.
- `const FONTE_LABEL = (source) => ...` (linha 1185) — rótulo da fonte, já usado
  em `App.jsx:1655` (`Fonte: {data.source ? FONTE_LABEL(data.source) : "—"}{data.at ? " · " + data.at : ""}`),
  3286, 3297 e 5925. REUSAR, não criar rótulo novo.
- `function PropostaLastreada({ r, operador, cp, busy, onAbrir, onFechar, posAberta })`
  (linha 3010) — ramo vazio (`!r.proposta`) nas linhas 3012-3021; ramo com proposta
  3022-3064. Tokens visuais em uso: `T.bgCard`, `T.bgBase`, `T.borderFaint`,
  `T.textPrimary`, `T.textSecondary`, `T.textMuted`, `T.textFaint`, `T.accent`,
  `T.positive`, `T.negative`, `MONO`. Chips: `<span>` com
  `borderRadius: "999px", background: T.bgBase, fontSize: "11px", padding: "4px 10px"`.

`web/tests/test_opcoes_proposta_ui.mjs` — guardião existente que PRECISA continuar
passando sem alteração:
- `/function PropostaLastreada\(\{ r, operador, cp, busy, onAbrir, onFechar, posAberta \}\)/`
  — a assinatura do componente NÃO pode ganhar prop nova.
- `app.indexOf("function PropostaLastreada") < app.indexOf("function OpcoesCamada")`
  e `propostaFn = app.slice(iPL, iOC)` — o corpo medido vai de uma função à outra.
- `/const isCall = p\.optionType === "call";/` e
  `/const cor = isCall \? T\.positive : T\.negative;/` — as duas linhas precisam
  permanecer TEXTUALMENTE iguais.
- `/\{!operador && \([\s\S]{0,120}\{p\.didatica\}/` — a distância entre `{!operador && (`
  e `{p.didatica}` precisa continuar <= 120 caracteres.
- `/\{operador && \([\s\S]{0,60}<button/` — <= 60 caracteres entre `{operador && (` e `<button`.

`web/src/copy.js` — dois ramos (`COPY.estudo`, `COPY.operador`) com PARIDADE DE
CHAVES obrigatória (`web/tests/test_copy_theme.mjs:27-28`). O ramo `estudo` é
varrido por `/registrar entrada|registrar saída|execute a saída|COMPRAR|VENDER/i`
sobre TODOS os valores (funções são chamadas com `("X","Y","Z")` e `(null,0,0)`) —
nenhum valor do ramo estudo pode conter os infinitivos "comprar"/"vender".
Precedente de chave idêntica nos dois modos (system notice, não voz de
professor/mesa): `propostaIndisponivelDegradada`, `verCadeiaCompleta`,
`avisoLiquidacaoForcada`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chaves de payoff e de frescor no copy.js (dois modos)</name>
  <files>web/src/copy.js, web/tests/test_opcoes_proposta_ui.mjs</files>
  <read_first>
    - web/src/copy.js (linhas 155-190 — bloco das operações lastreadas no ramo estudo; e o bloco correspondente no ramo operador, linhas 310-330)
    - web/tests/test_copy_theme.mjs (linhas 25-31 — paridade de chaves e varredura de vocabulário do ramo estudo)
    - web/tests/test_opcoes_proposta_ui.mjs (linhas 18-40 — lista `CHAVES_LASTREADAS` e asserções de vocabulário)
  </read_first>
  <action>
Acrescentar as chaves abaixo a AMBOS os ramos de `web/src/copy.js`, com texto
IDÊNTICO nos dois (são rótulo de dado e aviso de sistema, não voz de
professor/mesa — mesmo precedente de `propostaIndisponivelDegradada`), no bloco
das operações lastreadas, logo após `propostaVaziaTitulo`:

- `payoffTitulo`: "No vencimento, se levar até o fim"
- `payoffGanhoMaximo`: "ganho máximo"
- `payoffPerdaMaxima`: "perda máxima"
- `payoffBreakeven`: "empata em"
- `payoffIlimitado`: "ilimitado"
- `payoffSemDado`: "—"
- `payoffCaixaCredito`: "recebe hoje"
- `payoffCaixaDebito`: "custa hoje"
- `payoffCaixaNeutro`: "sem movimento de caixa hoje"
- `payoffNota`: `(precoObjeto) => \`Inclui sua posição em ações marcada a R$ ${precoObjeto} (preço de hoje). Resultado no vencimento, antes de custos.\``
- `fontePropostaLinha`: `(fonte, quando) => \`Fonte: ${fonte} · ${quando}\``
- `fontePropostaSemDado`: "Fonte do dado não declarada."

Regras que a redação precisa respeitar:
- Nenhum valor pode conter os infinitivos "comprar"/"vender" (varredura do ramo
  estudo em `test_copy_theme.mjs`). "recebe hoje"/"custa hoje" foram escolhidos por
  isso — não trocar por "recebe ao vender".
- Nenhuma chave pode conter texto de MANCHETE (a manchete vem de
  `proposta.manchete`, produzida por `skill_ref.opcoes_lastreadas_txt` — guardrail
  CVM). O guardião existente já afirma que nenhum valor de `copy.js` contém
  "Se você tivesse".
- Comentário curto no bloco registrando por que estas chaves são iguais nos dois
  modos, apontando o precedente.

Acrescentar as 12 chaves novas ao array `CHAVES_LASTREADAS` de
`web/tests/test_opcoes_proposta_ui.mjs` (que já afirma presença nos dois ramos).
  </action>
  <verify>
    <automated>node web/tests/test_copy_theme.mjs && node web/tests/test_opcoes_proposta_ui.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node web/tests/test_copy_theme.mjs` passa (paridade de chaves + vocabulário do ramo estudo)
    - `grep -c "payoffGanhoMaximo" web/src/copy.js` == 2 (uma em cada ramo)
    - `grep -c "fontePropostaLinha" web/src/copy.js` == 2
    - `node web/tests/test_opcoes_proposta_ui.mjs` passa com `CHAVES_LASTREADAS` estendido
  </acceptance_criteria>
  <done>As 12 chaves existem nos dois ramos, com paridade travada por teste.</done>
</task>

<task type="auto">
  <name>Task 2: Bloco de payoff e linha de frescor em PropostaLastreada</name>
  <files>web/src/App.jsx</files>
  <read_first>
    - web/src/App.jsx (linhas 245-255 — `price`/`nf2`; 1180-1190 — `FONTE_LABEL`; 1645-1660 — o padrão de linha de fonte já em produção; 3003-3065 — `PropostaLastreada` inteira)
    - web/src/copy.js (as chaves da Task 1)
    - web/tests/test_opcoes_proposta_ui.mjs (todas as asserções que medem `propostaFn` — precisam continuar verdes)
  </read_first>
  <action>
Duas edições em `web/src/App.jsx`, sem alterar a assinatura de `PropostaLastreada`
(o guardião existente pina a lista exata de props):

**(a) Helper de frescor.** Definir, IMEDIATAMENTE ANTES de
`function PropostaLastreada` (para que o slice `iPL..iOC` do guardião continue
medindo só o corpo do componente), um componente
`function FonteDoDadoProposta({ r, cp })` que renderiza uma `<div>` de
`fontSize: "10px", color: T.textFaint, marginTop: "10px"` com:
`r.source ? cp.fontePropostaLinha(FONTE_LABEL(r.source), r.at || "—") : cp.fontePropostaSemDado`.
Reusar `FONTE_LABEL` — não escrever um mapa de rótulo novo.

**(b) Uso em `PropostaLastreada`.** Renderizar `<FonteDoDadoProposta r={r} cp={cp} />`
como ÚLTIMO filho dos DOIS ramos: o ramo vazio (`!r.proposta`, linhas 3012-3021) e o
ramo com proposta (linhas 3027-3063, depois do CTA). "Não há proposta" também é uma
afirmação sobre dado de mercado — princípio 3 do CLAUDE.md não abre exceção para o
estado vazio.

**(c) Bloco de payoff**, inserido no ramo com proposta ENTRE o bloco de chips
(termina na linha 3040) e o bloco `{!operador && ...}` da didática (linha 3043).
Preservar a distância curta entre `{!operador && (` e `{p.didatica}` (o guardião
exige <= 120 caracteres) — ou seja, o bloco novo entra ANTES daquele trecho, sem
envolvê-lo.

Aritmética e nulos — regra exata, porque `null * 100 === 0` em JavaScript:
- `const est = p.estrutura || null;`
- Definir um helper local `const porLote = (v) => (typeof v === "number" ? v * (p.qtyAcoes || 0) : null);`
  — só multiplica NÚMERO. `null`/`undefined` continuam `null` e caem em `price(null)` → "—".
  Nunca escrever `est.ganho_maximo * p.qtyAcoes` direto.
- Ganho: se `est.ganho_ilimitado === true` → `cp.payoffIlimitado`; senão
  `"R$ " + price(porLote(est.ganho_maximo))`.
- Perda: se `est.perda_ilimitada === true` → `cp.payoffIlimitado`; senão
  `"R$ " + price(porLote(est.perda_maxima))`.
- Breakevens: `Array.isArray(est.breakevens) && est.breakevens.length
  ? est.breakevens.map((b) => price(b)).join(" / ") : cp.payoffSemDado`.
  Breakeven é PREÇO DO ATIVO, não valor do lote — NÃO multiplicar por `qtyAcoes`.
- Caixa: rótulo por `p.caixa.fluxo` — `"credito"` → `cp.payoffCaixaCredito`,
  `"debito"` → `cp.payoffCaixaDebito`, `"neutro"` → `cp.payoffCaixaNeutro`; valor
  `"R$ " + price(Math.abs(p.caixa.custoLiquidoTotal))` (o campo JÁ é do lote; não
  multiplicar de novo). No caso neutro, não exibir valor.
- Se `p.estrutura` for ausente (proposta de FECHAMENTO — `proposta_fechar` não
  devolve `estrutura`/`caixa`/`precoObjeto`), o bloco inteiro NÃO é renderizado.
  Guardar com `{est && (...)}`. Isso não é degradação: o card de fechamento
  descreve outra operação.

Layout: mesma linguagem visual dos chips já existentes — um `<div>` com
`marginTop: "10px", padding: "10px", borderRadius: "9px", background: T.bgBase`,
título em `fontSize: "10px", fontWeight: 800, letterSpacing: "0.04em",
color: T.textFaint` com `cp.payoffTitulo`, e as linhas rótulo/valor em
`fontSize: "11px"` com o rótulo em `T.textSecondary` e o valor em `MONO`/
`fontWeight: 800`/`T.textPrimary`. Fechar com `cp.payoffNota(price(p.precoObjeto))`
em `fontSize: "10px", color: T.textFaint`.

PROIBIDO neste plano: alterar a assinatura de `PropostaLastreada`, as linhas
`const isCall = ...` e `const cor = ...`, a manchete, os chips, o CTA, ou qualquer
texto vindo do motor.
  </action>
  <verify>
    <automated>cd web && npx vite build && node tests/test_opcoes_proposta_ui.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx vite build` completa sem erro (obrigatório: grep e teste estático não pegam erro de sintaxe JSX)
    - `node web/tests/test_opcoes_proposta_ui.mjs` passa — INCLUINDO todas as asserções pré-existentes (assinatura do componente, `isCall`, `cor`, distâncias de `{!operador && (`/`{operador && (`)
    - `grep -c "p.estrutura" web/src/App.jsx` >= 1 e `grep -c "FonteDoDadoProposta" web/src/App.jsx` == 3 (definição + 2 usos)
    - `grep -n "ganho_maximo \* " web/src/App.jsx` não retorna nada (nenhuma multiplicação direta de campo anulável)
  </acceptance_criteria>
  <done>O card mostra ganho máximo, perda máxima, breakevens, movimento de caixa e a fonte/horário do dado.</done>
</task>

<task type="auto">
  <name>Task 3: Guardiões de null-nunca-zero e de frescor declarado</name>
  <files>web/tests/test_opcoes_proposta_ui.mjs</files>
  <read_first>
    - web/src/App.jsx (o bloco de payoff e o `FonteDoDadoProposta` da Task 2)
    - web/tests/test_opcoes_proposta_ui.mjs (arquivo inteiro — estilo de asserção estática por slice + regex)
    - web/tests/test_fonte_cotacoes_visivel.mjs (precedente de guardião de fonte visível no front)
  </read_first>
  <action>
Acrescentar a `web/tests/test_opcoes_proposta_ui.mjs`, numa seção nova comentada
"Fase 17, Plano 04 — FLOW-01/FLOW-04", os guardiões:

1. "payoff nunca multiplica campo anulável direto": afirma que `propostaFn` NÃO casa
   `/est\.(ganho_maximo|perda_maxima)\s*\*/` e que existe um helper que checa
   `typeof v === "number"` antes de multiplicar (`/typeof v === "number"/.test(propostaFn)`).
   Docstring: em JS `null * 100 === 0`; multiplicar antes de checar transforma "não
   aplicável" em "R$ 0,00" — a regra "null nunca 0.0" do repositório (precedente
   `test_m3_format_pede_null_nunca_zero`) aplicada à UI.

2. "lado ilimitado vira palavra, não número": afirma que `propostaFn` contém
   `ganho_ilimitado` e `perda_ilimitada` e que `cp.payoffIlimitado` é referenciado
   pelo menos 2 vezes dentro de `propostaFn`.

3. "breakeven não é multiplicado pelo lote": afirma que a linha de `propostaFn` que
   menciona `breakevens` não contém `qtyAcoes`.

4. "frescor declarado nos DOIS ramos do card": localiza o ramo vazio (o trecho de
   `propostaFn` entre `if (!r.proposta)` e o `return` do ramo com proposta) e afirma
   que `<FonteDoDadoProposta` aparece pelo menos 2 vezes em `propostaFn`; e que
   `FonteDoDadoProposta` usa `FONTE_LABEL` e `cp.fontePropostaLinha` (nenhum rótulo
   de fonte hardcoded no componente).

5. "o front não recompõe rótulo de fonte": afirma que o corpo de
   `FonteDoDadoProposta` não contém as strings literais `"Yahoo"`, `"brapi"` ou
   `"MyData"` (todas vêm de `FONTE_LABEL`).

6. "bloco de payoff não aparece na proposta de fechamento": afirma que o bloco está
   guardado por uma condição sobre `p.estrutura` (regex `/\{est && \(/` ou
   equivalente presente em `propostaFn`).

Cabeçalho da seção registrando que estes guardiões são de FLOW-01/FLOW-04 e que
apagá-los exige nota explícita.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `node web/tests/test_opcoes_proposta_ui.mjs` passa com os 6 guardiões novos
    - Injeção de falha registrada no SUMMARY: trocando o helper por multiplicação direta (`est.ganho_maximo * p.qtyAcoes`), o guardião 1 reprova; após `git checkout -- web/src/App.jsx`, passa
    - `bash scripts/executar.sh --testes` verde (as DUAS suítes: pytest + web/tests/*.mjs)
    - `cd web && npx vite build` sem erro
  </acceptance_criteria>
  <done>A suíte falha se o payoff voltar a mostrar 0,00 no lugar de "não aplicável" ou se o card perder a declaração de fonte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resposta da API → renderização no card | números de risco que embasam a decisão do usuário atravessam aqui |
| `copy.js` → texto exibido | texto de UI; a manchete NÃO passa por aqui (guardrail CVM) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-17 | Tampering | "não aplicável" exibido como R$ 0,00 | mitigate | Helper `porLote` que só multiplica `typeof v === "number"`; guardião estático proibindo multiplicação direta de `ganho_maximo`/`perda_maxima` |
| T-17-18 | Spoofing | front compondo manchete/veredito próprio | mitigate | Bloco novo exibe só rótulo (`copy.js`) + número do motor; guardião existente de "nenhuma chave de copy.js carrega a manchete do motor" estendido às 12 chaves novas |
| T-17-19 | Information Disclosure | dado de mercado exibido sem fonte/horário | mitigate | `FonteDoDadoProposta` renderizado nos dois ramos do card; guardião 4 |
| T-17-20 | Tampering | rótulo de fonte chumbado no componente | mitigate | Reuso obrigatório de `FONTE_LABEL`; guardião 5 |
| T-17-21 | Repudiation | payoff de estrutura exibido sobre proposta de FECHAMENTO (que não tem `estrutura`) | mitigate | Bloco guardado por `{est && ...}`; guardião 6 |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhuma dependência nova — `git diff --stat web/package.json package-lock.json` vazio |
</threat_model>

<verification>
- `cd web && npx vite build` sem erro (obrigatório por guardrail do CLAUDE.md — o
  front foi editado).
- `bash scripts/executar.sh --testes` verde (suíte canônica, as duas suítes).
- `node web/tests/test_copy_theme.mjs` verde (paridade de chaves entre modos).
- `git diff --stat web/package.json package-lock.json` vazio.
- `git status --porcelain server/` vazio — este plano é front-only.
- A assinatura de `PropostaLastreada` no diff é idêntica à do HEAD anterior.
</verification>

<success_criteria>
- O usuário vê estrutura, prêmio, breakeven e ganho/perda máximos antes de decidir (FLOW-01).
- Nenhum valor "não aplicável" aparece como zero.
- Todo card de proposta declara fonte e horário do dado (FLOW-04).
- Nenhum guardião pré-existente de `test_opcoes_proposta_ui.mjs` foi alterado.
</success_criteria>

<output>
Create `.planning/phases/17-fluxo-de-aceite/17-04-SUMMARY.md` when done.
Registrar para o Plano 17-05: em que ponto exato de `PropostaLastreada` o bloco de
payoff foi inserido e quais asserções do guardião medem distâncias de caracteres
(para que a edição do collar não as quebre por acidente).
</output>
