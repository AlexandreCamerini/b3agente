---
phase: 17-fluxo-de-aceite
plan: 05
type: execute
wave: 3
depends_on: ["17-03", "17-04"]
files_modified:
  - web/src/api.js
  - web/src/persistence.js
  - web/src/copy.js
  - web/src/App.jsx
  - web/tests/test_opcoes_collar_ui.mjs
  - web/tests/test_opcoes_proposta_ui.mjs
autonomous: true
requirements: [FLOW-02, FLOW-03]

must_haves:
  truths:
    - "O cliente pede a capacidade multiperna e recebe, renderiza e permite aceitar uma trava protetora de 2 pernas"
    - "Nenhuma execução dispara sozinha: só o clique no CTA, confirmado, chama a rota de abertura"
    - "Modo Estudo continua sem nenhum botão de executar, inclusive no collar"
    - "O método novo existe nos DOIS stores (deviceStore e serverStore), com contrato idêntico"
  artifacts:
    - path: "web/src/api.js"
      provides: "optionsAbrirCollar + multiperna em optionsProposta"
      contains: "abrir-collar"
    - path: "web/src/persistence.js"
      provides: "optionsAbrirCollar nos dois stores"
      contains: "optionsAbrirCollar"
    - path: "web/src/App.jsx"
      provides: "renderização das 2 pernas + CTA de aceite do collar"
      contains: "collar"
    - path: "web/tests/test_opcoes_collar_ui.mjs"
      provides: "guardiões de aceite explícito, paridade de stores e ausência de CTA no Estudo"
  key_links:
    - from: "web/src/App.jsx"
      to: "store.optionsProposta(t, true)"
      via: "negociação de capacidade multiperna declarada pelo cliente"
      pattern: "optionsProposta\\(t, true\\)"
    - from: "web/src/App.jsx::onAbrirLastreada"
      to: "A.abrirCollar → store.optionsAbrirCollar → POST /api/options/lastreada/abrir-collar"
      via: "despacho por proposta.tipo === 'collar', dentro do handler de clique"
      pattern: "abrirCollar"
---

<objective>
FLOW-02/FLOW-03 no cliente: o app passa a declarar a capacidade multiperna,
renderizar a trava protetora (duas pernas, payoff já exibido pelo Plano 17-04) e
executá-la por aceite explícito na rota nova do Plano 17-03.

Purpose: o Plano 16-04 deixou registrado que "ligar o collar no cliente é passar
`multiperna=1`" e que executá-lo exigia um caminho de N pernas que não existia.
As duas peças agora existem (Planos 17-01 e 17-03). Este plano fecha o circuito
— e o faz sem afrouxar nada: `PropostaLastreada` mantém a assinatura pinada pelo
guardião, o despacho collar × perna única acontece no handler do card, e o Modo
Estudo continua sem CTA.

Output: `api.js`, `persistence.js` (os DOIS stores), `copy.js` (chaves nos dois
modos), `App.jsx`, `web/tests/test_opcoes_collar_ui.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-fluxo-de-aceite/17-CONTEXT.md
@.planning/phases/17-fluxo-de-aceite/17-03-SUMMARY.md
@.planning/phases/17-fluxo-de-aceite/17-04-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contratos reais. Não explorar o repo. -->

Rota (Plano 17-03): `POST /api/options/lastreada/abrir-collar`
Corpo: `{underlying, pernasContratos: [{contractSymbol, lado}, ...2 itens],
contratos, expiration}`. Respostas: 200 (`public_state` + `premiosUsados`),
403 (Modo Estudo), 400 (corpo inválido / recusa do motor), 409 (proposta
recalculada divergente), 502 (cadeia indisponível).

Proposta de collar (`server/app/opcoes_lastreadas.py::_propor_collar`):
`tipo: "collar"`, `contractSymbol: null`, `optionType: null`, `strike: null`,
`premioUnitario: null`, `premioTotal: null`, `strikeCall`, `strikePut`,
`pernasContratos: [{contractSymbol, optionType, lado, strike, premioUnitario}]`
(call em índice 0, put em índice 1), `contratos`, `qtyAcoes`, `expiration`,
`manchete`, `didatica`, `chips`, `liquidez`, `estrutura`, `caixa`, `precoObjeto`.

`web/src/api.js`:
- `optionsProposta: (t) => req("GET", "/api/options/proposta/" + encodeURIComponent(t), undefined, 30000)` (linha 285)
- `optionsAbrirLastreada: (body) => req("POST", "/api/options/lastreada/abrir", body)` (linha 286)

`web/src/persistence.js` — DOIS stores obrigatoriamente em paridade
(`web/tests/test_fase3_paridade_stores_generica.mjs` + guardrail do CLAUDE.md):
- `serverStore` (bloco ~linhas 255-290): delegação pura —
  `optionsProposta: (t) => api.optionsProposta(t)`,
  `optionsAbrirLastreada: (body) => api.optionsAbrirLastreada(body)`.
- `deviceStore` (bloco ~linhas 1215-1290): `async optionsProposta(t) { ensure();
  return api.optionsProposta(t); }` e `async optionsAbrirLastreada(body)` que,
  COM sessão, faz `const r = await api.optionsAbrirLastreada(body);
  _adotarCarteiraDoServidor(r); write(); const out = pub(); out.priceUsed = r &&
  r.priceUsed; return out;` e, SEM sessão, roda um espelho local completo de
  `store.py`.
- `export const store = isNative ? deviceStore() : serverStore();` (linha 1171).

`web/src/App.jsx`:
- Efeito que busca a proposta (linhas 3154-3161): `store.optionsProposta(t)` dentro
  de `if (opGate && opGate.liquida)`.
- `posAberta` (linhas 3216-3218): casa `myOptionPositions` por
  `opProposta.proposta.contractSymbol` — no collar esse campo é `null`, logo
  `posAberta` fica `null` e o CTA permanece "abrir". Não mudar essa lógica.
- `onAbrirLastreada` (linhas 3219-3228): valida, `window.confirm(cp.confirmAbrirCoberta(...))`
  só para call, e chama `A.abrirLastreada({underlying, contractSymbol, expiration, contratos})`.
- `A.abrirLastreada` (linha 7717) — wrapper que chama `store.optionsAbrirLastreada(body)`.
- `PropostaLastreada` (linha 3010): assinatura PINADA pelo guardião
  `/function PropostaLastreada\(\{ r, operador, cp, busy, onAbrir, onFechar, posAberta \}\)/`
  — NÃO adicionar prop. Linhas também pinadas: `const isCall = p.optionType === "call";`
  e `const cor = isCall ? T.positive : T.negative;` (no collar `optionType` é `null`,
  logo `isCall === false` e a cor sai `T.negative` — coerente: a trava protetora é
  operação defensiva; nenhuma das duas linhas muda).
  Linha 3032 renderiza `{p.contratos}× {isCall ? "CALL" : "PUT"} {r.ticker} · strike
  {price(p.strike)}` — no collar `p.strike` é `null` e precisa virar a linha de duas pernas.
  Eyebrow (linha 3025): `isCall ? cp.eyebrowPropostaCall : cp.eyebrowPropostaPut`.

`web/tests/test_opcoes_proposta_ui.mjs` — DUAS asserções que esta edição quebra e
que precisam ser atualizadas com nota (o repositório permite atualizar um guardião
em reversão/mudança deliberada, nunca apagá-lo):
- `const i = app.indexOf("store.optionsProposta(t)")` — a chamada passa a ser
  `store.optionsProposta(t, true)`. Atualizar o literal procurado para
  `"store.optionsProposta(t, true)"` e manter a asserção de que a chamada continua
  sob `if (opGate && opGate.liquida)`.
- `CHAVES_LASTREADAS` — acrescentar as chaves novas deste plano.

`web/src/copy.js` — paridade de chaves obrigatória entre `estudo` e `operador`
(`test_copy_theme.mjs:27`); ramo `estudo` não pode conter os infinitivos
"comprar"/"vender" em nenhum valor.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contrato de cliente — api.js, os dois stores e as chaves de copy</name>
  <files>web/src/api.js, web/src/persistence.js, web/src/copy.js</files>
  <read_first>
    - web/src/api.js (linhas 274-290)
    - web/src/persistence.js (linhas 255-290 — serverStore; 1165-1180 — a escolha do store; 1215-1290 — deviceStore)
    - web/src/copy.js (bloco das operações lastreadas nos dois ramos)
    - web/tests/test_fase3_paridade_stores_generica.mjs (o guardião de paridade que vai medir o método novo)
  </read_first>
  <action>
**`web/src/api.js`:**
- Alterar `optionsProposta` para `(t, multiperna) => req("GET",
  "/api/options/proposta/" + encodeURIComponent(t) + (multiperna ? "?multiperna=1" : ""),
  undefined, 30000)`. Parâmetro OPCIONAL: sem ele a chamada continua byte a byte a
  de hoje (o backend trata ausência e `multiperna=0` como o mesmo caminho).
- Acrescentar `optionsAbrirCollar: (body) => req("POST",
  "/api/options/lastreada/abrir-collar", body)` logo após `optionsAbrirLastreada`,
  com comentário registrando: rota SEPARADA de propósito — `/abrir` continua com a
  trava de servidor do Plano 16-04 e executa uma perna por chamada.

**`web/src/persistence.js` — os DOIS stores (guardrail: método novo entra nos dois):**
- `serverStore`: `optionsProposta: (t, multiperna) => api.optionsProposta(t, multiperna)`
  e `optionsAbrirCollar: (body) => api.optionsAbrirCollar(body)` (delegação pura,
  ao lado de `optionsAbrirLastreada`, fora de `sync.mutate`/outbox pela MESMA razão
  já comentada ali: reaplicar de fila offline devolveria caixa duas vezes).
- `deviceStore`: `async optionsProposta(t, multiperna) { ensure(); return
  api.optionsProposta(t, multiperna); }` e `async optionsAbrirCollar(body)` que:
  - COM sessão (`sync.hasSession()`): `const r = await api.optionsAbrirCollar(body);
    _adotarCarteiraDoServidor(r); write(); const out = pub(); out.premiosUsados = r &&
    r.premiosUsados; return out;` — espelho exato do padrão de `optionsAbrirLastreada`,
    trocando `priceUsed` por `premiosUsados` (duas pernas, dois prêmios).
  - SEM sessão: `throw new Error("Montar uma trava protetora exige estar conectado à
    sua conta — as duas pernas são validadas juntas no servidor.")`. Comentário
    obrigatório explicando a decisão: diferente das operações de UMA perna, o ramo
    local NÃO reimplementa a estrutura, porque as duas garantias que a tornam segura
    (re-derivação server-side da proposta e atomicidade das duas pernas sob
    `ORDER_LOCK`) não existem no aparelho; reimplementá-las produziria um segundo
    motor divergente do servidor. Login já é obrigatório no produto (guardrail do
    CLAUDE.md), então este ramo é caminho legado — e um erro nomeado é estado
    correto, não valor inventado (princípio 4).

**`web/src/copy.js` — acrescentar aos DOIS ramos, no bloco das operações lastreadas:**
- `eyebrowPropostaCollar`: estudo "ESTUDO · TRAVA PROTETORA" / operador "PROPOSTA · TRAVA PROTETORA"
- `collarPernasLinha`: `(n, ticker, strikeCall, strikePut) => \`${n}× TRAVA ${ticker} · call ${strikeCall} / put ${strikePut}\`` — IDÊNTICO nos dois modos (é descrição de dado, não voz)
- `ctaCollarDebito`: operador `(n, ticker, sc, sp, valor) => \`Montar ${n}× trava ${ticker} · call ${sc} / put ${sp} — custa R$ ${valor}\`` / estudo `() => "Ver como esta trava protetora funcionaria"`
- `ctaCollarCredito`: operador `(n, ticker, sc, sp, valor) => \`Montar ${n}× trava ${ticker} · call ${sc} / put ${sp} — recebe R$ ${valor}\`` / estudo `() => "Ver como esta trava protetora funcionaria"`
- `confirmAbrirCollar`: operador `(n, ticker, qty) => \`Montar ${n} trava(s) protetora(s) de ${ticker} trava ${qty} ação(ões) do seu lote-lastro (perna da call) até você encerrar a estrutura ou ela vencer. As duas pernas são abertas juntas — ou nenhuma. Continuar?\`` / estudo `() => ""` (o ramo estudo nunca chama `window.confirm`; a chave existe pela paridade, mesmo padrão de `confirmAbrirCoberta`)

Duas chaves de CTA (débito/crédito) em vez de uma com o texto montado no
componente: o front não compõe frase, escolhe chave. Nenhum valor do ramo estudo
pode conter "comprar"/"vender" — "Montar"/"trava protetora" foram escolhidos por isso.
  </action>
  <verify>
    <automated>node web/tests/test_copy_theme.mjs && node web/tests/test_fase3_paridade_stores_generica.mjs && node web/tests/test_api_parity.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "optionsAbrirCollar" web/src/persistence.js` >= 2 (uma ocorrência em cada store)
    - `grep -c "abrir-collar" web/src/api.js` == 1
    - `grep -c "eyebrowPropostaCollar" web/src/copy.js` == 2
    - `node web/tests/test_copy_theme.mjs` passa (paridade de chaves + vocabulário do ramo estudo)
    - `node web/tests/test_fase3_paridade_stores_generica.mjs` passa
    - `grep -n "multiperna" web/src/api.js` mostra o parâmetro opcional, e chamar `optionsProposta(t)` sem o segundo argumento produz a mesma URL de hoje
  </acceptance_criteria>
  <done>O cliente sabe pedir multiperna e sabe chamar a rota de abertura de collar; os dois stores estão em paridade.</done>
</task>

<task type="auto">
  <name>Task 2: Renderização das duas pernas e aceite explícito do collar</name>
  <files>web/src/App.jsx, web/tests/test_opcoes_proposta_ui.mjs</files>
  <read_first>
    - web/src/App.jsx (linhas 3003-3065 — `PropostaLastreada` já com o bloco de payoff do Plano 17-04; 3143-3162 — efeito de busca da proposta; 3214-3236 — `posAberta`, `onAbrirLastreada`, `onFecharLastreada`; 7710-7735 — wrappers `A.abrirLastreada`/`A.fecharLastreada`)
    - web/src/copy.js (chaves da Task 1)
    - web/tests/test_opcoes_proposta_ui.mjs (todas as asserções que medem `propostaFn` — precisam continuar verdes, exceto a de `store.optionsProposta(t)` que este plano atualiza com nota)
    - .planning/phases/17-fluxo-de-aceite/17-03-SUMMARY.md (forma exata do corpo aceito pela rota)
  </read_first>
  <action>
**(a) Declarar a capacidade.** No efeito das linhas 3154-3161, trocar
`store.optionsProposta(t)` por `store.optionsProposta(t, true)`. Comentário curto:
Fase 17 — o cliente agora sabe renderizar e aceitar estrutura de mais de uma perna
(ADR-025, Decisão 5; ADR-026), então declara a capacidade.

**(b) Renderizar o collar em `PropostaLastreada`**, SEM mudar a assinatura do
componente e SEM alterar as linhas `const isCall = ...` / `const cor = ...`:
- Acrescentar `const isCollar = p.tipo === "collar";` logo APÓS a linha de `cor`.
- Eyebrow (linha 3025): passa a ser
  `isCollar ? cp.eyebrowPropostaCollar : isCall ? cp.eyebrowPropostaCall : cp.eyebrowPropostaPut`.
- Linha de identificação do contrato (linha 3032): quando `isCollar`, renderizar
  `cp.collarPernasLinha(p.contratos, r.ticker, price(p.strikeCall), price(p.strikePut))`
  no lugar do texto de perna única. Manter o mesmo `<div>` com `fontFamily: MONO`.
- CTA (linhas 3048-3062): quando `isCollar` e `operador`, o rótulo vem de
  `p.caixa && p.caixa.fluxo === "credito" ? cp.ctaCollarCredito(...) : cp.ctaCollarDebito(...)`,
  com os argumentos `(p.contratos, r.ticker, price(p.strikeCall), price(p.strikePut),
  price(Math.abs((p.caixa && p.caixa.custoLiquidoTotal) || 0)))`. O `onClick`
  continua sendo `posAberta ? onFechar : onAbrir` — o despacho collar × perna única
  acontece no HANDLER (item c), não numa prop nova.
- Modo Estudo: nenhuma mudança estrutural — o bloco `{!operador && ...}` continua
  sendo o único ramo do Estudo, e o CTA continua dentro de `{operador && ...}`.
  Preservar as distâncias de caracteres que o guardião mede (<= 120 entre
  `{!operador && (` e `{p.didatica}`; <= 60 entre `{operador && (` e `<button`).

**(c) Aceite explícito no handler.** Em `onAbrirLastreada` (linhas 3219-3228),
acrescentar, ANTES do caminho de perna única:
- Se `p.tipo === "collar"`: exigir
  `window.confirm(cp.confirmAbrirCollar(p.contratos, t, p.qtyAcoes))` — a trava
  protetora TRAVA lastro pela perna da call, mesma razão que já obriga confirmação
  na venda coberta (T-14-24). Se o usuário cancelar, `return` sem chamar nada.
- Chamar `await A.abrirCollar({ underlying: t, pernasContratos: (p.pernasContratos ||
  []).map((perna) => ({ contractSymbol: perna.contractSymbol, lado: perna.lado })),
  contratos: p.contratos, expiration: p.expiration })`. Enviar SÓ `contractSymbol` e
  `lado` por perna — prêmio e strike vêm da proposta que o servidor re-deriva
  (Plano 17-03); reenviá-los daria a impressão de que o cliente os negocia.
- `setOpPropostaBusy(true)` / `finally setOpPropostaBusy(false)` como no caminho
  existente.

**(d) Wrapper `A.abrirCollar`.** Acrescentar em `App.jsx`, ao lado de
`A.abrirLastreada` (linha 7717), seguindo o MESMO formato do vizinho (mesma
estrutura de try/adoção de estado/tratamento de erro que `abrirLastreada` usa),
chamando `store.optionsAbrirCollar(body)`.

**(e) Atualizar o guardião existente com nota.** Em
`web/tests/test_opcoes_proposta_ui.mjs`, trocar o literal procurado
`"store.optionsProposta(t)"` por `"store.optionsProposta(t, true)"`, com comentário
datado registrando: mudança deliberada da Fase 17 (o cliente passa a declarar
capacidade multiperna); a asserção de que a chamada continua sob
`if (opGate && opGate.liquida)` permanece intacta. NÃO apagar a asserção.

PROIBIDO: alterar a assinatura de `PropostaLastreada`; alterar `posAberta`;
alterar a rota `/abrir` ou o caminho de perna única; compor manchete no front.
  </action>
  <verify>
    <automated>cd web && npx vite build && node tests/test_opcoes_proposta_ui.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx vite build` completa sem erro
    - `node web/tests/test_opcoes_proposta_ui.mjs` passa — incluindo a asserção pinada da assinatura de `PropostaLastreada` e as duas asserções de distância de caracteres
    - `grep -c "store.optionsProposta(t, true)" web/src/App.jsx` == 1
    - `grep -c "A.abrirCollar" web/src/App.jsx` >= 1 e `grep -c "abrirCollar:" web/src/App.jsx` == 1
    - `grep -n "window.confirm(cp.confirmAbrirCollar(" web/src/App.jsx` retorna exatamente 1 linha
    - `git diff web/src/App.jsx` NÃO altera as linhas `const isCall = p.optionType === "call";` e `const cor = isCall ? T.positive : T.negative;`
  </acceptance_criteria>
  <done>Uma trava protetora proposta aparece com as duas pernas e pode ser aceita com um clique confirmado, só no Modo Operador.</done>
</task>

<task type="auto">
  <name>Task 3: Guardião de aceite explícito e de não-execução automática</name>
  <files>web/tests/test_opcoes_collar_ui.mjs</files>
  <read_first>
    - web/src/App.jsx (as edições da Task 2)
    - web/src/persistence.js (os métodos da Task 1)
    - web/tests/test_opcoes_proposta_ui.mjs (estilo de asserção estática por slice + regex)
    - web/tests/test_carteira_lastro_ui.mjs (precedente de guardião de UI de operação lastreada)
  </read_first>
  <action>
Criar `web/tests/test_opcoes_collar_ui.mjs` (leitura estática de `App.jsx`,
`persistence.js`, `api.js` e import de `COPY`, mesmo padrão de
`test_opcoes_proposta_ui.mjs`), com os guardiões:

1. "capacidade multiperna declarada pelo cliente": `App.jsx` contém
   `store.optionsProposta(t, true)` e `api.js` monta `?multiperna=1`.

2. "aceite é sempre explícito — nenhuma execução em efeito": afirma que
   `A.abrirCollar(` / `store.optionsAbrirCollar(` NÃO aparecem dentro de nenhum
   `useEffect(` de `App.jsx`. Implementação: varrer as ocorrências de `useEffect(`
   e, para cada uma, extrair o bloco balanceado por parênteses e afirmar que
   nenhuma contém `abrirCollar`. Docstring: FLOW-02 — "recusar" é a ausência de
   clique, o que só é verdade se nada além do clique puder disparar a execução.

3. "confirmação antes de travar lastro": `window.confirm(cp.confirmAbrirCollar(`
   aparece exatamente uma vez e ANTES da chamada a `A.abrirCollar(` no mesmo
   handler (comparar índices dentro do slice do handler `onAbrirLastreada`).

4. "Modo Estudo não ganha botão no collar": dentro do slice de `PropostaLastreada`,
   afirma que existe exatamente UM `<button` e que ele continua dentro do bloco
   `{operador && (` (mesma medida do guardião existente, reafirmada para o caminho
   do collar).

5. "corpo enviado não carrega prêmio nem strike": o objeto passado a
   `A.abrirCollar(` no `App.jsx` não contém as strings `premio` nem `strike`.
   Docstring: o servidor re-deriva a proposta e usa os próprios prêmios (ADR-026,
   Decisão 2); mandar prêmio do cliente sugeriria que ele os negocia.

6. "paridade dos dois stores": `persistence.js` contém `optionsAbrirCollar` pelo
   menos 2 vezes, e o ramo sem sessão do `deviceStore` lança erro nomeado em vez de
   reimplementar a estrutura (afirmar presença de `throw new Error(` na vizinhança
   de `optionsAbrirCollar` no bloco do `deviceStore`).

7. "front não compõe manchete do collar": nenhum valor de `COPY.estudo`/
   `COPY.operador` contém as frases canônicas do motor para collar ("trava
   protetora" no sentido de manchete é do `skill_ref.py`) — reafirmar a asserção já
   existente de que nenhuma chave carrega texto de manchete, aplicada às chaves
   novas: `ctaCollarDebito`/`ctaCollarCredito`/`collarPernasLinha` avaliadas com
   argumentos de teste não podem conter "abate o custo" nem "Se você tivesse".

Cabeçalho do arquivo registrando: guardiões de FLOW-02/FLOW-03; apagá-los exige
nota explícita.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `node web/tests/test_opcoes_collar_ui.mjs` passa com os 7 guardiões
    - Injeção de falha registrada no SUMMARY: movendo a chamada de `A.abrirCollar` para dentro de um `useEffect`, o guardião 2 reprova; após `git checkout -- web/src/App.jsx`, passa
    - `bash scripts/executar.sh --testes` verde (as DUAS suítes)
    - `cd web && npx vite build` sem erro
    - `git status --porcelain server/` vazio — este plano é front-only
  </acceptance_criteria>
  <done>A suíte falha se a execução do collar puder disparar sem clique, sem confirmação, no Modo Estudo, ou com prêmio vindo do cliente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clique do usuário → chamada de execução | única origem legítima de uma ordem |
| cliente → rota de abertura de collar | o corpo montado aqui é o que a rota re-deriva e confere |
| `deviceStore` sem sessão → estado local | ramo legado sem servidor para validar a estrutura |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-22 | Elevation of Privilege | execução disparada por efeito/render em vez de clique | mitigate | Guardião 2 varre todos os `useEffect` e proíbe `abrirCollar` dentro deles |
| T-17-23 | Elevation of Privilege | CTA de execução aparecendo em Modo Estudo | mitigate | CTA permanece dentro de `{operador && (`; guardião 4; o servidor recusa com 403 mesmo se a UI tiver bug |
| T-17-24 | Tampering | cliente enviando prêmio/strike próprios | mitigate | Corpo carrega só `contractSymbol` + `lado` + `contratos`; guardião 5; a rota usa os prêmios da proposta re-derivada |
| T-17-25 | Tampering | segundo motor de execução no aparelho divergindo do servidor | mitigate | `deviceStore.optionsAbrirCollar` sem sessão lança erro nomeado — nenhum espelho local de estrutura de 2 pernas; guardião 6 |
| T-17-26 | Repudiation | usuário travando lastro sem saber | mitigate | `window.confirm(cp.confirmAbrirCollar(...))` declarando a trava, a quantidade e o "as duas juntas ou nenhuma"; guardião 3 |
| T-17-27 | Spoofing | front compondo manchete/veredito do collar | mitigate | Manchete continua vindo de `p.manchete`; guardiões existentes de CVM + guardião 7 sobre as chaves novas |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhuma dependência nova — `git diff --stat web/package.json package-lock.json` vazio |
</threat_model>

<verification>
- `cd web && npx vite build` sem erro (guardrail do CLAUDE.md: front editado).
- `bash scripts/executar.sh --testes` verde (suíte canônica, as duas suítes).
- `node web/tests/test_copy_theme.mjs`, `node web/tests/test_fase3_paridade_stores_generica.mjs`,
  `node web/tests/test_opcoes_proposta_ui.mjs`, `node web/tests/test_opcoes_collar_ui.mjs` verdes.
- `git status --porcelain server/` vazio.
- `git diff --stat web/package.json package-lock.json` vazio.
- A assinatura de `PropostaLastreada` e as linhas `isCall`/`cor` inalteradas no diff.
</verification>

<success_criteria>
- Uma trava protetora proposta é renderizada com as duas pernas e o payoff completo.
- O usuário aceita com uma ação explícita (clique + confirmação); recusar é não clicar.
- A execução usa a rota do Plano 17-03, que re-deriva e confere a proposta.
- Modo Estudo continua sem nenhum botão de executar.
- Os dois stores têm o método novo com contrato idêntico.
</success_criteria>

<output>
Create `.planning/phases/17-fluxo-de-aceite/17-05-SUMMARY.md` when done.
Registrar para o Plano 17-06: que o front está pronto e testado mas AINDA NÃO
publicado (nenhum bump, `server/web_dist` intocado).
</output>
