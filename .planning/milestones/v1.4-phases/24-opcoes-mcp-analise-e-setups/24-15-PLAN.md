---
phase: 24-opcoes-mcp-analise-e-setups
plan: 15
type: execute
wave: 8
depends_on: ["24-14"]
files_modified:
  - server/app/options_mcp_api.py
  - server/app/main.py
  - server/app/rbac.py
  - server/tests/test_mcp_cap.py
  - server/tests/test_adr013_cobertura_rotas.py
  - web-admin/src/App.jsx
  - web-admin/src/api.js
autonomous: true
requirements: [pedido do Alex 2026-09-11 — configurar o limite da cota de opções]

must_haves:
  truths:
    - "O limite da cota da aba Opções é configurável pelo portal admin, sem deploy"
    - "O valor configurado sobrevive a redeploy e vale para todo o processo"
    - "Quem muda fica registrado na auditoria"
    - "Sem nada configurado, o comportamento é exatamente o de hoje"
  artifacts:
    - path: "server/app/options_mcp_api.py"
      provides: "leitura e escrita da cota com precedência memória → DB → env → default"
      contains: "def set_cota_usuario_dia"
    - path: "web-admin/src/App.jsx"
      provides: "card de configuração da cota de opções"
      contains: "Cota da aba Opções"
---

<objective>
O Alex pediu (2026-09-11) um item de configuração para o limite da cota de
opções. Hoje os três tetos só existem como env do Railway:
`B3_MCP_COTA_USUARIO_DIA` (60), `B3_MCP_RATE_MIN` (20),
`B3_MCP_COTA_GLOBAL_DIA` (1800).

Purpose: mexer neles exige editar env e redeployar, o que é lento demais para
o que eles controlam. O teto que importa é compartilhado — 2.000 `tools/call`
por dia para TODA a base do Boris — e a Fase 24 acabou de multiplicar o consumo
por sessão (`/possibilidades` custa até 13). Quando o consumo real aparecer no
`/observabilidade`, o ajuste precisa ser de minutos, não de deploy.

Output: os três limites configuráveis pelo portal admin, no mesmo padrão que o
intervalo de spot da brapi já usa, com auditoria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-opcoes-mcp-analise-e-setups/24-CONTEXT.md
@CLAUDE.md

<padrao_a_copiar>
`server/app/brapi_budget.py` linhas ~265-305 — **é o precedente da casa e deve
ser seguido, não melhorado**:

```python
_SPOT_INTERVALO_KV = "brapiSpotIntervaloS"
SPOT_INTERVALO_DEFAULT_S = 300
_spot_intervalo_mem: Optional[int] = None

def spot_intervalo_s() -> int:
    """Ordem: memória → kv (sobrevive a deploy) → default."""

def set_spot_intervalo(segundos: int) -> int:
    """Aplica (mínimo 30s). Persiste no kv — sem deploy."""
```

A rota que o expõe, em `server/app/main.py` (~695-712):
`@app.post("/api/obs/brapi/projecao")` com
`Depends(require_permission("fontes_dados.configurar"))`, modo prévia
(`aplicar: false` devolve projeção sem mudar nada) e
`audit.record(_conn, user["id"], "brapi_spot_intervalo", None, "intervaloS",
anterior, vigente)`.

Hoje em `options_mcp_api.py` a leitura é `_int_env(nome, padrao)`, chamada A
CADA requisição — de propósito, porque o Railway muda env sem redeploy do
código e o teste usa `monkeypatch.setenv`. **Esse comportamento não pode
sumir**: o DB entra NA FRENTE da env, e a env continua valendo quando não há
valor no DB.
</padrao_a_copiar>

<guardrails>
- **Precedência exata:** memória → DB (kv) → env → default. Valor torto em
  qualquer camada cai para a seguinte; env inválida nunca derruba a rota nem,
  pior, desliga o cap (é o que o `_int_env` já garante hoje).
- **Mínimos que o código impõe**, não a UI: cota de usuário ≥ 1, rate ≥ 1,
  cota global ≥ 1. Um zero aqui desligaria o freio do teto compartilhado.
  Valor acima do teto do próprio serviço (2.000/dia) é ACEITO, mas a resposta
  avisa — quem manda no teto real é o serviço, e fingir que o Boris o controla
  seria pior que avisar.
- **Auditoria obrigatória** em toda escrita: `audit.record` com entidade
  `mcp_cota`, e a entidade entra em `rbac.ENTIDADES_POR_PERMISSAO` sob
  `fontes_dados.configurar` (mesma permissão do orçamento brapi: é teto de
  consumo de fonte de dados, não governança de IA).
- **Sem permissão nova.** Criar uma exigiria mexer nos grupos do ADR-013 e nos
  testes de bootstrap; `fontes_dados.configurar` já cobre exatamente esta
  classe de decisão.
- **A rota de escrita passa pelo guardião do ADR-013** — ela é `/api/obs/...`,
  fora do prefixo `/api/options/mcp`, então NÃO precisa de `_cap_check` (o
  guardião AST só olha funções decoradas com `@router.*` em
  `options_mcp_api.py`). Confirme rodando `test_mcp_guardioes.py`.
- **Não tocar** em `server/web_dist`, `web/src/version.js` nem na linha
  `SERVER_BUILD_ID`. O `web-admin/` tem publicação PRÓPRIA
  (`scripts/publicar-admin.sh`) e ela NÃO é deste plano.
- **Não chamar os mutadores de estado do `gsd-sdk`** (`CLAUDE.md`).
</guardrails>
</context>

<tasks>

<task type="auto">
  <name>Task 1: os três limites com precedência memória → DB → env → default</name>
  <read_first>
    - server/app/brapi_budget.py linhas 260-305 (o padrão, incluindo o `_DB_ENABLED` e o `try/except` que engole falha de kv)
    - server/app/options_mcp_api.py (`_int_env`, `_cota_usuario`, `_rate_min`, `_cota_global` e os usos em `_cap_check`)
  </read_first>
  <files>server/app/options_mcp_api.py</files>
  <action>
Para cada um dos três, um par leitura/escrita no padrão do `brapi_budget`:

- `cota_usuario_dia()` / `set_cota_usuario_dia(n)` — kv `mcpCotaUsuarioDia`
- `rate_min()` / `set_rate_min(n)` — kv `mcpRateMin`
- `cota_global_dia()` / `set_cota_global_dia(n)` — kv `mcpCotaGlobalDia`

As funções internas de hoje (`_cota_usuario`, `_rate_min`, `_cota_global`)
passam a delegar nas públicas — **um lugar só decide o valor vigente**; duas
portas divergem na primeira manutenção.

Cache em memória por processo, invalidado no `set`. Comentar por que o cache
existe e por que ele é seguro aqui: o valor muda por ação administrativa
explícita, não por evento de mercado, e o `set` roda no mesmo processo que lê.

Acrescente `limites()` devolvendo os três vigentes + a ORIGEM de cada um
(`"kv"`, `"env"` ou `"default"`). A origem é o que permite ao portal dizer se
o número veio do painel ou do Railway — sem ela, o admin muda pelo painel, a
env continua diferente, e ninguém entende qual manda.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/server && .venv/bin/python -m pytest tests/test_mcp_cap.py tests/test_options_mcp_api.py tests/test_options_mcp_leitura.py tests/test_opcoes_dsl.py tests/test_mcp_guardioes.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Sem kv e sem env → os defaults de hoje (60, 20, 1800).
    - Só env → o valor da env, `origem: "env"` (o comportamento atual não regrediu).
    - kv presente → o kv vence a env, `origem: "kv"`.
    - kv com lixo (não-inteiro, 0, negativo) → cai para env/default, nunca desliga o cap.
    - `set_*` persiste, invalida o cache e o valor sobrevive a uma releitura.
    - Toda a suíte de cap existente continua verde (ela usa `monkeypatch.setenv`).
  </acceptance_criteria>
  <done>Os três limites têm uma fonte de verdade só, configurável sem deploy, sem perder a env.</done>
</task>

<task type="auto">
  <name>Task 2: rota admin com prévia e auditoria</name>
  <read_first>
    - server/app/main.py linhas 690-715 (a rota do intervalo de spot: prévia, aplicação e `audit.record`)
    - server/app/rbac.py (`ENTIDADES_POR_PERMISSAO` e a entrada `opcoes_setup` que o 24-03 acrescentou)
  </read_first>
  <files>server/app/main.py, server/app/rbac.py</files>
  <action>
`GET /api/obs/opcoes/cota` — `Depends(require_permission("fontes_dados.configurar"))`.
Devolve `limites()` (os três, com origem) e o consumo de hoje: `usado` do
escopo global da seção `mcpUsageGlobal`, para o admin decidir olhando o número
real, não no escuro.

`POST /api/obs/opcoes/cota` — mesma permissão. Corpo
`{cotaUsuarioDia?, rateMin?, cotaGlobalDia?, aplicar?}`. Sem `aplicar: true`,
é PRÉVIA: valida, devolve o que mudaria e não escreve nada (mesmo contrato da
rota da brapi — o admin vê antes de confirmar).

Com `aplicar: true`: aplica só os campos presentes, e para CADA um que mudou
de valor, um `audit.record(_conn, user["id"], "mcp_cota", None, <campo>,
anterior, vigente)`. Um registro por campo, não um agregado: a auditoria
responde "quem mudou o quê", e um agregado não responde.

Valor acima de 2.000/dia (o teto do próprio serviço, contrato) é aceito e a
resposta traz `aviso` dizendo que o teto real é do serviço e que o Boris não o
controla. Rejeitar seria fingir que o Boris manda no que não manda; calar
seria deixar o admin achar que subiu um teto que não subiu.

`rbac.ENTIDADES_POR_PERMISSAO["fontes_dados.configurar"]` ganha `"mcp_cota"`
(hoje tem só `brapi_spot_intervalo`), com nota dizendo por que entrou.
`test_adr013_cobertura_rotas.py`: as duas rotas novas são gated, então NÃO
entram na allowlist pública — confirme que o guardião passa sem tocar nela.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/server && .venv/bin/python -m pytest tests/test_adr013_cobertura_rotas.py tests/test_adr013_rbac.py tests/test_mcp_cap.py tests/test_mcp_guardioes.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Sem sessão → 401; com sessão sem a permissão → 403; com a permissão → 200.
    - Prévia (`aplicar` ausente/falso) não escreve nada — provado relendo os limites depois.
    - Aplicar grava, muda o vigente e registra UM evento de auditoria POR CAMPO alterado.
    - Campo ausente no corpo não é alterado.
    - Valor inválido (0, negativo, texto) → 400, e nada é alterado.
    - Valor acima de 2.000 → aceito, com `aviso` no corpo.
    - A allowlist pública do ADR-013 NÃO cresceu.
  </acceptance_criteria>
  <done>O admin lê, simula e aplica os três limites, e cada mudança fica registrada com autor.</done>
</task>

<task type="auto">
  <name>Task 3: o card no portal admin</name>
  <read_first>
    - web-admin/src/App.jsx (o `Card title="Orçamento brapi (ADR-008)"` — layout, tokens e o padrão de aplicar com prévia)
    - web-admin/src/api.js (como as chamadas são declaradas)
  </read_first>
  <files>web-admin/src/App.jsx, web-admin/src/api.js</files>
  <action>
Card "Cota da aba Opções (ADR-027)", ao lado do card de orçamento brapi (é a
mesma família: teto de consumo de fonte externa).

Mostra os três limites com a ORIGEM de cada um — "painel", "variável de
ambiente" ou "padrão" —, o consumo global de hoje, e três campos numéricos com
botão de prévia e botão de aplicar. A prévia mostra o que mudaria antes de
gravar, como o card da brapi já faz.

O card diz, em uma linha, o que a pessoa precisa saber para decidir: o teto de
2.000 chamadas/dia é do SERVIÇO e é compartilhado por toda a base do Boris;
estes limites são o freio do Boris DENTRO dele. Sem essa frase, um admin pode
subir a cota por usuário achando que está aumentando o total disponível.

Sem permissão, o card não é renderizado — e o backend recusa de qualquer forma
(ADR-013: nunca escondido só na UI).
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/web-admin && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `npx vite build` do web-admin verde.
    - O card mostra os três limites, a origem de cada um e o consumo de hoje.
    - Prévia antes de aplicar.
    - A frase sobre o teto compartilhado do serviço está no card.
    - `web-admin` não foi publicado (isso é `scripts/publicar-admin.sh`, fora deste plano).
  </acceptance_criteria>
  <done>O limite é ajustável pelo painel, com a informação necessária para a decisão fazer sentido.</done>
</task>

</tasks>

<verification>
1. `bash scripts/executar.sh --testes` FORA do sandbox.
2. `cd web-admin && npx vite build`.
3. Saída no SUMMARY.
</verification>

<success_criteria>
- Três limites configuráveis sem deploy, com a env preservada como camada.
- Auditoria por campo.
- Card no portal com origem, consumo e a ressalva do teto compartilhado.
- Allowlist pública do ADR-013 inalterada.
</success_criteria>

<output>
Create `.planning/phases/24-opcoes-mcp-analise-e-setups/24-15-SUMMARY.md` when done
</output>
