---
phase: 24-opcoes-mcp-analise-e-setups
plan: 14
type: execute
wave: 7
depends_on: ["24-13"]
gap_closure: true
files_modified:
  - server/app/options_mcp_api.py
  - server/tests/test_opcoes_dsl.py
  - web/src/opcoes/CriarSetup.jsx
  - web/src/copy.js
  - web/tests/test_opcoes_criar_setup_ui.mjs
autonomous: true
requirements: [achado ao vivo 2026-09-11 — ensaio de 0 disparos que não testou nada]

must_haves:
  truths:
    - "Quando uma condição do setup nunca pôde ser avaliada, o ensaio diz isso em vez de deixar '0 disparos' passar por resultado"
    - "O aviso é derivado do que o próprio dry-run devolveu — janela declarada contra tamanho do período, sem chamada extra"
    - "Setup cujas janelas cabem no histórico não ganha aviso nenhum"
  artifacts:
    - path: "server/app/options_mcp_api.py"
      provides: "_ensaio_inconclusivo — quais condições nunca fecharam a janela"
      contains: "def _ensaio_inconclusivo"
---

<objective>
Achado ao vivo em 2026-09-11, reproduzido localmente contra o motor do serviço:
um setup com `sma 200` sobre um histórico de 48 pregões devolve
`disparos: 0` e `pregoes_avaliaveis: 31` — números que comunicam "testei e não
disparou" quando a verdade é "**nunca pôde disparar**".

Purpose: a lógica do serviço está correta (`AND` com um `False` conhecido é
`False`, e nos dias de RSI acima de 30 o dia é comprovadamente falso). O que
engana é a leitura. E é pior que o caso do 24-11: lá o campo vinha vazio e a
pessoa via que faltava algo; aqui vem um número plausível, e número plausível
não levanta suspeita. Alguém pode GRAVAR um setup acreditando que ele foi
validado contra o histórico.

Demonstração da impossibilidade, não opinião: se uma condição de um `AND`
nunca é verdadeira, `diario` nunca é `True`, logo `disparos` é obrigatoriamente
0. O ensaio não testou nada.

Output: um helper puro que detecta isso a partir do que o dry-run já devolveu,
e a faixa no card do ensaio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-opcoes-mcp-analise-e-setups/24-CONTEXT.md
@CLAUDE.md

<medicao_ja_feita>
Reproduzido em 2026-09-11 com o motor real do serviço (`~/dev/MCP/servers/mydata/setups.py`),
48 candles sintéticos e o setup que o modelo compilou ao vivo:

```
condição "close > sma200"  ->  0 valores não-None em 48   (NUNCA avaliável)
condição "rsi14 < 30"      -> 34 valores não-None em 48
diario: True=0  False=31  None=17
backtest.pregoes_avaliaveis: 31    backtest.disparos: 0
disparos_por_100_pregoes_avaliaveis: 0.0
```

Não refaça essa medição; ela é o insumo do plano.
</medicao_ja_feita>

<dados_disponiveis>
O dry-run (`/setups/compilar`, resposta 200) já traz tudo o que o helper
precisa — **nenhuma chamada nova**:
- `setup.conditions[]` — cada uma com `indicator` e, quando tem janela,
  `window`; a comparação pode ter `reference: {indicator, window}`, e a janela
  do lado direito conta igual;
- `setup.consecutive_days` (default 1);
- `backtest.periodo.pregoes` — o tamanho da série.

Indicadores COM janela (o resto não tem): `sma`, `ema`, `rsi`, `atr`,
`highest`, `lowest`, `rel_volume`, `change_pct`.
</dados_disponiveis>

<guardrails>
- **Não recalcular backtest nem reimplementar indicador.** O helper compara
  números que já vieram; é aritmética de janela, não análise técnica.
- **Não afirmar o que não se sabe.** O helper detecta "a janela não cabe no
  período" — que é demonstrável. NÃO tente inferir "o setup é ruim" nem
  "dispararia se houvesse mais dado".
- **Sem janela declarada → sem aviso.** Condição de padrão (`pattern`) e
  indicador direto (`close`, `hv21`) não têm janela e nunca disparam este
  aviso.
- `pregoes` ausente ou não-numérico → lista vazia, e a tela não afirma nada.
- Texto novo nas DUAS vozes de `copy.js`, chaves idênticas, `didatica-boris`
  obrigatória. Este texto é o mais delicado da fase: ele diz à pessoa que o
  número que ela está vendo não significa o que parece.
- **Não chamar os mutadores de estado do `gsd-sdk`** (`CLAUDE.md`).
- **Não tocar** em `server/web_dist`, `web/src/version.js` nem na linha
  `SERVER_BUILD_ID`.
</guardrails>
</context>

<tasks>

<task type="auto">
  <name>Task 1: _ensaio_inconclusivo no backend</name>
  <read_first>
    - server/app/options_mcp_api.py (`_lacunas_da_leitura`, do 24-11 — é o helper irmão e o estilo a seguir; e a rota `/setups/compilar`)
  </read_first>
  <files>server/app/options_mcp_api.py</files>
  <action>
```python
JANELA_POR_INDICADOR = ("sma", "ema", "rsi", "atr", "highest", "lowest",
                        "rel_volume", "change_pct")


def _ensaio_inconclusivo(setup, backtest) -> dict:
    """Condições cuja janela não cabe no histórico usado pelo ensaio.

    **Por que existe** (achado ao vivo, 2026-09-11): um setup com média de 200
    sobre 48 pregões volta `disparos: 0` e `pregoes_avaliaveis: 31`. Os dois
    números estão certos — `AND` com um `False` conhecido é `False`, e nos dias
    de RSI acima de 30 o dia é comprovadamente falso. O que está errado é o que
    a pessoa entende: "testei e não disparou", quando a verdade é que a
    condição da média NUNCA teve valor e o setup era indisparável.

    É pior que um campo vazio: vazio se vê, número plausível não. Alguém pode
    GRAVAR um setup acreditando que ele passou por um teste que não houve.

    A conta é de janela, não de análise técnica: uma janela de `w` sobre `n`
    pregões produz `n - w + 1` pontos. `<= 0` significa que a condição nunca
    teve valor; menos que `consecutive_days` significa que a sequência exigida
    é impossível mesmo com todos os pontos verdadeiros.
    """
```

Retorna `{"indisparavel": bool, "condicoes": [{"indicador", "janela",
"pontos", "motivo"}], "pregoes": n}`.

Regras, e nada além delas:
- para cada condição, considerar a janela do próprio lado E a de `reference`;
  a maior manda;
- `pontos = n - w + 1`;
- `pontos <= 0` → motivo `MOTIVO_JANELA_NUNCA_FECHOU`;
- `0 < pontos < consecutive_days` → motivo `MOTIVO_JANELA_CURTA_PARA_SEQUENCIA`;
- `indisparavel` é verdadeiro quando ALGUMA condição tem `pontos <= 0` e a
  lógica é `AND` (com `OR`, uma condição morta não impede as outras — não
  afirme indisparabilidade que não se pode provar);
- nenhuma condição problemática → `{"indisparavel": False, "condicoes": [],
  "pregoes": n}`.

Constantes de texto no bloco dos avisos e acrescentadas a `AVISOS`:

```python
MOTIVO_JANELA_NUNCA_FECHOU = (
    "esta condição precisa de mais pregões do que o histórico do ensaio tem, "
    "então ela não teve valor em nenhum dia — não é que não tenha ocorrido, "
    "é que não deu para verificar"
)
MOTIVO_JANELA_CURTA_PARA_SEQUENCIA = (
    "esta condição só teve valor em pregões demais no fim do histórico para "
    "formar a sequência que o setup exige"
)
AVISO_ENSAIO_INDISPARAVEL = (
    "Este ensaio não testou o setup: uma das condições nunca pôde ser "
    "verificada no histórico disponível, e por isso o número de disparos é "
    "zero por construção — não por raridade."
)
```

No retorno 200 de `/setups/compilar`, campo `ensaio` com o resultado. O
`backtest` continua VERBATIM ao lado.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/server && .venv/bin/python -m pytest tests/test_opcoes_dsl.py tests/test_guardrail_imperativo.py tests/test_mcp_guardioes.py -q</automated>
  </verify>
  <acceptance_criteria>
    - O caso medido (sma 200 + rsi 14, AND, 2 dias, 48 pregões) → `indisparavel: True`, uma condição listada com `pontos <= 0`.
    - Setup só com `rsi 14` e `sma 21` sobre 48 pregões → `condicoes: []` e `indisparavel: False`.
    - `logic: "OR"` com uma condição morta → a condição É listada, mas `indisparavel` é `False`.
    - Janela em `reference` (`close > sma200`) é detectada — é justamente a do caso real.
    - `pattern` e indicador sem janela nunca entram na lista.
    - `pregoes` ausente → lista vazia, sem exceção.
    - Os três textos em `AVISOS`; guardião imperativo verde.
  </acceptance_criteria>
  <done>O backend demonstra, a partir dos números que já tinha, quando o ensaio não testou nada.</done>
</task>

<task type="auto">
  <name>Task 2: a faixa no card do ensaio</name>
  <read_first>
    - web/src/opcoes/CriarSetup.jsx (o componente `Ensaio` e onde o backtest é exibido)
    - web/src/copy.js
  </read_first>
  <files>web/src/opcoes/CriarSetup.jsx, web/src/copy.js</files>
  <action>
No card do ensaio, ACIMA dos números do backtest (não abaixo: quem lê o número
primeiro já formou a conclusão), uma faixa quando `ensaio.indisparavel` for
verdadeiro, com `cp.opcoesEnsaioIndisparavel` e a lista das condições
afetadas — cada uma com o indicador, a janela e o motivo verbatim do backend.

Quando há condições listadas mas `indisparavel` é falso (caso `OR`, ou janela
curta para a sequência), a faixa aparece em tom mais discreto, com
`cp.opcoesEnsaioRessalva` — é ressalva, não invalidação.

O botão de gravar **continua existindo** nos dois casos. Não é a tela que
decide o que a pessoa pode fazer com um setup que ela escreveu; o que a tela
deve é não deixá-la concluir errado. Bloquear seria trocar um problema de
informação por um de autonomia.

Chaves novas nos dois modos: `opcoesEnsaioIndisparavel`,
`opcoesEnsaioRessalva`, `opcoesEnsaioCondicao` (função
`(indicador, janela, motivo) => ...`).
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/web && npx vite build && node tests/test_opcoes_criar_setup_ui.mjs && node tests/test_copy_theme.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx vite build` verde.
    - A faixa aparece ANTES dos números do backtest no fonte.
    - O botão de gravar continua presente no ramo de dry-run com `indisparavel: true`.
    - Sem `ensaio` no payload, nada é renderizado (compatível com o servidor anterior).
    - Chaves nos dois modos; conjunto `opcoes*` idêntico.
  </acceptance_criteria>
  <done>Quem vê "0 disparos" vê, antes, por que esse zero não significa o que parece.</done>
</task>

<task type="auto">
  <name>Task 3: guardiões</name>
  <files>server/tests/test_opcoes_dsl.py, web/tests/test_opcoes_criar_setup_ui.mjs</files>
  <action>
Backend: os seis casos do aceite da Task 1, por import direto do helper.
Front: a faixa existe, vem antes dos números, o botão de gravar sobrevive, e
`ensaio` ausente não renderiza nada. Com asserções de sanidade nas regexes.

Todos provados RED antes da correção; prova no SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2 && bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - Suíte canônica sem regressão sobre `2478 passed, 5 skipped` + 129 `.mjs`.
    - Cada regex de defeito com sanidade.
  </acceptance_criteria>
  <done>A regra "zero por construção não é zero por raridade" está travada nos dois lados.</done>
</task>

</tasks>

<verification>
1. `bash scripts/executar.sh --testes` FORA do sandbox (`dangerouslyDisableSandbox: true`).
2. `cd web && npx vite build`.
</verification>

<success_criteria>
- O ensaio do caso medido avisa que não testou o setup.
- Setup com janelas que cabem não ganha aviso.
- Nenhum número recalculado, nenhuma chamada nova, nenhum bloqueio de ação.
</success_criteria>

<output>
Create `.planning/phases/24-opcoes-mcp-analise-e-setups/24-14-SUMMARY.md` when done
</output>
