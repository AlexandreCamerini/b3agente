---
phase: 24-opcoes-mcp-analise-e-setups
plan: 17
type: execute
wave: 10
depends_on: ["24-16"]
files_modified:
  - server/app/main.py
  - server/app/rbac.py
  - server/tests/test_admin_plano_usuario.py
  - server/tests/test_adr013_rbac.py
  - web-admin/src/App.jsx
  - web-admin/src/api.js
  - web/tests/test_admin_plano_ui.mjs
autonomous: true
requirements: [pedido do Alex 2026-09-12 — mudar o plano do usuário pelo portal]

must_haves:
  truths:
    - "Um administrador muda o plano de uma conta pelo portal, sem tocar no banco"
    - "Toda mudança de plano fica na auditoria, com quem fez e o valor anterior"
    - "Plano inválido é recusado pelo backend, não só pela UI"
    - "Quem não tem `usuarios.gerenciar` não vê o controle E recebe 403 na rota"
  artifacts:
    - path: "server/app/main.py"
      provides: "POST /api/admin/users/{user_id}/plan"
      contains: "user_plan"
    - path: "web-admin/src/App.jsx"
      provides: "seletor de plano no card Usuários e papéis"
      contains: "plano"
---

<objective>
O Alex pediu (2026-09-12) mudar o plano de uma conta pelo portal admin.

Purpose: `pro` é, nas palavras do `server/app/plan.py`, "estado alcançável só
por atribuição manual" — e a única porta hoje é o SQLite do container, via
`scripts/plano-da-conta.sh` que esta sessão criou. Funciona para uma conta do
próprio dono; não funciona quando houver assinantes. O portal já tem o card
"Usuários e papéis", com a rota, a permissão e a auditoria montadas: falta o
eixo comercial ao lado do de governança.

**Isto reverte uma decisão registrada.** A rota de papéis diz, na docstring:
"Sem override de plano nesta rodada (decisão do Alex, ADR-013) — só papel de
governança", e o card no portal repete. As duas notas precisam ser ATUALIZADAS
dizendo que a decisão mudou, e quando — o repositório registra reversão
deliberada, não a apaga.

Output: rota de escrita de plano, com auditoria, e o controle no card.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md

<o_que_ja_existe>
`server/app/main.py`:
- `GET /api/admin/users` → `{usuarios: [...], gruposDisponiveis: [...]}`, com
  `require_permission("usuarios.gerenciar")`. `db.list_users` já devolve a
  coluna `plan` (ver `_USER_COLS` em `db.py`) — confirme antes de acrescentar.
- `POST /api/admin/users/{user_id}/roles` — **é o padrão a espelhar**: valida o
  usuário (404), lê o anterior, aplica, grava
  `audit.record(_conn, user["id"], "user_role", user_id, "roles", anterior, novo)`
  e devolve o estado novo. Tem também o freio de escalação: só quem é
  `role_admin` concede `role_admin`.

`server/app/plan.py`: `PLANOS_POR_ID = {"free": PLAN_FREE, "pro": PLAN_PRO}`,
`_ORDEM_PLANO = ["free", "pro"]`. A fonte da verdade dos ids é essa — NÃO
escreva uma lista nova de planos em lugar nenhum (nem no backend, nem na UI:
a UI recebe os ids do backend).

`server/app/rbac.py`: `ENTIDADES_POR_PERMISSAO["usuarios.gerenciar"] = {"user_role"}`.

`web-admin/src/App.jsx`: `function Usuarios({ user })` com o card, o
`window.confirm` antes de cada escrita e o tratamento de erro por mensagem.
</o_que_ja_existe>

<guardrails>
- **A lista de planos vem de `plan.PLANOS_POR_ID`**, servida pelo backend em
  `GET /api/admin/users` (ex.: `planosDisponiveis`), como `gruposDisponiveis`
  já faz com os papéis. Uma lista `["free","pro"]` escrita na UI seria a
  segunda cópia que não acompanha o dia em que existir um terceiro plano.
- **Auditoria obrigatória**, entidade `user_plan`, e a entidade entra em
  `rbac.ENTIDADES_POR_PERMISSAO["usuarios.gerenciar"]` (hoje só `user_role`) —
  senão o evento existe mas some da tela de auditoria de quem tem a permissão.
- **Confirmação explícita na UI**, como o card já faz para papéis: o efeito é
  imediato e o portal abre no celular (ADR-014).
- **Mudar o próprio plano é permitido e fica auditado.** Não invente freio de
  auto-promoção: quem tem `usuarios.gerenciar` já pode conceder a si mesmo
  qualquer papel de governança, e o registro com o nome de quem clicou é a
  mitigação que o ADR-013 escolheu para essa classe. Um freio novo aqui seria
  regra inventada, e assimétrica com o que existe ao lado.
- **Sem `require_subscription`.** `plan.requires_subscription` é hook do
  futuro (validar recibo de loja) e hoje "nunca exige" — não o toque.
- **Não tocar** em `server/web_dist`, `server/admin_dist`, `web/src/version.js`
  nem na linha `SERVER_BUILD_ID`. A publicação é etapa posterior.
- **Não chamar os mutadores de estado do `gsd-sdk`** (`CLAUDE.md`).
- Commit por heredoc com delimitador entre aspas — `-m "…"` com crases faz o
  shell executar o conteúdo (aconteceu nesta sessão).
</guardrails>
</context>

<tasks>

<task type="auto">
  <name>Task 1: rota de escrita do plano, com auditoria</name>
  <read_first>
    - server/app/main.py linhas 1100-1135 (`GET /api/admin/users` e `POST .../roles` inteiras)
    - server/app/plan.py (`PLANOS_POR_ID`, `_ORDEM_PLANO`, `current_plan`)
    - server/app/db.py (`_USER_COLS`, `list_users`, `get_user_by_id` — e se existe helper de escrita de plano; se não existir, crie um pequeno ao lado dos irmãos, no estilo do arquivo)
    - server/app/rbac.py (`ENTIDADES_POR_PERMISSAO`)
  </read_first>
  <files>server/app/main.py, server/app/rbac.py</files>
  <action>
**1. `GET /api/admin/users`** passa a devolver `planosDisponiveis` (de
`plan.PLANOS_POR_ID`, na ordem de `_ORDEM_PLANO`) e garante que cada usuário
traz `plan`. Se `db.list_users` já devolve, não duplique — só confirme.

**2. `POST /api/admin/users/{user_id}/plan`**, espelhando a rota de papéis:

```python
@app.post("/api/admin/users/{user_id}/plan")
async def admin_users_plan_post(user_id: str, body: dict = Body(default={}),
                                user: dict = Depends(require_permission("usuarios.gerenciar"))):
    """Eixo COMERCIAL da conta (ADR-010), separado do eixo de governança que a
    rota de papéis controla. `pro` era, até 2026-09-12, alcançável só por
    edição direta no SQLite do container (`scripts/plano-da-conta.sh`) —
    porta que não escala para além do próprio dono.

    Mudar o PRÓPRIO plano é permitido e fica auditado: quem tem
    `usuarios.gerenciar` já pode conceder a si mesmo qualquer papel de
    governança, e o registro com o nome de quem clicou é a mitigação que o
    ADR-013 escolheu para essa classe. Um freio só aqui seria assimétrico com
    o que existe ao lado.
    """
```

Corpo `{plano}`. Validação: usuário inexistente → 404; `plano` fora de
`plan.PLANOS_POR_ID` → 400 nomeando os aceitos (o backend recusa, não só a
UI). Lê o anterior, aplica, e grava
`audit.record(_conn, user["id"], "user_plan", user_id, "plan", anterior, novo)`.
Devolve `{"ok": True, "userId": user_id, "plano": novo}`.

**3.** `rbac.ENTIDADES_POR_PERMISSAO["usuarios.gerenciar"]` ganha `"user_plan"`,
com nota datada dizendo por que entrou.

**4.** A docstring de `POST .../roles` diz hoje "Sem override de plano nesta
rodada (decisão do Alex, ADR-013) — só papel de governança". **Atualize-a**:
a decisão mudou em 2026-09-12, o override existe agora na rota irmã, e o
motivo original (separar governança de monetização) continua valendo — são
duas rotas justamente porque são dois eixos. Não apague o registro antigo;
acrescente a reversão com a data.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/server && .venv/bin/python -m pytest tests/test_adr013_rbac.py tests/test_adr013_cobertura_rotas.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Sem sessão → 401; com sessão sem `usuarios.gerenciar` → 403; com → 200.
    - Usuário inexistente → 404. Plano inválido (`"premium"`, `""`, `None`, número) → 400 nomeando os aceitos.
    - Aplicar muda o plano de fato (relido por `db.get_user_by_id`) e grava UM evento `user_plan` com o anterior e o novo.
    - `planosDisponiveis` vem de `plan.PLANOS_POR_ID` — sem lista literal nova no `main.py`.
    - A allowlist pública do ADR-013 NÃO cresce (a rota é gated).
    - A docstring da rota de papéis registra a reversão com data.
  </acceptance_criteria>
  <done>O plano de uma conta muda por rota gated e auditada, sem tocar no banco.</done>
</task>

<task type="auto">
  <name>Task 2: o controle no card do portal</name>
  <read_first>
    - web-admin/src/App.jsx (`function Usuarios`, inteira — incluindo o comentário de topo que diz "SEM override de plano nesta rodada")
    - web-admin/src/api.js (`usersGet`, `userRole`)
  </read_first>
  <files>web-admin/src/App.jsx, web-admin/src/api.js</files>
  <action>
`api.js`: `userPlan: (uid, plano) => req("POST", "/api/admin/users/" + encodeURIComponent(uid) + "/plan", { plano })`.

No card, ao lado dos papéis de cada usuário, o plano atual e o controle para
trocá-lo. As opções vêm de `data.planosDisponiveis` — nunca de uma lista
escrita na UI.

Confirmação explícita antes de aplicar, no mesmo padrão e pelo mesmo motivo
que os papéis já fazem (efeito imediato, portal abre no celular, ADR-014). O
texto da confirmação nomeia o plano de destino e a conta.

Erro do backend aparece como mensagem, reusando o `msg` que o card já tem.
Alvo de toque de 44 px, como o resto do portal.

**Atualize o comentário de topo do componente**: ele diz "SEM override de
plano nesta rodada (decisão do Alex) — só leitura". A decisão mudou em
2026-09-12; registre a reversão em vez de apagar a nota.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/web-admin && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `npx vite build` do web-admin verde.
    - As opções de plano vêm de `data.planosDisponiveis`; nenhuma lista literal de planos no fonte do portal.
    - Confirmação antes de aplicar, nomeando plano e conta.
    - O plano atual de cada usuário aparece no card.
    - O comentário de topo registra a reversão com data.
  </acceptance_criteria>
  <done>O administrador vê e muda o plano no mesmo lugar onde já vê os papéis.</done>
</task>

<task type="auto">
  <name>Task 3: guardiões</name>
  <files>server/tests/test_admin_plano_usuario.py, web/tests/test_admin_plano_ui.mjs</files>
  <action>
**Backend** (`test_admin_plano_usuario.py`): os seis casos do aceite da Task 1,
pelo caminho HTTP completo, com banco temporário — no padrão dos testes de
rota já existentes. Inclua explicitamente:
- o 403 para quem tem OUTRA permissão admin mas não `usuarios.gerenciar`
  (prova que o gate é o certo, não "qualquer admin");
- o evento de auditoria com `entity == "user_plan"`, `old_value` e `new_value`;
- mudar o próprio plano funciona e fica auditado (é o comportamento decidido,
  e um teste evita que alguém "conserte" isso depois sem ler a razão).

**Front** (`test_admin_plano_ui.mjs`, estático, no estilo dos guardiões de
`web/tests/`): o portal não tem lista literal de planos; a confirmação existe
antes da chamada; o controle usa `data.planosDisponiveis`. Com asserções de
sanidade nas regexes.

Todos provados RED antes da correção; registre no SUMMARY quais falharam.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2 && bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - Suíte canônica sem regressão sobre `2542 passed, 5 skipped` + 130 `.mjs`.
    - Cada regex de defeito com sanidade.
  </acceptance_criteria>
  <done>A rota e a tela têm guardião; a decisão de permitir auto-promoção está travada por teste, com a razão escrita.</done>
</task>

</tasks>

<verification>
1. `bash scripts/executar.sh --testes` FORA do sandbox (`dangerouslyDisableSandbox: true`).
2. `cd web-admin && npx vite build`.
</verification>

<success_criteria>
- Plano muda pelo portal, gated e auditado.
- Lista de planos com fonte única no backend.
- As duas notas de "sem override de plano" atualizadas com a reversão datada.
</success_criteria>

<output>
Create `.planning/phases/24-opcoes-mcp-analise-e-setups/24-17-SUMMARY.md` when done
</output>
