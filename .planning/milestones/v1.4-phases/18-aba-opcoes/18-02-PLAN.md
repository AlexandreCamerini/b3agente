---
phase: 18-aba-opcoes
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - web/src/App.jsx
autonomous: true
requirements: [NAV-02]

must_haves:
  truths:
    - "Uma posição com proposta ativa mostra, dentro do próprio card em Posições, uma linha que abre o detalhe completo da estrutura"
    - "O detalhe aberto é o MESMO componente PropostaLastreada já usado no card de ativo — payoff, chips e manchete idênticos"
    - "Em Modo Operador o usuário consegue aceitar (venda coberta, put, collar) e encerrar a estrutura direto de Posições"
    - "Posição sem proposta ativa não ganha caixa vazia nem aviso — o card fica exatamente como está hoje"
    - "Nenhuma estrutura aparece sobre ticker sem posição real (o laço itera data.positions)"
  artifacts:
    - path: "web/src/App.jsx"
      provides: "componente PropostaDaPosicao — detalhe + aceite/encerramento escopados a uma posição"
      contains: "function PropostaDaPosicao"
    - path: "web/src/App.jsx"
      provides: "CarteiraScreen consumindo useOpcoesPropostas e renderizando o detalhe por posição"
      contains: "useOpcoesPropostas("
  key_links:
    - from: "web/src/App.jsx (CarteiraScreen)"
      to: "PropostaLastreada"
      via: "<PropostaDaPosicao> dentro do laço data.positions.map"
      pattern: "<PropostaDaPosicao"
    - from: "web/src/App.jsx (PropostaDaPosicao)"
      to: "A.abrirLastreada / A.abrirCollar / A.fecharLastreada"
      via: "handlers escopados ao ticker da posição"
      pattern: "A\\.abrirCollar\\("
---

<objective>
NAV-02: renderizar o detalhe completo da proposta de opções DENTRO do card da
posição correspondente em Posições/Portfólio, com o caminho de aceite e de
encerramento funcionando ali — sem tocar em `AtivoCard` (Watchlist/Radar
continuam com a superfície de descoberta que já têm).

Purpose: hoje `PropostaLastreada` só existe dentro de `AtivoCard`. A tira do
Plano 18-03 precisa de um alvo real para abrir; este plano cria esse alvo. É
também o que garante o requisito "nunca uma estrutura sobre ticker sem posição
real" de forma ESTRUTURAL — `CarteiraScreen` só itera `data.positions`.

Output: componente `PropostaDaPosicao` + `CarteiraScreen` consumindo
`useOpcoesPropostas` e um estado `opcoesFor` (mesma forma de `histFor`/`editFor`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-aba-opcoes/18-CONTEXT.md
@.planning/phases/18-aba-opcoes/18-PATTERNS.md
@.planning/phases/18-aba-opcoes/18-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contratos que este plano consome. Não explorar o codebase para descobri-los. -->

Do Plano 18-01 (já em `web/src/App.jsx`):
- `useOpcoesPropostas(tickers)` → `{ propostas, carregando }`
  `propostas[ticker]` = `{ gate, proposta }`, onde `proposta` é a resposta crua
  de `store.optionsProposta(t, true)` (o objeto que `PropostaLastreada` recebe
  como prop `r`), ou `null`.

Do Plano 18-01 (já em `web/src/copy.js`, nos dois ramos):
- `cp.linhaPropostaNaPosicao` (string) — rótulo da linha colapsada.

Componente reusado sem alterar a assinatura (guardião trava a assinatura em
`web/tests/test_opcoes_proposta_ui.mjs:50`):
- `function PropostaLastreada({ r, operador, cp, busy, onAbrir, onFechar, posAberta })`
  — web/src/App.jsx:3023.

Ações já existentes (web/src/App.jsx:7810-7838), consumidas como estão:
- `A.abrirLastreada({ underlying, contractSymbol, expiration, contratos })`
- `A.abrirCollar({ underlying, pernasContratos, contratos, expiration })`
- `A.fecharLastreada({ contractSymbol, contratos })`

Confirmações (já em `copy.js`, nos dois ramos):
- `cp.confirmAbrirCollar(contratos, ticker, qtyAcoes)`
- `cp.confirmAbrirCoberta(contratos, ticker, qtyAcoes)`
- `cp.confirmFecharCoberta(precoFormatado, qtyAcoes, ticker)`

Contexto de tela (web/src/App.jsx:8310 e :8320):
- `ctx.operador` é a FONTE ÚNICA do modo (guardião
  `web/tests/test_fase5_appmode_fonte_unica.mjs` proíbe rederivar de
  `data.config.appMode`). `CarteiraScreen` hoje desestrutura
  `const { data, quotes, analysis, A, goMercado, cp } = ctx;` (linha 3911) —
  `operador` precisa entrar nessa mesma desestruturação.
</interfaces>

<guardioes_a_nao_quebrar>
<!-- Asserções NEGATIVAS já verdes em web/tests/. Código novo em CarteiraScreen
     que viole qualquer uma delas quebra a suíte. -->
- `test_carteira_lastro_ui.mjs`: proibido escrever `qty - ...qtyTravada`
  (a subtração vem de `qtyLivre()` de `finance.js`).
- `test_carteira_lastro_ui.mjs`: proibido declarar `const livre = ...` que não
  seja `qtyLivre(...)`.
- `test_carteira_lastro_ui.mjs`: proibido qualquer `disabled={...qtyTravada...}`
  (guardrail "stop/alvo nunca são vetados").
- `test_carteira_lastro_ui.mjs`: TODA chamada de `portfolioMetrics(` precisa
  continuar passando `optionPositions`.
- `test_carteira_lastro_ui.mjs`: a string literal
  `<AvisoLiquidacao evento={eventoLiquidacaoRecente(data.history, p.t)} cp={cp} />`
  precisa continuar existindo, byte a byte.
- `test_opcoes_proposta_ui.mjs`: `app.indexOf("<PropostaLastreada")` (PRIMEIRA
  ocorrência) precisa continuar ANTES de `app.indexOf("<OpcoesCamada")`. A
  ocorrência nova, dentro de `CarteiraScreen` (linha ~3990+), é posterior às
  duas — não inverte nada. Não mover as definições de componente de lugar.
- `test_opcoes_proposta_ui.mjs`: a PRIMEIRA ocorrência de
  `store.optionsProposta(t, true)` precisa continuar sendo a de `AtivoCard`,
  com `if (opGate && opGate.liquida)` nos 200 caracteres anteriores.
  `PropostaDaPosicao` NÃO chama o store — recebe `r` pronto por prop.
</guardioes_a_nao_quebrar>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Componente PropostaDaPosicao — detalhe + aceite escopados a uma posição</name>
  <read_first>
    - web/src/App.jsx linhas 3023-3135 (`PropostaLastreada` — o componente reusado, para entender exatamente que prop cada caminho consome)
    - web/src/App.jsx linhas 3266-3330 (`myOptionPositions`, `posAberta`, `onAbrirLastreada` com o ramo collar, `onFecharLastreada` — o padrão EXATO a replicar por posição)
    - web/src/App.jsx linhas 4055-4082 (botão colapsável "Compras desta posição" — o padrão visual/ARIA da linha colapsada a copiar)
    - web/src/App.jsx linhas 7810-7838 (`A.abrirLastreada`/`A.abrirCollar`/`A.fecharLastreada` — o que cada uma espera no corpo)
    - web/tests/test_carteira_lastro_ui.mjs (asserções negativas que o código novo não pode violar)
    - web/tests/test_opcoes_proposta_ui.mjs (assinatura congelada de PropostaLastreada; ordem PropostaLastreada × OpcoesCamada)
  </read_first>
  <files>web/src/App.jsx</files>
  <action>
Criar `function PropostaDaPosicao({ t, r, cp, operador, A, data, aberto, onToggle })`
em `web/src/App.jsx`, posicionada IMEDIATAMENTE ANTES de
`function useOpcoesPropostas` (que o Plano 18-01 colocou logo antes de
`CarteiraScreen`). Ou seja: depois de `AtivoCard`, antes do hook.

Responsabilidade única: dado o resultado cru da proposta de UMA posição,
renderizar a afordância colapsada e, quando aberta, o `<PropostaLastreada>`
com os handlers de aceite/encerramento escopados àquele ticker.

Comportamento:

1. Guarda de silêncio: `if (!r || !r.proposta) return null;`
   Diferente da tira (Plano 18-03), o card de posição NÃO mostra estado vazio.
   Razão registrada no fonte em `web/src/App.jsx:3484-3489` (ADR-004): estado
   vazio repetido por card vira "seis avisos idênticos por tela". O estado
   vazio agregado de NAV-03 vive na tira, uma vez só. Escrever essa razão no
   comentário do componente, citando a linha de origem.

2. Linha colapsada: um `<button type="button" onClick={onToggle} aria-expanded={aberto}>`
   no MESMO padrão visual/ARIA do botão "Compras desta posição" (App.jsx:4063)
   — `display:flex`, largura total, `background:"transparent"`, `border:"none"`,
   rótulo à esquerda e chevron `{aberto ? "▴" : "▾"}` à direita. Rótulo à
   esquerda: `cp.linhaPropostaNaPosicao` (nunca texto hardcodado).
   Prefixar com o mesmo glifo de opções já usado na camada
   (`⚡`, App.jsx:3154) para dar continuidade visual com a tira.

3. Detalhe: `{aberto && <PropostaLastreada r={r} operador={operador} cp={cp} busy={busy} onAbrir={onAbrirLastreada} onFechar={onFecharLastreada} posAberta={posAberta} />}`
   — prop por prop, mesma ordem da chamada existente em `AtivoCard`
   (App.jsx:3492-3495). Não alterar a assinatura de `PropostaLastreada`.

4. Estado local: `const [busy, setBusy] = useState(false);` — não reusar nem
   levantar para `CarteiraScreen`; cada posição tem o próprio "em voo", senão
   aceitar em uma trava o botão das outras.

5. `posAberta`, replicando App.jsx:3266 e :3289-3291 escopado a `t`:
   filtrar `((data && data.optionPositions) || [])` por `underlying === t`,
   e casar `id === r.proposta.contractSymbol`. Retornar `null` quando não casa.
   NÃO nomear nenhuma variável local como `livre` e NÃO escrever subtração
   envolvendo `qtyTravada` (asserções negativas de `test_carteira_lastro_ui.mjs`).

6. `onAbrirLastreada` — réplica fiel de App.jsx:3292-3321 com `t` no lugar do
   `t` do closure de `AtivoCard`:
   - ramo collar (`r.proposta.tipo === "collar"`): `window.confirm(cp.confirmAbrirCollar(contratos, t, qtyAcoes))`;
     se recusado, retorna. Senão `setBusy(true)` e
     `await A.abrirCollar({ underlying: t, pernasContratos: (proposta.pernasContratos || []).map((perna) => ({ contractSymbol: perna.contractSymbol, lado: perna.lado })), contratos, expiration })`
     dentro de `try/finally` que devolve `setBusy(false)`. Enviar SÓ
     `contractSymbol` + `lado` por perna — prêmio e strike o servidor
     re-deriva (T-17-24); reenviá-los daria a impressão de que o cliente os
     negocia.
   - ramo single-leg: só a CALL coberta pede confirmação
     (`optionType === "call"` → `window.confirm(cp.confirmAbrirCoberta(contratos, t, qtyAcoes))`),
     porque a confirmação existe pela TRAVA do lastro e a PUT não trava nada
     (T-14-24). Depois `A.abrirLastreada({ underlying: t, contractSymbol, expiration, contratos })`.

7. `onFecharLastreada` — réplica de App.jsx:3322-3329:
   `window.confirm(cp.confirmFecharCoberta(price(premioTotal), qtyAcoes, t))`,
   depois `A.fecharLastreada({ contractSymbol, contratos })` em `try/finally`.

8. NÃO reimplementar o corpo de `PropostaLastreada`, NÃO compor manchete, NÃO
   chamar `store.optionsGate`/`store.optionsProposta` aqui — `r` chega pronto
   por prop, vindo do hook do Plano 18-01. Uma chamada de store dentro deste
   componente reintroduziria a busca dupla que o hook existe para evitar.

Comentário de cabeçalho no padrão da casa: citar Fase 18 (Plano 02, NAV-02),
dizer que este é o SEGUNDO ponto de renderização de `PropostaLastreada` (o
primeiro, em `AtivoCard`, continua intocado — Watchlist/Radar é superfície de
descoberta diferente) e que a garantia "nunca estrutura sobre ticker sem
posição real" é estrutural aqui porque o chamador itera `data.positions`.
  </action>
  <verify>
    <automated>cd web && npx vite build && node tests/test_opcoes_proposta_ui.mjs && node tests/test_carteira_lastro_ui.mjs && node tests/test_opcoes_collar_ui.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx vite build` conclui sem erro.
    - `grep -c "function PropostaDaPosicao" web/src/App.jsx` retorna 1.
    - O componente está entre `AtivoCard` e `useOpcoesPropostas` no arquivo: `node -e "const s=require('fs').readFileSync('web/src/App.jsx','utf8');const a=s.indexOf('function AtivoCard(');const p=s.indexOf('function PropostaDaPosicao');const h=s.indexOf('function useOpcoesPropostas');process.exit(a<p&&p<h?0:1)"`
    - Dentro do corpo de `PropostaDaPosicao` NÃO aparece `store.optionsGate` nem `store.optionsProposta` (o `r` chega por prop).
    - O corpo contém as três ações: `A.abrirCollar(`, `A.abrirLastreada(`, `A.fecharLastreada(`.
    - O corpo contém `window.confirm(cp.confirmAbrirCollar(`, `window.confirm(cp.confirmAbrirCoberta(` e `window.confirm(cp.confirmFecharCoberta(`.
    - O corpo abre com uma guarda que retorna `null` quando `!r || !r.proposta` (nenhum caixa vazia por posição).
    - O rótulo da linha colapsada vem de `cp.linhaPropostaNaPosicao` — `grep -c "cp.linhaPropostaNaPosicao" web/src/App.jsx` retorna ≥ 1 e nenhuma string literal equivalente aparece hardcodada no JSX.
    - `node web/tests/test_carteira_lastro_ui.mjs` sai com código 0 (nenhuma asserção negativa violada).
    - `node web/tests/test_opcoes_proposta_ui.mjs` sai com código 0 (assinatura de `PropostaLastreada` intacta, ordem `<PropostaLastreada>` antes de `<OpcoesCamada>` preservada).
  </acceptance_criteria>
  <done>`PropostaDaPosicao` existe, encapsula posAberta/aceite/encerramento por ticker, reusa `PropostaLastreada` sem alterar assinatura, e a suíte de guardiões de opções continua verde.</done>
</task>

<task type="auto">
  <name>Task 2: CarteiraScreen consome o hook e renderiza o detalhe por posição</name>
  <read_first>
    - web/src/App.jsx linhas 3905-3915 (topo de `CarteiraScreen`: estados `histFor`/`editFor`/`comprasOpen` e a desestruturação de `ctx`)
    - web/src/App.jsx linhas 3982-4006 (abertura do laço `data.positions.map` e o `<div key={p.t}>` do card — onde entra o `id` de âncora)
    - web/src/App.jsx linhas 4083-4098 (bloco das "duas ações-bloco" e a linha de links — a região onde o detalhe de opções entra)
    - web/src/App.jsx linhas 4099-4142 (`histFor === p.t` e `editFor === p.t` — o padrão de leitura de estado por ticker a copiar)
    - web/src/App.jsx linha 8310 (`operador: appMode === "operador"` — fonte única do modo)
    - web/tests/test_fase5_appmode_fonte_unica.mjs (por que `operador` tem que vir de `ctx`, nunca de `data.config.appMode`)
  </read_first>
  <files>web/src/App.jsx</files>
  <action>
Estender `CarteiraScreen` em quatro pontos cirúrgicos. Não reescrever a
função; editar no lugar.

1. Desestruturação de `ctx` (linha 3911): adicionar `operador` à lista
   existente, ficando `const { data, quotes, analysis, A, goMercado, cp, operador } = ctx;`.
   Não rederivar o modo de `data.config.appMode` em lugar nenhum — `ctx.operador`
   é fonte única e existe guardião travando a classe do erro.

2. Estado novo, ao lado de `histFor`/`editFor` (linhas 3907-3910):
   `const [opcoesFor, setOpcoesFor] = useState(null);` — mesma forma
   (um ticker por vez; abrir o de outra posição fecha o anterior). Comentar
   citando Fase 18 (Plano 02, NAV-02) e que o Plano 18-03 vai escrever nesse
   mesmo estado a partir da tira.

3. Chamada do hook, depois da desestruturação e antes do `return`:
   `const { propostas: opcoesPorTicker, carregando: opcoesCarregando } = useOpcoesPropostas(data.positions.map((p) => p.t));`
   Renomear na desestruturação exatamente com esses nomes — o Plano 18-03
   consome `opcoesPorTicker`/`opcoesCarregando` por nome. `opcoesCarregando`
   fica sem uso neste plano (a tira do 18-03 é quem o consome); manter a
   variável mesmo assim, com comentário dizendo para quem ela existe, para o
   18-03 não precisar mexer na assinatura da chamada.

4. Dentro do laço `data.positions.map((p) => {...})`:
   a. No `<div key={p.t} style={{ ...card, padding: "14px 15px" }}>` (linha
      3992), adicionar `id={"posicao-" + p.t}`. Isso é a ÂNCORA de scroll que
      o Plano 18-03 usa — mesmo mecanismo já em produção para o deep link do
      push (`document.getElementById("ativo-" + t)` + `scrollIntoView`,
      App.jsx:7446-7449). Nada mais muda nesse `<div>`.
   b. Renderizar o detalhe LOGO ABAIXO da linha de links de rodapé do card
      (o bloco que termina na linha 4098, com "Editar stop/alvo / Reanalisar /
      Histórico de análises") e ANTES do bloco `{histFor === p.t && (`:
      `<PropostaDaPosicao t={p.t} r={(opcoesPorTicker[p.t] || {}).proposta} cp={cp} operador={operador} A={A} data={data} aberto={opcoesFor === p.t} onToggle={() => setOpcoesFor(opcoesFor === p.t ? null : p.t)} />`
      Posição escolhida deliberadamente: opções são uma camada sobre a
      posição, não o primeiro assunto do card — a régua de risco, o plano e as
      duas ações-bloco continuam sendo o que o usuário vê primeiro.
      `PropostaDaPosicao` já devolve `null` quando não há proposta, então
      posição sem estrutura ativa não ganha nada visualmente.

Não alterar `AvisoLiquidacao`, `PlanRuler`, o bloco de compras, `histFor`,
`editFor` nem qualquer chamada de `portfolioMetrics`.
  </action>
  <verify>
    <automated>cd web && npx vite build && node tests/test_carteira_lastro_ui.mjs && node tests/test_opcoes_proposta_ui.mjs && node tests/test_fase5_appmode_fonte_unica.mjs && node tests/test_copy_theme.mjs && node tests/test_concentracao_carteira.mjs && node tests/test_fase2_portfolio.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx vite build` conclui sem erro.
    - `grep -c "<PropostaDaPosicao" web/src/App.jsx` retorna 1 e a ocorrência está dentro do laço `data.positions.map` de `CarteiraScreen` (índice no fonte entre `function CarteiraScreen(` e `function HistoricoScreen(`).
    - `grep -c 'id={"posicao-" + p.t}' web/src/App.jsx` retorna 1.
    - `CarteiraScreen` desestrutura `operador` de `ctx` — o fonte contém `const { data, quotes, analysis, A, goMercado, cp, operador } = ctx;` e NÃO contém nenhuma leitura nova de `.appMode`.
    - `node web/tests/test_fase5_appmode_fonte_unica.mjs` sai com código 0 (continua existindo exatamente 1 leitura de `.appMode` fora de comentário).
    - `grep -c "useOpcoesPropostas(" web/src/App.jsx` retorna 2 (a definição do 18-01 e esta única chamada).
    - A chamada do hook usa `data.positions.map((p) => p.t)` como argumento — a lista de tickers vem SÓ de posições reais (nunca watchlist, nunca radar).
    - `node web/tests/test_carteira_lastro_ui.mjs` sai com código 0; em particular a asserção da string literal de `<AvisoLiquidacao ... />` e a de `portfolioMetrics` com `optionPositions`.
    - `node web/tests/test_opcoes_proposta_ui.mjs` sai com código 0 — a primeira ocorrência de `<PropostaLastreada` continua sendo a de `AtivoCard`, antes de `<OpcoesCamada`.
    - `git diff web/src/App.jsx` não mostra alteração dentro do corpo de `AtivoCard`.
  </acceptance_criteria>
  <done>Toda posição com proposta ativa mostra a linha colapsada em Posições; abrir a linha revela `PropostaLastreada` completo com aceite/encerramento funcionando; build limpo e guardiões verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → motor de ordens (`A.abrirLastreada`/`abrirCollar`/`fecharLastreada`) | ponto onde uma ação do usuário vira operação simulada com trava de lastro |
| motor determinístico → UI | manchete/didática/payoff atravessam para a tela; a UI só arruma layout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Elevation of Privilege | aceite de estrutura em Modo Estudo | mitigate | O CTA só existe sob `operador` dentro de `PropostaLastreada` (defesa em UI já existente, T-14-23) e `operador` vem de `ctx.operador`, fonte única; o servidor recusa com 403 mesmo se a UI tivesse bug |
| T-18-05 | Tampering | corpo enviado ao aceitar collar | mitigate | Só `contractSymbol` + `lado` por perna são enviados; prêmio e strike são re-derivados no servidor (T-17-24) — o cliente não negocia preço |
| T-18-06 | Repudiation | trava de lastro sem consciência do usuário | mitigate | `window.confirm(cp.confirmAbrirCoberta/confirmAbrirCollar)` obrigatório antes de qualquer perna que trave ações (T-14-24 / T-17-26), replicado byte a byte do caminho já auditado |
| T-18-07 | Spoofing | manchete exibida em Posições | mitigate | `PropostaLastreada` é reusado sem alteração de assinatura; `p.manchete` renderizado direto; nenhuma composição client-side (guardrail CVM, guardião negativo já verde) |
| T-18-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote novo; `git diff --stat web/package.json package-lock.json` precisa sair vazio |
</threat_model>

<verification>
1. `cd web && npx vite build` — sintaxe JSX válida (guardrail CLAUDE.md).
2. `node web/tests/test_opcoes_proposta_ui.mjs` — assinatura e ordem preservadas.
3. `node web/tests/test_carteira_lastro_ui.mjs` — asserções negativas da carteira intactas.
4. `node web/tests/test_opcoes_collar_ui.mjs` — caminho de collar intacto.
5. `node web/tests/test_fase5_appmode_fonte_unica.mjs` — fonte única do modo.
6. `git diff --stat web/package.json package-lock.json` vazio.
</verification>

<success_criteria>
- Posição com proposta ativa exibe a linha `cp.linhaPropostaNaPosicao` no card
  em Posições; tocar abre `PropostaLastreada` completo (chips, payoff, fonte).
- Em Modo Operador, aceitar e encerrar funcionam a partir de Posições, com as
  mesmas confirmações do caminho de `AtivoCard`.
- Posição sem proposta ativa não ganha nenhum elemento novo.
- `AtivoCard` intocado; build limpo; nenhum guardião enfraquecido.
</success_criteria>

<output>
Create `.planning/phases/18-aba-opcoes/18-02-SUMMARY.md` when done
</output>
