---
phase: 16-biblioteca-de-estruturas
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/skill_ref.py
  - server/tests/test_opcoes_collar_vocab.py
autonomous: true
requirements: [LIB-03]

must_haves:
  truths:
    - "A frase da proposta de collar existe nos dois modos (operador fala como mesa, educacional descreve condição) e nasce só em `skill_ref.OPCOES_LASTREADAS`"
    - "Nenhum outro arquivo do repositório compõe manchete de collar — o front recebe a frase pronta (guardrail CVM)"
    - "A chave `collar` não cai no fallback `sem_setup` de `opcoes_lastreadas_txt`"
  artifacts:
    - path: "server/app/skill_ref.py"
      provides: "vocabulário canônico da trava protetora nos dois modos"
      contains: "\"collar\""
    - path: "server/tests/test_opcoes_collar_vocab.py"
      provides: "guardião do vocabulário + guardrail CVM do collar"
  key_links:
    - from: "server/tests/test_opcoes_collar_vocab.py"
      to: "server/app/skill_ref.py"
      via: "opcoes_lastreadas_txt('operador', 'collar', ...)"
      pattern: "opcoes_lastreadas_txt"
---

<objective>
Criar o vocabulário canônico da trava protetora (collar) em
`skill_ref.OPCOES_LASTREADAS`, nos dois modos, e o guardião que impede
qualquer outro arquivo de compor essa manchete.

Purpose: LIB-03 precisa de uma frase determinística própria. O guardrail CVM
do repositório é que a manchete do card nasce SÓ do motor determinístico via
`skill_ref` — o front nunca compõe. Sem a entrada de vocabulário, o Plano
16-03 cairia no fallback `sem_setup` e o collar apareceria com o texto errado.

Output: chaves `collar` em `OPCOES_LASTREADAS["operador"]` e
`["educacional"]`, com guardião dedicado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-biblioteca-de-estruturas/16-CONTEXT.md
@.claude/skills/didatica-boris/SKILL.md

<interfaces>
<!-- Estado atual do vocabulário (server/app/skill_ref.py:502-556). -->

OPCOES_LASTREADAS = {"operador": {...}, "educacional": {...}}
  chaves hoje: call_coberta, put_protecao, sem_lastro, sem_setup, degradado,
  caixa_insuficiente, liquidacao_forcada.
  operador/call_coberta: "Vender {n} call(s) de {ticker} strike {strike} por R$ {premioTotal}."
  educacional/call_coberta: "Se você tivesse vendido esta call coberta agora, receberia um prêmio de R$ {premioTotal} e travaria {qtyAcoes} ação(ões) até a recompra ou o vencimento."
  educacional/put_protecao: "Se você tivesse comprado esta put de proteção agora, pagaria R$ {premioTotal} para proteger {qtyAcoes} ação(ões) contra queda abaixo de R$ {strike}."

_OPCOES_LASTREADAS_ALIASES_SEM_SETUP = ("tendencia_de_alta",
  "sem_contrato_liquido", "sem_vencimento_elegivel")

opcoes_lastreadas_txt(modo, chave, **dados) -> str
  Modo desconhecido cai em "educacional". Chave desconhecida cai em
  `d["sem_setup"]`. Interpolação por `str.replace` de marcadores "{...}";
  valores numéricos chegam JÁ formatados por `num_br()` pelo chamador.

Guardiões que já cobrem este dict (server/tests/test_opcoes_lastreadas_proposta.py:18-56):
  test_vocab_chaves_de_operacao_existem_nos_dois_modos
  test_vocab_operador_fala_como_mesa_verbo_de_ordem
  test_vocab_educacional_descreve_condicao_nunca_ordem
  test_vocab_txt_interpolacao_completa_nao_deixa_marcador_solto
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Entrada `collar` no vocabulário dos dois modos</name>
  <files>server/app/skill_ref.py, server/tests/test_opcoes_collar_vocab.py</files>
  <read_first>
    - server/app/skill_ref.py linhas 480-556 (num_br, OPCOES_LASTREADAS, aliases, opcoes_lastreadas_txt)
    - server/tests/test_opcoes_lastreadas_proposta.py linhas 1-60 (estilo dos testes de vocabulário existentes)
    - .claude/skills/didatica-boris/SKILL.md seção "Vocabulário por modo"
  </read_first>
  <behavior>
    - `opcoes_lastreadas_txt("operador", "collar", n="3", ticker="PETR4", strikeCall="32,00", strikePut="28,00", qtyAcoes="300")` devolve a frase de mesa, começando com "Vender", contendo "PETR4", "32,00", "28,00" e nenhum marcador `{` sobrando.
    - `opcoes_lastreadas_txt("educacional", "collar", ...)` com os mesmos dados devolve frase condicional começando com "Se você tivesse", sem nenhum verbo de ordem no início e sem marcador `{` sobrando.
    - As duas frases são DIFERENTES entre si.
    - `OPCOES_LASTREADAS["operador"]["collar"] != OPCOES_LASTREADAS["operador"]["sem_setup"]` — o collar não é servido pelo fallback.
    - `opcoes_lastreadas_txt("modo_inexistente", "collar", ...)` devolve a frase educacional (mesma degradação já definida).
    - Nenhuma das duas frases contém promessa de lucro, garantia ou linguagem de enriquecimento: as palavras "garantido", "lucro certo", "sem risco" não aparecem.
  </behavior>
  <action>
    Em `server/app/skill_ref.py`, adicionar a chave `"collar"` nos DOIS
    sub-dicionários de `OPCOES_LASTREADAS` (linhas 515-534), mantendo a ordem
    de leitura (logo depois de `put_protecao`, antes de `sem_lastro`).

    Texto do modo operador (registro de mesa, verbo de ordem, sem valor
    monetário na frase):
    "Vender {n} call(s) de {ticker} strike {strikeCall} e comprar {n} put(s)
    strike {strikePut} sobre {qtyAcoes} ação(ões) — trava protetora: o prêmio
    da call abate o custo da put."

    Texto do modo educacional (condicional, descreve condição, nunca ordem):
    "Se você tivesse montado esta trava protetora agora, {qtyAcoes} ação(ões)
    ficariam protegidas contra queda abaixo de R$ {strikePut} e o ganho
    ficaria limitado a partir de R$ {strikeCall} — o prêmio da call vendida
    abate o custo da put comprada."

    Decisão registrada, não improvisar em cima: as duas frases NÃO carregam
    valor em reais. Diferente de `call_coberta`/`put_protecao`, o collar tem
    resultado líquido com sinal (débito quando a put custa mais que a call,
    crédito quando é o contrário), e `opcoes_lastreadas_txt` interpola por
    `str.replace` sem condicional — uma frase única com valor sinalizado
    imprimiria "por R$ -12,00" em metade dos casos. Custo líquido, breakeven e
    ganho/perda máximos viajam nos campos `caixa`/`estrutura`/`chips` que o
    Plano 16-03 preenche, e é a Fase 17 que os exibe. "Abate o custo" é
    verdade nos dois sentidos; "financiada pelo prêmio da call" (formulação
    inicial do 16-CONTEXT) seria falsa quando o abatimento é parcial —
    afirmar financiamento completo num produto educacional é exatamente o
    tipo de promessa que o CLAUDE.md proíbe.

    Adicionar comentário curto acima da entrada registrando essa decisão e
    citando o Plano 16-02.

    Criar `server/tests/test_opcoes_collar_vocab.py` com um teste por item do
    bloco `<behavior>`, no estilo dos testes de vocabulário existentes
    (importar `from app import skill_ref`, sem fixture de banco, sem rede).
  </action>
  <verify>
    <automated>(cd server && .venv/bin/python -m pytest tests/test_opcoes_collar_vocab.py tests/test_opcoes_lastreadas_proposta.py -q)</automated>
  </verify>
  <acceptance_criteria>
    - Test command acima: 0 failed.
    - `python -c "from app import skill_ref as s; print(sorted(s.OPCOES_LASTREADAS['operador']) == sorted(s.OPCOES_LASTREADAS['educacional']))"` (de `server/`) imprime `True` — paridade de chaves entre modos mantida.
    - `python -c "from app import skill_ref as s; print('collar' in s.OPCOES_LASTREADAS['operador'], 'collar' in s.OPCOES_LASTREADAS['educacional'])"` imprime `True True`.
    - O teste existente `test_vocab_chaves_de_operacao_existem_nos_dois_modos` continua verde sem alteração.
    - Nenhuma das duas frases é igual à frase de `sem_setup` do mesmo modo (asserção no teste, não inspeção visual).
  </acceptance_criteria>
  <done>Vocabulário do collar existe nos dois modos, com registro correto por modo e sem valor sinalizado interpolado.</done>
</task>

<task type="auto">
  <name>Task 2: Guardião CVM — a manchete do collar só nasce no skill_ref</name>
  <files>server/tests/test_opcoes_collar_vocab.py</files>
  <read_first>
    - server/tests/test_opcoes_gatilho.py linhas 100-200 (padrão de guardião estrutural via `ast`, não busca textual)
    - server/app/skill_ref.py linhas 502-514 (comentário que declara o guardrail)
    - web/src/App.jsx linhas 3006-3068 (componente `PropostaLastreada` — prova de que hoje o front só renderiza `p.manchete`/`p.didatica`, nunca compõe)
  </read_first>
  <action>
    Acrescentar a `server/tests/test_opcoes_collar_vocab.py` um guardião
    estrutural que reprove a reintrodução de manchete de collar fora do
    `skill_ref`:

    (a) Varrer `server/app/*.py` EXCETO `skill_ref.py`: nenhum arquivo pode
    conter as strings-âncora do texto canônico — "trava protetora" e "abate o
    custo". Filtrar comentários e docstrings via `ast` (percorrer a árvore e
    ignorar nós `ast.Expr`/`ast.Constant` que sejam docstring de módulo,
    classe ou função, e usar `io.open`+`ast.parse` em vez de grep textual,
    pelo mesmo motivo do guardião do `test_opcoes_gatilho.py`: uma busca
    textual reprovaria o próprio comentário que documenta a proibição).

    (b) Varrer `web/src/*.js` e `web/src/*.jsx` por leitura de texto simples
    (não há parser JS disponível): nenhuma das duas strings-âncora pode
    aparecer. Se aparecer, a mensagem de falha deve nomear o arquivo e citar
    o guardrail: a manchete do card de decisão vem só do motor determinístico.

    (c) Um teste que afirma que `skill_ref` é a ÚNICA origem: as duas
    strings-âncora aparecem em `server/app/skill_ref.py` (senão o guardião
    estaria protegendo texto que não existe mais e passaria vazio — falso
    verde clássico).

    Marcar no docstring do módulo de teste que este guardião não se apaga:
    reversão deliberada atualiza o guardião com nota, guardrail do repositório.
  </action>
  <verify>
    <automated>(cd server && .venv/bin/python -m pytest tests/test_opcoes_collar_vocab.py -q)</automated>
  </verify>
  <acceptance_criteria>
    - Test command acima: 0 failed.
    - Prova empírica de que o guardião reprova de verdade: inserir temporariamente a string "trava protetora" numa string de código (não comentário) de `server/app/opcoes_lastreadas.py`, rodar o arquivo de teste e confirmar que ele FALHA nomeando o arquivo; reverter com `git checkout -- server/app/opcoes_lastreadas.py` e confirmar `git status --porcelain server/app/opcoes_lastreadas.py` vazio e a suíte verde de novo. Registrar o resultado das duas rodadas na SUMMARY.
    - O guardião não reprova o próprio `skill_ref.py` nem comentários explicativos.
  </acceptance_criteria>
  <done>Existe um guardião que falha se qualquer arquivo fora do `skill_ref` passar a compor a manchete do collar, e a prova de que ele reprova foi executada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| motor determinístico → usuário | A manchete é o texto de decisão financeira exibido ao usuário; sua origem é o guardrail regulatório (CVM) do produto |
| LLM → card de decisão | A IA explica, nunca substitui a manchete — fronteira já vigente, esta fase acrescenta uma estrutura a ela |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Spoofing | manchete de proposta | mitigate | Guardião estrutural (Task 2) reprova composição de manchete de collar em qualquer arquivo fora de `skill_ref.py`, backend e front |
| T-16-06 | Information disclosure | texto exibido ao usuário | mitigate | Teste afirma ausência de "garantido"/"lucro certo"/"sem risco" nas frases; frase educacional descreve condição, nunca ordem |
| T-16-07 | Tampering | fallback silencioso de `opcoes_lastreadas_txt` | mitigate | Teste afirma que `collar` NÃO cai em `sem_setup` — sem ele, uma chave errada exibiria "a leitura técnica não indica..." para uma proposta real |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma instalação de pacote nesta fase; `requirements.txt`/`requirements-prod.txt` intocados |
</threat_model>

<verification>
- `(cd server && .venv/bin/python -m pytest tests/test_opcoes_collar_vocab.py tests/test_opcoes_lastreadas_proposta.py tests/test_opcoes_lastreadas_rotas.py -q)` — 0 failed.
- `git status --porcelain web/` vazio.
</verification>

<success_criteria>
- `OPCOES_LASTREADAS` tem `collar` nos dois modos, com registro distinto por modo.
- Guardião CVM do collar existe e foi provado por injeção de falha revertida.
- Nenhum teste de vocabulário pré-existente foi alterado.
</success_criteria>

<output>
Create `.planning/phases/16-biblioteca-de-estruturas/16-02-SUMMARY.md` when done.
Registrar o texto final exato das duas frases e o resultado das duas rodadas da
prova de injeção do guardião.
</output>
