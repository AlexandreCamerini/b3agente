---
phase: 16-biblioteca-de-estruturas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/opcoes_lastreadas.py
  - server/tests/test_opcoes_lastreadas_proposta.py
  - server/tests/test_opcoes_motor.py
autonomous: true
requirements: [LIB-01, LIB-02]

must_haves:
  truths:
    - "Venda coberta e put de proteção escolhem o contrato por `opcoes_motor.rastrear()` — não existe mais uma segunda implementação da régua de liquidez dentro de `opcoes_lastreadas.py`"
    - "A proposta de venda coberta declara ganho máximo, perda máxima e breakeven da estrutura completa (ação + opção), nunca da perna de opção isolada"
    - "A proposta de put de proteção declara perda máxima limitada e ganho ilimitado (perfil correto de put protetora sobre posição comprada)"
    - "A proposta separa o perfil de risco (`estrutura`) do movimento de caixa de hoje (`caixa`) — o custo da ação já detida não é apresentado como desembolso da operação proposta"
    - "Cadeia sem preço do objeto utilizável devolve `degradado`, nunca um contrato de strike arbitrário"
  artifacts:
    - path: "server/app/opcoes_lastreadas.py"
      provides: "propor() apoiado no motor comum de N pernas"
      contains: "opcoes_motor.rastrear"
    - path: "server/tests/test_opcoes_lastreadas_proposta.py"
      provides: "guardião da régua única + dos campos de payoff"
      contains: "estrutura"
  key_links:
    - from: "server/app/opcoes_lastreadas.py"
      to: "opcoes_motor.rastrear"
      via: "seleção de contrato"
      pattern: "opcoes_motor\\.rastrear\\("
    - from: "server/app/opcoes_lastreadas.py"
      to: "opcoes_motor.avaliar"
      via: "payoff da estrutura"
      pattern: "opcoes_motor\\.avaliar\\("
    - from: "server/app/opcoes_lastreadas.py"
      to: "opcoes_motor.perna_de_acao"
      via: "perna de lastro na estrutura"
      pattern: "opcoes_motor\\.perna_de_acao\\("
---

<objective>
Migrar a seleção de contrato e o cálculo de resultado de `propor()` (venda
coberta e put de proteção, em produção desde a Fase 14) para o motor comum de
N pernas da Fase 15 — `opcoes_motor.rastrear()` para escolher, `perna_de_acao`
+ `perna_de_contrato` + `avaliar()` para calcular.

Purpose: LIB-01/LIB-02 exigem que as duas estruturas já existentes passem a
nascer do motor comum, não de lógica local duplicada. Sem isso, o collar da
Fase 16 (LIB-03) seria uma terceira implementação paralela, e os campos de
payoff que a Fase 17 (FLOW-01) precisa exibir não existiriam.

Output: `propor()` sem `_escolher_contrato`/`_candidato_valido`, com os campos
`estrutura`, `caixa` e `precoObjeto` aditivos no dict `proposta`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-biblioteca-de-estruturas/16-CONTEXT.md
@docs/adr/024-limite-interno-rastrear-avaliar.md

<interfaces>
<!-- Contratos do motor da Fase 15 que este plano consome. Não explorar o
     código atrás deles: estão fixados por guardião (test_opcoes_fronteira.py)
     e a assinatura está congelada por `inspect.signature`. -->

server/app/opcoes_motor.py:
  LIQUIDEZ_MINIMA = 40
  rastrear(cadeia, filtros) -> list[dict]
    filtros: tipo ("call"|"put"), referencia (float|None), relacao
    ("acima" -> strike > referencia; "abaixo_ou_igual" -> strike <=
    referencia), criterio ("min"|"max", default "min"), liquidez_minima
    (default LIQUIDEZ_MINIMA), n (default 1).
    Devolve [] em toda porta fechada. Contratos ADR-004 ORIGINAIS, intactos.
    IMPORTANTE: `referencia` não-numérica é IGNORADA — nenhum filtro por
    strike é aplicado, e a seleção sai errada em silêncio.
  perna_de_contrato(contrato, lado, quantidade=1) -> dict
    lado ∈ {"compra","venda"}. Levanta ValueError se lastPrice for
    None/0/negativo, ou optionType não for call/put.
  perna_de_acao(ticker, preco, quantidade) -> dict
    Levanta ValueError se preco <= 0 ou quantidade <= 0.
  avaliar(pernas) -> dict  (delegação direta a perfil_da_estrutura)

server/app/opcoes_payoff.py — chaves devolvidas por perfil_da_estrutura():
  pernas, custo_liquido, fluxo ("debito"|"credito"|"neutro"), ganho_maximo
  (None se ilimitado), perda_maxima (None se ilimitada), ganho_ilimitado,
  perda_ilimitada, breakevens (list[float]), delta_total ({valor,
  pernas_sem_delta, motivo}), curva, unidade.
  CONVENÇÃO: tudo POR UNIDADE DO OBJETO (uma ação). Quantidade 1 em todas as
  pernas; o lote de 100 multiplica do lado de fora.

server/app/options_api.py:30-36 — _spot_from_chain_or_quote(chain, quote)
  devolve Optional[float]: **pode ser None** quando nem a cadeia nem a quote
  trazem preço utilizável.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Seleção de contrato pelo motor comum + guarda de preço do objeto</name>
  <files>server/app/opcoes_lastreadas.py, server/tests/test_opcoes_lastreadas_proposta.py, server/tests/test_opcoes_motor.py</files>
  <read_first>
    - server/app/opcoes_lastreadas.py (arquivo inteiro — 275 linhas)
    - server/app/opcoes_motor.py linhas 25-110 (LIQUIDEZ_MINIMA, _candidato_valido, rastrear)
    - server/tests/test_opcoes_lastreadas_proposta.py linhas 60-175 (fixtures `_contrato`/`_cadeia`/`_posicao`, `_SPOT=30.0`, `_HOJE=2026-08-31`, e os 9 testes de `propor`)
    - server/tests/test_opcoes_motor.py linhas 160-175 (guardião `test_corte_de_liquidez_tem_fonte_unica`)
  </read_first>
  <behavior>
    - `propor("PETR4", cadeia_ok, None, plano_vender, posicao, cash, "operador", hoje)` devolve `{"proposta": None, "motivo": "degradado"}` — nenhuma exceção, nenhum contrato escolhido.
    - Mesma coisa para `spot` igual a 0, negativo, `True` (bool) ou string.
    - Os 9 testes existentes de `propor` em test_opcoes_lastreadas_proposta.py continuam verdes sem alteração de asserção (call de menor strike acima do spot; put de maior strike até o spot; iliquidez -> `sem_contrato_liquido`; vencimento fora da janela -> `sem_vencimento_elegivel`; caixa curto -> `caixa_insuficiente`).
    - Empate de strike no lado da put: com dois puts de strike 28.0 e símbolos diferentes na ordem [PUT_A, PUT_B], `propor` devolve PUT_B (último empatado), porque `rastrear(criterio="max")` inverte a ordenação estável — o `max()` local devolvia PUT_A. Divergência deliberada, testada e comentada.
    - `opcoes_lastreadas` não expõe mais `_candidato_valido` nem `_escolher_contrato`.
  </behavior>
  <action>
    Em `server/app/opcoes_lastreadas.py`:

    (a) Inserir a guarda de preço do objeto DEPOIS da porta `sem_lastro`
    (hoje linhas 74-75) e ANTES da decisão de `tipo`: `spot` que não seja
    `int`/`float`, que seja `bool`, ou que seja <= 0 devolve
    `{"proposta": None, "motivo": "degradado"}`. Comentar o porquê no código:
    `_spot_from_chain_or_quote` (server/app/options_api.py:30-36) devolve
    `Optional[float]`; hoje um `spot=None` estoura `TypeError` na comparação
    `(c.get("strike") or 0) > spot` e o `except Exception` da rota
    (server/app/main.py:2404) converte em `degradado`. Depois desta migração
    `rastrear()` IGNORA `referencia` não-numérica (opcoes_motor.py:95-98) e
    devolveria o contrato de menor strike da cadeia inteira em silêncio —
    trocar uma exceção por uma seleção errada viola o princípio 4 do
    CLAUDE.md (nunca inventar valor quando o dado falhou).

    (b) Apagar `_candidato_valido` (linhas 45-50) e `_escolher_contrato`
    (linhas 53-59) inteiras. Apagar também o rebind `_LIQUIDEZ_MINIMA`
    (linha 23) e o comentário das linhas 20-22 que o explica — a fonte única
    do corte passa a ser o default de `rastrear()`, não um atributo de
    módulo. MANTER o import de `liquidity_score` de `.options_quant`: ele
    continua sendo usado por `_label_liquidez` e pelo campo `liquidez` das
    propostas (linhas 116 e 214).

    (c) Substituir o bloco de seleção (linhas 97-104) por duas chamadas a
    `opcoes_motor.rastrear(chain, filtros)`:
    - `call_coberta`: filtros `{"tipo": "call", "referencia": spot,
      "relacao": "acima", "criterio": "min", "n": 1}`
    - `put_protecao`: filtros `{"tipo": "put", "referencia": spot,
      "relacao": "abaixo_ou_igual", "criterio": "max", "n": 1}`
    NÃO passar `liquidez_minima` — o default de `rastrear` já é
    `LIQUIDEZ_MINIMA`, e repassá-lo aqui recriaria o acoplamento que esta
    task remove. Lista vazia devolve `{"proposta": None, "motivo":
    "sem_contrato_liquido"}`, exatamente como hoje; contrato escolhido é o
    índice 0 da lista.

    (d) Comentar acima da chamada do lado `put_protecao` a mudança de
    desempate: `max(validos, key=strike)` devolvia o PRIMEIRO strike máximo
    na ordem da cadeia; `rastrear(criterio="max")` faz
    `list(reversed(sorted_ascendente))[:1]` (opcoes_motor.py:105-108) e
    devolve o ÚLTIMO empatado. Cadeia real da B3 não repete strike no mesmo
    tipo e vencimento; a régua única vale mais que a preservação do
    desempate arbitrário anterior.

    (e) Corrigir a docstring do teste
    `test_proposta_fechar_estavel_mesmo_quando_propor_divergiria`
    (test_opcoes_lastreadas_proposta.py:232-235), que cita `_escolher_contrato`
    por nome — trocar por "nunca re-escolhe contrato pelo motor comum".

    Em `server/tests/test_opcoes_motor.py`, atualizar o guardião
    `test_corte_de_liquidez_tem_fonte_unica` (linhas 166-168): a asserção
    `opcoes_lastreadas._LIQUIDEZ_MINIMA is opcoes_motor.LIQUIDEZ_MINIMA` deixa
    de existir junto com o atributo. Guardrail do repositório: guardião não se
    apaga — reversão deliberada ATUALIZA o guardião com nota. Substituir por
    uma asserção MAIS forte, com docstring registrando a data e o motivo da
    troca (Fase 16, Plano 01): (1) `hasattr(opcoes_lastreadas,
    "_LIQUIDEZ_MINIMA")` é False e `hasattr(opcoes_lastreadas,
    "_candidato_valido")` é False — não sobrou corte local; (2) via
    `monkeypatch` de `opcoes_motor.rastrear` por um espião que registra as
    chamadas, `propor()` com plano de VENDER e cadeia válida chama
    `rastrear` ao menos uma vez e NÃO passa `liquidez_minima` nos filtros
    (o corte vem do default do motor).

    Novos testes em `server/tests/test_opcoes_lastreadas_proposta.py`, na
    Parte 2 (motor), reusando as fixtures `_cadeia`/`_posicao`/`_contrato`
    já existentes: spot None, 0, -1, True e "30" cada um devolvendo
    `degradado`; e o teste de empate de strike descrito em `<behavior>`.
  </action>
  <verify>
    <automated>(cd server && .venv/bin/python -m pytest tests/test_opcoes_lastreadas_proposta.py tests/test_opcoes_motor.py tests/test_opcoes_lastreadas_rotas.py -q)</automated>
  </verify>
  <acceptance_criteria>
    - Test command acima: 0 failed.
    - `grep -vE '^\s*#' server/app/opcoes_lastreadas.py | grep -cE 'def _(escolher_contrato|candidato_valido)'` devolve 0.
    - `grep -vE '^\s*#' server/app/opcoes_lastreadas.py | grep -c '_LIQUIDEZ_MINIMA'` devolve 0.
    - `grep -c 'opcoes_motor.rastrear(' server/app/opcoes_lastreadas.py` devolve 2 ou mais.
    - `python -c "from app import opcoes_lastreadas as o; print(hasattr(o,'_candidato_valido'), hasattr(o,'_LIQUIDEZ_MINIMA'))"` (rodado de `server/`) imprime `False False`.
    - O guardião `test_corte_de_liquidez_tem_fonte_unica` continua existindo em test_opcoes_motor.py com docstring citando a Fase 16 e o motivo da troca.
  </acceptance_criteria>
  <done>Seleção de contrato das duas estruturas passa por `opcoes_motor.rastrear()`; nenhuma régua de liquidez duplicada sobra no arquivo; spot inutilizável degrada explicitamente.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Payoff da estrutura e movimento de caixa como campos aditivos da proposta</name>
  <files>server/app/opcoes_lastreadas.py, server/tests/test_opcoes_lastreadas_proposta.py</files>
  <read_first>
    - server/app/opcoes_lastreadas.py (estado depois da Task 1, bloco `propor` linhas ~95-151)
    - server/app/opcoes_motor.py linhas 112-197 (perna_de_contrato, perna_de_acao, avaliar)
    - server/app/opcoes_payoff.py linhas 161-229 (perfil_da_estrutura — chaves devolvidas e convenção de unidade)
    - server/tests/test_opcoes_payoff.py linhas 82-160 (padrão dos testes de venda coberta e do custo líquido)
  </read_first>
  <behavior>
    - Venda coberta (spot 30.0, call strike 32.0, prêmio 1.0): `proposta["estrutura"]["ganho_ilimitado"] is False`, `["perda_ilimitada"] is False`, `["ganho_maximo"] == 3.0` (32 − 30 + 1), `["perda_maxima"] == 29.0` (30 − 1), `["breakevens"] == [29.0]`.
    - Put de proteção (spot 30.0, put strike 28.0, prêmio 0.9): `estrutura["ganho_ilimitado"] is True`, `["ganho_maximo"] is None`, `["perda_ilimitada"] is False`, `["perda_maxima"] == 2.9` (30 − 28 + 0.9).
    - Venda coberta: `proposta["caixa"] == {"custoLiquidoUnitario": -1.0, "custoLiquidoTotal": -300.0, "fluxo": "credito"}` para 3 contratos (300 ações) — a compra da ação já detida NÃO entra no caixa.
    - Put de proteção: `caixa["fluxo"] == "debito"` e `custoLiquidoUnitario` == prêmio unitário positivo.
    - `proposta["precoObjeto"] == 30.0` nas duas.
    - `estrutura["pernas"]` tem 2 entradas, a primeira com `tipo == "ACAO"` e `premio == spot`, a segunda com `tipo` CALL/PUT e `lado` "venda"/"compra" conforme o caso.
    - Campos pré-existentes intactos: `tipo`, `contractSymbol`, `optionType`, `strike`, `expiration`, `diasParaVencimento`, `contratos`, `qtyAcoes`, `premioUnitario`, `premioTotal`, `lastro`, `liquidez`, `manchete`, `didatica`, `chips` — nenhum removido, nenhum com valor diferente do de hoje.
    - `proposta_fechar()` NÃO ganha esses campos nesta task (ela descreve o fechamento de uma perna já aberta, não a montagem de uma estrutura) — teste explícito de que `"estrutura" not in proposta_fechar(...)["proposta"]`.
  </behavior>
  <action>
    Em `propor()`, depois de calcular `contratos`/`qty_acoes`/`premio_total` e
    ANTES de montar o dict `proposta`, montar as pernas e avaliar duas vezes,
    com propósitos distintos e nomeados:

    (a) `pernas_opcao = [opcoes_motor.perna_de_contrato(contrato, "venda" if
    tipo == "call_coberta" else "compra", quantidade=1)]`.

    (b) `pernas = [opcoes_motor.perna_de_acao(underlying, spot, quantidade=1),
    *pernas_opcao]` — a perna de lastro entra SEMPRE. Comentar o motivo no
    código: sem a perna ACAO, `perfil_da_estrutura` vê só a call vendida
    (inclinação −1) e classifica uma venda COBERTA como perda ilimitada
    (opcoes_payoff.py:198-204); apresentar isso ao usuário de um produto
    educacional seria descrever risco errado. `quantidade=1` nas duas pernas
    mantém a convenção "por unidade do objeto" que `opcoes_payoff` declara em
    `unidade` — o lote de 100 multiplica fora.

    (c) `estrutura = opcoes_motor.avaliar(pernas)` — perfil de RISCO da
    posição resultante. Vai inteiro em `proposta["estrutura"]`, sem renomear
    nenhuma chave (decisão registrada no 16-CONTEXT: seguir o vocabulário de
    `perfil_da_estrutura`, não inventar outro).

    (d) `caixa_pernas = opcoes_motor.avaliar(pernas_opcao)` — movimento de
    caixa de HOJE, só das pernas de opção. Comentar o motivo: `estrutura`
    inclui o preço da ação no `custo_liquido` (numa venda coberta,
    `custo_liquido` = spot − prêmio ≈ 28,5), e a ação JÁ é do usuário —
    exibir isso como custo da operação seria número certo respondendo a
    pergunta errada. `proposta["caixa"] = {"custoLiquidoUnitario":
    caixa_pernas["custo_liquido"], "custoLiquidoTotal": round(
    caixa_pernas["custo_liquido"] * qty_acoes, 2), "fluxo":
    caixa_pernas["fluxo"]}`.

    (e) `proposta["precoObjeto"] = round(float(spot), 2)` — o preço a partir
    do qual o payoff foi traçado, para a Fase 17 poder rotular a curva sem
    reconstituir de onde veio.

    Não envolver nada disso em `try`/`except`: `rastrear` só devolve contrato
    com `lastPrice` numérico > 0 (opcoes_motor.py:38-43) e a guarda de spot da
    Task 1 já garante o argumento de `perna_de_acao`, então um `ValueError`
    aqui seria defeito de programação, não estado de mercado — engolir viraria
    proposta silenciosamente incompleta.

    Testes novos em `server/tests/test_opcoes_lastreadas_proposta.py` com os
    números exatos do bloco `<behavior>` (fixtures `_SPOT = 30.0`,
    `_CALLS_PADRAO` strike 32.0 prêmio 1.0, `_PUTS_PADRAO` strike 28.0 — para
    o caso da put use um contrato de prêmio 0.9 como o teste
    `test_propor_plano_vender_propoe_put_com_maior_strike_ate_spot` já faz),
    incluindo o teste de campos pré-existentes intactos e o de
    `proposta_fechar` sem `estrutura`.
  </action>
  <verify>
    <automated>(cd server && .venv/bin/python -m pytest tests/test_opcoes_lastreadas_proposta.py tests/test_opcoes_lastreadas_rotas.py tests/test_opcoes_payoff.py -q)</automated>
  </verify>
  <acceptance_criteria>
    - Test command acima: 0 failed.
    - `grep -c 'opcoes_motor.avaliar(' server/app/opcoes_lastreadas.py` devolve 2 ou mais.
    - `grep -c 'perna_de_acao(' server/app/opcoes_lastreadas.py` devolve 1 ou mais.
    - Existe teste que afirma `ganho_ilimitado is False` para a venda coberta e `ganho_ilimitado is True` para a put de proteção (behavior assertion, não asserção de existência de chave).
    - Existe teste que afirma `caixa["fluxo"] == "credito"` na venda coberta e `"debito"` na put.
    - Suíte completa do backend sem regressão: `(cd server && .venv/bin/python -m pytest tests/ -q)` — falhas remanescentes, se houver, são exclusivamente `PermissionError ... load_verify_locations` (bloqueio de rede do sandbox, já documentado no 15-VERIFICATION.md); qualquer outra falha reprova a task.
  </acceptance_criteria>
  <done>Toda proposta de venda coberta e put de proteção carrega o perfil de risco da estrutura completa e o movimento de caixa da operação, separados e nomeados; nenhum campo pré-existente mudou.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provedor de cadeia → `propor()` | Cadeia ADR-004 vinda de rede (Yahoo/brapi/mydata): `strike`, `lastPrice`, `volume`, `openInterest` são dados NÃO confiáveis para aritmética cega |
| rota `/api/options/proposta/{ticker}` → `propor()` | `spot` derivado de dado externo, pode ser None |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | `propor()` seleção de contrato | mitigate | `rastrear()` valida `lastPrice` numérico > 0 e `liquidity_score >= 40` antes de qualquer contrato virar proposta (opcoes_motor.py:35-43); nenhum `float(x or 0)` no caminho de seleção |
| T-16-02 | Information disclosure | payoff exibido ao usuário | mitigate | Perna ACAO obrigatória: sem ela a venda coberta seria apresentada como perda ilimitada — risco descrito errado é desinformação financeira; teste de comportamento fixa `ganho_ilimitado is False` |
| T-16-03 | Tampering | `spot` None/0 vindo de dado degradado | mitigate | Guarda explícita devolve `degradado` (Task 1a); sem ela `rastrear` ignoraria a referência e escolheria strike arbitrário em silêncio |
| T-16-04 | Denial of service | `avaliar()` chamado 2× por proposta | accept | Aritmética pura O(n pernas) com n=2, sem I/O e sem rede; nenhuma chamada nova ao provedor (ENG-03 preservado) |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma instalação de pacote nesta fase; `server/requirements.txt` e `requirements-prod.txt` intocados — `git diff --stat` desses dois arquivos deve sair vazio ao fim do plano |
</threat_model>

<verification>
- `(cd server && .venv/bin/python -m pytest tests/test_opcoes_lastreadas_proposta.py tests/test_opcoes_lastreadas_rotas.py tests/test_opcoes_motor.py tests/test_opcoes_payoff.py tests/test_opcoes_fronteira.py tests/test_opcoes_gatilho.py -q)` — 0 failed.
- `git diff --stat server/requirements.txt server/requirements-prod.txt` vazio.
- `git status --porcelain web/` vazio (fase backend-only; nenhum arquivo de `web/` tocado).
</verification>

<success_criteria>
- Nenhuma implementação local de régua de liquidez ou de escolha de contrato sobra em `opcoes_lastreadas.py`.
- Proposta de venda coberta e de put de proteção trazem `estrutura`, `caixa` e `precoObjeto`, com os valores exatos do bloco `<behavior>` da Task 2.
- Todos os campos que a Fase 17/18 e o `14-UI-SPEC.md` já pressupõem continuam presentes e com o mesmo valor.
- Guardião `test_corte_de_liquidez_tem_fonte_unica` atualizado (não apagado), com nota da mudança.
</success_criteria>

<output>
Create `.planning/phases/16-biblioteca-de-estruturas/16-01-SUMMARY.md` when done.
Registrar na SUMMARY, explicitamente: (1) a decisão de incluir a perna ACAO no
payoff a preço de SPOT e não a preço médio da posição, com o racional (o
payoff descreve a estrutura montada agora, não o resultado acumulado da
posição — usar o preço médio inflaria o ganho máximo com lucro pré-existente);
(2) a mudança de desempate de strike introduzida por `rastrear(criterio="max")`.
</output>
