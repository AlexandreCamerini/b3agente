---
phase: 16-biblioteca-de-estruturas
plan: 03
type: execute
wave: 2
depends_on: [16-01, 16-02]
files_modified:
  - server/app/opcoes_lastreadas.py
  - server/tests/test_opcoes_collar.py
  - server/tests/test_opcoes_gatilho.py
autonomous: true
requirements: [LIB-03]

must_haves:
  truths:
    - "O motor propõe collar — call vendida + put comprada sobre a mesma posição, mesmo vencimento — quando a leitura técnica pede proteção e a put isolada não cabe no caixa"
    - "O payoff do collar é consolidado numa única avaliação de 3 pernas e sai travado dos DOIS lados (ganho e perda máximos, ambos finitos)"
    - "As duas pernas do collar são selecionadas pela mesma régua das estruturas de uma perna — `opcoes_motor.rastrear()`, liquidez ≥ 40, strike extremo"
    - "O collar declara as duas pernas nomeadas e nenhum `contractSymbol` único — nenhuma estrutura de duas pernas se disfarça de operação de uma perna"
    - "Sem o pedido explícito de estrutura multiperna, o comportamento do motor é byte a byte o de hoje"
  artifacts:
    - path: "server/app/opcoes_lastreadas.py"
      provides: "composição do collar sobre o motor de N pernas"
      contains: "collar"
    - path: "server/tests/test_opcoes_collar.py"
      provides: "guardião do gatilho, do payoff e da forma da proposta de collar"
  key_links:
    - from: "server/app/opcoes_lastreadas.py"
      to: "opcoes_motor.avaliar"
      via: "avaliação das 3 pernas do collar"
      pattern: "opcoes_motor\\.avaliar\\("
    - from: "server/app/opcoes_lastreadas.py"
      to: "skill_ref.opcoes_lastreadas_txt"
      via: "manchete e didática do collar"
      pattern: "opcoes_lastreadas_txt\\("
---

<objective>
Compor o collar (trava protetora) em `propor()`: as duas pernas que a venda
coberta e a put de proteção já selecionam, avaliadas juntas numa única
chamada a `opcoes_motor.avaliar()`, sobre a mesma posição.

Purpose: LIB-03. É também a prova de que o motor da Fase 15 compõe N pernas de
verdade — o mesmo `perfil_da_estrutura` que hoje serve estruturas de 2 pernas
passa a servir 3 (ação + call vendida + put comprada) sem uma linha de
aritmética nova.

Output: `propor()` capaz de devolver `{"proposta": {...tipo: "collar"...},
"motivo": "collar"}`, atrás do parâmetro explícito `multiperna` que o Plano
16-04 liga na rota.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-biblioteca-de-estruturas/16-CONTEXT.md
@.planning/phases/16-biblioteca-de-estruturas/16-01-SUMMARY.md
@.planning/phases/16-biblioteca-de-estruturas/16-02-SUMMARY.md
@docs/adr/024-limite-interno-rastrear-avaliar.md

<interfaces>
<!-- Já existente e consumido por este plano. -->

server/app/opcoes_motor.py
  rastrear(cadeia, filtros) -> list[dict]   # filtros: tipo, referencia,
    relacao ("acima"|"abaixo_ou_igual"), criterio ("min"|"max"), n
  perna_de_contrato(contrato, lado, quantidade=1) -> dict
  perna_de_acao(ticker, preco, quantidade) -> dict
  avaliar(pernas) -> dict

server/app/opcoes_payoff.perfil_da_estrutura() devolve:
  custo_liquido, fluxo, ganho_maximo, perda_maxima, ganho_ilimitado,
  perda_ilimitada, breakevens, delta_total, curva, unidade, pernas.
  Tudo POR UNIDADE DO OBJETO (uma ação); quantidade 1 em todas as pernas.

server/app/skill_ref.py (Plano 16-02)
  OPCOES_LASTREADAS["operador"]["collar"] e ["educacional"]["collar"]
  marcadores interpolados: {n} {ticker} {strikeCall} {strikePut} {qtyAcoes}
  num_br(valor) -> str  # formatação PT-BR, aplicada pelo CHAMADOR

server/app/opcoes_gatilho.py
  VIES_PROTECAO = "protecao"; VIES_PREMIO = "premio"
  do_plano(plano) -> {"avaliar": bool, "vies": str|None, "motivo": str}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gatilho de oferta e seleção das duas pernas do collar</name>
  <files>server/app/opcoes_lastreadas.py, server/tests/test_opcoes_collar.py</files>
  <read_first>
    - server/app/opcoes_lastreadas.py (estado após o Plano 16-01, `propor` inteira)
    - server/app/opcoes_motor.py linhas 46-110 (rastrear e seus filtros)
    - server/tests/test_opcoes_lastreadas_proposta.py linhas 60-100 (fixtures `_contrato`/`_cadeia`/`_posicao`, `_SPOT = 30.0`, `_HOJE`)
    - .planning/phases/16-biblioteca-de-estruturas/16-CONTEXT.md seção "LIB-03 — collar"
  </read_first>
  <behavior>
    - `propor(...)` sem `multiperna` (default) com plano de VENDER, `cash=50` e put de prêmio 0,90 devolve `{"proposta": None, "motivo": "caixa_insuficiente"}` — exatamente o de hoje.
    - Mesma chamada com `multiperna=True` devolve `motivo == "collar"` e `proposta["tipo"] == "collar"`.
    - Com `multiperna=True` e caixa folgado (`cash=100000`), o plano de VENDER continua devolvendo `put_protecao` — o collar não rouba o caso em que a put isolada cabe.
    - Com `multiperna=True` e plano de NÃO OPERAR (viés de prêmio), a resposta continua `call_coberta` — o collar não é oferecido no viés de prêmio.
    - Com `multiperna=True`, caixa curto e NENHUMA call líquida acima do spot na cadeia, a resposta volta a ser `caixa_insuficiente` — nunca uma trava de uma perna só.
    - Crédito líquido (put 0,90 / call 1,00 → −0,10 por ação): `contratos == 3` com `qty_livre == 300` e `cash == 50` — sem call para financiar não caberia nenhum.
    - Débito líquido (put 1,50 / call 0,50 → +1,00 por ação) com `cash == 250` e `qty_livre == 300`: `contratos == 2` (teto do caixa), não 3.
    - Débito líquido com `cash == 50`: `motivo == "caixa_insuficiente"` mesmo com `multiperna=True`.
    - As duas pernas compartilham o `expiration` da cadeia (a cadeia ADR-004 carrega um vencimento só) e a janela de prazo 15..60 dias continua valendo.
  </behavior>
  <action>
    Em `server/app/opcoes_lastreadas.py`:

    (a) Acrescentar a `propor()` o parâmetro SOMENTE-NOMEADO `multiperna:
    bool = False`, no fim da assinatura. Documentar na docstring: é negociação
    de capacidade do CLIENTE, não uma meia-entrega — o cliente que sabe
    exibir e aceitar uma estrutura de mais de uma perna pede `multiperna=True`
    e recebe o collar; o front publicado hoje (`PropostaLastreada` em
    web/src/App.jsx:3012-3068) casa proposta com posição por `contractSymbol`
    e executa por uma perna só, então receber um collar sem pedir produziria
    um CTA que abriria METADE da estrutura. O Plano 16-04 liga o parâmetro na
    rota; a Fase 17 liga no cliente.

    (b) Criar a função privada `_propor_collar(underlying, chain, spot,
    contrato_put, posicao, cash, modo, hoje, dias, qty_livre_val)`, chamada de
    dentro de `propor()` EXATAMENTE no ponto em que hoje se devolve
    `caixa_insuficiente` (o `if contratos < 1` do ramo `put_protecao`) e
    somente quando `multiperna` for verdadeiro. Comentar o racional da regra,
    marcando-a como julgamento de produto reversível (16-CONTEXT, decisão
    tomada com autonomia concedida): o collar financia a proteção com o
    prêmio da call, então faz sentido oferecê-lo exatamente onde a put pura
    já não cabe no caixa — em vez de devolver "caixa insuficiente" sem
    alternativa. Reverter essa regra no futuro é mexer só aqui, nunca no
    motor. Se `_propor_collar` devolver `None`, `propor()` cai no
    `caixa_insuficiente` de sempre.

    (c) Dentro de `_propor_collar`, selecionar a call pela MESMA chamada que o
    ramo `call_coberta` já usa: `opcoes_motor.rastrear(chain, {"tipo": "call",
    "referencia": spot, "relacao": "acima", "criterio": "min", "n": 1})`.
    Lista vazia devolve `None` (sem call líquida, sem collar). A put é a que
    `propor()` já escolheu — recebida por parâmetro, nunca re-selecionada.

    (d) Montar as pernas de opção e derivar o teto de caixa DELAS, não de
    aritmética paralela: `pernas_opcao = [
    opcoes_motor.perna_de_contrato(contrato_call, "venda", quantidade=1),
    opcoes_motor.perna_de_contrato(contrato_put, "compra", quantidade=1)]`;
    `caixa_pernas = opcoes_motor.avaliar(pernas_opcao)`;
    `custo_unit = caixa_pernas["custo_liquido"]` (positivo = débito por ação,
    negativo = crédito). Teto: `contratos = qty_livre_val // 100` quando
    `custo_unit <= 0`; senão `min(qty_livre_val // 100, int(cash // (100 *
    custo_unit)))`. `contratos < 1` devolve `None`. Comentar por que o teto
    sai de `avaliar()` e não de `premio_put - premio_call` escrito à mão: uma
    segunda aritmética do mesmo número é exatamente a duplicação que esta
    fase existe para eliminar.

    Testes novos em `server/tests/test_opcoes_collar.py` (arquivo novo, com
    fixtures locais espelhando `_contrato`/`_cadeia`/`_posicao` de
    test_opcoes_lastreadas_proposta.py; `_SPOT = 30.0`, `_HOJE = dt.date(2026,
    8, 31)`), um teste por item do bloco `<behavior>`.
  </action>
  <verify>
    <automated>(cd server && .venv/bin/python -m pytest tests/test_opcoes_collar.py tests/test_opcoes_lastreadas_proposta.py -q)</automated>
  </verify>
  <acceptance_criteria>
    - Test command acima: 0 failed.
    - `python -c "import inspect; from app import opcoes_lastreadas as o; s=inspect.signature(o.propor); p=s.parameters['multiperna']; print(p.kind.name, p.default)"` (de `server/`) imprime `KEYWORD_ONLY False`.
    - `grep -c 'def _propor_collar' server/app/opcoes_lastreadas.py` devolve 1.
    - `grep -vE '^\s*#' server/app/opcoes_lastreadas.py | grep -cE 'premio_put\s*-\s*premio_call|premio_call\s*-\s*premio_put'` devolve 0 — o custo líquido vem de `avaliar()`, não de subtração escrita à mão.
    - Existe teste afirmando `contratos == 2` no caso de débito com caixa 250 (teto real exercitado, não só o caminho feliz).
  </acceptance_criteria>
  <done>O collar é oferecido exatamente na porta onde a put isolada não cabe no caixa, com as duas pernas selecionadas pela régua comum e o teto de contratos derivado do custo líquido calculado pelo motor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Proposta de collar — payoff consolidado, caixa, pernas nomeadas e manchete</name>
  <files>server/app/opcoes_lastreadas.py, server/tests/test_opcoes_collar.py</files>
  <read_first>
    - server/app/opcoes_lastreadas.py (estado após a Task 1, incluindo `_propor_collar` e o dict `proposta` das outras duas estruturas)
    - server/app/opcoes_payoff.py linhas 161-229 (chaves e semântica de perfil_da_estrutura)
    - server/app/skill_ref.py (entrada `collar` criada pelo Plano 16-02, e `num_br`)
    - .planning/phases/16-biblioteca-de-estruturas/16-02-SUMMARY.md (texto exato das duas frases)
  </read_first>
  <behavior>
    Cenário canônico — spot 30,00; call strike 32,00 prêmio 1,00; put strike
    28,00 prêmio 0,90; posição 300 ações livres; caixa 50; plano de VENDER;
    `multiperna=True`:
    - `proposta["estrutura"]["ganho_ilimitado"] is False` E `["perda_ilimitada"] is False` — travado dos dois lados.
    - `estrutura["ganho_maximo"] == 2.1` e `estrutura["perda_maxima"] == 1.9`.
    - `estrutura["breakevens"] == [29.9]`.
    - `estrutura["pernas"]` tem 3 entradas, tipos na ordem `["ACAO", "CALL", "PUT"]`, lados `["compra", "venda", "compra"]`.
    - `proposta["caixa"] == {"custoLiquidoUnitario": -0.1, "custoLiquidoTotal": -30.0, "fluxo": "credito"}`.
    - `proposta["contractSymbol"] is None`, `proposta["optionType"] is None`, `proposta["strike"] is None`, `proposta["premioUnitario"] is None`, `proposta["premioTotal"] is None`.
    - `proposta["strikeCall"] == 32.0` e `proposta["strikePut"] == 28.0`.
    - `proposta["pernasContratos"]` tem 2 entradas com `contractSymbol`, `optionType`, `lado` ("venda" na call, "compra" na put), `strike` e `premioUnitario`.
    - `proposta["manchete"]` é idêntica a `skill_ref.opcoes_lastreadas_txt("operador", "collar", n="3", ticker="PETR4", strikeCall="32,00", strikePut="28,00", qtyAcoes="300")` e não contém nenhum `{`.
    - `proposta["didatica"]` é a mesma frase no modo educacional, presente também quando `modo == "operador"`.
    - `proposta["liquidez"]["score"]` é o MENOR dos dois scores das pernas, e o `label` corresponde a esse menor score.
    - `proposta["chips"]` tem 4 entradas com chaves `prazo`, `strikes`, `crédito líquido` (`custo líquido` quando o fluxo é débito) e `liquidez`.
    - `proposta["precoObjeto"] == 30.0`, `proposta["qtyAcoes"] == 300`, `proposta["contratos"] == 3`, `proposta["diasParaVencimento"] == 30`.
  </behavior>
  <action>
    Completar `_propor_collar` montando o dict de proposta:

    (a) `pernas = [opcoes_motor.perna_de_acao(underlying, spot, quantidade=1),
    *pernas_opcao]` e `estrutura = opcoes_motor.avaliar(pernas)` — UMA
    avaliação para as 3 pernas. É esta chamada que prova LIB-03: o mesmo
    `perfil_da_estrutura` que serve 2 pernas serve 3 sem aritmética nova.

    (b) `caixa` no mesmo formato do Plano 16-01: `{"custoLiquidoUnitario":
    caixa_pernas["custo_liquido"], "custoLiquidoTotal": round(
    caixa_pernas["custo_liquido"] * qty_acoes, 2), "fluxo":
    caixa_pernas["fluxo"]}`, reusando o `caixa_pernas` que a Task 1 já
    calculou — não avaliar de novo.

    (c) Campos de identidade de contrato: `contractSymbol`, `optionType`,
    `strike`, `premioUnitario` e `premioTotal` são `None`. Comentar no código:
    o collar não tem contrato único nem prêmio único; preencher qualquer um
    deles com o valor de UMA das pernas faria uma estrutura de duas pernas se
    passar por operação de uma perna para qualquer consumidor que case por
    `contractSymbol` (é assim que o front casa proposta com posição,
    web/src/App.jsx:3216-3217). `None` com o valor real em `pernasContratos`
    e `caixa` é a aplicação direta da regra "null nunca 0.0" do repositório.

    (d) `strikeCall`/`strikePut` numéricos; `pernasContratos` com as duas
    pernas nomeadas (contractSymbol, optionType, lado, strike,
    premioUnitario arredondado a 2 casas).

    (e) `liquidez`: calcular `liquidity_score` das DUAS pernas e usar o MENOR
    score, com `_label_liquidez` desse menor. Comentar: a estrutura só é tão
    negociável quanto a sua perna pior — exibir a melhor seria otimismo
    embutido no dado.

    (f) `manchete` e `didatica` via `skill_ref.opcoes_lastreadas_txt` com a
    chave `"collar"`, dados `n=str(contratos)`, `ticker=underlying`,
    `strikeCall=skill_ref.num_br(strike_call)`,
    `strikePut=skill_ref.num_br(strike_put)`, `qtyAcoes=str(qty_acoes)` —
    `didatica` sempre no modo `"educacional"`, exatamente como as outras duas
    estruturas fazem hoje.

    (g) `chips`: `prazo` ("{dias} dias"), `strikes` ("call R$ {num_br
    (strike_call)} / put R$ {num_br(strike_put)}"), o chip de caixa com chave
    "crédito líquido" quando `caixa["fluxo"] == "credito"` e "custo líquido"
    nos demais casos, valor "R$ {num_br(abs(custoLiquidoTotal))}", e
    `liquidez` com o label da perna pior.

    (h) Manter `lastro`, `expiration`, `diasParaVencimento`, `contratos`,
    `qtyAcoes`, `precoObjeto`, `tipo: "collar"` no mesmo formato das outras
    estruturas. `propor()` devolve `{"proposta": <dict>, "motivo": "collar"}`
    — `motivo` continua sendo o próprio `tipo` e uma chave de vocabulário
    válida (o Plano 16-02 criou a entrada), preservando o contrato descrito
    na docstring de `propor()`.

    Testes com os números exatos do bloco `<behavior>` em
    `server/tests/test_opcoes_collar.py`.
  </action>
  <verify>
    <automated>(cd server && .venv/bin/python -m pytest tests/test_opcoes_collar.py tests/test_opcoes_collar_vocab.py tests/test_opcoes_payoff.py -q)</automated>
  </verify>
  <acceptance_criteria>
    - Test command acima: 0 failed.
    - Existe teste que afirma `ganho_ilimitado is False and perda_ilimitada is False` para o collar (a afirmação central de LIB-03 — travado dos dois lados).
    - Existe teste que afirma `proposta["contractSymbol"] is None` e que `pernasContratos` tem exatamente 2 entradas com símbolos distintos.
    - Existe teste que compara `proposta["manchete"]` com a saída direta de `skill_ref.opcoes_lastreadas_txt` (prova de que a manchete não é composta no motor).
    - `grep -vE '^\s*#' server/app/opcoes_lastreadas.py | grep -c 'Vender {n} call'` devolve 0 — nenhum texto de manchete literal fora do skill_ref.
    - `grep -c 'opcoes_motor.avaliar(' server/app/opcoes_lastreadas.py` devolve 4 (2 do Plano 16-01 + 2 do collar) — nenhuma avaliação redundante extra.
  </acceptance_criteria>
  <done>A proposta de collar carrega payoff consolidado de 3 pernas, movimento de caixa, as duas pernas nomeadas, e manchete/didática vindas só do skill_ref.</done>
</task>

<task type="auto">
  <name>Task 3: Guardiões — paridade do gatilho, não-regressão do default e forma da estrutura</name>
  <files>server/tests/test_opcoes_collar.py, server/tests/test_opcoes_gatilho.py</files>
  <read_first>
    - server/tests/test_opcoes_gatilho.py linhas 130-165 (`_COMBOS_DECISAO_LADO`, `_VIES_POR_MOTIVO_DO_PROPOR`, `test_paridade_gatilho_com_motor_em_producao`)
    - server/app/opcoes_gatilho.py (VIES_PROTECAO / VIES_PREMIO)
    - server/app/opcoes_lastreadas.py (estado após as Tasks 1 e 2)
  </read_first>
  <action>
    (a) Em `server/tests/test_opcoes_gatilho.py`, estender o guardião de
    paridade: acrescentar `"collar": VIES_PROTECAO` a
    `_VIES_POR_MOTIVO_DO_PROPOR` e um segundo laço sobre
    `_COMBOS_DECISAO_LADO` chamando `propor(..., multiperna=True)` com caixa
    curto (`cash=50`) e uma cadeia que tenha call E put líquidas — o motivo
    devolvido tem de continuar batendo com o viés de `do_plano()`. A docstring
    do teste hoje diz "a unificação real só acontece na Fase 16": atualizar
    esse texto para registrar o que a Fase 16 de fato fez (o collar entra no
    viés de PROTEÇÃO, e o mapeamento continua espelhado em vez de unificado
    porque `propor()` precisa distinguir put isolada de collar, distinção que
    `do_plano()` deliberadamente não faz). Guardião se atualiza com nota,
    nunca se apaga.

    (b) Em `server/tests/test_opcoes_collar.py`, guardião de não-regressão do
    default: para cada um dos cenários já cobertos em
    `test_opcoes_lastreadas_proposta.py` (cadeia degradada, sem lastro, plano
    de COMPRAR, plano de VENDER com caixa folgado, plano de NÃO OPERAR,
    contratos ilíquidos, vencimento fora da janela, caixa curto), afirmar que
    `propor(...)` e `propor(..., multiperna=False)` devolvem resultados
    IGUAIS (`==` sobre o dict inteiro) — nenhuma diferença silenciosa entre
    a chamada de hoje e a chamada explícita.

    (c) Guardião de forma: para qualquer resultado de `propor()` cujo
    `motivo` seja `"collar"`, afirmar que `proposta["contractSymbol"] is
    None`, que `len(proposta["pernasContratos"]) == 2` e que
    `estrutura["pernas"]` tem 3 entradas. Comentar que este guardião existe
    para impedir que uma estrutura de duas pernas volte a caber no caminho de
    execução de uma perna só.
  </action>
  <verify>
    <automated>(cd server && .venv/bin/python -m pytest tests/test_opcoes_collar.py tests/test_opcoes_gatilho.py tests/test_opcoes_lastreadas_proposta.py tests/test_opcoes_motor.py tests/test_opcoes_fronteira.py -q)</automated>
  </verify>
  <acceptance_criteria>
    - Test command acima: 0 failed.
    - `grep -c '"collar": VIES_PROTECAO' server/tests/test_opcoes_gatilho.py` devolve 1.
    - A docstring de `test_paridade_gatilho_com_motor_em_producao` não contém mais a frase "só acontece na Fase 16" (foi substituída pelo registro do que a fase fez).
    - Suíte completa do backend: `(cd server && .venv/bin/python -m pytest tests/ -q)` — nenhuma falha além dos `PermissionError ... load_verify_locations` já documentados no 15-VERIFICATION.md.
  </acceptance_criteria>
  <done>Os três guardiões existem e passam; o comportamento sem `multiperna` é provadamente idêntico ao de antes da fase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cadeia de opções (rede) → `_propor_collar` | Prêmios e strikes das DUAS pernas vêm de fonte externa e alimentam o teto de contratos por caixa |
| motor → carteira do usuário | O número de contratos proposto define quanto do lastro e do caixa a Fase 17 comprometeria na execução |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-08 | Elevation of privilege | proposta de 2 pernas consumida por caminho de 1 perna | mitigate | `contractSymbol`/`optionType`/`strike`/`premio*` são `None` no collar e guardião de forma (Task 3c) impede regressão; a trava de execução no servidor entra no Plano 16-04 |
| T-16-09 | Tampering | teto de contratos por caixa | mitigate | Teto derivado de `avaliar(pernas_opcao)["custo_liquido"]`, fonte única; teste de débito com caixa 250 fixa `contratos == 2`; `contratos < 1` recusa a proposta em vez de arredondar |
| T-16-10 | Information disclosure | liquidez exibida da estrutura | mitigate | `liquidez` do collar usa o MENOR score das duas pernas — exibir o melhor esconderia a perna difícil de negociar |
| T-16-11 | Spoofing | manchete do collar | mitigate | Texto vem só de `skill_ref.opcoes_lastreadas_txt`; teste compara a manchete com a saída direta da função e o grep de literal de manchete no motor devolve 0 |
| T-16-12 | Denial of service | avaliações de payoff por proposta | accept | 2 chamadas puras O(3 pernas) por proposta de collar, sem I/O; nenhuma requisição nova ao provedor de cadeia (ENG-03 preservado) |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | Nenhuma instalação de pacote nesta fase; `requirements.txt`/`requirements-prod.txt` intocados |
</threat_model>

<verification>
- `(cd server && .venv/bin/python -m pytest tests/ -q)` — sem falha nova (ver ressalva de sandbox acima).
- `git status --porcelain web/` vazio.
- `git diff --stat server/requirements.txt server/requirements-prod.txt` vazio.
</verification>

<success_criteria>
- `propor(..., multiperna=True)` devolve collar no caso definido, com payoff travado dos dois lados e as duas pernas nomeadas.
- `propor()` sem o parâmetro é provadamente idêntico ao comportamento pré-fase em todos os cenários já cobertos.
- Nenhuma aritmética de prêmio líquido escrita à mão: o número vem de `opcoes_motor.avaliar()`.
- Nenhum texto de manchete fora do `skill_ref`.
</success_criteria>

<output>
Create `.planning/phases/16-biblioteca-de-estruturas/16-03-SUMMARY.md` when done.
Registrar: (1) a regra de quando o collar é oferecido, marcada como julgamento
de produto reversível; (2) a decisão de `contractSymbol`/`premioTotal` nulos no
collar e o motivo; (3) por que `propor()` continuou uma função com um helper
privado em vez de virar dispatcher de três funções (ou o contrário, se o diff
real pedir).
</output>
