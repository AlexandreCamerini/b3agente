---
phase: 19-motor-multi-candidato
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/opcoes_lastreadas.py
  - server/tests/test_opcoes_collar.py
autonomous: true
requirements: [MULTI-01]

must_haves:
  truths:
    - "Quando a leitura técnica é VENDER/baixa e a put isolada E o collar cabem, `propor(..., multiperna=True)` devolve os DOIS candidatos"
    - "A ordem é sempre put_protecao primeiro, collar depois — `proposta` continua sendo `candidatos[0]`"
    - "`motivo` continua sendo o tipo do candidato primário, com o mesmo vocabulário de sempre"
    - "Toda porta fechada (degradado/sem_lastro/sem_setup/sem_contrato_liquido/sem_vencimento_elegivel/caixa_insuficiente) devolve o dict EXATAMENTE como hoje, sem chave nova"
    - "Com `multiperna=False` o retorno é byte-idêntico ao de hoje, exceto pela chave aditiva `candidatos`"
  artifacts:
    - path: "server/app/opcoes_lastreadas.py"
      provides: "propor() com lista de candidatos"
      contains: "candidatos"
    - path: "server/tests/test_opcoes_collar.py"
      provides: "guardiões de coexistência e de não-regressão dos negativos"
      contains: "candidatos"
  key_links:
    - from: "server/app/opcoes_lastreadas.py:propor"
      to: "server/app/opcoes_lastreadas.py:_propor_collar"
      via: "chamada incondicional quando multiperna=True e tipo == put_protecao"
      pattern: "_propor_collar\\("
---

<objective>
`opcoes_lastreadas.propor()` deixa de escolher UMA estrutura por posição e passa
a devolver a lista completa de candidatos elegíveis — de forma ADITIVA, sem que
nenhum consumidor existente precise mudar uma linha.

Purpose: MULTI-01. Hoje o collar só existe como FALLBACK (só é tentado quando a
put pura não cabe no caixa). Quando as duas cabem, o usuário nunca vê a segunda
— o motor decide por ele. Este plano para de esconder uma estrutura que o motor
já sabe calcular.

Output: `propor()` devolve `{"proposta", "motivo", "candidatos"}` nos retornos
POSITIVOS; guardiões novos provando coexistência, ordem e não-regressão.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-motor-multi-candidato/19-CONTEXT.md
@.planning/phases/19-motor-multi-candidato/19-PATTERNS.md
@CLAUDE.md

<interfaces>
Contrato que este plano ESTABELECE e que os planos 19-02/19-03 consomem —
nenhum executor posterior precisa reabrir este arquivo para descobri-lo:

`opcoes_lastreadas.propor(underlying, chain, spot, plano, posicao, cash, modo,
hoje, *, multiperna=False)`

RETORNO POSITIVO (novo):
```
{"proposta": <dict>, "motivo": <str>, "candidatos": [<dict>, ...]}
```
- `candidatos` — lista com 1 ou 2 dicts, cada um na MESMA forma que `proposta`
  tem hoje (`tipo`/`contractSymbol`/`pernasContratos`/`manchete`/`didatica`/
  `chips`/`estrutura`/`caixa`/`precoObjeto`/...).
- `proposta` — SEMPRE `candidatos[0]`, nunca um dict recalculado à parte.
- `motivo` — SEMPRE `candidatos[0]["tipo"]` (`call_coberta`|`put_protecao`|
  `collar`), igual a hoje.
- Ordem quando há dois: `["put_protecao", "collar"]`.

RETORNO NEGATIVO (INALTERADO, sem a chave `candidatos`):
```
{"proposta": None, "motivo": "<degradado|sem_lastro|sem_setup|sem_contrato_liquido|sem_vencimento_elegivel|caixa_insuficiente>"}
```
Isto NÃO é esquecimento: ~15 guardiões em `server/tests/test_opcoes_*.py` fazem
igualdade EXATA de dict nesses retornos (`assert r == {"proposta": None,
"motivo": "caixa_insuficiente"}`). Acrescentar `"candidatos": []` neles
quebraria todos por nada. Consumidor lê SEMPRE com
`resultado.get("candidatos", [])` — nunca `resultado["candidatos"]`.

`_propor_collar(underlying, chain, spot, contrato_put, posicao, cash, modo,
hoje, dias, qty_livre_val)` — já existe, NÃO muda nesta fase. Recebe a put JÁ
selecionada por parâmetro (nunca resseleciona), devolve o dict completo do
collar ou `None` (sem call líquida acima do spot / custo líquido não cabe no
caixa).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: propor() monta a lista de candidatos</name>
  <read_first>
    - server/app/opcoes_lastreadas.py:1-336 (arquivo inteiro do motor: `_propor_collar` em :41-155 e `propor` em :158-336 — ler antes de editar, o bloco de fallback do collar está em :246-263)
    - .planning/phases/19-motor-multi-candidato/19-CONTEXT.md (seção "MULTI-01 — regra de quais candidatos coexistem" e "forma de retorno")
    - .planning/phases/19-motor-multi-candidato/19-PATTERNS.md (seção `opcoes_lastreadas.py — propor()`: bloco atual e disciplina de comentário da casa)
    - server/tests/test_opcoes_collar.py:52-150 (guardiões que já existem sobre este exato bloco)
  </read_first>
  <files>server/app/opcoes_lastreadas.py</files>
  <action>
Editar SOMENTE `propor()`. `_propor_collar()` não muda uma linha.

1. Antes do branch de decisão (hoje linha ~198, `plano = plano or {}`), declarar
   `colar = None` — a variável precisa existir nos dois branches positivos para
   a montagem final da lista.

2. Dentro de `if tipo == "put_protecao":` (hoje linha 246), inverter a ordem
   lógica do bloco:
   - manter o cap de caixa da put: `contratos = min(contratos, int(cash // (100 * premio)) if premio > 0 else 0)`;
   - LOGO EM SEGUIDA, quando `multiperna` for verdadeiro, chamar
     `_propor_collar(underlying, chain, spot, contrato, posicao, cash, modo, hoje, dias, qty_livre_val)`
     INCONDICIONALMENTE (fora de qualquer `if contratos < 1`) e guardar em `colar`;
   - só então manter o early-return de `contratos < 1`: se `colar is not None`,
     devolver `{"proposta": colar, "motivo": "collar", "candidatos": [colar]}`;
     senão devolver `{"proposta": None, "motivo": "caixa_insuficiente"}`
     EXATAMENTE como hoje (dict de dois campos, sem `candidatos`).

3. No retorno positivo final (hoje linha 336, `return {"proposta": proposta,
   "motivo": tipo}`), montar a lista: começar com `[proposta]` e acrescentar
   `colar` quando não for `None`; devolver
   `{"proposta": candidatos[0], "motivo": candidatos[0]["tipo"], "candidatos": candidatos}`.
   Não recalcular `motivo` a partir da variável `tipo` — derivar do candidato
   primário é o que torna a regra "proposta == candidatos[0]" verificável.

4. NÃO tocar em nenhum dos outros `return {"proposta": None, "motivo": ...}`
   (degradado, sem_lastro, sem_setup, sem_vencimento_elegivel,
   sem_contrato_liquido). Eles ficam com DOIS campos, sem `candidatos` — ver o
   bloco `<interfaces>` acima para a razão.

5. Atualizar a docstring de `propor()` (hoje :159-178) declarando a nova forma
   de retorno, a regra de ordem (`put_protecao` antes de `collar`), e por que os
   negativos não ganham a chave. Seguir a disciplina de comentário da casa:
   português, citando a origem da decisão ("Fase 19, MULTI-01"), narrando o PORQUÊ
   e não o o-quê. Substituir/ajustar o comentário do bloco do collar (hoje
   :249-257, que descreve a regra ANTIGA "oferecê-lo EXATAMENTE onde a put pura
   já não cabe") — a regra mudou, deixar o comentário velho seria documentação
   mentindo sobre o código.

Nenhuma mudança de manchete, de `skill_ref`, de `chips`, de payoff ou de
aritmética. Cada candidato já traz a própria manchete verbatim de
`skill_ref.opcoes_lastreadas_txt()` — guardrail CVM intocado.
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/test_opcoes_collar.py tests/test_opcoes_lastreadas_proposta.py tests/test_opcoes_gatilho.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"candidatos"' server/app/opcoes_lastreadas.py` retorna 3 ou mais (os retornos positivos).
    - Nenhum retorno negativo ganhou a chave: `grep -n 'proposta": None' server/app/opcoes_lastreadas.py | wc -l` continua igual ao valor de antes da edição, e nenhuma dessas linhas contém `candidatos`.
    - A chamada a `_propor_collar(` NÃO está aninhada dentro de um `if contratos < 1:` — verificável lendo o bloco e por `sed -n '/tipo == "put_protecao"/,/qty_acoes = contratos/p' server/app/opcoes_lastreadas.py` mostrando `_propor_collar(` no mesmo nível de indentação do `if contratos < 1:`, não dentro dele.
    - `_propor_collar` não foi modificada: `git diff server/app/opcoes_lastreadas.py` não mostra nenhuma linha alterada entre a `def _propor_collar` e a `def propor`.
    - Os testes de `tests/test_opcoes_collar.py`, `tests/test_opcoes_lastreadas_proposta.py` e `tests/test_opcoes_gatilho.py` que existiam antes deste plano passam sem edição (nenhum deles é tocado nesta task).
    - `git diff server/app/skill_ref.py` sai vazio.
  </acceptance_criteria>
  <done>`propor()` devolve `candidatos` nos caminhos positivos, com put_protecao no índice 0 quando os dois cabem; negativos e `_propor_collar` intocados; suíte de opções verde.</done>
</task>

<task type="auto">
  <name>Task 2: guardiões de coexistência, ordem e não-regressão</name>
  <read_first>
    - server/tests/test_opcoes_collar.py:1-150 (fixtures `_contrato`/`_cadeia`/`_posicao`/`_PLANO_VENDER` e os guardiões de gatilho já existentes)
    - server/app/opcoes_lastreadas.py (estado DEPOIS da Task 1)
    - .planning/phases/19-motor-multi-candidato/19-PATTERNS.md (seção "Tests — Pattern Assignments", com o esqueleto do teste de coexistência)
    - CLAUDE.md (guardrail "Guardiões de teste não se apagam — reversão deliberada atualiza o guardião com nota")
  </read_first>
  <files>server/tests/test_opcoes_collar.py</files>
  <action>
Acrescentar uma seção nova ao final da Parte 1 do arquivo, com cabeçalho de
comentário citando "Fase 19, MULTI-01". Nenhum teste existente é apagado.

Testes NOVOS a escrever (usar as fixtures locais já existentes, não criar
fixture nova):

1. `test_propor_multiperna_true_com_caixa_e_call_liquida_ambos_coexistem` —
   cadeia com `calls=[_contrato(32.0, "PETR4F32", "call", price=1.0)]` e
   `puts=[_contrato(28.0, "PETR4F28", "put", price=0.9)]`, `cash=100000`,
   `_PLANO_VENDER`, `multiperna=True`. Asserções: a lista de `tipo` de
   `r["candidatos"]` é exatamente `["put_protecao", "collar"]`;
   `r["proposta"] is r["candidatos"][0]` (mesmo objeto, não uma cópia);
   `r["proposta"]["tipo"] == "put_protecao"`; `r["motivo"] == "put_protecao"`.

2. `test_propor_multiperna_false_nunca_traz_collar_mesmo_com_os_dois_cabendo` —
   mesma cadeia e mesmo caixa do teste 1, mas SEM `multiperna`. Asserções:
   `[c["tipo"] for c in r["candidatos"]] == ["put_protecao"]` e
   `r["motivo"] == "put_protecao"` — a negociação de capacidade da Fase 16
   continua valendo.

3. `test_propor_call_coberta_tem_candidato_unico` — `_PLANO_NAO_OPERAR`, cadeia
   só com calls, `multiperna=True`. Asserções:
   `[c["tipo"] for c in r["candidatos"]] == ["call_coberta"]`,
   `r["motivo"] == "call_coberta"`. O viés de prêmio segue candidato único.

4. `test_propor_collar_sozinho_quando_a_put_nao_cabe_no_caixa` — cadeia com call
   e put líquidas, `cash=50`, `multiperna=True`. Asserções:
   `[c["tipo"] for c in r["candidatos"]] == ["collar"]`,
   `r["proposta"]["tipo"] == "collar"`, `r["motivo"] == "collar"` — o
   comportamento da Fase 16 preservado, agora expresso como lista de um.

5. `test_propor_negativos_nao_ganham_chave_candidatos` — parametrizar sobre pelo
   menos três portas fechadas alcançáveis com as fixtures do arquivo
   (`sem_setup` via `_PLANO_COMPRAR`; `caixa_insuficiente` via cash curto e
   cadeia sem call líquida; `sem_contrato_liquido` via cadeia de puts
   ilíquidas). Asserção: `"candidatos" not in r` em todos. Comentário no teste
   explicando que a ausência é DELIBERADA (compatibilidade com os ~15 guardiões
   de igualdade exata de dict).

6. `test_cada_candidato_tem_manchete_propria_do_skill_ref` — no cenário de
   coexistência do teste 1, asserções: as duas manchetes são strings não vazias
   e DIFERENTES entre si; cada uma é exatamente igual ao retorno de
   `skill_ref.opcoes_lastreadas_txt("operador", c["tipo"], ...)` para o candidato
   correspondente (montar `dados` com os mesmos campos que o motor usa — ver
   `opcoes_lastreadas.py` :270-274 e :99-104). Guardrail CVM: cada candidato
   carrega a manchete do motor verbatim, nenhuma é composta.

GUARDIÃO EXISTENTE a RE-VERIFICAR (não apagar, não reescrever a asserção):
`test_propor_multiperna_true_com_caixa_folgado_mantem_put_protecao` (hoje
:78-84). A cadeia dele NÃO tem calls (`_cadeia(puts=puts)`), então
`_propor_collar` continua devolvendo `None` e `r["motivo"] == "put_protecao"`
segue verdadeiro depois da Task 1. Rodar o teste e, confirmado que passa,
acrescentar UMA linha de comentário na docstring dele registrando que foi
re-verificado sob MULTI-01 e por que continua válido (a cadeia não tem call
líquida, então não existe segundo candidato). Se — e só se — ele falhar, parar e
reportar em vez de reescrever a asserção.

Depois de escrever, rodar a suíte canônica inteira (`bash scripts/executar.sh
--testes`). Se algum outro teste do repositório quebrar, NÃO apagar: reportar o
nome do teste, o motivo da quebra e a decisão proposta no SUMMARY.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && .venv/bin/python -m pytest tests/test_opcoes_collar.py -q` passa com pelo menos 6 testes a mais que antes do plano.
    - `cd server && .venv/bin/python -m pytest tests/test_opcoes_collar.py -q -k "com_caixa_folgado_mantem_put_protecao"` passa (guardião re-verificado, não alterado na asserção).
    - `git diff server/tests/test_opcoes_collar.py` não mostra NENHUMA linha `assert` removida ou alterada nos testes que já existiam — só linhas adicionadas (testes novos + a nota de re-verificação na docstring).
    - `bash scripts/executar.sh --testes` sai com código 0 (as DUAS suítes).
    - `grep -c 'candidatos' server/tests/test_opcoes_collar.py` >= 8.
  </acceptance_criteria>
  <done>Coexistência, ordem, candidato único, collar sozinho, negativos sem chave e manchete própria por candidato provados por teste; guardião da Fase 16 re-verificado com nota; suíte canônica verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rota HTTP → `propor()` | Este plano NÃO cruza essa fronteira: `opcoes_lastreadas.py` é módulo PURO (sem rede, sem banco, sem relógio interno). Todo dado entra por argumento já validado pelo chamador. |
| `propor()` → consumidor | A forma do retorno é o contrato; alargá-la de forma errada é o risco real deste plano. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | contrato de retorno de `propor()` | mitigate | `proposta` DEVE continuar sendo `candidatos[0]` e `motivo` o `tipo` dele; teste 1 da Task 2 assere identidade de objeto (`is`), não só igualdade — um `proposta` recalculado à parte poderia divergir do candidato que a UI exibe e virar aceite de estrutura diferente da mostrada. |
| T-19-02 | Tampering | retornos negativos | mitigate | Negativos permanecem dict de 2 campos; teste 5 da Task 2 trava a ausência da chave. Alargar o negativo quebraria ~15 guardiões de igualdade exata que hoje provam "porta fechada nunca vira proposta". |
| T-19-03 | Information disclosure / Repudiation | manchete de cada candidato | mitigate | Guardrail CVM: teste 6 da Task 2 prova que cada manchete vem verbatim de `skill_ref.opcoes_lastreadas_txt()` e que as duas são distintas — nenhuma composição no motor, nenhuma manchete reaproveitada entre candidatos. |
| T-19-04 | Denial of service | chamada extra a `_propor_collar()` no caminho feliz | accept | `_propor_collar` é puro e opera sobre a cadeia JÁ carregada em memória (uma passada de `rastrear` + duas de `avaliar`); nenhuma requisição de rede nova, nenhum acesso a banco. Custo desprezível diante do fetch de cadeia que já aconteceu. |
| T-19-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote novo é instalado neste plano — `grep -c` em `server/requirements*.txt` e `web/package.json` deve sair inalterado no diff. Portão de legitimidade não se aplica por ausência de instalação. |
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` verde (as duas suítes).
- `git diff --stat` limitado a `server/app/opcoes_lastreadas.py` e `server/tests/test_opcoes_collar.py`.
- Nenhuma mudança em `server/app/skill_ref.py`, `web/src/copy.js` ou `server/app/defaults.py` (paridade de prompts intocada).
</verification>

<success_criteria>
- Coexistência put_protecao + collar provada por teste, na ordem travada.
- `proposta`/`motivo` byte-compatíveis com hoje para todo consumidor existente.
- Negativos inalterados.
- Suíte canônica verde.
</success_criteria>

<output>
Create `.planning/phases/19-motor-multi-candidato/19-01-SUMMARY.md` when done
</output>

