---
phase: 19-motor-multi-candidato
plan: 03
type: execute
wave: 3
depends_on: ["19-02"]
files_modified:
  - web/src/App.jsx
  - web/tests/test_opcoes_multi_candidato_ui.mjs
  - web/tests/test_opcoes_collar_ui.mjs
autonomous: true
requirements: [MULTI-02]

must_haves:
  truths:
    - "Quando a posição tem mais de um candidato, o detalhe em Posições mostra os N lado a lado, cada um com manchete própria, payoff próprio e CTA próprio"
    - "Quando há um candidato só, a tela é exatamente a de hoje — nenhuma regressão visual ou funcional"
    - "Cada CTA de candidato dispara a rota certa: abrirCollar para o collar, abrirLastreada para put/call"
    - "Clicar em um candidato desabilita os CTAs de todos os irmãos daquela posição enquanto a ordem está em voo"
    - "Nenhuma string de copy nova é criada — todo texto vem de funções já existentes em copy.js ou da manchete do motor"
  artifacts:
    - path: "web/src/App.jsx"
      provides: "CandidatoOpcao e o ramo de N candidatos em PropostaDaPosicao"
      contains: "function CandidatoOpcao"
    - path: "web/tests/test_opcoes_multi_candidato_ui.mjs"
      provides: "guardiões estáticos do ramo multi-candidato"
      contains: "CandidatoOpcao"
  key_links:
    - from: "web/src/App.jsx:PropostaDaPosicao"
      to: "web/src/App.jsx:CandidatoOpcao"
      via: "render da linha horizontal quando candidatos.length > 1"
      pattern: "<CandidatoOpcao"
    - from: "web/src/App.jsx:CandidatoOpcao"
      to: "A.abrirLastreada / A.abrirCollar"
      via: "onAceitar(p) parametrizado pelo candidato clicado"
      pattern: "onAceitar"
---

<objective>
O detalhe da posição em Posições passa a mostrar os N candidatos lado a lado,
cada um aceitável por si, sem inventar nenhum padrão visual novo e sem nenhuma
função de store nova.

Purpose: MULTI-02. O motor (19-01) e as rotas (19-02) já entregam a lista; sem
esta camada o usuário continua vendo uma estrutura só e a fase não muda nada
para ele.

Output: componente `CandidatoOpcao`, ramo de N candidatos em `PropostaDaPosicao`,
handler de aceite parametrizado pelo candidato clicado, guardiões estáticos
novos e o guardião de collar da Fase 17 atualizado com nota.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-motor-multi-candidato/19-CONTEXT.md
@.planning/phases/19-motor-multi-candidato/19-UI-SPEC.md
@.planning/phases/19-motor-multi-candidato/19-PATTERNS.md
@.planning/phases/19-motor-multi-candidato/19-02-SUMMARY.md
@CLAUDE.md

<interfaces>
**Payload que a UI recebe** (entregue pelo Plano 19-02, rota
`GET /api/options/proposta/{ticker}?multiperna=1`, já chamada pelo hook
`useOpcoesPropostas` com `store.optionsProposta(t, true)` — App.jsx:4103):

```
{ ticker, providerStatus, modo, proposta, motivo, motivoTexto,
  putSemLastro, candidatos: [ ... ], source, at }
```
- `candidatos` é SEMPRE array (default `[]` no servidor); pode vir com 0, 1 ou 2
  itens. O ramo de proposta de FECHAMENTO devolve `[]`.
- `candidatos[0] === proposta` quando não vazio. Ordem: put_protecao, collar.
- Cada candidato tem a MESMA forma que `r.proposta` já tem hoje: `tipo`,
  `contractSymbol`, `optionType`, `strike`, `strikeCall`, `strikePut`,
  `pernasContratos`, `expiration`, `contratos`, `qtyAcoes`, `premioUnitario`,
  `premioTotal`, `manchete`, `didatica`, `chips`, `estrutura`, `caixa`,
  `precoObjeto`.

**Funções de aceite já existentes (App.jsx:8070-8098) — nenhuma nova:**
- `A.abrirLastreada({ underlying, contractSymbol, expiration, contratos })` —
  put de proteção e venda coberta.
- `A.abrirCollar({ underlying, pernasContratos: [{contractSymbol, lado}, ...],
  contratos, expiration })` — collar. Erro sai por `flash("Montar collar: " +
  e.message)` / `flash("Abrir operação lastreada: " + e.message)`.

**Copy já existente (web/src/copy.js) — nenhuma chave nova nesta fase:**
`cp.eyebrowPropostaCall`, `cp.eyebrowPropostaPut`, `cp.eyebrowPropostaCollar`,
`cp.collarPernasLinha(n, ticker, strikeCall, strikePut)`,
`cp.ctaVendaCoberta(n, ticker, strike, premio)`,
`cp.ctaPutProtecao(n, ticker, strike, premio)`,
`cp.ctaCollarCredito(n, ticker, sc, sp, valor)`,
`cp.ctaCollarDebito(n, ticker, sc, sp, valor)`,
`cp.confirmAbrirCoberta(n, ticker, qtyAcoes)`,
`cp.confirmAbrirCollar(n, ticker, qtyAcoes)`,
`cp.linhaPropostaNaPosicao` (SEM pluralizar),
`cp.payoffTitulo`, `cp.payoffGanhoMaximo`, `cp.payoffPerdaMaxima`,
`cp.payoffBreakeven`, `cp.payoffIlimitado`, `cp.payoffSemDado`,
`cp.payoffCaixaCredito`, `cp.payoffCaixaDebito`, `cp.payoffCaixaNeutro`,
`cp.payoffNota(preco)`, `cp.propostaIndisponivelDegradada`.

**Helpers já existentes em App.jsx:** `price(v)`, `MONO`, `T.*`,
`<FonteDoDadoProposta r={r} cp={cp} />`.

**Valores visuais LOCK do 19-UI-SPEC.md** (não arredondar, não inventar terceiro
padrão): item `flex: "0 0 auto"`, `minWidth: "210px"`, `minHeight: "44px"`,
`padding: "11px 12px"`, `borderRadius: "11px"`, `background: T.bgCard`,
`border: 1px solid T.borderFaint`; linha `display:"flex", gap:"10px",
overflowX:"auto", WebkitOverflowScrolling:"touch", scrollbarWidth:"none",
paddingBottom:"2px"`; eyebrow `10px/800/letterSpacing 0.04em/T.accent`; linha
mono `MONO 13px/800/T.textSecondary/marginTop 3px`; manchete `12.5px/700/
marginTop 4px/whiteSpace "normal"`, cor `T.positive` (call) ou `T.negative`
(put/collar) — NUNCA `T.accent` na manchete; chips `11px, padding 4px 10px,
borderRadius 999px, background T.bgBase`; caixa de payoff `padding 10px,
borderRadius 9px, background T.bgBase`; CTA `minHeight 40px, borderRadius 9px,
border 1px solid T.accent, background T.accentTint, color T.accent, 12.5px/800`.
</interfaces>

<guardioes_que_esta_mudanca_toca>
Levantado no planejamento por leitura dos testes — o executor não precisa
descobrir sozinho:

1. `web/tests/test_carteira_opcoes_tira.mjs:198` — `<PropostaLastreada` tem que
   aparecer EXATAMENTE 2x no fonte (AtivoCard + PropostaDaPosicao). Por isso o
   ramo multi NÃO pode renderizar `<PropostaLastreada` por candidato; é o motivo
   de existir o componente `CandidatoOpcao`.
2. `web/tests/test_carteira_opcoes_tira.mjs:170-174` — a fatia
   `PropostaDaPosicao → useOpcoesPropostas` precisa continuar contendo
   `if (!r || !r.proposta) return null;` e NÃO pode referenciar
   `cp.tiraOpcoesSemCobertura` nem `cp.tiraOpcoesSemSetup`. `CandidatoOpcao`
   cai dentro dessa fatia e também não pode referenciá-las.
3. `web/tests/test_carteira_opcoes_tira.mjs` — `store.optionsGate(` e
   `store.optionsProposta(` exatamente 2x cada no fonte. Este plano NÃO
   acrescenta busca nenhuma.
4. `web/tests/test_opcoes_proposta_ui.mjs` — assinatura de `PropostaLastreada`
   congelada e vários guardiões sobre a fatia `PropostaLastreada → OpcoesCamada`.
   `PropostaLastreada` NÃO é editada neste plano. Também é global:
   `!/"Vender " \+/.test(app)` e `!app.includes("Se você tivesse")` — proibido
   compor manchete no front.
5. `web/tests/test_opcoes_collar_ui.mjs:90-110` — QUEBRA DELIBERADA: hoje conta
   exatamente 2 handlers `const onAbrirLastreada = async () => {` e exige
   confirm-antes-de-executar dentro de cada um. Este plano renomeia o handler de
   `PropostaDaPosicao` para `aceitarCandidato(p)`; o guardião é ATUALIZADO com
   nota (Task 2), nunca apagado.
</guardioes_que_esta_mudanca_toca>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CandidatoOpcao e o ramo de N candidatos em PropostaDaPosicao</name>
  <read_first>
    - web/src/App.jsx:3016-3135 (`PropostaLastreada` — a fonte dos internos de um card: eyebrow, manchete, linha mono, chips, caixa de payoff com o helper `porLote`, split Estudo/Operador, CTA por tipo. NÃO editar este componente, só copiar o padrão.)
    - web/src/App.jsx:3908-3981 (`OportunidadesOpcoes` — o padrão de linha horizontal e do item de card)
    - web/src/App.jsx:3983-4118 (`PropostaDaPosicao` e `useOpcoesPropostas`)
    - web/src/App.jsx:8060-8098 (`A.abrirLastreada` / `A.abrirCollar` e o `flash` de erro)
    - web/src/copy.js (as chaves listadas em `<interfaces>` — confirmar que existem antes de usar; NÃO adicionar nenhuma)
    - .planning/phases/19-motor-multi-candidato/19-UI-SPEC.md (valores visuais e as duas decisões de interação)
    - web/tests/test_carteira_opcoes_tira.mjs (os guardiões de fatia que este arquivo tem que continuar satisfazendo)
  </read_first>
  <files>web/src/App.jsx</files>
  <action>
Duas mudanças em `web/src/App.jsx`, ambas dentro do cluster da Fase 18. Nenhuma
outra função do arquivo é tocada; `PropostaLastreada` e `AtivoCard` ficam como
estão.

**(A) Novo componente `CandidatoOpcao`.**
Declarar `function CandidatoOpcao({ p, r, cp, operador, busy, onAceitar })`
IMEDIATAMENTE DEPOIS do fecha-chaves de `PropostaDaPosicao` e ANTES de
`function useOpcoesPropostas` — posição escolhida para cair dentro da fatia que
os guardiões da Fase 18 já inspecionam.
Cabeçalho de comentário na disciplina da casa (português, PORQUÊ, citando
"Fase 19, MULTI-02"): é o item de UM candidato dentro da linha de N; reusa
verbatim o padrão visual do item de `OportunidadesOpcoes` (Fase 18) e os
internos de `PropostaLastreada` (Fase 14/17); NÃO renderiza
`<PropostaLastreada>` de propósito (o guardião de contagem 2x da Fase 18 trava
esse componente aos dois pontos de uso existentes).
Conteúdo, na ordem, usando os valores LOCK do bloco `<interfaces>`:
1. `const isCollar = p.tipo === "collar"; const isCall = p.optionType === "call";`
   `const cor = isCall ? T.positive : T.negative;` (nunca `T.accent`, com o
   comentário do guardrail repetido como já está em App.jsx:3944-3946)
   `const eyebrow = isCollar ? cp.eyebrowPropostaCollar : isCall ? cp.eyebrowPropostaCall : cp.eyebrowPropostaPut;`
   `const degradado = r.providerStatus !== "ok";`
   `const est = p.estrutura || null;`
   `const porLote = (v) => (typeof v === "number" ? v * (p.qtyAcoes || 0) : null);`
   — copiar o `porLote` com o mesmo `typeof` de App.jsx:3055: em JS
   `null * 100 === 0`, e multiplicar antes de checar transformaria "não
   aplicável" em "R$ 0,00" (regra null-nunca-zero aplicada à UI).
2. contêiner `<div>` do item (flex 0 0 auto, minWidth 210px, minHeight 44px,
   padding "11px 12px", borderRadius "11px", background `T.bgCard`, border
   `1px solid ${T.borderFaint}`).
3. eyebrow.
4. linha mono de identificação: `isCollar ? cp.collarPernasLinha(p.contratos,
   r.ticker, price(p.strikeCall), price(p.strikePut)) : ` template com
   `${p.contratos}× ${isCall ? "CALL" : "PUT"} ${r.ticker} · strike ${price(p.strike)}`
   — MESMA expressão de App.jsx:3061-3063.
5. manchete: `{p.manchete}` renderizada DIRETO, com o comentário do guardrail
   CVM já usado em App.jsx:3957-3959 (verbatim, nunca truncada, nunca composta).
6. chips: `(p.chips || []).map(...)` com o mesmo estilo de pill de
   App.jsx:3066-3070.
7. caixa de payoff guardada por `{est && (` — as quatro linhas de
   App.jsx:3072-3104 (ganho máximo com `est.ganho_ilimitado`, perda máxima com
   `est.perda_ilimitada`, breakevens SEM multiplicar por `qtyAcoes`, linha de
   caixa por `p.caixa.fluxo`, nota `cp.payoffNota(price(p.precoObjeto))`).
8. Modo Estudo (`!operador`): `{p.didatica}`, nenhum botão.
9. Modo Operador: `<button type="button" onClick={() => onAceitar(p)}
   disabled={busy || degradado}>` com o rótulo por tipo — `degradado ?
   cp.propostaIndisponivelDegradada :` collar crédito/débito por
   `p.caixa.fluxo` / `cp.ctaVendaCoberta` / `cp.ctaPutProtecao`, exatamente como
   App.jsx:3119-3129, MENOS o ramo `posAberta`/`ctaFecharLastreada` (o ramo
   multi só existe quando não há posição lastreada aberta — a rota devolve
   `candidatos: []` nesse caso; ver `<interfaces>`).

**(B) `PropostaDaPosicao` passa a ter dois ramos de render.**
1. Renomear o handler `onAbrirLastreada` (hoje :4015-4037) para
   `const aceitarCandidato = async (p) => {` — corpo IDÊNTICO, trocando apenas
   `const p = r.proposta;` pelo parâmetro. Manter a guarda de entrada como
   `if (!p) return;`, manter `window.confirm(cp.confirmAbrirCollar(...))` ANTES
   de `A.abrirCollar(`, manter `cp.confirmAbrirCoberta` só no ramo
   `p.optionType === "call"` (a put não trava lastro — T-14-24), manter
   `setBusy(true)` / `finally { setBusy(false); }`.
2. Logo abaixo da guarda `if (!r || !r.proposta) return null;`, derivar
   `const candidatos = Array.isArray(r.candidatos) ? r.candidatos : [];` e
   `const multi = candidatos.length > 1;`.
3. No JSX, dentro de `{aberto && (`, ramificar:
   - `multi` verdadeiro: renderizar a linha horizontal (valores LOCK) com
     `{candidatos.map((c) => <CandidatoOpcao key={c.tipo + "-" + (c.contractSymbol || "collar")} p={c} r={r} cp={cp} operador={operador} busy={busy} onAceitar={aceitarCandidato} />)}`
     seguida de UM `<FonteDoDadoProposta r={r} cp={cp} />` abaixo da linha
     (princípio 3 do CLAUDE.md: fonte e horário do dado também no ramo novo).
   - caso contrário: o `<PropostaLastreada ... />` de hoje, sem nenhuma outra
     mudança, com `onAbrir={() => aceitarCandidato(r.proposta)}`.
4. NÃO tocar em `onFecharLastreada`, em `posAberta`, no cabeçalho colapsável
   (`cp.linhaPropostaNaPosicao`, sem pluralizar) nem no hook
   `useOpcoesPropostas`.

**Busy compartilhado é intencional** (19-UI-SPEC.md, Decisão de Interação 1):
um único `busy` por posição, lido por TODOS os CTAs de candidato — clicar um
desabilita os irmãos de graça, sem estado novo. Não criar busy por candidato.
Não construir refetch depois do aceite; se o usuário tocar o irmão obsoleto, a
rejeição vem do servidor (Plano 19-02) e aparece pelo `flash` que já existe.

Ao final, rodar `npx vite build` a partir de `web/` — grep e teste estático não
pegam erro de sintaxe JSX (guardrail do CLAUDE.md).
  </action>
  <verify>
    <automated>cd web && npx vite build && cd .. && bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx vite build` termina com código 0.
    - `grep -c "function CandidatoOpcao" web/src/App.jsx` == 1, e o índice de `function CandidatoOpcao` é MAIOR que o de `function PropostaDaPosicao` e MENOR que o de `function useOpcoesPropostas`.
    - `grep -c "<PropostaLastreada" web/src/App.jsx` == 2 (guardião da Fase 18 preservado).
    - `grep -c "const aceitarCandidato = async (p) =>" web/src/App.jsx` == 1 e `grep -c "const onAbrirLastreada = async () =>" web/src/App.jsx` == 1 (só o de AtivoCard sobrou).
    - `grep -c "store.optionsGate(" web/src/App.jsx` == 2 e `grep -c "store.optionsProposta(" web/src/App.jsx` == 2 (nenhuma busca nova).
    - `git diff web/src/copy.js` sai VAZIO (zero string de copy nova, conforme 19-CONTEXT.md).
    - `git diff web/src/persistence.js web/src/api.js` sai VAZIO (nenhuma função de store nova).
    - `grep -n "T.accent" web/src/App.jsx` não mostra nenhuma ocorrência na mesma linha que `manchete`.
    - `grep -c "typeof v === \"number\"" web/src/App.jsx` >= 2 (o helper null-safe existe também em `CandidatoOpcao`).
    - `bash scripts/executar.sh --testes` sai com código 0 — ou, se falhar, falha SOMENTE em `test_opcoes_collar_ui.mjs` (o guardião que a Task 2 atualiza), com o nome do teste registrado no SUMMARY.
  </acceptance_criteria>
  <done>`CandidatoOpcao` existe e é renderizado em linha horizontal quando há mais de um candidato; um candidato só continua caindo no card de hoje; build do Vite verde.</done>
</task>

<task type="auto">
  <name>Task 2: guardiões estáticos do multi-candidato + atualização do guardião de collar</name>
  <read_first>
    - web/tests/test_opcoes_collar_ui.mjs (arquivo inteiro — helper `ok`, `extrairBalanceado`, e a seção :78-115 que conta os handlers)
    - web/tests/test_carteira_opcoes_tira.mjs (padrão de âncora de função por `indexOf` e de fatia entre componentes)
    - web/src/App.jsx (estado depois da Task 1)
    - CLAUDE.md (guardrail "Guardiões de teste não se apagam — reversão deliberada atualiza o guardião com nota")
  </read_first>
  <files>web/tests/test_opcoes_multi_candidato_ui.mjs, web/tests/test_opcoes_collar_ui.mjs</files>
  <action>
**(A) Atualizar `web/tests/test_opcoes_collar_ui.mjs` (guardião da Fase 17 que a
Task 1 quebra deliberadamente).**
Hoje ele localiza handlers pela regex `/const onAbrirLastreada = async \(\) => \{/g`
e exige `idxs.length === 2`. Depois da Task 1 existe UM `onAbrirLastreada`
(AtivoCard) e UM `aceitarCandidato(p)` (PropostaDaPosicao). Atualizar, não
apagar:
- passar a coletar os índices das DUAS formas — `const onAbrirLastreada = async () => {`
  e `const aceitarCandidato = async (p) => {` — e continuar exigindo TOTAL de 2
  handlers;
- manter INTACTAS as asserções por handler: corpo não vazio,
  `window.confirm(cp.confirmAbrirCollar(` exatamente 1 vez, `A.abrirCollar(`
  presente, e confirmação ANTES da execução;
- acrescentar nota de cabeçalho na seção citando "Fase 19, MULTI-02": o handler
  do detalhe de posição passou a ser parametrizado pelo candidato clicado
  (`aceitarCandidato(p)`) porque agora há N candidatos por posição; a regra
  protegida — confirmar antes de executar, em TODO caminho de aceite — não
  mudou.

**(B) Criar `web/tests/test_opcoes_multi_candidato_ui.mjs`.**
Mesmo estilo dos vizinhos: leitura estática com `readFileSync`, helper `ok(nome,
cond)`, `process.exit(1)` no final se houver falha, âncoras por `indexOf` e
fatias entre funções. Descoberto automaticamente pelo laço
`for t in web/tests/*.mjs` de `scripts/executar.sh` — nenhuma lista para
registrar. Cabeçalho de comentário explicando o que cada bloco protege.
Asserções mínimas:
1. `function CandidatoOpcao(` existe e está entre `function PropostaDaPosicao` e
   `function useOpcoesPropostas` no arquivo.
2. Na fatia de `CandidatoOpcao`: `{p.manchete}` renderizado direto (guardrail
   CVM) e NENHUMA concatenação de manchete — asserção negativa contra
   `p.manchete +` e `+ p.manchete`.
3. Na fatia de `CandidatoOpcao`: a cor da manchete usa `T.positive`/`T.negative`
   e a fatia NÃO contém `T.accent` na mesma linha em que aparece `p.manchete`.
4. Na fatia de `CandidatoOpcao`: existe o helper com `typeof v === "number"`
   antes de multiplicar por `qtyAcoes`, e NÃO existe
   `est.ganho_maximo *` nem `est.perda_maxima *` (null-nunca-zero na UI).
5. Na fatia de `CandidatoOpcao`: a linha que renderiza `breakevens` não contém
   `qtyAcoes` (breakeven é preço do ativo, não valor do lote).
6. Na fatia de `CandidatoOpcao`: o botão de CTA está condicionado a `operador` e
   a frase didática a `!operador` (Modo Estudo nunca recebe botão de executar).
7. Na fatia de `CandidatoOpcao`: `disabled={busy || degradado}` presente — o
   busy compartilhado da posição desabilita todos os irmãos.
8. Na fatia de `PropostaDaPosicao`: existe `candidatos.length > 1` (ou a
   variável derivada equivalente) governando o ramo novo, e o ramo de um
   candidato só continua renderizando `<PropostaLastreada`.
9. `<PropostaLastreada` aparece exatamente 2x no fonte inteiro (repete o
   guardião da Fase 18 aqui de propósito: é a regra que impede o ramo multi de
   se transformar num terceiro ponto de uso).
10. `onAceitar={aceitarCandidato}` (ou a passagem equivalente do MESMO handler)
    aparece no render de `CandidatoOpcao` — os candidatos compartilham um único
    caminho de aceite, não um por componente.
11. Nenhuma chave de copy nova: para cada `cp.X` referenciado dentro da fatia de
    `CandidatoOpcao`, `X` existe em `COPY.estudo` e em `COPY.operador`
    (importar `COPY` de `../src/copy.js`, como os vizinhos já fazem).
12. Nenhuma composição de frase proibida no fonte inteiro: `"Vender " +` ausente
    e `"Se você tivesse"` ausente (repete os guardiões globais da Fase 14).

Rodar a suíte canônica ao final. Se algum outro `.mjs` quebrar, aplicar o mesmo
procedimento de atualização com nota e registrar no SUMMARY.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `node web/tests/test_opcoes_multi_candidato_ui.mjs` sai com código 0 e imprime "todos os testes passaram".
    - `node web/tests/test_opcoes_collar_ui.mjs` sai com código 0.
    - `node web/tests/test_carteira_opcoes_tira.mjs` sai com código 0 (guardiões da Fase 18 intactos).
    - `node web/tests/test_opcoes_proposta_ui.mjs` sai com código 0 (`PropostaLastreada` não foi tocada).
    - `git diff web/tests/test_opcoes_collar_ui.mjs` não mostra nenhuma chamada `ok(` removida — só linhas adicionadas/ajustadas, e o arquivo contém a string `Fase 19`.
    - `grep -c "ok(" web/tests/test_opcoes_multi_candidato_ui.mjs` >= 12.
    - `bash scripts/executar.sh --testes` sai com código 0 (as DUAS suítes).
  </acceptance_criteria>
  <done>Guardião de collar da Fase 17 atualizado com nota (não apagado); arquivo novo de guardiões do multi-candidato verde; suíte canônica verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resposta da API → render do candidato | Dado do próprio backend; o risco é a UI REESCREVER a afirmação do motor (manchete) ou apresentar número não aplicável como zero. |
| clique do usuário → `A.abrirLastreada`/`A.abrirCollar` | Cruza para a rota de escrita; a UI é defesa em profundidade, o servidor recusa igual (Plano 19-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-11 | Tampering | manchete de cada candidato | mitigate | `{p.manchete}` renderizado direto, nunca concatenado nem truncado; guardiões 2 e 12 do arquivo novo travam isso. Guardrail CVM do CLAUDE.md — a IA/UI explica, nunca substitui a manchete do motor. |
| T-19-12 | Information disclosure | payoff com campo não aplicável | mitigate | Helper `typeof v === "number"` antes de multiplicar por `qtyAcoes` (em JS `null * 100 === 0`); guardião 4 trava. Mostrar "R$ 0,00" onde o dado não existe seria inventar valor (CLAUDE.md princípio 4). |
| T-19-13 | Elevation of privilege | Modo Estudo executando ordem | mitigate | CTA de candidato só existe sob `operador`, frase didática sob `!operador` (guardião 6). Defesa em UI; o servidor já devolve 403 em Modo Estudo independentemente (main.py :2450, :2533). |
| T-19-14 | Tampering | aceite duplo por clique rápido em candidatos irmãos | mitigate | `busy` único por posição desabilita TODOS os CTAs enquanto a ordem está em voo (guardião 7); e mesmo furando a UI, o servidor rejeita o irmão (Plano 19-02, T-19-07). |
| T-19-15 | Repudiation | erro de aceite silencioso | mitigate | Reusa o `flash("Abrir operação lastreada: "/"Montar collar: " + e.message)` já existente; nenhuma mensagem inline nova (19-UI-SPEC.md Decisão de Interação 1). |
| T-19-16 | Denial of service | fan-out de busca por candidato | mitigate | Nenhuma busca nova: `store.optionsGate(`/`store.optionsProposta(` continuam em exatamente 2 ocorrências cada (critério de aceite da Task 1). O payload só ficou maior. |
| T-19-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote novo — `git diff web/package.json web/package-lock.json` deve sair vazio; portão de legitimidade não se aplica por ausência de instalação. |
</threat_model>

<verification>
- `cd web && npx vite build` verde (obrigatório: teste estático não pega erro de sintaxe JSX).
- `bash scripts/executar.sh --testes` verde (as duas suítes).
- `git diff web/src/copy.js web/src/persistence.js web/src/api.js` vazio.
- Nenhum guardião apagado; os atualizados carregam nota citando a Fase 19.
</verification>

<success_criteria>
- Posição com dois candidatos mostra os dois lado a lado, cada um com manchete, payoff e CTA próprios.
- Posição com um candidato renderiza exatamente o card de hoje.
- Zero string de copy nova, zero função de store nova, zero busca nova.
</success_criteria>

<output>
Create `.planning/phases/19-motor-multi-candidato/19-03-SUMMARY.md` when done
</output>
