---
phase: 19-motor-multi-candidato
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - server/app/main.py
  - server/tests/test_opcoes_collar_rota.py
autonomous: true
requirements: [MULTI-01, MULTI-02]

must_haves:
  truths:
    - "A resposta de `GET /api/options/proposta/{ticker}` carrega a lista de candidatos"
    - "`POST /api/options/lastreada/abrir-collar` aceita um collar que NÃO é o candidato primário da lista"
    - "O cross-check contrato a contrato continua valendo para o candidato collar encontrado — nenhum corpo passa só por declarar tipo collar"
    - "Aceitar um candidato impede a execução do candidato irmão sobre a MESMA posição, nas duas ordens possíveis"
    - "A rota `POST /api/options/lastreada/abrir` (single-leg) segue sem nenhuma mudança"
  artifacts:
    - path: "server/app/main.py"
      provides: "candidatos na resposta de proposta e busca por tipo na rota de collar"
      contains: "candidatos"
    - path: "server/tests/test_opcoes_collar_rota.py"
      provides: "guardiões de aceite de candidato não-primário e de rejeição do irmão"
      contains: "candidatos"
  key_links:
    - from: "server/app/main.py:options_lastreada_abrir_collar"
      to: "opcoes_lastreadas.propor(..., multiperna=True)['candidatos']"
      via: "busca do candidato com tipo == collar, seguida do cross-check de contratos/pernas"
      pattern: "candidatos"
    - from: "server/app/main.py:options_proposta"
      to: "cliente web"
      via: "chave aditiva candidatos na resposta JSON"
      pattern: "\"candidatos\""
---

<objective>
As duas rotas de opções passam a falar a língua de N candidatos: a de LEITURA
expõe a lista, a de ESCRITA do collar procura o candidato certo dentro dela em
vez de assumir que o collar é o resultado primário.

Purpose: sem este plano, o motor da 19-01 fica invisível para o cliente E a
rota `abrir-collar` REGRIDE — hoje ela checa `motivo != "collar"`, e com
put_protecao e collar coexistindo o `motivo` passa a ser `put_protecao`, o que
faria a rota rejeitar com 409 um aceite legítimo de collar (19-CONTEXT.md).
Este plano também fecha a lacuna real de MULTI-02 critério 3 na ordem
put→collar (ver `<achado_de_planejamento>`).

Output: `candidatos` na resposta de proposta; busca por tipo na rota de collar
com o cross-check integral preservado; precedência de posição lastreada já
aberta honrada na rota de escrita; guardiões novos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-motor-multi-candidato/19-CONTEXT.md
@.planning/phases/19-motor-multi-candidato/19-PATTERNS.md
@.planning/phases/19-motor-multi-candidato/19-01-SUMMARY.md
@CLAUDE.md

<interfaces>
Contrato entregue pelo Plano 19-01 (não reabrir `opcoes_lastreadas.py` para
descobri-lo):

- Retorno POSITIVO de `propor()`: `{"proposta": <dict>, "motivo": <str>,
  "candidatos": [<dict>, ...]}`; `proposta` é `candidatos[0]`; ordem
  `["put_protecao", "collar"]` quando os dois cabem.
- Retorno NEGATIVO: `{"proposta": None, "motivo": <str>}` — SEM a chave
  `candidatos`. Por isso todo leitor usa `resultado.get("candidatos", [])`,
  nunca `resultado["candidatos"]`.
- `opcoes_lastreadas.proposta_fechar()` (ramo de posição já aberta em
  `options_proposta`) é OUTRA função e NUNCA devolve `candidatos` — o default
  do `.get()` é o que impede `KeyError` nesse ramo.

Cada dict de candidato tem `tipo`, `contratos`, `expiration` e, no caso do
collar, `pernasContratos` = lista de 2 dicts com
`contractSymbol`/`optionType`/`lado`/`strike`/`premioUnitario`.

Estado de referência de `server/app/main.py` (numeração de ANTES deste plano):
- `GET /api/options/proposta/{ticker}` — :2348-2440; dict de resposta em
  :2432-2440.
- `POST /api/options/lastreada/abrir` — :2443-2504. NÃO MUDA. Confirmado por
  leitura: valida `contractSymbol` direto contra a cadeia ao vivo e nunca chama
  `propor()`.
- `POST /api/options/lastreada/abrir-collar` — :2506-2640; validação de corpo em
  :2536-2550; re-derivação em :2552-2581; a checagem que quebra em :2583-2586;
  cross-check em :2588-2603; execução em :2605-2640.
</interfaces>

<achado_de_planejamento>
**A garantia "aceitar um não deixa aceitar o outro" NÃO é totalmente estrutural
— 19-CONTEXT.md supôs que sim e a leitura do código mostra que só vale numa das
duas ordens.** Verificado em `server/app/store.py`:

- **collar primeiro, put_protecao depois:** JÁ bloqueado. `abrir_collar`
  (:1242) incrementa `qtyTravada` em `contratos*100`; a tentativa seguinte de
  `comprar_put_protecao` bate na guarda de lastro (:1066-1072) e levanta
  `ValueError("Lastro insuficiente em ... para proteger N contrato(s).")` → 400.
  Nenhuma trava nova é necessária aqui, exatamente como 19-CONTEXT.md previu.
- **put_protecao primeiro, collar depois:** NÃO bloqueado hoje.
  `comprar_put_protecao` NUNCA escreve `qtyTravada` (é decisão de produto
  documentada em :1023-1026 — a put protege o downside e não compromete o lote).
  O lastro continua livre, o caixa costuma continuar suficiente, e
  `abrir_collar` executaria — o usuário terminaria com as DUAS estruturas
  concorrentes abertas sobre a mesma posição, violando MULTI-02 critério 3.

**Correção mínima adotada, sem inventar regra nova:** a rota de LEITURA
`options_proposta` já trata esse estado — quando existe posição lastreada aberta
no `underlying` (`pos_op_aberta`, :2382-2383), ela nunca oferece proposta nova,
só a de FECHAMENTO. A rota `abrir-collar` hoje replica só o "ramo B" desse
pipeline. Este plano faz a rota de ESCRITA honrar TAMBÉM a precondição do ramo
A. Não é uma trava nova: é a rota de escrita parando de contradizer a regra que
a rota de leitura já aplica desde a Fase 14.
</achado_de_planejamento>
</context>

<tasks>

<task type="auto">
  <name>Task 1: as duas rotas passam a falar a língua de N candidatos</name>
  <read_first>
    - server/app/main.py:2348-2440 (`options_proposta`, os dois ramos que produzem `resultado` e o dict de resposta)
    - server/app/main.py:2443-2504 (`options_lastreada_abrir` — ler para CONFIRMAR que nada muda aqui; não editar)
    - server/app/main.py:2506-2640 (`options_lastreada_abrir_collar` inteira)
    - server/app/opcoes_lastreadas.py (estado depois do Plano 19-01)
    - server/app/store.py:1021-1107 (`comprar_put_protecao` — a guarda de lastro que NÃO trava `qtyTravada`) e :1109-1253 (`abrir_collar` — a guarda que trava)
    - .planning/phases/19-motor-multi-candidato/19-PATTERNS.md (seções das duas rotas)
  </read_first>
  <files>server/app/main.py</files>
  <action>
Três edições cirúrgicas. Nada mais em `main.py` é tocado.

**(A) `GET /api/options/proposta/{ticker}` — chave aditiva.**
No dict de resposta (hoje :2432-2440), acrescentar
`"candidatos": resultado.get("candidatos", [])`. Usar `.get()` com default `[]`
OBRIGATORIAMENTE: o ramo `pos_op_aberta` chama `proposta_fechar()`, que não
devolve essa chave, e o `except Exception` da rota monta um dict de dois campos
— `resultado["candidatos"]` estouraria `KeyError` e transformaria uma leitura
degradada em 500. Comentário curto citando "Fase 19, MULTI-01" explicando que
o default cobre os ramos de fechamento e de degradação, não que ele mascara
falha de dado de mercado.

**(B) `POST /api/options/lastreada/abrir-collar` — precedência de posição
lastreada já aberta.**
Depois da validação de corpo (hoje até :2550) e ANTES do bloco de re-derivação
(hoje :2552), inserir: ler `store.get(_conn, "optionPositions", user_id=scope)`
e procurar uma posição com `p.get("underlying") == underlying` e `p.get("lastro")`
(mesmo predicado de `options_proposta` :2382-2383); existindo, levantar
`HTTPException(409, "Já existe uma operação lastreada aberta em {underlying} — "
"feche-a antes de montar outra estrutura.")` com o `underlying` interpolado.
Comentário obrigatório explicando o PORQUÊ, na disciplina da casa: a rota de
leitura já decide que uma posição com lastreada aberta recebe proposta de
FECHAMENTO e nunca uma proposta nova (`pos_op_aberta`, ramo A); a rota de
escrita replicava só o ramo B e, com MULTI-01, isso deixaria a segunda
estrutura concorrente executar sobre a mesma posição (put comprada não trava
`qtyTravada`, `store.py:1023-1026`). Citar "Fase 19, MULTI-02, critério 3".
Colocar ANTES da re-derivação de propósito: é precondição de estado, não
desconfiança do corpo, e evita um fetch de cadeia inútil.
O texto do 409 é mensagem técnica de validação; não contém nenhuma das
frases-âncora de manchete ("trava protetora"/"abate o custo") vigiadas por
`server/tests/test_opcoes_collar_vocab.py`.

**(C) `POST /api/options/lastreada/abrir-collar` — busca do candidato collar.**
Substituir a checagem de hoje (:2583-2586,
`if resultado.get("motivo") != "collar" or not resultado.get("proposta")`) por
uma busca dentro da lista: `p` passa a ser o PRIMEIRO elemento de
`resultado.get("candidatos") or []` cujo `tipo` é `"collar"`; não havendo,
levantar o MESMO `HTTPException(409, "O collar não está mais disponível — o
servidor recalculou a proposta e o resultado mudou.")` de hoje, com o texto
inalterado (é a mensagem que a UI já sabe exibir).
TUDO que vem depois permanece LITERALMENTE inalterado: o cross-check de
`contratos` (:2590-2591), o cross-check de `pernasContratos` por
`{contractSymbol: lado}` (:2592-2595), a localização de perna call/put por
`optionType` (:2600-2603), a busca dos contratos na cadeia, a montagem de
`contract_call`/`contract_put`, os prêmios vindos da proposta re-derivada e a
chamada a `store.abrir_collar`. O cross-check continua rodando contra o
candidato ENCONTRADO — em nenhuma hipótese "tem candidato do tipo collar" pode
virar aprovação sozinha.
Também permanece intocado o bloco de re-derivação (:2552-2581) com
`multiperna=True` FIXO, nunca lido do corpo — guardião
`test_rota_de_collar_nao_le_multiperna_do_corpo`.

Não alterar `POST /api/options/lastreada/abrir`. Não alterar `SERVER_BUILD_ID`
(o Plano 19-04 publica).
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/test_opcoes_collar_rota.py tests/test_opcoes_lastreadas_rotas.py tests/test_opcoes_collar_vocab.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"candidatos": resultado.get("candidatos", \[\])' server/app/main.py` retorna exatamente 1 linha.
    - `grep -c 'resultado\["candidatos"\]' server/app/main.py` retorna 0 (nunca indexação direta).
    - `grep -n 'resultado.get("motivo") != "collar"' server/app/main.py` retorna 0 linhas (a checagem antiga saiu).
    - `grep -n 'O collar não está mais disponível' server/app/main.py` continua retornando ao menos 1 linha (o texto do 409 foi reusado, não reescrito).
    - As quatro asserções de cross-check seguem presentes na rota: `grep -c 'não conferem com a proposta recalculada pelo servidor' server/app/main.py` >= 3.
    - `grep -c 'multiperna=True' server/app/main.py` >= 1 e `grep -c 'body.get("multiperna"' server/app/main.py` == 0.
    - `git diff server/app/main.py` NÃO mostra nenhuma linha alterada dentro de `options_lastreada_abrir` (rota single-leg).
    - `grep -n 'Já existe uma operação lastreada aberta' server/app/main.py` retorna 1 linha, e ela está ANTES da linha que contém `opcoes_lastreadas.propor(` dentro de `options_lastreada_abrir_collar`.
    - `cd server && .venv/bin/python -m pytest tests/test_opcoes_collar_vocab.py -q` passa (nenhuma frase-âncora de manchete vazou para código).
  </acceptance_criteria>
  <done>Proposta expõe `candidatos`; a rota de collar acha o candidato por tipo com o cross-check integral preservado e recusa aceite sobre posição que já tem lastreada aberta; rota single-leg intocada.</done>
</task>

<task type="auto">
  <name>Task 2: guardião existente que quebra + aceite de candidato não-primário</name>
  <read_first>
    - server/tests/test_opcoes_collar_rota.py:1-140 (fixtures `_expiracao_fixa`, `_snapshot_sem_setup`, `_plano_vender`, helpers `_pernas_collar_da_cadeia`, `_contratos_collar_da_cadeia`, `_seed_cenario_collar`, `_novo_escopo`, `_liga_operador`, `_seed_posicao`)
    - server/tests/test_opcoes_collar_rota.py:192-240 (`test_collar_aceito_com_proposta_fresca_executa_as_duas_pernas` e `test_collar_indisponivel_agora_devolve_409_sem_efeito_colateral`)
    - server/app/main.py (estado depois da Task 1)
    - CLAUDE.md (guardrail "Guardiões de teste não se apagam — reversão deliberada atualiza o guardião com nota")
  </read_first>
  <files>server/tests/test_opcoes_collar_rota.py</files>
  <action>
**(A) Guardião que a MULTI-01 quebra deliberadamente:
`test_collar_indisponivel_agora_devolve_409_sem_efeito_colateral` (hoje :225-247).**
O cenário dele é "caixa folgado, então a put isolada cabe e o collar NÃO é
proposto" — premissa que MULTI-01 revoga: com caixa folgado E call líquida na
cadeia mock, o collar passa a ser o SEGUNDO candidato. Rodar o teste primeiro e
confirmar a quebra real antes de mexer.
Atualizar, não apagar, seguindo o guardrail do repositório:
- manter o nome e a asserção `p_check["motivo"] == "put_protecao"` (continua
  verdadeira e agora prova algo mais forte: o primário é a put mesmo com o
  collar disponível);
- o que muda é o motivo do 409: o corpo envia `contratos: 1` enquanto o
  candidato collar re-derivado tem o número de contratos que o lastro sustenta,
  então a rejeição passa a vir do cross-check de contratos, com a mensagem "não
  conferem com a proposta recalculada pelo servidor". Ajustar a asserção de
  `detail` para esse texto e manter INTACTAS as asserções de ausência de efeito
  colateral (caixa e `optionPositions` inalterados);
- acrescentar nota na docstring citando "Fase 19, MULTI-01": o cenário original
  ("collar deixou de existir") não é mais alcançável por caixa folgado, o
  guardião foi reapontado para o cross-check, e a não-execução continua sendo
  exatamente o que ele protege.
Se OUTRO teste do arquivo quebrar, aplicar o mesmo procedimento: entender,
atualizar com nota, nunca apagar; registrar cada um no SUMMARY.

**(B) Teste NOVO: aceite do collar quando ele é `candidatos[1]`.**
`test_collar_aceito_mesmo_nao_sendo_o_candidato_primario` — seguir o padrão de
`test_collar_aceito_com_proposta_fresca_executa_as_duas_pernas`:
- escopo novo, `_liga_operador`, `_seed_posicao(uid, qty=300)`, caixa FOLGADO
  (ex.: `100 * premio_put + 10_000`) para que os DOIS candidatos caibam;
- `GET /api/options/proposta/PETR4?multiperna=1` e assertar
  `[c["tipo"] for c in body["candidatos"]] == ["put_protecao", "collar"]` e
  `body["motivo"] == "put_protecao"`;
- montar o corpo a partir do candidato de índice 1 (`contratos` e
  `pernasContratos` DELE, só `contractSymbol` + `lado` por perna) e postar em
  `/api/options/lastreada/abrir-collar`;
- assertar 200, `len(optionPositions) == 2`, os dois `contractSymbol` com os
  lados certos (`vendida`/`comprada`) e `qtyTravada == contratos * 100` na
  posição de PETR4 (travado UMA vez).

**(C) Teste NOVO de anti-regressão do cross-check:**
`test_collar_com_pernas_de_outro_candidato_nao_passa_so_por_existir_collar` —
mesmo cenário de (B), mas trocando um `contractSymbol` por outro contrato real
da cadeia (helper `_outro_call`). Esperado: 409 com "não conferem com a proposta
recalculada pelo servidor", caixa e `optionPositions` inalterados. Este é o
guardião de que a busca por `tipo == "collar"` NÃO afrouxou a defesa: achar o
candidato é condição necessária, nunca suficiente.
  </action>
  <verify>
    <automated>cd server && .venv/bin/python -m pytest tests/test_opcoes_collar_rota.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && .venv/bin/python -m pytest tests/test_opcoes_collar_rota.py -q` passa com pelo menos 2 testes a mais que antes do plano.
    - `cd server && .venv/bin/python -m pytest tests/test_opcoes_collar_rota.py -q -k "nao_sendo_o_candidato_primario"` passa.
    - `git diff server/tests/test_opcoes_collar_rota.py` não mostra nenhuma função `def test_` removida.
    - A docstring de `test_collar_indisponivel_agora_devolve_409_sem_efeito_colateral` contém a string `Fase 19` (nota de atualização deliberada do guardião).
    - `grep -c 'candidatos' server/tests/test_opcoes_collar_rota.py` >= 4.
  </acceptance_criteria>
  <done>Guardião quebrado atualizado com nota (não apagado); aceite de candidato não-primário e integridade do cross-check provados por teste de rota.</done>
</task>

<task type="auto">
  <name>Task 3: guardião de exclusão mútua entre candidatos irmãos</name>
  <read_first>
    - server/tests/test_opcoes_collar_rota.py (estado depois da Task 2 — fixtures e helpers a reusar)
    - server/app/store.py:1021-1107 (`comprar_put_protecao`: guarda de lastro e mensagem de `ValueError`; NÃO escreve `qtyTravada`)
    - server/app/store.py:1109-1253 (`abrir_collar`: guarda de lastro, tudo-ou-nada sob `ORDER_LOCK`, incremento de `qtyTravada`)
    - server/app/main.py:2443-2504 (rota `/abrir`, que converte `ValueError` em 400)
    - server/app/main.py (a precondição de posição lastreada aberta adicionada na Task 1)
    - .planning/phases/19-motor-multi-candidato/19-CONTEXT.md (guarda "aceitar um não deixa aceitar outro", MULTI-02 critério 3)
  </read_first>
  <files>server/tests/test_opcoes_collar_rota.py</files>
  <action>
Acrescentar uma seção nova ao final do arquivo, com cabeçalho de comentário
citando "Fase 19, MULTI-02, critério 3" e explicando a assimetria descoberta no
planejamento: a call vendida trava `qtyTravada`, a put comprada não — por isso as
duas ordens precisam de guardião próprio, e são mecanismos DIFERENTES que as
bloqueiam.

**Teste 1 — `test_aceitar_collar_impede_aceitar_a_put_irma_por_lastro`:**
- escopo novo em Modo Operador, posição de 300 ações, caixa folgado (os DOIS
  candidatos cabem);
- `GET /api/options/proposta/PETR4?multiperna=1`, guardar `candidatos[0]`
  (put_protecao) e `candidatos[1]` (collar) e assertar essa ordem;
- aceitar o collar (`POST .../abrir-collar` com as pernas do candidato 1) →
  esperar 200;
- tentar aceitar a put irmã (`POST .../abrir` com `contractSymbol`,
  `expiration` e `contratos` do candidato 0) → esperar status 400 e `detail`
  contendo "Lastro insuficiente" (mensagem que `comprar_put_protecao` já produz
  hoje, `store.py:1072` — não inventar texto novo);
- assertar que a carteira NÃO ganhou uma terceira posição de opção:
  `len(optionPositions) == 2` depois da segunda tentativa.

**Teste 2 — `test_aceitar_a_put_impede_aceitar_o_collar_irmao_por_precondicao`:**
- mesmo preparo (ordem inversa de aceite);
- aceitar a put (`POST .../abrir` com o candidato 0) → 200;
- tentar aceitar o collar irmão (`POST .../abrir-collar` com as pernas do
  candidato 1) → esperar 409 com `detail` contendo "Já existe uma operação
  lastreada aberta";
- assertar ausência de efeito colateral: `optionPositions` continua com UMA
  posição (a put) e `qtyTravada` da posição de PETR4 continua 0 (nenhuma perna
  do collar entrou).

**Teste 3 — `test_guarda_do_irmao_nao_e_trava_nova_no_store`:**
guardião estático de disciplina, no mesmo estilo de
`test_rota_de_collar_nao_le_multiperna_do_corpo` (usa `inspect.getsource`):
ler o fonte de `store.abrir_collar` e `store.comprar_put_protecao` e assertar
que NENHUMA das duas ganhou lock ou flag novos nesta fase — nenhuma ocorrência
de `threading.Lock`/`threading.RLock` além do `ORDER_LOCK` já existente, e
nenhuma string `candidato` no fonte delas. A exclusão mútua vem da validação de
lastro de sempre (uma ordem) e da precondição de rota (a outra), não de estado
novo no motor de carteira.

Rodar a suíte canônica completa ao final. Nenhum teste apagado.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && .venv/bin/python -m pytest tests/test_opcoes_collar_rota.py -q -k "impede_aceitar"` passa com 2 testes.
    - `cd server && .venv/bin/python -m pytest tests/test_opcoes_collar_rota.py -q -k "nao_e_trava_nova_no_store"` passa.
    - `git diff server/app/store.py` sai VAZIO (nenhuma trava nova foi adicionada ao motor de carteira nesta fase).
    - `bash scripts/executar.sh --testes` sai com código 0 (as DUAS suítes).
    - `git diff --stat` desta fase até aqui lista apenas `server/app/opcoes_lastreadas.py`, `server/app/main.py`, `server/tests/test_opcoes_collar.py` e `server/tests/test_opcoes_collar_rota.py`.
  </acceptance_criteria>
  <done>As duas ordens de aceite provadas por teste de ponta a ponta, cada uma com o mecanismo de rejeição que já existia; `store.py` intocado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → `POST /api/options/lastreada/abrir-collar` | Corpo controlado pelo cliente cruza para uma rota de ESCRITA que abre duas pernas reais na carteira simulada. |
| cliente → `GET /api/options/proposta/{ticker}` | Rota de leitura; o risco é vazar `KeyError`/500 em ramo degradado, não escrita. |
| rota → `store.abrir_collar` | Fronteira de execução: validação sob `ORDER_LOCK`, tudo-ou-nada. |
| rota → provedor de cadeia (Yahoo/brapi) | Dado externo; indisponibilidade vira 502 explícito nesta rota (nunca `degradado` silencioso, por ser escrita). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Tampering | busca do candidato collar em `abrir-collar` | mitigate | A busca por `tipo == "collar"` é condição NECESSÁRIA, nunca suficiente: o cross-check de `contratos` e o cross-check exato de `{contractSymbol: lado}` das duas pernas (main.py :2590-2595) continuam rodando contra o candidato ENCONTRADO. Guardião novo (Task 2C) prova que perna trocada segue dando 409. |
| T-19-06 | Tampering | corpo forjado declarando um collar que o servidor não propôs | mitigate | Nada do corpo é usado na execução: contratos, contratos-símbolo e prêmios vêm sempre da proposta RE-DERIVADA com `multiperna=True` fixo. Guardiões existentes `test_rota_de_collar_nao_le_multiperna_do_corpo` e `test_rota_de_collar_nao_usa_premio_do_corpo` permanecem no arquivo e devem continuar passando. |
| T-19-07 | Elevation of privilege | dupla execução de candidatos concorrentes sobre a mesma posição | mitigate | Duas defesas independentes, nenhuma inventada nesta fase: (a) collar→put bloqueado pela guarda de lastro de `comprar_put_protecao` sob `ORDER_LOCK`; (b) put→collar bloqueado pela precondição de posição lastreada já aberta, que a rota de leitura já aplica desde a Fase 14. Task 3 prova as duas ordens. |
| T-19-08 | Denial of service | `KeyError` em `candidatos` no ramo de fechamento/degradação de `options_proposta` | mitigate | `.get("candidatos", [])` obrigatório; critério de aceite proíbe indexação direta. Sem isso, uma posição lastreada aberta transformaria uma leitura normal em 500. |
| T-19-09 | Repudiation | rejeição sem rastro | accept | As rejeições reusam `registrar_rejeicao` já embutido em `store.py`; o 409 de precondição é anterior a qualquer escrita e não precisa de registro de ordem — não há ordem tentada no motor. |
| T-19-10 | Information disclosure | mensagem do 409 de precondição | accept | Revela apenas que existe posição lastreada no ticker do próprio escopo autenticado (`current_scope`); nenhum dado de outro usuário. |
| T-19-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote novo. `git diff server/requirements.txt server/requirements-prod.txt web/package.json` deve sair vazio; portão de legitimidade não se aplica por ausência de instalação. |
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` verde.
- `git diff server/app/store.py` vazio.
- Rota `/abrir` (single-leg) sem diff.
- `SERVER_BUILD_ID` não alterado à mão (o Plano 19-04 publica e sincroniza).
</verification>

<success_criteria>
- `GET /api/options/proposta/{ticker}` devolve `candidatos` nos três ramos sem 500.
- `abrir-collar` aceita collar não-primário e recusa perna trocada.
- As duas ordens de aceite de candidatos irmãos terminam em rejeição, com as mensagens que já existiam.
</success_criteria>

<output>
Create `.planning/phases/19-motor-multi-candidato/19-02-SUMMARY.md` when done
</output>
