---
phase: 26-otimizacao-ux-ia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/conceitos.py
  - server/app/main.py
  - server/app/metering.py
  - web/src/App.jsx
  - web/src/copy.js
  - server/tests/test_pet_todas_telas.py
  - .claude/skills/didatica-boris/SKILL.md
  - web/tests/test_pet_opcoes.mjs
  - web/tests/test_vocabulario_opcoes.mjs
  - web/tests/test_tour_opcoes.mjs
autonomous: true
requirements: [Fase A do 26-CONTEXT — sete itens independentes, barato e alto impacto]
---

<objective>
Fase A do `26-CONTEXT.md` (briefing original: `26-BRIEFING-ORIGINAL.md`). Sete
correções independentes entre si, todas verificadas arquivo:linha em
2026-09-12 à noite — os números do briefing original estavam desatualizados
(as Fases 24/25 acrescentaram centenas de linhas no mesmo dia); **use os
números do 26-CONTEXT.md, não os do briefing**.

Purpose: fechar lacunas de texto, ordem de validação e roteamento que custam
pouco e têm efeito imediato — a aba Opções invisível para o assistente de IA,
vocabulário de modo quebrado numa única aba, tour que ignora onde o usuário
está, uma frase regulatória divergente do próprio CLAUDE.md, um caminho de
ajuda morto, e documentação descrevendo comportamento que não existe mais.

**Sete tasks, uma por item.** Rode sequencialmente — vários tocam os mesmos
arquivos (`App.jsx`, `main.py`), e este projeto não usa isolamento de
worktree (`workflow.use_worktrees: false`), então paralelismo real colidiria.
Commit atômico por item.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-otimizacao-ux-ia/26-CONTEXT.md
@.planning/phases/26-otimizacao-ux-ia/26-BRIEFING-ORIGINAL.md
@CLAUDE.md

<guardrails>
- **Reconfirme cada referência com `grep` antes de editar** — mesmo as do
  26-CONTEXT.md podem ter deslocado de novo se outra sessão tocou a árvore
  entre a verificação e a execução (já aconteceu duas vezes nesta sessão).
- Guardrails do produto, não re-litigar: manchete do card só do motor
  determinístico (guardrail CVM); stop/alvo nunca vetado; paridade
  `defaults.py`×`catalog.js` e `deviceStore`×`serverStore`; nenhum item
  aproxima a IA de calcular ou ordenar o Radar (ADR-017).
- **Guardiões de teste não se apagam** — `test_pet_todas_telas.py` trava a
  lista de telas por igualdade exata; atualize com nota datada, não contorne.
- Texto idêntico nos dois modos onde for estado de sistema/conta (não voz de
  professor/mesa) — mas A3 é o OPOSTO: é onde o vocabulário por modo estava
  FALTANDO, então aqui a diferença entre os dois blocos é o objetivo.
- **Não chamar os mutadores de estado do `gsd-sdk`** (`CLAUDE.md`). STATE à mão.
- Commit por heredoc com delimitador entre aspas — `-m "…"` com crases faz o
  shell executar o conteúdo (já aconteceu nesta sessão).
- **Não tocar** em `server/web_dist`, `server/admin_dist`, `web/src/version.js`
  nem na linha `SERVER_BUILD_ID`. Publicação é etapa separada, com OK humano.
- Baseline a não regredir: rode `bash scripts/executar.sh --testes` e anote o
  número exato ANTES de começar — a árvore pode ter mudado desde a última
  medição desta sessão (`2682 passed, 5 skipped, 3 xfailed` + 134 `.mjs` era o
  último número conhecido, mas confirme).
</guardrails>
</context>

<tasks>

<task type="auto">
  <name>A1 — Registrar a aba Opções no assistente de IA</name>
  <read_first>
    - server/app/conceitos.py (`PET_TELAS`, ~linha 555)
    - server/app/main.py — a rota `GET /api/pet/resumo` inteira (o `if tela not in PET_TELAS` e o padrão dos ramos `_pet_resumo_*`) e a rota `POST /api/assistente` (o `HTTPException(400, "Tela desconhecida.")`, ~linha 4020, e onde `kb.resolver` é chamado, ~4027)
    - web/src/App.jsx — `petSnapshot` (`useMemo`, ~linha 9338), o `switch (petTela)` inteiro, para copiar o formato de cada `case`
    - web/src/opcoes/OpcoesScreen.jsx — o que já está em estado (ticker, tese, cadeia/proposta) para saber o que expor
    - server/tests/test_pet_todas_telas.py (a lista exata que o guardião trava, ~linha 43)
  </read_first>
  <action>
Reconfirme por grep as linhas reais antes de editar (a árvore pode ter mudado
desde o 26-CONTEXT.md).

1. `PET_TELAS` ganha `"opcoes"`.
2. No backend, um ramo `_pet_resumo_opcoes` (ou o `if`/`elif` equivalente aos
   outros ramos de `/api/pet/resumo`) no mesmo padrão dos demais — mas **sem
   inventar dado**: se a rota não tem de onde ler ticker/tese (é estado só do
   front), devolva o que for determinístico do lado do servidor e deixe claro
   o que falta, no mesmo espírito do princípio 4 do CLAUDE.md.
3. No front, `case "opcoes"` em `petSnapshot`, no formato dos irmãos (`return
   {...}` com o que a tela já tem: ticker escolhido, tese, vencimentos
   considerados — leia o estado real de `OpcoesScreen`/`useOpcoesMcp` antes
   de inventar campo).
4. `test_pet_todas_telas.py`: atualizar a lista/allowlist com nota datada
   explicando que "opcoes" entrou (não apagar a trava de igualdade exata).
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/server && .venv/bin/python -m pytest tests/test_pet_todas_telas.py tests/test_pet.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `GET /api/pet/resumo?tela=opcoes` não cai no fallback de `"mercado"` — a resposta não é indistinguível da de Watchlist.
    - `POST /api/assistente` com `tela:"pet:opcoes"` não devolve 400 só por a tela não ser reconhecida.
    - `test_pet_todas_telas.py` continua travando a lista completa, com "opcoes" incluída e nota da correção.
  </acceptance_criteria>
  <done>A aba Opções deixa de ser invisível para o assistente de IA.</done>
</task>

<task type="auto">
  <name>A2 — KB responde antes da checagem de tela</name>
  <read_first>
    - server/app/main.py — a rota `POST /api/assistente` inteira, com a ordem atual: checagem de `tela in PET_TELAS` e a chamada a `kb.resolver`
  </read_first>
  <action>
Mover a chamada a `kb.resolver(pergunta, voc)` para ANTES da checagem
`tela not in PET_TELAS`. Uma pergunta que a KB (83 verbetes) resolve com
confiança sai sem gastar IA e sem depender de a tela estar cadastrada —
efeito imediato inclusive para "opcoes" antes da A1 existir (mas A1 já roda
antes desta task na mesma sessão, então o benefício composto já vale para as
duas).

Cuidado: não mude o CONTRATO do que `kb.resolver` devolve nem sua assinatura
— só a ORDEM em que é chamado.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/server && .venv/bin/python -m pytest tests/ -q -k "assistente or kb"</automated>
  </verify>
  <acceptance_criteria>
    - Pergunta coberta pela KB (ex.: "o que é RSI?"), enviada com uma `tela` que NÃO existe em `PET_TELAS`, responde pela KB em vez de 400.
    - Pergunta NÃO coberta pela KB, com tela inválida, continua recusada como antes (a inversão não abre uma porta de tela inválida passar sem checagem nenhuma quando a KB não resolve).
  </acceptance_criteria>
  <done>A base de conhecimento tem prioridade sobre a validação de tela.</done>
</task>

<task type="auto">
  <name>A3 — Vocabulário de modo na aba Opções</name>
  <read_first>
    - web/src/copy.js — os blocos `estudo` e `operador`, em especial `tituloWatchlist`/`subtituloWatchlist` (o padrão a espelhar) e `tabOpcoes` (que NÃO muda)
    - web/src/opcoes/OpcoesScreen.jsx — onde o texto fixo de título/subtítulo é renderizado hoje
  </read_first>
  <action>
NÃO mexer em `tabOpcoes` (rótulo curto da barra) — mudar isso alteraria a
barra em produção sem necessidade, e não é o que este item pede.

Criar `tituloOpcoes`/`subtituloOpcoes` em `copy.js`, nos dois blocos, com
textos DIFERENTES entre si (ao contrário da regra geral desta sessão de
"texto idêntico nos dois modos" — aqui o ponto é justamente restaurar a
diferença de voz que a aba não tinha). Estudo = registro de professor
(explica o que a aba mostra); Operador = registro de mesa (direto ao
estado). Ler essas chaves em `OpcoesScreen.jsx` no lugar do texto fixo atual.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/web && npx vite build && node tests/test_vocabulario_opcoes.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `COPY.estudo.tituloOpcoes !== COPY.operador.tituloOpcoes` (ou pelo menos o subtítulo difere, se o título curto fizer sentido igual).
    - `OpcoesScreen.jsx` lê as chaves novas, não texto fixo.
    - `npx vite build` verde.
  </acceptance_criteria>
  <done>A aba Opções fala a língua do modo, como o resto do produto.</done>
</task>

<task type="auto">
  <name>A4 — Tour cobre a tela atual e a aba Opções</name>
  <read_first>
    - web/src/App.jsx — `tourPassos(cp)` e `ajudaSecoes(cp, operador)` inteiras, e onde `tab`/`useState("evolucao")` define a tela inicial
  </read_first>
  <action>
`tourPassos`: acrescentar um passo ZERO nomeando a tela em que o usuário já
está (Acompanhar — não Radar), e um passo novo (quinto) apresentando
Opções. Mesma função, mesmo padrão de placeholders.

`ajudaSecoes`: acrescentar uma seção sobre Opções, no mesmo formato
título/parágrafos das sete existentes.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/web && npx vite build && node tests/test_tour_opcoes.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `tourPassos(cp).length` aumentou em 1 (o zero) + 1 (Opções) sobre o valor anterior medido no início desta task.
    - O primeiro passo nomeia "Acompanhar", não "Radar".
    - `ajudaSecoes` inclui uma entrada cujo título menciona Opções.
  </acceptance_criteria>
  <done>O tour e a ajuda refletem onde o app abre e o que ele tem hoje.</done>
</task>

<task type="auto">
  <name>A5 — Uma frase só para evidência insuficiente</name>
  <read_first>
    - web/src/App.jsx — o comentário que afirma "verbatim do CLAUDE.md" e o texto renderizado logo abaixo
    - CLAUDE.md (raiz do repo) — a frase canônica
    - server/app/skill_ref.py — `vocab["operador"]["sem_setup"]`/`vocab["educacional"]["sem_setup"]` (NÃO tocar — é frase diferente, para propósito diferente)
  </read_first>
  <action>
Reconfirme por grep os três textos antes de editar. Escolher a frase do
`CLAUDE.md` ("Não há dados suficientes para concluir.") como canônica —
é a norma declarada do produto. Corrigir o texto renderizado em `App.jsx`
para bater byte a byte, e corrigir o comentário que afirma "verbatim" para
não mentir mais (ou apagar a afirmação falsa se o comentário não precisar
mais dela).

**Não tocar** em `skill_ref.py` — a frase de lá é para ausência de SETUP, um
conceito diferente de ausência de DADOS/evidência, e já está correta.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/web && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - O texto renderizado em `App.jsx` é idêntico, byte a byte, ao do `CLAUDE.md`.
    - Nenhum comentário no repo afirma "verbatim" sobre um texto que não bate.
    - `grep -rn "Não há dados suficientes"` no repo mostra só DUAS variantes com propósitos claramente distintos: a de evidência (agora unificada) e a de ausência de setup em `skill_ref.py` (intocada).
  </acceptance_criteria>
  <done>Uma frase canônica para evidência insuficiente, sem alegação falsa no comentário.</done>
</task>

<task type="auto">
  <name>A6 — Corrigir o caminho que a mensagem de cota indica</name>
  <read_first>
    - server/app/metering.py — as três ocorrências de "Perfil → Conta & preferências"
    - web/src/App.jsx — o tile atual (`title="IA & Boris"`)
  </read_first>
  <action>
Atualizar as três strings em `metering.py` de "Perfil → Conta &
preferências" para "Perfil → IA & Boris" (o caminho real hoje). Item
isolado, sem dependência das outras tasks desta fase.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/borisv2/server && .venv/bin/python -m pytest tests/ -q -k "metering"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "Conta & preferências" server/app/metering.py` não retorna nada.
    - As três mensagens de 402 citam "IA & Boris".
  </acceptance_criteria>
  <done>A mensagem de cota esgotada aponta para onde o botão realmente está.</done>
</task>

<task type="auto">
  <name>A7 — SKILL.md da didática, texto morto</name>
  <read_first>
    - .claude/skills/didatica-boris/SKILL.md — o trecho sobre o pet/mascote
    - web/src/App.jsx — a linha que importa `Boris` de `./pet/Boris.jsx`, e o comentário sobre a reversão que tirou a restrição "só Watchlist do Estudo"
  </read_first>
  <action>
Corrigir dois pontos no SKILL.md: (1) o componente se chama `Boris`, não
"Coruja"; (2) ele aparece em qualquer aba e nos dois modos, não "só na
Watchlist do Estudo" — cite a reversão registrada no código, sem inventar
data se não estiver certo do dia exato.

Documentação, sem teste automatizado — revisão de texto.
  </action>
  <verify>
    <automated>grep -n "Coruja\|só na Watchlist" /Users/acamerini/dev/borisv2/.claude/skills/didatica-boris/SKILL.md || echo "limpo"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Coruja"` no arquivo é 0.
    - O texto não afirma mais restrição de aba/modo para o mascote.
  </acceptance_criteria>
  <done>A skill descreve o comportamento real do produto.</done>
</task>

</tasks>

<verification>
1. `bash scripts/executar.sh --testes` FORA do sandbox (`dangerouslyDisableSandbox: true`), medido ANTES da primeira task e depois da última.
2. `cd web && npx vite build`.
3. Saída no SUMMARY, um bloco por item (A1-A7), citando o achado do briefing.
</verification>

<success_criteria>
- Os sete itens fechados, cada um com commit próprio.
- Nenhuma referência de linha usada às cegas — cada uma reconfirmada por grep no momento da edição.
- Guardiões existentes atualizados com nota, nunca apagados.
- Suíte canônica sem regressão.
</success_criteria>

<output>
Create `.planning/phases/26-otimizacao-ux-ia/26-01-SUMMARY.md` when done
</output>
