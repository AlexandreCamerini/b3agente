---
phase: 33-extra-o-dos-5-jobs-em-componentes-pr-prios
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/opcoes/uiOpcoes.jsx
  - web/src/opcoes/SecaoVigias.jsx
  - web/src/opcoes/OpcoesScreen.jsx
  - web/tests/test_opcoes_vigias_ui.mjs
  - web/tests/test_opcoes_subabas_ui.mjs
  - web/tests/test_opcoes_analisar_ui.mjs
  - web/tests/test_opcoes_consolidacao_ui.mjs
  - web/tests/test_carteira_opcoes_tira.mjs
  - web/tests/test_curadoria_ui.mjs
autonomous: true
requirements: [REORG-01, REORG-02, REORG-03, REORG-05]

must_haves:
  truths:
    - "D-01: o job 'gerenciar vigias' vive em web/src/opcoes/SecaoVigias.jsx, com o nome do padrão Secao* decidido com o Alex"
    - "D-02: os guardiões que ancoravam no bloco de vigias passam a ler a FONTE NOVA (SecaoVigias.jsx) ou a varrer o diretório inteiro — nenhum deles fica inerte apontando para um marcador que não existe mais"
    - "D-03: nada muda para o usuário — a sub-aba Setups abre com 'SEUS VIGIAS' no mesmo lugar, antes do seletor de ativos, com a mesma cascata carregando→erro→vazio→dados e o mesmo custo declarado dentro do botão"
    - "REORG-03: SecaoVigias não chama useOpcoesMcp nem store.* — recebe vigias/vigiasVivos/atualizarVigias por prop do orquestrador"
    - "A allowlist ARQUIVOS de test_opcoes_analisar_ui.mjs deixa de ser lista fixa: todo arquivo novo de web/src/opcoes/ entra automaticamente nas regras de lote/breakeven/razão/|| 0 — sem isso os 4 componentes das fases 33-02..33-05 nasceriam fora de cobertura"
  artifacts:
    - path: "web/src/opcoes/SecaoVigias.jsx"
      provides: "Job 2 (vigias) como componente próprio, props-in/callback-out"
      contains: "export default function SecaoVigias"
      min_lines: 90
    - path: "web/src/opcoes/uiOpcoes.jsx"
      provides: "Kicker/Aviso/ErroDoMcp/RecusaCobrada compartilhados entre o orquestrador e as seções novas — uma implementação só"
      contains: "export function ErroDoMcp"
    - path: "web/tests/test_opcoes_vigias_ui.mjs"
      provides: "Guardião do bloco de vigias reapontado para SecaoVigias.jsx, com as mesmas garantias"
  key_links:
    - from: "web/src/opcoes/OpcoesScreen.jsx"
      to: "web/src/opcoes/SecaoVigias.jsx"
      via: "<SecaoVigias .../> no lugar exato onde {blocoVigias} estava"
      pattern: "<SecaoVigias"
    - from: "web/src/opcoes/SecaoVigias.jsx"
      to: "web/src/opcoes/uiOpcoes.jsx"
      via: "import de Kicker/Aviso/ErroDoMcp"
      pattern: "from \"./uiOpcoes.jsx\""
---

<objective>
Extrair o job 2 ("gerenciar vigias") de `OpcoesScreen.jsx` para
`web/src/opcoes/SecaoVigias.jsx`, sem nenhuma mudança de comportamento, dado
ou ordem visível — e criar, no mesmo passo, o módulo de primitivos
compartilhados (`uiOpcoes.jsx`) que as outras quatro seções vão reusar em
33-02..33-05 em vez de duplicar.

Purpose: é o primeiro dos 5 planos sequenciais de D-03 e o mais isolado dos
cinco jobs — serve de calibragem do custo real de reescrever guardiões antes
dos jobs entrelaçados. A ordem foi decidida com o Alex exatamente para isolar
o risco de quebra de guardião.
Output: 2 arquivos novos em `web/src/opcoes/`, `OpcoesScreen.jsx` mais magro,
6 guardiões reapontados sem afrouxar garantia, suíte canônica verde.

**Escopo que este plano NÃO tem:** os outros 4 jobs, qualquer mudança visual,
qualquer mudança de navegação (isso é a Fase 34), e qualquer alteração em
`App.jsx` (`curadoriaAtiva` fica intocado — REORG-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/33-extra-o-dos-5-jobs-em-componentes-pr-prios/33-CONTEXT.md
@.planning/phases/33-extra-o-dos-5-jobs-em-componentes-pr-prios/33-PATTERNS.md
@.planning/research/PITFALLS.md

<interfaces>
Contratos que o executor precisa e que já existem no código — não explorar
para descobrir, está tudo aqui.

Estado e dados que hoje alimentam `blocoVigias` (declarados em
`OpcoesScreen.jsx`, linhas citadas verificadas em 2026-09-19):

- `vigias`, `vigiasVivos`, `atualizarVigias` — trios do hook
  `useOpcoesMcp(store, ticker)`, chamado UMA vez no topo de `OpcoesScreen`
  (linha ~380). Cada trio tem a forma `{ dados, carregando, erro }`.
- `listaDeVigias` (OpcoesScreen.jsx:577-579) — escolhe a FONTE da lista:
  `vigiasVivos.dados.vigias` quando o estado do dia foi medido, senão
  `vigias.dados.vigias`. NÃO reordena nada (o backend já ordena).
- `temEstadoDosVigias` (linha 580), `tickersEmCarteira` (linha 581),
  `irParaVigia` (definido no orquestrador: `(t) => { if (t && t !== ticker) escolherTicker(t); }`).
- `CUSTO_DA_ACAO` (linha 284) — tabela de custos; a entrada usada aqui é
  `listarVigias: 2`. Precedente de como ela desce por prop:
  `custos={CUSTO_DA_ACAO}` em `<CriarSetup>` e `<BotaoDesativar>`.
- `CartaoDeVigia` (OpcoesScreen.jsx:1805-1868) — usado SÓ pelo bloco de
  vigias. Depende de `txt`, `ehNum` (formatadores de uma linha) e dos tokens
  `T`.
- Componentes compartilhados hoje privados a `OpcoesScreen.jsx`:
  `Kicker` (177), `Aviso` (185), `ErroDoMcp` (1559), `RecusaCobrada` (1595 —
  usado DENTRO de `ErroDoMcp` e também na cascata principal, linha 861).
- Constantes de estilo: `BOTAO` (195), `desabilitado` (203),
  `CUSTO_NO_BOTAO` (214).

Convenção do diretório, já guardada por teste (não inventar outra):
`OportunidadesOpcoes.jsx:23-29` e `CuradoriaEstruturas.jsx:34-41` declaram
LOCALMENTE seu próprio bloco `VARKEY`/`TOKENS`/`T` e seus formatadores de uma
linha (`price`), em vez de importar de `App.jsx`. Isso é "espelho declarado" —
é a convenção, não duplicação proibida.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar uiOpcoes.jsx e SecaoVigias.jsx, e religar OpcoesScreen.jsx</name>
  <files>web/src/opcoes/uiOpcoes.jsx, web/src/opcoes/SecaoVigias.jsx, web/src/opcoes/OpcoesScreen.jsx</files>
  <read_first>
    - web/src/opcoes/OpcoesScreen.jsx — ler as faixas 170-235 (Kicker/Aviso/BOTAO/desabilitado/CUSTO_NO_BOTAO), 555-645 (blocoVigias e suas derivações), 795-830 (a árvore de render da sub-aba Setups), 1555-1620 (ErroDoMcp e RecusaCobrada) e 1795-1870 (CartaoDeVigia). Usar Grep para reconfirmar cada número de linha ANTES de editar — eles deslocam a cada edição.
    - web/src/opcoes/OportunidadesOpcoes.jsx — analog exato do padrão de extração (header de proveniência, bloco de tokens local, props-in/callback-out). Ler o arquivo inteiro (135 linhas).
    - .planning/phases/33-extra-o-dos-5-jobs-em-componentes-pr-prios/33-PATTERNS.md — seção "web/src/opcoes/SecaoVigias.jsx" (header de proveniência a replicar e lista de props).
  </read_first>
  <action>
Criar `web/src/opcoes/uiOpcoes.jsx` contendo, movidos VERBATIM de
`OpcoesScreen.jsx` (recortar de lá, não copiar — não pode sobrar cópia
nenhuma): `Kicker`, `Aviso`, `ErroDoMcp` e `RecusaCobrada`, todos exportados
por nome. `RecusaCobrada` vai junto porque é usado DENTRO de `ErroDoMcp`;
manter a segunda chamada de `<RecusaCobrada` que vive na cascata principal de
`OpcoesScreen.jsx` (ela passa a vir do import). O módulo declara seu próprio
bloco `VARKEY`/`TOKENS`/`T` local e o subconjunto de formatadores que esses
quatro componentes usam, seguindo a convenção de `OportunidadesOpcoes.jsx:23-29`.
Header de proveniência no topo, no mesmo tom de `OportunidadesOpcoes.jsx:1-22`,
dizendo: por que o módulo existe (uma implementação só de `ErroDoMcp`, que
ramifica nos 4 códigos do ADR-027 — duas cópias divergiriam na primeira
correção), que ele NÃO importa `App.jsx` nem `OpcoesScreen.jsx`, e qual a
regra de duplicação aceita neste diretório (token de tema, constante de estilo
e formatador de UMA linha podem ser espelho declarado local; qualquer coisa
com ramificação de lógica, NÃO).

Criar `web/src/opcoes/SecaoVigias.jsx` com `export default function SecaoVigias`
recebendo exatamente estas props e nada mais: `vigias`, `vigiasVivos`,
`atualizarVigias`, `listaDeVigias`, `temEstado`, `tickersEmCarteira`,
`ticker`, `onIr`, `custos`, `cp`. Mover para dentro dele, verbatim, o JSX de
`blocoVigias` (OpcoesScreen.jsx ~587-640) e a função `CartaoDeVigia`
(~1805-1868), que tem uso exclusivo aqui. Importar `Kicker`, `Aviso` e
`ErroDoMcp` de `./uiOpcoes.jsx`; declarar localmente o bloco de tokens `T`, os
formatadores `txt`/`ehNum` e as constantes de estilo `BOTAO`,
`CUSTO_NO_BOTAO`, `desabilitado` (espelho declarado — as constantes de estilo
NÃO saem de `OpcoesScreen.jsx`, porque guardiões ancoram nelas lá e porque
constante de estilo é a categoria de espelho aceita). O custo do botão passa a
ler `custos.listarVigias`, nunca um `2` digitado nem `CUSTO_DA_ACAO` importado.
Header de proveniência citando 33-01, o job 2, "comportamento idêntico, zero
funcionalidade nova (D-03)", e que só o orquestrador chama o hook (Emenda 3 do
ADR-027).

`listaDeVigias`/`temEstado`/`tickersEmCarteira` continuam DERIVADOS em
`OpcoesScreen.jsx` (linhas 577-581) e descem por prop — não recalcular nada
dentro da seção, pela mesma razão do comentário de OpcoesScreen.jsx:567-576
(a régua de ordenação é do backend; uma segunda implementação divergiria em
silêncio).

Em `OpcoesScreen.jsx`: apagar a const `blocoVigias`, a função
`CartaoDeVigia`, e as definições de `Kicker`/`Aviso`/`ErroDoMcp`/
`RecusaCobrada`; importar os quatro de `./uiOpcoes.jsx` e `SecaoVigias` de
`./SecaoVigias.jsx`; substituir a linha `{blocoVigias}` (hoje 816) por
`<SecaoVigias ... />` na MESMA posição da árvore — depois de `{blocoCuradoria}`
e antes de `{carteira.length > 0 ? seletor : null}`. Preservar o comentário da
Fase 27 acima dela ("SEMPRE renderizado, com ou sem ativo escolhido") e
acrescentar nota datada de 2026-09-19 dizendo que o bloco virou componente
nesta fase. `T`, `TOKENS`, `VARKEY`, `CUSTO_DA_ACAO`, `BOTAO`, `CAMPO`,
`ROLAGEM`, `desabilitado` e `CUSTO_NO_BOTAO` FICAM declarados em
`OpcoesScreen.jsx`.

NÃO commitar ao fim desta task: o commit acontece no fim da Task 2, com o
código e os guardiões no MESMO commit (Pitfall 3 do PITFALLS.md — guardião
reescrito depois é guardião que passou um intervalo inerte).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('web/src/opcoes/SecaoVigias.jsx','utf8'); const o=require('fs').readFileSync('web/src/opcoes/OpcoesScreen.jsx','utf8'); const u=require('fs').readFileSync('web/src/opcoes/uiOpcoes.jsx','utf8'); const fail=(m)=>{console.log('FALHOU '+m);process.exitCode=1}; if(/store\./.test(s))fail('SecaoVigias chama store.*'); if(/useOpcoesMcp|useOpcoesPropostas/.test(s))fail('SecaoVigias instancia hook'); if(/App\.jsx/.test(s)||/App\.jsx/.test(u))fail('import de App.jsx'); if(/useState/.test(s))fail('SecaoVigias declara estado local'); if(/function CartaoDeVigia/.test(o))fail('CartaoDeVigia sobrou em OpcoesScreen'); if(/function ErroDoMcp/.test(o))fail('ErroDoMcp sobrou em OpcoesScreen'); if(!/<SecaoVigias/.test(o))fail('OpcoesScreen nao renderiza SecaoVigias'); if(!/const CUSTO_DA_ACAO/.test(o))fail('CUSTO_DA_ACAO saiu de OpcoesScreen'); console.log(process.exitCode?'':'ok estrutura da extracao')"</automated>
    <automated>npx vite build</automated>
  </verify>
  <done>`SecaoVigias.jsx` e `uiOpcoes.jsx` existem; `OpcoesScreen.jsx` não contém mais `blocoVigias`, `CartaoDeVigia`, `ErroDoMcp` nem `RecusaCobrada` e renderiza `<SecaoVigias`; nenhuma cópia duplicada de nenhum dos quatro componentes movidos; `npx vite build` verde. Árvore de trabalho ainda NÃO commitada.</done>
</task>

<task type="auto">
  <name>Task 2: Reapontar os 6 guardiões afetados (mesmo commit da Task 1)</name>
  <files>web/tests/test_opcoes_vigias_ui.mjs, web/tests/test_opcoes_subabas_ui.mjs, web/tests/test_opcoes_analisar_ui.mjs, web/tests/test_opcoes_consolidacao_ui.mjs, web/tests/test_carteira_opcoes_tira.mjs, web/tests/test_curadoria_ui.mjs</files>
  <read_first>
    - web/tests/test_opcoes_subabas_ui.mjs, linhas 100-116 — o PRECEDENTE canônico de varredura por diretório (D-02). Mirrorar literalmente.
    - web/tests/test_opcoes_vigias_ui.mjs — arquivo inteiro (222 linhas). É o guardião mais afetado; cada asserção tem comentário dizendo por que existe, e a maioria cita injeção de defeito real.
    - web/tests/test_opcoes_analisar_ui.mjs, linhas 40-60 (a allowlist ARQUIVOS), 160-172 (fatia de ErroDoMcp), 404-412 (const BOTAO), 462-478 (RecusaCobrada nos dois ramos), 530-540 (segundo precedente de varredura por diretório).
    - .planning/research/PITFALLS.md — Pitfall 3 (guardião cego a migração de arquivo) e Pitfall 4 (escopo fixo).
  </read_first>
  <action>
Antes de editar, rodar o censo — a lista de 5 guardiões do CONTEXT.md é PISO,
não teto (o planejamento desta fase achou 2 arquivos a mais por este mesmo
censo): `grep -rln "OpcoesScreen.jsx" web/tests/*.mjs` e
`grep -n "blocoVigias\|CartaoDeVigia\|opcoesVigiasTitulo\|atualizarVigias\|ErroDoMcp\|RecusaCobrada\|const BOTAO" web/tests/*.mjs`.
Qualquer arquivo que apareça e não esteja na lista abaixo entra no escopo desta
task e é registrado no SUMMARY como achado.

Reescritas exigidas (para CADA uma: nota datada `2026-09-19, Fase 33 (33-01)`
explicando que o marcador migrou de ARQUIVO, não regrediu; e a garantia
original preservada, nunca afrouxada):

1. `test_opcoes_vigias_ui.mjs` — passar a ler `SecaoVigias.jsx` (além de
   `OpcoesScreen.jsx` e `useOpcoesMcp.js`). Seção 1: a ordem "vigias antes do
   seletor" passa a ser medida em `OpcoesScreen.jsx` por
   `indexOf("<SecaoVigias") < indexOf("carteira.length > 0 ? seletor")`, e a
   existência do bloco (`cp.opcoesVigiasTitulo`) e da incondicionalidade passa
   a ser medida em `SecaoVigias.jsx`; manter a checagem de índice `>= 0` ANTES
   de qualquer fatiamento (o `-1` silencioso é o modo como este guardião
   ficaria inerte). Seção 3: o botão é fatiado em `SecaoVigias.jsx`; a
   asserção do custo passa a exigir (a) a tabela `CUSTO_DA_ACAO` com
   `listarVigias: 2` ainda em `OpcoesScreen.jsx`, (b) `custos={CUSTO_DA_ACAO}`
   descendo para `<SecaoVigias`, e (c) `custos.listarVigias` DENTRO do botão —
   nunca um literal. Seções 4/5/6: fatiar `CartaoDeVigia` a partir de
   `SecaoVigias.jsx`; `onIr={irParaVigia}` e a definição de `irParaVigia`
   continuam sendo exigidas em `OpcoesScreen.jsx`. A asserção `!/não armado/i`
   passa a valer para `OpcoesScreen.jsx` E `SecaoVigias.jsx`. A seção 4b
   (`const chave = s.nomeNoServico || s.name`, `abrirGrafico(chave)`,
   `nome={chave}`/`nomeVisivel={s.name}`) é da LISTA DE SETUPS, não dos vigias
   — NÃO tocar nela aqui; ela migra no plano 33-03.
2. `test_opcoes_subabas_ui.mjs` regra 11 — trocar `"blocoVigias"` por
   `"<SecaoVigias"` na lista de nomes que "continuam referenciados", mantendo
   os outros cinco (`cabecalho`, `seletor`, `LastroDoAtivo`, `LeituraInterna`,
   `blocoLeituraDoServico`) intactos: eles NÃO se movem nesta fase.
3. `test_opcoes_analisar_ui.mjs` — (a) trocar a allowlist fixa `ARQUIVOS`
   (linha 55) por varredura `readdirSync(dirOpcoes).filter(/\.(js|jsx)$/)`,
   com sanity-check de contagem mínima (`>= 14`) e nota datada explicando que
   lista fixa deixaria os 4 componentes das fases 33-02..33-05 fora das regras
   de lote/breakeven/razão — mirrorando o padrão das linhas 534-537 do próprio
   arquivo. Medido no planejamento: a varredura do diretório inteiro passa hoje
   sem nenhum hit novo, então a mudança não afrouxa nem reprova em falso.
   (b) a fatia de `ErroDoMcp` (linha 166) passa a sair de `uiOpcoes.jsx`.
   (c) `const BOTAO` (linha ~405) continua exigido em `OpcoesScreen.jsx` — não
   mexeu de lugar; confirmar, não editar por reflexo. (d) `RecusaCobrada`
   (462-478): `function RecusaCobrada` passa a ser exigido em `uiOpcoes.jsx`, e
   a garantia "aparece nos DOIS ramos de erro" passa a ser 1 uso em
   `OpcoesScreen.jsx` (cascata principal) + 1 uso dentro de `ErroDoMcp` em
   `uiOpcoes.jsx` — a soma continua 2, e continua provando as duas portas.
4. `test_opcoes_consolidacao_ui.mjs` regras 1 e 8 — o quarto marcador
   `{blocoVigias}` vira `<SecaoVigias`. A relação de ordem
   (frase-ponte < Bloco A < Bloco B < vigias) e a proibição de
   `sticky`/`fixed`/`<input>` no trecho entre a frase-ponte e os vigias ficam
   idênticas em intenção.
5. `test_carteira_opcoes_tira.mjs` (achado do planejamento, NÃO estava na lista
   do CONTEXT.md) item 4, linhas ~172-175 — `indexOf("{blocoVigias}")` vira
   `indexOf("<SecaoVigias")`.
6. `test_curadoria_ui.mjs` (achado do planejamento, NÃO estava na lista do
   CONTEXT.md) linhas ~317-321 — o terceiro marcador `{blocoVigias}` vira
   `<SecaoVigias`; `{blocoOportunidades}`/`{blocoCuradoria}` ficam como estão
   (migram no 33-02).

Commitar código + guardiões em UM commit só, mensagem
`refactor(33-01): extrai SecaoVigias e uiOpcoes de OpcoesScreen`.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <done>Os 6 guardiões passam lendo a fonte NOVA; nenhuma asserção foi deletada ou trocada por uma mais fraca; cada mudança tem nota datada; `bash scripts/executar.sh --testes` (pytest + .mjs) exit 0, contagem de `.mjs` igual ou maior que a baseline; código e testes num commit único.</done>
</task>

<task type="auto">
  <name>Task 3: Prova negativa real dos guardiões reescritos + baseline</name>
  <files>web/src/opcoes/SecaoVigias.jsx, web/tests/test_opcoes_vigias_ui.mjs</files>
  <read_first>
    - web/tests/test_opcoes_vigias_ui.mjs — a versão já reescrita na Task 2 (é o alvo das injeções).
    - .planning/STATE.md — só a seção "Posição anterior (v1.4, antes do arquivamento)", parágrafo da quick 260916-g6p, como referência do padrão de prova negativa REAL exigido neste repositório (defeito reintroduzido à mão, guardião falha nomeando o arquivo, revert, guardião volta a passar).
  </read_first>
  <action>
Guardião que passa não prova que guarda. Injetar, UM POR VEZ, cada defeito
abaixo em `SecaoVigias.jsx`, rodar SÓ o guardião correspondente
(`node web/tests/test_opcoes_vigias_ui.mjs`, `node web/tests/test_opcoes_subabas_ui.mjs`),
confirmar exit 1 com a mensagem nomeando a asserção certa, e reverter com
`git checkout --` (conferir `git diff` vazio depois de cada revert):

1. Acrescentar `import { T } from "../App.jsx";` em `SecaoVigias.jsx` — a
   varredura por diretório de `test_opcoes_subabas_ui.mjs:100-116` tem de
   reprovar nomeando o arquivo. Esta é a prova de que o arquivo NOVO entrou na
   cobertura automática do invariante de duas vias (ADR-027 Emenda 3).
2. Trocar `custos.listarVigias` por `2` literal dentro do botão — o guardião
   de custo declarado tem de reprovar.
3. Trocar o texto de estado ausente do cartão por `"não armado"` — a asserção
   "sem medição não há veredito" (princípio 4 do CLAUDE.md) tem de reprovar.
4. Inverter a ordem de render: mover `<SecaoVigias` para DEPOIS do seletor em
   `OpcoesScreen.jsx` — a asserção "vigias antes da carteira" (D4 da Fase 27)
   tem de reprovar.

Se qualquer injeção NÃO reprovar, o guardião ficou vácuo: corrigir o guardião
e repetir. Registrar no SUMMARY, por injeção, o comando rodado e a linha de
saída real — descrição não conta como prova (precedente: as duas injeções
vácuas achadas na Fase 27).

Fechar com a baseline completa: `bash scripts/executar.sh --testes` e
`npx vite build`, registrando os números (pytest e `.mjs`) no SUMMARY. Estes
números são a baseline de entrada do plano 33-02.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
    <automated>git diff --stat && git status --porcelain</automated>
  </verify>
  <done>4 injeções feitas, cada uma reprovada pelo guardião esperado com a saída real registrada no SUMMARY, todas revertidas (`git status` limpo além do commit da Task 2); suíte canônica exit 0 com contagem registrada; `npx vite build` verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | Este plano move JSX já publicado entre arquivos do mesmo bundle de front. Nenhuma rota nova, nenhum input de usuário novo, nenhum dado novo cruzando cliente↔servidor, nenhuma superfície de autenticação tocada. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Information Disclosure | `CartaoDeVigia` movido para `SecaoVigias.jsx` | mitigate | O `nomeNoServico` carrega 8 hex do hash da conta e nunca pode chegar à tela (dano medido na injeção nº 4 do 27-01). A asserção `!/nomeNoServico/` passa a ser medida no arquivo NOVO — se o guardião continuasse lendo `OpcoesScreen.jsx`, ficaria inerte exatamente sobre este risco. |
| T-33-02 | Tampering | guardiões reescritos | mitigate | Prova negativa real por injeção (Task 3): guardião que passa sem guardar é a forma que uma regressão de compliance entraria calada. |
| T-33-03 | — (compliance, não segurança) | guardrail CVM de manchete | accept (neste plano) | `SecaoVigias` não renderiza `.manchete` (vigias não têm manchete de motor). A GENERALIZAÇÃO do guardrail (REORG-06) é do plano 33-02, onde nasce a seção que embute os renderizadores de candidato. Registrado aqui para não parecer esquecido: é controle regulatório (CVM), não de segurança, e não pode regredir. |
</threat_model>

<verification>
1. `bash scripts/executar.sh --testes` — as DUAS suítes (pytest + `web/tests/*.mjs`), exit 0. `scripts/test.sh` sozinho NÃO conta (é meia baseline).
2. `npx vite build` verde (o front foi editado; grep e teste estático não pegam erro de sintaxe JS).
3. Censo de guardiões re-rodado (`grep -rln "OpcoesScreen.jsx" web/tests/*.mjs`) e cada arquivo listado revisado — não basta a suíte estar verde.
4. As 4 provas negativas da Task 3, com saída real no SUMMARY.
5. `grep -n "store\.\|useOpcoesMcp\|useState" web/src/opcoes/SecaoVigias.jsx` → zero linhas (REORG-03 e Pitfall 6).
6. `git diff --stat web/src/App.jsx` → vazio (REORG-04: `curadoriaAtiva` intocado).
</verification>

<success_criteria>
- `web/src/opcoes/SecaoVigias.jsx` e `web/src/opcoes/uiOpcoes.jsx` existem e são importados por `OpcoesScreen.jsx`; nenhuma definição duplicada dos componentes movidos.
- Ordem visível idêntica: frase-ponte → Bloco A → Bloco B → vigias → seletor.
- 6 guardiões reapontados com nota datada, nenhuma garantia afrouxada, 4 provas negativas registradas.
- Suíte canônica verde (números registrados no SUMMARY, servem de baseline do 33-02) e `vite build` verde.
- `App.jsx` intocado.
- NÃO publicado: nenhum `scripts/bump.sh`, nenhum `publicar-web.sh`, nenhum push — publicação é passo humano separado, fora do escopo desta fase.
</success_criteria>

<output>
Create `.planning/phases/33-extra-o-dos-5-jobs-em-componentes-pr-prios/33-01-SUMMARY.md` when done, registrando: números da suíte (antes/depois), a saída real de cada injeção, e qualquer guardião achado pelo censo que não estava nesta lista.
</output>
