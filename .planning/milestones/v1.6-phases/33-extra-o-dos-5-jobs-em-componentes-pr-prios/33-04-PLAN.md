---
phase: 33-extra-o-dos-5-jobs-em-componentes-pr-prios
plan: 04
type: execute
wave: 4
depends_on: [33-03]
files_modified:
  - web/src/opcoes/SecaoComparar.jsx
  - web/src/opcoes/uiOpcoes.jsx
  - web/src/opcoes/OpcoesScreen.jsx
  - web/tests/test_opcoes_analisar_ui.mjs
  - web/tests/test_opcoes_custo_declarado.mjs
autonomous: true
requirements: [REORG-01, REORG-02, REORG-03, REORG-05]

must_haves:
  truths:
    - "D-01: o job 'comparar os vencimentos' vive em web/src/opcoes/SecaoComparar.jsx"
    - "D-03: o custo em chamadas continua aparecendo ANTES do disparo, dentro do próprio controle, com o número derivado (2×N+1, N ≤ 6) calculado no orquestrador e recebido por prop — nunca recalculado na seção"
    - "D-02: os guardiões que fatiavam este bloco por posição dentro de OpcoesScreen.jsx passam a medir o arquivo-fonte novo, sem afrouxar a ordem dos ramos nem a declaração de custo"
    - "REORG-02: a cascata carregando→erro→vazio com motivo→dados das possibilidades mantém a MESMA ordem de ramos; o erro continua escolhido pelo `code` via ErroDoMcp, nunca raspando a mensagem"
    - "REORG-03: SecaoComparar não chama store.* nem instancia hook — recebe possibilidades/verPossibilidades/vencimentos/chamadasPrevistas por prop"
    - "Pitfall 6: tese e lote NÃO são redeclarados dentro da seção — são o MESMO formulário do job 3 e continuam no orquestrador; alvo e stop também ficam lá, para não resetarem entre re-renders"
  artifacts:
    - path: "web/src/opcoes/SecaoComparar.jsx"
      provides: "Job 4 — comparação de vencimentos com custo declarado e cascata própria"
      contains: "export default function SecaoComparar"
      min_lines: 90
  key_links:
    - from: "web/src/opcoes/OpcoesScreen.jsx"
      to: "web/src/opcoes/SecaoComparar.jsx"
      via: "<SecaoComparar .../> no ramo DADOS, entre o job 3 e o job 5"
      pattern: "<SecaoComparar"
    - from: "web/src/opcoes/SecaoComparar.jsx"
      to: "web/src/opcoes/PayoffChart.jsx"
      via: "<PayoffChart> por vencimento, já alimentado pelo dado do backend"
      pattern: "<PayoffChart"
---

<objective>
Extrair o job 4 ("comparar os vencimentos") de `OpcoesScreen.jsx` para
`web/src/opcoes/SecaoComparar.jsx`, preservando comportamento, dados, ordem e —
o ponto mais sensível deste bloco — a declaração de custo ANTES do disparo.

Purpose: passo 4 de D-03. É o único dos cinco jobs sem módulo irmão já
extraído, então o padrão vem do trecho-fonte e de `CuradoriaEstruturas.jsx`
como referência de "bloco com cascata própria alimentado por prop".
Output: 1 componente novo, 2 guardiões reapontados, suíte canônica verde.

**Escopo que este plano NÃO tem:** recalcular qualquer coisa de custo dentro da
seção, mexer em `PayoffChart.jsx`, descer `tese`/`lote` para a seção, tocar
`App.jsx`, e qualquer mudança visual.

**Achado do planejamento (a constraint pedia a verificação):** SIM, há
guardiões que ancoram neste bloco por POSIÇÃO — `test_opcoes_analisar_ui.mjs`
itens 4 (custo antes do disparo), 5 (fatia "Possibilidades" entre
`cp.opcoesPossibilidadesTitulo` e `cp.opcoesSetupsTitulo`) e 13 (a razão
ganho/perda renderizada nas DUAS seções). O `test_opcoes_custo_declarado.mjs`
também toca (`cp.opcoesCustoChamadas(chamadasPrevistas)`), mas já foi convertido
para varredura de diretório no plano 33-03 — confirmar que sobrevive, não
reescrever por reflexo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/33-extra-o-dos-5-jobs-em-componentes-pr-prios/33-CONTEXT.md
@.planning/phases/33-extra-o-dos-5-jobs-em-componentes-pr-prios/33-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/phases/33-extra-o-dos-5-jobs-em-componentes-pr-prios/33-03-SUMMARY.md

<interfaces>
Medido no planejamento (2026-09-19) — reconfirmar linhas por Grep antes de editar.

Fonte a extrair, `OpcoesScreen.jsx`: do
`<Kicker>{cp.opcoesPossibilidadesTitulo || "COMPARAR OS VENCIMENTOS"}</Kicker>`
(hoje 1134) até imediatamente antes do `<Kicker>{cp.opcoesSetupsTitulo}`
(que o 33-03 já transformou em `<SecaoSetups`). Contém: a caixa `CAIXA`, o ramo
`N === 0` com `cp.opcoesSemVencimento`, a declaração de custo
`cp.opcoesCustoChamadas(chamadasPrevistas)`, os campos de alvo e stop, o botão
com `disabled={!temTese || !loteOk}` disparando `verPossibilidades(...)`, e a
cascata `possibilidades.carregando → possibilidades.erro (<ErroDoMcp>) →
possibilidades.dados sem itens (motivo do backend) → map por vencimento` com
`<PayoffChart>`, `<RazaoGanhoPerda razao={item.razaoGanhoPerda}>`, `<Cenarios>`
e o `item.erro` por vencimento.

O que FICA no orquestrador e desce por prop (regra do ARCHITECTURE.md, tabela
de Integration Points — "não deve fazer: recalcular localmente"):
- `const N_MAX_VENCIMENTOS = 6` (linha 254) e a derivação
  `const N = Math.min(vencimentos.length, N_MAX_VENCIMENTOS)` e
  `const chamadasPrevistas = 2 * N + 1;` — descem como `chamadasPrevistas`,
  `N`/`consultados` e `vencimentos` prontos. Motivo: o guardião cruza o número
  com a forma `2 * N + 1` e com `N_MAX_VENCIMENTOS = 6`, e duas contas do mesmo
  custo divergiriam na primeira manutenção.
- `tese`, `lote`, `temTese` (`!!tese`), `loteOk`, `alvo`/`setAlvo`,
  `stop`/`setStop` — `tese`/`lote` são o MESMO formulário do job 3 (Pitfall 6);
  `alvo`/`stop` são exclusivos deste job mas ficam no orquestrador para não
  resetar entre re-renders da mesma sessão de navegação.
- `possibilidades` (trio do hook) e `verPossibilidades` (o disparo).

Guardiões que ancoram aqui (medido por grep, 2026-09-19):
- `test_opcoes_analisar_ui.mjs` item 4 (linhas ~131-146): `iCusto = indexOf("cp.opcoesCustoChamadas")`
  e `iDispara = indexOf("verPossibilidades(")` no fonte de `OpcoesScreen.jsx`,
  com `iCusto < iDispara`; mais `Math.min(vencimentos.length, N_MAX_VENCIMENTOS)`,
  `2 * N + 1`, `const N_MAX_VENCIMENTOS = 6`, `cp.opcoesSemVencimento`,
  `disabled={!temTese || !loteOk}` e `const temTese = !!tese;`.
- `test_opcoes_analisar_ui.mjs` item 5 (linhas ~147-163): fatia
  `tela.slice(iPossib, iSetups)` exigindo, NESTA ordem,
  `possibilidades.carregando` → `possibilidades.erro` →
  `possibilidades.dados.motivo` → `possibilidades.dados.possibilidades.map`, e
  `<ErroDoMcp erro=` dentro da fatia.
- `test_opcoes_analisar_ui.mjs` item 13 (linhas ~419-430): `razaoGanhoPerda`
  renderizada nas duas seções (fatias de Analisar e de Possibilidades) e
  `razao={item.razaoGanhoPerda}`.
- `test_opcoes_custo_declarado.mjs` ~188-191: `const chamadasPrevistas = 2 * N + 1;`
  (fica em `OpcoesScreen.jsx`) e `opcoesCustoChamadas` a ≤80 caracteres de
  `(chamadasPrevistas)` — ESTA convive com a migração só se a varredura do
  33-03 tiver incluído `Secao*.jsx` na coleta de `linhasDeCusto`. Confirmar.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar SecaoComparar.jsx e religar OpcoesScreen.jsx</name>
  <files>web/src/opcoes/SecaoComparar.jsx, web/src/opcoes/uiOpcoes.jsx, web/src/opcoes/OpcoesScreen.jsx</files>
  <read_first>
    - web/src/opcoes/OpcoesScreen.jsx — faixa 1130-1225 (o bloco inteiro) e 240-300 (N_MAX_VENCIMENTOS, CUSTO_DA_ACAO, CAIXA/CAMPO/ROTULO/AJUDA). Reconfirmar por Grep de `cp.opcoesPossibilidadesTitulo`.
    - web/src/opcoes/CuradoriaEstruturas.jsx — analog de referência: bloco com cascata carregando→erro→vazio→dados dentro de componente próprio, alimentado por prop, com tokens locais declarados.
    - web/src/opcoes/SecaoSetups.jsx e web/src/opcoes/uiOpcoes.jsx — o estado do diretório depois do 33-03: o que já é importável em vez de redeclarado.
    - .planning/research/PITFALLS.md — Pitfall 6 (estado que atravessa jobs) e Pitfall 7 (não remover informação: o "os N primeiros de X" dos vencimentos truncados continua visível, sem clique novo).
  </read_first>
  <action>
Criar `web/src/opcoes/SecaoComparar.jsx`, `export default function SecaoComparar`,
recebendo por prop: `ticker`, `tese`, `temTese`, `lote`, `loteOk`, `alvo`,
`setAlvo`, `stop`, `setStop`, `vencimentos`, `consultados`,
`chamadasPrevistas`, `possibilidades`, `verPossibilidades`, `custos`, `cp`,
`palette`. Mover o JSX verbatim, com os comentários de decisão que o acompanham.

Regras que a extração não pode afrouxar:
- NENHUM `useState` novo. `alvo`/`stop` chegam com seus setters; `tese`/`lote`
  chegam somente-leitura (Pitfall 6).
- NENHUM recálculo: `chamadasPrevistas`, `vencimentos` e a contagem de
  truncamento chegam prontos. Proibido `Math.min(`, `N_MAX_VENCIMENTOS`,
  `2 * N + 1` dentro do arquivo novo.
- A declaração de custo aparece no fonte ANTES do disparo e DENTRO do controle
  (o `<span style={CUSTO_NO_BOTAO}>` do próprio botão) — descobrir que custou
  13 chamadas depois de gastá-las é recibo, não aviso.
- A cascata mantém a ordem exata dos ramos: `carregando` antes de `erro`, erro
  antes de vazio-com-motivo, vazio antes de dados. O motivo do backend vai
  VERBATIM (sem reescrita, sem concatenação).
- A frase de vencimentos truncados ("os N primeiros de X") continua visível sem
  clique extra (Pitfall 7 — reorganizar não é reduzir informação).
- Importar `Kicker`/`Aviso`/`ErroDoMcp` de `./uiOpcoes.jsx`. Se precisar de
  `RazaoGanhoPerda`, `Cenarios`, `Pernas`, `TabelaDeOpcoes`, `Linha` ou
  `LacunasDaLeitura` e o orquestrador AINDA os usa (o job 3 usa vários, e ele só
  sai no 33-05), mover cada um para `uiOpcoes.jsx` e importar dos dois lados —
  nunca duplicar. Se o orquestrador deixar de usar, pode ir direto para dentro
  do único consumidor.

Em `OpcoesScreen.jsx`: apagar o JSX movido, manter todas as derivações citadas
em `<interfaces>` e renderizar `<SecaoComparar ... />` na POSIÇÃO EXATA de hoje
— dentro do ramo "4. DADOS", depois do bloco do job 3 e antes de
`<SecaoSetups`.

NÃO commitar: commit único no fim da Task 2.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('web/src/opcoes/SecaoComparar.jsx','utf8'); const o=require('fs').readFileSync('web/src/opcoes/OpcoesScreen.jsx','utf8'); const sem=(x)=>x.replace(/\/\*[\s\S]*?\*\//g,'').split('\n').filter(l=>!/^\s*\/\//.test(l)).join('\n'); const c=sem(s); const f=(m)=>{console.log('FALHOU '+m);process.exitCode=1}; if(/store\.|api\./.test(c))f('SecaoComparar chama store/api'); if(/useOpcoesMcp|useOpcoesPropostas/.test(c))f('instancia hook'); if(/useState/.test(c))f('declara estado local (Pitfall 6)'); if(/App\.jsx/.test(c))f('importa App.jsx'); if(/N_MAX_VENCIMENTOS|2 \* N \+ 1|Math\.min\(/.test(c))f('recalcula custo/teto de vencimentos'); if(/\.manchete/.test(c))f('renderiza manchete (REORG-06)'); const iC=c.indexOf('opcoesCustoChamadas'), iD=c.indexOf('verPossibilidades('); if(!(iC>-1&&iD>iC))f('custo nao aparece antes do disparo'); const i1=c.indexOf('possibilidades.carregando'), i2=c.indexOf('possibilidades.erro'), i3=c.indexOf('possibilidades.dados.motivo'); if(!(i1>-1&&i2>i1&&i3>i2))f('ordem da cascata alterada'); if(!/<SecaoComparar/.test(o))f('OpcoesScreen nao renderiza SecaoComparar'); if(!/const N_MAX_VENCIMENTOS = 6/.test(o)||!/const chamadasPrevistas = 2 \* N \+ 1;/.test(o))f('derivacao de custo saiu do orquestrador'); if(o.indexOf('<SecaoComparar')>o.indexOf('<SecaoSetups'))f('ordem job4 antes de job5 quebrada'); console.log(process.exitCode?'':'ok estrutura de SecaoComparar')"</automated>
    <automated>npx vite build</automated>
  </verify>
  <done>`SecaoComparar.jsx` existe sem estado local e sem recálculo de custo; `OpcoesScreen.jsx` renderiza `<SecaoComparar` entre o job 3 e `<SecaoSetups`; custo antes do disparo; cascata na mesma ordem; `vite build` verde.</done>
</task>

<task type="auto">
  <name>Task 2: Reapontar os itens 4, 5 e 13 do guardião de Analisar (e conferir o de custo)</name>
  <files>web/tests/test_opcoes_analisar_ui.mjs, web/tests/test_opcoes_custo_declarado.mjs</files>
  <read_first>
    - web/tests/test_opcoes_analisar_ui.mjs — linhas 128-172 (itens 4 e 5) e 415-435 (item 13). Ler os comentários acima de cada asserção: o item 4 existe porque "descobrir que a consulta custou 13 chamadas depois de gastá-las não é aviso, é recibo".
    - web/tests/test_opcoes_custo_declarado.mjs — a coleta de `linhasDeCusto` como ficou depois do 33-03.
    - .planning/research/PITFALLS.md — Pitfall 3.
  </read_first>
  <action>
Censo: `grep -n "opcoesPossibilidadesTitulo\|verPossibilidades\|possibilidades\.\|N_MAX_VENCIMENTOS\|chamadasPrevistas\|razaoGanhoPerda\|opcoesSemVencimento\|loteOk" web/tests/*.mjs`.
Arquivo fora da lista entra no escopo e vai ao SUMMARY.

1. `test_opcoes_analisar_ui.mjs` item 4 — a garantia se divide em duas, sem
   perder nada: (a) em `OpcoesScreen.jsx` continuam exigidos
   `const N_MAX_VENCIMENTOS = 6`, `Math.min(vencimentos.length, N_MAX_VENCIMENTOS)`,
   `2 * N + 1` e `const temTese = !!tese;` — é lá que a conta vive; (b) em
   `SecaoComparar.jsx` passam a ser medidos `cp.opcoesCustoChamadas` ANTES de
   `verPossibilidades(`, `cp.opcoesSemVencimento` e
   `disabled={!temTese || !loteOk}`. Acrescentar uma asserção NOVA, que hoje não
   existe e que a extração torna possível e necessária: `SecaoComparar.jsx` não
   contém `N_MAX_VENCIMENTOS` nem `2 * N + 1` — "uma conta só" deixa de ser
   convenção e passa a ser travada.
2. `test_opcoes_analisar_ui.mjs` item 5 — a fatia "Possibilidades" passa a ser
   o arquivo `SecaoComparar.jsx` inteiro (não mais `slice(iPossib, iSetups)`),
   com os MESMOS quatro estados na mesma ordem e `<ErroDoMcp erro=` presente. A
   fatia "Analisar" continua saindo de `OpcoesScreen.jsx` até o 33-05 mudá-la.
   A asserção de ORDEM entre seções (`iAnalisar < iPossib < iSetups`) passa a
   medir, em `OpcoesScreen.jsx`, `cp.opcoesAnalisarTitulo` < `<SecaoComparar` <
   `<SecaoSetups` — ordem preservada, marcador trocado.
3. `test_opcoes_analisar_ui.mjs` item 13 — `razao={item.razaoGanhoPerda}` passa a
   ser medido em `SecaoComparar.jsx`; `razao={proposta.dados.razaoGanhoPerda}`
   continua em `OpcoesScreen.jsx` (sai no 33-05). A garantia "a razão é
   renderizada nas DUAS seções" continua valendo, agora somando os dois
   arquivos. `function RazaoGanhoPerda` passa a ser exigido onde o componente
   efetivamente ficou (`uiOpcoes.jsx` se foi movido, `OpcoesScreen.jsx` se não).
4. `test_opcoes_custo_declarado.mjs` — CONFERIR, não reescrever por reflexo: se
   a varredura do 33-03 já coleta `Secao*.jsx`, a asserção
   `opcoesCustoChamadas ... (chamadasPrevistas)` pode ter deixado de valer
   porque a linha do controle e a da conta passaram a viver em arquivos
   diferentes. Se for o caso, medir a proximidade dentro de `SecaoComparar.jsx`
   (onde o controle está) e a existência da conta em `OpcoesScreen.jsx`, com
   nota datada. Se ainda passa, registrar no SUMMARY que passou por
   cobertura real e não por acidente.

Commit único com a Task 1: `refactor(33-04): extrai SecaoComparar de OpcoesScreen`.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <done>Itens 4, 5 e 13 medem a topologia nova; asserção nova proíbe recálculo de custo na seção; o guardião de custo declarado foi conferido (e corrigido se tinha virado vácuo); suíte canônica exit 0; um commit só.</done>
</task>

<task type="auto">
  <name>Task 3: Prova negativa (custo, ordem da cascata, recálculo) e baseline</name>
  <files>web/src/opcoes/SecaoComparar.jsx</files>
  <read_first>
    - web/tests/test_opcoes_analisar_ui.mjs — itens 4, 5 e 13 já reescritos na Task 2.
  </read_first>
  <action>
Uma injeção por vez em `SecaoComparar.jsx`, rodando só o guardião do caso,
exigindo exit 1 com a asserção certa, revertendo com `git checkout --` e
conferindo `git diff` vazio. Comando e saída real de cada uma no SUMMARY:

1. Mover a declaração de custo para DEPOIS do botão (ou removê-la) — o item 4
   tem de reprovar. É a garantia central deste bloco.
2. Recalcular `chamadasPrevistas` dentro da seção (colar
   `const n = 2 * Math.min(vencimentos.length, 6) + 1;`) — a asserção nova da
   Task 2 tem de reprovar.
3. Inverter a cascata: pôr o ramo de vazio ANTES do de carregando — o item 5
   tem de reprovar (vazio pintado durante a consulta afirma "não há nada" sem
   ninguém ter medido; princípio 4 do CLAUDE.md).
4. Substituir `<ErroDoMcp erro={possibilidades.erro} .../>` por um texto que
   raspa `possibilidades.erro.message` — o item 5 tem de reprovar (erro
   escolhido pelo `code`, nunca pela mensagem).
5. Declarar `const [tese, setTese] = useState("")` dentro da seção — se nenhum
   guardião reprovar, criar a asserção que proíbe `useState` de
   `ticker`/`tese`/`vencimento`/`lote` em qualquer `Secao*.jsx` (Pitfall 6, e
   ela serve para os 5 componentes desta fase) e repetir.

Fechar com `bash scripts/executar.sh --testes` e `npx vite build`, números no
SUMMARY (baseline de entrada do 33-05).
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
    <automated>git status --porcelain</automated>
  </verify>
  <done>5 injeções com saída real registrada (a 5 gerou asserção nova se não existia, valendo para todos os `Secao*.jsx`), todas revertidas; suíte canônica exit 0 com números no SUMMARY; `vite build` verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | JSX já publicado muda de arquivo. O disparo de `verPossibilidades` continua saindo de clique explícito no mesmo controle, contra as mesmas rotas. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-12 | Denial of Service (cota do usuário, não do serviço) | disparo de `/possibilidades` | mitigate | É a consulta mais cara da aba (até 13 chamadas do cap compartilhado, ADR-027 §3.3). O gate é duplo e ambos migram intactos: custo declarado no controle ANTES do clique e `disabled` sem tese/lote. Nenhum efeito novo — a seção não dispara nada em mount (injeções 1 e 2 da Task 3 provam a cobertura). |
| T-33-13 | — (princípio 4 do CLAUDE.md) | cascata de possibilidades | mitigate | Vazio pintado durante a consulta afirmaria "nenhuma possibilidade" sem medição; o motivo do backend vai verbatim, sem reescrita. Injeções 3 e 4. |
| T-33-14 | Tampering | `chamadasPrevistas` | mitigate | Duas contas do mesmo custo divergem na primeira manutenção, e a que aparece no botão é a que o usuário lê. A conta fica UMA, no orquestrador, cruzada pelo guardião com `_cap_check` do backend; a asserção nova proíbe a segunda. |
</threat_model>

<verification>
1. `bash scripts/executar.sh --testes` — pytest + `.mjs`, exit 0.
2. `npx vite build` verde.
3. `grep -nE "useState|N_MAX_VENCIMENTOS|2 \* N \+ 1|store\." web/src/opcoes/SecaoComparar.jsx` → zero.
4. As 5 injeções da Task 3 com saída real no SUMMARY.
5. Conferir à mão: a frase de vencimentos truncados continua visível sem clique novo (Pitfall 7).
6. `git diff --stat web/src/App.jsx` vazio.
</verification>

<success_criteria>
- `SecaoComparar.jsx` existe sem estado local, sem recálculo de custo e sem chamada de rede.
- Custo antes do disparo, dentro do controle; cascata na mesma ordem; motivo do backend verbatim.
- Itens 4/5/13 do guardião de Analisar reapontados; asserção nova contra recálculo; guardião de custo conferido de verdade.
- Ordem visível idêntica; suíte canônica e `vite build` verdes; `App.jsx` intocado.
- NÃO publicado: sem `bump.sh`, sem `publicar-web.sh`, sem push.
</success_criteria>

<output>
Create `.planning/phases/33-extra-o-dos-5-jobs-em-componentes-pr-prios/33-04-SUMMARY.md` when done, com as 5 injeções (comando + saída real), o veredito sobre `test_opcoes_custo_declarado.mjs` (passou por cobertura ou foi corrigido) e os números da suíte.
</output>
