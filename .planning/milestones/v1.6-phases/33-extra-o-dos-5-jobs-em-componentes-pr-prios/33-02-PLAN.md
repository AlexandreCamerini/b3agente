---
phase: 33-extra-o-dos-5-jobs-em-componentes-pr-prios
plan: 02
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - web/src/opcoes/SecaoDescobrir.jsx
  - web/src/opcoes/OpcoesScreen.jsx
  - web/src/copy.js
  - web/tests/test_opcoes_consolidacao_ui.mjs
  - web/tests/test_curadoria_ui.mjs
  - web/tests/test_carteira_opcoes_tira.mjs
  - web/tests/test_opcoes_subabas_ui.mjs
autonomous: true
requirements: [REORG-01, REORG-02, REORG-03, REORG-04, REORG-05, REORG-06, REORG-07]

must_haves:
  truths:
    - "D-01: o job 'descobrir oportunidades cross-carteira' vive em web/src/opcoes/SecaoDescobrir.jsx, compondo OportunidadesOpcoes (Bloco A) e CuradoriaEstruturas (Bloco B) sem reescrever nenhum dos dois"
    - "D-05: a frase-ponte (cp.duasLeiturasIntro) migrou no MESMO commit que o Bloco B e continua imediatamente acima dele, incondicional, sem toggle nem colapso — é mitigação regulatória amarrada à adjacência física, não copy reposicionável"
    - "REORG-06: o guardrail CVM de manchete deixa de ser escopado a SubAbaOperar e passa a varrer web/src/opcoes/ inteiro — só os 4 renderizadores nomeados (PropostaLastreada, CandidatoOpcao, CuradoriaEstruturas, OportunidadesOpcoes) podem tocar .manchete; nenhuma Secao* pode"
    - "D-02: a generalização do guardrail e as regras de ordem migram para varredura de diretório/arquivo-fonte novo, no padrão de test_opcoes_subabas_ui.mjs:100-116 — nenhum guardião fica apontando para marcador que não existe mais"
    - "REORG-07: top/meta chegam a SecaoDescobrir pela MESMA referência de ctx.curadoria, sem .sort() nem .filter() em lugar nenhum do componente novo"
    - "REORG-04: curadoriaAtiva continua em App.jsx gateado só por tab — App.jsx não é tocado por este plano"
    - "D-04b: os dois blocos cross-carteira passam a exibir carimbo de frescor derivado do dado que a própria resposta já traz (at/source), nunca de Date.now(); sem o campo, a tela diz que não mediu em vez de inventar"
  artifacts:
    - path: "web/src/opcoes/SecaoDescobrir.jsx"
      provides: "Job 1 (descobrir) — frase-ponte + Bloco A + Bloco B, com carimbo de frescor"
      contains: "export default function SecaoDescobrir"
      min_lines: 80
    - path: "web/tests/test_opcoes_subabas_ui.mjs"
      provides: "Guardrail CVM de manchete generalizado por varredura de diretório (REORG-06)"
      contains: "readdirSync"
  key_links:
    - from: "web/src/opcoes/OpcoesScreen.jsx"
      to: "web/src/opcoes/SecaoDescobrir.jsx"
      via: "<SecaoDescobrir .../> no lugar exato de {fraseDuasLeituras}{blocoOportunidades}{blocoCuradoria}"
      pattern: "<SecaoDescobrir"
    - from: "web/src/opcoes/SecaoDescobrir.jsx"
      to: "web/src/opcoes/CuradoriaEstruturas.jsx"
      via: "<CuradoriaEstruturas> recebendo top/meta/concluido vindos de ctx.curadoria por prop"
      pattern: "<CuradoriaEstruturas"
---

<objective>
Extrair o job 1 ("descobrir oportunidades cross-carteira") para
`web/src/opcoes/SecaoDescobrir.jsx` — frase-ponte + Bloco A + Bloco B juntos,
nesta ordem, fisicamente adjacentes — generalizar o guardrail CVM de manchete
para varredura de diretório (REORG-06), e dobrar o TODO do carimbo de frescor
(D-04b, exceção explícita e aprovada a "zero feature nova").

Purpose: é o passo 2 de D-03. Os dois blocos e a frase-ponte entram JUNTOS por
decisão registrada: separar a frase-ponte do Bloco B seria regressão
regulatória (Pitfall 2 do PITFALLS.md), não ajuste visual. E é aqui que nasce
o primeiro componente novo capaz de exibir candidato — o momento certo para o
guardrail CVM deixar de ter escopo de uma variável só.
Output: 1 componente novo, guardrail CVM generalizado, carimbo de frescor nos
dois blocos, suíte canônica verde.

**Escopo que este plano NÃO tem:** mexer em `App.jsx` (`curadoriaAtiva`
intocado), mexer em `OportunidadesOpcoes.jsx`/`CuradoriaEstruturas.jsx` além do
que o carimbo de frescor exigir, qualquer mudança de navegação, e qualquer
alteração na rota da curadoria no backend sem passar pela Task 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/33-extra-o-dos-5-jobs-em-componentes-pr-prios/33-CONTEXT.md
@.planning/phases/33-extra-o-dos-5-jobs-em-componentes-pr-prios/33-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/todos/pending/carimbo-frescor-blocos-cross-carteira.md
@.planning/phases/33-extra-o-dos-5-jobs-em-componentes-pr-prios/33-01-SUMMARY.md

<interfaces>
O que o planejamento já MEDIU no código (não re-descobrir; confirmar e usar):

Fonte a mover, `OpcoesScreen.jsx` (linhas de 2026-09-19, reconfirmar por Grep):
- `const fraseDuasLeituras` (711-719) — parágrafo com `cp.duasLeiturasIntro`.
  Sem `onClick`, sem `aria-expanded`, usado numa linha isolada `{fraseDuasLeituras}` (810).
- `const blocoOportunidades` (721-733) — `<OportunidadesOpcoes propostas={opcoesPorTicker}
  carregando={opcoesPorTickerCarregando} positions={carteira} cp={cp} onAbrir={irParaOperar} />`.
- `const blocoCuradoria` (735-753) — `<CuradoriaEstruturas top meta carregando erro
  concluido narrativa narrando erroNarrativa onNarrar onRecarregar cp onAbrir
  onExecutar operador palette />`, com `concluido={!!(ctx && ctx.curadoria && ctx.curadoria.concluido)}`
  e `onExecutar={(cand, o) => ctx.A.executarCandidatoCurado(cand, o)}`.
- Uso na árvore: linhas 810-812, imediatamente antes de `<SecaoVigias` (que o
  33-01 pôs no lugar de `{blocoVigias}`).

Frescor — MEDIDO no planejamento, o dado JÁ EXISTE no cliente:
- `GET /api/options/curadoria` (server/app/main.py:3485-3488) devolve
  `{"top": ..., "modo": ..., "fonte": "deterministico", "at": now_str(), **meta}`,
  e `meta` inclui `source`.
- `useCuradoria` (web/src/App.jsx:4186-4189) faz `setTop(r.top)` e
  `setMeta(r || null)` — ou seja, `ctx.curadoria.meta.at` e
  `ctx.curadoria.meta.source` estão disponíveis HOJE, sem tocar no backend.
- `GET /api/options/proposta/{ticker}` devolve `"source": source, "at": now_str()`
  (main.py, bloco de return do handler `options_proposta`).
- `useOpcoesPropostas` guarda `{ gate, proposta }` por ticker — logo
  `opcoesPorTicker[t].proposta.at` e `.source` também estão disponíveis.
- Vocabulário de frescor que JÁ existe em `web/src/copy.js` nos DOIS ramos:
  `opcoesPregaoRotulo` (112/805), `opcoesFonteRotulo` (113/806),
  `opcoesFrescorEmDia`, `opcoesFrescorAtrasado`, `opcoesFrescorNaoMedido`
  (114-116/807-809), `opcoesAtrasoPregoes` (função), `opcoesAtrasoAjuda`.
- Padrão de como o app monta o chip de frescor hoje: `escolherFrescor`
  (OpcoesScreen.jsx:2004-2010) + o bloco do `chip` dentro de `const cabecalho`
  (~498-556), que decide por QUALIDADE (`medido === true`), nunca por quem
  respondeu primeiro, e onde `atraso.pregoes >= 1` é checado ANTES de "dado em dia".

Guardrail CVM — escopo real hoje: `test_opcoes_subabas_ui.mjs:141-154`, medido
só sobre a fatia `SubAbaOperar`. Arquivos que HOJE tocam `.manchete` (medido
por grep em 2026-09-19, são exatamente 4): `CandidatoOpcao.jsx`,
`CuradoriaEstruturas.jsx`, `OportunidadesOpcoes.jsx`, `PropostaLastreada.jsx`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Descoberta do frescor (D-04b) — confirmar a fonte antes de escrever UI</name>
  <files>(nenhum arquivo modificado — task de medição; registra o achado no SUMMARY)</files>
  <read_first>
    - .planning/todos/pending/carimbo-frescor-blocos-cross-carteira.md — os 4 passos que o TODO exige, nesta ordem.
    - server/app/main.py — o bloco `return` do handler de `GET /api/options/curadoria` e o de `GET /api/options/proposta/{ticker}` (localizar por Grep de `"/api/options/curadoria"` e `"/api/options/proposta`).
    - web/src/App.jsx — a função `useCuradoria` (localizar por Grep `function useCuradoria`), só para confirmar o que ela guarda em `meta`.
    - web/src/opcoes/OpcoesScreen.jsx — `function escolherFrescor` e o trecho do `chip` dentro de `const cabecalho`, como fonte do vocabulário e da regra de precedência a reusar.
  </read_first>
  <action>
Medir, não supor. Responder por escrito no SUMMARY, com `arquivo:linha` para
cada resposta:

1. Que campos de frescor a resposta de `/api/options/curadoria` realmente
   carrega hoje (`at`? `source`? `pregao`? `atraso`? `frescor.medido`?) e quais
   deles sobrevivem até `ctx.curadoria` no cliente.
2. Idem para `/api/options/proposta/{ticker}` e o que sobrevive até
   `opcoesPorTicker[t]`.
3. Existe alguma chave de `copy.js`, nos DOIS ramos, que já expresse "consultado
   em <hora>"? (grep em `web/src/copy.js`). Se não existir, é preciso UMA chave
   nova — e ela entra nos dois ramos, nunca em um só.

Então escolher o ramo, e registrar a escolha explicitamente:
- **Ramo FRONT-ONLY** (esperado pelo que o planejamento mediu: `at` + `source`
  já chegam nos dois casos): nenhuma linha de backend muda, nenhum consumo novo
  de `mydata_budget`, nenhuma chamada de rede nova. Segue para a Task 2.
- **Ramo BACKEND** (só se a medição contradisser o esperado): PARAR antes de
  editar a rota da curadoria e registrar no SUMMARY o custo contra o orçamento
  medido do `mydata_budget` (Fase 31, D1) — a rota da curadoria varre
  vencimentos por posição e é a mais cara da aba. Neste ramo, o carimbo de
  Bloco A entra e o de Bloco B fica registrado como pendência nomeada, em vez de
  a fase inteira virar mudança de rota.

Regra que vale nos dois ramos (princípio 3 e 4 do CLAUDE.md): o carimbo é
derivado do dado da RESPOSTA. `Date.now()` no cliente é proibido — carimbaria a
hora em que a tela desenhou, não a hora do dado. Campo ausente/nulo exibe o
vocabulário de "não medido" (`cp.opcoesFrescorNaoMedido`), nunca a hora de hoje
no lugar.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('server/app/main.py','utf8'); const i=s.indexOf('/api/options/curadoria'); const t=s.slice(i,i+2600); console.log('curadoria devolve at:', /\"at\": now_str\(\)/.test(t)); const j=s.indexOf('/api/options/proposta'); const p=s.slice(j,j+7000); console.log('proposta devolve at+source:', /\"at\": now_str\(\)/.test(p) && /\"source\": source/.test(p))"</automated>
  </verify>
  <done>SUMMARY contém as 3 respostas com `arquivo:linha`, o ramo escolhido (FRONT-ONLY ou BACKEND) e, se BACKEND, a conta de custo contra `mydata_budget`. Nenhum arquivo de produção alterado nesta task.</done>
</task>

<task type="auto">
  <name>Task 2: Criar SecaoDescobrir.jsx com a frase-ponte, os dois blocos e o carimbo de frescor</name>
  <files>web/src/opcoes/SecaoDescobrir.jsx, web/src/opcoes/OpcoesScreen.jsx, web/src/copy.js</files>
  <read_first>
    - web/src/opcoes/OpcoesScreen.jsx — faixas 705-760 (as três consts) e 800-820 (a árvore de render). Reconfirmar as linhas por Grep antes de editar.
    - web/src/opcoes/OportunidadesOpcoes.jsx e web/src/opcoes/CuradoriaEstruturas.jsx — as assinaturas de props dos dois componentes que a seção vai compor (ler os headers e a linha da `function`).
    - .planning/research/PITFALLS.md — Pitfall 1 (split muda quem decide e quem narra) e Pitfall 2 (frase-ponte).
    - web/src/opcoes/uiOpcoes.jsx — criado no 33-01; ver o que já está disponível para importar em vez de redeclarar.
  </read_first>
  <action>
Criar `web/src/opcoes/SecaoDescobrir.jsx`, `export default function SecaoDescobrir`,
recebendo por prop: `opcoesPorTicker`, `opcoesPorTickerCarregando`, `carteira`,
`curadoria` (o objeto `ctx.curadoria` inteiro, referência única), `onAbrir`,
`onExecutar`, `onNarrar`, `onRecarregar`, `operador`, `palette`, `cp`. O
componente é um WRAPPER FINO: compõe `<OportunidadesOpcoes>` e
`<CuradoriaEstruturas>` com as MESMAS props que recebem hoje, sem reescrever
nenhum dos dois e sem tocar no conteúdo deles.

Ordem interna obrigatória e literal: frase-ponte (`cp.duasLeiturasIntro`) →
Bloco A (`<OportunidadesOpcoes>`) → Bloco B (`<CuradoriaEstruturas>`). A
frase-ponte é renderizada incondicionalmente, numa linha isolada, sem `onClick`,
sem `aria-expanded`, sem nenhum wrapper condicional — copiar o comentário de
decisão D-05 que vive hoje acima dela em `OpcoesScreen.jsx` para dentro do
arquivo novo, VERBATIM, e acrescentar nota datada 2026-09-19 dizendo que ela
migrou junto com o Bloco B de propósito (Pitfall 2).

REORG-07, explícito: `curadoria.top` e `curadoria.meta` são consumidos por
REFERÊNCIA e por ÍNDICE. Proibido `.sort(`, `.filter(`, `.slice(` ou qualquer
recomposição desses dois dentro de `SecaoDescobrir.jsx` — a seleção é do motor
determinístico, e reordenar por conveniência de exibição faria a IA/UI decidir
o que o motor decidiu.

Carimbo de frescor (D-04b), no ramo escolhido na Task 1:
- Um carimbo por bloco, no cabeçalho de cada um (não um só para os dois — são
  motores e respostas diferentes, e um carimbo compartilhado afirmaria a mesma
  hora para duas medições distintas).
- Bloco B: derivar de `curadoria.meta.at` e `curadoria.meta.source`.
- Bloco A: derivar do `at`/`source` das respostas em `opcoesPorTicker`. Como é
  um bloco agregado sobre N tickers, o carimbo tem de dizer honestamente o que
  representa: usar o `at` MAIS ANTIGO entre os tickers exibidos (é a idade real
  do conjunto; o mais recente esconderia o dado velho). Se os `source`
  divergirem entre tickers, exibir o rótulo de fonte só quando for o mesmo para
  todos — nunca eleger um por maioria.
- Reusar `cp.opcoesFonteRotulo` para a fonte e `cp.opcoesFrescorNaoMedido` para
  o caso sem campo. Se a Task 1 concluiu que falta chave para a hora, criar UMA
  chave nova em `web/src/copy.js` nos DOIS ramos (`estudo` e `operador`),
  escrita no mesmo tom das vizinhas de frescor — voz diferente por modo onde as
  vizinhas também diferem, igual onde as vizinhas são iguais.
- Zero chamada de rede nova. Zero `Date.now()`.

Em `OpcoesScreen.jsx`: apagar as três consts (`fraseDuasLeituras`,
`blocoOportunidades`, `blocoCuradoria`), remover os imports de
`OportunidadesOpcoes`/`CuradoriaEstruturas` se deixarem de ser usados ali,
importar `SecaoDescobrir`, e substituir as três linhas 810-812 por
`<SecaoDescobrir ... />` na MESMA posição — imediatamente antes de
`<SecaoVigias`. Preservar o comentário da Fase 32 (32-03, D-02) que explica por
que os blocos ficam DENTRO da sub-aba Setups, com nota datada da Fase 33.
`ctx.curadoria` continua sendo lido em `OpcoesScreen.jsx` e descendo por prop —
nenhuma instância nova de `useCuradoria`/`useOpcoesPropostas` (REORG-03/04).

NÃO commitar: o commit sai no fim da Task 3, código e guardiões juntos.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('web/src/opcoes/SecaoDescobrir.jsx','utf8'); const o=require('fs').readFileSync('web/src/opcoes/OpcoesScreen.jsx','utf8'); const f=(m)=>{console.log('FALHOU '+m);process.exitCode=1}; if(/store\.|api\./.test(s))f('SecaoDescobrir chama store/api'); if(/useCuradoria|useOpcoesPropostas|useOpcoesMcp/.test(s))f('instancia hook'); if(/App\.jsx/.test(s))f('importa App.jsx'); if(/Date\.now\(\)/.test(s))f('carimbo derivado do relogio do cliente'); if(/\.(sort|filter)\(/.test(s.replace(/\/\*[\s\S]*?\*\//g,'')))f('reordena/filtra dado do motor (REORG-07)'); if(/\.manchete/.test(s))f('renderiza manchete propria (REORG-06)'); if(!/duasLeiturasIntro/.test(s))f('frase-ponte nao migrou'); const iF=s.indexOf('duasLeiturasIntro'), iA=s.indexOf('<OportunidadesOpcoes'), iB=s.indexOf('<CuradoriaEstruturas'); if(!(iF>-1&&iA>iF&&iB>iA))f('ordem frase->BlocoA->BlocoB nao preservada'); if(!/<SecaoDescobrir/.test(o))f('OpcoesScreen nao renderiza SecaoDescobrir'); if(o.indexOf('<SecaoDescobrir')>o.indexOf('<SecaoVigias'))f('SecaoDescobrir renderizada depois dos vigias'); console.log(process.exitCode?'':'ok estrutura de SecaoDescobrir')"</automated>
    <automated>npx vite build</automated>
  </verify>
  <done>`SecaoDescobrir.jsx` existe compondo os dois blocos com a frase-ponte imediatamente acima do Bloco B; `OpcoesScreen.jsx` renderiza `<SecaoDescobrir` antes de `<SecaoVigias`; carimbo de frescor visível nos dois blocos, derivado de `at`/`source` da resposta; chave nova de copy (se houver) nos DOIS ramos; `vite build` verde.</done>
</task>

<task type="auto">
  <name>Task 3: Generalizar o guardrail CVM (REORG-06) e reapontar os 4 guardiões</name>
  <files>web/tests/test_opcoes_subabas_ui.mjs, web/tests/test_opcoes_consolidacao_ui.mjs, web/tests/test_curadoria_ui.mjs, web/tests/test_carteira_opcoes_tira.mjs</files>
  <read_first>
    - web/tests/test_opcoes_subabas_ui.mjs — linhas 100-116 (padrão de varredura por diretório) e 141-154 (o guardrail CVM de escopo estreito que precisa generalizar).
    - web/tests/test_opcoes_consolidacao_ui.mjs — linhas 77-105 (regras 1 e 2), 144-150 (regra 8), 152-160 (regra 9), 168-178 (regra 11), 205-225 (regra 13).
    - web/tests/test_curadoria_ui.mjs — linhas 283-321 (as asserções que ancoram `<CuradoriaEstruturas`/`{blocoCuradoria}`/`ctx.curadoria.top` em OpcoesScreen.jsx).
    - web/tests/test_carteira_opcoes_tira.mjs — linhas 165-180 (item 4).
    - .planning/research/PITFALLS.md — Pitfall 4 (guardrail CVM com escopo fixo).
  </read_first>
  <action>
Censo primeiro: `grep -n "blocoOportunidades\|blocoCuradoria\|fraseDuasLeituras\|<OportunidadesOpcoes\|<CuradoriaEstruturas\|ctx.curadoria\|executarCandidatoCurado\|manchete" web/tests/*.mjs`.
Qualquer arquivo fora da lista abaixo entra no escopo e vai para o SUMMARY.

(A) REORG-06 — generalizar o guardrail CVM em `test_opcoes_subabas_ui.mjs`,
regra 5. Manter as duas asserções POSITIVAS de delegação em `SubAbaOperar`
(`<PropostaLastreada`, `<CandidatoOpcao`) como estão. Acrescentar uma regra
nova por varredura de diretório, no padrão das linhas 100-116: para todo
`.jsx` de `web/src/opcoes/`, se o arquivo não estiver na lista nomeada de
RENDERIZADORES autorizados (`PropostaLastreada.jsx`, `CandidatoOpcao.jsx`,
`CuradoriaEstruturas.jsx`, `OportunidadesOpcoes.jsx` — os 4 medidos por grep em
2026-09-19), então ele não pode conter `/\.manchete/` nem
`/\{[^}]*\bmanchete\b[^}]*\}/`. Duas asserções de sanidade obrigatórias, senão
a regra pode virar vácua: (1) a varredura acha pelo menos 15 arquivos; (2) cada
arquivo da lista autorizada CONTÉM de fato `.manchete` — uma allowlist que
envelheceu (arquivo que parou de renderizar manchete) esconderia o dia em que a
lista passou a autorizar algo que não existe. Nota datada explicando que o
escopo de variável única deixou de bastar porque a Fase 33 cria seções novas
capazes de exibir candidato.

(B) `test_opcoes_consolidacao_ui.mjs`:
- regra 1: os marcadores `{fraseDuasLeituras}`/`{blocoOportunidades}`/
  `{blocoCuradoria}` somem de `OpcoesScreen.jsx`. A garantia de ORDEM se divide
  em duas, e nenhuma das duas é mais fraca: em `OpcoesScreen.jsx`,
  `indexOf("<SecaoDescobrir") < indexOf("<SecaoVigias")`; em
  `SecaoDescobrir.jsx`, `indexOf("duasLeiturasIntro") < indexOf("<OportunidadesOpcoes")
  < indexOf("<CuradoriaEstruturas")`. Checar `>= 0` antes de comparar.
- regra 2 (frase-ponte incondicional, D-05): passa a ser medida em
  `SecaoDescobrir.jsx` — sem `aria-expanded`, sem `onClick` no trecho da
  frase, e o uso numa linha isolada sem `&&`/`?` envolvendo o marcador.
- regra 8 (nada de `sticky`/`fixed`/`<input>` entre a frase-ponte e os vigias):
  o trecho agora atravessa DOIS arquivos. Medir a concatenação
  `SecaoDescobrir.jsx` inteiro + o trecho de `OpcoesScreen.jsx` entre
  `<SecaoDescobrir` e `<SecaoVigias` — a intenção (nada se intromete entre a
  desambiguação e os vigias) fica idêntica.
- regra 9: `onExecutar={(cand, o) => ctx.A.executarCandidatoCurado(cand, o)}`
  passa a ser exigido exatamente 1× somando `OpcoesScreen.jsx` +
  `SecaoDescobrir.jsx` (o fio pode ficar no orquestrador e descer por prop, ou
  ir para a seção — o que NÃO pode é existir duas vezes).
- regra 11 (`scrollIntoView`/âncora `posicao-`): estender a proibição a
  `SecaoDescobrir.jsx` — melhor ainda, converter para varredura de diretório.
- regra 13: `concluido={...ctx.curadoria.concluido}` passa a ser exigido onde o
  fio realmente ficou (`OpcoesScreen.jsx` se a prop descer daí, ou
  `SecaoDescobrir.jsx`); a garantia WR-01 ("vazio nunca antes da primeira busca
  terminar") NÃO muda.

(C) `test_curadoria_ui.mjs` (achado do planejamento, não estava na lista do
CONTEXT.md): `<CuradoriaEstruturas` deixa de aparecer em `OpcoesScreen.jsx`.
Migrar para: exatamente 1× em toda a pasta `web/src/opcoes/` (varredura de
diretório), 0× em `App.jsx`; `<CuradoriaEstruturas` depois de
`<OportunidadesOpcoes` medido em `SecaoDescobrir.jsx`; a leitura
`ctx.curadoria.top`/`.meta` continua exigida em `OpcoesScreen.jsx` (é lá que a
fonte única é lida e repassada) — se o executor optou por passar o objeto
inteiro, a asserção passa a exigir `curadoria={ctx.curadoria}` em
`OpcoesScreen.jsx` E a leitura de `.top`/`.meta` em `SecaoDescobrir.jsx`. A
garantia é "uma fonte, N leituras, nenhuma segunda instância do hook" — não o
texto literal antigo.

(D) `test_carteira_opcoes_tira.mjs` item 4: `<OportunidadesOpcoes` passa a ser
1× na pasta `web/src/opcoes/` (varredura) e 0× em `App.jsx`; o par de ordem
`{blocoOportunidades}`→`{blocoVigias}` vira `<SecaoDescobrir`→`<SecaoVigias` em
`OpcoesScreen.jsx`.

Se a Task 2 criou chave nova em `copy.js`, acrescentá-la à lista `CHAVES` de
paridade entre modos do guardião pertinente (`test_opcoes_mcp_aba_ui.mjs` ou
`test_opcoes_vigias_ui.mjs`, o que já tiver a lista de frescor) — chave nova em
um ramo só deixa metade da base sem a informação.

Commit único com a Task 2: `feat(33-02): extrai SecaoDescobrir, generaliza guardrail CVM e carimba frescor`.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <done>Guardrail CVM cobre o diretório inteiro com as duas sanidades; as regras 1/2/8/9/11/13 de consolidação, as de `test_curadoria_ui.mjs` e o item 4 de `test_carteira_opcoes_tira.mjs` medem a topologia nova sem afrouxar garantia; suíte canônica exit 0; um commit só com o código.</done>
</task>

<task type="auto">
  <name>Task 4: Prova negativa do guardrail CVM e da frase-ponte + baseline</name>
  <files>web/src/opcoes/SecaoDescobrir.jsx</files>
  <read_first>
    - web/tests/test_opcoes_subabas_ui.mjs — a regra CVM já generalizada na Task 3.
    - web/tests/test_opcoes_consolidacao_ui.mjs — as regras 1/2/8 já reescritas.
  </read_first>
  <action>
Injetar um defeito por vez em `SecaoDescobrir.jsx`, rodar o guardião
correspondente, confirmar exit 1 nomeando a asserção certa, reverter com
`git checkout --` e conferir `git diff` vazio. Registrar comando e saída real
no SUMMARY para cada um:

1. Renderizar `{cand.manchete}` (ou qualquer `.manchete`) dentro de
   `SecaoDescobrir.jsx` — a regra CVM generalizada tem de reprovar nomeando o
   arquivo. Esta é a prova de REORG-06: antes desta fase, o mesmo defeito
   passava calado porque o guardião só olhava `SubAbaOperar`.
2. Envolver a frase-ponte num toggle (`onClick` + estado de colapso) — a regra
   2 de consolidação tem de reprovar (D-05).
3. Mover a frase-ponte para DEPOIS de `<CuradoriaEstruturas` — a regra de ordem
   interna tem de reprovar (Pitfall 2: adjacência, não existência).
4. Acrescentar `.sort()` sobre `curadoria.top` antes de passar ao Bloco B — se
   nenhum guardião reprovar, criar a asserção que reprova (REORG-07 não pode
   depender de code review manual) e só então seguir.
5. Trocar o carimbo de frescor por `new Date().toLocaleTimeString()` — se
   nenhum guardião reprovar, criar a asserção que proíbe `Date.now()`/`new Date(`
   como fonte de carimbo em `web/src/opcoes/` (princípio 3 e 4) e repetir.

Fechar com `bash scripts/executar.sh --testes` e `npx vite build`, registrando
os números — baseline de entrada do 33-03.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
    <automated>git status --porcelain</automated>
  </verify>
  <done>5 injeções com saída real registrada; as injeções 4 e 5 geraram asserção nova se ainda não existia; tudo revertido; suíte canônica exit 0 com números no SUMMARY; `vite build` verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | Movimentação de JSX já publicado + exibição de dois campos (`at`, `source`) que a resposta já entrega ao cliente hoje. Nenhuma rota nova, nenhum input novo, nenhum segredo, nenhum dado novo saindo do servidor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-04 | — (compliance CVM, não segurança) | `SecaoDescobrir.jsx` e o guardrail de manchete | mitigate | REORG-06: a manchete é do motor determinístico e vai verbatim; a generalização do guardião por varredura de diretório, com as duas sanidades da Task 3(A), é o controle. É controle REGULATÓRIO (CLAUDE.md, guardrail CVM), não de segurança — e não pode regredir por mudança de topologia de arquivo. |
| T-33-05 | — (compliance) | frase-ponte `cp.duasLeiturasIntro` | mitigate | Mitigação regulatória D-05 amarrada à adjacência física com o Bloco B; injeções 2 e 3 da Task 4 provam que o guardião a defende. |
| T-33-06 | Information Disclosure | carimbo de frescor | accept | `at` e `source` já são devolvidos ao mesmo cliente autenticado pelas mesmas rotas; exibi-los não amplia superfície. `source` nomeia o provedor de cadeia (Yahoo/brapi), informação já exposta no cabeçalho da aba hoje. |
| T-33-07 | Tampering | `curadoria.top` | mitigate | REORG-07: referência imutável, sem `.sort()`/`.filter()`; injeção 4 da Task 4 cria a asserção se ela não existir. Reordenar seria a IA/UI decidindo o que o motor decidiu (CLAUDE.md princípio 5). |
</threat_model>

<verification>
1. `bash scripts/executar.sh --testes` — pytest + `.mjs`, exit 0. `scripts/test.sh` sozinho não conta.
2. `npx vite build` verde.
3. `git diff --stat web/src/App.jsx` vazio (REORG-04).
4. `grep -c "manchete" web/src/opcoes/SecaoDescobrir.jsx` → 0 (ou só em linha de comentário; usar `grep -v '^\s*[/*]'` antes de contar).
5. As 5 injeções da Task 4 com saída real no SUMMARY.
6. Conferência manual, contra o comentário D-05 original: a leitura "as 4 melhores DESTE bloco, não do app" continua garantida no arranjo novo.
</verification>

<success_criteria>
- `SecaoDescobrir.jsx` compõe Bloco A e Bloco B sem reescrevê-los, com a frase-ponte incondicional imediatamente acima do Bloco B.
- Guardrail CVM generalizado por varredura de diretório, com sanidade de contagem e de allowlist não-vencida.
- Os dois blocos exibem carimbo de frescor derivado de `at`/`source` da resposta; ausência de campo vira "não medido", nunca hora inventada.
- Ordem visível idêntica à de hoje; `App.jsx` intocado; suíte canônica e `vite build` verdes.
- NÃO publicado: sem `bump.sh`, sem `publicar-web.sh`, sem push.
</success_criteria>

<output>
Create `.planning/phases/33-extra-o-dos-5-jobs-em-componentes-pr-prios/33-02-SUMMARY.md` when done, registrando: o achado da Task 1 (ramo FRONT-ONLY ou BACKEND, com `arquivo:linha`), as 5 injeções com saída real, e os números da suíte.
</output>
