---
phase: 14-opcoes-lastreadas
plan: 06
type: execute
wave: 5
depends_on: ["14-05"]
files_modified:
  - web/src/copy.js
  - web/src/App.jsx
  - web/tests/test_opcoes_proposta_ui.mjs
autonomous: true
requirements: []

must_haves:
  truths:
    - "O card do ativo mostra uma proposta pronta de venda coberta ou put de proteção, no mesmo formato visual da manchete de decisão"
    - "A cadeia completa continua acessível abaixo da proposta, para escolher outro strike ou vencimento"
    - "Em Modo Estudo a proposta é explicada sem nenhum botão de executar"
    - "Em Modo Operador a proposta ganha CTA, com confirmação que declara a trava do lastro antes de abrir"
    - "Sem lastro, sem setup ou com cadeia degradada, a ausência é dita com o motivo do motor"
  artifacts:
    - path: "web/src/App.jsx"
      provides: "Componente da proposta lastreada dentro do AtivoCard"
      contains: "PropostaLastreada"
    - path: "web/src/copy.js"
      provides: "Rótulos e confirmações das operações lastreadas nos dois modos"
      contains: "ctaVendaCoberta"
    - path: "web/tests/test_opcoes_proposta_ui.mjs"
      provides: "Guardião da proposta na UI (manchete do motor, split de modo, cadeia preservada)"
  key_links:
    - from: "web/src/App.jsx AtivoCard"
      to: "store.optionsProposta"
      via: "busca da proposta por ticker"
      pattern: "store\\.optionsProposta"
    - from: "web/src/App.jsx PropostaLastreada"
      to: "proposta.manchete"
      via: "renderização direta da frase do motor"
      pattern: "proposta\\.manchete"
---

<objective>
A superfície principal da fase: o **card de proposta** dentro do `AtivoCard`,
com a cadeia completa preservada como acordeão abaixo (D-4), e o split de modo
(D-5) — Estudo explica em linguagem condicional, Operador executa.

Purpose: é o que o usuário vê. O UI-SPEC já está aprovado; este plano o
implementa sem inventar sistema visual novo (reusa o token `T` e a linguagem do
Hero reconciliado v11).
Output: componente `PropostaLastreada`, fiação no `AtivoCard`, rótulos em
`copy.js`, guardião de UI, build do Vite verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-op-es-lastreadas-venda-coberta-e-put-de-prote-o-sobre-posi-e/14-CONTEXT.md
@.planning/phases/14-op-es-lastreadas-venda-coberta-e-put-de-prote-o-sobre-posi-e/14-UI-SPEC.md
@.planning/phases/14-op-es-lastreadas-venda-coberta-e-put-de-prote-o-sobre-posi-e/14-05-SUMMARY.md
@CLAUDE.md

<interfaces>
Store (plano 14-05, fonte `web/src/persistence.js`):
- `store.optionsProposta(t)` → `{ticker, providerStatus, modo, proposta, motivo, motivoTexto, putSemLastro}`
- `proposta` = `null` ou `{tipo:"call_coberta"|"put_protecao", contractSymbol, optionType, strike, expiration, diasParaVencimento, contratos, qtyAcoes, premioUnitario, premioTotal, lastro:{t,qtyLivre}, liquidez:{score,label}, manchete, didatica, chips:[{k,v}]}`
- `store.optionsAbrirLastreada({underlying, contractSymbol, expiration, contratos})`
- `store.optionsFecharLastreada({contractSymbol, contratos})`

UI existente (fonte `web/src/App.jsx`):
- `AtivoCard` (linha ~3018) recebe `vm` com `t, q, cur, pos, decColor, decBg, decM, operador, data, A, cp, contexto`
- manchete de decisão: bloco na linha ~3170 — eyebrow `fontSize 10px`/`letterSpacing 0.04em` + manchete `fontSize 17px`/`fontWeight 800`, cor `decColor`
- `OpcoesCamada` (linha ~2970) e `OpcaoContrato` (linha ~2901): acordeão da cadeia, gate por `opGate.liquida` (linha ~3246)
- `A.buyOption`/`A.sellOption` (linhas ~7496-7510) e o par MORTO `A.setOptionStop`/`A.setOptionAlvo` (linhas ~7512-7513)
- `window.confirm` já é o mecanismo de confirmação destrutiva do app (`resetPortfolio`, linha ~6568)
- tokens: `T.accent`, `T.accentTint`, `T.positive`, `T.negative`, `T.bgBase`, `T.bgCard`, `T.textSecondary`, `T.textFaint`, `MONO`

Guardião de copy (fonte `web/tests/test_copy_theme.mjs`): as chaves de `COPY.estudo` e `COPY.operador` devem ser IDÊNTICAS, e nenhum valor do ramo `estudo` pode casar com `/COMPRAR|VENDER/i` (a única isenção é a lista "Simular compra|Simular venda|venda simulada|compra simulada"). Consequência prática: os rótulos de Estudo não podem conter as palavras "vender"/"comprar".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rótulos e confirmações das operações lastreadas em copy.js</name>
  <files>web/src/copy.js, web/tests/test_opcoes_proposta_ui.mjs</files>
  <read_first>
    - web/src/copy.js (cabeçalho com as convenções e os dois ramos `estudo`/`operador`; ver `btnComprar`/`btnVender` como precedente de rótulo por modo)
    - web/tests/test_copy_theme.mjs (linhas 26-31: o guardião de chaves espelhadas e o de vocabulário de ordem no ramo estudo)
    - .planning/phases/14-op-es-lastreadas-venda-coberta-e-put-de-prote-o-sobre-posi-e/14-UI-SPEC.md (seção "Copywriting Contract" — texto verbatim)
  </read_first>
  <action>
Acrescentar em `web/src/copy.js` as MESMAS chaves nos dois ramos:

- `eyebrowPropostaCall` / `eyebrowPropostaPut` — Operador: `"PROPOSTA · VENDA COBERTA"` e `"PROPOSTA · PUT DE PROTEÇÃO"`. Estudo: `"ESTUDO · VENDA COBERTA"` e `"ESTUDO · PUT DE PROTEÇÃO"`.
- `ctaVendaCoberta(n, ticker, strike, premio)` / `ctaPutProtecao(n, ticker, strike, premio)` — Operador devolve o rótulo dinâmico do UI-SPEC (`"Vender {n}× CALL {TICKER} · strike {K} — recebe R$ {prêmio}"` e `"Comprar {n}× PUT {TICKER} · strike {K} — custa R$ {prêmio}"`). Estudo devolve rótulo SEM verbo de ordem — por exemplo `"Ver como esta operação funcionaria"` — porque o ramo estudo não renderiza CTA e o guardião de vocabulário rejeita "vender"/"comprar" ali.
- `ctaFecharLastreada(custo)` — Operador: `"Recomprar a call — R$ {custo}"`; Estudo: `"Como esta posição se encerra"`.
- `confirmAbrirCoberta(n, ticker, qty)` — texto verbatim do UI-SPEC: `"Vender {N} call(s) de {TICKER} trava {qty} ação(ões) do seu lote-lastro até você recomprar a call ou ela vencer. Continuar?"`. No ramo estudo a chave existe (paridade de chaves) devolvendo string vazia — o fluxo nunca a alcança.
- `confirmFecharCoberta(custo, qty, ticker)` — verbatim: `"Recomprar esta call por R$ {custo} destrava {qty} ação(ões) de {TICKER} imediatamente. Continuar?"`; ramo estudo devolve string vazia.
- `verCadeiaCompleta` — os dois modos: `"ver cadeia completa"`.
- `propostaIndisponivelDegradada` — os dois modos: `"Proposta indisponível — cotação de opções degradada."`
- `propostaVaziaTitulo` — os dois modos: `"Sem proposta agora"`.

Nenhuma dessas chaves duplica texto que já vem do backend: a MANCHETE e a frase didática vêm de `proposta.manchete`/`proposta.didatica` (motor determinístico, guardrail CVM) e o texto de ausência vem de `motivoTexto`. `copy.js` cuida só de rótulo de controle e de confirmação.

Iniciar `web/tests/test_opcoes_proposta_ui.mjs` com a parte de copy: as chaves novas existem nos dois ramos; `COPY.estudo.ctaVendaCoberta(...)` não contém "vender"/"comprar"; `COPY.operador.confirmAbrirCoberta(2,"PETR4",200)` contém "trava" e "200"; nenhuma chave nova carrega manchete de proposta (asserção negativa: nenhum valor do copy contém "Se você tivesse", que é texto do backend).
  </action>
  <verify>
    <automated>cd web &amp;&amp; node tests/test_copy_theme.mjs &amp;&amp; node tests/test_opcoes_proposta_ui.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && node tests/test_copy_theme.mjs` passa (chaves espelhadas + ramo estudo sem vocabulário de ordem)
    - `grep -c "ctaVendaCoberta" web/src/copy.js` retorna 2 (uma por modo)
    - `grep -c "Se você tivesse" web/src/copy.js` retorna 0 (a frase didática é do backend)
  </acceptance_criteria>
  <done>Todo rótulo de controle das operações lastreadas vem do `copy.js`, por modo, e nenhum texto de decisão foi duplicado no front.</done>
</task>

<task type="auto">
  <name>Task 2: Componente PropostaLastreada e fiação no AtivoCard</name>
  <files>web/src/App.jsx</files>
  <read_first>
    - web/src/App.jsx, linhas 2896-3012 (`OpcaoContrato`, `OpcoesCamada`) e 3018-3260 (`AtivoCard`: estados `opOpen/opGate/opChain`, `toggleOp`, `myOptionPositions`, `doBuyOption`/`doSellOption`, bloco da manchete e ponto de render de `OpcoesCamada`)
    - web/src/App.jsx, linhas 7490-7515 (`A.buyOption`, `A.sellOption`, `A.setOptionStop`, `A.setOptionAlvo`)
    - web/src/App.jsx, linha ~6568 (`window.confirm` do `resetPortfolio` — mecanismo de confirmação já adotado)
    - .planning/phases/14-op-es-lastreadas-venda-coberta-e-put-de-prote-o-sobre-posi-e/14-UI-SPEC.md na íntegra (escala 4/8/16/24 para os elementos NOVOS; tipografia 12/400, 10/800, 13/800, 17/800; cor da manchete por polaridade `T.positive` para CALL e `T.negative` para PUT, NUNCA `T.accent`)
  </read_first>
  <action>
1) Criar a função `PropostaLastreada({ r, operador, cp, busy, onAbrir, onFechar, posAberta })` em `web/src/App.jsx`, logo ACIMA de `OpcoesCamada`, seguindo o UI-SPEC:
- Sem `r` (ainda carregando): não renderiza nada (silêncio, não esqueleto — a proposta é secundária ao card).
- `r.proposta` nula: card discreto com `cp.propostaVaziaTitulo` (Label 10/800, `T.textFaint`) e `r.motivoTexto` (Body 12/400/1.5, `T.textMuted`). Quando `r.motivo === "degradado"`, o corpo é `cp.propostaIndisponivelDegradada`.
- `r.proposta` presente: superfície `T.bgCard`, `padding: "16px"`, `borderRadius: "11px"`, com — na ordem — eyebrow (`cp.eyebrowPropostaCall`/`cp.eyebrowPropostaPut`, 10px/800, `letterSpacing 0.04em`, cor `T.accent`), manchete `r.proposta.manchete` (17px/800, cor `T.positive` para `optionType === "call"` e `T.negative` para `"put"`), linha estrutural do contrato em `MONO` (13px/800, formato `"{contratos}× {CALL|PUT} {TICKER} · strike {strike}"`), fileira de chips com `r.proposta.chips` (gap 8px, mesmo formato de pílula já usado no card) e o disclaimer curto em Body.
- Modo Estudo (`operador === false`): renderiza `r.proposta.didatica` como corpo e NENHUM botão de executar.
- Modo Operador: botão primário com `cp.ctaVendaCoberta(...)`/`cp.ctaPutProtecao(...)`, borda/tint `T.accent`, `minHeight: "40px"` (mesma altura do CTA de compra do `OpcaoContrato`); desabilitado com o rótulo de degradado quando `r.providerStatus !== "ok"`. Com `posAberta` (já existe posição lastreada neste contrato), o botão vira `cp.ctaFecharLastreada(...)` chamando `onFechar`.
- Espaçamento entre o card da proposta e o acordeão da cadeia: 24px (`lg` do UI-SPEC).

2) Fiação no `AtivoCard`:
- Estado `opProposta` + `useEffect` por `t` chamando `store.optionsProposta(t)` (best-effort: falha só deixa `null`, igual `optionsGate`). Disparar a busca APENAS quando `opGate && opGate.liquida` for verdadeiro — sem gate de liquidez, a fase inteira fica dormente e nenhuma requisição extra é feita (D-8).
- Renderizar `<PropostaLastreada …>` imediatamente ACIMA de `<OpcoesCamada …>`, dentro do mesmo bloco condicional do gate.
- `onAbrir`: `window.confirm(cp.confirmAbrirCoberta(...))` para a CALL coberta antes de chamar `A.abrirLastreada`; a PUT de proteção não trava nada, então não usa confirmação destrutiva (declarar isso em comentário — a confirmação existe pela TRAVA, não pelo gasto).
- `onFechar`: `window.confirm(cp.confirmFecharCoberta(...))` antes de `A.fecharLastreada`.
- `posAberta` sai de `myOptionPositions.find((p) => p.id === r.proposta.contractSymbol)`.

3) Ações em `A` (junto de `A.buyOption`/`A.sellOption`): `abrirLastreada(body)` e `fecharLastreada(body)` chamando os métodos do store, absorvendo o estado devolvido com `setData`, com `flash` de sucesso e de erro (a mensagem do 403 de Modo Estudo e a do 502 de cadeia degradada já chegam prontas do servidor — não reescrever no front).

4) Remover o código morto `A.setOptionStop` e `A.setOptionAlvo` (D-1: o redesenho os substitui). O método `putOptionPosition` PERMANECE nos dois stores — o agente ainda o usa para o trailing das posições do modelo antigo; remover só a ponta morta da UI.

5) `OpcoesCamada`: trocar o rótulo do cabeçalho fechado para `cp.verCadeiaCompleta` quando existe proposta (a cadeia deixa de ser a entrada principal e passa a ser o "escolher outro strike"), mantendo o rótulo atual quando não há proposta. Nenhuma outra mudança nesse componente nem em `OpcaoContrato` — o UI-SPEC diz explicitamente que eles são reusados sem retrofit de escala.
  </action>
  <verify>
    <automated>cd web &amp;&amp; npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx vite build` conclui sem erro (convenção do repo: grep e teste estático não pegam erro de sintaxe JSX)
    - `grep -c "function PropostaLastreada" web/src/App.jsx` retorna 1
    - `grep -c "setOptionStop\|setOptionAlvo" web/src/App.jsx` retorna 0
    - `grep -c "store.optionsProposta" web/src/App.jsx` retorna 1
    - `grep -c "proposta.manchete" web/src/App.jsx` retorna 1 ou mais e `grep -c "T.accent" ` na linha da manchete retorna 0 (a manchete nunca usa a cor de acento — regra do UI-SPEC)
  </acceptance_criteria>
  <done>O card do ativo mostra a proposta do motor com CTA em Operador e explicação em Estudo, a cadeia completa continua um toque abaixo, e o par morto de stop/alvo de opção saiu da UI.</done>
</task>

<task type="auto">
  <name>Task 3: Guardião de UI da proposta</name>
  <files>web/tests/test_opcoes_proposta_ui.mjs</files>
  <read_first>
    - web/tests/test_hero_reconciliado.mjs (o guardião da manchete única — este novo é o irmão dele para a proposta: mesma técnica de leitura estática de `App.jsx` + regex)
    - web/tests/test_decisao_modo.mjs (padrão de asserção do split Estudo × Operador)
    - web/src/App.jsx (o código recém-escrito no Task 2)
  </read_first>
  <action>
Completar `web/tests/test_opcoes_proposta_ui.mjs` com as asserções de UI (leitura estática de `web/src/App.jsx`, sem build):
- A manchete da proposta vem do backend: existe `proposta.manchete` renderizado e NÃO existe composição de frase de proposta no front (asserção negativa contra as strings "Vender " + variável e "Se você tivesse" dentro de `App.jsx`).
- Split de modo: o bloco de CTA está sob condição de `operador`, e a frase didática (`didatica`) está sob a condição contrária — prova que Estudo não renderiza botão de executar.
- A cadeia sobrevive: `OpcoesCamada` continua sendo renderizada, e `PropostaLastreada` aparece ANTES dela no arquivo (índice de `indexOf` do JSX).
- Gate de dormência: a chamada de `store.optionsProposta` está dentro de condição que depende de `opGate`.
- Confirmação da trava: existe `window.confirm` com `cp.confirmAbrirCoberta` no caminho da CALL coberta.
- Cor da manchete por polaridade: existe `T.positive`/`T.negative` decidido por `optionType` no componente da proposta, e a manchete não usa `T.accent`.
- Código morto removido: `setOptionStop`/`setOptionAlvo` não aparecem mais em `App.jsx`, e `putOptionPosition` CONTINUA em `persistence.js` (o backend legado depende).
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && node tests/test_opcoes_proposta_ui.mjs` passa com 8 ou mais asserções
    - `bash scripts/executar.sh --testes` sem falhas (backend + web)
    - `cd web && node tests/test_hero_reconciliado.mjs` continua passando (a proposta não competiu com a manchete do ativo)
  </acceptance_criteria>
  <done>Existe um guardião que quebra se alguém fizer o front compor a manchete da proposta, sumir com a cadeia, ou dar CTA de executar ao Modo Estudo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API→UI | `manchete`/`didatica`/`motivoTexto` chegam do servidor e são renderizados como texto |
| usuário→UI | toque no CTA dispara ordem simulada com trava de lastro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-22 | Spoofing | manchete da proposta | mitigate | o front NUNCA compõe a frase — renderiza `proposta.manchete` do motor; guardião de teste quebra se alguém reintroduzir composição no cliente (guardrail CVM, mesmo regime da manchete de ação) |
| T-14-23 | Elevation of Privilege | CTA visível em Modo Estudo | mitigate | CTA condicionado a `operador` na UI E recusado com 403 no servidor (plano 14-03) — defesa em duas camadas, nunca só o botão |
| T-14-24 | Tampering | abertura acidental travando o lastro | mitigate | `window.confirm` declara quantas ações ficam travadas antes de abrir |
| T-14-25 | Information Disclosure | XSS por texto do servidor | accept | React escapa texto por padrão; nenhum `dangerouslySetInnerHTML` é introduzido |
| T-14-SC | Tampering | npm installs | accept | nenhuma dependência nova nesta fase |
</threat_model>

<verification>
- `cd web && npx vite build` verde
- `bash scripts/executar.sh --testes` verde
- `git diff --stat web/src/App.jsx` mostra o componente novo e a remoção do par morto; `OpcaoContrato` sem mudanças além do rótulo do cabeçalho
</verification>

<success_criteria>
- Proposta e cadeia convivem na mesma superfície, com a proposta acima
- Estudo explica e Operador executa, com trava declarada na confirmação
- Nenhum texto de decisão nasce no front
- Build do Vite passa (regra do repositório para qualquer mudança em `web/src/`)
</success_criteria>

<output>
Create `.planning/phases/14-op-es-lastreadas-venda-coberta-e-put-de-prote-o-sobre-posi-e/14-06-SUMMARY.md` when done
</output>
