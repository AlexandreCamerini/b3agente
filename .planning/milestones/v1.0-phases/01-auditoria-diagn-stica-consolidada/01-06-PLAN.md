---
phase: 01-auditoria-diagn-stica-consolidada
plan: 06
type: execute
wave: 2
depends_on: ["01-01", "01-02", "01-03", "01-04", "01-05"]
files_modified:
  - .planning/phases/01-auditoria-diagn-stica-consolidada/REPORT-01.md
autonomous: false
requirements: [REPORT-01]

must_haves:
  truths:
    - "Existe um único documento REPORT-01.md que abre com sumário executivo (Críticos e Altos) e segue com o detalhe técnico completo por dimensão (D-07)"
    - "A régua de severidade D-02, D-03, D-04, D-05 foi aplicada de forma CONSISTENTE entre as 5 dimensões: o mesmo problema de fundo não recebe severidades diferentes conforme a dimensão que o encontrou"
    - "Os 6 planos da fase seguiram a estrutura de execução decidida: 5 plans paralelos por dimensão na wave 1 mais 1 plano de consolidação na wave 2, dependente de todos os 5 (D-06)"
    - "Achados duplicados entre dimensões foram fundidos num achado único que cita todas as facetas"
    - "Os 19 requisitos da fase aparecem na tabela de rastreabilidade com status explícito"
    - "Nenhuma correção de código foi implementada — o relatório é diagnóstico puro"
  artifacts:
    - path: ".planning/phases/01-auditoria-diagn-stica-consolidada/REPORT-01.md"
      provides: "Relatório único consolidado da revisão geral do Boris+"
      contains: "## Sumário executivo"
  key_links:
    - from: "REPORT-01.md"
      to: "FINDINGS-STORY.md / FINDINGS-UX.md / FINDINGS-CODE.md / FINDINGS-GATE.md / FINDINGS-ADMIN.md"
      via: "cada achado consolidado cita o ID de origem (F-STORY-NN etc.)"
      pattern: "F-(STORY|UX|CODE|GATE|ADMIN)-"
    - from: "REPORT-01.md"
      to: ".planning/REQUIREMENTS.md"
      via: "tabela de rastreabilidade com os 19 requisitos"
      pattern: "REPORT-01"
---

<objective>
Consolidar os achados brutos das 5 dimensões (STORY, UX, CODE, GATE, ADMIN) em um
**único documento** `REPORT-01.md`, aplicando a régua de severidade de forma consistente
entre dimensões, deduplicando achados sobrepostos, e abrindo com um sumário executivo
para decisão rápida.

Purpose: este é o entregável real da fase. Ele alimenta diretamente o roadmap de fases
de correção (fora deste milestone). Se a severidade for inconsistente entre dimensões, o
Alex prioriza errado.

Output: `.planning/phases/01-auditoria-diagn-stica-consolidada/REPORT-01.md`

**Esta fase é READ-ONLY sobre o produto.** Nenhum arquivo em `server/`, `web/` ou
`web-admin/` pode ser modificado. O relatório recomenda; não corrige (PROJECT.md Out of
Scope: "Corrigir achados nesta fase").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-auditoria-diagn-stica-consolidada/01-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<severity_rubric>
Régua fechada pelo usuário em 01-CONTEXT.md. Este plano é o **dono da consistência** da
régua — os 5 planos da wave 1 classificaram cada um por conta própria; aqui a
classificação é revista de ponta a ponta e, quando divergir, corrigida com nota.

- **Crítico (D-02)** — viola um dos 10 princípios obrigatórios do `CLAUDE.md` do repo
  OU o guardrail CVM (manchete do card só do motor determinístico).
- **Alto (D-03)** — já causou incidente real documentado OU bloqueia uma decisão de
  negócio pendente.
- **Médio (D-04)** — risco real, ainda não materializado em incidente.
- **Baixo (D-05)** — polimento/consistência sem risco de produto.
</severity_rubric>

<tasks>

<task type="auto">
  <name>Task 1: Consolidar, deduplicar e normalizar severidade entre as 5 dimensões</name>

  <files>.planning/phases/01-auditoria-diagn-stica-consolidada/REPORT-01.md</files>

  <read_first>
    - `.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-STORY.md`
    - `.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-UX.md`
    - `.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-CODE.md`
    - `.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-GATE.md`
    - `.planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-ADMIN.md`
    - `.planning/phases/01-auditoria-diagn-stica-consolidada/01-CONTEXT.md` — D-02..D-05
      (régua) e D-07 (formato: documento único, sumário executivo primeiro)
    - `CLAUDE.md` — os 10 princípios (é a referência que decide o que é Crítico)
    - Ler cada FINDINGS uma única vez, por inteiro. Não reler faixas já lidas.
  </read_first>

  <action>
**1a. Montar o inventário completo.** Extrair TODOS os achados dos 5 arquivos
(`grep -n '^### F-' .planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-*.md`
dá a lista e a contagem de partida). Nenhum achado da wave 1 pode desaparecer sem
justificativa registrada — omissão silenciosa é falha do plano.

**1b. Deduplicar.** Dois achados são o MESMO quando compartilham a causa raiz, ainda que
descritos por facetas diferentes. Os planos da wave 1 já sinalizam candidatos com
`**Possível duplicata:** ...` na linha de Impacto — mas procurar também os não
sinalizados, comparando arquivo:linha de evidência. Pares esperados (confirmar, não
assumir):
  - gate "Executar" mudo — faceta de acessibilidade (UX-03) × faceta de dívida
    técnica/blast radius (CODE-03)
  - dois nomes "Operador" sem link — faceta pedagógica (STORY-02) × faceta de código (CODE-01)
  - transparência de cota quando o orçamento da brapi degrada — faceta de UX (UX-01,
    princípios 3 e 4) × faceta de gating (GATE-02)
  - kill-switch — faceta de observabilidade (ADMIN-02) × faceta de dívida técnica (CODE)

Achado fundido recebe um ID novo `C-NN`, cita os IDs de origem (`origem: F-UX-03,
F-CODE-05`) e consolida as evidências das duas facetas numa lista só. Achado sem
duplicata também recebe ID `C-NN` e cita sua origem única.

**1c. Normalizar a severidade.** Percorrer o inventário inteiro e responder, para cada
achado, as duas perguntas da régua **na mesma ordem para todos**:
  1. Viola um dos 10 princípios do `CLAUDE.md` ou o guardrail CVM? → **Crítico**
  2. Se não: já causou incidente real documentado, ou bloqueia decisão de negócio
     pendente? → **Alto**. Se não: risco real ainda não materializado? → **Médio**.
     Senão → **Baixo**.

Quando a severidade final divergir da atribuída pela wave 1, **registrar a mudança
explicitamente** no achado, em uma linha `- **Reclassificado:** Médio → Alto — {motivo}`.
Reclassificação silenciosa é proibida: o Alex precisa ver o julgamento.

Verificação de consistência obrigatória: dois achados com a mesma natureza de causa (ex.:
dois controles diferentes que dependem só de `title` para explicar por que estão
desabilitados) precisam ter a MESMA severidade, salvo diferença de impacto declarada em
uma frase.

**1d. Escrever `.planning/phases/01-auditoria-diagn-stica-consolidada/REPORT-01.md`**
com esta estrutura (D-07: documento único, sumário executivo primeiro). Nesta task,
escrever da seção `## Metodologia` em diante — o `## Sumário executivo` é escrito na
task 2, quando a contagem final já estiver estável:

```
# REPORT-01 — Revisão geral do Boris+ (b3-agente)

**Data:** {data}
**Escopo:** 5 dimensões (storyline pedagógico, UX/UI, código, gating de monetização,
portal admin) — diagnóstico, sem nenhuma correção implementada.

## Sumário executivo            <- escrito na task 2

## Metodologia
- Régua de severidade (D-02..D-05, com o texto literal de cada nível)
- Método de verificação por dimensão e limitações (consolidado dos 5
  `## Método de verificação` da wave 1 — o que foi verificado ao vivo, o que foi por
  API real, o que foi só por código; o que NÃO pôde ser verificado)
- Deduplicação: quantos achados brutos entraram, quantos foram fundidos, quantos saíram

## Achados por dimensão
### 1. Storyline pedagógico (STORY-01..04)
### 2. UX/UI (UX-01..04)
### 3. Código / dívida técnica (CODE-01..04)
### 4. Gating de monetização (GATE-01..03)
### 5. Portal de administração/observabilidade (ADMIN-01..03)

## Verificado e conforme        <- o que foi auditado e está OK, por dimensão

## Rastreabilidade de requisitos <- escrita na task 2
```

Dentro de cada subseção de dimensão, os achados vêm **ordenados por severidade
decrescente** (Crítico → Alto → Médio → Baixo), no formato:

```
#### C-01 — {título de uma linha}   [Crítico]
- **Dimensão:** UX  |  **Requisito:** UX-01  |  **Origem:** F-UX-03, F-CODE-05
- **Regra aplicada:** D-02 — {qual princípio do CLAUDE.md é violado, nomeado}
- **Evidência:** {arquivo:linha} — {o que está lá}; {tela/fluxo} — {o que apareceu}
- **Verificação:** {ao vivo | API real | código/docs}
- **Impacto:** {consequência concreta}
- **Recomendação:** {ação — uma frase se Baixo/Médio, um parágrafo se Alto/Crítico}
```

As tabelas grandes produzidas na wave 1 (mapa de recomputação de `appMode`, matriz de
estados, cobertura dos 13 conceitos, cobertura dos 5 fluxos financeiros, aba × permissão,
replay do kill-switch, cota física × cap comercial, features candidatas) entram no
relatório dentro da dimensão correspondente — são a evidência que sustenta os achados e
o insumo mais útil pro roadmap de correção. Não resumir a ponto de perder arquivo:linha.
  </action>

  <verify>
    <automated>R=.planning/phases/01-auditoria-diagn-stica-consolidada/REPORT-01.md; test -f "$R" && grep -q '^## Metodologia' "$R" && grep -q '^## Achados por dimensão' "$R" && for d in "1. Storyline" "2. UX" "3. Código" "4. Gating" "5. Portal"; do grep -q "$d" "$R" || exit 1; done && test "$(grep -cE '^#### C-[0-9]+' "$R")" -ge 1 && grep -q 'Origem:' "$R"</automated>
  </verify>

  <acceptance_criteria>
    - `REPORT-01.md` existe e é o ÚNICO documento do relatório (não há segundo arquivo
      de detalhe técnico — D-07)
    - Todo achado da wave 1 aparece no relatório com ID `C-NN`, ou está listado na
      seção de metodologia como fundido (com o ID de destino) — nenhum some sem registro
    - Cada `C-NN` cita `**Origem:**` com os IDs `F-{DIM}-NN` de onde veio
    - Cada `C-NN` cita `**Regra aplicada:** D-0X` e, quando Crítico, nomeia qual dos 10
      princípios do CLAUDE.md é violado
    - Achados reclassificados em relação à wave 1 trazem a linha `**Reclassificado:**`
      com motivo
    - Dentro de cada dimensão, os achados estão ordenados por severidade decrescente
    - As tabelas de evidência da wave 1 foram preservadas no relatório com arquivo:linha
    - `git status --porcelain server web web-admin` vazio
  </acceptance_criteria>

  <done>Todos os achados das 5 dimensões estão consolidados sob IDs C-NN, deduplicados, com severidade normalizada e reclassificações registradas.</done>
</task>

<task type="auto">
  <name>Task 2: Sumário executivo, rastreabilidade dos 19 requisitos e verificação de consistência final</name>

  <files>.planning/phases/01-auditoria-diagn-stica-consolidada/REPORT-01.md</files>

  <read_first>
    - `.planning/phases/01-auditoria-diagn-stica-consolidada/REPORT-01.md` (o que a
      task 1 escreveu)
    - `.planning/REQUIREMENTS.md` — a lista dos 19 requisitos e a tabela de Traceability
    - `.planning/ROADMAP.md` — os 6 Success Criteria da Phase 1 (o relatório precisa
      satisfazer todos)
  </read_first>

  <action>
**2a. Escrever o `## Sumário executivo`** no topo do documento (logo após o cabeçalho,
antes de `## Metodologia`) — é a seção que o Alex lê primeiro para decidir. Conteúdo,
nesta ordem:
  1. Uma tabela de contagem: `| Severidade | Qtd | Dimensões afetadas |` com 4 linhas
     (Crítico, Alto, Médio, Baixo) — números derivados da contagem real dos `C-NN`,
     não estimados.
  2. **Críticos** — um bullet por achado Crítico, no formato
     `**C-NN** — {título}. {Uma frase de impacto}. → {recomendação em uma frase}`
  3. **Altos** — mesmo formato.
  4. `### Leitura de conjunto` — 3 a 6 bullets de padrão transversal: o que se repete
     entre dimensões (ex.: "o mesmo padrão de estado recalculado aparece como bug de
     UX, de código e de narrativa"), qual dimensão concentra o risco, e o que o
     relatório NÃO conseguiu verificar (limitações herdadas da wave 1).
  5. `### Sugestão de sequenciamento` — agrupar os achados Críticos e Altos em 3 a 5
     blocos que fariam sentido virar fases de correção, **por dependência técnica**
     (o que precisa vir antes do quê), sem estimativa de tempo e sem decidir prioridade
     de negócio — a priorização é do Alex.

Médios e Baixos NÃO entram no sumário executivo (ficam no detalhe por dimensão) —
o sumário é para decisão rápida.

**2b. Escrever `## Rastreabilidade de requisitos`** ao final: tabela
`| Requisito | Dimensão | Achados (C-NN) | Status |` com **19 linhas**, uma por
requisito: STORY-01, STORY-02, STORY-03, STORY-04, UX-01, UX-02, UX-03, UX-04, CODE-01,
CODE-02, CODE-03, CODE-04, GATE-01, GATE-02, GATE-03, ADMIN-01, ADMIN-02, ADMIN-03,
REPORT-01. Status ∈ {`com achados`, `conforme`, `parcial — {limitação}`}. Nenhuma linha
pode ficar sem status. A linha REPORT-01 se refere ao próprio documento.

**2c. Passe final de consistência.** Verificar e corrigir, registrando o que mudou:
  - Nenhum achado propõe correção implementada nem número comercial (Out of Scope)
  - Nenhum achado sugere mudar o bundle id `com.alexandrecamerini.bolsia` (invariante
    do repo — se algum achado de branding esbarrar nisso, reescrever a recomendação)
  - Nenhuma recomendação viola um guardrail do `CLAUDE.md` (manchete só do motor
    determinístico; guardiões de teste não se apagam; histórico não se reescreve;
    paridade obrigatória entre os pares de arquivo)
  - Nenhum segredo (valor de `BRAPI_TOKEN`, chave de LLM, token de sessão, e-mail real
    de usuário) foi copiado para dentro do relatório
  - Toda severidade citada no sumário executivo bate com a do detalhe da dimensão
  - Os 6 Success Criteria da Phase 1 no `ROADMAP.md` estão todos atendidos por alguma
    seção do relatório — se algum não estiver, é lacuna e precisa ser preenchida ou
    declarada como limitação na Metodologia
  </action>

  <verify>
    <automated>R=.planning/phases/01-auditoria-diagn-stica-consolidada/REPORT-01.md; grep -q '^## Sumário executivo' "$R" && grep -q '^## Rastreabilidade de requisitos' "$R" && for r in STORY-01 STORY-02 STORY-03 STORY-04 UX-01 UX-02 UX-03 UX-04 CODE-01 CODE-02 CODE-03 CODE-04 GATE-01 GATE-02 GATE-03 ADMIN-01 ADMIN-02 ADMIN-03 REPORT-01; do grep -q "$r" "$R" || exit 1; done && ! grep -qE "sk-[a-zA-Z0-9]{16}|AIza[0-9A-Za-z_-]{10}" "$R" && test "$(ls .planning/phases/01-auditoria-diagn-stica-consolidada/REPORT-*.md | wc -l)" -eq 1</automated>
  </verify>

  <acceptance_criteria>
    - `## Sumário executivo` está no topo, antes de `## Metodologia`, e contém: tabela
      de contagem por severidade, bullets de todos os Críticos, bullets de todos os
      Altos, `### Leitura de conjunto` (3-6 bullets) e `### Sugestão de sequenciamento`
      (3-5 blocos por dependência técnica)
    - Nenhum Médio ou Baixo aparece no sumário executivo
    - `## Rastreabilidade de requisitos` tem exatamente 19 linhas, uma por requisito,
      todas com status preenchido
    - Os 6 Success Criteria da Phase 1 do `ROADMAP.md` estão atendidos, ou a lacuna está
      declarada explicitamente na Metodologia
    - Nenhuma recomendação viola invariante do repo (bundle id, guardrail CVM, guardiões
      de teste, histórico, paridade de pares de arquivo)
    - Existe exatamente UM arquivo `REPORT-*.md` no diretório da fase (D-07)
    - `git status --porcelain server web web-admin` vazio
  </acceptance_criteria>

  <done>O relatório abre com um sumário executivo acionável, rastreia os 19 requisitos e passou pelo controle de consistência.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Validação do REPORT-01 pelo Alex</name>

  <files>.planning/phases/01-auditoria-diagn-stica-consolidada/REPORT-01.md</files>

  <read_first>
    - `.planning/phases/01-auditoria-diagn-stica-consolidada/REPORT-01.md`
  </read_first>

  <action>
Apresentar o `REPORT-01.md` ao Alex e pausar para validação humana do julgamento de
severidade. Não seguir sozinho: a régua D-02..D-05 é objetiva, mas "já causou incidente
real documentado" (D-03) depende da memória do dono do produto, que o executor não tem.

Ao retomar, aplicar cada discordância apontada por ele diretamente no `REPORT-01.md`:
adicionar ao achado a linha `- **Reclassificado:** {antes} → {depois} — {motivo dado
pelo Alex}` e recalcular a tabela de contagem do sumário executivo e as listas de
Críticos/Altos. Achado que sobe para Crítico ou Alto passa a aparecer no sumário
executivo; achado que cai para Médio/Baixo sai de lá.

Se o Alex pedir correção de código durante a conversa, NÃO implementar: registrar o
pedido como item nomeado em `### Sugestão de sequenciamento` e lembrar que a fase é
diagnóstica por decisão dele mesmo (PROJECT.md Out of Scope: "Corrigir achados nesta
fase").
  </action>

  <what-built>
Relatório único `.planning/phases/01-auditoria-diagn-stica-consolidada/REPORT-01.md`,
consolidando os achados das 5 dimensões (storyline, UX, código, gating, admin) com
severidade normalizada pela régua D-02..D-05, deduplicação entre dimensões, sumário
executivo no topo e rastreabilidade dos 19 requisitos. Nenhuma correção de código foi
implementada.

Os 5 arquivos de achados brutos que o alimentaram continuam no mesmo diretório
(`FINDINGS-STORY.md`, `FINDINGS-UX.md`, `FINDINGS-CODE.md`, `FINDINGS-GATE.md`,
`FINDINGS-ADMIN.md`) — servem de anexo/auditoria do julgamento.
  </what-built>

  <how-to-verify>
1. Abrir `.planning/phases/01-auditoria-diagn-stica-consolidada/REPORT-01.md` e ler
   somente o `## Sumário executivo`.
2. Conferir a lista de **Críticos**: cada um deveria nomear qual dos 10 princípios do
   `CLAUDE.md` foi violado. Algum está lá que não deveria ser Crítico? Falta algum que
   você sabe que é?
3. Conferir a lista de **Altos**: cada um deveria apontar um incidente real já ocorrido
   ou uma decisão de negócio bloqueada. Bate com a sua memória do que aconteceu?
4. Ler `### Sugestão de sequenciamento` e dizer se os blocos fazem sentido como
   candidatos a fases de correção.
5. Escolher 2 achados quaisquer e conferir a evidência (`arquivo:linha`) no código — o
   apontado é o que está lá?
6. Conferir em `## Metodologia` o que NÃO pôde ser verificado ao vivo. Se alguma
   limitação te incomodar, diga qual: ela vira item de reverificação.
  </how-to-verify>

  <verify>
    <human-check>O Alex confirma que a classificação de severidade bate com a realidade do produto e que o relatório está pronto para virar insumo do roadmap de correção.</human-check>
    <automated>R=.planning/phases/01-auditoria-diagn-stica-consolidada/REPORT-01.md; test -f "$R" && grep -q '^## Sumário executivo' "$R" && ls .planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-STORY.md .planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-UX.md .planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-CODE.md .planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-GATE.md .planning/phases/01-auditoria-diagn-stica-consolidada/FINDINGS-ADMIN.md >/dev/null</automated>
  </verify>

  <acceptance_criteria>
    - O Alex leu o sumário executivo e se manifestou sobre a lista de Críticos e Altos
    - Toda discordância de severidade apontada por ele foi aplicada no documento, com a
      linha `**Reclassificado:**` registrando a mudança e o motivo
    - A tabela de contagem e as listas de Críticos/Altos foram recalculadas após as
      reclassificações
    - Nenhuma correção de código foi feita nesta fase, mesmo que solicitada na conversa
      — pedido de correção vira item para o roadmap de v2, não commit nesta fase
  </acceptance_criteria>

  <resume-signal>Digite "aprovado" ou aponte os achados cuja severidade deve mudar (e por quê)</resume-signal>

  <done>O relatório está validado pelo dono do produto e pronto para alimentar o roadmap de correção.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhuma nova | Fase read-only: consolida markdown. Não altera código de produção, não adiciona rota, não é deployada. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15 | Information Disclosure | REPORT-01.md | mitigate | Passe explícito na task 2 verifica que nenhum segredo (`BRAPI_TOKEN`, chave de LLM, token de sessão, e-mail real) foi copiado dos FINDINGS para o relatório; o `<automated>` da task 2 grepa padrões de chave |
| T-01-16 | Tampering | Código de produção | mitigate | Read-only: acceptance criteria de todas as tasks exige `git status --porcelain server web web-admin` vazio; o checkpoint reforça que pedido de correção vira item de roadmap, não commit |
| T-01-17 | Repudiation | Julgamento de severidade | mitigate | Toda reclassificação em relação à wave 1 é registrada na linha `**Reclassificado:**` com motivo — o julgamento fica auditável contra os FINDINGS originais, que permanecem no diretório |
| T-01-SC | Tampering | npm/pip installs | accept | Nenhum pacote instalado por este plano |
</threat_model>

<verification>
- Existe exatamente um `REPORT-*.md` no diretório da fase (D-07: documento único)
- `## Sumário executivo` vem antes de `## Metodologia` e de `## Achados por dimensão`
- Os 19 requisitos aparecem na tabela de rastreabilidade com status
- Os 5 arquivos `FINDINGS-*.md` continuam no diretório (anexo do julgamento)
- `git status --porcelain server web web-admin` vazio
</verification>

<success_criteria>
- Um único documento consolida as 5 dimensões, com severidade consistente entre elas
- Achados sobrepostos foram fundidos, com os IDs de origem preservados
- Os 6 Success Criteria da Phase 1 do ROADMAP.md estão atendidos ou a lacuna declarada
- Nenhuma correção de código implementada
</success_criteria>

<output>
Create `.planning/phases/01-auditoria-diagn-stica-consolidada/01-06-SUMMARY.md` when done
</output>
