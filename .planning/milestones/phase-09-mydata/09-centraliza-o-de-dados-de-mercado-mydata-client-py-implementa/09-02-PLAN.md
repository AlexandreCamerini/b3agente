---
phase: 09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - server/app/candle_provider.py
  - server/tests/test_candle_provider.py
  - server/tests/test_mydata_provider.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Com B3_CANDLE_PROVIDER=mydata, um pedido de candle diário é servido pelo mydata e o payload diz source=mydata"
    - "D-03: Se o mydata falhar, o mesmo pedido é servido pela brapi; se a brapi também falhar, pelo Yahoo — na MESMA requisição, sem o usuário ver erro"
    - "Um pedido intraday com mydata como primário NUNCA toca o mydata: vai direto ao Yahoo (ADR-001 intocada)"
    - "Uma chamada ao mydata só sai se houver vaga nas duas janelas de cota (60/min e 2.000/dia); sem vaga, cai para a fonte seguinte"
    - "O consumo do mydata é observável em /api/obs/usage ao lado do da brapi"
    - "D-01: Trocar de fonte continua sendo configuração (env), não refatoração — nenhum call site de candle muda"
  artifacts:
    - path: "server/app/candle_provider.py"
      provides: "MydataProvider registrado + cadeia de fallback de N saltos + gate de cota por provedor"
      contains: "class MydataProvider"
    - path: "server/tests/test_mydata_provider.py"
      provides: "Guardiões da cadeia mydata→brapi→yahoo e do gate de fatia/cota"
  key_links:
    - from: "server/app/candle_provider.py"
      to: "server/app/mydata_client.py"
      via: "MydataProvider.history delega a mydata_client.get_history"
      pattern: "mydata_client\\.get_history"
    - from: "server/app/candle_provider.py"
      to: "server/app/mydata_budget.py"
      via: "gate pode_gastar/debita antes da chamada"
      pattern: "mydata_budget\\.(pode_gastar|debita)"
    - from: "server/app/candle_provider.py::snapshot"
      to: "/api/obs/usage"
      via: "main.py:561 já chama candle_provider.snapshot() — orcamentoMydata sai de graça"
      pattern: "orcamentoMydata"
---

<objective>
Ligar o mydata na fatia DIÁRIA de candles: registrar `MydataProvider` no
registro de provedores e transformar o fallback de um salto (brapi→Yahoo) numa
CADEIA (mydata→brapi→Yahoo), conforme D-01 e D-03 do 09-CONTEXT.md.

Purpose: D-01 diz que o ganho todo está em não mexer em nenhum call site — o
`get_history()` de `candle_provider.py` já é o ponto único e o `candle_cache`
L2 já faz carga cheia + delta. Este plano só acrescenta um provedor ao registro
e corrige a limitação real que o 09-PATTERNS.md sinalizou: hoje só existe UM
fallback, e a degradação que D-03 pede tem dois saltos.

Output: `MydataProvider`, `fallback_names()`/`get_fallbacks()`, gate de fatia e
cota por provedor no roteador, e `orcamentoMydata` no snapshot. **A configuração
de produção NÃO muda neste plano** — o default de `B3_CANDLE_PROVIDER` continua
sendo o de hoje. A virada é o checkpoint do Plano 09-06, gated pela medição de
rate-limit do Plano 09-04 (Folded Todo do 09-CONTEXT.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-CONTEXT.md
@.planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-PATTERNS.md
@.planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-01-SUMMARY.md

<assumptions>
**A sugestão do 09-PATTERNS.md sobre fallback foi verificada e é FALSA.** O
PATTERNS sugeriu que bastaria pôr o mydata na frente porque "o BrapiProvider já
cai no Yahoo". Não cai: `BrapiProvider.history()` (candle_provider.py:185-186) é
uma delegação de duas linhas a `brapi.get_history`, sem qualquer fallback. Todo
o fallback brapi→Yahoo mora no roteador `get_history()` (linhas 271-322), na
forma de UMA variável `fb = get_fallback()`. Para a degradação de D-03
(mydata→brapi→Yahoo) existir, a cadeia precisa ser estendida de verdade. É o
que este plano faz.

**Compatibilidade obrigatória com o que já existe:**
- `snapshot()` (linha 129) chama `fallback_name()` e espera uma string única.
- `set_fallback(p)` é usado por testes existentes com UM provedor injetado
  (`server/tests/test_candle_provider.py`).
- O ramo `if prim.nome == "brapi"` (linhas 288-302) faz DUAS coisas hoje:
  valida o plano gratuito antes da rede (`brapi.valida_plano`, porque recusa
  por plano debita cota) e consulta/debita o orçamento (`brapi_budget`).
Nenhuma das três pode quebrar.

**Escopo negativo explícito (não fazer neste plano):**
- Não criar job de refresh em lote — a decisão da fase é pass-through pelo
  `candle_cache` existente (ver 09-01-PLAN.md `<assumptions>`).
- Não mexer em `candle_cache.py`: o merge entre fontes no diário já é permitido
  e já grava `src` por registro (ADR-008 Fase 4, errata da decisão 3) — é
  exatamente a proveniência que D-03 pede.
- Não mexer em `get_quote`/`get_quotes`/`get_quotes_exclusive`: spot ao vivo
  fica com a brapi (redefinição de escopo do ADR-008 declarada no goal da fase),
  e o COTAHIST não tem spot.
- Não mudar defaults de env em produção.
</assumptions>

<interfaces>
<!-- Estado ATUAL do arquivo a modificar. Extraído do código. -->

`server/app/candle_provider.py` hoje:

    class CandleProvider:            # linha 148 — contrato: history(ticker, rng, interval) -> dict
    class YahooProvider(...):        # linha 163, nome = "yahoo"
    class BrapiProvider(...):        # linha 174, nome = "brapi"
    _PROVEDORES = {"yahoo": YahooProvider, "brapi": BrapiProvider}     # linha 189
    def provider_name() -> str       # linha 193 — env B3_CANDLE_PROVIDER, default "yahoo"
    def get_provider() -> CandleProvider                               # linha 197, memoizado por nome
    def set_provider(p)                                                # linha 210, injeção de teste
    _fallback / _fb_injetado                                           # linhas 217-218
    def fallback_name() -> str       # linha 221 — env B3_CANDLE_FALLBACK; None → "yahoo" se primário != yahoo
    def get_fallback() -> Optional[CandleProvider]                     # linha 231
    def set_fallback(p)                                                # linha 246
    async def _chama(p, ticker, rng, interval) -> dict                 # linha 253 — instrumentação na fronteira
    async def get_history(ticker, rng="1mo", interval="1d") -> dict    # linha 271 — roteador
    def snapshot() -> dict           # linha 98 — usa provider_name() e fallback_name()

Roteamento atual de `get_history` (linhas 285-322), em prosa:
    prim = get_provider(); fb = get_fallback()
    se prim é brapi: valida_plano (ForaDoPlano → fb direto, sem rede/cota);
                     senão pode_gastar("delta") ? debita("delta") : fb direto
    tenta prim; exceção → fb
    série vazia e fb existe → tenta fb; se fb trouxe candles, devolve fb
    marca out["source"]

Da API do Plano 09-01 (ver 09-01-SUMMARY.md):
    mydata_client.get_history(ticker, rng, interval, *, fetch_json=None) -> dict
    mydata_client.valida_fatia(rng, interval) -> None   # levanta MydataForaDaFatia, sem rede
    mydata_client.MydataForaDaFatia(ValueError)
    mydata_client.MydataIndisponivel(RuntimeError)
    mydata_budget.pode_gastar(n=1, now=None) -> bool
    mydata_budget.debita(n=1, now=None) -> None
    mydata_budget.snapshot(now=None) -> dict
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MydataProvider no registro + cadeia de fallback de N saltos</name>

  <files>server/app/candle_provider.py, server/tests/test_mydata_provider.py</files>

  <read_first>
    - server/app/candle_provider.py (arquivo inteiro — é o alvo da modificação e o contrato)
    - server/app/mydata_client.py (após o Plano 09-01) — assinatura de `get_history` e `valida_fatia`
    - server/app/mydata_budget.py (após o Plano 09-01) — `pode_gastar`/`debita`/`snapshot`
    - server/tests/test_candle_provider.py — a classe `_Fake(cp.CandleProvider)` e o helper `_limpa()` que TODOS os testes novos devem reusar
  </read_first>

  <behavior>
    - `_PROVEDORES` tem exatamente três chaves: `yahoo`, `brapi`, `mydata`.
    - Com `B3_CANDLE_PROVIDER=mydata`, `get_provider().nome == "mydata"`.
    - `fallback_names()` com `B3_CANDLE_FALLBACK` ausente e primário `mydata` devolve `["brapi", "yahoo"]`.
    - `fallback_names()` com `B3_CANDLE_FALLBACK` ausente e primário `brapi` devolve `["yahoo"]` (comportamento de hoje preservado).
    - `fallback_names()` com `B3_CANDLE_FALLBACK` ausente e primário `yahoo` devolve `[]` (comportamento pré-ADR-008 preservado).
    - `fallback_names()` com `B3_CANDLE_FALLBACK="brapi,yahoo"` devolve `["brapi", "yahoo"]`; com `B3_CANDLE_FALLBACK=""` devolve `[]` (vazio continua desligando).
    - `fallback_names()` remove o próprio primário da cadeia e remove nome desconhecido, sem levantar.
    - `fallback_name()` continua devolvendo uma string: o primeiro nome da cadeia, ou `""`.
    - `set_fallback(p)` com um provedor único continua funcionando: `get_fallbacks()` devolve `[p]` e `get_fallback()` devolve `p`.
    - Todos os testes já existentes em `server/tests/test_candle_provider.py` continuam passando sem edição.
  </behavior>

  <action>
    Editar `server/app/candle_provider.py`.

    **1. Classe do provedor.** Logo após `BrapiProvider` (linha 186), criar
    `class MydataProvider(CandleProvider)` com `nome = "mydata"` e
    `async def history(self, ticker, rng, interval="1d")` delegando a
    `mydata_client.get_history(ticker, rng=rng, interval=interval)` — mesma
    forma de duas linhas de `BrapiProvider`. Docstring curta dizendo: hub
    `mydata.acamerini.app` (`~/dev/cvm-financas`), fonte do COTAHIST oficial;
    cobre SÓ a fatia diária, porque o COTAHIST só publica após o fechamento do
    pregão — o intraday continua do Yahoo por ADR-001, que esta fase não
    reabre. Acrescentar `mydata_client` (e `mydata_budget`) ao import de topo
    do módulo, na mesma linha de estilo do `from . import brapi, brapi_budget, yahoo`.

    **2. Registro.** `_PROVEDORES = {"yahoo": YahooProvider, "brapi": BrapiProvider, "mydata": MydataProvider}`.

    **3. Cadeia de fallback.** Trocar a variável única `_fallback` por uma
    lista `_fallbacks: list[CandleProvider] = []`, mantendo `_fb_injetado`.
      - `def fallback_names() -> list[str]`: lê `B3_CANDLE_FALLBACK`. Se a env
        NÃO está definida (`is None`), o default por primário é:
        `mydata` → `["brapi", "yahoo"]`; `brapi` → `["yahoo"]`; `yahoo` → `[]`.
        Se está definida, faz split por vírgula, `strip().lower()` em cada item,
        descarta vazios. Em ambos os casos, filtra fora o `provider_name()` e os
        nomes que não estão em `_PROVEDORES`, e deduplica preservando a ordem.
        Um `B3_CANDLE_FALLBACK=""` continua devolvendo `[]` — vazio desliga, que
        é o contrato de hoje documentado na docstring da linha 222.
      - `def fallback_name() -> str`: passa a ser um wrapper — primeiro nome de
        `fallback_names()` ou `""`. NÃO remover: `snapshot()` (linha 129) e os
        testes existentes dependem dela.
      - `def get_fallbacks() -> list[CandleProvider]`: se `_fb_injetado`, devolve
        `_fallbacks`; senão instancia cada nome de `fallback_names()`, ignorando
        silenciosamente classe ausente (backup mal configurado não derruba o
        app — regra já escrita na linha 240). Memoiza comparando a lista de
        nomes instanciados com `fallback_names()`.
      - `def get_fallback() -> Optional[CandleProvider]`: primeiro de
        `get_fallbacks()` ou `None`. Preserva o contrato atual.
      - `def set_fallback(p)`: `_fallbacks = [p] if p is not None else []`;
        `_fb_injetado = p is not None`. Acrescentar
        `def set_fallbacks(ps: list)` para os testes novos injetarem a cadeia
        inteira (`_fb_injetado = bool(ps)`).

    **4. Snapshot.** Em `snapshot()` (linha 127-142), trocar
    `"fallback": fallback_name() or None` por
    `"fallback": fallback_name() or None` MAIS uma chave nova
    `"fallbacks": fallback_names()` (aditivo — nenhum consumidor perde campo), e
    acrescentar `"orcamentoMydata": mydata_budget.snapshot() if "mydata" in (provider_name(), *fallback_names()) else None`,
    espelhando exatamente a linha 133 (`"orcamentoBrapi"`). Como `main.py:561`
    já expõe `candle_provider.snapshot()` em `/api/obs/usage`, isso torna o
    consumo do mydata observável sem tocar em `main.py`.

    Criar `server/tests/test_mydata_provider.py` cobrindo `<behavior>`, reusando
    a classe `_Fake` e o helper `_limpa()` de `server/tests/test_candle_provider.py`
    (importar de lá ou replicar — não inventar um terceiro idioma). Todo teste
    que mexe em env usa `monkeypatch`. `_limpa()` precisa passar a chamar
    `cp.set_fallbacks([])` também.
  </action>

  <verify>
    <automated>cd server &amp;&amp; python -m pytest tests/test_mydata_provider.py tests/test_candle_provider.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - `cd server && python -m pytest tests/test_mydata_provider.py tests/test_candle_provider.py -x -q` sai com código 0.
    - `cd server && python -c "from app import candle_provider as c; print(sorted(c._PROVEDORES))"` imprime `['brapi', 'mydata', 'yahoo']`.
    - `cd server && B3_CANDLE_PROVIDER=mydata python -c "from app import candle_provider as c; print(c.fallback_names())"` imprime `['brapi', 'yahoo']`.
    - `cd server && B3_CANDLE_PROVIDER=brapi python -c "from app import candle_provider as c; print(c.fallback_names(), repr(c.fallback_name()))"` imprime `['yahoo'] 'yahoo'`.
    - `cd server && B3_CANDLE_PROVIDER=yahoo python -c "from app import candle_provider as c; print(c.fallback_names())"` imprime `[]`.
    - `cd server && B3_CANDLE_PROVIDER=mydata B3_CANDLE_FALLBACK= python -c "from app import candle_provider as c; print(c.fallback_names())"` imprime `[]`.
    - `grep -c 'class MydataProvider' server/app/candle_provider.py` retorna 1.
    - `grep -v '^#' server/app/candle_provider.py | grep -c 'orcamentoMydata'` retorna >= 1.
    - Nenhum teste existente foi editado: `git diff --stat server/tests/test_candle_provider.py` mostra 0 arquivos alterados OU apenas a linha de `_limpa()`.
  </acceptance_criteria>

  <done>
    `mydata` é um provedor selecionável por env e o fallback deixou de ser um
    salto só, sem quebrar `fallback_name()`, `set_fallback()` nem os guardiões
    existentes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Roteador com gate por provedor e degradação mydata→brapi→Yahoo</name>

  <files>server/app/candle_provider.py, server/tests/test_mydata_provider.py</files>

  <read_first>
    - server/app/candle_provider.py (estado após a Task 1) — o roteador `get_history` nas linhas 271-322 é o alvo exato
    - server/app/brapi.py (linhas 66-80) — `valida_plano` e por que ela roda antes da rede (recusa por plano DEBITA cota)
    - server/app/brapi_budget.py (linhas 139-160) — `pode_gastar`/`debita` e o gate `em_pregao`
    - server/app/mydata_client.py + server/app/mydata_budget.py (após o Plano 09-01)
  </read_first>

  <behavior>
    - Primário mydata sadio: `get_history("PETR4", rng="1mo")` devolve os candles do mydata e `out["source"] == "mydata"`.
    - Primário mydata que levanta: cai na brapi; `out["source"] == "brapi"`; o Yahoo NÃO é chamado.
    - Primário mydata e brapi ambos levantando: cai no Yahoo; `out["source"] == "yahoo"`.
    - Os três levantando: a exceção do ÚLTIMO da cadeia sobe (nada é inventado — princípio 4 do CLAUDE.md).
    - Primário mydata devolvendo série VAZIA: tenta o próximo da cadeia; se ele trouxer candles, devolve os dele com o `source` dele (é o modo de falha de 31/07/2026, já tratado hoje para um salto).
    - Primário mydata com `interval="15m"`: o `MydataProvider` NÃO é chamado (fake registra zero chamadas) e o pedido vai para a cadeia; o Yahoo serve.
    - Primário mydata sem vaga de cota (`mydata_budget` monkeypatched para `pode_gastar → False`): o `MydataProvider` NÃO é chamado e a brapi serve.
    - Primário mydata com vaga: `mydata_budget.debita` é chamado exatamente uma vez por chamada de rede ao mydata.
    - Na cadeia, quando a brapi é FALLBACK e o pedido está fora do plano gratuito dela (ex.: `rng="2y"`), a brapi é PULADA sem tocar a rede e o Yahoo serve.
    - Na cadeia, quando a brapi é FALLBACK e o orçamento dela está esgotado, a brapi é PULADA e o Yahoo serve.
    - Comportamento pré-existente intacto: primário brapi com `ForaDoPlano` continua indo direto ao Yahoo, e continua debitando a fatia `delta` quando serve.
  </behavior>

  <action>
    Editar o roteador `get_history()` em `server/app/candle_provider.py`
    (linhas 271-322), generalizando o gate que hoje está grudado em
    `if prim.nome == "brapi"`.

    Extrair uma função privada
    `def _gate(p: CandleProvider, rng: str, interval: str) -> Optional[str]`
    que devolve `None` quando o provedor pode ser chamado, ou uma string curta
    com o MOTIVO da recusa quando não pode. Ela NÃO faz rede e NÃO debita:
      - `p.nome == "brapi"`: tenta `brapi.valida_plano(rng, interval)`; se
        levantar `brapi.ForaDoPlano`, devolve `"fora do plano"`. Depois,
        `brapi_budget.pode_gastar("delta")` falso devolve `"sem orçamento"`.
      - `p.nome == "mydata"`: tenta `mydata_client.valida_fatia(rng, interval)`;
        `MydataForaDaFatia` devolve `"fora da fatia"`. Depois,
        `mydata_budget.pode_gastar()` falso devolve `"sem cota"`.
      - qualquer outro nome (yahoo): devolve `None`.

    E uma função privada `def _debita(p: CandleProvider) -> None` que debita o
    orçamento do provedor imediatamente antes da chamada de rede:
    `brapi` → `brapi_budget.debita("delta")`; `mydata` → `mydata_budget.debita()`;
    outros → nada.

    Reescrever o corpo de `get_history` como uma passada única sobre a lista
    `[get_provider(), *get_fallbacks()]`:
      1. Para cada provedor da lista, na ordem:
         a. `motivo = _gate(p, rng, interval)`; se houver motivo, PULA para o
            próximo (sem rede, sem cota) e guarda o motivo para diagnóstico.
         b. `_debita(p)`; `out = await _chama(p, ticker, rng, interval)`.
         c. Exceção → guarda como `ultimo_erro` e PULA para o próximo.
         d. Série vazia → guarda `out` como `melhor_vazio` (primeiro que veio) e
            PULA para o próximo. Série vazia continua contando como FALHA na
            instrumentação, o que `_chama` já faz.
         e. Série não-vazia → `out["source"] = p.nome; return out`.
      2. Esgotada a lista: se houver `melhor_vazio`, devolve ele com
         `source` do provedor que o produziu (preserva o comportamento atual, em
         que uma série vazia do primário sem fallback útil é devolvida, não vira
         erro). Se não houver nada e houver `ultimo_erro`, RELANÇA `ultimo_erro`.
         Se a lista inteira foi PULADA pelo gate e não há erro nem vazio,
         levanta `RuntimeError` citando os motivos acumulados (é configuração
         errada, e falhar alto explicando o que falta é a postura declarada na
         docstring de `BrapiProvider`).

    Atualizar a docstring de `get_history` para descrever a cadeia (hoje ela
    descreve "backup" no singular) mantendo os três critérios de roteamento
    já nomeados — PLANO/FATIA, ORÇAMENTO, FALHA — agora aplicados a cada elo.

    **Preservar exatamente:** a instrumentação continua em `_chama` (a razão está
    escrita na docstring da linha 253-257: se ela migrasse para dentro de um
    provedor, trocar de fonte apagaria o contador e o gatilho do plano B sumiria
    em silêncio). `_registra` continua recebendo `provedor=p.nome`.

    Estender `server/tests/test_mydata_provider.py` com todos os casos de
    `<behavior>`, usando `set_provider`/`set_fallbacks` com `_Fake` de nomes
    `"mydata"`, `"brapi"` e `"yahoo"`, e `monkeypatch.setattr` em
    `cp.mydata_budget.pode_gastar` / `cp.brapi_budget.pode_gastar` para os casos
    de cota. Contar as chamadas de cada fake é o que prova que o provedor pulado
    não tocou a rede.
  </action>

  <verify>
    <automated>cd server &amp;&amp; python -m pytest tests/test_mydata_provider.py tests/test_candle_provider.py tests/test_candle_cache.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - `cd server && python -m pytest tests/test_mydata_provider.py tests/test_candle_provider.py tests/test_candle_cache.py -x -q` sai com código 0.
    - `bash scripts/executar.sh --testes` sai com código 0 (suíte canônica COMPLETA — pytest + `web/tests/*.mjs`).
    - `grep -c 'def _gate' server/app/candle_provider.py` retorna 1 e `grep -c 'def _debita' server/app/candle_provider.py` retorna 1.
    - `grep -v '^#' server/app/candle_provider.py | grep -c 'get_fallbacks()'` retorna >= 1 (o roteador consome a cadeia, não `get_fallback()`).
    - Existe teste que prova os dois saltos: `grep -cE 'def test_.*(cadeia|dois_saltos|mydata_brapi_yahoo)' server/tests/test_mydata_provider.py` retorna >= 1.
    - Existe teste que prova que intraday não toca o mydata: `grep -cE 'def test_.*intraday' server/tests/test_mydata_provider.py` retorna >= 1.
    - O contador de instrumentação continua na fronteira: `grep -c '_registra(' server/app/candle_provider.py` retorna >= 2 e nenhuma chamada a `_registra` aparece dentro de `class MydataProvider`.
    - Comportamento inalterado sem env nova: `cd server && python -c "from app import candle_provider as c; print(c.provider_name())"` imprime o mesmo valor de antes deste plano (`yahoo` na ausência de `B3_CANDLE_PROVIDER`).
  </acceptance_criteria>

  <done>
    O roteador percorre a cadeia inteira aplicando gate de fatia/plano e de cota
    por elo, degrada mydata→brapi→Yahoo na mesma requisição, e nunca inventa
    candle: esgotada a cadeia, o erro sobe.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Env de operador → seleção de fonte | `B3_CANDLE_PROVIDER` / `B3_CANDLE_FALLBACK` decidem de onde vem todo número de preço do app |
| Fonte externa (mydata/brapi/Yahoo) → motor determinístico | Candle não confiável vira setup, sinal e ordem simulada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Tampering | mistura silenciosa de fontes na mesma série | accept | Merge entre fontes no DIÁRIO já é decisão registrada (ADR-008 Fase 4, errata da decisão 3: as fontes entregam o mesmo print bruto da B3, validado em 11/08/2026) e `candle_cache` grava `src` por registro. D-03 escolhe explicitamente disponibilidade sobre pureza de proveniência única |
| T-09-08 | Denial of Service (auto-infligido) | cadeia de 3 elos multiplicando chamadas | mitigate | O gate `_gate` recusa ANTES da rede (fatia, plano, cota); série vazia e exceção avançam UMA vez por elo, sem retry adicional na fronteira; o teto de cota do minuto do `mydata_budget` limita o pico |
| T-09-09 | Elevation of Privilege | `B3_CANDLE_FALLBACK` apontando para provedor arbitrário | mitigate | `fallback_names()` filtra contra `_PROVEDORES` — nome desconhecido é descartado silenciosamente, nunca instanciado. Não há caminho de env para código arbitrário |
| T-09-10 | Repudiation | não saber qual fonte serviu um número | mitigate | `out["source"]` é sempre marcado com o nome do elo que serviu; `snapshot()` agrega por provedor (`porProvedor`) e agora inclui `orcamentoMydata`; `/api/obs/usage` já expõe tudo |
| T-09-04 | Spoofing (429 forjado) | 429 do mydata acionando fallback | mitigate | Herdada do Plano 09-01: o efeito de um 429 é só descer um elo da cadeia; nenhuma guarda é desligada, nenhuma cota é ampliada. O contador local decide antes do header |
| T-09-SC | Tampering | instalação de pacote | mitigate | Nenhum pacote novo. Se o executor concluir que precisa de dependência nova, PARE e abra checkpoint humano bloqueante |
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` verde (as DUAS suítes).
- `cd server && python -c "from app import main"` importa a aplicação sem erro (o roteador foi reescrito; erro de sintaxe ou import circular apareceria aqui).
- `cd server && python -m pytest tests/ -q` verde na suíte inteira do backend — o roteador de candles é consumido por setups, indicadores, agente e opções; regressão aqui aparece longe do arquivo editado.
- Configuração de produção inalterada: `grep -rn "B3_CANDLE_PROVIDER" server/ --include=*.py` mostra apenas a leitura em `candle_provider.provider_name()`, sem novo default gravado em lugar nenhum.
</verification>

<success_criteria>
- `mydata` selecionável por env, com cadeia mydata→brapi→Yahoo funcionando na
  mesma requisição.
- Intraday nunca toca o mydata.
- Cota consultada e debitada antes de cada chamada de rede ao mydata.
- Consumo do mydata visível em `/api/obs/usage` sem editar `main.py`.
- Nenhum call site de candle modificado (D-01 cumprida).
- Suíte canônica verde.
</success_criteria>

<output>
Create `.planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-02-SUMMARY.md` when done.
Registre no SUMMARY: (a) a lista final de campos novos em `snapshot()`;
(b) qualquer teste existente que precisou de ajuste e por quê; (c) o valor
medido/estimado de chamadas ao mydata por carga cheia de um ticker (`rng="2y"`)
e por delta (`rng="1mo"`) — o Plano 09-04 vai partir desses números.
</output>
