---
phase: 09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa
plan: 06
type: execute
wave: 5
depends_on: ["09-04", "09-05"]
files_modified:
  - web/src/App.jsx
  - web/src/disclaimers.js
  - web/src/version.js
  - server/web_dist
autonomous: false
requirements: []

must_haves:
  truths:
    - "Quando o dado vem do mydata, a tela diz 'MyData' — não um código cru nem uma fonte errada"
    - "O banner do app deixa de afirmar que as cotações vêm do Yahoo Finance, afirmação que já estava desatualizada desde o ADR-008"
    - "O front atualizado está PUBLICADO em server/web_dist com carimbo novo — não fica testado e nunca no ar"
    - "A virada de B3_CANDLE_PROVIDER/B3_OPTIONS_PROVIDER em produção só acontece com o veredito CABE do Plano 09-04 na mesa e com o Alex aprovando"
    - "O Alex sabe reverter a virada por env, sem deploy, antes de ela acontecer"
  artifacts:
    - path: "web/src/App.jsx"
      provides: "Rótulo de fonte reconhecendo mydata"
      contains: "mydata"
    - path: "web/src/disclaimers.js"
      provides: "Banner do app com a fonte de cotações correta"
    - path: "server/web_dist"
      provides: "Front publicado com o carimbo novo (única árvore que o Railway enxerga)"
  key_links:
    - from: "web/src/App.jsx::FONTE_LABEL"
      to: "payload.source do backend"
      via: "source pode valer mydata a partir desta fase"
      pattern: "mydata"
    - from: "scripts/bump.sh + scripts/publicar-web.sh"
      to: "server/web_dist"
      via: "publicação manual obrigatória — merge não publica o front"
      pattern: "web_dist"
---

<objective>
Fechar a fase: alinhar os rótulos de fonte no front, publicar o build, e virar
a produção para o mydata sob aprovação humana, com o veredito da medição na mesa.

Purpose: duas razões independentes.
(1) O princípio 3 do CLAUDE.md do repo exige que todo dado de mercado exiba a
fonte. `FONTE_LABEL` só conhece `brapi` e `yahoo`, e o banner do app ainda diz
que as cotações vêm do "Yahoo Finance" — afirmação já errada desde o ADR-008 e
que fica pior com o mydata no ar.
(2) O ROADMAP e o 09-CONTEXT.md tornam a medição de rate-limit critério de
aceite: a virada não pode ser um efeito colateral de um merge, tem que ser uma
decisão registrada, com número, e com o caminho de volta conhecido antes.

Output: front corrigido, buildado, carimbado e publicado em `server/web_dist`;
e a virada de produção feita (ou adiada) pelo Alex, com checklist executado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-CONTEXT.md
@.planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-03-SUMMARY.md
@.planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-04-SUMMARY.md
@.planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-05-SUMMARY.md

<assumptions>
**Por que existe uma task de publicação.** Lição registrada no STATE do
projeto: plano que toca `web/src/` sem task de bump + `publicar-web.sh` fica
testado e nunca vai ao ar — merge de PR NÃO publica o front. `server/web_dist`
é a única árvore que o Railway enxerga (`rootDirectory=/server`), e o guardrail
do repo proíbe editá-la à mão: ela só é escrita por `scripts/publicar-web.sh`.

**Por que o checkpoint da virada é aqui e não antes.** Só depois de
09-01..09-05 existe um estado coerente para o Alex verificar de uma vez: cliente
pronto, cadeia de fallback pronta, opções prontas, cota medida, ingestão
paralela aposentada. Virar antes seria pedir aprovação de metade da migração.

**Regra de processo desta fase (lição do incidente da Fase 8, STATE.md).**
Esta fase tem checkpoint humano bloqueante. **Nenhum `git push` de nenhuma wave
sai antes da aprovação da Task 3**, mesmo que o commit de uma wave pareça
inócuo isolado. Commits locais podem existir; push, não.

**Escopo negativo:**
- Não tocar `web/src/persistence.js` — nenhum método novo de store, então a
  paridade `deviceStore` ↔ `serverStore` não é acionada.
- Não tocar `server/app/defaults.py` nem `web/src/catalog.js` — a paridade
  byte a byte de prompts não é acionada.
- Não mexer no bundle iOS (`ios_dist`, TestFlight): o app nativo carrega bundle
  local e pega a mudança na próxima entrega iOS, fora do escopo desta fase.
  Registrar isso como pendência no SUMMARY.
</assumptions>

<interfaces>
Estado atual dos dois pontos a corrigir:

`web/src/App.jsx:1122-1124` — o comentário afirma que `source` só vale
"brapi" | "yahoo", e o rótulo repassa qualquer outro valor cru:

    // ADR-008: rótulo de exibição da fonte do dado (CLAUDE.md #3 ...).
    // `source` no payload é sempre "brapi" | "yahoo"; qualquer outro valor
    // (fonte nova, futuro) aparece como veio, sem mascarar.
    const FONTE_LABEL = (source) => (source === "brapi" ? "brapi" : source === "yahoo" ? "Yahoo" : source);

`web/src/disclaimers.js:8-10` — `DISCLAIMERS.appBanner`:

    "Boris+ é um simulador EDUCACIONAL: usa cotações reais do mercado (Yahoo Finance)
     e dinheiro SIMULADO. Nada aqui sugere operar com dinheiro real nem constitui
     recomendação de investimento."

Textos de contexto que citam a cadeia de fontes e ficam desatualizados
(candidatos, o executor confere caso a caso e ajusta só os que afirmam a fonte):
    web/src/App.jsx:392   "As cotações vêm da brapi (fonte principal) e do Yahoo (backup)..."
    web/src/App.jsx:3116  "candle_provider (brapi com Yahoo de reserva); sem ..."
    web/src/App.jsx:5768  "O provedor de cotações (brapi, com Yahoo de backup) ..."
    web/src/App.jsx:6620  "... que hoje é brapi primeiro, Yahoo como backup (ADR-008) ..."
    web/src/App.jsx:6622  "... a existência é confirmada no provedor de cotações (brapi/Yahoo)."

Comandos de publicação (ordem OBRIGATÓRIA, guardrail do repo):
    bash scripts/bump.sh              # carimba web/src/version.js
    bash scripts/publicar-web.sh      # builda e copia para server/web_dist
Nunca editar `server/web_dist` direto.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rótulos de fonte no front reconhecem o mydata</name>

  <files>web/src/App.jsx, web/src/disclaimers.js</files>

  <read_first>
    - web/src/App.jsx (linhas 1118-1128) — `FONTE_LABEL` e o comentário que precisa ser atualizado junto
    - web/src/disclaimers.js (arquivo inteiro, ~60 linhas) — é a fonte única dos textos de enquadramento; o guardrail diz "reutilize SEMPRE estas strings, não escreva disclaimers soltos na UI"
    - web/src/App.jsx (linhas 385-395, 3110-3120, 5760-5775, 6615-6625) — os textos de contexto que afirmam a cadeia de fontes
    - .planning/phases/.../09-05-SUMMARY.md — a redação da ADR-020 é a referência canônica do que dizer sobre as fontes
  </read_first>

  <action>
    **1. `FONTE_LABEL` (`web/src/App.jsx:1124`).** Acrescentar o caso
    `source === "mydata"` devolvendo `"MyData"` (nome próprio do hub, capitalizado
    como o produto do outro lado se chama). Manter o repasse cru como último
    caso — a postura de "fonte nova aparece como veio, sem mascarar" continua
    valendo para a próxima. Atualizar o comentário logo acima: `source` agora
    pode valer `"mydata" | "brapi" | "yahoo"`, e citar a ADR-020 ao lado da
    ADR-008.

    **2. `DISCLAIMERS.appBanner` (`web/src/disclaimers.js`).** Trocar
    `(Yahoo Finance)` por uma formulação que não nomeie um provedor específico —
    por exemplo, "usa cotações reais do mercado" sem parêntese de fonte. Razão:
    o banner é um texto fixo, a fonte é dinâmica e agora tem três valores
    possíveis com fallback em cadeia; nomear uma delas no texto estático é
    exatamente o defeito que se está corrigindo, e nomear as três envelhece na
    próxima mudança. A fonte real de cada dado já é exibida por `FONTE_LABEL`,
    que é o mecanismo correto para o princípio 3 do CLAUDE.md do repo.
    **Não** alterar nenhuma outra chave de `DISCLAIMERS`.

    **3. Textos de contexto do App.jsx.** Nos pontos listados em `<interfaces>`,
    atualizar a descrição da cadeia para o estado pós-fase: o diário vem do
    MyData, com brapi e Yahoo como reserva nessa ordem; o spot ao vivo é da
    brapi; o intraday continua do Yahoo. Manter o tom e o tamanho de cada texto —
    são frases curtas de contexto, não é lugar de explicar a arquitetura. Onde o
    texto citar `ADR-008`, acrescentar `/ADR-020`. Se algum desses trechos for
    comentário de código e não texto renderizado, atualizar mesmo assim: o
    comentário errado é o que faz a próxima pessoa reescrever a lógica errada.

    **4. Build obrigatório.** `cd web && npx vite build`. Guardrail do repo:
    front editado exige build antes de declarar pronto — grep e teste estático
    não pegam erro de sintaxe JS num arquivo de 7.600 linhas.
  </action>

  <verify>
    <automated>cd web &amp;&amp; npx vite build</automated>
  </verify>

  <acceptance_criteria>
    - `cd web && npx vite build` sai com código 0.
    - `bash scripts/executar.sh --testes` sai com código 0 (suíte canônica COMPLETA: pytest + `web/tests/*.mjs`).
    - `grep -c '"mydata"' web/src/App.jsx` retorna >= 1 e `grep -c 'MyData' web/src/App.jsx` retorna >= 1.
    - `grep -c 'Yahoo Finance' web/src/disclaimers.js` retorna 0.
    - O resto do arquivo de disclaimers ficou intacto: `git diff --numstat web/src/disclaimers.js` mostra no máximo `1  1` (uma linha alterada).
    - `FONTE_LABEL` ainda repassa fonte desconhecida: existe no arquivo o ramo final que devolve `source` cru (`grep -c ': source' web/src/App.jsx` retorna >= 1).
    - `grep -c 'ADR-020' web/src/App.jsx` retorna >= 1.
    - Paridade não acionada: `git diff --stat web/src/persistence.js web/src/catalog.js server/app/defaults.py` não lista nenhum arquivo.
  </acceptance_criteria>

  <done>
    A tela sabe dizer "MyData" quando a fonte for essa, o banner deixou de
    afirmar uma fonte errada, os textos de contexto descrevem a cadeia real, e
    o build passa.
  </done>
</task>

<task type="auto">
  <name>Task 2: Carimbar a entrega e publicar o front</name>

  <files>web/src/version.js, server/web_dist</files>

  <read_first>
    - scripts/bump.sh (linhas 1-40) — o que o carimbo significa e por que esquecê-lo produz "bundle amputado" que o `test_ios_assets.mjs` pega
    - scripts/publicar-web.sh (linhas 1-40) — o que ele builda, para onde copia, e as flags `--so-build`/`--so-publicar`
    - web/src/version.js — o carimbo atual
    - CLAUDE.md (seção Guardrails do repositório) — "Publicação: `scripts/bump.sh` antes de `publicar-web.sh`; nunca editar `server/web_dist` direto"
  </read_first>

  <action>
    Na ordem, sem inverter:

    1. `bash scripts/bump.sh` — avança o `BUILD_ID` em `web/src/version.js`.
       Registrar no SUMMARY o carimbo gerado.
    2. `bash scripts/publicar-web.sh` — builda o front e copia para
       `server/web_dist`, sincronizando o carimbo.
    3. Revisar o diff de `server/web_dist` (é grande por natureza — hashes de
       chunk mudam) e confirmar que `index.html` e os assets referenciados
       existem e carregam o carimbo novo.
    4. Rodar `bash scripts/executar.sh --testes` DEPOIS da publicação: a suíte
       `web/tests/*.mjs` inclui guardiões de asset/bundle que só fazem sentido
       contra o `web_dist` publicado.

    **Não editar `server/web_dist` à mão em nenhuma circunstância.** Se o
    resultado da publicação parecer errado, corrija a origem em `web/src/` e
    republique.

    Deploy só-backend não se aplica aqui (o front mudou), então nada de bump
    manual de `SERVER_BUILD_ID`: o `publicar-web.sh` já deriva o carimbo do
    servidor do `BUILD_ID`.
  </action>

  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>

  <acceptance_criteria>
    - `bash scripts/executar.sh --testes` sai com código 0 (as DUAS suítes) rodando APÓS a publicação.
    - `git diff --stat web/src/version.js` mostra o arquivo alterado (o bump aconteceu).
    - `git status --short server/web_dist` mostra arquivos alterados (a publicação aconteceu).
    - O carimbo é o mesmo nos dois lados: `grep -o 'F[0-9]\{2\}-[0-9]\{8\}-[0-9]\{2\}' web/src/version.js | head -1` e o mesmo valor aparece em `server/web_dist` (`grep -rl "$(grep -o 'F[0-9]\{2\}-[0-9]\{8\}-[0-9]\{2\}' web/src/version.js | head -1)" server/web_dist | head -1` imprime pelo menos um arquivo).
    - Nenhuma edição manual em `web_dist`: o diff de `server/web_dist` contém apenas arquivos gerados (`index.html`, `assets/*`, manifest/service worker), nenhum `.md` nem `.py`.
  </acceptance_criteria>

  <done>
    O front corrigido está publicado em `server/web_dist` com carimbo novo, e a
    suíte canônica está verde contra o estado publicado — não fica testado e
    nunca no ar.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Virada de produção para o mydata — aprovação e verificação ao vivo</name>

  <read_first>
    - docs/MEDICAO-Mydata-*.md (seção Veredito) — pré-condição da virada; sem `CABE`, a resposta correta é `adiar`
    - .planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-04-SUMMARY.md — pico chamadas/min, total/dia, `intervaloMinimoSeguro`, se a perna ao vivo rodou
    - .planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-03-SUMMARY.md — o `liquidity_score` medido sem open interest
    - docs/adr/020-centralizacao-de-dados-no-mydata.md (seção Reversibilidade) — como desfazer por env, sem deploy
  </read_first>

  <action>
    Esta task é um checkpoint humano BLOQUEANTE. O executor NÃO altera nenhuma
    variável de ambiente no Railway, não faz deploy e não faz `git push`.

    O que o executor faz:
    1. Ler os quatro artefatos de `<read_first>` e montar o resumo dos números
       (veredito, pico chamadas/min, total chamadas/dia, `intervaloMinimoSeguro`,
       `liquidity_score` sem open interest) — números REAIS extraídos dos
       arquivos, nunca estimados.
    2. Se o veredito for `NÃO CABE`, dizer isso em primeiro lugar e recomendar
       `adiar` explicitamente, antes de apresentar o roteiro.
    3. Apresentar ao Alex o bloco `<what-built>` e o roteiro de
       `<how-to-verify>`, na ordem, com os números do passo 1 preenchidos.
    4. Aguardar a resposta. A mudança das variáveis no Railway é ação do Alex.
    5. Registrar a resposta e, se `aprovado`, o resultado de cada passo
       verificado ao vivo. Só então liberar o `git push` da fase.
  </action>

  <what-built>
    A migração completa está pronta e testada, com a produção AINDA nas fontes
    antigas. O que foi automatizado nesta fase:

    - `server/app/mydata_client.py` — cliente do hub `mydata.acamerini.app`,
      auth `X-API-Key`, retry, paginação por cursor, recusa de intraday sem rede.
    - `server/app/mydata_budget.py` — cota 60/min · 2.000/dia, contador diário
      persistido, reconciliação com os headers `X-Quota-*` do hub.
    - `server/app/candle_provider.py` — `MydataProvider` registrado e o fallback
      deixou de ser um salto só: agora é cadeia mydata→brapi→Yahoo, com gate de
      fatia e de cota por elo.
    - `server/app/options_provider_mydata.py` + `options_provider.py` — cadeia de
      opções com IV e gregas vindas prontas do hub, atrás do mesmo contrato
      `providerStatus` do ADR-004, SEM fallback para o Yahoo (D-04).
    - `scripts/medir-mydata.py` + `docs/MEDICAO-Mydata-<data>.md` — a medição de
      rate-limit que o ROADMAP exige como critério de aceite.
    - Ingestão paralela de COTAHIST aposentada e `docs/adr/020` escrita.
    - Front publicado com o rótulo de fonte reconhecendo o MyData.

    **Nada disso está ativo em produção.** `B3_CANDLE_PROVIDER` e
    `B3_OPTIONS_PROVIDER` continuam nos valores de antes. A virada é uma mudança
    de variável de ambiente no Railway — sua decisão, não um efeito do merge.
  </what-built>

  <how-to-verify>
    **Antes de virar — leia os números (2 min).**

    1. Abra `docs/MEDICAO-Mydata-<data>.md` e leia a seção **Veredito**.
       - Se o veredito for **NÃO CABE**: PARE. Não vire nada. Responda
         `adiar` e diga se prefere negociar aumento de quota com o lado
         cvm-financas ou reduzir a cadência de varredura para o
         `intervaloMinimoSeguro` calculado. A fase fecha sem a virada, e o
         código fica pronto atrás das env vars.
       - Se for **CABE**, siga.
    2. Confirme os quatro números do 09-04-SUMMARY: pico chamadas/min, total
       chamadas/dia, `intervaloMinimoSeguro`, e se a perna ao vivo rodou de fato
       (não só a projeção).
    3. Leia o `liquidity_score` medido no 09-03-SUMMARY para um contrato líquido
       SEM open interest. Se ele ficou abaixo de 40, a linha de opções vai sumir
       do card para ativos que hoje aparecem — decida se aceita esse efeito agora
       ou se quer ajustar o corte antes de virar as opções.

    **Como reverter (saiba ANTES de virar).**

    Sem deploy, sem código: no Railway, apagar/reajustar as variáveis
    `B3_CANDLE_PROVIDER` e `B3_OPTIONS_PROVIDER` para os valores anteriores e
    reiniciar o serviço. O comportamento antigo volta inteiro. Confirme que sabe
    onde ficam essas variáveis no painel antes de seguir.

    **Virada — candles primeiro, opções depois. Uma de cada vez.**

    4. No Railway, defina `MYDATA_TOKEN` (chave de prefixo `f00b4554`) se ainda
       não estiver lá. Não defina `MYDATA_URL` — o default já é
       `https://mydata.acamerini.app`.
    5. Defina `B3_CANDLE_PROVIDER=mydata`. Não mexa em `B3_CANDLE_FALLBACK`: o
       default com mydata de primário já é `brapi,yahoo`.
    6. Aguarde o redeploy e abra `/api/health` — confirme que responde e que o
       `SERVER_BUILD_ID` bate com o carimbo publicado na Task 2.
    7. Com uma conta de administração, abra `/api/obs/usage` e confirme:
       - `candles.provedor` == `"mydata"`;
       - `candles.fallbacks` == `["brapi", "yahoo"]`;
       - `candles.orcamentoMydata` presente, com `gastoDia` começando a subir;
       - `candles.porProvedor.mydata.taxaFalha` baixa (idealmente 0).
    8. No app, abra o card de um ativo do Radar (ex.: PETR4). Confirme:
       - o gráfico diário carrega, com candles até o último pregão;
       - o rótulo de fonte mostra **MyData**;
       - nenhum estado de erro ou "sem histórico".
    9. Abra um ativo FORA do catálogo padrão, digitando o código. Confirme que a
       existência ainda é validada e o histórico carrega (é o caminho que mais
       exercita a cadeia de fallback).
    10. Troque o gráfico para intraday (15min). Confirme que continua
        carregando — este é o teste de que o mydata é recusado sem rede e o
        Yahoo assume, conforme a ADR-001 que esta fase NÃO reabre.
    11. Volte a `/api/obs/usage` e confirme que `orcamentoMydata.gastoMinuto`
        ficou abaixo de 60 durante toda a janela.

    **Só depois de 5-11 estarem OK, vire as opções.**

    12. Defina `B3_OPTIONS_PROVIDER=mydata` e aguarde o redeploy.
    13. Abra a cadeia de opções de PETR4 no app. Confirme:
        - a lista de vencimentos aparece e NÃO traz vencimento já liquidado;
        - a cadeia carrega com strikes, prêmio, bid/ask;
        - a volatilidade implícita aparece na maior parte dos contratos (parte
          nula é legítima e esperada — a B3 não publica prêmio para tudo);
        - a fonte exibida é MyData;
        - a linha de opções do card do ativo (o gate de liquidez) continua
          aparecendo para os ativos onde aparecia antes — este é o efeito do
          `openInterest` ausente que você avaliou no passo 3.
    14. Desligue temporariamente o acesso (ou apenas espere um erro real, se
        ocorrer) e confirme que uma falha de opções mostra o aviso de
        indisponibilidade e BLOQUEIA a compra simulada — nunca uma cadeia
        inventada. Se não for prático forçar a falha, apenas confirme que
        entende que esse é o comportamento esperado (D-04, ADR-004).

    **Fechamento.**

    15. Confirme que nenhum número da carteira mudou de forma inesperada:
        saldo, posições, preço médio e P&L continuam iguais aos de antes da
        virada. Candle é insumo; nenhum cálculo de carteira deveria se mover só
        por troca de fonte.
  </how-to-verify>

  <acceptance_criteria>
    - Os números apresentados ao Alex saíram literalmente de `docs/MEDICAO-Mydata-*.md`, `09-04-SUMMARY.md` e `09-03-SUMMARY.md` — nenhum valor estimado pelo executor.
    - Se o veredito da medição for `NÃO CABE`, a recomendação apresentada é `adiar` e nenhuma variável é virada.
    - O executor não alterou nenhuma variável no Railway nem fez `git push` durante esta task.
    - A resposta do Alex (`aprovado` | `adiar` | descrição de falha com o número do passo) está registrada literalmente no SUMMARY.
    - Se `aprovado`: os valores finais de `B3_CANDLE_PROVIDER` e `B3_OPTIONS_PROVIDER` em produção estão registrados, e os passos 7, 8, 10, 13 e 15 têm resultado anotado (OK ou o que foi observado).
    - Se `adiar`: o motivo e a condição de retomada estão registrados, e o código permanece pronto atrás das env vars inalteradas.
  </acceptance_criteria>

  <resume-signal>
    Responda `aprovado` (virada feita e verificada), `adiar` (veredito NÃO CABE
    ou você prefere esperar — diga o motivo), ou descreva o que deu errado com o
    número do passo.
  </resume-signal>

  <done>
    O Alex respondeu `aprovado` (virada feita e os passos 7, 8, 10, 13 e 15
    verificados ao vivo) ou `adiar` (com motivo e condição de retomada
    registrados). Em qualquer dos dois casos, os valores finais de
    `B3_CANDLE_PROVIDER` e `B3_OPTIONS_PROVIDER` em produção estão registrados e
    o caminho de rollback por env foi confirmado como conhecido antes da virada.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Painel do Railway → produção | `MYDATA_TOKEN`, `B3_CANDLE_PROVIDER` e `B3_OPTIONS_PROVIDER` mudam a fonte de todo dado de mercado do app em produção |
| `web/src/` → `server/web_dist` | É o único caminho pelo qual o front chega ao usuário; edição manual do destino quebra o rastro do carimbo |
| Texto renderizado → usuário | Rotular a fonte errada é violação direta do princípio 3 do CLAUDE.md do repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-26 | Information Disclosure | `MYDATA_TOKEN` entrando no bundle do front | mitigate | A credencial é lida SÓ por `server/app/mydata_client.py` a partir de `os.environ`; nenhuma variável `VITE_*` nova é criada nesta fase. Critério de aceite grepa o `web_dist` publicado por qualquer padrão de chave |
| T-09-27 | Repudiation | virar produção sem registro do porquê | mitigate | A virada é `checkpoint:human-verify` bloqueante, com o veredito da medição como pré-condição explícita e com `adiar` como resposta de primeira classe |
| T-09-28 | Denial of Service | virar as duas fatias de uma vez e não saber qual quebrou | mitigate | O roteiro vira candles (passos 5-11) e só depois opções (passo 12), com verificação entre as duas |
| T-09-29 | Tampering | dado de fonte nova alterando número de carteira | mitigate | Passo 15 verifica explicitamente que saldo, posições, preço médio e P&L não se moveram. O motor é determinístico e candle é insumo — movimento aqui seria defeito, não efeito |
| T-09-30 | Spoofing | usuário achando que vê Yahoo quando vê MyData (ou vice-versa) | mitigate | `FONTE_LABEL` ganha o caso `mydata` e o banner deixa de nomear provedor fixo; passos 8 e 13 verificam o rótulo ao vivo |
| T-09-31 | Denial of Service | front publicado sem carimbo novo (bundle amputado) | mitigate | Ordem obrigatória `bump.sh` → `publicar-web.sh`, critério de aceite compara o carimbo dos dois lados, e `web/tests/test_ios_assets.mjs` roda depois da publicação |
| T-09-SC | Tampering | instalação de pacote | mitigate | Nenhum pacote novo. `npx vite build` usa o `node_modules` já existente de `web/`. Se o executor concluir que precisa de dependência nova, PARE e abra checkpoint humano bloqueante |
</threat_model>

<verification>
- `cd web && npx vite build` verde.
- `bash scripts/executar.sh --testes` verde APÓS a publicação.
- Nenhum segredo no bundle publicado: `grep -rlE '[A-Za-z0-9]{24,}' server/web_dist/assets/ | xargs -r grep -l 'MYDATA' ` não retorna nada, e `grep -rc 'MYDATA_TOKEN' server/web_dist` retorna 0.
- Carimbo consistente entre `web/src/version.js`, `server/web_dist` e `/api/health` depois do deploy.
- **Push só depois da aprovação da Task 3** (regra desta fase, lição do incidente da Fase 8).
</verification>

<success_criteria>
- Front corrigido, buildado, carimbado e publicado.
- Virada de produção feita — ou explicitamente adiada com motivo registrado —
  com o veredito da medição na mesa e o caminho de rollback conhecido antes.
- Nenhum número de carteira alterado pela troca de fonte.
- Suíte canônica verde.
</success_criteria>

<output>
Create `.planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-06-SUMMARY.md` when done.
Registre no SUMMARY: (a) o carimbo publicado; (b) a resposta do Alex no
checkpoint (`aprovado` × `adiar`) e, se adiado, o motivo e a condição de
retomada; (c) os valores finais de `B3_CANDLE_PROVIDER` e `B3_OPTIONS_PROVIDER`
em produção; (d) a pendência do bundle iOS (a mudança de rótulo só chega ao app
nativo na próxima entrega TestFlight).
</output>
