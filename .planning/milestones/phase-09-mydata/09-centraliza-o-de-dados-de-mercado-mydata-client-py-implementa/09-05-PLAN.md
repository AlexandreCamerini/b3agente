---
phase: 09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa
plan: 05
type: execute
wave: 4
depends_on: ["09-04"]
files_modified:
  - server/app/b3_historical.py
  - server/tests/test_b3_historical.py
  - server/app/agent.py
  - server/app/db.py
  - server/app/main.py
  - server/app/rbac.py
  - docs/adr/019-cotahist-diario-b3.md
  - docs/adr/020-centralizacao-de-dados-no-mydata.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "O Boris deixa de manter uma ingestão própria e paralela do COTAHIST — a duplicação que criou esta fase acabou"
    - "A decisão sobre o destino do código de b3_historical (commit local b3fdf02, não pushado) foi tomada pelo Alex, não presumida pelo executor"
    - "Nenhum caminho de código no servidor baixa arquivo da B3 por conta própria"
    - "O que sair do disco sai por um commit, não por um `rm` solto — a árvore de trabalho fica limpa e o git registra a saída"
    - "O registro histórico da decisão de 25/08 (ADR-019) permanece legível — histórico se carimba, não se reescreve"
    - "Existe uma ADR nova dizendo o que foi superado, por quê, e o que deliberadamente NÃO foi"
  artifacts:
    - path: "docs/adr/020-centralizacao-de-dados-no-mydata.md"
      provides: "Supersessão parcial de ADR-019 e ADR-008; não-supersessão de ADR-001 e ADR-004"
      contains: "ADR-019"
    - path: "docs/adr/019-cotahist-diario-b3.md"
      provides: "Registro da época, carimbado com o status de superada"
      contains: "Superada parcialmente pela ADR-020"
  key_links:
    - from: "server/app/agent.py::scheduler_loop"
      to: "b3_historical.maybe_run"
      via: "REMOÇÃO do hook — o laço não pode mais baixar COTAHIST"
      pattern: "b3_historical"
---

<objective>
Aposentar a ingestão paralela de COTAHIST do Boris, agora que o mydata cobre a
fatia diária, e registrar a supersessão numa ADR nova.

Purpose: o achado que originou esta fase é literal — em 25/08 o Boris
reimplementou em paralelo a ingestão do COTAHIST que o hub já roda em produção,
"duas capturas do mesmo arquivo oficial, com proveniências separadas: exatamente
o que a fronteira existe para impedir". Manter os dois é pagar duas vezes pelo
mesmo dado e criar dois números possíveis para o mesmo pregão.

Output: a ingestão paralela sai (pela via que o Alex decidir), o hook do
scheduler que baixava arquivo da B3 sai, o ADR-019 é carimbado, e `docs/adr/020`
registra o que foi superado e o que deliberadamente não foi.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-CONTEXT.md
@.planning/notes/boris-pp-centralizacao-dados-mydata.md
@docs/adr/019-cotahist-diario-b3.md
@.planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-04-SUMMARY.md

<assumptions>
**Estado de partida, reverificado em 2026-08-27 e a ser RECONFIRMADO na Task 1.**
O trabalho do ADR-019 está **COMMITADO LOCALMENTE e NÃO PUSHADO**:

    commit b3fdf02  feat(b3-historical): acervo diário oficial COTAHIST da B3 (ADR-019)
                    7 arquivos, +754/-1

    novos:       server/app/b3_historical.py            (+471)
                 server/tests/test_b3_historical.py     (+130)
                 docs/adr/019-cotahist-diario-b3.md     (+62)
    modificados: server/app/agent.py   (+8:  hook b3_historical.maybe_run em scheduler_loop)
                 server/app/db.py      (+36: tabelas b3_daily_imports e b3_daily_quotes)
                 server/app/main.py    (+46: import + GET/POST /api/admin/b3/cotahist*)
                 server/app/rbac.py    (+-1: entidade "b3_daily_import" em fontes_dados.configurar)

Três fatos que mudam o custo de cada saída — e que a versão anterior deste plano
não tinha:

1. **O commit existe e é do próprio Alex**, feito deliberadamente antes desta
   fase. A mensagem dele diz o porquê, literalmente: *"commitando antes da Fase 9
   (ADR-020/mydata_client.py) aposentar esta fatia, para preservar o 'antes' da
   migração no histórico."* A intenção declarada é preservar o registro — isso é
   entrada da decisão da Task 1, não detalhe de bastidor.
2. **Nada foi pushado.** `git log --oneline origin/main..HEAD` lista b3fdf02 (e
   os commits de doc ao redor). Como o deploy do Railway vem do `main` remoto,
   **nenhuma linha jamais rodou em produção**: nenhuma tabela nasceu no SQLite do
   Railway, nenhum servidor do Boris baixou arquivo da B3.
3. **No banco de DESENVOLVIMENTO local, porém, a ingestão JÁ RODOU.**
   `server/data/b3_agente.db` tem `b3_daily_imports` (1 linha) e
   `b3_daily_quotes` (16.846 linhas). Isso não afeta produção, mas significa que
   a frase "as tabelas nunca foram criadas" é falsa para o disco do Alex — e é
   por isso que o item (c) da Task 2 proíbe `DROP TABLE` e manda parar se o
   assunto aparecer.
4. **Nada tocou esses 7 arquivos depois de b3fdf02**
   (`git log b3fdf02..HEAD -- <os 7>` sai vazio). Uma remoção limpa não tem
   conflito com trabalho posterior.

**Guardrails do repositório que se aplicam aqui:**
- "Histórico não se reescreve" vale para `qa/`, `ESTADO-*`, `CHECKOUT-*` e
  RELEASES. `docs/adr/` registra decisões e também não tem texto apagado — uma
  ADR superada ganha cabeçalho de status, não some. Consequência mecânica direta:
  **`git revert b3fdf02` é a ferramenta errada**, porque ele apagaria
  `docs/adr/019-cotahist-diario-b3.md` junto.
- "Não force push" (CLAUDE.md do repo) e "não-destrutivo por padrão: confirme
  antes de deletar" (CLAUDE.md global).
- "Guardiões de teste não se apagam — reversão deliberada atualiza o guardião
  com nota." `test_b3_historical.py` é guardião de um comportamento revertido
  de propósito; a nota é a ADR-020 e a mensagem do commit de remoção.

**Escopo negativo:** este plano NÃO vira nenhum default de produção
(`B3_CANDLE_PROVIDER`/`B3_OPTIONS_PROVIDER` ficam como estão — isso é o Plano
09-06) e NÃO toca `web/`.
</assumptions>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decisão do Alex sobre o destino do código do ADR-019</name>

  <read_first>
    - Rodar `git status --short`, `git log --oneline origin/main..HEAD` e `git show --stat b3fdf02` ANTES de apresentar a decisão, e reportar o estado REAL encontrado. Se ele divergir do descrito em `<assumptions>` (por exemplo, o commit já foi pushado, ou alguém mexeu nos arquivos depois), apresentar a divergência primeiro e recalibrar as opções — não seguir com o roteiro escrito.
    - docs/adr/019-cotahist-diario-b3.md
    - .planning/notes/boris-pp-centralizacao-dados-mydata.md
  </read_first>

  <action>
    NÃO executar nenhuma remoção, `rm`, `git rm`, `git commit`, `git revert`,
    `git reset`, `git rebase` ou edição de arquivo nesta task. A única ação
    permitida é DIAGNÓSTICO E APRESENTAÇÃO:

    1. Rodar `git status --short` e `git show --stat b3fdf02`, e comparar com o
       estado descrito em `<assumptions>` (commit de 7 arquivos, +754/-1).
    2. Rodar `git log --oneline origin/main..HEAD` para confirmar que b3fdf02
       continua **local e não pushado**. Se ele já estiver em `origin/main`, o
       leque de saídas muda (reescrever histórico deixa de ser sequer discutível)
       — sinalizar e recalibrar.
    3. Rodar `git log --oneline b3fdf02..HEAD -- server/app/b3_historical.py server/tests/test_b3_historical.py server/app/agent.py server/app/db.py server/app/main.py server/app/rbac.py docs/adr/019-cotahist-diario-b3.md`
       para confirmar que nada tocou esses arquivos depois do commit. Se algo
       tocou, a remoção precisa de sequenciamento e isso entra na apresentação.
    4. Confirmar o estado das tabelas no banco local:
       `sqlite3 server/data/b3_agente.db "select name from sqlite_master where type='table' and name like 'b3_daily%';"`
       e as contagens. O esperado é que EXISTAM com dado dentro (ver
       `<assumptions>`, item 3) — reportar o número real encontrado, porque ele é
       o custo concreto de qualquer conversa sobre limpar banco.
    5. Apresentar ao Alex o estado real encontrado, as três opções abaixo com
       seus prós e contras, e a recomendação. Se o estado real divergir do
       esperado, apresentar a divergência PRIMEIRO e recalibrar as opções antes
       de pedir a decisão.
    6. Aguardar a resposta. A Task 2 é quem executa.
  </action>

  <decision>
    O que fazer com a implementação de ingestão paralela do COTAHIST
    (`b3_historical.py` + 91 linhas de fiação em 4 arquivos + ADR-019), que já
    está no histórico local em b3fdf02, ainda não foi pushada, e que esta fase
    aposenta.
  </decision>

  <context>
    Em 25/08 o Boris implementou a leitura diária do COTAHIST da B3 (ADR-019:
    baixa o ZIP, valida o TXT de posições fixas de 245 colunas, grava em
    `b3_daily_quotes`, SHA-256 para idempotência, job no scheduler às 20:30 BRT).
    Um dia depois, a auditoria do lado cvm-financas apontou que o hub já roda
    exatamente essa ingestão em produção — mesma fonte, mesmo arquivo,
    proveniências separadas. Foi o achado que criou esta Fase 9.

    Os Planos 09-01 a 09-03 já entregaram o caminho pelo mydata; o 09-04 já
    mediu se a cota aguenta.

    **O que mudou desde a primeira versão desta decisão:** o trabalho do ADR-019
    deixou de ser rascunho na árvore e virou o commit b3fdf02, feito pelo próprio
    Alex, com a intenção declarada de "preservar o 'antes' da migração no
    histórico". Ele NÃO foi pushado — então continua verdade que nada disso rodou
    em produção: nenhuma tabela existe no SQLite do Railway, nenhum download saiu
    de um servidor do Boris. No disco de desenvolvimento, porém, a importação
    rodou: as duas tabelas existem com ~16,8 mil cotações.

    A consequência prática é que a pergunta encolheu. "Perder o trabalho" saiu de
    cena: `git show b3fdf02` recupera as 754 linhas para sempre. O que resta
    decidir é se o módulo continua no caminho de código, e se o commit que o
    trouxe fica no histórico.

    Um detalhe que pesa na balança: o ADR-019 entrega algo que o mydata NÃO
    entrega hoje — a linha COTAHIST CRUA, com `bdi_code`, `market_type`,
    `specification`, `reference_term`, `average`, `exercise_price`. O
    `gold_cotacoes` do mydata é um recorte à vista (`tp_mercado='010'`) já
    normalizado. Nada no Boris consome esses campos crus hoje, e o mydata expõe
    `/v1/meta/linhagem` para rastro. Mas é uma capacidade que sai junto.
  </context>

  <options>
    <option id="remover">
      <name>Remover no commit seguinte — b3fdf02 fica no histórico como o "antes"</name>
      <pros>
        Dedup total e imediata: uma fonte, uma proveniência, zero código morto no
        caminho ativo. Com b3fdf02 já no histórico, esta saída entrega as DUAS
        coisas que antes disputavam entre si — a limpeza e o rastro: o código é
        recuperável para sempre por `git show b3fdf02`, e o `git log` conta a
        cronologia honesta de 25→27/08 exatamente como o Alex quis ao commitar.
        Menor superfície: uma dependência externa a menos (download direto de
        `bvmf.bmfbovespa.com.br`) e um par de endpoints admin a menos. É a leitura
        literal do achado que criou a fase.
      </pros>
      <cons>
        O `CREATE TABLE` das duas tabelas passa a existir no histórico: se algum
        dia b3fdf02 for publicado SOZINHO, sem o commit de remoção junto, o deploy
        cria as tabelas no SQLite do Railway e elas ficam lá vazias para sempre
        (`init_db` é aditivo, não tem DROP). Mitigação, não risco aberto: nada foi
        pushado ainda, e a regra de processo desta fase já segura o push da fase
        inteira até o checkpoint do 09-06 — remoção e commit original viajam no
        mesmo push. No banco local do Alex as tabelas continuam existindo com as
        16,8 mil linhas; limpar isso é ação destrutiva separada, fora do escopo
        desta task. Tensão real com o guardrail "guardiões de teste não se
        apagam — reversão deliberada atualiza o guardião com nota":
        `test_b3_historical.py` (o guardião) deixa de existir na árvore — a
        nota que o guardrail exige vive no commit message + ADR-020, não
        dentro do arquivo, ao contrário de como `congelar` satisfaz a mesma
        regra literalmente (nota no topo do próprio arquivo).
      </cons>
    </option>
    <option id="congelar">
      <name>Manter o módulo, desligado e sem fiação</name>
      <pros>
        `b3_historical.py` e seu teste continuam no repositório como plano B
        executável; só o hook do scheduler, as rotas admin, as tabelas e a
        entidade RBAC saem. Se o mydata falhar estruturalmente, religar é pequeno.
      </pros>
      <cons>
        Código morto com teste verde é a pior categoria de dívida: parece
        mantido, não é exercitado, e envelhece em silêncio contra um layout de
        arquivo (COTAHIST, 245 colunas de posição fixa) que ninguém monitora.
        Contradiz o próprio motivo da fase, e o teste verde dá falsa confiança de
        que o caminho funciona. Com b3fdf02 já no histórico, o argumento de
        "guardar para não perder" perdeu força: guardar no `git log` custa zero e
        não envelhece; guardar no `main` custa manutenção.
      </cons>
    </option>
    <option id="desfazer-o-commit">
      <name>Reescrever o histórico local — b3fdf02 nunca existiu</name>
      <pros>
        Mecanicamente possível, e só por pouco tempo: nada foi pushado, então um
        `git rebase --onto` / `git reset` ainda pode tirar b3fdf02 do histórico
        sem force-push no remoto. Resultado: `main` limpo, sem nenhum registro de
        que a ingestão paralela existiu, e sem o `CREATE TABLE` em lugar nenhum
        do histórico — o risco (já pequeno) da opção `remover` some.
      </pros>
      <cons>
        Contradiz frontalmente a intenção que o próprio Alex escreveu na mensagem
        do commit ("para preservar o 'antes' da migração no histórico"), e o
        guardrail do repositório de não reescrever histórico. Pior: b3fdf02 é
        também o commit que trouxe `docs/adr/019-cotahist-diario-b3.md` — apagá-lo
        apaga a ADR do disco e do histórico, sendo que a Task 2 precisa carimbá-la
        e a ADR-020 da Task 3 precisa referenciá-la. Seria preciso re-adicionar a
        ADR à mão, um remendo que entrega menos rastro por mais trabalho. E
        reescrita de histórico local nesta fase colide com a regra de processo do
        push único no fim (mais chance de conflito, nenhum ganho).
      </cons>
    </option>
  </options>

  <recommendation>
    `remover`. O commit b3fdf02 resolveu de graça o único trade-off real que essa
    decisão tinha: preservar o rastro. Com ele no histórico, remover o código do
    caminho ativo não perde nada — `git show b3fdf02` restaura as 754 linhas
    quando (e se) o mydata falhar estruturalmente — e entrega a dedup que motivou
    a fase inteira. `congelar` paga manutenção por uma garantia que o `git log`
    já dá. `desfazer-o-commit` desfaz uma decisão deliberada do Alex, arrasta
    junto a ADR-019 que ainda precisamos, e troca um risco já mitigado por um
    trabalho manual de recomposição.

    Mecânica importante para a Task 2: `remover` NÃO é `git revert b3fdf02`. Um
    revert cego apagaria `docs/adr/019-cotahist-diario-b3.md`, violando o
    guardrail de histórico e quebrando a referência da ADR-020. A remoção é
    explícita: `git rm` dos dois arquivos de código + desfiar a→d + carimbar a
    ADR-019, num commit só.
  </recommendation>

  <acceptance_criteria>
    - A saída de `git status --short`, `git log --oneline origin/main..HEAD` e `git show --stat b3fdf02` foi apresentada LITERALMENTE ao Alex, não parafraseada.
    - A contagem real de `b3_daily_imports`/`b3_daily_quotes` no banco local foi apresentada (número, não adjetivo).
    - Qualquer divergência em relação ao estado descrito em `<assumptions>` foi sinalizada antes da pergunta — em especial se b3fdf02 já estiver pushado.
    - Nenhum arquivo foi criado, removido, editado ou commitado durante esta task: `git status --short` e `git rev-parse HEAD` imediatamente após são idênticos aos de antes.
    - A resposta do Alex foi registrada literalmente (`remover` | `congelar` | `desfazer-o-commit` | outra), e a Task 2 só começa depois dela.
  </acceptance_criteria>

  <resume-signal>Responda: `remover`, `congelar`, `desfazer-o-commit` — ou descreva outra saída.</resume-signal>

  <done>
    O Alex escolheu uma das saídas (ou descreveu outra), com o estado real do git
    e do banco local na mesa, e a escolha está registrada literalmente. Nenhum
    arquivo foi tocado nesta task.
  </done>
</task>

<task type="auto">
  <name>Task 2: Executar a decisão e desfiar o scheduler</name>

  <files>server/app/b3_historical.py, server/tests/test_b3_historical.py, server/app/agent.py, server/app/db.py, server/app/main.py, server/app/rbac.py, docs/adr/019-cotahist-diario-b3.md</files>

  <read_first>
    - A resposta do Alex na Task 1 (obrigatório — não executar nada antes dela)
    - server/app/agent.py (linhas 1145-1210) — o bloco de hooks do `scheduler_loop`, para remover SÓ o do `b3_historical` sem encostar nos vizinhos (analytics, automacao, obs-metricas, fundamentals.maybe_warm, signal_ledger_job)
    - server/app/db.py (linhas 140-190) — as duas tabelas adicionadas e o que vem antes/depois delas em `init_db`
    - server/app/main.py (linhas 40-47 e 710-762) — o import e as duas rotas admin
    - server/app/rbac.py (linhas 85-95) — o dicionário `ENTIDADES_POR_PERMISSAO`
  </read_first>

  <action>
    Executar a saída escolhida pelo Alex. Em TODAS as saídas, a fiação abaixo é
    desfeita — é ela que faz o servidor baixar arquivo da B3, e é ela que a fase
    aposenta:

    **a) `server/app/agent.py`** — remover o bloco `try/except` que faz
    `from . import b3_historical` e `await b3_historical.maybe_run(conn)`
    (inserido logo depois do hook `[obs-metricas]`, ~8 linhas). Os hooks vizinhos
    ficam intactos.

    **b) `server/app/main.py`** — remover a linha de import
    `from . import b3_historical` e as duas rotas `GET /api/admin/b3/cotahist` e
    `POST /api/admin/b3/cotahist/import` (funções `admin_b3_cotahist_get` e
    `admin_b3_cotahist_import`, ~46 linhas contíguas entre `admin_summary` e o
    bloco de comentário da central de administração do ADR-013). Não encostar em
    `admin_summary` nem no bloco seguinte.

    **c) `server/app/db.py`** — remover os dois `CREATE TABLE IF NOT EXISTS`
    (`b3_daily_imports`, `b3_daily_quotes`) e seus dois `CREATE INDEX`.
    **Não** acrescentar `DROP TABLE` e **não** mexer em banco nenhum. O `init_db`
    deste repo é aditivo e idempotente por desenho: tirar o `CREATE` não apaga
    dado. Em produção não há o que derrubar (b3fdf02 nunca foi pushado, logo nunca
    deployado). No banco local do Alex as tabelas EXISTEM com ~16,8 mil linhas
    (ver `<assumptions>`) e **continuam existindo, órfãs e inertes** — isso é
    esperado e aceito. Se o Alex pedir a limpeza do banco local, isso é uma ação
    destrutiva com confirmação própria, FORA do escopo desta task: PARE e reporte.

    **d) `server/app/rbac.py`** — reverter
    `"fontes_dados.configurar": {"brapi_spot_intervalo", "b3_daily_import"}`
    para `{"brapi_spot_intervalo"}`. Verificar se algum teste de RBAC assere o
    conteúdo desse conjunto e ajustá-lo junto, se houver.

    **e) `server/app/b3_historical.py` e `server/tests/test_b3_historical.py`** —
    conforme a decisão. Atenção: os dois arquivos são RASTREADOS pelo git desde
    b3fdf02. `rm` solto não reverte nada — deixa a árvore suja e o histórico
    intacto. Toda saída termina em COMMIT, não em disco mexido.
      - `remover`: `git rm server/app/b3_historical.py server/tests/test_b3_historical.py`,
        e commitar **num único commit** junto com a fiação desfeita (a→d) e o
        carimbo do item (f). **NÃO usar `git revert b3fdf02`**: o revert cego
        apagaria `docs/adr/019-cotahist-diario-b3.md`, que precisa sobreviver
        carimbada (guardrail "histórico não se reescreve" + a ADR-020 da Task 3
        referencia esse arquivo). Mensagem do commit deve citar `ADR-020` e o hash
        `b3fdf02` — é ela que cumpre o guardrail "reversão deliberada atualiza o
        guardião com nota", já que o guardião sai do disco.
      - `congelar`: manter os dois arquivos rastreados, desfazer só a fiação
        (a→d), e acrescentar no topo de `b3_historical.py` um bloco de comentário
        dizendo em que data e por qual decisão ele saiu do caminho ativo,
        apontando para `docs/adr/020`. No topo de `test_b3_historical.py`, a nota
        que o guardrail do repositório exige ("reversão deliberada atualiza o
        guardião com nota"). Commitar tudo (fiação + notas + item f) num commit só.
      - `desfazer-o-commit`: **NÃO executar de forma autônoma.** Reescrita de
        histórico (`git rebase`/`git reset`) colide com o guardrail do repositório
        e apaga `docs/adr/019-cotahist-diario-b3.md` junto. PARE, reporte o plano
        exato de comandos (incluindo como a ADR-019 seria re-adicionada) e exija
        confirmação humana explícita antes de qualquer comando que altere
        histórico. `git push --force` continua proibido em qualquer hipótese.

    **f) `docs/adr/019-cotahist-diario-b3.md`** — em TODAS as saídas: acrescentar
    um cabeçalho de status logo abaixo do título, no formato exato
    `**Status:** Superada parcialmente pela ADR-020 (AAAA-MM-DD) — a fatia diária passa a vir do mydata.`
    **Não alterar uma linha do texto original abaixo dele.** O repositório não
    reescreve o registro do que se pensava na época; ele carimba. Este carimbo
    entra no MESMO commit da saída escolhida.

    Ao final, provar por grep que o servidor não tem mais caminho para baixar
    COTAHIST da B3 por conta própria, e por `git status`/`git log` que a saída
    virou commit, não sujeira na árvore.
  </action>

  <verify>
    <automated>cd server &amp;&amp; python -c "from app import main" &amp;&amp; python -m pytest tests/ -q</automated>
  </verify>

  <acceptance_criteria>
    - `cd server && python -c "from app import main"` importa sem erro.
    - `cd server && python -m pytest tests/ -q` sai com código 0.
    - `bash scripts/executar.sh --testes` sai com código 0 (suíte canônica COMPLETA: pytest + `web/tests/*.mjs`).
    - `grep -vE '^\s*#' server/app/agent.py | grep -c 'b3_historical'` retorna 0.
    - `grep -vE '^\s*#' server/app/main.py | grep -c 'b3_historical'` retorna 0.
    - `grep -vE '^\s*#' server/app/db.py | grep -cE 'b3_daily_quotes|b3_daily_imports'` retorna 0.
    - `grep -vE '^\s*#' server/app/rbac.py | grep -c 'b3_daily_import'` retorna 0.
    - Nenhum download direto da B3 sobrou: na saída `remover`, `grep -rlE 'bvmf.bmfbovespa.com.br|COTAHIST_D' server/app/` não imprime nada; na saída `congelar`, imprime SOMENTE `server/app/b3_historical.py`.
    - Os hooks vizinhos do scheduler continuam lá: `grep -cE 'analytics|maybe_warm|signal_ledger' server/app/agent.py` retorna >= 3.
    - `docs/adr/019-cotahist-diario-b3.md` existe e contém `Superada parcialmente pela ADR-020`, com o corpo original intacto — provar por `git diff b3fdf02 -- docs/adr/019-cotahist-diario-b3.md`: apenas linhas ADICIONADAS (nenhuma linha começando com `-`, exceto o cabeçalho `---` do próprio diff), no máximo 2.
    - **Estado do git, não só do disco** — em `remover` e `congelar`:
      - `git status --short -- server/app/b3_historical.py server/tests/test_b3_historical.py server/app/agent.py server/app/db.py server/app/main.py server/app/rbac.py docs/adr/019-cotahist-diario-b3.md` não imprime NADA (nada untracked, nada unstaged, nada staged-e-não-commitado).
      - `git log --oneline -1` aponta para um commit novo, criado por esta task, com `ADR-020` na mensagem.
    - Na saída `remover`, adicionalmente:
      - `git ls-files server/app/b3_historical.py server/tests/test_b3_historical.py` não imprime nada (deixaram de ser rastreados).
      - `git log --diff-filter=D --oneline -1 -- server/app/b3_historical.py` imprime o commit desta task.
      - `test ! -f server/app/b3_historical.py && test ! -f server/tests/test_b3_historical.py && echo OK` imprime `OK`.
      - A mensagem do commit cita `b3fdf02`: `git log -1 --format=%B | grep -c b3fdf02` retorna >= 1.
    - Na saída `congelar`, adicionalmente:
      - `grep -c 'ADR-020' server/tests/test_b3_historical.py` retorna >= 1 (o guardião ganhou a nota exigida pelo guardrail do repositório).
      - `git ls-files server/app/b3_historical.py` continua imprimindo o caminho (segue rastreado).
    - `b3fdf02` continua no histórico: `git cat-file -e b3fdf02^{commit} && echo OK` imprime `OK` (só a saída `desfazer-o-commit`, se autorizada explicitamente pelo Alex, pode violar isto).
  </acceptance_criteria>

  <done>
    Nenhum caminho de código no servidor baixa COTAHIST da B3 por conta própria;
    a fiação em `agent.py`, `main.py`, `db.py` e `rbac.py` foi desfeita sem
    encostar nos vizinhos; o que saiu do disco saiu por commit e a árvore está
    limpa; o ADR-019 ficou carimbado, não reescrito; e a suíte canônica está
    verde.
  </done>
</task>

<task type="auto">
  <name>Task 3: ADR-020 — o que foi superado, e o que deliberadamente não foi</name>

  <files>docs/adr/020-centralizacao-de-dados-no-mydata.md</files>

  <read_first>
    - docs/adr/019-cotahist-diario-b3.md — formato e tom das ADRs deste repo
    - docs/adr/008-fonte-de-cotacoes-selecionavel.md — o escopo de brapi que esta fase reduz
    - docs/adr/001-fonte-de-dados-intraday.md — o que NÃO é tocado
    - docs/adr/004-fonte-de-opcoes-na-v2.md — o contrato que NÃO é reaberto
    - .planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-CONTEXT.md (seção `<canonical_refs>`)
    - Os SUMMARYs de 09-01 a 09-04 — decisões e números REAIS para citar (nada de número inventado nesta ADR)
    - docs/MEDICAO-Mydata-*.md — a medição do Plano 09-04
  </read_first>

  <action>
    Escrever `docs/adr/020-centralizacao-de-dados-no-mydata.md` no formato das
    ADRs existentes do repo. Seções obrigatórias:

    **Contexto** — o achado literal: em 25/08 o Boris reimplementou em paralelo a
    ingestão do COTAHIST que o hub `mydata.acamerini.app` (`~/dev/cvm-financas`)
    já rodava em produção; duas capturas do mesmo arquivo oficial, com
    proveniências separadas. Citar a auditoria do lado cvm-financas (26/08) e a
    nota `.planning/notes/boris-pp-centralizacao-dados-mydata.md`.

    **Decisão** — as quatro decisões travadas do 09-CONTEXT.md, cada uma com o ID
    original para rastro:
      - D-01: `MydataProvider` atrás da interface `CandleProvider`, reusando o
        cache L2 e todos os call sites atuais.
      - D-02: `options_provider_mydata` atrás do mesmo contrato `providerStatus`.
      - D-03: candles degradam mydata→brapi→Yahoo, priorizando disponibilidade
        sobre pureza de proveniência única; `candle_cache` grava `src` por registro.
      - D-04: opções NÃO caem no Yahoo — falha vai direto a
        `providerStatus="degraded"`, porque o Yahoo é exatamente a fonte instável
        (401/403/429) que a migração elimina.
    Mais as decisões que o planejamento resolveu e que precisam de registro:
      - refresh diário é PASS-THROUGH pelo `candle_cache` existente, não job em
        lote — nenhum hook novo no scheduler;
      - IV e gregas vêm pré-calculadas do hub; nenhum recálculo local redundante;
      - `openInterest` não tem fonte no COTAHIST — registrar o efeito MEDIDO no
        `liquidity_score` (número que veio do 09-03-SUMMARY, não estimativa);
      - o provedor de opções passa a ser selecionável por env
        (`B3_OPTIONS_PROVIDER`) como ALAVANCA DE ROLLBACK, não como fallback
        automático;
      - o fallback de candles deixou de ser um salto só e virou cadeia
        (`B3_CANDLE_FALLBACK` aceita lista separada por vírgula).

    **Status das ADRs anteriores** — uma tabela, sem ambiguidade:

    | ADR | O que era | Status após esta |
    |---|---|---|
    | 001 — intraday Yahoo 15min | Yahoo dono do intraday | **INTOCADA.** O COTAHIST só publica após o fechamento do pregão; a fatia intraday não tem substituto no mydata |
    | 004 — fonte de opções na v2 | contrato `providerStatus` | **NÃO REABERTA.** Só a implementação por trás mudou |
    | 008 — brapi master de diário/spot | brapi master das duas fatias | **ESCOPO REDUZIDO** a spot ao vivo. O diário passa ao mydata; a brapi vira o primeiro elo de fallback |
    | 019 — COTAHIST diário próprio | ingestão própria do ZIP da B3 | **SUPERADA na fatia diária.** Registrar aqui a decisão da Task 1 sobre o destino do código, com o hash do commit que a implementou (`b3fdf02`) e o do commit que a executou |

    **Consequências** — o que se ganha (uma proveniência só, `sha256`/`arquivo`
    até o arquivo oficial em toda linha, IV e gregas prontas, uma superfície de
    ingestão a menos) e o que se paga (dependência de um serviço externo com cota
    de 60/min · 2.000/dia; perda dos campos crus do COTAHIST que o
    `gold_cotacoes` não expõe; `openInterest` ausente nas opções).

    **Medição** — os números do Plano 09-04, com link para
    `docs/MEDICAO-Mydata-<data>.md`: pico chamadas/min, total chamadas/dia,
    veredito e `intervaloMinimoSeguro`. Uma ADR deste repo não afirma
    suficiência de cota sem número — é a mesma postura de `ADR-008` e de
    `docs/MEDICAO-Brapi-2026-08-11.md`.

    **Reversibilidade** — como voltar: `B3_CANDLE_PROVIDER=brapi` e
    `B3_OPTIONS_PROVIDER=yahoo` restauram o comportamento anterior sem deploy de
    código. Se a saída da Task 1 tiver sido `remover`, registrar também o caminho
    de recuperação do código: `git show b3fdf02` traz de volta as 754 linhas da
    ingestão própria. Deixar isso explícito é o que torna a migração barata de
    desfazer.
  </action>

  <verify>
    <automated>test -f docs/adr/020-centralizacao-de-dados-no-mydata.md &amp;&amp; grep -q 'ADR-019' docs/adr/020-centralizacao-de-dados-no-mydata.md &amp;&amp; grep -q 'B3_CANDLE_PROVIDER' docs/adr/020-centralizacao-de-dados-no-mydata.md &amp;&amp; echo OK</automated>
  </verify>

  <acceptance_criteria>
    - `docs/adr/020-centralizacao-de-dados-no-mydata.md` existe.
    - Contém as 6 seções: `grep -ciE '^#+ *(Contexto|Decisão|Status das ADRs|Consequências|Medição|Reversibilidade)' docs/adr/020-centralizacao-de-dados-no-mydata.md` retorna 6.
    - As quatro decisões travadas estão rastreadas por ID: `grep -cE 'D-0[1234]' docs/adr/020-centralizacao-de-dados-no-mydata.md` retorna >= 4.
    - As quatro ADRs anteriores aparecem na tabela de status: `for a in 001 004 008 019; do grep -q "ADR-$a\|| $a " docs/adr/020-centralizacao-de-dados-no-mydata.md || echo "FALTA ADR-$a"; done` não imprime nada.
    - O destino do código do ADR-019 está registrado com hash: `grep -c 'b3fdf02' docs/adr/020-centralizacao-de-dados-no-mydata.md` retorna >= 1.
    - A seção Medição cita números concretos e o link do documento: `grep -cE 'MEDICAO-Mydata' docs/adr/020-centralizacao-de-dados-no-mydata.md` retorna >= 1 e não há `TBD`/`XXX` no arquivo.
    - A reversibilidade é literal: `grep -c 'B3_OPTIONS_PROVIDER=yahoo' docs/adr/020-centralizacao-de-dados-no-mydata.md` retorna >= 1.
    - Nenhuma chave completa no arquivo: `grep -cE '[a-f0-9]{32,}' docs/adr/020-centralizacao-de-dados-no-mydata.md` retorna 0.
  </acceptance_criteria>

  <done>
    Existe uma ADR que diz o que foi superado (019 na fatia diária, 008 no
    escopo), o que NÃO foi (001 e 004), com que números a decisão foi tomada,
    onde o código aposentado ficou recuperável, e como desfazer por env sem
    deploy.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor → histórico local não pushado | b3fdf02 (+754 linhas) é um commit deliberado do Alex, ainda fora do remoto: um `reset`/`rebase`/`revert` errado destrói registro que ninguém mais tem |
| Executor → SQLite (produção e desenvolvimento) | Remoção de `CREATE TABLE` em `init_db` beira a fronteira de dado persistido; o banco local já tem 16,8 mil linhas nessas tabelas |
| `docs/adr/` → registro histórico do projeto | Apagar texto de ADR destrói o raciocínio da época — e um `git revert b3fdf02` faria exatamente isso sem avisar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-21 | Tampering (destruição de trabalho) | commit local b3fdf02, não pushado | mitigate | Task 1 é `checkpoint:decision` BLOQUEANTE e obriga a reconfirmar `git status`/`git log origin/main..HEAD` antes de apresentar as opções; nenhuma remoção acontece antes da resposta do Alex; divergência do estado esperado obriga a recalibrar. A saída `desfazer-o-commit` (reescrita de histórico) NÃO é executável de forma autônoma e exige confirmação humana própria; `git push --force` é proibido |
| T-09-22 | Tampering (dado persistido) | remoção dos `CREATE TABLE` em `db.init_db` | mitigate | A ação PROÍBE `DROP TABLE` e qualquer mexida em banco. Remover o `CREATE` de um `init_db` idempotente não apaga dado: em produção não há tabela (b3fdf02 nunca foi pushado, logo nunca deployado); no banco local as tabelas ficam órfãs e inertes, e limpá-las é ação destrutiva separada que obriga PARAR e reportar |
| T-09-23 | Repudiation | apagar o registro da decisão de 25/08 | mitigate | ADR-019 é preservado em todas as saídas, apenas carimbado com uma linha de status; `git revert b3fdf02` é explicitamente PROIBIDO por apagar o arquivo junto; critério de aceite usa `git diff b3fdf02 -- docs/adr/019-*.md` para provar que só houve linha adicionada |
| T-09-24 | Denial of Service | remover o hook errado do `scheduler_loop` e derrubar radar/ordens | mitigate | A ação nomeia o bloco exato pelo vizinho anterior (`[obs-metricas]`) e o critério de aceite verifica que os hooks vizinhos (analytics, `maybe_warm`, `signal_ledger`) continuam presentes; `pytest tests/` completo roda depois |
| T-09-25 | Elevation of Privilege | reverter `rbac.py` de forma a alargar permissão | mitigate | A reversão REMOVE uma entidade do conjunto de `fontes_dados.configurar` — reduz superfície, não amplia. Critério de aceite grepa por `b3_daily_import` em `rbac.py` |
| T-09-26 | Repudiation (estado ambíguo) | `rm` solto em arquivo rastreado deixando árvore suja | mitigate | Critérios de aceite medem ESTADO DO GIT, não do disco: `git status --short` dos 7 arquivos precisa sair vazio, `git ls-files` precisa confirmar o desrastreamento e `git log --diff-filter=D` precisa apontar o commit que removeu |
| T-09-SC | Tampering | instalação de pacote | mitigate | Nenhum pacote novo. Se o executor concluir que precisa de dependência nova, PARE e abra checkpoint humano bloqueante |
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` verde (as DUAS suítes).
- `cd server && python -c "from app import main"` sem erro.
- `git status --short` mostra a árvore limpa para os 7 arquivos do b3fdf02 e nenhum arquivo de outra sessão destruído por acidente.
- `git log --oneline -2` mostra o commit desta task em cima de b3fdf02, e `git cat-file -e b3fdf02^{commit}` continua válido.
- `grep -rn 'b3_historical' server/` só retorna linhas dentro do próprio módulo (saída `congelar`) ou nada (saída `remover`).
- **Regra de processo desta fase (lição do incidente da Fase 8, ver STATE.md):** esta fase tem checkpoint humano bloqueante. Nenhum `git push` de NENHUMA wave sai antes da aprovação do checkpoint do Plano 09-06, mesmo que o commit de uma wave pareça inócuo isolado. Isso é o que garante que b3fdf02 e o commit de remoção viajem juntos e que nenhum deploy intermediário crie as tabelas órfãs no Railway.
</verification>

<success_criteria>
- A decisão sobre o destino do código do ADR-019 foi do Alex, com o estado real
  do git (commit b3fdf02, não pushado) e do banco local na mesa.
- Nenhum caminho no servidor baixa COTAHIST da B3 por conta própria.
- A saída virou commit: árvore limpa, `git log` registra a remoção, b3fdf02
  segue recuperável.
- ADR-019 carimbado, não reescrito; ADR-020 escrita com números reais.
- Suíte canônica verde.
</success_criteria>

<output>
Create `.planning/phases/09-centraliza-o-de-dados-de-mercado-mydata-client-py-implementa/09-05-SUMMARY.md` when done.
Registre no SUMMARY: (a) a saída escolhida pelo Alex e a justificativa dele, se
houver; (b) o `git status --short` / `git log --oneline origin/main..HEAD`
encontrados na Task 1 comparados ao esperado, incluindo a contagem das tabelas no
banco local; (c) o hash do commit criado pela Task 2 e confirmação de que
b3fdf02 segue no histórico; (d) confirmação de que o ADR-019 teve só a linha de
status acrescentada.
</output>
