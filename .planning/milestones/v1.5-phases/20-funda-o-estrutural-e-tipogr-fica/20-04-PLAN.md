---
phase: 20-funda-o-estrutural-e-tipogr-fica
plan: 04
type: execute
wave: 4
depends_on: ["20-03"]
files_modified:
  - web/src/version.js
  - server/app/main.py
  - server/web_dist
autonomous: true
requirements: [FIX-01, FIX-02, SYS-04, TYPO-01, TYPO-02, TYPO-03, MOTION-03]

must_haves:
  truths:
    - "A fundação estrutural e tipográfica está publicada — não fica testada no repo e ausente do bundle que o servidor serve"
    - "O carimbo de entrega é novo e coerente nos três elos: web/src/version.js, server/app/main.py e o bundle em server/web_dist"
    - "Os cinco critérios de sucesso da fase valem no BUNDLE DE PRODUÇÃO (minificado), não só no servidor de desenvolvimento"
    - "Nada foi enviado a origin: a decisão sobre publicar junto as Fases 17/18/19 (checkpoints humanos pendentes) continua sendo do Alex"
  artifacts:
    - path: "web/src/version.js"
      provides: "BUILD_ID novo desta entrega"
      contains: "BUILD_ID"
    - path: "server/web_dist"
      provides: "bundle publicado com a fundação da Fase 20"
  key_links:
    - from: "web/src/version.js"
      to: "server/web_dist"
      via: "scripts/bump.sh seguido de scripts/publicar-web.sh"
      pattern: "BUILD_ID"
    - from: "web/src/version.js"
      to: "server/app/main.py:SERVER_BUILD_ID"
      via: "sincronização automática dentro de publicar-web.sh"
      pattern: "SERVER_BUILD_ID"
---

<objective>
Publicar a Fase 20 e provar que os cinco critérios de sucesso valem no bundle
de produção, não só no Vite de desenvolvimento.

Purpose: este repositório já pagou por isso — "Fase sem plano de publicação
front": plano que toca `web/src/` sem bump + publicação fica mergeado, testado
e nunca vai ao ar. Além disso, tudo o que esta fase entrega é CSS: minificação,
ordem de regras no bundle e serialização de style inline pelo build de produção
são exatamente as coisas que podem divergir do dev server. Especificamente, o
seletor de atributo `[style*="ui-monospace"]` do TYPO-01 depende de como o
build final serializa o atributo `style` — verificar isso só em `:5173` seria
verificar a coisa errada.

Output: carimbo novo nos três elos, `server/web_dist` republicado, suíte
canônica verde DEPOIS da publicação, e os cinco critérios remedidos contra o
bundle de produção.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-funda-o-estrutural-e-tipogr-fica/20-CONTEXT.md
@.planning/phases/20-funda-o-estrutural-e-tipogr-fica/20-03-SUMMARY.md
@CLAUDE.md

<interfaces>
`scripts/bump.sh` — avança o `BUILD_ID` em `web/src/version.js`. Sem argumento,
gera o próximo do dia (`F10-AAAAMMDD-NN`). É comando MANUAL de propósito: o
carimbo marca ENTREGA, não build.

`scripts/publicar-web.sh` — builda `web/` e publica em `server/web_dist`, a
ÚNICA árvore que o Railway enxerga (`rootDirectory=/server`). Ele TAMBÉM
sincroniza `SERVER_BUILD_ID` em `server/app/main.py` a partir do carimbo local
— por isso, com front sendo publicado, NÃO se edita `SERVER_BUILD_ID` à mão.
Nunca editar `server/web_dist` à mão. Ordem obrigatória: `bump.sh` ANTES de
`publicar-web.sh`.

`bash scripts/executar.sh --testes` — suíte canônica (pytest + `web/tests/*.mjs`).
Roda DEPOIS da publicação: há guardiões de carimbo/bundle que só têm o que
medir com o `web_dist` novo no lugar.

`bash scripts/executar.sh --prod` — sobe o modo PROD local: build do web +
servidor único em `:8787`, servindo o bundle real em vez do Vite. É onde os
cinco critérios são remedidos.
`bash scripts/executar.sh --stop` — encerra e libera as portas.

Carimbo vigente antes desta fase: `F10-20260905-01` (em `web/src/version.js` e
em `server/app/main.py:1013`).
</interfaces>

<risco_herdado>
**A publicação é cumulativa e carrega TRÊS fases sem checkpoint humano aprovado.**
`.planning/STATE.md` registra:
- **Fase 17** (`17-06-PLAN.md`, Task 2): checkpoint ADIADO, não aprovado —
  mercado fechado na tentativa, roteiro de 10 passos nunca exercitado.
- **Fase 18** (`18-05-PLAN.md`, Task 2): checkpoint PENDENTE, exercitado só
  parcialmente no iPhone em 2026-09-03.
- **Fase 19** (`19-04-PLAN.md`, Task 2): checkpoint PENDENTE, roteiro não
  rodado — depende de mercado aberto e de uma posição com dois candidatos.

Esses três fluxos JÁ estão dentro de `server/web_dist` (a Fase 19 publicou em
2026-09-04). A Fase 20 não os introduz nem os agrava: ela é CSS de shell, não
toca o motor de opções, o fluxo de aceite nem contrato de API. O que segue em
aberto é a decisão a/b/c do Alex sobre **enviar tudo isso a `origin`** — e essa
decisão continua sendo dele.

**Este plano NÃO dá `git push`.** Publicar em `server/web_dist` é operação
local no repositório; o deploy só acontece quando o `origin` recebe. O SUMMARY
deve reafirmar que a decisão a/b/c segue aberta e que nada foi empurrado.
</risco_herdado>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump do carimbo e publicação do front</name>
  <read_first>
    - scripts/bump.sh (cabeçalho — por que o bump é manual e o que quebra sem ele)
    - scripts/publicar-web.sh (a sincronização automática de `SERVER_BUILD_ID` e o destino do build)
    - web/src/version.js (carimbo atual: `F10-20260905-01`)
    - server/app/main.py:1010-1021 (`SERVER_BUILD_ID` e `/api/health`)
    - .planning/phases/20-funda-o-estrutural-e-tipogr-fica/20-03-SUMMARY.md (estado da suíte antes da publicação)
  </read_first>
  <files>web/src/version.js, server/app/main.py, server/web_dist</files>
  <action>
Nesta ordem exata, sem pular passo:
1. `bash scripts/bump.sh` — sem argumento (próximo carimbo do dia; o vigente é
   `F10-20260905-01`, então o esperado é `F10-20260905-02`).
2. `bash scripts/publicar-web.sh` — builda `web/` e publica em
   `server/web_dist`; ele sincroniza `SERVER_BUILD_ID` sozinho a partir do
   carimbo local.
3. `bash scripts/executar.sh --testes` — DEPOIS da publicação, para que os
   guardiões de carimbo/bundle meçam o `web_dist` novo.
4. Conferir os TRÊS elos do carimbo: `web/src/version.js`,
   `server/app/main.py:SERVER_BUILD_ID` e o bundle dentro de `server/web_dist`
   carregam o MESMO `BUILD_ID`.
5. Conferir que o CSS da fase chegou ao bundle minificado: procurar em
   `server/web_dist` pelas assinaturas das quatro regras novas —
   `overflow-x:hidden` na regra do shell, `font-variant-numeric` com
   `tabular-nums`, `prefers-reduced-motion` (duas ocorrências) e o valor
   `720px`. Como o CSS desta fase vive dentro de um template string JS
   (`GlobalStyle()`), ele sai no bundle JS, não num `.css` separado —
   procurar nos dois.

Não editar `server/web_dist` à mão. Não editar `SERVER_BUILD_ID` à mão (o
script sincroniza; edição manual só existe em deploy só-backend, que não é o
caso). **Não dar `git push`** — ver `<risco_herdado>`.

Registrar no SUMMARY o carimbo gerado e a contagem de testes das duas suítes.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `git diff web/src/version.js` mostra um `BUILD_ID` novo no formato `F10-AAAAMMDD-NN`, diferente de `F10-20260905-01`.
    - `git status --porcelain server/web_dist` mostra arquivos modificados (a publicação de fato aconteceu).
    - Os três elos batem: `grep -o 'F10-[0-9]*-[0-9]*' web/src/version.js | head -1` é igual ao valor de `sed -n 's/.*SERVER_BUILD_ID = "\([^"]*\)".*/\1/p' server/app/main.py`, e `grep -rl "$(grep -o 'F10-[0-9]*-[0-9]*' web/src/version.js | head -1)" server/web_dist | head -1` retorna ao menos um arquivo.
    - `grep -rl 'tabular-nums' server/web_dist | head -1` retorna ao menos um arquivo, e o mesmo para `prefers-reduced-motion` e `overflow-x:hidden`.
    - `bash scripts/executar.sh --testes` sai com código 0 DEPOIS da publicação (as duas suítes).
    - `git diff --stat web/package.json web/package-lock.json server/requirements.txt server/requirements-prod.txt` sai vazio.
    - Nenhum `git push` foi executado (`git status -sb` mostra commits à frente de `origin`).
  </acceptance_criteria>
  <done>Carimbo novo nos três elos, `server/web_dist` republicado com o CSS da fase dentro, suíte canônica verde após a publicação, nada empurrado para `origin`.</done>
</task>

<task type="auto">
  <name>Task 2: Remedir os cinco critérios contra o bundle de produção</name>
  <read_first>
    - .planning/ROADMAP.md (seção da Phase 20 — os cinco critérios de sucesso, texto literal)
    - .planning/phases/20-funda-o-estrutural-e-tipogr-fica/20-01-SUMMARY.md, 20-02-SUMMARY.md, 20-03-SUMMARY.md (os números medidos no dev server, para comparar)
    - scripts/executar.sh:47-56 (modo `--prod`)
  </read_first>
  <files>nenhum arquivo editado nesta task (verificação); o resultado vai para o SUMMARY</files>
  <action>
Subir `bash scripts/executar.sh --prod` (build de produção servido em `:8787`),
abrir `http://localhost:8787` pela MCP do Chrome, logar com a conta local e
remedir os CINCO critérios do ROADMAP contra o bundle minificado. Repetir as
mesmas medições dos planos anteriores, agora em produção:

1. **375 × 812** — `.b3-shell` e `<main>` com `scrollWidth === clientWidth`;
   nenhum elemento com `right > 375` além dos trilhos intencionais. Tocar um
   item de trilho horizontal e focar um campo de texto, confirmando que header,
   conteúdo e barra inferior NÃO se deslocam juntos (o sintoma original do
   FIX-01).
2. **375 × 812** — badge de status de mercado em Modo Estudo trunca com
   reticência visível; nada do Topbar sai do viewport.
3. **1280 × 900** — wrapper de conteúdo com 720px, centrado, alinhado com o
   `BottomNav`.
4. **Dígitos** — `getComputedStyle(...).fontVariantNumeric === "tabular-nums"`
   num valor financeiro real. Este é o critério com maior risco de divergir
   entre dev e prod, porque depende de o build final serializar a substring
   `ui-monospace` no atributo `style`. Se em produção der `normal`, PARAR: a
   decisão mecânica da opção (a) não sobrevive ao build e precisa ser reaberta
   com o orquestrador, não contornada aqui.
5. **Movimento reduzido** — com a preferência emulada: transição da raiz
   zerada, troca de tema instantânea, `.spin` e `.tt-track` com
   `animationName === "none"` e ticker parado em dois screenshots com intervalo.
   E, sem a emulação, o ticker voltando a andar.
6. **Fredoka** — `getComputedStyle(h1).fontFamily` começando por `Fredoka` em
   pelo menos 3 telas, com `document.fonts.check('600 22px Fredoka')`
   verdadeiro (a fonte precisa estar sendo servida pelo bundle de produção, não
   só pelo dev server).

Encerrar com `bash scripts/executar.sh --stop`.

No SUMMARY, montar uma tabela dos cinco critérios do ROADMAP × resultado
medido em produção. Para o critério 4 do ROADMAP, registrar explicitamente a
distinção já documentada em `20-UI-SPEC.md`: a metade do alinhamento de dígitos
(TYPO-01) está fechada agora; a metade "todo valor financeiro usa um dos três
tamanhos nomeados" (TYPO-02) é deferimento declarado do `20-CONTEXT.md` para as
Fases 21/22, que migram tela a tela. Não marcar esse item como pronto sem a
ressalva, e não marcar como falha — é escopo transferido, registrado.

Reafirmar no SUMMARY: nada foi enviado a `origin`; a decisão a/b/c sobre as
Fases 17/18/19 segue aberta com o Alex.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - O SUMMARY contém uma tabela dos 5 critérios de sucesso do ROADMAP com o resultado medido em `:8787` (produção), cada linha com o número medido, não com adjetivo.
    - Critério 1: `.b3-shell` e `main` com `scrollWidth === clientWidth` em 375px, e o registro de que focar um campo/tocar um trilho não desloca o app.
    - Critério 2: `scrollWidth > clientWidth` no span do badge com `textOverflow: ellipsis` computado, e reticência visível no screenshot.
    - Critério 3: largura medida do wrapper em 1280px = 720px, alinhado ao `BottomNav`.
    - Critério 4: `fontVariantNumeric === "tabular-nums"` num valor financeiro real do bundle de produção; e a ressalva escrita sobre a metade TYPO-02 deferida para as Fases 21/22.
    - Critério 5: `animationName === "none"` em `.spin` e `.tt-track` sob movimento reduzido + ticker parado nos dois screenshots + `fontFamily` começando por `Fredoka` em ≥3 telas com `document.fonts.check` verdadeiro.
    - `bash scripts/executar.sh --testes` sai com código 0.
    - O SUMMARY afirma explicitamente que nenhum `git push` foi feito e que a decisão a/b/c segue aberta.
  </acceptance_criteria>
  <done>Os cinco critérios do ROADMAP estão medidos contra o bundle de produção com números registrados, a divergência conhecida do critério 4 está documentada em vez de escondida, e o repositório segue sem push para `origin`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo local → `origin`/Railway | A publicação em `server/web_dist` é local; o deploy real acontece no push. Este plano para antes desse limite de propósito. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-10 | Repudiation | Fase testada mas invisível em produção (falta de bump/publicação) | mitigate | Task 1 obriga `bump.sh` → `publicar-web.sh` → suíte, com os três elos do carimbo como critério de aceite |
| T-20-11 | Tampering | Divergência dev × prod no seletor de atributo do TYPO-01 (minificação/serialização do `style`) | mitigate | Task 2 remede `fontVariantNumeric` contra `:8787` e manda PARAR se divergir, em vez de contornar |
| T-20-12 | Elevation of privilege | Empurrar a `origin` as Fases 17/18/19 sem checkpoint aprovado, embutindo decisão do Alex numa task de publicação | mitigate | Proibição explícita de `git push` neste plano; critério de aceite verifica commits à frente de `origin` sem envio |
| T-20-13 | Information disclosure | Conta local de teste e SQLite local entrarem no commit | mitigate | Critério de aceite limita o diff a `web/src/version.js`, `server/app/main.py` e `server/web_dist` |
| T-20-SC | Tampering | instalações npm/pip/cargo | mitigate | Zero pacote novo na fase inteira; `git diff` dos quatro manifests vazio como critério de aceite |
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` verde DEPOIS da publicação.
- Três elos do carimbo coerentes.
- CSS da fase presente dentro de `server/web_dist`.
- Cinco critérios do ROADMAP medidos em `:8787` (bundle de produção).
- Nenhum push para `origin`.
</verification>

<success_criteria>
A fundação da Fase 20 está publicada, provada no bundle de produção e travada
por guardião na suíte canônica; a decisão de enviar a `origin` (que arrasta as
Fases 17/18/19) fica registrada como aberta, sem ser tomada por um agente.
</success_criteria>

<output>
Create `.planning/phases/20-funda-o-estrutural-e-tipogr-fica/20-04-SUMMARY.md` when done
</output>
