---
phase: 20-funda-o-estrutural-e-tipogr-fica
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - web/src/App.jsx
  - web/tests/test_fase20_fundacao_visual.mjs
autonomous: true
requirements: [TYPO-03, MOTION-03]

must_haves:
  truths:
    - "O H1 de cada tela aparece na fonte da marca (Fredoka), como o wordmark do Topbar já faz — nas 15 ocorrências vivas, incluindo os dois branches do EficienciaIAScreen"
    - "Com 'reduzir movimento' ligado no sistema, nenhuma transição do app roda — incluindo a transição de tema/modo, que vive no elemento raiz e não nos descendentes"
    - "Com 'reduzir movimento' ligado, o marquee do ticker e o spinner continuam PARADOS (animation:none), não em flicker acelerado"
    - "Nenhum tamanho, peso, margem ou letterSpacing de H1 mudou — a única diferença é a família tipográfica"
  artifacts:
    - path: "web/src/App.jsx"
      provides: "fontFamily: DISPLAY nos 15 H1 de tela + bloco @media prefers-reduced-motion abrangente em GlobalStyle()"
      contains: "prefers-reduced-motion"
    - path: "web/tests/test_fase20_fundacao_visual.mjs"
      provides: "asserções de TYPO-03 (15 H1) e MOTION-03 (regra + ordem de fonte) somadas ao guardião"
  key_links:
    - from: "const DISPLAY"
      to: "os 15 elementos <h1> de nível-tela"
      via: "chave fontFamily acrescentada ao objeto de style inline já existente"
      pattern: "fontFamily: DISPLAY"
    - from: "bloco @media (prefers-reduced-motion: reduce) abrangente"
      to: "regras .b3{transition} e .b3-mode-switch{transition !important}"
      via: "ordem de fonte no template string do GlobalStyle (empate de especificidade resolve pelo que vem depois)"
      pattern: "transition-duration"
---

<objective>
Fechar a identidade tipográfica dos títulos (TYPO-03: Fredoka em todo H1 de
tela) e o respeito à preferência de movimento reduzido do sistema (MOTION-03).

Purpose: são as duas últimas peças da fundação e as duas que as Fases 22/23
consomem — a 23 depende explicitamente do gate de `prefers-reduced-motion`
existir antes de introduzir motion novo. Fazer isso agora significa que toda
animação futura já nasce coberta, sem código extra.

Output: `web/src/App.jsx` com os 15 H1 na fonte da marca e o bloco de
`prefers-reduced-motion` abrangente em `GlobalStyle()`; guardião estendido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-funda-o-estrutural-e-tipogr-fica/20-CONTEXT.md
@.planning/phases/20-funda-o-estrutural-e-tipogr-fica/20-UI-SPEC.md
@.planning/phases/20-funda-o-estrutural-e-tipogr-fica/20-PATTERNS.md
@.planning/phases/20-funda-o-estrutural-e-tipogr-fica/20-02-SUMMARY.md
@CLAUDE.md

<interfaces>
`web/src/App.jsx:243` — `const DISPLAY = "'Fredoka', " + SANS;` já degrada para
a pilha Nunito/sistema se a Fredoka não carregar (WKWebView offline). Nenhum
fallback novo é necessário.

`web/src/App.jsx:805` — prova do idioma exato a copiar (wordmark do Topbar, que
já usa a constante): `fontFamily: DISPLAY` é apenas mais uma chave dentro do
objeto de style inline. Sem wrapper, sem elemento novo, sem classe.

Regras de `GlobalStyle()` que MOTION-03 precisa cobrir (estado real verificado):
- `web/src/App.jsx:291` — `.b3{ transition:background .25s ease, color .25s ease; }`
  Isto está no ELEMENTO raiz, que carrega as classes `b3 b3-shell b3-theme-*`
  (`App.jsx:8919`). Um seletor `.b3 *` NÃO casa com ele.
- `web/src/App.jsx:312` — `.b3-mode-switch, .b3-mode-switch *{ transition: background-color .35s ease, color .35s ease, border-color .35s ease, fill .35s ease !important; }`
  A classe `b3-mode-switch` é adicionada ao `document.documentElement` (o `html`)
  por ~450ms durante a troca de modo (`App.jsx:7667-7668`), então
  `.b3-mode-switch *` casa com TODO descendente de `html`, inclusive o `.b3`.
- `web/src/App.jsx:317` — `.b3 .spin{ animation:b3spin .8s linear infinite; }`
- `web/src/App.jsx:322` — `.b3 .tt-track{ animation:b3tt 52s linear infinite; }`
- `web/src/App.jsx:323` — `@media (prefers-reduced-motion: reduce){ .b3 .tt-track,.b3 .spin{ animation:none !important; } }`
  Regra estreita EXISTENTE, especificidade (0,2,0). Mantida.

`bash scripts/executar.sh` / `--stop` / `--testes`; `cd web && npx vite build`.

Inventário dos 15 `<h1>` vivos (verificado por leitura direta em 2026-09-05;
os números de linha são do arquivo ANTES das edições dos planos 20-01/20-02 e
já vão estar deslocados — localizar por função + texto, nunca pelo número):

| # | Função | Texto/prop | Style atual |
|---|---|---|---|
| 1 | `EvolucaoScreen` | `{cp.saudacao(name \|\| null)}` | `margin:0, fontSize:"23px", fontWeight:700, letterSpacing:"-0.01em"` |
| 2 | `AjudaScreen` | `Como funciona` | `margin:0, fontSize:"22px", fontWeight:700` |
| 3 | `MercadoScreen` | `{cp.tituloWatchlist}` | `margin:0, fontSize:"24px", fontWeight:700, letterSpacing:"-0.01em"` |
| 4 | `CarteiraScreen` | `{cp.tituloPortfolio}` | `margin:0, fontSize:"22px", fontWeight:700` |
| 5 | `HistoricoScreen` | `Histórico de operações` | `margin:"0 0 16px", fontSize:"22px", fontWeight:700` |
| 6 | `AgenteScreen` | `Operador IA` | `margin:0, fontSize:"22px", fontWeight:700` |
| 7 | `AiConfigScreen` | `IA & Boris` | `margin:"0 0 6px", fontSize:"22px", fontWeight:700` |
| 8 | `NotificacoesScreen` | `Notificações` | `margin:"0 0 18px", fontSize:"22px", fontWeight:700` |
| 9 | `AtividadeIAScreen` | `Atividade da IA` | `margin:0, fontSize:"22px", fontWeight:700` |
| 10 | `EficienciaIAScreen` (branch deslogado) | `Eficiência da IA` | `margin:0, fontSize:"22px", fontWeight:700` |
| 11 | `EficienciaIAScreen` (branch logado) | `Eficiência da IA` | `margin:0, fontSize:"22px", fontWeight:700` |
| 12 | `LogsDebugScreen` | `Diagnóstico` | `margin:"0 0 6px", fontSize:"22px", fontWeight:700` |
| 13 | `FonteDadosScreen` | `Fonte de dados` | `margin:"0 0 6px", fontSize:"22px", fontWeight:700` |
| 14 | `RadarScreen` | `{cp.tituloRadar}` | `margin:0, fontSize:"24px", fontWeight:700, letterSpacing:"-0.01em"` |
| 15 | `ConfigScreen` | `Preferências` | `margin:"0 0 18px", fontSize:"22px", fontWeight:700` |

Os itens 10 e 11 são DOIS renders condicionais vivos da mesma tela, não código
morto — os dois precisam receber a fonte, senão a tela muda de fonte ao logar.
Fora do inventário e NÃO tocados: o título do `BackHeader` (é um `<div>`, não
um `<h1>`) e os wordmarks do `Topbar`/`WelcomeAuthScreen` (já usam `DISPLAY`).
</interfaces>

<decisao_do_planner>
**Correção mecânica no seletor do MOTION-03 — intenção do `20-CONTEXT.md`
honrada, texto literal não.** O `20-CONTEXT.md` especifica
`.b3 *, .b3 *::before, .b3 *::after` e declara que o objetivo é "cobrir a
transição de tema/modo já existente (`.b3{transition:...}`, `App.jsx:291`)".
Esses dois pedidos são incompatíveis: `.b3 *` casa com DESCENDENTES de `.b3`,
nunca com o próprio elemento `.b3` — que é exatamente onde a transição de tema
mora. Aplicado ao pé da letra, o requisito falharia no caso que ele nomeia.

O plano segue a INTENÇÃO: a lista de seletores inclui também `.b3` e
`.b3-mode-switch`/`.b3-mode-switch *` (a segunda porque a classe vive no `html`
e a regra da linha 312 carrega `!important`, precisando ser vencida por empate
de especificidade + ordem de fonte). Isto amplia a cobertura, nunca reduz.

**A regra estreita da linha 323 é mantida, e não por ordem de fonte.**
`animation-duration: 0.01ms` NÃO para uma animação `infinite` — faz um ciclo
completo a cada 0,01ms, para sempre: strobe, o oposto do que a preferência de
movimento reduzido existe para evitar. Só `animation: none` para o marquee e o
spinner. A regra estreita (`.b3 .tt-track`, `.b3 .spin`, especificidade (0,2,0))
vence a regra ampla (`.b3 *`, (0,1,0)) por ESPECIFICIDADE, independente de
ordem — a análise de "empate resolvido por ordem" registrada em
`20-UI-SPEC.md`/`20-PATTERNS.md` está incorreta neste ponto. Ainda assim, o
posicionamento é prescrito e verificado, porque contra as regras de `transition`
das linhas 291 e 312 o empate de especificidade É real e a ordem decide.
</decisao_do_planner>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fredoka nos 15 H1 de tela</name>
  <read_first>
    - web/src/App.jsx — as 15 regiões listadas na tabela do `<interfaces>` acima, localizadas por nome de função + texto do título
    - web/src/App.jsx:805 (wordmark do Topbar — prova do idioma `fontFamily: DISPLAY` dentro do style inline)
    - .planning/phases/20-funda-o-estrutural-e-tipogr-fica/20-PATTERNS.md (seção 6 — os dois exemplos "antes/depois" das duas formas de style encontradas)
    - .planning/phases/20-funda-o-estrutural-e-tipogr-fica/20-02-SUMMARY.md
  </read_first>
  <files>web/src/App.jsx</files>
  <action>
Em cada um dos 15 `<h1>` da tabela do `<interfaces>`, acrescentar
`fontFamily: DISPLAY` como chave NOVA no objeto de style inline que já existe.

Regras rígidas:
- Não reordenar, não renomear e não remover nenhuma chave existente (`margin`,
  `fontSize`, `fontWeight`, `letterSpacing` onde houver). O tamanho e o peso de
  cada título permanecem exatamente como estão — a fase muda família, não
  escala.
- Não envolver o `<h1>` em wrapper novo, não criar classe CSS, não criar
  componente.
- Não tocar em `<h2>`, `<h3>`, texto de corpo, nem no título do `BackHeader`
  (que é um `<div>` com `fontSize:"18px"`, fora do escopo declarado).
- Os dois `<h1>` de `EficienciaIAScreen` (branch deslogado e branch logado) são
  ambos vivos: os DOIS recebem a mudança.
- Localizar cada ponto pelo nome da função da tela e pelo texto do título; os
  números de linha da tabela são anteriores às edições dos planos 20-01/20-02 e
  já estão deslocados.

Antes de terminar, contar: `grep -c "<h1"` no arquivo e conferir que o total de
`<h1>` com `fontFamily: DISPLAY` bate com o total de `<h1>` existentes. Se
aparecer um `<h1>` fora da tabela, PARAR e registrar — pode ser um H1 novo
vindo das Fases 17/18/19 (que editaram o mesmo arquivo e não foram publicadas
nem enviadas a `origin`), e o inventário precisa ser corrigido em vez de
ignorado.
  </action>
  <verify>
    <automated>cd web && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '<h1' web/src/App.jsx` e `grep '<h1' web/src/App.jsx | grep -c 'fontFamily: DISPLAY'` retornam o MESMO número, e esse número é ≥ 15.
    - `grep '<h1' web/src/App.jsx | grep -c 'fontSize: "22px"'` continua 11 e `grep '<h1' web/src/App.jsx | grep -c 'fontSize: "24px"'` continua 2 (nenhum tamanho mudou).
    - `grep '<h1' web/src/App.jsx | grep -c 'letterSpacing: "-0.01em"'` continua 3.
    - Os dois `<h1>` de `Eficiência da IA` estão ambos com `fontFamily: DISPLAY` (`grep -n 'Eficiência da IA' web/src/App.jsx` mostra as duas linhas, ambas com a chave).
    - `cd web && npx vite build` sai com código 0.
    - `git diff --stat web/src/App.jsx` mostra ~15 linhas alteradas e nenhum outro arquivo.
  </acceptance_criteria>
  <done>Os 15 H1 de tela carregam a fonte da marca, com tamanho, peso, margem e letterSpacing intocados, e o build passa.</done>
</task>

<task type="auto">
  <name>Task 2: Gate de prefers-reduced-motion abrangente em GlobalStyle()</name>
  <read_first>
    - web/src/App.jsx:281-330 (`GlobalStyle()` completo no estado pós-20-02 — todas as regras de transição/animação e a ordem em que aparecem)
    - web/src/App.jsx:7660-7672 (onde a classe `b3-mode-switch` entra e sai do `document.documentElement`)
    - web/src/App.jsx:8915-8925 (as classes que o elemento raiz carrega: `b3 b3-shell b3-theme-*`)
    - .planning/phases/20-funda-o-estrutural-e-tipogr-fica/20-PATTERNS.md (seção 5 — a regra estreita existente e a restrição de ordem)
  </read_first>
  <files>web/src/App.jsx</files>
  <action>
Acrescentar UM bloco `@media (prefers-reduced-motion: reduce)` novo ao template
string de `GlobalStyle()`, com comentário CSS acima explicando a decisão.

**Posição exata:** imediatamente ANTES da linha do bloco estreito já existente
(`@media (prefers-reduced-motion: reduce){ .b3 .tt-track,.b3 .spin{ animation:none !important; } }`),
que é a última linha do template. Isso garante os dois invariantes de uma vez:
está DEPOIS das regras de `transition` das linhas 291 e 312 (empate de
especificidade → o mais tarde vence → as transições de tema e de troca de modo
são efetivamente zeradas) e o bloco estreito continua por último e legível.

**Seletores do bloco novo** — a lista precisa incluir o elemento raiz, não só
os descendentes:
`.b3`, `.b3 *`, `.b3 *::before`, `.b3 *::after`, `.b3-mode-switch`,
`.b3-mode-switch *`.

**Propriedades do bloco novo:**
- `transition-duration: 0.01ms !important;`
- `animation-duration: 0.01ms !important;`
- `animation-iteration-count: 1 !important;`

**Comentário acima do bloco** (em português, no estilo dos comentários longos
que já existem em `GlobalStyle()` — carrega decisão, não repete o código),
registrando as três razões que não se leem do código:
1. por que `.b3` entra na lista além de `.b3 *`: a transição de tema mora no
   elemento raiz e `.b3 *` só casa com descendentes — sem `.b3` na lista, o
   requisito MOTION-03 falharia justamente no caso que ele nomeia;
2. por que `.b3-mode-switch`/`.b3-mode-switch *` entram: a classe vive no `html`
   por ~450ms na troca de modo e a regra dela carrega `!important`; só empate de
   especificidade resolvido por ordem de fonte a vence;
3. por que o bloco estreito `animation:none` das duas animações `infinite`
   (marquee do ticker e spinner) CONTINUA existindo e não foi substituído:
   `animation-duration: 0.01ms` não para animação infinita, faz um ciclo a cada
   0,01ms para sempre — strobe, o oposto do que a preferência pede. A regra
   estreita tem especificidade maior e vence por si, mas a explicação precisa
   ficar no arquivo para ninguém "limpar a duplicata" depois.

Não apagar, não mesclar e não reescrever o bloco estreito existente. Não mexer
nas regras das linhas 291, 312, 317 e 322.
  </action>
  <verify>
    <automated>cd web && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'prefers-reduced-motion' web/src/App.jsx` retorna 2 (o bloco existente + o novo).
    - `grep -c 'transition-duration: 0.01ms' web/src/App.jsx` retorna 1 e `grep -c 'animation-iteration-count: 1' web/src/App.jsx` retorna 1.
    - A lista de seletores do bloco novo contém `.b3,` (o elemento raiz) e `.b3-mode-switch` — verificável por `grep -n 'transition-duration: 0.01ms' -B3 web/src/App.jsx`.
    - Ordem de fonte confirmada: o número de linha da regra `.b3-mode-switch, .b3-mode-switch *{ transition:` é MENOR que o da linha `transition-duration: 0.01ms`, que por sua vez é MENOR que o da linha `.b3 .tt-track,.b3 .spin{ animation:none`.
    - `grep -c 'animation:none !important' web/src/App.jsx` continua 1 (o bloco estreito não foi apagado nem mesclado).
    - `cd web && npx vite build` sai com código 0.
  </acceptance_criteria>
  <done>O gate abrangente existe, cobre a raiz e a troca de modo, está na posição de fonte correta, e a trava específica das duas animações infinitas segue intacta e documentada.</done>
</task>

<task type="auto">
  <name>Task 3: Verificar ao vivo fonte e movimento reduzido + fechar o guardião</name>
  <read_first>
    - web/tests/test_fase20_fundacao_visual.mjs (guardião dos planos 20-01/20-02 — estender)
    - web/src/App.jsx (estado pós-Tasks 1 e 2)
    - .planning/phases/20-funda-o-estrutural-e-tipogr-fica/20-02-SUMMARY.md (método de medição ao vivo e conta local de teste)
  </read_first>
  <files>web/tests/test_fase20_fundacao_visual.mjs</files>
  <action>
**Parte 1 — verificação ao vivo.** Subir `bash scripts/executar.sh`, abrir
`http://localhost:5173` pela MCP do Chrome, logar com a conta local e:
- **TYPO-03:** navegar pelas 5 abas da barra inferior + Histórico + Preferências
  e, em cada tela, ler `getComputedStyle(document.querySelector('h1')).fontFamily`
  — deve começar por `Fredoka`. Confirmar também que a Fredoka de fato CARREGOU
  (`document.fonts.check('600 22px Fredoka')`), não só que foi pedida: se a
  família estiver na cascata mas a fonte não carregar, o título cai no Nunito e
  o resultado visual é indistinguível do estado anterior. Tirar screenshot de 3
  telas e descrever se o título parece a mesma família do wordmark "Boris+" do
  Topbar.
- **MOTION-03:** emular `prefers-reduced-motion: reduce` no navegador e medir,
  com a preferência LIGADA:
  - `getComputedStyle(document.querySelector('.b3-shell')).transitionDuration`
    ≈ `0.01ms` (o elemento raiz, o caso que o seletor corrigido cobre);
  - acionar a troca de tema/modo em Preferências e confirmar que a mudança de
    cor é instantânea, sem fade;
  - `getComputedStyle(el).animationName` do elemento `.spin` (spinner) e do
    `.tt-track` (marquee do ticker) = `none`, e confirmar visualmente por dois
    screenshots com ~1s de intervalo que o ticker está PARADO (mesma posição),
    não em flicker acelerado. Este é o teste que separa a implementação certa
    da armadilha do `0.01ms` em animação infinita.
  - Desligar a emulação e confirmar que o ticker volta a andar (o gate não pode
    quebrar o comportamento padrão).
- Encerrar com `bash scripts/executar.sh --stop`.

**Parte 2 — fechar o guardião.** Acrescentar a
`web/tests/test_fase20_fundacao_visual.mjs`, no mesmo padrão `ok(nome, condicao)`:
- TYPO-03: todo `<h1>` do arquivo tem `fontFamily: DISPLAY` (contagem de `<h1>`
  igual à contagem de `<h1>` com a chave), e o total é ≥ 15. Comentário deve
  registrar que a trava é por igualdade de contagem justamente para pegar H1
  NOVO introduzido por fase futura sem a fonte da marca.
- MOTION-03: existem dois blocos `prefers-reduced-motion`; o bloco amplo lista
  `.b3` (não só `.b3 *`) e `.b3-mode-switch`; o bloco estreito com
  `animation:none` continua existindo; e a ordem de fonte é a prescrita
  (`.b3-mode-switch` transition < bloco amplo < bloco estreito), verificada por
  comparação de `indexOf` no texto do arquivo.
Filtrar comentários antes de qualquer contagem por token.

**Parte 3.** Rodar a suíte canônica inteira.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - O SUMMARY registra, para pelo menos 5 telas distintas, o valor lido de `getComputedStyle(h1).fontFamily`, todos começando por `Fredoka`, e o resultado de `document.fonts.check('600 22px Fredoka')`.
    - O SUMMARY registra `transitionDuration` do `.b3-shell` sob movimento reduzido e afirma que a troca de tema foi instantânea.
    - O SUMMARY registra `animationName === "none"` para `.spin` e `.tt-track` sob movimento reduzido, e afirma, por comparação de dois screenshots com intervalo, que o ticker está PARADO (não em strobe).
    - O SUMMARY registra que, com a emulação desligada, o ticker volta a se mover.
    - `cd web && node tests/test_fase20_fundacao_visual.mjs` sai com código 0 e cobre agora os 7 requisitos da fase (FIX-01, FIX-02, SYS-04, TYPO-01, TYPO-02, TYPO-03, MOTION-03).
    - O guardião é sensível: removendo temporariamente `fontFamily: DISPLAY` de um `<h1>` o teste falha (verificar e desfazer).
    - `bash scripts/executar.sh --testes` sai com código 0 (duas suítes).
  </acceptance_criteria>
  <done>Os 15 H1 renderizam em Fredoka de verdade (fonte carregada, não só pedida), o movimento reduzido zera transições e mantém as duas animações infinitas paradas, e o guardião cobre os 7 requisitos da fase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhuma nova | Família tipográfica e uma media query de CSS. Sem rota, sem entrada, sem cálculo financeiro, sem IA. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Denial of Service | `animation-duration: 0.01ms` aplicado a animação `infinite` produz strobe — risco vestibular, o oposto do que MOTION-03 existe para evitar | mitigate | Bloco estreito `animation:none` preservado com especificidade maior; `animation-iteration-count: 1` no bloco amplo; verificação ao vivo por dois screenshots comparando posição do ticker |
| T-20-08 | Repudiation | Declarar TYPO-03 pronto com a Fredoka pedida mas não carregada (cai em Nunito, indistinguível) | mitigate | Critério de aceite exige `document.fonts.check` além de `getComputedStyle` |
| T-20-09 | Tampering | H1 novo vindo das Fases 17/18/19 (mesmo arquivo, não publicadas/enviadas) escapar do inventário de 15 | mitigate | Task 1 obriga parar e reportar se `grep -c "<h1"` exceder o inventário; guardião trava por IGUALDADE de contagem, não por número fixo |
| T-20-SC | Tampering | instalações npm/pip/cargo | mitigate | Zero pacote novo; a Fredoka já é fonte do projeto desde a Fase 3 do rebranding |
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` verde.
- `cd web && npx vite build` sem erro.
- `getComputedStyle(h1).fontFamily` começa por Fredoka em ≥5 telas, com a fonte carregada.
- Sob movimento reduzido: transição da raiz zerada, ticker e spinner parados.
</verification>

<success_criteria>
TYPO-03 e MOTION-03 fechados e verificados ao vivo, com o gate de movimento
reduzido cobrindo o elemento raiz (correção mecânica sobre o texto literal do
CONTEXT, intenção preservada) e sem trocar a trava correta das duas animações
infinitas por uma que produziria strobe.
</success_criteria>

<output>
Create `.planning/phases/20-funda-o-estrutural-e-tipogr-fica/20-03-SUMMARY.md` when done
</output>
