---
phase: 21-duplica-o-removida-e-portf-lio-consolidado
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - web/src/App.jsx
  - web/tests/test_fase3_c19_card_status.mjs
  - web/tests/test_auditoria_status_strip.mjs
  - web/tests/test_historico_setup_card_ui.mjs
autonomous: true
requirements: [DEDUP-02]

must_haves:
  truths:
    - "Na tela Operador IA, modo do app / operador no servidor / executar-sinalizar aparecem uma única vez — sem um card de texto repetindo o que o controle logo abaixo já mostra"
    - "O link 'Trocar modo →' continua existindo e navegável em Operador IA, com o mesmo destino (Perfil) — a remoção do card não tirou a navegação do usuário"
    - "As duas linhas de transparência do gate de entrada automática (cp.entradaAuto.regra e .contraste, ADR-017 Bloco 4) continuam na tela, mudaram de lar, não sumiram"
    - "Os três guardiões que travavam o card removido registram a reversão deliberada com nota datada, em vez de serem apagados — e passam a travar o estado NOVO"
  artifacts:
    - path: "web/src/App.jsx"
      provides: "AgenteScreen sem o card de status redundante, com 'Trocar modo →' e as linhas entradaAuto realocadas"
      contains: "Trocar modo →"
    - path: "web/tests/test_fase3_c19_card_status.mjs"
      provides: "registro da reversão do C-19 — guardião reescrito para travar a ausência do card e a sobrevivência do que não era redundante"
      contains: "DEDUP-02"
    - path: "web/tests/test_auditoria_status_strip.mjs"
      provides: "registro da reversão da tira de status da auditoria 2026-08-07"
      contains: "DEDUP-02"
    - path: "web/tests/test_historico_setup_card_ui.mjs"
      provides: "recorte 2 repontado do card C-19 para o card ENTRADA AUTOMÁTICA (novo lar das linhas de transparência)"
      contains: "ENTRADA AUTOMÁTICA"
  key_links:
    - from: "web/src/App.jsx (AgenteScreen, link relocado)"
      to: "tela Perfil"
      via: "onClick={() => A.go(\"perfil\")}"
      pattern: "A\\.go\\(\"perfil\"\\)"
    - from: "web/src/App.jsx (card ENTRADA AUTOMÁTICA)"
      to: "copy.js COPY[modo].entradaAuto.regra / .contraste"
      via: "ctx.cp.entradaAuto.* (nunca frase hardcodada)"
      pattern: "cp\\.entradaAuto\\.(regra|contraste)"
---

<objective>
Remover da tela Operador IA o card de texto que repete, em modo read-only, os
três fatos que o card funcional logo abaixo já mostra de forma acionável — e
preservar as duas coisas dentro dele que NÃO eram redundantes.

Purpose: DEDUP-02 é o único item desta fase que apaga conteúdo. O
`21-UI-SPEC.md` já flagrou que uma leitura literal do `21-CONTEXT.md`
("remover inteiramente o card") derrubaria junto a transparência do ADR-017
Bloco 4. Este plano encontrou um segundo risco que nenhum dos artefatos de
fase viu: **três guardiões de teste travam exatamente o card que sai**, dois
deles com `process.exit(1)` em marcador ausente. Sem tratá-los, a suíte
canônica fica vermelha e a regra do CLAUDE.md ("guardiões de teste não se
apagam — reversão deliberada atualiza o guardião com nota") é violada.

Output: `AgenteScreen` sem o card duplicado, com o link e a transparência em
novos lares, e os três guardiões reescritos como REGISTRO DA REVERSÃO,
travando o estado novo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/21-duplica-o-removida-e-portf-lio-consolidado/21-CONTEXT.md
@.planning/phases/21-duplica-o-removida-e-portf-lio-consolidado/21-UI-SPEC.md
@.planning/phases/21-duplica-o-removida-e-portf-lio-consolidado/21-PATTERNS.md
@.planning/phases/21-duplica-o-removida-e-portf-lio-consolidado/21-01-SUMMARY.md
@CLAUDE.md

<interfaces>
Recorte de `AgenteScreen` a ser editado (linhas re-verificadas ao vivo em
2026-09-05 — RE-VERIFIQUE com grep antes de editar, o plano 21-01 mexeu em
`App.jsx` e pode ter deslocado tudo o que vem depois da linha 4363):

- **4765-4774** — comentário `{/* C-19 (REPORT-01) · D-02 revisado ... */}`
- **4775** — `<div>` externo do card (chrome `background: T.bgBase, border: ...`)
- **4776-4787** — a linha de 3 `<span>` de status (Modo do app / Operador no
  servidor / Executar/sinalizar)
- **4788-4790** — `<button>` "Trocar modo →" (`onClick={() => A.go("perfil")}`)
- **4792-4797** — comentário `{/* ADR-017 Bloco 4 (Plano 08-02) ... */}`
- **4798-4801** — as duas linhas `{ctx.cp.entradaAuto.regra}` e
  `{ctx.cp.entradaAuto.contraste}`
- **4802** — fechamento do `</div>` externo do card

Destinos das duas realocações:
- **4808-4810** — o `<p>` de introdução da tela; o link "Trocar modo →" vai
  logo DEPOIS do `</p>`, antes do comentário do card-herói (4812).
- **4970-4988** — o card "ENTRADA AUTOMÁTICA"; as duas linhas de
  transparência vão logo depois do `</p>` da descrição (4977), antes do
  `<Toggle>` (4979), aninhadas dentro do mesmo `<div>` de texto.

`bash scripts/executar.sh --testes` — suíte canônica (pytest + todos os
`web/tests/*.mjs`).
`npx vite build` (dentro de `web/`) — obrigatório após editar o front.
</interfaces>

<achado_de_planejamento_guardioes>
**Este é o item mais importante deste plano e não está em nenhum artefato de
fase.** `21-PATTERNS.md` declara "Files scanned: 3" e só examinou
`test_copy_theme.mjs`. Varredura completa dos 115 arquivos de `web/tests/`
feita durante o planejamento encontrou TRÊS guardiões que quebram:

1. **`web/tests/test_fase3_c19_card_status.mjs`** — o arquivo inteiro é sobre
   este card. Faz `process.exit(1)` já na linha 33 se o marcador
   `"C-19 (REPORT-01)"` sumir de `AgenteScreen`. Suas 8 asserções travam:
   o card existir antes do `<h1>`, os 3 rótulos, cada badge ler a fonte
   canônica, ser read-only, ter um único `onClick` para `A.go("perfil")`,
   e `"Modo do app:"` aparecer exatamente 1×.

2. **`web/tests/test_auditoria_status_strip.mjs`** — 4 asserções sobre a
   existência de `"Modo do app:"` em `AgenteScreen` antes de
   `"OPERADOR NO SERVIDOR"`, sobre a expressão
   `{operador ? "📈 Operador" : "🎓 Estudo"}` e sobre o link para Perfil
   dentro de uma janela de 800 caracteres.

3. **`web/tests/test_historico_setup_card_ui.mjs`** — o "Recorte 2" (linhas
   52-66) isola o card pelo mesmo marcador `C-19 (REPORT-01)` e aborta se
   ele sumir. As asserções (6)(7) travam os 3 badges e as (8) travam
   `cp.entradaAuto.regra`/`.contraste` DENTRO desse recorte. As asserções
   (1)-(5) e (9) do arquivo são sobre outro assunto (o item de setup do
   histórico) e devem ficar INTACTAS.

**Regra do CLAUDE.md que se aplica:** "Guardiões de teste não se apagam —
reversão deliberada atualiza o guardião com nota." DEDUP-02 é uma reversão
deliberada da decisão C-19/D-02 (Fase 3) e da auditoria 2026-08-07. Os três
arquivos são REESCRITOS com nota datada, nunca deletados, e passam a travar o
estado novo. Deletar qualquer um dos três é falha de execução deste plano.

**O que legitima a reversão (verificado ao vivo, registre no SUMMARY):** o
modo do app segue visível em TODA tela pelo chip do `Topbar` —
`App.jsx:9011` passa `modeChip={cp.chipModo}` ("MODO ESTUDO"/"MODO
OPERADOR"), fora do switch de rotas. Os outros dois fatos (servidor
ligado/desligado, executar × sinalizar) seguem no card-herói
`OPERADOR NO SERVIDOR` (`App.jsx:4816-4833`), que os mostra com o toggle
real. Nenhuma informação some da tela.
</achado_de_planejamento_guardioes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover o card redundante e realocar link e transparência</name>
  <read_first>
    - web/src/App.jsx:4763-4835 (o card a remover, o `<p>` de introdução e o card-herói — RE-VERIFICAR as linhas com `grep -n "C-19 (REPORT-01)" web/src/App.jsx` antes de editar)
    - web/src/App.jsx:4970-4991 (o card ENTRADA AUTOMÁTICA — destino das linhas de transparência)
    - web/src/App.jsx:9011 (Topbar com `modeChip={cp.chipModo}` — a prova de que o modo do app não some da tela)
    - .planning/phases/21-duplica-o-removida-e-portf-lio-consolidado/21-UI-SPEC.md (Structural Contract, DEDUP-02 — os 3 destinos já estão escritos, literais)
    - .planning/phases/21-duplica-o-removida-e-portf-lio-consolidado/21-PATTERNS.md (seção 3 — o JSX exato dos dois blocos relocados)
  </read_first>
  <files>web/src/App.jsx</files>
  <action>
Três operações em `AgenteScreen`, nesta ordem.

**(1) Apagar — o que é genuinamente redundante.**
Remover, num bloco só: o comentário `{/* C-19 (REPORT-01) · D-02 revisado ... */}`
(~4765-4774), o `<div>` externo do card (~4775), os três `<span>` de status
(~4776-4787), o comentário `{/* ADR-017 Bloco 4 ... */}` (~4792-4797) e o
`</div>` de fechamento (~4802).

O comentário do C-19 sai JUNTO com o código: ele descreve exatamente o que
está sendo removido ("os 3 interruptores", "card ÚNICO, não duas tiras",
"Read-only: nenhum badge altera estado"). Deixá-lo para trás seria uma
descrição obsoleta de código inexistente — neste repositório comentário
carrega histórico de decisão, e remoção sem o comentário é remoção
incompleta (CLAUDE.md, "Comments — carry decision history").

O comentário do ADR-017 Bloco 4, ao contrário, descreve o CONTEÚDO das duas
linhas de transparência (o número do contraste ser referência fixa de
backtest, os dois números morarem na mesma string de `copy.js`) — ele viaja
JUNTO com as linhas para o novo lar na operação (3), não é apagado.

**(2) Realocar o link "Trocar modo →".**
Inserir o `<button>` logo depois do `</p>` de introdução da tela (~4810) e
antes do comentário do card-herói (~4812). JSX idêntico ao original, com duas
diferenças mínimas e justificadas:
- `padding: 0` vira `padding: "8px 0 0"` (o botão vira linha própria, precisa
  do respiro que a flex-row antiga dava);
- `flex: "none"` sai (era propriedade de item de flex-row; o container que a
  usava não existe mais).
Copy (`Trocar modo →`), handler (`A.go("perfil")`), cor (`T.accent`), peso e
tamanho ficam byte a byte iguais. Zero string nova, zero interação nova.

NÃO aninhar o botão dentro do card-herói: o layout daquele card não é tocado
por esta fase (`21-CONTEXT.md`, Deferred Ideas — "qualquer redesenho maior do
AgenteScreen além de remover o card duplicado" está fora de escopo).

**(3) Realocar as duas linhas de transparência do `entradaAuto`.**
Mover o `<div>` com `{ctx.cp.entradaAuto.regra}` e
`{ctx.cp.entradaAuto.contraste}` — junto do comentário do ADR-017 Bloco 4 —
para dentro do card "ENTRADA AUTOMÁTICA" (~4970-4988), aninhado no mesmo
`<div>` que já contém o título e o `<p>` de descrição, logo depois do
`</p>` (~4977) e antes do `<Toggle>` (~4979).

Estilo inalterado: `fontSize: "11px"`, `color: T.textMuted` na primeira linha
e `T.textFaint` na segunda, `lineHeight: 1.45`, `marginTop: "2px"` na
segunda. Continuam lendo `ctx.cp.entradaAuto.*` — nunca uma frase hardcodada
(há guardião no repositório travando isso: asserção (9) de
`test_historico_setup_card_ui.mjs`, que compara contra os valores literais
de `COPY[modo].entradaAuto`).

Este é o lar tematicamente correto: as duas linhas explicam o gate de
elegibilidade da entrada automática, e este é o único card da tela que trata
de entrada automática.

**Depois das três operações:**
1. `cd web && npx vite build` — pega erro de sintaxe JSX que grep não pega.
2. `bash scripts/executar.sh --testes`.

**A suíte VAI ficar vermelha nesta task, e isso é esperado.** Os três
guardiões listados em `<achado_de_planejamento_guardioes>` falham agora. NÃO
tocar em nenhum dos três nesta task — eles são tratados nas Tasks 2 e 3.
Registrar no SUMMARY a lista exata dos testes que falharam e confirmar que
são exatamente esses três, nenhum a mais. Se falhar um QUARTO teste, PARAR e
reportar: a varredura de guardiões do planejamento errou e a premissa precisa
ser reaberta.
  </action>
  <verify>
    <automated>cd web && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "C-19 (REPORT-01)" web/src/App.jsx` retorna `0`.
    - `grep -c "Modo do app:" web/src/App.jsx` retorna `0`.
    - `grep -c "Operador no servidor:" web/src/App.jsx` retorna `0` (o rótulo do card removido; o título do card-herói é `OPERADOR NO SERVIDOR`, sem dois-pontos, e segue presente).
    - `grep -c "Trocar modo →" web/src/App.jsx` retorna exatamente `1`. (Antes desta task retornava 2: o botão E a menção dentro do comentário do C-19. A string NÃO colide com `"Trocar para Modo Operador →"`, que é outra string e segue intocada.)
    - `grep -c "Trocar para Modo Operador" web/src/App.jsx` retorna `2` — os dois links condicionais existentes seguem intocados.
    - `grep -c "cp.entradaAuto.regra" web/src/App.jsx` retorna `1` e `grep -c "cp.entradaAuto.contraste" web/src/App.jsx` retorna `1` — realocadas, não duplicadas nem apagadas (mesma contagem de antes da fase).
    - As duas linhas de transparência estão DENTRO do recorte do card ENTRADA AUTOMÁTICA: o índice de `cp.entradaAuto.regra` em `App.jsx` é maior que o de `ENTRADA AUTOMÁTICA` e menor que o de `id="alloc"`.
    - O link relocado está ANTES do card-herói: o índice de `Trocar modo →` é menor que o índice de `OPERADOR NO SERVIDOR`.
    - `cd web && npx vite build` conclui com código 0.
    - `bash scripts/executar.sh --testes` falha em EXATAMENTE três arquivos: `test_fase3_c19_card_status.mjs`, `test_auditoria_status_strip.mjs` e `test_historico_setup_card_ui.mjs` — a lista está registrada no SUMMARY.
    - `git diff --stat` lista apenas `web/src/App.jsx`.
  </acceptance_criteria>
  <done>O card redundante saiu, o link e a transparência do ADR-017 estão em novos lares funcionais, o build compila, e a vermelhidão da suíte está circunscrita e documentada aos três guardiões previstos.</done>
</task>

<task type="auto">
  <name>Task 2: Reescrever os dois guardiões dedicados como registro da reversão</name>
  <read_first>
    - web/tests/test_fase3_c19_card_status.mjs (arquivo inteiro — as 8 asserções e a estratégia de isolamento por marcador)
    - web/tests/test_auditoria_status_strip.mjs (arquivo inteiro — as 4 asserções e a janela de 800 caracteres)
    - CLAUDE.md (guardrail "Guardiões de teste não se apagam — reversão deliberada atualiza o guardião com nota")
    - web/src/App.jsx (estado JÁ editado pela Task 1 — o AgenteScreen sem o card, com o link e a transparência relocados)
    - web/src/App.jsx:9011 (Topbar `modeChip={cp.chipModo}` — a evidência de que o modo do app segue visível)
  </read_first>
  <files>web/tests/test_fase3_c19_card_status.mjs, web/tests/test_auditoria_status_strip.mjs</files>
  <action>
Reescrever os DOIS arquivos, mantendo os nomes (o nome aponta para a decisão
histórica que agora é revertida — renomear perderia esse elo). NÃO deletar
nenhum dos dois. NÃO reduzir a suíte a menos asserções do que tinha.

**Cabeçalho obrigatório nos dois arquivos** — nota datada de reversão, no
padrão do repositório (`qa/NN`, `Fase N`, comentário datado que carrega o
porquê). Deve conter, em prosa:
- a data (2026-09-05) e a origem (Fase 21 do milestone v1.5, requisito
  DEDUP-02);
- o que a decisão original travava e por que ela foi revertida: o card era
  read-only e repetia os três fatos que o card-herói `OPERADOR NO SERVIDOR`
  logo abaixo já mostra de forma acionável, com o toggle real;
- para onde foi cada informação: o modo do app vive no chip do `Topbar`
  (`App.jsx:9011`, `modeChip={cp.chipModo}`, presente em toda tela); o
  servidor ligado/desligado e o executar × sinalizar vivem no card-herói; o
  link "Trocar modo →" foi relocado para logo abaixo do parágrafo de
  introdução; as duas linhas de transparência do ADR-017 Bloco 4 foram para
  dentro do card "ENTRADA AUTOMÁTICA";
- a afirmação explícita de que o guardião NÃO foi apagado — ele passou a
  travar o estado novo, incluindo a não-regressão (o card não pode voltar).

**`test_fase3_c19_card_status.mjs` — asserções novas** (manter o helper
`ok(nome, cond)`, o isolamento de `AgenteScreen` por
`indexOf("function AgenteScreen(")` até o próximo `"\nfunction "`, e o
`process.exit(1)` com mensagem se `AgenteScreen` não for encontrado):
1. o marcador `"C-19 (REPORT-01)"` NÃO existe mais em `AgenteScreen`
   (não-regressão: o card não volta);
2. `"Modo do app:"` aparece 0× em `AgenteScreen`;
3. `"Executar/sinalizar:"` aparece 0× em `AgenteScreen`;
4. o card-herói segue lá e segue mostrando os dois fatos acionáveis: o
   recorte contém `OPERADOR NO SERVIDOR`, `"ATIVO"`/`"INATIVO"` e
   `modoEfetivo === "executar"`;
5. o modo do app segue visível globalmente: no arquivo INTEIRO (não no
   recorte), `modeChip={cp.chipModo}` existe — a informação não sumiu do
   app, mudou de lugar;
6. o link sobreviveu: `"Trocar modo →"` aparece exatamente 1× em
   `AgenteScreen`, com `onClick={() => A.go("perfil")}` na mesma vizinhança;
7. o link está ANTES do card-herói (índice de `Trocar modo →` menor que o de
   `OPERADOR NO SERVIDOR`) — continua visível sem rolar;
8. a transparência sobreviveu: `cp.entradaAuto.regra` e
   `cp.entradaAuto.contraste` aparecem exatamente 1× cada em `AgenteScreen`.

**`test_auditoria_status_strip.mjs` — asserções novas** (mesma estrutura de
isolamento). A INTENÇÃO original do arquivo (auditoria 2026-08-07, itens
3+4) era: "a distinção entre 'esta tela configura parâmetros' e 'o
interruptor mestre mora em Perfil' não pode ficar só implícita". Essa
intenção sobrevive à reversão e é o que o guardião passa a travar:
1. a tira antiga não existe: `"Modo do app:"` aparece 0× em `AgenteScreen`;
2. a expressão de emoji da tira antiga
   (`{operador ? "📈 Operador" : "🎓 Estudo"}`) não aparece mais em
   `AgenteScreen`;
3. o caminho para o interruptor mestre segue explícito na tela: existe pelo
   menos um `A.go("perfil")` em `AgenteScreen`;
4. esse caminho é o link textual relocado — `"Trocar modo →"` exatamente 1×,
   e ele aparece ANTES do card-herói `OPERADOR NO SERVIDOR`;
5. o link é incondicional (não está dentro de um bloco `{!operador && (`):
   afirmar que, no recorte entre o `</p>` de introdução e o comentário do
   card-herói, não há `{!operador &&` antes do botão. Distinguir do link
   `"Trocar para Modo Operador →"`, que É condicional e segue intocado.

Depois de reescrever os dois, rodar cada um isoladamente com `node` e
confirmar código 0. NÃO tocar em `test_historico_setup_card_ui.mjs` nesta
task (é a Task 3). NÃO tocar em `App.jsx` nesta task.
  </action>
  <verify>
    <automated>node web/tests/test_fase3_c19_card_status.mjs && node web/tests/test_auditoria_status_strip.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Os dois arquivos ainda existem com os nomes originais (`git status` não mostra deleção nem rename).
    - `node web/tests/test_fase3_c19_card_status.mjs` sai com código 0 e imprime `todos os testes passaram`.
    - `node web/tests/test_auditoria_status_strip.mjs` sai com código 0.
    - `grep -c "DEDUP-02" web/tests/test_fase3_c19_card_status.mjs` e `grep -c "DEDUP-02" web/tests/test_auditoria_status_strip.mjs` retornam ao menos `1` — a nota datada de reversão está no arquivo.
    - `grep -c "2026-09-05" web/tests/test_fase3_c19_card_status.mjs` retorna ao menos `1`.
    - `test_fase3_c19_card_status.mjs` tem pelo menos 8 chamadas de `ok(` — a suíte não encolheu em relação às 8 asserções originais.
    - `test_auditoria_status_strip.mjs` tem pelo menos 4 chamadas de `ok(`.
    - Os dois arquivos continuam abortando com `process.exit(1)` e mensagem explícita se `function AgenteScreen(` não for encontrado — nunca passam em silêncio por marcador ausente.
    - `git status --porcelain web/src/App.jsx web/tests/test_historico_setup_card_ui.mjs` não mostra mudança nova nesta task.
  </acceptance_criteria>
  <done>As duas decisões revertidas (C-19/D-02 da Fase 3 e a tira da auditoria 2026-08-07) estão registradas nos próprios guardiões com nota datada, e os dois arquivos passaram a travar o estado novo, incluindo a não-regressão do card.</done>
</task>

<task type="auto">
  <name>Task 3: Repontar o recorte C-19 do guardião de histórico e fechar a suíte</name>
  <read_first>
    - web/tests/test_historico_setup_card_ui.mjs (arquivo inteiro — em especial o "Recorte 2", linhas 52-66, e as asserções (6)(7)(8) na seção "CARD C-19", linhas ~107-124)
    - web/src/App.jsx:4970-4991 (o card ENTRADA AUTOMÁTICA já com as linhas de transparência relocadas pela Task 1)
    - web/tests/test_fase3_c19_card_status.mjs (já reescrito na Task 2 — usar a mesma redação de nota de reversão, para os dois arquivos contarem a mesma história)
  </read_first>
  <files>web/tests/test_historico_setup_card_ui.mjs</files>
  <action>
Este arquivo NÃO é sobre o card removido — ele cobre dois assuntos. Só a
metade "Recorte 2 / CARD C-19" é afetada. Preservação cirúrgica:

**Não tocar** nas asserções (1)-(5) (nível "item de setup do histórico":
`<HistoricoPill>`, `entradaAutoTxt(` com `setup: s.nome`, a condicional
`operador &&`) nem na asserção (9) (o arquivo inteiro não hardcoda os valores
literais de `COPY[modo].entradaAuto`). A (9) é especialmente valiosa e segue
válida depois da realocação — ela prova que a transparência continua vindo de
`copy.js` e não virou frase solta ao mudar de lar.

**Repontar o Recorte 2.** Hoje ele isola o bloco por
`MARCADOR_C19 = "C-19 (REPORT-01)"` até
`MARCADOR_FIM_CARD = '<div style={{ display: "flex", alignItems: "center", gap: "2px"'`,
e aborta se o marcador de início sumir — que é exatamente o que aconteceu.
Trocar o recorte para o card que virou o novo lar da transparência:
- marcador de início: `"ENTRADA AUTOMÁTICA"`;
- marcador de fim: `'id="alloc"'` (o slider de alocação, primeiro elemento
  logo depois do bloco de texto do card — verificado em `App.jsx:4986`);
- manter o `abort(...)` com mensagem explícita nos dois marcadores, na mesma
  redação de hoje ("se renomeado, este guardião TEM que abortar, nunca passar
  em silêncio"). O comportamento de abortar é o valor do arquivo — preservar.

**Reescrever as asserções da seção C-19:**
- as (6)(7) (os 3 badges `Modo do app:` / `Operador no servidor:` /
  `Executar/sinalizar:`) somem — o card não existe mais. Em lugar delas,
  afirmar a não-regressão: `"C-19 (REPORT-01)"` não aparece em `AgenteScreen`.
- as (8) mudam de recorte, não de conteúdo: `cp.entradaAuto.regra` e
  `cp.entradaAuto.contraste` agora precisam estar dentro do recorte do card
  ENTRADA AUTOMÁTICA (a regex `/cp\.entradaAuto\.regra/` continua tolerando
  `ctx.cp.entradaAuto...`, como hoje).
- renomear os rótulos/comentários da seção de "CARD C-19 (status do
  Operador)" para "CARD ENTRADA AUTOMÁTICA (novo lar da transparência do
  gate)", com a mesma nota datada de reversão da Task 2 (2026-09-05, Fase
  21, DEDUP-02) explicando por que o recorte mudou de card.

**Fechamento da fase de código:**
1. `cd web && npx vite build`.
2. `bash scripts/executar.sh --testes` — agora VERDE, as duas suítes.
3. Registrar no SUMMARY a contagem de testes das duas suítes e a confirmação
   de que os três guardiões foram reescritos (não apagados), com o número de
   asserções antes e depois de cada um.

Se qualquer teste seguir vermelho, PARAR e reportar em vez de afrouxar
asserção: um guardião que passa por ter sido enfraquecido é pior do que um
guardião vermelho.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `web/tests/test_historico_setup_card_ui.mjs` ainda existe com o nome original (sem deleção, sem rename).
    - `node web/tests/test_historico_setup_card_ui.mjs` sai com código 0.
    - `grep -c "ENTRADA AUTOMÁTICA" web/tests/test_historico_setup_card_ui.mjs` retorna ao menos `1` (o recorte foi repontado).
    - `grep -c "DEDUP-02" web/tests/test_historico_setup_card_ui.mjs` retorna ao menos `1` (nota datada de reversão presente).
    - O arquivo ainda contém as asserções (1)-(5) e (9) intactas: `grep -c "HistoricoPill" ` retorna ao menos `1`, `grep -c "entradaAutoTxt(" ` retorna ao menos `1`, e a checagem de "não hardcoda `COPY[modo].entradaAuto`" segue presente varrendo o arquivo INTEIRO (não o recorte).
    - O número total de chamadas `ok(` no arquivo é maior ou igual ao de antes desta task (a suíte não encolheu).
    - O arquivo segue abortando com mensagem explícita se os marcadores de início/fim do recorte não forem encontrados.
    - `cd web && npx vite build` conclui com código 0.
    - `bash scripts/executar.sh --testes` sai com código 0 — as DUAS suítes verdes.
    - `git diff --stat` do plano inteiro lista exatamente 4 arquivos: `web/src/App.jsx` e os 3 guardiões; `git diff web/package.json web/package-lock.json` sai vazio.
    - Nenhum `git push` foi executado.
  </acceptance_criteria>
  <done>A suíte canônica voltou ao verde com os três guardiões reescritos em vez de apagados, cada um contando por que a decisão anterior foi revertida e travando o estado novo — incluindo a não-regressão do card e a sobrevivência da transparência do ADR-017.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conteúdo redundante → conteúdo único não-redundante | O card removido misturava as duas coisas. Apagar o bloco inteiro atravessaria o limite e derrubaria conteúdo que só existia ali. |
| decisão histórica registrada em guardião → mudança de produto | Um guardião apagado converte reversão deliberada em perda silenciosa de histórico. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05 | Information disclosure | Perda silenciosa de `cp.entradaAuto.regra`/`.contraste` (transparência do ADR-017 Bloco 4) ao apagar o card inteiro | mitigate | Task 1 realoca em vez de apagar; critério de aceite exige contagem 1 de cada, dentro do recorte do card ENTRADA AUTOMÁTICA; asserção (8) repontada na Task 3 |
| T-21-06 | Denial of service | Usuário perde o caminho navegável para trocar de modo (o link "Trocar modo →" some com o card) | mitigate | Task 1 realoca o botão com handler idêntico; asserções 6 e 7 do guardião reescrito travam presença, unicidade e posição antes do card-herói |
| T-21-07 | Repudiation | Apagar os 3 guardiões para "fazer a suíte passar", destruindo o registro das decisões C-19/D-02 e da auditoria 2026-08-07 | mitigate | Tasks 2 e 3 reescrevem com nota datada; critérios de aceite exigem os nomes de arquivo preservados, contagem de `ok(` não menor, e a string `DEDUP-02` presente nos três |
| T-21-08 | Spoofing | Guardião reescrito virar no-op (marcador inexistente → recorte vazio → tudo "passa") | mitigate | Os três arquivos mantêm `process.exit(1)`/`abort(...)` com mensagem explícita em marcador ausente; critério de aceite verifica esse comportamento |
| T-21-09 | Tampering | Transparência do gate virar frase hardcodada ao mudar de lar, saindo de `copy.js` | mitigate | Asserção (9) de `test_historico_setup_card_ui.mjs` preservada intacta, varrendo o arquivo inteiro contra os valores literais de `COPY[modo].entradaAuto` |
| T-21-10 | Denial of service | Erro de sintaxe JSX nas três operações de recorte/colagem | mitigate | `npx vite build` obrigatório nas Tasks 1 e 3, com código 0 como critério de aceite |
| T-21-SC | Tampering | instalações npm/pip/cargo | mitigate | Zero pacote novo neste plano; `git diff` de `web/package.json`/`package-lock.json` vazio como critério de aceite |
</threat_model>

<verification>
- `C-19 (REPORT-01)`, `Modo do app:` e `Executar/sinalizar:` com 0 ocorrências em `App.jsx`.
- `Trocar modo →` com exatamente 1 ocorrência, antes do card-herói.
- `cp.entradaAuto.regra` e `.contraste` com 1 ocorrência cada, dentro do card ENTRADA AUTOMÁTICA.
- Os 3 guardiões existem com os nomes originais, com nota datada de reversão e sem encolher em número de asserções.
- `npx vite build` verde.
- `bash scripts/executar.sh --testes` verde (as duas suítes).
- Nenhuma dependência nova, nenhum push.
</verification>

<success_criteria>
O critério 3 do ROADMAP da Fase 21 está satisfeito — modo do app, operador no
servidor e executar/sinalizar aparecem uma única vez em Operador IA — sem que
nada não-redundante tenha sido perdido no caminho, e com o histórico da
reversão gravado nos próprios guardiões que travavam a decisão anterior.
</success_criteria>

<output>
Create `.planning/phases/21-duplica-o-removida-e-portf-lio-consolidado/21-02-SUMMARY.md` when done
</output>
