---
phase: 23-motion-com-proposito-e-ilustracao-unificada
plan: 04
type: execute
wave: 3
depends_on: ["23-02", "23-03"]
files_modified:
  - web/src/version.js
  - server/app/main.py
  - server/web_dist
  - web/src/pet/BorisFlat.jsx
  - .planning/phases/20-funda-o-estrutural-e-tipogr-fica/20-HUMAN-UAT.md
autonomous: true
requirements: [MOTION-01, MOTION-02, ILUS-01]

must_haves:
  truths:
    - "A Fase 23 está publicada — não fica testada no repositório e ausente do bundle que o servidor serve"
    - "O carimbo de entrega é novo e coerente nos três elos: web/src/version.js, server/app/main.py e o bundle em server/web_dist"
    - "O bundle é NOVO de verdade, não um web_dist antigo que passaria em checagens de presença: o carimbo ANTERIOR não existe mais dentro de server/web_dist"
    - "Os cinco critérios de sucesso da fase foram remedidos contra o BUNDLE DE PRODUÇÃO (minificado), ou registrados como abertos com roteiro executável e servidor no ar"
    - "Os dois itens de prefers-reduced-motion desta fase entraram no 20-HUMAN-UAT.md existente, sem criar um documento de pendência novo"
    - "Nada foi enviado a origin: a decisão a/b/c sobre publicar junto as Fases 17/18/19 (checkpoints humanos pendentes) continua sendo do Alex"
  artifacts:
    - path: "web/src/version.js"
      provides: "BUILD_ID novo desta entrega"
      contains: "BUILD_ID"
    - path: "server/web_dist"
      provides: "bundle publicado com os dois motions e a ilustração flat"
    - path: ".planning/phases/20-funda-o-estrutural-e-tipogr-fica/20-HUMAN-UAT.md"
      provides: "os itens de UAT de MOTION-01 e MOTION-02 sob reduced-motion, anexados ao documento pendente existente"
      contains: "MOTION-01"
  key_links:
    - from: "web/src/version.js"
      to: "server/web_dist"
      via: "scripts/bump.sh seguido de scripts/publicar-web.sh"
      pattern: "BUILD_ID"
    - from: "web/src/version.js"
      to: "server/app/main.py:SERVER_BUILD_ID"
      via: "sincronização automática dentro de publicar-web.sh"
      pattern: "SERVER_BUILD_ID"
---

<objective>
Publicar a Fase 23 e provar que os cinco critérios de sucesso valem no bundle
de produção — incluindo os três julgamentos que só olho humano faz: o
movimento lê como entrada (e não como flicker), o pulso lê como confirmação (e
não como glitch), e a ilustração nova é inequivocamente o mesmo Boris.

Purpose: este repositório já pagou por isso ("Fase sem plano de publicação
front": plano que toca `web/src/` sem bump + publicação fica mergeado, testado
e nunca vai ao ar). A `Nota de publicação` da Fase 23 no ROADMAP existe por
causa desse histórico — e esta fase é a que mais depende de verificação ao
vivo do milestone inteiro, porque nenhuma das três entregas é mensurável por
regex além da estrutura.

Output: carimbo novo nos três elos, `server/web_dist` republicado, suíte
canônica verde DEPOIS da publicação, os cinco critérios remedidos contra o
bundle (ou entregues ao orquestrador com roteiro exato e servidor no ar), e os
dois itens de `prefers-reduced-motion` anexados ao `20-HUMAN-UAT.md` que já
existe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/23-motion-com-proposito-e-ilustracao-unificada/23-CONTEXT.md
@.planning/phases/23-motion-com-proposito-e-ilustracao-unificada/23-UI-SPEC.md
@.planning/phases/23-motion-com-proposito-e-ilustracao-unificada/23-01-SUMMARY.md
@.planning/phases/23-motion-com-proposito-e-ilustracao-unificada/23-02-SUMMARY.md
@.planning/phases/23-motion-com-proposito-e-ilustracao-unificada/23-03-SUMMARY.md
@.planning/phases/22-componentes-compartilhados-trilho-cones-mascote/22-04-SUMMARY.md
@CLAUDE.md

<interfaces>
`scripts/bump.sh` — avança o `BUILD_ID` em `web/src/version.js`. Sem argumento,
gera o próximo do dia (`F10-AAAAMMDD-NN`). É comando MANUAL de propósito: o
carimbo marca ENTREGA, não build.

`scripts/publicar-web.sh` — builda `web/` (`npm ci` + `vite build`) e publica
em `server/web_dist`, a ÚNICA árvore que o Railway enxerga
(`rootDirectory=/server`). Ele **apaga e recria** o destino
(`rm -rf server/web_dist && cp -r web/dist server/web_dist`) — por isso a
checagem de AUSÊNCIA do carimbo anterior é válida como prova de build novo. Ele
TAMBÉM sincroniza `SERVER_BUILD_ID` em `server/app/main.py` a partir do carimbo
local — por isso, com front sendo publicado, NÃO se edita `SERVER_BUILD_ID` à
mão. Nunca editar `server/web_dist` à mão. Ordem obrigatória: `bump.sh` ANTES
de `publicar-web.sh`.

`bash scripts/executar.sh --testes` — suíte canônica (pytest + `web/tests/*.mjs`).
Roda DEPOIS da publicação: há guardiões de carimbo/bundle que só têm o que
medir com o `web_dist` novo no lugar.

`bash scripts/executar.sh --prod` — sobe o modo PROD local: build do web +
servidor único em `:8787`, servindo o bundle real em vez do Vite. É onde os
cinco critérios são remedidos.
`bash scripts/executar.sh --stop` — encerra e libera as portas.

Carimbo vigente antes desta fase: **`F10-20260906-01`** (em `web/src/version.js`
e em `server/app/main.py`), publicado pela Fase 22 (plano 22-04).
**Re-verificar com `grep BUILD_ID web/src/version.js` antes do bump** — se
outra entrega tiver acontecido no meio, é o valor lido que vale, não este.
</interfaces>

<assinaturas_no_bundle>
As mudanças desta fase são JSX, CSS-em-template-literal e SVG inline. Nomes de
variável e de componente (`BorisFlat`, `vistosRef`, `finalizar`, `isNovo`)
**não sobrevivem à minificação** e não servem de assinatura. Sobrevivem:
literais de string, nomes de classe/keyframe CSS dentro do template literal, e
os atributos literais do SVG.

**Devem estar PRESENTES em `server/web_dist`:**

| Assinatura | Prova o quê |
|---|---|
| `b3cardEnter` | MOTION-01 — o keyframe de entrada chegou ao bundle |
| `card-enter` | MOTION-01 — a classe existe (a aplicação condicional é JSX minificado, mas a classe é literal) |
| `translateY(8px)` | MOTION-01 — a geometria contratada no `23-UI-SPEC.md`, não uma versão "quase" |
| `b3valuePulse` | MOTION-02 — o keyframe do pulso chegou |
| `value-pulse` | MOTION-02 — a classe existe |
| `scale(1.08)` | MOTION-02 — a amplitude contratada |
| `120ms ease-out` | MOTION-02 — a duração contratada, e a mesma que o `setTimeout` usa |
| `prefers-reduced-motion` | MOTION-03 (Fase 20) — o gate que os dois motions herdam continua no bundle |
| `#2a3a6b` | ILUS-01 — o azul-marinho de marca (já existia via `LogoMark`; presença necessária, não suficiente) |
| **o `d` do corpo de `BorisFlat.jsx`** | ILUS-01 — **a única assinatura EXCLUSIVA da ilustração nova.** Ler o valor literal do path/elipse do corpo em `web/src/pet/BorisFlat.jsx` (o único elemento que não é cópia do `LogoMark`) e grepar por ele. Registrar no SUMMARY o literal usado. |
| o BUILD_ID novo | o carimbo desta entrega |

**Devem estar AUSENTES de `server/web_dist` — a checagem mais valiosa das duas
listas:** o **carimbo ANTERIOR** (`F10-20260906-01`, ou o que o `grep` do
`<interfaces>` devolver antes do bump). Um bundle antigo (build que falhou em
silêncio) passaria em todas as checagens de presença herdadas; **nunca** passa
numa checagem de ausência de algo que existia antes. Como o
`publicar-web.sh` faz `rm -rf` do destino, o carimbo anterior tem que sumir por
completo. Usar `grep -rlI` (pula binário).

**NÃO procurar como ausência:**
- `boris.png`, `boris-stage`, `owl-body`, `breath` — o `PetFab` segue com o PNG
  animado de propósito (fronteira de escopo do kickoff). Encontrá-los no bundle
  é o comportamento CORRETO, não uma falha da troca.
- `@keyframes b3spin` / `b3shimmer` / `b3tt` — as três animações da Fase 20
  seguem vivas.
- `requestAnimationFrame` — existe no `Boris.jsx` (sincronia de boca com áudio),
  fora do escopo desta fase.
</assinaturas_no_bundle>

<risco_herdado>
**A publicação é cumulativa e carrega TRÊS fases sem checkpoint humano
aprovado.** `.planning/STATE.md` registra:
- **Fase 17** (`17-06-PLAN.md`, Task 2): checkpoint ADIADO, não aprovado —
  mercado fechado na tentativa, roteiro de 10 passos nunca exercitado.
- **Fase 18** (`18-05-PLAN.md`, Task 2): checkpoint PENDENTE, exercitado só
  parcialmente no iPhone em 2026-09-03.
- **Fase 19** (`19-04-PLAN.md`, Task 2): checkpoint PENDENTE, roteiro não
  rodado.

Esses três fluxos JÁ estão dentro de `server/web_dist` (a Fase 19 publicou em
2026-09-04; as Fases 20, 21 e 22 republicaram depois). A Fase 23 não os
introduz nem os agrava: ela é 100% visual (dois motions e uma ilustração) e
não toca o motor de opções, o fluxo de aceite, nem contrato de API — **nenhum
arquivo de `server/app/*.py` além do `SERVER_BUILD_ID` sincronizado pelo
script**. O que segue em aberto é a decisão a/b/c do Alex sobre **enviar tudo
isso a `origin`** — e essa decisão continua sendo dele.

**Este plano NÃO dá `git push`.** Publicar em `server/web_dist` é operação
local no repositório; o deploy só acontece quando o `origin` recebe. O SUMMARY
deve reafirmar que a decisão a/b/c segue aberta e que nada foi empurrado.
</risco_herdado>

<environment_limitation_known_upfront>
**Precedente confirmado nos 12 planos das Fases 20, 21 e 22**
(`20-01`..`22-04`-SUMMARY.md): subagentes spawnados via Task **NÃO herdam as
ferramentas MCP de navegador** do orquestrador (`mcp__computer-use__*`,
`mcp__claude-in-chrome__*`) — bug upstream anthropics/claude-code#13898.

A Task 2 deste plano é escrita para NÃO travar por causa disso. Se as
ferramentas de navegador não estiverem ligadas ao executor, ele faz tudo o que
é automatizável (publicar, medir por grep no bundle, subir o servidor de
produção, **deixá-lo NO AR**) e entrega ao orquestrador um roteiro numerado e
específico no SUMMARY, seguindo o padrão da seção "Orchestrator Live
Re-Verification" que as Fases 20, 21 e 22 usaram.

O que NUNCA é aceitável: aproximar, estimar ou declarar verificado um item
visual que não foi de fato medido. Item não medido é item registrado como
aberto.

**Duas dependências de estado que nenhum agente controla, e que precisam ser
ditas ao orquestrador em vez de contornadas:**
1. **MOTION-02 executado × pendente** depende de o mercado estar ABERTO ou
   FECHADO. Com o mercado fechado só o ramo `pendente` é exercitável ao vivo
   (que, sozinho, já prova o item mais importante: que ele NÃO pulsa). O ramo
   executado fica aberto até haver pregão. Não forjar.
2. **O modal do Boris só monta uma vez por conta** (`borisIntroVisto`). O
   `23-02-SUMMARY.md` registrou o caminho para reabri-lo — reusar esse
   caminho, não inventar outro.
</environment_limitation_known_upfront>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump do carimbo, publicação do front e prova de bundle novo</name>
  <read_first>
    - scripts/bump.sh (cabeçalho — por que o bump é manual e o que quebra sem ele)
    - scripts/publicar-web.sh (a sincronização automática de `SERVER_BUILD_ID`, o `rm -rf` do destino e a conferência do carimbo no dist)
    - web/src/version.js (carimbo atual — LER, não assumir `F10-20260906-01`)
    - server/app/main.py (a linha de `SERVER_BUILD_ID` — localizar com `grep -n "SERVER_BUILD_ID" server/app/main.py`)
    - web/src/pet/BorisFlat.jsx (para extrair o literal do corpo — a assinatura exclusiva de ILUS-01 no bundle)
    - .planning/phases/23-motion-com-proposito-e-ilustracao-unificada/23-03-SUMMARY.md (estado da suíte antes da publicação)
  </read_first>
  <files>web/src/version.js, server/app/main.py, server/web_dist</files>
  <action>
**Passo 0 — registrar o carimbo ANTERIOR antes de qualquer coisa:**
`grep -o 'F10-[0-9]*-[0-9]*' web/src/version.js | head -1`. Guardar o valor —
é ele que precisa DESAPARECER de `server/web_dist` no fim. Registrar no
SUMMARY.

**Passo 0b — extrair a assinatura exclusiva de ILUS-01:** abrir
`web/src/pet/BorisFlat.jsx` e copiar o literal do elemento do CORPO (o `d` do
path, ou os `cx`/`cy`/`rx`/`ry` da elipse) — o único elemento que não é cópia
do `LogoMark`. Registrar o literal no SUMMARY; é ele que prova no bundle que a
ilustração nova chegou (e não só o `LogoMark`, que já estava lá).

Depois, nesta ordem exata, sem pular passo:

1. `bash scripts/bump.sh` — sem argumento. O esperado é o próximo do dia (ou o
   primeiro de hoje, se a data virou).
2. `bash scripts/publicar-web.sh` — builda `web/` e publica em
   `server/web_dist`; ele sincroniza `SERVER_BUILD_ID` sozinho a partir do
   carimbo local.
3. `bash scripts/executar.sh --testes` — DEPOIS da publicação, para que os
   guardiões de carimbo/bundle meçam o `web_dist` novo.
4. Conferir os TRÊS elos do carimbo: `web/src/version.js`,
   `server/app/main.py:SERVER_BUILD_ID` e o bundle dentro de `server/web_dist`
   carregam o MESMO `BUILD_ID`.
5. Conferir as duas listas de `<assinaturas_no_bundle>` dentro de
   `server/web_dist`, com `grep -rlI`:
   - as assinaturas de PRESENÇA (`b3cardEnter`, `card-enter`,
     `translateY(8px)`, `b3valuePulse`, `value-pulse`, `scale(1.08)`,
     `120ms ease-out`, `prefers-reduced-motion`, `#2a3a6b`, o literal do corpo
     do `BorisFlat`, o BUILD_ID novo) — cada uma retorna ao menos um arquivo;
   - a assinatura de AUSÊNCIA: o carimbo do Passo 0 — **retorna VAZIO**. Esta
     é a checagem que prova que o build novo de fato substituiu o antigo.

Não editar `server/web_dist` à mão. Não editar `SERVER_BUILD_ID` à mão (o
script sincroniza; edição manual só existe em deploy só-backend, que não é o
caso). **Não dar `git push`** — ver `<risco_herdado>`.

Registrar no SUMMARY o carimbo anterior, o carimbo gerado, o literal do corpo
do `BorisFlat` e a contagem de testes das duas suítes.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `git diff web/src/version.js` mostra um `BUILD_ID` novo no formato `F10-AAAAMMDD-NN`, diferente do carimbo registrado no Passo 0.
    - `git status --porcelain server/web_dist` mostra arquivos modificados (a publicação de fato aconteceu).
    - Os três elos batem: `grep -o 'F10-[0-9]*-[0-9]*' web/src/version.js | head -1` é igual ao valor de `sed -n 's/.*SERVER_BUILD_ID = "\([^"]*\)".*/\1/p' server/app/main.py`, e `grep -rlI "$(grep -o 'F10-[0-9]*-[0-9]*' web/src/version.js | head -1)" server/web_dist | head -1` retorna ao menos um arquivo.
    - As 11 assinaturas de presença retornam ao menos um arquivo cada em `grep -rlI ... server/web_dist`.
    - **`grep -rlI "<carimbo anterior>" server/web_dist` retorna VAZIO** — o bundle é novo.
    - `bash scripts/executar.sh --testes` sai com código 0 DEPOIS da publicação (as duas suítes).
    - `git diff --stat web/package.json web/package-lock.json server/requirements.txt server/requirements-prod.txt` sai VAZIO.
    - `git diff --stat server/app/` lista SOMENTE `server/app/main.py`, e o diff dele é somente a linha de `SERVER_BUILD_ID` — esta fase não toca backend.
    - `git status --porcelain resources/ios server/ios_dist` sai VAZIO — o ícone publicado do app não foi tocado (critério 5 do ROADMAP).
    - Nenhum `git push` foi executado (`git status -sb` mostra commits à frente de `origin`).
  </acceptance_criteria>
  <done>Carimbo novo nos três elos, `server/web_dist` republicado, prova de bundle NOVO pela ausência do carimbo anterior, suíte canônica verde após a publicação, nada empurrado para `origin`.</done>
</task>

<task type="auto">
  <name>Task 2: Remedir os cinco critérios contra o bundle de produção e anexar os itens de UAT</name>
  <read_first>
    - .planning/ROADMAP.md (seção da Phase 23 — os cinco critérios de sucesso, texto literal)
    - .planning/phases/23-motion-com-proposito-e-ilustracao-unificada/23-01-SUMMARY.md, 23-02-SUMMARY.md, 23-03-SUMMARY.md (os roteiros de verificação ao vivo já escritos — CONSOLIDAR, não reescrever do zero)
    - .planning/phases/20-funda-o-estrutural-e-tipogr-fica/20-HUMAN-UAT.md (o documento pendente onde os itens de reduced-motion desta fase entram — ler o formato do "Test 1" e o bloco Summary)
    - .planning/phases/22-componentes-compartilhados-trilho-cones-mascote/22-04-SUMMARY.md (o padrão de handoff "Pendente de verificação ao vivo (orquestrador)" a seguir)
    - scripts/executar.sh (os modos `--prod` e `--stop`)
  </read_first>
  <files>web/src/pet/BorisFlat.jsx, .planning/phases/20-funda-o-estrutural-e-tipogr-fica/20-HUMAN-UAT.md</files>
  <action>
**Parte A — anexar os itens de UAT ao documento que já existe.**

`23-CONTEXT.md` é explícito: os itens de `prefers-reduced-motion` desta fase
entram no **MESMO** `20-HUMAN-UAT.md` pendente, **não** num documento novo.

Em `.planning/phases/20-funda-o-estrutural-e-tipogr-fica/20-HUMAN-UAT.md`:
- **Não reescrever o "Test 1"** nem o bloco `## Gaps` — o texto da época se
  preserva (guardrail do repositório: histórico não se reescreve).
- ANEXAR, na seção `## Tests`, dois itens novos no mesmo formato
  (`### N. <título>` / `expected:` / `result: [pending]`):
  - **2. MOTION-01 sob reduced-motion** — com "Reduzir Movimento" ligado no
    SO, abrir a Watchlist e o Radar no build novo (citar o `BUILD_ID` gerado
    na Task 1) e confirmar que os cards aparecem no estado final DIRETO, sem
    fade nem subida, e sem perda de informação. Desligar a preferência e
    confirmar que a transição de ~200ms volta.
  - **3. MOTION-02 sob reduced-motion** — com a preferência ligada, confirmar
    uma ordem e verificar que o modal fecha **na hora**, sem pulso e **sem os
    ~120ms de espera** (é o portão em JS, não só o CSS — o item existe para
    pegar exatamente a versão "não anima, mas ainda demora").
- Atualizar o campo `updated:` do frontmatter e os contadores do bloco
  `## Summary` (`total`, `pending`) para refletir os três itens.
- Registrar em ambos os itens a mesma limitação de ferramenta já documentada
  no Test 1 (nenhuma ferramenta deste ambiente expõe
  `Emulation.setEmulatedMedia` via CDP) — os itens são para o Alex rodar no
  SO, não para um agente emular.

**Parte B — remedir os cinco critérios do ROADMAP contra o bundle.**

Subir `bash scripts/executar.sh --prod` (build de produção servido em `:8787`)
e remedir os CINCO critérios contra o bundle minificado, com uma conta local
logada.

**1. Entrada de card (MOTION-01, critério 1).** Watchlist e Radar:
   - abrir a Watchlist e observar a entrada dos cards (fade + subida, não
     salto);
   - adicionar um ticker novo à watchlist e confirmar que **só o card novo**
     anima;
   - deixar a lista assentar, provocar um re-render (tick de preço, ou trocar
     o filtro de direção e voltar) e confirmar que os cards já vistos **não**
     re-animam — este é o item que distingue "movimento com propósito" de
     "flicker";
   - no Radar, rodar duas varreduras e confirmar que só ticker ausente da
     anterior anima;
   - registrar, para pelo menos um card, o `animationName` e o
     `animationDuration` computados no DOM logo após a entrada (esperado
     `b3cardEnter` / `0.2s`).

**2. Pulso na confirmação (MOTION-02, critério 2).**
   - **Mercado ABERTO:** confirmar uma compra e uma **venda TOTAL**, e
     observar o valor ("Custo estimado" / "Valor estimado") pulsar antes de o
     modal fechar. A venda total é o caso crítico (`23-03-PLAN.md`,
     `<armadilha_do_sellmodal>`): se o modal sumir sem pulso, o `setData`
     ficou fora do `finalizar()`.
   - **Mercado FECHADO:** confirmar uma ordem e verificar que o modal fecha na
     hora com a pill/toast de PENDENTE, **sem pulso e sem atraso**.
   - **Ordem rejeitada** (quantidade acima do caixa): nenhum pulso, toast de
     erro imediato, rejeição no histórico como antes.
   - **Duplo toque em Confirmar** dentro da janela de 120ms: **uma única
     ordem** — conferir no HISTÓRICO e no caixa, não só na tela.
   - Se o mercado estiver fechado no momento da verificação, o ramo executado
     fica ABERTO com essa razão registrada. **Não forjar, não simular por
     dedução.**

**3. Reduced-motion (critério 3).** Não é remedível neste ambiente — é o
   conteúdo da Parte A. Registrar como PENDENTE apontando para os Tests 2 e 3
   do `20-HUMAN-UAT.md`, com o `BUILD_ID` a testar.

**4. Ilustração unificada (ILUS-01, critério 4).**
   - abrir o modal "Este é o Boris" (caminho de reabertura registrado no
     `23-02-SUMMARY.md`) e comparar **lado a lado** com o `LogoMark` do topo e
     com o ícone do app: mesmo azul de corpo, mesmos óculos redondos âmbar,
     mesmo bico;
   - **tema ESCURO** — o item de risco da fase: a silhueta se separa do card
     (`bgCard` `#1b1f2e`; contraste medido do corpo `#2a3a6b` = **1,49:1**) ou
     lê como borrão com óculos flutuando? Se reprovar, aplicar o **remédio
     pré-autorizado** do `23-02-PLAN.md` (`<risco_de_contraste_medido>`):
     contorno de 1 unidade em `#eef1f8` no path do rosto/corpo de
     `web/src/pet/BorisFlat.jsx` — **nenhum outro remédio está autorizado**.
     Se aplicar: refazer `bash scripts/publicar-web.sh` e
     `bash scripts/executar.sh --testes`, e registrar no SUMMARY o valor
     inicial, o final e a razão;
   - **tema CLARO**: mesma conferência (contraste 10,96:1, esperado passar
     folgado);
   - **Modo Operador**: óculos e bico continuam ÂMBAR, sem seguir o acento do
     modo (regra travada em `App.jsx:202-205`);
   - confirmar que o `PetFab` segue com a arte antiga e que as duas conviverem
     não parece defeito.

**5. Ícone do app intocado (critério 5).** Provado estaticamente na Task 1
   (`git status --porcelain resources/ios server/ios_dist` vazio). Registrar
   como FECHADO com essa evidência — não precisa de conferência visual.

Encerrar com `bash scripts/executar.sh --stop`.

**Se as ferramentas de navegador não estiverem disponíveis a você** (ver
`<environment_limitation_known_upfront>`): NÃO improvise, NÃO estime e NÃO
declare verificado o que não mediu. Em vez disso:
- faça a Parte A inteira (ela não depende de navegador);
- faça a parte automatizável da Parte B: suba `--prod`, confirme por
  `curl`/grep que `:8787` está servindo o bundle com o carimbo novo;
- **deixe o servidor NO AR** (não rode `--stop`), para o orquestrador reusar
  sem pagar outro ciclo de build;
- escreva no SUMMARY uma seção **"Pendente de verificação ao vivo
  (orquestrador)"** consolidando os itens 1, 2 e 4 acima como roteiro
  numerado, cada um com a tela, o seletor/propriedade computada a ler e o
  valor esperado — no mesmo formato que `22-04-SUMMARY.md` usou. **Consolidar
  os roteiros já escritos nos SUMMARYs 23-01/23-02/23-03 num só**, sem
  duplicar itens;
- **para o item 4, incluir o caminho de ajuste**: qual arquivo
  (`web/src/pet/BorisFlat.jsx`), qual remédio (contorno `#eef1f8` de 1
  unidade), e que republicar exige `publicar-web.sh` + suíte de novo. Sem isso
  o orquestrador consegue julgar mas não consegue corrigir;
- diga explicitamente qual porta e qual carimbo o orquestrador vai encontrar,
  e se o mercado estava aberto ou fechado no momento (muda o que é
  exercitável no item 2).

No SUMMARY, montar uma tabela dos cinco critérios do ROADMAP × resultado, cada
linha com o valor computado/contagem medida ou com "aberto — roteiro no item
N", nunca com adjetivo.

Reafirmar no SUMMARY: nada foi enviado a `origin`; a decisão a/b/c sobre as
Fases 17/18/19 segue aberta com o Alex.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `20-HUMAN-UAT.md` ganhou os itens `### 2.` (MOTION-01) e `### 3.` (MOTION-02), cada um com `expected:` citando o `BUILD_ID` novo e `result: [pending]`; o frontmatter `updated:` e os contadores do `## Summary` foram atualizados.
    - O texto do `### 1.` e do bloco `## Gaps` do `20-HUMAN-UAT.md` está byte a byte como antes (`git diff` não mostra alteração nessas regiões).
    - **Nenhum arquivo `23-HUMAN-UAT.md` foi criado** (`ls .planning/phases/23-*/` não lista nenhum) — decisão explícita do `23-CONTEXT.md`.
    - O SUMMARY contém uma tabela dos 5 critérios de sucesso do ROADMAP com o resultado medido em `:8787` (produção) — cada linha com um valor computado/contagem medida, ou marcada explicitamente como aberta com roteiro, nunca com adjetivo.
    - Critério 1: `animationName`/`animationDuration` computados registrados para pelo menos um card (esperado `b3cardEnter` / `0.2s`), mais o veredito do teste de não-re-animação após re-render.
    - Critério 2: os quatro sub-casos (compra executada, venda TOTAL executada, pendente, rejeitada) e o teste de duplo toque, cada um com resultado ou marcado ABERTO com a razão (mercado fechado, p.ex.) — nunca deduzido.
    - Critério 3: registrado como PENDENTE apontando para os Tests 2 e 3 do `20-HUMAN-UAT.md`, com o `BUILD_ID` a testar.
    - Critério 4: veredito nos DOIS temas e no Modo Operador. Se houve ajuste em `BorisFlat.jsx`, o SUMMARY traz valor inicial, valor final e a razão — e a suíte + a publicação foram refeitas.
    - Critério 5: FECHADO com a evidência estática (`git status` vazio em `resources/ios` e `server/ios_dist`).
    - Se as ferramentas de navegador não estiverem disponíveis: o SUMMARY contém a seção "Pendente de verificação ao vivo (orquestrador)" com roteiro numerado CONSOLIDADO (sem duplicar os roteiros dos SUMMARYs 23-01/02/03), porta, carimbo, estado do mercado E o caminho de ajuste de `BorisFlat.jsx`; o servidor foi deixado NO AR (nenhum `--stop` executado); e os critérios não medidos ficam marcados como ABERTOS, jamais como fechados.
    - Nenhum valor visual aproximado ou estimado aparece no SUMMARY.
    - `bash scripts/executar.sh --testes` sai com código 0 (rodado de novo se houve republicação).
    - O SUMMARY afirma explicitamente que nenhum `git push` foi feito e que a decisão a/b/c segue aberta.
  </acceptance_criteria>
  <done>Os cinco critérios do ROADMAP estão medidos contra o bundle de produção com valores registrados — ou entregues ao orquestrador como roteiro executável com o servidor já no ar — os dois itens de reduced-motion entraram no `20-HUMAN-UAT.md` existente, e o repositório segue sem push para `origin`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo local → `origin`/Railway | A publicação em `server/web_dist` é local; o deploy real acontece no push. Este plano para antes desse limite de propósito. |
| medição real → afirmação no SUMMARY | Um item visual declarado verificado sem ter sido medido contamina o registro da fase e a decisão do Alex depois. |
| build novo → build antigo em silêncio | Checagens de PRESENÇA passam num bundle velho; só a checagem de AUSÊNCIA do carimbo anterior distingue. |
| pendência desta fase → documento de pendência da Fase 20 | Criar um `23-HUMAN-UAT.md` novo fragmentaria a mesma limitação de ferramenta em dois lugares e faria a Fase 20 parecer fechada. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-22 | Repudiation | Fase testada mas invisível em produção (falta de bump/publicação) | mitigate | Task 1 obriga `bump.sh` → `publicar-web.sh` → suíte, com os três elos do carimbo como critério de aceite |
| T-23-23 | Spoofing | Build falhar em silêncio e o `web_dist` antigo passar nas checagens de presença | mitigate | Critério de aceite exige o carimbo ANTERIOR ausente do bundle — checagem que só um bundle novo passa |
| T-23-24 | Spoofing | Declarar ILUS-01 fechado sem conferência nos dois temas, apesar do contraste medido de 1,49:1 no escuro | mitigate | Item 4 da Task 2 é obrigatório nos dois temas + Modo Operador; remédio pré-autorizado com caminho de ajuste no handoff |
| T-23-25 | Spoofing | Declarar MOTION-02 fechado sem exercitar o ramo pendente e o duplo toque | mitigate | Item 2 da Task 2 lista os quatro sub-casos + duplo toque; item não exercitável fica ABERTO com a razão (mercado fechado) |
| T-23-26 | Elevation of privilege | Empurrar a `origin` as Fases 17/18/19 sem checkpoint aprovado, embutindo decisão do Alex numa task de publicação | mitigate | Proibição explícita de `git push`; critério de aceite verifica commits à frente de `origin` sem envio |
| T-23-27 | Tampering | Fase visual arrastar alteração de backend ou o ícone do app junto na publicação | mitigate | Critérios de aceite limitam o diff de `server/app/` à linha de `SERVER_BUILD_ID` e exigem `git status` vazio em `resources/ios` e `server/ios_dist` |
| T-23-28 | Repudiation | Criar um `23-HUMAN-UAT.md` paralelo, fragmentando a pendência de reduced-motion | mitigate | Parte A da Task 2 anexa ao `20-HUMAN-UAT.md`; critério de aceite exige que nenhum `23-HUMAN-UAT.md` exista |
| T-23-29 | Repudiation | Reescrever o Test 1 / os Gaps do `20-HUMAN-UAT.md` ao anexar (histórico não se reescreve) | mitigate | Critério de aceite exige diff zero nessas regiões |
| T-23-SC | Tampering | instalações npm/pip/cargo | mitigate | Zero pacote novo na fase inteira; `git diff` dos quatro manifests vazio como critério de aceite |
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` verde DEPOIS da publicação.
- Três elos do carimbo coerentes; carimbo anterior AUSENTE do bundle.
- As 11 assinaturas de presença dentro de `server/web_dist`, inclusive o literal exclusivo do corpo do `BorisFlat`.
- Diff de `server/app/` limitado à linha de `SERVER_BUILD_ID`; `resources/ios` e `server/ios_dist` sem diff.
- Cinco critérios do ROADMAP medidos em `:8787`, ou registrados como abertos com roteiro consolidado e servidor no ar.
- Tests 2 e 3 anexados ao `20-HUMAN-UAT.md`, com Test 1 e Gaps intactos; nenhum `23-HUMAN-UAT.md` criado.
- Nenhum push para `origin`.
</verification>

<success_criteria>
A Fase 23 está publicada e provada no bundle de produção (inclusive pela
ausência do carimbo anterior), os dois motions e a ilustração estão travados
por guardião na suíte canônica, os julgamentos que só olho humano faz estão
medidos ou entregues como roteiro executável com o servidor no ar, a pendência
de `prefers-reduced-motion` mora num documento só, e a decisão de enviar a
`origin` (que arrasta as Fases 17/18/19) fica registrada como aberta, sem ser
tomada por um agente.
</success_criteria>

<output>
Create `.planning/phases/23-motion-com-proposito-e-ilustracao-unificada/23-04-SUMMARY.md` when done
</output>
