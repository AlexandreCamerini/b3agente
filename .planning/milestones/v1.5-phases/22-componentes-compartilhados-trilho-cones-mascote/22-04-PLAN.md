---
phase: 22-componentes-compartilhados-trilho-cones-mascote
plan: 04
type: execute
wave: 4
depends_on: ["22-03"]
files_modified:
  - web/src/version.js
  - server/app/main.py
  - server/web_dist
  - web/src/App.jsx
autonomous: true
requirements: [SYS-01, SYS-02, SYS-03]

must_haves:
  truths:
    - "A Fase 22 está publicada — não fica testada no repositório e ausente do bundle que o servidor serve"
    - "O carimbo de entrega é novo e coerente nos três elos: web/src/version.js, server/app/main.py e o bundle em server/web_dist"
    - "A REMOÇÃO chegou ao bundle, não só a adição: nenhum dos nove emojis desta fase existe dentro de server/web_dist"
    - "Os três critérios de sucesso da fase valem no BUNDLE DE PRODUÇÃO (minificado), não só no servidor de desenvolvimento"
    - "A sombra do PetFab foi conferida a olho nos DOIS temas — SYS-03 só é declarado fechado depois disso, e o valor pode ter sido ajustado"
    - "Nada foi enviado a origin: a decisão a/b/c sobre publicar junto as Fases 17/18/19 (checkpoints humanos pendentes) continua sendo do Alex"
  artifacts:
    - path: "web/src/version.js"
      provides: "BUILD_ID novo desta entrega"
      contains: "BUILD_ID"
    - path: "server/web_dist"
      provides: "bundle publicado com o trilho unificado, os ícones SVG e a sombra por tema"
  key_links:
    - from: "web/src/version.js"
      to: "server/web_dist"
      via: "scripts/bump.sh seguido de scripts/publicar-web.sh"
      pattern: "BUILD_ID"
    - from: "web/src/version.js"
      to: "server/app/main.py:SERVER_BUILD_ID"
      via: "sincronização automática dentro de publicar-web.sh"
      pattern: "SERVER_BUILD_ID"
---

<objective>
Publicar a Fase 22 e provar que os três critérios de sucesso valem no bundle de
produção — incluindo a única conferência que só olho humano faz: a sombra do
mascote nos dois temas.

Purpose: este repositório já pagou por isso ("Fase sem plano de publicação
front": plano que toca `web/src/` sem bump + publicação fica mergeado, testado
e nunca vai ao ar). A `Nota de publicação` da Fase 22 no ROADMAP existe por
causa desse histórico. E o plano 22-03 fechou deixando SYS-03 explicitamente em
aberto na calibragem — este plano é onde ele fecha ou é ajustado.

Output: carimbo novo nos três elos, `server/web_dist` republicado, suíte
canônica verde DEPOIS da publicação, os três critérios remedidos contra o bundle
(ou entregues ao orquestrador com roteiro exato, se a ferramenta de navegador
não estiver disponível ao executor), e o veredito de SYS-03 registrado com o
valor final.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-componentes-compartilhados-trilho-cones-mascote/22-CONTEXT.md
@.planning/phases/22-componentes-compartilhados-trilho-cones-mascote/22-UI-SPEC.md
@.planning/phases/22-componentes-compartilhados-trilho-cones-mascote/22-01-SUMMARY.md
@.planning/phases/22-componentes-compartilhados-trilho-cones-mascote/22-02-SUMMARY.md
@.planning/phases/22-componentes-compartilhados-trilho-cones-mascote/22-03-SUMMARY.md
@.planning/phases/21-duplica-o-removida-e-portf-lio-consolidado/21-04-SUMMARY.md
@CLAUDE.md

<interfaces>
`scripts/bump.sh` — avança o `BUILD_ID` em `web/src/version.js`. Sem argumento,
gera o próximo do dia (`F10-AAAAMMDD-NN`). É comando MANUAL de propósito: o
carimbo marca ENTREGA, não build.

`scripts/publicar-web.sh` — builda `web/` e publica em `server/web_dist`, a
ÚNICA árvore que o Railway enxerga (`rootDirectory=/server`). Ele TAMBÉM
sincroniza `SERVER_BUILD_ID` em `server/app/main.py` a partir do carimbo local —
por isso, com front sendo publicado, NÃO se edita `SERVER_BUILD_ID` à mão. Nunca
editar `server/web_dist` à mão. Ordem obrigatória: `bump.sh` ANTES de
`publicar-web.sh`.

`bash scripts/executar.sh --testes` — suíte canônica (pytest + `web/tests/*.mjs`).
Roda DEPOIS da publicação: há guardiões de carimbo/bundle que só têm o que medir
com o `web_dist` novo no lugar.

`bash scripts/executar.sh --prod` — sobe o modo PROD local: build do web +
servidor único em `:8787`, servindo o bundle real em vez do Vite. É onde os três
critérios são remedidos.
`bash scripts/executar.sh --stop` — encerra e libera as portas.

Carimbo vigente antes desta fase: `F10-20260905-03` (em `web/src/version.js` e
em `server/app/main.py`). Publicado pela Fase 21 (plano 21-04).
</interfaces>

<assinaturas_no_bundle>
As mudanças desta fase são JSX e estilo inline — nomes de variável e de
componente (`carouselTrackStyle`, `TierDot`, `NavIcon`) **não sobrevivem à
minificação** e não servem de assinatura. Sobrevivem: literais de string,
literais de hex e nomes de CSS custom property.

**Devem estar PRESENTES em `server/web_dist`:**

| Assinatura | Prova o quê |
|---|---|
| `x proximity` | SYS-01 — o valor de `scrollSnapType` do helper chegou ao bundle |
| `--shadow-fab` | SYS-03 — a chave nova de `PALETTE` virou variável CSS por tema (via `VARKEY`/`themeVarBlock`, sem plumbing manual) |
| `#f59e0b` | SYS-02 — a cor do tier "moderada", literal exclusivo desta fase (era 0 ocorrência antes) |
| `Varredura automática de hoje` | SYS-02 — o rótulo textual do Radar sobreviveu à saída do 📡 |
| `Stop/alvo (IA)` | SYS-02 — o rótulo de Posições sobreviveu |
| `chave configurada` | SYS-02 — o rótulo de config sobreviveu |

**Devem estar AUSENTES de `server/web_dist` — a checagem mais valiosa das
duas listas:** os NOVE glifos removidos: `🎓 📈 ✨ ✅ 📡 🟢 🟡 ⚪ 🔴`.

Um bundle antigo (build que falhou em silêncio) passaria nas seis checagens de
presença herdadas de fases anteriores, mas **nunca** passa numa checagem de
ausência de algo que existia antes. Usar `grep -rlI` (pula binário) para as duas
listas.

**NÃO** procurar `⚡ ✎ ↻ ✕ ✓ ⚠ →` no bundle: são microcópia fora de escopo e
seguem presentes de propósito.
</assinaturas_no_bundle>

<risco_herdado>
**A publicação é cumulativa e carrega TRÊS fases sem checkpoint humano
aprovado.** `.planning/STATE.md` registra:
- **Fase 17** (`17-06-PLAN.md`, Task 2): checkpoint ADIADO, não aprovado —
  mercado fechado na tentativa, roteiro de 10 passos nunca exercitado.
- **Fase 18** (`18-05-PLAN.md`, Task 2): checkpoint PENDENTE, exercitado só
  parcialmente no iPhone em 2026-09-03.
- **Fase 19** (`19-04-PLAN.md`, Task 2): checkpoint PENDENTE, roteiro não rodado.

Esses três fluxos JÁ estão dentro de `server/web_dist` (a Fase 19 publicou em
2026-09-04; as Fases 20 e 21 republicaram em 2026-09-05). A Fase 22 não os
introduz nem os agrava: ela é 100% visual (rolagem, ícone, sombra) e não toca o
motor de opções, o fluxo de aceite, nem contrato de API — **nenhum arquivo de
`server/app/*.py` além do `SERVER_BUILD_ID` sincronizado pelo script**. O que
segue em aberto é a decisão a/b/c do Alex sobre **enviar tudo isso a `origin`** —
e essa decisão continua sendo dele.

**Este plano NÃO dá `git push`.** Publicar em `server/web_dist` é operação local
no repositório; o deploy só acontece quando o `origin` recebe. O SUMMARY deve
reafirmar que a decisão a/b/c segue aberta e que nada foi empurrado.
</risco_herdado>

<ferramenta_de_navegador>
**Precedente confirmado nos 8 planos das Fases 20 e 21** (`20-01`..`20-04` e
`21-01`..`21-04`-SUMMARY.md): subagentes spawnados via Task NÃO herdam as
ferramentas MCP de navegador do orquestrador (`mcp__computer-use__*`,
`mcp__claude-in-chrome__*`) — bug upstream anthropics/claude-code#13898.

A Task 2 deste plano é escrita para NÃO travar por causa disso. Se as
ferramentas de navegador não estiverem ligadas ao executor, ele faz tudo o que é
automatizável (publicar, medir por grep no bundle, subir o servidor de produção,
deixá-lo NO AR) e entrega ao orquestrador um roteiro numerado e específico no
SUMMARY, seguindo exatamente o padrão da seção "Orchestrator Live
Re-Verification" que as Fases 20 e 21 usaram.

O que NUNCA é aceitável: aproximar, estimar ou declarar verificado um item
visual que não foi de fato medido. Item não medido é item registrado como
aberto.

**Esta fase é fácil de verificar sem mercado aberto** — as três mudanças são
estruturais/visuais e não dependem de cotação ao vivo. O único item que pede
estado de conta específico é o ponto de tier do Radar (precisa de pelo menos um
ativo com `confluencia` no payload); os outros oito sites de ícone, os quatro
trilhos e a sombra do PetFab aparecem em qualquer conta, inclusive vazia.
</ferramenta_de_navegador>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump do carimbo e publicação do front</name>
  <read_first>
    - scripts/bump.sh (cabeçalho — por que o bump é manual e o que quebra sem ele)
    - scripts/publicar-web.sh (a sincronização automática de `SERVER_BUILD_ID` e o destino do build)
    - web/src/version.js (carimbo atual: `F10-20260905-03`)
    - server/app/main.py (a linha de `SERVER_BUILD_ID` — localizar com `grep -n "SERVER_BUILD_ID" server/app/main.py`)
    - .planning/phases/22-componentes-compartilhados-trilho-cones-mascote/22-03-SUMMARY.md (estado da suíte antes da publicação, e a pendência registrada de SYS-03)
  </read_first>
  <files>web/src/version.js, server/app/main.py, server/web_dist</files>
  <action>
Nesta ordem exata, sem pular passo:

1. `bash scripts/bump.sh` — sem argumento. O vigente é `F10-20260905-03`, então
   o esperado é o próximo do dia (ou o primeiro de hoje, se a data virou).
2. `bash scripts/publicar-web.sh` — builda `web/` e publica em
   `server/web_dist`; ele sincroniza `SERVER_BUILD_ID` sozinho a partir do
   carimbo local.
3. `bash scripts/executar.sh --testes` — DEPOIS da publicação, para que os
   guardiões de carimbo/bundle meçam o `web_dist` novo.
4. Conferir os TRÊS elos do carimbo: `web/src/version.js`,
   `server/app/main.py:SERVER_BUILD_ID` e o bundle dentro de `server/web_dist`
   carregam o MESMO `BUILD_ID`.
5. Conferir as duas listas de `<assinaturas_no_bundle>` dentro de
   `server/web_dist`, com `grep -rlI`:
   - as SEIS assinaturas de presença (`x proximity`, `--shadow-fab`, `#f59e0b`,
     `Varredura automática de hoje`, `Stop/alvo (IA)`, `chave configurada`) —
     cada uma retorna ao menos um arquivo;
   - os NOVE glifos removidos (`🎓 📈 ✨ ✅ 📡 🟢 🟡 ⚪ 🔴`) — **cada um retorna
     VAZIO**. Esta é a checagem que prova que a REMOÇÃO chegou ao bundle, não só
     que a adição chegou.

Não editar `server/web_dist` à mão. Não editar `SERVER_BUILD_ID` à mão (o script
sincroniza; edição manual só existe em deploy só-backend, que não é o caso).
**Não dar `git push`** — ver `<risco_herdado>`.

Registrar no SUMMARY o carimbo gerado e a contagem de testes das duas suítes.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `git diff web/src/version.js` mostra um `BUILD_ID` novo no formato `F10-AAAAMMDD-NN`, diferente de `F10-20260905-03`.
    - `git status --porcelain server/web_dist` mostra arquivos modificados (a publicação de fato aconteceu).
    - Os três elos batem: `grep -o 'F10-[0-9]*-[0-9]*' web/src/version.js | head -1` é igual ao valor de `sed -n 's/.*SERVER_BUILD_ID = "\([^"]*\)".*/\1/p' server/app/main.py`, e `grep -rlI "$(grep -o 'F10-[0-9]*-[0-9]*' web/src/version.js | head -1)" server/web_dist | head -1` retorna ao menos um arquivo.
    - As seis assinaturas de presença retornam ao menos um arquivo cada em `grep -rlI ... server/web_dist`.
    - **`grep -rlI "🎓" server/web_dist`, e o mesmo para `📈 ✨ ✅ 📡 🟢 🟡 ⚪ 🔴`, retornam TODOS vazio** — os nove glifos saíram do bundle.
    - `bash scripts/executar.sh --testes` sai com código 0 DEPOIS da publicação (as duas suítes).
    - `git diff --stat web/package.json web/package-lock.json server/requirements.txt server/requirements-prod.txt` sai vazio.
    - `git diff --stat server/app/` lista SOMENTE `server/app/main.py`, e o diff dele é somente a linha de `SERVER_BUILD_ID` — esta fase não toca backend.
    - Nenhum `git push` foi executado (`git status -sb` mostra commits à frente de `origin`).
  </acceptance_criteria>
  <done>Carimbo novo nos três elos, `server/web_dist` republicado com as adições E as nove remoções dentro, suíte canônica verde após a publicação, nada empurrado para `origin`.</done>
</task>

<task type="auto">
  <name>Task 2: Remedir os três critérios contra o bundle de produção e fechar a calibragem de SYS-03</name>
  <read_first>
    - .planning/ROADMAP.md (seção da Phase 22 — os três critérios de sucesso, texto literal)
    - .planning/phases/22-componentes-compartilhados-trilho-cones-mascote/22-01-SUMMARY.md, 22-02-SUMMARY.md, 22-03-SUMMARY.md (o que já está provado por guardião, para não repetir trabalho nem reabrir o que está fechado)
    - .planning/phases/22-componentes-compartilhados-trilho-cones-mascote/22-UI-SPEC.md (seção SYS-03 — o critério visual literal: halo separa a silhueta sem virar disco/badge)
    - .planning/phases/21-duplica-o-removida-e-portf-lio-consolidado/21-04-SUMMARY.md (o padrão de handoff "Orchestrator Live Re-Verification" a seguir se o navegador não estiver disponível)
    - scripts/executar.sh (os modos `--prod` e `--stop`)
  </read_first>
  <files>web/src/App.jsx (somente se a calibragem de SYS-03 exigir ajuste do valor de PALETTE.light.shadowFab)</files>
  <action>
Subir `bash scripts/executar.sh --prod` (build de produção servido em `:8787`) e
remedir os TRÊS critérios do ROADMAP contra o bundle minificado, com uma conta
local logada.

**1. Trilho único (SYS-01)** — visitar as quatro telas que têm trilho
horizontal e, em cada uma, medir no DOM do container:
`getComputedStyle(el).scrollSnapType` e `getComputedStyle(primeiroItem).scrollSnapAlign`.
   - home/Acompanhar, carrossel de setups → esperado `x mandatory` / `center`;
   - Watchlist, filtro "MODELO DE ANÁLISE" → esperado `x proximity` / `start`;
   - Posições, tira "Oportunidades de opções" → `x proximity` / `start`;
   - Posições, linha de candidatos dentro do detalhe de uma posição com
     proposta → `x proximity` / `start`.
   Em 375×812, registrar TAMBÉM, para o filtro da Watchlist, quantos chips
   ficam visíveis (`clientWidth` do container ÷ largura do chip): esperado ~3,
   **não 1**. É a prova numérica de que a Decisão 1 foi respeitada e o peek de
   84% não vazou para fora do HERO. Se a tira de opções ou a linha de candidatos
   não tiver item hoje (conta sem proposta ativa), registrar como aberto e
   apontar que o snap está provado estaticamente pelo guardião do plano 22-01 —
   **não forjar dado**.

**2. Zero emoji, ícone no traço do NavIcon (SYS-02)** — percorrer Perfil,
Watchlist, Posições, Radar e a tela de configuração de chave, e registrar:
   - contagem de emoji visível: **zero** em todas as telas percorridas;
   - para cada um dos 9 sites, que o rótulo textual continua lá
     (`Estudo`, `Operador`, `Indicadores`, `Reanalisar`/`{btnAnalise}`,
     `Stop/alvo (IA)`, `chave configurada`, `Varredura automática de hoje`,
     `Forte`/`Moderada`/`Neutra`/`Fraca`) e que o ícone ao lado é um `<svg>` no
     DOM, não um nó de texto;
   - o `stroke` computado de pelo menos dois ícones em cada tema, confirmando
     que ele acompanha a cor do texto vizinho (é o efeito do `currentColor`) —
     legibilidade nos dois temas é o critério literal do ROADMAP;
   - o `<select>` de skill do Boris: as duas `<option>` leem
     `... · Estudo` / `... · Operador`, sem emoji;
   - o ponto de tier do Radar: `fill` computado do `<circle>` bate com a tabela
     (`#22c55e`/`#f59e0b`/`#9ca3af`/`#ef4444`) e o rótulo textual do tier
     aparece ao lado. Se nenhum ativo do Radar tiver `confluencia` hoje,
     registrar como aberto com a referência à prova estática do plano 22-03.

**3. Mascote separado do fundo (SYS-03) — a conferência que fecha a fase.**
Este item é o motivo desta task existir e **não pode ser pulado nem aproximado**
(o plano 22-03 entregou SYS-03 explicitamente como "implementado, calibragem
pendente"). Procedimento:
   - encontrar uma tela onde um card claro fique DIRETAMENTE atrás da posição
     fixa do FAB (canto inferior direito, `right:14px, bottom:92px`) — Posições
     e Watchlist com lista cheia servem;
   - **no tema ESCURO:** confirmar que a silhueta da coruja se separa do card
     atrás; registrar o valor computado de `--shadow-fab` (esperado
     `rgba(0,0,0,0.45)`, idêntico ao de antes da fase — o escuro não muda);
   - **no tema CLARO:** confirmar (a) que o halo separa a silhueta do card e
     (b) que NÃO aparece um "disco"/badge chapado ao redor da arte — a arte já
     tem transparência e contorno próprios, a sombra tem que ler como
     profundidade, não como fundo. Registrar o valor computado de
     `--shadow-fab` (partida: `rgba(15,20,28,0.22)`);
   - **se o tema claro estiver fraco demais (sem separação) ou pesado demais
     (borrão/disco):** ajustar SOMENTE o valor de `PALETTE.light.shadowFab` em
     `web/src/App.jsx`, refazer `bash scripts/publicar-web.sh` e
     `bash scripts/executar.sh --testes`, e reconferir. Registrar no SUMMARY o
     valor inicial, o valor final e a razão do ajuste. O guardião do plano
     22-03 só exige que `light` seja DIFERENTE de `dark` — qualquer valor
     calibrado passa.
   - Fazer as duas conferências também com o app em **Modo Operador**, já que o
     `MODE_OPERADOR` sobrescreve variáveis de tema por cima do dark/light — é
     uma passada de screenshot, não uma bateria nova.

Encerrar com `bash scripts/executar.sh --stop`.

**Se as ferramentas de navegador não estiverem disponíveis a você** (ver
`<ferramenta_de_navegador>`): NÃO tente improvisar, NÃO estime valores e NÃO
declare verificado o que não mediu. Em vez disso:
- faça a parte automatizável: suba `--prod`, confirme por `curl`/grep que
  `:8787` está servindo o bundle com o carimbo novo;
- **deixe o servidor NO AR** (não rode `--stop`), para o orquestrador reusar sem
  pagar outro ciclo de build;
- escreva no SUMMARY uma seção "Pendente de verificação ao vivo (orquestrador)"
  com os itens 1-3 acima como roteiro numerado, cada um com a tela, o
  seletor/propriedade computada a ler e o valor esperado — no mesmo formato que
  o `21-04-SUMMARY.md` usou;
- **para o item 3, incluir também o caminho de ajuste**: qual arquivo, qual
  chave (`PALETTE.light.shadowFab`), qual valor está lá, e que republicar exige
  `publicar-web.sh` + suíte de novo. Sem isso o orquestrador consegue julgar mas
  não consegue corrigir;
- diga explicitamente qual porta e qual carimbo o orquestrador vai encontrar.

No SUMMARY, montar uma tabela dos três critérios do ROADMAP × resultado, cada
linha com o número/valor computado medido ou com "aberto — roteiro no item N",
nunca com adjetivo. Marcar SYS-03 como FECHADO apenas se a conferência dos dois
temas tiver de fato acontecido.

Reafirmar no SUMMARY: nada foi enviado a `origin`; a decisão a/b/c sobre as
Fases 17/18/19 segue aberta com o Alex.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - O SUMMARY contém uma tabela dos 3 critérios de sucesso do ROADMAP com o resultado medido em `:8787` (produção) — cada linha com um valor computado/contagem medida, ou marcada explicitamente como aberta com roteiro, nunca com adjetivo.
    - Critério 1: `scrollSnapType` e `scrollSnapAlign` computados registrados para os 4 trilhos (ou o subconjunto exercitável, com os demais registrados como abertos), mais a contagem de chips visíveis do filtro da Watchlist em 375px (esperado ~3, não 1).
    - Critério 2: contagem de emoji visível igual a zero nas telas percorridas; os 9 rótulos textuais confirmados presentes; `stroke` computado de pelo menos dois ícones registrado em CADA tema; as duas `<option>` sem emoji.
    - Critério 3: valor computado de `--shadow-fab` registrado nos DOIS temas, com o veredito de cada um sobre separação × disco/badge. Se houve ajuste, o SUMMARY traz valor inicial, valor final e a razão — e a suíte foi rodada de novo após republicar.
    - Se as ferramentas de navegador não estiverem disponíveis: o SUMMARY contém a seção "Pendente de verificação ao vivo (orquestrador)" com roteiro numerado, porta, carimbo E o caminho de ajuste de `PALETTE.light.shadowFab`; o servidor foi deixado NO AR (nenhum `--stop` executado); e SYS-03 fica marcado como ABERTO, jamais como fechado.
    - Nenhum valor visual aproximado ou estimado aparece no SUMMARY.
    - `bash scripts/executar.sh --testes` sai com código 0 (rodado de novo se houve republicação).
    - O SUMMARY afirma explicitamente que nenhum `git push` foi feito e que a decisão a/b/c segue aberta.
  </acceptance_criteria>
  <done>Os três critérios do ROADMAP estão medidos contra o bundle de produção com valores registrados — ou entregues ao orquestrador como roteiro executável com o servidor já no ar — a calibragem de SYS-03 tem veredito nos dois temas, e o repositório segue sem push para `origin`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo local → `origin`/Railway | A publicação em `server/web_dist` é local; o deploy real acontece no push. Este plano para antes desse limite de propósito. |
| medição real → afirmação no SUMMARY | Um item visual declarado verificado sem ter sido medido contamina o registro da fase e a decisão do Alex depois. |
| build novo → build antigo em silêncio | Checagens de PRESENÇA de texto passam num bundle velho; só a checagem de AUSÊNCIA distingue. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-19 | Repudiation | Fase testada mas invisível em produção (falta de bump/publicação) | mitigate | Task 1 obriga `bump.sh` → `publicar-web.sh` → suíte, com os três elos do carimbo como critério de aceite |
| T-22-20 | Spoofing | Build falhar em silêncio e o `web_dist` antigo passar nas checagens de "texto novo presente" | mitigate | Critério de aceite exige os NOVE glifos AUSENTES do bundle — checagem que só um bundle novo passa |
| T-22-21 | Spoofing | Declarar SYS-03 fechado sem a conferência nos dois temas, herdando a pendência aberta pelo plano 22-03 | mitigate | Item 3 da Task 2 é obrigatório; critério de aceite exige `--shadow-fab` computado nos dois temas ou SYS-03 marcado ABERTO |
| T-22-22 | Elevation of privilege | Empurrar a `origin` as Fases 17/18/19 sem checkpoint aprovado, embutindo decisão do Alex numa task de publicação | mitigate | Proibição explícita de `git push`; critério de aceite verifica commits à frente de `origin` sem envio |
| T-22-23 | Spoofing | Item visual declarado verificado sem medição, por falta de ferramenta de navegador | mitigate | Task 2 tem caminho de handoff explícito, proíbe estimativa e exige a seção "Pendente de verificação ao vivo" com roteiro numerado e caminho de ajuste |
| T-22-24 | Tampering | Fase visual arrastar alteração de backend junto na publicação | mitigate | Critério de aceite limita o diff de `server/app/` à linha de `SERVER_BUILD_ID` |
| T-22-25 | Information disclosure | Conta local de teste e SQLite local entrarem no commit | mitigate | Critério de aceite limita o diff a `web/src/version.js`, `web/src/App.jsx`, `server/app/main.py` e `server/web_dist` |
| T-22-SC | Tampering | instalações npm/pip/cargo | mitigate | Zero pacote novo na fase inteira; `git diff` dos quatro manifests vazio como critério de aceite |
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` verde DEPOIS da publicação.
- Três elos do carimbo coerentes.
- Seis assinaturas presentes no bundle E os nove glifos ausentes dele.
- Diff de `server/app/` limitado à linha de `SERVER_BUILD_ID`.
- Três critérios do ROADMAP medidos em `:8787`, ou registrados como abertos com roteiro e servidor no ar.
- `--shadow-fab` computado nos dois temas, com veredito — ou SYS-03 marcado ABERTO.
- Nenhum push para `origin`.
</verification>

<success_criteria>
A unificação da Fase 22 está publicada, provada no bundle de produção (inclusive
pela ausência dos nove emojis) e travada por guardião na suíte canônica; a
calibragem visual do mascote tem veredito humano nos dois temas em vez de ser
presumida; e a decisão de enviar a `origin` (que arrasta as Fases 17/18/19)
fica registrada como aberta, sem ser tomada por um agente.
</success_criteria>

<output>
Create `.planning/phases/22-componentes-compartilhados-trilho-cones-mascote/22-04-SUMMARY.md` when done
</output>
