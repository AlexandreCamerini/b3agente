---
phase: 04-corre-o-m-dio-storyline-ux
plan: 06
type: execute
wave: 3
depends_on: ["04-04", "04-05"]
files_modified:
  - web/src/finance.js
  - web/src/api.js
  - web/src/persistence.js
  - web/src/App.jsx
  - web/tests/test_finance.mjs
  - web/tests/test_benchmark_curva.mjs
autonomous: true
requirements: [FIX-C03]
must_haves:
  truths:
    - "O Passo 8 mostra o retorno da carteira ao lado do retorno do Ibovespa no mesmo período"
    - "O usuário vê um número que responde 'foi bom ou ruim?': a diferença vs. Ibovespa"
    - "Quando o Ibovespa não vem, a curva da carteira continua igual e o app diz que a comparação está indisponível"
  artifacts:
    - path: "web/src/finance.js"
      provides: "equityCurve devolve datas + benchmarkSerie() alinha o índice por data"
      exports: ["equityCurve", "benchmarkSerie"]
    - path: "web/src/App.jsx"
      provides: "CapitalCurve com 2ª série tracejada, legenda e célula VS. IBOVESPA"
      contains: "IBOVESPA"
    - path: "web/tests/test_benchmark_curva.mjs"
      provides: "guardião da 2ª série e do modo de falha"
  key_links:
    - from: "web/src/App.jsx CapitalCurve"
      to: "GET /api/benchmark/ibov"
      via: "store.benchmarkIbov(period) nos dois stores"
      pattern: "benchmarkIbov"
    - from: "web/src/App.jsx CapitalCurve"
      to: "finance.benchmarkSerie"
      via: "alinhamento por data + path tracejado"
      pattern: "benchmarkSerie"
---

<objective>
Fechar FIX-C03: a curva de capital do Passo 8 passa a mostrar o Ibovespa ao lado
do resultado da carteira simulada, com o número que de fato responde à pergunta
do Core Value ("foi bom ou ruim?").

Purpose: `equityCurve` (`finance.js`) calcula `retAcum`/`drawdown` só sobre a
própria carteira; sem referência externa o usuário leigo não consegue avaliar o
próprio resultado — exatamente o raciocínio que o produto promete ensinar.

Output: aritmética pura de alinhamento por data em `finance.js`, cliente HTTP nos
dois stores, `CapitalCurve` com segunda série, legenda, quarta célula de KPI e
degradação explícita.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-corre-o-m-dio-storyline-ux/04-CONTEXT.md
@.planning/phases/04-corre-o-m-dio-storyline-ux/04-UI-SPEC.md
@.planning/phases/04-corre-o-m-dio-storyline-ux/04-04-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
<!-- Citações verificadas contra o fonte vivo em 2026-08-21. RE-GREP mesmo assim. -->

`web/src/finance.js`:
- `equityCurve(snapshots, budget, livePatr, todayYmd)` linhas 70-108. `snapshots` é
  `[{data: "YYYY-MM-DD", patrimonio: number, base?: number}]`. Retorna hoje
  `{curve, series, days, retAcum, drawdown, base, end}`. `curve` é
  `[base, ...plot]` quando `b > 0`; o ÚLTIMO ponto de `plot` reflete o patrimônio
  ao vivo (substitui o snapshot de hoje quando `last.data === todayYmd`, senão é
  anexado).
- `portfolioMetrics(...)` linhas 38-59.

`web/src/App.jsx`:
- `function CapitalCurve({ ctx })` linhas 1571-1643.
  `const P = usePalette()` (linha 1575 — hex já mesclado; SVG neste WebKit não
  resolve `var(--x)` de forma confiável, por isso `P.*` e não `T.*` dentro do SVG),
  `gid` do gradiente (1576), `ec = equityCurve(...)` (1582), montagem do `path`
  normalizado ao `viewBox 300x92` (1590-1598) com `y = 84 - ((v - min)/span) * 72`,
  helper `stat(label, value, color)` (1599-1604) em 9.5px `T.textFaint` + valor
  MONO 14px, título "PATRIMÔNIO SIMULADO" (1607), `<svg viewBox="0 0 300 92">`
  (1613), linha de base `y=84` (1620), área+linha da carteira (1622-1626),
  placeholder tracejado do estado vazio (1627), linha de 3 células
  `RETORNO ACUMULADO` / `DRAWDOWN (DESDE O PICO)` / `DIAS REGISTRADOS` (1631-1635).
- Segundo consumo de `equityCurve` na linha 1680 e um terceiro na 7766 — NÃO
  precisam mudar, mas confirme que continuam compilando após a mudança de retorno.

`web/src/api.js`: padrão de método — `history: (t, period) => req("GET", "/api/history/" + ... , undefined, 15000)`.
`TIMEOUT_MS = 15000` para chamadas normais.

`web/src/persistence.js`: `serverStore` (linha 97) e `deviceStore` (linha 223) —
todo método de rede pass-through existe nos DOIS; guardião `web/tests/test_api_parity.mjs`.

Contrato da rota (fixado pelo Plano 04-04):
`GET /api/benchmark/ibov?period=<periodo>` →
200 `{"t":"^BVSP","nome":"Ibovespa","fonte":"yahoo","candles":[{"date":"YYYY-MM-DD","close":float}],"asOf":"YYYY-MM-DD"}`
502 `{"detail":"Comparação com o Ibovespa indisponível agora."}`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Aritmética pura do benchmark alinhado por data (finance.js)</name>
  <files>web/src/finance.js, web/tests/test_finance.mjs</files>
  <behavior>
    - `equityCurve` passa a devolver `datas`, array do MESMO comprimento de `curve`: `null` no ponto-base (o baseline do orçamento não tem data), a `data` de cada snapshot nos demais, e `todayYmd` no ponto ao vivo anexado.
    - `benchmarkSerie(candles, datas)` devolve `null` quando `candles` tem menos de 2 velas utilizáveis ou quando menos de 2 datas ficam cobertas.
    - Para cada `data` não-nula, usa o fechamento do índice NAQUELA data; se o índice não negociou naquele dia, usa o último fechamento REAL anterior (não é valor inventado: é onde o índice estava). Sem nenhum fechamento anterior, o ponto é `null`.
    - `pct[i]` é a variação percentual desde o primeiro ponto COBERTO, não desde o primeiro candle recebido.
    - O ponto-base (`datas[0] === null`) recebe `0` quando existe ponto coberto adiante — os dois começam em 0% no mesmo x, como manda o UI-SPEC.
    - `retAcum` do benchmark é o último `pct` não-nulo; `null` quando não há nenhum.
    - Nenhum `NaN`, `Infinity` ou `-0` sai da função (fechamento zero/negativo é descartado como vela inutilizável).
  </behavior>
  <read_first>
    - web/src/finance.js (arquivo inteiro, 191 linhas — em especial `equityCurve` 70-108 e o comentário sobre por que a base não pode ser um campo de formulário)
    - web/tests/test_finance.mjs (arquivo inteiro — padrão `ok(...)`/`near(...)` e os casos já travados de `equityCurve`)
    - .planning/phases/04-corre-o-m-dio-storyline-ux/04-UI-SPEC.md (seção "FIX-C03", itens 1 e 5)
    - CLAUDE.md (princípio 5: cálculo determinístico; princípio 4: nunca invente valor)
  </read_first>
  <action>
Escreva PRIMEIRO os casos em `web/tests/test_finance.mjs` (bloco novo, abaixo dos
de `equityCurve`), depois implemente em `web/src/finance.js`.

1. `equityCurve` ganha `datas` no objeto de retorno. Mudança ADITIVA: não altere
   nem reordene nenhuma chave existente (`curve`, `series`, `days`, `retAcum`,
   `drawdown`, `base`, `end`) — três call sites em `App.jsx` dependem delas.
   Monte `datas` exatamente em paralelo com a construção de `plot` (linhas 89-98):
   mesma decisão de substituir-o-snapshot-de-hoje × anexar; `null` na posição do
   ponto-base quando `b > 0`.

2. Exporte `benchmarkSerie(candles, datas)` retornando
   `{ pct: (number|null)[], retAcum: number|null, cobertura: number }` ou `null`.
   - `cobertura` = quantos pontos ficaram com valor (serve para a UI decidir se
     vale desenhar).
   - Função PURA: sem `fetch`, sem `Date.now()`, sem estado de módulo. É a mesma
     disciplina de `portfolioMetrics`/`sizingPlano` — I/O fica no chamador.
   - Construa um índice `data -> close` a partir de `candles` e uma lista de datas
     ORDENADA para a busca do último fechamento anterior. Descartar vela com
     `close` não-numérico, `<= 0` ou `date` ausente.
   - Guard clauses no topo (padrão desta base): entrada inválida devolve `null`,
     nunca lança.

Não toque em `portfolioMetrics`, `dayReturnPct`, `sizingPlano` nem
`historicoEstado`.
  </action>
  <verify>
    <automated>cd web && node tests/test_finance.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `equityCurve(snaps, 10000, 10500, "2026-08-21").datas.length === equityCurve(...).curve.length` em pelo menos 3 combinações (com base, sem base, com ponto ao vivo anexado).
    - Todos os casos de `equityCurve` que já existiam em `test_finance.mjs` continuam passando sem edição.
    - `benchmarkSerie([], ["2026-08-20"])` === `null`; `benchmarkSerie(null, null)` === `null`.
    - Data sem pregão no índice → usa o fechamento REAL anterior (teste com uma vela em 2026-08-19 e data de carteira em 2026-08-20 sem vela: `pct` no ponto usa o close de 19).
    - Data anterior a qualquer vela → aquele `pct` é `null` (não 0, não extrapolado).
    - `pct` do primeiro ponto coberto é exatamente `0`.
    - `retAcum` bate com `(ultimoClose - primeiroCloseCoberto) / primeiroCloseCoberto * 100` até 1e-9.
    - Vela com `close: 0` ou `close: -5` é descartada e não produz `Infinity`/`NaN` em nenhum `pct`.
    - `grep -n "fetch\|Date.now\|localStorage" web/src/finance.js` continua sem retorno (o módulo segue puro).
  </acceptance_criteria>
  <done>`finance.js` sabe alinhar a série do índice às datas reais da carteira, sem inventar ponto e sem impureza; os casos novos e todos os antigos passam.</done>
</task>

<task type="auto">
  <name>Task 2: Cliente HTTP do benchmark nos dois stores (paridade)</name>
  <files>web/src/api.js, web/src/persistence.js</files>
  <read_first>
    - web/src/api.js (linhas 230-260: bloco de métodos de leitura — `history`, `conceitos`, `kbBuscar`, `technicals` — e `TIMEOUT_MS`; e o `enrichErrorMessage` para entender como o `detail` do 502 chega ao `e.message`)
    - web/src/persistence.js (linhas 97-200: superfície do `serverStore`; linhas 145-165: onde os pass-throughs de leitura ficam; e a mesma região do `deviceStore`)
    - web/tests/test_api_parity.mjs (o guardião)
    - CLAUDE.md (guardrail: método novo entra nos DOIS stores)
    - .planning/phases/04-corre-o-m-dio-storyline-ux/04-04-SUMMARY.md (contrato exato da rota)
  </read_first>
  <action>
Adiciona o acesso à rota `GET /api/benchmark/ibov`.

1. `web/src/api.js`: método
   `benchmarkIbov: (period) => req("GET", "/api/benchmark/ibov" + (period ? "?period=" + encodeURIComponent(period) : ""), undefined, TIMEOUT_MS)`
   posicionado junto dos outros métodos de leitura de dado de mercado
   (perto de `history`/`technicals`). Use `TIMEOUT_MS` (15s), não `TIMEOUT_LLM`:
   não é chamada de IA.

2. `web/src/persistence.js`: `benchmarkIbov: (period) => api.benchmarkIbov(period)`
   no `serverStore` E no `deviceStore`. É pass-through nos dois (dado público de
   mercado, sem estado local) — mas TEM que existir nos dois, senão o app nativo
   quebra ao abrir o Passo 8. É o guardrail de paridade do CLAUDE.md, verificado
   por `test_api_parity.mjs`.

Nenhuma outra mudança neste arquivo. Não adicione cache no cliente: o servidor
já cacheia 15 min (`benchmark.py`), e um segundo cache aqui criaria duas noções
de frescor para o mesmo dado.
  </action>
  <verify>
    <automated>cd web && node tests/test_api_parity.mjs && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "benchmarkIbov" web/src/api.js` == 1 e `grep -c "benchmarkIbov" web/src/persistence.js` == 2 (um em cada store).
    - O método de `api.js` usa `TIMEOUT_MS`, não `TIMEOUT_LLM`.
    - O `period` é passado por `encodeURIComponent`.
    - `test_api_parity.mjs` verde.
    - `npx vite build` em `web/` conclui sem erro.
  </acceptance_criteria>
  <done>Os dois stores expõem `benchmarkIbov(period)` com o mesmo contrato, sem cache duplicado.</done>
</task>

<task type="auto">
  <name>Task 3: Segunda série, legenda e célula VS. IBOVESPA no CapitalCurve (FIX-C03, UI)</name>
  <files>web/src/App.jsx, web/tests/test_benchmark_curva.mjs</files>
  <read_first>
    - .planning/phases/04-corre-o-m-dio-storyline-ux/04-UI-SPEC.md (seção "FIX-C03" INTEIRA — os 5 itens do contrato visual, com cor, dash, z-order, legenda, célula nova e modo de falha)
    - web/src/App.jsx (linhas 1567-1645: `CapitalCurve` completo, incluindo o comentário do qa/29 sobre `usePalette()` no SVG)
    - web/src/App.jsx (grep `usePalette` para confirmar quais chaves `P.*` estão disponíveis, em especial `P.textDim`)
    - web/src/finance.js (o `benchmarkSerie` da Task 1)
    - web/src/persistence.js (o `benchmarkIbov` da Task 2)
    - web/tests/test_chart_colors_theme_aware.mjs (guardião existente de cor de gráfico — confirme que continua verde e siga a mesma convenção `P.*` dentro do SVG)
  </read_first>
  <action>
Implementa o contrato visual do UI-SPEC para FIX-C03 dentro de `CapitalCurve`.

Busca do dado: `useEffect` que chama `store.benchmarkIbov(period)` uma vez por
montagem (`period` = `(data.config || {}).candlePeriod`), com estado local
`const [ibov, setIbov] = useState(null)` e `const [ibovErro, setIbovErro] = useState(false)`.
O `catch` seta `ibovErro` e NÃO derruba nada — a curva da carteira é
independente. Use o padrão de efeito com flag `alive` que o arquivo já usa (ver
`ConceitoSheet`, linhas 2396-2404).

`const bm = ibov ? benchmarkSerie(ibov.candles, ec.datas) : null;`

Escala compartilhada: converta a curva da carteira para % desde `ec.base`
(`pctCarteira[i] = ec.base > 0 ? ((ec.curve[i] - ec.base) / ec.base) * 100 : 0`) e
calcule `min`/`max`/`span` sobre a UNIÃO de `pctCarteira` e dos `bm.pct` não-nulos.
Duas séries só são comparáveis na MESMA escala. Mantenha a mesma fórmula de
mapeamento de hoje (`y = 84 - ((v - min) / span) * 72`) e o mesmo `viewBox`.
Quando `bm` é `null`, a escala usa só `pctCarteira` — e como a transformação de
patrimônio para % é afim e monotônica, a curva desenhada fica visualmente
IDÊNTICA à de hoje (nenhuma regressão visual no caminho sem benchmark).

Segunda série (item 1 do UI-SPEC): `<path d={ibovPath} fill="none" stroke={P.textDim} strokeWidth="1.5" strokeDasharray="3 3" />`.
Sem área, sem gradiente. `P.textDim` (neutro, já existente) — deliberadamente
NÃO `P.positive`/`P.negative`/`P.accent`: a cor semântica pertence ao resultado do
usuário; o benchmark é contexto. O tracejado é o segundo diferenciador, que
funciona em escala de cinza e para daltônicos. Ponto `null` INTERROMPE o path
(reinicia com `M` no próximo ponto válido) — nunca ligue por cima de um buraco.

Z-order (item 2): o path do Ibovespa vem ANTES do path+área da carteira no JSX,
para a carteira ficar por cima em qualquer cruzamento.

Legenda (item 3): duas `<span>` inline logo abaixo da linha do título
"PATRIMÔNIO SIMULADO" (linha 1607), 10px `T.textMuted`, reusando o registro do
helper `stat()` (letter-spacing 0.04em): amostra de linha SÓLIDA 14px em
`up ? T.positive : T.negative` + rótulo `sua carteira`; amostra TRACEJADA em
`T.textDim` + rótulo `Ibovespa`. Sem overlay flutuante no gráfico — esse padrão
de interação não existe neste componente e não é hora de introduzi-lo.

Quarta célula (item 4): na linha de `stat(...)` (1631-1635), acrescenta
`stat("VS. IBOVESPA", <diferença formatada com sinal>, dif >= 0 ? T.positive : T.negative)`
onde `dif = ec.retAcum - bm.retAcum`. Renderiza SOMENTE quando `bm && bm.retAcum != null`.
Este é o número que responde "foi bom ou ruim?" — a curva sozinha não responde.
Reuse o helper `pct(...)` de formatação já usado nas outras células.

Modo de falha (item 5): quando `ibovErro` (ou `bm === null` com `ibov` carregado),
NÃO esconda o gráfico e NÃO desenhe linha reta fabricada. Renderiza a curva da
carteira exatamente como hoje, omite série/legenda/célula e mostra UMA linha 11px
`T.textFaint` abaixo do gráfico com a copy verbatim do UI-SPEC:
`Comparação com o Ibovespa indisponível agora.`
(É a mesma frase que o backend devolve no 502 — texto único.)

Estado vazio (`!hasSeries`): nada muda. O placeholder tracejado da linha 1627
continua igual e a busca do benchmark não precisa nem acontecer.

Cria `web/tests/test_benchmark_curva.mjs`: guardião estático travando (a) o path
do Ibovespa usa `strokeDasharray` e `P.textDim`, e NÃO usa `P.positive`/
`P.negative`/`P.accent`; (b) o path do Ibovespa aparece ANTES do path da carteira
no fonte (z-order); (c) a célula `VS. IBOVESPA` existe e é condicional; (d) a
frase de indisponibilidade é verbatim e usa `T.textFaint`, não `T.negative`;
(e) não há nenhuma constante de "linha plana"/valor default para o benchmark
(busque por `|| 0` no ramo do benchmark — não pode existir).
  </action>
  <verify>
    <automated>cd web && node tests/test_benchmark_curva.mjs && node tests/test_chart_colors_theme_aware.mjs && node tests/test_finance.mjs && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `CapitalCurve` contém `strokeDasharray="3 3"` e `stroke={P.textDim}` no path do benchmark.
    - No bloco do benchmark NÃO aparecem `P.positive`, `P.negative` nem `P.accent`.
    - O índice da string do path do Ibovespa é MENOR que o índice do `<path d={path}` da carteira dentro da função (z-order verificado por posição).
    - `grep -c "VS. IBOVESPA" web/src/App.jsx` == 1 e a célula está sob condição que exige `retAcum != null` do benchmark.
    - `grep -c "Comparação com o Ibovespa indisponível agora." web/src/App.jsx` == 1, com `T.textFaint`.
    - Nenhum `|| 0` / `?? 0` no caminho que produz `bm.pct` ou o path do benchmark (nada de linha plana fabricada).
    - `benchmarkSerie` é importado de `./finance.js` no topo do `App.jsx` (mesma linha de import de `equityCurve`, linha 15).
    - `test_chart_colors_theme_aware.mjs` continua verde.
    - `npx vite build` em `web/` conclui sem erro.
  </acceptance_criteria>
  <done>O Passo 8 mostra a carteira e o Ibovespa na mesma escala, com a diferença explicitada num KPI, e degrada com uma frase única quando o índice não vem — sem nunca desenhar um dado que não existe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → cálculo do front | `candles` do benchmark vêm da rede e entram numa aritmética exibida como resultado financeiro |
| erro de rede → tela | falha do Ibovespa não pode contaminar a leitura do patrimônio do usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-18 | Tampering | `benchmarkSerie` recebendo candles adulterados/malformados | mitigate | guard clauses descartam vela sem `date`, com `close` não-numérico ou `<= 0`; entrada inválida devolve `null` em vez de lançar ou produzir `NaN` |
| T-04-19 | Information Disclosure | mistura do benchmark com o patrimônio do usuário | mitigate | as duas séries são calculadas separadas e só compartilham a ESCALA de exibição; `equityCurve` não muda de fórmula, e o guardião de `test_finance.mjs` trava os valores antigos |
| T-04-20 | Denial of Service | busca do benchmark a cada render | mitigate | uma chamada por montagem (`useEffect` com flag `alive`), sem retry automático; servidor cacheia 15 min |
| T-04-21 | Repudiation | benchmark indisponível renderizado como resultado ruim/bom | mitigate | célula `VS. IBOVESPA` só existe com `retAcum != null`; ponto sem cobertura interrompe o path; guardião proíbe `|| 0` no caminho |
| T-04-SC | Tampering | instalação de pacotes | accept | nenhum pacote novo em `web/package.json` |
</threat_model>

<verification>
- `cd web && npx vite build` — obrigatório.
- `bash scripts/executar.sh --testes` — suíte canônica verde antes de fechar o plano.
- Verificação ao vivo recomendada: com o backend rodando, abrir o Passo 8 com o Ibovespa OK e depois com a rota forçada a 502, confirmando que a curva da carteira não muda de forma no segundo caso.
</verification>

<success_criteria>
- `equityCurve` devolve `datas` sem quebrar nenhum dos três call sites nem os testes antigos.
- `benchmarkSerie` alinha por data real, nunca extrapola e nunca produz `NaN`.
- `benchmarkIbov` existe nos dois stores.
- `CapitalCurve` desenha a segunda série tracejada por baixo da carteira, com legenda e a célula `VS. IBOVESPA`, e degrada com a frase única.
- Suíte canônica verde e `npx vite build` verde.
</success_criteria>

<output>
Create `.planning/phases/04-corre-o-m-dio-storyline-ux/04-06-SUMMARY.md` when done
</output>
