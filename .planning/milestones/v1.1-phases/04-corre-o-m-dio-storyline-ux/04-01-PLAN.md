---
phase: 04-corre-o-m-dio-storyline-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/conceitos.py
  - server/app/kb.py
  - server/app/explicacao_det.py
  - server/tests/test_diversificacao.py
  - server/tests/test_explicacao_det.py
autonomous: true
requirements: [FIX-C01, FIX-C05]
must_haves:
  truths:
    - "O conceito 'diversificação' existe no produto e responde em Estudo e Operador"
    - "Existe um compositor determinístico capaz de explicar um ativo sem nenhuma chamada de LLM"
    - "A explicação determinística nunca inventa número: campo ausente some do texto"
  artifacts:
    - path: "server/app/conceitos.py"
      provides: "conceito 'diversificacao' (naoAcontece/oQueE/oQueAcontece/veja)"
      contains: "\"diversificacao\""
    - path: "server/app/kb.py"
      provides: "verbete 'diversificacao' derivado de conceitos (família plano_risco)"
      contains: "\"diversificacao\": \"plano_risco\""
    - path: "server/app/explicacao_det.py"
      provides: "montar() — explicação determinística a partir do snapshot técnico"
      exports: ["montar"]
      min_lines: 80
    - path: "server/tests/test_explicacao_det.py"
      provides: "guardião do compositor determinístico"
    - path: "server/tests/test_diversificacao.py"
      provides: "guardião do verbete novo"
  key_links:
    - from: "server/app/kb.py"
      to: "server/app/conceitos.py"
      via: "_FAMILIA_DO_CONCEITO -> _de_conceito -> conceitos.montar"
      pattern: "_FAMILIA_DO_CONCEITO"
    - from: "server/app/explicacao_det.py"
      to: "server/app/kb.py"
      via: "kb.verbete/kb.formatar para o texto do setup detectado"
      pattern: "kb\\.(verbete|formatar|buscar)"
---

<objective>
Criar as DUAS fontes determinísticas que o resto da Fase 4 consome: o conceito
"diversificação" (FIX-C05, hoje com ZERO ocorrência no produto inteiro) e o
compositor `explicacao_det.montar()` que produz a explicação do Passo 7 quando a
IA não está disponível (FIX-C01).

Purpose: FIX-C01 e FIX-C05 partem da mesma fonte 0-custo (`conceitos.py`/`kb.py`,
per CONTEXT.md "Specific Ideas"). Este plano entrega só o motor — a fiação nas
rotas é do Plano 04-04, a UI é dos planos de front. Nada aqui chama LLM.

Output: `server/app/explicacao_det.py` (novo), conceito+verbete `diversificacao`,
dois guardiões.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-corre-o-m-dio-storyline-ux/04-CONTEXT.md
@CLAUDE.md
@.claude/skills/didatica-boris/SKILL.md
</context>

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do fonte vivo em 2026-08-21. -->

`server/app/conceitos.py` — cada entrada de `CONCEITOS` tem a forma:
`{"titulo": {"educacional": str, "operador": str}, "campos": tuple[str, ...],
  "naoAcontece": [str|((estado,...), str)], "oQueE": [...], "oQueAcontece": [...],
  "veja": [ids]}`.
`montar(cid, modo, dados, resumido)` interpola SÓ as chaves listadas em `campos`
(allowlist); parágrafo com placeholder de campo ausente é DESCARTADO
(`_render`), nunca renderizado com lacuna.

`server/app/kb.py` — `_FAMILIA_DO_CONCEITO: dict[cid -> familia]` alimenta
`_conceitos_verbetes()`, que chama `_de_conceito(cid)` e deriva o texto de
`conceitos.montar(cid, modo, None)`. Famílias válidas (9): "indicadores",
"estrutura", "familias", "modelos", "setups", "plano_risco", "fundamentos",
"mercado_b3", "estados_app".
Funções públicas: `catalogo()`, `verbete(vid)`, `buscar(pergunta, limite)`,
`formatar(v, modo)`, `resolver(pergunta, modo)`.

`server/app/technical_snapshot.get(...)` devolve o snapshot cujo `["context"]`
carrega (chaves relevantes aqui): `trend.bias`, `volatility.atr14`,
`volatility.atr14Pct`, `volatility.bollingerUpper/Lower`, `levels.supports`,
`levels.resistances`, `historyStats.low63/high63`, `dataQuality`,
`setupsRadar = {"veredito", "confluencia", "melhorSetup", "setups"}`.
O snapshot também tem `asOf` (última barra FECHADA), `barraEmFormacao`,
`periodBars`, `period`, `snapshotId`.

`server/app/setups.avaliar(...)` produz `veredito` ∈
{"Estudar alta", "Estudar baixa", "Monitorar", "Sem setup no momento"} e cada
setup em `setups` tem `nome`, `lado`, `confluencia`, `gatilho`, `invalidacao`,
`alvoSugerido`, `referencia`, `criterios`, `aposentado`, `historico`.

Guardião JÁ EXISTENTE que este plano precisa satisfazer (não é opcional):
`server/tests/test_kb_espelho.py` critério 4 — "cada id de `conceitos.CONCEITOS`
precisa ter verbete com o MESMO id na KB". Adicionar o conceito SEM registrar em
`_FAMILIA_DO_CONCEITO` quebra a suíte.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Conceito + verbete "diversificacao" (FIX-C05, camada determinística)</name>
  <files>server/app/conceitos.py, server/app/kb.py, server/tests/test_diversificacao.py</files>
  <read_first>
    - server/app/conceitos.py (linhas 40-310: comentário da allowlist `campos`, CONCEITOS["stop"] e CONCEITOS["r"] como modelo de forma; linhas 363-430: `_valores`/`_render`/`montar`)
    - server/app/kb.py (linhas 60-145: `_FAMILIA_DO_CONCEITO`, `_de_conceito`, `_conceitos_verbetes`; linhas 919-978: `_PLANO_RISCO_EXTRA` como modelo de prosa de família plano_risco)
    - server/tests/test_kb_espelho.py (docstring: os 4 critérios de espelho)
    - server/tests/test_conceitos.py (os guardiões que a copy nova tem que passar: `test_todo_placeholder_do_catalogo_tem_de_onde_vir`, `test_catalogo_generico_nao_carrega_numero_de_ativo_nenhum`, `test_veja_so_aponta_para_conceito_que_existe`, `test_primeira_resposta_e_que_o_app_nao_compra_nada`)
    - server/tests/test_kb.py (`test_modo_educacional_sem_verbo_de_ordem_obvio`, `test_nenhum_texto_usa_expressao_proibida_fora_de_negacao`)
    - .claude/skills/didatica-boris/SKILL.md (vocabulário por modo)
  </read_first>
  <action>
Implementa FIX-C05 (lado backend) per decisão do CONTEXT.md: "verbete novo em
`server/app/kb.py` (mesmo padrão dos demais) + aviso na tela de Carteira".

Em `server/app/conceitos.py`, adiciona a entrada `"diversificacao"` ao dict
`CONCEITOS`, na seção de risco (junto de `stop`/`alvo`/`r`). Por que em
`conceitos.py` e não só em `kb.py`: o drill-down "saiba mais" da UI (`ConceitoSheet`
→ `POST /api/conceito/{cid}`) lê `conceitos.CONCEITOS`; `kb.py` hoje só é
alcançável pelo assistente e por `/api/kb/buscar`, e `App.jsx` não chama
`kbBuscar` em lugar nenhum (grep confirma). Registrando em `conceitos.py`, o
verbete de `kb.py` nasce de graça via `_de_conceito` — que é exatamente a regra
D4 do módulo ("REFERÊNCIA, NÃO CÓPIA").

Forma da entrada, seguindo o padrão dos vizinhos:
- `titulo`: {"educacional": "Diversificação", "operador": "Diversificação"}.
- `campos`: `("ticker", "pct")` — SÓ isso. Qualquer placeholder que você escrever
  no texto tem que estar nessa tupla, senão `test_todo_placeholder_do_catalogo_tem_de_onde_vir`
  falha. `_valores` aplica formatação pt-BR; campo ausente derruba o parágrafo
  inteiro (é o comportamento desejado: o catálogo genérico, chamado sem `dados`,
  NÃO pode carregar número de ativo nenhum — guardião
  `test_catalogo_generico_nao_carrega_numero_de_ativo_nenhum`).
- `naoAcontece`: primeiro parágrafo tem que dizer que o app não compra nem vende
  nada (guardião `test_primeira_resposta_e_que_o_app_nao_compra_nada`); segundo
  parágrafo deixa claro que diversificar não é garantia de lucro nem de proteção
  contra perda (princípio 6/8 do CLAUDE.md).
- `oQueE`: 1º parágrafo é a DEFINIÇÃO genérica (sem número — é ele que
  `resumido=True` corta); parágrafos seguintes ancorados em `{ticker}`/`{pct}`
  explicando o que concentração alta significa NAQUELA carteira.
- `oQueAcontece`: o que o app faz (mostra o aviso na Carteira quando um ativo
  passa de 50% do patrimônio simulado), o que NÃO faz (não bloqueia compra, não
  redistribui nada, não recomenda ativo) e a régua conceitual: risco de um único
  evento negativo derrubar a carteira inteira, relação com drawdown.
- `veja`: `["r", "stop"]` (ambos existem em CONCEITOS — link morto é filtrado,
  mas escrever id fantasma é bug).

Vocabulário: modo `educacional` descreve condição, NUNCA usa verbo de ordem
(comprar/vender/venda já/entre); modo `operador` fala como mesa mas continua sem
promessa de resultado. Os textos por modo saem dos ROTULOS/`titulo` — o corpo é
compartilhado, como nos conceitos vizinhos.

Em `server/app/kb.py`, adiciona `"diversificacao": "plano_risco"` ao dict
`_FAMILIA_DO_CONCEITO`. Nada mais: `_conceitos_verbetes()` já percorre esse dict.

Cria `server/tests/test_diversificacao.py` como guardião do achado C-05: prova
que "diversific" deixou de ter zero ocorrência no produto, que o conceito
responde nos dois modos, que o catálogo genérico não vaza número, e que o
verbete existe na KB com família `plano_risco`.
  </action>
  <verify>
    <automated>cd server && ./.venv/bin/python -m pytest -q tests/test_diversificacao.py tests/test_kb_espelho.py tests/test_conceitos.py tests/test_kb.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from app import conceitos; assert 'diversificacao' in conceitos.CONCEITOS"` passa (rodado de `server/`).
    - `kb.verbete("diversificacao")` retorna dict com `familia == "plano_risco"` e `texto` não-vazio nos dois modos.
    - `conceitos.montar("diversificacao", "educacional", None)` NÃO contém nenhum dígito no corpo dos três blocos (catálogo genérico sem número).
    - `conceitos.montar("diversificacao", "educacional", {"ticker": "PETR4", "pct": 63})` contém "PETR4" e "63".
    - `grep -rc "diversific" server/app/conceitos.py server/app/kb.py` retorna valor > 0 nos dois arquivos.
    - `tests/test_kb_espelho.py` continua verde (o critério 4 do espelho é o que prova que conceito e verbete não divergiram).
    - `tests/test_kb.py::test_modo_educacional_sem_verbo_de_ordem_obvio` continua verde com o texto novo no catálogo.
  </acceptance_criteria>
  <done>O conceito e o verbete "diversificacao" existem, respondem nos dois modos, passam todos os guardiões de vocabulário e de espelho existentes, e há um guardião próprio travando o achado C-05.</done>
</task>

<task type="auto">
  <name>Task 2: Compositor determinístico da explicação do Passo 7 (FIX-C01, motor)</name>
  <files>server/app/explicacao_det.py, server/tests/test_explicacao_det.py</files>
  <read_first>
    - server/app/technical_snapshot.py (linhas 55-175: o shape de `context`, `setupsRadar`, `asOf`, `barraEmFormacao`, `_compact_setups`)
    - server/app/setups.py (linhas 550-600: `avaliar()` — veredito canônico, campos de cada setup; linhas 585-620: MODEL_EXPLANATION)
    - server/app/kb.py (linhas 1501-1600: `catalogo`, `verbete`, `buscar`, `formatar`)
    - server/app/skill_ref.py (linhas 239-300: `TIMING`/`timing_txt`; linhas 384-400: `DIDATICA` — a ordem em que se ensina: indicador → correlação → leitura → o que muda a leitura)
    - server/app/main.py (linhas 2083-2130: `_pet_resumo_evolucao` — precedente de composição determinística em lista de frases, com números formatados e ressalva de idade do dado)
    - server/app/conceitos.py (linhas 309-400: helpers `_num`/`_emr`/`_pct`/`_rr` de formatação pt-BR — reuse, não reescreva a formatação)
  </read_first>
  <action>
Implementa o motor de FIX-C01 per decisão do CONTEXT.md: "quando a IA não
estiver disponível, montar uma explicação mínima determinística a partir do
setup/indicador já calculado, usando `conceitos.py`/`kb.py` — nunca um card
vazio ou só o erro técnico".

Cria `server/app/explicacao_det.py`, módulo PURO (zero I/O de rede, zero LLM,
zero acesso a `db`/`store`), com a função pública:

`montar(ticker: str, snap: dict, modo: str = "educacional", quote: dict | None = None) -> dict`

Retorno: `{"markdown": str, "fonte": "deterministico", "verbetes": list[str],
"semDados": bool, "asOf": str | None}`.

Estrutura do markdown, na ordem do `skill_ref.DIDATICA` (é o esqueleto
pedagógico canônico do produto — não invente outra ordem):
1. **O que o motor detectou agora** — `veredito` de `context["setupsRadar"]`
   (frase canônica de `setups.avaliar`, copiada verbatim — guardrail CVM do
   CLAUDE.md: a manchete vem SÓ do motor determinístico), nome do melhor setup e
   a confluência em %.
2. **O que cada indicador marca** — de `context`: viés de tendência
   (`trend.bias`), ATR(14) e ATR% (`volatility.atr14`/`atr14Pct`), suporte e
   resistência mais próximos (`levels.supports`/`resistances`), bandas de
   Bollinger. Cada linha só entra se o número EXISTE: campo `None`/ausente some
   da saída, nunca vira 0,0 nem "—" com número inventado (CLAUDE.md item 4 e
   convenção "null nunca 0.0").
3. **O que isso significa** — texto do verbete correspondente, obtido por
   `kb.buscar(nome_do_setup, limite=1)` + `kb.formatar(v, modo)`; se não achar
   verbete do setup, cai no verbete genérico da família (`kb.verbete("risco-rr")`
   / `kb.verbete("stop")`). Registra os ids usados em `verbetes` do retorno.
4. **O que mudaria a leitura** — `gatilho`/`invalidacao` do melhor setup, quando
   presentes.
5. **Idade do dado** — `snap["asOf"]` (última barra FECHADA) e, se
   `snap["barraEmFormacao"]` existir, a ressalva de que a barra em formação foi
   descartada. Frase no mesmo tom das já usadas em `_pet_resumo_evolucao`
   ("nunca é agora").

Vocabulário: `modo` escolhe entre educacional e operador; no educacional NENHUM
verbo de ordem, só descrição de condição. Não escreva vocabulário novo solto —
onde já existir frase canônica em `skill_ref`/`setups`/`kb`, interpole de lá.

Degradação: se `context["setupsRadar"]` não tem setup E nenhum indicador
utilizável estiver presente, retorna `{"markdown": "", "semDados": True, ...}`.
Quem decide o que mostrar nesse caso é a UI (Plano 04-05 renderiza "Não há dados
suficientes para uma explicação agora." — frase mandatória do CLAUDE.md).

NÃO adicione fallback de erro da LLM aqui: este módulo não sabe que a IA
existe. A mensagem de indisponibilidade é montada na rota (Plano 04-04) a partir
de `llm.public_error(e)`, para que nenhum detalhe cru do provedor (URL, chave,
stack) vaze no texto do usuário — ver `<threat_model>` T-04-01.

Cria `server/tests/test_explicacao_det.py` com snapshots sintéticos (dicts
montados no teste, sem rede): caso com setup completo, caso só com indicadores,
caso vazio (`semDados`), caso com `atr14=None` (a linha some, não vira 0,0),
caso em modo operador, e o guardião de vocabulário (educacional sem verbo de
ordem).
  </action>
  <verify>
    <automated>cd server && ./.venv/bin/python -m pytest -q tests/test_explicacao_det.py</automated>
  </verify>
  <acceptance_criteria>
    - `explicacao_det.montar("PETR4", snap_completo, "educacional")` devolve `markdown` com o veredito EXATO de `context["setupsRadar"]["veredito"]` (comparação de substring literal, não parafraseada).
    - Snapshot com `volatility.atr14 = None` → a string "0,0" e "0.0" NÃO aparecem em `markdown` e a linha de ATR está ausente.
    - Snapshot sem setups e sem indicadores → `semDados is True` e `markdown == ""`.
    - `fonte == "deterministico"` em todos os retornos.
    - `verbetes` contém pelo menos um id que `kb.verbete(id)` resolve (sem link morto).
    - `grep -n "llm\|httpx\|requests\|db\.\|store\." server/app/explicacao_det.py | grep -v '^\s*#'` não retorna nada (módulo puro).
    - Modo educacional: nenhuma das strings "compre", "venda agora", "entre agora" aparece em `markdown` (mesmo crivo do `test_kb.py::test_modo_educacional_sem_verbo_de_ordem_obvio`).
  </acceptance_criteria>
  <done>`explicacao_det.montar()` produz explicação didática completa a partir de um snapshot, degrada para `semDados` sem inventar número, e o guardião trava os 6 casos acima.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conteúdo → usuário | texto gerado pelo servidor vai direto pra tela do usuário; erro de provedor não pode vazar aqui |
| catálogo didático → assistente/LLM | `kb.catalogo()` alimenta prompt do assistente; texto novo entra no contexto de IA |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | `explicacao_det.montar` | mitigate | módulo não recebe nem formata exceção de LLM; mensagem de indisponibilidade é montada na rota via `llm.public_error(e)` (Plano 04-04), nunca `str(e)` |
| T-04-06 | Tampering | `conceitos.CONCEITOS["diversificacao"]` | mitigate | `campos` é allowlist `("ticker","pct")`; chamador não consegue injetar rótulo/vocabulário via `dados` (guardião existente `test_chamador_nao_reescreve_o_vocabulario_canonico`) |
| T-04-07 | Repudiation | texto didático novo no prompt do assistente | accept | conteúdo estático versionado em git, revisado por guardião de vocabulário; sem entrada de usuário no texto |
| T-04-SC | Tampering | instalação de pacotes | accept | este plano não instala nenhum pacote npm/pip (stdlib + módulos internos apenas) |
</threat_model>

<verification>
- `cd server && ./.venv/bin/python -m pytest -q` — suíte backend inteira verde (os guardiões de kb/conceitos são os que denunciam divergência).
- `bash scripts/executar.sh --testes` — suíte canônica (backend + web) verde antes de fechar o plano.
</verification>

<success_criteria>
- `conceitos.CONCEITOS` e o catálogo da KB contêm `diversificacao`; `test_kb_espelho.py` verde.
- `server/app/explicacao_det.py` existe, é puro, e `montar()` cobre os 6 casos do guardião.
- Zero regressão: suíte canônica verde.
- Nenhuma rota alterada neste plano (a fiação é do 04-04).
</success_criteria>

<output>
Create `.planning/phases/04-corre-o-m-dio-storyline-ux/04-01-SUMMARY.md` when done
</output>

