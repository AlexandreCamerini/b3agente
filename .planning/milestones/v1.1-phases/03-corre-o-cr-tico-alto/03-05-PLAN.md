---
phase: 03-corre-o-cr-tico-alto
plan: 05
type: execute
wave: 3
depends_on: ["03-02", "03-04"]
files_modified:
  - server/app/timing_watch.py
  - server/app/main.py
  - web-admin/src/api.js
  - web-admin/src/App.jsx
  - server/tests/test_fase3_timing_watch_kill.py
  - web/tests/test_fase3_admin_timing_watch.mjs
autonomous: true
requirements: [FIX-C35]

must_haves:
  truths:
    - "O estado do 2º kill-switch (push do gatilho, `timing_watch`) é visível no portal admin — aba Automação e KPI na Visão Geral"
    - "Um admin com `execucao_automatica.controlar` liga/desliga esse kill-switch em runtime, sem redeploy"
    - "O override sobrevive a reinício do processo (persistido em `admin_config`), no MESMO padrão memória→DB→env do kill-switch do agente"
    - "A env `B3_TIMING_PUSH_KILL` continua sendo lida AO VIVO quando não há override explícito — ligar a env no Railway continua funcionando"
    - "Toda transição do toggle fica registrada em `admin_audit_log`, como já acontece com o kill-switch do agente"
    - "Um admin sem `execucao_automatica.controlar` não vê o botão E não consegue alternar pela rota (o gate real é do backend)"
  artifacts:
    - path: "server/app/timing_watch.py"
      provides: "kill_switch_on com padrão memória→DB→env, set_kill_switch e configure_db"
      contains: "timingWatchKillSwitch"
    - path: "server/app/main.py"
      provides: "GET/PUT /api/admin/timing-watch/kill-switch + campo no /api/agent/status"
      contains: "timing-watch/kill-switch"
    - path: "web-admin/src/api.js"
      provides: "timingWatchKillSwitchGet/Put"
    - path: "web-admin/src/App.jsx"
      provides: "TimingWatchKillSwitchBox na aba Automação + KPI 'PUSH DO GATILHO' na Visão Geral"
      contains: "PUSH DO GATILHO"
    - path: "server/tests/test_fase3_timing_watch_kill.py"
      provides: "guardião: precedência memória→DB→env, persistência, RBAC das duas rotas, audit log"
    - path: "web/tests/test_fase3_admin_timing_watch.mjs"
      provides: "guardião estático do portal: box, KPI, confirm e gate de permissão presentes"
  key_links:
    - from: "server/app/main.py"
      to: "server/app/timing_watch.py"
      via: "timing_watch.set_kill_switch / kill_switch_on nas rotas admin e no /api/agent/status"
      pattern: "timing_watch\\.(set_)?kill_switch"
    - from: "server/app/timing_watch.py"
      to: "server/app/db.py"
      via: "db.admin_config_get/set com a chave timingWatchKillSwitch"
      pattern: "admin_config_(get|set)"
    - from: "web-admin/src/App.jsx"
      to: "/api/admin/timing-watch/kill-switch"
      via: "api.timingWatchKillSwitchGet / Put"
      pattern: "timingWatchKillSwitch"
---

<objective>
Dar ao 2º kill-switch do sistema — o que para o push do gatilho para TODA a
base (`timing_watch`) — a mesma visibilidade e o mesmo controle em runtime que
o kill-switch do Operador já tem desde o ADR-013.

Purpose: hoje `timing_watch.kill_switch_on()` lê SÓ a env `B3_TIMING_PUSH_KILL`,
nenhuma das 10 abas do portal mostra o estado, e nenhuma rota o expõe — se ele
for ligado, o aviso de gatilho para para todo mundo e o único caminho de volta
é redeploy. É o mesmo padrão de risco que já produziu o incidente do
kill-switch do agente (ligado sem querer, execução automática parada por 2,5
dias), agora numa superfície irmã (REPORT-01 C-35).
Output: `timing_watch` com padrão memória→DB→env; duas rotas admin; campo no
`/api/agent/status`; `TimingWatchKillSwitchBox` + KPI no portal; 2 guardiões.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-corre-o-cr-tico-alto/03-CONTEXT.md
@.planning/phases/03-corre-o-cr-tico-alto/03-UI-SPEC.md
@CLAUDE.md
@docs/adr/013-rbac-e-entitlements.md

<interfaces>
<!-- O molde exato a clonar. Use direto; não reexplore o repositório. -->

`server/app/agent.py:132-204` — o padrão memória→DB→env COMPLETO, com os dois
achados de revisão já corrigidos (try/except na leitura do DB; `_KILL_DB_CHECKED`
para não bater no SQLite a cada tick):
```python
_DB_CONN = None
_DB_ENABLED = False
_KILL_MEM = None          # None = sem override explícito
_KILL_DB_CHECKED = False

def configure_db(conn) -> None: ...
def reset_kill_switch_cache() -> None: ...        # só para testes
def set_kill_switch(on: bool, actor: str = None) -> bool:
    # grava _KILL_MEM SEMPRE; db.admin_config_set(..., "agentKillSwitch", ..., updated_by=actor) best-effort
def kill_switch_on() -> bool:
    # memória -> DB (uma vez por processo se não houver override) -> env AO VIVO -> default False
```

`server/app/main.py:61` — onde o agente é ligado ao banco:
```python
agent_mod.configure_db(_conn)
```

`server/app/main.py:668-676` — o par de rotas a espelhar:
```python
@app.get("/api/admin/agent/kill-switch")
async def admin_kill_switch_get(user: dict = Depends(require_permission("execucao_automatica.ver"))):
    return {"on": agent_mod.kill_switch_on()}

@app.put("/api/admin/agent/kill-switch")
async def admin_kill_switch_put(body: dict = Body(default={}), user: dict = Depends(require_permission("execucao_automatica.controlar"))):
    anterior = agent_mod.kill_switch_on()
    novo = bool((body or {}).get("on"))
    agent_mod.set_kill_switch(novo, actor=user["id"])
    if anterior != novo:
        audit.record(_conn, user["id"], "agent_kill_switch", None, "on", anterior, novo)
    return {"on": novo}
```

`server/app/timing_watch.py:39,58-59` — o alvo:
```python
# Chave de desligamento: `B3_TIMING_PUSH_KILL=1`.
def kill_switch_on() -> bool:
    return os.environ.get("B3_TIMING_PUSH_KILL") == "1"
```
Consumidor: `server/app/agent.py:832-833` (`if timing_watch.kill_switch_on(): return 0`).

`server/app/main.py:2175-2187` — `/api/agent/status`, onde o portal lê os KPIs
(`st = agent_mod.status_snapshot(_conn)` e depois campos extras são acrescidos
à mão — é aqui que o KPI novo entra, NÃO dentro de `status_snapshot`).

`web-admin/src/api.js:67-68` — o par de métodos a espelhar:
```javascript
killSwitchGet: () => req("GET", "/api/admin/agent/kill-switch"),
killSwitchPut: (on) => req("PUT", "/api/admin/agent/kill-switch", { on }),
```

`web-admin/src/App.jsx:432-482` — `KillSwitchBox` completo (Card + Kv + caption
+ confirm + botão + fallback de permissão) — o clone estrutural.
`web-admin/src/App.jsx:96` — o KPI `"KILL-SWITCH"` na `VisaoGeral`.
`web-admin/src/App.jsx:491` — `<KillSwitchBox user={user} />` dentro de `Automacao`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: timing_watch.kill_switch_on ganha o padrão memória→DB→env + set_kill_switch</name>
  <files>server/app/timing_watch.py, server/tests/test_fase3_timing_watch_kill.py</files>

  <read_first>
    - `server/app/agent.py:128-204` — `_DB_CONN`/`_DB_ENABLED`/`_KILL_MEM`/`_KILL_DB_CHECKED`, `configure_db`, `reset_kill_switch_cache`, `set_kill_switch`, `kill_switch_on` (com os docstrings que explicam POR QUE cada guarda existe)
    - `server/app/timing_watch.py:1-60` — docstring do módulo (as regras de risco do push) e `kill_switch_on()` atual
    - `server/app/agent.py:828-840` — o consumidor (`if timing_watch.kill_switch_on(): return 0`)
    - `server/app/db.py:522-545` — `admin_config_get` / `admin_config_set`
    - `server/tests/test_agent.py::test_kill_switch_e_janela_de_pregao` — o guardião que exige leitura AO VIVO da env quando não há override (o mesmo contrato vale aqui)
    - `.planning/milestones/v1.0-phases/01-auditoria-diagn-stica-consolidada/REPORT-01.md` — seção `#### C-35` (linha 731)
    - `.planning/codebase/TESTING.md` — padrão de teste com `monkeypatch` de env e DB temporário
  </read_first>

  <behavior>
    - `kill_switch_on()` sem nenhum override e sem env → `False`.
    - `B3_TIMING_PUSH_KILL=1` na env, sem override → `True`; mudar a env em runtime tem efeito IMEDIATO (o resultado da env nunca é cacheado em memória).
    - `set_kill_switch(True, actor=uid)` → `kill_switch_on()` devolve `True` na hora, mesmo antes de qualquer releitura do banco.
    - Após `set_kill_switch(True)`, um processo novo (novo `connect` + `configure_db` + `reset_kill_switch_cache`) lê `True` do banco mesmo com a env ausente.
    - Override do banco tem precedência sobre a env: `admin_config` com `False` + `B3_TIMING_PUSH_KILL=1` → `False`.
    - Falha do SQLite na leitura NÃO propaga: `kill_switch_on()` cai para a env/default e o `scheduler_loop` continua.
    - Falha do SQLite na escrita NÃO impede o efeito em memória (é o controle de emergência).
    - Sem `configure_db` (ex.: teste isolado, boot parcial), tudo funciona lendo só memória→env.
  </behavior>

  <action>
    Em `server/app/timing_watch.py`, replicar o bloco de kill-switch de
    `agent.py:128-204`, adaptado: chave de `admin_config` =
    `"timingWatchKillSwitch"`, env = `"B3_TIMING_PUSH_KILL"`, valores de env
    aceitos como ligado = `("1", "true", "TRUE", "yes")` após `.strip()`
    (alinhar com `agent.kill_switch_on`, que hoje é mais tolerante que o
    `== "1"` atual do `timing_watch` — a tolerância maior é o comportamento
    canônico da casa; declarar essa ampliação em comentário para não parecer
    acidental).
    Adicionar: `_DB_CONN`, `_DB_ENABLED`, `_KILL_MEM`, `_KILL_DB_CHECKED`,
    `configure_db(conn)`, `reset_kill_switch_cache()` (só para testes, com o
    mesmo docstring de justificativa) e `set_kill_switch(on, actor=None)`
    (escrita no DB best-effort dentro de `try/except`, memória sempre
    atualizada primeiro).
    Reescrever `kill_switch_on()` na ordem memória → DB (uma única consulta
    por processo quando não houver override, via `_KILL_DB_CHECKED`) → env
    AO VIVO → `False`. Copiar a intenção dos docstrings de `agent.py`
    (por que try/except, por que `_KILL_DB_CHECKED`, por que o resultado da
    env nunca vira `_KILL_MEM`), citando C-35 como origem.
    Acrescentar ao docstring do MÓDULO uma linha atualizando a frase
    "Chave de desligamento: `B3_TIMING_PUSH_KILL=1`" para refletir que agora
    existe também override em runtime pelo portal admin.
    NÃO alterar `avaliar_usuario` nem qualquer regra de push.

    Criar `server/tests/test_fase3_timing_watch_kill.py` (cabeçalho de
    guardião citando C-35 e o incidente do kill-switch do agente). Cobrir
    todos os 8 pontos de `<behavior>`, no padrão da casa: DB temporário via
    `db.connect(tmpdir)`, `monkeypatch.setenv/delenv` para a env,
    `timing_watch.reset_kill_switch_cache()` entre casos, e um caso com
    `db.admin_config_get` monkeypatchado para levantar (prova de que o
    `scheduler_loop` não é derrubado).
  </action>

  <acceptance_criteria>
    - `cd server && ./.venv/bin/python -m pytest -q tests/test_fase3_timing_watch_kill.py` sai 0 com ao menos 8 testes.
    - `grep -c "timingWatchKillSwitch" server/app/timing_watch.py` ≥ 2 (leitura + escrita).
    - `grep -c "B3_TIMING_PUSH_KILL" server/app/timing_watch.py` ≥ 1 (a env continua sendo lida).
    - `cd server && ./.venv/bin/python -m pytest -q tests/test_timing.py tests/test_agent.py` verde — em especial `test_kill_switch_e_janela_de_pregao`, que trava a leitura ao vivo da env no agente.
    - `cd server && ./.venv/bin/python -m pytest -q` verde (suíte inteira).
    - Nenhuma alteração em `server/app/agent.py` nesta task.
  </acceptance_criteria>

  <verify>
    <automated>cd server && ./.venv/bin/python -m pytest -q tests/test_fase3_timing_watch_kill.py && ./.venv/bin/python -m pytest -q</automated>
  </verify>

  <done>`timing_watch` tem kill-switch com override persistido, precedência memória→DB→env, leitura ao vivo da env preservada e nenhuma falha de SQLite capaz de derrubar o laço.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rotas admin do kill-switch do push + campo no /api/agent/status</name>
  <files>server/app/main.py, server/tests/test_fase3_timing_watch_kill.py</files>

  <read_first>
    - `server/app/main.py:55-70` — bloco de bootstrap onde `agent_mod.configure_db(_conn)` é chamado
    - `server/app/main.py:668-676` — o par de rotas do kill-switch do agente (molde exato)
    - `server/app/main.py:2175-2187` — `/api/agent/status` e como campos extras são acrescidos ao `st`
    - `server/app/rbac.py:22-33` — `GRUPOS`, em especial `execucao_automatica: {"execucao_automatica.ver", "execucao_automatica.controlar"}` — NENHUMA permissão nova é criada
    - `server/app/audit.py:13-17` — `audit.record(conn, actor_id, entity, entity_id, field, old, new)`
    - `server/tests/test_adr013_rbac.py` — padrão de teste de rota RBAC-gated (403 sem permissão)
    - `.planning/phases/03-corre-o-cr-tico-alto/03-UI-SPEC.md` — "Phase-Specific Component Notes → C-35" (mesma permissão, sem string nova)
  </read_first>

  <behavior>
    - `GET /api/admin/timing-watch/kill-switch` com `execucao_automatica.ver` → `{"on": <bool>}`.
    - A mesma rota sem essa permissão → 403.
    - `PUT /api/admin/timing-watch/kill-switch {"on": true}` com `execucao_automatica.controlar` → `{"on": true}` e `timing_watch.kill_switch_on()` passa a `True` imediatamente.
    - A mesma rota com apenas `execucao_automatica.ver` → 403 (o gate real é do backend, não do botão escondido).
    - Uma transição (anterior != novo) grava uma linha em `admin_audit_log` com `entity == "timing_watch_kill_switch"` e `field == "on"`; uma chamada que não muda o estado NÃO grava.
    - `GET /api/agent/status` passa a devolver `timingWatchKillSwitch` (bool), refletindo `timing_watch.kill_switch_on()`.
    - `timing_watch.configure_db(_conn)` é executado no bootstrap, junto de `agent_mod.configure_db(_conn)`.
  </behavior>

  <action>
    Em `server/app/main.py`:
    1. No bloco de bootstrap, ao lado de `agent_mod.configure_db(_conn)`,
       acrescentar `timing_watch.configure_db(_conn)` (importando
       `timing_watch` no mesmo estilo dos demais imports do app, se ainda não
       estiver importado no topo — hoje ele só é importado localmente dentro
       de `agent.py`).
    2. Criar o par de rotas, imediatamente APÓS as rotas do kill-switch do
       agente (adjacência deliberada: os dois interruptores lidos juntos),
       espelhando byte-a-byte a estrutura do molde acima e trocando:
       caminho → `/api/admin/timing-watch/kill-switch`; módulo →
       `timing_watch`; `entity` do audit → `"timing_watch_kill_switch"`.
       As permissões são as MESMAS (`execucao_automatica.ver` /
       `execucao_automatica.controlar`) — nenhuma string de permissão nova.
    3. Na rota `/api/agent/status`, acrescentar
       `st["timingWatchKillSwitch"] = timing_watch.kill_switch_on()` junto dos
       outros campos que a rota já acrescenta ao `st`. NÃO mexer em
       `agent.status_snapshot()` (o snapshot é do agente; o push do gatilho é
       outro subsistema — misturar os dois dentro do snapshot criaria
       acoplamento que a rota não precisa).

    Acrescentar a `server/tests/test_fase3_timing_watch_kill.py` os testes de
    rota (padrão `TestClient` + `B3_DB_PATH` monkeypatchado + reimport de
    `app.main`, ver TESTING.md), cobrindo os 7 pontos de `<behavior>`:
    200 com permissão, 403 sem, efeito imediato do PUT, 403 no PUT com só
    `.ver`, linha em `admin_audit_log` na transição, ausência de linha quando
    não muda, e `timingWatchKillSwitch` presente em `/api/agent/status`.
  </action>

  <acceptance_criteria>
    - `cd server && ./.venv/bin/python -m pytest -q tests/test_fase3_timing_watch_kill.py` sai 0 com ao menos 15 testes (8 da Task 1 + 7 desta).
    - `grep -c "timing-watch/kill-switch" server/app/main.py` retorna 2 (GET + PUT).
    - `grep -c "timing_watch.configure_db" server/app/main.py` retorna 1.
    - `grep -v '^\s*#' server/app/rbac.py | grep -c "execucao_automatica"` inalterado em relação ao HEAD anterior — nenhuma permissão nova foi criada.
    - `cd server && ./.venv/bin/python -m pytest -q` verde (suíte inteira).
  </acceptance_criteria>

  <verify>
    <automated>cd server && ./.venv/bin/python -m pytest -q tests/test_fase3_timing_watch_kill.py && ./.venv/bin/python -m pytest -q</automated>
  </verify>

  <done>Duas rotas admin RBAC-gated controlam o kill-switch do push em runtime, com auditoria de transição, e `/api/agent/status` expõe o estado para o portal.</done>
</task>

<task type="auto">
  <name>Task 3: TimingWatchKillSwitchBox e KPI "PUSH DO GATILHO" no portal admin</name>
  <files>web-admin/src/api.js, web-admin/src/App.jsx, web/tests/test_fase3_admin_timing_watch.mjs</files>

  <read_first>
    - `web-admin/src/api.js:53-68` — `agentStatus`, `killSwitchGet`, `killSwitchPut` (molde exato dos dois métodos novos)
    - `web-admin/src/App.jsx:432-482` — `KillSwitchBox` inteiro: `Card`, `Kv`, caption, `window.confirm`, botão com `borderColor`/`color` invertidos pelo estado, fallback de permissão
    - `web-admin/src/App.jsx:89-100` — `VisaoGeral` e a linha de KPIs (`Kpi label="KILL-SWITCH"`)
    - `web-admin/src/App.jsx:486-495` — `Automacao`, onde `<KillSwitchBox user={user} />` é renderizado
    - `web-admin/src/App.jsx:14-18` — tokens `T` (o botão usa `T.positive`/`T.negative`, NUNCA `T.accent`)
    - `.planning/phases/03-corre-o-cr-tico-alto/03-UI-SPEC.md` — "Copywriting Contract" (as 6 linhas de C-35, texto literal), "Phase-Specific Component Notes → C-35" (posição, hierarquia visual), "Color / Surface B"
    - `web/tests/test_editor_texto_byte_exato.mjs` — precedente de teste em `web/tests/` inspecionando `web-admin/src/`
  </read_first>

  <action>
    Em `web-admin/src/api.js`, acrescentar, imediatamente após
    `killSwitchGet`/`killSwitchPut`:
    ```
    timingWatchKillSwitchGet: () => req("GET", "/api/admin/timing-watch/kill-switch"),
    timingWatchKillSwitchPut: (on) => req("PUT", "/api/admin/timing-watch/kill-switch", { on }),
    ```

    Em `web-admin/src/App.jsx`, criar `TimingWatchKillSwitchBox({ user, reload })`
    como clone estrutural EXATO de `KillSwitchBox` (mesmo `Card`, mesmo `Kv`,
    mesma caption, mesmo `window.confirm`, mesmo botão, mesmo fallback de
    permissão, mesma permissão `execucao_automatica.controlar`), trocando
    apenas o cliente de API e os textos, que são LITERAIS do Copywriting
    Contract do UI-SPEC:
    - título do Card: `Kill-switch do push de gatilho (timing_watch)`
    - `Kv` "Estado vigente": ligado → `LIGADO (push do gatilho parado)` (tom
      negative); desligado → `desligado (push do gatilho roda normalmente)`
      (tom positive)
    - caption: `Override em runtime sobre a env B3_TIMING_PUSH_KILL — some no próximo deploy só se ninguém mexer aqui de novo.`
    - confirm ao DESLIGAR: `Desligar o kill-switch do push de gatilho? O aviso de gatilho volta a ser enviado normalmente.`
    - confirm ao LIGAR: `Ligar o kill-switch do push de gatilho? Nenhum aviso de gatilho será enviado a ninguém enquanto estiver ligado.`
    - botão: `Aplicando…` / `Desligar kill-switch` / `Ligar kill-switch`
    - sem permissão: `Sem permissão para alternar (requer execucao_automatica.controlar).`
    Renderizar `<TimingWatchKillSwitchBox user={user} />` em `Automacao`
    IMEDIATAMENTE após `<KillSwitchBox user={user} />` — par casado, mesma
    ordem da tabela de replay do incidente no REPORT-01; sem destaque visual
    próprio (nenhum uso de `T.accent`).

    Em `VisaoGeral`, acrescentar um `Kpi` irmão do `"KILL-SWITCH"`, mesmo
    componente e mesmo mapeamento de tom: `label="PUSH DO GATILHO"`,
    `value={data.timingWatchKillSwitch ? "ligado" : "desligado"}`,
    `tone={data.timingWatchKillSwitch ? "negative" : "positive"}`.

    Nenhum componente, cor, peso ou padding novo. Comentário curto acima do
    componente citando C-35 e o motivo (o 2º interruptor era invisível e só
    saía por redeploy).

    Criar `web/tests/test_fase3_admin_timing_watch.mjs`, Pattern A, lendo
    `web-admin/src/App.jsx` e `web-admin/src/api.js` via `readFileSync`.
    Assertivas: os dois métodos novos existem em `api.js` com os caminhos
    exatos; o componente `TimingWatchKillSwitchBox` existe; as 6 strings
    literais de copy aparecem 1x cada; o gate `execucao_automatica.controlar`
    é verificado dentro do componente novo (regex sobre o trecho entre
    `function TimingWatchKillSwitchBox` e o próximo `function `); há um
    `window.confirm` dentro desse mesmo trecho; o KPI `"PUSH DO GATILHO"`
    existe em `VisaoGeral`; o componente novo NÃO usa `T.accent`; e
    `KillSwitchBox` continua existindo e sendo renderizado em `Automacao`
    (o box antigo não foi substituído).
    Cabeçalho de guardião citando C-35 e o incidente de 2,5 dias.
  </action>

  <acceptance_criteria>
    - `node web/tests/test_fase3_admin_timing_watch.mjs` sai 0 e imprime "TODOS OS TESTES PASSARAM".
    - `cd web-admin && npx vite build` conclui sem erro.
    - `grep -c "PUSH DO GATILHO" web-admin/src/App.jsx` retorna 1.
    - `grep -c "KillSwitchBox" web-admin/src/App.jsx` ≥ 4 (definição + uso do antigo, definição + uso do novo) e `grep -c "Kill-switch do Operador" web-admin/src/App.jsx` retorna 1 (o box antigo intacto).
    - `grep -c "T.accent" web-admin/src/App.jsx` inalterado em relação ao HEAD anterior — nenhum uso novo de acento.
    - Nenhuma string de permissão nova: `grep -o 'execucao_automatica\.[a-z]*' web-admin/src/App.jsx | sort -u` devolve exatamente `execucao_automatica.controlar`.
  </acceptance_criteria>

  <verify>
    <automated>node web/tests/test_fase3_admin_timing_watch.mjs && cd web-admin && npx vite build</automated>
  </verify>

  <done>A aba Automação mostra os dois kill-switches como par casado, a Visão Geral mostra os dois KPIs, e o admin com permissão alterna o do push sem redeploy, com confirmação explícita.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| portal admin (navegador/browser in-app) → rotas `/api/admin/*` | ação administrativa destrutiva atravessa a rede |
| `admin_config` (SQLite) → decisão de execução do `scheduler_loop` | override persistido controla se TODA a base recebe push de gatilho |
| env do Railway → mesma decisão | segundo caminho de ativação, sem registro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-18 | Elevation of Privilege | `PUT /api/admin/timing-watch/kill-switch` | mitigate | `Depends(require_permission("execucao_automatica.controlar"))` no backend, mesma permissão do kill-switch do agente; guardião de rota assere 403 para quem tem só `.ver`. A UI esconder o botão NÃO é o gate (ADR-013). |
| T-03-19 | Repudiation | quem ligou/desligou o interruptor | mitigate | `audit.record(..., entity="timing_watch_kill_switch", field="on", ...)` em toda transição, e `db.admin_config_set(..., updated_by=actor)` grava o autor; guardião verifica a linha no `admin_audit_log`. |
| T-03-20 | Denial of Service | ativação acidental para 100% da base (o incidente que motivou o achado) | mitigate | `window.confirm` obrigatório com texto que declara o alcance ("Nenhum aviso de gatilho será enviado a ninguém"); estado agora visível em DOIS lugares (KPI na Visão Geral + Kv na Automação), encurtando a janela de descoberta. |
| T-03-21 | Denial of Service | falha do SQLite na leitura do override derrubando o `scheduler_loop` | mitigate | leitura dentro de `try/except` e `_KILL_DB_CHECKED` para no máximo uma consulta por processo sem override — regra copiada de `agent.kill_switch_on`, cujo docstring registra exatamente esse achado de revisão. |
| T-03-22 | Tampering | cliente forjando o estado do interruptor | mitigate | o estado exibido vem sempre do backend (`GET`), nunca de estado local; o `PUT` só aceita `{"on": bool}` e o valor é normalizado com `bool(...)` server-side. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | nenhum pacote novo é instalado neste plano. |
</threat_model>

<verification>
- `cd server && ./.venv/bin/python -m pytest -q` — suíte backend verde.
- `node web/tests/test_fase3_admin_timing_watch.mjs` — guardião do portal verde.
- `bash scripts/executar.sh --testes` — suíte canônica sem falha nova.
- `cd web-admin && npx vite build` — build do portal sem erro.
- Verificação humana (fica para `/gsd:verify-phase`, exige portal publicado —
  lembrar que merge de PR NÃO publica o front; o passo de publicação é
  separado): abrir a aba Automação e confirmar os dois boxes; alternar o novo
  e confirmar que o KPI da Visão Geral acompanha; confirmar que uma conta sem
  `execucao_automatica.controlar` vê a mensagem de "sem permissão".
</verification>

<success_criteria>
- `timing_watch.kill_switch_on()` segue o padrão memória→DB→env, com env ao vivo preservada.
- Duas rotas admin RBAC-gated, com auditoria de transição.
- Portal mostra o 2º interruptor na Automação e o 2º KPI na Visão Geral.
- Dois guardiões novos verdes; suíte canônica e build do portal sem falha nova.
</success_criteria>

<output>
Create `.planning/phases/03-corre-o-cr-tico-alto/03-05-SUMMARY.md` when done
</output>
