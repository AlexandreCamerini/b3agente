---
phase: 06-instrumentacao-assertividade-adr015
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/store.py
  - server/app/agent.py
  - server/tests/test_agent.py
  - server/tests/test_persistence.py
autonomous: true
requirements: [ADR15-04]
must_haves:
  truths:
    - "Uma venda simulada de AÇÃO grava POR QUE vendeu, no mesmo vocabulário que a venda de opção já usa"
    - "Uma venda disparada pelo agente por stop rompido fica com `motivo: 'stop'` no histórico; por alvo atingido, `motivo: 'alvo'`"
    - "Venda manual do usuário continua sem pedir classificação — cai no default `'manual'`"
    - "A taxa stop×alvo de carteira de ação passa a ser computável a partir do `history`"
    - "A divergência entre o critério 4 do ROADMAP (`3 call sites automáticos`) e o repo (1 call site) fica registrada com evidência, para o Plano 03 emendar o ROADMAP"
  artifacts:
    - path: "server/app/store.py"
      provides: "sell() com parâmetro motivo, paridade com sell_option()"
      contains: "motivo: str = \"manual\""
    - path: "server/app/agent.py"
      provides: "call site automático passando o motivo real"
      contains: "motivo=\"stop\" if breach_stop else \"alvo\""
    - path: "server/tests/test_agent.py"
      provides: "guardião do motivo real no histórico do ciclo automático"
      contains: "motivo"
  key_links:
    - from: "server/app/agent.py (_avaliar posições)"
      to: "store.sell"
      via: "kwarg motivo derivado de breach_stop/hit_alvo"
      pattern: "store\\.sell\\(.*motivo="
    - from: "store.sell"
      to: "history entry VENDA"
      via: "chave motivo no dict inserido"
      pattern: "\"motivo\": motivo"
---

<objective>
`store.sell()` ganha o parâmetro `motivo`, com o mesmo contrato de
`sell_option()` (`'manual' | 'stop' | 'alvo' | 'vencimento'`), e o call site
automático do Operador em `agent.py` passa o motivo real.

Purpose: hoje `sell()` só tem `origem` (QUEM disparou), nunca `motivo` (POR
QUE) — o `agent.py` já calcula "stop atingido"/"alvo atingido" (linha 838) e
joga fora, usando o texto só no Diário. Consequência levantada no ADR-015: a
taxa stop×alvo da carteira de AÇÃO não é computável a partir do histórico, por
design da assinatura. A venda de OPÇÃO já resolveu isso desde o ADR-005.

Output: paridade de contrato entre `sell()` e `sell_option()`, motivo real
persistido nas saídas automáticas, e testes travando os dois.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-instrumentacao-assertividade-adr015/06-CONTEXT.md
@docs/adr/015-assertividade-do-motor-de-recomendacao.md
@CLAUDE.md

<interfaces>
<!-- Contratos extraídos do código. Não explorar o repo. -->

server/app/store.py:621 (assinatura ATUAL):
  sell(conn, t, price, user_id=None, qty=None, origem="manual")
  history entry hoje (linha ~640):
    {"date":…, "type":"VENDA", "t":…, "qty":…, "price":…, "pnl":…, "origem": origem}

server/app/store.py:704 (contrato de REFERÊNCIA a replicar):
  sell_option(conn, contract_id, price, user_id=None, qty=None,
              motivo: str = "manual", origem: str = "manual")
  history entry (linha ~731): {..., "motivo": motivo, "origem": origem}
  Docstring documenta a distinção: `motivo` = POR QUE vendeu
  ('manual'|'stop'|'alvo'|'vencimento'); `origem` = QUEM disparou
  (manual|automatico|sistema). Um stop batido pelo Operador é
  motivo='stop', origem='automatico'; o mesmo stop fechado à mão é
  motivo='stop', origem='manual'.

TODOS os call sites de `store.sell(` no repo (verificado por grep):
  server/app/agent.py:852       origem="automatico"   -> ÚNICO automático, ganha motivo real
  server/app/pending_orders.py:296  origem="pendente"  -> ordem de venda PEDIDA pelo usuário, fica no default 'manual'
  server/app/main.py:1698       (rota /api/sell)       -> venda manual, fica no default 'manual'
  Todos usam kwargs para user_id/qty/origem — nenhum é posicional além de (conn, t, price).

server/app/agent.py:838 (já existente, dentro do laço `for pos in list(positions)`):
  breach_stop = par["rules"]["stop"] and pos.get("stop") is not None and price <= pos["stop"]
  hit_alvo    = par["rules"]["alvo"] and pos.get("alvo") is not None and price >= pos["alvo"]
  motivo = "stop atingido" if breach_stop else "alvo atingido"   # texto do Diário, NÃO reutilizar como valor
</interfaces>

<decision_note>
06-CONTEXT.md e o critério 4 do ROADMAP falam em "os 3 call sites automáticos
do Operador em agent.py". O grep mostra UM só call site de `store.sell` em
`agent.py` (linha 852); o outro (linha 580) é `store.sell_option`, que JÁ passa
`motivo` desde o ADR-005. Este plano altera o call site que existe e prova por
grep que não há outro — não inventar call sites para bater a contagem do
CONTEXT. A Task 3 registra a divergência com evidência para o Plano 03 emendar
o ROADMAP (é o único plano da fase que escreve nesse arquivo).
</decision_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: store.sell() aceita e persiste `motivo`, em paridade com sell_option()</name>
  <files>server/app/store.py, server/tests/test_persistence.py</files>
  <read_first>
    - server/app/store.py linhas 621-645 (`sell` inteira)
    - server/app/store.py linhas 704-737 (`sell_option` — docstring e history entry, o contrato a replicar)
    - server/app/store.py linhas 740-753 (`close_option_vencida` — exemplo de motivo não-manual)
    - server/tests/test_persistence.py (localizar os testes de compra/venda de ação para colocar os novos ao lado)
  </read_first>
  <behavior>
    - `sell(conn, "PETR4", 30.0, user_id="u1")` grava a entrada VENDA com `motivo == "manual"` (default) e `origem == "manual"`.
    - `sell(conn, "PETR4", 30.0, user_id="u1", motivo="stop", origem="automatico")` grava `motivo == "stop"` e `origem == "automatico"` — os dois eixos são independentes.
    - Venda PARCIAL (`qty=100` numa posição de 200) grava o `motivo` igual, sem alterar `avg` nem o PnL proporcional.
    - `sell` de ticker sem posição continua devolvendo None sem gravar nada.
    - O PnL devolvido e o `cash` resultante são idênticos aos de antes da mudança (o parâmetro é puramente descritivo).
  </behavior>
  <action>
    Em `server/app/store.py`, função `sell` (linha 621):

    1. Acrescentar o parâmetro `motivo: str = "manual"` ANTES de `origem`,
    espelhando exatamente a ordem de `sell_option` — a assinatura fica
    `sell(conn, t, price, user_id=None, qty=None, motivo: str = "manual",
    origem: str = "manual")`. Todos os call sites atuais usam kwargs para
    `user_id`/`qty`/`origem`, então a inserção não quebra chamada posicional
    nenhuma (confirmar por grep antes de considerar a task pronta).

    2. Acrescentar `"motivo": motivo` ao dict inserido em `history`, na mesma
    posição relativa que `sell_option` usa (antes de `"origem"`).

    3. Estender a docstring com o mesmo texto de contrato de `sell_option`,
    adaptado: `motivo` (ADR-015/ADR15-04) = POR QUE vendeu —
    'manual'|'stop'|'alvo'|'vencimento'; `origem` = QUEM disparou. Registrar em
    uma frase que o histórico ANTERIOR a esta mudança fica sem a chave (None no
    `.get`) e NÃO é reescrito — mesmo padrão adotado pelo ADR-012 quando `origem`
    entrou.

    4. Não alterar aritmética alguma: `sold`, `pnl`, `cash`, o ramo parcial e o
    `db.kv_set` ficam byte-a-byte como estão. Esta é uma mudança de
    instrumentação; qualquer alteração de número aqui violaria o escopo da fase.

    5. NÃO espelhar em `web/src/persistence.js`: a venda local do `deviceStore`
    (linha ~1075) também não carrega `origem`, precedente do ADR-012 — o campo
    vive no motor do servidor. Registrar essa decisão no comentário para o próximo
    leitor não achar que é esquecimento de paridade.

    Testes em `server/tests/test_persistence.py`: escrever primeiro (RED) os casos
    do `<behavior>`, no estilo dos testes vizinhos.
  </action>
  <verify>
    <automated>(cd server && ./.venv/bin/python -m pytest tests/test_persistence.py -q)</automated>
  </verify>
  <acceptance_criteria>
    - `(cd server && ./.venv/bin/python -c "from app import store; import inspect; p=list(inspect.signature(store.sell).parameters); q=list(inspect.signature(store.sell_option).parameters); print(p[-2:], q[-2:])")` imprime `['motivo', 'origem'] ['motivo', 'origem']`.
    - `grep -n '"motivo": motivo' server/app/store.py | wc -l` == 2 (venda de ação + venda de opção).
    - `grep -n "store.sell(" server/app/*.py | grep -v "sell_option" | wc -l` == 3 (agent, pending_orders, main) — nenhum call site novo ou perdido.
    - `git diff server/app/store.py` não altera nenhuma linha que calcule `sold`, `pnl` ou `cash`.
    - `pytest tests/test_persistence.py -q` passa 100%.
  </acceptance_criteria>
  <done>`sell()` tem `motivo` na mesma ordem/contrato de `sell_option()` e grava a chave no `history`; aritmética de PnL/caixa intacta.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: o ciclo automático do Operador passa o motivo real</name>
  <files>server/app/agent.py, server/tests/test_agent.py</files>
  <read_first>
    - server/app/agent.py linhas 820-860 (laço de posições: `breach_stop`, `hit_alvo`, `motivo`, tetos e `store.sell`)
    - server/app/agent.py linhas 570-585 (call site de `store.sell_option` com `motivo` — o padrão já aplicado às opções)
    - server/tests/test_agent.py linhas 1-90 (helpers `_conn`, `_seed`, `_quotes`, `_run` e `test_executa_venda_no_stop_e_registra_log`)
  </read_first>
  <behavior>
    - Posição com `stop` rompido e `mode="executar"`: a entrada VENDA no `history` tem `motivo == "stop"` e `origem == "automatico"`.
    - Posição com `alvo` atingido (e `alvoDinamico` desligado): `motivo == "alvo"`.
    - O texto do evento no Diário continua exatamente `"Proteção simulada: {ticker} vendido (stop atingido) a R$ …"` — a frase longa NÃO vira o valor do campo.
    - Modo `"apenas sinalizar"`, teto diário estourado ou teto por operação estourado: nenhuma venda é feita, logo nenhuma entrada de histórico com motivo (os `continue` existentes ficam intactos).
  </behavior>
  <action>
    Em `server/app/agent.py`, no laço `for pos in list(positions)` (linha ~852):

    1. Passar o motivo curto para o store, derivando do MESMO booleano que já
    decide o texto do Diário: `store.sell(conn, pos["t"], price, user_id=scope,
    motivo="stop" if breach_stop else "alvo", origem="automatico")`.

    2. NÃO reutilizar a variável `motivo` da linha 838 (ela vale "stop
    atingido"/"alvo atingido" e é o texto do Diário — o contrato do campo é o
    vocabulário curto de `sell_option`). Também não renomear nem reescrever essa
    variável: os eventos do Diário e os guardiões que casam com essas frases
    continuam como estão.

    3. Manter o comentário `# ADR-012 (Fase 3)` existente e acrescentar
    `ADR15-04` ao lado, para o próximo leitor achar as duas decisões que moldaram
    a chamada.

    4. Não tocar em `breach_stop`, `hit_alvo`, trailing, alvo dinâmico, tetos,
    `_bump_ops` nem `_avaliar_entradas`. Nenhuma regra de execução muda nesta
    fase.

    Testes em `server/tests/test_agent.py`: escrever primeiro (RED) os casos do
    `<behavior>` reusando os helpers `_conn`/`_seed`/`_quotes`/`_run` e o padrão de
    `test_executa_venda_no_stop_e_registra_log` (linha 48). Ler o `history` do
    escopo `"u1"` via `db.kv_get` e assertar o campo `motivo`.
  </action>
  <verify>
    <automated>(cd server && ./.venv/bin/python -m pytest tests/test_agent.py tests/test_agent_options.py tests/test_agent_modo_estudo.py -q)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'store.sell(conn, pos\["t"\], price, user_id=scope, motivo=' server/app/agent.py` retorna 1 linha.
    - `grep -n 'motivo = "stop atingido" if breach_stop else "alvo atingido"' server/app/agent.py` continua retornando 1 linha (texto do Diário intacto).
    - `grep -rn "store.sell(" server/app/agent.py | grep -v sell_option | grep -c "motivo=" ` == 1 (o único call site automático de ação passa motivo).
    - `grep -n "motivo=" server/app/pending_orders.py server/app/main.py | grep "store.sell(" | wc -l` == 0 (vendas manuais/pendentes continuam no default).
    - `pytest tests/test_agent.py -q` passa 100%.
  </acceptance_criteria>
  <done>Venda automática por stop grava `motivo='stop'` e por alvo grava `motivo='alvo'`; texto do Diário e tetos inalterados.</done>
</task>

<task type="auto">
  <name>Task 3: verificação canônica das duas suítes + registro da divergência do ROADMAP</name>
  <files>—</files>
  <read_first>
    - CLAUDE.md (seção "Validação obrigatória" e guardrail de paridade `deviceStore` ↔ `serverStore`)
    - web/tests/test_api_parity.mjs e web/tests/test_fase3_paridade_stores_generica.mjs (guardiões de paridade dos stores)
    - .planning/ROADMAP.md linhas 265-290 (critérios de sucesso da Phase 6)
  </read_first>
  <action>
    1. Rodar a suíte canônica (pytest + `web/tests/*.mjs`). Conferir explicitamente
    que os guardiões de paridade dos stores continuam verdes: o parâmetro novo é
    do motor Python e não muda a superfície de API consumida pelo front, então
    nenhum deles deve reagir. Se algum reagir, PARAR e reavaliar — significa que a
    paridade `deviceStore` ↔ `serverStore` foi tocada e a decisão de não espelhar
    precisa ser revista com o Alex, não contornada.

    2. Registrar no SUMMARY, em seção própria intitulada
    `## Emenda pendente no ROADMAP — critério 4`, a divergência VERIFICADA entre o
    critério 4 da Phase 6 e o repo, com a evidência e o texto de substituição
    pronto. Quem edita `.planning/ROADMAP.md` é o Plano 03 (Task 3) — duas plans da
    mesma wave não escrevem no mesmo arquivo. Conteúdo obrigatório:

      - Critério 4 hoje diz "os 3 call sites automáticos em `agent.py` passam o
        motivo real".
      - Evidência (colar a saída do grep no SUMMARY):
        `grep -n "store.sell(" server/app/agent.py | grep -v sell_option` → 1 linha
        (852). O call site da linha ~580 é `store.sell_option`, que já passa
        `motivo` desde o ADR-005.
      - Texto de substituição literal a aplicar: "4. `store.sell()` aceita `motivo`
        com o mesmo contrato de `sell_option()`
        (`'manual'|'stop'|'alvo'|'vencimento'`); o ÚNICO call site automático de
        `store.sell` em `agent.py` (linha ~852) passa o motivo real
        (`breach_stop`/`hit_alvo`) — os demais call sites (`pending_orders.py`,
        rota `/api/sell`) são vendas pedidas pelo usuário e ficam no default
        `'manual'`. [ADR15-04]"
      - Registrar também a decisão de não espelhar `motivo` no `deviceStore` e o
        precedente (ADR-012 / `origem`).
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/executar.sh --testes` termina com exit code 0.
    - Nenhum `[X]` na saída das suítes web, em especial `test_api_parity.mjs` e `test_fase3_paridade_stores_generica.mjs`.
    - O SUMMARY registra a decisão de não espelhar `motivo` no `deviceStore` e o precedente (ADR-012 / `origem`).
    - O SUMMARY contém a seção `## Emenda pendente no ROADMAP — critério 4` com a saída do grep e o texto de substituição literal.
  </acceptance_criteria>
  <done>Suíte canônica verde, guardiões de paridade intactos e a emenda do critério 4 registrada no SUMMARY para o Plano 03 aplicar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rota `/api/sell` (cliente) → `store.sell` | entrada do usuário; `motivo` NÃO é exposto como parâmetro de rota |
| agente server-side → `store.sell` | chamada interna, motivo derivado de booleano determinístico |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Spoofing | usuário forjar `motivo='stop'` numa venda manual e falsear a própria estatística | mitigate | `main.py:1698` não repassa `motivo` do body — a rota fica no default `'manual'`; o campo só é setado por código do servidor |
| T-06-11 | Tampering | motivo divergir do que de fato aconteceu (venda por alvo gravada como stop) | mitigate | Motivo derivado do MESMO `breach_stop` que decide a venda, na mesma expressão; teste assere os dois ramos |
| T-06-12 | Repudiation | histórico antigo sem `motivo` interpretado como 'manual' | accept | Chave ausente = `None` no `.get`, nunca default silencioso; documentado na docstring, mesmo padrão do `origem` (ADR-012) |
| T-06-20 | Repudiation | verificador da fase cobrar "3 call sites" que não existem e reprovar entrega correta | mitigate | Divergência registrada com evidência de grep no SUMMARY; Plano 03 emenda o critério no ROADMAP |
| T-06-SC | Tampering | instalação de pacote | accept | Nenhuma dependência nova (`server/requirements.txt` intocado) |
</threat_model>

<verification>
1. `bash scripts/executar.sh --testes` verde.
2. `sell` e `sell_option` com a mesma ordem de parâmetros finais (`motivo`, `origem`).
3. `git diff --stat` limitado a `server/app/store.py`, `server/app/agent.py` e os dois arquivos de teste.
4. Nenhuma alteração em `web/src/persistence.js` (decisão registrada, não esquecimento).
</verification>

<success_criteria>
- `store.sell()` aceita `motivo` com o contrato de `sell_option()` e persiste no `history`. [ADR15-04]
- O call site automático de `agent.py` passa `'stop'`/`'alvo'` reais; venda manual e ordem pendente ficam em `'manual'`.
- Nenhum número da carteira mudou (PnL, caixa, posição parcial idênticos).
- Divergência do critério 4 do ROADMAP registrada com evidência no SUMMARY.
- Suíte canônica verde.
</success_criteria>

<output>
Create `.planning/phases/06-instrumentacao-assertividade-adr015/06-04-SUMMARY.md` when done
</output>
