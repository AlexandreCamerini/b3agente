---
phase: 06-instrumentacao-assertividade-adr015
plan: 05
type: execute
wave: 2
depends_on: ["06-04"]   # overlap de arquivo: agent.py também é tocado pelo Plano 04 (wave 1)
files_modified:
  - server/app/setups.py
  - server/app/agent.py
  - server/tests/test_auditoria_prompts.py
  - web/src/finance.js
  - web/src/copy.js
  - web/src/App.jsx
  - web/src/catalog.js
  - web/tests/test_alvo_dinamico_ui.mjs
  - web/tests/test_rr_min_fonte_unica.mjs
autonomous: true
requirements: [ADR15-05]
must_haves:
  truths:
    - "Existe UMA fonte do R:R mínimo no backend (`skill_ref.RR_MIN`) e UMA no front (`finance.js`), amarradas por teste cruzado"
    - "Mudar `skill_ref.RR_MIN` e não acompanhar nos outros pontos faz a suíte falhar — hoje só `skill_ref` tinha rede"
    - "O guardião cruzado compara VALOR (número parseado do fonte e texto derivado dele), não a grafia da linha de código — `RR_MIN_TXT` é computado em runtime no `skill_ref.py` e não existe como literal no fonte"
    - "O valor numérico continua 1.5 — esta fase consolida a fonte, não decide um novo limiar"
    - "Os literais que vivem dentro de prompt espelhado byte-a-byte continuam literais, e ficam cobertos pelo guardião cruzado"
    - "O texto renderizado ao usuário continua `1,5:1` — nenhum `${RR_MIN_TXT}` literal chega à tela"
  artifacts:
    - path: "web/src/finance.js"
      provides: "RR_MIN e RR_MIN_TXT — fonte única do front"
      contains: "export const RR_MIN"
    - path: "web/tests/test_rr_min_fonte_unica.mjs"
      provides: "guardião cruzado front × skill_ref.py + anti-interpolação-morta"
      contains: "skill_ref.py"
    - path: "server/tests/test_auditoria_prompts.py"
      provides: "guardião cruzado setups × agent × front"
      contains: "RR_MINIMO"
  key_links:
    - from: "server/app/setups.py"
      to: "server/app/skill_ref.py"
      via: "RR_MINIMO = skill_ref.RR_MIN"
      pattern: "RR_MINIMO = skill_ref\\.RR_MIN"
    - from: "server/app/agent.py"
      to: "server/app/skill_ref.py"
      via: "RR_MINIMO = skill_ref.RR_MIN"
      pattern: "RR_MINIMO = skill_ref\\.RR_MIN"
    - from: "web/src/copy.js"
      to: "web/src/finance.js"
      via: "import { RR_MIN_TXT }"
      pattern: "RR_MIN_TXT"
---

<objective>
Consolidar o R:R mínimo (1,5:1) numa fonte única por camada e criar o guardião
de teste cruzado que hoje não existe.

Purpose: o número vive em 3 constantes Python independentes
(`skill_ref.RR_MIN`, `setups.RR_MINIMO`, `agent.RR_MINIMO`) e 7 literais no
front, e um único teste cobre só `skill_ref` (`test_auditoria_prompts.py:167`).
O comentário em `conceitos.py:354-356` já registra que esse valor "viveu como
1,5 em quatro lugares" — a auditoria de 2026-07 resolveu o texto didático, não
os dois motores nem o front. Enquanto isso não fecha, qualquer decisão futura
sobre o limiar significa editar 10 lugares sem rede de segurança.

Output: `setups` e `agent` importando de `skill_ref`; uma constante única
exportada no front; guardião cruzado Python e guardião cruzado JS — incluindo
a checagem de que a interpolação REALMENTE interpola (a forma errada em JSX ou
em string aspeada passaria em grep, em build e no runner ingênuo, e mandaria
`${RR_MIN_TXT}:1` literal para a tela do usuário).

NÃO É ESCOPO: mudar o valor numérico (continua 1.5). Subir para 2:1 é decisão
de produto separada, explicitamente diferida (06-CONTEXT.md, Deferred Ideas).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-instrumentacao-assertividade-adr015/06-CONTEXT.md
@docs/adr/015-assertividade-do-motor-de-recomendacao.md
@CLAUDE.md

<interfaces>
<!-- Contratos extraídos do código. Não explorar o repo. -->

server/app/skill_ref.py:25-34 (FONTE ÚNICA, já existente, SEM imports — módulo
folha, sem risco de ciclo). O FONTE REAL, verbatim:
  RR_MIN = 1.5
  RR_IDEAL = 2.0
  # Formato pt-BR para interpolação em prompts ("1,5" / "2").
  RR_MIN_TXT = f"{RR_MIN:g}".replace(".", ",")
  RR_IDEAL_TXT = f"{RR_IDEAL:g}".replace(".", ",")
  ATENÇÃO para quem escrever o guardião JS: `RR_MIN_TXT = "1,5"` NÃO EXISTE no
  fonte — o "1,5" é COMPUTADO em runtime a partir de `RR_MIN`. Só `RR_MIN = 1.5`
  é literal e parseável do arquivo.

server/app/setups.py:559  RR_MINIMO = 1.5   # hoje setups.py não importa NADA do pacote
server/app/agent.py:446   RR_MINIMO = 1.5   # agent.py já faz `from . import db, store`

server/app/agent.py:445   ALVO_ATR_MULT = 1.5   # NÃO É O MESMO 1.5 — multiplicador de ATR. NÃO CONSOLIDAR.
web/src/App.jsx:252       const rr = ... ? 1.5 : ... ? 2.5 : 2.0   # R:R-alvo por horizonte do PERFIL. NÃO É o piso. NÃO CONSOLIDAR.

Literais de R:R no front — SINTAXE EXATA DE CADA SITE (verificado lendo o código;
a forma de interpolação é DIFERENTE em cada um e usar a errada é inerte):

  web/src/copy.js:134   (dentro do ramo `operador:`, NÃO existe equivalente no ramo
                        `estudo:` — o texto de estudo não cita R:R)
      subtituloRadar: "… e R:R — abaixo de 1,5:1 a mesa não opera.",
      -> string com ASPAS DUPLAS. `${}` não interpola em aspas. Converter a string
         inteira para BACKTICKS.

  web/src/copy.js:147   (também só no ramo `operador:`)
      comoAnalisaCorpo: "… alvos com R:R mínimo de 1,5:1 — abaixo disso, não se opera. …",
      -> string com ASPAS DUPLAS. Converter para BACKTICKS.

  web/src/App.jsx:4009  TEXTO JSX CRU dentro de <p style={{…}}> … </p>
      … e o R:R recalculado continuar ≥ 1,5:1 (Princípio 5), o alvo é estendido …
      -> NÃO é string. `${RR_MIN_TXT}` aqui renderiza LITERALMENTE na tela.
         A forma correta é a EXPRESSÃO JSX `{RR_MIN_TXT}:1` — sem cifrão, sem crase.

  web/src/App.jsx:4347  PROMPT_META.carteiraStopAlvoOperador.hint
      hint: "… R:R mínimo de 1,5:1, execução sempre do usuário. …",
      -> string com ASPAS DUPLAS. Converter para BACKTICKS.

  web/src/catalog.js:45   dentro de defaultSkillTextOperador()      -> ESPELHO de defaults.default_skill_text_operador()
  web/src/catalog.js:100  dentro do template literal carteiraStopAlvo         -> PARIDADE BYTE-A-BYTE
  web/src/catalog.js:147  dentro do template literal carteiraStopAlvoOperador -> PARIDADE BYTE-A-BYTE
      -> os TRÊS ficam literais. Ver <decision_note>.

web/src/App.jsx:15 já importa de finance.js:
  import { portfolioMetrics, dayReturnPct, equityCurve, markPrice, sizingPlano } from "./finance.js";
web/src/copy.js:15 hoje importa só:
  import { DISCLAIMERS } from "./disclaimers.js";

Guardiões que leem CÓDIGO-FONTE e reagem a estes literais (verificado por grep):
  web/tests/test_alvo_dinamico_ui.mjs:37
      ok("cita o piso de R:R do Princípio 5", /R:R recalculado continuar ≥ 1,5:1/.test(src));
      -> `src` é o TEXTO-FONTE de App.jsx. Interpolar quebra este guardião: ele precisa ser
         ATUALIZADO (com nota), nunca apagado.
  server/tests/test_auditoria_prompts.py:197-210  test_a8ii_paridade_defaults_carteira_com_catalog_js
      m = re.search(chave + r":\s*`([^`]*)`", src);  assert m.group(1) == prompts[chave]
      -> compara o TEXTO-FONTE do template literal do catalog.js com a string do servidor.
         Interpolar `${...}` nesses dois blocos QUEBRA a paridade (o fonte deixa de ser
         igual à string avaliada). Estes dois literais NÃO podem virar interpolação.
  server/tests/test_auditoria_prompts.py:166-172  test_a8_rr_min_fonte_unica  -> guardião a ESTENDER

Como `scripts/executar.sh` roda os runners web (importa para o runner novo):
  cwd = `web/`, saída redirecionada para /dev/null, só o exit code vira `[ok]`/`[X]`.
  Consequência: caminho relativo errado num `readFileSync` vira `[X]` MUDO. Por isso
  o caminho do `skill_ref.py` é resolvido por `new URL(..., import.meta.url)` — o
  mesmo raciocínio que `test_auditoria_prompts.py` já aplica no lado Python.
</interfaces>

<decision_note>
06-CONTEXT.md sugere "uma constante única exportada que os 3 arquivos [do front]
importam". Isso é possível em `copy.js` e `App.jsx`, mas NÃO em `catalog.js`
linhas 100/147: aquele texto é espelho byte-a-byte de `defaults.py` e o guardião
de paridade compara o CÓDIGO-FONTE, não a string avaliada — interpolar ali
derruba o guardrail de prompts do repo, que é invariante ("prompts, byte a byte
— teste trava"). `catalog.js:45` é espelho de `default_skill_text_operador()` e
segue a mesma regra. Decisão: interpolar onde é texto de UI; manter literal onde
é espelho de prompt, e amarrar esses literais à fonte única por TESTE CRUZADO em
vez de por import. O efeito prático pedido pelo CONTEXT (divergiu ⇒ suíte
quebra) é entregue igual.
</decision_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: os dois motores Python importam o R:R de skill_ref + guardião cruzado</name>
  <files>server/app/setups.py, server/app/agent.py, server/tests/test_auditoria_prompts.py</files>
  <read_first>
    - server/app/skill_ref.py linhas 25-35 (RR_MIN, RR_IDEAL e os _TXT)
    - server/app/setups.py linhas 545-565 (cabeçalho de regras + `RR_MINIMO`) e linha 25 (bloco de imports atual)
    - server/app/setups.py linhas 630-640 (uso de `RR_MINIMO` no gate — só leitura, NÃO alterar)
    - server/app/agent.py linhas 435-455 (`MAX_ALVO_EXTENSOES`, `ALVO_ATR_MULT`, `RR_MINIMO`) e linhas 24-31 (imports)
    - server/tests/test_auditoria_prompts.py linhas 164-215 (seção A8/A8ii, guardiões a estender)
  </read_first>
  <behavior>
    - `setups.RR_MINIMO == skill_ref.RR_MIN` e `agent.RR_MINIMO == skill_ref.RR_MIN`, com valor 1.5.
    - O guardião novo falha se alguém redeclarar um literal numérico em `setups.py` ou `agent.py` (assertiva sobre o CÓDIGO-FONTE, não só sobre o valor em runtime — senão trocar `skill_ref.RR_MIN` por outro 1.5 hardcoded passaria batido).
    - `agent.ALVO_ATR_MULT` continua `1.5` e continua sendo uma constante independente (o guardião não pode amarrá-la a `RR_MIN`).
    - Todos os testes existentes de `plano_operacional` e do alvo dinâmico passam sem alteração (gate de R:R inalterado em comportamento).
  </behavior>
  <action>
    1. `server/app/setups.py`: acrescentar `from . import skill_ref` ao bloco de
    imports (hoje o módulo importa só `typing.Optional`; `skill_ref` não importa
    nada, então não há ciclo). Trocar `RR_MINIMO = 1.5` (linha 559) por
    `RR_MINIMO = skill_ref.RR_MIN`, preservando o comentário lateral existente e
    acrescentando a referência ADR15-05. Não tocar em mais nada do módulo — o
    gate (`if rr2 < RR_MINIMO`, linhas ~635-637), `_confluencia`,
    `plano_operacional` e `detect_setups` estão explicitamente fora do escopo da
    fase.

    2. `server/app/agent.py`: acrescentar `skill_ref` ao import existente
    (`from . import db, store, skill_ref` ou linha própria, o que casar com o
    estilo do arquivo). Trocar `RR_MINIMO = 1.5` (linha 446) por
    `RR_MINIMO = skill_ref.RR_MIN`. NÃO tocar em `ALVO_ATR_MULT = 1.5` (linha
    445): é o multiplicador de ATR do alvo dinâmico, outro número que por acaso
    vale 1,5. Acrescentar comentário de uma linha dizendo isso, para o próximo
    leitor não "consolidar" o errado.

    3. `server/tests/test_auditoria_prompts.py`, na seção "A8 — R:R mínimo/ideal
    com fonte única": acrescentar `test_a8iii_rr_min_fonte_unica_nos_dois_motores_e_no_front`,
    que assere:
       - `setups.RR_MINIMO == skill_ref.RR_MIN` e `agent.RR_MINIMO == skill_ref.RR_MIN`;
       - o código-fonte de `setups.py` e de `agent.py` contém `RR_MINIMO = skill_ref.RR_MIN`
         (leitura do arquivo, como `test_a8ii_paridade_defaults_carteira_com_catalog_js`
         já faz com o `catalog.js`) — prova que a fonte é a importada e não um literal novo;
       - `agent.ALVO_ATR_MULT == 1.5` continua declarado como literal próprio (guardião
         de que a consolidação não invadiu a constante errada);
       - `web/src/finance.js` declara `RR_MIN` e `RR_MIN_TXT` com os MESMOS valores de
         `skill_ref` (regex no fonte: `RR_MIN = ([0-9.]+)` e `RR_MIN_TXT = "([^"]+)"`;
         aqui o lado Python compara contra `skill_ref.RR_MIN`/`skill_ref.RR_MIN_TXT` em
         RUNTIME — o módulo está importado, então o valor computado está disponível e
         não é preciso parsear o próprio `skill_ref.py`);
       - em `web/src/copy.js`, `web/src/App.jsx` e `web/src/catalog.js`, TODA ocorrência
         do padrão `(\d,\d):1` é igual a `skill_ref.RR_MIN_TXT + ":1"` — divergiu, o teste
         acusa e diz qual arquivo.
    A docstring do teste explica por que ele existe: ADR-015 mostrou 3 constantes
    Python + 7 literais no front com um único guardião cobrindo um deles.

    Escrever o teste ANTES das mudanças de código (RED) — ele deve falhar
    primeiro por causa dos literais de `setups`/`agent`, e a parte do front só
    fecha depois da Task 2 (é esperado; a Task 1 fica com o teste vermelho na
    metade do front até a Task 2 rodar, e a verificação final é a da Task 3).
  </action>
  <verify>
    <automated>(cd server && ./.venv/bin/python -m pytest tests/test_setups.py tests/test_setups_br.py tests/test_plano_operacional.py tests/test_agent.py tests/test_skill_ref.py -q)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RR_MINIMO = skill_ref.RR_MIN" server/app/setups.py server/app/agent.py` retorna 1 em cada arquivo.
    - `grep -n "RR_MINIMO = 1.5" server/app/setups.py server/app/agent.py` não retorna nada.
    - `grep -n "ALVO_ATR_MULT = 1.5" server/app/agent.py` retorna 1 linha (constante independente preservada).
    - `(cd server && ./.venv/bin/python -c "from app import setups, agent, skill_ref; print(setups.RR_MINIMO, agent.RR_MINIMO, skill_ref.RR_MIN)")` imprime `1.5 1.5 1.5`.
    - `grep -n "def test_a8iii" server/tests/test_auditoria_prompts.py` retorna 1 linha.
    - `pytest tests/test_setups.py tests/test_plano_operacional.py tests/test_agent.py -q` passa 100% sem edição de teste existente.
  </acceptance_criteria>
  <done>`setups.RR_MINIMO` e `agent.RR_MINIMO` leem de `skill_ref.RR_MIN`; `test_a8iii` cobre os dois motores e o front; `ALVO_ATR_MULT` intacto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fonte única do R:R no front (sintaxe correta por site) + guardião JS cruzado</name>
  <files>web/src/finance.js, web/src/copy.js, web/src/App.jsx, web/src/catalog.js, web/tests/test_alvo_dinamico_ui.mjs, web/tests/test_rr_min_fonte_unica.mjs</files>
  <read_first>
    - web/src/finance.js linhas 1-30 (cabeçalho de definições e primeiro export — onde a constante entra)
    - web/src/copy.js linhas 1-20 (cabeçalho, convenções e o import de disclaimers.js), linhas 18-50 (ramo `estudo:`, que NÃO cita R:R) e linhas 120-150 (ramo `operador:`, onde estão os dois literais)
    - web/src/App.jsx linha 15 (import de finance.js), linhas 4005-4012 (texto JSX cru) e 4344-4350 (string `hint:`)
    - web/src/catalog.js linhas 30-55, 88-105 e 140-150
    - server/app/skill_ref.py linhas 25-35 (CONFIRMAR de olho: `RR_MIN = 1.5` é literal; `RR_MIN_TXT` é `f"{RR_MIN:g}".replace(".", ",")`, computado — não existe como string literal no fonte)
    - web/tests/test_alvo_dinamico_ui.mjs (arquivo inteiro — é curto; entender o que `src` é)
    - web/tests/ (qualquer runner recente, para copiar o estilo: `ok(nome, cond)`, contador `fails`, `process.exit`)
    - server/tests/test_auditoria_prompts.py linhas 196-210 (o guardião de paridade que proíbe interpolar catalog.js:100/147)
  </read_first>
  <behavior>
    - `import { RR_MIN, RR_MIN_TXT } from "./finance.js"` devolve `1.5` e `"1,5"`.
    - `COPY.operador.subtituloRadar` e `COPY.operador.comoAnalisaCorpo` contêm `"1,5:1"` EM RUNTIME — texto final byte-a-byte idêntico ao de hoje. (O ramo `estudo` não cita R:R e não muda.)
    - O código-fonte de `web/src/copy.js` e de `web/src/App.jsx` NÃO contém mais o literal `1,5:1`.
    - No fonte de `copy.js`, as duas linhas alteradas usam CRASE e `${RR_MIN_TXT}:1`.
    - No fonte de `App.jsx`, o parágrafo do alvo dinâmico usa a expressão JSX `{RR_MIN_TXT}:1` — e NÃO contém a sequência `${` (que ali seria texto literal na tela).
    - No fonte de `App.jsx`, a string `hint:` de `carteiraStopAlvoOperador` usa CRASE e `${RR_MIN_TXT}:1`.
    - O código-fonte de `web/src/catalog.js` CONTINUA com os 3 literais `1,5:1` (interpolar ali quebraria a paridade byte-a-byte com `defaults.py`).
    - `web/tests/test_rr_min_fonte_unica.mjs` falha, com mensagem nomeando o arquivo, se: o NÚMERO `RR_MIN` parseado do fonte de `server/app/skill_ref.py` divergir do `RR_MIN` do front (ou o texto derivado dele divergir do que o front exibe); sobrar `1,5:1` literal em copy/App; sobrar `${` no trecho JSX; ou o `skill_ref.py` não for legível no caminho resolvido.
    - `npx vite build` conclui sem erro (JSX válido após a interpolação).
  </behavior>
  <action>
    1. `web/src/finance.js`: exportar a fonte única, logo depois do bloco de
    comentário de definições e antes de `markPrice`:
    `RR_MIN` (número 1.5) e `RR_MIN_TXT` (string "1,5", formato pt-BR para
    interpolação em texto). Comentário obrigatório: espelho de
    `server/app/skill_ref.py` (RR_MIN/RR_MIN_TXT), amarrado por
    `web/tests/test_rr_min_fonte_unica.mjs` e por `test_a8iii` no backend;
    `finance.js` é o módulo de números determinísticos do front, é onde a
    constante pertence.

    2. `web/src/copy.js`: acrescentar `import { RR_MIN_TXT } from "./finance.js";`
    ao lado do import de `disclaimers.js`. Alterar EXATAMENTE as duas linhas do
    ramo `operador:` (134 `subtituloRadar` e 147 `comoAnalisaCorpo`) — o ramo
    `estudo:` não cita R:R e não deve ser tocado. As duas hoje são strings com
    ASPAS DUPLAS: trocar as aspas por CRASES (template literal) e o `1,5:1` por
    `${RR_MIN_TXT}:1`. Aspas duplas com `${}` NÃO interpolam — sairia
    `${RR_MIN_TXT}:1` literal na tela, passando por grep e por build. O texto
    final tem de sair IDÊNTICO ao de hoje.

    3. `web/src/App.jsx`: acrescentar `RR_MIN_TXT` ao import já existente de
    `./finance.js` (linha 15). Os dois sites têm sintaxe DIFERENTE:
       - linha ~4009 — TEXTO JSX CRU dentro de `<p …>…</p>`: usar a EXPRESSÃO JSX
         `{RR_MIN_TXT}:1`, isto é `… o R:R recalculado continuar ≥ {RR_MIN_TXT}:1
         (Princípio 5) …`. SEM cifrão e SEM crase: `${RR_MIN_TXT}` em texto JSX
         renderiza literalmente na tela do usuário.
       - linha ~4347 — `hint:` de `PROMPT_META.carteiraStopAlvoOperador`, string
         com ASPAS DUPLAS: converter para CRASES e usar `${RR_MIN_TXT}:1`.
       NÃO tocar na linha 252 (`const rr = ... 1.5 : ... 2.5 : 2.0`): é o R:R-ALVO
       por horizonte do perfil, não o piso do gate — consolidar ali mudaria
       comportamento de sizing.

    4. `web/src/catalog.js`: NÃO interpolar. Acrescentar um comentário curto acima
    de `defaultSkillTextOperador()` e acima dos dois template literals
    (`carteiraStopAlvo`, `carteiraStopAlvoOperador`) explicando que os literais de
    R:R ficam literais de propósito — o guardião
    `test_a8ii_paridade_defaults_carteira_com_catalog_js` compara o CÓDIGO-FONTE
    com a string do servidor, e a amarração com a fonte única é feita por
    `test_a8iii` (Python) e `test_rr_min_fonte_unica.mjs` (JS).

    5. `web/tests/test_alvo_dinamico_ui.mjs` linha 37: ATUALIZAR o guardião para
    casar com a forma interpolada — a regex passa a exigir a EXPRESSÃO JSX, algo
    como `/R:R recalculado continuar ≥ \{RR_MIN_TXT\}:1/` — mantendo a intenção
    original (o texto do alvo dinâmico cita o piso de R:R). Acrescentar nota no
    comentário do teste dizendo que a mudança é ADR15-05 (fonte única), não
    afrouxamento — guardião do repo não se apaga, se atualiza com nota. Manter a
    linha 35 (`1,5× o ATR(14)`) como está: é o ATR, não o R:R.

    6. Criar `web/tests/test_rr_min_fonte_unica.mjs` no estilo dos runners do
    diretório (`import { readFileSync }`, helper `ok(nome, cond)`, contador
    `fails`, `process.exit(fails ? 1 : 0)`). Regras obrigatórias:

       a. TODO caminho de arquivo é resolvido por
          `new URL("../src/…", import.meta.url)` / `new URL("../../server/app/skill_ref.py",
          import.meta.url)` — NUNCA caminho relativo ao cwd. `scripts/executar.sh`
          roda os runners com `cwd=web` e joga a saída em /dev/null: um caminho
          errado viraria um `[X]` mudo, sem diagnóstico. Envolver a leitura do
          `skill_ref.py` num try/catch que, na falha, imprime a mensagem
          `[X] skill_ref.py ilegível em <caminho resolvido> — o guardião cruzado
          front×backend não rodou` (com o caminho via `fileURLToPath`) e conta
          como falha. Comentário no topo do arquivo explicando essa regra e o
          motivo (mesmo raciocínio que `test_auditoria_prompts.py` aplica do lado
          Python). O caminho do `skill_ref.py` aceita override pela variável de
          ambiente `B3_SKILL_REF_PATH` (default = o resolvido por `new URL`),
          existente APENAS para a sabotagem controlada em sandbox exigida no
          aceite: assim o guardião é exercitado contra uma CÓPIA alterada, sem
          nunca mutar o arquivo real do servidor.

       b. Checagens:
          - `RR_MIN === 1.5` e `RR_MIN_TXT === "1,5"` importados de `../src/finance.js`;
          - CRUZAMENTO FRONT × BACKEND, feito sobre o VALOR e não sobre a grafia da
            linha de código: parsear do fonte de `server/app/skill_ref.py` SOMENTE
            o número, com a regex `/RR_MIN\s*=\s*([0-9.]+)/`. NÃO procurar
            `RR_MIN_TXT = "1,5"` como literal de fonte — o arquivo real (linhas
            30-33) declara `RR_MIN = 1.5` e logo abaixo
            `RR_MIN_TXT = f"{RR_MIN:g}".replace(".", ",")`, ou seja o `"1,5"` é
            COMPUTADO em runtime e nunca aparece verbatim no fonte; um match
            literal falharia já na primeira execução. A partir do número parseado,
            DERIVAR em JS o texto esperado (`String(parseFloat(m[1])).replace(".", ",")`,
            que dá `"1,5"` para `1.5`) e assertar: (i) o número parseado ===
            `RR_MIN` do front; (ii) o texto derivado === `RR_MIN_TXT` do front;
            (iii) o texto derivado é o que os textos do front de fato exibem
            (checagens de `COPY.operador.*` e do trecho JSX "renderizado" logo
            abaixo). Se a regex não casar, falhar com mensagem explícita
            (`RR_MIN não encontrado em <caminho>`) em vez de seguir com `NaN`;
          - `COPY.operador.subtituloRadar` e `COPY.operador.comoAnalisaCorpo`
            contêm `RR_MIN_TXT + ":1"` EM RUNTIME (import de `../src/copy.js`) —
            é esta checagem que pega a interpolação morta em string aspeada;
          - o fonte de `copy.js` e de `App.jsx` não contém mais `1,5:1` literal;
          - ANTI-INTERPOLAÇÃO-MORTA EM JSX (o caso que nenhuma das checagens acima
            pega, porque `App.jsx` não é importável em Node): recortar do fonte de
            `App.jsx` o trecho do parágrafo do alvo dinâmico (do `<p` que precede
            "R:R recalculado" até o `</p>` seguinte) e assertar que ele
            (i) contém `{RR_MIN_TXT}:1`, (ii) NÃO contém a sequência `${`, e
            (iii) ao substituir `{RR_MIN_TXT}` pelo texto DERIVADO do backend,
            produz a frase `R:R recalculado continuar ≥ 1,5:1` — a "renderização"
            simulada, que é a assertiva POSITIVA de que o usuário vê o número do
            backend e não o nome da variável;
          - o fonte de `App.jsx` na linha do `hint:` de `carteiraStopAlvoOperador`
            usa crase e contém `${RR_MIN_TXT}:1`;
          - toda ocorrência de `(\d,\d):1` no fonte de `catalog.js` é igual a
            `RR_MIN_TXT + ":1"` (os 3 espelhos de prompt seguem literais e alinhados).
       Cabeçalho do arquivo explicando a lacuna que ele fecha (ADR-015: 3 constantes
       + 7 literais, 1 guardião), por que a checagem de `${` existe e por que o
       cruzamento com o backend parseia o NÚMERO e deriva o texto (o `_TXT` do
       Python não é literal de fonte).
  </action>
  <verify>
    <automated>(cd web && node tests/test_rr_min_fonte_unica.mjs && node tests/test_alvo_dinamico_ui.mjs && node tests/test_copy_theme.mjs && npx vite build >/dev/null && echo BUILD_OK)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const RR_MIN" web/src/finance.js` >= 1 e `grep -c "RR_MIN_TXT" web/src/finance.js` >= 1.
    - `grep -c "1,5:1" web/src/copy.js web/src/App.jsx` retorna 0 em cada arquivo.
    - `grep -c "1,5:1" web/src/catalog.js` retorna 3 (os espelhos de prompt permanecem literais).
    - `grep -c '${RR_MIN_TXT}:1' web/src/copy.js` == 2 (as duas linhas do ramo operador, agora em crase).
    - `grep -c '{RR_MIN_TXT}:1' web/src/App.jsx` == 2 (a expressão JSX + a string `hint:`) e `grep -n '${RR_MIN_TXT}:1' web/src/App.jsx` retorna APENAS a linha do `hint:` (o texto JSX não pode ter cifrão).
    - `grep -n "RR_MIN_TXT" web/src/copy.js web/src/App.jsx` mostra o import em cada arquivo além das interpolações.
    - O runner NÃO casa `RR_MIN_TXT` como literal de fonte do Python: `grep -c 'RR_MIN_TXT\s*=\s*"' web/tests/test_rr_min_fonte_unica.mjs` == 0 e `grep -c 'RR_MIN\\\\s\*=' web/tests/test_rr_min_fonte_unica.mjs` >= 1 (parse só do número).
    - `(cd web && node -e "import('./src/copy.js').then(m=>console.log(m.COPY.operador.subtituloRadar.includes('1,5:1'), m.COPY.operador.comoAnalisaCorpo.includes('1,5:1')))")` imprime `true true`.
    - `(cd web && node tests/test_rr_min_fonte_unica.mjs)` sai com código 0 e imprime as checagens (nenhum `[X]`).
    - Sabotagem controlada nº 1 — JSX (rodar e DESFAZER): trocar em `App.jsx` a expressão JSX por `${RR_MIN_TXT}:1` faz `node tests/test_rr_min_fonte_unica.mjs` sair com código 1. Registrar no SUMMARY que a verificação foi feita e revertida.
    - Sabotagem controlada nº 2 — backend, em SANDBOX (sem tocar no arquivo real): copiar `server/app/skill_ref.py` para um diretório temporário, trocar na CÓPIA `RR_MIN = 1.5` por `RR_MIN = 2.0` e rodar `(cd web && B3_SKILL_REF_PATH=<caminho-da-cópia> node tests/test_rr_min_fonte_unica.mjs)` → sai com código 1, acusando divergência front × backend (front diz `1,5`, backend derivado diz `2`). Registrar no SUMMARY que a sabotagem foi feita em cópia e que `server/app/skill_ref.py` nunca foi mutado (`git diff server/app/skill_ref.py` vazio).
    - `(cd web && npx vite build)` conclui com exit code 0 (obrigatório por CLAUDE.md quando o front é editado).
    - `git diff web/tests/test_alvo_dinamico_ui.mjs` ATUALIZA a assertiva da linha 37 e não a remove.
  </acceptance_criteria>
  <done>`finance.js` exporta `RR_MIN`/`RR_MIN_TXT`; `copy.js` e o `hint:` do `App.jsx` usam crase com `${RR_MIN_TXT}`, o texto JSX usa `{RR_MIN_TXT}`; `catalog.js` segue literal; guardião novo (cruzamento por VALOR com o `skill_ref.py`, anti-`${` e caminho por `import.meta.url`) e o atualizado verdes; as duas sabotagens controladas falham como esperado; `vite build` ok.</done>
</task>

<task type="auto">
  <name>Task 3: verificação canônica das duas suítes + build do front</name>
  <files>—</files>
  <read_first>
    - CLAUDE.md (seções "Validação obrigatória" e "Guardrails do repositório")
    - server/tests/test_auditoria_prompts.py (guardiões de paridade de prompt)
  </read_first>
  <action>
    Rodar a suíte canônica (pytest + `web/tests/*.mjs`) e, como o front foi
    editado, `npx vite build` — grep e teste estático não pegam erro de sintaxe
    JS (CLAUDE.md). Conferir explicitamente que
    `test_a8ii_paridade_defaults_carteira_com_catalog_js` continua verde: é o
    guardrail invariante de prompt byte-a-byte, e é o teste que quebraria se
    alguém interpolasse `catalog.js` por engano.

    Conferir também, pelo runner novo, que o runner NÃO passou "por sorte": ele
    tem de rodar as duas leituras de arquivo (front e `skill_ref.py`) e imprimir a
    lista de checagens quando executado direto (fora do `executar.sh`, que
    silencia a saída).

    No SUMMARY, registrar: o valor NÃO mudou (1.5); quais literais viraram
    interpolação, em QUAL forma (crase vs expressão JSX) e quais ficaram literais
    e por quê; o resultado das DUAS sabotagens controladas da Task 2 (JSX e
    backend em sandbox, esta última sem mutar `server/app/skill_ref.py`); e que o
    guardião `test_alvo_dinamico_ui.mjs` foi atualizado (não removido), com a
    nota.
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes && (cd web && npx vite build >/dev/null && echo BUILD_OK)</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/executar.sh --testes` termina com exit code 0.
    - Nenhum `[X]` na saída das suítes web, incluindo o runner novo.
    - `(cd web && npx vite build)` conclui com exit code 0.
    - `(cd server && ./.venv/bin/python -m pytest tests/test_auditoria_prompts.py -q)` passa 100%.
    - `git diff server/app/skill_ref.py` está VAZIO (a sabotagem nº 2 foi feita em cópia).
    - O SUMMARY documenta a decisão sobre `catalog.js` (literal por causa da paridade byte-a-byte), a forma de interpolação usada em cada site e o resultado das duas sabotagens controladas.
  </acceptance_criteria>
  <done>Suíte canônica verde, `vite build` verde e decisões registradas no SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| constante de gate → decisão "NÃO OPERAR" | o R:R mínimo é o que barra um plano de geometria ruim; alterá-lo por acidente muda o que o produto recomenda |
| bundle do front → prompt do dispositivo | `catalog.js` carrega prompts embutidos no app iOS; divergir do servidor faz o aparelho operar com regra diferente da web |
| fonte do front → tela do usuário | interpolação inerte manda `${RR_MIN_TXT}` literal para a tela; passa em grep, em build e em teste ingênuo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Tampering | consolidação atingir a constante errada (`ALVO_ATR_MULT`, `App.jsx:252`) e mudar comportamento de execução/sizing | mitigate | Ambas nomeadas no plano como "NÃO CONSOLIDAR"; guardião `test_a8iii` assere `ALVO_ATR_MULT == 1.5` declarado à parte; suíte de `test_agent.py`/`test_plano_operacional.py` cobre o gate |
| T-06-14 | Tampering | interpolar `catalog.js` e quebrar a paridade byte-a-byte de prompt (aparelho passa a operar com prompt divergente) | mitigate | Literais mantidos + `test_a8ii_paridade_defaults_carteira_com_catalog_js` verde é critério de aceite explícito |
| T-06-21 | Tampering | interpolação inerte (`${}` em aspas duplas ou em texto JSX) mandando o nome da variável para a tela | mitigate | Sintaxe exata especificada por site; runner novo checa runtime do `copy.js`, ausência de `${` no trecho JSX e a frase "renderizada"; sabotagem controlada exigida no aceite |
| T-06-15 | Repudiation | guardião atualizado sem rastro do porquê | mitigate | Nota obrigatória no comentário de `test_alvo_dinamico_ui.mjs` e no SUMMARY (regra do repo: guardião não se apaga) |
| T-06-22 | Repudiation | runner novo falhando mudo por caminho relativo errado sob `executar.sh` | mitigate | Caminhos por `new URL(..., import.meta.url)` + try/catch com mensagem nomeando o caminho resolvido |
| T-06-23 | Repudiation | guardião cruzado casando `RR_MIN_TXT` como literal do fonte Python (valor é computado em runtime) e falhando/passando por motivo errado | mitigate | Regra explícita: parsear só `RR_MIN\s*=\s*([0-9.]+)` e DERIVAR o texto em JS; sabotagem nº 2 em sandbox prova que a divergência é detectada sem mutar o arquivo real |
| T-06-SC | Tampering | instalação de pacote | accept | Nenhuma dependência nova em `web/package.json` nem em `server/requirements.txt`; `npx vite build` usa o Vite já instalado |
</threat_model>

<verification>
1. `bash scripts/executar.sh --testes` verde + `npx vite build` verde.
2. `setups.RR_MINIMO`, `agent.RR_MINIMO` e `finance.RR_MIN` todos derivam/espelham `skill_ref.RR_MIN`, com teste que falha na divergência.
3. Nenhum `${` sobrevive em texto JSX; o texto exibido continua `1,5:1`.
4. Valor numérico inalterado (1.5) em todos os pontos.
5. `git diff --stat` limitado aos 9 arquivos de `files_modified` (`server/app/skill_ref.py` NÃO aparece).
</verification>

<success_criteria>
- Existe uma única constante-fonte do R:R mínimo por camada (`skill_ref.RR_MIN` no backend, `finance.js` no front) e um teste guardião cruzado que falha se qualquer ponto divergir. [ADR15-05]
- O guardião cruzado JS compara VALOR (número parseado do `skill_ref.py` + texto derivado dele), nunca a grafia de `RR_MIN_TXT`, que é computada em runtime no Python.
- `setups.RR_MINIMO` e `agent.RR_MINIMO` leem de `skill_ref`; `copy.js` e `App.jsx` leem de `finance.js`, cada site com a sintaxe que de fato interpola.
- Nenhum `${RR_MIN_TXT}` literal chega à tela — provado por checagem de runtime, por ausência de `${` no trecho JSX e por sabotagem controlada.
- `catalog.js` mantém os literais de prompt espelhado, cobertos por teste, com o motivo documentado no código e no SUMMARY.
- O valor continua 1.5 e nenhum comportamento de gate mudou.
</success_criteria>

<output>
Create `.planning/phases/06-instrumentacao-assertividade-adr015/06-05-SUMMARY.md` when done
</output>
