---
phase: 05-corre-o-m-dio-c-digo-gate-admin
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/App.jsx
  - web/tests/test_fase5_appmode_fonte_unica.mjs
  - web/tests/test_entrada_automatica_ui.mjs
autonomous: true
requirements: [FIX-C21, FIX-C23]

must_haves:
  truths:
    - "Existe uma única derivação de 'estamos em Modo Operador?' em App.jsx; todo consumidor lê dela"
    - "Um guardião falha se uma leitura independente de data.config.appMode voltar ao arquivo"
    - "O toggle mestre de Entrada automática tem atributo HTML disabled real fora do Modo Operador"
    - "O toggle desabilitado fica visivelmente inerte (opacidade e cursor), no mesmo padrão dos dois controles gateados vizinhos"
  artifacts:
    - path: "web/src/App.jsx"
      provides: "Leituras de appMode migradas para a fonte única + Toggle com prop disabled"
      contains: "ctx.operador"
    - path: "web/tests/test_fase5_appmode_fonte_unica.mjs"
      provides: "Guardião estático que fecha a CLASSE do erro de recomputação"
    - path: "web/tests/test_entrada_automatica_ui.mjs"
      provides: "Guardião existente estendido com a seção FIX-C23"
  key_links:
    - from: "componentes de tela em App.jsx"
      to: "ctx.operador"
      via: "leitura da fonte única em vez de recomputar"
      pattern: "ctx\\.operador"
    - from: "call site do toggle Entrada automática"
      to: "componente Toggle"
      via: "prop disabled aplicada ao <button>"
      pattern: "disabled=\\{!operador\\}"
---

<objective>
Fechar FIX-C21 (10 pontos recalculam `appMode` de forma independente em vez de
ler a fonte única) e FIX-C23 (o toggle mestre "Entrada automática" não tem
atributo HTML `disabled` real nem feedback próprio).

Purpose: os dois são "dívida silenciosa que ainda não quebrou". C-21 é a
classe de erro que já produziu DOIS incidentes de paridade neste projeto
(carteira nativa e sincronização de `appMode`); C-23 é o único dos três
controles gateados da mesma tela sem `disabled` — teclado e leitor de tela
ainda anunciam um controle operável que não faz nada.
Output: `App.jsx` com uma única derivação de modo e o Toggle com `disabled`;
um guardião novo e um estendido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-CONTEXT.md
@.planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-UI-SPEC.md
@CLAUDE.md
</context>

<interfaces>
<!-- Estado verificado por grep em 2026-08-23. As linhas do REPORT-01 (auditoria
     de 2026-08-18) NÃO valem mais: App.jsx passou de 7.2k para 8.181 linhas
     depois das Fases 2/3/4/8. Re-grepar antes de editar. -->

Fonte única, já existente (`web/src/App.jsx:7788-7794`):
```js
const ctx = {
  // qa/audit-2026-08-07 (item 5): fonte ÚNICA de "estamos em Modo Operador?"
  // Novo código deve ler `ctx.operador`, não redevirar de data.config.appMode.
  operador: !!(data && data.config && data.config.appMode === "operador"),
```
Derivação local dentro de `App()` (linha 6836), usada por tema/classe/copy:
```js
const appMode = (data && data.config && data.config.appMode) === "operador" ? "operador" : "estudo";
```

Inventário completo das 12 leituras (grep `appMode` em 2026-08-23):

| Linha | Escopo | Forma atual | Destino |
|-------|--------|-------------|---------|
| 1767 | `EvolucaoScreen({ ctx })` | `const operador = (data.config && data.config.appMode) === "operador"` | `ctx.operador` |
| 1979 | `ModoTrabalhoCard({ ctx })` | `const mode = c.appMode === "operador" ? "operador" : "estudo"` | `ctx.operador ? "operador" : "estudo"` |
| 2199 | `AjudaScreen({ ctx })` | `const operador = (ctx.data.config && ctx.data.config.appMode) === "operador"` | `ctx.operador` |
| 3376 | `MercadoScreen({ ctx })` | idem 1767 | `ctx.operador` |
| 4540 | `SkillSection({ ctx, ... })` | `const modoAtivo = ... ? "operador" : "estudo"` | `ctx.operador ? "operador" : "estudo"` |
| 5996 | `RadarScreen({ ctx })` | idem 1767, dentro de um callback de render | `ctx.operador` |
| 6196 | `RadarScreen({ ctx })` | ternário inline escolhendo `DISCLAIMERS.operador`/`DISCLAIMERS.radar` | `ctx.operador ? ... : ...` |
| 6836 | `App()` | `const appMode = ...` | **PERMANECE** — é a derivação única dentro de `App()` |
| 7018 | `App()` | `const modoApp = (data && data.config && data.config.appMode) \|\| "estudo"` | `appMode` (variável local da linha 6836) |
| 7430 | `App()` (`runStopAlvoFor`) | `(((data.config \|\| {}).appMode === "operador") ? ... : ...)` | `appMode === "operador" ? ... : ...` |
| 7794 | `App()` (montagem de `ctx`) | recomputa de `data.config.appMode` | `operador: appMode === "operador"` |
| 7993 | `App()` (snapshot `perfil`) | `modo: cfg.appMode \|\| "estudo"` | `modo: appMode` |

Escritas de `appMode` (NÃO tocar): linhas 2005 (`A.saveConfig({ appMode: m })`)
e 2078 (`A.saveConfig({ ..., appMode: "operador" })`). Nenhuma casa com
`.appMode` como LEITURA, então não colidem com o guardião.

Componente `Toggle` (`web/src/App.jsx:328-336`) — hoje sem `disabled`:
```js
function Toggle({ on, onClick, label }) {
  const s = { bg: on ? T.accentTintHi : T.knob, border: on ? T.accent : T.borderSubtle, knob: on ? "24px" : "2px", color: on ? T.accent : T.textFaint };
  return (
    <button onClick={onClick} role="switch" aria-checked={on} aria-label={label}
      style={{ position: "relative", width: "50px", height: "28px", ... }}>
      <span style={{ position: "absolute", top: "2px", left: s.knob, ... }} />
    </button>
  );
}
```
Call site alvo (linha 4240):
```js
<Toggle on={!!ag.entradaAuto && operador} onClick={() => operador && putAg({ entradaAuto: !ag.entradaAuto })} label="Entrar automaticamente" />
```
Irmãos JÁ corretos na MESMA tela, cujo padrão numérico deve ser copiado:
- slider `allocPct` (linha ~4247): `disabled={!operador}`, seção envolta em `opacity: operador ? 1 : 0.6`
- botão "Executar" (linha ~4104): `disabled={desabilitado}`, `opacity: desabilitado ? 0.6 : 1`, `cursor: desabilitado ? "not-allowed" : "pointer"`

Outros 8 call sites de `<Toggle>` no arquivo (4090, 4269, 4425, 4438, 4448,
4657, 4676, 4681) — **fora do escopo de C-23**; a prop nova é opcional e o
default mantém o comportamento atual deles.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrar as leituras de appMode para a fonte única (FIX-C21)</name>
  <files>web/src/App.jsx</files>
  <read_first>
    - web/src/App.jsx — NÃO ler o arquivo inteiro (8.181 linhas). Rodar `grep -n "appMode" web/src/App.jsx` primeiro e ler apenas as janelas de ±15 linhas em torno de cada ocorrência, mais o bloco 7788-7800 (montagem de `ctx`) e 6830-6845 (derivação local)
    - .planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-CONTEXT.md — decisão de C-21
  </read_first>
  <action>
    Primeiro RE-GREPAR (`grep -n "appMode" web/src/App.jsx`) e confrontar com a
    tabela do bloco `<interfaces>`. As linhas foram medidas em 2026-08-23; se
    divergirem, a TABELA vale como intenção e o grep vale como endereço.
    Registrar no SUMMARY qualquer ocorrência encontrada que não esteja na
    tabela — é sinal de que outra fase introduziu recomputação nova.

    Aplicar a migração linha a linha, conforme a coluna "Destino". Regras:
      • componentes que recebem `{ ctx }` leem `ctx.operador` (ou
        `ctx.operador ? "operador" : "estudo"` quando o consumidor quer a
        string, não o booleano) — nunca reintroduzir a expressão de guarda;
      • pontos dentro de `App()` leem a variável local `appMode` (linha 6836),
        que é a derivação única daquele escopo;
      • a montagem de `ctx` (linha 7794) passa a derivar de `appMode`
        (`operador: appMode === "operador"`), eliminando a última recomputação
        — hoje até a "fonte única" recomputa por conta própria;
      • linha 6836 PERMANECE como está: é a origem.

    É migração MECÂNICA: mesmo valor calculado, uma origem só. Nenhuma
    mudança de comportamento é aceitável. Para cada substituição, conferir
    que a expressão nova é logicamente idêntica à antiga — atenção ao
    fallback `|| "estudo"` da linha 7018 e ao `(data.config || {})` da 7430,
    que a variável `appMode` já cobre por construção (ela sempre devolve
    `"operador"` ou `"estudo"`).

    Atualizar o comentário da linha 7789-7791: ele diz "antes recalculado de
    forma independente em 10+ lugares" — passar para o tempo presente,
    registrando que FIX-C21 (2026-08-23) fechou a migração e que o guardião
    `web/tests/test_fase5_appmode_fonte_unica.mjs` trava a classe do erro.

    Não tocar nas duas ESCRITAS (`saveConfig({ appMode: ... })`) nem em
    `web/src/persistence.js`.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/bolsia/b3-agente/web && npx vite build && cd .. && bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx vite build` conclui sem erro (grep e teste estático não pegam erro de sintaxe JSX — guardrail do CLAUDE.md)
    - `grep -v '^\s*//' web/src/App.jsx | grep -c '\.appMode'` retorna 1, e a única ocorrência está na linha da derivação `const appMode =`
    - `grep -c "ctx.operador" web/src/App.jsx` retorna >= 7
    - `grep -n "operador: appMode === \"operador\"" web/src/App.jsx` casa (a fonte única deixou de recomputar)
    - `bash scripts/executar.sh --testes` exit 0, incluindo `web/tests/test_appmode_sincroniza_servidor.mjs` e `test_decisao_modo.mjs` intactos
  </acceptance_criteria>
  <done>Existe uma única derivação de modo em `App.jsx`; todos os 11 pontos restantes leem dela; nenhum comportamento mudou.</done>
</task>

<task type="auto">
  <name>Task 2: Toggle com disabled real no gate de Entrada automática (FIX-C23)</name>
  <files>web/src/App.jsx</files>
  <read_first>
    - web/src/App.jsx — linhas 320-345 (componente `Toggle` e vizinhos), 4085-4115 (botão "Executar", padrão numérico de dimming), 4230-4265 (o call site alvo, o slider `allocPct` e o parágrafo explicativo)
    - .planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-UI-SPEC.md — seção "FIX-C23 — Toggle disabled", contrato de 7 itens
  </read_first>
  <action>
    Aplicar o contrato do UI-SPEC, item por item:

    1. Estender a assinatura para `Toggle({ on, onClick, label, disabled })`.
    2. Aplicar `disabled={disabled}` no `<button>` — atributo HTML REAL, que é
       o ponto inteiro de C-23, não enfeite.
    3. Somar ao objeto de estilo já existente do botão:
       `opacity: disabled ? 0.6 : 1` e `cursor: disabled ? "not-allowed" : "pointer"`.
       São os MESMOS valores numéricos do botão "Executar" e da seção do
       slider `allocPct` — não inventar outro fator de esmaecimento.
    4. NÃO mexer em `aria-checked`/`aria-label` (já existem); o atributo
       `disabled` sozinho basta para tecnologia assistiva anunciar o estado.
    5. NÃO mexer na lógica de cor (`s.bg`/`s.border`/`s.knob`/`s.color`): um
       toggle desabilitado mantém o visual de `on=false` que já tem hoje, só
       esmaecido. Zero regressão de cor a verificar. Em especial, o toggle
       desabilitado NUNCA pode exibir `T.accent`.
    6. No call site (linha ~4240), acrescentar `disabled={!operador}`. MANTER
       o guard `operador &&` do `onClick` como defesa em profundidade — o
       UI-SPEC é explícito: não remover.
    7. NENHUMA cópia nova. O parágrafo das linhas ~4253-4260 ("Disponível no
       Modo Operador — em Modo Estudo a entrada continua só por aviso." +
       link "Trocar para Modo Operador →") já diz o que precisa ser dito; o
       que faltava era o controle CONCORDAR com o texto.

    Os outros 8 call sites de `<Toggle>` ficam sem a prop (default
    `undefined` ⇒ `disabled={undefined}` ⇒ comportamento idêntico ao de hoje).
    Conferir que nenhum deles regrediu no build e na suíte.

    Requisito de peso visual (CLAUDE.md item 9 / UI-SPEC): `0.6` é o piso —
    não escurecer mais. O estado desabilitado tem de continuar legível, não
    sumir.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/bolsia/b3-agente/web && npx vite build && cd .. && bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx vite build` conclui sem erro
    - `grep -n "function Toggle({ on, onClick, label, disabled })" web/src/App.jsx` casa
    - `grep -n 'disabled={disabled}' web/src/App.jsx` casa dentro do `<button>` do `Toggle`
    - `grep -n 'disabled={!operador}' web/src/App.jsx` casa no call site de `label="Entrar automaticamente"`
    - `grep -n 'not-allowed' web/src/App.jsx` casa dentro do `Toggle`
    - O `onClick` do call site continua contendo `operador &&` (defesa em profundidade preservada)
    - Nenhuma string de cópia nova no diff do call site: `git diff web/src/App.jsx` não mostra texto de UI adicionado nessa região
    - `bash scripts/executar.sh --testes` exit 0
  </acceptance_criteria>
  <done>O toggle mestre é um controle HTML desabilitado de verdade fora do Modo Operador, esmaecido no mesmo padrão dos irmãos, sem cópia nova e sem mudança de cor.</done>
</task>

<task type="auto">
  <name>Task 3: Guardiões — classe do erro de appMode e estado disabled do Toggle</name>
  <files>web/tests/test_fase5_appmode_fonte_unica.mjs, web/tests/test_entrada_automatica_ui.mjs</files>
  <read_first>
    - web/tests/test_fase3_fonte_technicals.mjs — o formato canônico de guardião estático (`readFileSync` + regex + `ok(nome, cond)` + `process.exit`)
    - web/tests/test_entrada_automatica_ui.mjs (arquivo inteiro) — o guardião existente do mesmo controle, a ser ESTENDIDO
    - web/src/App.jsx — o resultado dos Tasks 1 e 2
  </read_first>
  <action>
    (a) Criar `web/tests/test_fase5_appmode_fonte_unica.mjs`, guardião estático
    sobre o fonte de `App.jsx` (o arquivo não é importável fora do build — é
    o padrão da casa). Filtrar linhas de comentário antes de contar (o fonte
    tem comentários que citam `appMode` de propósito; contar sem filtrar torna
    o guardião auto-invalidável). Assertar:
      • exatamente 1 ocorrência de `.appMode` em posição de LEITURA no fonte
        sem comentários, e ela está na linha que contém `const appMode =`;
      • o bloco de montagem de `ctx` contém `operador: appMode === "operador"`
        (a fonte única deriva da derivação local, não recomputa);
      • `ctx.operador` aparece pelo menos 7 vezes (prova que houve MIGRAÇÃO,
        não simples remoção das leituras);
      • as duas ESCRITAS (`saveConfig({ appMode:`) continuam existindo — o
        guardião não pode induzir alguém a apagar a troca de modo.
    Cabeçalho do arquivo explicando FIX-C21, o histórico (dois incidentes de
    paridade neste projeto), e que o teste fecha a CLASSE do erro, não a
    instância.

    (b) ESTENDER `web/tests/test_entrada_automatica_ui.mjs` (não criar arquivo
    novo, não apagar nada do que já está lá — guardrail do CLAUDE.md) com uma
    seção "FIX-C23 (2026-08-23)" assertando:
      • a assinatura de `Toggle` inclui `disabled`;
      • o `<button>` do `Toggle` carrega `disabled={disabled}`;
      • o `Toggle` aplica `opacity: disabled ? 0.6 : 1` e
        `cursor: disabled ? "not-allowed" : "pointer"`;
      • o call site de `label="Entrar automaticamente"` carrega
        `disabled={!operador}` E mantém `operador &&` no `onClick`;
      • o parágrafo explicativo existente continua no fonte (nenhuma cópia
        foi trocada por conta da mudança);
      • o `Toggle` NÃO passou a referenciar `T.accent` de forma condicionada
        a `disabled` (o esmaecimento é por opacidade, nunca por cor de
        destaque num controle inerte).
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/bolsia/b3-agente/web && node tests/test_fase5_appmode_fonte_unica.mjs && node tests/test_entrada_automatica_ui.mjs && cd .. && bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && node tests/test_fase5_appmode_fonte_unica.mjs` exit 0
    - `cd web && node tests/test_entrada_automatica_ui.mjs` exit 0
    - Prova negativa executada e registrada no SUMMARY: reintroduzir manualmente uma leitura `data.config.appMode` num componente faz o guardião novo FALHAR; reverter e confirmar que volta a passar
    - Prova negativa do C-23: remover `disabled={disabled}` do `<button>` faz o guardião estendido FALHAR; reverter
    - O guardião filtra linhas de comentário antes de contar (`grep -n "//" web/tests/test_fase5_appmode_fonte_unica.mjs` mostra a lógica de filtro)
    - Nenhuma asserção pré-existente de `test_entrada_automatica_ui.mjs` foi removida (`git diff` só mostra adição)
    - `bash scripts/executar.sh --testes` exit 0
  </acceptance_criteria>
  <done>Reintroduzir a recomputação de `appMode` ou remover o `disabled` do Toggle passa a quebrar a suíte, com as duas provas negativas executadas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Modo Estudo × Modo Operador | a fronteira de PRODUTO que decide se automação de entrada pode existir; recomputação divergente pode abrir um lado no outro |
| usuário com teclado/leitor de tela → controle gateado | um `<button>` sem `disabled` é focável e anunciado como operável |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-20 | Elevation of privilege | leitura divergente de `appMode` liberando UI de Operador em Modo Estudo | mitigate | Fonte única + guardião estático que falha se uma leitura independente voltar |
| T-05-21 | Tampering | migração alterando comportamento em vez de só a origem do dado | mitigate | Substituição mecânica com equivalência lógica conferida caso a caso; build + suíte canônica completa |
| T-05-22 | Spoofing | controle que parece operável mas não faz nada (teclado/leitor de tela) | mitigate | `disabled` HTML real no `<button>`, mais esmaecimento no padrão dos irmãos gateados |
| T-05-23 | Repudiation | guardião apagado para "fazer passar" uma reversão | mitigate | `test_entrada_automatica_ui.mjs` é ESTENDIDO com nota datada, nunca reescrito; `git diff` só-adição é critério de aceite |
| T-05-SC | Tampering | instalação de pacote (npm/pip/cargo) | accept | Nenhum pacote novo; os `.mjs` usam só `fs`/`path` e o build usa o Vite já instalado |
</threat_model>

<verification>
- `cd web && npx vite build` passa (obrigatório: front editado)
- `bash scripts/executar.sh --testes` passa
- As duas provas negativas (appMode e Toggle) executadas e registradas
- `git diff` de `test_entrada_automatica_ui.mjs` é só-adição
</verification>

<success_criteria>
- FIX-C21: uma única derivação de modo em `App.jsx`, com a classe do erro travada por guardião
- FIX-C23: `disabled` HTML real + feedback visual no padrão já estabelecido, sem cópia nova e sem mudança de cor
- Zero mudança de comportamento funcional
</success_criteria>

<output>
Create `.planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-05-SUMMARY.md` when done
</output>
