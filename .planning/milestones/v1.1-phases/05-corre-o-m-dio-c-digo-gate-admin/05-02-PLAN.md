---
phase: 05-corre-o-m-dio-c-digo-gate-admin
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/metering.py
  - server/app/main.py
  - server/tests/test_fase5_gate_mensal.py
  - server/tests/test_fase5_alerta_gasto_ia.py
autonomous: true
requirements: [FIX-C33, FIX-C38]

must_haves:
  truths:
    - "O gate de plano recebe a contagem REAL de análises do mês corrente da conta, não o literal 0"
    - "O contador mensal vive dentro de metering.py — não existe segundo contador paralelo"
    - "A virada de DIA não zera o acumulado do MÊS; a virada de MÊS zera"
    - "O painel de Custos recebe do backend um estado de alerta preventivo de gasto de IA calculado por regra determinística"
    - "Quando não há limiar configurado ou histórico suficiente, o alerta declara isso — nunca devolve 'dentro do normal' falso"
  artifacts:
    - path: "server/app/metering.py"
      provides: "Ledger mensal por usuário + função pura de alerta preventivo de gasto"
      contains: "def month_used"
    - path: "server/app/main.py"
      provides: "_gate_analise com contagem real, /api/ai/quota expondo monthUsed, alertaGastoIA em _usage_snapshot, 2 campos novos em _CONFIG_IA_CAMPOS"
      contains: "alertaGastoIA"
    - path: "server/tests/test_fase5_gate_mensal.py"
      provides: "Guardião do ledger mensal e do call site do gate"
    - path: "server/tests/test_fase5_alerta_gasto_ia.py"
      provides: "Guardião da função pura de alerta + do payload de /api/obs/usage"
  key_links:
    - from: "server/app/main.py::_gate_analise"
      to: "server/app/metering.py::month_used"
      via: "argumento used_this_month de plan.can_analyze"
      pattern: "plan\\.can_analyze\\(metering\\.month_used\\("
    - from: "server/app/main.py::_usage_snapshot"
      to: "server/app/metering.py::alerta_gasto"
      via: "cálculo determinístico sobre a série diária de analytics"
      pattern: "metering\\.alerta_gasto\\("
---

<objective>
Fechar a metade BACKEND de FIX-C33 (gate comercial deixa de ser chamado com
`0` hardcoded) e de FIX-C38 (alerta preventivo antes do teto global de gasto
de IA, complementar ao hard stop que já existe).

Purpose: C-33 é lacuna estrutural — hoje inofensiva porque
`max_analyses_per_month` é `None`, mas no dia em que o ADR-010 populasse o
número comercial o gate compararia `0 >= limite` e liberaria todo mundo para
sempre. C-38 é o oposto: o hard stop existe e funciona, mas só avisa DEPOIS de
cortar — não há banda de aviso.
Output: `metering.py` ganha ledger mensal + função pura de alerta; `main.py`
liga os dois fios; 2 arquivos de teste novos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-CONTEXT.md
@.planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-UI-SPEC.md
@CLAUDE.md
</context>

<interfaces>
<!-- Contratos existentes. Extraídos do código em 2026-08-23. -->

`server/app/plan.py` — o contrato de contagem JÁ está escrito lá (linhas 17-26)
e nomeia esta fase: *"Quando o ADR-010 ligar `max_analyses_per_month`, o valor
de `used_this_month` passado a `can_analyze` TEM de derivar do ledger de
`metering` (C-33, fase 5) — nunca de um segundo contador paralelo."* Esse
parágrafo é o requisito; o código tem de passar a satisfazê-lo.

```python
def can_analyze(used_this_month: int, plan: Optional[dict] = None) -> tuple  # (permitido, motivo)
def can_add_ticker(current_count: int, plan: Optional[dict] = None) -> tuple
def current_plan(user: Optional[dict] = None) -> dict   # {"id","max_watchlist","max_analyses_per_month",...}
```

`server/app/metering.py` (contador ÚNICO de uso de IA, hoje só DIÁRIO):
```python
SECTION = "aiUsage"            # kv escopado por user_id: {"day","count","rl"}
GLOBAL_SECTION = "aiUsageGlobal"  # kv sem escopo:        {"day","count"}
def _today() -> str            # "%Y-%m-%d" em UTC
def _load(conn, user_id, section=SECTION) -> dict     # zera quando day != hoje
def _load_global(conn, section=GLOBAL_SECTION) -> dict
def check(conn, user_id, *, quota, rate_per_min, custo=1, cap_global=None, ...) -> (bool, str|None)
def consume(conn, user_id, *, custo=1, section=SECTION, global_section=GLOBAL_SECTION) -> int
def snapshot(conn, user_id, quota, section=SECTION) -> {"day","used","quota","remaining"}
def global_snapshot(conn, cap=None, section=GLOBAL_SECTION) -> {"day","used","cap","remaining"}
```

`server/app/main.py`, pontos exatos a mexer:
- linha ~450, dentro de `_gate_analise`: `allowed, reason = plan.can_analyze(0, plan=plano)`
  com o comentário `# C-33 (fase 5): contagem real do mês; ...`
- linha ~1013: `plan.can_add_ticker(len(store.get(_conn, "watchlist", user_id=scope)), ...)`
  — **já passa o estado real**, nada a mudar; confirmar e registrar no SUMMARY
- linha ~457 `/api/ai/quota`: devolve `{"managed","loggedIn","byok","quota"}`
- linha ~521 `_usage_snapshot()`: dict consumido por `/api/obs/usage` e `/api/admin/summary`
- linha ~693: `_CONFIG_IA_CAMPOS = ("llmProvider","llmModel","llmDailyQuota","llmRatePerMin","llmGlobalDailyCap")`
- linha ~703 `admin_config_ia_put`: valida `llmProvider` contra whitelist, grava via
  `db.admin_config_set` / `db.admin_config_delete`, audita com `audit.record(...)`,
  `""`/`None` = "reverte pro env"

`server/app/analytics.py` (série histórica diária, banco SEPARADO `_analytics_conn`):
```python
METRICAS_CONHECIDAS = ("custo_ia_tokens_dia", "ia_analises_gerenciadas_dia", ...)
def serie_metrica(conn, metric: str, dias: int = 30, _now=None) -> [{"day": "YYYY-MM-DD", "value": float}]
```
`ia_analises_gerenciadas_dia` é gravado 1×/dia por
`agent._coletar_metricas_diarias` a partir de `metering.global_snapshot(...)["used"]`
— é a MESMA grandeza que o teto global (`cap_global`) limita.

Guardiões existentes que NÃO podem quebrar (server/tests/test_fase3_gate_plano.py):
- `test_plan_can_analyze_aparece_exatamente_uma_vez_no_main` conta
  `"plan.can_analyze("` == 1 no fonte sem comentários — a mudança tem de
  manter UMA ocorrência só
- `test_as_duas_rotas_de_analise_chamam_gate_analise` exige `_gate_analise(` >= 3
- os testes de precedência fazem `monkeypatch.setattr(main.plan, "can_analyze", ...)`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ledger mensal em metering.py + gate com contagem real (FIX-C33)</name>
  <files>server/app/metering.py, server/app/main.py, server/tests/test_fase5_gate_mensal.py</files>
  <read_first>
    - server/app/metering.py (arquivo inteiro, 135 linhas)
    - server/app/plan.py (arquivo inteiro, ~110 linhas) — o contrato de contagem no docstring do módulo
    - server/app/main.py — linhas 437-470 (`_gate_analise` + `/api/ai/quota`) e 1005-1020 (`can_add_ticker`)
    - server/tests/test_fase3_gate_plano.py — linhas 1-80 (padrão de isolamento) e 190-215 (guardiões estáticos que a mudança não pode quebrar)
  </read_first>
  <behavior>
    - `month_used` num banco novo devolve 0
    - `consume` uma vez ⇒ `month_used == 1`; `consume(custo=5)` ⇒ soma 5
    - virada de DIA (mudar `_today`) NÃO zera o acumulado do mês
    - virada de MÊS zera o acumulado (novo mês começa em 0)
    - o acumulado mensal é por escopo: `user_id="a"` não vê o de `user_id="b"`
    - `_gate_analise` chama `plan.can_analyze` com o inteiro devolvido por `metering.month_used`, não com `0`
    - `/api/ai/quota` de conta logada devolve `monthUsed` inteiro; de escopo anônimo devolve `monthUsed: None`
  </behavior>
  <action>
    Em `server/app/metering.py`, adicionar o acumulado MENSAL num registro kv
    SEPARADO do diário — não estender o dict de `SECTION`, porque `_load`
    devolve um dict novo sempre que o dia vira e isso apagaria o mês junto.
    Espelhar a forma de `_load_global`:
      • constante `MONTH_SECTION = "aiUsageMonth"` (kv ESCOPADO por `user_id`);
      • `_month() -> str` devolvendo `"%Y-%m"` em UTC, no mesmo `datetime.now(timezone.utc)`
        que `_today()` já usa (mesma âncora de fuso, nunca duas noções de tempo);
      • `_load_month(conn, user_id, section=MONTH_SECTION) -> {"month","count"}`,
        devolvendo `{"month": _month(), "count": 0}` quando o registro é de
        outro mês ou está corrompido (mesmo padrão defensivo de `_load_global`);
      • `month_used(conn, user_id, section=MONTH_SECTION) -> int`;
      • `consume(...)` incrementa o mensal junto com o diário e o global,
        respeitando o mesmo `custo`.
    Comentar no código POR QUE o mês vive num registro próprio (a virada de
    dia zeraria o mês se compartilhassem o registro) — é exatamente a classe
    de bug que o teste de rollover cobre.

    Estender `snapshot(...)` para incluir `"month"` e `"monthUsed"` no dict
    devolvido (a UI de cota já consome esse dict; acrescentar chave é aditivo).

    Em `server/app/main.py`:
      • `_gate_analise`: trocar `plan.can_analyze(0, plan=plano)` por
        `plan.can_analyze(metering.month_used(_conn, scope), plan=plano)` e
        reescrever o comentário `# C-33 (fase 5): ...` para o tempo presente,
        dizendo que a contagem vem do ledger de `metering` e citando o
        contrato do `plan.py`. Manter UMA única ocorrência textual de
        `plan.can_analyze(` no arquivo (guardião existente conta exatamente 1).
      • `/api/ai/quota`: acrescentar ao dict de resposta, em nível RAIZ (não
        dentro de `quota`, que só existe quando há IA gerenciada sem BYOK):
        `"monthUsed"` = `metering.month_used(_conn, scope)` quando há `scope`,
        `None` quando anônimo; e `"monthLimit"` =
        `_plano_do_escopo(scope).get("max_analyses_per_month")` (hoje `None`).
        O front lê essas duas chaves no Plano 05-07 — o contrato é este.
      • `can_add_ticker` (linha ~1013): NÃO mexer; já recebe
        `len(watchlist)`. Registrar no SUMMARY que a metade "can_add_ticker"
        de FIX-C33 já estava correta antes desta fase.

    Não ativar limite comercial nenhum: `PLAN_FREE`/`PLAN_PRO` continuam com
    `max_analyses_per_month = None` (ADR-010 é decisão de negócio pendente,
    fora do escopo — 05-CONTEXT.md, seção Deferred).

    Criar `server/tests/test_fase5_gate_mensal.py` cobrindo o bloco
    `<behavior>` acima. Para as viradas de dia/mês, injetar o tempo por
    `monkeypatch.setattr(metering, "_today", ...)` / `"_month"` (não usar
    `freezegun`, não está nas dependências). Para o call site do gate, usar o
    padrão de espião de `test_fase3_gate_plano.py`: `monkeypatch.setattr(main.plan,
    "can_analyze", ...)` capturando o argumento posicional e assertando que
    é o inteiro do ledger (montar estado com `metering.consume` antes e
    assertar que o gate recebeu esse mesmo número, não `0`). Incluir um
    guardião ESTÁTICO no mesmo arquivo assertando que o fonte de `main.py`
    sem comentários NÃO contém mais `can_analyze(0` — fecha a classe do erro,
    não só a instância.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/bolsia/b3-agente && bash scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/test.sh` exit 0, incluindo os guardiões pré-existentes de `server/tests/test_fase3_gate_plano.py`
    - `grep -n "def month_used" server/app/metering.py` casa
    - `grep -c "plan.can_analyze(" server/app/main.py` retorna 1
    - `grep -v '^\s*#' server/app/main.py | grep -c "can_analyze(0"` retorna 0
    - `grep -n "monthUsed" server/app/main.py` casa dentro da rota `/api/ai/quota`
    - `grep -n "max_analyses_per_month" server/app/plan.py` continua mostrando `None` nos dois planos (nenhum limite comercial ligado)
    - `grep -c "def test_" server/tests/test_fase5_gate_mensal.py` retorna >= 6
  </acceptance_criteria>
  <done>`_gate_analise` passa a contagem real do mês vinda do ledger de `metering`; a virada de dia não zera o mês; `/api/ai/quota` expõe `monthUsed`/`monthLimit`; nenhum limite comercial foi ativado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Função pura de alerta preventivo de gasto de IA (FIX-C38, cálculo)</name>
  <files>server/app/metering.py, server/tests/test_fase5_alerta_gasto_ia.py</files>
  <read_first>
    - server/app/metering.py — o arquivo já lido no Task 1, agora com o ledger mensal
    - server/app/analytics.py — `METRICAS_CONHECIDAS` e `serie_metrica` (linhas ~232-270)
    - server/app/agent.py — `_coletar_metricas_diarias` (linhas 85-112), que prova que `ia_analises_gerenciadas_dia` vem de `metering.global_snapshot(...)["used"]`
    - .planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-UI-SPEC.md — seção "FIX-C38 — Preventive spend alert", contrato de leitura
  </read_first>
  <behavior>
    - limiar `None`, `""`, `0` ou negativo ⇒ `{"configurado": False, "avaliavel": False, "acima": False}`
    - série com menos de 3 dias passados válidos ⇒ `configurado True`, `avaliavel False`, `motivo` citando histórico insuficiente
    - média dos dias passados == 0 ⇒ `avaliavel False`, `motivo` citando média zero (nunca dividir por zero, nunca inventar percentual)
    - hoje 140 contra média 100 e limiar 30 ⇒ `desvioPct == 40.0` e `acima is True`
    - hoje 120 contra média 100 e limiar 30 ⇒ `desvioPct == 20.0` e `acima is False`
    - o dia de HOJE, se estiver na série, é EXCLUÍDO do cálculo da média (senão o valor de hoje se compara consigo mesmo)
    - a janela respeita `janela_dias`: com 30 pontos na série e `janela_dias=7`, só os 7 dias passados mais recentes entram na média
  </behavior>
  <action>
    Adicionar em `server/app/metering.py` uma função PURA (sem I/O, sem
    `conn` — testável offline, como o docstring do módulo promete):

    `def alerta_gasto(usado_hoje, serie, limiar_pct, janela_dias, hoje=None) -> dict`

    onde `serie` é a lista `[{"day": "YYYY-MM-DD", "value": float}]` que
    `analytics.serie_metrica` devolve e `hoje` é a string do dia (default
    `_today()`). Devolve exatamente estas chaves:
    `{"configurado": bool, "avaliavel": bool, "motivo": str|None, "hoje": int|float,
      "media": float|None, "desvioPct": float|None, "limiarPct": float|None,
      "janelaDias": int|None, "acima": bool}`.

    Regras (todas cobertas pelo bloco `<behavior>`): descartar da série o
    ponto cujo `day == hoje`; ordenar por `day` e pegar os `janela_dias`
    pontos mais recentes; `media = sum/len` arredondada a 2 casas;
    `desvioPct = round((usado_hoje / media - 1) * 100, 1)`;
    `acima = desvioPct >= limiar_pct`. `motivo` só é preenchido quando
    `avaliavel is False`, e diz qual dos dois casos ocorreu.

    Definir também `ALERTA_JANELA_PADRAO = 7` no módulo, usada quando o admin
    não configurou janela.

    Regra de produto inegociável (CLAUDE.md item 4): quando não dá para
    avaliar, `acima` é `False` MAS `avaliavel` é `False` — quem renderiza tem
    de distinguir "dentro do normal" de "não sei". Nunca devolver um
    "dentro do normal" que na verdade significa ausência de dado. Comentar
    isso no código.

    Escolha de grandeza (documentar no docstring): o alerta compara a MESMA
    grandeza que o hard stop limita — análises gerenciadas do dia
    (`ia_analises_gerenciadas_dia` / `metering.global_snapshot()["used"]`),
    não tokens. Tokens/dia seguem como sparkline separado na aba Custos.

    Criar `server/tests/test_fase5_alerta_gasto_ia.py` com testes unitários
    puros de `alerta_gasto` cobrindo cada item do `<behavior>` (sem banco,
    sem TestClient — a função não faz I/O). O teste de rota entra no Task 3.
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/bolsia/b3-agente && bash scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/test.sh` exit 0
    - `grep -n "def alerta_gasto" server/app/metering.py` casa
    - `grep -n "ALERTA_JANELA_PADRAO" server/app/metering.py` casa
    - `grep -c "def test_" server/tests/test_fase5_alerta_gasto_ia.py` retorna >= 7
    - Nenhum teste de `alerta_gasto` abre banco: `grep -c "db.connect\|tempfile" server/tests/test_fase5_alerta_gasto_ia.py` pode ser > 0 só por causa do teste de rota do Task 3
  </acceptance_criteria>
  <done>`metering.alerta_gasto` calcula o desvio de gasto de hoje contra a média da janela, distingue "não configurado" de "não avaliável" de "dentro do normal", e está coberto por testes unitários puros.</done>
</task>

<task type="auto">
  <name>Task 3: Ligar o alerta ao payload de Custos e ao formulário de config (FIX-C38, fios)</name>
  <files>server/app/main.py, server/tests/test_fase5_alerta_gasto_ia.py</files>
  <read_first>
    - server/app/main.py — linhas 512-560 (`_usage_snapshot` + `/api/obs/usage`) e 686-735 (`_CONFIG_IA_CAMPOS`, `admin_config_ia_get`, `admin_config_ia_put`)
    - server/app/db.py — `admin_config_get`/`admin_config_set`/`admin_config_delete` (grep pelos nomes)
    - server/app/analytics.py — `serie_metrica` (assinatura e formato de retorno)
    - server/tests/test_adr013_rbac.py — padrão de teste de rota admin com permissão (`llm.configurar`, `observabilidade.ver`)
  </read_first>
  <action>
    Em `server/app/main.py`:

    1. Acrescentar dois campos a `_CONFIG_IA_CAMPOS`:
       `"llmAlertaGastoPct"` e `"llmAlertaJanelaDias"`. Ficam automaticamente
       no GET e no PUT (o loop já itera a tupla), com auditoria e semântica
       "vazio = limpa o override" já prontas.

    2. Em `admin_config_ia_put`, validar os DOIS campos novos ANTES de gravar:
       valor não vazio tem de converter para número > 0, senão
       `HTTPException(400, ...)` com mensagem PT-BR acionável nomeando o campo
       (ex.: `"Limiar de alerta de gasto: use um número maior que zero (ou vazio para desligar o alerta)."`).
       Validar SÓ os dois campos novos — não retro-validar `llmDailyQuota`/
       `llmRatePerMin`/`llmGlobalDailyCap` (mudança de contrato fora de escopo).

    3. Em `_usage_snapshot()`, acrescentar a chave `"alertaGastoIA"`, montada
       assim: ler `limiar = db.admin_config_get(_conn, "llmAlertaGastoPct")` e
       `janela = db.admin_config_get(_conn, "llmAlertaJanelaDias") or metering.ALERTA_JANELA_PADRAO`;
       obter a série com
       `analytics.serie_metrica(_analytics_conn, "ia_analises_gerenciadas_dia", dias=<janela + folga>)`;
       obter o valor de HOJE de `metering.global_snapshot(_conn, cap)["used"]`
       (o mesmo já calculado logo acima no dict — reusar a variável, não
       chamar duas vezes); chamar `metering.alerta_gasto(...)` e pôr o dict
       devolvido em `"alertaGastoIA"`.
       Envolver a leitura da série num `try/except` que degrada para
       `{"configurado": <bool>, "avaliavel": False, "motivo": "série indisponível", ...}` —
       o painel de observabilidade nunca pode cair inteiro porque o banco de
       analytics está indisponível (é o mesmo princípio do `try/except` do
       rollup em `analytics.maybe_run`).

    4. Não criar rota nova. `/api/obs/usage` e `/api/admin/summary` já
       carregam `_usage_snapshot()` — o campo viaja neles.

    Acrescentar a `server/tests/test_fase5_alerta_gasto_ia.py` os testes de
    ROTA (TestClient, banco temporário, padrão de `test_adr013_rbac.py`):
      • `GET /api/obs/usage` de admin traz `alertaGastoIA` com as chaves do
        contrato e `configurado is False` quando nada foi configurado;
      • `PUT /api/admin/config/ia` com `{"llmAlertaGastoPct": 30, "llmAlertaJanelaDias": 7}`
        devolve 200 e passa a refletir em `GET /api/admin/config/ia`;
      • `PUT` com `{"llmAlertaGastoPct": "abc"}` e com `-5` devolvem 400 com
        mensagem nomeando o campo;
      • `PUT` com `{"llmAlertaGastoPct": ""}` limpa o override (volta a
        `configurado False`);
      • uma linha de auditoria é gravada na alteração (mesma asserção que os
        testes de ADR-013 já fazem para os outros campos).
  </action>
  <verify>
    <automated>cd /Users/acamerini/dev/bolsia/b3-agente && bash scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/test.sh` exit 0
    - `grep -n "llmAlertaGastoPct" server/app/main.py` casa dentro de `_CONFIG_IA_CAMPOS` e na validação do PUT
    - `grep -n "alertaGastoIA" server/app/main.py` casa dentro de `_usage_snapshot`
    - `grep -n "metering.alerta_gasto" server/app/main.py` casa exatamente 1×
    - `curl`-equivalente coberto por teste: `GET /api/obs/usage` responde 200 com `alertaGastoIA` presente mesmo sem nenhuma métrica diária gravada
    - Nenhuma rota nova criada: `grep -c "@app.get(\"/api/obs" server/app/main.py` mantém o valor anterior à mudança
  </acceptance_criteria>
  <done>`/api/obs/usage` e `/api/admin/summary` carregam `alertaGastoIA` calculado deterministicamente; o admin configura limiar e janela pelo formulário já existente, com validação e auditoria.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente autenticado → `/api/analyze*` | consumo de IA paga pela conta do servidor atravessa aqui |
| admin → `PUT /api/admin/config/ia` | número não confiável vira parâmetro de alerta de FinOps |
| banco de analytics (separado) → `_usage_snapshot` | série histórica pode estar ausente/corrompida |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Elevation of privilege | gate de plano lendo `0` fixo | mitigate | Task 1 passa `metering.month_used`; guardião estático proíbe o literal `can_analyze(0` voltar |
| T-05-07 | Tampering | segundo contador paralelo de uso de IA | mitigate | Ledger mensal vive DENTRO de `metering.py`, incrementado só por `consume()`; nenhum outro módulo mantém contagem |
| T-05-08 | Information disclosure | `monthUsed` de outra conta | mitigate | `month_used` lê kv ESCOPADO por `user_id`; teste assere isolamento entre dois escopos |
| T-05-09 | Tampering | `PUT /api/admin/config/ia` com limiar não numérico/negativo | mitigate | Validação explícita nos 2 campos novos antes de gravar, com 400 acionável |
| T-05-10 | Repudiation | mudança de limiar sem rastro | mitigate | Reusa `audit.record` do fluxo já existente de `_CONFIG_IA_CAMPOS` (ADR-013: toda escrita admin audita) |
| T-05-11 | Denial of service | banco de analytics indisponível derruba `/api/obs/usage` inteiro | mitigate | `try/except` degradando para `avaliavel: False, motivo: "série indisponível"` |
| T-05-12 | Spoofing | alerta "dentro do normal" falso por ausência de dado | mitigate | `avaliavel` separado de `acima`; nunca devolver tranquilidade sem base (CLAUDE.md item 4) |
| T-05-SC | Tampering | instalação de pacote (npm/pip/cargo) | accept | Nenhum pacote novo — só stdlib (`datetime`) e módulos internos já em `server/requirements.txt` |
</threat_model>

<verification>
- `bash scripts/test.sh` passa, incluindo `test_fase3_gate_plano.py` intacto
- `grep -c "plan.can_analyze(" server/app/main.py` == 1
- `PLAN_FREE["max_analyses_per_month"]` continua `None` (nenhum limite comercial ligado)
- `/api/obs/usage` devolve `alertaGastoIA` mesmo com banco de analytics vazio
</verification>

<success_criteria>
- FIX-C33 (backend): gate recebe a contagem real do mês, do ledger de `metering`; `monthUsed`/`monthLimit` expostos em `/api/ai/quota` para o front consumir
- FIX-C38 (backend): alerta preventivo determinístico, configurável por limiar e janela, auditado, degradando honestamente quando não há base
- Nenhum limite comercial ativado (ADR-010 permanece decisão pendente)
</success_criteria>

<output>
Create `.planning/phases/05-corre-o-m-dio-c-digo-gate-admin/05-02-SUMMARY.md` when done
</output>
