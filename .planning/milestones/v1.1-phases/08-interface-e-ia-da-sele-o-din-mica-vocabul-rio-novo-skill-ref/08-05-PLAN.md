---
phase: 08-interface-e-ia-da-sele-o-din-mica-vocabul-rio-novo-skill-ref
plan: 05
type: execute
wave: 4
depends_on: ["08-02", "08-03", "08-04"]
files_modified:
  - web/src/version.js
  - server/web_dist
autonomous: false
requirements: [ADR17-B34-04]

must_haves:
  truths:
    - "A entrega está buildada, publicada e carimbada — sem nenhum comando de produção executado por agente"
    - "O Alex aprova explicitamente ANTES de a entrada automática gated ir para produção"
    - "A verificação ao vivo confirma as duas pontas: a vitrine do histórico na tela e o gate no ciclo do Operador"
  artifacts:
    - path: "web/src/version.js"
      provides: "BUILD_ID novo da entrega (carimbo que prova qual versão está no ar)"
      contains: "BUILD_ID"
    - path: "server/web_dist"
      provides: "front publicado na única árvore que o Railway enxerga"
  key_links:
    - from: "web/src/version.js (BUILD_ID)"
      to: "server/web_dist/assets/*.js"
      via: "scripts/bump.sh + scripts/publicar-web.sh (nunca editar server/web_dist à mão)"
      pattern: "BUILD_ID"
---

<objective>
Preparar a entrega desta fase até a borda do deploy e PARAR: rodar as duas suítes, buildar o
front, bumpar o carimbo, publicar em `server/web_dist` — e então entregar ao Alex o checkpoint
bloqueante que precede a ida do religamento de `entradaAuto` para produção.

Purpose: o Plano 08-02 muda comportamento REAL de execução automática de dinheiro simulado do
usuário, e não é reversível sem novo deploy. O 08-CONTEXT.md exige checkpoint humano antes do
deploy dessa mudança específica, no mesmo padrão do 07-06. Além disso, o histórico deste
projeto registra três defeitos (sync device→servidor, orçamento sem flush, link do Operador)
que passaram por toda a suíte e só apareceram na verificação ao vivo — a tela do Radar/card é
exatamente esse tipo de superfície.

Output: build/publicação prontos e commitados, e a aprovação (ou a lista de divergências) do
Alex.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/skills/didatica-boris/SKILL.md
@.planning/phases/08-interface-e-ia-da-sele-o-din-mica-vocabul-rio-novo-skill-ref/08-CONTEXT.md
@.planning/phases/08-interface-e-ia-da-sele-o-din-mica-vocabul-rio-novo-skill-ref/08-02-SUMMARY.md
@.planning/phases/08-interface-e-ia-da-sele-o-din-mica-vocabul-rio-novo-skill-ref/08-03-SUMMARY.md
@.planning/phases/08-interface-e-ia-da-sele-o-din-mica-vocabul-rio-novo-skill-ref/08-04-SUMMARY.md

<interfaces>
<!-- Comandos canônicos do repositório (CLAUDE.md + skill didatica-boris) -->
  bash scripts/executar.sh --testes     # AS DUAS suítes (pytest + web/tests/*.mjs)
  cd web && npx vite build              # obrigatório após editar o front
  bash scripts/bump.sh                  # avança o BUILD_ID (padrão F10-AAAAMMDD-NN)
  bash scripts/publicar-web.sh          # builda + copia para server/web_dist + sincroniza carimbo
                                        # NÃO faz deploy: o Railway redeploya no PUSH

<!-- Host de produção -->
  web/src/api.js:23  PROD_BASE = "https://boris.semente.dev"
  server/railway.json  healthcheckPath = "/api/health"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validação completa, carimbo e publicação do front (sem deploy)</name>
  <files>web/src/version.js, server/web_dist</files>
  <read_first>
    - CLAUDE.md (secções "Validação obrigatória" e "Guardrails do repositório" — a suíte
      canônica, o `npx vite build` obrigatório, e a ordem `bump.sh` antes de `publicar-web.sh`)
    - scripts/publicar-web.sh (linhas 1-60 — o que ele faz e o que ele NÃO faz: build, cópia
      para `server/web_dist`, sincronização do `SERVER_BUILD_ID`; deploy é o push)
    - scripts/bump.sh (o padrão do carimbo `F10-AAAAMMDD-NN` e por que esquecer o bump não é
      inofensivo)
    - web/src/version.js (o BUILD_ID atual)
    - .claude/skills/didatica-boris/SKILL.md ("Caminho de deploy")
  </read_first>
  <action>
    Execute, nesta ordem, e PARE se algum passo falhar (não "corrija e siga" sem registrar):

    1. `bash scripts/executar.sh --testes` — as DUAS suítes. Registre a contagem de testes.
    2. `cd web && npx vite build` — prova de ausência de erro de sintaxe no JSX.
    3. `bash scripts/bump.sh` — carimbo novo com a DATA REAL do dia (não invente data;
       o `bump.sh` deriva sozinho quando chamado sem argumento).
    4. `bash scripts/publicar-web.sh` — build + publicação em `server/web_dist`.
    5. `git status --short` — confira que `server/web_dist` e `web/src/version.js` aparecem
       modificados e que NENHUM arquivo untracked inesperado entrou no caminho do commit.

    PROIBIDO neste plano (é o portão do checkpoint da Task 2):
      - `git push`, `bash atualizar.sh`, `bash entregar.sh`, qualquer comando `railway *`;
      - editar `server/web_dist` à mão;
      - qualquer comando que toque o banco ou o ambiente de produção.

    Commit local é permitido e esperado (o push é do Alex — é ele que dispara o redeploy do
    Railway).
  </action>
  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/executar.sh --testes` sai 0, com as DUAS suítes reportadas.
    - `cd web && npx vite build` sai 0.
    - `grep -c "BUILD_ID" web/src/version.js` ≥ 1 e o valor mudou em relação ao HEAD anterior (`git diff HEAD -- web/src/version.js | grep -c '^+.*BUILD_ID'` = 1).
    - `BID=$(sed -n 's/.*BUILD_ID = "\([^"]*\)".*/\1/p' web/src/version.js); grep -rq "$BID" server/web_dist/assets/*.js` sai 0 — o dist publicado carrega o carimbo novo.
    - `git log --oneline -1` NÃO foi seguido de push: `git status -sb | head -1` mostra a branch à frente do remoto (`ahead`).
    - Nenhuma ocorrência de `railway`, `atualizar.sh`, `entregar.sh` ou `git push` no histórico de comandos executados por este plano.
  </acceptance_criteria>
  <done>A entrega está buildada, carimbada, publicada em `server/web_dist` e commitada localmente — pronta para o Alex empurrar, e nada tocou produção.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint humano — deploy e verificação ao vivo do religamento gated</name>
  <read_first>
    - .planning/phases/08-.../08-02-SUMMARY.md (o predicado exato do gate e os 5 pares
      setup×lado elegíveis registrados no Adendo 2 do ADR-017)
    - docs/adr/017-revisao-de-setups-e-selecao-dinamica.md (Adendo 1 e Adendo 2)
    - web/src/api.js (linha 23 — o host real de produção, para preencher os comandos)
    - .planning/phases/07-.../07-06-PLAN.md (o formato do checkpoint da Fase 7, que este
      replica)
  </read_first>
  <action>
    NÃO execute deploy, push, `railway ssh` nem qualquer comando de produção — esta task é um
    portão bloqueante.

    Faça o seguinte e pare:
    1. Confirme que a Task 1 fechou verde (as duas suítes, o build e o carimbo publicado).
    2. Monte a mensagem do checkpoint reproduzindo LITERALMENTE os passos do bloco
       `<how-to-verify>` abaixo, com o host real (`https://boris.semente.dev`) e o BUILD_ID
       novo preenchidos — nenhum placeholder `<host>` ou `<build>` pode sobrar.
    3. Liste, na mensagem, os 5 pares setup×lado que o Adendo 2 registrou como elegíveis, para
       que o Alex saiba o que esperar como universo da entrada automática.
    4. Aguarde o sinal de retomada.

    Se o Alex responder com divergência, trate como defeito a corrigir NESTA fase (não como
    escopo novo): identifique se a causa está no vocabulário (08-01), no gate (08-02), no
    nível ticker (08-03) ou no nível setup / card do Operador (08-04), corrija, rode as duas
    suítes de novo, republique e volte ao checkpoint. Registre o ciclo no SUMMARY.
  </action>
  <acceptance_criteria>
    - `bash scripts/executar.sh --testes` sai 0 antes de apresentar o checkpoint.
    - `cd web && npx vite build` sai 0 antes de apresentar o checkpoint.
    - A mensagem apresentada contém os passos numerados com comandos concretos, o host real e o BUILD_ID real — nenhum placeholder restante.
    - Nenhum comando de produção (`git push`, `atualizar.sh`, `entregar.sh`, `railway *`) foi executado pelo agente.
    - A execução só prossegue após sinal explícito de retomada.
  </acceptance_criteria>
  <what-built>
    O Bloco 3 e o Bloco 4 do ADR-017, com suíte verde:
    - Vocabulário canônico dos 6 estados do histórico medido em `server/app/skill_ref.py`, com
      espelho byte-idêntico em `web/src/copy.js` e guardião cruzado que falha em sabotagem
      controlada (Plano 08-01).
    - `_avaliar_entradas` sem a suspensão cega: `ENTRADA_AUTO_SUSPENSA_ADR017` não existe mais;
      no lugar, gate por `signal_ledger.historico_snapshot` que só deixa passar
      `elegivel is True` — `False`, `None`, setup ausente e falha de leitura bloqueiam em
      silêncio (Plano 08-02, Adendo 2 do ADR-017).
    - Radar e Watchlist mostrando `setupElegivel`/`setupHistorico` por ticker, nos 6 estados,
      com `aria-label` em todo pill (Plano 08-03).
    - Lista de setups do card mostrando `historico` por setup, o rótulo de aposentado e a frase
      de gate POR SETUP (nomeando o setup, só em Modo Operador), mais a linha de transparência
      agregada no card de status do Operador (Plano 08-04).
    - Front buildado, carimbado e publicado em `server/web_dist`, commitado localmente.

    O que Claude NÃO pode fazer sozinho: empurrar o commit (é o push que dispara o redeploy do
    Railway) e confirmar no app real, no navegador e no iPhone, que a tela e o ciclo do
    Operador se comportam como prometido. É mudança de comportamento de execução automática de
    dinheiro simulado do usuário, não reversível sem novo deploy.
  </what-built>
  <how-to-verify>
    1. **Revise o diff** — `git log --oneline origin/main..HEAD` e `git diff origin/main --stat`.
       Esperado: `skill_ref.py`, `copy.js`, `agent.py`, `App.jsx`, `finance.js`, os testes,
       `docs/adr/017-...md`, `web/src/version.js` e `server/web_dist`. NÃO deve aparecer
       `regime.py`, `setups.py`, `signal_ledger.py`, `signal_ledger_job.py`, `defaults.py` nem
       `catalog.js`.

    2. **Deploy** — `git push`. O Railway redeploya sozinho (`rootDirectory=/server`).

    3. **Confirme que o servidor subiu com o carimbo novo** —
       `curl -s https://boris.semente.dev/api/health`
       Esperado: 200, com `SERVER_BUILD_ID` igual ao BUILD_ID desta entrega.

    4. **Radar (web)** — abra https://boris.semente.dev, vá ao Radar. Esperado:
       - ao lado do melhor setup de cada ativo, um pill de elegibilidade;
       - ativos com setup elegível mostram o pill VERDE; inelegível mostra VERMELHO com o
         MESMO peso visual (não apagado, não menor);
       - ativo cujo setup ainda não tem amostra mostra pill NEUTRO/cinza — nunca vermelho;
       - nenhum ativo sumiu da lista, nenhum erro no console.

    5. **Card de setup** — em qualquer ativo, toque "+ Ver critérios do setup". Esperado:
       - cada setup listado mostra o próprio histórico (expectância da janela, n, ano da
         janela) ou o estado neutro quando não houver;
       - os setups aposentados (Ponto Contínuo, Setup 9.2, Inside Bar baixa, Máx/Mín LW 9.4
         baixa) CONTINUAM na lista, com rótulo tracejado "APOSENTADO (ADR-016)" — o nome não
         está riscado nem apagado;
       - onde o dado é velho, o carimbo aparece com "⏱" e mais apagado, mas o número continua
         legível;
       - EM MODO OPERADOR, cada setup traz também a linha nomeada de gate ("Entrada automática
         disponível para {setup} — elegibilidade medida em {ano}." ou "…bloqueada para {setup} —
         sem vantagem estatística medida nesta janela."), e ela só aparece nos setups elegíveis
         como "disponível". Em Modo Estudo essa linha NÃO pode aparecer — se aparecer, reprove.

    6. **Modo Operador — card de status** — troque para Modo Operador (Perfil) e abra a aba
       Operador IA. Esperado, no primeiro card da tela, além dos três badges de sempre:
       - a regra do gate ("só executa em setup com vantagem estatística medida na janela
         anterior…");
       - a linha de contraste com OS DOIS números juntos ("−0,099R … +0,005R … empate, não
         lucro"). Se aparecer só o positivo, é defeito — reprove.

    7. **iPhone** — abra o app instalado. Esperado: os itens 4, 5 e 6 valem igual (o backend
       serve o mesmo front). Se a tela do iPhone estiver com o layout antigo, é bundle nativo
       velho, não defeito desta fase — anote e siga.

    8. **Entrada automática gated (o item crítico)** — com Modo Operador + Operador no servidor
       ligado + `entradaAuto` ligado, acompanhe por um pregão. Esperado:
       - entrada automática só acontece em ticker cujo setup do gatilho está entre os
         elegíveis (a mensagem listará os 5 pares atuais);
       - gatilho de setup NÃO elegível não abre posição e NÃO gera evento de aviso — é
         bloqueio silencioso, por decisão registrada;
       - o Diário/eventos não mostra erro novo relacionado ao ledger;
       - o consumo da brapi em Perfil → Observabilidade não tem salto (o gate lê do cache do
         ledger, não da fonte de mercado).

    9. **Rollback, se precisar** — `git revert` do commit desta fase e novo push devolve a
       suspensão? NÃO: a suspensão foi removida. O caminho de emergência é o kill-switch do
       agente (Perfil → Observabilidade / portal admin) ou desligar `entradaAuto` na conta.
       Confirme que você sabe acionar um dos dois ANTES de aprovar o passo 8.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva o que divergiu (passo, comando, o que apareceu vs. o esperado)</resume-signal>
  <done>O Alex confirmou ao vivo: carimbo novo no `/api/health`, pill de elegibilidade no Radar com negativo do mesmo peso que positivo e neutro para sem-amostra, histórico por setup no card com os aposentados presentes e rotulados, regra + contraste (dois números) no card de status do Operador, e entrada automática disparando apenas em setup elegível, sem erro novo e sem salto no consumo da brapi.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agente → repositório remoto / Railway | O push é o gatilho de deploy; executá-lo por conta própria contornaria o checkpoint |
| Gate de elegibilidade → carteira simulada do usuário em produção | Mudança de comportamento de execução automática, não reversível sem novo deploy |
| `server/web_dist` versionado → produção | É a única árvore de front que o Railway enxerga; publicar um dist velho é entregar a versão errada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-20 | Elevation of Privilege | Deploy sem aprovação humana | mitigate | Checkpoint `gate="blocking"`; aceite da Task 1 exige `git status -sb` mostrando `ahead` (não pushado) e ausência de qualquer comando de produção |
| T-08-21 | Tampering | `server/web_dist` fora de sincronia com o carimbo | mitigate | `bump.sh` antes de `publicar-web.sh`; aceite verifica o BUILD_ID dentro dos assets publicados |
| T-08-22 | Denial of Service | Entrada automática indevida em produção | mitigate | Passo 8 do checkpoint acompanha um pregão; passo 9 exige que o Alex confirme que sabe acionar o kill-switch ANTES de aprovar |
| T-08-23 | Repudiation | Divergência entre o prometido na UI e o gate real | mitigate | Passos 6 e 8 verificam as duas pontas na mesma sessão |

Sem instalação de pacote npm/pip/cargo neste plano — o gate de legitimidade de pacote não se aplica.
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` sai 0.
- `cd web && npx vite build` sai 0.
- BUILD_ID novo presente em `server/web_dist/assets/*.js`.
- Aprovação explícita do Alex registrada no SUMMARY, com o resultado de cada passo do `<how-to-verify>`.
</verification>

<success_criteria>
- Entrega buildada, carimbada, publicada e commitada localmente, sem nenhum comando de produção executado por agente.
- Checkpoint apresentado com host e carimbo reais, sem placeholder.
- Aprovação (ou correção + reapresentação) registrada.
</success_criteria>

<output>
Create `.planning/phases/08-interface-e-ia-da-sele-o-din-mica-vocabul-rio-novo-skill-ref/08-05-SUMMARY.md` when done
</output>
