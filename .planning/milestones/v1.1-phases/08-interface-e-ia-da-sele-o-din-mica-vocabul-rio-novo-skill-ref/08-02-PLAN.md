---
phase: 08-interface-e-ia-da-sele-o-din-mica-vocabul-rio-novo-skill-ref
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app/agent.py
  - server/tests/test_entrada_automatica_suspensa_adr017.py
  - server/tests/test_entrada_automatica.py
  - docs/adr/017-revisao-de-setups-e-selecao-dinamica.md
autonomous: true
requirements: [ADR17-B34-04]

must_haves:
  truths:
    - "A entrada automática do Modo Operador volta a existir, mas só para o setup que a seleção dinâmica mediu como positivo"
    - "Setup nunca medido ou com amostra insuficiente NÃO executa — ausência de evidência bloqueia, igual a evidência negativa"
    - "Uma falha do ledger não abre a porteira: erro na consulta bloqueia a entrada, nunca libera"
    - "Toda a mecânica anterior (lote de 100, allocPct, maxOpsDia, maxValorOp, dedupe com o vigia) segue idêntica"
  artifacts:
    - path: "server/app/agent.py"
      provides: "gate de elegibilidade dentro de _avaliar_entradas, no lugar da suspensão cega"
      contains: "historico_snapshot"
    - path: "server/tests/test_entrada_automatica_suspensa_adr017.py"
      provides: "guardião reescrito: prova que o gate bloqueia False/None/ausente e libera True"
      contains: "elegivel"
    - path: "docs/adr/017-revisao-de-setups-e-selecao-dinamica.md"
      provides: "Adendo 2 — implementação do Bloco 4 (religamento gated)"
      contains: "Adendo 2"
  key_links:
    - from: "server/app/agent.py:_avaliar_entradas"
      to: "signal_ledger.historico_snapshot"
      via: "import local dentro da função (evita ciclo), leitura única antes do laço de candidatos"
      pattern: "signal_ledger\\.historico_snapshot\\("
    - from: "server/app/agent.py:_avaliar_entradas"
      to: "timing.montar -> r['setup']"
      via: "nome do setup do gatilho usado como chave do snapshot de histórico"
      pattern: "r\\.get\\(\"setup\"\\)"
---

<objective>
Trocar a suspensão cega da entrada automática (`ENTRADA_AUTO_SUSPENSA_ADR017 = True`) por um
gate fino: dentro de `_avaliar_entradas`, só executa o gatilho cujo setup a seleção dinâmica
mediu como `elegivel is True` na janela anterior fechada.

Purpose: é a Decisão 3 do ADR-017 fechando o ciclo — a suspensão sempre foi um gate reversível
esperando o Bloco 1, que já está em produção desde 2026-08-21. Hoje, na prática, o gate
restringe a entrada automática a 5 pares setup×lado (123 de fundo alta, IFR2 alta, PFR alta,
Setup 9.1 alta, Setup 9.3 alta) sem nenhuma lista hardcodada: o dado decide, e muda sozinho a
cada virada de janela.

Output: `agent.py` com o gate, os dois guardiões de entrada automática atualizados (nenhum
apagado) e o Adendo 2 no ADR-017. O deploy em produção NÃO acontece neste plano — o checkpoint
humano bloqueante vive no Plano 08-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/08-interface-e-ia-da-sele-o-din-mica-vocabul-rio-novo-skill-ref/08-CONTEXT.md
@docs/adr/017-revisao-de-setups-e-selecao-dinamica.md
@server/app/agent.py
@server/app/signal_ledger.py

<interfaces>
<!-- Tudo já entregue e testado na Fase 7. NADA aqui é recalculado. -->

server/app/signal_ledger.py:249
  def historico_snapshot(conn, ttl_s: int = 300, now: float | None = None) -> dict
  # {nome_do_setup: {expR, n, medidoAte, elegivel, insuficiente,
  #                  expRJanela, nJanela, janelaRef, calculadoEm}}
  # Cache em processo (TTL 300 s). NUNCA levanta: todo o corpo é try/except e
  # devolve {} (ou o último cache) em falha de banco.

server/app/setups.py:703  plano["setup"] = setup.get("nome")
server/app/timing.py:82    r["setup"] = plano.get("setup")
  # => `r.get("setup")` em _avaliar_entradas JÁ é o NOME COMPLETO do setup
  #    ("IFR2 (alta)", "PFR (alta)", ...), a MESMA chave do snapshot acima.

server/app/agent.py:1107-1113 — padrão de import local já usado para
  `signal_ledger_job`: replicar para `signal_ledger`, evita ciclo de import.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate de elegibilidade no lugar da suspensão cega</name>
  <files>server/app/agent.py, server/tests/test_entrada_automatica_suspensa_adr017.py</files>
  <read_first>
    - server/app/agent.py (linhas 589-693: o comentário-cabeçalho da Fase B, a flag
      `ENTRADA_AUTO_SUSPENSA_ADR017` na linha 603, o guard `if not (par["entradaAuto"] and
      app_mode == "operador")`, o bloco de imports locais na linha 621, o laço
      `for ticker in candidatos:` e a checagem `r.get("estado") != "gatilho" or
      r.get("decisaoDiaria") != "COMPRAR"` na linha 645)
    - server/app/agent.py (linhas 1107-1113: o padrão de import local de `signal_ledger_job`
      dentro de função, que este plano replica para `signal_ledger`)
    - server/app/signal_ledger.py (linhas 222-267: `_fundir` e `historico_snapshot` — o shape
      exato do dict e a garantia de que a função nunca levanta)
    - server/tests/test_entrada_automatica_suspensa_adr017.py (arquivo inteiro — é ele que vai
      ser REESCRITO, nunca apagado)
    - .planning/phases/08-.../08-CONTEXT.md (secção "Religamento do Modo Operador" — a regra
      exata de True/False/None)
  </read_first>
  <behavior>
    - `elegivel is True` para o setup do gatilho: a entrada executa normalmente (`store.buy`
      chamado, `executed` incrementado, evento `kind: "buy"` registrado, dedupe do
      `timingWatch` gravado) — toda a mecânica de hoje, intacta.
    - `elegivel is False`: nada executa, `executed` continua 0, `events` fica VAZIO (bloqueio
      silencioso — não é erro, é o gate funcionando), `positions` continua vazio.
    - `elegivel is None` (amostra insuficiente na janela): bloqueia, mesmo comportamento do
      caso False.
    - Setup do gatilho AUSENTE do snapshot (nunca medido): bloqueia, silencioso.
    - `r.get("setup")` vazio/None: bloqueia, silencioso (não há setup a qualificar).
    - Snapshot vazio `{}` (ledger nunca agregado, ou falha de banco que `historico_snapshot`
      já engoliu): nenhuma entrada executa — o gate falha FECHADO.
    - Com `elegivel is True` mas orçamento insuficiente para 1 lote / teto diário atingido /
      teto por operação excedido: o comportamento e as mensagens de `warn` de hoje continuam
      idênticos (o gate roda ANTES, não substitui essas checagens).
    - `historico_snapshot` é consultado no máximo UMA vez por chamada de `_avaliar_entradas`,
      mesmo com vários candidatos (verificável por spy de contagem).
    - `agent` não expõe mais o atributo `ENTRADA_AUTO_SUSPENSA_ADR017`.
  </behavior>
  <action>
    Em `server/app/agent.py`:

    1. Remova a constante `ENTRADA_AUTO_SUSPENSA_ADR017` (linha 603) e o `if
       ENTRADA_AUTO_SUSPENSA_ADR017: return executed` (linhas 615-616). Reescreva o
       comentário-cabeçalho da seção (linhas 597-602) registrando a substituição: a suspensão
       da Decisão 3 do ADR-017 sempre foi gate reversível esperando o Bloco 1; com o Bloco 1
       em produção (2026-08-21), ela vira um gate por elegibilidade medida do setup do
       gatilho. Cite que hoje isso restringe a 5 pares setup×lado, sem lista hardcodada.

    2. Acrescente `signal_ledger` ao bloco de imports locais que já existe dentro da função
       (linha 621, `from . import candles as candles_mod, intraday, radar_daily, timing,
       timing_watch`). Import local, mesmo motivo já documentado ali: evitar ciclo e não
       cobrar leitura extra de quem não ligou a feature.

    3. Depois do early-return `if not candidatos: return executed` (linha 629-630) e antes do
       laço, leia UMA vez: `hist = signal_ledger.historico_snapshot(conn) or {}`.

    4. Dentro do laço `for ticker in candidatos:`, LOGO DEPOIS da checagem de
       `estado != "gatilho" or decisaoDiaria != "COMPRAR"` (linha 645-646) e ANTES da checagem
       de `price`, insira o gate: pegue `nome_setup = r.get("setup")`; se `nome_setup` for
       falsy, `continue`; senão consulte `(hist.get(nome_setup) or {}).get("elegivel")` e
       `continue` quando o valor não for exatamente `True` (isto é, `is not True` — cobre
       False, None e ausência num único predicado).

       O bloqueio é SILENCIOSO: nenhum `events.append`, nenhum log — mesmo comportamento da
       suspensão de hoje, porque não é erro (08-CONTEXT.md). Comente essa escolha na linha do
       `continue`, para que o próximo leitor não a confunda com um caminho de erro engolido.

    5. NÃO toque em `store.buy`, no cálculo de `qty`, nos tetos, no dedupe do `timingWatch`,
       nem em qualquer outra passada do ciclo. NÃO toque em `regime.py`, `setups.py`,
       `signal_ledger.py` nem `signal_ledger_job.py` (fence do 08-CONTEXT.md).

    6. REESCREVA `server/tests/test_entrada_automatica_suspensa_adr017.py` mantendo o NOME do
       arquivo (guardrail do repositório: guardião de teste não se apaga; reversão deliberada
       atualiza o guardião com nota). O docstring do módulo passa a registrar: a suspensão
       cega da Decisão 3 foi substituída, em 2026-08-21 (Fase 08, Bloco 4), por gate de
       elegibilidade — o que este arquivo guarda agora é que o gate BLOQUEIA nos três casos
       negativos e LIBERA só em `elegivel is True`. Cubra cada item do bloco `<behavior>`,
       reusando o cenário de fixture que o arquivo já monta (radar_daily.store_result +
       intraday kv + cash + appMode operador), com `monkeypatch` de
       `signal_ledger.historico_snapshot` para controlar o veredito por caso. O plano da fixture
       passa a usar `"setup": "IFR2 (alta)"` (nome real do catálogo de `setups.py`) no lugar do
       literal inventado `"rompimento"` — o lookup do gate é por nome exato, então a fixture só
       prova o acoplamento de chave se usar um nome que o catálogo realmente produz.
  </action>
  <verify>
    <automated>cd server && ./.venv/bin/python -m pytest -q tests/test_entrada_automatica_suspensa_adr017.py</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && ./.venv/bin/python -m pytest -q tests/test_entrada_automatica_suspensa_adr017.py` sai 0 com ao menos 6 testes (True libera; False, None, ausente, setup vazio e snapshot vazio bloqueiam).
    - `grep -rn "ENTRADA_AUTO_SUSPENSA_ADR017" server/` não retorna nenhuma linha.
    - `grep -c "signal_ledger" server/app/agent.py` ≥ 2 (import local do job já existente + o novo).
    - `cd server && ./.venv/bin/python -c "from app import agent; assert not hasattr(agent,'ENTRADA_AUTO_SUSPENSA_ADR017')"` sai 0.
    - O arquivo `server/tests/test_entrada_automatica_suspensa_adr017.py` continua existindo (`git status` mostra modificado, nunca deletado).
    - `grep -c '"IFR2 (alta)"' server/tests/test_entrada_automatica_suspensa_adr017.py` ≥ 1 e `grep -c '"rompimento"'` no mesmo arquivo retorna 0.
    - `git diff --stat server/app/regime.py server/app/setups.py server/app/signal_ledger.py server/app/signal_ledger_job.py` vazio.
  </acceptance_criteria>
  <done>A entrada automática executa somente para setup com `elegivel is True` no snapshot do ledger; qualquer outro veredito, ausência de dado ou falha de leitura bloqueia em silêncio.</done>
</task>

<task type="auto">
  <name>Task 2: Fixture da mecânica + Adendo 2 no ADR-017</name>
  <files>server/tests/test_entrada_automatica.py, docs/adr/017-revisao-de-setups-e-selecao-dinamica.md</files>
  <read_first>
    - server/tests/test_entrada_automatica.py (linhas 25-60: a fixture autouse
      `_desliga_suspensao_adr017` que hoje monkeypatcha a flag, e `_conn()` — que cenário de
      plano/setup cada teste monta. Hoje a linha 69 grava `"setup": "rompimento"`, que NÃO é
      nome de setup do catálogo de `setups.py` — os nomes reais são "IFR2 (alta)",
      "Setup 9.1 (alta)", "Pullback à média (alta)", "Rompimento com volume (alta)"...)
    - server/app/agent.py (o gate recém-escrito na Task 1 — o ponto exato que a fixture
      precisa neutralizar)
    - docs/adr/017-revisao-de-setups-e-selecao-dinamica.md (linhas 117-200: "Decisão 3",
      "Consequências" e o formato do "Adendo 1 — implementação do Bloco 1 (2026-08-21)", que o
      Adendo 2 deve imitar)
  </read_first>
  <action>
    0. Em `server/tests/test_entrada_automatica.py` (linha 69) E em
       `server/tests/test_entrada_automatica_suspensa_adr017.py` (linha 39), troque o literal
       `"setup": "rompimento"` por `"setup": "IFR2 (alta)"`. "rompimento" é nome inventado: não
       existe em `setups.py`. Como o gate faz lookup por NOME EXATO no snapshot do ledger, uma
       fixture com nome fora do catálogo exercita o predicado sem exercitar o acoplamento de
       chave que o T-08-08 declara mitigar. A chave do snapshot stub passa a ser a MESMA string.

    1. Em `server/tests/test_entrada_automatica.py`, substitua a fixture autouse
       `_desliga_suspensao_adr017` por uma fixture autouse que monkeypatcha
       `signal_ledger.historico_snapshot` para devolver um snapshot em que o setup usado pelos
       cenários deste arquivo aparece com `elegivel: True` (mantenha o resto do shape de
       `_fundir` — `n`, `expR`, `insuficiente: False`, `janelaRef`, `medidoAte` — para que o
       stub não vire um contrato mais frouxo que o real). Renomeie a fixture para algo como
       `_setup_elegivel_no_ledger` e reescreva o docstring: este arquivo cobre a MECÂNICA
       (lote, orçamento, maxOpsDia, maxValorOp, dedupe com o vigia); o GATE tem guardião
       próprio em `test_entrada_automatica_suspensa_adr017.py`. Nenhum teste de mecânica muda
       de asserção.

    2. Em `docs/adr/017-revisao-de-setups-e-selecao-dinamica.md`, acrescente ao FIM do arquivo
       um "## Adendo 2 — implementação do Bloco 4: religamento gated (2026-08-21)", no mesmo
       formato do Adendo 1. Registre: (a) que `ENTRADA_AUTO_SUSPENSA_ADR017` deixou de existir
       e foi substituída pelo gate em `_avaliar_entradas`; (b) a regra exata — só
       `elegivel is True` passa, `False`/`None`/ausente bloqueiam em silêncio, e falha de
       leitura do ledger falha FECHADO; (c) os 5 pares setup×lado elegíveis no bootstrap de
       produção de 2026-08-21 (123 de fundo alta, IFR2 alta, PFR alta, Setup 9.1 alta, Setup
       9.3 alta), explicitando que é foto do dado e não lista hardcodada; (d) que o deploy em
       produção passou por checkpoint humano bloqueante, com ponteiro para o Plano 08-05.

       NÃO reescreva o texto histórico do ADR (Decisão 3, Consequências, Adendo 1) — o
       histórico do repositório não se reescreve; o Adendo 2 registra o que mudou depois.
  </action>
  <verify>
    <automated>cd server && ./.venv/bin/python -m pytest -q tests/test_entrada_automatica.py tests/test_entrada_automatica_suspensa_adr017.py</automated>
  </verify>
  <acceptance_criteria>
    - `cd server && ./.venv/bin/python -m pytest -q tests/test_entrada_automatica.py` sai 0 com o MESMO número de testes de antes (nenhum teste de mecânica removido).
    - `grep -c "ENTRADA_AUTO_SUSPENSA_ADR017" server/tests/test_entrada_automatica.py` retorna 0.
    - `grep -c "historico_snapshot" server/tests/test_entrada_automatica.py` ≥ 1.
    - `grep -c '"rompimento"' server/tests/test_entrada_automatica.py server/tests/test_entrada_automatica_suspensa_adr017.py` retorna 0 nos dois arquivos.
    - `grep -c 'IFR2 (alta)' server/tests/test_entrada_automatica.py server/tests/test_entrada_automatica_suspensa_adr017.py` ≥ 1 nos dois arquivos.
    - `grep -c "^## Adendo 2" docs/adr/017-revisao-de-setups-e-selecao-dinamica.md` retorna 1.
    - `grep -v '^#' docs/adr/017-revisao-de-setups-e-selecao-dinamica.md | grep -c "IFR2 (alta)"` ≥ 1.
    - `git diff docs/adr/017-revisao-de-setups-e-selecao-dinamica.md | grep -c '^-[^-]'` retorna 0 (só adição; nenhuma linha histórica removida).
    - `bash scripts/executar.sh --testes` sai 0.
  </acceptance_criteria>
  <done>A suíte de mecânica roda contra o gate real (com snapshot stub elegível) em vez de contra uma flag desligada, e o ADR-017 registra o religamento.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ledger (SQLite kv) → decisão de execução automática | Um valor lido do banco passa a decidir se dinheiro simulado do usuário é movimentado sem interação |
| `scheduler_loop` → `_avaliar_entradas` | O laço único que atende toda a base; uma exceção nova aqui afeta stop/alvo de todos |
| Nome do setup (`r["setup"]`) → chave do snapshot | String vinda do plano determinístico usada como chave de lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Elevation of Privilege | `_avaliar_entradas` gate | mitigate | Predicado `is not True` — só o booleano verdadeiro passa; False, None, chave ausente e snapshot vazio caem todos no mesmo `continue`. Teste dedicado por caso |
| T-08-06 | Denial of Service | `historico_snapshot` no caminho do ciclo | mitigate | Leitura única por chamada (fora do laço) e cache de 300 s já embutido na função; teste com spy conta as chamadas |
| T-08-07 | Tampering | Falha de banco liberando entrada | mitigate | `historico_snapshot` devolve `{}` em falha; `{}` faz todo lookup dar `None` → gate fecha. Teste explícito de snapshot vazio |
| T-08-08 | Spoofing | Divergência entre o nome do setup no plano e a chave do ledger | mitigate | Ambos vêm de `setups.detect_setups` → `s["nome"]` (`setups.py:703`, `signal_replay.py:79`); as duas fixtures trocam o literal inventado `"rompimento"` por `"IFR2 (alta)"` — nome real do catálogo — para que o lookup por nome exato seja de fato exercitado |
| T-08-09 | Repudiation | Bloqueio silencioso indistinguível de bug | accept | Decisão explícita do 08-CONTEXT.md (não é erro, não gera warning). A transparência vive na UI (Planos 08-03/08-04), não no log do ciclo |

Sem instalação de pacote npm/pip/cargo neste plano — o gate de legitimidade de pacote não se aplica.
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` — as DUAS suítes verdes.
- `grep -rn "ENTRADA_AUTO_SUSPENSA_ADR017" server/` sem resultado.
- `git diff --stat` não lista `regime.py`, `setups.py`, `signal_ledger.py`, `signal_ledger_job.py`.
- NENHUM deploy: `atualizar.sh`, `entregar.sh`, `railway` e `git push` não são executados neste plano.
</verification>

<success_criteria>
- A entrada automática executa somente com `elegivel is True` para o setup do gatilho.
- Os três casos negativos (False, None, ausente) e a falha de leitura bloqueiam, em silêncio.
- A mecânica de lote/orçamento/tetos/dedupe segue com a mesma cobertura de teste de antes.
- ADR-017 tem o Adendo 2, por adição, sem reescrever texto histórico.
</success_criteria>

<output>
Create `.planning/phases/08-interface-e-ia-da-sele-o-din-mica-vocabul-rio-novo-skill-ref/08-02-SUMMARY.md` when done
</output>
