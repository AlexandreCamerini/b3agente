---
phase: 08-interface-e-ia-da-sele-o-din-mica-vocabul-rio-novo-skill-ref
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - web/src/finance.js
  - web/src/App.jsx
  - web/tests/test_finance.mjs
  - web/tests/test_historico_ui.mjs
autonomous: true
requirements: [ADR17-B34-02]

must_haves:
  truths:
    - "No Radar e na Watchlist, o usuário vê se o melhor setup do ticker tem vantagem estatística medida, sem abrir o card"
    - "Resultado negativo aparece com o MESMO destaque visual do positivo"
    - "Amostra insuficiente e nunca medido NUNCA aparecem estilizados como negativo"
    - "Dado velho degrada o carimbo de tempo, nunca esconde nem bloqueia o número"
    - "Todo pill novo tem equivalente legível por leitor de tela (nenhum glifo ✓/✗ sozinho)"
  artifacts:
    - path: "web/src/finance.js"
      provides: "historicoEstado() e historicoDesatualizado() — derivação pura dos 6 estados"
      contains: "historicoEstado"
    - path: "web/src/App.jsx"
      provides: "componente HistoricoPill + consumo de setupHistorico/setupElegivel no AtivoCard e no Radar"
      contains: "HistoricoPill"
    - path: "web/tests/test_historico_ui.mjs"
      provides: "guardião de UI dos estados, cores e acessibilidade do pill"
      contains: "HistoricoPill"
    - path: "web/tests/test_finance.mjs"
      provides: "guardião das funções puras de estado e de desatualização"
      contains: "historicoEstado"
  key_links:
    - from: "web/src/App.jsx:HistoricoPill"
      to: "web/src/copy.js (COPY[modo].historico / .historicoRotulo)"
      via: "import de copyFor/historicoTxt — nenhuma frase hardcodada no componente"
      pattern: "historicoTxt|cp\\.historicoRotulo"
    - from: "web/src/App.jsx:RadarScreen (radarVm)"
      to: "results[i].setupHistorico / results[i].setupElegivel"
      via: "campos já anexados por regime.ranquear no payload de /api/scan"
      pattern: "setupHistorico"
---

<objective>
Dar vitrine, no nível do TICKER, ao dado que o Bloco 1 já entrega em cada resultado de
`/api/scan`: `setupElegivel` (bool|null) e `setupHistorico` (dict|null). Criar o componente
`HistoricoPill` — a peça reusada pelo Plano 08-04 — e ligá-lo no Radar e na Watchlist.

Purpose: hoje nenhum componente do front lê esses campos (confirmado ao vivo: o card do ativo
está idêntico ao de antes do deploy do Bloco 1). O usuário ordena o Radar por um eixo que já
usa elegibilidade e não vê a razão da ordem.

Output: duas funções puras em `finance.js`, o componente `HistoricoPill`, o consumo no
`AtivoCard` (Watchlist/Home) e na linha de chips do Radar, e dois guardiões.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/skills/didatica-boris/SKILL.md
@.planning/phases/08-interface-e-ia-da-sele-o-din-mica-vocabul-rio-novo-skill-ref/08-CONTEXT.md
@.planning/phases/08-interface-e-ia-da-sele-o-din-mica-vocabul-rio-novo-skill-ref/08-UI-SPEC.md
@.planning/phases/08-interface-e-ia-da-sele-o-din-mica-vocabul-rio-novo-skill-ref/08-01-SUMMARY.md
@web/src/copy.js
@web/src/finance.js

<interfaces>
<!-- Contrato do dado (Fase 7, já em produção — NÃO recalcular) -->

results[i] de /api/scan (server/app/regime.py:256-294, `ranquear` muta a lista):
  setupHistorico: dict | null   // histórico do melhor setup OPERÁVEL
  setupElegivel:  bool | null   // null = insuficiente OU nunca medido
  melhorSetup:    string | null
  setups:         array         // usado só no Plano 08-04

historico (server/app/signal_ledger.py:222-246):
  {expR: number|null, n: number, medidoAte: "YYYY-MM-DD"|null,
   elegivel: boolean|null, insuficiente: boolean,
   expRJanela: number|null, nJanela: number,
   janelaRef: "AAAA"|null, calculadoEm: "ISO-8601 UTC"|null}

<!-- Contrato de texto (Plano 08-01, mesmo wave anterior) -->
web/src/copy.js:
  COPY[modo].historico       = {elegivel, inelegivel, insuficiente, nunca_medido, aposentado, desatualizado}
  COPY[modo].historicoRotulo = {elegivel, inelegivel, insuficiente, nunca_medido, aposentado}
  export function historicoTxt(mode, estado, vals)   // interpola {janela} e {medidoAte}

<!-- Primitivas de UI a REUSAR, nunca recriar (web/src/App.jsx) -->
  MONO           (linha 223)      fonte dos números
  T.*            (tokens CSS var) T.positive/positiveTint10, T.negative/negativeTint10,
                                  T.textFaint, T.textDim, T.textMuted, T.bgBase, T.bgCard, T.borderDashed
  REC_STYLE      (linha 1013)     o par [cor, tint] que "Estudar alta"/"Estudar baixa" já usam
  SR_ONLY        (linha ~2299)    estilo de texto só-para-leitor-de-tela
  ConfluenceRing (linha ~5553)    padrão role="img" aria-label
  padding de pill: "4px 10px", borderRadius "999px", fontSize 11px, fontWeight 700
                                  (convenção de TODO chip existente — não inventar outra)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derivação pura dos estados do histórico em finance.js</name>
  <files>web/src/finance.js, web/tests/test_finance.mjs</files>
  <read_first>
    - web/src/finance.js (linhas 1-40 e 115-160: o cabeçalho que declara o módulo como "fonte
      única de números determinísticos do front", o estilo de guard clause e de retorno em
      objeto nomeado das funções existentes)
    - web/tests/test_finance.mjs (o estilo de assert deste runner)
    - .planning/phases/08-.../08-UI-SPEC.md ("Interaction & State Inventory" — a definição
      exata de cada um dos estados e a regra de que `desatualizado` é MODIFICADOR, nunca
      estado próprio)
    - server/app/signal_ledger.py (linhas 222-246 — o shape real; em especial: quando o setup
      não estava na janela, `_fundir` grava `elegivel: null` E `insuficiente: true`)
  </read_first>
  <behavior>
    - `historicoEstado(historico, aposentado)`:
      • `aposentado === true` → "aposentado" (precedência sobre tudo; um setup aposentado nunca
        é rotulado como inelegível).
      • `historico` `undefined`/`null` → "nunca_medido".
      • `historico.insuficiente === true` OU `historico.elegivel == null` → "insuficiente".
      • `historico.elegivel === true` → "elegivel".
      • `historico.elegivel === false` → "inelegivel".
    - `historicoDesatualizado(historico, hojeYmd)` devolve boolean:
      • `true` quando a data de referência (`medidoAte`, ou o prefixo YYYY-MM-DD de
        `calculadoEm` quando `medidoAte` for nulo) é mais velha que 2 DIAS ÚTEIS antes de
        `hojeYmd`.
      • `false` quando não há data alguma (ausência de carimbo não vira "velho" — nunca
        inventar estado; a UI só degrada com evidência).
      • Sábado e domingo não contam como dia útil; feriados NÃO são considerados (limitação
        deliberada, documentada no comentário: erra sempre para o lado de "não degradar").
      • Data no futuro → `false`.
    - As duas funções são puras: sem `Date.now()` interno (a data de hoje é argumento), sem
      I/O, sem exceção para entrada malformada (string vazia, objeto vazio, número no lugar de
      string → cai nos defaults acima).
  </behavior>
  <action>
    Acrescente ao fim de `web/src/finance.js` uma seção nova com comentário-cabeçalho
    registrando: ADR-017 Bloco 3, estas funções derivam ESTADO a partir do dicionário
    `historico` que o backend já calculou — nenhum número novo é computado aqui, e a regra de
    que ausência de evidência não é evidência negativa ("Dois pisos de amostra", ADR-017) está
    codificada na precedência do estado "insuficiente" sobre "inelegivel".

    Exporte `export function historicoEstado(historico, aposentado)` e
    `export function historicoDesatualizado(historico, hojeYmd)`, com guard clause de entrada
    (estilo das funções vizinhas) e o comportamento do bloco acima.

    Para a contagem de dias úteis, faça a aritmética sobre `Date.UTC` a partir das partes
    YYYY-MM-DD (nunca `new Date(string)` com fuso implícito — no WKWebView isso desloca o dia)
    e conte para trás a partir de `hojeYmd` até acumular 2 dias úteis; a data de referência é
    "desatualizada" se for ESTRITAMENTE anterior a esse limite.

    Acrescente os testes ao fim de `web/tests/test_finance.mjs` (append, sem remover nada),
    cobrindo cada bullet do bloco `<behavior>`, inclusive: um caso de sexta-feira → segunda
    (o fim de semana não envelhece o dado) e um caso de `calculadoEm` sem `medidoAte`.
  </action>
  <verify>
    <automated>cd web && node tests/test_finance.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && node tests/test_finance.mjs` sai 0 e cobre os 6 estados + os 5 casos de desatualização.
    - `cd web && node -e "import('./src/finance.js').then(m=>{if(m.historicoEstado({elegivel:null,insuficiente:true})!=='insuficiente')process.exit(1);if(m.historicoEstado(null)!=='nunca_medido')process.exit(1);if(m.historicoEstado({elegivel:false},true)!=='aposentado')process.exit(1)})"` sai 0.
    - `cd web && node -e "import('./src/finance.js').then(m=>{if(m.historicoDesatualizado({medidoAte:'2026-08-14'},'2026-08-17')!==false)process.exit(1)})"` sai 0 (sexta → segunda: 1 dia útil, não degrada).
    - `grep -v '^//' web/src/finance.js | grep -c "new Date(\"" ` retorna 0 — nenhuma construção de Date a partir de string solta.
    - `cd web && npx vite build` sai 0.
  </acceptance_criteria>
  <done>O front tem uma fonte única e testada para decidir QUAL estado de histórico exibir, independente de componente.</done>
</task>

<task type="auto">
  <name>Task 2: Componente HistoricoPill (contrato visual dos 6 estados)</name>
  <files>web/src/App.jsx</files>
  <read_first>
    - web/src/App.jsx (linhas 1013-1031: `REC_STYLE` — o par [cor, tint] a REUSAR para
      positivo/negativo; e o formato tupla que o codebase usa para mapa de estilo por estado)
    - web/src/App.jsx (linhas 2860-2875: o helper `chip()` dentro de `AtivoCard` — o padding
      "4px 10px", o `borderRadius: "999px"`, o `fontWeight: 700` do rótulo e o `800` do valor)
    - web/src/App.jsx (linhas ~2295-2310: `SR_ONLY` e o padrão do botão só-para-leitor
      "O que é X?" do `SetorAlvo`; e linha ~5553-5568: `ConfluenceRing`, o padrão
      `role="img" aria-label=...`)
    - web/src/App.jsx (linhas 60-160: os tokens `T.*` disponíveis — confirme os nomes exatos
      `positive`, `positiveTint10`, `negative`, `negativeTint10`, `textFaint`, `textDim`,
      `textMuted`, `borderDashed`, `bgBase`, `bgCard`)
    - .planning/phases/08-.../08-UI-SPEC.md (tabelas "Color", "Typography", "Spacing Scale" —
      o mapeamento estado→par de token é contrato aprovado, não escolha do implementador)
    - web/src/copy.js (as chaves `historico`/`historicoRotulo` e o helper `historicoTxt`)
  </read_first>
  <action>
    Crie em `web/src/App.jsx`, perto de `ConfluenceRing` (antes de `RadarScreen`, para ficar no
    mesmo bairro dos outros primitivos de leitura do Radar), o componente
    `function HistoricoPill({ historico, elegivel, aposentado, operador, hojeYmd, compacto })`.

    Regras:
    - O estado vem de `historicoEstado(historico, aposentado)` (import de `./finance.js`,
      junto dos imports já existentes daquele módulo). NÃO reimplemente a derivação aqui.
      A prop `elegivel` existe só por paridade defensiva com o par que `regime._elegibilidade`
      devolve (`server/app/regime.py:231-253`): as três saídas possíveis são `(None, None)`,
      `(historico, None)` e `(historico, bool(...))` — `elegivel` NUNCA vem não-nulo com
      `historico` nulo. Portanto `historico` é a única fonte do estado; não escreva ramo de
      reconciliação para uma divergência que o backend não produz.
    - O rótulo desenhado é `cp.historicoRotulo[estado]` e o texto acessível é
      `historicoTxt(modoJS, estado, { janela: historico?.janelaRef, medidoAte: historico?.medidoAte })`.
      NENHUMA string literal de vocabulário dentro do componente.
    - Mapa de cor por estado, exatamente como o UI-SPEC define (reusando tokens, zero hex novo):
        elegivel     → [T.positive, T.positiveTint10]
        inelegivel   → [T.negative, T.negativeTint10]   (MESMO peso visual do positivo:
                       mesma fonte, mesmo tamanho, mesma opacidade de fundo — nunca dim)
        insuficiente → [T.textFaint, T.bgBase]
        nunca_medido → [T.textFaint, T.bgBase]
        aposentado   → [T.textMuted, T.bgCard] com `border: "1px dashed " + T.borderDashed`
                       (os outros quatro NÃO têm borda tracejada)
    - Tipografia/espaçamento: `fontSize: "11px"`, `fontWeight: 700`, `padding: "4px 10px"`,
      `borderRadius: "999px"`, `lineHeight: 1.2`; números (expR, n) em `fontFamily: MONO` e
      `fontWeight: 800`.
    - Acessibilidade obrigatória: o `<span>` do pill leva `role="img"` e
      `aria-label={fraseAcessivel}` — o glifo ✓/✗ nunca vai sozinho para o leitor de tela.
    - Quando `compacto` for falso, o componente também renderiza, ao lado do pill, os números
      da janela quando existirem: `expRJanela` formatado em R com vírgula decimal e sinal
      explícito (ex.: "+0,072R" / "−0,105R", usando o minus U+2212 como o resto do app), o
      `nJanela` como "n=123", e o `janelaRef`. Valor `null` NUNCA vira 0 — a métrica
      simplesmente não é desenhada (regra de casa: null, nunca 0.0).
    - Modificador de desatualização: se `historicoDesatualizado(historico, hojeYmd)` for
      verdadeiro, renderize o carimbo de tempo com `color: T.textDim` e prefixo "⏱ ", e
      acrescente ao `aria-label` a frase `historicoTxt(modoJS, "desatualizado", {...})`. O
      pill em si NÃO muda de cor — um "elegível" velho continua visualmente positivo
      (ADR-017 Decisão 2: degrada, nunca bloqueia).
    - `hojeYmd` default: derive de `new Date()` no ponto de USO (o componente aceita o
      argumento para permanecer testável), formatando como YYYY-MM-DD em horário local.

    Ligue o componente ao `AtivoCard`: dentro do bloco de chips de ANÁLISE
    (`contexto !== "radar"`, App.jsx ~3054-3073), depois do chip de `melhorSetup`, renderize
    `<HistoricoPill .../>` alimentado por `sc.setupHistorico` e `sc.setupElegivel` — e SÓ
    quando `sc.melhorSetup` existir (sem melhor setup não há o que qualificar; renderizar o
    pill ali seria ruído, não estado). A Watchlist já passa o objeto `sc` inteiro para o
    `AtivoCard`, então os dois campos chegam sem mudança de view-model.

    O pill é sinal SECUNDÁRIO (UI-SPEC, "Focal point"): entra DEPOIS do `melhorSetup` na linha
    de chips, nunca antes da confluência nem do `ConfluenceRing`.
  </action>
  <verify>
    <automated>cd web && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx vite build` sai 0.
    - `grep -c "function HistoricoPill" web/src/App.jsx` retorna 1.
    - `grep -c "historicoEstado" web/src/App.jsx` ≥ 1 e `grep -c "historicoDesatualizado" web/src/App.jsx` ≥ 1 — a derivação vem de `finance.js`, não é reimplementada.
    - `grep -c "aria-label" web/src/App.jsx` aumentou em relação ao HEAD anterior (`git diff HEAD -- web/src/App.jsx | grep -c '^+.*aria-label'` ≥ 1).
    - `grep -v '^\s*//' web/src/App.jsx | grep -c "Sem histórico medido ainda"` retorna 0 — nenhuma frase de vocabulário hardcodada.
    - `cd web && node tests/test_vocabulario_espelho.mjs` sai 0 (a asserção "App.jsx não hardcoda o vocabulário novo", do Plano 08-01, continua verde).
    - `cd web && node tests/test_hero_reconciliado.mjs && node tests/test_radar_watchlist.mjs && node tests/test_fundamento_ui.mjs` saem 0 — os guardiões do card do ativo não regrediram.
  </acceptance_criteria>
  <done>Existe um componente único que desenha os 6 estados com o contrato visual aprovado, com texto vindo de `copy.js` e equivalente acessível em todos eles; a Watchlist já o exibe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Radar consumindo setupHistorico/setupElegivel + guardião de UI</name>
  <files>web/src/App.jsx, web/tests/test_historico_ui.mjs</files>
  <read_first>
    - web/src/App.jsx (linha ~5776: a montagem de `radarVm`, que hoje passa
      `sc: { spark, confluencia, melhorSetup }` — um SUBCONJUNTO; e linhas 5778-5791: a linha
      de chips do Radar com `confiança {tierLabel}`, `FundamentoChip`, `RegimeChip` e o texto
      de `melhorSetup`, que é onde o pill entra)
    - web/src/App.jsx (o `HistoricoPill` criado na Task 2)
    - web/tests/test_radar_regime_chip.mjs (o guardião mais próximo em forma: como esta base
      testa um chip do Radar lendo `App.jsx` como texto, sem build e sem DOM)
    - .planning/phases/08-.../08-UI-SPEC.md ("Where these render" e "Focal point")
  </read_first>
  <behavior>
    - `App.jsx` contém `setupHistorico` e `setupElegivel` dentro do objeto `sc` do `radarVm`
      (o Radar deixa de estrangular o view-model).
    - A linha de chips do Radar renderiza `<HistoricoPill` depois do texto de `melhorSetup`.
    - O mapa de estilo do `HistoricoPill` usa `T.positive`/`T.positiveTint10` para `elegivel` e
      `T.negative`/`T.negativeTint10` para `inelegivel` — e nenhum dos dois usa `T.accent`
      (regra do UI-SPEC: acento é identidade de marca/modo, não veredito de elegibilidade).
    - `insuficiente` e `nunca_medido` usam o par neutro `T.textFaint`/`T.bgBase` — o guardião
      falha se qualquer um dos dois for mapeado para `T.negative`.
    - `aposentado` é o ÚNICO estado com `dashed` no estilo.
    - Nenhum dos textos de `COPY[modo].historico`/`historicoRotulo` aparece literal em
      `App.jsx`.
    - Todo pill carrega `aria-label`.
  </behavior>
  <action>
    1. Em `web/src/App.jsx`, na montagem de `radarVm` (linha ~5776), acrescente
       `setupHistorico: r.setupHistorico` e `setupElegivel: r.setupElegivel` ao objeto `sc`.
       Registre em comentário curto por que o `sc` do Radar é um subconjunto explícito (e não
       o `r` inteiro): o `AtivoCard` do Radar renderiza um cabeçalho enxuto, e passar o
       resultado inteiro reintroduziria no card campos que o Radar decidiu não mostrar.

    2. Na linha de chips do Radar (dentro do `<AtivoCard ... contexto="radar">`, logo depois do
       `<span>` que mostra `r.melhorSetup ... critérios ... alinhado ao regime`), renderize
       `<HistoricoPill historico={r.setupHistorico} elegivel={r.setupElegivel}
       operador={operador} />`, condicionado a `r.melhorSetup` existir. Mesma regra de
       secundariedade da Task 2: entra depois, nunca antes do `ConfluenceRing`/confluência.

    3. Crie `web/tests/test_historico_ui.mjs` — guardião estático que lê `web/src/App.jsx` e
       importa `COPY` de `../src/copy.js` (padrão de `test_radar_regime_chip.mjs` +
       `test_rr_min_fonte_unica.mjs`; caminhos por `new URL(..., import.meta.url)`, nunca
       relativos ao cwd). Cubra cada bullet do bloco `<behavior>`. Para os mapas de cor, recorte
       o corpo de `function HistoricoPill` do fonte e faça as asserções DENTRO desse recorte
       (assertar contra o arquivo inteiro daria falso-positivo: `T.negative` aparece em dezenas
       de outros lugares). Falhe explicitamente se o recorte vier vazio.

       Ao gravar asserções por contagem, filtre comentários antes de contar
       (`grep -v '^\s*//'` equivalente em JS) — o próprio comentário do componente cita os
       nomes dos estados e invalidaria a contagem.
  </action>
  <verify>
    <automated>cd web && node tests/test_historico_ui.mjs && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && node tests/test_historico_ui.mjs` sai 0.
    - `cd web && npx vite build` sai 0.
    - `git diff HEAD -- web/src/App.jsx | grep -c 'setupHistorico'` ≥ 1.
    - Sabotagem controlada: trocar, numa cópia em sandbox de `App.jsx`, o par de `insuficiente` para `T.negative` faz `test_historico_ui.mjs` FALHAR; restaure em seguida. O arquivo real não é alterado.
    - `bash scripts/executar.sh --testes` sai 0 (as DUAS suítes).
    - `cd web && node tests/test_radar.mjs && node tests/test_radar_leitura_rapida.mjs && node tests/test_radar_regime_chip.mjs` saem 0.
  </acceptance_criteria>
  <done>O Radar e a Watchlist mostram a elegibilidade medida do melhor setup de cada ticker, nos 6 estados, com guardião que trava cor, acessibilidade e origem do texto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Payload de `/api/scan` → renderização | Campos podem vir ausentes/nulos (provedor desligado, ledger vazio, backend antigo em cache) |
| Estado visual → interpretação do usuário | Confundir "sem dado" com "ruim" é dano de produto direto (decisão financeira simulada baseada em leitura errada) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Information Disclosure | `HistoricoPill` estado neutro × negativo | mitigate | `historicoEstado` dá precedência a "insuficiente" sobre "inelegivel"; guardião falha se o par neutro for mapeado para `T.negative` |
| T-08-11 | Tampering | Manipulação visual de resultado negativo | mitigate | `inelegivel` usa o mesmo peso tipográfico e a mesma opacidade de tint do `elegivel` (par de `REC_STYLE`); asserção no guardião |
| T-08-12 | Denial of Service | Payload sem os campos (backend antigo/cache) | mitigate | Guard clause em `historicoEstado` devolve "nunca_medido"; o pill só renderiza com `melhorSetup` presente; `npx vite build` prova ausência de erro de sintaxe |
| T-08-13 | Repudiation | Número exibido divergindo do medido | mitigate | Nenhum cálculo novo no front: `expRJanela`/`nJanela`/`janelaRef` são desenhados como vieram; `null` nunca vira 0 |
| T-08-14 | Elevation of Privilege | Vocabulário nascendo solto no componente | mitigate | Asserção do Plano 08-01 (App.jsx não contém as frases) + asserção própria no `test_historico_ui.mjs` |

Sem instalação de pacote npm/pip/cargo neste plano — o gate de legitimidade de pacote não se aplica.
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` — as DUAS suítes verdes.
- `cd web && npx vite build` sai 0 (obrigatório: grep e teste estático não pegam erro de sintaxe JSX).
- Guardiões de regressão do Radar/card verdes: `test_radar.mjs`, `test_radar_leitura_rapida.mjs`, `test_radar_regime_chip.mjs`, `test_hero_reconciliado.mjs`, `test_radar_watchlist.mjs`.
</verification>

<success_criteria>
- Radar e Watchlist exibem os 6 estados de elegibilidade do melhor setup por ticker.
- Negativo tem o mesmo destaque visual que positivo; insuficiente e nunca-medido são neutros, nunca negativos.
- Dado velho degrada só o carimbo de tempo.
- Todo pill tem `aria-label`; nenhuma frase de vocabulário vive no componente.
</success_criteria>

<output>
Create `.planning/phases/08-interface-e-ia-da-sele-o-din-mica-vocabul-rio-novo-skill-ref/08-03-SUMMARY.md` when done
</output>
