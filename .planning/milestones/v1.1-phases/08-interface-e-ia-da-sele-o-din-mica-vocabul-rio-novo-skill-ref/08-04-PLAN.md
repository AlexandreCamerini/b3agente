---
phase: 08-interface-e-ia-da-sele-o-din-mica-vocabul-rio-novo-skill-ref
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-03"]
files_modified:
  - web/src/App.jsx
  - web/tests/test_historico_setup_card_ui.mjs
autonomous: true
requirements: [ADR17-B34-03, ADR17-B34-04]

must_haves:
  truths:
    - "Cada setup listado no card do ativo carrega o próprio histórico medido, não só a confluência"
    - "Setup aposentado continua na lista com rótulo próprio — o usuário entende que é arquivamento, não bug nem remoção"
    - "Os 4 estados completos aparecem: nunca medido, amostra insuficiente, dado desatualizado, aposentado"
    - "O usuário do Modo Operador vê, no card de status, POR QUE a entrada automática só dispara em alguns setups"
    - "No Modo Operador, cada setup listado diz — nomeando o setup — se a entrada automática está disponível ou bloqueada PARA ELE, e em que janela isso foi medido"
    - "O contraste do backtest aparece sempre com os dois números juntos"
  artifacts:
    - path: "web/src/App.jsx"
      provides: "histórico por setup na lista de critérios + linha de transparência do gate no card C-19"
      contains: "entradaAuto"
    - path: "web/tests/test_historico_setup_card_ui.mjs"
      provides: "guardião dos 4 estados por setup e da linha de transparência do gate"
      contains: "aposentado"
  key_links:
    - from: "web/src/App.jsx (lista de setups do card)"
      to: "results[i].setups[n].historico / .aposentado"
      via: "campos anexados por setups.detect_setups no payload de /api/scan"
      pattern: "s\\.historico|s\\.aposentado"
    - from: "web/src/App.jsx (item de lista de setup)"
      to: "web/src/copy.js entradaAutoTxt"
      via: "frase parametrizada por setup ({setup}/{janelaRef}) — transparência do gate no nível do setup, 08-UI-SPEC"
      pattern: "entradaAutoTxt"
    - from: "web/src/App.jsx:AgenteScreen (card C-19)"
      to: "web/src/copy.js COPY[modo].entradaAuto"
      via: "cp.entradaAuto.regra e cp.entradaAuto.contraste — nenhuma frase hardcodada"
      pattern: "cp\\.entradaAuto"
---

<objective>
Fechar a vitrine no nível do SETUP: cada item de `sres["setups"]` já carrega `historico` e
`aposentado`, e nada disso aparece hoje. Ligar os 4 estados completos na lista de setups do
card do ativo, e acrescentar ao card de status único do Operador (C-19) a linha que explica o
gate de entrada automática que o Plano 08-02 acabou de ligar.

Purpose: é aqui que "aposentado ≠ apagado" fica claro para quem já viu esses 6 setups antes do
Bloco 0, e é aqui que o usuário do Modo Operador descobre por que a entrada automática voltou
restrita — sem isso, o religamento do 08-02 é uma mudança de comportamento invisível.

Output: lista de setups com histórico por setup, rótulo de aposentado, modificador de dado
velho, linha de transparência do gate no card C-19 e um guardião novo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/skills/didatica-boris/SKILL.md
@.planning/phases/08-interface-e-ia-da-sele-o-din-mica-vocabul-rio-novo-skill-ref/08-CONTEXT.md
@.planning/phases/08-interface-e-ia-da-sele-o-din-mica-vocabul-rio-novo-skill-ref/08-UI-SPEC.md
@.planning/phases/08-interface-e-ia-da-sele-o-din-mica-vocabul-rio-novo-skill-ref/08-01-SUMMARY.md
@.planning/phases/08-interface-e-ia-da-sele-o-din-mica-vocabul-rio-novo-skill-ref/08-03-SUMMARY.md
@web/src/copy.js

<interfaces>
<!-- Já entregue pelo Bloco 1 (Fase 7). NADA é recalculado aqui. -->

results[i].setups[n] (server/app/setups.py:532-584, `detect_setups`):
  nome:        string          // "IFR2 (alta)", "Setup 9.2 (baixa)", ...
  confluencia: number
  criterios:   [{criterio, ok, obrigatorio?}]
  aposentado:  boolean         // SEMPRE presente (setups.py:556)
  historico:   dict | null     // presente só quando o provedor está ligado
                               // (setups.py:557-558 — sem provedor a CHAVE NEM EXISTE)

<!-- Do Plano 08-03 (wave 2) -->
web/src/finance.js:
  export function historicoEstado(historico, aposentado) -> "elegivel"|"inelegivel"|"insuficiente"|"nunca_medido"|"aposentado"
  export function historicoDesatualizado(historico, hojeYmd) -> boolean
web/src/App.jsx:
  function HistoricoPill({ historico, elegivel, aposentado, operador, hojeYmd, compacto })

<!-- Do Plano 08-01 (wave 1) -->
web/src/copy.js:
  COPY[modo].entradaAuto = { regra, contraste, por_setup_disponivel, por_setup_bloqueado }
    // regra/contraste          -> texto AGREGADO do card C-19 (Task 2)
    // por_setup_*              -> frase POR SETUP, parametrizada ({setup}, {janelaRef}) (Task 1)
  export function entradaAutoTxt(mode, estado, vals)
    // estado "disponivel" libera a frase positiva; qualquer outro valor cai em bloqueado
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Histórico por setup na lista de critérios do card</name>
  <files>web/src/App.jsx</files>
  <read_first>
    - web/src/App.jsx (linhas ~5862-5882: o botão "+ Ver critérios do setup" e o bloco
      `{isOpen && (r.setups || []).map((s, si) => ...)}` com a linha
      `<span>{s.nome}</span><span style={{ fontFamily: MONO, ... }}>{s.confluencia}%</span>` —
      é ESTE item de lista que ganha o histórico)
    - web/src/App.jsx (o `HistoricoPill` criado no Plano 08-03 — a assinatura e o que ele já
      desenha, para NÃO duplicar rótulo nem número aqui)
    - web/src/finance.js (`historicoEstado`, `historicoDesatualizado`)
    - web/src/copy.js (`COPY[modo].entradaAuto.por_setup_disponivel` /
      `.por_setup_bloqueado` e o helper `entradaAutoTxt`, criados no Plano 08-01)
    - .planning/phases/08-.../08-UI-SPEC.md (bloco "Entrada automática gate transparency",
      linhas ~111-116 — as DUAS frases parametrizadas que este item de lista precisa desenhar)
    - .planning/phases/08-.../08-UI-SPEC.md ("Interaction & State Inventory" itens 1-4 e
      "Focal point": `s.nome` + `s.confluencia%` seguem sendo a âncora primária; o histórico é
      pill TRAILING, lido depois)
    - .planning/phases/08-.../08-CONTEXT.md (secção "Setup aposentado" — o setup CONTINUA na
      lista, com rótulo próprio, nunca confundido com setup mantido de baixa confluência)
  </read_first>
  <action>
    No item de lista de setup (`{isOpen && (r.setups || []).map(...)}`), acrescente, na MESMA
    linha do cabeçalho `s.nome` / `s.confluencia%` mas depois deles (gap de 8px, conforme a
    escala do UI-SPEC), `<HistoricoPill historico={s.historico} aposentado={s.aposentado}
    operador={operador} />` com `compacto` FALSO — aqui os números da janela (`expRJanela`,
    `nJanela`, `janelaRef`) devem aparecer, porque é a superfície de estudo do setup.

    Se o cabeçalho ficar apertado no mobile, quebre em duas linhas (`flexWrap: "wrap"` no
    contêiner que já existe) — NÃO reduza o peso tipográfico do pill negativo para "caber"
    (manipulação visual de resultado é proibida pela skill `didatica-boris`).

    Acrescente, logo abaixo do cabeçalho do item, o carimbo de tempo do dado quando existir:
    `medidoAte` (ou o prefixo YYYY-MM-DD de `calculadoEm` na ausência dele), em
    `fontSize: "11px"`, `color: T.textFaint`, e — quando `historicoDesatualizado` for
    verdadeiro — `color: T.textDim` com prefixo "⏱ ". Sem carimbo no payload, a linha
    simplesmente não é desenhada (nunca inventar data).

    Transparência do gate NO NÍVEL DO SETUP (08-UI-SPEC, "Entrada automática gate
    transparency" — as duas frases parametrizadas que o Plano 08-01 pôs em
    `COPY[modo].entradaAuto`): quando `operador` for verdadeiro, renderize, logo abaixo do
    carimbo de tempo do item, UMA linha com
    `entradaAutoTxt(modoJS, estadoGate, { setup: s.nome, janelaRef: s.historico?.janelaRef })`,
    em `fontSize: "11px"`, `lineHeight: 1.45`, `color: T.textMuted`.

    `estadoGate` é `"disponivel"` SOMENTE quando `historicoEstado(s.historico, s.aposentado)`
    devolver `"elegivel"`; em `inelegivel`, `insuficiente`, `nunca_medido` e `aposentado` é
    `"bloqueado"`. É exatamente o predicado `elegivel is True` que o Plano 08-02 implementou em
    `_avaliar_entradas` — a tela não pode prometer nada diferente do gate real.

    Em Modo Estudo a linha NÃO é renderizada: não existe entrada automática em Estudo, e
    anunciar disponibilidade ali seria falso. As duas strings vivem nos dois ramos de `copy.js`
    só por paridade de chave do guardião cruzado.

    Esta linha é POR SETUP e NÃO substitui nem duplica `cp.entradaAuto.regra`/`.contraste`, que
    seguem sendo o texto agregado do card C-19 (Task 2): uma responde "por que só alguns
    setups", a outra responde "e este aqui?". Nenhuma frase literal no JSX — tudo vem do helper.

    Setup aposentado: o `HistoricoPill` já entrega o rótulo e a borda tracejada. O item da
    lista NÃO deve ganhar nenhum tratamento adicional que sugira remoção (não esmaecer o nome
    do setup, não riscar, não esconder critérios) — "aposentado ≠ apagado". Registre isso em
    comentário no código, citando ADR-017 Decisão 1.

    Os 4 estados completos DEVEM ser todos alcançáveis a partir do payload real:
      • chave `historico` ausente (provedor desligado) e `historico: null` (nunca medido)
        caem os dois em "nunca_medido" — do ponto de vista do usuário é o mesmo fato, e o
        08-UI-SPEC ("Error state") determina explicitamente que uma falha do provedor renderiza
        como estado vazio, NUNCA como banner de erro vermelho;
      • `insuficiente: true` ou `elegivel: null` → "insuficiente";
      • carimbo velho → modificador;
      • `aposentado: true` → rótulo próprio.

    NÃO altere o cálculo de `critOk`/`critTot`, nem a ordenação da lista, nem o filtro
    `MIN_CONFLUENCIA` (é do backend). NÃO mexa em `RegimeChip`, `FundamentoChip` ou
    `ConfluenceRing`.
  </action>
  <verify>
    <automated>cd web && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx vite build` sai 0.
    - `git diff HEAD -- web/src/App.jsx | grep -c '^+.*s\.historico'` ≥ 1 e `git diff HEAD -- web/src/App.jsx | grep -c '^+.*s\.aposentado'` ≥ 1.
    - `grep -v '^\s*//' web/src/App.jsx | grep -c "Padrão gráfico identificado"` retorna 0 — o rótulo de aposentado vem de `copy.js`, não do componente.
    - `cd web && node tests/test_vocabulario_espelho.mjs` sai 0.
    - `git diff HEAD -- web/src/App.jsx | grep -c '^+.*entradaAutoTxt'` ≥ 1.
    - `grep -v '^\s*//' web/src/App.jsx | grep -c "Entrada automática disponível para"` retorna 0 e o mesmo para "Entrada automática bloqueada para" — as frases por setup vêm de `copy.js`.
    - `cd web && node tests/test_radar.mjs && node tests/test_radar_leitura_rapida.mjs && node tests/test_historico_ui.mjs` saem 0.
  </acceptance_criteria>
  <done>Abrir "Ver critérios do setup" mostra, por setup, o histórico medido nos 4 estados completos, com o setup aposentado presente e rotulado; em Modo Operador, cada setup também diz — nomeando-o — se a entrada automática está disponível (e em que janela) ou bloqueada.</done>
</task>

<task type="auto">
  <name>Task 2: Linha de transparência do gate no card de status do Operador</name>
  <files>web/src/App.jsx</files>
  <read_first>
    - web/src/App.jsx (linhas 3811-3885: `AgenteScreen`, o comentário-âncora
      "C-19 (REPORT-01)", os três `<span>` de badge — Modo do app / Operador no servidor /
      Executar/sinalizar — e o botão "Trocar modo →" que fecha o card)
    - web/tests/test_fase3_c19_card_status.mjs (arquivo inteiro — ele recorta o bloco do card
      entre o marcador "C-19 (REPORT-01)" e o wrapper do `<h1>`; QUALQUER mudança tem que
      manter os dois marcadores intactos, senão o guardião aborta com "marcador não
      encontrado")
    - web/tests/test_auditoria_status_strip.mjs e web/tests/test_agente_modo_estudo_ui.mjs
      (os outros dois guardiões que recortam `AgenteScreen`)
    - web/src/copy.js (`COPY[modo].entradaAuto` — `regra` e `contraste`, criadas no 08-01)
    - .planning/phases/08-.../08-UI-SPEC.md ("Entrada automática gate transparency" e o
      "Checkpoint humano note": é transparência, NÃO um diálogo de confirmação; nenhum toggle
      novo)
  </read_first>
  <action>
    Em `AgenteScreen`, DENTRO do card de status único (entre o fecho da linha dos três badges
    e o botão "Trocar modo →", ou logo abaixo da linha de badges dentro do mesmo contêiner —
    o que preservar os dois marcadores que `test_fase3_c19_card_status.mjs` usa), acrescente
    uma linha ADITIVA, read-only, com:

      • `cp.entradaAuto.regra` — `fontSize: "11px"`, `color: T.textMuted`, `lineHeight: 1.45`;
      • `cp.entradaAuto.contraste` — mesma tipografia, `color: T.textFaint`, em elemento
        próprio logo abaixo. Os dois números moram na MESMA string vinda de `copy.js`; não os
        separe em dois nós, não formate um deles em destaque.

    `cp` já está disponível no escopo de `AgenteScreen`? Confirme na leitura; se não estiver,
    obtenha-o pelo mesmo caminho que as outras telas usam (`ctx.cp`) — não crie um segundo
    caminho de resolução de modo.

    Regras não-negociáveis:
      - NENHUM toggle, botão ou controle novo: o card C-19 é read-only por decisão registrada
        no próprio comentário do bloco ("Read-only: nenhum badge altera estado").
      - NÃO remova nem reordene os três badges existentes — a linha é aditiva.
      - NÃO altere o texto do comentário-âncora "C-19 (REPORT-01)" nem o wrapper do
        `<h1>Operador IA</h1>`: são os marcadores de recorte do guardião existente.
      - Nenhuma frase literal no JSX: tudo vem de `cp.entradaAuto`.

    Acrescente comentário curto acima da linha registrando: ADR-017 Bloco 4 — a entrada
    automática deixou de ser suspensão cega e virou gate por elegibilidade medida (Plano
    08-02); esta linha é a transparência exigida pelo 08-UI-SPEC, e o número do contraste é
    referência FIXA de backtest (ADR-016/017), nunca cálculo vivo.
  </action>
  <verify>
    <automated>cd web && node tests/test_fase3_c19_card_status.mjs && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && node tests/test_fase3_c19_card_status.mjs` sai 0 (nenhum marcador quebrado, os 3 badges intactos).
    - `cd web && node tests/test_auditoria_status_strip.mjs && node tests/test_agente_modo_estudo_ui.mjs && node tests/test_agente_hero_infodot.mjs && node tests/test_agent_server_toggle.mjs && node tests/test_entrada_automatica_ui.mjs` saem 0.
    - `cd web && npx vite build` sai 0.
    - `git diff HEAD -- web/src/App.jsx | grep -c 'cp\.entradaAuto'` ≥ 2 (regra + contraste).
    - `grep -v '^\s*//' web/src/App.jsx | grep -c "0,099R"` retorna 0 — o número vive só em `copy.js`.
  </acceptance_criteria>
  <done>Quem está no Modo Operador lê, no primeiro elemento da tela, a regra do gate e o contraste honesto do backtest — com os dois números sempre juntos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Guardião dos 4 estados por setup e da transparência do gate</name>
  <files>web/tests/test_historico_setup_card_ui.mjs</files>
  <read_first>
    - web/tests/test_fase3_c19_card_status.mjs (o padrão de recorte por marcador com abort
      explícito quando o marcador some — replicar, para o guardião nunca virar no-op)
    - web/tests/test_historico_ui.mjs (o guardião criado no Plano 08-03 — para não duplicar
      asserções; este arquivo cobre o NÍVEL SETUP e o card C-19, aquele cobre o nível ticker)
    - web/src/App.jsx (as duas regiões editadas nas Tasks 1 e 2)
    - web/src/copy.js
  </read_first>
  <behavior>
    - Falha se o item de lista de setup deixar de renderizar `HistoricoPill` com
      `s.historico`/`s.aposentado`.
    - Falha se o nome do setup aposentado for esmaecido, riscado ou removido da lista (asserta
      que o bloco do item NÃO contém `textDecoration: "line-through"` nem `opacity` menor que 1
      aplicado ao `s.nome`).
    - Falha se `s.confluencia` deixar de ser a âncora primária (o `HistoricoPill` aparece
      DEPOIS de `s.confluencia` no fonte do item).
    - Falha se o item de lista de setup não chamar `entradaAutoTxt` passando `s.nome` como
      `setup` (transparência do gate por setup — 08-UI-SPEC "Entrada automática gate
      transparency").
    - Falha se a linha por setup não estiver condicionada a `operador` (em Modo Estudo ela não
      pode aparecer).
    - Falha se `App.jsx` contiver, literal, "Entrada automática disponível para" ou "Entrada
      automática bloqueada para".
    - Falha se o card C-19 perder qualquer um dos três badges originais.
    - Falha se `cp.entradaAuto.regra` ou `cp.entradaAuto.contraste` não forem renderizados no
      card C-19.
    - Falha se `App.jsx` contiver, literal, "0,099R", "0,005R" ou qualquer valor de
      `COPY[modo].entradaAuto`.
    - Falha se qualquer marcador de recorte usado pelo próprio guardião sumir (abort com
      mensagem, nunca "0 asserções, tudo ok").
  </behavior>
  <action>
    Crie `web/tests/test_historico_setup_card_ui.mjs` no padrão estático da casa: lê
    `web/src/App.jsx` como texto (caminho por `new URL(..., import.meta.url)`, nunca relativo
    ao cwd — `scripts/executar.sh` roda com cwd=web) e importa `COPY` de `../src/copy.js`.

    Recorte duas regiões, cada uma com abort explícito se o marcador não for achado:
      1. O item de lista de setup — âncora do início: a expressão que abre o `.map` sobre
         `r.setups`; fim: o fecho desse `.map`. Confirme as âncoras reais no fonte antes de
         escrever a regex (o arquivo mudou nesta fase).
      2. O bloco do card C-19 dentro de `AgenteScreen` — reuse a mesma estratégia de
         `test_fase3_c19_card_status.mjs` (marcador "C-19 (REPORT-01)" até o wrapper do `<h1>`).

    Escreva uma asserção por bullet do bloco `<behavior>`. Ao contar ocorrências, filtre
    comentários antes de contar — os comentários novos citam nominalmente os estados e os
    números do contraste, e uma contagem crua sobre o arquivo bruto se auto-invalidaria.

    Escreva o comentário do topo explicando o que este guardião trava e por que ele existe
    separado do `test_historico_ui.mjs` (nível ticker × nível setup + card do Operador).
  </action>
  <verify>
    <automated>cd web && node tests/test_historico_setup_card_ui.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && node tests/test_historico_setup_card_ui.mjs` sai 0 com pelo menos 11 linhas "ok ".
    - Sabotagem controlada: numa cópia em sandbox de `App.jsx`, remover o `HistoricoPill` do item de setup faz o guardião FALHAR; remover a chamada de `entradaAutoTxt` do item de setup faz o guardião FALHAR; remover um dos três badges do card C-19 faz o guardião FALHAR; renomear o marcador "C-19 (REPORT-01)" faz o guardião ABORTAR com mensagem (não passar em silêncio). Restaure a cópia em seguida; o arquivo real não é alterado.
    - `bash scripts/executar.sh --testes` sai 0 (as DUAS suítes).
    - `cd web && npx vite build` sai 0.
  </acceptance_criteria>
  <done>Regredir qualquer um dos 4 estados por setup, esconder o setup aposentado ou apagar a transparência do gate quebra a suíte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Payload de setup → renderização | `historico` pode ser ausente, `null`, ou vir com campos nulos; `aposentado` sempre existe |
| Card de status do Operador → expectativa do usuário sobre execução automática | O que a tela promete precisa bater com o gate real de `agent.py` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-15 | Information Disclosure | Contraste do backtest | mitigate | Os dois números vivem numa string única em `copy.js`; guardião falha se `App.jsx` contiver o literal, e o 08-01 falha se um número aparecer sem o outro |
| T-08-16 | Tampering | Setup aposentado desaparecendo ou parecendo bug | mitigate | Asserção de que o item continua na lista, sem line-through nem esmaecimento do nome; rótulo canônico vindo de `copy.js` |
| T-08-17 | Repudiation | Card C-19 prometendo comportamento diferente do gate de `agent.py` | mitigate | A frase `regra` descreve exatamente o predicado implementado no 08-02 (`elegivel is True`); o checkpoint do 08-05 confere os dois ao vivo |
| T-08-18 | Denial of Service | Regressão silenciosa dos guardiões do `AgenteScreen` | mitigate | Marcadores de recorte preservados; aceite roda os 5 guardiões existentes da tela; guardião novo ABORTA se o marcador sumir |
| T-08-19 | Elevation of Privilege | Controle novo no card read-only | mitigate | Linha estritamente aditiva e read-only; `test_fase3_c19_card_status.mjs` continua verde |
| T-08-21 | Spoofing | Linha por setup anunciando "entrada automática disponível" onde o gate bloqueia | mitigate | `estadoGate` é `"disponivel"` só quando `historicoEstado` devolve `"elegivel"` — mesmo predicado de `_avaliar_entradas` (08-02); todo outro estado cai em bloqueado, e `entradaAutoTxt` também falha fechada; guardião asserta a condição |

Sem instalação de pacote npm/pip/cargo neste plano — o gate de legitimidade de pacote não se aplica.
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` — as DUAS suítes verdes.
- `cd web && npx vite build` sai 0.
- Guardiões de regressão do `AgenteScreen` verdes: `test_fase3_c19_card_status.mjs`, `test_auditoria_status_strip.mjs`, `test_agente_modo_estudo_ui.mjs`, `test_agente_hero_infodot.mjs`, `test_agent_server_toggle.mjs`, `test_entrada_automatica_ui.mjs`.
</verification>

<success_criteria>
- Cada setup da lista mostra o próprio histórico medido, cobrindo os 4 estados completos.
- Setup aposentado permanece visível, com rótulo próprio e sem tratamento de "removido".
- O card de status do Operador explica a regra do gate e o contraste, com os dois números juntos.
- Cada setup da lista, em Modo Operador, carrega a frase parametrizada de disponibilidade/bloqueio da entrada automática (08-UI-SPEC), nomeando o setup e a janela de medição.
- Guardião novo falha nas quatro sabotagens controladas.
</success_criteria>

<output>
Create `.planning/phases/08-interface-e-ia-da-sele-o-din-mica-vocabul-rio-novo-skill-ref/08-04-SUMMARY.md` when done
</output>
