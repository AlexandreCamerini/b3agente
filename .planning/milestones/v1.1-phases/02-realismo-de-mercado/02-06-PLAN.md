---
phase: 02-realismo-de-mercado
plan: 06
type: execute
wave: 5
depends_on: [02-05]
files_modified:
  - web/src/copy.js
  - web/src/App.jsx
  - web/tests/test_ordens_pendentes_ui.mjs
autonomous: true
requirements: [MERC-02, MERC-03, MERC-04]

must_haves:
  truths:
    - "Ao confirmar uma ordem com o mercado fechado, o usuário lê ANTES que ela ficará pendente e que o caixa é reservado já na confirmação"
    - "Uma ordem pendente aparece numa seção 'Pendentes' no topo do histórico, com o valor reservado visível"
    - "O usuário cancela uma ordem pendente em dois passos e vê o caixa voltar imediatamente"
    - "Confirmar uma ordem pendente NÃO abre o fluxo de stop/alvo (não existe posição ainda)"
    - "Se o status do mercado estiver indisponível, o app não finge que está aberto nem bloqueia o usuário — pede nova tentativa"
    - "Se uma ordem pendente foi auto-cancelada na abertura, o usuário vê o motivo ao abrir o app (toast), não só no diário de diagnóstico do Operador"
  artifacts:
    - path: "web/src/App.jsx"
      provides: "Estado fechado nos modais, seção Pendentes no HistoricoScreen, cancelamento em dois passos"
      contains: "Pendentes"
    - path: "web/src/copy.js"
      provides: "Textos de ordem pendente nos DOIS modos"
      contains: "ordemPendente"
    - path: "web/tests/test_ordens_pendentes_ui.mjs"
      provides: "Guardiões de copy, de render condicional e do fluxo de cancelamento"
      min_lines: 70
  key_links:
    - from: "web/src/App.jsx (BuyModal/SellModal)"
      to: "ctx.mercado.aberto"
      via: "mesma fonte única do badge — sem segunda consulta"
      pattern: "mercado"
    - from: "web/src/App.jsx (HistoricoScreen)"
      to: "data.pendingOrders"
      via: "seção Pendentes renderiza só quando há ordem"
      pattern: "pendingOrders"
    - from: "web/src/App.jsx (efeito de resumo na abertura, ~linha 6535)"
      to: "data.agentLog"
      via: "toast também para warn com tag pendente-cancelada, não só kind buy"
      pattern: "pendente-cancelada"
    - from: "web/src/App.jsx (A.cancelPendingOrder)"
      to: "store.cancelPendingOrder"
      via: "ação do ctx chama o store e adota o estado devolvido"
      pattern: "store\\.cancelPendingOrder"
---

<objective>
Fechar o ciclo na interface: o usuário entende ANTES de confirmar que a ordem
ficará pendente e que o caixa é reservado; vê a ordem pendente no histórico com
o valor reservado; e cancela em dois passos, com o caixa voltando na hora.

Purpose: MERC-02/03/04 do ponto de vista de quem usa. Sem isto, a ordem some da
tela e o caixa "evapora" — exatamente o oposto do que a fase promete.
Output: estado fechado nos modais, seção Pendentes, cancelamento em dois passos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-realismo-de-mercado/02-CONTEXT.md
@.planning/phases/02-realismo-de-mercado/02-UI-SPEC.md
@.planning/phases/02-realismo-de-mercado/02-05-SUMMARY.md
@.claude/skills/didatica-boris/SKILL.md
@CLAUDE.md

<interfaces>
Dados disponíveis no cliente (planos 02-02 e 02-04)
- `ctx.mercado` = `{aberto, diaDePregao, abertura, fechamento, agoraBRT, afterMarket}` |
  `null` (carregando) | `{erro: true}` (indisponível)
- `ctx.data.pendingOrders` = lista de
  `{id, criadaEm, tipo: "COMPRA"|"VENDA", t, qty, precoReferencia, caixaReservado|null,
    avgReservado|null, origem, meta, ultimaTentativaEm, ultimoErro}`
- `ctx.data.caixaReservado` = number
- `store.cancelPendingOrder(id) -> public_state atualizado` (existe nos DOIS stores)
- `POST /api/buy|/api/sell` com mercado fechado devolve `{..., pendente: true, order, priceUsed: null, precoReferencia}`

web/src/App.jsx (pontos exatos de enxerto)
- `HistoricoScreen({ ctx })` — linha 3647. Cabeçalho da tabela na linha 3660-3662:
  `padding: "11px 15px"`, `fontSize: "10px"`, `letterSpacing: "0.06em"`, `MONO`,
  colunas com flex `1.6 / 1 / 0.9 / 0.6 / 1 / 1.1` (DATA, TIPO, ATIVO, QTD, PREÇO, RESULTADO).
  Linha de dado: `padding: "12px 15px"`, `MONO`, `fontSize: "13px"` (linha 3664)
- `BuyModal({ ctx })` — linha 6144; rótulo "COMPRA SIMULADA" na linha 6156; disclaimer
  atual "O preço final é o da cotação no momento da confirmação (servidor)." na linha 6180;
  validação `ok = cost <= data.cash && q.price != null` na linha 6150 e o aviso de caixa na 6179
- `SellModal({ ctx })` — linha 6192; rótulo "VENDA SIMULADA" na linha 6210; disclaimer na linha 6237
- `A.confirmBuy` — linha 6732 (depois do sucesso, abre `setStopAlvoFor(bm.t)` + `A.runStopAlvoFor`)
- `A.confirmSell` — linha 6684
- `flash(msg)` — toast de 2600ms
- Botão destrutivo de referência: `Excluir conta` (`App.jsx:582`, borda+texto `T.negative`,
  fundo transparente) e o secundário `Cancelar` (`App.jsx:587`)

UI-SPEC (contrato visual force-approved — valores NÃO negociáveis)
- pill "PENDENTE": `padding: "3px 9px"`, `borderRadius: "999px"`, `fontSize: "10.5px"`,
  `fontWeight: 800`, cor `T.warn` sobre tint (nunca `T.positive`/`T.negative`, que
  significam operação já concluída)
- botão de cancelar: ícone `✕`, alvo de toque 40px, `aria-label="Cancelar ordem pendente de {ticker}"`,
  na posição da coluna RESULTADO
- confirmação em DOIS passos, inline, largura total abaixo da linha: "Manter ordem"
  (secundário) e "Confirmar cancelamento" (`T.negative`, borda+texto, fundo transparente)
- seção "Pendentes" só renderiza quando há ≥ 1 ordem — sem estado vazio próprio
- disclaimer do modal com mercado fechado substitui a linha atual, e o rótulo do botão
  de confirmar NÃO muda
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BuyModal/SellModal com estado "mercado fechado" (MERC-02/03)</name>
  <files>web/src/copy.js, web/src/App.jsx, web/tests/test_ordens_pendentes_ui.mjs</files>
  <read_first>
    - web/src/App.jsx (linhas 6144-6250: os dois modais inteiros; linhas 6680-6750: confirmSell e confirmBuy)
    - web/src/copy.js (convenções do cabeçalho + as chaves adicionadas no plano 02-05)
    - .planning/phases/02-realismo-de-mercado/02-UI-SPEC.md (Copywriting Contract — as linhas exatas de disclaimer aberto/fechado e o erro de status)
    - web/tests/test_copy_theme.mjs (guardião de simetria de chaves)
    - CLAUDE.md (princípio 9: estados completos; princípio 4: sem inventar quando a fonte falha)
  </read_first>
  <behavior>
    - Chaves novas de copy existem nos DOIS modos e o guardião de simetria continua verde.
    - `ctx.mercado.aberto === false`: os modais mostram o pill "PENDENTE" ao lado do
      rótulo (COMPRA SIMULADA / VENDA SIMULADA) e trocam a linha de disclaimer pela que
      explica pendência + reserva de caixa + horário de abertura.
    - `ctx.mercado.aberto === true`: modais renderizam exatamente como hoje (nenhum pill,
      disclaimer original) — teste de não-regressão por inspeção da fonte.
    - `ctx.mercado` com `{erro: true}`: o modal mostra o aviso de status indisponível com
      ação de nova tentativa, e o botão Confirmar CONTINUA habilitado (não bloqueia, não
      assume nada).
    - O rótulo do botão de confirmar (`ctx.cp.confirmarCompra`/`confirmarVenda`) NÃO muda
      em nenhum dos casos.
    - A validação `ok`/"Caixa insuficiente" continua idêntica (a reserva debita o mesmo
      valor que uma ordem imediata — D-05).
    - `A.confirmBuy` com resposta `pendente: true`: exibe o toast de ordem pendente e NÃO
      chama `setStopAlvoFor`/`runStopAlvoFor` (não há posição para proteger ainda).
    - `A.confirmSell` com resposta `pendente: true`: exibe o toast de ordem pendente em
      vez do toast de venda executada.
  </behavior>
  <action>
    Em `web/src/copy.js`, acrescentar aos DOIS ramos as chaves:
    `ordemPendentePill` (string, "PENDENTE"), `ordemPendenteAvisoCompra` e
    `ordemPendenteAvisoVenda` (funções `(abertura) => string` com o texto da UI-SPEC:
    mercado fechado agora, a ordem fica pendente, executa ao preço de abertura do
    próximo pregão às {HH:MM}, e o caixa é reservado já na confirmação),
    `mercadoStatusFalhouNaOrdem` (string, com a frase de nova tentativa),
    `toastOrdemPendente` (função `(qty, ticker) => string`) e
    `toastOrdemPendenteCancelada` (string). Tom por modo (Estudo = professor, Operador =
    mesa), FATO idêntico. Sem vocabulário de ordem no ramo `estudo` além do que o
    guardião já permite nas chaves existentes — se a frase precisar nomear a operação,
    usar a mesma forma já aceita nas chaves `toastCompra`/`toastVenda` do ramo.

    Em `web/src/App.jsx`, nos dois modais:
    • derivar `const fechado = ctx.mercado && ctx.mercado.aberto === false;` e
      `const statusIndisponivel = !!(ctx.mercado && ctx.mercado.erro);` — ler SEMPRE de
      `ctx.mercado` (fonte única do plano 02-05), nunca consultar a rota de novo;
    • quando `fechado`, renderizar o pill ao lado do rótulo do modal com os valores
      exatos da UI-SPEC (`padding: "3px 9px"`, `borderRadius: "999px"`, `fontSize:
      "10.5px"`, `fontWeight: 800`, cor `T.warn` sobre o tint correspondente) e
      substituir a linha de disclaimer (linha 6180 no BuyModal, 6237 no SellModal) pelo
      texto de `ordemPendenteAviso*`, passando `ctx.mercado.abertura`;
    • quando `statusIndisponivel`, renderizar acima dos botões o aviso
      `mercadoStatusFalhouNaOrdem` com um botão de nova tentativa que chama a mesma
      ação de recarregar status exposta pelo `ctx` (se ela ainda não existir, expor
      `ctx.recarregarMercado` no plano-05-criado efeito, reusando o mesmo setter — não
      criar uma segunda consulta);
    • NÃO mexer no stepper de quantidade, na prévia de custo, no layout dos botões nem
      na validação `ok`.

    Em `A.confirmBuy` (linha 6732) e `A.confirmSell` (linha 6684): ramificar pelo campo
    `pendente` da resposta do store. Pendente ⇒ `flash(cp.toastOrdemPendente(qty, t))`,
    fechar o modal, e no caso da compra PULAR `setStopAlvoFor`/`A.runStopAlvoFor`,
    com comentário explicando que stop/alvo pertencem a uma posição que ainda não
    existe. Manter o `track("trade_simulated", ...)` mas acrescentar
    `pendente: true` às propriedades, para a analítica não confundir ordem pendente com
    execução.

    Criar `web/tests/test_ordens_pendentes_ui.mjs` (padrão A + inspeção estática da
    fonte de `App.jsx`, como nos guardiões de UI já existentes em
    `web/tests/test_agente_modo_estudo_ui.mjs`), cobrindo o bloco `<behavior>`:
    simetria das chaves novas, presença do texto de pendência no recorte de cada modal,
    ausência de troca do rótulo do botão de confirmar, e o ramo de `confirmBuy` que não
    chama `setStopAlvoFor` quando `pendente`.
  </action>
  <verify>
    <automated>cd web && node tests/test_ordens_pendentes_ui.mjs && node tests/test_copy_theme.mjs && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ordemPendenteAvisoCompra" web/src/copy.js` == 2 (uma por modo); idem
      para `ordemPendenteAvisoVenda`, `toastOrdemPendente`, `mercadoStatusFalhouNaOrdem`.
    - `node web/tests/test_copy_theme.mjs` sai 0.
    - Recorte de `function BuyModal` contém `ordemPendenteAvisoCompra` e `T.warn`;
      recorte de `function SellModal` contém `ordemPendenteAvisoVenda`.
    - `grep -n "confirmarCompra\|confirmarVenda" web/src/App.jsx` continua com o mesmo
      número de ocorrências de antes (o rótulo do CTA não foi ramificado).
    - Recorte de `confirmBuy` mostra `setStopAlvoFor` dentro de um ramo condicionado a
      NÃO-pendente.
    - `cd web && npx vite build` sai 0.
  </acceptance_criteria>
  <done>
    Quem confirma uma ordem com o mercado fechado sabe, antes de clicar, o que vai
    acontecer com a ordem e com o caixa.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seção "Pendentes" no HistoricoScreen (MERC-02/03)</name>
  <files>web/src/App.jsx, web/tests/test_ordens_pendentes_ui.mjs</files>
  <read_first>
    - web/src/App.jsx (linhas 3647-3677: HistoricoScreen inteiro — a tabela que a seção nova espelha)
    - web/src/App.jsx (linhas 6535-6552: efeito de resumo do agente na abertura — hoje filtra `e.kind === "buy"`)
    - .planning/phases/02-realismo-de-mercado/02-UI-SPEC.md (Pending Orders Section: hierarquia, cor do pill, ausência de estado vazio próprio)
    - web/src/App.jsx (linha 935: pill "no portfólio" — `padding: "3px 9px"`, `borderRadius: "999px"`, `fontSize: "10.5px"`, `fontWeight: 800`, o padrão exato a copiar)
  </read_first>
  <behavior>
    - Com `data.pendingOrders` vazio, a seção não renderiza NADA (nem cabeçalho, nem
      caixa vazia) e a tela continua idêntica à de hoje.
    - Com ≥ 1 ordem, a seção "Pendentes" aparece ACIMA da tabela de operações, com o
      mesmo tratamento visual de cabeçalho/linha (10px MONO no header, 13px MONO no dado).
    - O pill da coluna TIPO usa `T.warn`, nunca `T.positive`/`T.negative`.
    - O subtítulo da seção mostra o total reservado (`money(data.caixaReservado)`) e
      deixa explícito que o valor volta ao caixa se a ordem for cancelada.
    - Uma ordem com `ultimoErro` preenchido mostra o motivo da última tentativa
      (transparência de falha de dado — princípio 4), sem esconder a ordem.
    - A tela de histórico com `history` vazio MAS com ordem pendente mostra a seção
      Pendentes (o estado vazio de histórico não pode engolir a fila).
    - Abrindo o app depois de um auto-cancelamento na abertura, o resumo mostra o toast
      com o MOTIVO vindo do evento (`kind === "warn"` e `tag === "pendente-cancelada"`),
      não só a contagem de ações do agente.
    - Um `warn` do agente SEM a `tag` (qualquer outro aviso do Operador) continua não
      gerando toast — o filtro é pela `tag`, não pelo `kind`.
  </behavior>
  <action>
    Em `web/src/App.jsx`, dentro de `HistoricoScreen` (linha 3647), inserir antes do
    bloco atual (`data.history.length === 0 ? ... : ...`) um bloco condicional
    `{(data.pendingOrders || []).length > 0 && (...)}` — o idioma de render condicional
    já usado no arquivo (`{logged && (...)}`, `{srvErr && (...)}`); nenhum estado vazio
    próprio, por decisão da UI-SPEC.

    Estrutura da seção: título "Pendentes" no mesmo peso do H1 da tela? NÃO — subordinado:
    usar o tratamento de kicker/cabeçalho da própria tabela (10px MONO, `letterSpacing:
    "0.06em"`, `T.textFaint`) dentro de um `card` com `overflow: "hidden"`, idêntico ao
    da tabela de operações. Subtítulo com o total reservado, em 11.5px `T.textMuted`.
    Colunas: reusar os mesmos flex do cabeçalho existente (`1.6 / 1 / 0.9 / 0.6 / 1 /
    1.1`) com os rótulos DATA (=`criadaEm`), TIPO (pill PENDENTE + COMPRA/VENDA), ATIVO,
    QTD, PREÇO (=`precoReferencia`, rotulado como referência, nunca como preço de
    execução) e a última coluna reservada para a ação de cancelar (Task 3).

    O pill PENDENTE copia byte a byte os valores de `App.jsx:935` (`padding: "3px 9px"`,
    `borderRadius: "999px"`, `fontSize: "10.5px"`, `fontWeight: 800`) trocando só a cor
    para `T.warn` sobre o tint correspondente — a UI-SPEC registra por escrito o porquê:
    `T.positive`/`T.negative` significam operação CONCLUÍDA e leriam como "já executou".

    Quando `o.ultimoErro` existir, renderizar uma linha secundária discreta (11px,
    `T.warn`) com o motivo e o `ultimaTentativaEm` — a ordem tentou executar e não
    conseguiu; esconder isso violaria o princípio 4.

    ALÉM DA SEÇÃO: estender o efeito de resumo na abertura (`web/src/App.jsx`, ~linha
    6535, hoje `const acts = news.filter((e) => e.kind === "buy")`) para também recolher
    `news.filter((e) => e.tag === "pendente-cancelada")` e, quando houver, chamar
    `flash(e.text)` + `notify.send("Boris+", e.text)` com o texto do PRÓPRIO evento —
    o texto já vem pronto do motor determinístico (plano 02-01) com ticker, preços e
    motivo, então a UI não recompõe a frase (guardrail: manchete vem do motor).
    Manter o toast de ações do agente como está; se houver os dois, disparar o de
    cancelamento por último (é o que exige ação do usuário). Comentar POR QUE:
    auto-cancelamento é a única forma de uma ordem pendente sumir sem o usuário ter
    pedido; sem este toast (e sem o push do plano 02-03) ele só descobriria no "Diário
    do Operador", que é tela de diagnóstico, não caminho de usuário do Modo Estudo.
    Não criar chave de copy nova para isso — o texto é do evento, e duplicá-lo em
    `copy.js` criaria duas fontes para a mesma frase.

    Estender `web/tests/test_ordens_pendentes_ui.mjs` com inspeção estática do recorte
    de `HistoricoScreen`: contém `pendingOrders`, contém `T.warn`, contém
    `caixaReservado`; e o render é condicionado a `length > 0` (regex do padrão
    `(data.pendingOrders || []).length > 0 &&`).
  </action>
  <verify>
    <automated>cd web && node tests/test_ordens_pendentes_ui.mjs && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - Recorte de `function HistoricoScreen` contém `pendingOrders`, `caixaReservado`,
      `T.warn` e a string `Pendentes`.
    - O recorte NÃO contém `T.positive` associado ao pill de pendente (assertado pelo
      teste no trecho do pill).
    - `grep -n "3px 9px" web/src/App.jsx` mostra o pill novo usando exatamente esse padding.
    - Teste assere a condicional de `length > 0` (ausência de estado vazio próprio).
    - `grep -c "pendente-cancelada" web/src/App.jsx` == 1 e a ocorrência está dentro do
      efeito de resumo da abertura (o recorte que contém `agentSummaryDone`).
    - Teste assere que o filtro do toast de cancelamento usa a `tag` (não `e.kind ===
      "warn"` solto), e que o texto exibido é `e.text` — sem string literal de motivo
      escrita no front.
    - `cd web && npx vite build` sai 0.
  </acceptance_criteria>
  <done>
    A ordem pendente e o caixa reservado ficam visíveis onde o usuário já procura o
    histórico, sem tela nova.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cancelamento em dois passos com devolução imediata (MERC-04)</name>
  <files>web/src/App.jsx, web/tests/test_ordens_pendentes_ui.mjs</files>
  <read_first>
    - web/src/App.jsx (linhas 575-595: o padrão `Excluir conta` — confirmação em dois passos, cores dos dois botões)
    - web/src/App.jsx (linhas 6680-6760: como as ações do `A` tratam erro com `flash`)
    - web/src/App.jsx (a seção Pendentes criada na Task 2)
    - .planning/phases/02-realismo-de-mercado/02-UI-SPEC.md (Copywriting Contract → Destructive confirmation; touch target 40px; aria-label exato)
  </read_first>
  <behavior>
    - Cada linha pendente tem um botão só-ícone `✕` com
      `aria-label="Cancelar ordem pendente de {ticker}"` e alvo de toque de 40px.
    - Um toque expande a confirmação inline (largura total, abaixo da linha) com o valor
      reservado no texto e dois botões de texto: "Manter ordem" e "Confirmar cancelamento".
    - "Manter ordem" fecha a confirmação sem chamar nada.
    - "Confirmar cancelamento" chama `store.cancelPendingOrder(id)`, adota o estado
      devolvido (`setData`), e mostra o toast de cancelamento.
    - Erro do servidor (404/401/rede) vira `flash` com a mensagem do erro — nunca some
      em silêncio, nunca finge sucesso local.
    - Só UMA confirmação fica aberta por vez (abrir a de outra linha fecha a anterior).
    - Nenhum caminho de um clique só executa o cancelamento.
  </behavior>
  <action>
    Em `web/src/App.jsx`, acrescentar ao objeto de ações `A` o método
    `cancelPendingOrder: async (id) => { try { const s = await
    store.cancelPendingOrder(id); setData(s); flash(cp.toastOrdemPendenteCancelada); }
    catch (e) { flash("Cancelamento: " + (e.message || e)); } }` — mesma forma dos
    vizinhos (`sell`, `setStop`), com o erro subindo para o toast.

    Na seção Pendentes, controlar a confirmação com um estado local do componente
    (`const [confirmando, setConfirmando] = useState(null)` guardando o id) — um por
    vez, por construção. Botão gatilho: só-ícone `✕`, `width: "40px", height: "40px"`
    (valor do botão de perfil do Topbar, `App.jsx:748`, conforme a exceção documentada
    na UI-SPEC), `aria-label` interpolado com o ticker. Linha de confirmação: largura
    total abaixo da linha compacta, com o texto citando `money(o.caixaReservado)` para
    COMPRA (e a quantidade/posição devolvida para VENDA), e os dois botões — "Manter
    ordem" no estilo secundário de `App.jsx:587` e "Confirmar cancelamento" no estilo
    destrutivo de `App.jsx:582` (borda + texto `T.negative`, fundo transparente).

    O botão de confirmar deve ficar desabilitado enquanto a chamada está em voo (estado
    local de "enviando"), para que um duplo toque não dispare dois cancelamentos — o
    servidor devolve 404 no segundo, mas a UI não deve provocar isso.

    Estender `web/tests/test_ordens_pendentes_ui.mjs` com inspeção estática:
    `aria-label="Cancelar ordem pendente de` presente; `40px` no botão gatilho;
    as duas strings dos botões de confirmação presentes; `store.cancelPendingOrder`
    chamado exatamente 1 vez no arquivo; e nenhuma chamada de `store.cancelPendingOrder`
    fora de um handler que passa pela confirmação (assertar que o identificador
    `confirmando` aparece no mesmo recorte).
  </action>
  <verify>
    <automated>cd web && node tests/test_ordens_pendentes_ui.mjs && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "store.cancelPendingOrder" web/src/App.jsx` == 1.
    - `grep -n 'aria-label="Cancelar ordem pendente de' web/src/App.jsx` retorna 1 linha.
    - `grep -n "Manter ordem" web/src/App.jsx` e `grep -n "Confirmar cancelamento" web/src/App.jsx`
      retornam 1 linha cada.
    - Teste assere que o recorte da seção Pendentes contém `confirmando` e que o botão
      gatilho tem `width: "40px"`.
    - `bash scripts/executar.sh --testes` sai 0.
    - `cd web && npx vite build` sai 0.
  </acceptance_criteria>
  <done>
    Cancelar uma ordem pendente exige dois passos, explica o que volta para o caixa, e
    reflete o estado real devolvido pelo servidor.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| toque do usuário → mutação de caixa | cancelar devolve dinheiro reservado; um toque acidental muda estado financeiro |
| resposta do servidor → tela | o estado exibido depois do cancelamento tem que ser o do servidor, não um palpite local |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-27 | Tampering | cancelamento acidental (um toque) | mitigate | confirmação inline em dois passos, no padrão `Excluir conta`; nenhum caminho de um clique só |
| T-02-28 | Repudiation | duplo toque disparando dois cancelamentos | mitigate | botão de confirmar desabilitado enquanto a chamada está em voo; o servidor ainda responde 404 no segundo (plano 02-01) |
| T-02-29 | Spoofing | UI mostrando caixa devolvido sem o servidor ter confirmado | mitigate | `setData` só com o `public_state` devolvido pela rota; erro vira `flash`, nunca sucesso otimista |
| T-02-30 | Information Disclosure | modal afirmando "mercado aberto" quando o status falhou | mitigate | terceiro estado explícito com nova tentativa; Confirmar permanece habilitado mas nada é presumido (UI-SPEC) |
| T-02-31 | Tampering | pill pendente lido como operação concluída | mitigate | cor `T.warn` reservada para "aguardando execução"; teste trava a ausência de `T.positive`/`T.negative` no pill |
| T-02-36 | Repudiation | ordem pendente auto-cancelada desaparecendo sem explicação na tela | mitigate | toast na abertura do app para eventos com `tag == "pendente-cancelada"`, com o texto do motor; complementa o push do plano 02-03 para quem não tem push ligado |
| T-02-SC | Tampering | instalação de pacote npm/pip/cargo | accept | nenhuma dependência nova; sem shadcn/registry (UI-SPEC, Registry Safety) |
</threat_model>

<verification>
- `for t in web/tests/*.mjs; do node "$t" >/dev/null || echo "FALHOU: $t"; done` não imprime nada.
- `cd web && npx vite build` sai 0.
- `bash scripts/executar.sh --testes` sai 0.
</verification>

<success_criteria>
- Success Criteria 2, 3 e 5 da Phase 2 visíveis na interface.
- Nenhuma tela nova; tudo dentro de `HistoricoScreen`, `BuyModal` e `SellModal`.
- Estado de falha de status tratado sem presumir nem bloquear.
</success_criteria>

<output>
Create `.planning/phases/02-realismo-de-mercado/02-06-SUMMARY.md` when done
</output>
