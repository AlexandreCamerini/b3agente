---
phase: 02-realismo-de-mercado
plan: 05
type: execute
wave: 4
depends_on: [02-04]
files_modified:
  - web/src/copy.js
  - web/src/App.jsx
  - web/tests/test_status_mercado_ui.mjs
autonomous: true
requirements: [MERC-01]

must_haves:
  truths:
    - "Usuário NÃO logado, na tela de entrada, vê se a B3 está aberta ou fechada agora"
    - "Usuário logado vê o mesmo status no Topbar, nos dois modos (Estudo e Operador)"
    - "Quando a consulta de status falha, o app diz 'indisponível' — nunca chuta aberto nem fechado"
    - "O status exibido vem de uma única fonte no app (um estado, dois pontos de render)"
    - "Criar uma ordem pendente não faz o patrimônio da Topbar encolher (o reservado é somado de volta)"
  artifacts:
    - path: "web/src/App.jsx"
      provides: "MarketStatusBadge + estado mercado no root + render em WelcomeAuthScreen e Topbar"
      contains: "MarketStatusBadge"
    - path: "web/src/copy.js"
      provides: "Textos do badge nos DOIS modos (chaves simétricas)"
      contains: "mercadoAberto"
    - path: "web/tests/test_status_mercado_ui.mjs"
      provides: "Guardiões de simetria de copy, dos 3 estados e do ponto de render pré-login"
      min_lines: 60
  key_links:
    - from: "web/src/App.jsx (root)"
      to: "store.marketStatus()"
      via: "useEffect de boot, antes de qualquer login"
      pattern: "store\\.marketStatus\\("
    - from: "web/src/App.jsx (WelcomeAuthScreen)"
      to: "MarketStatusBadge"
      via: "render pré-autenticação (D-08)"
      pattern: "MarketStatusBadge"
    - from: "web/src/App.jsx (Topbar)"
      to: "MarketStatusBadge"
      via: "linha de modo, após o divisor ·"
      pattern: "MarketStatusBadge"
---

<objective>
Tornar o status real do pregão visível ao usuário: um componente único
`MarketStatusBadge` alimentado por um único estado no root do app, renderizado
na tela de LOGIN (antes de autenticar) e no Topbar (depois).

Purpose: MERC-01 / Success Criterion 1 do ROADMAP / D-08. Hoje esse status só
existe pós-login, escondido na tela de diagnóstico do Operador (`App.jsx:5052`).
Output: badge nos dois pontos, textos por modo em `copy.js`, guardiões.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-realismo-de-mercado/02-CONTEXT.md
@.planning/phases/02-realismo-de-mercado/02-UI-SPEC.md
@.planning/phases/02-realismo-de-mercado/02-04-SUMMARY.md
@.claude/skills/didatica-boris/SKILL.md
@CLAUDE.md

<interfaces>
Contrato entregue pelo plano 02-04
- `store.marketStatus() -> {aberto, diaDePregao, abertura: "10:00", fechamento: "16:55", agoraBRT, afterMarket}`
- `portfolioMetrics(positions, quotes, cash, reservado)` — 4º argumento opcional, `patr` inclui `reservado`
- `public_state` (em `ctx.data`) traz `pendingOrders` e `caixaReservado`

web/src/App.jsx (pontos exatos de enxerto)
- `WelcomeAuthScreen({ ctx, onAuthed })` — linhas 611-685; o bloco de marca+tagline termina
  no parágrafo de disclaimer das linhas 644-646, e é logo abaixo dele que o badge entra (UI-SPEC)
- `Topbar({ patr, dia, caixa, name, onProfile, modeChip })` — linhas 707-753; a LINHA DE MODO
  é o `div` das linhas 730-739 (dot 7px + `modeChip` + divisor `·` + `name`)
- `const ctx = {...}` — linha 7214; `ctx.operador` é a fonte única de "estamos em Modo Operador"
- `portfolioMetrics(...)` é chamado em 7 lugares: linhas 1532, 1632, 3441, 7172, 7332, 7355, 7365
- `flash(msg)` (linha ~6371) — toast de 2600ms
- Tokens: `T.positive`, `T.negative`, `T.warn`, `T.textFaint`, `MONO`, `DISPLAY` (definidos em `App.jsx:40-230`)

web/src/copy.js
- `export const COPY = { estudo: {...}, operador: {...} }` — chaves DEVEM ser idênticas nos
  dois ramos; o guardião `web/tests/test_copy_theme.mjs:27` compara
  `Object.keys(COPY.estudo).sort()` com `Object.keys(COPY.operador).sort()`
- vocabulário de ordem ("comprar"/"vender") é PROIBIDO no ramo `estudo` (guardião)
- `copyFor(modo)` é o acessor usado por `App.jsx` (`ctx.cp`)

UI-SPEC (contrato visual — 02-UI-SPEC.md, force-approved; valores NÃO negociáveis)
- badge = dot (7px, `borderRadius: "50%"`, `boxShadow: "0 0 0 3px <tint>"`) + label
  10-11px, `fontWeight: 800`, `letterSpacing` 0.05-0.07em
- aberto → `T.positive`; fechado → `T.negative`; indisponível → `T.warn`
- o badge NUNCA usa `T.accent` (precisa renderizar igual pré-login, sem contexto de modo)
- em `WelcomeAuthScreen` fica ABAIXO da tagline/disclaimer, nunca depois da dobra do formulário
- em `Topbar` entra na linha de modo, após o mesmo divisor `·` já usado ali
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Textos do badge em copy.js (chaves simétricas nos dois modos)</name>
  <files>web/src/copy.js, web/tests/test_status_mercado_ui.mjs</files>
  <read_first>
    - web/src/copy.js (arquivo inteiro — 162 linhas; ler o cabeçalho de convenções e os dois ramos)
    - web/tests/test_copy_theme.mjs (o guardião de simetria de chaves, linha 27)
    - .planning/phases/02-realismo-de-mercado/02-UI-SPEC.md (seção "Copywriting Contract" — textos exatos)
    - .claude/skills/didatica-boris/SKILL.md (vocabulário por modo: Estudo = professor, Operador = mesa)
  </read_first>
  <behavior>
    - `Object.keys(COPY.estudo).sort()` continua igual a `Object.keys(COPY.operador).sort()`
      depois das chaves novas (o guardião existente não pode quebrar).
    - `copyFor("estudo").mercadoAberto` e `copyFor("operador").mercadoAberto` existem e são strings.
    - `copyFor(m).mercadoFechado("10:00")` produz um texto que contém "10:00"; chamado sem
      argumento (horário desconhecido), NÃO inventa horário nenhum.
    - `copyFor(m).mercadoIndisponivel` não contém as palavras "aberto" nem "fechado" —
      estado indisponível não pode ser lido como afirmação sobre o mercado.
    - Nenhuma das chaves novas do ramo `estudo` contém vocabulário de ordem
      ("comprar"/"vender"/"compre"/"venda") — o guardião existente já cobre, o teste novo reforça.
  </behavior>
  <action>
    Em `web/src/copy.js`, acrescentar aos DOIS ramos (`estudo` e `operador`) as chaves:
    `mercadoAberto` (string), `mercadoFechado` (função `(abertura) => string`),
    `mercadoIndisponivel` (string).

    Textos base vêm do Copywriting Contract da UI-SPEC: aberto = "Mercado aberto";
    fechado = "Mercado fechado" com a segunda linha "abre {HH:MM}" SOMENTE quando o
    horário vier (sem horário, só "Mercado fechado" — CLAUDE.md princípio 4: nunca
    inventar um dado que a fonte não deu); indisponível = "Status do mercado
    indisponível". A diferença entre os modos é de TOM, não de fato: no ramo `estudo` o
    texto pode ensinar (ex.: acrescentar que a B3 negocia das 10:00 às 16:55 em dia
    útil), no ramo `operador` fica seco. Fato idêntico nos dois — nunca dois estados
    diferentes para o mesmo dado.

    Comentar as chaves novas com o marcador de fase (Phase 2 / MERC-01 / D-08) na mesma
    voz dos comentários existentes do arquivo.

    Criar `web/tests/test_status_mercado_ui.mjs` (padrão A, checker booleano `ok(nome,
    cond)`), com os casos de `<behavior>` acima, importando `copyFor` de `../src/copy.js`.
    O arquivo continuará crescendo nas tasks seguintes.
  </action>
  <verify>
    <automated>cd web && node tests/test_status_mercado_ui.mjs && node tests/test_copy_theme.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mercadoAberto" web/src/copy.js` == 2 (uma por modo); idem para
      `mercadoFechado` e `mercadoIndisponivel`.
    - `node web/tests/test_copy_theme.mjs` sai 0 (simetria de chaves preservada).
    - Teste assere que `copyFor("estudo").mercadoFechado()` (sem argumento) não contém
      nenhum padrão `\d{1,2}:\d{2}`.
    - Teste assere que `mercadoIndisponivel` não casa com `/aberto|fechado/i`.
  </acceptance_criteria>
  <done>
    Os três estados do badge têm texto próprio nos dois modos, simétricos e sem
    afirmação inventada.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Estado único de mercado no root + componente MarketStatusBadge</name>
  <files>web/src/App.jsx, web/tests/test_status_mercado_ui.mjs</files>
  <read_first>
    - web/src/App.jsx (linhas 40-240: tokens T.*, MONO/DISPLAY; linhas 6260-6400: estados do root e `flash`; linhas 7214-7270: montagem do `ctx`)
    - web/src/App.jsx (linhas 5040-5060: o ÚNICO lugar que hoje lê `pregaoAberto`, na tela de diagnóstico do Operador)
    - .planning/phases/02-realismo-de-mercado/02-UI-SPEC.md (Phase-Specific Component Notes → Market Status Badge)
    - web/tests/test_brand_book_v2_tokens.mjs (guardião de tokens/contraste — o badge não pode introduzir cor fora do sistema)
  </read_first>
  <behavior>
    - Existe UM estado no root (`mercado`) e ele é a única fonte do badge — grep prova
      que `store.marketStatus()` é chamado em exatamente 1 lugar do arquivo.
    - `MarketStatusBadge` renderiza 3 estados e nada mais: aberto (`T.positive`),
      fechado (`T.negative`), indisponível (`T.warn`).
    - Enquanto a primeira consulta está em voo, o badge não afirma nada (nada renderiza,
      ou o idioma de `spin` já usado em `WelcomeAuthScreen` — nunca "fechado" por default).
    - Falha de rede na consulta cai em `indisponível`, sem `flash` de erro (não é erro do
      usuário) e sem derrubar a tela.
    - O badge NÃO usa `T.accent` em nenhum estado.
    - O status é reconsultado quando o app volta do background/foco, para não ficar
      exibindo "fechado" às 10:05 de quem deixou a tela aberta.
  </behavior>
  <action>
    Em `web/src/App.jsx`, criar o componente `MarketStatusBadge({ mercado, cp })` perto
    dos outros componentes de chrome (logo antes de `Topbar`, linha 707). Props: o
    objeto de status (`null` = ainda carregando, `{erro: true}` = indisponível) e o
    dicionário de copy do modo. Visual EXATAMENTE conforme UI-SPEC: `span` do dot com
    `width/height: "7px"`, `borderRadius: "50%"`, `boxShadow: "0 0 0 3px <tint da cor
    semântica>"`, seguido do label com `fontSize: "10px"`, `fontWeight: 800`,
    `letterSpacing: "0.06em"`, `whiteSpace: "nowrap"`. Cores só de
    `T.positive`/`T.negative`/`T.warn` (+ os tints correspondentes) — comentar POR QUE
    o badge não usa `T.accent`: ele precisa renderizar idêntico pré-login (sem contexto
    de modo) e nos dois modos, e `T.accent` muda de teal para dourado entre eles.
    Quando fechado e `mercado.abertura` existir, mostrar a segunda linha "abre {HH:MM}";
    sem `abertura`, omitir — nunca compor um horário.

    No root do app, criar `const [mercado, setMercado] = useState(null)` junto dos outros
    estados de topo, e um `useEffect` de boot que chama `store.marketStatus()`,
    gravando o payload em caso de sucesso e `{ erro: true }` em caso de falha
    (`try/catch`, sem `flash`). Reconsultar em `visibilitychange`/`focus` do documento e
    num intervalo de baixa frequência (60s é suficiente: a granularidade da informação é
    de minutos), sempre limpando listener e timer no cleanup do `useEffect`.

    Expor `mercado` no objeto `ctx` (linha 7214) — o mesmo canal por onde
    `WelcomeAuthScreen` e o resto da árvore leem tudo. NÃO recalcular status em nenhum
    outro lugar: a tela de diagnóstico do Operador (linha 5052) continua lendo o
    `srv.pregaoAberto` dela, que é o status do SERVIDOR (inclui kill-switch e
    heartbeat), semanticamente diferente; deixar um comentário curto ali dizendo que os
    dois coexistem de propósito e qual é qual.

    Estender `web/tests/test_status_mercado_ui.mjs` com verificações por inspeção
    estática da fonte de `App.jsx` (técnica de primeira classe neste repositório —
    `App.jsx` não é importável fora de um build): `store.marketStatus(` aparece
    exatamente 1 vez; `MarketStatusBadge` está definido e a definição menciona
    `T.positive`, `T.negative` e `T.warn`; a definição NÃO menciona `T.accent`; existe
    limpeza de listener (`removeEventListener`) no efeito.
  </action>
  <verify>
    <automated>cd web && node tests/test_status_mercado_ui.mjs && node tests/test_brand_book_v2_tokens.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "store.marketStatus(" web/src/App.jsx` == 1.
    - `grep -n "function MarketStatusBadge" web/src/App.jsx` retorna 1 linha, posicionada
      antes de `function Topbar`.
    - No corpo de `MarketStatusBadge`: `T.positive`, `T.negative` e `T.warn` presentes e
      `T.accent` ausente (assertado pelo teste, com o recorte da função).
    - `grep -n "removeEventListener" web/src/App.jsx` cobre o efeito novo.
    - `cd web && npx vite build` sai 0.
  </acceptance_criteria>
  <done>
    Um estado, um componente, três estados visuais, nenhuma afirmação inventada durante
    o carregamento ou na falha.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render pré-login e pós-login + patrimônio com caixa reservado</name>
  <files>web/src/App.jsx, web/tests/test_status_mercado_ui.mjs</files>
  <read_first>
    - web/src/App.jsx (linhas 611-685 WelcomeAuthScreen; linhas 707-753 Topbar; linhas 1520-1540, 1625-1640, 3435-3450, 7165-7180, 7325-7370 — os 7 call sites de portfolioMetrics)
    - .planning/phases/02-realismo-de-mercado/02-UI-SPEC.md (placement e hierarquia visual: o badge é subordinado ao CTA "Entrar" e ao avatar do Topbar)
    - web/src/finance.js (assinatura nova de portfolioMetrics, do plano 02-04)
  </read_first>
  <behavior>
    - `WelcomeAuthScreen` renderiza `MarketStatusBadge` logo abaixo do parágrafo de
      disclaimer (linhas 644-646), acima do formulário — visível sem rolar.
    - O badge pré-login funciona SEM sessão (a consulta é a rota pública; nenhum caminho
      de `hasSession()` gateia a renderização).
    - `Topbar` renderiza o mesmo componente na linha de modo, após o divisor `·` já
      usado ali, e continua renderizando corretamente quando `modeChip`/`name` são vazios.
    - Todos os 7 call sites de `portfolioMetrics` passam `data.caixaReservado` como 4º
      argumento, de forma undefined-safe.
    - Com `caixaReservado > 0`, o patrimônio exibido no Topbar não cai — teste com
      números fechados sobre a função pura (`portfolioMetrics`), não sobre o DOM.
  </behavior>
  <action>
    Em `web/src/App.jsx`:
    (a) `WelcomeAuthScreen` (linha 611): renderizar `<MarketStatusBadge mercado={ctx.mercado}
    cp={ctx.cp} />` dentro do bloco de marca, logo após o `<p>` de disclaimer das linhas
    644-646, centralizado no mesmo eixo do parágrafo. Manter a tela self-contained e
    undefined-safe (o comentário da linha 610 é contrato: ela não acessa campos de
    `data`) — o badge só depende de `ctx.mercado` e `ctx.cp`, nunca de `ctx.data`.
    Hierarquia visual: o badge é secundário ao CTA "Entrar" (UI-SPEC Dimension 2) — não
    aumentar seu tamanho nem dar-lhe fundo sólido.
    (b) `Topbar` (linha 707): aceitar a prop nova `mercado` e renderizar o badge dentro
    do `div` da linha de modo (linhas 730-739), depois de `name`, precedido do MESMO
    divisor `·` (`<span style={{ color: T.textFaint, fontSize: "11px", flex: "none" }}>·</span>`)
    já usado ali — reusar a convenção, não inventar separador. Passar a prop no ponto
    onde `Topbar` é instanciado.
    (c) atualizar os 7 call sites de `portfolioMetrics` para passar
    `data.caixaReservado` (ou `(data && data.caixaReservado) || 0` onde `data` pode ser
    nulo no boot) como 4º argumento. Conferir cada um com
    `grep -n "portfolioMetrics(" web/src/App.jsx` antes e depois — a contagem tem que
    bater, e nenhum pode ficar com 3 argumentos.

    Estender `web/tests/test_status_mercado_ui.mjs` com: inspeção estática provando que
    `MarketStatusBadge` aparece dentro do corpo de `WelcomeAuthScreen` E dentro do corpo
    de `Topbar` (recortar cada função da fonte e procurar dentro); que o corpo de
    `WelcomeAuthScreen` não passou a referenciar `ctx.data`; que
    `grep`/regex de `portfolioMetrics(` não encontra nenhuma chamada com 3 argumentos; e
    um caso numérico com a função pura importada de `../src/finance.js` provando a
    conservação do patrimônio.
  </action>
  <verify>
    <automated>cd web && node tests/test_status_mercado_ui.mjs && node tests/test_finance.mjs && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MarketStatusBadge" web/src/App.jsx` >= 3 (definição + 2 renders).
    - Recorte de `function WelcomeAuthScreen` contém `MarketStatusBadge` e NÃO contém `ctx.data`.
    - Recorte de `function Topbar` contém `MarketStatusBadge`.
    - `grep -c "portfolioMetrics(" web/src/App.jsx` == 8 (1 import + 7 chamadas) e o teste
      assere que nenhuma chamada tem 3 argumentos.
    - `cd web && npx vite build` sai 0.
    - `bash scripts/executar.sh --testes` sai 0.
  </acceptance_criteria>
  <done>
    Status real do pregão visível antes e depois do login, com o patrimônio conservando
    o caixa reservado.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rede → tela pré-autenticação | primeira superfície renderizada, antes de qualquer sessão |
| payload de status → afirmação sobre o mercado | um dado ausente pode virar afirmação falsa na tela |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-23 | Spoofing | badge afirmando estado sem dado | mitigate | 3 estados explícitos; carregando não renderiza afirmação; falha vira `indisponível` com `T.warn` (CLAUDE.md princípio 4) |
| T-02-24 | Information Disclosure | tela de login lendo estado de conta | mitigate | `WelcomeAuthScreen` continua sem tocar `ctx.data`; badge depende só de `ctx.mercado` e `ctx.cp`; teste trava a ausência de `ctx.data` no recorte da função |
| T-02-25 | Tampering | status "cacheado eternamente" mostrando fechado no pregão | mitigate | reconsulta em `visibilitychange`/`focus` e a cada 60s, com cleanup de listener/timer |
| T-02-26 | Denial of Service | polling agressivo contra a rota pública | mitigate | 60s de intervalo e uma consulta por evento de foco; a rota é O(1) sem I/O (plano 02-02) |
| T-02-SC | Tampering | instalação de pacote npm/pip/cargo | accept | nenhuma dependência nova; shadcn deliberadamente NÃO inicializado (UI-SPEC, Registry Safety) |
</threat_model>

<verification>
- `for t in web/tests/*.mjs; do node "$t" >/dev/null || echo "FALHOU: $t"; done` não imprime nada.
- `cd web && npx vite build` sai 0 (obrigatório: front editado).
- `bash scripts/executar.sh --testes` sai 0.
</verification>

<success_criteria>
- Success Criterion 1 da Phase 2 satisfeito: usuário não-logado vê o status real do
  mercado, calculado por `pregao.py`.
- Um único estado alimenta os dois pontos de render.
- Nenhum estado inventado no carregamento ou na falha.
- Patrimônio conserva o caixa reservado nos 7 pontos de cálculo.
</success_criteria>

<output>
Create `.planning/phases/02-realismo-de-mercado/02-05-SUMMARY.md` when done
</output>
