---
phase: 07-sele-o-din-mica-por-desempenho-hist-rico-ledger-de-sinais-re
plan: 06
type: execute
wave: 3
depends_on: ["07-03", "07-04", "07-05"]
files_modified:
  - server/app/agent.py
  - server/app/main.py
  - docs/adr/017-revisao-de-setups-e-selecao-dinamica.md
  - server/tests/test_signal_ledger_scheduler.py
autonomous: false
requirements: [ADR17-B1, ADR17-B1-03, ADR17-B1-05]

must_haves:
  truths:
    - "O hook diário do ledger roda sozinho no mesmo laço do Radar diário, sem segundo scheduler"
    - "Uma falha do hook não derruba o heartbeat, o kill-switch nem o ciclo de stop/alvo dos usuários"
    - "O histórico medido chega ao Radar em produção — detect_setups tem provedor ligado no boot"
    - "O bootstrap foi rodado em produção e o Radar mostra elegibilidade real, verificada ao vivo"
  artifacts:
    - path: "server/app/agent.py"
      provides: "hook do ledger no scheduler_loop, com try/except próprio"
      contains: "signal_ledger_job"
    - path: "server/app/main.py"
      provides: "fiação do provedor de histórico no boot"
      contains: "set_historico_provider"
    - path: "docs/adr/017-revisao-de-setups-e-selecao-dinamica.md"
      provides: "adendo de implementação do Bloco 1 (o que foi construído e onde)"
      contains: "Adendo"
  key_links:
    - from: "server/app/agent.py:scheduler_loop"
      to: "signal_ledger_job.maybe_run"
      via: "await, no mesmo bloco de radar_daily/analysis_outcomes, em try/except próprio"
      pattern: "signal_ledger_job\\.maybe_run\\("
    - from: "server/app/main.py (boot)"
      to: "setups.set_historico_provider"
      via: "lambda que fecha sobre _conn e chama signal_ledger.historico_snapshot"
      pattern: "set_historico_provider\\("
---

<objective>
Ligar as pontas: pendurar o hook diário do ledger no `scheduler_loop` e ligar o provedor de
histórico no boot do servidor, para que o número medido chegue de fato ao Radar. Fechar com
verificação AO VIVO em produção — inclusive a execução do bootstrap.

Purpose: os Planos 01-05 construíram peças corretas e testadas que ainda não se falam. Este
plano é a fiação, e ela é a parte com maior risco de defeito só visível ao vivo: o histórico
do projeto registra três defeitos (sync device→servidor, orçamento sem flush, link Operador)
que passaram por toda a suíte e só apareceram na verificação real. Por isso o plano termina
num checkpoint humano, não num teste verde.

Output: `agent.py` com o hook, `main.py` com a fiação, adendo de implementação no ADR-017, e
a confirmação ao vivo de que o ledger existe em produção e o Radar consome a elegibilidade.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/07-sele-o-din-mica-por-desempenho-hist-rico-ledger-de-sinais-re/07-CONTEXT.md
@docs/adr/017-revisao-de-setups-e-selecao-dinamica.md
@docs/OPERACAO-ledger-de-sinais.md
@server/app/agent.py
@server/app/main.py

<interfaces>
<!-- Tudo já construído nos Planos 01-05; aqui só se consome -->

server/app/signal_ledger_job.py (Plano 04):
  async def maybe_run(conn) -> dict|None      # gate próprio, try/except próprio, to_thread
  LAST_RUN: dict                              # telemetria no formato de radar_daily.LAST_DAILY

server/app/signal_ledger.py (Plano 02):
  def historico_snapshot(conn, ttl_s=300, now=None) -> dict

server/app/setups.py (Plano 05):
  def set_historico_provider(fn) -> None      # fn: Callable[[], dict] | None

server/app/signal_ledger_bootstrap.py (Plano 03):
  # python -m app.signal_ledger_bootstrap --anos 15 --rng 15y
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pendurar o hook diário do ledger no scheduler_loop</name>
  <files>server/app/agent.py, server/tests/test_signal_ledger_scheduler.py</files>
  <read_first>
    - server/app/agent.py (linhas 1024-1100: assinatura e docstring de `scheduler_loop`, o
      heartbeat persistido, `_alertar_kill_switch`, o bloco de hooks de analytics e o bloco
      `if radar_fetch is not None and not kill_switch_on() and pregao.is_trading_day():` com
      `radar_daily.maybe_run`, `analysis_outcomes.maybe_run` e `fundamentals.maybe_warm` —
      é DENTRO desse bloco que o hook novo entra)
    - server/app/radar_daily.py (`maybe_run` — o contrato de "nunca levanta" que o hook novo
      também respeita)
    - server/app/signal_ledger_job.py (Plano 04 — `maybe_run`, `enabled`, `LAST_RUN`)
    - .planning/phases/07-.../07-CONTEXT.md (Claude's Discretion: "Onde exatamente no
      scheduler_loop pendurar o novo hook — perto de radar_daily/analysis_outcomes")
  </read_first>
  <behavior>
    - Uma passada de `scheduler_loop(..., once=True)` com `radar_fetch` fornecido e dia útil
      chama `signal_ledger_job.maybe_run` exatamente uma vez.
    - `signal_ledger_job.maybe_run` que levanta NÃO derruba a passada: o laço segue e as
      demais etapas continuam executando.
    - Com o kill-switch do agente LIGADO, o hook não é chamado (está dentro do mesmo bloco
      guardado que `radar_daily`).
    - Em dia não útil (`pregao.is_trading_day()` falso), o hook não é chamado.
    - Sem `radar_fetch` (suítes/uso avulso), o hook não é chamado.
    - O hook é chamado DEPOIS de `radar_daily.maybe_run` na mesma passada (ordem verificável
      por lista de chamadas registradas em um spy).
  </behavior>
  <action>
    Em `server/app/agent.py`, dentro de `scheduler_loop`, no bloco
    `if radar_fetch is not None and not kill_switch_on() and pregao.is_trading_day():`,
    acrescente o hook novo DEPOIS da chamada a `fundamentals.maybe_warm(...)` — última etapa
    do bloco. A ordem importa e é a razão de estar aí: o Radar diário (08:45) já encheu o
    `candle_cache` com os candles do dia, e o hook do ledger (09:15) lê exatamente esse cache
    via `candle_cache.peek`, sem gastar requisição (ADR-008).

    Use `from . import signal_ledger_job` como import LOCAL dentro da função, no padrão dos
    outros hooks deste laço (`radar_daily`, `analysis_outcomes`, `fundamentals`, `pregao`) —
    a razão declarada no arquivo é evitar ciclo de import.

    Envolva a chamada em `try/except Exception` PRÓPRIO, com `print("[ledger-diario] ...")` —
    exatamente como os demais hooks do laço. `signal_ledger_job.maybe_run` já tem o seu
    `try/except` interno; este é o segundo cinto, pelo mesmo motivo que os outros hooks têm o
    deles: nenhuma falha aqui pode chegar ao heartbeat, ao kill-switch ou ao ciclo de
    stop/alvo dos usuários.

    Atualize a docstring de `scheduler_loop` acrescentando uma linha no mesmo estilo das
    existentes ("FASE 4 (1.3)", "qa/30 (Fase A)", "qa/47"): "ADR-017 (Bloco 1): o mesmo laço
    também avança o ledger de sinais resolvidos — hook próprio, gate próprio
    (`B3_LEDGER_DAILY_HHMM`, default 09:15), lendo do `candle_cache` sem custo de rede."

    Crie `server/tests/test_signal_ledger_scheduler.py` cobrindo `<behavior>`, no estilo dos
    testes existentes de `scheduler_loop` (procure em `server/tests/` por testes que já usam
    `once=True` e monkeypatch dos hooks; reutilize a mesma montagem). O teste de ORDEM é o
    mais importante: registre as chamadas numa lista e assere que o índice de `radar_daily` é
    menor que o de `signal_ledger_job`.
  </action>
  <verify>
    <automated>bash scripts/test.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "signal_ledger_job" server/app/agent.py` retorna ≥ 2 linhas (import local + chamada).
    - `grep -n "await signal_ledger_job.maybe_run(conn)" server/app/agent.py` retorna 1 linha.
    - `grep -n "\[ledger-diario\]" server/app/agent.py` retorna ≥ 1 linha — o `except` próprio existe e loga com prefixo identificável.
    - A chamada está DENTRO do bloco guardado por `radar_fetch is not None and not kill_switch_on() and pregao.is_trading_day()` e DEPOIS de `fundamentals.maybe_warm` (verificável lendo o trecho).
    - `grep -n "ADR-017" server/app/agent.py` retorna ≥ 1 linha na docstring de `scheduler_loop`.
    - Existe teste que prova a ordem `radar_daily` → `signal_ledger_job` na mesma passada.
    - Existe teste que prova que uma exceção do hook não interrompe a passada.
    - `bash scripts/test.sh` verde; nenhum teste existente de agente/scheduler quebrado.
  </acceptance_criteria>
  <done>O ledger avança sozinho todo dia útil, no laço que já existe, sem segundo scheduler e sem poder derrubar o agente.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ligar o provedor de histórico no boot e registrar o adendo no ADR-017</name>
  <files>server/app/main.py, docs/adr/017-revisao-de-setups-e-selecao-dinamica.md, server/tests/test_signal_ledger_scheduler.py</files>
  <read_first>
    - server/app/main.py (linhas 48-70: o bloco de `configure_db(_conn)` — `candle_cache`,
      `brapi_budget`, `agent_mod`, `timing_watch` — que é onde a fiação de boot vive, cada
      uma com o comentário explicando o que quebraria sem ela)
    - server/app/setups.py (Plano 05 — `set_historico_provider`)
    - server/app/signal_ledger.py (Plano 02 — `historico_snapshot`, `TTL_HISTORICO_S`)
    - server/app/technical_snapshot.py (linha ~133: o caminho por onde `detect_setups` é
      chamado em produção, para entender o que a fiação habilita de fato)
    - docs/adr/016-qualidade-do-sinal-do-motor-de-setups.md (o formato dos "Adendos" — é o
      padrão de registro incremental de ADR usado neste repositório)
  </read_first>
  <behavior>
    - Após o boot, `setups._HISTORICO_PROVIDER` não é `None`.
    - Com o kv de agregação populado, um `detect_setups` disparado pelo caminho real
      (`technical_snapshot.build`) devolve setups com a chave `historico` preenchida para os
      nomes presentes no kv.
    - Com o kv VAZIO (instalação nova, bootstrap ainda não rodado), o mesmo caminho devolve
      setups com `historico=None` e nenhuma exceção — a tela funciona antes do bootstrap.
    - `regime.ranquear` sobre resultados desse caminho anexa `setupElegivel=None` quando não
      há janela calculada, e o `radarScore` não é penalizado.
  </behavior>
  <action>
    (a) Em `server/app/main.py`, no bloco de fiação de boot (logo após
    `timing_watch.configure_db(_conn)`, linha ~66), acrescente:
    `setups.set_historico_provider(lambda: signal_ledger.historico_snapshot(_conn))`,
    importando `signal_ledger` junto dos demais imports de módulo do topo (e `setups`, se
    ainda não estiver importado no escopo do topo — ele já é usado em `main.py:1313`).

    Escreva o comentário-decisão no mesmo estilo dos vizinhos, dizendo o que quebraria sem a
    linha: sem ela, `detect_setups` nunca anexa `historico` e `regime.ranquear` nunca vê
    `elegivel` — todo o ledger existiria sem consequência nenhuma na tela. E por que é uma
    lambda e não um import direto dentro de `setups.py`: manter `setups.py` puro (sem I/O,
    sem banco), que é o que permite a suíte inteira rodar sem banco e o que impede I/O no
    caminho síncrono quente do STU.

    (b) Acrescente ao fim de `docs/adr/017-revisao-de-setups-e-selecao-dinamica.md` uma seção
    `## Adendo 1 — implementação do Bloco 1 (2026-08-21)` registrando, em tabela curta:
    o módulo/arquivo entregue para cada item da Decisão 2 (ledger, bootstrap, hook diário,
    guard do Yahoo, `detect_setups`, `regime.ranquear`), e três decisões de implementação que
    o ADR não previa e que precisam ficar registradas:

      1. **Ledger write-on-resolution** — não existe linha "pendente": `sinais_do_ticker` só
         avalia barras com horizonte cheio à frente, então o cursor anda 10 barras atrás da
         última barra fechada e todo sinal nasce resolvido. `status` distingue `resolvido` de
         `sem_gatilho`, não "pendente vs. resolvido".
      2. **Cursor derivado do ledger** — `MAX(data_sinal)` por ticker, não uma chave `kv`
         paralela: não pode divergir do dado nem precisar de reconciliação após `--reset`.
      3. **Bootstrap em `server/app/`, não em `scripts/`** — o Railway publica com
         `rootDirectory=/server`; um comando em `scripts/` seria inexecutável em produção.

    Registre também os pesos escolhidos em `regime.ranquear` (`+10` elegível / `-10`
    inelegível / `0` insuficiente, contra `+5` do `gatilhoAlinhado`) e a posição do termo na
    tupla de ordenação (entre momentum e gatilho), com a justificativa: a evidência medida
    desempata acima do timing e abaixo do eixo de regime/momentum que o ADR-016 validou.

    NÃO altere nenhuma linha já existente do ADR-017 — o texto da época se preserva; adendo é
    acréscimo.

    (c) Acrescente a `server/tests/test_signal_ledger_scheduler.py` os casos de `<behavior>`
    desta task, exercitando o caminho REAL (`technical_snapshot.build` → `detect_setups`) com
    o provedor ligado por `setups.set_historico_provider`, e o caso de kv vazio.
  </action>
  <verify>
    <automated>bash scripts/test.sh</automated>
    <automated>cd server && python3 -c "import ast; ast.parse(open('app/main.py').read()); print('main.py compila')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "set_historico_provider" server/app/main.py` retorna 1 linha, dentro do bloco de fiação de boot (antes de qualquer definição de rota).
    - `grep -n "historico_snapshot(_conn)" server/app/main.py` retorna 1 linha.
    - `grep -n "^## Adendo" docs/adr/017-revisao-de-setups-e-selecao-dinamica.md` retorna 1 linha.
    - `git diff docs/adr/017-revisao-de-setups-e-selecao-dinamica.md | grep -c "^-"` == 0 — nada foi removido do ADR, só acrescentado.
    - O adendo cita `signal_replay.py`, `signal_ledger.py`, `signal_ledger_job.py`, `signal_ledger_bootstrap.py`, `yahoo.confere_granularidade`, `setups.set_historico_provider` e `regime.W_HISTORICO_ELEGIVEL`.
    - Existe teste que assere que, com kv vazio, o caminho real devolve `historico=None` sem levantar.
    - `bash scripts/test.sh` verde.
  </acceptance_criteria>
  <done>O histórico medido chega ao Radar em produção, o ADR registra o que foi construído e as três decisões que ele não previa.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação ao vivo — bootstrap em produção e Radar consumindo elegibilidade</name>
  <read_first>
    - docs/OPERACAO-ledger-de-sinais.md (Plano 03 — os comandos exatos, local e Railway)
    - server/app/signal_ledger_bootstrap.py (os argumentos reais da CLI e o resumo impresso)
    - server/app/signal_ledger_job.py (as chaves kv `signalLedgerLastRun` e `signalLedgerJanelaFechada`)
    - server/railway.json (o host/serviço real, para preencher os comandos apresentados ao Alex)
  </read_first>
  <action>
    NÃO execute o bootstrap em produção por conta própria — esta task é um portão bloqueante.

    Faça o seguinte e pare:
    1. Rode `bash scripts/executar.sh --testes` e confirme que as DUAS suítes estão verdes.
    2. Rode `B3_TESTE_REDE=1 bash scripts/test.sh` e confirme o guard do Yahoo contra o
       endpoint real.
    3. Monte a mensagem do checkpoint para o Alex reproduzindo LITERALMENTE os 7 passos do
       bloco `<how-to-verify>` abaixo, com o host real do serviço preenchido no lugar de
       `<host>` (leia de `server/railway.json` ou pergunte).
    4. Aguarde o sinal de retomada.

    Se o Alex responder com divergência, trate como defeito a corrigir NESTA fase (não como
    escopo novo): identifique se a causa é a fiação (Tasks 1/2 deste plano) ou uma das peças
    (Planos 01-05), corrija e registre no SUMMARY.
  </action>
  <acceptance_criteria>
    - `bash scripts/executar.sh --testes` exit 0 antes de apresentar o checkpoint.
    - `B3_TESTE_REDE=1 bash scripts/test.sh` exit 0 antes de apresentar o checkpoint.
    - A mensagem apresentada ao Alex contém os 7 passos numerados com os comandos concretos e o host real (nenhum placeholder `<host>` restante).
    - Nenhum comando de produção (`railway ssh`, `atualizar.sh`, bootstrap) foi executado pelo agente.
    - A execução só prossegue após sinal explícito de retomada.
  </acceptance_criteria>
  <what-built>
    Toda a cadeia do Bloco 1 do ADR-017, automatizada e com suíte verde:
    - `server/app/signal_replay.py` — fonte única do replay determinístico (barreira tripla).
    - `yahoo.confere_granularidade` — guard cobrindo diário/semanal/mensal, comprovado contra
      o endpoint real do Yahoo (Plano 01, teste `B3_TESTE_REDE=1`).
    - Tabela `signal_ledger` no banco principal, idempotente, com as duas agregações.
    - `python -m app.signal_ledger_bootstrap` — comando manual, executável dentro do container.
    - Hook diário no `scheduler_loop` (09:15 BRT), lendo do `candle_cache` sem gastar
      requisição de brapi.
    - `detect_setups` anexando `historico` e `regime.ranquear` usando `elegivel` na ordenação.

    O que Claude NÃO pode fazer sozinho: rodar o bootstrap no banco de PRODUÇÃO (operação
    pesada, destrutiva-por-flag, no ambiente do Alex) e confirmar o resultado no app real.
  </what-built>
  <how-to-verify>
    1. **Deploy** — `bash scripts/atualizar.sh --somente-deploy` (deploy só-backend: bumpe
       `SERVER_BUILD_ID` à mão antes, conforme o guardrail do repositório).

    2. **Confirme que o servidor subiu** — `curl -s https://<host>/api/health` responde 200.

    3. **Rode o bootstrap** (segue `docs/OPERACAO-ledger-de-sinais.md`):
       `railway ssh` e, dentro do container:
       `/opt/venv/bin/python3 -m app.signal_ledger_bootstrap --anos 15 --rng 15y`
       Esperado: progresso por ticker em stderr e, ao final, um resumo em stdout com o total
       de linhas no ledger (ordem de grandeza esperada: ~126k, conforme ADR-016), quantos
       setups têm n≥40 na janela e o `medidoAte`.

    4. **Confirme o ledger** — ainda no `railway ssh`:
       `/opt/venv/bin/python3 -c "from app import db, signal_ledger as sl; c=db.connect(); print(sl.contar(c)); print(db.kv_get(c,'signalLedger:janela',user_id=None))"`
       Esperado: contagem > 0 e um payload com `janelaRef`, `minN: 40` e `porSetup` com pelo
       menos um setup marcado `elegivel: true` ou `false` (não só `null`).

    5. **Confirme no app** — abra o Radar (web e iPhone). Esperado:
       - a tela carrega normalmente (nenhuma regressão visual — o Bloco 3 ainda não expõe o
         histórico, então NÃO deve aparecer nada novo na UI);
       - a ordem do Radar pode ter mudado em relação a antes do bootstrap — isso é o efeito
         pretendido;
       - nenhum ativo sumiu da lista.

    6. **Confirme o hook diário** — no dia seguinte (dia útil, após 09:15 BRT), verifique em
       Perfil → Observabilidade (ou `railway logs`) que não há erro com prefixo
       `[ledger-diario]`, e que `signalLedgerLastRun` no kv tem a data de hoje:
       `/opt/venv/bin/python3 -c "from app import db; c=db.connect(); print(db.kv_get(c,'signalLedgerLastRun',user_id=None))"`

    7. **Confirme o orçamento da brapi** — em Perfil → Observabilidade, o consumo do dia NÃO
       deve ter salto atribuível ao ledger (o hook lê do cache; o bootstrap usa Yahoo).
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva o que divergiu (passo, comando, saída obtida vs. esperada)</resume-signal>
  <done>O Alex confirmou ao vivo: ledger populado em produção, janela com elegibilidade real (não só `null`), Radar carregando sem regressão e sem nenhum ativo sumido, hook diário sem erro `[ledger-diario]` e sem salto no consumo da brapi.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hooks novos → `scheduler_loop` | O laço único que sustenta heartbeat, kill-switch e stop/alvo de toda a base |
| Boot do servidor → caminho quente por request | Uma fiação errada no boot afeta todas as rotas que passam pelo STU |
| Operador humano → banco de produção | O bootstrap escreve ~126k linhas no banco vivo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-23 | Denial of Service | `scheduler_loop` | mitigate | `try/except` próprio em torno do hook + `try/except` interno em `maybe_run` + `asyncio.to_thread` para o trabalho pesado; teste que prova que a exceção não interrompe a passada |
| T-07-24 | Denial of Service | boot do FastAPI | mitigate | A fiação é uma lambda que só executa sob demanda; `historico_snapshot` já degrada para `{}` em falha de banco, então um banco frio no boot não impede o servidor de subir |
| T-07-25 | Repudiation | verificação só por teste | mitigate | Checkpoint humano obrigatório com passos numerados e saída esperada — o repositório tem três precedentes de defeito que passaram na suíte e só apareceram ao vivo |
| T-07-26 | Denial of Service | orçamento brapi (ADR-008) | mitigate | Passo 7 da verificação confere o consumo do dia na Observabilidade; o hook usa `peek` e o bootstrap usa Yahoo |
| T-07-27 | Tampering | bootstrap no banco vivo | accept | Operação idempotente por UNIQUE; `--reset` exige flag explícita; o runbook documenta o efeito. Executada pelo dono do produto, com confirmação explícita |

Sem instalação de pacote npm/pip/cargo neste plano — o gate de legitimidade de pacote não se aplica.
</threat_model>

<verification>
1. `bash scripts/executar.sh --testes` — as DUAS suítes verdes (gate canônico do repositório).
2. `grep -rn "signal_ledger_job\|signal_ledger_bootstrap" server/app/agent.py server/app/main.py` — o job está no laço; o bootstrap NÃO está em lugar nenhum automático.
3. `git status --porcelain web/ web-admin/` retorna vazio — fase backend pura, sem risco de quebrar a paridade `deviceStore`/`serverStore` nem de exigir rebuild do front.
4. Checkpoint da Task 3 aprovado pelo Alex.
</verification>

<success_criteria>
- `signal_ledger_job.maybe_run` é chamado uma vez por passada útil do `scheduler_loop`, depois de `radar_daily`, dentro do mesmo bloco guardado, com `try/except` próprio.
- `setups.set_historico_provider` está ligado no boot e o caminho real (`technical_snapshot.build` → `detect_setups`) entrega `historico` quando há dado e `None` quando não há, sem levantar.
- O ADR-017 ganhou o adendo de implementação, sem nenhuma linha original removida.
- O bootstrap foi executado em produção, o ledger tem linhas, a janela tem elegibilidade real e o Radar carrega sem regressão — verificado pelo Alex.
</success_criteria>

<output>
Create `.planning/phases/07-sele-o-din-mica-por-desempenho-hist-rico-ledger-de-sinais-re/07-06-SUMMARY.md` when done
</output>
