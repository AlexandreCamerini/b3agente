---
phase: 11-ciclo-de-vida-e-monitoramento
plan: 03
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - server/tests/test_put_lifecycle_sem_carteira.py
  - docs/adr/022-ciclo-de-vida-da-sugestao-de-put.md
  - docs/OPERACAO-ciclo-de-vida-put.md
autonomous: true
requirements: [PUTLIFE-01, PUTLIFE-02, PUTLIFE-03, PUTLIFE-04]

must_haves:
  truths:
    - "Um ciclo de vida completo (armada→executada→monitorada→fechada) deixa optionPositions, cash e history byte-idênticos — provado por comportamento, não por afirmação"
    - "Nenhum módulo desta fase alcança as funções de compra/venda de opção do motor, nem por import nem por menção"
    - "Nenhuma sugestão fica em limbo: toda linha ou está num estado terminal, ou avançou hoje, ou tem a data da pendência gravada"
    - "Nenhuma superfície visível ao usuário foi introduzida — o guardião da Fase 10 continua verde e ganha cobertura do módulo novo"
    - "A decisão de manter a simulação fora da carteira real fica registrada num ADR permanente, que sobrevive ao diretório .planning/"
  artifacts:
    - path: "server/tests/test_put_lifecycle_sem_carteira.py"
      provides: "guardião permanente: carteira intocada, ausência de superfície, anti-limbo"
      min_lines: 120
    - path: "docs/adr/022-ciclo-de-vida-da-sugestao-de-put.md"
      provides: "registro permanente da resolução da tensão de desenho e das 5 transições"
      contains: "PUTLIFE-02"
    - path: "docs/OPERACAO-ciclo-de-vida-put.md"
      provides: "runbook: quando roda, env vars, por que fecha em 0 hoje, query de inspeção"
  key_links:
    - from: "server/tests/test_put_lifecycle_sem_carteira.py"
      to: "server/app/store.py (optionPositions/cash/history)"
      via: "asserção de igualdade antes/depois do ciclo completo"
      pattern: "optionPositions"
    - from: "docs/adr/022-ciclo-de-vida-da-sugestao-de-put.md"
      to: "docs/adr/021-ponte-gatilho-put.md"
      via: "continuidade da decisão de onde a sugestão mora"
      pattern: "ADR-021"
---

<objective>
Provar — por comportamento e por leitura de fonte, não por afirmação — que o
ciclo de vida inteiro roda sem tocar um byte da carteira do usuário e sem
criar nenhuma superfície visível; e registrar permanentemente, fora do
`.planning/`, a decisão de desenho que sustenta isso.

Purpose: fechar os 4 requisitos da fase com evidência automatizada que
sobrevive ao próximo merge (mesma postura do guardião de PUT-03 da Fase 10) e
deixar o Alex com um ADR + runbook legíveis de manhã, sem precisar
reconstruir o raciocínio a partir de plano de execução.

Output: `test_put_lifecycle_sem_carteira.py`, ADR-022,
`docs/OPERACAO-ciclo-de-vida-put.md`, suíte canônica fechada 2x.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/notes/decisoes-autonomas-v1.2.md
@.planning/phases/11-ciclo-de-vida-e-monitoramento/11-01-PLAN.md
@.planning/phases/11-ciclo-de-vida-e-monitoramento/11-01-SUMMARY.md
@.planning/phases/11-ciclo-de-vida-e-monitoramento/11-02-SUMMARY.md
@docs/adr/021-ponte-gatilho-put.md
@docs/OPERACAO-ponte-gatilho-put.md
@server/tests/test_put_bridge_sem_superficie.py

<interfaces>
<!-- Contratos que o executor consome. Não explore o codebase atrás deles. -->

server/app/store.py (SÓ LEITURA nesta fase — nunca chamar as funções de escrita):
  SECTIONS = [… "cash", "positions", "history", … "optionPositions", "pendingOrders"]
  def get(conn, key, user_id=None)     # leitura de seção — permitido nos testes

server/app/put_lifecycle.py (Planos 11-01/11-02):
  forma_adr003, resolver_spots, intrinseco, decidir, MOTIVO_VENCIMENTO,
  run_diario(conn, now=None) -> dict, async maybe_run(conn)

server/app/put_suggestions.py (Plano 11-01):
  ESTADOS, ESTADOS_ROTULO, TERMINAIS, TRANSICOES, COLUNAS_CICLO,
  registrar, listar, listar_abertas, transicionar, registrar_pendencia, contar

server/tests/test_put_bridge_sem_superficie.py (Fase 10 — MOLDE a estender):
  _sem_comentarios(texto)   # filtra linhas iniciadas por # antes do grep (D-10-N)
  8 testes: leitura de fonte (main.py, skill_ref.py, todos os .js/.jsx de
  web/src e web-admin/src, defaults.py, catalog.js) + 3 de comportamento
  (status_snapshot sem chave "put"; nenhuma rota de app.main.app.routes com
  "put" no path; agregações do ADR-017 com porSetup == {} depois de gravar
  sugestões).
</interfaces>
</context>

<assumptions>

## A-11-10: o guardião é de COMPORTAMENTO, não de diff

O guardião da Fase 10 (`test_put_bridge_sem_superficie.py`) já estabeleceu a
regra (D-10-M): um teste que LÊ O FONTE e exercita COMPORTAMENTO sobrevive ao
merge seguinte; um teste que inspeciona `git diff` morre no primeiro merge.
Este plano segue a mesma postura, com uma prova a mais que a Fase 10 não
precisava: rodar o ciclo de vida COMPLETO e comparar a carteira antes/depois.

## A-11-11: o teste de carteira intocada compara valor, não ausência de chamada

Mockar `store.buy_option` e assertar "não foi chamado" prova menos: qualquer
caminho novo que escrevesse direto em `db.kv_set(conn, "optionPositions", …)`
passaria. A asserção correta é sobre o ESTADO: ler as três seções
(`optionPositions`, `cash`, `history`) antes e depois de um ciclo completo e
exigir igualdade estrutural. Isso pega qualquer via de escrita, conhecida ou
não.

## A-11-12: por que este plano não muda nenhum arquivo de produção

Task 1 é só teste; Task 2 é só documentação. `git diff --stat` sobre
`server/app/` ao fim deste plano deve ser VAZIO — se não for, algo saiu do
escopo e vira PARADA DURA documentada, não correção silenciosa.
</assumptions>

<guardrails_absolutos>
Idênticos aos planos 11-01/11-02 — repetidos porque valem por task:

- **Nenhum `git push`, nenhum deploy, nenhuma mudança de env var de produção.**
- **`B3_OPTIONS_PROVIDER` nunca é lido nem alterado.**
- **Nenhuma superfície visível**: não tocar `web/`, `web-admin/`,
  `server/app/skill_ref.py`, `server/app/main.py`, `server/app/defaults.py`.
- **Nenhum suporte a opção vendida/short em qualquer forma.**
- **Nunca chamar `store.buy_option`/`sell_option`/`close_option_vencida`/
  `set_option_position`** — nem nos testes, exceto no ponto explicitamente
  autorizado do teste de contraprova (RED) descrito na Task 1, que é revertido
  em seguida.
- **Nenhum arquivo de `server/app/` é editado neste plano.**

**PARADA DURA por item:** teste que não fecha em 2 tentativas → pare NAQUELE
item, documente na SUMMARY, siga para o próximo.

**Base de diff (D-EXEC-10-03-01):** ⚠ não use `origin/main`. Para os diffs de
FASE, use o tip da Fase 10:
`FASE10=$(git log --format=%H --grep='docs(phase-10): evolve PROJECT.md' -1)`
(no clone principal isso resolve para `6e79363`). Se o comando não achar nada
(worktree resetado), caia para `FASE10=6e79363` e registre isso na SUMMARY.

**Armadilha de docstring (D-EXEC-10-01-01/02):** `grep -v '^#'` não filtra
docstring. O ARQUIVO DE TESTE deste plano PRECISA conter os literais
proibidos (é ele que faz o grep sobre os módulos de produção) — isso é
esperado e correto. Os guardiões de grep desta fase apontam para
`server/app/put_lifecycle.py` e `server/app/put_suggestions.py`, **nunca**
para o próprio arquivo de teste.

**Toda decisão autônoma vai para `.planning/notes/decisoes-autonomas-v1.2.md`**
no formato D-EXEC-11-03-NN.
</guardrails_absolutos>

<tasks>

<task type="auto">
  <name>Task 1: Guardião permanente — carteira intocada, ausência de superfície, anti-limbo</name>

  <files>
    server/tests/test_put_lifecycle_sem_carteira.py
  </files>

  <read_first>
    - server/tests/test_put_bridge_sem_superficie.py inteiro (o molde: helper
      `_sem_comentarios`, mistura de leitura de fonte + comportamento,
      docstring de cabeçalho explicando por que apagar o arquivo é reverter um
      requisito, não limpar teste morto)
    - server/app/store.py linhas 1-30 (`SECTIONS`, `get`) e 705-815
      (as 4 funções de escrita de opção — para saber exatamente quais nomes o
      guardião de grep deve proibir)
    - server/app/put_lifecycle.py e server/app/put_suggestions.py como
      ficaram nos Planos 11-01/11-02
    - .planning/phases/11-ciclo-de-vida-e-monitoramento/11-01-PLAN.md seção
      `<assumptions>` A-11-01 (o raciocínio que este guardião materializa)
  </read_first>

  <action>
Crie `server/tests/test_put_lifecycle_sem_carteira.py` com docstring de
cabeçalho no formato do guardião da Fase 10: explique em 5-8 linhas que este
arquivo materializa a decisão A-11-01 (a simulação do ciclo de vida vive nas
colunas da tabela de sugestões, nunca na carteira do usuário) e que
afrouxá-lo/apagá-lo é reverter PUTLIFE-02/PUT-03, não limpar teste morto.

Implemente 3 blocos:

**Bloco A — leitura de fonte (estrutural).** Reuse `_sem_comentarios` (copie
o helper do guardião da Fase 10; não importe entre arquivos de teste se esse
não for o padrão local):
  A1. `put_lifecycle.py` e `put_suggestions.py` não contêm nenhum de
      `buy_option`, `sell_option`, `close_option_vencida`,
      `set_option_position`, `optionPositions` — **em nenhuma linha, nem em
      docstring** (comentários filtrados, o resto conta).
  A2. Nenhum dos dois importa `store` (`from .store`, `from . import store`,
      `import store`).
  A3. Nenhum dos dois menciona `web/`, `skill_ref`, `defaults`, ou qualquer
      chave de vocabulário visível — extensão direta do guardião de PUT-03
      para os módulos novos.
  A4. `put_lifecycle.py` não menciona nenhuma fonte de rede
      (`options_provider`, `mydata_client`, `candle_provider`, `httpx`) nem
      `B3_OPTIONS_PROVIDER`.
  A5. `agent.py` não contém a string `putLifecycle` (nenhuma telemetria no
      snapshot de status).

**Bloco B — comportamento (a prova que importa).**
  B1. **Carteira intocada num ciclo COMPLETO.** Monte um banco temporário com
      um usuário que tem carteira de verdade: `cash`, `positions` com 1
      posição, `history` com 2 entradas e `optionPositions` com 1 posição de
      opção pré-existente (grave as seções via `db.kv_set`, sem chamar as
      funções de escrita de opção). Registre 1 sugestão. Rode `run_diario`
      quatro vezes, avançando `now`/o cache de candles a cada rodada, até a
      linha chegar a `fechada`. Assere:
        - a linha percorreu `armada → executada_simulada → monitorada → fechada`
        - `store.get(conn, "optionPositions", user_id=uid)`,
          `store.get(conn, "cash", …)` e `store.get(conn, "history", …)` são
          **iguais** (`==`) aos valores capturados ANTES da primeira rodada
        - o `json.dumps(..., sort_keys=True)` das três seções também bate
          (defesa contra igualdade frouxa por mutação in-place)
  B2. **Mesma prova no ramo sem execução:** sugestão sem `premio` que vence →
      `expirada_sem_uso`, carteira igualmente intocada.
  B3. **Anti-limbo.** Depois de uma rodada sobre um conjunto misto (uma linha
      com candle, uma sem candle, uma já terminal), TODA linha de
      `put_suggestions.listar(conn)` satisfaz: estado em `TERMINAIS`, **ou**
      `estadoEm` == data da rodada (avançou hoje), **ou** `pendenteDesde` não
      é `None`. Nenhuma linha escapa das três condições.
  B4. **Estado inválido é irrepresentável pela API.** Para cada par
      (origem, destino) do produto cartesiano `ESTADOS × ESTADOS` que NÃO
      está em `TRANSICOES[origem]`, `transicionar` devolve 0 e o estado no
      banco não muda. Mais: `transicionar(..., "estado_que_nao_existe")`
      devolve 0.
  B5. **Nenhuma rota nova.** Nenhum path de `app.main.app.routes` contém
      `lifecycle`, `ciclo`, nem qualquer novo path com `put` além dos que já
      existiam (reuse a asserção do guardião da Fase 10).
  B6. **Agregações do ADR-017 seguem cegas.** Depois de um ciclo completo,
      `signal_ledger.agregar_cumulativo(conn)["porSetup"] == {}` e
      `agregar_janela` idem — o ranking visível do Radar não enxerga nada
      disto (mesma asserção da Fase 10, agora com o ciclo de vida rodando).

**Bloco C — prova de RED (execute, documente na SUMMARY, reverta).**
Acrescente TEMPORARIAMENTE, ao fim de `server/app/put_lifecycle.py`, uma
linha de CÓDIGO sentinela (não comentário — a filtragem de comentário
mascararia a prova), por exemplo `_SENTINELA_RED = "optionPositions"`.
Rode a suíte deste arquivo e confirme que A1 FALHA. Depois:
`git checkout -- server/app/put_lifecycle.py`, confirme
`git diff --stat -- server/app/put_lifecycle.py` VAZIO e a suíte de volta a
100%. Cole as duas saídas na SUMMARY. (Mesmo protocolo que o Plano 10-03
usou com `skill_ref.py`.)

Commit único: `test(11-03): guardião do ciclo de vida — carteira intocada,
sem superfície, sem limbo`.
  </action>

  <verify>
    <automated>cd server &amp;&amp; ./.venv/bin/python -m pytest tests/test_put_lifecycle_sem_carteira.py tests/test_put_bridge_sem_superficie.py -q</automated>
  </verify>

  <acceptance_criteria>
    `PY` = `/Users/acamerini/dev/bolsia/b3-agente/server/.venv/bin/python`.

    1. `cd server && $PY -m pytest tests/test_put_lifecycle_sem_carteira.py -q`
       → exit 0, **≥ 12 testes**, nenhum `skip` para `put_lifecycle.py`,
       `put_suggestions.py` ou `agent.py`.
    2. `cd server && $PY -m pytest tests/test_put_bridge_sem_superficie.py -q`
       → exit 0, 8 passed (guardião da Fase 10 sem regressão).
    3. Prova de RED executada e colada na SUMMARY: com o sentinela, ≥ 1 teste
       FALHA; após `git checkout --`,
       `git diff --stat -- server/app/put_lifecycle.py` **vazio** e a suíte
       do arquivo volta a exit 0.
    4. `git diff --stat "$FASE10" -- server/app/store.py` → **saída vazia**
       (a carteira real não recebeu uma linha em toda a Fase 11).
    5. `git diff --stat "$FASE10" -- web/ web-admin/ server/app/skill_ref.py server/app/main.py server/app/defaults.py`
       → **saída vazia**.
    6. `git diff --stat -- server/app/` ao fim desta task → **saída vazia**
       (esta task só cria arquivo de teste).
    7. `cd server && $PY -m pytest -q` → exit 0, sem regressão.
  </acceptance_criteria>

  <done>
    Existe prova automatizada e permanente de que um ciclo de vida completo
    deixa `optionPositions`/`cash`/`history` idênticos, de que nenhum estado
    inválido é gravável pela API, de que nenhuma linha fica em limbo
    silencioso, e de que nenhuma superfície nova apareceu — com a prova de RED
    documentada.
  </done>
</task>

<task type="auto">
  <name>Task 2: ADR-022 + runbook de operação + fechamento da suíte canônica</name>

  <files>
    docs/adr/022-ciclo-de-vida-da-sugestao-de-put.md
    docs/OPERACAO-ciclo-de-vida-put.md
  </files>

  <read_first>
    - docs/adr/021-ponte-gatilho-put.md inteiro (formato exato: Status/Data/
      Decisor/Base/Contexto/N Decisões/Consequências com "Fica mais fácil" /
      "Fica mais difícil" / "A revisitar"; e o item (a) de "A revisitar", que
      esta fase agora pode responder parcialmente)
    - docs/OPERACAO-ponte-gatilho-put.md inteiro (formato do runbook: 6 seções
      numeradas, incluindo "por que fecha em 0 em produção hoje")
    - .planning/phases/11-ciclo-de-vida-e-monitoramento/11-01-PLAN.md
      `<assumptions>` (A-11-01..A-11-05) e 11-02-PLAN.md (A-11-06..A-11-09) —
      é este raciocínio que vira ADR permanente
    - as duas SUMMARYs desta fase (11-01, 11-02) — para citar os números reais
      (contagem de testes, comportamento provado), nunca números inventados
  </read_first>

  <action>
**1. `docs/adr/022-ciclo-de-vida-da-sugestao-de-put.md`** — formato do
ADR-021, com 4 decisões:

  - **Contexto**: a Fase 10 deixou a sugestão em `armada` sem ciclo de vida;
    o ROADMAP pedia reuso de `optionPositions` e dos ADR-003/004/005; as duas
    decisões de arquitetura travadas do milestone (EOD ponta a ponta; só put
    comprada long-only) continuam valendo e não são reabertas.
  - **Decisão 1 — a simulação vive nas colunas da própria tabela de sugestões,
    não na carteira do usuário.** Este é o coração do ADR. Registre as DUAS
    evidências duras, com caminho e linha: (a) as três seções da carteira
    estão em `store.SECTIONS` e portanto são superfície visível — uma
    "sugestão invisível" que vira posição real contradiz o objetivo de topo do
    milestone; (b) a segunda passada do ciclo do agente retorna cedo quando a
    carteira não tem posição de opção real, tornando a leitura literal
    tecnicamente inerte. Explique o que "reusar os ADR-003/004/005" passou a
    significar em concreto: forma ADR-003 sem quantidade, ADR-004 (não marcar
    sobre dado não confiável), ADR-005 (intrínseco vindo da MESMA função pura
    do motor + vocabulário de motivo verbatim). Alternativa descartada:
    chamar as funções de compra/venda de opção do motor — com a razão.
  - **Decisão 2 — as 5 transições e o gatilho da execução simulada.** Tabela
    origem→destinos com a condição de cada uma; e a regra de que toda
    sugestão `armada` com prêmio real executa (A-11-03), com a razão de
    medição (amostra máxima antes de expor) e a alternativa descartada
    (filtro adicional = regra de produto inventada sem origem em artefato).
    Registre também que `pnl_por_acao` é POR AÇÃO porque não existe coluna de
    quantidade — e que essa ausência é uma das garantias long-only do ADR-021
    Decisão 2, preservada.
  - **Decisão 3 — o monitoramento roda no gate diário do laço existente, não
    dentro da segunda passada por usuário.** As 3 razões de A-11-06 (inerte /
    acoplaria a medição ao kill-switch, com o precedente do incidente de 2,5
    dias / código novo no caminho mais crítico), e a afirmação explícita de
    que "nenhum scheduler novo, nenhum cron externo" — a parte vinculante do
    critério do ROADMAP — é cumprida literalmente.
  - **Decisão 4 — custo de rede zero e o que acontece quando falta preço.**
    `candle_cache.peek` (o Radar já pagou), a consequência aceita (ticker fora
    do universo do Radar fica pendente), e a postura de nunca completar por
    default (princípio 4 do CLAUDE.md). Registre que, com
    `B3_OPTIONS_PROVIDER=yahoo` (default de produção, não alterado), a ponte
    da Fase 10 segue dormente e portanto **o ciclo de vida nasce sem nenhuma
    linha para varrer em produção** — por desenho, não por defeito, exatamente
    como o ADR-021 Decisão 3 documentou para a ponte.
  - **Consequências** (Fica mais fácil / Fica mais difícil / A revisitar).
    Em "A revisitar", inclua obrigatoriamente: (a) o item (a) do ADR-021
    ("alargar o lado do gatilho quando a Fase 11 tiver dado real para
    calibrar") agora tem instrumento, mas ainda não tem dado, porque a ponte
    está dormente; (b) expor a medição (relatório/consulta) é decisão do Alex,
    fora deste milestone; (c) WR-01 (atomicidade do gate de orçamento) segue
    pendente de decisão do Alex — repetido aqui por visibilidade, sem ser
    resolvido.

**2. `docs/OPERACAO-ciclo-de-vida-put.md`** — runbook no formato de
`docs/OPERACAO-ponte-gatilho-put.md`, 6 seções numeradas:
  1. O que o ciclo de vida faz e quando roda (09:45, depois da ponte).
  2. As 2 env vars (`B3_PUT_LIFECYCLE_HHMM`, `B3_PUT_LIFECYCLE_OFF`) —
     **nenhuma definida em produção**.
  3. Por que `linhas: 0` em produção hoje é o desenho (a ponte está dormente
     com o provedor default), com ponteiro para o §3 do runbook da ponte.
  4. Como inspecionar: a query SQL literal
     (`SELECT estado, COUNT(*) FROM put_suggestions GROUP BY estado;` e uma
     query de linhas pendentes por `pendente_desde`) + a linha de log
     `[put-lifecycle]`.
  5. Limites de consumo: **zero requisição de rede** — e por quê.
  6. O que explicitamente NÃO existe nesta fase: rota HTTP, push, UI, efeito
     na carteira do usuário, opção vendida, preço de opção ao vivo,
     monitoramento intradiário.

**3. Fechamento da fase:** rode `bash scripts/executar.sh --testes`
**DUAS vezes** (exigência do contrato de autonomia) e cole as duas contagens
na SUMMARY; elas precisam ser idênticas. Confirme `git status -sb` sem
tracking remoto (nenhum push) e `git ls-remote --heads origin | grep
worktree-agent` vazio.

Commit único: `docs(11-03): ADR-022 (ciclo de vida da sugestão de put) + doc
de operação`.
  </action>

  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>

  <acceptance_criteria>
    1. `ls docs/adr/022-ciclo-de-vida-da-sugestao-de-put.md docs/OPERACAO-ciclo-de-vida-put.md`
       lista os dois.
    2. `grep -c "PUTLIFE-02" docs/adr/022-ciclo-de-vida-da-sugestao-de-put.md` ≥ 1.
    3. `grep -c "ADR-021" docs/adr/022-ciclo-de-vida-da-sugestao-de-put.md` ≥ 1.
    4. `grep -cE "ADR-003|ADR-004|ADR-005" docs/adr/022-ciclo-de-vida-da-sugestao-de-put.md` ≥ 3.
    5. `grep -c "expirada sem uso" docs/adr/022-ciclo-de-vida-da-sugestao-de-put.md` ≥ 1
       (rótulos do ROADMAP citados, não só os tokens).
    6. `grep -cE "B3_PUT_LIFECYCLE_HHMM|B3_PUT_LIFECYCLE_OFF" docs/OPERACAO-ciclo-de-vida-put.md` ≥ 2.
    7. `git diff --stat -- docs/adr/021-*.md docs/OPERACAO-ponte-gatilho-put.md docs/adr/003*.md docs/adr/004*.md docs/adr/005*.md`
       → **saída vazia** (esta fase é ADITIVA à documentação; histórico não
       se reescreve).
    8. `git diff --stat -- server/app/` → **saída vazia** (Task 2 é só doc).
    9. `bash scripts/executar.sh --testes` → exit 0 nas **DUAS** execuções,
       com contagem de backend e de `.mjs` **idêntica** entre elas; cole as
       duas saídas resumidas.
    10. `git status -sb` mostra branch sem tracking remoto e
        `git ls-remote --heads origin | grep -c worktree-agent` == 0
        (nenhum push, guardrail absoluto).
    11. `git diff --stat "$FASE10" -- server/app/store.py web/ web-admin/ server/app/skill_ref.py server/app/main.py server/app/defaults.py`
        → **saída vazia** (prova de fase inteira; ⚠ NÃO use `origin/main` como
        base — ver guardrails).
  </acceptance_criteria>

  <done>
    A decisão que sustenta a fase está registrada num ADR permanente com as
    duas evidências duras, o runbook responde "por que 0 linhas?" a um grep de
    distância, e a suíte canônica fechou verde duas vezes com contagem
    idêntica, sem push e sem uma linha de código de produção alterada por este
    plano.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| código desta fase → carteira do usuário | A fronteira que a fase inteira existe para não cruzar; este plano é quem a prova |
| `.planning/` (efêmero) → `docs/adr/` (permanente) | O raciocínio precisa sobreviver ao diretório de planejamento |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Elevation of Privilege | `optionPositions`/`cash`/`history` | mitigate | Teste de COMPORTAMENTO comparando as três seções antes/depois de um ciclo completo (pega qualquer via de escrita, inclusive `db.kv_set` direto — mais forte que mock de função), + guardião de leitura de fonte com prova de RED |
| T-11-12 | Tampering | estado da simulação | mitigate | Produto cartesiano `ESTADOS × ESTADOS`: toda transição não declarada devolve 0 e não muda o banco |
| T-11-13 | Repudiation | linha em limbo | mitigate | Asserção de tricotomia (terminal ∨ avançou hoje ∨ pendência datada) sobre TODAS as linhas depois de uma rodada mista |
| T-11-14 | Information Disclosure | rotas HTTP / ranking do Radar | mitigate | Nenhum path novo em `app.main.app.routes`; agregações do ADR-017 seguem `porSetup == {}` com o ciclo de vida rodando |
| T-11-15 | Repudiation | perda do raciocínio ao fim do milestone | mitigate | ADR-022 em `docs/adr/` (permanente), aditivo — ADR-021 e os ADR-003/004/005 não são reescritos |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — nenhum pacote novo neste plano (só teste e markdown) |
</threat_model>

<verification>
- `bash scripts/executar.sh --testes` verde DUAS vezes, contagem idêntica
  (pytest do backend + `web/tests/*.mjs`). `scripts/test.sh` sozinho não conta.
- `git diff --stat "$FASE10" -- server/app/store.py` VAZIO — a carteira real
  não recebeu uma linha na Fase 11 inteira.
- `git status -sb` sem tracking remoto; nenhum branch `worktree-agent-*` em
  `git ls-remote --heads origin`.
- ⚠ Base de diff: `$FASE10` (tip da Fase 10), **nunca** `origin/main`
  (D-EXEC-10-03-01).
</verification>

<success_criteria>
1. Guardião automatizado prova, por comportamento, que a carteira do usuário
   fica byte-idêntica ao longo de um ciclo de vida completo.
2. Nenhum estado inválido é gravável e nenhuma linha fica em limbo.
3. Nenhuma superfície visível nova (rota, vocabulário, front, admin,
   telemetria, ranking do Radar) — guardião da Fase 10 verde e estendido.
4. ADR-022 e runbook existem, são aditivos, e citam as evidências duras.
5. Suíte canônica verde 2x com contagem idêntica; nenhum push, nenhum deploy.
6. Os 4 requisitos da fase (PUTLIFE-01..04) têm prova automatizada apontável.
</success_criteria>

<output>
Crie `.planning/phases/11-ciclo-de-vida-e-monitoramento/11-03-SUMMARY.md`
no formato de `10-03-SUMMARY.md`, incluindo:
- tabela de acceptance criteria com o resultado literal de cada comando
- a prova de RED (saída com sentinela + saída após reverter)
- as duas contagens de `scripts/executar.sh --testes`
- uma seção "Fase 11 completa" mapeando cada requisito (PUTLIFE-01..04) ao
  teste/arquivo que o prova

Acrescente cada decisão autônoma a
`.planning/notes/decisoes-autonomas-v1.2.md` no formato D-EXEC-11-03-NN.
</output>
