---
phase: 10-ponte-gatilho-put
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - server/tests/test_put_bridge_sem_superficie.py
  - docs/adr/021-ponte-gatilho-put.md
  - docs/OPERACAO-ponte-gatilho-put.md
autonomous: true
requirements: [PUT-03]

must_haves:
  truths:
    - "Nenhuma rota HTTP, push, card, texto de vocabulário ou aba de admin expõe a sugestão de put — provado por teste guardião que lê o código-fonte, não só por inspeção de diff"
    - "O diff da fase inteira não toca web/src, web-admin/src, skill_ref.py, copy.js, persistence.js, catalog.js nem defaults.py"
    - "O ranking visível do Radar não muda: as duas agregações do signal_ledger (ADR-017) não enxergam nenhuma linha de sugestão de put"
    - "A decisão de onde a sugestão mora e a decisão sobre a corrida WR-01 ficam registradas em ADR, não só em nota de planejamento"
    - "A suíte canônica (`bash scripts/executar.sh --testes`) fica verde, rodada duas vezes com resultado idêntico"
  artifacts:
    - path: "server/tests/test_put_bridge_sem_superficie.py"
      provides: "Guardião de PUT-03 — lê o fonte das superfícies e prova ausência de vazamento"
      min_lines: 60
    - path: "docs/adr/021-ponte-gatilho-put.md"
      provides: "ADR registrando D-10-A (tabela própria), D-10-H (WR-01 herdado com mitigação) e D-10-I (acesso pelo seletor)"
      contains: "put_suggestions"
    - path: "docs/OPERACAO-ponte-gatilho-put.md"
      provides: "Como ligar/desligar, onde olhar, quais envs e por que a ponte nasce dormente em produção"
  key_links:
    - from: "server/tests/test_put_bridge_sem_superficie.py"
      to: "server/app/main.py, server/app/skill_ref.py, web/src/, web-admin/src/"
      via: "leitura do fonte + asserção de contagem zero"
      pattern: "put_suggestions|put_bridge"
---

<objective>
Provar PUT-03 (nada visível ao usuário) com um guardião que sobrevive à fase, e
registrar em ADR as duas decisões de arquitetura que o Alex vai querer revisar:
onde a sugestão mora e o que foi feito com a corrida WR-01.

Purpose: um `git diff` limpo prova a fase de hoje; um teste guardião prova todos
os dias seguintes. A Fase 11 vai mexer nesta camada — o guardião é o que impede
que a exposição aconteça por acidente antes da medição interna estar pronta.
Output: teste guardião, ADR-021, doc de operação.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/notes/decisoes-autonomas-v1.2.md
@.planning/phases/10-ponte-gatilho-put/10-01-SUMMARY.md
@.planning/phases/10-ponte-gatilho-put/10-02-SUMMARY.md

@docs/adr/020-centralizacao-de-dados-no-mydata.md
@docs/OPERACAO-ledger-de-sinais.md
</context>

<assumptions>
**D-10-M — o guardião de PUT-03 é um teste que LÊ O FONTE, não um teste que
inspeciona um diff.** Diff é relativo a um ponto no tempo e some no merge
seguinte; um teste que abre `server/app/main.py`/`skill_ref.py` e conta
ocorrências continua valendo amanhã. É o mesmo raciocínio dos "guardiões de
teste" que o CLAUDE.md do repositório já protege ("guardiões de teste não se
apagam").

**D-10-N — o guardião conta ocorrências com as linhas de comentário
FILTRADAS.** `grep -c` cru conta o próprio comentário que explica a regra,
tornando a documentação auto-invalidante. O teste tira comentários (`#` em
Python, `//` e `/* */` em JS) antes de contar.

**D-10-O — a ponte nasce dormente em produção e isso é documentado como
comportamento esperado, não como pendência.** Com `B3_OPTIONS_PROVIDER=yahoo`
(default intocado), o contrato do Yahoo não traz `exerciseStyle`, a triagem
descarta tudo e o resumo diário fecha com `sugestoes: 0` e motivo
`"nenhuma put elegível"`. Quem ler o log em produção precisa saber que isso é
o desenho, não um bug — daí o doc de operação.
</assumptions>

<constraints>
Idênticos aos dos Planos 01 e 02:

- Nenhum `git push`, nenhum deploy, nenhuma mudança de env var de produção.
- `B3_OPTIONS_PROVIDER` NUNCA é alterado.
- Nenhuma superfície visível: NÃO tocar `web/src/`, `web-admin/src/`,
  `server/app/skill_ref.py`, `server/app/main.py`, `server/app/defaults.py`,
  `web/src/catalog.js`, `web/src/copy.js`, `web/src/persistence.js`. Este plano
  só ACRESCENTA um arquivo de teste e dois arquivos de documentação.
- Nenhum suporte a opção vendida/short/margem/atribuição em qualquer forma.
- Nenhum pacote novo (npm/pip/cargo). Se parecer necessário, PARE e documente.
- Teste que falhar e não fechar em 2 tentativas de reparo: PARE e documente
  exatamente o que falhou — NÃO relaxe a asserção do guardião para passar. Se o
  guardião pegar um vazamento real, o vazamento é que sai, não a asserção.
- Documentação é ADITIVA: nenhum parágrafo existente de ADR ou doc é removido
  ou reescrito (regra "histórico não se reescreve").
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Guardião de PUT-03 — provar que nada da ponte alcança uma superfície</name>

  <read_first>
    - .planning/REQUIREMENTS.md, item PUT-03 — o texto exato do requisito que
      este teste materializa.
    - .planning/ROADMAP.md, Phase 10, Success Criteria item 4 — a lista literal
      dos arquivos que o roadmap manda conferir (`App.jsx`, `persistence.js`,
      `copy.js`, `skill_ref.py`).
    - server/tests/test_options_provider_mydata.py — o estilo de teste do
      repositório (docstring de cabeçalho explicando o que o arquivo prova,
      asserções negativas explícitas).
    - server/app/put_bridge.py e server/app/put_suggestions.py — os nomes que o
      guardião vai procurar nas superfícies.
  </read_first>

  <files>server/tests/test_put_bridge_sem_superficie.py</files>

  <action>
Crie `server/tests/test_put_bridge_sem_superficie.py` com docstring de
cabeçalho explicando: este arquivo materializa PUT-03; ele não testa
comportamento de código, testa AUSÊNCIA de acoplamento entre a ponte e
qualquer superfície visível; e apagá-lo/afrouxá-lo é reverter uma decisão de
produto, não limpar teste (regra "guardiões de teste não se apagam").

Helper único no topo:

    RAIZ = pathlib.Path(__file__).resolve().parents[2]   # raiz do repositório

    def _sem_comentarios(texto: str, linguagem: str) -> str:
        """Remove comentários antes de contar ocorrências — sem isso o próprio
        comentário que explica a regra faz o teste passar/falhar sozinho
        (D-10-N)."""

    TOKENS = ("put_bridge", "put_suggestions", "putSuggestion", "putBridge",
              "sugestão de put", "sugestao_de_put")

Testes (cada um com `pytest.skip` se o arquivo alvo não existir no checkout —
`web-admin/` pode não estar presente em todo ambiente):

  - `test_main_py_nao_referencia_a_ponte`: lê `server/app/main.py`, remove
    comentários, e para CADA token de `TOKENS` a contagem é 0. Mensagem de
    falha nomeia o token encontrado e diz "PUT-03: nenhuma rota HTTP pode
    expor a sugestão de put nesta fase".
  - `test_skill_ref_nao_menciona_put_de_protecao`: mesmo padrão sobre
    `server/app/skill_ref.py` — o vocabulário visível ao usuário não ganhou
    nenhuma frase sobre put de proteção.
  - `test_front_do_app_nao_referencia_a_ponte`: varre TODOS os `*.js`/`*.jsx`
    de `web/src/` e aplica os mesmos tokens (comentários JS removidos).
  - `test_portal_admin_nao_referencia_a_ponte`: idem para `web-admin/src/`.
  - `test_defaults_e_catalog_intactos_quanto_a_put`: `server/app/defaults.py` e
    `web/src/catalog.js` não contêm nenhum dos tokens (a paridade byte-a-byte
    de prompts continua sem menção à ponte).
  - `test_status_snapshot_nao_expoe_a_ponte`: importa `app.agent`, monta um
    banco temporário, chama `agent.status_snapshot(conn)` e afirma que nenhuma
    chave do dict devolvido (em qualquer nível) contém `"put"` —
    caso-insensível. Prova D-10-L pelo COMPORTAMENTO real, não por grep.
  - `test_nenhuma_rota_serve_a_tabela`: importa `app.main`, percorre
    `app.main.app.routes` e afirma que nenhum `route.path` contém `"put"` —
    caso-insensível.
  - `test_agregacoes_do_adr017_nao_enxergam_sugestao_de_put`: banco temporário,
    grava N sugestões via `put_suggestions.registrar`, chama
    `signal_ledger.agregar_cumulativo(conn)` e `signal_ledger.agregar_janela(
    conn, 2025)` e afirma `porSetup == {}` nas duas. É a prova de que o ranking
    VISÍVEL do Radar não muda (a via de vazamento que nenhum grep de front-end
    pegaria — D-10-A do Plano 01).

Nenhum destes testes toca a rede.
  </action>

  <verify>
    <automated>cd server &amp;&amp; .venv/bin/python -m pytest tests/test_put_bridge_sem_superficie.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `cd server && .venv/bin/python -m pytest tests/test_put_bridge_sem_superficie.py -q` exit 0, 8 testes passando (nenhum deles em `skip` para `server/app/main.py`, `server/app/skill_ref.py` e `web/src/`).
    - Verificação de RED manual, registrada no SUMMARY: acrescente TEMPORARIAMENTE uma linha de CÓDIGO (nunca um comentário — o guardião filtra comentários), `_PUT_BRIDGE_SENTINELA = "put_suggestions"`, ao fim de `server/app/skill_ref.py`; rode o teste e confirme que ele FALHA; desfaça com `git checkout -- server/app/skill_ref.py` e confirme `git diff --stat -- server/app/skill_ref.py` vazio. Sem essa prova o guardião pode estar verde por não estar olhando nada.
    - `git diff --stat -- web/ web-admin/ server/app/skill_ref.py server/app/main.py server/app/defaults.py` vazio ao fim da task.
  </acceptance_criteria>

  <done>
    Existe um teste que falha se alguém expuser a ponte numa rota, no
    vocabulário, no front, no portal admin, no status do agente ou nas
    agregações do ADR-017 — e ficou provado que ele realmente falha quando
    deveria.
  </done>
</task>

<task type="auto">
  <name>Task 2: ADR-021 + doc de operação + fechamento da suíte canônica</name>

  <read_first>
    - docs/adr/020-centralizacao-de-dados-no-mydata.md — formato de ADR do
      repositório (Status/Data/Decisor/Contexto/Decisão/Consequências/A
      revisitar) e a nota aditiva datada que a Fase 0 acrescentou ao final.
    - docs/OPERACAO-ledger-de-sinais.md — formato do doc de operação irmão
      (seções numeradas, comandos reais, o que olhar quando dá errado).
    - .planning/notes/decisoes-autonomas-v1.2.md — a nota WR-01 no topo, que
      este ADR precisa responder explicitamente.
    - Os `<assumptions>` dos Planos 01 e 02 (D-10-A a D-10-O) — o ADR é a
      versão permanente desse raciocínio; o plano some, o ADR fica.
  </read_first>

  <files>docs/adr/021-ponte-gatilho-put.md, docs/OPERACAO-ponte-gatilho-put.md</files>

  <action>
Passo 1 — `docs/adr/021-ponte-gatilho-put.md`, no formato exato do ADR-020:

    # ADR-021: Ponte gatilho→put — onde a sugestão mora
    **Status:** Aceito
    **Data:** <data real da execução>
    **Decisor:** Alex (executado sob contrato de autonomia noturna)
    **Base:** ROADMAP v1.2 Fase 10; requisitos PUT-01/PUT-02/PUT-03;
    achado WR-01 do `09-REVIEW.md`.

Seções obrigatórias:

  - **Contexto** — o que a fase constrói (detector EOD dispara sobre ticker em
    carteira → seleciona put candidata → grava com proveniência), e as duas
    decisões travadas do milestone (EOD ponta a ponta; só put COMPRADA
    long-only, sem margem e sem atribuição).
  - **Decisão 1 — tabela própria `put_suggestions`, não o `signal_ledger`.**
    Reproduza os 3 argumentos de D-10-A do Plano 01, com destaque para o
    principal: `agregar_cumulativo` é `GROUP BY setup` sobre a tabela inteira,
    então uma linha de opção mudaria `expR`, que é o número que
    `regime.ranquear()` usa para pesar setups no Radar VISÍVEL. Cite a
    alternativa descartada (coluna `tipo` + filtro nas duas agregações) e por
    que foi rejeitada.
  - **Decisão 2 — garantias estruturais de long-only.** `CHECK(option_type =
    'put')`, ausência de qualquer coluna de quantidade/margem/garantia/lado,
    `estilo_exercicio` e `iv` `NOT NULL`. Declare explicitamente: não existe
    caminho de código nesta fase que possa produzir uma posição vendida; se
    algum dia existir, ele terá de derrubar o CHECK primeiro — que é o ponto.
  - **Decisão 3 — acesso pelo seletor `options_provider`, e a ponte nasce
    dormente.** D-10-I do Plano 02, incluindo a consequência aceita (com
    `B3_OPTIONS_PROVIDER=yahoo`, que é o default de produção e não muda neste
    milestone, o Yahoo não publica `exerciseStyle` e a triagem fecha em zero).
    Diga que isso espelha o gate da Fase 0, que também nasceu dormente.
  - **Decisão 4 — WR-01 (check-then-debit não-atômico): herdado, com
    mitigação, e reservado para decisão do Alex.** Reproduza a análise de
    D-10-H do Plano 02: a janela `_gate`→primeiro `_debita` não contém `await`,
    logo não intercala num laço de eventos de thread única; a ponte não abre
    janela nova; as duas mitigações concretas (iteração sequencial com teste de
    reentrância; teto `MAX_TICKERS_DIA=10`, ≤20 de 2.000/dia). Registre
    explicitamente o que NÃO foi feito (lock/CAS em `mydata_budget`) e por quê
    (padrão compartilhado com `candle_provider`/`brapi_budget`; mexer mudaria
    consumidores fora desta fase). Aponte para a nota no topo de
    `.planning/notes/decisoes-autonomas-v1.2.md`.
  - **Consequências** — "fica mais fácil" / "fica mais difícil" / "a revisitar",
    no formato do ADR-003. Em "a revisitar", liste: (a) alargar
    `LADO_GATILHO`; (b) tornar `mydata_budget` atômico; (c) expor a sugestão na
    UI, que é PUTUI-01 e depende da medição interna deste milestone.

Passo 2 — `docs/OPERACAO-ponte-gatilho-put.md`, no formato de
`docs/OPERACAO-ledger-de-sinais.md`, seções numeradas:

  1. O que a ponte faz e quando roda (dentro do `scheduler_loop`, 1x/dia útil,
     default 09:30, depois do Radar 08:45 e do ledger 09:15).
  2. Variáveis de ambiente: `B3_PUT_BRIDGE_HHMM` (horário) e `B3_PUT_BRIDGE_OFF`
     (desliga). Deixe explícito que NENHUMA delas foi definida em produção por
     este milestone e que a ponte é inofensiva com elas ausentes.
  3. Por que o resumo em produção fecha com `sugestoes: 0` hoje (D-10-O) — é o
     desenho, não um defeito.
  4. Como inspecionar: a query SQL de leitura da tabela
     (`SELECT ... FROM put_suggestions ORDER BY criado_em DESC LIMIT 20`) e a
     linha de log `[put-bridge]`.
  5. Limites de consumo: 2 requisições por ticker, teto de 10 tickers/dia.
  6. O que NÃO existe nesta fase: nenhuma rota, nenhum push, nenhuma UI,
     nenhuma execução simulada (isso é Fase 11) e nenhuma opção vendida.

Passo 3 — fechamento:
  a. Rode `bash scripts/executar.sh --testes` DUAS vezes e registre no SUMMARY
     os dois resultados (contagem de testes e exit code) — devem ser idênticos.
     Se divergirem, PARE: é isolamento de teste, não flakiness aceitável.
  b. Rode e cole no SUMMARY a saída de
     `git diff --stat origin/main..HEAD -- web/ web-admin/ server/app/skill_ref.py server/app/main.py server/app/defaults.py server/app/mydata_budget.py server/app/options_provider.py server/app/options_provider_mydata.py`
     — deve ser vazia. Esta é a prova literal do Success Criteria 4 do ROADMAP.
  c. Rode `git log --oneline origin/main..HEAD` e confirme que NÃO houve push
     (`git status -sb` mostra a branch à frente do remoto).
  d. NÃO rode `npx vite build`: nenhum arquivo de front foi tocado (o item (b)
     prova). Registre isso no SUMMARY em vez de rodar o build por hábito.
  </action>

  <verify>
    <automated>bash scripts/executar.sh --testes</automated>
  </verify>

  <acceptance_criteria>
    - `bash scripts/executar.sh --testes` exit 0 nas DUAS execuções, com contagem de testes idêntica; os dois números registrados no SUMMARY.
    - `git diff --stat origin/main..HEAD -- web/ web-admin/ server/app/skill_ref.py server/app/main.py server/app/defaults.py server/app/mydata_budget.py server/app/options_provider.py server/app/options_provider_mydata.py` produz saída VAZIA, colada no SUMMARY.
    - `ls docs/adr/021-ponte-gatilho-put.md docs/OPERACAO-ponte-gatilho-put.md` lista os dois arquivos.
    - `grep -c "WR-01" docs/adr/021-ponte-gatilho-put.md` retorna ≥ 1.
    - `grep -c "MAX_TICKERS_DIA" docs/adr/021-ponte-gatilho-put.md` retorna ≥ 1.
    - `git diff --stat -- docs/adr/020-centralizacao-de-dados-no-mydata.md docs/OPERACAO-ledger-de-sinais.md` vazio (documentação nova é aditiva; a antiga não foi reescrita).
    - `git status -sb | head -1` mostra a branch à frente do remoto (nenhum push).
  </acceptance_criteria>

  <done>
    As decisões de arquitetura desta fase existem fora do diretório de
    planejamento, num ADR que o Alex pode ler de manhã e contestar; o operador
    sabe por que a ponte fecha em zero em produção; e a suíte canônica está
    verde de forma reprodutível, com a ausência de mudança de superfície
    provada por comando, não por afirmação.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ponte → superfícies (rotas, front, admin, vocabulário) | a fronteira que PUT-03 declara fechada; o guardião é o cadeado |
| ponte → agregações do ADR-017 | fronteira INTERNA que, se rompida, muda o ranking visível do Radar sem tocar em nenhum arquivo de front |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-13 | Information Disclosure | qualquer superfície | mitigate | `test_put_bridge_sem_superficie.py` lê o fonte de `main.py`/`skill_ref.py`/`web/src`/`web-admin/src`/`defaults.py`/`catalog.js` e conta ocorrências com comentários filtrados; mais dois testes de comportamento (`status_snapshot` e `app.routes`) |
| T-10-14 | Tampering | ranking do Radar (ADR-017) | mitigate | `test_agregacoes_do_adr017_nao_enxergam_sugestao_de_put` prova `porSetup == {}` nas duas agregações depois de gravar sugestões |
| T-10-15 | Repudiation | decisões da fase | mitigate | ADR-021 registra D-10-A, long-only estrutural, acesso pelo seletor e a disposição de WR-01, fora do diretório de planejamento |
| T-10-16 | Tampering | guardião afrouxado no futuro | accept | Documentado no cabeçalho do teste + regra do CLAUDE.md ("guardiões de teste não se apagam"); reversão deliberada obriga a atualizar o guardião com nota |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | Nenhum pacote novo. `npx vite build` deliberadamente NÃO é rodado porque nenhum arquivo de front foi tocado — e isso é verificado, não presumido |
</threat_model>

<verification>
1. `cd server && .venv/bin/python -m pytest tests/test_put_bridge_sem_superficie.py -q` → exit 0.
2. Prova de RED do guardião executada e registrada (sentinela temporário em `skill_ref.py` derruba o teste; revertido; `git diff --stat` vazio).
3. `bash scripts/executar.sh --testes` → exit 0, duas vezes, mesma contagem.
4. `git diff --stat origin/main..HEAD -- web/ web-admin/ server/app/skill_ref.py server/app/main.py server/app/defaults.py server/app/mydata_budget.py server/app/options_provider.py server/app/options_provider_mydata.py` → vazio.
5. `docs/adr/021-ponte-gatilho-put.md` e `docs/OPERACAO-ponte-gatilho-put.md` existem e citam WR-01 e o teto diário.
6. Nenhum `git push`, nenhum deploy, nenhuma env var de produção alterada.
</verification>

<success_criteria>
- PUT-03 tem um guardião automatizado que prova ausência de superfície por
  leitura de fonte E por comportamento (status do agente, rotas do FastAPI,
  agregações do ADR-017), e ficou demonstrado que ele falha quando deveria.
- ADR-021 registra onde a sugestão mora, a garantia estrutural de long-only, o
  acesso pelo seletor e a disposição explícita de WR-01.
- O doc de operação explica por que a ponte fecha em zero em produção.
- Suíte canônica verde, reprodutível, com o diff de superfícies provado vazio
  por comando.
</success_criteria>

<output>
Create `.planning/phases/10-ponte-gatilho-put/10-03-SUMMARY.md` when done.
Registre no SUMMARY: as duas contagens da suíte canônica, a saída (vazia) do
`git diff --stat` das superfícies, a prova de RED do guardião, e qualquer
decisão autônoma nova — replicada em
`.planning/notes/decisoes-autonomas-v1.2.md`.
</output>
